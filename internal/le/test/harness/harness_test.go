package testharness

// This file held the tests of `le test harness`, which exec'd the harness
// binary. D-8 deleted that producer (harness.go): every harness command is now
// `le test <name>`, tested by TestEveryHarnessCommandForwards. The file and its
// directory are left for the owner to remove.
