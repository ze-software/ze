// The agreement test the gated marker names. The fixture proves the marker
// resolves to a declared test, so the body is not what the gate reads.
package fixture

import "testing"

func TestSpeedsMatchTheModel(t *testing.T) {}
