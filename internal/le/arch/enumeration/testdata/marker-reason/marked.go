// A policy list carrying the marker and a reason.
package fixture

// enumeration: exempt (these two are the families the installer image ships with, which no registry holds)
var shipped = []string{"ipv4/unicast", "ipv6/multicast"}
