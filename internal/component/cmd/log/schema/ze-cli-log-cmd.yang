module ze-cli-log-cmd {
    namespace "urn:ze:cli:log:cmd";
    prefix clilogcmd;
    import ze-extensions { prefix ze; }
    description "View and control daemon logging";
    revision 2026-03-16 { description "Initial revision"; }

    container log {
        config false;
        description "View log entries and adjust log levels at runtime";

        container levels {
            config false;
            ze:command "ze-bgp:log-levels";
            description "Show what log level each subsystem is using.
Lists every registered logger with its current level. Use 'log set'
to change a level at runtime without restarting.";
        }

        container set {
            config false;
            ze:command "ze-bgp:log-set";
            description "Change a subsystem's log level without restarting.
Usage: log set <logger> <level>. Takes effect immediately. Set to
debug when troubleshooting, then back to info when you are done.";
            leaf logger {
                type string;
                description "Logger/subsystem name";
            }
            leaf level {
                type enumeration {
                    enum disabled;
                    enum debug;
                    enum info;
                    enum warn;
                    enum err;
                }
                description "Target log level";
            }
        }

        container recent {
            config false;
            ze:command "ze-bgp:log-recent";
            description "Show recent log entries from the in-memory ring.
Filters (all optional): level <lvl>, component <name>, count <N>.
Newest entries first. Useful when you cannot access the log file
directly.";
            leaf level {
                type enumeration {
                    enum disabled;
                    enum debug;
                    enum info;
                    enum warn;
                    enum err;
                }
                description "Filter by log level";
            }
            leaf component {
                type string;
                description "Filter by component name";
            }
            leaf count {
                type uint32;
                description "Maximum number of entries";
            }
        }
    }
}
