// Package capability implements BGP capability negotiation per RFC 5492,
// including multiprotocol extensions, 4-byte ASN, ADD-PATH, extended messages,
// and graceful restart capabilities.
package capability
