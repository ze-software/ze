// test-relax: TestZZDecodePseudonode was a throwaway decode check used while
// minting the isis-dis.ci pseudo-node fixture (isis-8). The decode + field
// assertions it performed are now permanently covered by pseudonode_ci_test.go
// (TestISISPseudonodeCIFixtureDecodes). Removed as obsolete scaffolding.
package packet
