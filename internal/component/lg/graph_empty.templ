// Design: docs/architecture/web-interface.md -- looking glass graph component

package lg

// graphEmpty is the drawing a graph request answers when it has nothing to
// draw, or too many nodes to draw legibly.
//
// It is markup: two fixed elements and one message. renderGraphSVG and
// renderNextHopGraphSVG are NOT markup in the same sense. Every attribute they
// write is a coordinate computeLayout produced, so they stay in Go. AC-7 of
// spec-web-templ-migration states that boundary and why it holds.
templ graphEmpty(msg string) {
	<svg xmlns="http://www.w3.org/2000/svg" width="400" height="50"><text x="10" y="30" font-family="monospace" font-size="14" fill="currentColor">{ msg }</text></svg>
}
