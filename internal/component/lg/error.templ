// Design: docs/architecture/web-interface.md -- looking glass error component

package lg

// errorBanner shows an engine or validation error above a page's content.
templ errorBanner(message string) {
	if message != "" {
		<div class="error-banner">{ message }</div>
	}
}
