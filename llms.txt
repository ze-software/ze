# Ze

> Ze is an open-source configuration and protocol engine. The network operating system built on it speaks BGP, manages Linux interfaces, programs the FIB, and serves the same YANG-modeled configuration through CLI, SSH, web, API, and MCP. Its core holds the supervisor, message bus, config provider, and plugin manager; protocols and services arrive as subsystems or plugins.

Pre-release: no tagged versions yet, built continuously from the main branch. AGPLv3 open source. See the ExaBGP [migration path](use-cases/exabgp-migration/index.md).

This file is intentionally denormalized for AI use. It includes the high-signal product inventory and then the normal page map, so common questions should not require fetching many separate pages. Page links still point at Markdown `index.md` files first, with rendered web URLs beside them for humans.

## Product snapshot

- Purpose: configuration and protocol engine for Linux routing, plus a network operating system built on that core.
- Protocols and subsystems in the shipped daemon: BGP, IS-IS, OSPF, BFD, static routes, policy routing, FIB programming, interfaces, firewall, traffic control, DNS, DHCP, NTP, IPsec, L2TP, PPPoE, telemetry, web UI, SSH CLI, MCP, and plugins.
- Operator surfaces: SSH CLI with commit and rollback, generated command reference, server-rendered web workbench, looking glass, telemetry, gNMI, gRPC, MCP, JSON/YAML/NDJSON/table output, and shell-like output pipes derived from the schema where possible.
- Dataplane: Linux netlink, nftables, eBPF, AF_PACKET, psample, optional VPP integrations, and namespace-aware testing.
- Release state: pre-release, main-branch builds, no tagged stable release yet.
- License and repos: AGPLv3. Canonical repository: https://github.com/ze-software/ze. Discord: https://discord.gg/T8s7CjPDne.
- Current generated counts: 52 shipped or experimental feature cards, 4 roadmap cards, 409 CLI commands, 36 config sections, 91 plugin registrations, 48 direct Go dependencies, 38 weekly change entries.
- Test evidence counts: 27,200+ unit tests, 79 fuzz targets, 1,800+ end-to-end transcript steps, 132 interop scenarios across 9 target implementations.
- Generated date: 2026-09-01.

## Quality and verification model

Ze uses layered proof because bugs appear at different boundaries.

- Local Go tests: package behavior, parser rules, encoders, state transitions, validation paths, and error shapes. Current scale: 27,200+ unit tests.
- Race, coverage, and fuzz: fuzz targets are normal Go tests with generated input. Current scale: 79 fuzz targets.
- gomu mutation checks: mutate production Go code and rerun tests to find weak assertions. gomu is advisory, not the default CI gate.
- Functional `.ci` transcripts: drive processes, CLI commands, files, HTTP, syslog, peers, daemons, exits, and BGP wire expectations. BGP failures are decoded structurally, not shown as raw hex only.
- Browser `.wb` transcripts: drive the rendered web UI through real browser flows.
- Editor `.et` transcripts: drive the headless interactive editor.
- QEMU: runs Linux-only behavior from macOS or CI where netlink, nftables, eBPF, PPP, network namespaces, and kernel modules exist.
- Interop: 132 scenarios against 9 target implementations, including FRR, BIRD, GoBGP, RustyBGP, OpenBGPD, ExaBGP, and other real daemons where applicable.
- Verify workflow: `./le verify worktree` runs the whole native verification population against a fixed commit in a detached worktree, writes stage logs under `tmp/`, groups related failures, and prints narrow rerun commands. `./le repository` is the narrower handoff gate.
- Rule for regressions: do not hide a failure with a skip or loose assertion. Move the proof to the layer that can see the real behavior, add the narrow test, rerun it, then rerun the gate that should have caught it.

Useful commands: `go test -race -run TestName ./internal/...`, `./le fuzz run`, `gomu run`, `bin/ze-test bgp plugin 42 -v`, `./le qemu netns-test`, `./le integration interop`, `./le evidence release-candidate`.

## Comparison positioning

- BGP comparison lens: Ze is compared with BIRD, FRR, OpenBGPD, GoBGP, bio-rd, ExaBGP, RustyBGP, rustbgpd, and freeRtr across AFI/SAFI, core protocol, policy, security, observability, APIs, operations, and best-path behavior.
- Network OS lens: Ze is compared with VyOS and freeRtr across routing, interfaces, firewall, NAT, VPN, AAA, services, management APIs, automation, packaging, observability, tests, and implementation model.
- Evidence policy: capability claims should cite upstream code, official feature documentation, or the integration layer that owns the behavior. `Unclear`, `Partial`, and `Not found` are valid outcomes when evidence does not support a stronger claim.
- Comparison pages are advice for product decisions, not marketing copy.

## Feature inventory

### Built for demanding operators. (33 cards: 33 current)
Ze starts with a configuration and protocol engine. The shipped network operating system adds BGP, interface management, FIB programming, plugins, operator tools, a minimal appliance runtime, and diagnostics as one product.
- AI Tool Interfaces [automate, current]: chips: MCP, Generated, AI tools; MCP exposes CLI/API commands; AI tools read structured output; Plugins expose discoverable tools; link: https://ze-software.net/features/ai-first/index.md
- SSH CLI [operate, current]: chips: Built-in SSH, RBAC; Manage Ze without OS shell accounts; Profiles, audit, and accounting; commit, rollback, diff, completion; link: https://ze-software.net/features/cli-commands/index.md
- YANG Configuration [operate, current]: chips: YANG, ExaBGP; Schema-driven validation; One model feeds every surface; Plugin defined config and commands; link: https://ze-software.net/features/bgp-configuration/index.md
- Output Formatting [operate, current]: chips: Shell-like pipes, Offline; table, json, yaml, ndjson; match, count, first/last; Offline via ze format; Every command, no rendering flags; link: https://ze-software.net/features/formatting/index.md
- Web Workbench [operate, current]: chips: HTMX, SSE; YANG-driven config tree; Same CLI grammar in browser; Live updates via SSE; link: https://ze-software.net/features/web-interface/index.md
- Looking Glass [operate, current]: chips: Routes, Topology, Birdwatcher; Peer and route viewer; Topology graph; SSE streaming for live state; link: https://ze-software.net/features/looking-glass/index.md
- System Readiness [operate, current]: chips: ze doctor, ze explain; Offline pre-start checks; Health, warnings, and errors; Structured remediation with `ze explain`; link: https://ze-software.net/guides/production-diagnostics/index.md
- Native BGP Engine [routing, current]: chips: BGP, IPv4/IPv6, FlowSpec; Full implementation in Go; Lazy parsing, buffer-first encoding; Negotiated capabilities; link: https://ze-software.net/features/bgp-protocol/index.md
- Static Routes [routing, current]: chips: ECMP, BFD, PBR; Named tables, policy routing; BFD-tracked failover; Multi-path ECMP groups; link: https://ze-software.net/guides/static-routes/index.md
- BFD [routing, current]: chips: RFC 5880, Auth; Single-hop and multi-hop; GTSM, jitter, BGP integration; SHA1/MD5 auth, echo mode; link: https://ze-software.net/features/bgp-protocol/index.md
- MRT Recording [routing, current]: chips: RFC 6396, Analysis; Updates, messages, RIB snapshots; Strftime file rotation; Show, inject, replay, filter; link: https://ze-software.net/guides/mrt-analysis/index.md
- DNS Resolver [services, current]: chips: Cache, Pipes; Built-in cached resolver; | resolve and | origin pipe operators; No external daemon needed; link: https://ze-software.net/features/dns-resolver/index.md
- Plugin System [automate, current]: chips: ExaBGP, RPKI, Policy; Plugins add commands, RPCs, events; YANG roots join CLI and web; Independent, composable; link: https://ze-software.net/reference/plugins/index.md
- Programmable [automate, current]: chips: REST, gRPC, gNMI; REST API, gRPC, gNMI; Shared engine for identical output; Automate from any language; link: https://ze-software.net/features/api-commands/index.md
- AI-First Design [automate, current]: chips: Self-describing, Skills; Self-describing command catalogue from the live binary; Every command is an automation surface; Structured diagnostics and repair plans; link: https://ze-software.net/features/ai-first/index.md
- MCP Integration [automate, current]: chips: MCP, OAuth 2.1; Streamable HTTP transport, OAuth 2.1 resource server; Server-initiated elicitation, task-augmented tool calls; MCP Apps UI with embedded panels; link: https://ze-software.net/features/mcp-integration/index.md
- ExaBGP Compatibility [automate, current]: chips: Migration, Bridge; Automatic config migration; Plugin bridge for existing workflows; Migration path for existing scripts; link: https://ze-software.net/features/exabgp-compatibility/index.md
- Evidence Over Claims [observe, current]: chips: Fuzz, Interop, Docker; Unit, functional, fuzz, chaos; Performance benchmarks; Interop vs FRR, BIRD, GoBGP; link: https://ze-software.net/features/interoperability-testing/index.md
- Development Activity [observe, current]: chips: Heatmap, Live data; A year of commits and added lines, at a glance; Built from git history each time; Current Go code composition; link: https://ze-software.net/project/activity/index.md
- Prometheus Telemetry [observe, current]: chips: Netdata, Prometheus; 138 metrics from /proc and /sys; Netdata naming, drop-in replacement; Existing Grafana dashboards keep working; link: https://ze-software.net/guides/monitoring/index.md
- Health Registry [observe, current]: chips: HTTP, 503; /health HTTP endpoint; Per-component status checks; BGP, FIB, IPsec, L2TP, VPP; link: https://ze-software.net/features/index.md
- Host Inventory [observe, current]: chips: CPU, NIC, SMART; CPU, NIC, DMI, memory, thermal; SMART disk health and self-tests; JSON output for pipelines; link: https://ze-software.net/features/index.md
- Crash Capture [observe, current]: chips: Panic, Syslog; Automatic panic stack traces; Ring buffer context (last 64 entries); show crashes CLI command; link: https://ze-software.net/features/index.md
- Tech-Support Bundle [observe, current]: chips: Offline, JSON; 20 modules, pure Go, no shell-outs; Structured JSON per module; Privacy-by-default, gokrazy-safe; link: https://ze-software.net/features/index.md
- Production Diagnostics [observe, current]: chips: CLI, MCP; 11 built-in tools replacing ss, dmesg, lsof; tcpdump, traceroute, ping, mtr; All exposed via MCP for AI debugging; link: https://ze-software.net/guides/production-diagnostics/index.md
- Secure by Default [secure, current]: chips: SSH, RBAC, RPKI, ASPA; SSH access to the CLI; RPKI route origin validation; No other daemons needed; link: https://ze-software.net/reference/plugins/index.md
- TACACS+ AAA [secure, current]: chips: RFC 8907, Accounting; SSH login via TACACS+; Command accounting START/STOP; Server failover, local fallback; link: https://ze-software.net/guides/tacacs/index.md
- Audit Trail [secure, current]: chips: Commits, Auth; Config commit, discard, and reload; Failed auth on every interface; Filter by action, actor, and time; link: https://ze-software.net/guides/audit/index.md
- PKI Store [secure, current]: chips: X.509, TLS; YANG-modelled certificate management; Chain validation, expiry checks; Shared by IPsec, TLS, mutual auth; link: https://ze-software.net/features/index.md
- Minimal Appliance Mode [platform, current]: chips: Appliance, Server; Kernel, init, Ze runtime; No package manager or general shell; ISO/PXE bare-metal install; Linux server with systemd; link: https://ze-software.net/guides/appliance/index.md
- Runs Itself [platform, current]: chips: Update, Systemd; Binary self-update; Built-in readiness checks; No orchestrator needed; link: https://ze-software.net/features/introspection/index.md
- Docker Support [platform, current]: chips: Daemon only, Scratch, Compose; Static binary on scratch base; Compose support included; Optional build tags; link: https://ze-software.net/features/index.md
- Feature Gates [platform, current]: chips: 36 subsystems, Default on; Compile out whole subsystems, BGP included; Smaller binary, smaller attack surface; Config fails closed on blocks the build lacks; link: https://ze-software.net/guides/quickstart/index.md

### Experimental and growing. (19 cards: 19 experimental)
Implemented and tested, still waiting for production evidence.
- IPsec VPN [services, experimental]: chips: IKEv2, X.509, EAP; Full IKEv2 engine, rekeying, DPD; NAT-T, keepalive, XFRM interfaces; EAP-MSCHAPv2, EAP-TLS, road warrior; link: https://ze-software.net/features/index.md
- L2TPv2 BNG [services, experimental]: chips: PPP, RADIUS, CQM; RFC 2661 LNS and LAC with PPP; RADIUS auth, accounting, CoA; CQM monitoring, shaping, web UI; link: https://ze-software.net/guides/l2tp/index.md
- PPPoE Access [services, experimental]: chips: RFC 2516, PPP; Access concentrator with discovery FSM; Shared PPP driver with L2TP; PAP, CHAP-MD5, and MS-CHAPv2 authentication; link: https://ze-software.net/guides/pppoe/index.md
- Interface Management [services, experimental]: chips: Netlink, DHCP; Ethernet, VLAN, bridge, WireGuard; 8 tunnel kinds, DHCP client; NTP sync, offload tuning, mirroring; link: https://ze-software.net/features/interfaces/index.md
- Firewall [services, experimental]: chips: nftables, NAT; 15 match types, 19 actions; SNAT, DNAT, masquerade; FlowSpec-to-firewall bridge; link: https://ze-software.net/guides/firewall/index.md
- Policy Routing [services, experimental]: chips: nftables, PBR; L3/L4 match criteria; Table steering, next-hop actions; TCP-MSS clamping, interface wildcards; link: https://ze-software.net/guides/policy-routing/index.md
- VPP Data Plane [services, experimental]: chips: DPDK, GoVPP; FIB programming via GoVPP; MPLS label operations; Per-interface Prometheus metrics; link: https://ze-software.net/guides/vpp/index.md
- MPLS / LDP / RSVP-TE [routing, experimental]: chips: Labels, Signaling; Kernel MPLS FIB, push/swap/pop; LDP discovery and sessions; RSVP-TE ERO, bandwidth admission; link: https://ze-software.net/features/index.md
- OSPFv2 / OSPFv3 [routing, experimental]: chips: RFC 2328, RFC 5340, ECMP; One ospf engine, IPv4 and IPv6 address families; SPF/ABR, NSSA, virtual links, NBMA/P2MP; Redistribution, SR, BFD, graceful restart; link: https://ze-software.net/guides/ospf/index.md
- IS-IS [routing, experimental]: chips: ISO 10589, Dual-stack; L1/L2 link-state IGP over Layer 2; RFC 5304/5310 authentication, key chains; Dual-stack IPv6, redistributes with BGP; link: https://ze-software.net/guides/isis/index.md
- VRRP [routing, experimental]: chips: RFC 9568, RFC 3768, Virtual MAC; First-hop gateway redundancy, IPv4 and IPv6; Per-group virtual-MAC macvlan for L2 failover; keepalived interop, compile-out; link: https://ze-software.net/guides/vrrp/index.md
- Flow Export [observe, experimental]: chips: sFlow, NetFlow, IPFIX; sFlow v5, NetFlow v9, IPFIX; Packet sampling, conntrack flows; BGP next-hop enrichment; link: https://ze-software.net/guides/flow-export/index.md
- DDoS and Anomaly Detection [secure, experimental]: chips: DDoS, Anomaly, FlowSpec; Volumetric and behavioral detection; Bounded incident history with durations; Local and upstream auto-mitigation; link: https://ze-software.net/guides/anomaly/index.md
- ISO and PXE Install [platform, experimental]: chips: PXE, ISO; PXE bare-metal provisioning; Installer ISO media; Local systemd install and uninstall; link: https://ze-software.net/guides/ze-install/index.md
- Kernel Tunables [platform, experimental]: chips: Sysctl, Profiles; Three-layer precedence; Named profiles (DSR, router, hardened); Originals restored on stop; link: https://ze-software.net/features/index.md
- AS112 Anycast DNS [services, experimental]: chips: AS112, Anycast; Authoritative sink zones on four fixed anycast addresses (RFC 7534/7535); Conditional BGP origination via healthcheck-gated watchdog; Anycast IPs bound on lo automatically, never operator-typed; link: https://ze-software.net/guides/as112/index.md
- Segment Routing [routing, experimental]: chips: SAFI 73, SRv6; SR-Policy NLRI (RFC 9830), SAFI 73; MPLS and SRv6 binding SID, tunnel encap; ExaBGP bridge for SR-Policy migration; link: https://github.com/ze-software/ze/blob/main/internal/component/bgp/plugins/nlri/srpolicy
- Fleet Management [automate, experimental]: chips: Managed config, TLS hub; Per-client configuration and pinned hub certificate; Cached config with reconnect and heartbeat; Version hashing and two-phase fetch; link: https://ze-software.net/guides/fleet-config/index.md
- IRR Route Filtering [secure, experimental]: chips: IRR, as-set; Prefix-lists from IRR data, live in the engine; Sourced from PeeringDB and RADB; Opt-in per peer, group or global filter chain; link: https://ze-software.net/guides/irr-filtering/index.md

### Specified, not built. (4 cards: 4 aspiration)
Aspirations with written, reviewed specs. Nothing here is usable today.
- OSPF L3VPN PE-CE [routing, aspiration]: chips: RFC 4576, RFC 4577, L3VPN; PE-CE DN bit loop prevention; Domain ID, route type, VPN route tag; Blocked on VRF/MPLS L3VPN infrastructure; link: https://github.com/ze-software/ze/blob/main/plan/spec-ospf-ext-13-l3vpn-dn-bit.md
- VRF [routing, aspiration]: chips: VRF, L3VPN; VRF as a first-class concept; Per-VRF BGP stacks, YANG config; Kernel VRF devices, table binding; link: https://github.com/ze-software/ze/blob/main/plan/spec-vrf-0-umbrella.md
- Kernel Lockdown [secure, aspiration]: chips: Lockdown, Integrity; Kernel lockdown integrity mode; Blocks unsigned modules, kexec, /dev/mem; Design reviewed, waiting for schedule; link: https://github.com/ze-software/ze/blob/main/plan/spec-kernel-lockdown-hardening.md
- Cloud-Init Provisioning [platform, aspiration]: chips: Cloud-init, User-data; Appliance identity from cloud metadata; SSH keys and config via user-data; No pre-baked seed image needed; link: https://github.com/ze-software/ze/blob/main/plan/spec-install-9-cloud-init.md

## Configuration model roots

Top-level YANG-derived config roots. Child names are direct children only, enough to orient without fetching the full reference.

- `anomaly`: Behavioral anomaly detection and response subsystem. Children: detect, observe, shape.
- `bfd`: BFD control configuration for Ze. Children: bind-v6, enabled, multi-hop-session, persist-dir, profile, single-hop-session.
- `bgp`: Border Gateway Protocol routing configuration. Peers inherit from group defaults; groups inherit from this global level. Children: admin-distance, blackhole, bmp, community, filter, group, healthcheck, multipath, peer, policy, rib, route-server, router-id, rpki, ....
- `class-of-service`: Named class-of-service profiles Children: ieee-802.1p.
- `connected`: Connected route redistribution. Presence enables the plugin. Children: none.
- `control-plane-protection`: Control-plane policing configuration. Children: bgp.
- `ddos`: Distributed denial-of-service detection and mitigation subsystem. Children: detect, flowspec, flowtriq, local, observe.
- `environment`: Environment settings for API transports Children: api-server, bgp, bmp, chaos, cli, daemon, exabgp, gnmi, l2tp, log, looking-glass, mcp, ntp, pprof, ....
- `exabgp`: Top-level container for ExaBGP-compatibility configuration. Children: bridge.
- `fib`: Forwarding Information Base configuration. Children: kernel, p4, vpp.
- `firewall`: Ze-managed nftables firewall tables. Table names are bare in config; ze_ prefix added by component. Children: backend, flush-on-shutdown, global-options, irr, table.
- `flow-export`: Flow export (sFlow, NetFlow v9, IPFIX) configuration Children: collector, conntrack, enrichment, sampling.
- `interface`: Interface-level class-of-service bindings and inline QoS maps (container-merge with ze-iface-conf). Removing the cos plugin removes all QoS surface from interfaces. Children: backend, bridge, dhcp-auto, dummy, ethernet, loopback, monitor, pppoe-client, tunnel, veth, wireguard, xfrm.
- `isis`: IS-IS routing instance configuration. Children: hostname, interfaces, key-chains, level, level-1, level-2, lsp-lifetime, lsp-refresh-interval, net, overload, system-id.
- `kernel`: Kernel route redistribution. Presence enables the plugin. Children: none.
- `l2tp`: L2TPv2 tunnel subsystem settings (RFC 2661). Presence of this block with any content implies the subsystem is enabled. Use 'enabled false' to disable explicitly, or 'enabled true'... Children: allow-no-auth, auth, auth-method, authentication, cqm-enabled, enabled, event-ring-size-per-session, hello-interval, hello-retries, max-logins, max-sessions, max-tunnels, ncp, pool, ....
- `ldp`: Label Distribution Protocol configuration Children: hello-hold-time, hello-interval, interfaces, keepalive-time, lsr-id, transport-address.
- `mrt`: MRT dump configuration Children: add-path, all, direction, extended-timestamp, peer-filter, routes, updates.
- `ospf`: OSPFv2 routing instance configuration. Children: address-family, areas, default-information, extended-link, extended-prefix, fast-reroute, graceful-restart, interfaces, key-chains, max-metric, maximum-paths, opaque, redistribute, reference-bandwidth, ....
- `pki`: PKI certificate and key store. Presence of this block enables certificate-based authentication for IPsec VPN, TLS, and other subsystems. Children: ca, certificate.
- `plugin`: Plugin configuration Children: external, hub, internal.
- `policy`: Policy routing configuration. Children: route.
- `pppoe`: PPPoE access concentrator settings (RFC 2516). Presence of this block with any content implies the subsystem is enabled. Use 'enabled false' to disable explicitly, or 'enabled... Children: ac-name, allow-no-auth, auth-method, cookie-timeout, enabled, interface, max-sessions, padi-rate-limit, service-name.
- `redistribute`: Route redistribution between protocols. Each destination protocol has its own container with import rules. Adding a new destination protocol means adding a new key here. Children: destination.
- `rib`: System RIB configuration. Children: admin-distance.
- `routing-table`: Named routing table definitions. Each entry maps a name to a kernel routing table ID. Children: table.
- `rsvp-te`: RSVP-TE traffic engineering configuration Children: bypass, interface, refresh-multiplier, refresh-period, router-id, tunnel.
- `service`: Service settings Children: as112, dhcp-server, geodns, image-server, tftp-server.
- `static`: Static route configuration. Children: table.
- `storage`: Storage device management Children: smart.
- `sysctl`: Kernel tunable management. Children: profile, setting.
- `system`: System-level settings Children: archive, authentication, authorization, commit-revisions, conntrack, console, dns, domain, host, name-server, peeringdb, tuning, update-check.
- `telemetry`: Telemetry export configuration Children: prometheus.
- `traffic`: Traffic subsystem: QoS control and byte-usage accounting. Children: control, usage.
- `vpn`: VPN subsystems. Children: ipsec.
- `vpp`: VPP data plane configuration. Children: api-socket, cpu, dpdk, enabled, external, lcp, memory, plugins, stats.

## Plugin registry

Each registration comes from the Go runtime registry. Config roots come from plugin metadata and YANG files.

- `anomaly-detect`: Behavioral anomaly detector (report-only): per-entity pattern-of-life over trafficfeature Config roots: anomaly/detect. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/anomaly/detect`.
- `anomaly-observe`: Behavioral anomaly observability: incident lifecycle store and show anomaly observe CLI Config roots: anomaly/observe. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/anomaly/observe`.
- `anomaly-shape`: Shadow-first autonomous anomaly responder: per-source rate-limit with arm/auto-revert/kill-switch Config roots: anomaly/shape. Dependencies: firewall. Optional: none. YANG files: 1. Source: `internal/plugins/anomaly/shape`.
- `as112`: AS112 anycast DNS node: authoritative sink for misdirected RFC 1918 / link-local reverse-DNS queries (RFC 7534, RFC 7535) Config roots: service. Dependencies: none. Optional: none. YANG files: 2. Source: `internal/plugins/as112`.
- `bfd`: Bidirectional Forwarding Detection (RFC 5880, 5881, 5883) Config roots: bfd. Dependencies: none. Optional: none. YANG files: 3. Source: `internal/component/bfd`.
- `bgp`: BGP routing daemon Config roots: bgp. Dependencies: none. Optional: none. YANG files: 2. Source: `internal/component/bgp/plugin`.
- `bgp-adj-rib-in`: Adj-RIB-In storage (raw hex replay) Config roots: none. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/adj_rib_in`.
- `bgp-aigp`: Accumulated IGP Metric (RFC 7311) Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/aigp`.
- `bgp-bmp`: BMP receiver and sender (RFC 7854, 8671) Config roots: bgp, environment. Dependencies: none. Optional: none. YANG files: 2. Source: `internal/component/bgp/plugins/bmp`.
- `bgp-capa`: Core BGP capability decoding (multiprotocol, asn4, add-path, paths-limit, extended-nexthop, extended-message) Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/capa`.
- `bgp-filter-aspath`: Named AS-path regex filter (ordered entries, first match wins, accept/reject) Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/filter_aspath`.
- `bgp-filter-aspath-length`: Named AS-path length filter (accept/reject based on hop count) Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/filter_aspath_length`.
- `bgp-filter-community`: Community tag/strip filter (standard, large, extended) Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/filter_community`.
- `bgp-filter-community-match`: Named community match filter (ordered entries, first match wins, accept/reject) Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/filter_community_match`.
- `bgp-filter-family`: Named address-family policy filter: remove a family's NLRI or tear down the session Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/filter_family`.
- `bgp-filter-irr`: IRR-based prefix-list filter for eBGP peers Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 2. Source: `internal/component/bgp/plugins/filter_irr`.
- `bgp-filter-modify`: Named route attribute modifier (set local-preference, med, origin, next-hop) Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/filter_modify`.
- `bgp-filter-prefix`: Named prefix-list filter (CIDR + ge/le + accept/reject) Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/filter_prefix`.
- `bgp-filter-remove-private-as`: Named AS-path action filter that removes RFC 6996 Private Use ASNs Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/filter_remove_private_as`.
- `bgp-gr`: Graceful Restart capability and mechanism plugin Config roots: bgp. Dependencies: bgp, bgp-rib. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/gr`.
- `bgp-healthcheck`: Service healthcheck plugin with watchdog route control Config roots: bgp. Dependencies: bgp, bgp-watchdog. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/healthcheck`.
- `bgp-hostname`: FQDN capability decoding Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/hostname`.
- `bgp-llnh`: Link-Local Next-Hop capability plugin Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/llnh`.
- `bgp-nlri-evpn`: EVPN family plugin Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/nlri/evpn`.
- `bgp-nlri-flowspec`: FlowSpec NLRI encoding/decoding Config roots: none. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/nlri/flowspec`.
- `bgp-nlri-labeled`: Labeled Unicast family plugin (RFC 8277) Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/nlri/labeled`.
- `bgp-nlri-ls`: BGP-LS family plugin Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/nlri/ls`.
- `bgp-nlri-mup`: Mobile User Plane family plugin (draft-ietf-bess-mup-safi) Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/nlri/mup`.
- `bgp-nlri-mvpn`: Multicast VPN family plugin (RFC 6514) Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/nlri/mvpn`.
- `bgp-nlri-rtc`: Route Target Constraint family plugin (RFC 4684) Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/nlri/rtc`.
- `bgp-nlri-srpolicy`: SR-Policy family plugin (RFC 9830, SAFI 73) Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/nlri/srpolicy`.
- `bgp-nlri-vpls`: VPLS family plugin (RFC 4761) Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/nlri/vpls`.
- `bgp-nlri-vpn`: VPN family plugin Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/nlri/vpn`.
- `bgp-persist`: Route Persistence Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/persist`.
- `bgp-redistribute`: Route redistribution ingress filter with loop prevention and family filtering Config roots: none. Dependencies: bgp. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/redistribute_ingress`.
- `bgp-rib`: Route Information Base storage Config roots: bgp. Dependencies: none. Optional: none. YANG files: 2. Source: `internal/component/bgp/plugins/rib`.
- `bgp-role`: RFC 9234 BGP Role capability Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/role`.
- `bgp-route-refresh`: Route Refresh capability decoding Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 3. Source: `internal/component/bgp/plugins/route_refresh`.
- `bgp-rpki`: RPKI origin validation via RTR protocol Config roots: bgp. Dependencies: bgp, bgp-adj-rib-in. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/rpki`.
- `bgp-rpki-decorator`: Correlates UPDATE + RPKI events into merged update-rpki events Config roots: none. Dependencies: bgp, bgp-rpki. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/rpki_decorator`.
- `bgp-rr`: Route Reflector Config roots: none. Dependencies: bgp-adj-rib-in. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/rr`.
- `bgp-rs`: Route Server Config roots: bgp. Dependencies: none. Optional: bgp-adj-rib-in. YANG files: 1. Source: `internal/component/bgp/plugins/rs`.
- `bgp-softver`: Software Version capability (code 75) Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 1. Source: `internal/component/bgp/plugins/softver`.
- `bgp-watchdog`: Watchdog route management plugin Config roots: bgp. Dependencies: bgp. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/watchdog`.
- `connected`: Connected routes: redistribute directly connected interface prefixes Config roots: connected. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/connected`.
- `copp`: Control-plane policing: rate-limit new TCP connections to BGP listen port Config roots: control-plane-protection. Dependencies: firewall. Optional: none. YANG files: 1. Source: `internal/plugins/copp`.
- `cos`: 802.1p class-of-service profile definitions Config roots: class-of-service. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/cos`.
- `ddos-detect`: Automatic DDoS attack detector with two-stage detection Config roots: ddos/detect. Dependencies: interface. Optional: none. YANG files: 1. Source: `internal/plugins/ddos/detect`.
- `ddos-flowspec`: DDoS FlowSpec/RTBH responder: upstream mitigation with leak-probe clear Config roots: ddos/flowspec. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/ddos/flowspec`.
- `ddos-flowtriq`: DDoS incident reporter for Flowtriq cloud API Config roots: ddos/flowtriq. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/ddos/flowtriq`.
- `ddos-local`: DDoS local responder: on-host nft drop on attack detection Config roots: ddos/local. Dependencies: firewall. Optional: none. YANG files: 1. Source: `internal/plugins/ddos/local`.
- `ddos-observe`: DDoS observability: incident store and show ddos status/incidents CLI Config roots: ddos/observe. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/ddos/observe`.
- `dhcpserver`: DHCP server: address assignment for LAN clients (RFC 2131) Config roots: service. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/dhcpserver`.
- `exabgp-bridge`: In-process ExaBGP compatibility bridge: runs an operator ExaBGP-format script as a subprocess and translates to/from ze events (RFC-agnostic transport shim) Config roots: exabgp. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/exabgp/bridgeplugin`.
- `fib-kernel`: FIB kernel: programs OS routes from system RIB via netlink/route socket Config roots: fib/kernel. Dependencies: rib, sysctl. Optional: none. YANG files: 1. Source: `internal/plugins/fib/kernel`.
- `fib-p4`: FIB P4: programs P4 switch forwarding entries from system RIB via gRPC/P4Runtime Config roots: fib/p4. Dependencies: rib. Optional: none. YANG files: 1. Source: `internal/plugins/fib/p4`.
- `fib-vpp`: FIB VPP: programs VPP FIB entries from system RIB via GoVPP binary API Config roots: fib/vpp. Dependencies: rib, vpp. Optional: none. YANG files: 1. Source: `internal/plugins/fib/vpp`.
- `firewall`: Packet filter and NAT rules (nftables on Linux) Config roots: firewall. Dependencies: none. Optional: none. YANG files: 2. Source: `internal/component/firewall`.
- `firewall-irr`: IRR-based prefix-list filtering for firewall rules Config roots: firewall. Dependencies: firewall. Optional: none. YANG files: 2. Source: `internal/component/firewall/plugins/irr`.
- `flow-export`: sFlow, NetFlow v9, and IPFIX counter export Config roots: flow-export. Dependencies: interface. Optional: none. YANG files: 1. Source: `internal/plugins/flowexport`.
- `flowspec-firewall`: Translates BGP FlowSpec routes into nftables firewall rules Config roots: none. Dependencies: firewall. Optional: none. YANG files: 0. Source: `internal/plugins/flowspec-firewall`.
- `geodns`: GeoDNS server: DNS answers selected by client source IP (RFC 1035, RFC 7871 client-subnet) Config roots: service. Dependencies: none. Optional: none. YANG files: 2. Source: `internal/plugins/geodns`.
- `iface-dhcp`: DHCP client: DHCPv4/DHCPv6 lease acquisition and renewal Config roots: none. Dependencies: interface. Optional: none. YANG files: 0. Source: `internal/plugins/iface/dhcp`.
- `iface-ra`: Router Advertisement sender: advertises IPv6 prefixes, flags, and resolvers on a LAN (RFC 4861) Config roots: none. Dependencies: interface. Optional: none. YANG files: 0. Source: `internal/plugins/iface/ra`.
- `ike`: IKEv2 engine for native IPsec VPN Config roots: vpn, pki. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/component/ike/engine`.
- `imageserver`: Image server: HTTP provisioning for disk images and boot files Config roots: service. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/imageserver`.
- `interface`: OS network interface monitoring and management Config roots: interface. Dependencies: sysctl. Optional: none. YANG files: 6. Source: `internal/component/iface`.
- `isis`: Intermediate System to Intermediate System (ISO/IEC 10589, RFC 1195): native link-state IGP Config roots: isis. Dependencies: fib-kernel, sysctl. Optional: none. YANG files: 2. Source: `internal/plugins/isis`.
- `kernel`: Kernel routes: redistribute externally-installed kernel routes into BGP Config roots: kernel. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/kernel`.
- `l2tp-auth-local`: Static local user list for L2TP PPP authentication Config roots: l2tp. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/component/l2tp/plugins/authlocal`.
- `l2tp-auth-radius`: RADIUS authentication and accounting for L2TP PPP sessions Config roots: l2tp. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/component/l2tp/plugins/authradius`.
- `l2tp-pool`: IPv4 address and IPv6 prefix pool for L2TP PPP sessions Config roots: l2tp. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/component/l2tp/plugins/pool`.
- `l2tp-shaper`: Traffic shaping for L2TP subscriber sessions Config roots: l2tp. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/component/l2tp/plugins/shaper`.
- `ldp`: Label Distribution Protocol (RFC 5036): MPLS label distribution Config roots: ldp. Dependencies: fib-kernel, sysctl. Optional: none. YANG files: 2. Source: `internal/plugins/ldp`.
- `loop`: Route loop detection (RFC 4271 S9, RFC 4456 S8) Config roots: none. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/component/bgp/reactor/filter`.
- `memlock`: Memory lock: keep the running executable resident under memory pressure Config roots: none. Dependencies: none. Optional: none. YANG files: 0. Source: `internal/plugins/memlock`.
- `mrt`: MRT routing information export (RFC 6396) Config roots: mrt. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/mrt`.
- `ntp`: NTP client: system clock synchronization Config roots: environment. Dependencies: none. Optional: none. YANG files: 2. Source: `internal/plugins/ntp`.
- `ospf`: Open Shortest Path First v2 (RFC 2328): native link-state IPv4 IGP Config roots: ospf. Dependencies: interface, fib-kernel, sysctl. Optional: none. YANG files: 2. Source: `internal/plugins/ospf`.
- `policy-routes`: Policy-based routing: nftables packet marking and ip rule table selection Config roots: policy. Dependencies: firewall. Optional: none. YANG files: 2. Source: `internal/plugins/policyroute`.
- `redistribute-orchestrator`: Redistribute orchestrator: dispatches protocol route events to registered consumers Config roots: redistribute. Dependencies: bgp. Optional: none. YANG files: 0. Source: `internal/component/bgp/plugins/redistribute_egress`.
- `rib`: System RIB: selects best route across protocols by admin distance Config roots: rib. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/component/sysrib`.
- `routing-table`: Named routing table registry: maps names to kernel table IDs Config roots: routing-table. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/routingtable`.
- `rsvp-te`: RSVP-TE: Resource Reservation Protocol - Traffic Engineering (RFC 3209) Config roots: rsvp-te. Dependencies: fib-kernel, sysctl. Optional: none. YANG files: 2. Source: `internal/plugins/rsvpte`.
- `static`: Static routes: config-driven kernel/VPP route programming with ECMP Config roots: static. Dependencies: routing-table. Optional: interface. YANG files: 2. Source: `internal/plugins/static`.
- `sysctl`: Kernel tunable management: three-layer precedence, restore on stop Config roots: sysctl. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/component/sysctl`.
- `tftpserver`: TFTP server: read-only file serving for PXE boot (RFC 1350, RFC 2347 option negotiation) Config roots: service. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/plugins/tftpserver`.
- `traffic`: Traffic control (tc) qdisc, class, and filter management Config roots: traffic/control. Dependencies: none. Optional: vpp. YANG files: 1. Source: `internal/component/traffic`.
- `traffic-usage`: eBPF TCX per-port and per-IP byte accounting Config roots: traffic/usage. Dependencies: interface. Optional: none. YANG files: 2. Source: `internal/plugins/trafficusage`.
- `vpp`: VPP data plane lifecycle management Config roots: vpp. Dependencies: none. Optional: none. YANG files: 1. Source: `internal/component/vpp`.
- `vrrp`: Virtual Router Redundancy Protocol (RFC 9568 / RFC 3768): first-hop gateway redundancy Config roots: interface. Dependencies: interface. Optional: none. YANG files: 2. Source: `internal/plugins/vrrp`.

## CLI command surface

The command catalog is generated from `ze help command --json`, not hand-written. Modes: 97 daemon, 19 offline, 293 read-only.
`daemon` commands require a running Ze daemon. `read-only` commands query state. `offline` commands can run without daemon state. `pipes` groups operators as `always`, `with-rows`, `when-streaming`, or `local-only`; these qualifiers are part of the contract. An operator absent from those groups is refused by name.

### `announce` commands (3)
- `announce blackhole` (daemon; wire ze-bgp:announce-blackhole; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args prefix:string; usage `announce blackhole <prefix> [tag <key> <value>] [for <duration>]`): Originate a blackhole route on demand.
- `announce flowspec` (daemon; wire ze-bgp:announce-flowspec; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `announce flowspec [destination-ipv4 <prefix> ...] [destination-ipv6 <prefix> ...] [destination-port <value> ...] [dscp <value> ...] [flow-label <value> ...] [fragment <value> ...] [icmp-code <value> ...] [icmp-type <value> ...] [next-header <value> ...] [packet-length <value> ...] [port <value> ...] [protocol <value> ...] [rd <value>] [source-ipv4 <prefix> ...] [source-ipv6 <prefix> ...] [source-port <value> ...] [tcp-flags <value> ...] (community <value>|rate-limit <bytes-per-second>|discard) [tag <key> <value>] [for <duration>]`): Originate a FlowSpec rule on demand (RFC 8955).
- `announce unicast` (daemon; wire ze-bgp:announce-unicast; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args prefix:string; usage `announce unicast <prefix> [next-hop <address>] [community <value> ...] [tag <key> <value>] [for <duration>]`): Originate a unicast route on demand.

### `clear` commands (21)
- `clear bgp rib in` (daemon; wire ze-rib-api:clear-in; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear bgp rib in`): Remove all routes received from a peer.
- `clear bgp rib out` (daemon; wire ze-rib-api:clear-out; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear bgp rib out`): Re-advertise all routes to a peer.
- `clear debug` (offline): Clear the default debug profile.
- `clear dns cache` (daemon; wire ze-clear:dns-cache; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear dns cache`): Flush all DNS cache entries and reset all DNS cache counters.
- `clear dns cache record` (daemon; wire ze-clear:dns-cache-record; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string, type:enum; usage `clear dns cache record <name> [type <A|AAAA|MX|NS|TXT|CNAME|PTR>]`): Evict DNS cache entries for one record name, or one name and type when a type is provided.
- `clear dns cache stats` (daemon; wire ze-clear:dns-cache-stats; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear dns cache stats`): Reset DNS cache hit, miss, eviction, and expiry counters without removing cached entries.
- `clear firewall irr as-set` (daemon; wire ze-clear:firewall-irr-as-set; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args as-set:string; usage `clear firewall irr as-set <as-set>`): Remove the cached IRR prefix-list for an AS-SET.
- `clear firewall irr asn` (daemon; wire ze-clear:firewall-irr-asn; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args asn:string; usage `clear firewall irr asn <asn>`): Remove the cached IRR prefix-list for an ASN.
- `clear interface counters` (daemon; wire ze-clear:interface-counters; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear interface counters`): Zero the Rx/Tx counters for every managed interface.
- `clear interface name counters` (daemon; wire ze-clear:interface-counters; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `clear interface name <name> counters`): Zero the Rx/Tx counters for one interface.
- `clear isis adjacency` (daemon; wire ze-clear:isis-adjacency; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear isis adjacency`): Tear down every IS-IS adjacency so neighbors re-form.
- `clear isis counters` (daemon; wire ze-clear:isis-counters; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear isis counters`): Reset IS-IS observational counters and the SPF log.
- `clear l2tp session all` (daemon; wire ze-l2tp-api:session-teardown-all; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear l2tp session all`): Disconnect every L2TP session on this box.
- `clear l2tp session id` (daemon; wire ze-l2tp-api:session-teardown; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear l2tp session id`): Disconnect one subscriber session.
- `clear l2tp tunnel all` (daemon; wire ze-l2tp-api:tunnel-teardown-all; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear l2tp tunnel all`): Tear down every L2TP tunnel on this box.
- `clear l2tp tunnel id` (daemon; wire ze-l2tp-api:tunnel-teardown; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear l2tp tunnel id`): Gracefully tear down one L2TP tunnel.
- `clear ospf counters` (daemon; wire ze-clear:ospf-counters; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear ospf counters`): Reset the OSPF SPF-run history.
- `clear ospf neighbor` (daemon; wire ze-clear:ospf-neighbor; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear ospf neighbor`): Tear down every OSPF adjacency so neighbors re-form.
- `clear ospf process` (daemon; wire ze-clear:ospf-process; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear ospf process`): Full OSPF reset: tear down every adjacency and re-run SPF.
- `clear vpn ipsec sa` (daemon; wire ze-clear:vpn-ipsec-sa; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear vpn ipsec sa`): Tear down IKE Security Associations.
- `clear vrrp statistics` (daemon; wire ze-clear:vrrp-statistics; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `clear vrrp statistics`): Reset every VRRP virtual router's counters to zero.

### `create` commands (9)
- `create interface address` (daemon; wire ze-iface:interface-addr-add; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string, prefix:union; usage `create interface address <name> <prefix>`): Add an IP address to an interface.
- `create interface bridge name` (daemon; wire ze-iface:interface-create-bridge; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `create interface bridge name <name>`): Create a Linux bridge for L2 forwarding.
- `create interface bridge name address` (daemon; wire ze-iface:interface-addr-add; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string, prefix:union; usage `create interface bridge name <name> address <prefix>`): Add an IP address to the bridge.
- `create interface bridge name unit` (daemon; wire ze-iface:interface-unit-add; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string, vid:uint; usage `create interface bridge name <name> unit <vid>`): Add a VLAN sub-interface to the bridge.
- `create interface dummy name` (daemon; wire ze-iface:interface-create-dummy; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `create interface dummy name <name>`): Create a dummy (loopback-style) interface.
- `create interface dummy name address` (daemon; wire ze-iface:interface-addr-add; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string, prefix:union; usage `create interface dummy name <name> address <prefix>`): Add an IP address to the dummy.
- `create interface dummy name unit` (daemon; wire ze-iface:interface-unit-add; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string, vid:uint; usage `create interface dummy name <name> unit <vid>`): Add a VLAN sub-interface to the dummy.
- `create interface unit` (daemon; wire ze-iface:interface-unit-add; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string, vid:uint; usage `create interface unit <name> <vid>`): Add a VLAN sub-interface (802.1Q tagged).
- `create interface veth name` (daemon; wire ze-iface:interface-create-veth; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string, peer:string; usage `create interface veth name <name> <peer>`): Create a veth pair (two linked virtual Ethernet interfaces).

### `debug` commands (4)
- `debug ip ospf inject opaque` (daemon; wire ze-debug:ospf-inject; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `debug ip ospf inject opaque scope <link|area|as> id <opaque-id> [type <type>] [hex <body> ...] [tlv <type> <value-hex> ...] [withdraw]`): Inject a crafted IPv4 opaque LSA into the local LSDB (RFC 5250).
- `debug ipv6 ospf inject lsa` (daemon; wire ze-debug:ospfv3-inject; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `debug ipv6 ospf inject lsa type <ls-type> id <link-state-id> [scope <link|area|as>] [hex <body> ...] [withdraw]`): Inject a crafted OSPFv3 LSA into the local LSDB (RFC 5340).
- `debug ospf inject disable` (daemon; wire ze-debug:ospf-inject-disable; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `debug ospf inject disable`): Disable OSPF debug LSA injection.
- `debug ospf inject enable` (daemon; wire ze-debug:ospf-inject-enable; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `debug ospf inject enable`): Enable OSPF debug LSA injection (shared across both address families).

### `delete` commands (6)
- `delete bgp peer` (daemon; wire ze-delete:bgp-peer; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `delete bgp peer <selector>`): Remove a peer from the running config.
- `delete debug module` (offline): Disable debug for a subsystem, or remove one of its flags/scopes.
- `delete debug profile name` (offline): Delete a named debug profile.
- `delete interface name` (daemon; wire ze-iface:interface-delete; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `delete interface name <name>`): Delete an interface from the kernel.
- `delete interface name address` (daemon; wire ze-iface:interface-addr-del; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string, prefix:union; usage `delete interface name <name> address <prefix>`): Remove an IP address from an interface.
- `delete interface name unit` (daemon; wire ze-iface:interface-unit-del; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string, vid:uint; usage `delete interface name <name> unit <vid>`): Remove a VLAN sub-interface.

### `doctor` commands (1)
- `doctor` (offline): Check that this system is ready to run Ze.

### `explain` commands (1)
- `explain` (offline): Explain one diagnostic code Ze printed.

### `generate` commands (1)
- `generate wireguard keypair` (offline): Generate a WireGuard keypair with the system wg binary.

### `help` commands (3)
- `help` (read-only; wire ze-bgp:help; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `help`): Show available commands at this level.
- `help ai` (offline): Print the agent reference this binary builds from its own registries.
- `help command` (offline): List every command this binary carries with its summary.

### `monitor` commands (8)
- `monitor bgp` (read-only; wire ze-bgp:monitor; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `monitor bgp`): Live BGP peer dashboard that refreshes automatically.
- `monitor event` (read-only; wire ze-event:monitor; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `monitor event`): Stream live events as they happen.
- `monitor interface rate` (read-only; wire ze-monitor:interface-rate; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `monitor interface rate`): Stream per-second traffic rates for your interfaces.
- `monitor ping` (read-only; wire ze-monitor:ping; pipes always: json ndjson table text yaml raw no-more save match count first last display fill resolve origin, when-streaming: log, local-only: save; shape tab; address-fields target; usage `monitor ping`): Continuous ping with live loss and RTT statistics.
- `monitor system netlink` (read-only; wire ze-monitor:system-netlink; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `monitor system netlink`): Watch kernel networking changes in real time.
- `monitor traceroute` (read-only; wire ze-monitor:traceroute; pipes always: json ndjson table text yaml raw no-more save match count first last display fill resolve origin, when-streaming: log, local-only: save; shape tab; address-fields addr; usage `monitor traceroute`): Live mtr-style traceroute that updates continuously.
- `monitor traffic stat` (read-only; wire ze-monitor:traffic-stat; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `monitor traffic stat [name <name>]`): Start streaming traffic monitor (per-second snapshots).
- `monitor vpn ipsec` (read-only; wire ze-monitor:vpn-ipsec; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `monitor vpn ipsec`): Watch IPsec SA events as they happen.

### `peer` commands (8)
- `peer announce blackhole` (daemon; wire ze-bgp:announce-blackhole; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string, prefix:string; usage `peer <selector> announce blackhole <prefix> [tag <key> <value>] [for <duration>]`): Originate a blackhole route on demand.
- `peer announce flowspec` (daemon; wire ze-bgp:announce-flowspec; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `peer <selector> announce flowspec [destination-ipv4 <prefix> ...] [destination-ipv6 <prefix> ...] [destination-port <value> ...] [dscp <value> ...] [flow-label <value> ...] [fragment <value> ...] [icmp-code <value> ...] [icmp-type <value> ...] [next-header <value> ...] [packet-length <value> ...] [port <value> ...] [protocol <value> ...] [rd <value>] [source-ipv4 <prefix> ...] [source-ipv6 <prefix> ...] [source-port <value> ...] [tcp-flags <value> ...] (community <value>|rate-limit <bytes-per-second>|discard) [tag <key> <value>] [for <duration>]`): Originate a FlowSpec rule on demand (RFC 8955).
- `peer announce unicast` (daemon; wire ze-bgp:announce-unicast; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string, prefix:string; usage `peer <selector> announce unicast <prefix> [next-hop <address>] [community <value> ...] [tag <key> <value>] [for <duration>]`): Originate a unicast route on demand.
- `peer raw` (daemon; wire ze-bgp:peer-raw; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `peer raw <selector>`): Send raw bytes into a peer's TCP stream (dangerous).
- `peer update` (daemon; wire ze-bgp:peer-update; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `peer update <selector>`): Send a pre-built BGP UPDATE to a peer.
- `peer withdraw all` (daemon; wire ze-bgp:withdraw-all; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `peer <selector> withdraw all`): Withdraw every on-demand announcement.
- `peer withdraw id` (daemon; wire ze-bgp:withdraw-id; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string, id:string; usage `peer <selector> withdraw id <id>`): Withdraw one announcement by the id that show announcements reports.
- `peer withdraw tag` (daemon; wire ze-bgp:withdraw-tag; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string, key:string, value:string; usage `peer <selector> withdraw tag <key> [value <value>]`): Withdraw the announcements carrying a tag.

### `plugin` commands (10)
- `plugin ack` (read-only; wire ze-bgp:plugin-ack; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `plugin ack`): Choose sync or async event delivery.
- `plugin command complete` (read-only; wire ze-plugin:command-complete; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `plugin command complete`): List the completion candidates for a partial plugin command.
- `plugin command help` (read-only; wire ze-plugin:command-help; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `plugin command help`): Show one plugin command's summary, explanation, arguments, and source plugin.
- `plugin command list` (read-only; wire ze-plugin:command-list; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `plugin command list`): List every command a plugin registered, with its summary.
- `plugin encoding` (read-only; wire ze-bgp:plugin-encoding; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `plugin encoding`): Choose json or text encoding for plugin events.
- `plugin format` (read-only; wire ze-bgp:plugin-format; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `plugin format`): Choose how BGP message bytes appear in events.
- `plugin help` (read-only; wire ze-plugin:help; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `plugin help`): List the subcommands the plugin verb accepts.
- `plugin session bye` (read-only; wire ze-plugin:session-bye; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `plugin session bye`): Tell the daemon this session is disconnecting.
- `plugin session ping` (read-only; wire ze-plugin:session-ping; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `plugin session ping`): Answer a health check with the daemon process id.
- `plugin session ready` (read-only; wire ze-plugin:session-ready; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `plugin session ready`): Signal that this plugin process finished its initialization.

### `request` commands (33)
- `request as112 healthcheck` (daemon; wire ze-as112:health; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args target:string; usage `request as112 healthcheck [target <target>]`): One-shot authoritative query against an anycast service address (or the given target), exit 0 iff the expected AS112 answer comes back.
- `request bgp rib inject` (daemon; wire ze-rib-api:inject; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request bgp rib inject`): Inject a synthetic route into the Adj-RIB-In.
- `request bgp rib withdraw` (daemon; wire ze-rib-api:withdraw; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request bgp rib withdraw`): Withdraw a route from the Adj-RIB-In.
- `request cache expire` (daemon; wire ze-bgp:cache-expire; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args id:string; usage `request cache expire <id>`): Remove a cached message immediately.
- `request cache forward` (daemon; wire ze-bgp:cache-forward; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args id:string, selector:string; usage `request cache forward <id> <selector>`): Forward a cached UPDATE to peers matching a selector.
- `request cache release` (daemon; wire ze-bgp:cache-release; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args id:string; usage `request cache release <id>`): Ack without forwarding (cache consumer) or undo retain (API).
- `request cache retain` (daemon; wire ze-bgp:cache-retain; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args id:string; usage `request cache retain <id>`): Prevent eviction of a cached message.
- `request commit` (daemon; wire ze-bgp:commit; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request commit`): Group route changes into named atomic commits.
- `request config archive` (daemon; wire ze-config-archive:trigger; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request config archive`): Save a snapshot of the current running configuration.
- `request halt` (daemon; wire ze-system:daemon-quit; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request halt`): Dump goroutine stacks to stderr and terminate immediately.
- `request interface down` (daemon; wire ze-iface:interface-down; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `request interface <name> down`): Shut down an interface.
- `request interface mac` (daemon; wire ze-iface:interface-mac; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string, address:string; usage `request interface <name> mac <address>`): Set the MAC address on an interface.
- `request interface migrate` (daemon; wire ze-iface:interface-migrate; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request interface migrate from <source> to <destination> address <prefix> [create <dummy|veth|bridge>] [timeout <duration>]`): Move IP addresses between interfaces with minimal downtime.
- `request interface mtu` (daemon; wire ze-iface:interface-mtu; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string, bytes:uint; usage `request interface <name> mtu <bytes>`): Set the MTU on an interface.
- `request interface up` (daemon; wire ze-iface:interface-up; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `request interface <name> up`): Bring an interface up.
- `request l2tp outgoing-call remote called` (daemon; wire ze-l2tp-api:outgoing-call; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args remote:string, called:string; usage `request l2tp outgoing-call remote <remote> called <called>`): Place an LNS-side outgoing call (RFC 2661 S10.4).
- `request log level` (daemon; wire ze-bgp:log-set; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args logger:string, target:enum; usage `request log level <logger> <disabled|debug|info|warn|err>`): Change a subsystem's log level without restarting.
- `request ospf graceful-restart` (daemon; wire ze-ospf:graceful-restart-prepare; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request ospf graceful-restart`): Trigger a planned OSPFv2 graceful restart (RFC 3623 section 2.1).
- `request peer borr` (daemon; wire ze-bgp:peer-borr; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `request peer <selector> borr`): Start an Enhanced Route Refresh cycle (RFC 7313).
- `request peer clear soft` (daemon; wire ze-bgp:peer-clear-soft; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `request peer <selector> clear soft`): Soft-clear a peer without dropping the session.
- `request peer eorr` (daemon; wire ze-bgp:peer-eorr; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `request peer <selector> eorr`): Finish an Enhanced Route Refresh cycle (RFC 7313).
- `request peer flush` (daemon; wire ze-bgp:peer-flush; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `request peer <selector> flush`): Wait until all queued updates for a peer are sent.
- `request peer pause` (daemon; wire ze-bgp:peer-pause; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `request peer <selector> pause`): Pause reading from a peer's TCP socket.
- `request peer plugin session ready` (daemon; wire ze-plugin:session-peer-ready; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `request peer <selector> plugin session ready`): Signal that per-peer plugin setup is complete.
- `request peer refresh` (daemon; wire ze-bgp:peer-refresh; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `request peer <selector> refresh`): Ask a peer to re-send all routes (RFC 2918).
- `request peer resume` (daemon; wire ze-bgp:peer-resume; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `request peer <selector> resume`): Resume reading from a previously paused peer.
- `request peer teardown` (daemon; wire ze-bgp:peer-teardown; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string, cease-subcode:uint; usage `request peer <selector> teardown <cease-subcode>`): Tear down a peer session.
- `request quiesce` (daemon; wire ze-system:quiesce; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request quiesce`): Block until every subsystem has drained pending async work, then reply.
- `request reboot` (daemon; wire ze-system:daemon-reboot; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request reboot`): Gracefully shutdown then reboot the system.
- `request reload` (daemon; wire ze-system:daemon-reload; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request reload`): Reload the configuration without restarting.
- `request shutdown` (daemon; wire ze-system:daemon-shutdown; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request shutdown`): Gracefully shutdown: drain connections, close peers, exit.
- `request subscribe` (daemon; wire ze-bgp:subscribe; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request subscribe`): Start receiving events of one or more types.
- `request unsubscribe` (daemon; wire ze-bgp:unsubscribe; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `request unsubscribe`): Stop receiving events you previously subscribed to.

### `resolve` commands (11)
- `resolve cymru asn-name` (read-only; wire ze-resolve:cymru-asn-name; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args asn:uint; usage `resolve cymru asn-name <asn>`): Find out who owns an AS number.
- `resolve dns a` (read-only; wire ze-resolve:dns-a; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args hostname:string; usage `resolve dns a <hostname>`): Look up IPv4 addresses (A records) for a hostname.
- `resolve dns aaaa` (read-only; wire ze-resolve:dns-aaaa; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args hostname:string; usage `resolve dns aaaa <hostname>`): Look up IPv6 addresses (AAAA records) for a hostname.
- `resolve dns ptr` (read-only; wire ze-resolve:dns-ptr; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args ip-address:string; usage `resolve dns ptr <ip-address>`): Reverse-lookup an IP address to its hostname (PTR).
- `resolve dns txt` (read-only; wire ze-resolve:dns-txt; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args hostname:string; usage `resolve dns txt <hostname>`): Look up TXT records for a hostname.
- `resolve irr expand` (read-only; wire ze-resolve:irr-expand; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `resolve irr expand`): Expand an AS-SET into its member AS numbers.
- `resolve irr prefix` (read-only; wire ze-resolve:irr-prefix; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `resolve irr prefix`): Get all prefixes announced by an AS-SET's members.
- `resolve peeringdb as-set` (read-only; wire ze-resolve:peeringdb-as-set; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args asn:uint; usage `resolve peeringdb as-set <asn>`): Find the IRR AS-SET registered for an ASN in PeeringDB.
- `resolve peeringdb max-prefix` (read-only; wire ze-resolve:peeringdb-max-prefix; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args asn:uint; usage `resolve peeringdb max-prefix <asn>`): Get max-prefix limits for an ASN from PeeringDB.
- `resolve ping` (read-only; wire ze-resolve:ping; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args target:string, source:string, count:uint, size:uint; usage `resolve ping <target> [source <source>] [count <count>] [size <size>]`): Ping from the router with optional source binding.
- `resolve traceroute` (read-only; wire ze-resolve:traceroute; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args target:string, source:string, max-hops:uint, timeout:string, probes:uint; usage `resolve traceroute <target> [source <source>] [max-hops <max-hops>] [timeout <timeout>] [probes <probes>]`): Traceroute from the router with optional source binding.

### `set` commands (5)
- `set debug active name` (offline): Load a named debug profile and apply it to the running daemon.
- `set debug module` (offline): Enable debug output for one subsystem.
- `set debug profile name` (offline): Save the current debug state as a named profile.
- `set debug timeout` (offline): Set how long debug output stays enabled.
- `set system file-descriptors` (daemon; wire ze-set:system-file-descriptors; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args limit:union; usage `set system file-descriptors [limit <limit|max>]`): Raise the file descriptor limit for the daemon process.

### `show` commands (258)
- `show aaa accounting` (read-only; wire ze-show:aaa-accounting; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show aaa accounting`): Show AAA accounting counters and any dropped records.
- `show announcements` (read-only; wire ze-bgp:show-announcements; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args tag:string, selector:string, family:string; usage `show announcements [tag <tag>] [selector <selector>] [family <family>]`): List active on-demand announcements.
- `show anomaly detect` (read-only; wire ze-show:anomaly; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show anomaly detect`): Show recent behavioral anomaly incidents (report-only): source entity, cohort, fired features with their deviation z-scores, combined score, and severity.
- `show anomaly observe` (read-only; wire ze-show:anomaly-observe; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show anomaly observe`): Show the behavioral anomaly incident lifecycle, newest first.
- `show anomaly shape` (read-only; wire ze-show:anomaly-shape; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show anomaly shape`): Show the shadow-first anomaly responder status: mode (shadow/armed), action, kill-switch state, and the currently armed source entities with live firewall actions.
- `show arp` (read-only; wire ze-show:arp; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show arp`): Show the IPv4 ARP table (shortcut for 'show neighbor ipv4').
- `show as112` (read-only; wire ze-show:as112; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show as112`): AS112 node status.
- `show audit` (read-only; wire ze-show:audit; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args action:string, actor:string, surface:string, since:string, until:string, count:uint; usage `show audit [action <action>] [actor <actor>] [surface <surface>] [since <since>] [until <until>] [count <count>]`): Show who did what and when on this box.
- `show bfd profile` (read-only; wire ze-bfd-api:show-profile; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show bfd profile`): Show BFD timer profiles with effective values.
- `show bfd profile name` (read-only; wire ze-bfd-api:show-profile; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show bfd profile name <name>`): Show one BFD profile by name.
- `show bfd session address` (read-only; wire ze-bfd-api:show-session; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args address:string; usage `show bfd session address <address>`): Show full detail for one BFD session.
- `show bfd sessions` (read-only; wire ze-bfd-api:show-sessions; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show bfd sessions`): List all active BFD sessions.
- `show bgp` (read-only; wire ze-bgp:overview; pipes always: json ndjson table text yaml raw no-more save match count first last display fill resolve origin, when-streaming: log, local-only: save; shape tab; address-fields address; aliases `peers`=`display peers`, `summary`=`display router-id local-as uptime peers-configured peers-established`; usage `show bgp`): BGP peers, sessions, RIB, and protocol tools.
- `show bgp decode` (read-only; wire ze-show:bgp-decode; usage `show bgp decode`): Decode a hex-encoded BGP message into readable JSON.
- `show bgp encode` (read-only; wire ze-show:bgp-encode; usage `show bgp encode`): Turn a route announcement into wire-format hex.
- `show bgp health` (read-only; wire ze-show:bgp-health; pipes always: json ndjson table text yaml raw no-more save match count first last display fill resolve origin, when-streaming: log, local-only: save; shape tab; address-fields peer; usage `show bgp health`): Quick health check for all your BGP peers.
- `show bgp irr` (read-only; wire ze-show:irr-status; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show bgp irr`): Show IRR filter status per ASN.
- `show bgp irr check` (read-only; wire ze-show:irr-check; pipes always: json ndjson table text yaml raw no-more save, when-streaming: log, local-only: save; shape doc; args peer:string, prefix:string; usage `show bgp irr check <peer> <prefix>`): Check if a prefix is accepted by the IRR filter.
- `show bgp irr prefix` (read-only; wire ze-show:irr-prefix; pipes always: json ndjson table text yaml raw no-more save match count first last display, when-streaming: log, local-only: save; shape map; args peer:string; usage `show bgp irr prefix <peer>`): Show IRR-resolved prefixes for a peer.
- `show bgp peer capabilities` (read-only; wire ze-bgp:peer-capabilities; pipes always: json ndjson table text yaml raw no-more save match count first last display fill resolve origin, when-streaming: log, local-only: save; shape tab; address-fields peer; args selector:string; usage `show bgp peer <selector> capabilities`): Show what capabilities were negotiated with a peer.
- `show bgp peer detail` (read-only; wire ze-bgp:peer-detail; pipes always: json ndjson table text yaml raw no-more save match count first last display fill resolve origin, when-streaming: log, local-only: save; shape tab; address-fields local-ip next-hop-address; args selector:string; usage `show bgp peer <selector> detail`): Show full detail for one or more peers.
- `show bgp peer history` (read-only; wire ze-bgp:peer-history; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; args selector:string; usage `show bgp peer <selector> history`): Show FSM state transitions for a peer over time.
- `show bgp peer list` (read-only; wire ze-bgp:peer-list; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show bgp peer list`): List your peers, one line each.
- `show bgp peer rib` (read-only; wire ze-bgp:peer-rib; pipes always: json ndjson table text yaml raw no-more save match count first last display fill resolve origin, when-streaming: log, local-only: save; shape tab; address-fields peer next-hop; args selector:string; usage `show bgp peer <selector> rib [sent|advertised|received|sent-received]`): Show RIB data scoped to one peer.
- `show bgp peer statistics` (read-only; wire ze-bgp:peer-statistics; pipes always: json ndjson table text yaml raw no-more save match count first last display fill resolve origin, when-streaming: log, local-only: save; shape tab; address-fields address; args selector:string; usage `show bgp peer <selector> statistics`): Show UPDATE throughput for your peers.
- `show bgp rib` (read-only; wire ze-rib-api:routes; pipes always: json ndjson table text yaml raw no-more save match count first last display fill resolve origin, when-streaming: log, local-only: save; shape tab; address-fields peer next-hop; filters `advertised` `community` `count` `family` `first` `graph` `histogram` `last` `match` `path` `peer` `prefix` `received`; usage `show bgp rib`): Query routes in the BGP RIB.
- `show bgp rib best` (read-only; wire ze-rib-api:best; pipes always: json ndjson table text yaml raw no-more save match count first last display fill resolve origin, when-streaming: log, local-only: save; shape tab; address-fields best-peer next-hop; filters `community` `count` `family` `first` `graph` `histogram` `last` `match` `path` `peer` `prefix` `reason`; usage `show bgp rib best`): Show the winning route for each prefix.
- `show bgp rib best status` (read-only; wire ze-rib-api:best-status; pipes always: json ndjson table text yaml raw no-more save, when-streaming: log, local-only: save; shape doc; usage `show bgp rib best status`): Check whether best-path computation is still running.
- `show bgp rib rpf` (read-only; wire ze-rib-api:rpf; pipes always: json ndjson table text yaml raw no-more save, when-streaming: log, local-only: save; shape doc; address-fields source next-hop; usage `show bgp rib rpf`): Reverse-path forwarding lookup in the Loc-RIB.
- `show bgp rib status` (read-only; wire ze-rib-api:status; pipes always: json ndjson table text yaml raw no-more save, when-streaming: log, local-only: save; shape doc; usage `show bgp rib status`): Get a quick RIB overview without dumping routes.
- `show bmp collectors` (read-only; wire ze-show:bmp-collectors; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show bmp collectors`): Show BMP collector connection status.
- `show bmp peers` (read-only; wire ze-show:bmp-peers; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show bmp peers`): Show BGP peers as seen through BMP monitoring.
- `show bmp rib` (read-only; wire ze-show:bmp-rib; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show bmp rib`): Show routes received via BMP monitoring sessions.
- `show bmp sessions` (read-only; wire ze-show:bmp-sessions; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show bmp sessions`): Show active BMP receiver sessions.
- `show cache` (read-only; wire ze-bgp:cache-list; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show cache`): List cached BGP UPDATE message IDs with their retain and consumer state.
- `show capture` (read-only; wire ze-show:capture; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args protocol:enum, tunnel-id:string, count:uint, peer:string; usage `show capture [protocol <l2tp|bgp>] [tunnel-id <tunnel-id>] [count <count>] [peer <peer>]`): Show captured control-plane messages.
- `show capture interface` (read-only; wire ze-show:capture-interface; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args iface:string, count:uint, duration:string, snap-len:uint, format:enum, protocol:string; usage `show capture interface [iface <iface>] [count <count>] [duration <duration>] [snap-len <snap-len>] [format <pcap|text>] [protocol <protocol>]`): Capture live packets on an interface (like tcpdump).
- `show capture raw` (read-only; wire ze-show:capture-raw; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args action:enum, protocol:enum, format:enum, count:uint; usage `show capture raw [action <start|stop|dump>] [protocol <l2tp|bgp>] [format <pcap|json>] [count <count>]`): Control raw byte capture for protocol debugging.
- `show command complete` (read-only; wire ze-bgp:command-complete; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show command complete`): Get tab-completion candidates for a partial command.
- `show command help` (read-only; wire ze-bgp:command-help; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show command help`): Show usage and arguments for a specific command.
- `show command list` (read-only; wire ze-bgp:command-list; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show command list`): List every command the daemon knows about.
- `show config cat` (read-only; wire ze-show:config-cat; args id:string; usage `show config cat <id>`): Print the full text of a stored configuration snapshot.
- `show config diff` (read-only; wire ze-show:config-diff; pipes always: json ndjson table text yaml raw no-more save, when-streaming: log, local-only: save; shape doc; usage `show config diff`): Compare two configuration versions side by side.
- `show config dump` (read-only; wire ze-show:config-dump; pipes always: json ndjson table text yaml raw no-more save, when-streaming: log, local-only: save; shape doc; usage `show config dump`): Show the fully resolved configuration tree.
- `show config fmt` (read-only; wire ze-show:config-fmt; usage `show config fmt`): Pretty-print the configuration with consistent formatting.
- `show config graph` (offline): Show how components and peers depend on each other (DOT graph format).
- `show config history` (read-only; wire ze-show:config-history; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show config history`): List available configuration rollback points.
- `show config list` (read-only; wire ze-show:config-list; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show config list`): List all configuration files stored in the database.
- `show crashes` (read-only; wire ze-show:crashes; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show crashes [name <name>]`): View saved crash reports from panics.
- `show data cat` (read-only; wire ze-show:data-cat; args key:string; usage `show data cat <key>`): Print the raw content of a blob store entry.
- `show data list` (read-only; wire ze-show:data-list; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show data list`): List everything stored in the ZeFS blob store.
- `show data registered` (read-only; wire ze-show:data-registered; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show data registered`): List the key patterns registered by all subsystems.
- `show ddos flowspec` (read-only; wire ze-show:ddos-flowspec; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ddos flowspec`): Show the upstream FlowSpec/RTBH DDoS mitigation status: whether a rule is currently announced, the target vector it covers, and whether the leak-probe is running.
- `show ddos incidents` (read-only; wire ze-show:ddos-incidents; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ddos incidents`): Show the recent DDoS incident ring (newest first).
- `show ddos local` (read-only; wire ze-show:ddos-local; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ddos local`): Show the on-host DDoS mitigation status: whether an nft drop rule is currently installed and the target vector (prefix / proto / port) it covers.
- `show ddos status` (read-only; wire ze-show:ddos-status; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ddos status`): Show DDoS observation status: whether the incident store is running, the number of currently active attacks, and the number of incidents retained in the ring.
- `show debug` (read-only; wire ze-debug:debug-state; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show debug`): Show live debug state from the running daemon.
- `show debug profile` (offline): Show stored debug profiles (list, 'name ' for one, add 'module ' to filter).
- `show dns cache list` (read-only; wire ze-show:dns-cache-list; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show dns cache list`): List all non-expired DNS cache entries, sorted by shortest TTL first.
- `show dns cache record` (read-only; wire ze-show:dns-cache-record; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show dns cache record <name>`): Show DNS cache entries for one record name.
- `show dns cache stats` (read-only; wire ze-show:dns-cache-stats; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show dns cache stats`): Show DNS cache hit, miss, eviction, expiry, and hit-rate counters without changing cache contents.
- `show dns lookup` (read-only; wire ze-show:dns-lookup; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args hostname:string, type:enum; usage `show dns lookup <hostname> [type <A|AAAA|MX|NS|TXT|CNAME|PTR>]`): Look up a DNS name from the router.
- `show doctor` (read-only; wire ze-show:doctor; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show doctor`): Check if this box is ready to run Ze.
- `show env get` (read-only; wire ze-show:env-get; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; args name:string; usage `show env get <name>`): Show one environment variable in detail.
- `show env list` (read-only; wire ze-show:env-list; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show env list`): List all Ze environment variables with their current values.
- `show env registered` (read-only; wire ze-show:env-registered; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show env registered`): List every registered environment variable with metadata.
- `show errors` (read-only; wire ze-show:errors; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show errors`): Show recent errors across all subsystems, newest first.
- `show event delivery` (read-only; wire ze-show:event-delivery; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show event delivery`): Show which peers feed which attached processes.
- `show event list` (read-only; wire ze-bgp:event-list; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show event list`): List every event type you can subscribe to.
- `show event namespaces` (read-only; wire ze-show:event-namespaces; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show event namespaces`): List all event namespaces and how many events each has logged.
- `show event recent` (read-only; wire ze-show:event-recent; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show event recent`): Show recent events, newest first.
- `show firewall group` (read-only; wire ze-show:firewall-group; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show firewall group`): Show members of a firewall address/port group.
- `show firewall irr` (read-only; wire ze-show:firewall-irr-status; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show firewall irr`): Show IRR filter status for all cached ASN/AS-SET entries.
- `show firewall irr prefix` (read-only; wire ze-show:firewall-irr-prefix; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show firewall irr prefix <name>`): Show IRR-resolved prefixes for a cached entry.
- `show firewall ruleset` (read-only; wire ze-show:firewall-ruleset; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show firewall ruleset <name>`): Show the live firewall ruleset with per-term counters.
- `show flow export` (read-only; wire ze-show:flow-export; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show flow export [name <name>]`): Show flow export (NetFlow/IPFIX) collector status.
- `show flow recent` (read-only; wire ze-show:flow-recent; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args dst:string; usage `show flow recent [dst <dst>]`): Show recent conntrack flow records from the bounded recent-flow ring.
- `show geodns` (read-only; wire ze-show:geodns; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show geodns`): GeoDNS server status: enabled, bind addresses/port, client-IP source mode, zones, nameserver/host-set/source counts, and the current SOA serial.
- `show gnmi` (read-only; wire ze-show:gnmi; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show gnmi`): Show whether the gNMI server is running and how it is configured.
- `show health` (read-only; wire ze-show:health; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show health`): Show the health of every component and the overall status of this box.
- `show host` (read-only; wire ze-show:host-all; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show host`): Hardware inventory for this box.
- `show host all` (read-only; wire ze-show:host-all; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show host all`): Show the full hardware inventory in one shot.
- `show host cpu` (read-only; wire ze-show:host-cpu; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show host cpu`): Show what CPUs are in this box.
- `show host dmi` (read-only; wire ze-show:host-dmi; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show host dmi`): Show the box's identity from SMBIOS/DMI.
- `show host kernel` (read-only; wire ze-show:host-kernel; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show host kernel`): Show the running kernel version and boot parameters.
- `show host memory` (read-only; wire ze-show:host-memory; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show host memory`): Show installed memory and ECC health.
- `show host nic` (read-only; wire ze-show:host-nic; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show host nic`): Show physical NICs installed in this box.
- `show host platform` (read-only; wire ze-show:host-platform; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show host platform`): Show platform capabilities and constraints.
- `show host storage` (read-only; wire ze-show:host-storage; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show host storage`): Show storage devices attached to this box.
- `show host thermal` (read-only; wire ze-show:host-thermal; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show host thermal`): Show temperature sensors and thermal throttle events.
- `show interface` (read-only; wire ze-show:interface; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show interface`): Show network interfaces on this box.
- `show interface brief` (read-only; wire ze-show:interface-brief; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show interface brief`): One-line summary per interface: name, state, IP, and MTU.
- `show interface errors` (read-only; wire ze-show:interface-errors; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show interface errors`): Show interfaces that have errors or drops.
- `show interface name counters` (read-only; wire ze-show:interface-counters; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show interface name <name> counters`): Show counters for one interface.
- `show interface name detail` (read-only; wire ze-show:interface-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show interface name <name> detail`): Show full detail for one interface.
- `show interface rate` (read-only; wire ze-show:interface-rate; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show interface rate`): Show per-second traffic rates on your interfaces.
- `show interface scan` (read-only; wire ze-show:interface-scan; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show interface scan`): Discover and classify all OS interfaces.
- `show interface type` (read-only; wire ze-show:interface-type; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args type:string; usage `show interface type <type>`): Show only interfaces of a given type.
- `show isis database` (read-only; wire ze-show:isis-database; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show isis database`): Show the IS-IS link-state database.
- `show isis database detail` (read-only; wire ze-show:isis-database-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show isis database detail`): Show the IS-IS link-state database with TLV detail.
- `show isis hostname` (read-only; wire ze-show:isis-hostname; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show isis hostname`): Show the IS-IS dynamic-hostname mapping (RFC 5301).
- `show isis interface` (read-only; wire ze-show:isis-interface; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show isis interface`): Show IS-IS-enabled circuits.
- `show isis neighbor` (read-only; wire ze-show:isis-neighbor; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show isis neighbor`): Show IS-IS adjacencies.
- `show isis route` (read-only; wire ze-show:isis-route; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show isis route`): Show IS-IS-computed routes.
- `show isis route ipv6` (read-only; wire ze-show:isis-route-ipv6; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show isis route ipv6`): Show IS-IS-computed IPv6 routes (RFC 5308).
- `show isis spf-log` (read-only; wire ze-show:isis-spf-log; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show isis spf-log`): Show recent IS-IS SPF runs.
- `show l2tp` (read-only; wire ze-l2tp-api:summary; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp`): L2TP tunnel, session, and subscriber state.
- `show l2tp config` (read-only; wire ze-l2tp-api:config; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp config`): Show the resolved L2TP configuration.
- `show l2tp cqm` (read-only; wire ze-l2tp-api:cqm; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp cqm`): Show subscriber line quality (CQM latency buckets).
- `show l2tp echo` (read-only; wire ze-l2tp-api:echo; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp echo`): Show LCP echo health for a subscriber session.
- `show l2tp health` (read-only; wire ze-show:l2tp-health; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp health`): Find your worst L2TP sessions at a glance.
- `show l2tp listeners` (read-only; wire ze-l2tp-api:listeners; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp listeners`): Show which UDP sockets are listening for L2TP.
- `show l2tp observer` (read-only; wire ze-l2tp-api:observer; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp observer`): Show recent events for a session (debug aid).
- `show l2tp reliable` (read-only; wire ze-l2tp-api:reliable; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp reliable`): Show the reliable transport window for a tunnel.
- `show l2tp session history` (read-only; wire ze-l2tp-api:session-history; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp session history`): Show state transitions for a session over time.
- `show l2tp session id` (read-only; wire ze-l2tp-api:session; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args id:string; usage `show l2tp session id <id>`): Show full detail for one L2TP session.
- `show l2tp session traffic` (read-only; wire ze-l2tp-api:session-traffic; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp session traffic`): Show traffic counters for a subscriber's PPP interface.
- `show l2tp sessions` (read-only; wire ze-l2tp-api:sessions; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp sessions`): List all active L2TP sessions.
- `show l2tp statistics` (read-only; wire ze-l2tp-api:statistics; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp statistics`): Show aggregate L2TP protocol counters.
- `show l2tp tunnel history` (read-only; wire ze-l2tp-api:tunnel-history; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp tunnel history`): Show state transitions for a tunnel over time.
- `show l2tp tunnel id` (read-only; wire ze-l2tp-api:tunnel; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args id:string; usage `show l2tp tunnel id <id>`): Show full detail for one L2TP tunnel.
- `show l2tp tunnels` (read-only; wire ze-l2tp-api:tunnels; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show l2tp tunnels`): List all active L2TP tunnels.
- `show ldp binding` (read-only; wire ze-show:ldp-binding; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ldp binding`): Show LDP FEC-to-label bindings.
- `show ldp neighbor` (read-only; wire ze-show:ldp-neighbor; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ldp neighbor`): Show LDP neighbors and their session state.
- `show log levels` (read-only; wire ze-bgp:log-levels; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show log levels`): Show what log level each subsystem is using.
- `show log recent` (read-only; wire ze-bgp:log-recent; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args level:enum, component:string, count:uint; usage `show log recent [level <disabled|debug|info|warn|err>] [component <component>] [count <count>]`): Show recent log entries from the in-memory ring.
- `show metrics list` (read-only; wire ze-bgp:metrics-list; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show metrics list`): List all registered metric names (no values).
- `show metrics name` (read-only; wire ze-show:metrics-query; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show metrics name <name> [label <key> <value> ...]`): Show one Prometheus metric by name.
- `show metrics pool` (read-only; wire ze-bgp:pool-stats; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show metrics pool`): Show attribute pool memory usage and dedup efficiency.
- `show metrics values` (read-only; wire ze-bgp:metrics-values; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show metrics values`): Dump all metrics in Prometheus text format.
- `show mpls forwarding` (read-only; wire ze-show:mpls-forwarding; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args limit:uint; usage `show mpls forwarding [limit <limit>]`): Show MPLS forwarding entries installed in the kernel.
- `show neighbor` (read-only; wire ze-show:neighbor; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args family:enum; usage `show neighbor [family <ipv4|ipv6|any|all>]`): Show the ARP and neighbor discovery table.
- `show ospf` (read-only; wire ze-show:ospf; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf`): OSPFv2 process summary: router-id, areas, ABR/ASBR status, and stub-router (max-metric) state (RFC 2328).
- `show ospf border-routers` (read-only; wire ze-show:ospf-border-routers; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf border-routers`): Show routes to OSPF area-border and AS-boundary routers.
- `show ospf database` (read-only; wire ze-show:ospf-database; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database`): Show the OSPF link-state database.
- `show ospf database asbr-summary` (read-only; wire ze-show:ospf-database-asbr-summary; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database asbr-summary`): Show only ASBR-Summary-LSAs (Type 4).
- `show ospf database external` (read-only; wire ze-show:ospf-database-external; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database external`): Show only AS-external-LSAs (Type 5).
- `show ospf database network` (read-only; wire ze-show:ospf-database-network; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database network`): Show only Network-LSAs (Type 2).
- `show ospf database nssa-external` (read-only; wire ze-show:ospf-database-nssa-external; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database nssa-external`): Show only NSSA-external-LSAs (Type 7, RFC 3101).
- `show ospf database opaque-area` (read-only; wire ze-show:ospf-database-opaque-area; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database opaque-area`): Show only area-scope opaque-LSAs (Type 10, RFC 5250).
- `show ospf database opaque-area detail` (read-only; wire ze-show:ospf-database-opaque-area-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database opaque-area detail`): Decode each area-scope opaque LSA body into its typed TLVs (TE / Router-Information / Extended / Segment-Routing) or a generic type/length/hex view (spec-ospf-ext-14, RFC 5250).
- `show ospf database opaque-as` (read-only; wire ze-show:ospf-database-opaque-as; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database opaque-as`): Show only AS-scope opaque-LSAs (Type 11, RFC 5250).
- `show ospf database opaque-as detail` (read-only; wire ze-show:ospf-database-opaque-as-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database opaque-as detail`): Decode each AS-scope opaque LSA body into its typed TLVs (TE / Router-Information / Extended / Segment-Routing) or a generic type/length/hex view (spec-ospf-ext-14, RFC 5250).
- `show ospf database opaque-link` (read-only; wire ze-show:ospf-database-opaque-link; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database opaque-link`): Show only link-local opaque-LSAs (Type 9, RFC 5250).
- `show ospf database opaque-link detail` (read-only; wire ze-show:ospf-database-opaque-link-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database opaque-link detail`): Decode each link-local opaque LSA body into its typed TLVs (TE / Router-Information / Extended / Segment-Routing) or a generic type/length/hex view (spec-ospf-ext-14, RFC 5250).
- `show ospf database router` (read-only; wire ze-show:ospf-database-router; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database router`): Show only Router-LSAs (Type 1).
- `show ospf database router-information` (read-only; wire ze-show:ospf-database-router-information; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database router-information`): Show the Router Information LSAs (RFC 7770) for both address families.
- `show ospf database summary` (read-only; wire ze-show:ospf-database-summary; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf database summary`): Show only Summary-LSAs (Type 3, inter-area network).
- `show ospf graceful-restart` (read-only; wire ze-show:ospf-graceful-restart; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf graceful-restart`): Show OSPFv2 (IPv4) Graceful Restart state (RFC 3623).
- `show ospf instance` (read-only; wire ze-show:ospf-instance; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf instance`): Show the configured OSPFv2 instances (RFC 6549 Multi-Instance).
- `show ospf interface` (read-only; wire ze-show:ospf-interface; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf interface`): Show OSPF-enabled interfaces.
- `show ospf interface detail` (read-only; wire ze-show:ospf-interface-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf interface detail`): Show full per-interface state (spec-ospf-ext-14): ISM, DR/BDR election detail, all three timers, and the opaque-capable neighbour count.
- `show ospf ipv6` (read-only; wire ze-show:ospf-ipv6; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6`): Show the OSPFv3 (IPv6) address-family instances (RFC 5838).
- `show ospf ipv6 database` (read-only; wire ze-show:ospfv3-database; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 database`): Show the OSPFv3 (IPv6) link-state database with each native scope-aware LSA decoded (RFC 5340).
- `show ospf ipv6 database detail` (read-only; wire ze-show:ospfv3-database-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 database detail`): Decode every OSPFv3 LSA body with its scope-aware header (RFC 5340 section A.4.2.1).
- `show ospf ipv6 database extended` (read-only; wire ze-show:ospfv3-database-extended; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 database extended`): Show the RFC 8362 extended OSPFv3 LSAs (E-Router / E-Network / E-Inter-Area / E-AS-External / E-Link / E-Intra-Area-Prefix) decoded into named TLVs.
- `show ospf ipv6 database router detail` (read-only; wire ze-show:ospfv3-database-router-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 database router detail`): Decode each OSPFv3 Router-LSA body.
- `show ospf ipv6 database router-information` (read-only; wire ze-show:ospfv3-database-router-information; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 database router-information`): Show the OSPFv3 Router Information LSAs (RFC 7770, function code 12) decoded into capability bits and TLVs.
- `show ospf ipv6 database scope area` (read-only; wire ze-show:ospfv3-database-scope-area; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 database scope area`): Show only area-scope (S2/S1 = 01) LSAs.
- `show ospf ipv6 database scope as` (read-only; wire ze-show:ospfv3-database-scope-as; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 database scope as`): Show only AS-scope (S2/S1 = 10) LSAs.
- `show ospf ipv6 database scope link` (read-only; wire ze-show:ospfv3-database-scope-link; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 database scope link`): Show only link-local (S2/S1 = 00) LSAs, including the per-interface Link-LSA store.
- `show ospf ipv6 database segment-routing` (read-only; wire ze-show:ospfv3-database-segment-routing; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 database segment-routing`): Summarise the OSPFv3 Segment Routing content (RFC 8666) carried in the RI and extended LSAs.
- `show ospf ipv6 graceful-restart` (read-only; wire ze-show:ospf-ipv6-graceful-restart; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 graceful-restart`): Show OSPFv3 (IPv6) Graceful Restart state (RFC 5187).
- `show ospf ipv6 instance` (read-only; wire ze-show:ospfv3-instance; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 instance`): Enumerate the active OSPFv3 address-family instances (RFC 5838 section 2): each with its address family, Instance ID, area count, and neighbor count.
- `show ospf ipv6 interface` (read-only; wire ze-show:ospf-ipv6-interface; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 interface`): Show OSPFv3 (IPv6-family) interfaces and their RFC 4552 IPsec status.
- `show ospf ipv6 interface detail` (read-only; wire ze-show:ospfv3-interface-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 interface detail`): Show full per-interface OSPFv3 state (spec-ospf-ext-14): ISM, DR/BDR by Router ID, timers, the local Interface ID and Instance ID.
- `show ospf ipv6 neighbor` (read-only; wire ze-show:ospfv3-neighbor; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 neighbor`): Show OSPFv3 (IPv6) neighbors: the link-local address as identity, adjacency state, DR/BDR by Router ID, and dead time.
- `show ospf ipv6 neighbor detail` (read-only; wire ze-show:ospfv3-neighbor-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 neighbor detail`): Show full per-neighbor OSPFv3 state (spec-ospf-ext-14): the advertised Interface ID, DD sequence, decoded Options (R/V6/E/N/AF), list sizes, last NSM event, and timers.
- `show ospf ipv6 segment-routing` (read-only; wire ze-show:ospf-ipv6-segment-routing; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 segment-routing`): Show OSPFv3 (IPv6) Segment Routing state (RFC 8666).
- `show ospf ipv6 spf` (read-only; wire ze-show:ospfv3-spf; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 spf`): Show the OSPFv3 (IPv6) per-area SPF run history.
- `show ospf ipv6 spf detail` (read-only; wire ze-show:ospfv3-spf-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ipv6 spf detail`): Explain why each OSPFv3 route won (spec-ospf-ext-14), AF/Instance-ID tagged.
- `show ospf ldp-sync` (read-only; wire ze-show:ospf-ldp-sync; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf ldp-sync`): Show OSPF LDP-IGP synchronization state (RFC 5443, RFC 6138).
- `show ospf neighbor` (read-only; wire ze-show:ospf-neighbor; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf neighbor`): Show OSPF neighbors.
- `show ospf neighbor detail` (read-only; wire ze-show:ospf-neighbor-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf neighbor detail`): Show full per-neighbor state (spec-ospf-ext-14): DD sequence, decoded Options (including the RFC 5250 O-bit), request/summary list sizes, last NSM event, and timers.
- `show ospf route` (read-only; wire ze-show:ospf-route; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf route`): Show OSPF-computed routes.
- `show ospf route fast-reroute` (read-only; wire ze-show:ospf-route-fast-reroute; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf route fast-reroute`): Show OSPF fast-reroute (LFA / TI-LFA) backups (RFC 5286).
- `show ospf segment-routing` (read-only; wire ze-show:ospf-segment-routing; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf segment-routing`): Show OSPFv2 (IPv4) Segment Routing state (RFC 8665).
- `show ospf spf` (read-only; wire ze-show:ospf-spf; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf spf`): Show recent OSPF SPF runs.
- `show ospf spf detail` (read-only; wire ze-show:ospf-spf-detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf spf detail`): Explain why each route won (spec-ospf-ext-14): the candidate paths considered per prefix, the winning cost, and the RFC 2328 section 16.4 path-preference tie-break.
- `show ospf te-database` (read-only; wire ze-show:ospf-te-database; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf te-database`): Show the OSPF Traffic Engineering Database (RFC 3630 / RFC 5392).
- `show ospf virtual-links` (read-only; wire ze-show:ospf-virtual-links; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show ospf virtual-links`): Show OSPF virtual links (RFC 2328 section 15).
- `show ping` (read-only; wire ze-show:ping; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args dest:string, count:uint, size:uint, timeout:string; usage `show ping [dest <dest>] [count <count>] [size <size>] [timeout <timeout>]`): Ping a target from the router itself.
- `show pki certificate name` (read-only; wire ze-show:pki-certificate; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show pki certificate name <name>`): Inspect a specific certificate in detail.
- `show pki certificate name bundle pem` (read-only; wire ze-show:pki-certificate-bundle-pem; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show pki certificate name <name> bundle pem`): Export the certificate, its intermediates and its private key as one PEM stream.
- `show pki certificate name fingerprint` (read-only; wire ze-show:pki-certificate-fingerprint; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show pki certificate name <name> fingerprint [sha256|sha384|sha512]`): Show the hash of the certificate, to verify its identity against the one another system reports.
- `show pki certificate name pem` (read-only; wire ze-show:pki-certificate-pem; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show pki certificate name <name> pem`): Export the certificate, and any intermediate it carries, as PEM.
- `show pki certificates` (read-only; wire ze-show:pki-certificates; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show pki certificates`): List all loaded certificates with expiry dates.
- `show plugins` (offline): Every plugin compiled into this binary, with its families, RFCs, capability codes and the setup outcome its own init() recorded.
- `show policy chain peer` (read-only; wire ze-show:policy-chain; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `show policy chain peer <selector> [import|export]`): Show the import/export filter chain applied to a peer.
- `show policy list` (read-only; wire ze-show:policy-list; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show policy list`): List all available filter types and named instances.
- `show policy routes` (read-only; wire ze-show:policy-routes; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show policy routes`): Show policy-based routing rules.
- `show policy test peer` (read-only; wire ze-show:policy-test; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string, direction:enum; usage `show policy test peer <selector> <import|export> [filter <name>] update <hex> [source-asn4 <true|false>]`): Test what your policy does to a specific UPDATE.
- `show pppoe` (read-only; wire ze-pppoe-api:summary; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show pppoe`): PPPoE session and protocol state.
- `show pppoe interfaces` (read-only; wire ze-pppoe-api:interfaces; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show pppoe interfaces`): Show which interfaces are accepting PPPoE sessions.
- `show pppoe session id` (read-only; wire ze-pppoe-api:session; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args id:string; usage `show pppoe session id <id>`): Show full detail for one PPPoE session.
- `show pppoe sessions` (read-only; wire ze-pppoe-api:sessions; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show pppoe sessions`): List all active PPPoE sessions.
- `show pppoe statistics` (read-only; wire ze-pppoe-api:statistics; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show pppoe statistics`): Show PPPoE protocol message counters.
- `show probe-round` (read-only; wire ze-show:probe-round; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args dest:string, probes:uint, max-hops:uint, timeout:string; usage `show probe-round [dest <dest>] [probes <probes>] [max-hops <max-hops>] [timeout <timeout>]`): Run a parallel traceroute probe round to a target.
- `show reload-status` (read-only; wire ze-show:reload-status; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show reload-status`): Show how many config reloads the daemon has processed.
- `show route` (read-only; wire ze-show:route; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args prefix:string, limit:uint; usage `show route [prefix <prefix>] [limit <limit>]`): Show the kernel routing table.
- `show route lookup` (read-only; wire ze-show:route-lookup; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args ip:string; usage `show route lookup <ip>`): Look up which route the kernel would use for a given IP.
- `show rr peers` (read-only; wire ze-show:rr-peers; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show rr peers`): Show route reflector client peers.
- `show rr status` (read-only; wire ze-show:rr-status; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show rr status`): Show whether the route reflector is active.
- `show rsvp-te fast-reroute` (read-only; wire ze-show:rsvp-te-fast-reroute; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show rsvp-te fast-reroute`): Show RSVP-TE Fast Reroute (RFC 4090) protection state.
- `show rsvp-te interface` (read-only; wire ze-show:rsvp-te-interface; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show rsvp-te interface`): Show RSVP-TE bandwidth allocation per interface.
- `show rsvp-te lsp` (read-only; wire ze-show:rsvp-te-lsp; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show rsvp-te lsp`): Show RSVP-TE label-switched paths.
- `show rsvp-te tunnel` (read-only; wire ze-show:rsvp-te-tunnel; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show rsvp-te tunnel`): Show configured RSVP-TE tunnels and their current state.
- `show runtime memory` (read-only; wire ze-show:system-memory; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show runtime memory`): Show the Go runtime allocator memory stats.
- `show schema events` (read-only; wire ze-show:schema-events; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show schema events`): List all notification types defined in YANG API modules.
- `show schema handlers` (read-only; wire ze-show:schema-handlers; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show schema handlers`): Show which handler serves each YANG module.
- `show schema list` (read-only; wire ze-show:schema-list; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show schema list`): List all YANG schemas loaded by the daemon.
- `show schema methods` (read-only; wire ze-show:schema-methods; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show schema methods`): List all RPC methods defined in YANG API modules.
- `show schema protocol` (read-only; wire ze-show:schema-protocol; pipes always: json ndjson table text yaml raw no-more save, when-streaming: log, local-only: save; shape doc; usage `show schema protocol`): Show the wire protocol version and format details.
- `show static` (read-only; wire ze-show:static; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show static`): Show static routes defined in the configuration.
- `show status` (read-only; wire ze-system:daemon-status; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show status`): Show process status, uptime, and resource usage.
- `show storage smart` (read-only; wire ze-show:storage-smart; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show storage smart`): Show disk health via SMART data.
- `show subscriber` (read-only; wire ze-subscriber-api:summary; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show subscriber`): Show a summary of all subscriber sessions.
- `show subscriber id detail` (read-only; wire ze-subscriber-api:detail; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args id:string; usage `show subscriber id <id> detail`): Show everything about one subscriber session.
- `show system conntrack` (read-only; wire ze-show:system-conntrack; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show system conntrack`): Show the kernel connection tracking table.
- `show system cpu` (read-only; wire ze-show:system-cpu; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show system cpu`): Show CPU utilization context for the daemon.
- `show system date` (read-only; wire ze-show:system-date; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show system date`): Show the daemon's current wall-clock time and timezone.
- `show system file-descriptors` (read-only; wire ze-show:system-file-descriptors; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args mode:enum; usage `show system file-descriptors [mode <summary|detail>]`): Show how many file descriptors the daemon has open.
- `show system goroutines` (read-only; wire ze-show:system-goroutines; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args mode:enum; usage `show system goroutines [mode <summary|blocked|full>]`): Dump goroutine stacks for debugging hangs or deadlocks.
- `show system kernel-log` (read-only; wire ze-show:system-kernel-log; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args level:enum, count:uint; usage `show system kernel-log [level <emerg|alert|crit|err|warning|notice|info|debug>] [count <count>]`): Show kernel log messages (dmesg-style).
- `show system memory` (read-only; wire ze-show:system-memory-map; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show system memory`): Show how much memory the daemon is using, from the OS's view.
- `show system ntp` (read-only; wire ze-show:system-ntp; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show system ntp`): NTP clock synchronization status.
- `show system ntp peers` (read-only; wire ze-show:system-ntp-peers; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show system ntp peers`): Show NTP peers with offset, RTT, stratum, and reachability.
- `show system platform` (read-only; wire ze-show:system-platform; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show system platform`): Show what kind of platform the daemon is running on.
- `show system profile` (read-only; wire ze-show:system-profile; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args type:enum, duration:string; usage `show system profile [type <cpu|heap|goroutine|allocs>] [duration <duration>]`): Capture a runtime profile for performance analysis.
- `show system sockets` (read-only; wire ze-show:system-sockets; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args protocol:enum, state:string, port:uint; usage `show system sockets [protocol <tcp|udp>] [state <state>] [port <port>]`): Show open TCP and UDP sockets on this box.
- `show system subsystem list` (read-only; wire ze-show:system-subsystem-list; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show system subsystem list`): List every registered subsystem and whether it is running.
- `show system update` (read-only; wire ze-show:system-update; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show system update`): Check if a firmware update is available.
- `show system update history` (read-only; wire ze-show:system-update-history; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show system update history`): Show recent firmware update activity.
- `show tcp-check` (read-only; wire ze-show:tcp-check; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args host:string, port:uint, source:string, timeout:string; usage `show tcp-check <host> <port> [source <source>] [timeout <timeout>]`): Test TCP connectivity to a remote host and port.
- `show traceroute` (read-only; wire ze-show:traceroute; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args dest:string, max-hops:uint, timeout:string, probes:uint; usage `show traceroute [dest <dest>] [max-hops <max-hops>] [timeout <timeout>] [probes <probes>]`): Trace the network path from this router to a target.
- `show traffic control` (read-only; wire ze-show:traffic; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show traffic control`): Show traffic control (QoS) configuration per interface.
- `show traffic feature` (read-only; wire ze-show:traffic-feature; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show traffic feature [name <name>]`): Show neutral per-source traffic feature signals.
- `show traffic stat` (read-only; wire ze-show:traffic-stat; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show traffic stat [name <name>]`): Show aggregated traffic snapshot (interface rates, top talkers, top ports, severity).
- `show traffic usage` (read-only; wire ze-show:traffic-usage; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show traffic usage [name <name>]`): Show per-interface traffic byte counters captured by eBPF TCX.
- `show uptime` (read-only; wire ze-show:uptime; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show uptime`): Show how long the daemon has been running.
- `show version` (read-only; wire ze-show:version; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show version`): Show the running Ze version and build date.
- `show vpn ipsec dataplane drift` (read-only; wire ze-show:vpn-ipsec-dataplane-drift; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show vpn ipsec dataplane drift`): Compare what the IKE engine believes against what the kernel holds.
- `show vpn ipsec dataplane policy` (read-only; wire ze-show:vpn-ipsec-dataplane-policy; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show vpn ipsec dataplane policy`): Show the Security Policy Database the kernel holds.
- `show vpn ipsec dataplane sa` (read-only; wire ze-show:vpn-ipsec-dataplane-sa; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args spi:uint; usage `show vpn ipsec dataplane sa [spi <spi>]`): Show the Security Association Database the kernel holds.
- `show vpn ipsec peer name` (read-only; wire ze-show:vpn-ipsec-peer; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args name:string; usage `show vpn ipsec peer name <name>`): Show full detail for one IPsec peer.
- `show vpn ipsec sa` (read-only; wire ze-show:vpn-ipsec-sa; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show vpn ipsec sa`): Show all IKE and Child Security Associations.
- `show vpn ipsec status` (read-only; wire ze-show:vpn-ipsec-status; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show vpn ipsec status`): Quick IPsec health check.
- `show vpp runtime` (read-only; wire ze-show:vpp-runtime; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show vpp runtime`): Show VPP graph node processing statistics.
- `show vpp trace clear` (read-only; wire ze-show:vpp-trace-clear; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show vpp trace clear`): Discard the captured VPP trace buffer.
- `show vpp trace show` (read-only; wire ze-show:vpp-trace-show; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show vpp trace show`): Retrieve packets captured since the last trace start.
- `show vpp trace start` (read-only; wire ze-show:vpp-trace-start; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show vpp trace start`): Start capturing packets in the VPP dataplane.
- `show vrrp` (read-only; wire ze-show:vrrp; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show vrrp`): Show every VRRP virtual router.
- `show vrrp interface name` (read-only; wire ze-show:vrrp-interface; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args value:string; usage `show vrrp interface name [value <value>]`): Show the VRRP virtual routers on one parent interface.
- `show vrrp statistics` (read-only; wire ze-show:vrrp-statistics; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show vrrp statistics`): Show per-virtual-router counters.
- `show warnings` (read-only; wire ze-show:warnings; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `show warnings`): Show active warnings across all subsystems.
- `show yang completion` (read-only; wire ze-show:yang-completion; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show yang completion`): Show YANG paths available for tab completion.
- `show yang doc` (read-only; wire ze-show:yang-doc; usage `show yang doc`): Generate command reference docs from YANG schemas.
- `show yang tree` (read-only; wire ze-show:yang-tree; pipes always: json ndjson table text yaml raw no-more save match count first last display fill, when-streaming: log, local-only: save; shape tab; usage `show yang tree`): Print the YANG tree for a module in a readable hierarchy.

### `skills` commands (1)
- `skills` (offline): List the agent skills this binary carries, or fetch one by name.

### `support` commands (1)
- `support` (offline): Collect logs, config, state and diagnostics into one archive.

### `system` commands (8)
- `system command complete` (read-only; wire ze-system:command-complete; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `system command complete`): List the completion candidates for a partial command.
- `system command help` (read-only; wire ze-system:command-help; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `system command help`): Show the detailed help for one command.
- `system command list` (read-only; wire ze-system:command-list; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `system command list`): List every command the daemon knows.
- `system dispatch` (read-only; wire ze-system:dispatch; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `system dispatch`): Dispatch a text command through the command dispatcher.
- `system help` (read-only; wire ze-system:help; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `system help`): List the subcommands the system verb accepts.
- `system subsystem list` (read-only; wire ze-system:subsystem-list; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `system subsystem list`): List the subsystems registered in the daemon.
- `system version api` (read-only; wire ze-system:version-api; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `system version api`): Show the IPC protocol version.
- `system version software` (read-only; wire ze-system:version-software; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `system version software`): Show the ze software version.

### `update` commands (13)
- `update bgp irr all` (daemon; wire ze-update:irr-all; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `update bgp irr all`): Refresh all IRR prefix-lists immediately.
- `update bgp irr as-set` (daemon; wire ze-update:irr-as-set; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args as-set:string; usage `update bgp irr as-set <as-set>`): Refresh IRR prefix-list for a specific AS-SET.
- `update bgp irr asn` (daemon; wire ze-update:irr-asn; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args asn:string; usage `update bgp irr asn <asn>`): Refresh IRR prefix-list for a specific ASN.
- `update bgp peer prefix` (daemon; wire ze-update:bgp-peer-prefix; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args selector:string; usage `update bgp peer <selector> prefix`): Refresh max-prefix limits from PeeringDB.
- `update firewall irr all` (daemon; wire ze-update:firewall-irr-all; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `update firewall irr all`): Refresh all cached IRR prefix-lists.
- `update firewall irr as-set` (daemon; wire ze-update:firewall-irr-as-set; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args as-set:string; usage `update firewall irr as-set <as-set>`): Fetch or refresh IRR prefix-list for an AS-SET.
- `update firewall irr asn` (daemon; wire ze-update:firewall-irr-asn; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args asn:string; usage `update firewall irr asn <asn>`): Fetch or refresh IRR prefix-list for an ASN.
- `update serve` (offline): Serve this binary and its version manifest for update checks.
- `update system firmware apply` (daemon; wire ze-update:system-firmware-apply; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `update system firmware apply`): Full upgrade: download, verify, stage, and restart.
- `update system firmware check` (daemon; wire ze-update:system-firmware-check; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `update system firmware check`): Check for a new firmware version right now.
- `update system firmware download` (daemon; wire ze-update:system-firmware-download; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `update system firmware download`): Download the latest firmware image right now.
- `update system firmware restart` (daemon; wire ze-update:system-firmware-restart; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `update system firmware restart`): Reboot into the already-staged firmware.
- `update system firmware rollback` (daemon; wire ze-update:system-firmware-rollback; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `update system firmware rollback`): Roll back to the previous firmware and restart.

### `validate` commands (1)
- `validate config` (offline): Check a config for errors without applying it.

### `withdraw` commands (3)
- `withdraw all` (daemon; wire ze-bgp:withdraw-all; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; usage `withdraw all`): Withdraw every on-demand announcement.
- `withdraw id` (daemon; wire ze-bgp:withdraw-id; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args id:string; usage `withdraw id <id>`): Withdraw one announcement by the id that show announcements reports.
- `withdraw tag` (daemon; wire ze-bgp:withdraw-tag; pipes always: json ndjson table text yaml raw no-more save, with-rows: match count first last display fill, when-streaming: log, local-only: save; args key:string, value:string; usage `withdraw tag <key> [value <value>]`): Withdraw the announcements carrying a tag.

## Vendor command equivalents

Curated vendor command equivalents joined with the live Ze command catalog generated from ze help command --json. Ze paths are exact join keys; vendor commands are migration hints with provenance and confidence.
Updated: 2026-07-04.
Vendors: Junos MX (object-rooted), IOS XR (family-rooted-for-routing), SR OS (instance-rooted), VyOS (family-rooted-for-routing).

- BGP: BGP session summary. Ze: `show bgp`. Junos MX: `show bgp summary` (operational, verified). IOS XR: `show bgp summary` (operational, verified); `show bgp ipv4 unicast summary` (operational, verified). SR OS: `show router bgp summary` (operational, verified). VyOS: `show bgp ipv4 summary` (operational, verified); `show ip bgp summary` (operational, legacy).
- BGP: BGP peer list. Ze: `show bgp peer list`.
- BGP: Detailed BGP peer state. Ze: `show bgp peer detail`.
- BGP: Negotiated BGP capabilities for a peer. Ze: `show bgp peer capabilities`.
- BGP: Routes received from one BGP peer. Ze: `show bgp peer rib`. Junos MX: `show route receive-protocol bgp <peer>` (operational, verified).
- BGP: Routes advertised to one BGP peer. Ze: `show bgp peer rib`. Junos MX: `show route advertising-protocol bgp <peer>` (operational, verified).
- BGP: Best BGP routes. Ze: `show bgp rib best`. Junos MX: `show route protocol bgp` (operational, verified). IOS XR: `show bgp ipv4 unicast` (operational, verified).
- BGP: BGP route for a prefix. Ze: `show bgp rib best`. Junos MX: `show route <prefix> protocol bgp` (operational, verified). IOS XR: `show bgp ipv4 unicast <prefix>` (operational, verified). VyOS: `show ip bgp <prefix>` (operational, legacy).
- BGP: Refresh BGP routes without a hard reset. Ze: `request peer refresh`. Junos MX: `clear bgp neighbor <peer> soft-inbound` (operational, verified). IOS XR: `clear bgp ipv4 unicast <peer> soft in` (operational, verified). VyOS: `reset bgp <peer> soft in` (operational, verified).
- BGP: Hard reset one BGP peer. Ze: `request peer teardown`. Junos MX: `clear bgp neighbor <peer>` (operational, verified). IOS XR: `clear bgp ipv4 unicast <peer>` (operational, verified). VyOS: `reset bgp <peer>` (operational, verified).
- BGP: Pause or resume a BGP peer without deleting configuration. Ze: `request peer pause`, `request peer resume`.
- BGP: Stream live BGP events or updates. Ze: `monitor bgp`.
- BGP: BMP collector, peer, and session visibility. Ze: `show bmp sessions`, `show bmp peers`, `show bmp collectors`, `show bmp rib`.
- BGP: RPKI cache and validation session state. Ze: `show bgp irr`, `show bgp irr check`, `show bgp irr prefix`.
- Routing: Routing table. Ze: `show route`. Junos MX: `show route` (operational, verified). VyOS: `show ip route` (operational, verified).
- Routing: Route lookup for a prefix or address. Ze: `show route lookup`. Junos MX: `show route <prefix>` (operational, verified). VyOS: `show ip route <prefix>` (operational, verified).
- Routing: Static route configuration or state. Ze: `show static`.
- Interfaces: Interface list and brief status. Ze: `show interface brief`. VyOS: `show interfaces` (operational, verified).
- Interfaces: Detailed interface state. Ze: `show interface name detail`. VyOS: `show interfaces <name>` (operational, verified).
- Interfaces: Interface counters and error counters. Ze: `show interface name counters`, `show interface errors`.
- Interfaces: Clear interface counters. Ze: `clear interface counters`, `clear interface name counters`. VyOS: `clear interfaces ethernet <name> counters` (operational, verified).
- Neighbors: ARP and neighbor cache. Ze: `show arp`, `show neighbor`. VyOS: `show arp` (operational, verified).
- Firewall: Firewall ruleset and counters. Ze: `show firewall ruleset`.
- Firewall: Firewall address, network, and port groups. Ze: `show firewall group`.
- Firewall: Policy-based routing rules. Ze: `show policy routes`.
- VPN and access: IPsec security associations. Ze: `show vpn ipsec sa`, `show vpn ipsec status`.
- VPN and access: L2TP tunnel and session state. Ze: `show l2tp tunnels`, `show l2tp sessions`, `clear l2tp tunnel id`, `clear l2tp session id`.
- VPN and access: PPPoE subscriber sessions. Ze: `show pppoe sessions`, `show pppoe statistics`.
- Routing protocols: OSPF overview. Ze: `show ospf`, `show ospf ipv6`.
- Routing protocols: OSPF neighbor state. Ze: `show ospf neighbor`, `show ospf ipv6 neighbor`.
- Routing protocols: OSPF link-state database. Ze: `show ospf database`, `show ospf ipv6 database`.
- Routing protocols: Clear OSPF process, counters, or neighbor state. Ze: `clear ospf process`, `clear ospf counters`, `clear ospf neighbor`.
- Routing protocols: IS-IS neighbors, database, routes, and interfaces. Ze: `show isis neighbor`, `show isis database`, `show isis route`, `show isis interface`.
- Routing protocols: MPLS, LDP, and RSVP-TE state. Ze: `show ldp neighbor`, `show ldp binding`, `show mpls forwarding`, `show rsvp-te lsp`, `show rsvp-te tunnel`.
- Routing protocols: BFD session state. Ze: `show bfd sessions`, `show bfd session address`.
- System: Software version and uptime. Ze: `show version`, `show uptime`. VyOS: `show system uptime` (operational, verified).
- System: CPU, memory, platform, and host health. Ze: `show system cpu`, `show system memory`, `show system platform`, `show health`, `show doctor`. VyOS: `show hardware cpu` (operational, verified); `show system memory` (operational, verified).
- System: System time and NTP state. Ze: `show system date`, `show system ntp`, `show system ntp peers`. VyOS: `show date` (operational, verified); `show ntp` (operational, verified).
- Operations: Logs, warnings, and errors. Ze: `show log recent`, `show warnings`, `show errors`. VyOS: `show log` (operational, verified).
- Operations: Metrics inventory and values. Ze: `show metrics list`, `show metrics values`, `show metrics name`.
- Diagnostics: Ping and traceroute diagnostics. Ze: `show ping`, `show traceroute`, `resolve ping`, `resolve traceroute`. SR OS: `ping <target>` (operational, verified). VyOS: `ping <target>` (operational, verified); `traceroute <target>` (operational, verified).
- Diagnostics: DNS lookup and cache inspection. Ze: `show dns cache list`, `show dns cache record`, `show dns cache stats`, `show dns lookup`, `resolve dns a`, `resolve dns aaaa`, `resolve dns ptr`. VyOS: `show dns` (operational, verified).
- Configuration: Show running, candidate, and diffed configuration. Ze: `show config dump`, `show config diff`, `show config history`, `show config fmt`. VyOS: `show configuration` (operational, verified); `compare` (configuration, verified).
- Configuration: Validate and commit configuration. Ze: `request commit`. VyOS: `commit` (configuration, verified).
- Lifecycle: Reload, reboot, halt, or shut down. Ze: `request reload`, `request reboot`, `request halt`, `request shutdown`. VyOS: `reboot` (operational, verified); `poweroff` (operational, verified).
- Security: Certificate inventory. Ze: `show pki certificates`, `show pki certificate name`.
- Vendor-only gaps: LLDP neighbor discovery. Ze: .
- Vendor-only gaps: NAT translations and pools. Ze: .

## Dependency rationale

Direct Go modules are grouped by why Ze needs them. This is generated from go.mod plus curated rationale, not copied from package names alone.

### Terminal, Web & SSH Interfaces (9)
- `charm.land/bubbletea/v2`: The Elm-style TUI framework driving Ze's interactive CLI: config editor, dashboard, monitor, ping and traceroute views, run per SSH session.
- `charm.land/bubbles/v2`: Pre-built TUI widgets (text input, viewport) for the CLI's interactive screens.
- `charm.land/lipgloss/v2`: Styling and layout for the CLI's colors, borders, and widths.
- `charm.land/wish/v2`: SSH server middleware that wires each incoming session to run the CLI's Bubbletea program.
- `charm.land/ssh`: SSH session and public-key types that the wish server and the CLI's key-based auth build on.
- `github.com/a-h/templ`: Compiles the typed server-rendered components used by Ze's web configuration interface and BGP looking glass into Go.
- `github.com/charmbracelet/colorprofile`: Forces a deterministic color profile in render tests, so CLI screenshot/layout tests don't depend on the terminal running them.
- `github.com/muesli/reflow`: ANSI-aware text width calculation, so the CLI's prompt and status bar line up correctly despite embedded color codes.
- `github.com/creack/pty`: Opens a pseudo-terminal in a Linux integration test that exercises serial console handling.

### Networking & Protocols (6)
- `github.com/miekg/dns`: DNS message parsing and serving underneath Ze's DNS server engine, the AS112 blackhole plugin, and GeoDNS. It's the library underpinning CoreDNS.
- `github.com/insomniacslk/dhcp`: DHCPv4/DHCPv6 client used for interface lease handling and the installer's disk-provisioning DHCP client.
- `github.com/beevik/ntp`: NTP client queries used by the ntp plugin.
- `golang.zx2c4.com/wireguard/wgctrl`: Creates and configures WireGuard interfaces and peers from the interface netlink backend.
- `github.com/mdlayher/packet`: Raw AF_PACKET sockets behind the `diag capture` CLI command.
- `github.com/packetcap/go-pcap`: Only its filter subpackage: parses tcpdump/BPF-style filter expressions for `diag capture`. Packet capture itself goes through mdlayher/packet instead.

### Linux Kernel Interfaces (7)
- `github.com/vishvananda/netlink`: The main library for configuring the Linux network stack: links, addresses, routes, neighbors, bridges, VLANs, tunnels, IPsec, QoS. Used across interfaces, traffic, FIB, OSPF, ISIS, and MPLS.
- `github.com/vishvananda/netns`: Linux network-namespace handles, used alongside netlink for namespace-scoped route watching.
- `github.com/google/nftables`: Programs the kernel's nftables firewall (tables, chains, rules, sets) over netlink.
- `github.com/mdlayher/netlink`: Low-level generic netlink sockets, used to flush conntrack flow entries for flow export.
- `github.com/mdlayher/genetlink`: Generic netlink family dialing, used to attach to the kernel's psample subsystem for sampled flow export.
- `github.com/cilium/ebpf`: Assembles and attaches eBPF programs in pure Go, no clang or libbpf needed, to count traffic for the trafficusage plugin.
- `filippo.io/mlockexe`: Locks the running ze executable's own file-backed pages in memory with mlock2(2) and MLOCK_ONFAULT, so the kernel cannot evict the daemon's text under memory pressure. A router short of memory reclaims page cache, and the running daemon IS...

### Routing & Dataplane (2)
- `github.com/gaissmai/bart`: Balanced Adaptive Radix Trie: the longest-prefix-match structure underneath Ze's RIB store.
- `go.fd.io/govpp`: Binary-API client for FD.io VPP, used by the optional VPP dataplane backends for interfaces, firewall, traffic, FIB, and IKE.

### Config, RPC & Telemetry (5)
- `github.com/openconfig/goyang`: Parses and validates the YANG module definitions behind Ze's config schema, CLI completion, and validation engine.
- `github.com/openconfig/gnmi`: Generated gNMI protobuf/gRPC types that Ze's gNMI server implements for Get/Set/Subscribe/Capabilities.
- `google.golang.org/grpc`: Backs Ze's gRPC servers: gNMI and Ze's own management API.
- `google.golang.org/protobuf`: Runtime support for the generated protobuf message types behind Ze's gRPC API.
- `google.golang.org/grpc/cmd/protoc-gen-go-grpc`: Build-time only: the protoc plugin used to regenerate the gRPC API's Go bindings from ze.proto.

### Observability (3)
- `github.com/prometheus/client_golang`: Ze's internal metrics backend: counters, gauges, and histograms against a private registry.
- `github.com/prometheus/procfs`: Parses Linux /proc for the telemetry collector: CPU, memory, network device and socket stats, conntrack, softnet.
- `github.com/sirupsen/logrus`: Only to satisfy GoVPP's logging interface, bridged into Ze's own slog logger via a hook. Not used as Ze's own logger.

### Standard Library Extensions (golang.org/x) (7)
- `golang.org/x/crypto`: SSH protocol primitives and certificate signing for the SSH server, plus password hashing.
- `golang.org/x/mod`: Parses and rewrites go.mod files when the appliance build prepares an isolated instance of the tree, so an image is never built from the tracked working copy.
- `golang.org/x/net`: Raw ICMP packet connections for the traceroute plugin, and hostname normalization for MCP's auth.
- `golang.org/x/sys`: Low-level Linux syscalls: disk sync and reboot in the installer, used throughout the Linux-specific components.
- `golang.org/x/term`: Reads passwords without echo and detects an interactive terminal during CLI login.
- `golang.org/x/tools`: Build-time only: goimports, pinned via a tools.go tracking file, never compiled into Ze's binaries.
- `golang.org/x/vuln`: Build-time only: govulncheck, pinned through the tools.go tracking file, which reports known vulnerabilities in the modules Ze vendors.

### Testing & Build Tooling (9)
- `github.com/stretchr/testify`: Assertion and require helpers used across the Go unit test suite.
- `github.com/gokrazy/tools`: Drives gokrazy appliance image builds from Ze's appliance build tooling.
- `github.com/gokrazy/updater`: Referenced only in a regression test against Ze's own vendored update-push logic, written locally after a bug was found upstream. Not used in production.
- `github.com/sivchari/gomu`: Mutation-testing tool, run as a native Go command to advisory-score how well the test suite exercises the code. Not a build or CI gate.
- `gopkg.in/yaml.v3`: Parses GitHub Actions workflow YAML in verification tests so aliases, duplicate keys, and malformed trigger structures fail closed.
- `github.com/golangci/golangci-lint/v2`: The linter behind `./le verify lint run`, pinned through the tools.go tracking file so every machine and every CI job lints with one version.
- `honnef.co/go/tools`: Staticcheck, run beside golangci-lint from the same tools.go pin, for the analyses golangci-lint does not carry.
- `github.com/yuin/goldmark`: Renders the Markdown of `docs/` and the website into the published HTML pages, in the Go site build that replaced the retired Python renderers.
- `github.com/anmitsu/go-shlex`: Splits an operator-typed command line into words for the stress-reproduction runner, so a quoted argument survives.

## Complete documentation index

- [Ze Architecture](https://ze-software.net/architecture/index.md): A one-page guide to how Ze is structured. For the full design document with rationale, wire format details, and performance analysis, see DESIGN.md. For the canonical architecture reference, see... (web: https://ze-software.net/architecture/)
- [API Commands](https://ze-software.net/architecture/api/commands/index.md): Source: ExaBGP `reactor/api/command/`, `reactor/api/dispatch/` Purpose: Document all API commands for compatibility (web: https://ze-software.net/architecture/api/commands/)
- [Configuration Syntax Changes](https://ze-software.net/reference/deprecations/index.md): Ze has not shipped a stable configuration release yet. There is no public deprecation lifecycle for old config versions, and Ze does not promise a numbered generation ladder for config syntax. (web: https://ze-software.net/reference/deprecations/)
- [Interoperability Testing](https://ze-software.net/architecture/testing/interop/index.md): Ze validates protocol correctness against production BGP daemons in two complementary ways: live session interop tests (Docker containers running real daemons) and byte-level wire format validation against ExaBGP (Ze's... (web: https://ze-software.net/architecture/testing/interop/)
- [Ze Features](https://ze-software.net/reference/feature-status/index.md): Ze is an open-source configuration and protocol engine written in Go. The network operating system built on it runs on any Linux or as a gokrazy appliance. (web: https://ze-software.net/reference/feature-status/)
- [AI-First Design](https://ze-software.net/features/ai-first/index.md): Ze is built around a single command and discovery surface. Commands, configuration nodes, RPCs, events, and plugin metadata are registered once, then exposed through MCP and the operator interfaces that need them. AI... (web: https://ze-software.net/features/ai-first/)
- [API Commands](https://ze-software.net/features/api-commands/index.md): Commands sent through `ze cli`, `ze cli -c`, `ze show`, or process stdin. (web: https://ze-software.net/features/api-commands/)
- [BGP Protocol](https://ze-software.net/features/bgp-protocol/index.md): When several BGP candidates tie under multipath selection, Ze carries the winner and its equal-cost sibling next hops into the shared Loc-RIB. The system RIB then emits one ECMP group to the active FIB backend... (web: https://ze-software.net/features/bgp-protocol/)
- [RFC Implementation Status](https://ze-software.net/reference/rfcs/index.md): This page is Ze's public standards status page. It lists RFCs that Ze implements, partially implements, has deferred, or has explicitly decided not to implement. It is a product support view rather than a formal IETF... (web: https://ze-software.net/reference/rfcs/)
- [CLI Commands](https://ze-software.net/features/cli-commands/index.md): `show`, `clear`, `monitor`, `request`, `set`, and `delete` are the six verbs. The words after the verb are the YANG path. A bare form with no verb is not in the command tree, and the dispatcher answers `unknown command`... (web: https://ze-software.net/features/cli-commands/)
- [Configuration](https://ze-software.net/features/bgp-configuration/index.md): This page walks through BGP peer configuration specifically, since it is the most-configured surface. For every other subsystem's config syntax (interfaces, firewall, L2TP, DHCP, and the rest of Ze's 38 plugin groups)... (web: https://ze-software.net/features/bgp-configuration/)
- [DNS Resolver](https://ze-software.net/features/dns-resolver/index.md): Built-in DNS resolver component providing cached DNS queries to all Ze components. Uses `github.com/miekg/dns` (the library CoreDNS is built on). (web: https://ze-software.net/features/dns-resolver/)
- [ExaBGP Compatibility](https://ze-software.net/features/exabgp-compatibility/index.md): Automatic detection and migration of ExaBGP configuration files (web: https://ze-software.net/features/exabgp-compatibility/)
- [Fleet Management](https://ze-software.net/features/fleet-management/index.md): Ze supports centralized configuration for multi-node deployments. A central hub serves configuration to remote ze instances over TLS. (web: https://ze-software.net/features/fleet-management/)
- [Output Formatting](https://ze-software.net/features/formatting/index.md): Every command's output goes through the same pipe pipeline, whether you're poking around interactively or scripting against ze. One operator set, three ways to use it: set a persistent default, pipe it inline, or apply... (web: https://ze-software.net/features/formatting/)
- [Interface Management](https://ze-software.net/features/interfaces/index.md): Ze manages Linux network interfaces via pure netlink (no iproute2 shell-outs). JunOS-style two-layer model: physical interfaces with named logical units. (web: https://ze-software.net/features/interfaces/)
- [Interoperability Testing](https://ze-software.net/features/interoperability-testing/index.md): Ze ships a Docker-based interoperability test suite that verifies protocol correctness against real third-party BGP implementations. Tests are not mocks -- they launch actual daemon instances in containers and exchange... (web: https://ze-software.net/features/interoperability-testing/)
- [Self-Documenting System](https://ze-software.net/features/introspection/index.md): Ze is self-documenting: every plugin, environment variable, RPC, event type, and CLI command is registered at startup and discoverable at runtime. Nothing exists unregistered -- the system enforces this with... (web: https://ze-software.net/features/introspection/)
- [Looking Glass](https://ze-software.net/features/looking-glass/index.md): Ze includes a built-in looking glass that exposes BGP session state and route information via both an HTMX web UI and a birdwatcher-compatible REST API. The looking glass runs as a separate HTTP server on its own port... (web: https://ze-software.net/features/looking-glass/)
- [MCP Integration](https://ze-software.net/features/mcp-integration/index.md): Ze includes an MCP (Model Context Protocol) server that makes the BGP daemon AI-ready. Any AI assistant (Claude, GPT, or custom agents) can connect via MCP and fully control Ze -- the same operations available through... (web: https://ze-software.net/features/mcp-integration/)
- [Web Interface](https://ze-software.net/features/web-interface/index.md): Ze includes an HTTPS web interface for configuration viewing, editing, and runtime command execution through a browser. (web: https://ze-software.net/features/web-interface/)
- [Behavioural Anomaly Detection](https://ze-software.net/guides/anomaly/index.md): Ze includes a report-only behavioural anomaly detector. It builds a per-source behavioural baseline, scores how far each source deviates from its own history and from its peers, and correlates weak signals into a single... (web: https://ze-software.net/guides/anomaly/)
- [AS112](https://ze-software.net/guides/as112/index.md): Ze's `as112` plugin runs an AS112 anycast DNS node: an authoritative sink for misdirected RFC 1918 and link-local reverse-DNS queries (RFC 7534), plus the EMPTY.AS112.ARPA DNAME-redirection sink (RFC 7535). It answers... (web: https://ze-software.net/guides/as112/)
- [Authentication](https://ze-software.net/guides/authentication/index.md): Ze supports local operator users defined in the daemon configuration, in addition to the bootstrap super-admin written to `database.zefs` by `ze init`. This guide covers adding configured users, hashing their passwords... (web: https://ze-software.net/guides/authentication/)
- [Authorization](https://ze-software.net/guides/authorization/index.md): Ze uses profile-based command authorization (RBAC) to control which commands each user can execute. Profiles are defined in the daemon configuration alongside user accounts. Each user is assigned one or more profiles... (web: https://ze-software.net/guides/authorization/)
- [Audit Trail](https://ze-software.net/guides/audit/index.md): Ze keeps a local structured audit log for operator actions that change state or fail authentication. The log is append-only from the user's point of view: there is a query command, but no command to delete or truncate... (web: https://ze-software.net/guides/audit/)
- [Benchmarking](https://ze-software.net/guides/benchmarking/index.md): Ze includes `ze-perf`, a standalone tool for measuring BGP route propagation latency through a device under test (DUT). It works with any BGP implementation, including Ze. (web: https://ze-software.net/guides/benchmarking/)
- [BFD: Bidirectional Forwarding Detection](https://ze-software.net/guides/bfd/index.md): Status: the plugin is live, the production transport is hardened (GTSM, IP_TTL=255 outbound, SO_BINDTODEVICE for single-hop and multi-VRF, RFC 5880 §6.8.7 TX jitter), the BGP peer opt-in is wired through the reactor... (web: https://ze-software.net/guides/bfd/)
- [BMP (BGP Monitoring Protocol)](https://ze-software.net/guides/bmp/index.md): Ze implements RFC 7854 BMP in both directions: as a receiver (accepting feeds from routers) and as a sender (streaming state to collectors). (web: https://ze-software.net/guides/bmp/)
- [Command Reference](https://ze-software.net/guides/command-reference/index.md): Ze commands fall into two categories: shell commands that run locally and runtime commands sent to the running daemon via SSH. (web: https://ze-software.net/guides/command-reference/)
- [Configuration Editor](https://ze-software.net/guides/config-editor/index.md): Ze includes an interactive configuration editor with YANG-driven tab completion, rollback history, and live validation. (web: https://ze-software.net/guides/config-editor/)
- [DDoS Detection and Auto-Mitigation](https://ze-software.net/guides/ddos-mitigation/index.md): Ze can automatically detect volumetric DDoS attacks on its interfaces, mitigate them locally or upstream, and report incidents to the Flowtriq cloud. (web: https://ze-software.net/guides/ddos-mitigation/)
- [FlowSpec protected router](https://ze-software.net/guides/flowspec-protected-router/index.md): Use this when a Ze router receives BGP FlowSpec rules and should turn them into nftables filters, while also protecting its own BGP control plane from connection floods. (web: https://ze-software.net/guides/flowspec-protected-router/)
- [FlowSpec route reflector](https://ze-software.net/guides/flowspec-route-reflector/index.md): Use this when you want one Ze node to receive FlowSpec routes from mitigation systems or edge routers and reflect them to iBGP clients. (web: https://ze-software.net/guides/flowspec-route-reflector/)
- [Firewall](https://ze-software.net/guides/firewall/index.md): Ze manages nftables packet filter and NAT rules from a single `firewall { }` YANG section. The abstract data model describes matches (from) and actions (then); the nft backend lowers them to nftables kernel expressions. (web: https://ze-software.net/guides/firewall/)
- [Flow Export](https://ze-software.net/guides/flow-export/index.md): Ze exports interface counters and per-flow records to external collectors over UDP. The `flowexport` component is a registered component: it loads only when a `flow-export { }` section is present in the config. Counter... (web: https://ze-software.net/guides/flow-export/)
- [IS-IS](https://ze-software.net/guides/isis/index.md): Ze implements IS-IS (Intermediate System to Intermediate System, ISO/IEC 10589 as updated by RFC 1195 / RFC 5305 / RFC 5308 / ...) as a native link-state interior gateway protocol that runs directly over Layer 2. This... (web: https://ze-software.net/guides/isis/)
- [Native IKEv2 and IPsec](https://ze-software.net/guides/ipsec/index.md): Ze implements native IKEv2 in Go for route-based IPsec VPN tunnels. It does not require strongSwan, libreswan, or another external IKE daemon. The IKE engine, cryptographic primitives, wire codec, and XFRM dataplane... (web: https://ze-software.net/guides/ipsec/)
- [Filter BGP Imports with IRR](https://ze-software.net/guides/irr-filtering/index.md): Ze can build a BGP import prefix-list from an ASN or IRR AS-SET. The `bgp-filter-irr` plugin resolves the AS-SET through an IRR whois server, keeps the resulting IPv4 and IPv6 prefixes in ZeFS, and rejects received... (web: https://ze-software.net/guides/irr-filtering/)
- [L2TPv2 LNS subsystem](https://ze-software.net/guides/l2tp/index.md): Ze includes a native L2TPv2 (RFC 2661) LNS/LAC implementation used as a BNG (Broadband Network Gateway) component: it terminates L2TP tunnels over UDP, runs PPP negotiation (LCP, authentication, IPCP/IPv6CP), assigns... (web: https://ze-software.net/guides/l2tp/)
- [OSPF](https://ze-software.net/guides/ospf/index.md): Ze includes an experimental native OSPF engine under the `ospf` config root. The same engine drives OSPFv2 for IPv4 and OSPFv3 for the `address-family ipv6` subsection: interface and neighbor state, LSDB flooding, SPF... (web: https://ze-software.net/guides/ospf/)
- [Monitoring](https://ze-software.net/guides/monitoring/index.md): Ze provides real-time BGP event monitoring and a live peer dashboard through the CLI. Commands follow verb-first syntax: `monitor `. (web: https://ze-software.net/guides/monitoring/)
- [MRT Analysis](https://ze-software.net/guides/mrt-analysis/index.md): Ze includes `ze-analyze`, a standalone tool for analysing real-world BGP data from public route collectors (RIPE RIS, RouteViews). It processes MRT dump files to extract statistics that inform ze's internal buffer... (web: https://ze-software.net/guides/mrt-analysis/)
- [netlab](https://ze-software.net/labs/netlab/index.md): netlab builds a lab from a YAML topology and starts each node under containerlab. Ze runs as one of those nodes. netlab calls this the daemon tier: one YAML file and one directory of Jinja2 templates, with no Ansible... (web: https://ze-software.net/labs/netlab/)
- [Policy Routing](https://ze-software.net/guides/policy-routing/index.md): Ze supports policy-based routing (PBR) to steer traffic through alternate routing tables or next-hops based on L3/L4 match criteria. This is used for content filtering (Surfprotect), split tunneling, and traffic... (web: https://ze-software.net/guides/policy-routing/)
- [Plugins](https://ze-software.net/guides/plugins/index.md): Ze uses a plugin architecture for all features beyond core BGP session management. Plugins handle RIB storage, route reflection, graceful restart, RPKI validation, NLRI encoding, and more. (web: https://ze-software.net/guides/plugins/)
- [PPPoE Access Concentrator](https://ze-software.net/guides/pppoe/index.md): Ze implements an RFC 2516 PPPoE access concentrator for direct-attach subscriber access. PPPoE is the alternative to L2TP: subscribers connect over Ethernet to the BNG without an intermediate LAC/LNS tunnel. (web: https://ze-software.net/guides/pppoe/)
- [Production Diagnostics Guide](https://ze-software.net/guides/production-diagnostics/index.md): Symptom-based troubleshooting using Ze's built-in diagnostic commands. `ze doctor`, health checks, warning/error reports, support bundles, crash capture, and runtime probes are part of the product, so operators can... (web: https://ze-software.net/guides/production-diagnostics/)
- [Health Checks and System Readiness](https://ze-software.net/guides/system-readiness/index.md): Ze provides two complementary systems for monitoring operational health: offline doctor checks (pre-start readiness) and runtime health monitoring (continuous anomaly detection during operation). (web: https://ze-software.net/guides/system-readiness/)
- [Quick Start](https://ze-software.net/guides/quickstart/index.md): Get Ze running with two BGP peers in under 5 minutes. (web: https://ze-software.net/guides/quickstart/)
- [RADIUS admin AAA](https://ze-software.net/guides/radius/index.md): Ze authenticates operator logins (SSH, web, MCP) against RADIUS servers (RFC 2865) when the `system.authentication.radius` block is present. Local bcrypt users keep working as the fallback so an unreachable server... (web: https://ze-software.net/guides/radius/)
- [Route Injection](https://ze-software.net/guides/route-injection/index.md): Ze supports injecting routes at runtime through text, hex, or base64 encoded UPDATE commands. Routes can be sent from the CLI, from external plugins, or from process scripts. (web: https://ze-software.net/guides/route-injection/)
- [RPKI Origin Validation](https://ze-software.net/guides/rpki/index.md): Ze validates received BGP routes against RPKI ROA data. Invalid routes are rejected before entering the RIB. The feature connects to RTR cache servers (RFC 8210), downloads Validated ROA Payloads (VRPs), and applies the... (web: https://ze-software.net/guides/rpki/)
- [Static Routes](https://ze-software.net/guides/static-routes/index.md): Ze supports static routes with ECMP, weighted load balancing, BFD-tracked failover, blackhole, and reject. Routes are programmed directly to the kernel via netlink (or VPP when available). Routes are grouped under named... (web: https://ze-software.net/guides/static-routes/)
- [Public looking glass](https://ze-software.net/guides/public-looking-glass/index.md): Use this when you want a read-only HTTP looking glass for BGP peers, route lookup, prefix search, and AS-path graphs. (web: https://ze-software.net/guides/public-looking-glass/)
- [TACACS+ AAA](https://ze-software.net/guides/tacacs/index.md): Ze authenticates SSH logins against TACACS+ servers (RFC 8907) when the `system.authentication.tacacs` block is present. Local bcrypt users keep working as the fallback so an unreachable server cannot lock you out of... (web: https://ze-software.net/guides/tacacs/)
- [Operator access with SSH and RBAC](https://ze-software.net/guides/operator-access-rbac/index.md): Use this when the box already has Ze installed and `database.zefs` created, and you want real operator accounts instead of a single bootstrap admin. (web: https://ze-software.net/guides/operator-access-rbac/)
- [Traffic Usage](https://ze-software.net/guides/traffic-usage/index.md): Ze accounts per-(port, protocol) and, optionally, per-IP byte totals on operator-selected interfaces using eBPF programs attached via TCX. The counters are exported as Prometheus metrics and viewed with `show traffic... (web: https://ze-software.net/guides/traffic-usage/)
- [Terminal Demonstrations](https://ze-software.net/demos/terminal/index.md): These recordings run real Ze commands against isolated local fixtures. Each checked-in tape file defines every keystroke, pause, and terminal size. A release can therefore regenerate the recordings when Ze changes. (web: https://ze-software.net/demos/terminal/)
- [VRRP](https://ze-software.net/guides/vrrp/index.md): VRRP gives a LAN a gateway address that survives the loss of the router holding it. Two or more routers share a virtual IP; one is Active (Master) and answers for it, the others wait. If the Active router dies, a Backup... (web: https://ze-software.net/guides/vrrp/)
- [VPP Data Plane](https://ze-software.net/guides/vpp/index.md): Status: the VPP component manages the VPP process lifecycle (startup, crash recovery, DPDK NIC binding, GoVPP connection), the `fib-vpp` plugin programs routes from ze's system RIB directly into VPP's FIB via the GoVPP... (web: https://ze-software.net/guides/vpp/)
- [VM Appliance](https://ze-software.net/guides/appliance/index.md): Build a bootable VM image with Ze baked in using gokrazy. The default target is x86_64, and `image.arch` in `appliance.json` selects another architecture for `ze appliance build`. At runtime, the appliance is minimal... (web: https://ze-software.net/guides/appliance/)
- [Installation](https://ze-software.net/guides/ze-install/index.md): Ze provides commands for local installation, remote PXE provisioning, and appliance ISO installer media. (web: https://ze-software.net/guides/ze-install/)
- [Build and install Ze on Ubuntu](https://ze-software.net/guides/ubuntu-build-install/index.md): This page starts from a blank Ubuntu server and leaves you with an installed `ze` binary, a `database.zefs`, an SSH listener, a systemd service, and one place to add Ze features. (web: https://ze-software.net/guides/ubuntu-build-install/)
- [Performance Comparison](https://ze-software.net/performance/bgp/index.md): Ze-perf establishes two BGP sessions with a device under test (DUT): a sender and a receiver. The sender injects routes and records when each was sent. The receiver parses incoming UPDATEs and records when each prefix... (web: https://ze-software.net/performance/bgp/)
- [VPP Deployment Reference](https://ze-software.net/architecture/vpp-deployment/index.md): Source: 83 blog articles from ipng.ch (Pim van Pelt / IPng Networks, AS8298 / AS50869), VyOS VPP integration, and GoVPP documentation. (web: https://ze-software.net/architecture/vpp-deployment/)
- [Command Ownership and Plugin Structure](https://ze-software.net/architecture/command-ownership/index.md): Every feature in Ze passes two folder tests: (web: https://ze-software.net/architecture/command-ownership/)
- [Route Selection](https://ze-software.net/architecture/route-selection/index.md): Every route received by ze goes through two phases before it can become the best path for a prefix. Phase 1 determines whether the route is valid and eligible. Phase 2 determines which eligible route wins. A route that... (web: https://ze-software.net/architecture/route-selection/)
- [Ze System Architecture](https://ze-software.net/architecture/system-architecture/index.md): Status: Superseded / legacy design note (kept for background only) Last Updated: 2026-01-30 Purpose: Describes Ze's hub/orchestrator mode with separate plugin processes (web: https://ze-software.net/architecture/system-architecture/)
- [.ci Test File Format](https://ze-software.net/architecture/testing/ci-format/index.md): The `.ci` format is used by Ze's test runner to define functional tests. It supports embedded files (Tmpfs), test options, expectations, and commands. (web: https://ze-software.net/architecture/testing/ci-format/)
- [Documentation Testing](https://ze-software.net/contribute/documentation-testing/index.md): Ze ships native Go tools that compare documentation with the live registries and source tree. Pre-commit verification selects the checks affected by a change. (web: https://ze-software.net/contribute/documentation-testing/)
- [RFC Implementation Guide for Ze](https://ze-software.net/contribute/rfc-implementation-guide/index.md): This guide provides a step-by-step checklist for implementing an RFC in Ze. Use it alongside `planning.md` to ensure complete implementations. (web: https://ze-software.net/contribute/rfc-implementation-guide/)
- [Testing Guide](https://ze-software.net/contribute/testing/index.md): This guide covers how to run tests, what the different test types are, and how to interpret their output. For the full technical reference (`.ci` format spec, `.et` directives, fuzz target list), see... (web: https://ze-software.net/contribute/testing/)
- [SRv6 (Segment Routing over IPv6)](https://ze-software.net/features/srv6/index.md): Ze receives BGP routes carrying SRv6 Prefix-SID attributes (RFC 8669, RFC 9252), extracts SRv6 SIDs, validates them, and programs encapsulation into the FIB. Operates as an ingress PE: consumes received SRv6 SIDs but... (web: https://ze-software.net/features/srv6/)
- [Glossary](https://ze-software.net/reference/glossary/index.md): Terms used in Ze documentation and operator output. (web: https://ze-software.net/reference/glossary/)
- [ADD-PATH and PATHS-LIMIT](https://ze-software.net/guides/add-path/index.md): ADD-PATH (RFC 7911) allows multiple paths per prefix by including a Path Identifier with each NLRI. Route servers use it to forward all available paths rather than one best path. (web: https://ze-software.net/guides/add-path/)
- [REST and gRPC API](https://ze-software.net/guides/api/index.md): Ze exposes a programmatic API over REST (HTTP/JSON) and gRPC. Both transports share one engine -- they produce identical command output and support the same commands. (web: https://ze-software.net/guides/api/)
- [BGP Peering](https://ze-software.net/guides/bgp-peering/index.md): Use this guide after the quick start when a peer needs groups, multiple address families, capabilities, prefix limits, or an explicit relationship role. (web: https://ze-software.net/guides/bgp-peering/)
- [BGP Policy](https://ze-software.net/guides/bgp-policy/index.md): Ze applies ordered import and export filter chains to BGP updates. Define named policy objects once, attach them globally, to a group, or to one peer, then verify both accepted routes and the routes sent onward. (web: https://ze-software.net/guides/bgp-policy/)
- [BGP Resilience](https://ze-software.net/guides/bgp-resilience/index.md): BGP resilience is a set of separate mechanisms. Route Refresh reapplies policy without dropping the session. Graceful Restart retains forwarding during a restart. RIB persistence retains outbound state. Route reflection... (web: https://ze-software.net/guides/bgp-resilience/)
- [BGP Role (RFC 9234)](https://ze-software.net/guides/bgp-role/index.md): BGP Role enables route leak prevention by declaring the relationship between peers. When configured, ze adds the Only To Customer (OTC) path attribute to enforce proper route propagation based on the business... (web: https://ze-software.net/guides/bgp-role/)
- [Chaos Testing](https://ze-software.net/guides/chaos-testing/index.md): Ze includes a chaos testing mode that injects faults during operation to verify the daemon handles failures correctly. This is useful for validating configuration changes, testing plugin resilience, and finding edge... (web: https://ze-software.net/guides/chaos-testing/)
- [CLI Reference](https://ze-software.net/guides/cli/index.md): Ze provides an interactive CLI and single-command execution for runtime queries and control. All CLI access goes through the daemon's SSH server. (web: https://ze-software.net/guides/cli/)
- [Config Archive](https://ze-software.net/guides/config-archive/index.md): Ze archives configuration files to local or remote destinations. Archives can be triggered manually from the CLI, automatically on every editor commit, or on a schedule. (web: https://ze-software.net/guides/config-archive/)
- [Configuration](https://ze-software.net/guides/configuration-model/index.md): Ze uses a JUNOS-like hierarchical configuration format. (web: https://ze-software.net/guides/configuration-model/)
- [ZeBGP Debugging Tools](https://ze-software.net/guides/debugging-tools/index.md): This document describes the debugging tools available in ZeBGP for troubleshooting config parsing, BGP message encoding, and session issues. (web: https://ze-software.net/guides/debugging-tools/)
- [Developer Setup](https://ze-software.net/contribute/developer-setup/index.md): Set up a Ze development environment with all build, lint, and test dependencies. (web: https://ze-software.net/contribute/developer-setup/)
- [Environment Variables](https://ze-software.net/guides/environment-variables/index.md): Ze reads environment variables for daemon-wide settings and BGP protocol tuning. Config-backed keys have matching paths under `environment { }`. Bootstrap, safety, and test keys can be environment-only. (web: https://ze-software.net/guides/environment-variables/)
- [Fleet Configuration](https://ze-software.net/guides/fleet-config/index.md): Ze supports centralized configuration management for multi-node deployments. A central hub serves configuration to remote ze instances over TLS. (web: https://ze-software.net/guides/fleet-config/)
- [gNMI Guide](https://ze-software.net/guides/gnmi/index.md): Ze includes a gNMI (gRPC Network Management Interface) server that exposes YANG-modeled configuration over the industry-standard gNMI protocol. This allows management by tools like gnmic, Ansible, and network... (web: https://ze-software.net/guides/gnmi/)
- [Graceful Restart](https://ze-software.net/guides/graceful-restart/index.md): Graceful Restart (RFC 4724) preserves forwarding state across BGP session restarts. When a peer goes down and comes back, routes are held during the restart window instead of being immediately withdrawn, preventing... (web: https://ze-software.net/guides/graceful-restart/)
- [Healthcheck](https://ze-software.net/guides/bgp-healthcheck/index.md): Ze's healthcheck plugin monitors service availability by running shell commands and controls BGP route announcement/withdrawal via watchdog groups. It is the successor to ExaBGP's healthcheck program. (web: https://ze-software.net/guides/bgp-healthcheck/)
- [Lifecycle, Rollback, and Recovery](https://ze-software.net/guides/lifecycle/index.md): Use this guide for routine daemon control and configuration recovery. Ze separates configuration reload, process restart, and host reboot because they have different routing consequences. (web: https://ze-software.net/guides/lifecycle/)
- [Logging](https://ze-software.net/guides/logging/index.md): Ze uses hierarchical, structured logging with runtime-configurable levels per subsystem. (web: https://ze-software.net/guides/logging/)
- [Looking Glass](https://ze-software.net/guides/looking-glass/index.md): Ze includes a built-in looking glass that provides public, read-only access to BGP session state and route information. It runs as a separate HTTP server from the authenticated web UI. (web: https://ze-software.net/guides/looking-glass/)
- [Chaos MCP Server](https://ze-software.net/guides/mcp/chaos/index.md): ze-chaos includes an MCP (Model Context Protocol) server that lets AI assistants query chaos test state, detect problems, and control the chaos scheduler. (web: https://ze-software.net/guides/mcp/chaos/)
- [MCP Elicitation](https://ze-software.net/guides/mcp/elicitation/index.md): Ze can ask an MCP client for a value it needs but was not given. Under protocol revision `2026-07-28` it does so through Multi Round-Trip Requests (MRTR): the server does not send the client a request, it returns one. (web: https://ze-software.net/guides/mcp/elicitation/)
- [MCP Integration](https://ze-software.net/guides/mcp/overview/index.md): Ze includes an MCP (Model Context Protocol) server that lets AI assistants control BGP operations. The server runs inside the daemon and wraps the same command dispatcher used by the CLI and web interface. (web: https://ze-software.net/guides/mcp/overview/)
- [MCP Remote Access](https://ze-software.net/guides/mcp/remote-access/index.md): Ze's MCP server defaults to loopback binding (`127.0.0.1`). Two patterns make it reachable from other machines: (web: https://ze-software.net/guides/mcp/remote-access/)
- [MPLS](https://ze-software.net/guides/mpls/index.md): Ze can act as a Linux MPLS label-switching router (LSR). BGP labeled-unicast routes (RFC 8277) received from peers are programmed into the kernel MPLS FIB as label-push entries, so Ze forwards labeled traffic without... (web: https://ze-software.net/guides/mpls/)
- [Operations](https://ze-software.net/guides/operations/index.md): Running Ze in production: SSH access, signals, health checks, environment variables, and troubleshooting. (web: https://ze-software.net/guides/operations/)
- [Route Filters](https://ze-software.net/guides/redistribution/index.md): Route filters let plugins act as route filters on import (ingress) and export (egress). Filters are configured per peer, group, or globally using named references in a `filter {}` config block. Named filter types are... (web: https://ze-software.net/guides/redistribution/)
- [RSVP-TE](https://ze-software.net/guides/rsvp-te/index.md): RSVP-TE (RFC 3209, on top of RFC 2205 RSVP) signals explicitly-routed MPLS LSP tunnels with bandwidth reservation. It runs directly over IP as protocol 46 and requires `CAP_NET_RAW`. (web: https://ze-software.net/guides/rsvp-te/)
- [Self-Update Guide](https://ze-software.net/guides/self-update/index.md): Ze can automatically download, verify, and install firmware updates on non-gokrazy deployments. This guide covers server setup, client configuration, manual operations, and fleet deployment. (web: https://ze-software.net/guides/self-update/)
- [Traffic Control](https://ze-software.net/guides/traffic-control/index.md): Ze programs per-interface queueing disciplines, classes, and filters from a single `traffic { control { } }` YANG section. The same config is consumed by two backends; the operator chooses one via the `backend` leaf. (web: https://ze-software.net/guides/traffic-control/)
- [Web Interface](https://ze-software.net/guides/web-interface/index.md): Ze provides an HTTPS web interface for browsing configuration, editing values, and running commands through a browser. The web UI uses the same YANG schemas, user database, and command grammar as the SSH CLI. (web: https://ze-software.net/guides/web-interface/)
- [Project History](https://ze-software.net/project/history/index.md): Ze grew from the operational lessons of ExaBGP. ExaBGP made BGP programmable by treating routes and protocol events as input and output for ordinary processes. That model proved that a routing tool did not need to... (web: https://ze-software.net/project/history/)
- [Plugin Development](https://ze-software.net/developers/plugins/index.md): Ze plugins receive events, add configuration and commands, validate or apply configuration changes, implement address families and capabilities, and call the shared command dispatcher. External and in-process plugins... (web: https://ze-software.net/developers/plugins/)
- [Adding Commands](https://ze-software.net/developers/plugins/commands/index.md): Plugins can expose commands for runtime interaction via the ze API. (web: https://ze-software.net/developers/plugins/commands/)
- [Plugin Handlers](https://ze-software.net/developers/plugins/handlers/index.md): Handlers process engine-initiated callbacks during startup and at runtime. They are registered on the `sdk.Plugin` before calling `Run()`. (web: https://ze-software.net/developers/plugins/handlers/)
- [Plugin Metrics](https://ze-software.net/developers/plugins/metrics/index.md): Plugins that have observable runtime state register their own Prometheus metrics via the existing `ConfigureMetrics` callback in the `Registration` struct. (web: https://ze-software.net/developers/plugins/metrics/)
- [Plugin Protocol](https://ze-software.net/developers/plugins/protocol/index.md): Ze plugins communicate with the engine via newline-framed YANG RPCs over a single bidirectional connection. Internal plugins use `net.Pipe()` for startup; external plugins connect back via TLS. (web: https://ze-software.net/developers/plugins/protocol/)
- [YANG Schema Authoring](https://ze-software.net/developers/plugins/schema/index.md): Plugins declare their configuration schema using YANG (RFC 7950). This guide covers the basics for plugin authors. (web: https://ze-software.net/developers/plugins/schema/)
- [Testing Your Plugin](https://ze-software.net/developers/plugins/testing/index.md): This guide covers testing strategies for ze plugins using the SDK (`pkg/plugin/sdk`). (web: https://ze-software.net/developers/plugins/testing/)
- [Why Ze / Why Not Ze](https://ze-software.net/project/why-ze/index.md): An honest assessment of when Ze is the right tool and when it is not. (web: https://ze-software.net/project/why-ze/)
- [Use Cases](https://ze-software.net/use-cases/index.md): Use these pages when you want Ze to play a concrete role in a network. (web: https://ze-software.net/use-cases/)
- [AS112 anycast DNS inside a network](https://ze-software.net/use-cases/as112/index.md): Run Ze as the AS112 sink for RFC 1918 and link-local reverse DNS, then learn the four AS112 covering prefixes from Ze over BGP. (web: https://ze-software.net/use-cases/as112/)
- [ExaBGP Migration](https://ze-software.net/use-cases/exabgp-migration/index.md): Ze provides tools for converting ExaBGP configurations and running existing ExaBGP plugins with ze as the BGP engine. (web: https://ze-software.net/use-cases/exabgp-migration/)
- [BGP performance testing with Ze](https://ze-software.net/use-cases/bgp-performance/index.md): A route-server performance test is simple in outline: create a route source, connect it to a device under test, watch a monitor peer, and publish how many routes arrived and how long convergence took. (web: https://ze-software.net/use-cases/bgp-performance/)
- [Route Server at an IXP](https://ze-software.net/use-cases/route-server/index.md): Use Ze as an RFC 7947 route server when each member should maintain one eBGP session and receive eligible routes from the other members without next-hop rewriting. (web: https://ze-software.net/use-cases/route-server/)
- [Transit Edge with RPKI](https://ze-software.net/use-cases/transit-edge-rpki/index.md): This deployment shape uses Ze at an Internet edge with two transit sessions, origin validation, explicit import policy, and local preference for deterministic failover. (web: https://ze-software.net/use-cases/transit-edge-rpki/)
- [FlowSpec Injection](https://ze-software.net/use-cases/flowspec-injection/index.md): Use Ze to inject or relay BGP FlowSpec rules when an automation system must distribute traffic filters to routers. Keep the route source, policy, and withdrawal path explicit because a stale FlowSpec rule can affect... (web: https://ze-software.net/use-cases/flowspec-injection/)
- [Chaos-Tested BGP Peering](https://ze-software.net/use-cases/chaos-tested-peering/index.md): Use `ze-chaos` before deployment to prove that a peer configuration converges, survives selected faults, and produces a reproducible failure record when it does not. (web: https://ze-software.net/use-cases/chaos-tested-peering/)
- [AS-Path Topology](https://ze-software.net/use-cases/as-path-topology/index.md): Use the built-in Looking Glass to turn the AS paths for a prefix into a topology view. The graph is an operational aid for comparing what different peers advertise, not a complete map of the Internet. (web: https://ze-software.net/use-cases/as-path-topology/)

## Page map

Every link points to the page Markdown mirror first. The web URL is the human-rendered version of the same page.

## Start

### Install
- [Quickstart](https://ze-software.net/guides/quickstart/index.md): Two BGP peers in under five minutes (web: https://ze-software.net/guides/quickstart/)
- [Install Ze](https://ze-software.net/guides/ze-install/index.md): Run Ze on Linux or as a bootable appliance (web: https://ze-software.net/guides/ze-install/)
- [Ubuntu build](https://ze-software.net/guides/ubuntu-build-install/index.md): Compile Ze, install it, create zefs, and start SSH (web: https://ze-software.net/guides/ubuntu-build-install/)
### Learn
- [Architecture](https://ze-software.net/architecture/index.md): How the pieces fit together (web: https://ze-software.net/architecture/)
- [BGP configuration](https://ze-software.net/features/bgp-configuration/index.md): Peer settings, validation, inheritance, and policy (web: https://ze-software.net/features/bgp-configuration/)
- [CLI basics](https://ze-software.net/features/cli-commands/index.md): Diff, commit, history, and operator commands (web: https://ze-software.net/features/cli-commands/)

## Evaluate

### Capabilities and fit
- [Features](https://ze-software.net/features/index.md): 52 features, color-coded by category (web: https://ze-software.net/features/)
- [Compare](https://ze-software.net/compare/index.md): Compare Ze with BIRD, FRR, VyOS, and more (web: https://ze-software.net/compare/)
- [Performance](https://ze-software.net/performance/index.md): Measured BGP benchmarks and reports (web: https://ze-software.net/performance/)
### Evidence
- [Code quality](https://ze-software.net/quality/index.md): Tests, QEMU, fuzzing, and mutation checks (web: https://ze-software.net/quality/)
- [Testing health](https://ze-software.net/quality/health/index.md): Measured test state and how it moves over time (web: https://ze-software.net/quality/health/)
- [RFC compliance gate](https://ze-software.net/quality/rfc-compliance/index.md): RFC requirements, tests, gaps, and public claims checked by AI (web: https://ze-software.net/quality/rfc-compliance/)
- [Dependencies](https://ze-software.net/reference/dependencies/index.md): 48 direct packages, generated from go.mod (web: https://ze-software.net/reference/dependencies/)
- [Security](https://ze-software.net/security/index.md): Security policy and vulnerability reporting (web: https://ze-software.net/security/)

## Docs

### Operate
- [Documentation](https://ze-software.net/docs/index.md): All docs, organised by what you want to do (web: https://ze-software.net/docs/)
- [Production diagnostics](https://ze-software.net/guides/production-diagnostics/index.md): Start from symptoms and debug safely (web: https://ze-software.net/guides/production-diagnostics/)
- [Monitoring](https://ze-software.net/guides/monitoring/index.md): Metrics, health, and operational visibility (web: https://ze-software.net/guides/monitoring/)
- [Lifecycle and rollback](https://ze-software.net/guides/lifecycle/index.md): Start, reload, update, recover, and verify (web: https://ze-software.net/guides/lifecycle/)
- [Fleet management](https://ze-software.net/guides/fleet-config/index.md): Managed config, TLS hub, caching, and reconnect (web: https://ze-software.net/guides/fleet-config/)
### Guides and interfaces
- [Guides](https://ze-software.net/guides/index.md): Installation, configuration, operation, and troubleshooting (web: https://ze-software.net/guides/)
- [REST and gRPC](https://ze-software.net/guides/api/index.md): Commands, config sessions, authentication, and streaming (web: https://ze-software.net/guides/api/)
- [gNMI](https://ze-software.net/guides/gnmi/index.md): YANG Get, Set, Subscribe, TLS, and metrics (web: https://ze-software.net/guides/gnmi/)
- [Web interface](https://ze-software.net/guides/web-interface/index.md): Configuration workbench, commands, and live state (web: https://ze-software.net/guides/web-interface/)
- [Developer docs](https://ze-software.net/developers/index.md): Build plugins and contribute to Ze (web: https://ze-software.net/developers/)

## Examples

### Use cases
- [Use cases](https://ze-software.net/use-cases/index.md): Complete examples for real network roles (web: https://ze-software.net/use-cases/)
- [IXP route server](https://ze-software.net/use-cases/route-server/index.md): Member policy, RPKI validation, and replay (web: https://ze-software.net/use-cases/route-server/)
- [Transit edge with RPKI](https://ze-software.net/use-cases/transit-edge-rpki/index.md): Dual-transit policy and tested failover (web: https://ze-software.net/use-cases/transit-edge-rpki/)
- [FlowSpec injection](https://ze-software.net/use-cases/flowspec-injection/index.md): Authorised, atomic, reversible mitigation routes (web: https://ze-software.net/use-cases/flowspec-injection/)
- [ExaBGP migration](https://ze-software.net/use-cases/exabgp-migration/index.md): Migrate process scripts to Ze (web: https://ze-software.net/use-cases/exabgp-migration/)
### Labs and demonstrations
- [Testing labs](https://ze-software.net/labs/index.md): Interop labs you can run against real daemons (web: https://ze-software.net/labs/)
- [BGP interop](https://ze-software.net/labs/bgp-interop/index.md): FRR, BIRD, and GoBGP sessions (web: https://ze-software.net/labs/bgp-interop/)
- [IPsec and IKEv2](https://ze-software.net/labs/ipsec-interop/index.md): Ze IKE initiator against strongSwan (web: https://ze-software.net/labs/ipsec-interop/)
- [netlab topology](https://ze-software.net/labs/netlab/index.md): Ze as a netlab daemon node with a three-node topology (web: https://ze-software.net/labs/netlab/)
- [VPP dataplane](https://ze-software.net/labs/vpp-dataplane/index.md): FIB, traffic, and firewall in real VPP (web: https://ze-software.net/labs/vpp-dataplane/)
- [Appliance installer](https://ze-software.net/labs/appliance-install/index.md): HTTP/PXE, ISO, Ventoy, and tested boot paths (web: https://ze-software.net/labs/appliance-install/)
- [Terminal demos](https://ze-software.net/demos/terminal/index.md): Reproducible recordings of Ze workflows (web: https://ze-software.net/demos/terminal/)

## Reference

### Live system
- [Reference index](https://ze-software.net/reference/index.md): Commands, configuration, plugins, standards, and feature status (web: https://ze-software.net/reference/)
- [CLI reference](https://ze-software.net/reference/cli/index.md): 409 commands, generated from the live binary (web: https://ze-software.net/reference/cli/)
- [Configuration reference](https://ze-software.net/reference/configuration/index.md): %(config_sections)s sections, indexed from live YANG (web: https://ze-software.net/reference/configuration/)
- [Plugin catalogue](https://ze-software.net/reference/plugins/index.md): Find runtime plugins by purpose, config, or dependency (web: https://ze-software.net/reference/plugins/)
### Standards and lookup
- [RFC status](https://ze-software.net/reference/rfcs/index.md): Implemented RFCs, partial support, and known gaps (web: https://ze-software.net/reference/rfcs/)
- [Command equivalents](https://ze-software.net/reference/command-equivalents/index.md): Search Ze commands across Junos MX, IOS XR, SR OS, and VyOS (web: https://ze-software.net/reference/command-equivalents/)
- [Deprecated options](https://ze-software.net/reference/deprecations/index.md): Removed configuration syntax and replacements (web: https://ze-software.net/reference/deprecations/)
- [Glossary](https://ze-software.net/reference/glossary/index.md): Routing, policy, configuration, and test terms (web: https://ze-software.net/reference/glossary/)

## Project

### Progress and history
- [Project overview](https://ze-software.net/project/index.md): Progress, history, talks, and contribution paths (web: https://ze-software.net/project/)
- [Roadmap](https://ze-software.net/project/roadmap/index.md): What works today and what remains before the first release (web: https://ze-software.net/project/roadmap/)
- [Changes](https://ze-software.net/project/changes/index.md): 38 weekly updates, newest first (web: https://ze-software.net/project/changes/)
- [Milestones](https://ze-software.net/project/milestones/index.md): Landmark features, oldest to newest (web: https://ze-software.net/project/milestones/)
- [Activity](https://ze-software.net/project/activity/index.md): A year of commits, at a glance (web: https://ze-software.net/project/activity/)
- [Project history](https://ze-software.net/project/history/index.md): How ExaBGP's programmable model grew into Ze (web: https://ze-software.net/project/history/)
### Community and notes
- [FAQ](https://ze-software.net/faq/index.md): Common questions before trying Ze (web: https://ze-software.net/faq/)
- [Blog](https://ze-software.net/blog/index.md): Project notes and release stories (web: https://ze-software.net/blog/)
- [Talks](https://ze-software.net/talks/index.md): Talk slides and conference material (web: https://ze-software.net/talks/)
- [Contribute](https://ze-software.net/contribute/index.md): How to send code, reports, and interop results (web: https://ze-software.net/contribute/)
- [Contributor testing](https://ze-software.net/contribute/testing/index.md): How to choose and run the right test layer (web: https://ze-software.net/contribute/testing/)

## More

- [Discord](https://discord.gg/T8s7CjPDne): community and support
- [GitHub](https://github.com/ze-software/ze): canonical repository, issues, wiki
