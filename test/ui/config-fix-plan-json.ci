# Test: `ze config fix --plan` — repair plan with stable IDs
# VALIDATES: AC-9 fix-plan emits repair candidates with stable repair.id values

tmpfs=bad.conf:terminator=EOF_CONF
invalid syntax here
EOF_CONF

cmd=foreground:seq=1:exec=ze config fix --plan bad.conf
expect=exit:code=0
expect=stdout:contains=diagnostics
expect=stdout:contains=config-parse
