# Test: ze install help exposes local, systemd, and remote targets
# VALIDATES: service-1-systemd CLI surface is reachable through the shell entry point.

cmd=foreground:seq=1:exec=ze install -h
expect=exit:code=0
expect=stdout:contains=local
expect=stdout:contains=systemd
expect=stdout:contains=remote
