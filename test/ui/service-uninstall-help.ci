# Test: ze uninstall systemd help is reachable
# VALIDATES: service-1-systemd uninstall option is exposed through the CLI.

cmd=foreground:seq=1:exec=ze uninstall systemd -h
expect=exit:code=0
expect=stdout:contains=ze systemd uninstall
