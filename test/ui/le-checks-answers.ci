# Test: the built le product runs every native checks gate over the real
# accept-only: the registered native fixture enforces this scenario's behavioral contract
# checkout and preserves each command's data, rendering, exit, stream, scope,
# ordering, negative-case, and filesystem contracts.

option=timeout:value=1800s

cmd=foreground:seq=1:exec=ze-test fixture ui/le-checks-answers
expect=exit:code=0
expect=stdout:contains=OK
