# Test: the built le personality runs all documentation gates over the real
# accept-only: the registered native fixture enforces this scenario's behavioral contract
# checkout, returns structured answers, rejects row-only operators on document
# answers, and confines generated-file writes to isolated roots.

option=timeout:value=900s

cmd=foreground:seq=1:exec=ze-test fixture ui/le-docvalid-answers
expect=exit:code=0
expect=stdout:contains=OK
