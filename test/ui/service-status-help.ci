# Test: ze service status help is reachable
# VALIDATES: service-1-systemd status option is exposed through the CLI.

cmd=foreground:seq=1:exec=ze service status -h
expect=exit:code=0
expect=stdout:contains=ze service status
