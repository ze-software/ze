# Test: ze install systemd help is reachable
# VALIDATES: service-1-systemd install option is exposed through the CLI.

cmd=foreground:seq=1:exec=ze install systemd -h
expect=exit:code=0
expect=stdout:contains=ze systemd install
