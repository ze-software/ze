# Test: the compiled le personality dispatches registered commands, renders
# accept-only: the registered native fixture enforces this scenario's behavioral contract
# shared pipe operators, and permanently holds the completed native cutover.
#
# VALIDATES: spec-le-is-a-ze-binary AC-1, AC-7, and AC-12 through the product
# binary and its real composition root, personality selection, and argv handling.

option=timeout:value=180s

cmd=foreground:seq=1:exec=ze-test fixture ui/le-binary-dispatches
expect=exit:code=0
expect=stdout:contains=OK