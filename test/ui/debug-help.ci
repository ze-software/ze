# Test: ze debug with no args shows usage
# VALIDATES: CLI help output for debug command (toggle grammar, see 891-granular-debug)
cmd=foreground:seq=1:exec=ze debug
expect=exit:code=1
expect=stdout:contains=Toggle
expect=stdout:contains=show
expect=stdout:contains=profile
