# Test: show debug profile renders the stored debug-state table
# VALIDATES: verb-first stored debug view (set/delete/show grammar)
cmd=foreground:seq=1:exec=ze show debug profile name default
expect=exit:code=0
expect=stdout:contains=MODULE
expect=stdout:contains=LEVEL
expect=stdout:contains=FLAGS
