# Test: ze debug show renders the active debug-state table
# VALIDATES: structured debug-state display (toggle grammar, see 891-granular-debug)
cmd=foreground:seq=1:exec=ze debug show
expect=exit:code=0
expect=stdout:contains=MODULE
expect=stdout:contains=LEVEL
expect=stdout:contains=FLAGS
