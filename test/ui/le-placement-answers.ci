# Test: the built le personality exercises the module-tier, repository, and
# accept-only: the registered native fixture enforces this scenario's behavioral contract
# digest-anchor gates over the real checkout through the command engine.
#
# The native fixture builds into its private temporary directory, executes only
# the resulting product binary, and checks pages, operators, refusals, status
# codes, help visibility, stream placement, ordering, and report coverage.

option=timeout:value=600s

cmd=foreground:seq=1:exec=ze-test fixture ui/le-placement-answers
expect=exit:code=0
expect=stdout:contains=PASS: the three placement gates satisfy their checkout contract
