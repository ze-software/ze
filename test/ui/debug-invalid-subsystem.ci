# Test: ze debug enable with invalid subsystem shows error and valid list
# VALIDATES: AC-9 of spec-debug-flags.md
cmd=foreground:seq=1:exec=ze debug enable nonexistent.bogus
expect=exit:code=1
expect=stderr:contains=unknown subsystem
expect=stderr:contains=valid subsystems
