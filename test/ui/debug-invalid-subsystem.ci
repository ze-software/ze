# Test: set debug module with a malformed subsystem shows error and valid list
# VALIDATES: invalid-subsystem help (verb-first set grammar)
cmd=foreground:seq=1:exec=ze set debug module bgp/bad
expect=exit:code=1
expect=stderr:contains=unknown subsystem
expect=stderr:contains=valid subsystems
