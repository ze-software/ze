# Test: the built le personality publishes the functional suite contract,
# accept-only: the registered native fixture enforces this scenario's behavioral contract
# refuses an aggregate integration run, renders both answers through pipe
# operators, and reports both areas as fully claimed.

option=timeout:value=900s

cmd=foreground:seq=1:exec=ze-test fixture ui/le-functional-answers
expect=exit:code=0
expect=stdout:contains=OK
