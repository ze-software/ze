# Test: ze completion words -- ValueHints include log levels for request log level
# VALIDATES: AC-3 -- request log level shows log levels via ValueHints

cmd=foreground:seq=1:exec=ze completion words run request log level
expect=exit:code=0
expect=stdout:contains=debug
expect=stdout:contains=warn
expect=stdout:contains=err
expect=stdout:contains=disabled
