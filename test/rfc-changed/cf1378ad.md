# RFC-tagged test changes this session's commits carry

The owner's approval for changing a test that carries an `RFC requirement:` tag.
One row per test. An author MUST NOT write a row for their own change: the row
records Thomas's decision, and an author writing it is a forgery rather than a
shortcut (`docs/contributing/rfc-implementation-guide.md`).

| Test | Reason |
| ------ | ------ |
| TestNotApplicableDischargeReadsRFCTagCarriersAtTheCommit | Thomas approved this on 2026-09-09, in these words: "fix the red test". He was shown, before answering: that the test is RED and superseded, that AC-4 no longer depends on it because `TestNotApplicableDischargeReadsTaggedUnitsAtTheCommitParent` (`internal/le/commit/discharge_rfc_test.go`) proves the same criterion in both polarities with each half forced red separately, and that while it stays in place `go test ./internal/le/commit/` is red for every session touching that package. The change this row authorises is the DELETION of the superseded test. It binds no RFC: its `RFC requirement: rfc9999-1` is a FIXTURE string, spelled inside a Go literal that the test writes into a fake tagged file so `changedTaggedUnits` has something to find. `rfcTagPattern` (`internal/le/commit/rfcchange.go`) matches raw text, so the pattern reads the test function itself as a carrier for a requirement id that names no real RFC and appears in no `rfc/short/` summary. No conformance claim is lost, because none was ever made: nothing in `rfc/` cites this unit, and `rfc9999` does not exist. The test is red for its own fixture rather than for the product: it calls `commitFixture` BEFORE `debtRowFor`, so the commit it names never carries the ledger shard the commit-to-row binding requires, and it fatals at the ADMITTING case without ever reaching the refusing half it was written for. The replacement carries the fixture lines in the working order and spells its tag as an assembled string, `"\t// RFC requirement: " + rfcTagRequirement`, the idiom `internal/le/rfc/check_test.go` already uses, so it cannot freeze itself the way this one did. The recurrence is recorded in `plan/journal/guard-blocks-its-own-authors-repair.md`. |
