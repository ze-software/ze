| Test | Reason |
|------|--------|
| TestEAPTLSMutualAuthHandshakeSucceeds | Approved by Thomas on 2026-09-07 for the RFC 9190 Section 5.4 revocation work. Nothing this test asserts changed. Its file's shared PKI helper gained a CRL issuer: `eapTLSPKI` carries `trustedCA`, `trustedCAKey` and `trustedCRLPEM`, the test CAs gained `x509.KeyUsageCRLSign` because `x509.CreateRevocationList` refuses an issuer whose KeyUsage is set and omits it, and a `newCRL` helper was added. The gate fires because a tag's carrier is the whole FILE, so one added byte flags every tag in it. `RFC5216-2.4-4 positive` is proven by the same body as before: the key material handed to the caller after a successful mutual-auth handshake. |
| TestEAPTLSServerRejectsUntrustedClientChain | Approved by Thomas on 2026-09-07, same change and same file as the row above. The body is untouched; `RFC5216-5.3-1 negative` still asserts the authenticator path-validates the client chain and refuses an untrusted one. The empty CRL the harness now configures is a real answer, "no certificate this CA issued is revoked", so it cannot mask a path-validation failure. |
| TestEAPTLSPeerRejectsUntrustedServerChain | Approved by Thomas on 2026-09-07, same change and same file. The body is untouched; `RFC5216-5.3-1 negative` still asserts the peer path-validates the server chain and refuses an untrusted one. |
| TestEAPTLSPeerWithoutCARefusesToStart | Approved by Thomas on 2026-09-07, same change and same file. The body is untouched; `RFC5216-5.3-1 negative` still asserts a peer that cannot path-validate refuses to start rather than trusting the chain. |
| TestEAPTLSPeerFirstResponseCarriesClientHello | Approved by Thomas on 2026-09-07. The whole diff to this file is one added line, `CRLPEM: pki.trustedCRLPEM,` in a config literal. No assertion, expected value or offset moved. `RFC5216-2.1.1-2 positive` is proven by the same body: the peer's first EAP-Response carries the TLS ClientHello. |
| TestRFC3748EAPTLSValidatesItsPerPacketMIC | Approved by Thomas on 2026-09-07. The diff to this file is `CRLPEM:` lines in config literals and nothing else. `RFC3748-5.1-2 negative` is proven by the same body. |
| TestRFC3748KeyDerivingMethodAuthenticatesBothEnds | Approved by Thomas on 2026-09-07. Same file, same one-field addition. `RFC3748-7.5-1 negative` is proven by the same body: one octet changed inside an authenticated exchange still breaks it. |
| TestRFC5216PeerRepliesBeforeItTerminates | Approved by Thomas on 2026-09-07. The whole diff to this file is two `CRLPEM:` lines in config literals. `RFC5216-2.1.3-6 positive` is proven by the same body. |
| TestRFC5216SuccessfulTerminationSendsFlightAckThenSuccess | Approved by Thomas on 2026-09-07. The diff to this file is `CRLPEM:` lines only. `RFC5216-2.1.3-8 positive` is proven by the same body: the closing flight is acknowledged and then EAP-Success is sent. |
| TestRFC5216NoClosingFlightOrSuccessWhenThePeerIsRejected | Approved by Thomas on 2026-09-07. Same file, same one-field addition. `RFC5216-2.1.3-8 negative` is proven by the same body: a rejected peer receives neither the closing flight nor EAP-Success. |
| TestRFC5216PeerSendsItsAlertRatherThanTheNoDataResponse | Approved by Thomas on 2026-09-07. Same file, same one-field addition. `RFC5216-2.1.3-7 negative` is proven by the same body. |
| TestRFC5216ServerRepliesEAPFailureToPeerAlert | Approved by Thomas on 2026-09-07. The diff to this file is `CRLPEM:` lines only. `RFC5216-2.1.3-1 positive` is proven by the same body: the server answers a peer alert with EAP-Failure. |
| TestRFC5216ServerSendsNoEAPFailureWhenBothSidesAuthenticate | Approved by Thomas on 2026-09-07. Same file, same one-field addition. `RFC5216-2.1.3-4 negative` is proven by the same body. |
| TestEAPTLS13SendsProtectedSuccessIndication | Approved by Thomas on 2026-09-07. The diff to this file is `CRLPEM:` lines in config literals. `RFC9190-2.5-2 positive` is proven by the same body: the protected success indication is sent and no EAP-Request precedes it. |
| TestEAPTLS13RefusedClientGetsNoSuccessIndication | Approved by Thomas on 2026-09-07. Same file, same one-field addition. `RFC9190-2.5-1 negative` is proven by the same body: a refused client receives no success indication. |
| TestEAPTLS12SendsNoProtectedSuccessIndication | Approved by Thomas on 2026-09-07. Same file, same one-field addition. `RFC9190-2.5-1 negative` is proven by the same body: Section 2.5 applies to TLS 1.3 alone, so a TLS 1.2 session sends no protected success indication. |
