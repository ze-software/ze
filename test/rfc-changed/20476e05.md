| Test | Reason |
|------|--------|
| TestMigrationSessionIsIBGP | Approved by Thomas on 2026-09-07, who instructed this fix. Two changes. First, the table row "peer on a third asn with migration configured" is removed: it asserted that setMigrationAS ACCEPTS a remote AS outside the migrating pair, and the producer refuses exactly that with ErrMigrationASNotPeerAS, so the case died on its own require.NoError before reaching an assertion. RFC 7705 Section 4.2 scopes the mechanism to "configured iBGP peers" and to two ASNs, "either the globally configured ASN or a locally configured ASN", so refusing a third is the conformant behavior and the row was wrong about the product. No coverage is lost: the refusal is asserted by TestMigrationConfigRefusesTheTwoShapesSection42CannotDescribe, whose row "remote is neither of the pair" expects ErrMigrationASNotPeerAS and checks MigrationAS stays zero, and again by TestMigrationIBGPVerdictAgreesAcrossEverySite, which requires that error for the same pair. Second, the negative claim is NARROWED to drop the clause "a peer AS that is neither of the two is external even with the mechanism on", which the removed row was there to prove and which this body no longer checks. RFC7705-4.2-4 negative stays proven by the surviving row "peer on the legacy asn with no migration configured", where the same two AS numbers with no migration AS configured give IsIBGP false, IsEBGP true and an owed eBGP prepend. The third-AS-is-external property is still proven, over an ADVERTISED AS, by assert.False on isIBGPWith(migrationStrangerAS) at the end of TestMigrationIBGPVerdictAgreesAcrossEverySite. |
