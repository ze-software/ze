| Test | Reason |
|------|--------|
| TestCanonicalCollapsesEveryClientShapeOntoOneKey | No owner decision sits inside this one, and none was sought: it is a call-site adaptation that carries no claim change and no weakening. `api.SessionRequest.Canonical` now takes an `api.Topology` rather than a bare `[]api.Link`, because a MULTI-HOP request derives its local address from the interface the route to the peer leaves by, which the link table alone cannot answer. Every caller took the new argument. The test's edits are `req.Canonical(linkTable())` becoming `req.Canonical(Topology{Links: linkTable()})`, and `VRF: DefaultVRF` on the two fixture links, which `deriveLink` now requires because a link in another VRF is no longer a candidate for a request that does not name it. Its `RFC5882-4.4-1 positive` claim is untouched, its table of client shapes is untouched, and its assertion is the same: every shape reaches one key. The run is strictly stronger, because a link the request has no business being given is now excluded rather than matched. |
| TestPinnedSessionReachesTheSharedKey | A tag ADDED to a test this commit also adds, so nothing was reworded, weakened or retired. `RFC5882-4.4-1 positive` already held `TestBFDSharedSessionSameKey`, which proves the REGISTRY half: two requests carrying the same key share one refcounted session. That says nothing about whether two different clients ever produce the same key, which is where Section 4.4 was actually unmet: OSPF and a strict BGP peer to one neighbor built two keys and opened two sessions on one link. This unit drives the pinned client's real parser and request builder through `api.SessionRequest.Canonical` and asserts the key it arrives at; `TestStrictPeerRequestReachesTheSharedKey` and `TestOSPFNeighborRequestReachesTheSharedKey` assert the same key from the other two clients. Thomas chose this shape on 2026-09-09 ("make the two builders agree on the key") and the public row was NOT lowered: `rfc/short/rfc5882.md` still reads `Partial`, and the seven configurations where the derivation refuses and two sessions remain are listed on that page. The discrimination record is `rfc/discrimination/rfc5882.json`. |
