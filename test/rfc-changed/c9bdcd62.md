| Test | Reason |
|------|--------|
| TestLocRIBRouteMonitoringUsesFourByteASNs | Another session's change, landed by the first-green-push sweep of 2026-09-13 rather than authored here. One line moves: `buildLocRIBUpdateBody(family.IPv4Unicast, e)` becomes `buildLocRIBUpdateBody(family.IPv4Unicast, &e)`, following a producer signature that now takes its entry by pointer. The same mechanical edit lands at four untagged call sites in the same file. Nothing the tagged unit demonstrates moves: the body still builds a Loc-RIB route-monitoring message and still asserts the four-byte ASN encoding it asserted before, so the claim it carries on the ledger is the claim it carried. No discrimination record is owed, because the demonstrated behavior is unchanged and the tag's prose still describes what the body checks; a record would prove the same break it proved when the tag was written. |
