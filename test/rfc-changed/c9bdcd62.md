| Test | Reason |
|------|--------|
| TestHandleRefresh_InternalState | STRENGTHENED, not weakened: no assertion was removed, and the two it carried (peer still up, ribOut unchanged) still run. The body now reads the route commands the refresh emits, so it asserts what its tag already claimed: both IPv4 unicast routes are re-advertised, the IPv6 one is not, each carries meta["replay"], and the two RFC 7313 markers bracket them. Its old body could not have been red through the defect this change fixes -- `sendRoutes` (rib_commands.go) omitted meta["replay"], the destination peer's Adj-RIB-Out suppressed every re-advertised route under RFC 4271 Section 9.2, and the refresh answered with BoRR and EoRR alone, which RFC 7313 Section 4 turns into a purge of the whole family at the receiver. `test/plugin/plugin-refresh.ci` case 550 was red on that; this unit was green. The claim moved (it now names meta["replay"]), so a fresh discrimination record is owed and recorded. |
