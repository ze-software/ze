| Test | Reason |
|------|--------|
| rfc7606-54-bgpls-override-propagates | CORRECTED INPUT, not a weakened assertion. The fixture sent a Link-State Node NLRI whose Total NLRI Length was 11: a protocol-id, an 8-octet identifier, and two trailing octets that cannot frame a 4-octet TLV header. RFC 9552 Section 8.2.2 lists "The sum of all TLV lengths found in a Link-State NLRI corresponds to the Total NLRI Length field of all its descriptors" among the syntactic validations a BGP-LS Speaker MUST perform, so `bgplsNLRIWellFormed` (internal/component/bgp/message/rfc7606_bgpls_nlri.go) was right to discard it. The case asserted that ze should propagate bytes no conformant speaker may relay, so it could only ever have been green over a defect. It now sends the 13-octet form ze's own codec treats as well-formed, and the assertion it exists for is UNCHANGED: the unknown type 99 is preserved IN PLACE beside the type it parses. |
| rfc9552-52-rs-opaque-withdraw-peer-down | Same corrected input, plus a stale exact frame. Its peer-down withdrawal arrives as TWO UPDATEs, one NLRI each, because both peer blocks set `group-updates disable` and `nlriUnitLen` (reactor_api_batch.go) answers 1 for that; and a non-unicast withdrawal has carried the announce attribute block since 5def98e623. Neither is a defect and neither was asserted. The file now pins both frames, in the order `sendBatchedWithdrawals` sorts them, so it asserts MORE than before: the opaque type-99 withdrawal it is named for, and the type-1 withdrawal beside it. `server_opaque_withdrawal_test.go` pinned the same impossible 23-octet section and takes the well-formed bytes too. No ze product code changed. |
