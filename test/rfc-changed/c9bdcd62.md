| Test | Reason |
|------|--------|
| TestAHSAIdentifiedBySPIAndProtocol | Owner approved, 2026-09-14. CALL SITE ONLY, no assertion changed. buildIPsecSA gained a destination and a direction when the OSPFv3 IPsec SA stopped being one unreachable wildcard state and became one state per OSPF destination; the call supplies AllSPFRouters and the shared direction. The body still asserts the configured SPI and the AH protocol number, which are properties of the configuration and not of the destination. RFC4302-2.4-1 is proven exactly as before. |
| TestAHSAAddressMatchIndication | Owner approved, 2026-09-14. CALL SITE ONLY, no assertion changed. Same two arguments. The body asserts the state selector, which the redesign KEEPS: every per-destination state carries the same {::/0, ::/0, proto 89} selector, so the wildcard prefixes and the upper-protocol narrowing this test pins are untouched. RFC4302-2.4-5 is proven exactly as before. |
| TestIPsecSAProtocolNumber | Owner approved, 2026-09-14. CALL SITE ONLY, no assertion changed. Same two arguments on both calls. The body asserts that an esp interface builds protocol 50 and an ah interface 51, neither of which depends on the destination. RFC4302-2-1 is proven exactly as before. |
| TestIPsecSAAddressMatchIndication | Owner approved, 2026-09-14. CALL SITE ONLY, no assertion changed. Same two arguments. The body asserts the selector's /0 prefixes and its OSPF upper protocol, which the per-destination redesign preserves on every state it installs. RFC4303-2.1-2 is proven exactly as before. |
| TestIPsecSAReplayWindow | Owner approved, 2026-09-14. CALL SITE ONLY, no assertion changed. Same two arguments on both calls. The body asserts the replay window a manual SA carries with and without replay enabled, which is configuration and not destination. Its requirement is proven exactly as before. |
