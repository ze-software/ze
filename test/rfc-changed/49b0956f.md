| Test | Reason |
|------|--------|
| TestRFC6793MalformedAS4PathDiscarded | Approved by Thomas on 2026-09-09: "we can remove support for tomstone delete", the ruling that unblocked deleting `aspath_rewrite.go`. THE CLAIM IS UNCHANGED, WORD FOR WORD: the `RFC requirement:` lines above this test are not edited, and every assertion keeps its expected value. What moved is the ENTRY POINT. The test drove `RewriteASPath`, the whole-payload rewrite, which had no non-test caller (`plan/journal/unwired-feature.md`, 2026-09-05) and is deleted in this change; it now drives `ASPathEdit.Record`, the per-destination edit set both forward rails really use. The requirement is proven over the same rule either way, because both rails resolve the AS4_PATH question through the single declaration `AS4PathForRewrite` (`aspath_as4.go`). Two mechanical consequences, neither of them a weakening: the fixture gained an advertised prefix, because RFC 4271 Section 5.1.2 b conditions the prepend on a route being advertised and `Record` enforces that where the rewrite did not, so without it the test would have exercised the withdraw-only rail while claiming to test the prepend; and the assertions read the recorded edit rather than a rebuilt payload, through `rfc6793Egress`, which reproduces what the rebuild does (a suppressed attribute is absent, a generated one takes its generated value, an untouched one comes from the payload). Tags: RFC6793-6-4 positive. Still asserts that a malformed AS4_PATH from an OLD speaker is discarded, the AS_PATH prepend still happens, and the emitted AS4_PATH is derived from the AS_PATH with the malformed octets contributing nothing. |
| TestRFC6793WellFormedAS4PathNotDiscarded | Approved by Thomas on 2026-09-09: "we can remove support for tomstone delete", the ruling that unblocked deleting `aspath_rewrite.go`. THE CLAIM IS UNCHANGED, WORD FOR WORD: the `RFC requirement:` lines above this test are not edited, and every assertion keeps its expected value. What moved is the ENTRY POINT. The test drove `RewriteASPath`, the whole-payload rewrite, which had no non-test caller (`plan/journal/unwired-feature.md`, 2026-09-05) and is deleted in this change; it now drives `ASPathEdit.Record`, the per-destination edit set both forward rails really use. The requirement is proven over the same rule either way, because both rails resolve the AS4_PATH question through the single declaration `AS4PathForRewrite` (`aspath_as4.go`). Two mechanical consequences, neither of them a weakening: the fixture gained an advertised prefix, because RFC 4271 Section 5.1.2 b conditions the prepend on a route being advertised and `Record` enforces that where the rewrite did not, so without it the test would have exercised the withdraw-only rail while claiming to test the prepend; and the assertions read the recorded edit rather than a rebuilt payload, through `rfc6793Egress`, which reproduces what the rebuild does (a suppressed attribute is absent, a generated one takes its generated value, an untouched one comes from the payload). Tags: RFC6793-6-4 negative. Still asserts the counterpart: a well-formed received AS4_PATH is kept and its four-octet AS numbers survive into the AS4_PATH sent onward, with the same expected sequence. |
| TestRFC6793ReceivedConfedInAS4PathDiscarded | Approved by Thomas on 2026-09-09: "we can remove support for tomstone delete", the ruling that unblocked deleting `aspath_rewrite.go`. THE CLAIM IS UNCHANGED, WORD FOR WORD: the `RFC requirement:` lines above this test are not edited, and every assertion keeps its expected value. What moved is the ENTRY POINT. The test drove `RewriteASPath`, the whole-payload rewrite, which had no non-test caller (`plan/journal/unwired-feature.md`, 2026-09-05) and is deleted in this change; it now drives `ASPathEdit.Record`, the per-destination edit set both forward rails really use. The requirement is proven over the same rule either way, because both rails resolve the AS4_PATH question through the single declaration `AS4PathForRewrite` (`aspath_as4.go`). Two mechanical consequences, neither of them a weakening: the fixture gained an advertised prefix, because RFC 4271 Section 5.1.2 b conditions the prepend on a route being advertised and `Record` enforces that where the rewrite did not, so without it the test would have exercised the withdraw-only rail while claiming to test the prepend; and the assertions read the recorded edit rather than a rebuilt payload, through `rfc6793Egress`, which reproduces what the rebuild does (a suppressed attribute is absent, a generated one takes its generated value, an untouched one comes from the payload). Tags: RFC6793-6-3 positive and negative. Still asserts that AS_CONFED_SEQUENCE and AS_CONFED_SET segments received in an AS4_PATH are dropped while the AS_SEQUENCE carried beside them is retained, with the same expected AS numbers. |
| TestRFC6793ConstructedAS4PathExcludesConfed | Approved by Thomas on 2026-09-09: "we can remove support for tomstone delete". The test drove `RewriteASPath`, which that ruling deletes along with the draft-mangin-idr-attr-tombstone-00 Section 5.3 clear it was the only caller of. It now drives `ASPathEdit.Record`, the rail that survives, through the shared helper `rfc6793Prepend`. THE REQUIREMENT TAG BLOCK IS BYTE-IDENTICAL and so are the fixture and every assertion about what ze emits: what changed is the producer the test enters through, and that producer is the one a real egress uses. Discriminated: a probe forcing `as4PathForPath` to return nil reddens this test along with the others that moved with it |
| TestRFC6793TranscodeEmitsAS4PathForNonMappable | Approved by Thomas on 2026-09-09: "we can remove support for tomstone delete". The test drove `RewriteASPath`, which that ruling deletes along with the draft-mangin-idr-attr-tombstone-00 Section 5.3 clear it was the only caller of. It now drives `ASPathEdit.Record`, the rail that survives, through the shared helper `rfc6793Prepend`. THE REQUIREMENT TAG BLOCK IS BYTE-IDENTICAL and so are the fixture and every assertion about what ze emits: what changed is the producer the test enters through, and that producer is the one a real egress uses. Discriminated: a probe forcing `as4PathForPath` to return nil reddens this test along with the others that moved with it |
| TestRFC6793TranscodeOmitsAS4PathWhenAllMappable | Approved by Thomas on 2026-09-09: "we can remove support for tomstone delete". The test drove `RewriteASPath`, which that ruling deletes along with the draft-mangin-idr-attr-tombstone-00 Section 5.3 clear it was the only caller of. It now drives `ASPathEdit.Record`, the rail that survives, through the shared helper `rfc6793Prepend`. THE REQUIREMENT TAG BLOCK IS BYTE-IDENTICAL and so are the fixture and every assertion about what ze emits: what changed is the producer the test enters through, and that producer is the one a real egress uses. Discriminated: a probe forcing `as4PathForPath` to return nil reddens this test along with the others that moved with it |
