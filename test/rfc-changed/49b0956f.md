| Test | Reason |
|------|--------|
| TestFamilyRIB_InsertClearsStale | Approved by Thomas on 2026-09-08, who decided the ingest-collapse shape and said "start implementation". THE ONLY EDIT IS THE DROPPED ARGUMENT: `Insert(attrs, nlri, false)` becomes `Insert(attrs, nlri)`, and the same for `ParseRouteEntry`, because A-5 found every caller passes a four-octet width once the reconciliation happens at ingest and `ai/rules/no-layering.md` deletes a constant parameter rather than defaulting it. Not one assertion, input, fixture or claim is touched, and the test stays green. The requirement it carries is about RIB behaviour rather than about the AS width, so nothing it proves depends on the argument that left |
| TestFamilyRIB_InsertNewDuringStale | Approved by Thomas on 2026-09-08, who decided the ingest-collapse shape and said "start implementation". THE ONLY EDIT IS THE DROPPED ARGUMENT: `Insert(attrs, nlri, false)` becomes `Insert(attrs, nlri)`, and the same for `ParseRouteEntry`, because A-5 found every caller passes a four-octet width once the reconciliation happens at ingest and `ai/rules/no-layering.md` deletes a constant parameter rather than defaulting it. Not one assertion, input, fixture or claim is touched, and the test stays green. The requirement it carries is about RIB behaviour rather than about the AS width, so nothing it proves depends on the argument that left |
| TestPeerRIB_AddPath | Approved by Thomas on 2026-09-08, who decided the ingest-collapse shape and said "start implementation". THE ONLY EDIT IS THE DROPPED ARGUMENT: `Insert(attrs, nlri, false)` becomes `Insert(attrs, nlri)`, and the same for `ParseRouteEntry`, because A-5 found every caller passes a four-octet width once the reconciliation happens at ingest and `ai/rules/no-layering.md` deletes a constant parameter rather than defaulting it. Not one assertion, input, fixture or claim is touched, and the test stays green. The requirement it carries is about RIB behaviour rather than about the AS width, so nothing it proves depends on the argument that left |
| TestRFC4271OverlappingRoutesBothInstalled | Approved by Thomas on 2026-09-08, who decided the ingest-collapse shape and said "start implementation". THE ONLY EDIT IS THE DROPPED ARGUMENT: `Insert(attrs, nlri, false)` becomes `Insert(attrs, nlri)`, and the same for `ParseRouteEntry`, because A-5 found every caller passes a four-octet width once the reconciliation happens at ingest and `ai/rules/no-layering.md` deletes a constant parameter rather than defaulting it. Not one assertion, input, fixture or claim is touched, and the test stays green. The requirement it carries is about RIB behaviour rather than about the AS width, so nothing it proves depends on the argument that left |
| TestRFC4271PartialBitClearedOnReadvertisedWellKnown | Approved by Thomas on 2026-09-08, who decided the ingest-collapse shape and said "start implementation". THE ONLY EDIT IS THE DROPPED ARGUMENT: `Insert(attrs, nlri, false)` becomes `Insert(attrs, nlri)`, and the same for `ParseRouteEntry`, because A-5 found every caller passes a four-octet width once the reconciliation happens at ingest and `ai/rules/no-layering.md` deletes a constant parameter rather than defaulting it. Not one assertion, input, fixture or claim is touched, and the test stays green. The requirement it carries is about RIB behaviour rather than about the AS width, so nothing it proves depends on the argument that left |
| TestRFC4271PartialBitPreservedOnUnknownTransitive | Approved by Thomas on 2026-09-08, who decided the ingest-collapse shape and said "start implementation". THE ONLY EDIT IS THE DROPPED ARGUMENT: `Insert(attrs, nlri, false)` becomes `Insert(attrs, nlri)`, and the same for `ParseRouteEntry`, because A-5 found every caller passes a four-octet width once the reconciliation happens at ingest and `ai/rules/no-layering.md` deletes a constant parameter rather than defaulting it. Not one assertion, input, fixture or claim is touched, and the test stays green. The requirement it carries is about RIB behaviour rather than about the AS width, so nothing it proves depends on the argument that left |
| TestRFC4271PartialBitSurvivesLengthReframing | Approved by Thomas on 2026-09-08, who decided the ingest-collapse shape and said "start implementation". THE ONLY EDIT IS THE DROPPED ARGUMENT: `Insert(attrs, nlri, false)` becomes `Insert(attrs, nlri)`, and the same for `ParseRouteEntry`, because A-5 found every caller passes a four-octet width once the reconciliation happens at ingest and `ai/rules/no-layering.md` deletes a constant parameter rather than defaulting it. Not one assertion, input, fixture or claim is touched, and the test stays green. The requirement it carries is about RIB behaviour rather than about the AS width, so nothing it proves depends on the argument that left |
| TestRFC4271SamePrefixReplacesRatherThanAccumulates | Approved by Thomas on 2026-09-08, who decided the ingest-collapse shape and said "start implementation". THE ONLY EDIT IS THE DROPPED ARGUMENT: `Insert(attrs, nlri, false)` becomes `Insert(attrs, nlri)`, and the same for `ParseRouteEntry`, because A-5 found every caller passes a four-octet width once the reconciliation happens at ingest and `ai/rules/no-layering.md` deletes a constant parameter rather than defaulting it. Not one assertion, input, fixture or claim is touched, and the test stays green. The requirement it carries is about RIB behaviour rather than about the AS width, so nothing it proves depends on the argument that left |
| TestRFC4271WithdrawRemovesFromAdjRIBIn | Approved by Thomas on 2026-09-08, who decided the ingest-collapse shape and said "start implementation". THE ONLY EDIT IS THE DROPPED ARGUMENT: `Insert(attrs, nlri, false)` becomes `Insert(attrs, nlri)`, and the same for `ParseRouteEntry`, because A-5 found every caller passes a four-octet width once the reconciliation happens at ingest and `ai/rules/no-layering.md` deletes a constant parameter rather than defaulting it. Not one assertion, input, fixture or claim is touched, and the test stays green. The requirement it carries is about RIB behaviour rather than about the AS width, so nothing it proves depends on the argument that left |
| TestRFC7752DifferentIdentifierIsDifferentRoutingUniverse | Approved by Thomas on 2026-09-08, who decided the ingest-collapse shape and said "start implementation". THE ONLY EDIT IS THE DROPPED ARGUMENT: `Insert(attrs, nlri, false)` becomes `Insert(attrs, nlri)`, and the same for `ParseRouteEntry`, because A-5 found every caller passes a four-octet width once the reconciliation happens at ingest and `ai/rules/no-layering.md` deletes a constant parameter rather than defaulting it. Not one assertion, input, fixture or claim is touched, and the test stays green. The requirement it carries is about RIB behaviour rather than about the AS width, so nothing it proves depends on the argument that left |
| TestRFC7752SameIdentifierIsOneRoutingUniverse | Approved by Thomas on 2026-09-08, who decided the ingest-collapse shape and said "start implementation". THE ONLY EDIT IS THE DROPPED ARGUMENT: `Insert(attrs, nlri, false)` becomes `Insert(attrs, nlri)`, and the same for `ParseRouteEntry`, because A-5 found every caller passes a four-octet width once the reconciliation happens at ingest and `ai/rules/no-layering.md` deletes a constant parameter rather than defaulting it. Not one assertion, input, fixture or claim is touched, and the test stays green. The requirement it carries is about RIB behaviour rather than about the AS width, so nothing it proves depends on the argument that left |
