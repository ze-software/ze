| Test | Reason |
|------|--------|
| TestRFC6793AggregatorOfTheWrongWidthIsNotRead | Approved by Thomas on 2026-09-08, who decided the shape and then said "approve the design, start implementation". The design stores four-octet AS numbers only and reconciles the AS_PATH/AS4_PATH pair ONCE at ingest, which moves the RFC 6793 Section 4.2.3 rule out of `internal/component/bgp/plugins/rib/storage/attrparse.go` into `internal/core/bgp/attribute/as4.go`: the reactor is a component and may not import a plugin (`ai/rules/architecture.md`). THE ONLY EDIT TO THIS TEST IS ITS CALL: `ParseAttributes(raw, false)` becomes `ParseAttributes(raw)`, because A-5 found every caller passes a constant width once the collapse exists and `ai/rules/no-layering.md` deletes the parameter rather than defaulting it. Not one assertion, input or claim is touched. The test is RED, and that is the honest state rather than a weakening: it drives `storage.ParseAttributes` directly, and the behaviour it asserts now happens upstream of that function. Its successor of the same name in `internal/core/bgp/attribute/rfc6793_reconcile_test.go` carries the same requirement tag over the same inputs, so no requirement loses a carrier. Whether this copy is deleted or re-pointed at the ingest path is named in the owner report and is his decision, which is why it stands red rather than being removed here |
| TestRFC6793AggregatorWithASTransPromotesAS4Aggregator | Approved by Thomas on 2026-09-08, who decided the shape and then said "approve the design, start implementation". The design stores four-octet AS numbers only and reconciles the AS_PATH/AS4_PATH pair ONCE at ingest, which moves the RFC 6793 Section 4.2.3 rule out of `internal/component/bgp/plugins/rib/storage/attrparse.go` into `internal/core/bgp/attribute/as4.go`: the reactor is a component and may not import a plugin (`ai/rules/architecture.md`). THE ONLY EDIT TO THIS TEST IS ITS CALL: `ParseAttributes(raw, false)` becomes `ParseAttributes(raw)`, because A-5 found every caller passes a constant width once the collapse exists and `ai/rules/no-layering.md` deletes the parameter rather than defaulting it. Not one assertion, input or claim is touched. The test is RED, and that is the honest state rather than a weakening: it drives `storage.ParseAttributes` directly, and the behaviour it asserts now happens upstream of that function. Its successor of the same name in `internal/core/bgp/attribute/rfc6793_reconcile_test.go` carries the same requirement tag over the same inputs, so no requirement loses a carrier. Whether this copy is deleted or re-pointed at the ingest path is named in the owner report and is his decision, which is why it stands red rather than being removed here |
| TestRFC6793AggregatorWithRealASIgnoresAS4Attributes | Approved by Thomas on 2026-09-08, who decided the shape and then said "approve the design, start implementation". The design stores four-octet AS numbers only and reconciles the AS_PATH/AS4_PATH pair ONCE at ingest, which moves the RFC 6793 Section 4.2.3 rule out of `internal/component/bgp/plugins/rib/storage/attrparse.go` into `internal/core/bgp/attribute/as4.go`: the reactor is a component and may not import a plugin (`ai/rules/architecture.md`). THE ONLY EDIT TO THIS TEST IS ITS CALL: `ParseAttributes(raw, false)` becomes `ParseAttributes(raw)`, because A-5 found every caller passes a constant width once the collapse exists and `ai/rules/no-layering.md` deletes the parameter rather than defaulting it. Not one assertion, input or claim is touched. The test is RED, and that is the honest state rather than a weakening: it drives `storage.ParseAttributes` directly, and the behaviour it asserts now happens upstream of that function. Its successor of the same name in `internal/core/bgp/attribute/rfc6793_reconcile_test.go` carries the same requirement tag over the same inputs, so no requirement loses a carrier. Whether this copy is deleted or re-pointed at the ingest path is named in the owner report and is his decision, which is why it stands red rather than being removed here |
| TestRFC6793EqualCountsPrependNothing | Approved by Thomas on 2026-09-08, who decided the shape and then said "approve the design, start implementation". The design stores four-octet AS numbers only and reconciles the AS_PATH/AS4_PATH pair ONCE at ingest, which moves the RFC 6793 Section 4.2.3 rule out of `internal/component/bgp/plugins/rib/storage/attrparse.go` into `internal/core/bgp/attribute/as4.go`: the reactor is a component and may not import a plugin (`ai/rules/architecture.md`). THE ONLY EDIT TO THIS TEST IS ITS CALL: `ParseAttributes(raw, false)` becomes `ParseAttributes(raw)`, because A-5 found every caller passes a constant width once the collapse exists and `ai/rules/no-layering.md` deletes the parameter rather than defaulting it. Not one assertion, input or claim is touched. The test is RED, and that is the honest state rather than a weakening: it drives `storage.ParseAttributes` directly, and the behaviour it asserts now happens upstream of that function. Its successor of the same name in `internal/core/bgp/attribute/rfc6793_reconcile_test.go` carries the same requirement tag over the same inputs, so no requirement loses a carrier. Whether this copy is deleted or re-pointed at the ingest path is named in the owner report and is his decision, which is why it stands red rather than being removed here |
| TestRFC6793LeadingASSetIsTakenWhole | Approved by Thomas on 2026-09-08, who decided the shape and then said "approve the design, start implementation". The design stores four-octet AS numbers only and reconciles the AS_PATH/AS4_PATH pair ONCE at ingest, which moves the RFC 6793 Section 4.2.3 rule out of `internal/component/bgp/plugins/rib/storage/attrparse.go` into `internal/core/bgp/attribute/as4.go`: the reactor is a component and may not import a plugin (`ai/rules/architecture.md`). THE ONLY EDIT TO THIS TEST IS ITS CALL: `ParseAttributes(raw, false)` becomes `ParseAttributes(raw)`, because A-5 found every caller passes a constant width once the collapse exists and `ai/rules/no-layering.md` deletes the parameter rather than defaulting it. Not one assertion, input or claim is touched. The test is RED, and that is the honest state rather than a weakening: it drives `storage.ParseAttributes` directly, and the behaviour it asserts now happens upstream of that function. Its successor of the same name in `internal/core/bgp/attribute/rfc6793_reconcile_test.go` carries the same requirement tag over the same inputs, so no requirement loses a carrier. Whether this copy is deleted or re-pointed at the ingest path is named in the owner report and is his decision, which is why it stands red rather than being removed here |
| TestRFC6793LeadingConfedSegmentIsPrepended | Approved by Thomas on 2026-09-08, who decided the shape and then said "approve the design, start implementation". The design stores four-octet AS numbers only and reconciles the AS_PATH/AS4_PATH pair ONCE at ingest, which moves the RFC 6793 Section 4.2.3 rule out of `internal/component/bgp/plugins/rib/storage/attrparse.go` into `internal/core/bgp/attribute/as4.go`: the reactor is a component and may not import a plugin (`ai/rules/architecture.md`). THE ONLY EDIT TO THIS TEST IS ITS CALL: `ParseAttributes(raw, false)` becomes `ParseAttributes(raw)`, because A-5 found every caller passes a constant width once the collapse exists and `ai/rules/no-layering.md` deletes the parameter rather than defaulting it. Not one assertion, input or claim is touched. The test is RED, and that is the honest state rather than a weakening: it drives `storage.ParseAttributes` directly, and the behaviour it asserts now happens upstream of that function. Its successor of the same name in `internal/core/bgp/attribute/rfc6793_reconcile_test.go` carries the same requirement tag over the same inputs, so no requirement loses a carrier. Whether this copy is deleted or re-pointed at the ingest path is named in the owner report and is his decision, which is why it stands red rather than being removed here |
| TestRFC6793LongerAS4PathIsIgnored | Approved by Thomas on 2026-09-08, who decided the shape and then said "approve the design, start implementation". The design stores four-octet AS numbers only and reconciles the AS_PATH/AS4_PATH pair ONCE at ingest, which moves the RFC 6793 Section 4.2.3 rule out of `internal/component/bgp/plugins/rib/storage/attrparse.go` into `internal/core/bgp/attribute/as4.go`: the reactor is a component and may not import a plugin (`ai/rules/architecture.md`). THE ONLY EDIT TO THIS TEST IS ITS CALL: `ParseAttributes(raw, false)` becomes `ParseAttributes(raw)`, because A-5 found every caller passes a constant width once the collapse exists and `ai/rules/no-layering.md` deletes the parameter rather than defaulting it. Not one assertion, input or claim is touched. The test is RED, and that is the honest state rather than a weakening: it drives `storage.ParseAttributes` directly, and the behaviour it asserts now happens upstream of that function. Its successor of the same name in `internal/core/bgp/attribute/rfc6793_reconcile_test.go` carries the same requirement tag over the same inputs, so no requirement loses a carrier. Whether this copy is deleted or re-pointed at the ingest path is named in the owner report and is his decision, which is why it stands red rather than being removed here |
| TestRFC6793LongerASPathPrependsLeadingHops | Approved by Thomas on 2026-09-08, who decided the shape and then said "approve the design, start implementation". The design stores four-octet AS numbers only and reconciles the AS_PATH/AS4_PATH pair ONCE at ingest, which moves the RFC 6793 Section 4.2.3 rule out of `internal/component/bgp/plugins/rib/storage/attrparse.go` into `internal/core/bgp/attribute/as4.go`: the reactor is a component and may not import a plugin (`ai/rules/architecture.md`). THE ONLY EDIT TO THIS TEST IS ITS CALL: `ParseAttributes(raw, false)` becomes `ParseAttributes(raw)`, because A-5 found every caller passes a constant width once the collapse exists and `ai/rules/no-layering.md` deletes the parameter rather than defaulting it. Not one assertion, input or claim is touched. The test is RED, and that is the honest state rather than a weakening: it drives `storage.ParseAttributes` directly, and the behaviour it asserts now happens upstream of that function. Its successor of the same name in `internal/core/bgp/attribute/rfc6793_reconcile_test.go` carries the same requirement tag over the same inputs, so no requirement loses a carrier. Whether this copy is deleted or re-pointed at the ingest path is named in the owner report and is his decision, which is why it stands red rather than being removed here |
| TestRFC6793MalformedAS4PathIsDiscarded | Approved by Thomas on 2026-09-08, who decided the shape and then said "approve the design, start implementation". The design stores four-octet AS numbers only and reconciles the AS_PATH/AS4_PATH pair ONCE at ingest, which moves the RFC 6793 Section 4.2.3 rule out of `internal/component/bgp/plugins/rib/storage/attrparse.go` into `internal/core/bgp/attribute/as4.go`: the reactor is a component and may not import a plugin (`ai/rules/architecture.md`). THE ONLY EDIT TO THIS TEST IS ITS CALL: `ParseAttributes(raw, false)` becomes `ParseAttributes(raw)`, because A-5 found every caller passes a constant width once the collapse exists and `ai/rules/no-layering.md` deletes the parameter rather than defaulting it. Not one assertion, input or claim is touched. The test is RED, and that is the honest state rather than a weakening: it drives `storage.ParseAttributes` directly, and the behaviour it asserts now happens upstream of that function. Its successor of the same name in `internal/core/bgp/attribute/rfc6793_reconcile_test.go` carries the same requirement tag over the same inputs, so no requirement loses a carrier. Whether this copy is deleted or re-pointed at the ingest path is named in the owner report and is his decision, which is why it stands red rather than being removed here |
| TestRFC6793UnadjacentConfedSegmentIsNotPrepended | Approved by Thomas on 2026-09-08, who decided the shape and then said "approve the design, start implementation". The design stores four-octet AS numbers only and reconciles the AS_PATH/AS4_PATH pair ONCE at ingest, which moves the RFC 6793 Section 4.2.3 rule out of `internal/component/bgp/plugins/rib/storage/attrparse.go` into `internal/core/bgp/attribute/as4.go`: the reactor is a component and may not import a plugin (`ai/rules/architecture.md`). THE ONLY EDIT TO THIS TEST IS ITS CALL: `ParseAttributes(raw, false)` becomes `ParseAttributes(raw)`, because A-5 found every caller passes a constant width once the collapse exists and `ai/rules/no-layering.md` deletes the parameter rather than defaulting it. Not one assertion, input or claim is touched. The test is RED, and that is the honest state rather than a weakening: it drives `storage.ParseAttributes` directly, and the behaviour it asserts now happens upstream of that function. Its successor of the same name in `internal/core/bgp/attribute/rfc6793_reconcile_test.go` carries the same requirement tag over the same inputs, so no requirement loses a carrier. Whether this copy is deleted or re-pointed at the ingest path is named in the owner report and is his decision, which is why it stands red rather than being removed here |
| TestRFC6793LoneAS4AggregatorIsKeptUninterpreted | Same call-only edit as the ten rows above, and the same owner approval, but this one is NOT merely relocated and must not be read as such. It pins a REAL behaviour change: a lone AS4_AGGREGATOR arriving with no AGGREGATOR beside it used to be kept in the RIB's `OtherAttrs` ("the four-octet aggregating AS is still there to be read later"), and the reconciler now DROPS it and reports the discard, which is what `TestRFC6793LoneAS4AggregatorIsDropped` asserts in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`. RFC 6793 Section 4.2.3 writes its AGGREGATOR rules for the case where BOTH attributes are received, and RFC 4271 Section 5 asks an implementation to pass on an optional transitive attribute it does not understand, so dropping a lone one is a decision rather than a consequence. It is left RED on purpose so the change cannot land unnoticed, and it is put to Thomas in the owner report |
