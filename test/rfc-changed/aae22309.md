# RFC-tagged test changes this session's commits carry

The owner's approval for changing a file that carries an `RFC requirement:` tag.
One row per test. An author MUST NOT write a row for their own change: the row
records Thomas's decision, and an author writing it is a forgery rather than a
shortcut (`docs/contributing/rfc-implementation-guide.md`).

In both files below the tagged functions themselves did not change. The audit is
file-granular, so the row names the tagged function and describes the edit made
elsewhere in its file.

| Test | Reason |
| ------ | ------ |
