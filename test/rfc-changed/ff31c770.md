# RFC-tagged test changes this session's commits carry

The owner's approval for changing a test that carries an `RFC requirement:` tag.
One row per test. An author MUST NOT write a row for their own change: the row
records Thomas's decision, and an author writing it is a forgery rather than a
shortcut (`docs/contributing/rfc-implementation-guide.md`).

| Test | Reason |
| ------ | ------ |
| TestOwnerAutoDetection | Thomas approved this on 2026-09-08, answering "Approve, arity only" to a question that named the test, its six tags, and the exact edit. The test is `internal/plugins/vrrp/groups_test.go::TestOwnerAutoDetection` and it carries RFC3768-5.3.4-1, RFC5798-5.2.4-1 and RFC9568-5.2.4-1, each in both polarities: the router that owns the virtual address runs at priority 255, and a non-owner is not raised to it. Interface tracking widened `GroupSpec.EffectivePriority` to take the summed decrement, so the test's four call sites read `EffectivePriority(0)` where they read `EffectivePriority()`. Nothing else in the test moved. Both assertions keep their expected values, 255 for the owner and 100 for the non-owner, every tag line is byte-identical, no claim was reworded, and no assertion was added, removed or relaxed. Zero is the value that makes the new argument say what the old signature said, because no tracked interface is down in this test's fixtures, so each of the six requirements is proven on exactly the behavior it was proven on before. |
