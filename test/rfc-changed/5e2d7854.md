# RFC-tagged test changes this session's commits carry

The owner's approval for changing a test that carries an `RFC requirement:` tag.
One row per test. An author MUST NOT write a row for their own change: the row
records Thomas's decision, and an author writing it is a forgery rather than a
shortcut (`docs/contributing/rfc-implementation-guide.md`).

| Test | Reason |
| ------ | ------ |
| rfc2661-emitted-control-shape | Thomas approved this on 2026-09-08, in these words: "the 2 ci are approved". The 2 files he was shown are `test/l2tp/rfc2661-emitted-control-shape.ci` and `rfc/discrimination/rfc2661.json`, and the change to them is purely additive. The `.ci` gains three `expect=stdout:contains=PASS: ...` lines, one for each requirement the carrier already binds, and the JSON gains the three discrimination records those lines let it cite. Every assertion, tag and claim that was already in the carrier is untouched, and no line was removed or relaxed. The three lines are owed because a functional record is refused with `citation-gone` when it can cite only the whole-suite assertion, so each of RFC2661-4.1-1, RFC2661-4.1-2 and RFC2661-x-1 needs an assertion of its own to cite. Each new line is positive: the fixture prints `PASS: <check>` only when that check held. All three requirements stay proven on the same emitted datagram bytes, read by the same external parser, with the same quoted requirement text: RFC2661-4.1-2 on the Message Type AVP being first in the SCCRP, RFC2661-x-1 on control header reserved bits 8-11 being zero, and RFC2661-4.1-1 on every emitted AVP carrying reserved bits 2-5 clear. Each record was written after the `.ci` was observed RED with `writeSCCRPBody`, `WriteControlHeader` and `WriteAVPHeader` disabled in turn. |
