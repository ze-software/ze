# RFC-tagged test changes this session's commits carry

The owner's approval for changing a test that carries an `RFC requirement:` tag.
One row per test. An author MUST NOT write a row for their own change: the row
records Thomas's decision, and an author writing it is a forgery rather than a
shortcut (`docs/contributing/rfc-implementation-guide.md`).

| Test | Reason |
| ------ | ------ |
| TestEAPTLSAuthenticatorSendsTheAlertBeforeItReportsTheFailure | Approved by Thomas in session, 2026-09-07, after reading the diff. The test drove `tlsMethod` directly and stopped on `m.alertSent`, the field the fix deletes; it now drives a real `eap.Session` and stops on the recorded cause. Both RFC 5216 Section 2.1.3 claims were reworded to state what the body checks at the wire: the alert round sends an EAP-Request and no EAP-Failure or EAP-Success, and the empty reply draws exactly one EAP-Failure. It gains an assertion it did not have, failing if the cause is recorded only on the EAP-Failure round, which is the defect being fixed. Both claims carry recorded discrimination proofs. |
| TestEAPTLS13RefusedClientGetsNoSuccessIndication | Approved by Thomas in session, 2026-09-07, after reading the diff. One condition changed, `method.alertSent == nil` to `refusal.FinalRequest == nil`, reading the same absence from the field that replaced the deleted one. The RFC 9190 Section 2.5 negative claims are unchanged. |
| checkResponderEAPTLS13RevokedClient | Approved by Thomas in session, 2026-09-07, after reading the diff. Assertions 1 through 10 are untouched. It gains assertion 11, that ze reports the refusal to its own operator, and assertion 13, that the report names the certificate, the serial, the withdrawing CA and RFC 9190 Section 5.4. Strictly additive: the RFC9190-5.4-1 positive claim proves more than it did, not less. Verified in the lab on 2026-09-07 by a forced red on each new assertion independently. |
