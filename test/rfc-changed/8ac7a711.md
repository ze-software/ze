| Test | Reason |
| ------ | ------ |
| reactor_api_batch_attr_order_test | Thomas approved this on 2026-09-06, having been shown the whole diff: two call lines, and no assertion. `buildBatchAnnounceUpdate` took ten positional parameters, four of them a bare `false`, and now takes one `announceFacts` struct. `RFC4271-5-7` keeps exactly the evidence it had: the same ascending-attribute-order walk over the same emitted block, with the same quoted requirement. The `buildBatchRail` helper above it changed on the same line and for the same reason. |
| TestAnnounceStripsLocalPrefTowardExternalPeer | Same approval, same change, five call lines across `reactor_api_origin_test.go` (this test twice, plus `TestAnnounceOversizeDropsWithNamedLog`, `TestAnnounceRejectsDuplicateBaseAttribute` and `TestAnnounceBuilderModeIsEditSetOverEmptyBase`). `RFC4271-5.1.5-2` positive and `RFC4271-5.1.5-1` negative assert on the same attribute codes of the same UPDATE. One cost is worth recording rather than hiding: a positional `false /*eBGP*/` is visible in the source and an omitted struct field is not, so the second half of this test now states its eBGP premise by leaving `isIBGP` out. The assertion is unchanged and the arm still discriminates. |
| TestAnnounceRailKeepsPrefixSIDInsideTheSRDomain | Same approval, same change, one call site. `RFC8669-8-1` positive and negative both read attribute 40's presence on the emitted UPDATE, and `propagatePrefixSID` moved from the ninth positional argument to a named field of the struct, which is the one input this test varies. |
| TestHandleOpenRejectsPeerASZero | Thomas approved this on 2026-09-06 as a CORRECTION rather than a rename: the fixture gains a `configuredAS` column, so each case builds its session for the AS that case presents. It was needed because `validateOpenPeerAS` (`session_open_as.go`) enforces RFC 4271 Section 6.2 and refuses an OPEN whose My AS is not the configured remote AS. The case `as-trans with a real four-octet AS` presented 196608 to a session built for 65002, so it was refused for the MISMATCH and never reached the AS-zero rule its tag claims. `RFC7607-2-4`'s negative arm therefore demonstrated nothing, and the tag over-claimed on the public ledger. It now proves what it says: a real AS is accepted because it is not zero. This is the second row of the "what this gate cannot see" table in `test/rfc-changed.md` -- a fixture whose inputs made an arm vacuous reads as a plain data edit to a textual comparison. |
