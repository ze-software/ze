| Test | Reason |
| ------ | ------ |
| TestRFC8669LabelIndexTLVReservedAndFlagsClearOnTransmission | Thomas approved this on 2026-09-06, having read the whole of the change: `ParsePrefixSID(...)` became `attribute.ParsePrefixSID(...)` and `sid.Bytes[n]` became `sid[n]`. Nothing else moved. The producer changed package because `internal/component/bgp/config` already imports `internal/component/bgp/plugins/cmd/update`, so `update text` could not reach the Prefix-SID encoder without an import cycle; it now lives at `internal/core/bgp/attribute/prefixsid.go` and returns `[]byte` rather than a one-field wrapper. RFC8669-3.1-1 is proven exactly as before: the same three assertions on the same offsets of the same wire bytes, with the same quoted requirement. This is the first row of the "what this gate cannot see" table in `test/rfc-changed.md` -- the tagged test's producer moved in the same commit, and the textual comparison cannot follow a call across a package. |
| TestRFC8669OriginatorSRGBTLVFlagsClearOnTransmission | Same approval, same change, same reason: the call gained the `attribute.` qualifier and the slice lost its wrapper field. The Originator SRGB TLV's Flags-clear assertion, its offsets and its RFC 8669 Section 3.2 quote are untouched, so RFC8669-3.2-1 keeps exactly the evidence it had. |
| TestParsePrefixSIDSRv6_ReservedFieldsZero | Same approval, same change. `ParsePrefixSIDSRv6` moved with its sibling and answers `[]byte`, so `ps.Bytes` became `ps`. Every byte this test reads is the same byte at the same offset: the outer TLV type 5, the 28-octet length, and RFC9252-3.1-1's Reserved-must-be-zero. The move is what makes `send bgp <sel> update text bgp-prefix-sid-srv6 ( ... )` reach this encoder at all, which is the behaviour `test/exabgp-compat/api/api-ipv4.ci` and `api-ipv6.ci` assert on the wire. |
