| Test | Reason |
|------|--------|
| TestRFC4271SelfNextHopRouteIsNotInstalled | Approved by Thomas on 2026-09-12: "fixes the rib Insert signature that leaves HEAD not compiling". No claim moved. The `RFC requirement:` block above this test is byte-identical, and so is every assertion. The one edit is the call `peerRIB.Insert(fam, attr, nlri, true)` losing its trailing argument, because `4e790af234` deleted the `asn4` parameter from `PeerRIB.Insert` once every caller passed a four-octet width. That commit updated the `storage` package's own tests and not this one, so this file has not COMPILED at HEAD since. This is the opposite of a weakening: the test asserted nothing at HEAD because its package had no test binary, and it runs green again here. Tags RFC4271-5.1.3-2 positive and negative and RFC4271-6.3-2 positive are unchanged. |
| TestRFC4271SelfNextHopDoesNotShadowASoundAlternative | Approved by Thomas on 2026-09-12: "fixes the rib Insert signature that leaves HEAD not compiling". The `RFC requirement:` block and every assertion are byte-identical; the only edit is the `asn4` argument that `4e790af234` deleted from `PeerRIB.Insert` leaving this call site. The package did not compile at HEAD, so the test ran nowhere and now runs green. Tag RFC4271-5.1.3-2 negative is unchanged. |
| TestRFC4271LocRIBNextHopComesFromNextHopAttribute | Approved by Thomas on 2026-09-12: "fixes the rib Insert signature that leaves HEAD not compiling". The `RFC requirement:` block and every assertion are byte-identical; the only edit is the `asn4` argument that `4e790af234` deleted from `PeerRIB.Insert` leaving this call site. The package did not compile at HEAD, so the test ran nowhere and now runs green. Tags RFC4271-9.1.2-3 negative and RFC4271-9.1.2.1-1 negative are unchanged. |
| TestRFC4271ExternalRouteDegreeOfPreferenceFromLocalPolicy | Approved by Thomas on 2026-09-12: "fixes the rib Insert signature that leaves HEAD not compiling". The `RFC requirement:` block and every assertion are byte-identical; the only edit is the `asn4` argument that `4e790af234` deleted from `PeerRIB.Insert` leaving this call site. The package did not compile at HEAD, so the test ran nowhere and now runs green. Tag RFC4271-5.1.5-5 positive is unchanged. |
| TestRFC4271DegreeOfPreferenceNotAHardcodedConstant | Approved by Thomas on 2026-09-12: "fixes the rib Insert signature that leaves HEAD not compiling". The `RFC requirement:` block and every assertion are byte-identical; the only edit is the `asn4` argument that `4e790af234` deleted from `PeerRIB.Insert` leaving this call site. The package did not compile at HEAD, so the test ran nowhere and now runs green. Tag RFC4271-5.1.5-5 negative is unchanged. |
| TestRIBBestChangeNoPublishSameBest | Approved by Thomas on 2026-09-12: "fixes the rib Insert signature that leaves HEAD not compiling". The `RFC requirement:` block and every assertion are byte-identical; the only edit is the `asn4` argument that `4e790af234` deleted from `PeerRIB.Insert` leaving this call site. The package did not compile at HEAD, so the test ran nowhere and now runs green. Tags RFC4271-9-3 negative, RFC4271-9.2-7 negative and RFC4271-9.2-8 negative are unchanged. |
| TestRIBBestChangeWithdraw | Approved by Thomas on 2026-09-12: "fixes the rib Insert signature that leaves HEAD not compiling". The `RFC requirement:` block and every assertion are byte-identical; the only edit is the `asn4` argument that `4e790af234` deleted from `PeerRIB.Insert` leaving this call site. The package did not compile at HEAD, so the test ran nowhere and now runs green. Tags RFC4271-9-3 positive, RFC4271-9.2-7 positive and RFC4271-9.1.2-2 negative are unchanged. |
| TestLocRIBMirror | Approved by Thomas on 2026-09-12: "fixes the rib Insert signature that leaves HEAD not compiling". The `RFC requirement:` block and every assertion are byte-identical; the only edit is the `asn4` argument that `4e790af234` deleted from `PeerRIB.Insert` leaving this call site. The package did not compile at HEAD, so the test ran nowhere and now runs green. Tags RFC4271-9.1.2-2 positive, RFC4271-9.1.2-3 positive, RFC4271-9.1.2.1-1 positive and RFC4271-9.2-8 positive are unchanged. |
| TestDeleteWithCommunity | Approved by Thomas on 2026-09-12: "fixes the rib Insert signature that leaves HEAD not compiling". The `RFC requirement:` block and every assertion are byte-identical; the only edit is the `asn4` argument that `4e790af234` deleted from `PeerRIB.Insert` leaving this call site. The package did not compile at HEAD, so the test ran nowhere and now runs green. Tag RFC9494-4.2-5 positive is unchanged. |
| TestOSPFNeighborRequestReachesTheSharedKey | Approved by Thomas on 2026-09-12: "fixes the rib Insert signature that leaves HEAD not compiling", which names the `rib` break and covers the second package HEAD cannot build for the same class of reason. The `RFC requirement:` block and the single assertion are byte-identical. The one edit is `req.Canonical(nil)` becoming `req.Canonical(api.Topology{})`: `Canonical` takes a `Topology` STRUCT (`internal/component/bfd/api/session_identity.go`), so `nil` was never a legal argument and this file has never compiled. The empty struct changes no answer: this request names both the interface and the local address, so `Canonical` returns at its `r.Interface != "" && r.Local.IsValid()` guard and never reads `Topology.Links`. That guard IS what the test claims, that OSPF builds a request which is already canonical, so BGP to the same neighbor meets it rather than opening a second session. Tag RFC5882-4.4-1 positive is unchanged. |
