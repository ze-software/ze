| Test | Reason |
|------|--------|
| TestMigrationOpenCarriesResolvedASN | No owner decision sits inside this one, and none was sought: it is a call-site adaptation that carries no claim change and no weakening. `buildOpen` (`internal/component/bgp/reactor/session_negotiate.go`) now returns an error as well as the message, because `coalescePathsLimit` refuses a PATHS-LIMIT population it cannot encode (more than 51 distinct AFI/SAFI tuples, or a plugin-supplied value whose declared size is not `2 + 5n`). Every caller had to take the second value. The test's single edit is `open := session.buildOpen(...)` becoming `open, err := session.buildOpen(...)` followed by `require.NoError(t, err)`. Its RFC 7705 claim, its table and all four of its assertions (`open.MyAS`, `open.ASN4`, and the two ASN capability checks) are untouched, and the run is now strictly stronger than before: an error from `buildOpen` used to be impossible to express and would have surfaced as a nil-message panic, and it is now an explicit failure naming the test. |
