# RFC-tagged tests this commit changes

Each row records one owner approval for a change to a test that carries an
`RFC requirement:` tag. Such a test is the proof behind a public compliance
claim in `docs/features/rfc-status.md`, and `./le rfc check` counts it as
that proof. `ai/rules/testing.md` refuses every behavior change to one that the
owner did not approve. This file is the record of the approvals the owner gave.

Two native gates read it. The write hook in
`internal/le/hookruntime/writeedit.go` calls `weakened.Proposed` and refuses an
edit until this file names the test it changes. The commit gate in
`internal/le/commit/rfcchange.go` recomputes the tagged tests changed by the
prospective commit and refuses a missing or stale row.

Both use `internal/le/rfc/goscope.go` for tag carriers, function units, and
file-scope fallback, so they cannot disagree about what a row covers. A
reformat, a comment edit, and a Go import-only edit are not behavior changes. A
rename is.

The hook reads this file from disk, so the row is written BEFORE the edit. A row
written after the refusal buys nothing until the edit is made again.

A commit that changes no tagged test carries the table with no rows.

## Who writes the row

This is the one difference from `test/weakened.md`, and it decides everything
else. A weakening row is the author's own justification, and a reviewer reads it
to judge the author. A row here is the OWNER's decision, written down by the
author who asked for it.

An author cannot approve their own change. `ai/rules/testing.md` says it in one
line: a row in `test/weakened.md` does not authorize changing a tagged test,
because self-service justification is not user approval. A row here with no
answer from the owner behind it is a forgery, not a shortcut.

So the Reason column holds what the owner approved, not what the author wanted.

## The commit carries this file

`internal/le/commit/actions.go` refuses a commit that changes a tagged test and
does not name `test/rfc-changed.md` in its own `--file` list. A row that stays in
the working tree records nothing. The approval must sit in git history beside the
change it authorizes, because that is the only place a later reader can find it.

## The file is replaced per commit

Delete the rows of the last commit. Write the rows of this one. This file never
accumulates.

The reason is the mechanism it replaces, and `test/weakened.md` states it in
full: a justification explains one diff, and storing that record permanently is
what built the pile. The pile here is 255 `rfc-test-change-approved:` comments
across 120 test files. Nobody can read 255 approvals, so nobody reads them, so
writing one costs nothing.

Git history holds every past entry. `git log -p -- test/rfc-changed.md` shows the
rows of any commit beside the change they approved.

## The in-file marker is the old mechanism

`// rfc-test-change-approved: <date> <what and why>` is the record this file
replaces. It is a comment, so it stays in the test file after the diff it
explains is gone.

**No gate reads one.** The hook demanded a marker until 2026-08-19, and the
commit gate accepted one while it did, because a gate refusing it would have
refused every author for obeying the other gate. The hook reads this file now,
so both acceptances are gone: a marker approves nothing, and writing a new one
records nothing.

The sweep landed on 2026-08-19: 268 markers across 125 files, and 27 `test-relax:`
comments beside them, the mechanism `test/weakened.md` had already replaced. No
test carrier holds either token now.

The sweep is worth one paragraph, because deleting a retired token is not the
same as deleting what it said. Those 268 markers were 1475 lines of prose, and
about one block in six stated a fact about its own test that exists nowhere else:
a measured vacuity finding, a fixture precondition, a pointer to where coverage
moved. 57 of them survive as ordinary comments with the approval framing removed.
A mechanism can be retired without throwing away what people wrote under it.

## What this gate cannot see

The comparison is textual, and it is made against HEAD after comments and
whitespace are removed. Read the "what this gate saw" line in a refusal before
you write the row. The gate can be wrong, and the row is where you say so.

| The change | What the gate reports | Why |
|------------|-----------------------|-----|
| An assertion moved into a `t.Helper()` outside the tagged func | the tagged test changed | the scope cannot follow a call, which `tag_scope` records as its own known limit |
| The tagged test moved to a sibling file in the same commit | the test is gone from the old file | each path is compared against its own HEAD text, and no rename detection runs |
| The tagged test renamed | the old name is gone and nothing replaces it | a name is code, and telling a rename from a rewrite needs a Go parser |
| An assertion ADDED to a tagged test | the tagged test changed | any behavior change to the evidence needs the owner, and stronger is still different |
| A reformat, a comment edit, a Go import-only edit | nothing | the detector removes comments and whitespace before it compares |

The first three cost an author one row explaining that the gate was wrong. That
is the price of a gate that fails toward asking. `internal/le/weakened/actions.go`
reported two such false findings on 2026-08-19, one for a helper moved to a
sibling file and one for an error check extracted into a `t.Helper()` that also
added an assertion. Both are the first row of this table.

## The test name

| Carrier | The name |
|---------|----------|
| Go, inside a top-level func | the enclosing `func TestXxx` |
| Go, with a tag no top-level func encloses | the file stem |
| `.ci` or `.et` | the file stem, because each such file is one test |

`internal/le/rfc/goscope.go` resolves each one, and the same resolver serves
`test/weakened.md`. `FunctionUnits` returns top-level Go functions with their
names, while `ScopeReader` treats every non-Go carrier as one file-scoped unit.

Row two is `tag_scope`'s own fallback. A tag can sit outside every function span:
a hoisted table, or a tag separated from its func by a blank line. The gate then
treats the whole file as the unit, because a narrower answer would be a guess.
The stem is the only name available, so a change in `a_test.go` is written as
`a_test`.

A bare name is accepted when it resolves to exactly one changed test in the
commit. Write `package.TestName` when it does not, where the package is the
directory holding the file.

## The reason

Name what the owner approved, and say why the tagged requirement is still
proven after the change. A reason that does not answer the second question
approves a compliance claim losing its evidence.

Every requirement id the gate attributes to a name is covered by that name's one
row. Quote the id, so a reader can open `rfc/short/` beside it.

| Test | Reason |
|------|--------|
| llgr-egress-state-unloaded | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 4, reject= 2, cmd= 3, and it still carries the same requirement tags, RFC9494-4.3-3. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| local-pref-strip-ebgp | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 3, reject= 4, cmd= 3, and it still carries the same requirement tags, RFC4271-5.1.5-2. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| med-locally-set-reaches-peer | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 1, reject= 0, cmd= 3, and it still carries the same requirement tags, RFC4271-5.1.4-1. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| med-not-propagated-across-as | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 4, reject= 4, cmd= 2, and it still carries the same requirement tags, RFC4271-5.1.4-1, RFC7947-x-3. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| med-removal-before-decision | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 4, reject= 2, cmd= 2, and it still carries the same requirement tags, RFC4271-5.1.4-2. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| med-removal-configured | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 5, reject= 4, cmd= 2, and it still carries the same requirement tags, RFC4271-5.1.4-2, RFC4271-5.1.4-4. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| med-removal-export-refused | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 4, reject= 5, cmd= 2, and it still carries the same requirement tags, RFC4271-5.1.4-2. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| originated-nexthop-peer-own | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 3, reject= 4, cmd= 2, and it still carries the same requirement tags, RFC4271-5.1.3-1. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| rfc4271-partial-unknown-transitive | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 3, reject= 2, cmd= 2, and it still carries the same requirement tags, RFC4271-5-3. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| rfc7606-54-bgpls-override-propagates | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 2, reject= 2, cmd= 2, and it still carries the same requirement tags, RFC7606-5.4-1. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| rfc7606-54-discard-unrecognized-mup-nlri | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 2, reject= 3, cmd= 2, and it still carries the same requirement tags, RFC7606-5.4-1. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| rfc7606-54-discard-unrecognized-nlri | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 2, reject= 3, cmd= 2, and it still carries the same requirement tags, RFC7606-5.4-1. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| rfc7606-receive-combinations | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 8, reject= 3, cmd= 2, and it still carries the same requirement tags, RFC7606-5.1-3. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| rfc7606-relay-one-field | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 3, reject= 3, cmd= 2, and it still carries the same requirement tags, RFC7606-5.1-2, RFC7606-5.1-3. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| rfc7606-reset | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 2, reject= 0, cmd= 2, and it still carries the same requirement tags, RFC7606-3.a-1, RFC7606-3.j-1, RFC7606-5.3-6. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| rfc7606-withdraw | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 3, reject= 0, cmd= 2, and it still carries the same requirement tags, RFC7606-7.1-1. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| rfc9552-52-rs-opaque-withdraw-peer-down | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 5, reject= 3, cmd= 3, and it still carries the same requirement tags, RFC9552-5.2-8. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| role-otc-fwd-withdraw | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 4, reject= 3, cmd= 3, and it still carries the same requirement tags, RFC9234-5-4. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| role-otc-rs-client-dest-stamp | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 3, reject= 3, cmd= 3, and it still carries the same requirement tags, RFC9234-5-4. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| role-otc-rs-withdraw-eor | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 6, reject= 3, cmd= 3, and it still carries the same requirement tags, RFC9234-5-4. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| wellknown-no-advertise-egress | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 4, reject= 6, cmd= 4, and it still carries the same requirement tags, RFC1997-Well-2, RFC1997-Well-3. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| wellknown-no-export-egress | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 3, reject= 4, cmd= 4, and it still carries the same requirement tags, RFC1997-Well-1. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| wellknown-no-export-withdraw-egress | Thomas approved this on 2026-08-28, with the measurement in front of him. The change is the migration retiring the embedded Python plugin this carrier wrote to disk: 1 `tmpfs=` block goes, and the fixture it held is now compiled Go in `internal/test/fixture`. Nothing the carrier asserts moved. Its directive counts are identical before and after, expect= 5, reject= 4, cmd= 2, and it still carries the same requirement tags, RFC1997-Well-1. The gate is right to ask: `behaviorBytes` (`internal/le/rfc/tags.go`) compares the carrier's text with `tmpfs=` bodies dropped, and a whole block leaving changes that text whether or not an assertion did. |
| ipsec-error-notify-no-loop | Thomas approved this on 2026-08-28, with the measurement in front of him. The migration retired the embedded Python fixture this carrier fed in, so what changed sits inside a `stdin=` block: the peer script or the config, not an assertion. Every `expect=` and `reject=` line is byte-identical to HEAD, all 5 of them, the counts are unchanged (expect 2, reject 3, cmd 2), and the requirement tags are the same: RFC7296-2.21.4-1, RFC7296-2.21.4-2, RFC7296-2.21.4-4, RFC7296-2.21.4-5. This carrier surfaced only after `dropCarrierBlocks` (`internal/le/rfc/tags.go`) was keyed on `tmpfs=`; while it also dropped `stdin=` bodies the gate could not see this change at all, which is the defect that fix repaired. |
| radius-acct-wire | Thomas approved this on 2026-08-28, with the measurement in front of him. The migration retired the embedded Python fixture this carrier fed in, so what changed sits inside a `stdin=` block: the peer script or the config, not an assertion. Every `expect=` and `reject=` line is byte-identical to HEAD, all 5 of them, the counts are unchanged (expect 5, reject 0, cmd 3), and the requirement tags are the same: RFC2866-4.1-1. This carrier surfaced only after `dropCarrierBlocks` (`internal/le/rfc/tags.go`) was keyed on `tmpfs=`; while it also dropped `stdin=` bodies the gate could not see this change at all, which is the defect that fix repaired. |
| rfc2661-emitted-control-shape | Thomas approved this on 2026-08-28, with the measurement in front of him. The migration retired the embedded Python fixture this carrier fed in, so what changed sits inside a `stdin=` block: the peer script or the config, not an assertion. Every `expect=` and `reject=` line is byte-identical to HEAD, all 3 of them, the counts are unchanged (expect 3, reject 0, cmd 2), and the requirement tags are the same: RFC2661-4.1-1, RFC2661-4.1-2, RFC2661-x-1. This carrier surfaced only after `dropCarrierBlocks` (`internal/le/rfc/tags.go`) was keyed on `tmpfs=`; while it also dropped `stdin=` bodies the gate could not see this change at all, which is the defect that fix repaired. |
| rfc2661-sccrq-tunnel-id-zero | Thomas approved this on 2026-08-28, with the measurement in front of him. The migration retired the embedded Python fixture this carrier fed in, so what changed sits inside a `stdin=` block: the peer script or the config, not an assertion. Every `expect=` and `reject=` line is byte-identical to HEAD, all 4 of them, the counts are unchanged (expect 4, reject 0, cmd 2), and the requirement tags are the same: RFC2661-24.10-1. This carrier surfaced only after `dropCarrierBlocks` (`internal/le/rfc/tags.go`) was keyed on `tmpfs=`; while it also dropped `stdin=` bodies the gate could not see this change at all, which is the defect that fix repaired. |
| adj-rib-in-replay-addpath-source | Thomas approved this on 2026-08-28, with the measurement in front of him. The migration retired the embedded Python fixture this carrier fed in, so what changed sits inside a `stdin=` block: the peer script or the config, not an assertion. Every `expect=` and `reject=` line is byte-identical to HEAD, all 10 of them, the counts are unchanged (expect 7, reject 3, cmd 4), and the requirement tags are the same: RFC7911-2-2, RFC7911-3-1. This carrier surfaced only after `dropCarrierBlocks` (`internal/le/rfc/tags.go`) was keyed on `tmpfs=`; while it also dropped `stdin=` bodies the gate could not see this change at all, which is the defect that fix repaired. |
| adj-rib-in-replay-rfc2545-next-hop | Thomas approved this on 2026-08-28, with the measurement in front of him. The migration retired the embedded Python fixture this carrier fed in, so what changed sits inside a `stdin=` block: the peer script or the config, not an assertion. Every `expect=` and `reject=` line is byte-identical to HEAD, all 8 of them, the counts are unchanged (expect 5, reject 3, cmd 3), and the requirement tags are the same: RFC2545-3-1, RFC2545-3-2. This carrier surfaced only after `dropCarrierBlocks` (`internal/le/rfc/tags.go`) was keyed on `tmpfs=`; while it also dropped `stdin=` bodies the gate could not see this change at all, which is the defect that fix repaired. |
| bgp-rs-relay-aspath-transparency | Thomas approved this on 2026-08-28, with the measurement in front of him. The migration retired the embedded Python fixture this carrier fed in, so what changed sits inside a `stdin=` block: the peer script or the config, not an assertion. Every `expect=` and `reject=` line is byte-identical to HEAD, all 8 of them, the counts are unchanged (expect 4, reject 4, cmd 2), and the requirement tags are the same: RFC7947-x-1. This carrier surfaced only after `dropCarrierBlocks` (`internal/le/rfc/tags.go`) was keyed on `tmpfs=`; while it also dropped `stdin=` bodies the gate could not see this change at all, which is the defect that fix repaired. |
| community-attributes-json | Thomas approved this on 2026-08-28, with the measurement in front of him. The migration retired the embedded Python fixture this carrier fed in, so what changed sits inside a `stdin=` block: the peer script or the config, not an assertion. Every `expect=` and `reject=` line is byte-identical to HEAD, all 1 of them, the counts are unchanged (expect 1, reject 0, cmd 2), and the requirement tags are the same: RFC8955-7.3-1, RFC8955-7.5-1. This carrier surfaced only after `dropCarrierBlocks` (`internal/le/rfc/tags.go`) was keyed on `tmpfs=`; while it also dropped `stdin=` bodies the gate could not see this change at all, which is the defect that fix repaired. |
| community-blackhole-noexport | Thomas approved this on 2026-08-28, with the measurement in front of him. The migration retired the embedded Python fixture this carrier fed in, so what changed sits inside a `stdin=` block: the peer script or the config, not an assertion. Every `expect=` and `reject=` line is byte-identical to HEAD, all 8 of them, the counts are unchanged (expect 4, reject 4, cmd 2), and the requirement tags are the same: RFC7999-3.2-1. This carrier surfaced only after `dropCarrierBlocks` (`internal/le/rfc/tags.go`) was keyed on `tmpfs=`; while it also dropped `stdin=` bodies the gate could not see this change at all, which is the defect that fix repaired. |
| deadpeer-holddown | Thomas approved this on 2026-08-28, with the measurement in front of him. The migration retired the embedded Python fixture this carrier fed in, so what changed sits inside a `stdin=` block: the peer script or the config, not an assertion. Every `expect=` and `reject=` line is byte-identical to HEAD, all 6 of them, the counts are unchanged (expect 4, reject 2, cmd 2), and the requirement tags are the same: RFC4271-8.2.2-1. This carrier surfaced only after `dropCarrierBlocks` (`internal/le/rfc/tags.go`) was keyed on `tmpfs=`; while it also dropped `stdin=` bodies the gate could not see this change at all, which is the defect that fix repaired. |
| TestShutdownNotifySendsCeaseFromEveryConnectedState | Thomas approved this on 2026-08-28, with the measurement in front of him. The tagged requirement is RFC4271-8.2.2-18 and its assertions are byte-identical to HEAD; what moved in this file is vocabulary the migration renamed elsewhere in it. The gate is right to ask, because `behaviorBytes` (`internal/le/rfc/tags.go`) compares comment-stripped text and cannot tell a rename from a changed expectation. |
| audit_test | Thomas approved this on 2026-08-28. The change IS the repair: the ids this file carried (RFC9999-2-1) name no RFC. They are fixture text the scanner under test must find, and written whole they also made this test file a tag carrier, so every edit to it demanded owner approval for a requirement that does not exist. The literal is now spelled in two pieces behind `rfcTagMarker`, the idiom `internal/le/contract_test.go` already uses to stop `staleLeReferences` matching its own corpus. The fixtures' runtime bytes are unchanged, so every assertion still drives the same input. |
| TestARequirementProvenOnlyByNightlyEvidenceIsMarkedOnItsOwnRow | Thomas approved this on 2026-08-28. The change IS the repair: the ids this file carried (RFC9999-2-1) name no RFC. They are fixture text the scanner under test must find, and written whole they also made this test file a tag carrier, so every edit to it demanded owner approval for a requirement that does not exist. The literal is now spelled in two pieces behind `rfcTagMarker`, the idiom `internal/le/contract_test.go` already uses to stop `staleLeReferences` matching its own corpus. The fixtures' runtime bytes are unchanged, so every assertion still drives the same input. |
| TestAGoTagIsReadAnywhereInTheFile | Thomas approved this on 2026-08-28. The change IS the repair: the ids this file carried (RFC1-2-3, RFC4-5-6, RFC9-9-9) name no RFC. They are fixture text the scanner under test must find, and written whole they also made this test file a tag carrier, so every edit to it demanded owner approval for a requirement that does not exist. The literal is now spelled in two pieces behind `rfcTagMarker`, the idiom `internal/le/contract_test.go` already uses to stop `staleLeReferences` matching its own corpus. The fixtures' runtime bytes are unchanged, so every assertion still drives the same input. |
| TestAPythonTagIsReadOnlyWhereThePythonReaderSaysItIsAComment | Thomas approved this on 2026-08-28. The change IS the repair: the ids this file carried (RFC1-2-3, RFC4-5-6, RFC9-9-9) name no RFC. They are fixture text the scanner under test must find, and written whole they also made this test file a tag carrier, so every edit to it demanded owner approval for a requirement that does not exist. The literal is now spelled in two pieces behind `rfcTagMarker`, the idiom `internal/le/contract_test.go` already uses to stop `staleLeReferences` matching its own corpus. The fixtures' runtime bytes are unchanged, so every assertion still drives the same input. |
| TestATagWithAnUnknownPolarityIsRefused | Thomas approved this on 2026-08-28. The change IS the repair: the ids this file carried (RFC1-2-3, RFC4-5-6, RFC9-9-9) name no RFC. They are fixture text the scanner under test must find, and written whole they also made this test file a tag carrier, so every edit to it demanded owner approval for a requirement that does not exist. The literal is now spelled in two pieces behind `rfcTagMarker`, the idiom `internal/le/contract_test.go` already uses to stop `staleLeReferences` matching its own corpus. The fixtures' runtime bytes are unchanged, so every assertion still drives the same input. |
| TestATagWithoutAPolarityIsRefused | Thomas approved this on 2026-08-28. The change IS the repair: the ids this file carried (RFC1-2-3, RFC4-5-6, RFC9-9-9) name no RFC. They are fixture text the scanner under test must find, and written whole they also made this test file a tag carrier, so every edit to it demanded owner approval for a requirement that does not exist. The literal is now spelled in two pieces behind `rfcTagMarker`, the idiom `internal/le/contract_test.go` already uses to stop `staleLeReferences` matching its own corpus. The fixtures' runtime bytes are unchanged, so every assertion still drives the same input. |
| TestATerminatorBlockBodyIsNotCISyntax | Thomas approved this on 2026-08-28. The change IS the repair: the ids this file carried (RFC1-2-3, RFC4-5-6, RFC9-9-9) name no RFC. They are fixture text the scanner under test must find, and written whole they also made this test file a tag carrier, so every edit to it demanded owner approval for a requirement that does not exist. The literal is now spelled in two pieces behind `rfcTagMarker`, the idiom `internal/le/contract_test.go` already uses to stop `staleLeReferences` matching its own corpus. The fixtures' runtime bytes are unchanged, so every assertion still drives the same input. |
| TestATrailingPeriodOnATagIsPunctuationRatherThanPartOfTheWord | Thomas approved this on 2026-08-28. The change IS the repair: the ids this file carried (RFC1-2-3, RFC4-5-6, RFC9-9-9) name no RFC. They are fixture text the scanner under test must find, and written whole they also made this test file a tag carrier, so every edit to it demanded owner approval for a requirement that does not exist. The literal is now spelled in two pieces behind `rfcTagMarker`, the idiom `internal/le/contract_test.go` already uses to stop `staleLeReferences` matching its own corpus. The fixtures' runtime bytes are unchanged, so every assertion still drives the same input. |
| TestFixture | Thomas approved this on 2026-08-28. The file goes with the retired tooling tree, and the requirement ids it carried were fixtures for the tag scanner rather than proofs of any RFC: RFC9999-2-1, RFC9999-2-2, RFC9999-9-9. No obligation loses its evidence, because none was bound here. The Go port's own tag tests live in `internal/le/rfc/tags_test.go`. |
| TestRFCBothHalvesRefuseAScenarioCheckThatCannotBeRead | Thomas approved this on 2026-08-28. The file goes with the retired tooling tree, and the requirement ids it carried were fixtures for the tag scanner rather than proofs of any RFC: RFC9999-2-1, RFC9999-2-2, RFC9999-9-9. No obligation loses its evidence, because none was bound here. The Go port's own tag tests live in `internal/le/rfc/tags_test.go`. |
| TestRFCBothHalvesRefuseATagInACarrierNothingRuns | Thomas approved this on 2026-08-28. The file goes with the retired tooling tree, and the requirement ids it carried were fixtures for the tag scanner rather than proofs of any RFC: RFC9999-2-1, RFC9999-2-2, RFC9999-9-9. No obligation loses its evidence, because none was bound here. The Go port's own tag tests live in `internal/le/rfc/tags_test.go`. |
| healthFixtureTests | Thomas approved this on 2026-08-28. The file goes with the retired tooling tree, and the requirement ids it carried were fixtures for the tag scanner rather than proofs of any RFC: RFC1000-1-1. No obligation loses its evidence, because none was bound here. The Go port's own tag tests live in `internal/le/rfc/tags_test.go`. |
| TestSend | Thomas approved this on 2026-08-28. The file goes with the retired tooling tree, and the requirement ids `scripts/dev/rfc_parity_test.go` carried were fixtures for the tag scanner rather than proofs of any RFC: RFC9999-2-1, RFC9999-2-2, RFC9999-9-9. No obligation loses its evidence, because none was bound here. The Go port's own tag tests live in `internal/le/rfc/tags_test.go`. |
