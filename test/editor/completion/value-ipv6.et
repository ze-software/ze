# Test: IPv6 address completion
# VALIDATES: IPv6 peer addresses work
# PREVENTS: IPv6 configuration broken

tmpfs=test.conf:terminator=EOF_CONF
bgp {
  router-id 1.2.3.4;
  peer 2001:db8::1 {
    peer-as 65001;
  }
}
EOF_CONF

option=file:path=test.conf
option=timeout:value=10s

# Verify IPv6 peer is navigable
input=type:text=edit bgp peer 2001:db8::1
input=enter
expect=context:path=bgp.peer.2001:db8::1
expect=error:none

# Completions should work in IPv6 peer context
input=type:text=set
input=space
expect=completion:contains=peer-as,hold-time,connection
