# Test: Set command works through list paths from root
# VALIDATES: spec-editor-2 AC-1: set bgp peer <ip> hold-time <value> works from root
# VALIDATES: spec-editor-2 AC-6: set bgp peer <ip> description <value> works from root
# PREVENTS: List key being treated as leaf name instead of key value

tmpfs=test.conf:terminator=EOF_CONF
bgp {
  router-id 1.2.3.4;
  local-as 65000;
  peer 1.1.1.1 {
    peer-as 65001;
  }
}
EOF_CONF

option=file:path=test.conf
option=timeout:value=10s

# Set hold-time through list from root context
input=type:text=set bgp peer 1.1.1.1 hold-time 120
input=enter
expect=status:contains=Set
expect=dirty:true
expect=error:none

# Set description through list from root context
input=type:text=set bgp peer 1.1.1.1 description "my peer"
input=enter
expect=status:contains=Set
expect=error:none
