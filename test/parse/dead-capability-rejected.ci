# Test: dead capabilities are rejected
# multi-session, operational, aigp are not supported by ze
stdin=config:terminator=EOF_CONF
bgp {
	peer 10.0.0.3 {
		local-address 10.0.0.2
		local-as 65533
		peer-as 65533
		capability {
			aigp
		}
	}
}
EOF_CONF

cmd=foreground:seq=1:exec=ze validate -:stdin=config
expect=exit:code=1
expect=stderr:contains=unknown field in capability: aigp
