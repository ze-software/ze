# Test: valid RBAC profile config parses successfully
#
# VALIDATES: AC-1 — authorization profiles with run/edit sections parse without error.
# PREVENTS: YANG schema or config parser rejects valid authorization config.

stdin=config:terminator=EOF_CONF
bgp {
	peer peer1 {
		connection {
			remote {
				ip 127.0.0.1
			}
			local {
				ip 127.0.0.1
			}
		}
		session {
			asn {
				local 65533
				remote 65533
			}
		}
	}
}

system {
	authentication {
		user operator {
			password "$2a$10$abcdefghijklmnopqrstuuABCDEFGHIJKLMNOPQRSTUVWXYZ012"
			profile [ restricted ]
		}
		user admin {
			password "$2a$10$abcdefghijklmnopqrstuuABCDEFGHIJKLMNOPQRSTUVWXYZ012"
			profile [ admin ]
		}
	}
	authorization {
		profile admin {
			run {
				default-action allow
			}
			edit {
				default-action allow
			}
		}
		profile restricted {
			run {
				default-action deny
				entry 10 {
					action allow
					match "peer list"
				}
				entry 20 {
					action allow
					match "summary"
				}
			}
			edit {
				default-action deny
			}
		}
	}
}
EOF_CONF

cmd=foreground:seq=1:exec=ze config validate -:stdin=config
expect=exit:code=0
expect=stdout:contains=configuration valid
