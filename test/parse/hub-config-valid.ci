# Test: plugin hub config with server block is valid
# VALIDATES: hub config block is accepted by YANG schema parser
# PREVENTS: hub config being rejected as unknown key

stdin=ze-bgp:terminator=EOF_CONF
plugin {
    hub {
        server local {
            host 127.0.0.1;
            port 12700;
            secret abcdefghijklmnopqrstuvwxyz012345;
        }
    }
}
bgp {
    peer peer1 {
    	connection {
    		remote {
    			ip 127.0.0.1
    		}
    		local {
    			ip 127.0.0.1
    		}
    	}
    	session {
    		asn {
    			local 1
    			remote 1
    		}
    		router-id 1.2.3.4
    	}
    }
}
EOF_CONF

cmd=foreground:seq=1:exec=ze bgp validate -:stdin=ze-bgp
expect=exit:code=0
