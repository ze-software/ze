plugin {
	external announce-routes {
		run "ze-test interop-bgp process bgp-flowspec-gobgp announce-routes";
		encoder json;
	}
}
bgp {
	peer peer1 {
		connection {
			remote { ip 172.30.0.5; }
			local  { ip 172.30.0.2; }
		}
		session {
			asn { local 65001; remote 65002; }
			router-id 172.30.0.2;
			family {
				ipv4/unicast { prefix { maximum 10000; } }
				ipv4/flow { prefix { maximum 10000; } }
			}
		}
		attach process announce-routes {
			send [ update ];
		}
	}
}
