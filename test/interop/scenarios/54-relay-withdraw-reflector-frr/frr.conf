frr defaults traditional
hostname frr
! debugging level, not informational: check.py reads FRR's own per-UPDATE decode
! and its attribute-error verdict, and both are emitted only at debug level.
log file /tmp/frr.log debugging
!
debug bgp updates
debug bgp updates in
!
! FRR originates one prefix of its own. It is not asserted on: it is the
! injector's READINESS BARRIER. Ze reflects it to the injector, and the injector
! releases nothing until it arrives, which is how the scenario knows the FRR
! session is established and the forward rail is carrying traffic. A timer would
! only assume both. Blackhole so zebra has a matching route for `network`.
ip route 10.20.0.0/24 blackhole
!
router bgp 65000
 bgp router-id 172.30.0.3
 no bgp ebgp-requires-policy
 ! Internal session: this is the whole point of the scenario. Ze reads the
 ! destination as non-external, which is the condition on the RFC 4456 Section 8
 ! attribute injection under test.
 neighbor 172.30.0.2 remote-as 65000
 neighbor 172.30.0.2 update-source 172.30.0.3
 !
 address-family ipv4 unicast
  neighbor 172.30.0.2 activate
  network 10.20.0.0/24
 exit-address-family
!
