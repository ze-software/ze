# ExaBGP config with route-refresh
# Should inject RIB plugin for refresh response
neighbor 10.0.0.1 {
	router-id 1.1.1.1;
	local-address 10.0.0.2;
	local-as 65001;
	peer-as 65002;
	capability {
		route-refresh;
	}
}
