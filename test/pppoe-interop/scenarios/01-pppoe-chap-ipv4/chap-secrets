# accel-ppp chap-secrets: client * secret * (server and IP wildcarded; the IP
# is assigned from [ip-pool]). Matches the username/password in ze.conf.
# Columns: <username> <server> <password> <ip>
alice	*	s3cr3t	*
