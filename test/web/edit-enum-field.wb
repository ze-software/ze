# Test: environment log container shows logging-related fields
# VALIDATES: log container from YANG renders its leaf children
# PREVENTS: nested container leaves not rendering

option=timeout:value=30s

action=open:path=/show/environment/log/
expect=element:text=level
