# Test: container node shows child links in fragment view
# VALIDATES: container nodes render children as navigation links
# PREVENTS: empty container rendering, missing child entries

option=skip:reason=requires Finder mode (ze.web.ui=finder)
option=timeout:value=30s

action=open:path=/show/bgp/
expect=element:text=group
expect=element:text=peer
expect=element:text=session
