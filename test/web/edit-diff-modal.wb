# Test: BGP view shows leaf fields alongside child containers
# VALIDATES: container template renders both fields and navigation links
# PREVENTS: leaves hidden when children exist, mixed content broken

option=timeout:value=30s

action=open:path=/show/bgp/
expect=element:text=Router ID
expect=element:text=peer
expect=element:text=group
expect=element:text=local
