# Test: admin peer page loads with breadcrumb navigation
# VALIDATES: navigating to /admin/peer/ renders valid page with admin link
# PREVENTS: admin sub-path routing broken

option=timeout:value=30s

action=open:path=/admin/peer/
expect=element:text=admin
expect=element:not-text=panic
