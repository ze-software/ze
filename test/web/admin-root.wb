# Test: admin root page loads without crashing
# VALIDATES: /admin/ handler processes request and returns valid page
# PREVENTS: admin handler crash, routing error on /admin/

option=timeout:value=30s

action=open:path=/admin/
expect=element:not-text=panic
expect=element:text=CLI
