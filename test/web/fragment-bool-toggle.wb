# Test: environment daemon container shows boolean leaf fields
# VALIDATES: daemon container renders daemonize and drop fields
# PREVENTS: boolean leaves not rendering in container view

option=timeout:value=30s

action=open:path=/show/environment/daemon/
expect=element:text=BACK
