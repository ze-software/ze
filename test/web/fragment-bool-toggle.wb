# Test: boolean field renders toggle buttons in config editor
# VALIDATES: YANG boolean leaves render as tristate toggle (yes/no/default)
# PREVENTS: booleans rendered as text inputs, missing tristate template

option=timeout:value=30s

action=open:path=/config/edit/environment/daemon/
expect=element:text=daemonize
expect=element:text=drop
