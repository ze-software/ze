# Test: BGP view shows different leaf field labels
# VALIDATES: leaf nodes render with their YANG names as labels
# PREVENTS: missing field labels, broken leaf rendering in containers

option=timeout:value=30s

action=open:path=/show/bgp/
expect=element:text=Router ID
expect=element:text=Listen address
