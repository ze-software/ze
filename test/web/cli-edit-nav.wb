# Test: CLI bar is present and accepts text input
# VALIDATES: CLI text input renders and can be filled
# PREVENTS: CLI bar missing from layout, input not interactable

option=timeout:value=30s

action=open:path=/show/
expect=element:text=type a command

action=fill:text=type a command:value=show bgp
expect=element:text=type a command
