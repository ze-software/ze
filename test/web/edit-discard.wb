# Test: BGP local container shows expected leaf fields
# VALIDATES: navigating to BGP local shows AS field and back link
# PREVENTS: nested container rendering broken

option=timeout:value=30s

action=open:path=/show/bgp/local/
expect=element:text=Autonomous System Number
expect=element:text=BACK
