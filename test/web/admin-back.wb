# Test: admin page has breadcrumb for navigation
# VALIDATES: admin breadcrumb renders with clickable admin link
# PREVENTS: missing breadcrumb on admin pages

option=timeout:value=30s

action=open:path=/admin/peer/
expect=element:text=admin
