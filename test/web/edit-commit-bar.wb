# Test: page layout includes CLI bar and toolbar elements
# VALIDATES: layout chrome renders with CLI bar, theme toggle, and error button
# PREVENTS: missing toolbar elements, broken layout template

option=skip:reason=requires Finder mode (ze.web.ui=finder)
option=timeout:value=30s

action=open:path=/show/bgp/
expect=element:id=cli-input
expect=element:text=CLI
expect=element:text=Errors
