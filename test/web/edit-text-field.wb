# Test: BGP container shows text-type leaf fields
# VALIDATES: router-id and listen leaf fields render in BGP view
# PREVENTS: text leaf fields missing from container view

option=timeout:value=30s

action=open:path=/show/bgp/
expect=element:text=Router ID
expect=element:text=Listen address
