# Test: admin peer subpage loads with breadcrumb
# VALIDATES: /admin/peer/ renders with admin breadcrumb link
# PREVENTS: admin path routing broken, breadcrumb missing

option=timeout:value=30s

action=open:path=/admin/peer/
expect=element:text=admin
expect=element:text=CLI
