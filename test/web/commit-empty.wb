# Test: config commit page loads gracefully with no pending changes
# VALIDATES: GET /config/commit/ without changes does not crash
# PREVENTS: crash on empty commit, server error on no-changes state

option=timeout:value=30s

action=open:path=/config/commit
action=wait:ms=2000
expect=element:not-text=panic
