# Test: CLI bar exists on nested pages
# VALIDATES: CLI input bar renders on non-root pages
# PREVENTS: CLI bar missing on deep navigation pages

option=timeout:value=30s

action=open:path=/show/bgp/
expect=element:id=cli-input
expect=element:text=Router ID
