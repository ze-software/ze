# Test: list node renders list view with entry management
# VALIDATES: YANG list nodes render with entry sidebar and add form
# PREVENTS: list rendering as container, missing add entry form

option=timeout:value=30s

action=open:path=/show/bgp/group/
expect=element:text=add
expect=breadcrumb:contains=bgp,group
