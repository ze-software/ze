# Test: list node renders with add-entry button
# VALIDATES: YANG list nodes render with + new button for entry creation
# PREVENTS: list rendering without entry management

option=timeout:value=30s

action=open:path=/show/bgp/group/
expect=element:id=btn-add-entry
