# Test (AC-2): native builder selection maps supported architectures to one
# backend and prefers docker before QEMU.

option=timeout:value=20s


cmd=foreground:seq=1:exec=ze-test fixture install/kernel-arch-mapping-single
expect=exit:code=0
expect=stdout:contains=OK
