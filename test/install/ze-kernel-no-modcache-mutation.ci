# Test: the compiled worker uses isolated work/output directories and does not
# mutate the repository module cache.

option=timeout:value=30s

cmd=foreground:seq=1:exec=ze-test fixture install/ze-kernel-no-modcache-mutation
expect=exit:code=0
expect=stdout:contains=OK
