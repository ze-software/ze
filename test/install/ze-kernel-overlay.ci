# Test: the native runtime builder writes an isolated output tree and provenance.

option=timeout:value=40s

cmd=foreground:seq=1:exec=ze-test fixture install/ze-kernel-overlay
expect=exit:code=0
expect=stdout:contains=OK
