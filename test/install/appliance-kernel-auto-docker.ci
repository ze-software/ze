# Test: ze appliance kernel without --builder delegates to the native shared
# driver, which prefers docker when it is available. Only docker is faked so
# the docker-first auto-selection path wins.

option=timeout:value=30s


cmd=foreground:seq=1:exec=ze-test fixture install/appliance-kernel-auto-docker
expect=exit:code=0
expect=stdout:contains=OK
