# Test: the native QEMU builder accepts aarch64 as an alias for arm64 and
# continues to repository-path validation instead of rejecting the arch itself.

option=timeout:value=20s


cmd=foreground:seq=1:exec=ze-test fixture install/kernel-qemu-arch-alias
expect=exit:code=0
expect=stdout:contains=OK
