# Test: runtime native-worker behavior builds compressed kernels and modules,
# then installs modules into the requested output tree.

option=timeout:value=20s


cmd=foreground:seq=1:exec=ze-test fixture install/kernel-builder-packages
expect=exit:code=0
expect=stdout:contains=OK
