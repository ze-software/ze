# Test (AC-11): the compiled worker owns kernel source acquisition and uses the
# same native build path for runtime and installer outputs.

option=timeout:value=20s


cmd=foreground:seq=1:exec=ze-test fixture install/kernel-tarball-dedup
expect=exit:code=0
expect=stdout:contains=OK
