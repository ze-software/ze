# Test: the native resolver composes base, profile, and shared fragments into a
# stable cache identity without legacy build records.

option=timeout:value=20s

cmd=foreground:seq=1:exec=ze-test fixture install/kernel-compose "$ZE_REPO_ROOT"
expect=exit:code=0
expect=stdout:contains=OK
