# Test: ze install local -h — local subcommand help
# --systemd/--no-systemd removed: systemd is a separate install target (ze install systemd)

cmd=foreground:seq=1:exec=ze install local -h
expect=exit:code=0
expect=stdout:contains=--prefix
expect=stdout:contains=--dry-run
