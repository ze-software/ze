# Test: ze uninstall local -h — uninstall local help

cmd=foreground:seq=1:exec=ze uninstall local -h
expect=exit:code=0
expect=stdout:contains=--prefix
expect=stdout:contains=--purge
expect=stdout:contains=--dry-run
expect=stdout:contains=--yes
