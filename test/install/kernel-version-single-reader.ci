# Test (AC-15): the native builder validates the requested kernel version and
# records the same value in build provenance.

option=timeout:value=20s

cmd=foreground:seq=1:exec=ze-test fixture install/kernel-version-single-reader
expect=exit:code=0
expect=stdout:contains=OK
