# Test: the native profile resolver expands shipped base and shared fragments,
# and cache identities reflect the resolved profile inputs.

option=timeout:value=20s


cmd=foreground:seq=1:exec=ze-test fixture install/appliance-kernel-registry "$ZE_REPO_ROOT"
expect=exit:code=0
expect=stdout:contains=OK
