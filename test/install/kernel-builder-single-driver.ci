# Test (AC-1): one compiled shared driver owns docker and QEMU kernel builds and
# emits provenance for the selected backend.

option=timeout:value=20s


cmd=foreground:seq=1:exec=ze-test fixture install/kernel-builder-single-driver "$ZE_REPO_ROOT"
expect=exit:code=0
expect=stdout:contains=OK
