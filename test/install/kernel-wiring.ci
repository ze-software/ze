# Kernel builder wiring: one compiled driver owns installer and runtime backend
# selection and provenance.
option=timeout:value=20s


cmd=foreground:seq=1:exec=ze-test fixture install/kernel-wiring "$ZE_REPO_ROOT"
expect=exit:code=0
expect=stdout:contains=OK
