# Test: the shared kernel builder is compiled, shell-free, and enforces worker invariants.

option=timeout:value=20s


cmd=foreground:seq=1:exec=ze-test fixture install/kernel-builder-no-shell "$ZE_REPO_ROOT"
expect=exit:code=0
expect=stdout:contains=OK
