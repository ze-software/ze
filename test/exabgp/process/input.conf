# ExaBGP config with process
# Process should be wrapped with ze exabgp plugin bridge
process my-plugin {
	run /path/to/plugin.py;
	encoder json;
}

neighbor 10.0.0.1 {
	router-id 1.1.1.1;
	local-address 10.0.0.2;
	local-as 65001;
	peer-as 65002;
	api {
		processes [ my-plugin ];
	}
}
