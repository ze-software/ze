# ExaBGP config with graceful-restart
# Should inject RIB plugin (ZeBGP delegates RIB to plugins)
neighbor 10.0.0.1 {
	router-id 1.1.1.1
	local-address 10.0.0.2
	local-as 65001
	peer-as 65002
	capability {
		graceful-restart 120
	}
}
