# ExaBGP config with nexthop block (RFC 8950)
# Extended Next Hop encoding for IPv4 NLRI with IPv6 next-hop
# Capability is inferred from nexthop block presence
neighbor 10.0.0.1 {
	router-id 1.1.1.1
	local-address 10.0.0.2
	local-as 65001
	peer-as 65002
	nexthop {
		ipv4 unicast ipv6
	}
}
