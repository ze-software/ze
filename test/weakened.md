| Test | Reason |
|------|--------|
| TestPeerRawEntryPointRefusesAnUnattachedProcess | Renamed, not removed. It is TestPeerRawEntryPointNeedsTheRawSendWord in the same file, and the gate reports a deletion because telling a rename from a rewrite needs a Go parser. Nothing left the suite: the old test drove `peer <addr> raw` through the registered command and asserted that an UNATTACHED process is refused and reaches no socket, and both assertions survive verbatim as the first block of the new function. What changed is the name, because the old one had become a false description. Raw was gated on attachment alone, so `sendUpdateOnly(railGranted)` was the ACCEPTING case; the owner ruling of 2026-08-30 put raw behind `send [ raw ]`, so that same case is now a refusal and the test gained it as a second negative. A test named for refusing the unattached cannot carry an assertion about a process the peer does attach. Coverage rises here rather than moving: one negative and one positive became two negatives and one positive, over the same entry point. |
