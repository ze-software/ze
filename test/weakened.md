| Test | Reason |
|------|--------|
| internal/component/iface/cli/scan_linux_test.go | Deleted with the registration it covered. Both its tests called `command.ServeLocal` on `show interface scan`, a command created in this checkout that was never asked for and that 1ea3eca53 removed. Nothing in the file survives that removal: with the path unserved, `served` is false and both tests reach their `t.Fatalf`. The linux walk they belonged to went with the same commit, which took `LinuxCompletionMarker`, `RunLinux` and `Invocation.NeedsLinux` because `show interface scan` was its only member. `ze interface scan` keeps its human output, which `internal/component/iface/cli/scan_test.go` still asserts |
