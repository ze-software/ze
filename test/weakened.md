| Test | Reason |
|------|--------|
| TestAFlagThatRepeatsAPipeOperatorIsAFinding | Renamed to `TestARenderingFlagIsAFindingOnEveryCommand` and strengthened, not removed. The old name states what F3 checked when it fired only on a path already registered through `MustRegisterLocalData`, and F3 now fires on any rendering flag on any command of the `ze` surface, so the name would have described a narrower check than the code performs. Every assertion the old test made survives as the served case, and the new test adds two the old one could not make: that an unregistered command draws the same finding with a message naming the registration that fixes it, and that a flag rendering nothing draws none under either registration. `TestASessionDefaultThatLowersIntoTheOperatorIsNotAFinding` covers the one exemption beside it |
