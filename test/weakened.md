| Test | Reason |
|------|--------|
| TestTheRunEmitsOneCommandPerSuitePlusTheTwoPhases | Renamed to `TestTheRunEmitsOneCommandPerSuitePlusEveryPhase`, and strictly stronger. The name said two phases where the run now has three, so the count it asserted is raised rather than lowered: `phaseCount` is 3, and both the command count and the reported phase count are checked against it, as before. `TestAFailingSuiteIsNamedAndTheRunExitsNonZero` reads the same constant, so the two cannot drift apart. A new test beside it, `TestTheRunExecutesTheInstallerTestsUnderTheirOwnTag`, requires the new phase to carry `ze_installer` and refuses `go vet` in the VM. |
