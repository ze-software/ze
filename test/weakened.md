| Test | Reason |
|------|--------|
| TestReviewArtifactIsHashPinnedToEveryCodeFile | Assertion count 4 to 2, and nothing left the suite. The two that went are setup error checks, the `os.MkdirAll` and `os.WriteFile` that plant the fixture artifact. Both moved into `writeArtifactAt` and still `t.Fatal` there, so the same failure still stops the same test. The two real assertions are untouched: a clean artifact passes, and an edit to a covered file makes it stale. What the test stopped doing is inventing the artifact's NAME, which is the point of the change. It now plants the file where `specsession.ReviewArtifactPath` says the recorder writes it, so the fixture can no longer agree with a lookup the recorder disagrees with, which is how this shape of defect stayed invisible to both packages' suites |
