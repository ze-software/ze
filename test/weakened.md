| Test | Reason |
|------|--------|
| ifacera_test | File-scope row for the four symbols below, which left this file together. It shrank because the RFC 4861 timing arithmetic it covered moved to `internal/core/ndp`, where the PPP subscriber sender can share it instead of carrying a second copy that was missing the rate limit. Nothing this file asserted is now unasserted: every test moved with the code it drives, and each one now covers two senders rather than one. What stayed here is the metrics and socket work, which is what the file is for |
| newTestRand | Moved, not deleted. It seeds every timing test from a fixed sequence so a failure reproduces, and it moved with the three tests it serves into `internal/core/ndp/schedule_test.go`, where the arithmetic it exercises now lives. Byte-identical; `git show HEAD:internal/plugins/iface/ra/ifacera_test.go` and the new file agree on it |
| TestRAIntervalBounds | Moved to `internal/core/ndp/schedule_test.go` with the function it covers. Assertions unchanged: every drawn interval falls inside MinRtrAdvInterval and MaxRtrAdvInterval, and the first MAX_INITIAL_RTR_ADVERTISEMENTS are capped. It now covers BOTH senders, because the LAN sender and the PPP subscriber sender share that arithmetic where only the LAN one had it |
| TestRASolicitedDelayBounds | Moved to `internal/core/ndp/schedule_test.go` with the function it covers. Assertions unchanged, and it now covers both senders for the same reason |
| TestRARateLimit | Moved to `internal/core/ndp/schedule_test.go` with the function it covers. Assertions unchanged, and it now covers both senders for the same reason. This is the requirement the PPP sender was missing, which is why the arithmetic moved rather than being written twice |
| TestRAPeriodicIntervalDerivesFromTheRouterLifetime | Deleted, because what it asserted is now forbidden. It pinned a FIXED unsolicited interval of `raRouterLifetime / 3`, which was the right shape while the interval was a constant. RFC 4861 Section 6.2.4 requires that interval to be drawn afresh for every advertisement, "a uniformly distributed random value between the interface's configured MinRtrAdvInterval and MaxRtrAdvInterval", so a test demanding one fixed value would have to fail for the sender to conform. Its subject moved rather than vanished: `TestRAScheduleRandomizesTheUnsolicitedInterval` and `ndp.TestRAIntervalBounds` assert every drawn interval falls inside those bounds and that two draws differ, `TestRAScheduleCapsTheInitialAdvertisements` keeps the Section 6.2.4 cap on the first three, and `TestRAScheduleIntervalsFitTheRouterLifetime` keeps the relationship the deleted test was reaching for, that an interval stays inside the advertised lifetime. Coverage of the interval is wider afterwards, not narrower: the old test could not have failed on a sender that never varied |
