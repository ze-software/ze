| Test | Reason |
|------|--------|
| TestTheCommandPageRendererKeepsItsAbsentOnlyGuard | Deleted, and replaced in the same file by `TestNoBuildStagePublishesTheCommandFixture`, which asserts strictly more. The deleted test pinned the absent-only guard on the command-page render, and that guard was correct only while the pages had no producer: it existed because `docvalid` emits the drift checker's contract fixture, and commit `9f45348a7` published that fixture over 396 real pages, 481 bytes where a 10KB page had been. Phase 4 gives those pages a real producer, so the guard goes because something better replaced it. The old test proved ONE published page survived a build. The new one stubs the page producers out, runs a build, and proves that no build stage wrote ANY of four command surfaces: `reference/cli/index.html`, its mirror, the equivalents index, and `llms.txt`. Reverting the producer and restoring the fixture bootstrap fails it, which is the discrimination proof the phase report carries |
| assets_build_test | File-scope row for the assertion count, 13 to 12, which is arithmetic from the rewrite above and not a separate loss. The deleted test needed two setup assertions to plant a published page on disk before building over it; the replacement plants nothing, because its subject is the ABSENCE of any write rather than the survival of one file. The file's other test, `TestAnAssetEditReachesTheArtifact`, is untouched and still pins the other half of AC-18: an edit to `website/assets/css/` reaching a reader |
| TestBuildStagesADeployableArtifact | One `t.Fatal(err)` became `t.Errorf` with a message naming the value, so the fatal count falls 17 to 16. Nothing left the suite: the same condition is still asserted, and the test now reports what the published catalog actually held instead of stopping at a bare error. Downgrading a fatal to an error makes a run report MORE, not less, because the assertions after it still execute |
