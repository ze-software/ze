| Test | Reason |
|------|--------|
| TestBuildRepublishesStaleCommandSurfaces | Renamed to `TestBuildLeavesAPublishedPageAlone`, and one of its two assertions inverted. Not this session's change: it is the `yang` session's repair of `9f45348a7`, carried here because git stages whole files and its `build.go` hunks cannot be separated from the producer pass in the same file. What left the suite is the assertion that a build REWRITES `reference/cli/index.html` from the live catalog. That assertion encoded the defect as the expectation: the renderer behind it, `renderNativeCommandSurfaces`, emits the drift checker's contract fixture rather than a page, and publishing it cut 396 real pages from about 10KB to 481 bytes. The catalog half of the test is untouched and still fails when `data/cli-commands.json` names a command the live registries dropped, so no catalog coverage is given up. The page ground is not merely given up either, it is reversed on purpose: the new assertion is byte equality against a published page carrying a head and a title, which the fixture renderer cannot produce. The real page producer is restored by `plan/spec-site-renderers-in-go.md` phase 4, and until it exists no test can honestly assert that a build rewrites that page |
