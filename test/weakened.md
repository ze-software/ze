| Test | Reason |
|------|--------|
| stubLiveCommandCatalog | Renamed to `stubLiveInputs` and widened, not removed. It is a helper rather than a test, and every caller moved with it in the same edit. It stubbed ONE live input, the command catalog; a build now refreshes three from the live product before any producer runs, the catalog plus the plugin registry plus the YANG configuration tree, and each costs a compile of `cmd/ze` against a real checkout. A test over a synthetic tree that stubbed one and forgot the others would fail on a missing `feature-gates.txt` rather than on the thing it set out to check, so the helper states all three together. Nothing left the suite: every test that called the old name calls the new one, with the same catalog argument and stricter setup |
