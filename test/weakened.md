| Test | Reason |
|------|--------|
| TestConfigShowJSON | Deleted with the flag it covered. It drove `ze config show --json` and asserted the JSON encoding that command wrote itself. Rendering is the pipe layer's job (`ai/rules/cli.md`, "`--flag` or Keyword"), so the flag and its encoder are gone and there is no JSON form of this command left to assert over. What the test actually defended, that the subtree reaches a reader unmasked of nothing and masked of every secret, is unchanged and still asserted by `TestConfigShowMasksASecret` over the text form |
| cmd_show_test | Three assertions removed, twenty-five kept. Two were the `data whole tree` and `data at path` cases of `TestConfigShowMasksASecret`, which ran the masking check against `dataTree`; the third was the `dataTree` half of `TestConfigShowUnparsableConfig`. `dataTree` is deleted in this commit because `show config tree` answered the same parsed configuration as `show config dump`, so registering it created duplication rather than removing it. Both properties survive over the form that still exists: the secret masking is asserted on the text form whole-tree and at a path, and the unparsable case still requires the text form to print the broken file at the root and to refuse at a path |
