| Test | Reason |
|------|--------|
| TestActionsCarryTheSevenActionContracts | Renamed in place, not removed. It is `TestActionsCarryTheEightActionContracts` in `internal/le/terminaldemo/terminaldemo_test.go`, and the gate reports a deletion because telling a rename from a deletion needs a Go parser. The count is in the name because the test pins the whole action table, so an added action must change it: `image-build` builds the renderer container, which the make retirement dropped without replacing. Every row the old function asserted is still asserted, in the same order, with its verb, its writes flag and its reason text; the new row sits between `release-check-all` and `binaries-build-ze`. The keyword-grammar half also gained `image-build` to the list of verbs that must take no value. |
| TestInstallDockerHostSelection | Moved to another package, not removed. Its subject, `qemu.InstallDockerHost`, had no non-test caller: it was written for the appliance build and never wired. The behavior now lives in `internal/le/dockerhost.Environment`, and all three of the old assertions are re-asserted in `TestEnvironmentSelection` (`internal/le/dockerhost/dockerhost_test.go`): the darwin selection, an existing DOCKER_HOST left untouched, and linux unchanged. That file adds three cases the old test had no place for: an absent socket, an empty `DOCKER_HOST=` read as unset, and a duplicated variable read at its last occurrence. `internal/le/qemu/install_test.go` keeps its own half as `TestApplianceEnvNamesTheDockerSocket`, which drives the selection through `applianceEnv` and fails when the wiring is removed. |
