| Test | Reason |
|------|--------|
| cli-completion-ghost-json | Renamed to `cli-completion-ghost.ci`, not removed. It drove `ze config completion --json --ghost` and asserted the ghost-completion payload. The rendering flag is deleted, so the scenario drives `ze config completion --ghost` and asserts the same payload from the text form. Its four expectations moved with it. |
| cli-completion-json-output | Three expectations removed, four kept. They asserted the JSON encoding `ze config completion --json` wrote. That flag and the `show config completion` registration behind it are both deleted here, so no JSON form of this command survives to assert over. The completion candidates themselves are still asserted. |
| config-fix-plan-json | One expectation removed, four kept. It asserted the JSON `ze config fix --json` encoded. The flag and the `show config fix` registration are deleted, so the scenario now drives `ze config fix --plan` and asserts the plan the operator reads. |
| pipe-local-command | Four expectations removed, twenty-one kept. Each was a `COVERED:` marker for one of the local-data commands this commit deletes, plus the `OK: N/N` total that counts them. The markers for every surviving registration are unchanged. |
| tacacs-show | One expectation removed, four kept. It asserted `ze cli -c "show tacacs servers ... json"` rendered the probe through the pipe layer. That registration is deleted. The scenario still drives `ze tacacs show` and asserts its rows, with the column order the renderer now produces. |
| cli.main_test | In `internal/component/config/schema/cli/main_test.go`: 101 assertions to 77. The removed ones drove `ze schema list`, `show`, `handlers`, `methods` and `events` with a rendering flag and asserted the JSON each command encoded itself. Those flags are deleted and the surviving registrations render the same payloads through the json operator, which `pipe-local-command.ci` asserts. |
| cli.TestTheDeclaredColumnsAreTheRowFields | In `internal/component/iface/cli/scan_test.go`: it held the `show interface scan` column declaration against the fields its rows carried. Both the declaration and the answer go with the registration. |
| cli.TestAnUnsupportableOperatorIsRefusedByName | In `internal/component/iface/cli/scan_test.go`: it drove a row operator against the `show interface scan` answer and required the refusal to name it. The registration is deleted. |
| rowsOf | Deleted with its only callers. A helper in `internal/component/tacacs/cli/main_test.go` that decoded the `show tacacs servers` payload into rows so the column and shape tests could read it. Both of those tests go with the registration. |
| scan_test | Seventeen assertions to one, in `internal/component/iface/cli/scan_test.go`. The removed ones covered the `show interface scan` data answer. The surviving assertion covers `filterManaged`, which `ze interface scan` still uses. |
| schema_data_test | Twenty-nine assertions to fourteen. The removed ones are the four `show schema module` tests. The tests of the surviving schema answers keep every assertion. |
| TestCmdListJSON | Deleted with the flag it covered. It drove `ze schema list --json` and asserted the JSON that command encoded itself. `show schema list` renders the same payload through the json operator, which `pipe-local-command.ci` asserts. |
| TestCmdShowJSON | Deleted with the flag it covered. `ze schema show --json` no longer exists, and the `show schema module` registration written for it is removed here. |
| TestCmdHandlersJSON | Deleted with the flag it covered, as `TestCmdListJSON`. `show schema handlers` renders the same payload through the json operator. |
| TestCmdMethodsJSON | Deleted with the flag it covered, as `TestCmdListJSON`. `show schema methods` renders the same payload through the json operator. |
| TestCmdEventsJSON | Deleted with the flag it covered, as `TestCmdListJSON`. `show schema events` renders the same payload through the json operator. |
| TestShowSchemaModuleAnswersOneModule | Deleted with the registration it covered. It pinned the document `show schema module` answered. That command was created in this checkout, was never asked for, and is removed here. |
| TestShowSchemaModuleRendersAsJSON | Deleted with the registration it covered, as above. |
| TestShowSchemaModuleRefusesRowOperatorsByName | Deleted with the registration it covered. The refusal property is still asserted over `show schema protocol`, which declares the same one-document shape and survives. |
| TestShowSchemaModuleRefusesAnythingButOneModuleName | Deleted with the registration it covered. The argument checking it asserted belonged to the deleted handler. |
| TestTheScanAnswerIsServedInThisProcess | Deleted with the registration it covered. `ze interface scan` keeps its text output and has no data answer to serve. |
| TestProbeAnswersStructuredRowsRatherThanText | Deleted with the registration it covered. It proved the `show tacacs servers` payload was structured data rather than rendered text. |
| TestOneAnswerRendersAsJSONAndAsYAML | Deleted with the registration it covered. It proved one `show tacacs servers` payload fed both the json and the yaml operator, which was the argument for registering it. |
| TestTheDefaultRenderingCarriesTheDeclaredColumnOrder | Deleted with the declaration it covered. `show tacacs servers` declared its column order through `RegisterColumns` and that call is removed, so `ze tacacs show` renders columns alphabetically. |
