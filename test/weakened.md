| Test | Reason |
|------|--------|
| le.repoRoot | Replaced by `personalityRepoRoot`; `TestNormalZeLinksNoInternalLe` and `TestStandaloneLeAndZeLeHaveIdenticalSurface` use it to locate the checkout they build and inspect. |
| TestAGoTargetRunsInThisProcessAndAMakeTargetDoesNot | Two changes, one row. First, one Make-only refusal was downgraded from fatal to nonfatal so the test can report it with the native callback checks. Second, the package renamed its Make-target vocabulary to native-action vocabulary (`gate` to `checker`, `runTarget` to `runAction`, `TargetOrder` to `actionOrderList`, `goTargets` to `goActions`), and the undeclared-target case merged its separate `Failed` and `Code` checks into one compound condition reported through a single message that still names both fields. The all-native router still requires failure, code 2, and the exact no-native-callback message, and the same failure conditions are still caught. |
| scripts/** | The retired tooling tree. Every development producer that lived here is now compiled Go under `internal/le/`, and each Python module's tests went with the module they tested. `TestNoPythonLeRemains` (`internal/le/contract_test.go`) refuses the tree's return, and each `internal/le/` package carries its own Go tests. |
| demos/terminal/render_test.py | Python tests for the demo renderer, retired with the Python renderer. `internal/le/terminaldemo` carries the renderer and `terminaldemo_test.go` its tests. |
| demos/terminal/test_render.py | Second Python test module for the same retired renderer. Covered by `internal/le/terminaldemo/terminaldemo_test.go`. |
| internal/component/command/registry/local_data_runtime_evidence_test.go | Retired with its subject: its five cases judged a Python runtime-evidence contract, including `TestRuntimeEvidencePythonContractRejectsSpoofs`. `local_data_functional_coverage_test.go` in the same package now covers local-data registration from the live registry with no Python process. |
| internal/le/census_test.go | Retired with its subject. It judged the parity census, which counted ported against unported gates. The final tree declares no census, which AC-9 of spec-le-is-a-ze-binary requires, so there is no count left to check. |
| internal/le/evidence/docker_run_test.go | Retired with the Docker evidence lifecycle it called. `internal/le/evidence/evidence_test.go` covers the release-candidate evidence command that remains. |
| internal/le/functional/dockerexec_test.go | Retired with the Docker-exec analyzer it drove. NOT REPLACED: no file under `internal/le/functional/` names that analyzer and no test in the package covers its fixpoint derivation or its anti-vacuity floor. The behaviour is unproven and the area's own listing still advertises it. |
| internal/le/leaction/forks_test.go | Retired with its subject. It pinned which gates were forked to a script and what the listing showed for one. No action forks a script now, so the derivation it kept honest no longer exists. |
| internal/le/parity/parity_test.go | Retired with the parity census package. See the `internal/le/census_test.go` row. |
| internal/le/pyfmt/pyfmt_test.go | Retired with the Python formatter it wrapped. No Python source remains to format. |
| internal/le/pylint/pylint_test.go | Retired with the Python linter it wrapped. No Python source remains to lint. |
| internal/le/rfc/audit_python_test.go | Retired with its subject: it compared the Go RFC audit against the Python producer that no longer exists. `internal/le/rfc/audit_test.go` covers the Go audit on its own terms. |
| internal/le/rfc/constants_python_test.go | Retired with its subject. It read the Python module's constants and set them against the Go package's, which is impossible with the module deleted. |
| internal/le/rfc/pyfmt_test.go | Retired with the Python formatting helper it checked. |
| internal/le/rules/constants_python_test.go | Retired with its subject, as the `internal/le/rfc/constants_python_test.go` row. |
| internal/le/rules/digest_constants_python_test.go | Retired with its subject: the Python digest constants it compared against are gone with the Python producer. |
| internal/le/ste/constants_python_test.go | Retired with its subject, as the `internal/le/rfc/constants_python_test.go` row. |
| internal/le/testhealth/constants_python_test.go | Retired with its subject, as the `internal/le/rfc/constants_python_test.go` row. |
| test/interop-ipsec/lab_test.py | Python test of the Python IPsec lab driver, retired with it. The lab is now `internal/le/interoplab`, covered by `interoplab_test.go`. |
| test/interop/native_parity_test.go | Retired with its subject: it compared the native interop runner against the Python runner, and the Python runner is deleted, so the comparison has one side. `internal/le/interoplab/interoplab_test.go` covers the native runner on its own terms. |
| test/interop/scenario55_check_test.go | Retired with its subject: it guarded the failure-path reading of `scenarios/bgp-wire-edit-api-origin-bird/check.py`, and that file no longer exists in the scenario directory. |
| test/interop/speaker/test_engine.py | Python test of the Python speaker engine, retired with it. |
| test/perf/run_test.py | Python test of the Python perf runner, retired with it. `internal/le/perfbench` carries the Go replacement. |
| test/scripts/ze_api_test.py | Python test of the Python plugin API helper, retired with the embedded Python fixtures that imported it. Fixtures are compiled Go in `internal/test/fixture` and are covered by that package's tests. |
| test/ui/le-dev-python-answers.ci | Retired with its subject: it drove the Python `le`, which no longer exists. |
| test/ui/le-evidence-docker-answers.ci | Retired with its subject: it drove the Python evidence Docker path, replaced by `internal/le/evidence`. |
| website/tools/test_build_boundary.py | Python test of a Python website tool, retired with it. `internal/le/sitebuild` carries the Go replacement and its tests. |
| website/tools/test_render_blog.py | Python test of a Python website tool, retired with it. Covered by `internal/le/sitebuild`. |
| website/tools/test_render_cli_catalog.py | Python test of a Python website tool, retired with it. The catalog is now `internal/le/wikicatalog`, covered by its own tests. |
| website/tools/test_render_command_equivalents.py | Retired with its subject twice over: the Python renderer is gone, and the page it rendered listed Make target equivalents for a Makefile that no longer exists. |
| website/tools/test_render_demos.py | Python test of a Python website tool, retired with it. Covered by `internal/le/sitebuild` and `internal/le/terminaldemo`. |
| website/tools/test_render_doc.py | Python test of a Python website tool, retired with it. Covered by `internal/le/sitebuild`. |
| website/tools/test_render_llms.py | Python test of a Python website tool, retired with it. Covered by `internal/le/sitebuild`. |
| website/tools/test_site_assets.py | Python test of a Python website tool, retired with it. `internal/le/webassets` covers the per-page asset sets and `internal/le/sitebuild` the site build. |
| TestInteropRunnerFailsClosedWithoutDocker | Kept its name and rewritten natively: it now calls `interopbgp.RunAt` in-process instead of shelling out to the deleted `run.py`. Two subprocess-only assertions (the python3 lookup skip and the `exec.ExitError` type check) dropped because there is no subprocess left that can fail to start. |
| aScenarioName | Retired with its subject. It picked a scenario directory for the Python probe harness at `test/interop/testdata/runner_probe.py`, which is deleted; native tests select a fixed scenario name directly. |
| pythonOrSkip | Retired with its subject. It resolved `python3` on PATH for the deleted Python interop harness; native tests call Go functions directly and need no interpreter. |
| probeEnv | Retired with its subject. It built a stripped environment so a shelled-out probe could not delete a concurrent run's containers; native tests fake Docker calls in-process via `recordingRunner` and run no real subprocess to sandbox. |
| runProbe | Retired with its subject. It drove `test/interop/testdata/runner_probe.py` in one of its modes; that script and its directory are deleted. |
| assertSummaryPrinted | Retired with its subject. It checked the deleted `run.py`'s printed pass/fail summary line; the native runner reports through a typed `Report` struct instead of parsed stdout. |
| TestInteropRunnerReportsWhenInterruptedMidNote | Retired with its subject. It guarded a Python-specific hazard: a Ctrl-C during `run.py`'s post-failure `docker logs` read discarding the whole run's summary. The native runner has no separate signal-handled failure-note step; every Docker call runs under one `context.Context`, so there is no equivalent interrupt window to test. |
| TestInteropRunnerReportsWhenTeardownFails | Folded into `TestSuiteCleansUpAfterSetupFailure`, whose `report.Scenarios[0].CleanupErrors` assertion still proves a teardown removal failure is recorded rather than swallowed, and whose command-order check still proves both container and network cleanup still run after a failure. |
| TestInteropRunnerDeniesWhenThePreCleanFails | Folded into `TestRemoveContainerAcceptsOnlyExactAbsence`, whose "removal already in progress" case still proves a real pre-clean failure denies the scenario rather than letting it proceed. |
| TestInteropRunnerPreCleanExemptsOnlyTheAbsentNetwork | Folded into `TestRemoveContainerAcceptsOnlyExactAbsence`, whose exact-container-name-absent case versus different-container-name case still proves only the exact absence answer is exempted, never a same-shaped "not found" for something else. |
| TestInteropRunnerPinsTheImageItBuilt | Folded into `TestDockerBuildNetworkAndContainerArguments` and `TestDockerBuildRejectsEmptyImageID`, which together still prove `Docker.Build` returns only the immutable `sha256:` image ID, never a mutable tag, and that the returned reference is exactly what a container's `docker run` argv carries. |
| TestInteropRunnerReusesTheSharedTagWithoutABuild | Covered by `TestSuiteRunsLifecycleAndCheckerInOrder`, whose `NoBuild: true` scenario still proves the suite runs containers straight from the declared image string with no `docker build` in the recorded command order. |
| TestASelectorFailureWidensToEveryPackage | Retired with its subject. It simulated a shelled-out selector script failing via a `Run` recorder callback; `Scope.Resolve` no longer shells out at all, so there is no external selector process left that can fail to run. Native failure-to-widen paths (an unreadable changed file, a moved module dependency) are covered directly in `internal/le/changed/selector_test.go`, for example `TestSelectorWidensWhenTheModuleGraphMoves`. |
| TestAnEmptySelectorAnswerIsAnAnswerRatherThanAWidening | Covered by `TestSelectorMapsTheExternalPluginExample` in `internal/le/changed/selector_test.go`, which still proves an empty package answer exits 0 with `Widened` false rather than falling back to `./...`. |
| TestTheScopeReportRendersOnePackagePerLine | Folded into `TestTheScopeReportPreservesEveryPrintMode`, which keeps the exact one-package-per-line default rendering assertion and extends it to the tags and both print modes. |
| TestTheSelectorIsNamedByAnAbsolutePathOfTheCheckout | Retired with its subject. It checked that `Resolve` named an external selector script by absolute path on the command line it built; the selector is now native Go code inside `internal/le/changed/selector.go` with no script path to name. |
| selectorCall | Retired with its subject. It built the argv string the deleted `Run` recorder matched against; the native selector calls Go functions directly rather than building a command line. |
| scope_test | The whole-file assertion count fell from 18 to 15, echoing the same consolidation described above rather than a distinct loss: three scenario functions and the `selectorCall` helper were deleted along with the shell-out path, while the surviving functions gained their own `code != 0` checks against the new native `Resolve` signature. |
| TestParityMapsEveryProducerGoldenRow | Renamed to `TestParityMapsEveryTypedDispatcherRow`, which keeps the same population and per-fixture pass assertions and adds a duplicate-identity check and an exact per-exit-code population count. |
| TestEveryBehavioralCategoryFailsWhenItsProducerMappingIsOmitted | The scenario it built (a synthetic Python mapping string, then a category omitted from it) is no longer constructible: categories are typed Go values, not parsed text. The same fail-closed guarantee is proven natively by `TestFixtureCatalogMutationsFailClosed`'s "missing category" subtest, and the static invariant that every category currently has a mapping is `TestEveryBehavioralCategoryHasTypedProducerMapping`. |
| TestFixtureProducerMapsEveryCategoryAndCheckCallsite | Renamed to `TestFixtureCatalogExactPopulationAndContent`, which keeps the same population-completeness invariant and checks far more of the catalog's content: exact exit-code counts, message-kind counts, and duplicate fixture identities. |
| TestRunFailsClosedWhenProducerPopulationIsMissing | Renamed to `TestRunFailsClosedWhenHookPopulationIsMissing`, which still requires exit code 2 when required source is missing, now naming the `hook-source-drift` result rather than the deleted golden producer file. |
| TestActionIsTheGatelessUnitVerb | Renamed and expanded to `TestActionOwnsUnitAndEveryRuntimeHook`, which still checks the `unit` verb is registered with no gate and no tree writes, and now checks the other 20 hook verbs `hookcheck` absorbed from the ported hook scripts. |
| TestGoldenParserRejectsAnUnterminatedPopulation | Retired with its subject. `parseGolden` and the Python-sourced golden table text it parsed are both deleted; the golden tables (`BASH_GOLDEN` and siblings) are now Go struct literals in `internal/le/hookcheck/parity_catalog.go`, compiled rather than parsed, so there is no parse failure left to trigger. |
| TestEveryGateDeclaresOneRunnerAndAReason | Renamed to `TestEveryActionDeclaresOneRunnerAndAReason` in the repo-wide Gate to Action rename; it still requires exactly one of a native callback or an external command, plus a reason, per row. |
| TestFormerScriptGatesUseNativeCallbacks | Renamed to `TestNativeActionsUseNativeCallbacks`, which still requires `interop-ipsec`, `interop`, and `stress-bird` to resolve to a native callback with no external argv, and adds `StressAction` to the checked verbs. |
| TestEveryVerbIsTypeableAndUnique | Kept its name. Its two separate presence checks (`iface-test`, `ze-interop-test`) became one loop over four required verbs (`iface`, `interop`, `StressAction`, `stress-bird`), so the lexical assertion count fell even though the loop checks more verbs than the two calls it replaced. |
| TestAMistypedGateIsRefusedWithTwo | Renamed to `TestAMistypedActionIsRefusedWithTwo`, which still requires `Answer` to return code 2 for an unknown verb, now spelled `no-such-action`. |
| gateNamed | Renamed to `actionNamed`, which still looks up one row of `Table()` by verb or fails the test; its return type changed from `Gate` to `Action` with the same table rename. |
| TestVerbIsDerivedFromTheGateName | Renamed to `TestActionsUseDeclaredVerbs`, which still requires the two fixture actions to answer verbs `check` and `write`. It drops the `rows[1].Gate` check because `Action` no longer carries a `Gate` field: there is no Make target left for a verb to derive from or report. |
| TestGatesAnswersTheTargetsTheTableNames | Retired with its subject. `Area.Gates()`, the method that answered the Make target of each action for parity, is deleted along with `leaction.go`'s `Gate` field; there is no Make target left to claim parity with. |
| TestRefusalsAnswerDifferentCodes | Renamed to `TestRefusalsAnswerUsageCode`. Both refusal shapes now share one usage-error code: the extra-argument case changed from exit 1 to exit 2, matching the new name, and the test still asserts an exact code for each shape plus zero actions run. |
| leaction_test | The whole-file assertion count fell from 39 to 36. `TestVerbIsDerivedFromTheGateName` and `TestGatesAnswersTheTargetsTheTableNames` no longer exist under those exact names, so the detector counts their entire old bodies as deleted functions rather than as a same-named assertion decrease; the residual is that old content's now-unattributed assertion loss, already accounted for by the two rows above. |
| TestChaosGatesKeepExactCommandsAndEnvironments | Renamed to `TestChaosActionsKeepExactCommandsAndEnvironments` in the repo-wide Gate to Action rename; it still pins the exact command argv and environment overrides per row. |
| TestUnitTargetRunsSimulatorThenCLI | Renamed to `TestUnitActionsRunSimulatorThenCLI`, which still requires the simulator to run before the CLI unit action. |
| TestNamedToolFailureKeepsItsReportAndCode | Kept its name and its assertions; `gaterun.GateReport`/`Gate` became `gaterun.ActionReport`/`Action` with the table rename, and two pairs of separate `t.Fatalf`/`t.Errorf` calls were combined with a logical OR into one call each, which is why the lexical assertion count fell from 10 to 8 while the same conditions are still checked.  |
| TestAreaMetadataClaimsExactlyThreeChecks | Renamed to `TestAreaMetadataDeclaresExactlyThreeActions`, which still pins the public command surface and write flags the listing exposes. |
| privilegedIPLines | Retired with its subject. It regex-scanned embedded Python (`ip(...)`) and shell (`ip link add`) text inside `.ci` files; those setup scripts are gone. Native detection walks the compiled Go fixture's AST instead, in `collectFixtureRegistrations` and `inspectFixtureExpression`. |
| undeclaredPrivilegedIP | Retired with its subject. It matched `privilegedIPLines`' text-scan corpus against `declaresNetAdmin`; its logic now lives inline in `TestNativeIPRouteFixturesDeclareNetAdmin`, matching `privilegedFixtureNames`'s AST-derived population against each name's `.ci` callers from `nativeFixtureCallers`. |
| TestCIPrivilegedIPDeclaresNetAdmin | Renamed to `TestNativeIPRouteFixturesDeclareNetAdmin`, which still fails when a privileged iproute2 mutation has no `.ci` caller declaring `option=needs-linux:caps=net-admin`, now sourced from compiled fixtures instead of embedded Python. |
| TestPrivilegedIPDetectorDiscriminates | Renamed to `TestNativeIPRouteDetectorDiscriminates`, which still proves the detector recognizes a real privileged mutation, now by parsing a small Go source fixture through `inspectFixtureExpression` rather than matching a regex against a text line. |
| TestCapabilities | Retired with its subject. `Manager.AddCapability` and the manager-wide `Manager.Capabilities()` collector are deleted as dead code in the same diff; nothing in production called either method, and `Process.Capabilities()` (a different, protobuf-backed per-process API) already covers plugin capability declarations. |
| manager_test | The whole-file assertion count fell from 35 to 32, exactly the three assertions `TestCapabilities` carried. The detector counts that function's removal as a deleted-function finding rather than a same-named assertion decrease, so its loss surfaces here instead. |
| TestDiagnosticActionsAreGatelessKeywordCommands | Renamed to `TestDiagnosticActionsAreListedKeywordCommands`, which still requires both diagnostic verbs to be listed. It drops the `row.Gate != "" a logical OR len(row.Forks) != 0` check because `leaction.Action` no longer carries `Gate` or `Forks` fields; there is no Make-target parity left to assert none of.  |
| l2tpdiag_test | The whole-file assertion count fell from 14 to 13, exactly the one dropped `Gate`/`Forks` check above. Because the renamed function no longer matches its old name, the detector counts that loss as a deleted-function finding rather than a same-named decrease, so it surfaces here instead. |
| TestActionsClaimBothReadOnlyGates | Renamed to `TestActionsDeclareBothReadOnlyPlatforms`, which still requires exactly two read-only actions with a reason and no tree writes. It drops the `Gates()`/Make-target-name check and the `Forks` check because both are retired, and adds an exact-verb check (`darwin`, `freebsd`) in their place. |
| platformvet_test | The whole-file assertion count fell from 39 to 38, the net of the renamed function's dropped and added checks. Because the old name no longer exists in the new file, the detector attributes that function's loss to a deleted-function finding rather than a same-named decrease, so the net surfaces here instead. |
| TestTheActionTableClaimsTheThreeGatesAndNamesTheTwoThatWrite | Renamed to `TestTheActionTableDeclaresThreeNativeActionsAndTwoWrites`, which still requires exactly three actions with two of them writing and the same help hint. It drops the `Gates()`/Make-target-name check and the `Forks` check (both retired) and adds an exact-verb check (`check`, `update`, `record`) in their place. |
| testhealth_test | The whole-file assertion count fell from 95 to 94, the net of the renamed function's dropped and added checks. Because the old name no longer exists in the new file, the detector attributes that function's loss to a deleted-function finding rather than a same-named decrease, so the net surfaces here instead. |
| TestWeakenedAreaClaimsExactNativeGates | Renamed to `TestWeakenedAreaPublishesNativeActions`, which still requires the area's exact verb list and that none of them write. It drops the `Gates()`/`row.Gate`/`Forks` checks (all retired) and the exact `Why` wording, checking only that `Why` is non-empty, and adds the new `proposed` verb the area gained. |
| weakened.parity_test | The whole-file assertion count fell from 33 to 32, the net of the renamed function's dropped and loosened checks. Because the old name no longer exists in the new file, the detector attributes that function's loss to a deleted-function finding rather than a same-named decrease, so the net surfaces here instead. |
| TestNativeScenarioNameParity | Renamed to `TestNativeScenarioPopulationIsExact`, which still requires the scenario directory names to match the reviewed list in lexical order, and adds a check that `l2tp.ScenarioNames()` names the same population. |
| TestProducerConfigByteParity | Renamed to `TestNativeConfigBytesArePinned`, which still sha256-pins every config, secret, and Dockerfile byte for byte. It drops the `run.py`, `lab.py`, `check.py`, and `radius-mock.py` entries because those Python producers are deleted; `TestL2TPTreeIsGoOnly` now asserts no `.py`/`.pyc`/`.sh` file remains in the tree at all. |
| TestProseInADemoScriptIsNotACallSite | Renamed to `TestProseInADemoDefinitionIsNotACallSite`, which still requires narrated prose to leave `result.DemoLaunch` empty; the fixture format changed from a shell `run.sh` demo script to a `demo.tape` recording, matching the demo tooling this migration replaced it with. |
| featuretags.TestBothReportsRenderTheScriptWording | Renamed to `TestBothReportsRenderNativeWording`, which still pins the exact text of all four report shapes; only the stale-file remediation hint changed from `run make generate` to `run ./le feature-tags write`. |
| TestCheckerPopulationMatchesProducer | Kept its name and still requires every scenario directory to have a typed checker. It drops the `check.py`-existence filter (every scenario now has a native checker, so the filter is a no-op) and the `specialAuditOperations[name]` mapping check; that hand-maintained map is deleted, and the same "every bespoke checker is accounted for" property is now proven more thoroughly by the new `TestRFCInteropCheckerBindings`, which exercises each bound checker against a recording lab. |
| TestEveryKnobHasTheDefaultTheMakeFragmentDeclares | Renamed to `TestEveryKnobHasTheNativeDefault`, which still pins the same default paths, parallelism, timeout, skip list, and binaries; the Makefile-fragment framing is dropped because the defaults are now native Go constants (`newAllTests`, from the former exported `NewAllTests`). |
| TestTheAreaHoldsTheTwoGates | Renamed to `TestTheAreaPublishesNativeActions`, which drops the retired `actions.Gates()` check and still requires every action's exact verb and write flag, now over four actions (`check`, `tree-check`, `generate`, `generated-check`) instead of the original two. |
| render_test | Not a weakening. The RFC9999 test's own assertion (a `**nightly-only**` marker in the rendered shard) is unchanged; the detector's RFC-tag check fired because the fixture's carrier path moved from `test/interop/scenarios/one/check.py` to `internal/le/interoplab/bgp/scenario_test.go`, and several unrelated symbol and CLI-command renames (`make ze-interop-test` to `./le integration interop`, `RenderExtractionTable` to `renderExtractionTable`, and others) touch other lines of the same file. |
| TestTheTagUniverseComesFromTheMakeFiles | Renamed to `TestTheTagUniverseComesFromTheNativeFeatureManifest`, which still requires the tag universe to hold at least two tags including `ze_core` and to error on a tree with none; `TagUniverse` became unexported `tagUniverse`, and its source moved from parsing Makefiles to loading the native feature manifest. |
| TestTheListingSaysWhichActionWrites | Kept its name and its write-flag and reason assertions unchanged; it drops the `strings.HasPrefix(row.Gate, "ze-")` check because `leaction.Action` no longer carries a `Gate` field, the same retirement seen across the other action-table tests in this migration. |
| ze.Capabilities | Retired with its subject. `ze.Capability` and `PluginManager.Capabilities()` are deleted (the same dead manager-wide capability collector removed in `internal/component/plugin/manager/manager_test.go`); `mockPluginManager.Capabilities()` and the `_ = ze.Capability{}` compile-check both dropped because the interface method and the type no longer exist. |
| verify-scope-freshness-scoped | Renamed to a compiled fixture. The multi-stage shell orchestration (`setup.sh`, `assert-scope.sh`, `assert-moved.sh`, and the rest) is now `misc_fixture_runner_scope.go`'s registered fixture, invoked by one `cmd=` line, and its eight `stdout:contains` checks still cover the FRESH-for-mine, STALE-for-theirs, STALE-after-my-edit, moved-path, and red-run cases the shell version asserted. |
| le-rfc-answers | Renamed to a compiled fixture. The embedded Python comparison against the deleted `scripts/dev/rfc_requirements.py` is now `ui_fixture_le_rfc_answers.go`'s registered fixture, a 670-line comparison that still checks the envelope, the pipe operators, the listing text, and the refusal codes; the removed "table-driven case" match was a false positive on a Python dict comprehension inside the deleted script, not an actual test table. |
| TestNativeScenarioPopulationMatchesPythonProducer | Retired with its subject: test/interop-pppoe/run.py and the scenario check.py files it compared against are deleted. TestNativeScenarioPopulationIsExact now pins the two reviewed scenario names directly and checks them against both `pppoeleaf.ScenarioNames()` and the fixture directories on disk. |
| TestNativeImagesConfigsAndPPPDArgvMatchPythonProducer | Retired with its subject: run.py and lab.py, whose pppd argv, Dockerfile names, and config mounts this test cross-checked, are deleted. TestNativeConfigBytesArePinned now pins the Dockerfiles and scenario configs by sha256 hash instead of comparing them to Python source text. |
| producerScenarioNames | Retired with its subject: this helper read check.py directories out of the deleted Python producer tree to build the comparison list TestNativeScenarioPopulationMatchesPythonProducer used. TestNativeScenarioPopulationIsExact reads the fixture directories directly instead. |
| nativeCheckerNames | Retired with its subject: this helper parsed pppoe.go's AST to recover the native checker names for comparison against the Python producer. TestNativeScenarioPopulationIsExact calls `pppoeleaf.ScenarioNames()` directly rather than parsing source. |
| nativeStringConstants | Retired with its subject: this AST-parsing helper recovered Go string constants (image tags) to compare byte-for-byte with the deleted Python producer's literals. TestNativeConfigBytesArePinned pins the same Dockerfiles by sha256 hash instead of extracting and comparing constants. |
| pythonPPPDArguments | Retired with its subject: this helper regex-parsed the Python `pppd_dial` function out of the deleted lab.py to build the pppd argv list TestNativeImagesConfigsAndPPPDArgvMatchPythonProducer compared against. No Python source remains to parse. |
| goPPPDArguments | Retired with its subject: this helper parsed the Go checker's AST to recover its pppd argv list for comparison with the Python producer's. TestNativeConfigBytesArePinned proves the scenario configs by hash instead of comparing extracted argv lists. |
| parseGo | Retired with its subject: this shared `go/parser` helper backed nativeCheckerNames, nativeStringConstants, and goPPPDArguments, all of which existed to compare native Go source against the deleted Python producer. Nothing in the current file parses Go source at test time. |
| interop-pppoe.readFile | Retired with its subject: this helper read Python producer files (run.py, lab.py) and the Go checker source for substring comparisons. The current tests read scenario fixtures directly through `os.ReadFile` in TestNativeConfigBytesArePinned instead of through a shared helper.  |
| pppoe_parity_test | File-level marker for the whole parity file's rewrite: TestPPPoETreeIsGoOnly is new and asserts no Python or shell artifact remains in the tree, closing the loop the retired rows above describe. |
| SetReusePort | Renamed to `setReusePort` as part of unexporting the `l2tpDiagnosticLinuxOps` interface; the fake still records `setsockopt reuseport` and is exercised by TestPPPoXDiagnosticPreservesTheSocketNetlinkAndIoctlOrder and TestPPPoXDiagnosticRepresentsEverySetupAndIoctlExit. |
| BindIPv4 | Renamed to `bindIPv4` in the same interface unexport; the fake still records `bind udp` and is exercised by the same PPPoX diagnostic order and failure-injection tests. |
| IoctlGetInt | Renamed to `ioctlGetInt`; the fake still records `ioctl get channel` and its failure path is still exercised by TestPPPoXDiagnosticRepresentsEverySetupAndIoctlExit. |
| IoctlSetInt | Renamed to `ioctlSetInt`; the fake still distinguishes `ioctl attach channel` from `ioctl connect unit` by request code, and both paths are still exercised by TestPPPoXDiagnosticRepresentsEverySetupAndIoctlExit. |
| IoctlGetSetInt | Renamed to `ioctlGetSetInt`; the fake still records `ioctl new unit` and its failure path is still exercised by TestPPPoXDiagnosticRepresentsEverySetupAndIoctlExit. |
| OpenPPP | Renamed to `openPPP`; the fake still records `open /dev/ppp` twice (channel attach and unit open) and both occurrences are still exercised by TestPPPoXDiagnosticPreservesTheSocketNetlinkAndIoctlOrder. |
| ProcPPPoL2TP | Renamed to `procPPPoL2TP`; the fake still records `read /proc/net/pppol2tp` and its content is still exercised by TestConnectFailureReportsBothRetainedKernelObjects. |
| DevPPP | Renamed to `devPPPText`; the fake still records `stat /dev/ppp` and its content is still exercised by TestConnectFailureReportsBothRetainedKernelObjects and TestProcAndDeviceFailureDumpsKeepEveryProducerBranch. |
| TestAPythonTagIsReadOnlyWhereThePythonReaderSaysItIsAComment | Retired with its subject: ScanPythonTags, the tokenizing Python reader that distinguished docstrings and string literals from real comments, is gone now that every Python interop checker it read is deleted. Only `scanLegacyPythonTags`, a crude line scanner kept for this cutover and never selected by a current-tree carrier, remains. |
| TestAPythonFileNoReaderCanTrustIsRefusedRatherThanReportedEmpty | Retired with its subject: the tokenizer-based refusal for unparsable Python (unterminated strings, mismatched brackets, bad dedents) lived in ScanPythonTags, which is gone along with the Python files it scanned. `scanLegacyPythonTags` performs no such refusal and current-tree carriers never select it. |
| TestAValidPythonFileIsNotRefused | Retired with its subject: this test proved ScanPythonTags's tokenizer did not over-refuse ordinary Python. The tokenizer and the Python source it read are both gone. |
| TestTheFirstScheduledWorkflowNamingATargetIsTheOneRecorded | Renamed to TestTheFirstScheduledWorkflowNamingAnActionIsTheOneRecorded, which still proves the first workflow in sorted file order wins the credit, now over `./le` native actions (`scheduledActionsFrom`) instead of Make targets. |
| TestEveryBareWordAfterMakeIsATarget | Retired with its subject: `MakeTargetsIn`, which parsed bare words after `make` in a workflow step, has no successor because the Makefile it served is deleted. TestNativeActionsInWorkflowCommands covers the same workflow-command-parsing role for `./le <action>` invocations via `nativeActionsIn`. |
| tags_test | File-level marker: TestChangedTagsDistinguishesBehaviorFromLayoutAndComments is new, proving ChangedTags still separates behavior edits from comment/layout-only ones now that the CI terminator-block fixture text no longer embeds a Python shebang. |
| TestStressBirdRoundsMatchThePythonProducer | Retired with its subject: test/stress/scenarios/04-bulk-ipv4-bird/check.py is deleted. TestStressBirdRoundsRemainTheBaselineShape now pins the four BIRD rounds (prefix counts, bases, peer waits) as a Go literal instead of parsing them out of the Python scenario. |
| TestStressBirdGateMetadataAndConfigurationMatchTheProducer | Retired with its subject: the retired Python le application tree, integration.py, which this test read for the gate name, scenario, and root-requirement text, is deleted along with all of scripts/. TestStressBirdActionMetadataAndConfiguration still checks StressBirdAction, stressBirdScenario, and pins the bird.conf content, but no longer cross-checks the deleted Python gate registration. |
| TestStressBirdRecordedEffectsCoverTheProducerLifecycle | Retired with its subject: test/stress/harness.py, whose namespace, BIRD, peer, query, and teardown commands this test read to prove the recorder covers the same lifecycle, is deleted. TestStressBirdRecordedEffectsAreNonVacuous still asserts the same seven recorded effects, now pinned directly instead of cross-checked against Python source. |
| readStressBirdProducer | Retired with its subject: this helper read the Python producer files (check.py, integration.py, harness.py) that are now deleted. Its remaining role, reading bird.conf, is now done inline with `os.ReadFile` in TestStressBirdActionMetadataAndConfiguration. |
| stressbird_parity_test | File-level marker for the whole file's shrinkage as producer cross-checks were replaced by pinned baseline assertions (TestStressBirdRoundsRemainTheBaselineShape, TestStressBirdActionMetadataAndConfiguration, TestStressBirdRecordedEffectsAreNonVacuous), each accounted for in the rows above. |
| TestMakeParamsKeepsOnlyVariableDefinitions | Retired with its subject: `MakeParams`, which parsed `MAKEFLAGS` to recover a job's `VAR=value` definitions, is deleted along with the Makefile it served. The job key is now derived directly from the full native argv, package path included, so no separate flag-parsing step exists to test. |
| TestMakeParamsDropsTheAdmissionKnobs | Retired with its subject: the four admission env knobs (ZE_RUN_SLOTS, ZE_JOB_STALL_SECONDS, ZE_VERIFY_MAX_LOCK_AGE, MAY_ATTACH) this test proved `MakeParams` excluded from the key are moot now that `MakeParams` and MAKEFLAGS parsing are both gone. |
| TestJobKeyIsTheCommandAndTheParameters | Renamed to TestJobKeyIsTheCommand, which still hashes the exact byte stream used for the key; the stream is now `CMD=<argv>\n` alone since `jobKey` takes the complete native command instead of a command plus a separate parameter list. |
| jobkey_test | File-level marker: the header VALIDATES/PREVENTS comment was rewritten from citing the 2026-08-19 MAKEFLAGS-splitting defect and a shell/Go byte-stream contract to a shorter native-command statement, reflecting that `jobKey` no longer has a shell-side counterpart to stay byte-compatible with. |
| TestFullStagesMatchesCurrentPrecommitPopulation | Renamed to TestFullStagesMatchesNativeActionPopulation, which still pins the exact ordered 43-stage population, now by native action name (e.g. `verify-lint/run`) instead of `ze-*` gate name, and additionally rejects a duplicate stage name. |
| TestRegisteredStageContractsNameExactRootAndActionArgs | Folded into TestFullStagesMatchesNativeActionPopulation, which replaced the 43-entry hand-typed `stage()` comparison map with a self-consistency check: each live stage's `Identity` must equal `stage(current.Identity.Command, current.Identity.Args...).Identity`, so a wrong root or arg list still fails without duplicating the whole table in the test. |
| TestEveryActionStageCarriesExplicitArgs | Renamed to TestEveryActionStageCarriesExplicitArgsOrIsBare, which still requires every stage outside the four-item bare allowlist to declare explicit args; the population-count assertion it also carried moved to TestFullStagesMatchesNativeActionPopulation's length check. |
| TestRunCallsEveryRegisteredGateInOrderAndWritesStatus | Renamed to TestRunCallsEveryRegisteredActionInOrderAndWritesStatus, unchanged in substance: it still proves `run()` calls every stage in order and writes `tmp/ze-verify.status` under the given root. Note specific to this file: neither the old nor the new version proves the certificate survives a caller that discards its root after the run, such as `./le verify worktree`'s throwaway worktree; both only prove the write happens under whatever root is passed in, which is the mechanism behind that separately reported defect, not a check against it. |
| TestInstallerPlansMatchBothPythonProducerBuilds | Renamed to TestInstallerPlansPreserveBothArchitectureBuilds, which still pins the exact argv, environment, output path, and action identity for both amd64 and arm64 installer builds; only the field name (`plan.action` instead of `plan.gate`) and the Python-producer framing in the name changed. |
| TestBuildAreaClaimsThreeWritingActions | Renamed to TestBuildAreaDeclaresThreeWritingActions, which still checks the three build actions' verbs, writes flags, and reasons. It drops the `actions.Gates()` comparison and the `row.Forks` check because the `Gate` and `Forks` fields were removed from `leaction.Row` codebase-wide now that no action forks a subprocess. |
| buildartifacts.parity_test | File-level marker covering the same two renames as the rows above; no other coverage in this file changed. |
| TestNativeScenarioRegistryExactlyMatchesPythonProducer | Retired with its subject: run.py, whose scenario directories this test read via `check.py` presence, is deleted. TestNativeScenarioRegistryIsExact now pins the twenty scenario names directly and checks them against both `ipsec.ScenarioNames()` and the fixture directories on disk. |
| TestEveryNativeScenarioHasCompleteProducerFiles | Retired with its subject: this test required each scenario's `check.py` to exist and define `check()`; `check.py` is deleted for every scenario. TestEveryNativeScenarioHasCompleteInputs still requires every scenario's mandatory and scenario-specific fixture files, and additionally rejects native-renderer boilerplate leaking into a source config. |
| TestNativeGateMetadataMatchesRunPy | Retired with its subject: run.py and lab.py, whose FRR image tag, timeouts, subnet, and per-container IPs this test cross-checked against the native adapter, are deleted. The gate identity assertion survives in TestIPsecTreeIsGoOnly (`ipsec.Action`), and the FRR image, 90-second session timeout, and 172.28.0.0/24 network are still pinned by TestScenarioPlansPreserveTopologyAndInputs in internal/le/interoplab/ipsec/ipsec_test.go. |
| TestTheAreaHoldsBothGatesAndOnlyUpdateWrites | Renamed to TestTheAreaHoldsBothNativeActionsAndOnlyUpdateWrites, which still checks the area holds exactly two actions (check, update) and only update writes. It drops the `row.Gate` name checks because the `Gate` field was removed from `leaction.Row`. |
| discoveryindex_test | File-level marker: TestFeedsNamesTheIndexEachSourceDrifts dropped its `Makefile`, `mk/check-rules.mk`, and `scripts/dev/package_map.py` cases, since those files no longer exist in the tree, replacing them with a single `internal/le/discoveryindex/discoveryindex.go` case that still proves `IsSource` classifies a tracked Go file as index-worthy. |
| TestGuestEvidenceActionsAreNativeAndGateless | Renamed to TestGuestEvidenceActionsAreNative, which still checks every guest action is native; it drops the `row.Gate != ""` and `row.Forks` checks because both fields were removed from `leaction.Row` codebase-wide. |
| guestlabs_test | File-level marker for the `GuestLabReport` type being unexported to `guestLabReport` throughout the file; every call site and assertion is otherwise unchanged. |
| TestTheAreaHoldsTwoGatesAndTwoVerbs | Renamed and expanded to TestTheAreaPublishesFourNativeActions: the old test checked `actions.Gates()` held exactly `ze-tier-check` and `ze-tier-selftest`. The area grew a `report` and a `write-baseline` verb, so the new test checks all four verbs (check, selftest, report, write-baseline) and that only write-baseline writes. |
| tier_test | File-level marker for the package-wide export-to-unexport rename applied across this file's other unchanged test functions (`NestedNamespaces`->`nestedNamespaces`, `CollectEdges`->`collectEdges`, `PlacementFixture`->`placementFixture`, `GateResult`->`CheckResult`, `report.Gates`->`report.Checks`, `FirstFailure`->`firstFailure`, and similar), none of which altered any assertion. |
| TestDraftDirIsInvisibleToRepoGates | Renamed to TestDraftDirIsInvisibleToRepoChecks. The Python-based repo-wide .ci scanners it pinned (scripts/dev/verify_wiring_docs.py, scripts/dev/ci_observer_recover_check.py, scripts/dev/docker_exec_checked.py, scripts/dev/rfc_requirements.py, scripts/checks/ci_dispatch_commands.go) are deleted along with scripts/; the renamed test now pins the same source-text-contains-the-skip shape against their native successors (internal/le/docwiring/checks.go, internal/le/rfc/carriers.go, and the two Go scanners that already existed). |
| TestDraftReadmeNamesEveryGate | Renamed to TestDraftReadmeNamesEveryCheck, which still requires the README's table to name every current recursive .ci scanner and to point at the ratchet test; the legacy Python producer names it required are gone because those scripts are deleted. |
| validate_test | File-level marker for a pure addition, not a removal: TestRPKIOriginASFromASPathRFC6811 is new, tagged `RFC requirement: RFC6811-2-1`, and proves `rpkiOriginASFromASPath` derives the origin from the AS_PATH's last AS_SEQUENCE segment, treats AS_SET as unusable, and falls back to the local AS for a confederation segment or an empty path. Nothing existing was weakened or removed. |
| TestOnlyTheUpdateActionWrites | Same test, same name: it still requires exactly four actions (check/update on both the design-doc index and its reverse-index mirror) and that only the two update verbs write. It drops the `row.Gate` and `row.Forks` checks because both fields were removed from `leaction.Row`, and the mirror's verbs were renamed from `ze-doc-index-check`/`ze-doc-index-update` to `index-check`/`index-update`. |
| TestRunCarriesTheGateAndItsCommandIntoTheAnswer | Renamed to TestRunCarriesTheActionAndCommandIntoTheAnswer, unchanged in substance: it still runs a command that exits 3 and checks the report carries that code and the full command; only `report.Gate` became `report.Action` and the fixture name changed from `ze-probe-check` to `probe`. |
| TestActionsPreserveTheProducerRegistryRow | Renamed and expanded to TestActionsPublishTheNativeRegistryRows: it still checks the `report` action's verb, writes flag, and reason (using `Verb` instead of the retired `Gate` field), and now also checks a second `validate` action that did not exist before. The new `validate` action gets its own coverage in the newly added TestValidateActionUsesClosedFileKeywordGrammar. |
| TestInstallerActionsAreNativeAndComplete | Same test, same name: it still requires all four installer actions (install-test, install-iso-test, install-scenarios-test, install-ventoy-test) to be present. It drops the `action.Gate != ""` and `action.Forks` checks because both fields were removed from `leaction.Row` codebase-wide. |
| TestActionsClaimBothWritingGates | Renamed to TestActionsPublishBothWrites, which still checks both scratch actions (links-ensure, migrate) and that both write. It drops the `action.Gate` and `actions.Gates()` checks because both were removed with the `Gate` field's retirement; the verbs it now checks are `links-ensure` and `migrate` instead of `ze-scratch-links-ensure` and `ze-scratch-migrate`. |
| TestTablePinsEveryUnitGroup | Same test, same name: it still pins the five unit groups' population, order, package patterns, reasons, and non-writing contract. It drops the `row.Gate != want[index].Name` and `row.Forks` checks because both fields were removed from `leaction.Row`; the pinned type was renamed from `Gate` to `Group` with `Name` becoming `Verb`. |
| TestActionClaimsReadOnlyGateAndTranslatesProducerEnvironment | Renamed and expanded to TestActionsDeclareWorktreeCurrentAndList: the old test checked exactly one action (worktree) was read-only and that `optionsFrom` still translates `COMMIT`/`KEEP` environment into `Options`. The package grew `current` and `list` actions, so the new test checks all three verbs in order and keeps the same environment-translation assertion unchanged. Neither version, old or new, proves the verification certificate survives this package's worktree teardown; both check only that the worktree action itself is read-only and that its options parse, which is narrower than certificate persistence. |
| verify-scope-selector | Retired with its subject: the three inline shell scripts (assert-ssh.sh, assert-unclassified.sh, assert-dependency-moved.sh), which each drove `make ze-verify-scope-selector`, are deleted from the fixture along with the Make target. `internal/le/changed/scope.go`'s selector is now driven by a compiled Go fixture (`internal/test/fixture/misc_fixture_runner_scope.go`) invoked via `ze-test fixture runner/verify-scope-selector`; all five original stdout assertions (ssh-selects-its-gated-importer, ssh-reaches-one-feature, unclassified-path-is-named, unclassified-path-narrows-to-its-readers, dependency-move-widens-and-names-the-path) are unchanged. |
| pipe-local-command-runtime | Retired with its subject: the inline Python driver (run.py), which exercised all 15 local-data commands over a real `ze cli -c` pipe, is deleted from the fixture. `internal/test/fixture/misc_fixture_drivers.go` performs the identical 15-command sequence and local one-shot save as a compiled Go fixture invoked via `ze-test fixture ui/pipe-local-command-runtime`. The .ci file itself now asserts only 4 of the 15 individual `COVERED:` markers by name instead of all 15; detection is unchanged since the fixture still fails before printing the final `OK: 15/15...` line if any of the 15 fails, but which command broke is less precise from the .ci assertions alone for 11 of the 15. |
| TestABindingBindsTheNextTopLevelDef | Renamed to `TestABindingBelongsToItsGoFunctionDocComment`, which parses the doc comment above a top-level Go function instead of a Python `# ze point:` comment above a `def`, and still proves multi-line binding, list joining, and the `none` reason are preserved. |
| TestABindingAboveNoCheckIsReportedNotDropped | Renamed to `TestABindingOutsideAFunctionDocCommentIsNotAttached`, same orphan-binding and empty-ref assertions, now over a Go `var` and a bare `func` instead of the equivalent Python statements. |
| TestATypoInABindingFailsAsDangling | Renamed to `TestATypoInAGoBindingFailsAsDangling`, unchanged dangling-reference assertion, now parsing a malformed Go doc comment instead of a Python one. |
| TestTheDispatcherRosterReadsWhatItRuns | Folded into `TestTheNativeActionRegistryFailsClosed`'s first case, which still proves a wired registry's every declared check is read into the roster. |
| TestADispatcherWithNoChecksTableReadsItsMain | Retired with its subject. The Python fallback that inferred a dispatcher's checks by scanning its `main()` calls when no `CHECKS` tuple existed does not apply to native hooks: `nativeHookActions` is a required Go map, and `TestTheNativeActionRegistryFailsClosed`'s "registry is absent" case now fails closed instead of falling back to a call-site scan. |
| TestADispatcherThatCannotBeScannedIsRefused | Folded into `TestTheNativeActionRegistryFailsClosed`'s "Go cannot be parsed" case, which still refuses an unparsable source file. |
| TestAnUnwiredDispatcherIsRefusedInBothDirections | Retired with its subject. The dynamic Python-dispatcher/`settings.json` wiring model it drove (`dispatchers()`) is gone; hooks are compiled Go actions dispatched through one registry. `TestEveryConfiguredHookUsesRegisteredNativeAction` (`internal/le/hookcheck/parity_test.go`) now checks every `settings.json` hook command names a registered native action, covering one of the two directions this test checked. |
| TestThePublishedTableIsReadUnderItsHeading | Renamed to `TestThePublishedTableIsReadUnderItsGoSourceHeading`, same table-row parsing assertions, now reading a Go-source doc heading instead of a Python-dispatcher heading. |
| coverage_test | File-level residual: the whole file's lexical assertion count fell from 60 to 58 as the four dispatcher tests above folded into `TestTheNativeActionRegistryFailsClosed`'s shared case table; no single renamed function absorbs that count. The gate-map ratchet tests (`TestARenamedPointCannotBeLaunderedIntoDeclaredNone`, `TestAPointGatedAtHeadAndGatedByNothingNowFails`, and their neighbors) are untouched by this migration and are not among this file's flagged symbols. |
| writeInstallerKernelRegistry | Still the same fixture-writing helper; it now also writes native Go builder fixtures (`main.go`, `driver.go`) instead of Python (`build.py`, `run.py`, `ksource.py`) and shares one loop across its three directories, which lexically carries fewer `t.Fatal` calls than the two separate loops it replaced. No assertion or coverage was dropped. |
| TestKernelInvalidatesInstallerRequest | Renamed to `TestKernelInvalidatesInstallerVariant`. The `.request` marker it also checked came from the Makefile-driven build path and no longer exists (`installerKernelRequestFile` is gone from `cmd_kernel.go`); the surviving `.variant` assertion, the one the ISO reader still trusts, continues under the new name. |
| TestRuntimeKernelInvalidatesRequest | Retired with its subject. `gokrazy/kernel/Makefile`'s `$(REQUEST)` record no longer exists on the runtime path either, and no replacement marker file needs invalidating; the runtime kernel cache is now invalidated by content hashing instead (`TestRuntimeCacheVariantIncludesEveryPatchInput`). |
| TestKernelUndeletableRequestStopsResolve | Renamed to `TestKernelUndeletableVariantStopsResolve`, same guard-failure assertion, now driven against the surviving `.variant` record instead of the retired `.request` one. |
| TestKernelBuildPyInvalidatesCache | Folded into `TestKernelBuilderSourceInvalidatesCache`'s first half, which still proves editing the native builder source (`driver.go`) changes the cache variant. |
| TestKernelBuilderModuleInvalidatesCache | Folded into `TestKernelBuilderSourceInvalidatesCache`'s second half, which still proves a source file added to the native builder directory changes the cache variant. |
| TestRunBuilderArgvDocker | Renamed to `TestDefaultKernelBuildDockerRequest`. It no longer spies on a shelled-out `python3` process's argv log; it substitutes `nativeKernelBuildFn` and asserts the `kernelbuilder.Request` struct fields, proving the same build parameters reach the builder. |
| TestRunBuilderArgvRuntime | Renamed to `TestDefaultKernelBuildQEMURuntimeRequest`, same idea for the runtime/QEMU path: asserts on `kernelbuilder.Request` fields instead of a captured subprocess argv log. |
| TestPyrightIsJudgedOnItsSummaryNotItsExitCode | Retired with its subject. pyright, the Python-project LSP server this test judged, is no longer a dev-setup dependency; only gopls remains, and its health check has its own pre-existing tests (`TestGoplsMustAnswerWithSymbols`). |
| TestAPyrightReplyBehindABootstrapPreambleIsStillARepl | Retired with its subject, same reason: `PyrightSummary` and the nodeenv-preamble parsing it drove are gone along with pyright support. |
| TestPyrightThatAnalysedNothingIsBroken | Retired with its subject, same reason: pyright's zero-files-analyzed health check no longer exists. |
| TestTheCrossPlatformRoutesWinOverThePackageManager | Renamed to `TestGoInstallRouteWinsOverThePackageManager`. It keeps the go-install-wins-over-apt assertion; the pipx-install-wins half is dropped because pipx-based installs (`PipxInstall`, used by the old `ruff` row) no longer exist in the tool table. |
| TestAnInstallThatLandsOffPathIsPending | Renamed to `TestAGoInstallThatLandsOffPathIsPending`. Same off-PATH-is-pending assertion, now driven through a `GoInstall` tool (`gopls`, PATH fix `~/go/bin`) instead of a `PipxInstall` tool (`uv`, PATH fix `~/.local/bin`), since pipx-based installs are retired. |
| TestPipxComesBeforeEveryToolThatInstallsThroughIt | Retired with its subject. `PipxInstall` no longer exists on any row in the required-tools table, so the ordering invariant it checked (pipx precedes every tool that installs through it) has nothing left to order. |
| TestDocumentActionsCoverVerifierIdentities | Renamed to `TestDocumentActionsCoverNativeOperations`. The `Gate` field it also checked no longer exists on `ActionRow` (`gate string` dropped repo-wide in favor of a directly typed `Verb`); the surviving assertion, that the three actions carry their verb and why, continues under the new name. |
| TestMalformedPythonCheckSourceFailsClosed | Genuinely gone with no successor. `scrubPython`, the Python checker-source scrubber it drove, no longer exists anywhere in the package; nothing replaced its fail-closed-on-malformed-source proof because there is no longer a Python checker source to scrub. |
| TestLiveDocumentCorpusMatchesLegacyProducer | Retired with its subject. It proved the native scanner matched `scripts/dev/check_doc_links.py`'s output byte for byte over the live repository; that legacy Python producer is deleted, so there is nothing left to compare against. |
| legacyLinks | Retired with its subject, same reason: this helper shelled out to the now-deleted `scripts/dev/check_doc_links.py` to build the comparison text for the test above. |
| links_test | File-level residual: the whole file's assertion count fell from 47 to 41, entirely accounted for by the two deletions above (their own losses show only at the file level because their new-side text is empty). No other assertion in the file was weakened. |
| TestEveryVerbIsDerivedFromItsGate | Retired with its subject, partly folded. The `gate` field and its `ze-<name>-check` naming convention no longer exist in `actions.go`; verbs are now typed directly. Its "why is not empty" and "answer is not nil" checks survive, folded into the renamed `TestActionTableDeclaresThreeNativeVerbs` below. |
| TestTheActionTableHoldsTheThreeGates | Renamed to `TestActionTableDeclaresThreeNativeVerbs`, same three-action population check now keyed on verb strings instead of `ze-`-prefixed gate names, and it absorbs the why/answer completeness checks from the retired test above. |
| TestRefusalsAnswerTheirOwnCodes | Renamed to `TestRefusalsAnswerUsageCode`. This tracks a real production change confirmed in `actions.go`: `refuseValue` now returns usage code 2 (was 1), unifying "unknown action" and "extra argument" under one usage-error code; the test still proves both refusal paths. |
| actions_test | File-level residual: the file's assertion count fell from 30 to 27, matching the assertions `TestEveryVerbIsDerivedFromItsGate` dropped minus the two it handed to its renamed successor. No other assertion in the file was weakened. |
| TestTreeHashMatchesTheShellOverAFixtureTree | Renamed to `TestTreeHashMatchesTheFixtureTranscript`, which compares `TreeHash` against an inline Go transcript instead of the retired `scripts/dev/verify-status.sh` and proves the same match-over-a-clean-fixture property. Neither version forces a git command to fail inside a valid repository, so this pairing never covered the fail-open gap in `writeDiff`/`writeUntracked` a separate reading found. |
| TestTreeHashSeesEveryChangeTheShellSees | Renamed to `TestTreeHashSeesEveryFixtureChange`, unchanged in the tracked-then-untracked change sequence it drives and still requiring the hash to move and match an independent transcript at each step. This is not the check that would catch the fail-open defect: like its predecessor, it never makes `git diff HEAD` or `git ls-files -o` fail inside the fixture repo, so a hash that silently omitted a failed git call would still pass both the old and the new version. |
| TestTreeHashOutsideARepositoryIsStillAnAnswer | Still present under the same name, but its assertion count fell from 2 to 1: it dropped the cross-check against the shell producer (now retired) and keeps only the exact NO_HEAD-digest pin for a directory with no commit. This is the no-repository case, not the in-repo git-command-failure case the fail-open finding describes, so it does not bear on that gap either. |
| shellTreeHash | Retired with its subject. `scripts/dev/verify-status.sh`, the shell half this helper shelled out to, is deleted in the native migration; replaced by `fixtureTreeHash`, which assembles the same hash transcript inline via git commands instead of invoking the deleted script. |
| TestAllocGateCoversRecordPath | Still present under the same name, but narrower: it dropped the Makefile-parsing portion (`mk/test-alloc.mk` no longer exists) and keeps only the ceiling-registered and benchmark-exists checks. The package-coverage assertion it used to make now lives in `TestAllocPlanPreservesBenchmarkContract` (`internal/le/verifydeps/verifydeps_test.go`), which asserts the alloc benchmark command literally includes `./internal/component/plugin`. |
| readAllocGateMakefile | Retired with its subject. `mk/test-alloc.mk` is deleted; the package list it read is now `allocPackages`, a plain Go slice literal in `internal/le/verifydeps/verifydeps.go`, needing no file read. |
| allocGatePackages | Retired with its subject, same reason: the `ALLOC_GATE_PACKAGES` Makefile variable it parsed no longer exists. |
| packagePatternsCover | Retired with its subject: the native gate benchmarks the fixed `allocPackages` slice directly; `TestAllocPlanPreservesBenchmarkContract` checks the literal argv rather than matching a `/...`-suffixed pattern against a directory name. |
| TestDevSetupMatchesDoctor | Genuinely gone with no successor. It compared `applianceDoctorChecks()` against the `APPLIANCE_CHECKS` table in `the retired Python le devtools tree, tools.py`; that Python script is deleted and no second table exists to drift from, so the parity check is now meaningless rather than merely unproven, since `applianceDoctorChecks()` is the sole definition. |
| dev_setup_drift_test | File-level residual: the file's assertion count fell from 6 to 3 because `TestDevSetupMatchesDoctor`'s loss registers only at the file level (its new-side text is empty); the file's one surviving test, `TestRootVendorContainsGopls`, is unrelated new coverage for the vendored gopls command. |
| TestRegistrationRejectsBadGrammar | Moved unchanged to `internal/component/plugin/server/command_registry_test.go` (same package). Every case and assertion is identical; only `pluginserver.NewCommandRegistry()` became the in-package `newCommandRegistry()`. No coverage was lost, just relocated to the command-registry-specific file. |
| grammar_audit_test | File-level residual from the relocation above: the file's own assertion count fell from 11 to 4 because the moved test's text is now empty here; the same 11 assertions still run in `command_registry_test.go`. |
| TestActionsClaimExactGateMetadataAndArgumentCodes | Renamed to `TestActionsClaimExactMetadataAndArgumentCodes`. Drops the `Gate` and `Forks` fields, both removed repo-wide from the shared `leaction.Row` type, and the "extra value" refusal now checks code 2 instead of 1, matching the unified usage-error code seen elsewhere in this migration; verb/why/writes and the refusal paths are still proven. |
| TestCopiedRulesRemainPresentInTheVendoredProducer | Renamed and upgraded to `TestUpstreamScannerContractDigest`. It used to grep the vendored `third_party/web/htmx-upgrade-check.py` and `scripts/dev/htmx_upgrade_check.py` for each copied rule string; both Python files are deleted. The new test pins a SHA-256 of the upstream source (by version and URL) and a SHA-256 of the local translation tables (`scannerContractDigest`), so any drift between upstream and Ze's tables is still caught, now by a content digest rather than a substring scan of a file that no longer exists. |
| TestEveryActionCarriesItsGateAndNoneWrites | Renamed to `TestEveryActionIsNativeAndReadOnly`. Same population and no-writes invariant, now checked against `Actions().Actions` verb strings instead of a `Gates()` list of `ze-<name>` gate names, since that naming convention is retired. |
| TestTheGateCodeIsThreeSoACallerCanTellItFromAUsageError | Renamed to `TestCheckCodeIsThreeSoACallerCanTellItFromAUsageError`, same clean-is-0/grown-is-3 exit code assertion, using the now-unexported `newCheckReport` instead of the exported `NewGateReport`. |
| TestTheActionTableIsGatelessAndComplete | Renamed to `TestTheActionTableIsComplete`. Keeps the five-verb population check; drops the "claims no parity gate" assertion because the `Gate`/`Gates()` concept it disproved no longer exists for any action table in this migration, so there is nothing left to disclaim. |
| verifydeps_test | File-level residual: the file's assertion count fell from 46 to 44, exactly the two assertions the renamed test above dropped (its own loss shows at the file level because its new-side text is empty under the old name). |
| TestCopyTestScripts | Genuinely gone with no successor. It verified `copyTestScripts` copied `.py` test-support modules (e.g. `ze_api.py`) into an observer's tmpfs workdir; `copyTestScripts` no longer exists anywhere in the runner package. Python-based test observers are retired from the runner, and no native equivalent needs this copy step. |
| runner_exec_util_test | File-level residual: the file's assertion count fell from 56 to 46, matching the deleted function's own loss (it shows at the file level because its new-side text is empty). |
| engine.Capabilities | Retired with its subject. This was a stub method on the test-only `testPlugins` type implementing `ze.PluginManager`; the `Capabilities()` method is gone from that interface and from every production implementation, so the stub no longer needs it. Not related to the Make/Python migration. |
| derivedPageAssets | Still the same helper, still asserting the same derived per-page asset sets, but it now calls `webassets.Pages(root)` in-process instead of shelling out to `go run scripts/codegen/web_assets.go --json` and parsing its stdout as JSON; the two separate `require.NoErrorf` checks (subprocess exit, then JSON decode) collapse into one `require.NoError` on the single function-call error, which is why the lexical assertion count reads 3 to 2. `scripts/codegen/web_assets.go` no longer exists. |
| EUID | Renamed to `effectiveUID`, tracking the identical rename on the `stressBirdSystem` interface and its real implementation (`internal/le/integration/stressbird.go`); the stub's behavior and every caller's assertion are unchanged. |
| pluginimports.TestBothReportsRenderTheScriptWording | Renamed to `TestBothReportsRenderNativeWording`, same current/stale report-text assertions, updated from the retired Python producer's wording ("run make generate") to the native CLI's own wording ("./le plugin-imports write"). |
| TestQEMUActionsIncludeTheHostRun | Still present under the same name, but it dropped the assertion that the "run" action claims no `Gate` and no `Forks`; both fields no longer exist on the action-row type, so there is nothing left to disclaim. The population and verb-order assertions are unchanged. |
| TestRFCCheckActionIsClaimedReadOnly | Still present under the same name, but narrower: it now locates the row by `action.Verb != "check"` instead of filtering on `action.Gate != "ze-rfc-check"`, and it dropped the `len(action.Forks) != 0` check, since `Gate` and `Forks` no longer exist on the action-row type. The read-only (`Writes` false) assertion is unchanged. |
| TestActionsCarryTheSixGateContracts | Renamed to `TestActionsCarryTheSixActionContracts`. Same six-action population, writes-metadata, and why-text checks, now keyed on plain verbs (`check-all`) instead of `ze-terminal-demo-*` gate names; the accompanying `len(row.Forks) != 0` check is dropped since `Forks` no longer exists on the action-row type. |
| TestTheScratchTreeIsEmptiedBeforeUse | Renamed to `TestTheScratchTreeUsesTheNativeSessionPathAndIsEmptied`, which keeps the original stale-file-is-removed-on-reuse assertion and adds a new one: the scratch path must resolve under the native per-session scratch directory (`tmp/session/<date>-<id>/scratch/tracked-build/<pid>`). A second new test, `TestTheScratchTreePropagatesSessionResolutionFailure`, was added alongside it to prove a malformed session root fails closed. The successor is strictly stronger, not weaker. |
| yangglue.TestBothReportsRenderTheScriptWording | Renamed to `TestBothReportsRenderNativeWording`, same current/stale/written report-text assertions, updated from the retired Python producer's wording ("yang_glue: ... run make generate") to the native CLI's own wording ("./le yang-glue check/write"). |
| verify-scope-debt-clear | No real weakening. Three sequential shell scripts (setup, assert-green-gate, assert-red-gate), each shelling to `python3 scripts/dev/commit_helper.py ... debt-clear`, are unified into one native fixture invocation, `ze-test fixture runner/verify-scope-debt-clear` (`verifyScopeDebtClearDriver`, `internal/test/fixture/misc_fixture_runner.go`), which reproduces every case: the green gate clears its row against the committed worktree, the red gate leaves its row open and reports its exit code and output, and the unrunnable review row stays open. The "removing expectations (8 -> 6)" count is the 3-to-1 drop in `cmd=` execution steps; all 5 `expect=` assertions are unchanged, with one message updated to reflect the retired `make -n ze-verify-debt-clear` recipe check. |
| verify-scope-wiring-attribution | No real weakening, same pattern as its sibling above. Four sequential shell scripts are unified into one native fixture invocation, `ze-test fixture runner/verify-scope-wiring-attribution` (`internal/test/fixture/misc_fixture_runner.go`). The "removing expectations (9 -> 6)" count is the 4-to-1 drop in `cmd=` execution steps; all 5 `expect=` assertions are unchanged. |
| buildScopeSelector | Retired with its subject. It compiled `scripts/checks/verify_scope_selector.go` into a temporary binary so the test could exec it; the selector is now a package function the test calls directly, so there is nothing to build. |
| TestSelectorMapsPythonAndRFCPaths | Renamed to `TestSelectorMapsNativeRFCAndCorpusPaths`, which still checks the RFC and corpus path kinds. The Python kind it also covered went with the Python tree, so no mapping is left for it to assert. |
| TestSelectorScopesARealisticDirtyTree | Kept its name and its end-to-end AC-8 coverage over the whole kind list. One of its four failure reports was folded into another, so it makes three rather than four, and the same conditions are still checked. |
| TestSelectorFailsOpenOnUnsafePath | Renamed to `TestSelectorFailsOpenWhenTheSeedIsNoPackage`, which still requires the selector to widen rather than answer a narrow scope it cannot justify. |
| TestAGoModWithNoToolchainLineIsNotAnError | Replaced, because it pinned the defect. It asserted that a go.mod with no toolchain line yields no pin, which is the silently-off state `goToolchainPin` exists to prevent: this repository's go.mod declares `go 1.27.0` and no toolchain line, so every le action ran under the ambient toolchain and the export-data protection was absent. `TestTheGoDirectivePinsTheToolchainWhenNoToolchainLineDoes` now requires the go directive to supply the pin and to reach the environment, `TestAToolchainLineWinsOverTheGoDirective` keeps the precedence, and `TestAGoModDeclaringNeitherIsNotAnError` keeps the genuinely-empty case this test was reaching for. |
