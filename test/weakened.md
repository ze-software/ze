| Test | Reason |
|------|--------|
| TestACommandEquivalentPageReadsAsThePublishedPage | Assertion count 5 to 3, and the two that went were the strict-equality halves of a comparison the owner deliberately released. This commit ENRICHES the command pages, so they now carry more than the published page did: an argument's enumerated values, the backend, the task support level and the subcommands, none of which the published page states. A test demanding the rendered page equal the published one would have to fail for the enrichment to exist, which makes it a test asserting the defect. It is loosened rather than deleted: the page is now compared as a word subsequence of the published text and the mirror as a line subsequence, so every fact the published page carried must still appear, in order, and only ADDITIONS are tolerated. The discrimination proof is in the phase report and it is the point of the row: removing the Mode row from the detail card still fails this test with `the detail page drops "ModeRead-only"`. The sibling `TestACommandRowReadsAsThePublishedRow` is loosened the same way and keeps its mode and pipes cells exact |
