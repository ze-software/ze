| Test | Reason |
|------|--------|
| TestProbeOneReceivedIDCoversManyPrefixes | Renamed to `TestPathIDKeyFollowsWhatTheSourceFramed`, and strengthened rather than weakened. It asserted "one key, one identifier, several prefixes", which is the property this commit deliberately removes: one identifier covering many prefixes is exactly why the table could not be freed per prefix. Its SUBJECT survives unchanged — how many prefixes one received identifier covers — and the answer is now decided by what the source framed, so the test pins that instead: a framing source on the raw rail, the same on the re-encode rail, and a source framing none, which keeps a full table at one entry and is what leaves `bgp-addpath-readvertise-collision-frr` safe. The re-encode case is new coverage: no test anywhere reached that branch, because every existing add-path fixture registers one context for both ends and takes the raw rail, so a mutation keying it on the source alone survived until this case existed. The file carried no `RFC requirement:` tag before and carries two now |
| TestProbeWithdrawOnlyUpdateGrowsPathIDTable | Renamed to `TestWithdrawOnlyUpdateFreesEveryIdentifierItBuys`, and it gains assertions rather than losing them. It had ONE assertion, `delivered == 1`, and logged every number it measured. It now asserts them: a 1623-octet withdraw-only UPDATE naming 200 identifiers buys exactly 200 entries during the relay and exactly zero once the cache evicts it. The logs are kept. Exact counts throughout, never "less than", because "less than" passes on a leak that is merely slower |
