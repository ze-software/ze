| Test | Reason |
|------|--------|
| TestTheLegacyRewriteRunsBeforeTheDerivedProducers | Deleted with the wiring it asserted. It pinned that the legacy-URL rewrite runs BETWEEN the page producers and the producers that read what they wrote, so a mirror the search index and `llms-full.txt` inline carries the address a reader reaches rather than a retired one. The owner turned that wiring off on 2026-08-30: the site maps nothing to an old page before the first release, neither through a redirect stub nor through rewriting a retired address inside a published page, and whether to keep redirects at all is reconsidered after release. A test asserting the position of a pass that does not run would have to fail for the decision to hold. What is NOT given up: `rewriteLegacyPublicURLs` keeps `TestRedirectsApplyInTheRecordedOrder` above it, which still proves the 177 replacements apply in the recorded order, each over the output of the one before, and still discriminates — the same two moves recorded forwards give `c` and reversed give `b`. So the transformation stays covered and only its place in a build is unasserted, which is the honest state while the build does not run it |
| redirect_test | File-scope row for the assertion count, which is arithmetic from the deletion above rather than a separate loss. The file keeps three tests over the redirect machinery: the recorded application order, the stub markup, and the route table's derivation. `redirect.go` itself is unchanged in behaviour and keeps its code; it simply no longer registers its producer, and the file states at that point why it is dormant and what re-enables it |
