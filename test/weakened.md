| Test | Reason |
|------|--------|
| TestConfigDiffJSON | Renamed and rewritten in place, not removed. It is `TestConfigDiffAnswersThreeKeyedSets` in `internal/component/config/cli/cmd_diff_test.go`, and the gate reports a deletion because telling a rename from a rewrite needs a Go parser. The `--json` flag left `show config diff`: the pipe layer renders the answer now, so a test that captured stdout under `--json` was asserting over a flag the command no longer has. The new function calls `dataDiff` and asserts the same three keys, added, removed and changed, on the payload the renderer receives. AC-10 keeps its evidence at the producer instead of at the writer. |
| cmd_diff_test | The assertion count falls from 29 to 27 because the stdout capture the old JSON test needed, the pipe open, the close and the byte-slice read, went with the flag. No case left the suite: every `cmdDiff` call in the file lost only its `--json` argument, and the JSON shape is asserted on the payload in `TestConfigDiffAnswersThreeKeyedSets`. |
| TestValidatePendingRequiresJSON | Deleted because the requirement it pinned no longer exists. It asserted that `--pending` without `--json` exits 1; `validate config` has no `--json` flag now, the pipe layer renders the answer, and `--pending` is accepted on its own. A test that asserts an error for the only calling form the command still has would fail on correct behavior. The AC-19 error paths keep their tests: `TestValidatePendingMissingDraft` and `TestValidatePendingRejectsStdin`, both now called without the flag. |
| TestValidatePendingReadsDraft | The assertion count falls from 6 to 4 because the stdout capture and the JSON unmarshal of what was printed are replaced by one assertion on the report text, that it names the verdict `configuration invalid`. The envelope and diagnostics keys it used to check on stdout are now asserted on the payload, in the new `TestValidateConfigAnswersDiagnosticsAndFails`, which also pins the case this recovery's `RegisterLocalMeta` fix exists for: a rejected configuration answers its diagnostics AND exits 1. |
