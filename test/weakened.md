| Test | Reason |
|------|--------|
| TestBuildPreservesExistingArtifactSeed | Two setup error checks left, for the seeded `data/cli-commands.json` the test no longer needs, and the test's subject moved from `reference/cli/index.html` to `blog/index.html`. Its stated purpose is the SNAPSHOT: a full build must copy the Pages checkout aside before it cleans, so a page survives. It proved that with a page the build GENERATES, so what it actually pinned was `refreshNativeSurfaces` refusing to rewrite an existing command surface, and that refusal is the defect fixed in the same commit. A non-generated page proves the snapshot and nothing else. The ground given up is covered and strengthened by `TestBuildRepublishesStaleCommandSurfaces` in the same file, which asserts the opposite of the old expectation on both the catalog and the page |
