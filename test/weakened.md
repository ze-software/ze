| Test | Reason |
|------|--------|
| TestTheRetiredLedgerRefusesARowThatDeclaresNothing | Deleted with the mechanism it covered. It exercised `retiredRowsSince`, which validated a row in the retirement ledger against the point ids git HEAD carried. The owner removed the ledger on 2026-08-30: a point that leaves the corpus is recorded by git history, not by a hand-written forwarding address. There is no parser left to refuse a malformed row, so the test asserts over nothing |
| TestARowCommittedAtHeadIsNotRejudged | Deleted with the mechanism it covered. It proved the ledger was a scope rather than an allowlist, by showing a row already committed at HEAD was neither re-judged nor able to mint a second retirement. Both properties belong to a ledger this commit removes |
| TestTheCorpusRatchetReportsIdentityNotACount | Deleted with the mechanism it covered. It proved `corpusShrink` reported WHICH instruction left rather than a count an addition could mask. The shrink ratchet is gone, so no code makes that comparison. The property it defended, that a specific instruction leaving must be visible, is now answered by `git log` over `ai/rules/points/` |
| TestAMissingBaselinePrintsItsAbsenceRatherThanAZero | One assertion of three removed, and the other two kept. It required the page to print both "REGRESSED: no HEAD baseline" and "SHRUNK: no HEAD point baseline" when no baseline existed, so a ratchet that never ran could not report a permissive zero. The SHRUNK section no longer exists. The same guarantee for the surviving ratchet is unchanged: the test still requires the REGRESSED absence line and still refuses a printed "REGRESSED: 0" |
