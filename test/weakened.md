| Test | Reason |
|------|--------|
| analyse | Renamed, not removed. `coverageFixture.analyse` in `internal/le/rules/session_coverage_test.go` is `coverageFixture.analyze` in the same file, and the gate reports a deletion because telling a rename from a rewrite needs a Go parser. The helper is a fixture method, not a test: it loads the coverage rules, reads the transcript files, and returns the report the three `TestSessionCoverage*` functions assert over. Its body is unchanged apart from the producer it calls, `analyseSessionCoverage` renamed to `analyzeSessionCoverage` by the same US-English sweep. Every caller and every assertion survives verbatim, so no case left the suite. |
