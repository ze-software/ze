| Test | Reason |
|------|--------|
| TestACarrierFixtureBlockIsNotTestedBehaviour | Not removed: renamed to `TestACarrierFixtureBlockIsNotTestedBehavior` in the same file, `internal/le/rfc/tags_test.go`, by a misspell fix. Every assertion is unchanged, and the detector reads a rename as a deletion because it matches on the function name |
| TestRFCCheckRealTreeReportsFiveRFC9552Violations | Replaced by three stronger tests in `internal/le/rfc/check_test.go`, not dropped: `TestRFCCheckReportsThePlantedViolation`, `TestRFCCheckClearsTheViolationWhenBothPolaritiesAreProven` and `TestRFCCheckRunsOverTheRealCheckout`. The old test asserted a COUNT of five violations, which goes stale the moment one is proven and says nothing about whether the gate can still see a violation. The replacements plant one and prove both polarities, so they would fail if the gate stopped detecting. 151 lines replace 25 |
