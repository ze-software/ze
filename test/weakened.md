| Test | Reason |
|------|--------|
| ipv4-announce-withdraw | No assertion left the suite. The deleted line is `cmd=api:conn=1:seq=2:text=peer * update text nlri ipv4/unicast eor`, which the gate counts as an expectation and which asserts nothing: `cmd=` sets the Cmd LABEL on the message at that conn/seq (`internal/test/runner/record_parse.go`), and this one named a command no driver ever issues. `announceWithdraw09` (`internal/test/fixture/plugin_fixture_09.go`) sends the announce and the withdraw only, so the frame at seq=2 is ze's own initial-sync End-of-RIB and the label was false. Every `expect=` survives byte for byte, the marker's hex included, and so does its POSITION after the plugin's announce, which is the assertion the owner's 2026-08-30 ruling on plugin-injected routes makes load-bearing. `mup-ipv4-announce.ci` deleted the same false label from its own file for the same reason. |
| ipv6-announce-withdraw | Same deletion, same driver: the `cmd=api ... nlri ipv6/unicast eor` label named a command `announceWithdraw09` never issues. Every `expect=` survives, marker hex and position included. |
