# Test weakenings this commit accepts

One test is RENAMED and rewritten, and nothing left the suite. Half of what it
asserted was the branch this commit deletes, so keeping it unchanged would
have required the product to keep behavior that is wrong.

`TestOrchestratorAppliesParticipantThatYieldedNoOperations` made two claims.
The first is the one that matters: a participant whose root yields no
operations still gets its change applied, so nothing is silently dropped. The
second is the one this commit deletes: it required BOTH participants to
receive a section apply and required `operationEvents` to be empty, which is
the fallback itself. It asserted that a decomposed root loses its ordering
whenever an uncovered root joins the transaction.

`TestExecuteMixedRootTakesOperationPath` keeps the first claim in the same
words. The uncovered participant must receive exactly one section apply, which
is the old `want 1` on `iface` unchanged. The second claim is inverted, on
purpose: the decomposed root must now receive zero section applies and must
appear in `operationEvents`. That inversion is the defect being fixed, not
coverage being reduced.

The assertion still discriminates. Reverting the coarse-node synthesis
(`for _, name := range uncovered` to `uncovered[:0]`) leaves the uncovered
participant with no section apply, which the kept `want 1` rejects. The same
revert was observed turning `test/reload/config-apply-ordering-coarse-root.ci`
red at step 5, and green again after restore.


## Phase 3: the rules these tests drove are deleted

Three tests asserted the behavior of constraint rules this commit removes.
Each one's subject is gone, so none could be kept: a test of a deleted rule
either fails to compile or asserts nothing.

`TestDependencyGraphSimple` built a graph from two rules whose relations,
`ResourceRelationInterfaceAddress` and `ResourceRelationAddressUsedBy`, are
both deleted here. It made three claims, and all three survive:
an interface is created before its address, and an address exists before a
peer uses it, are now asserted by
`TestBuildOperationGraphDerivesProducerBeforeConsumer` and by the `iface-create`
and `bgp-peer-local-address-change` cases of
`TestBuildOperationGraphDerivedEdgesMatchDeletedRules`; the third claim, that
no transitive edge appears, is asserted more strictly than before, because
that test compares the WHOLE edge set with `ElementsMatch` rather than
checking one absent pair.

`TestBGPConstraintRulesOrderPeerAgainstAddress` and
`TestBGPListenerConstraintRules` asserted the four `bgp` constraint rules,
all four deleted here. For the two PEER rules the claim is replaced:
`TestBGPOperationsDeclareConsumeAddress` asserts what the decomposer declares,
and `TestBGPOperationsOrderAgainstAnAddressOnAnyInterface` asserts the ordering
that declaration earns. The replacement is wider: the deleted rules selected
the `add-peer` label, so a `modify-peer` rebinding the same address got no
edge, and the derivation gives it one.

The two LISTENER rules are replaced by nothing, and the coverage that goes with
them is not replaced either. `decomposeBGPOperations` emitted no listener
operation before this commit and emits none after it (checked at
`a6ea1ad0b~1`), so both rules selected an operation that never existed, and
`TestBGPListenerConstraintRules` asserted the rule registry rather than any
ordering the daemon can produce. Nothing declares a listener's address
consumption today: `internal/core/bgp/configop` carries `add-peer`,
`remove-peer` and `modify-peer` and no listener label. The listener's ordering
is owed when a listener operation is emitted, and it is owed a test then.

## Review round 2: the deadline stops being a per-tier max

Two deadline tests asserted the tier computation that review finding I-1
deletes. `computeTieredDeadline` took the max budget within a dependency tier
and summed the tiers, on the premise that a tier applied concurrently. Nothing
applies concurrently: an engine handler fires inside the emitter's goroutine
and the bridge performs the plugin RPC inside that handler, so a publish
returns only when the plugin has answered, and the ordered path then applies
one node at a time under one absolute deadline.

`TestOrchestratorDependencyGraphDeadline` asserted 13s for four participants
budgeted 10, 5, 3 and 2. The answer is now 20s, and the test is
`TestApplyDeadlineSumsEveryParticipantBudget` over the same four participants
and the same budgets. The claim is not reduced, it is corrected: the old
expectation would have aborted a transaction in which the third and fourth
participants applied correctly.

`TestOrchestratorTieredDeadlineCycleFallback` made `tierFn` return an error to
reach the flat-max fallback branch. The deadline calls `tierFn` never, so that
branch has no code and the test has no subject. What it protected beyond the
branch was "a zero deadline is an instant timeout", and
`TestApplyDeadlineDefaultsWhenNoParticipantDeclaresABudget` keeps exactly that,
asserting the 30-second default for a participant set that declares nothing.

Both replacements discriminate. The sum test was observed RED against the
per-tier max at `apply deadline = 10s, want 20s`
(`tmp/session/2026-09-08-cbc36cee-41ac-4afd-8b71-1bae841964d9/scratch/cao2-i1-deadline-red.log`).
The default test fences a value no revert in this change moves, so it has no
red phase, and it is recorded as a kept claim rather than as proof.

## Phase 5: the ordering policy becomes the owner's

The requirement is remove-then-add with the binder stopped across the move
(`docs/architecture/config/apply-ordering.md`, "The requirement"). Every test
below asserted make-before-break, which was inherited from a 2026-05-27 review
table and was never what the owner asked for. None of them is dropped: each is
rewritten to assert the order the requirement gives, and several now refuse the
order they used to require.

Nothing left the suite. The count of assertions went up, because the swap
driver now checks a per-address ordering as well as the invariant it inverted,
and the placement test gained two cases for the phase 5 boundary.

Two things have NOT been proven on the real path.
`test/reload/config-apply-ordering-address-swap.ci` and
`test/reload/config-apply-ordering-mixed-root.ci` both carry
`option=needs-linux:caps=net-admin` and skip on the darwin host this change was
made on. Their assertion functions changed with the policy, so the walk each of
them passed under the old policy no longer covers them. What ran instead is the
two-arm unit test over each assertion function, which accepts the requirement's
order and refuses the policy it replaced. The walk is named in each file's
DISCRIMINATION header and is outstanding.

**One test is left RED and is not in the table, because it did not leave the
suite and this change is not entitled to edit it.**
`TestIfaceSameSubnetSwapOrdersAddBeforeRemove`
(`internal/component/iface/operation_address_swap_test.go`) asserts
`add-address` at index 0 and `remove-address` at index 1, which is
make-before-break, and its own header says the inversion "must be a deliberate
reviewed decision rather than silent drift". This is that decision: the owner's
requirement removes before it adds. The file sits in a package another session
is editing concurrently and is already modified in the working tree by that
session, so inverting the assertion here would collide with work in flight. The
assertion is owed the inversion, and the claim about the IPv4 primary/secondary
guard in its header is now carried by
`internal/plugins/iface/netlink/addr_primary.go`, which states that the ordered
apply path no longer reaches the hazard and the reconcile path still does.

The table below carries every row in this file.

| Test | Reason |
|------|--------|
| TestTopologicalSortCycleResolution | Its subject is deleted. It asserted that a swap was a cycle, that tryRelaxCycle broke it, and that the creations came back marked AllowDual. TestTopologicalSortSwapsAddressesBreakBeforeMake asserts the same swap over the same four operations, and requires both removals before both additions |
| TestTopologicalSortThreeWayRotation | Same subject, same replacement shape: TestTopologicalSortRotatesAddressesBreakBeforeMake asserts the rotation sorts with every removal before every addition, and with no cycle |
| TestTopologicalSortRelaxesCycleByVerbAndKind | Its claim, that the solver decides on the verb and the kind rather than on the operation label, is KEPT in TestTopologicalSortOrdersASwapWhoseLabelsItDoesNotKnow over the same unknown labels. What changed is the order it reads back |
| TestTopologicalSortRejectsNonAddressCycle | Renamed to TestTopologicalSortRejectsEveryCycle. Its two cases are unchanged and still pass. Its comment claimed an address-only cycle relaxes, which is now false for every cycle |
| TestTopologicalSortPlacesSectionNodeBetweenCreatesAndDestroys | Renamed to TestTopologicalSortPlacesSectionNodeBetweenAddressingAndBinders. One case inverted with the policy (a coarse node runs AFTER the destructions now, because phases 1 to 3 are the destructions), one was rewritten for remove-then-add, and two were added for the phase 5 boundary the deleted name check used to stand in for |
| TestReloadTxApplyBGPLast | Its subject is deleted: sortParticipantsBGPLast moved the participant literally named "bgp" to the tail of the slice. TestReloadTxAppliesCoarseSectionsBeforeBinderStarts asserts the invariant that sort was standing in for, over a root with a generated name, with the decomposing participant registered FIRST so neither a name check nor the slice order can produce the answer |
| TestAssertSwapWindowAcceptsBothCreatesBeforeBothDestroys | Renamed to TestAssertSwapOrderAcceptsBothDestroysBeforeBothCreates, over a fixture text rewritten to the requirement's order |
| TestAssertSwapWindowRefusesBreakBeforeMake | INVERTED on purpose, to TestAssertSwapOrderRefusesMakeBeforeBreak. It used to refuse the order the requirement asks for. It now refuses the order the deleted policy produced, and names the failure "held both addresses" |
| TestAssertMixedRootOrderRefusesAnAddressRemovedFirst | Its claim was that the old address must not be removed before the coarse root's route is installed, which is make-before-break. TestAssertMixedRootOrderRefusesTheNewAddressArrivingFirst refuses the new address arriving before the old one leaves. The load-bearing half, TestAssertMixedRootOrderRefusesARouteInstalledFirst, is unchanged |
| solver_test | Three assertions went with the dual-presence policy this commit deletes: they asserted `Params.AllowDual` set on the relaxed creations and the make-before-break order `tryRelaxCycle` produced. The flag, `markDualPresence` and `tryRelaxCycle` are all deleted, so the assertions have no subject. The order they asserted is now asserted the other way by `TestTopologicalSortSwapsAddressesBreakBeforeMake` and `TestTopologicalSortRotatesAddressesBreakBeforeMake`, and every cycle is now rejected rather than relaxed. |
| TestConfigOperationMarshal | One assertion of fourteen: it round-tripped `Params.AllowDual` through the plugin contract. That field is deleted from `pkg/plugin/rpc/types.go` in this commit, because nothing outside the solver ever read it and the policy it labelled is gone. The remaining thirteen assertions are unchanged. |
| TestIfaceOperationDecomposerMixedDiffFallsBack | Asserted the DEFECT this commit fixes. `ifaceDiffHasDecomposableChanges` declined the whole root when the diff held one key it did not recognise, so a commit editing an MTU and moving an address emitted no address operation and the session stayed up while the address moved under it. The decomposer now covers its whole root, so there is no fall-back to assert. Replaced by `TestIfaceOperationDecomposerMixedDiffStillMovesTheAddress` and, at the entry point, `TestReloadStopsABinderWhenTheCommitAlsoEditsAnMTU`. |
| TestIfaceOperationDecomposerUnsupportedDiffFallsBack | Same subject as the row above, at the other door: a diff key the decomposer could not classify discarded the operations already accumulated. The classifier is deleted, so nothing can be dropped by misreading a key. Covered by the same two replacements. |
| TestIfaceOperationDecomposerUnsupportedTypeFallsBack | Same defect, third door: an interface of a type this package cannot create (tunnel, wireguard, xfrm) discarded the address operations already accumulated. Every address LEAVING the host is now an operation whatever carries its interface, which is what `DisturbedAddresses` reads. The interface itself still rides the configure operation, asserted by the decomposer tests that replace this one. |
| TestIfaceSameSubnetSwapOrdersAddBeforeRemove | RENAMED to `TestIfaceSameSubnetSwapOrdersRemoveBeforeAdd` and its assertion inverted, in the same file. Its own header required that inversion to be a deliberate reviewed decision rather than silent drift; the owner's apply order of 2026-09-11 is that decision, and it removes before it adds. Nothing left the suite: the test still drives the same renumber and still asserts a strict order, the other way round. The Linux primary/secondary hazard its old order created is gone with the order, and the guard in `internal/plugins/iface/netlink/addr_primary.go` stays for the reconcile path, which still adds first. |
| TestAssertMixedRootOrderAcceptsAddressThenRouteThenRemoval | Renamed and its claim corrected, in the same file. It accepted the sequence new address, route, old address removed, which is the make-before-break order this commit deletes. Its replacement accepts the requirement's order: the old address is removed, the new one is added, then the route that binds it is installed. Nothing left the suite, and the refusal arm beside it is unchanged. |
