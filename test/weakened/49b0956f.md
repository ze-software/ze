# Test weakenings this commit accepts

One test leaves the suite and no coverage goes with it. `TestZZProbeExtended`
never asserted anything about what ze produced.

Its body loops the announce entry point over four combinations of update groups
and body size, calls `require.NoError`, and writes the message count to
`t.Logf`. A defect in the announce path that changed those bytes, dropped a
message, or emitted the wrong ones would leave it green, so it could not go red
for any product reason. Its own header called it a "Throwaway probe", and it
landed inside `3f25545edb`, a fix commit that did not need it.

The announce path it touched is covered by the tests that assert on what it
writes, `zzprobe_announce_extended_test.go` being the only file in the package
whose name says it is a probe rather than a test.

Removed at the owner's instruction, 2026-09-08.

| Test | Reason |
|------|--------|
| rfc6793_reconstruct_test | The case above is what left this file in THIS commit. The 13 that remain are phase-2 residue of the same move and are still red: they call `storage.ParseAttributes` directly and assert a reconstruction that now happens upstream of it. Ten have exact-name successors in the attribute package. They are named in the commit body and left for the owner, because deleting a test file is his decision |
| TestParseAttributesReconstructionCost | Replaced rather than removed. It asserted 10 allocations for a reconstruction `ParseAttributes` no longer performs, so its premise is gone. `TestParseAttributesReconciliesNothing` stands in its place and asserts the opposite and stronger property: handed an OLD-speaker shape, the function interns the AS_PATH as it arrived and takes no leading part from it. A ParseAttributes that reconstructed again would corrupt a path that was already correct |
| TestRFC6793LoneAS4AggregatorIsKeptUninterpreted | Removed because the behaviour it pinned is gone, and the change is deliberate rather than incidental. It asserted that a lone AS4_AGGREGATOR is retained in `OtherAttrs`; the reconciler drops it and reports the discard, which `TestRFC6793LoneAS4AggregatorIsDropped` asserts in the attribute package. RFC 6793 is silent on the shape: every rule in Section 4.2.3 is conditioned on "When both of the attributes are received", and Section 4.2.2 makes a conformant sender always place AS_TRANS in an AGGREGATOR beside it. FRR and BIRD disagree, read on 2026-09-09: FRR fabricates an AGGREGATOR from the AS4 value (`bgp_attr_munge_as4_attrs`), BIRD unsets it unconditionally before the pairing test (`bgp_process_as4_attrs`). Ze matches BIRD |
| rfc6396_mrt_aspath_test | Both its tests, TestRFC6396RIBEntryASPathStoredFourByte and TestRFC6396RIBEntryASPathFourByteSessionUnchanged, moved to `internal/core/bgp/attribute/rfc6793_reconcile_test.go` under the same names when the RFC 6793 reconciliation moved there: both asserted that a two-octet AS_PATH is stored four-octet encoded, which the ingest collapse now decides and this package no longer does. The file is left in place, empty and carrying a note that says where its cases went, because deleting a test file is the owner's to approve |
| TestRFC6396RIBEntryASPathFourByteSessionUnchanged | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6396RIBEntryASPathStoredFourByte | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793AggregatorOfTheWrongWidthIsNotRead | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793AggregatorWithASTransPromotesAS4Aggregator | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793AggregatorWithRealASIgnoresAS4Attributes | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793ASPathAloneNotInventedIntoFourOctet | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793EqualCountsPrependNothing | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793LeadingASSetIsTakenWhole | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793LeadingConfedSegmentIsPrepended | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793LongerAS4PathIsIgnored | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793LongerASPathPrependsLeadingHops | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793MalformedAS4PathIsDiscarded | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793ReceivedAS4AggregatorAccepted | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793ReceivedAS4PathAcceptedAlongsideASPath | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| TestRFC6793UnadjacentConfedSegmentIsNotPrepended | Moved, not dropped. It drove `storage.ParseAttributes` over an OLD-speaker payload and asserted the RFC 6793 Section 4.2.3 reconciliation, which left this package on 2026-09-09 for `internal/core/bgp/attribute` and now runs once at ingest. A test of the same name, over the same inputs and carrying the same requirement tag, stands in `internal/core/bgp/attribute/rfc6793_reconcile_test.go`; the successor was checked by name before this one was removed. Removing it at the owner's instruction on 2026-09-09 to clear the 15 reds the move left |
| rfc6793_as4_test | The three cases this file held all moved to `internal/core/bgp/attribute/rfc6793_reconcile_test.go` under their own names, listed in the rows above. What stays is the wire fixture block, still read by `internal/component/bgp/plugins/rib/storage/rfc6793_reconstruct_test.go` |
