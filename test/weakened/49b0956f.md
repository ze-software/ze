# Test weakenings this commit accepts

One test leaves the suite and no coverage goes with it. `TestZZProbeExtended`
never asserted anything about what ze produced.

Its body loops the announce entry point over four combinations of update groups
and body size, calls `require.NoError`, and writes the message count to
`t.Logf`. A defect in the announce path that changed those bytes, dropped a
message, or emitted the wrong ones would leave it green, so it could not go red
for any product reason. Its own header called it a "Throwaway probe", and it
landed inside `3f25545edb`, a fix commit that did not need it.

The announce path it touched is covered by the tests that assert on what it
writes, `zzprobe_announce_extended_test.go` being the only file in the package
whose name says it is a probe rather than a test.

Removed at the owner's instruction, 2026-09-08.

| Test | Reason |
|------|--------|
| TestRewriteASPath_ClearsTombstoneTransitiveAtEBGPBoundary | Approved by Thomas on 2026-09-09: "we can remove support for tomstone delete", read narrowly as the draft-mangin-idr-attr-tombstone-00 Section 5.3 Transitive clear, the behaviour no production path performed. This test WAS the Section 5.3 assertion. Nothing replaces it, because nothing replaces the behaviour: `docs/architecture/wire/attributes.md` now states that the clear is not implemented, so the ledger and the tree agree. |
| TestRewriteASPath_NonTransitiveTombstoneUnchanged | Approved by Thomas on 2026-09-09: "we can remove support for tomstone delete", read narrowly as the draft-mangin-idr-attr-tombstone-00 Section 5.3 Transitive clear, the behaviour no production path performed. The negative pole of the same Section 5.3 assertion: an already non-transitive marker forwarded unchanged. With no clear left there is no polarity to hold apart. |
| TestRewriteASPath_ClearsTombstoneTransitiveExtendedLength | Approved by Thomas on 2026-09-09: "we can remove support for tomstone delete", read narrowly as the draft-mangin-idr-attr-tombstone-00 Section 5.3 Transitive clear, the behaviour no production path performed. The extended-length case of the same Section 5.3 clear, proving it touched only the Transitive bit. Same reason. |
| TestTombstoneTransitiveClearedOnEveryPrependPath | Approved by Thomas on 2026-09-09: "we can remove support for tomstone delete", read narrowly as the draft-mangin-idr-attr-tombstone-00 Section 5.3 Transitive clear, the behaviour no production path performed. The same Section 5.3 clear asserted across all three prepend paths (`aspath_aggregator_probe_test.go`). Same reason. |
| TestTombstoneCodePointIsUnified | Approved by Thomas on 2026-09-09: "we can remove support for tomstone delete", read narrowly as the draft-mangin-idr-attr-tombstone-00 Section 5.3 Transitive clear, the behaviour no production path performed. NOT removed: REDUCED to its first third. It proved WriteTombstone emits 252, and that the EBGP egress recognized 252 and not the retired 253. The first survives and still runs. The recognition half went with the Section 5.3 clear, which was the only reader of a tombstone on egress, so there is no consumer left for a code point to be unified FOR. |
| TestPrependTombstonesMalformedAggregator | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. `ASPathEdit.recordAggregator` does NOT tombstone an AGGREGATOR whose length it cannot read: its default arm returns and the attribute travels on unchanged, which its own comment justifies under RFC 4271 Section 5.1.7 for an optional transitive attribute this speaker has no basis to rewrite. So the assertion has no successor on the live prepend rail, and saying it did would be false. The behaviour is recorded in `plan/journal/unwired-feature.md`. The transcode rail still tombstones (`aspath_transcode.go`), and `TestTranscodeASPath_4to2_MalformedAggregator` covers that. |
| TestRewriteASPath_LengthsCorrect | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. It asserted that the per-attribute length and the body's attrLen field both moved by the prepend's shift. Neither is `ASPathEdit`'s to write: it records a generator, and the exactly-sized one-pass rebuild writes both length fields. The wireu half of the property is `materialize` (`aspath_slot_test.go`), which every generator assertion goes through and which requires GenWrite to write exactly GenLen bytes and GenLen to be stable across calls. The body half belongs to `buildModifiedPayload` (`internal/component/bgp/reactor/forward_build.go`) and its own tests. |
| TestRewriteASPath_RoundTrip | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. It asserted that a rewritten payload parses back with the local AS first and that the NLRI is preserved byte for byte. Same split as the row above: `ASPathEdit` produces no payload, so payload preservation is the rebuild's property and is asserted where the rebuild runs. The AS_PATH half is `TestASPathSlotShiftPrependIsExact`. |
| TestRewriteASPath_WithWithdrawn | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. It asserted the offset arithmetic when the withdrawn-routes field is non-empty. The live rail answers a different and stronger question about the same input: RFC 4271 Section 5.1.2 b conditions the prepend on a route being ADVERTISED, so `TestASPathSlotPrependOnlyWhenAdvertising` (`advertise_test.go`) asserts that a withdraw-carrying UPDATE gets no prepend at all. The offset arithmetic is the rebuild's. |
| TestRewriteASPath_Malformed | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. Successor: `TestASPathSlotRefusesTruncatedPayload`, which drives `ASPathEdit.Record` with a body too short for its own length fields and with an attribute section longer than the bytes present, and requires an error for each. `FuzzASPathEditRecord` carries the arbitrary-input half, seeded with this file's corpus. |
| FuzzRewriteASPath | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. Successor: `FuzzASPathEditRecord` (`aspath_slot_test.go`), same four fuzz arguments in the same order, so the existing corpus entry was copied to `testdata/fuzz/FuzzASPathEditRecord/` and passes. The old corpus directory is left on disk: removing it is a test deletion and needs the owner. |
| TestRewriteASPath_ExistingSequenceASN4 | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. Successor: `TestASPathSlotShiftPrependIsExact`, which asserts the same prepend onto a leading AS_SEQUENCE at matching widths and additionally pins that it takes the byte-shifting path. |
| TestRewriteASPath_ExistingSequenceASN2 | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. Successor: `TestASPathSlotDerivesAS4Path`, which runs the same prepend-and-narrow (SrcASN4 true, DstASN4 false) and asserts the narrowed AS_PATH value bytes. |
| TestRewriteASPath_NoASPath | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. Successor: `TestASPathSlotInsertsWhenAbsent`. |
| TestRewriteASPath_FirstSegmentIsSet | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. MOVED, not dropped: `TestASPathSlotPrependsBeforeALeadingASSet` (`aspath_slot_test.go`) asserts the same two segments in the same order over `ASPathEdit.Record`. |
| TestRewriteASPath_FullSequence255 | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. MOVED: `TestASPathSlotStartsANewSegmentWhenTheLeadingOneIsFull`, which additionally asserts the full segment keeps all 255 of its AS numbers. |
| TestRewriteASPath_ASTransEncoding | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. MOVED and WIDENED: `TestASPathSlotPrependsASTransForANonMappableLocalAS` runs it as a table with the mappability boundary, so it also absorbs TestRewriteASPath_AS4PathMappabilityBoundary. |
| TestRewriteASPath_AS4PathMappabilityBoundary | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. MOVED into `TestASPathSlotPrependsASTransForANonMappableLocalAS`, with the same three cases: 65535 last mappable and no AS4_PATH, 65536 first non-mappable, 4294967295. |
| TestRewriteASPathDual_ExistingSequence | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. Successor: `TestASPathSlotDualOrder`. |
| TestRewriteASPathDual_NoASPath | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. MOVED: `TestASPathSlotDualInsertsWhenAbsent`. |
| TestRewriteASPathDual_ASN2 | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. MOVED and WIDENED: `TestASPathSlotDualNarrowsToOldSpeaker` runs both a mappable and a non-mappable override, so it also absorbs TestRewriteASPathDual_AS4Path. |
| TestRewriteASPathDual_AS4Path | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. MOVED into `TestASPathSlotDualNarrowsToOldSpeaker`'s non-mappable subtest, which asserts the same AS_PATH and AS4_PATH values. |
| TestRewriteASPath_AggregatorNonMappable | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. Successor: `TestASPathSlotDerivesAggregatorASTrans`, which asserts the six-octet AGGREGATOR carrying AS_TRANS and the eight-octet AS4_AGGREGATOR carrying the real AS with the IP unchanged. |
| TestRewriteASPath_AggregatorMappable | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. MOVED: `TestASPathSlotNarrowsAMappableAggregator`, which asserts the mappable AS is carried as itself and that RFC 6793 Section 4.2.2's MUST NOT for the AS4_AGGREGATOR is honoured. |
| TestRewriteASPath_AggregatorSameEncoding | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. Successor: `TestPrependKeepsValidAggregatorOnEveryPath` (`aspath_aggregator_probe_test.go`), which this change re-expressed over `ASPathEdit.Record` and which asserts across both prepend paths that a matching-width AGGREGATOR is not named in the edit set at all. |
| TestRewriteASPath_AS4PathWireBytes | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. MOVED: `TestASPathSlotAS4PathWireBytes`, which keeps the byte-exact expectation and its RFC derivation for both attribute VALUES. The attribute headers and the body framing left with the rewrite, because the rebuild writes them. |
| TestRewriteASPath_AS4PathFromNonMappablePathASN | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. Successor: `TestASPathSlotDerivesAS4Path`, which uses the same shape: the non-mappable AS comes from the received AS_PATH rather than from the local AS. |
| TestRewriteASPath_AS4PathSameEncodingASN2 | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. MOVED: `TestASPathSlotDerivesAS4PathFromAnOldSpeakerSource`, the matching-width two-octet case where nothing else forces the derivation to run. |
| TestRewriteASPath_AS4PathPrependedToReceivedAS4Path | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. Successor: `TestRFC6793WellFormedAS4PathNotDiscarded` (`rfc6793_as4_test.go`), which this change re-expressed over `ASPathEdit.Record` and which asserts the local AS ahead of the received AS4_PATH. The exactly-one-AS4_PATH half is structural on the edit set: an accumulator holds one operation per attribute code. |
| TestRewriteASPath_NoAS4PathWhenAllMappable | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. Successor: `TestASPathSlotSuppressesAS4PathWhenNotObliged`, which asserts the same MUST NOT and goes further: a received AS4_PATH is actively suppressed rather than merely not derived. |
| TestRewriteASPath_NoAS4PathToNewSpeaker | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. Successor: `TestRFC6793NoAS4PathBetweenNewSpeakers` (`rfc6793_as4_test.go`), which this change re-expressed over `ASPathEdit.Record` and which asserts neither AS4_PATH nor AS4_AGGREGATOR reaches a four-octet peer. |
| TestRewriteASPath_AS4PathExcludesConfedSegments | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. MOVED: `TestASPathSlotExcludesConfedFromAS4Path`, which asserts the confederation segment stays in AS_PATH and is absent from AS4_PATH, by type and by AS number. |
| TestRFC6793PrependedAS4PathReconstructsTheWholePath | Removed with the code it drove. The whole-payload rewrite it drove (`RewriteASPath`/`RewriteASPathDual`, `aspath_rewrite.go`) was deleted in the same change: `ASPathEdit.Record` replaced it on both forward rails and `plan/journal/unwired-feature.md` recorded on 2026-09-05 that neither entry point had a non-test caller. MOVED: `TestASPathSlotPrependedAS4PathReconstructsTheWholePath`, which keeps the round trip through `attribute.MergeAS4Path` and the same expected reconstruction. |
| TestRFC6793ReceivedConfedInAS4PathDiscarded | Nothing left the suite. The test drove `RewriteASPath`, which Thomas ruled removed on 2026-09-09, and now drives the EBGP prepend rail through the shared helper `rfc6793Prepend`. Its `require.NoError(t, err, "UPDATE processing continues")` moved INTO that helper, which is why the counter sees one assertion fewer in the test body and none fewer in the run. Every other assertion, the RFC requirement tag block and the fixture are byte-identical, and the test was observed RED under a probe that stops `as4PathForPath` returning a path |
| TestRFC6793WellFormedAS4PathNotDiscarded | Nothing left the suite. The test drove `RewriteASPath`, which Thomas ruled removed on 2026-09-09, and now drives the EBGP prepend rail through the shared helper `rfc6793Prepend`, whose own `require.NoError` covers the error check the body used to make. Every remaining assertion, the RFC requirement tag block and the fixture are byte-identical, and the test was observed RED under a probe that stops `as4PathForPath` returning a path |
| TestRFC6793MalformedAS4PathDiscarded | Nothing left the suite. The test drove `RewriteASPath`, which Thomas ruled removed on 2026-09-09, and now drives the EBGP prepend rail through the shared helper `rfc6793Prepend`, whose own `require.NoError` covers the error check the body used to make. Every remaining assertion, the RFC requirement tag block and the fixture are byte-identical, and the test was observed RED under a probe that stops `as4PathForPath` returning a path |
| TestRFC6793NoAS4PathBetweenNewSpeakers | Nothing left the suite. The test drove `RewriteASPath`, which Thomas ruled removed on 2026-09-09, and now drives the EBGP prepend rail through the shared helper `rfc6793Prepend`, whose own `require.NoError` covers the error check the body used to make. Every remaining assertion, the RFC requirement tag block and the fixture are byte-identical, and the test was observed RED under a probe that stops `as4PathForPath` returning a path |
| TestPrependKeepsValidAggregatorOnEveryPath | Re-expressed over `ASPathEdit.Record`, the rail that survives, rather than over the deleted `RewriteASPath`. What it asserts about a valid AGGREGATOR surviving a prepend is unchanged; only the producer it drives moved |
| TestRewriteASPath_MalformedAggregatorTombstone | Retired with the behaviour it witnessed. It asserted that `RewriteASPath` tombstones an AGGREGATOR whose length it cannot read. `ASPathEdit.Record` does not do that: its `recordAggregator` default arm returns and the attribute travels on, which is a REAL loss that predates this commit and is recorded in `plan/journal/unwired-feature.md` rather than hidden here. The transcode rail still tombstones and keeps `TestTranscodeASPath_4to2_MalformedAggregator` |
| buildTombstoneAttr | A helper of the retired tombstone-forward tests, with no reader left once `TestTombstoneCodePointIsUnified` shed its recognition half |
| countAttrOccurrences | Same: a helper whose last reader was a retired tombstone-forward test |
| findAttrInPayload | Same: a helper whose last reader was a retired tombstone-forward test |
| aspath_rewrite_test | The file's tests moved to `aspath_slot_test.go` over `ASPathEdit.Record` with the deletion of `aspath_rewrite.go`. What remains is the five payload builders that `aspath_transcode_test.go`, `rfc6793_as4_test.go` and `tombstone_test.go` still read, so the file stays until Thomas approves the rename, which is a delete plus a create |
| tombstone_forward_test | Now a package clause and a comment. Its two tests witnessed the draft-mangin-idr-attr-tombstone-00 Section 5.3 clear that Thomas ruled removed on 2026-09-09, and its helpers lost their last reader with them. It awaits his `rm` |
