| Test | Reason |
|---|---|
| TestSessionEventDrivesAddressAndPortID | Replaced private session-map, field-copy, and direct packet-builder assertions with decoded UDP Accounting-Start and Stop address and NAS-Port-Id checks across reload. The test now asserts delivered packets rather than internal storage. Its corrected RFC claim has fresh native discrimination. |
| TestSessionTeardownRemovesAddress | Removed the assertion that PPP must issue a separate RemoveRoute call for the kernel's connected point-to-point route. Linux removes that route with the installed address. TestRemovePointToPointAddressAllowsReuse proves address and connected-route removal on Linux; native same-transport LCP restart proves address reuse through PPP. |
| TestIPv6CPNoResponseBeforeNetworkPhase | Removed a zero-value state precondition and the requirement to retain an early packet in a private buffer. Pre-network traffic must be discarded, not replayed into a later lifetime. No-response and no-termination assertions remain; lifecycle tests separately reject stale completion. Native discrimination remains blocked by the canonical manager_test.go duplicate declarations. |
