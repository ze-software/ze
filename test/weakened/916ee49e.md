| Test | Reason |
|---|---|
| TestAcctSessionCallingStationID | Removed the field-copy test that supplied an uninitialized RADIUS client. TestRADIUSAcctStart now drives session IP assignment through the real UDP accounting path and asserts the decoded Calling-Station-Id alongside status and username. The observable packet contract replaces private session-map assertions. |
| acct_test | Assertion count changes from 40 to 39 because two private-map assertions became one decoded packet-attribute assertion in TestRADIUSAcctStart. The full authradius package race run passed; packet identity remains checked through the network boundary. |
