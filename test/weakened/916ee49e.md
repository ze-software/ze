| Test | Reason |
|---|---|
| Emit | Removed the captureBus recorder with the obsolete publish-only forwarding contract. The replacement uses the real event server; an emitted request is not installation acceptance. |
| Subscribe | Removed the captureBus no-op subscription helper. The replacement subscribes through the real server and does not forge native-owner acceptance. |
| lastEntry | Removed assertions about the fake bus envelope and payload type. Replacement checks retain the swap request fields and exercise reservation behavior when native acceptance is absent. |
| TestBusFIBEmitsEntries | Replaced field-copy assertions over unacknowledged events with forwarding-acceptance tests for ingress, transit and egress. TestSwapForwardingRequest retains ordinary swap fields; native generation proof checks installed forwarding and teardown. |
| TestBusFIBNilBus | Removed the incorrect claim that missing native forwarding ownership succeeds. TestForwardingAcceptancePrecedesReservation requires rejection without an owner and reservation only after acceptance. |
| fib_test | The lower assertion count removes fake-envelope plumbing and nil-bus success. Replacement tests defend signaling against unaccepted forwarding, including repeated transit-to-egress conversion. Native packet proof covers replacement and cleanup. |
