| Test | Reason |
|---|---|
| proposed_test | No existing assertion or test was removed. The added `t.Skip` is fixture source supplied to the weakening checker; the new test requires rejection when a cleaned path leaves the archive. |
| parity_test | No existing assertion or test was removed. The added `t.Skip` is fixture source for an archive-to-canonical rename; archived assertions must not supply the canonical baseline. |
| consistency_test | No existing test was weakened. The new unreadable-directory test skips only for root, which can bypass directory permissions. It ran without a skip under the current non-root user and failed against the old walker. |
