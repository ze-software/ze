# Test weakenings this commit accepts

Nothing left the suite. Every row below is the SAME body, moved from
`probe_test.go` to `probe_linux_test.go` under `//go:build linux`, so the
checker reads the departure from one file and cannot see the arrival in the
other. `git show --stat` carries both halves.

The move is what makes these rows run at all. `UDPTransport.SendDF` answers
`probe.ErrDFUnsupported` and writes nothing off Linux, because no other platform
Ze builds for carries `IP_MTU_DISCOVER` or `IP_RECVERR`
(`internal/component/ike/transport/udp_other.go`). Every row below asks the
engine to send a probe, so on darwin each asserted a send the platform declares
absent: they failed, eleven of them after a five-second owner-loop timeout. On
Linux they run exactly as before, in the ordinary unit pass.

| Test | Reason |
|---|---|
| TestPaddedInformationalReachesTheOwnerLoop | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| probeGridSize | Helper, moved verbatim to probe_linux_test.go beside its only callers |
| probeAsk | Helper, moved verbatim to probe_linux_test.go beside its only callers |
| probeReply | Helper, moved verbatim to probe_linux_test.go beside its only callers |
| probeUnanswered | Helper, moved verbatim to probe_linux_test.go beside its only callers |
| probeDeliver | Helper, moved verbatim to probe_linux_test.go beside its only callers |
| probeSentOctets | Helper, moved verbatim to probe_linux_test.go beside its only callers |
| TestPaddedInformationalReachesRequestedSize | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| TestProbeCarriesOneStatusNotifyOnly | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| TestProbeNeverTouchesDPDState | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| dpdAssertUnchanged | Helper, moved verbatim to probe_linux_test.go beside its only callers |
| TestProbeUnansweredFailsTheSALikeDPD | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| TestProbeAnsweredOnDFClearCopyDoesNotFailTheSA | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| TestProbeRetransmitIsBitwiseIdenticalWithDFClear | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| TestProbeSizeCeiling | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| TestProbeRoundsDownToTheCipherGrid | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| TestProbeAdvancesMsgIDLikeDPD | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| TestProbeRefusedWhilstRekeyPending | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| TestErrQueueTooBigTriggersDFClearAtOnce | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| TestZeResponderIgnoresThePrivateStatusNotify | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| TestProbeRefusesANonIPv4Peer | Moved verbatim to probe_linux_test.go. It sends a padded probe, which needs the DF bit Linux alone carries |
| probe_test | The 123 assertions the file lost are the ones the 21 rows above took with them to probe_linux_test.go. The 10 that stay are the leaf-registration row and the rekey-retire row, which send nothing and run on every platform |
