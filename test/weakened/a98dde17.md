# Test weakenings this commit accepts

No test left a suite. Every row below is a RENAME: the Go field that holds a
command's long explanation was `LongHelp` and is now `Description`, the field
that holds its one-line summary was `Description` and is now `ShortHelp`, and
each test that named the old field in its own name now names the new one. The
body, the fixture and the assertion of each one are the same function under the
new name, in the same file, in this commit.

| Test | Reason |
|------|--------|
| TestReferenceRPCCarriesLongHelp | Renamed `TestReferenceRPCCarriesDescription` (`internal/component/aihelp/aihelp_test.go`). Same assertion over the same fixture. |
| TestRuntimeTreeCarriesLongHelp | Renamed `TestRuntimeTreeCarriesDescription` (`internal/component/cli/client/long_help_test.go`). Same assertion over the same fixture. |
| TestRevealCandidateExplanationUsesLongHelp | Renamed `TestRevealCandidateExplanationUsesDescription` (`internal/component/cli/model_keys_test.go`). Same assertion over the same fixture. |
| TestCommandRowsCarryLongHelp | Renamed `TestCommandRowsCarryDescription` (`internal/component/plugin/server/system_test.go`). Same assertion over the same fixture, under the `description` key the row now carries. |
| TestHelpShapeCapsAnEnumButDoesNotDemandALongHelp | Renamed `TestHelpShapeCapsAnEnumButDoesNotDemandADescription` (`internal/le/docvalid/helpshape_schema_test.go`). Same assertion. |
| TestHelpShapeRefusesAConfigLeafWithNoLongHelp | Renamed `TestHelpShapeRefusesAConfigLeafWithNoDescription` (`internal/le/docvalid/helpshape_schema_test.go`). The refusal it asserts is now spelled `missing-description`. |
| TestHelpShapeRefusesAConfigNodeWithNoDescription | Renamed `TestHelpShapeRefusesAConfigNodeWithNoSummary` (`internal/le/docvalid/helpshape_schema_test.go`). It asserts the refusal of a node with no SUMMARY, which the old name called a description; the refusal is now spelled `missing-short-help`. |
| localWithNoLongHelp | Helper renamed `localWithNoDescription` (`internal/le/docvalid/helpshape_test.go`). Not a test; the fixture builder the three local-command tests below share. |
| TestHelpShapeRefusesACommandWithNoLongHelp | Renamed `TestHelpShapeRefusesACommandWithNoDescription` (`internal/le/docvalid/helpshape_test.go`). Same assertion, refusal spelled `missing-description`. |
| TestHelpShapeRefusesAnRPCWithNoLongHelp | Renamed `TestHelpShapeRefusesAnRPCWithNoDescription` (`internal/le/docvalid/helpshape_test.go`). Same assertion, refusal spelled `missing-description`. |
| TestHelpShapeRefusesALocalWithNoLongHelp | Renamed `TestHelpShapeRefusesALocalWithNoDescription` (`internal/le/docvalid/helpshape_test.go`). Same assertion, refusal spelled `missing-description`. |
| TestWriteEditWarnsOnAYangHelpThatRestatesItsDescription | Renamed `TestWriteEditWarnsOnAYangDescriptionThatRestatesItsShortHelp` (`internal/le/hookruntime/writeedit_test.go`). Same fixture: the two texts equal; the warning is now spelled `description-restates-short-help`. |
| TestWriteEditWarnsOnAYangDescriptionPastTheCharCap | Renamed `TestWriteEditWarnsOnAYangShortHelpPastTheCharCap` (`internal/le/hookruntime/writeedit_test.go`). The hook caps the SUMMARY, which the YANG now spells `ze:help`; same 97-character fixture, same `char-cap` assertion. |
| TestWriteEditWarnsOnAYangDescriptionPastTheWordCap | Renamed `TestWriteEditWarnsOnAYangShortHelpPastTheWordCap` (`internal/le/hookruntime/writeedit_test.go`). Same fixture, same `word-cap` assertion. |
| TestWriteEditWarnsOnAYangDescriptionWithNoFullStop | Renamed `TestWriteEditWarnsOnAYangShortHelpWithNoFullStop` (`internal/le/hookruntime/writeedit_test.go`). Same fixture, same assertion. |
| TestWriteEditWarnsOnAYangDescriptionCarryingASemicolon | Renamed `TestWriteEditWarnsOnAYangShortHelpCarryingASemicolon` (`internal/le/hookruntime/writeedit_test.go`). Same fixture, same assertion. |
