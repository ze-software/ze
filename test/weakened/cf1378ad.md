| Test | Reason |
|------|--------|
| discharge | Nothing left the suite: the helper's body MOVED, the same way `debtRowFor` below did. `discharge` now calls a new `dischargeAs`, which holds the same `t.Fatalf` on a parse failure and the same call into `dischargeDebt`, so the detector counts one assertion off `discharge` and does not count the one that arrived on the function beside it. The extraction was needed because a fixture must now write two records for ONE debt row under two DIFFERENT session names, which is how two sessions reach one row, and `discharge` had the session hardcoded. That case is what proves an `owner` attestation is not masked by a later derivation, which R-3 exists to keep visible. |
