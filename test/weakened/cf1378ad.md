| Test | Reason |
|------|--------|
| debtRowFor | Nothing left the suite: the helper's body MOVED. `debtRowFor` now calls a new `debtRowRecord`, which holds the same `t.Fatalf` and the same ledger read, so the detector counts three assertions off `debtRowFor` and does not count the three that arrived on the function beside it. The extraction was needed because a fixture must now build a row covering SEVERAL commits, which means recording the same gate and reason twice so `recordDebt` extends one row rather than appending two. The lookup got stricter while it moved: it matched on gate and subject, and an extended row's subject cell carries `(+N more)` and no longer equals what was written, so it now matches on gate and reason, which `recordDebt` treats as the row's identity. A fixture that extended a row would have found no row under the old predicate and read a `t.Fatalf` as the answer. |
