| Test | Reason |
|------|--------|
| TestNotApplicableDischargeReadsRFCTagCarriersAtTheCommit | Deleted, approved by Thomas in `test/rfc-changed/cf1378ad.md`. The criterion it named did not leave the suite: AC-4 is proven in both polarities by `TestNotApplicableDischargeReadsTaggedUnitsAtTheCommitParent` (`internal/le/commit/discharge_rfc_test.go`), asserting the same two facts on the same producer, `changedTaggedUnits` reached through `verifyKindNotApplicable`'s `gateRFCChangeOK` branch. This one proved NEITHER: it called `commitFixture` before `debtRowFor`, so the commit it named never carried the ledger shard the commit-to-row binding requires, and it fatalled at the ADMITTING case without ever reaching the refusing half. Its own repair was frozen by its fixture string, a `rfc9999-1` that names no real RFC and is cited by nothing in `rfc/`, because `rfcTagPattern` matches raw text and read the function itself as a tag carrier. The replacement assembles the tag, so it cannot freeze itself. |
| discharge_test | Three assertions left the FILE and the criterion they covered did not leave the suite. `TestNotApplicableDischargeReadsRFCTagCarriersAtTheCommit` was deleted, approved by Thomas in `test/rfc-changed/cf1378ad.md`, and AC-4 is proven in both polarities by `TestNotApplicableDischargeReadsTaggedUnitsAtTheCommitParent` (`internal/le/commit/discharge_rfc_test.go`), which asserts the same two facts: a commit changing no tagged unit discharges, and a commit changing one is refused naming the unit. The deleted test could not prove either. It called `commitFixture` before `debtRowFor`, so the commit it named never carried the ledger shard the commit-to-row binding requires, and it fatalled at the ADMITTING case without ever reaching the refusing half. Its own repair was frozen by its fixture string: the `rfc9999-1` inside it names no real RFC and is cited by nothing in `rfc/`, but `rfcTagPattern` matches raw text, so the hook read the function as a tagged unit and refused every edit to it. |
