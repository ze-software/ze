# Test weakenings this commit accepts

One test is RENAMED, not removed. It asserted the one-wildcard-SA model that
commit `fed1c8f3c0` retired, and its new name says what it asserts now.

| Test | Reason |
|------|--------|
| TestIPsecSAIsWildcardWithOSPFSelector | Renamed `TestIPsecSAPerDestinationWithOSPFSelector` in the same file. It asserted one state under `::`, which no OSPF flow could resolve; it now asserts the same SPI, key and {::/0, ::/0, proto 89} selector on every per-destination state, and that the multicast states carry no direction. The owner's row for the tag change is in `test/rfc-changed/c9a630a5.md` |
