# Test weakenings this commit accepts

The FIB integration tests (`internal/plugins/fib/kernel/integration_linux_test.go`)
gained a kernel forwarding-decision observation per test; one helper changed
shape on the way.

| Test | Reason |
|------|--------|
| addProtocolRoute | The three `require` calls moved into `addProtocolRouteWithMetric`, which it now delegates to with metric 0, so the metric test can pre-install a competing route at metric 20 through the same helper. Nothing it asserted left the suite |
