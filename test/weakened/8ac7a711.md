| Test | Reason |
|------|--------|
| reload-rapid-sighup | The assertion is INVERTED, and the behaviour under it changed on purpose: a route-set edit used to restart the peer and now swaps on the running session. It asserted two connections and both routes re-sent on connection 2, because its FIRST SIGHUP added a route and that used to restart the peer. It now asserts one connection, the added route on the established session, and the second SIGHUP left where it was as the no-op reload. What it exists to prove is unchanged and better fenced: two reloads in a row leave the daemon alive and the session up. The `reject=stderr:pattern=peer restart required` rule is new and covers BOTH reloads, where the old fixture could only count connections and so could not tell a legitimate restart from a spurious one. |
| persist-across-restart | The same inversion, for the same reason as `reload-rapid-sighup` above. Its route-set reload restarted the peer, so it asserted two connections and both routes re-sent; it now asserts one connection and the added route on the running session, with the SIGTERM moved to that connection. Its subject is the persist plugin surviving a reload, and that is asserted more strictly than before: the session it must survive is now the SAME session throughout, so a plugin that breaks the connection fails the fixture where a reconnect used to hide it. |
