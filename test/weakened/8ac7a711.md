| Test | Reason |
|------|--------|
| TestParseUpdateText_PathIDRejectTopLevel | The test PINNED the gap this commit closes. `path-information` was refused at the top level of `update text` while `rd` and `label`, the two sibling NLRI modifiers, were accepted in both positions; `ParseUpdateText` had a `case kwRD:` and a `case kwLabel:` and no `case kwPathInfo:`, so the token fell to the default and was named as unexpected. The asymmetry was the defect, and a test asserting the refusal asserted the defect. It is replaced in the same file by `TestParseUpdateText_PathIDBothPositionsAgree`, which is strictly stronger: it drives BOTH positions and asserts they set the same path identifier, so the old refusal cannot return without it going red, and neither can the two positions diverge. RFC 7911 Section 3 governs the four-octet field, and `test/exabgp-compat/api/api-attributes-path.ci` carries the wire bytes the new test asserts against. |
