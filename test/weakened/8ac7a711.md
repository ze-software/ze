| Test | Reason |
|------|--------|
| TestNeighborCreateIsRefusedByName | The test asserted a REFUSAL this commit removes the reason for. `create neighbor <ip> ...` was refused because ze created no BGP peer at runtime: `ze-bgp:peer-add` was declared in `internal/component/bgp/yang/ze-bgp-api.yang` with no handler and no CLI path, and the bridge said so by name. That declaration now has its handler (`handleBgpPeerAdd`, `internal/component/bgp/plugins/cmd/peer/create.go`) and the command exists, so a test asserting the line is refused would assert a gap that is closed. Three tests replace it in the same file and cover strictly more: `TestNeighborCreateWritesTheZeCreateCommand` pins the translated command, `TestNeighborCreateSelectorIsTheCreatedPeer` pins the selector the flush uses, and `TestNeighborCreateRefusesWhatZeCannotHonour` keeps the refusal for the parameters ze genuinely cannot honour, `family-allowed in-open` among them. The end-to-end proof is `test/exabgp-compat/api/api-peer-lifecycle.ci`, whose script creates the only peer over the API and which now passes. |
| TestMigrateCompatFixturesRefusingMultiSession | Renamed, and widened in the same edit. It asserted that the migration refuses a config asking for `multi-session`; it is now `TestMigrateCompatFixturesRefusingUnimplemented` in the same file, and it asserts that the refusal names EVERY unimplemented capability the config asks for rather than the first one it meets. api-open.conf asks for `multi-session` AND `operational`, and the old refusal named only the first, so an operator learned about the second by running the command again. The new test would go red against the old behaviour, which the old test would not: breaking `refuseUnimplementedCapabilities` to return on its first match reddens the api-open case with `the refusal does not name "operational"`. Nothing is refused less than before, and the two drafts remain unimplemented. |
