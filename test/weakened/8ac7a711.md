| Test | Reason |
|------|--------|
| TestMigrationRefusesUnsupportedCap | The assertion is INVERTED, and the behaviour under it changed on purpose. It asserted `ze exabgp migrate` errors on `capability { multi-session; }` or `{ operational; }`; it is now `TestMigrationWarnsUnsupportedCap` in the same file and asserts the migration converts the config, names the keyword in a warning that also names the peer, and leaves the keyword out of the output. The refusal converted NOTHING for an operator whose only unconvertible line was one expired draft, and it was also incomplete as a guard: `aigp enable` was taken off the refusal list when ze implemented the AIGP codec and no translation was put in its place, so that keyword was dropped in silence (`plan/journal/silent-fall-through.md`). The warning is derived rather than listed -- `migrateCapability` declares the keywords it translates and `untranslatedCapabilities` warns about every other key in the block -- so `aigp`, and any keyword ExaBGP grows next, is now named too. Nothing is negotiated less than before: ze never sent either capability. The end-to-end proof is `test/exabgp-compat/api/api-multisession.ci` and `api-open.ci`, whose configs the migration refused outright and now converts. |
| TestMigrateCompatFixturesRefusingUnimplemented | Same change, same reason, in the fixture test: it is now `TestMigrateCompatFixturesWarningUnimplemented` and asserts on `result.Warnings` where it asserted on the error. It gained an assertion rather than losing one: the migrated config is serialized and checked to NOT carry the keyword, which the refusal test never reached. Its `PREVENTS` clause named "the refusal being softened into a warning" as a failure to prevent, and that clause is what this row overturns: a warning that reaches the operator's stderr is not the silent drop `ai/rules/principles.md` names, and the refusal's own cost was a config that would not convert at all. |
