| Test | Reason |
|---|---|
| TestLocalAuthHandlerType | Owner: "Approve correction". Delete the contradictory empty-table no-auth rejection test. Explicit no-auth admission is the approved policy; `TestLocalAuthMethodNoneAccepted` now uses the real username-less request. PAP/CHAP credential rejection and `TestRunAuthPhaseRequiredAuthRejectsNoMethod` retain the credential and downgrade guards. |
| TestLocalAuthMethodNoneAccepted | Owner: "Approve correction". Remove the fabricated local user and username. Real no-auth requests contain neither. The acceptance assertion remains and failed against the old credential-first implementation. |
| auth_test | Owner: "Approve correction". The assertion count falls from 8 to 7 because `TestLocalAuthHandlerType` asserted the rejected policy. The corrected username-less acceptance regression and existing PAP/CHAP rejection assertions preserve the approved contract. |
