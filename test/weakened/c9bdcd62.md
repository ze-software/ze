# Test weakenings this commit accepts

| Test | Reason |
|------|--------|
| rfc6793_as4_test | The whole file, `internal/component/bgp/plugins/rib/storage/rfc6793_as4_test.go`, deleted with the owner's approval on 2026-09-13. It holds four `[]byte` wire fixtures and NO test function, no helper and no assertion: the tests that read them were removed by `58e79f57e2`, which moved the RFC 6793 Section 4.2.3 reconstruction to `internal/core/bgp/attribute` and verified each successor by name. All four names are live in `internal/core/bgp/attribute/rfc6793_reconcile_test.go` with 3 to 7 uses each (`rfc6793WireASPathWithASTrans` 5, `rfc6793WireAS4Path` 5, `rfc6793WireAggregatorASTrans` 3, `rfc6793WireAS4Aggregator` 7), so nothing the suite demonstrates moves. Nothing referenced them here, which is why golangci-lint reported all four as `unused` and they were the ONLY lint findings in the whole sweep: eight flavor passes reported 0 issues and the default and GOOS=linux/integration passes reported exactly these four. |
