# Test weakenings this commit accepts

| Test | Reason |
|------|--------|
| withCurrentRFCIndexCommand | Deleted with the difference it existed to paper over. It patched one phrase in the frozen `published-blog-reference` fixtures where gh-pages `2fa8fa2ad` (2026-08-29) still carried an older RFC index command than the renderer emits, so the comparison could pass without the fixture being right. The fixtures are now refreshed to the current published state, which `origin/gh-pages` HEAD `7a71f67208` carries byte-identically, so the phrase agrees and a corrector between the two sides would hide the next real divergence rather than reveal it. No assertion is lost: both call sites compare the same two things they compared before, now without a patch in between, which is a STRONGER comparison. `TestAnArticleRendersItsKeyPointsAndProseNumbers` is added in the same change to hold the key-points and `{{ze:}}` assertions that left the reference article when `7dffbf36ea` deleted its front matter, so those did not go with it. |
