# Test weakenings this commit accepts

| Test | Reason |
|------|--------|
| awkPrograms | A helper in `docker/lab_image_tags_test.go`, not a test and holding no assertion: it extracted the awk program each Dockerfile inlined to read `feature-gates.txt`. Commit be0133897c collapsed nineteen copies of that walk into one shell reader, `feature-tags`, so no recipe inlines an awk program any more and the helper answers an empty list for every file. Its two callers are replaced by `derivesFeatureSet`, which asserts the property the helper was a proxy for: the recipe sources `feature-tags` and reads `feature-gates.txt` through it, so its feature set is derived rather than spelled. The suite is stronger for it, because the old rows pinned the SHAPE of the walk and would have gone red on any correct replacement. The third row also demanded that inline awk of `test/interop/Dockerfile.ze`, which compiles nothing and never carried one; it could not pass and is replaced by the condition that makes the derivation owed, the recipe starting to compile. |
