# Test weakenings this commit accepts

| Test | Reason |
|------|--------|
| TestProbeUnclaimedRoutes | Deleted with its file, `internal/le/site/zzprobe_p10b_test.go`. It holds no assertion ABOUT the site. It carries seven `t.Fatal` calls and every one is error handling on its own I/O (`docs pages: %v`, `published routes: %v`, and five bare `t.Fatal(err)`), so the only way it can fail is its own setup failing; what it learns about the product it writes with `t.Logf`, which cannot fail a run. An earlier draft of this row said "every statement is a t.Logf". That was wrong, repeated from a report without reading the file, and the corrected reason is the one above. It is red because it copies `tmp/session/2026-08-29-36ab4fc7-.../scratch`, a session directory that no longer exists. Its own comment says "It is deleted before the phase ends", so it is committed scratch rather than a test anyone wrote to hold a property. Owner approved the deletion on 2026-09-13. |
| TestScratchProducerCensus | Deleted with its file, `internal/le/site/zzscratch_census_test.go`, on the same grounds and approval. Its three `t.Fatal` calls are `pages: %v`, `mkdir: %v` and `copy %s: %v`: setup errors, not product assertions. The `copy` is the one that fails, against the same dead scratch tree. |
| TestScratchDocsDest | Deleted with `zzscratch_census_test.go`. Same file, same shape: no assertion about the site, failure only through its own I/O. |
| TestProbeSharedHeader | Deleted with `zzprobe_p10b_test.go`. Same file, same shape. |
| TestProbeAuthoredPages | Deleted with `zzprobe_p10b_test.go`. Same file, same shape. The two files are one piece of committed scratch carrying five functions, not five tests anybody relies on: `copyScratchTree` and `itoa` are referenced nowhere outside them and go with them. |
| itoa | A three-line integer-to-string helper inside `internal/le/site/zzscratch_census_test.go`, deleted with the file. It is not a test and holds no assertion; the gate names it because it lives in a `_test.go` file the commit removes. Its only callers are the `t.Logf` lines in the two probe files, which go with it. `strconv.Itoa` is what the rest of the package uses. |
| copyScratchTree | A helper inside the same file that copies a scratch directory tree, deleted with it. Not a test and holds no assertion. Its only caller is the probe that copies `tmp/session/2026-08-29-36ab4fc7-.../scratch`, the dead directory that makes both files red, and it is referenced nowhere outside the two probe files. |
