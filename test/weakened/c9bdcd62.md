# Test weakenings this commit accepts

| Test | Reason |
|------|--------|
| personalityFeatureTags | The helper's own `bufio` walk over `feature-gates.txt` is deleted, and with it the scanner error check the walk owned. Nothing it asserted is lost: the helper answers the same tag list, now from `featuretags.DaemonTags(root)`, which is the one reader this commit leaves standing and which carries the refusals the deleted lines carried plus two the helper never had, a malformed line and an unclean package path. The test the helper serves is unchanged and still asserts that `ze_le` is a PERSONALITY and never a gate, so a manifest declaring it would put le's own commands in every shipped binary. Verified: the package is green with the full 37-tag set, and `shellagreement_test.go` holds the Go reader against the shell one. |
| readFeatureGateTags | The helper's own `os.ReadFile` walk over `feature-gates.txt` is deleted, and with it the line-splitting and the read error it owned. Nothing it asserted is lost: it answers the same tag column, now from `featuretags.DaemonTags`. Its old comment is the defect stated by its own author, "Mirrors the reader in internal/test/runner so both stay in sync with the manifest format", which asks a human to keep two parsers in step. The test it serves is unchanged and still judges the gokrazy config against the manifest; what changes is that it now judges against the manifest rather than against its own idea of it. |
| gatedYANGModules | The open-coded `bufio` walk in `internal/component/plugin/all/config_claims_test.go` is deleted, including its own field-count check for a malformed line. Nothing is lost: `featuretags.Gates(root)` answers the same `<tag> <package>` rows and carries that refusal plus one the walk never had, an unclean package path. The test is unchanged and still asserts every gated plugin package is reachable from the composition root. |
