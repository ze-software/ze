# Test weakenings this commit accepts

| Test | Reason |
|------|--------|
| TestNewBlobSelfHealsCorruptStore | Automatic destructive self-healing is removed by AC-2/D-6. TestOpenBlobRefusesCorruptionWithoutSelfHealing proves refusal preserves corrupt bytes and creates no archive. |
| TestNewBlobOpensValidStoreInPlace | TestBlobArtifactPersistenceAndBasenameMapping creates, closes, reopens, and checks persisted artifact content without overwriting it. |
| TestBlobStorageListFilesystemDirectory | Shared conformance raw and config lists cover filesystem-directory input and immediate full-key results for both encodings. |
| TestResolveDirKey | Removed helper-shape assertions; shared conformance checks observable directory listing and returned-key readback. Tree paths outside the owned folder are explicitly refused. |
| pointerTestStores | Construction checks moved into newTreeStorage and newBlobStorageAt; both require successful construction and checked cleanup. |
| newBlobStorage | Construction and cleanup checks are in newBlobStorageAt, which this helper calls. |
| TestFilesystemStorageReadWrite | Shared conformance operations covers create, larger replacement, readback, rename, and removal on the tree and artifact. |
| TestFilesystemStorageCreatesDirs | TestCreatePrivatePermissionsAndReopen writes meta/deep/key beneath a newly created nested config directory and checks all node modes. |
| TestFilesystemStorageExists | Shared conformance operations checks absent, present after write, and absent after removal. |
| TestFilesystemStorageList | Shared conformance raw and config lists checks exact immediate-child and recursive key populations. |
| TestFilesystemStorageAtomicWrite | Shared conformance operations checks longer replacement content. Private publication tests cover retained parent descriptors and barriers; abandoned private staging outside the namespace is now permitted by D-9. |
| TestFilesystemStorageLockCycle | Shared conformance guard covers read, write, listing pending writes, removal, and release. Lifetime owner locking replaces the obsolete no-lock-file implementation assumption. |
| TestFilesystemStorageGuardDoubleRelease | Shared conformance guard requires two successful releases and refuses use after release. |
| TestFilesystemStorageReadNonExistent | Shared conformance operations requires fs.ErrNotExist from missing reads on both encodings. |
| TestFilesystemStorageRemoveNonExistent | Shared conformance operations requires fs.ErrNotExist from missing removal on both encodings. |
| TestFilesystemStorageDefaultPerm | TestCreatePrivatePermissionsAndReopen checks 0600 frames and 0700 directories, even when the caller requests 0777. |
| TestBlobStorageReadWrite | Shared conformance operations retains byte-exact write, larger replacement, rename, and remove checks for blob artifacts. |
| TestBlobStorageMultiConfig | Shared conformance operations verifies independent contents of the renamed config and other.conf. |
| TestBlobStorageExists | Shared conformance operations checks absent, present, and removed keys on artifacts. |
| TestBlobStorageList | Shared conformance raw and config lists asserts exact keys including drafts and non-config files. |
| TestBlobStorageLockCycle | Shared conformance guard preserves guarded read/write/remove and metadata behavior on artifacts. |
| TestBlobStorageMigration | Implicit loose-file migration is deliberately removed. TestCreateBlobDoesNotImportLooseFiles proves the loose file remains unchanged and the artifact stays empty. Explicit import has separate recovery tests. |
| TestBlobStorageFallback | The removed NewBlob constructor and fallback no longer exist. Explicit open/create and missing-store refusals are tested under the new contract. |
| TestBlobStorageReadNonExistent | Shared conformance operations requires fs.ErrNotExist on missing artifact reads. |
| TestBlobStorageGuardDoubleRelease | Shared conformance guard checks idempotent release and closed-guard refusal on artifacts. |
| TestBlobStorageCrossProcessLock | The old test used two goroutines, not processes. Shared conformance guard serialization retains the ordered final-value assertion; owner-contention tests separately cover independent handles. |
| TestBlobStorageListConfigs | Shared conformance lists verifies all returned keys and payloads. The old test performed its own suffix filtering; that local filtering was not a storage API contract. |
| TestBlobStorageFilePrefix | Shared conformance writes namespaced keys and reads returned full keys unchanged; artifact persistence also tests basename mapping. |
| TestResolveKeyIdempotent | Helper-specific mapping assertions are replaced by shared full-key write/list/readback and strict outside-folder refusal. |
| TestBlobStorageListReturnsFullKeys | Shared conformance raw and config lists asserts exact file/active and meta key strings. |
| TestBlobStorageListRoundTrip | Shared conformance reads every listed immediate key unchanged and compares its exact payload. |
| TestBlobMigrateFilesystemPrefixed | Implicit migration is prohibited by the new contract; TestCreateBlobDoesNotImportLooseFiles asserts no import or loose-file modification. |
| TestFilesystemStorageRename | Shared conformance operations checks old-name disappearance, new-name payload, and unrelated config preservation. |
| TestFilesystemStorageRenameNonExistent | Shared conformance operations requires fs.ErrNotExist for a missing rename source. |
| TestBlobStorageRename | Shared conformance operations checks artifact rename payload and old-name disappearance. |
| TestFilesystemWriteVersionAndList | Shared conformance versions and metadata checks exact newest-first stamps, paths, and payloads on the tree. |
| TestFilesystemListVersionsEmpty | Shared conformance versions and metadata first requires an empty version list. |
| TestBlobWriteVersionAndList | Shared conformance versions and metadata checks exact newest-first stamps, paths, and both artifact payloads. |
| TestBlobListVersionsEmpty | Shared conformance versions and metadata requires an initially empty artifact version list. |
| TestBlobWriteVersionViaGuard | Shared conformance writes the newer version through a guard and reads its exact payload and modifier afterward. |
| TestParseVersionStampRejectsTraversal | TestParseVersionStampRejectsInvalid includes the same traversal input and requires an error. |
| TestParseVersionStampRejectsOversizeMillis | TestParseVersionStampRejectsInvalid retains the oversize-millisecond refusal. The old exact error-wording assertion is removed. |
| TestApplyEvolutionsBackupCreated | The loose rollback-directory setup and location assertion are obsolete; this same test now lists stored versions and reads them through Storage, still requiring the original 26.05.01 schema stamp before evolved write-back. |
| TestApplyEvolutionsDataUpdated | Only the loose rollback-directory creation/error assertion was removed from setup. This test still requires changed outcome bytes and the exact evolved schema stamp with an explicit-file source bound to the tree store. |
| TestApplyEvolutionsWriteBackMatchesDisk | The removed assertion checked creation of the obsolete loose rollback directory. Consumer coverage remains in this test: applyEvolutions returns bytes exactly matching the explicit config file on disk. |
| TestBuildLGServiceEmptyNameKeepsStorageRules | The removed error substring required the obsolete blob-only certificate contract. The same test still requires explicit TLS without storage to fail with no service, and defaulted TLS without storage to start; nil now represents unavailable storage. |
| TestBootPowerUsersIsSilentWhenAdminIsDisabled | The removed Close success assertion was fixture preparation for reopening a raw blob. The caller now supplies its live tree owner; this test retains readable credential/admin-disabled setup and asserts both no users and no warning output. |
| TestOpenStateOnlyStoreNeedsConfigDir | The removed helper and its empty-directory sentinel belonged to independently opening a state-only blob. TestStdinAbsentStoreCreatesNothing covers the replacement absent-store contract by requiring ErrNoStore, a nil handle and an unchanged directory; it does not exercise empty-directory daemon startup. |
| TestOpenStateOnlyStoreCreatesAndReopens | Independent database.zefs creation is obsolete. TestDaemonStateSharesConfigStore writes state through statestore.Put, writes config through the same owner, closes/reopens the live tree and requires the exact surviving state bytes. |
| TestOpenStateOnlyStoreDoesNotMigrateConfig | The state-only blob helper is removed. TestStdinAbsentStoreCreatesNothing requires absent-store inspection to leave the loose config alone; TestExplicitSourceRestartUsesFile separately proves an offline explicit-file edit wins over old stored content on restart. |
| TestOpenStateOnlyStoreIgnoresConfigBackendToggle | The ze.storage.blob toggle and filesystem-to-blob extraction premise are obsolete. TestDaemonStateSharesConfigStore proves state remains durable through config writes and reopen under the single live-tree owner; the removed absent-key assertion is not reproduced in that hub test. |
| statestore_test | The four state-only blob/helper cases are replaced by TestDaemonStateSharesConfigStore and TestStdinAbsentStoreCreatesNothing. They assert surviving shared-owner state and absent-store non-creation; blob filename, backend-toggle and helper-sentinel expectations retire with their APIs. |
| TestRunnerBlobStorage | TestRunnerTreeStorage replaces blob selection with the live tree and retains edit/dirty/error assertions while adding a decoded persistent-draft assertion for file/active/test.conf.change.thomas. |
| TestRunnerBlobStorageWritesBlobNotFile | TestRunnerTreeStorageWritesTreeNotFile retains the committed edit assertion and both loose-fixture assertions: the original address remains and the edited address is absent. It reads the committed address from the tree through OpenReadOnly. |
| TestRunnerBlobStorageRefusesFileExpectation | The blob-specific rejection contract is intentionally removed. The documented runner contract permits expect=file for loose fixtures/exports and expect=key for persisted values; TestRunnerTreeStorage checks a stored draft and TestRunnerTreeStorageWritesTreeNotFile distinguishes committed tree content from the unchanged fixture. |
| runner_test | Blob editor and commit cases become TestRunnerTreeStorage and TestRunnerTreeStorageWritesTreeNotFile, with persistent draft/commit and unchanged-fixture assertions. The removed file-expectation refusal is obsolete under the documented file/key distinction; unknown backend rejection remains tested. |
| TestDefaultConfigName | The removed assertion pinned the private fallbackConfigName constant. This test now calls resolve.DefaultConfig and still asserts ze.conf for nil storage and missing identity, plus ze-first.conf for the stored identity. |
| TestEditRefusesWithoutZefs | TestEditRefusesWithoutStore retains the observable error-exit contract with a nil store and explicit config path. The filesystem-store fallback fixture and blob-only admission rule are obsolete; neither old nor replacement test asserts the diagnostic wording. |
| TestResolveSSHStorage | All three removed subcases asserted backend classification or implicit blob reopening, which the shared-owner contract removes. TestResolveHostKeyFilesystemMode/tree storage instead exercises the SSH consumer and requires a persisted PEM private key through the supplied tree and no loose .pub file. |
| ssh_test | The removed TestResolveSSHStorage aggregate covers obsolete blob pass-through, filesystem upgrade and filesystem retention decisions. SSH now receives its caller's store; TestResolveHostKeyFilesystemMode/tree storage covers persistent host-key consumption of that tree. |
| TestResolveStorageWithDiag_Fallback | Always returning usable fallback storage without error diagnostics is intentionally retired. TestResolveStorageWithDiag_ReadOnly requires a valid read-only handle and ErrReadOnly on mutation; TestStorageOpenDiagnostics requires named missing/unsafe-store diagnostics and no implicit tree creation. |
| TestTerminalCompareRollbackScopesToShowPath | The removed MkdirAll assertion was loose rollback-fixture setup. History is now seeded as file/<stamp>/<name> through the manager's Storage; this test retains exact rollback/current router-id checks and exclusion of unrelated interface/uplink output. |
| TestDoctorReportsUnusableWebTLSPair | Removed assertions configured/restored the obsolete backend toggle and created loose certificate directories. The tree fixture now receives cert/key values through WriteKey; the test still invokes diagnostic.RunDoctorChecks and requires the unusable-pair TLS diagnostic through the registered doctor provider. |
| tempStorePath | Replaced by tempStore, which creates one owned tree Storage and closes it in cleanup; caller tests no longer open independent blob handles. |
| TestPrefixStoreRefresh | Retains IPv4/IPv6 counts, populated cache and meta/irr/AS-TEST existence through Storage; only direct blob reopen setup is removed. Independent daemon restart persistence is exercised by irr-cache-smoke/results/summary.json; this unit does not reopen the storage owner. |
| TestMigrateOldCache | Retains migrated AS-SET and family counts, legacy-key removal and per-entry-key creation through Storage; direct blob reopen setup is removed. Independent daemon restart persistence is exercised by irr-cache-smoke/results/summary.json; this unit does not reopen the storage owner. |
| TestMigrateMultiPrefix | Same test still seeds three valid IPv4 prefixes plus JUNK and two IPv6 prefixes, then asserts counts 3 and 2; only blob-handle setup becomes owned Storage. |
| TestPrefixStoreOpenNameKeyMismatch | Same test still rejects the tampered entry under both claimed and actual names while loading AS64500; only seed writes move to owned Storage. |
| support.TestMain | Only registered the retired ze.storage.blob switch and ran tests; crash kind, content and readiness assertions remain in TestSupportCrashesModuleCarriesKernelReadiness and TestSupportCrashesModuleWithNoReports. |
| crashes.TestMain | Only registered the retired ze.storage.blob switch and ran tests; unreadable/configured readiness, renderable fields and offline crash-kind assertions remain in readiness_test.go. |
| TestResolveDBPath_HonorsConfigDirEnv | Replaced by TestResolveStoreDirHonorsConfigDirEnv, which sets ze.config.dir and asserts ResolveStoreDir returns that exact directory; appending database.zefs is obsolete. |
| seedSSHDefaultDB | Blob fixture for retired banner-based ownership detection; TestInitForceRefusesOwnerRegardlessTarget and TestInitForceIgnoresListenersWithoutOwner seed live-tree SSH metadata directly. |
| startBannerListener | Banner-emitting fixture is obsolete because init no longer infers ownership from SSH; TestInitForceIgnoresListenersWithoutOwner uses an unrelated silent TCP listener. |
| TestDaemonRunningIgnoresNonZeListener | Banner classification is intentionally removed; TestInitForceIgnoresListenersWithoutOwner asserts successful forced replacement despite an unrelated listener. |
| TestDaemonRunningAcceptsZeBanner | A Ze SSH banner is no longer ownership evidence; TestInitForceRefusesOwnerRegardlessTarget asserts forced-init refusal and unchanged credentials with a real owner and no local listener. |
| TestDaemonRunningFalseWhenPortUnreachable | Unreachable SSH no longer authorizes replacement; TestInitForceRefusesOwnerRegardlessTarget points metadata at an unreachable remote target and still asserts owner refusal and unchanged metadata. |
| TestDaemonRunningTimesOutOnSilentListener | The bounded banner-probe contract is retired with the probe; TestInitForceIgnoresListenersWithoutOwner asserts successful init against a listener that never accepts. |
| newFakeBootStore | Replaced by newDaemonStateClient backed by tree Storage; TestBootCountMonotonicAcrossRestart retains increasing counts and exact first=1, third=3 assertions. |
| ospf.ReadFile | Retired fake file-store methods become StateGet; TestBootCountMonotonicAcrossRestart retains counter assertions, and GR tests retain fact roundtrip, expiry, clearing and absent-record assertions. |
| ospf.WriteFile | Retired fake file-store methods become StatePut/StateIncrement; TestBootCountMonotonicAcrossRestart retains counts 1 through 3, while TestRestartFactPersistsAcrossRestart retains deadline, reason, adjacency, interface-ID and prefix-LSA-ID checks. |
| TestBootCountNilStoreFallsBack | Clock fallback is intentionally removed: TestBootCountNilStoreRefused asserts an error and zero count, and TestOSPFStateFailureRefusesStartupAndPrepare rejects startup and prepared restart after state failure. |
| TestOSPFStateDirRequiresAPinnedConfigDir | Plugin-local directory resolution and clock degradation are retired; TestOSPFStateInitializationAfterConfigure asserts no counter before initialization and packet boot words 1,1,2, while TestBootCountNilStoreRefused rejects missing state. |
| TestOSPFStoreOpenersHonorTheGate | The two plugin-local openers are retired in favor of daemon state; TestOSPFStateInitializationAfterConfigure and TestOSPFStateFailureRefusesStartupAndPrepare cover deferred initialization and repeated startup/prepare refusal on state errors. |
| chown | Fake helper becomes transferOwnership for one locked tree transfer; TestServiceInstallGeneratesUnit retains target-account transfer coverage and TestServiceInstallRefusesOwnedStore asserts no unit or systemctl action after transfer refusal. |
| TestApplySnapshotsOriginalBeforeReplace | Drops the exact call trace but retains in-memory and stored original fq checks; TestNetlinkIntegration_RestoreOriginalQdiscAfterRestart observes kernel fq→htb→fq using the stored snapshot, and refusal tests retain pre-replacement rejection checks. |
| equalStringSlices | Removed with exact call-trace comparisons; TestRestoreOriginalUsesSnapshotNotFQCodelDefault and TestPersistedSnapshotSurvivesBackendRestart now compare restored qdisc parameters, with persisted snapshot equality in the restart case. |
| TestZeDaemonShouldForceFileStorage | The plain-versus-web file/blob switch is intentionally removed because live storage is always a tree; TestZeDaemonConfigArgIndex retains daemon/source argument routing for plain, web and subcommand forms. |
| runner_exec_test | Aggregate reduction is the retired TestZeDaemonShouldForceFileStorage only; inspected remaining argument-index, readiness-enable and child UID/GID assertions are retained. |
| doctor-store-self-heal | Implicit quarantine/recreation is intentionally forbidden; doctor-store-refuses-legacy-blob.ci asserts exit 1, doctor-storage-unavailable, preserved malformed seed, no replacement archive and no live-tree contents. |
