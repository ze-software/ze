# Test weakenings this commit accepts

None.

The three rows this shard held were landed by `c3b6433ba8`, which deleted two
AS-path parsers and moved every case of their tables, by name, into the test of
the parser that survives. `./le commit create` prunes a row an earlier commit of
this session landed, so that record now lives in the commit's own blob.

| Test | Reason |
|------|--------|
