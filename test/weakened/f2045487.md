# Test weakenings this commit accepts

Every test below proved one behavior: that repository checks skip
`plan/verification-evidence/`. The owner removed that directory on
2026-09-24, and this commit removes the exemption helper and every skip
branch that called it. A test of a guard that no longer exists guards
nothing, so each one leaves with the code it tested. The path-cleaning half
of `TestProposedVerificationArchiveIsExcludedBeforeReads` stays, renamed
`TestProposedCleansAPathThatLeavesItsDirectory`.

Later commits in this session land work from sessions that have ended. Their
rows are copied verbatim from the reason file that session wrote
(`test/weakened/916ee49e.md` or `7e722ae0.md`), so each weakening lands with
its reason.

| Test | Reason |
|------|--------|
| dispatch_registry_test | The file's registry, slot-presence and copied-identity assertions were replaced by runtime unknown-RPC refusal, authorized route execution with anonymous denial, and cancellation reaching the registered command through both SDK direct paths. |
| TestOpUpdateRouteInjectsInternalIdentity | TestUpdateRouteRPCWithAuthorization replaces inspection of the injected username with actual authorization: anonymous sends are refused and trusted route pushes execute exactly once through socket, direct and typed transports. |
| TestPluginRPCRegistryCoversAllPaths | Removed assertions pinning the private registry's exact method count and typed-slot layout; TestPluginRPCUnknownMethodRefused now exercises unknown-method rejection through runtime dispatch, while TestUpdateRouteRPCWithAuthorization exercises registered socket, direct and typed route execution. |
| TestWireBridgeDispatchInstallsTypedSlots | Replaced Has* wiring checks and an error-text-only dispatch check with TestUpdateRouteRPCWithAuthorization's executed typed route and TestUpdateRouteDirectCancellation's observable SDK cancellation behavior. |
