# Test weakenings this commit accepts

Every test below proved one behavior: that repository checks skip
`plan/verification-evidence/`. The owner removed that directory on
2026-09-24, and this commit removes the exemption helper and every skip
branch that called it. A test of a guard that no longer exists guards
nothing, so each one leaves with the code it tested. The path-cleaning half
of `TestProposedVerificationArchiveIsExcludedBeforeReads` stays, renamed
`TestProposedCleansAPathThatLeavesItsDirectory`.

Later commits in this session land work from sessions that have ended. Their
rows are copied verbatim from the reason file that session wrote
(`test/weakened/916ee49e.md` or `7e722ae0.md`), so each weakening lands with
its reason.

| Test | Reason |
|------|--------|
| TestParseCommunityLists_MapForm | TestConfiguredCommunityMatchesAttributeValue now exercises single-entry map-form configuration through actual filter accept/reject decisions instead of inspecting the parsed entry's copied fields. |
| TestParseCommunityLists_MultiEntryMapFormRejected | Removed the error-message substring assertion while retaining the requirement that an unordered multi-entry map configuration is rejected. |
| TestParseCommunityLists_NameTooLong | Removed the error-message substring assertion while retaining rejection of a list name exceeding the configured length limit. |
| TestParseCommunityListsTwoEntriesInNonLexicalOrder | Replaced parsed-entry position assertions with the filter's accept/reject result for an UPDATE containing both communities, preserving both reversed-order cases. |
| TestParseModifyMatch | Replaced stored-match field assertions with TestConfiguredMatchModifiesOnlyMatchingRoute's next-hop rewrite and unchanged-route outcomes while retaining invalid-configuration rejection cases. |
| TestParseOneCommunityEntry | Replaced parsed-field and error-wording assertions with TestConfiguredCommunityMatchesAttributeValue's accept/reject outcomes and TestParseOneCommunityEntryRejectsInvalidConfig's invalid-input and boundary checks. |
