# Test weakenings this commit accepts

Every test below proved one behavior: that repository checks skip
`plan/verification-evidence/`. The owner removed that directory on
2026-09-24, and this commit removes the exemption helper and every skip
branch that called it. A test of a guard that no longer exists guards
nothing, so each one leaves with the code it tested. The path-cleaning half
of `TestProposedVerificationArchiveIsExcludedBeforeReads` stays, renamed
`TestProposedCleansAPathThatLeavesItsDirectory`.

Later commits in this session land work from sessions that have ended. Their
rows are copied verbatim from the reason file that session wrote
(`test/weakened/916ee49e.md` or `7e722ae0.md`), so each weakening lands with
its reason.

| Test | Reason |
|------|--------|
| header_test | The file-level removal is the same obsolete IPv4-mapped fixture, whose ffff-padding assumption contradicts RFC 7854 Section 4.2 and is replaced by the zero-padding assertions in TestPeerHeaderFromEvent and TestParseIPIntoIPv4. |
| TestBMPPeerHeaderIPv4Mapped | The removed fixture incorrectly prescribed IPv4-mapped ffff bytes, whereas RFC 7854 Section 4.2 requires twelve leading zero bytes, now checked by TestPeerHeaderFromEvent and TestParseIPIntoIPv4 with native IPv6 preserved by TestParseIPIntoIPv6. |
| TestBMPPeerUpSkippedOnCacheMiss | TestPeerUpOnCacheMissNeverReachesTheCollector replaces the vacuous cache-miss check by observing the collector stream and proving no Peer Up precedes the subsequent Peer Down when the required OPEN messages are absent. |
| TestHandleSenderNoSenders | The assertion-free no-sender smoke test is superseded by TestRFC7854AdjReplayEndsEachFamily, which processes peer events without a collector and then verifies the surviving routes and per-family completion markers on a newly attached collector. |
