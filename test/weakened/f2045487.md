# Test weakenings this commit accepts

Every test below proved one behavior: that repository checks skip
`plan/verification-evidence/`. The owner removed that directory on
2026-09-24, and this commit removes the exemption helper and every skip
branch that called it. A test of a guard that no longer exists guards
nothing, so each one leaves with the code it tested. The path-cleaning half
of `TestProposedVerificationArchiveIsExcludedBeforeReads` stays, renamed
`TestProposedCleansAPathThatLeavesItsDirectory`.

Later commits in this session land work from sessions that have ended. Their
rows are copied verbatim from the reason file that session wrote
(`test/weakened/916ee49e.md` or `7e722ae0.md`), so each weakening lands with
its reason.

| Test | Reason |
|------|--------|
| flowspec-fw-untranslatable-keeps-others | Rewritten for RIB-selected install. The bare 10.1.0.0/24 string check is subsumed by the daddr rule pattern, and the stderr "rule refused"/"253" log-text checks are replaced by the refused-rule counters for both unknown-protocol and unsupported-component, so each refusal is still observed. The reject now covers both refused prefixes. |
| flowspec-fw-withdraw-removes-table | Rewritten around a driver that sends a marker only after observing both rules in the kernel, then checks the kept rule survives the MP_UNREACH and the table count is 0 after peer loss. The removed "rules removed count=1" log assertions judged by daemon wording; the kernel observation before and after replaces them. |
