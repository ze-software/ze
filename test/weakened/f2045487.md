# Test weakenings this commit accepts

Every test below proved one behavior: that repository checks skip
`plan/verification-evidence/`. The owner removed that directory on
2026-09-24, and this commit removes the exemption helper and every skip
branch that called it. A test of a guard that no longer exists guards
nothing, so each one leaves with the code it tested. The path-cleaning half
of `TestProposedVerificationArchiveIsExcludedBeforeReads` stays, renamed
`TestProposedCleansAPathThatLeavesItsDirectory`.

Later commits in this session land work from sessions that have ended. Their
rows are copied verbatim from the reason file that session wrote
(`test/weakened/916ee49e.md` or `7e722ae0.md`), so each weakening lands with
its reason.

| Test | Reason |
|------|--------|
| assertRollbackKeptPolicy | authFailureFixture.assertLiveChildUnchanged replaces rollback-specific assertions with policy-to-state endpoint resolution and preservation of the surviving Child's exact SPI pair. |
| child_policy_rollback_test | The shared mbUseHandoffDataplane backend and four response-failure tests replace rollback-specific scaffolding while preserving peer-Delete policy-lifetime tests and checking migrated SAD/SPD endpoints. |
| newRollbackFixture | newAuthFailureFixture replaces the rollback fixture with an established, migrated Child so response-building failures must preserve the live tunnel's actual SAD and policy endpoints. |
| notify_error_test | The source scanner incorrectly rejected valid RFC4555 extension notifications, so TestMobikeAuthExtensionErrorsRequirePeerUnderstanding now exercises RFC7296's actual peer-understanding restriction through authenticated exchanges. |
| ntfConstantValue | The source scanner incorrectly rejected valid RFC4555 extension notifications, so TestMobikeAuthExtensionErrorsRequirePeerUnderstanding now exercises RFC7296's actual peer-understanding restriction through authenticated exchanges. |
| ntfRFCErrorType | The source scanner incorrectly rejected valid RFC4555 extension notifications, so TestMobikeAuthExtensionErrorsRequirePeerUnderstanding now exercises RFC7296's actual peer-understanding restriction through authenticated exchanges. |
| ntfScanNotifyUse | The source scanner incorrectly rejected valid RFC4555 extension notifications, so TestMobikeAuthExtensionErrorsRequirePeerUnderstanding now exercises RFC7296's actual peer-understanding restriction through authenticated exchanges. |
| runStopCase | rtxPeerLink replaces duplicated socket setup and aligns the SA's configured source with its real loopback socket, while TestClearSendsIKEDelete and TestPlainStopSendsNoDelete retain their packet-level assertions. |
| TestBuildAuthResponseAuthRollbackKeepsLivePolicy | AUTH encoding now precedes dataplane installation, and TestBuildAuthResponseAuthFailureKeepsMovedChild verifies an authentication failure preserves the migrated Child's SAD and policy endpoints. |
| TestBuildAuthResponseBuildRollbackKeepsLivePolicy | Response encryption now precedes dataplane installation, and TestBuildAuthResponseEncryptionFailureKeepsMovedChild verifies an encryption failure preserves the migrated Child's SAD and policy endpoints. |
| TestBuildAuthResponseCertRollbackKeepsLivePolicy | Certificate encoding now precedes dataplane installation, and TestBuildAuthResponseCertFailureKeepsMovedChild verifies a certificate-encoding failure preserves the migrated Child's SAD and policy endpoints. |
| TestBuildAuthResponseRollbackRemovesPolicyWhenNothingElseSharesIt | TestBuildAuthResponseFailureLeavesEmptyDataplane replaces the obsolete install-then-rollback expectation by asserting that failed authentication with no surviving Child leaves both SAD and SPD empty. |
| TestNattCachedReplayIsBoundedByBothGuards | The removed fatal branch was guarded by literal false, while the required setup receive, unprotected-replay silence and rate-limit assertions remain with the SA source corrected to its loopback socket. |
| TestNtfNotifyVocabularyIsRFCDefined | The source scanner incorrectly rejected valid RFC4555 extension notifications, so TestMobikeAuthExtensionErrorsRequirePeerUnderstanding now exercises RFC7296's actual peer-understanding restriction through authenticated exchanges. |
| useSPDDataplane | The SPD-only backend helper was replaced by mbUseHandoffDataplane, which retains policy-lifetime coverage while also modeling SAD and policy-template endpoints. |
