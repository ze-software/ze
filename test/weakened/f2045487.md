# Test weakenings this commit accepts

Every test below proved one behavior: that repository checks skip
`plan/verification-evidence/`. The owner removed that directory on
2026-09-24, and this commit removes the exemption helper and every skip
branch that called it. A test of a guard that no longer exists guards
nothing, so each one leaves with the code it tested. The path-cleaning half
of `TestProposedVerificationArchiveIsExcludedBeforeReads` stays, renamed
`TestProposedCleansAPathThatLeavesItsDirectory`.

Later commits in this session land work from sessions that have ended. Their
rows are copied verbatim from the reason file that session wrote
(`test/weakened/916ee49e.md` or `7e722ae0.md`), so each weakening lands with
its reason.

| Test | Reason |
|------|--------|
| aspa_tracker_test | The file-level change removes the empty-tracker no-op test while retaining tracked-route removal, replacement and cache-driven revalidation coverage. |
| TestASPANormalizePrepends | Replaced duplicate slice-normalization coverage with TestASPACompressedUpdatePrependsValid and TestASPACompressedUpdatePreservesHops, which observe emitted ASPA verdicts and route acceptance for compressed and nonconsecutive hops. |
| TestASPATrackerKeepsTheGroupIdentity | Removed the tracker field-copy assertion because TestASPARevalidationDispatchesWithTheGroup verifies group identity in the actual ASPA-change validation request. |
| TestASPATrackerRemoveNonexistent | Removed the empty-tracker count/no-panic assertion, which exercised no route transition; TestASPATrackerRemove retains coverage of withdrawing a tracked route and clearing its reverse index. |
| TestExtractOriginAS | Migrated the rightmost-AS_SEQUENCE assertion to TestRPKIOriginFromWireSequence through the shared segment-aware wire decoder after removing the unused raw-hex extractor. |
| TestExtractOriginASEmpty | Removed the obsolete raw-hex helper test; TestRPKIOriginASFromASPathRFC6811 and TestJSONOriginUsesLocalASAndRawSegmentTypes cover empty-path local-AS semantics, while TestASPAJSONRailKeepsSegmentTypes rejects malformed supplied raw attributes. |
| TestExtractOriginASSet | Migrated the AS_SET-to-OriginNone assertion to TestRPKIOriginFromWireASSet through the shared segment-aware wire decoder. |
| TestHandlePDUVersionDowngrade | Replaced obsolete version-zero acceptance with actual connection closure on an unsupported lower version, consistent with the implemented RTR version-one minimum. |
| TestIsFatalError | Collapsed redundant fatal-code rows to the No Data Available versus Unsupported Version boundary, with fatal transport behavior exercised by TestHandlePDUVersionDowngrade and TestASPAProviderListErrorOnWire. |
| TestNoDataAvailableKeepsResetQueryMode | Replaced a direct handler-state assertion with an open-TCP exchange proving repeated No Data Available responses cause Reset retries and a later completed load switches the next query to Serial. |
| TestOriginRevalidationKeepsTheGroupIdentity | Removed the tracker field-copy assertion because TestOriginRevalidationDispatchesWithTheGroup verifies that the group identity reaches the actual cache-change validation request. |
| TestParseASPAPDUReservedCustomerAS | Retained rejection of customer AS zero using the correct 8210bis wire layout and removed the incidental error-wording assertion. |
| TestParseASPAPDUReservedProviderAS | Replaced the incorrect blanket AS-zero provider rejection with TestParseASPAZeroProvider, which accepts an AS0-only provider set and rejects a mixed AS0 list. |
| TestParseASPAPDUSelfRef | Migrated the fixture to the actual 8210bis wire layout and replaced error-text matching with ErrorIs(errASPAProviderList), retaining rejection of a customer in its own provider list. |
| TestParseASPAPDUUnknownAFI | Removed the obsolete AFI-skip test because the implemented 8210bis ASPA PDU has no AFI field; TestParseASPAPDUReservedIgnored covers its reserved bits instead. |
| TestParseASPAPDUUnsorted | Migrated to the actual 8210bis wire layout and typed provider-list errors while testing both descending and duplicate providers rather than error wording. |
| TestParseRPKIConfigASPAPolicyDefaults | Removed the obsolete log-only default assertion because the completed ASPA policy defaults to rejecting Invalid routes, whose observable rejection is exercised by TestASPACompressedUpdatePreservesHops. |
| TestPollingCadenceAtLeastHourly | Replaced the default-value-only polling assertion with TestRunPollsOnRefreshAfterSuccessRetryAfterFailure, which observes actual version-one Reset and Serial queries at the negotiated refresh interval. |
| TestRPKISourceAddress | Removed assertions that the constructor merely copied sourceAddress into a private field; the test still checks configured and omitted source-address parsing. |
| validate_test | The file-level import and helper-test changes follow removal of the unused raw-hex origin extractor, retaining sequence and AS_SET assertions through the shared wire-decoding path. |
| TestRunRegistered | Its body was only t.Parallel; the file's one check was a compile-time assertion that Run is func([]string) int, which the mock's registration already enforces at compile time. The file now carries TestRTRMockASPAPDUWireFormat, which pins the ASPA PDU byte layout the mock sends. |
