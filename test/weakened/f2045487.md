# Test weakenings this commit accepts

Every test below proved one behavior: that repository checks skip
`plan/verification-evidence/`. The owner removed that directory on
2026-09-24, and this commit removes the exemption helper and every skip
branch that called it. A test of a guard that no longer exists guards
nothing, so each one leaves with the code it tested. The path-cleaning half
of `TestProposedVerificationArchiveIsExcludedBeforeReads` stays, renamed
`TestProposedCleansAPathThatLeavesItsDirectory`.

| Test | Reason |
|------|--------|
| TestVerificationArchiveBoundary | Proved the verification-archive exemption. The owner removed plan/verification-evidence on 2026-09-24 and this commit deletes the exemption with it, so no path the test guarded exists and no code under test remains. |
| lepath_test | Assertion count falls because TestVerificationArchiveBoundary leaves with the removed helper. |
| TestVerificationArchiveDirectoryIsNotOpened | Proved the verification-archive exemption. The owner removed plan/verification-evidence on 2026-09-24 and this commit deletes the exemption with it, so no path the test guarded exists and no code under test remains. |
| consistency_test | Assertion count falls because TestVerificationArchiveDirectoryIsNotOpened leaves with the removed skip. |
| TestVerificationArchiveCannotContributeDocumentFindings | Proved the verification-archive exemption. The owner removed plan/verification-evidence on 2026-09-24 and this commit deletes the exemption with it, so no path the test guarded exists and no code under test remains. |
| TestVerificationArchiveCannotSupplyALiveHookName | Proved the verification-archive exemption. The owner removed plan/verification-evidence on 2026-09-24 and this commit deletes the exemption with it, so no path the test guarded exists and no code under test remains. |
| links_test | Assertion count falls because the two archive tests leave with the removed skip. |
| TestVerificationArchiveChangesDoNotReadTheBaseline | Proved the verification-archive exemption. The owner removed plan/verification-evidence on 2026-09-24 and this commit deletes the exemption with it, so no path the test guarded exists and no code under test remains. |
| TestMovingACanonicalTestIntoTheArchiveIsRemoval | Proved the verification-archive exemption. The owner removed plan/verification-evidence on 2026-09-24 and this commit deletes the exemption with it, so no path the test guarded exists and no code under test remains. |
| TestMovingAnArchivedTestCannotSupplyACanonicalBaseline | Proved the verification-archive exemption. The owner removed plan/verification-evidence on 2026-09-24 and this commit deletes the exemption with it, so no path the test guarded exists and no code under test remains. |
| parity_test | Assertion count falls because the three archive tests leave with the removed rename rewrite. |
| TestProposedVerificationArchiveIsExcludedBeforeReads | Proved the verification-archive exemption. The owner removed plan/verification-evidence on 2026-09-24 and this commit deletes the exemption with it, so no path the test guarded exists and no code under test remains. Its path-cleaning assertions survive in TestProposedCleansAPathThatLeavesItsDirectory. |
| proposed_test | Assertion count falls by the archive-exclusion assertions only; the path-cleaning ones remain. |
