# Test weakenings this commit accepts

Every test below proved one behavior: that repository checks skip
`plan/verification-evidence/`. The owner removed that directory on
2026-09-24, and this commit removes the exemption helper and every skip
branch that called it. A test of a guard that no longer exists guards
nothing, so each one leaves with the code it tested. The path-cleaning half
of `TestProposedVerificationArchiveIsExcludedBeforeReads` stays, renamed
`TestProposedCleansAPathThatLeavesItsDirectory`.

Later commits in this session land work from sessions that have ended. Their
rows are copied verbatim from the reason file that session wrote
(`test/weakened/916ee49e.md` or `7e722ae0.md`), so each weakening lands with
its reason.

| Test | Reason |
|------|--------|
| TestKeepaliveClampedOnNegotiation | The old test performed its own SetKeepaliveTime(negotiatedHold/3) clamp rather than exercising negotiation; its replacement, TestKeepaliveMinimumInterval, proves jitter cannot fire a KEEPALIVE before the one-second minimum. |
| TestKeepaliveDefaultDerivation | Replaced the getter and wall-clock subsecond assertions with deterministic firing-boundary assertions proving that a 12-second hold derives a four-second keepalive interval whose minimum jittered expiry is three seconds. |
| TestKeepaliveExplicit | Replaced the setter echo and wall-clock subsecond assertions with deterministic firing-boundary assertions proving that an explicit eight-second keepalive expires at its six-second jitter boundary rather than using hold/3. |
