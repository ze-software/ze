# Test weakenings this commit accepts

Every test below proved one behavior: that repository checks skip
`plan/verification-evidence/`. The owner removed that directory on
2026-09-24, and this commit removes the exemption helper and every skip
branch that called it. A test of a guard that no longer exists guards
nothing, so each one leaves with the code it tested. The path-cleaning half
of `TestProposedVerificationArchiveIsExcludedBeforeReads` stays, renamed
`TestProposedCleansAPathThatLeavesItsDirectory`.

Later commits in this session land work from sessions that have ended. Their
rows are copied verbatim from the reason file that session wrote
(`test/weakened/916ee49e.md` or `7e722ae0.md`), so each weakening lands with
its reason.

| Test | Reason |
|------|--------|
| replyWithPrivLvl | The profileReplies/newProfileServer fixtures now obtain privilege from a separate shell AUTHOR response instead of incorrectly treating PAP reply data as a privilege level. |
| TestAccountantCommandStartStop | The nonempty-ID/no-panic probe is replaced by TestRFC8907TrustedDispatchIdentitiesReachWire and TestRFC8907StartAndStopShareTaskID, which check actual START/STOP delivery and correlation. |
| TestAccountantEnqueueAfterStopNoPanic | TestAccountantRefusesAfterStopWithoutLoggingCommand exercises both post-stop calls and asserts two observable refusals without exposing a command secret, rather than only asserting no panic. |
| TestAccountantStopIdempotent | TestRFC8907AccountingStopDrainsAcceptedStarts calls Stop twice and Start after shutdown while requiring every accepted record to reach the server, retaining idempotency with a delivery assertion. |
| TestAccountantTaskIDMonotonic | TestAccountantTaskIDsSpanGenerations adds cross-generation uniqueness while TestRFC8907ActiveTaskIDsAreDistinct and TestRFC8907TaskIDNeverReappears retain stronger within-generation task-ID coverage. |
| TestAcctRequestStartStopFlags | TestRFC8907TrustedDispatchIdentitiesReachWire checks START and STOP flag values on actual delivered accounting packets, replacing the isolated marshal-only flag check. |
| TestDispatcherAccountingSkipsNoUsername | TestDispatcherAccountingWithoutUsername replaces the incorrect identity-based exemption by requiring START and STOP for commands entered with an empty or absent caller context, as RFC 8907 Section 8.3 requires. |
| TestExecLogRedactsBeforeTruncation | Schema-aware redaction may shorten or mask the input before truncation, so the test retains whole-hash and hash-prefix secrecy checks without requiring the incidental bytes-total marker. |
| TestRFC8907ZeroLengthDataIsReadAsAbsent | The assertion now inspects the client's absent binary PAP data directly rather than deriving a privilege profile from authentication data, with present-zero-byte behavior retained in TestRFC8907PresentZeroByteIsNotReadAsAbsent. |
| TestRunRegistered | (TACACS+ mock) Its body was only t.Parallel; the Run signature is enforced by the mock's registration at compile time. The file now tests exact observer authorization grants against the mock. |
| TestTacacsAccountingDropCounter | Dropping on queue saturation is replaced by bounded backpressure, verified by TestRFC8907AccountingSaturationRetainsEveryStart, while TestAccountantRefusesAfterStopWithoutLoggingCommand still checks the refusal counter. |
| TestTacacsAuthenticatorAuthenticatedImpliesProfiles | TestTacacsAuthenticatorProfileMappingShapes explicitly requires successful mapped profiles and rejects empty, nil and missing mappings, unlike the removed conditional invariant that also passed when every login was rejected. |
| TestTacacsAuthenticatorErrorStatus | The test retains infrastructure-error and non-ErrAuthRejected assertions but drops the incidental server-message text assertion because ERROR handling now occurs in client reply-status validation. |
| TestTacacsAuthorizerPassRepl | An empty PASS_REPL removes the command rather than approving it, now covered by TestTacacsAuthorizerEmptyPassReplDenies alongside TestRFC8907PassReplPreservesExactCommand and TestRFC8907PassReplCannotAuthorizeOriginalCommand. |
| TestTacacsClientRejectsBadResponseHeader | RFC 8907 requires ignoring unknown header flags, now covered by TestRFC8907UnknownHeaderFlagsIgnored and TestRFC8907UnknownFlagsDoNotMaskDowngrade, while wrong type, version and sequence rejection assertions remain. |
| TestWebAuthLocalPreservedWithBundle | TestWebAuthRemoteRejectionStopsOrdinaryFallback now enforces terminal central rejection, while TestWebAuthRecoverySurvivesRemoteRejection and TestWebAuthLocalFallbackOnBackendUnavailable preserve the separately authorized recovery and outage paths. |
