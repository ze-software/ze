# Test weakenings this commit accepts

Every test below proved one behavior: that repository checks skip
`plan/verification-evidence/`. The owner removed that directory on
2026-09-24, and this commit removes the exemption helper and every skip
branch that called it. A test of a guard that no longer exists guards
nothing, so each one leaves with the code it tested. The path-cleaning half
of `TestProposedVerificationArchiveIsExcludedBeforeReads` stays, renamed
`TestProposedCleansAPathThatLeavesItsDirectory`.

Later commits in this session land work from sessions that have ended. Their
rows are copied verbatim from the reason file that session wrote
(`test/weakened/916ee49e.md` or `7e722ae0.md`), so each weakening lands with
its reason.

| Test | Reason |
|------|--------|
| declByName | Removed the helper used only by the deleted command-catalog and field-copy assertions; retained declareShapes parses real declarations for behavioral CLI-pipe tests. |
| rib_shape_test | The file retains populated-route CLI first, count, match and rendering regressions; the reduction removes only declaration mirrors and their unused catalog helper. |
| rib_validation_test | The file-level reduction removes flag-only TestEnableValidation, whose command behavior is covered by the lifecycle and timeout replay regressions in rib_validation_config_test.go. |
| TestCommandDeclsDeclareTheAnswerShape | Removed mirrored declaration-field assertions while retaining TestShowAnswersRowsKeyedByPeer, TestShowCountsThePeers, TestShowRendersInEveryFormat and TestShowMatchesInsideARow through the declared-shape CLI pipeline. |
| TestDeclaredShapeSpellingIsOneTheEngineParses | Removed the hardcoded valid-spelling copy because retained declareShapes calls the engine ParseAnswerShape before the behavioral pipe tests. |
| TestEnableValidation | Replaced default/internal-flag assertions with TestRPKIDisablePreservesReceivedGenerations and TestValidationTimeoutControlsPendingReplay, which exercise route availability, generation-sensitive replay and timeout transitions. |
