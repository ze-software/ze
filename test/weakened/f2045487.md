# Test weakenings this commit accepts

Every test below proved one behavior: that repository checks skip
`plan/verification-evidence/`. The owner removed that directory on
2026-09-24, and this commit removes the exemption helper and every skip
branch that called it. A test of a guard that no longer exists guards
nothing, so each one leaves with the code it tested. The path-cleaning half
of `TestProposedVerificationArchiveIsExcludedBeforeReads` stays, renamed
`TestProposedCleansAPathThatLeavesItsDirectory`.

Later commits in this session land work from sessions that have ended. Their
rows are copied verbatim from the reason file that session wrote
(`test/weakened/916ee49e.md` or `7e722ae0.md`), so each weakening lands with
its reason.

| Test | Reason |
|------|--------|
| TestPeerDownWithdrawalFollowsReplay | It tested the bgp-rs copy mechanism (holdForReplays, heldWithdrawals) that resent a peer-down withdrawal after a destination's replay. The reactor now queues such a withdrawal behind the destination's held forwards itself, so the copy is gone with its test. TestAnnounceRailWithdrawJoinsTheFence in the reactor proves the same ordering where it now lives: the withdrawal follows a parked live announce, and a replayed announce passes it. |
| replay_order_test | Assertion count falls because TestPeerDownWithdrawalFollowsReplay leaves with the rs copy mechanism it tested; the ordering it guarded is now asserted in the reactor by TestAnnounceRailWithdrawJoinsTheFence. |
