# Test weakenings this commit accepts

Every row is a doctor check that moved to the package owning the config it
reads. The test moved with it, case for case, and the doctor package kept no
copy because the check it drove no longer exists there. The commit is correct
without each row because the owner package holds the same assertion over the
same check body, now reached through `diagnostic.RegisterDoctorCheck`.

| Test | Reason |
|------|--------|
| TestDoctorBMPCollectors | moved to `internal/component/bgp/plugins/bmp/doctor_test.go` as `TestCheckBMPCollectorsReportsUnreachableCollectors`, `TestCheckBMPCollectorsProbesTheSchemaDefaultPort` and `TestCheckBMPCollectorsIsSilentWithoutCollectors`; the check moved to `bmp/doctor.go` |
| TestDoctorRPKIServers | moved to `internal/component/bgp/plugins/rpki/doctor_test.go` as `TestCheckRPKICacheServersReportsUnreachableCaches`, `TestCheckRPKICacheServersProbesTheRTRDefaultPort` and `TestCheckRPKICacheServersIsSilentWithoutCaches`; the check moved to `rpki/doctor.go` |
| TestReachProbeTimeout | the probe moved to `internal/core/diagnostic/doctor_probe.go` in 9e3298e3d6 and `doctor_probe_test.go` holds `TestDoctorProbeTimeout` over the same cases |
| checks_reach_test | emptied: every function above moved with its check |
| TestCheckBGPPeerConfigDoesNotMutateTree | moved to `internal/component/bgp/config/doctor_checks_test.go` as `TestDoctorCheckBGPPeerConfigDoesNotMutateTree` |
| TestCheckBGPPeerConfigReportsRejection | moved, `TestDoctorCheckBGPPeerConfigReportsRejection` in the same file |
| TestCheckBGPPeerConfigSilentWhenValid | moved, `TestDoctorCheckBGPPeerConfigSilentWhenValid` |
| TestCheckBGPPeerConfigSilentWithoutEngine | folded into `TestDoctorCheckBGPPeerConfigSilentWhenValid`, which drives the check with no engine registered |
| TestCheckBGPPeersWithoutRoleDoesNotMutateTree | moved, `TestDoctorCheckBGPPeersWithoutRoleDoesNotMutateTree` |
| TestCheckBGPPeersWithoutRoleReportsEveryPeer | moved, `TestDoctorCheckBGPPeersWithoutRoleReportsEveryPeer` |
| TestCheckBGPPeersWithoutRoleSilentWhenEveryPeerDeclaresOne | moved, `TestDoctorCheckBGPPeersWithoutRoleSilentWhenEveryPeerDeclaresOne` |
| withPeerValidator | test seam for the peer-config check; the owner test file carries its own |
| withRolelessPeerReporter | test seam for the no-role check; the owner test file carries its own |
| bgpTreeWithPeer | fixture builder; the owner test file carries `peerConfigWithFamily` and `parsedTree` |
| checks_config_bgp_peer_test | emptied: every function above moved with its check |
| TestCheckBGPCaptureDirectoryDisabled | moved, `TestDoctorCheckBGPCaptureDirectoryDisabled` in `bgp/config/doctor_checks_test.go` |
| TestCheckBGPCaptureDirectoryInGroup | moved, `TestDoctorCheckBGPCaptureDirectoryInGroup` |
| TestCheckBGPCaptureDirectoryNotWritable | moved, `TestDoctorCheckBGPCaptureDirectoryNotWritable` |
| TestCheckBGPCaptureDirectoryUsesDefault | moved, `TestDoctorCheckBGPCaptureDirectoryUsesDefault` |
| TestCheckBGPCaptureDirectoryWritable | moved, `TestDoctorCheckBGPCaptureDirectoryWritable` |
| newCaptureTree | fixture builder, moved under the same name to the owner test file |
| checks_bgp_capture_test | emptied: every function above moved with its check |
| TestCheckSysctlProcfs | moved to `internal/component/sysctl/doctor_test.go` as `TestCheckSysctlProcfsReportsAnUnwritableTree` and `TestCheckSysctlProcfsIsSilentWithoutASysctlBlock` |
| TestDoctorBGPMD5_NoPeers | folded into `TestDoctorCheckBGPMD5SilentWithoutMD5` in `bgp/config/doctor_checks_test.go`, whose first assertion drives the check over an empty tree |
| TestDoctorBGPMD5_PeerWithoutMD5 | folded into `TestDoctorCheckBGPMD5SilentWithoutMD5`, same peer without a password |
| TestDoctorBGPMD5_PeerWithMD5 | moved, `TestDoctorCheckBGPMD5ReportsAPeerOnAPlatformWithoutIt` |
| TestFilterInstanceName | the helper moved to `bgp/config` in 9e3298e3d6 with `checkConfigReferences`; `peers_leakfilter_test.go` holds `TestFilterInstanceNameStripsEveryPrefixForm` over the same three forms |
| doctor_test | reduced: the MD5 and filter-name rows above and every `doctor_test.go` row below left with their checks; the CLI, listener, plugin, store and platform-dispatch tests stay |
| hasDiagCode | moved under the same name to `internal/component/config/doctor_test.go`, driving `checkSemantics` |
| TestDoctorFlagsGnmiExposure | moved to `internal/component/config/doctor_test.go` as `TestCheckSemanticsFlagsGnmiExposure`, same tokenless 0.0.0.0 tree and `config-gnmi-invalid` |
| TestDoctorGnmiLoopbackAndTokenAreClean | moved, `TestCheckSemanticsGnmiLoopbackAndTokenAreClean` in the same file, same loopback and token cases |
| checks_config_test | reduced: `hasDiagCode` and the two gNMI semantics tests above moved with the semantics check; the two listener-probe tests stay |
| fakeSysClassNet | moved under the same name to `internal/component/iface/doctor_linux_test.go` |
| sysClassNetStack | moved under the same name to `internal/component/iface/doctor_linux_test.go` |
| sysClassNetLink | moved under the same name to `internal/component/iface/doctor_linux_test.go` |
| ethernetTree | moved under the same name to `internal/component/iface/doctor_linux_test.go` |
| TestCheckInterfacesFollowsOSNameAlias | moved to `internal/component/iface/doctor_linux_test.go` as `TestCheckEthernetInterfacesFollowsOSNameAlias` |
| TestCheckInterfacesFollowsMACMatch | moved, `TestCheckEthernetInterfacesFollowsMACMatch` with the same five subtests (one device, none, two, bridge, vlan) |
| TestCheckInterfacesUnselectedEntryUnchanged | moved, `TestCheckEthernetInterfacesUnselectedEntryUnchanged` |
| TestCheckInterfacesMACOverrideBoundByName | moved, `TestCheckEthernetInterfacesMACOverrideBoundByName` with the same four subtests |
| TestCheckFirewallBackend | moved to `internal/plugins/firewall/nft/doctor_linux_test.go` as `TestCheckFirewallNftablesWarnsWithoutTheModule`, which asserts `doctor-firewall-nftables` at warning for an unset and an `nft` backend |
| TestCheckTelemetryProcfs | moved to `internal/component/telemetry/exporter/doctor_linux_test.go` as `TestCheckTelemetryProcfsWarnsWhenProcStatIsUnreadable`, same unreadable procfs and `doctor-telemetry-procfs` at warning |
| TestCheckConntrackProcfs | moved to `internal/component/config/system/doctor_conntrack_linux_test.go` as `TestCheckConntrackProcfsReportsAnUnwritableKey`, same missing `nf_conntrack_max` and `doctor-conntrack-procfs` at warning |
| TestCheckPolicyRouteNetlink | moved to `internal/plugins/policyroute/doctor_linux_test.go` as `TestCheckPolicyRouteNetlinkReportsAnUnopenableHandle`, same refused handle and `doctor-policyroute-netlink` at warning |
| TestCheckNTPClockPrivilege_Missing | moved to `internal/plugins/ntp/doctor_linux_test.go` as `TestCheckNTPClockPrivilegeWarnsWithoutCapSysTime`, same empty CapEff mask under uid 1000 |
| TestCheckNTPClockPrivilege_Present | folded into `TestCheckNTPClockPrivilegeIsSilentWhenGranted` in the same file, whose first case is the CAP_SYS_TIME mask |
| TestCheckNTPClockPrivilege_Disabled | folded into `TestCheckNTPClockPrivilegeIsSilentWhenGranted`, whose last case is an empty tree |
| TestCheckVPPDPDK_ModuleMissing | moved to `internal/component/vpp/doctor_dpdk_linux_test.go` as `TestCheckVPPDPDKReportsMissingModulesAndDevice`, which counts the VFIO and PCI diagnostics rather than testing for one of each |
| TestCheckVPPDPDK_NoInterfaces | folded into `TestCheckVPPDPDKIsSilentWithoutDPDKInterfaces` in the same file, the `dpdkTree("")` case |
| TestCheckVPPDPDK_NotVPP | folded into `TestCheckVPPDPDKIsSilentWithoutDPDKInterfaces`, the netlink-backend case |
| checks_linux_test | reduced: the interface, firewall, telemetry, sysctl, conntrack, policy-route, NTP privilege and VPP DPDK tests above moved with their checks; the kernel-module, random-seed and machine-id tests stay |
| TestReadLoadedModulesDistinguishesEmptyFromUnreadable | the reader moved to `internal/component/kernelcap/modules_linux.go` as `LoadedModules`; `modules_linux_test.go` holds `TestLoadedModulesDistinguishesEmptyFromUnreadable` over the same three inputs |
| checks_mpls_linux_test | reduced: the module-reader test above moved with the reader; the MPLS capability tests stay |
| redistTree | moved to `internal/component/config/doctor_redistribute_test.go` as `redistDoctorTree` |
| TestRedistributeCheckSilentOnAWorkingRule | moved under the same name to `internal/component/config/doctor_redistribute_test.go` |
| TestRedistributeCheckNamesAnUnknownSource | moved under the same name to the same file |
| TestRedistributeCheckNamesAnUnknownDestination | moved under the same name to the same file |
| TestRedistributeCheckReportsBothHalves | moved under the same name to the same file |
| TestRedistributeCheckSilentWithNoBlock | moved under the same name to the same file, with a nil-tree case |
| checks_redistribute_test | reduced: the five unit tests and their fixture above moved with the check; `codes` and the runner test stay |
| rirSourceTree | moved under the same name to `internal/component/resolve/cmd/doctor_rir_test.go` |
| TestDoctorReportsADelegationSourceTheRefreshWillRefuse | moved under the same name to `internal/component/resolve/cmd/doctor_rir_test.go` |
| TestDoctorSaysNothingWhenNoDelegationSourceIsConfigured | moved under the same name to the same file |
| TestDoctorRIRSourceCodeIsRegistered | folded into `TestRIRSourcesDoctorCheckRegistered` in the same file, which looks the code up and asserts its title and description |
| checks_rir_sources_test | emptied: every function above moved with its check |
| generateTestCert | the PEM minting moved to `internal/core/diagnostic/doctor_cert_test.go` as `testCertPEM` |
| generateTestCertDER | the DER minting moved to `internal/component/pki/doctor_config_certs_test.go` as `configCertDER` |
| generateTestCertPEM | the cert-plus-key minting moved to `internal/component/web/doctor_material_test.go` as `webMaterialCertPEM` |
| generateTestCertKey | the shared key generation is inlined in each of the three helpers above |
| resolvConfTree | moved under the same name to `internal/component/config/system/doctor_test.go` |
| sshEnabledTree | moved under the same name to `internal/component/ssh/doctor_test.go` |
| webEnabledTree | moved under the same name to `internal/component/web/doctor_material_test.go` |
| TestCheckCertExpiry_Valid | the helper moved to `internal/core/diagnostic` as `DoctorCertExpiry`; `doctor_cert_test.go` holds `TestDoctorCertExpiry_Valid` |
| TestCheckCertExpiry_Expired | moved, `TestDoctorCertExpiry_Expired` in the same file |
| TestCheckCertExpiry_NotYetValid | moved, `TestDoctorCertExpiry_NotYetValid` |
| TestCheckCertExpiry_ExpiringSoon | moved, `TestDoctorCertExpiry_ExpiringSoon` |
| TestCheckCertExpiry_InvalidPEM | moved, `TestDoctorCertExpiry_InvalidPEM` |
| TestCheckCertExpiry_BadDER | moved, `TestDoctorCertExpiry_BadDER` |
| TestCheckCertPair_KeyMissing | the helper moved to `internal/core/diagnostic` as `DoctorCertPair`; `doctor_cert_test.go` holds `TestDoctorCertPair_KeyMissing` |
| TestCheckSSHHostKey_Missing | moved under the same name to `internal/component/ssh/doctor_test.go` |
| TestCheckSSHHostKey_Present | moved under the same name to the same file |
| TestCheckSSHHostKey_NotEnabled | moved under the same name to the same file, with a nil-tree case |
| TestCheckSSHHostKey_EmptyDir | moved under the same name to the same file |
| TestCheckDHCPInterfaces | moved to `internal/plugins/dhcpserver/doctor_test.go` as `TestCheckDHCPInterfacesReportsAMissingInterface`, same missing `ze-doctor-missing0` and `doctor-dhcp-iface` at error |
| TestCheckTACACSServers | moved to `internal/component/tacacs/doctor_test.go` as `TestCheckTACACSServersReportsUnreachableServers`, same refused probe and `doctor-tacacs-unreachable` at warning |
| TestCheckPKICerts_MissingCA | moved to `internal/component/pki/doctor_config_certs_test.go` as `TestCheckConfiguredCertificates_MissingCA` |
| TestCheckPKICerts_ExpiredCA | moved, `TestCheckConfiguredCertificates_ExpiredCA` in the same file |
| TestCheckWebTLS_NoCerts | moved to `internal/component/web/doctor_material_test.go` as `TestCheckWebTLSMaterial_NoCerts` |
| TestCheckWebTLS_ExpiredCert | moved, `TestCheckWebTLSMaterial_ExpiredCert` in the same file |
| TestCheckWebTLS_CertWithoutKey | moved, `TestCheckWebTLSMaterial_CertWithoutKey` |
| TestCheckWebTLS_KeyWithoutCert | moved, `TestCheckWebTLSMaterial_KeyWithoutCert` |
| TestCheckWebTLS_Disabled | moved, `TestCheckWebTLSMaterial_Disabled`, with a nil-tree case |
| TestCheckWebTLS_MatchingPair | moved, `TestCheckWebTLSMaterial_MatchingPair` |
| TestCheckWebTLS_MismatchedPair | moved, `TestCheckWebTLSMaterial_MismatchedPair` |
| TestCheckWebTLS_UnreadableKey | moved, `TestCheckWebTLSMaterial_UnreadableKey` |
| TestRunChecksReportsUnusableWebTLSPair | moved to `internal/component/web/doctor_material_test.go` as `TestDoctorReportsUnusableWebTLSPair`, driving `diagnostic.RunDoctorChecks` over the same config file and store |
| TestCheckPlatformReturnsPlatformInfo | renamed in place to `TestResolveDoctorPlatformReturnsPlatformInfo` in `internal/component/doctor/doctor_test.go`, same env override and same three assertions |
| TestCheckNTPCoherenceGokrazyNoNTP | folded into `TestCheckNTPClientReportsADisabledClientPerPlatform` in `internal/plugins/ntp/doctor_test.go`, the gokrazy row at error |
| TestCheckNTPCoherenceSystemdNoNTP | folded into `TestCheckNTPClientReportsADisabledClientPerPlatform`, the systemd row at warning |
| TestCheckNTPCoherenceDarwinNoNTP | folded into `TestCheckNTPClientReportsADisabledClientPerPlatform`, the darwin row with no diagnostic |
| TestCheckNTPCoherenceUnknownNoNTP | folded into `TestCheckNTPClientReportsADisabledClientPerPlatform`, the unknown row with no diagnostic |
| TestCheckNTPCoherenceGokrazyNTPEnabled | folded into `TestCheckNTPClientIsSilentWhenAServerAnswers` in the same file, whose first case is an enabled client on gokrazy with a server that answers |
| TestDoctorNTPClientReadiness | moved to `internal/plugins/ntp/doctor_test.go` as `TestCheckNTPClientReportsUnreachableServers`, same refused probe and `doctor-ntp-server-unreachable` |
| TestCheckSystemdServiceInstallMissingAccountAndExecutable | moved to `internal/plugins/systemd/doctor_test.go` as `TestCheckServiceInstallMissingAccountAndExecutable`, same three codes |
| TestCheckSystemdServiceInstallExecutableOK | moved, `TestCheckServiceInstallExecutableOK` in the same file |
| TestCheckSystemdServiceSkipsGokrazy | moved, `TestCheckServiceInstallSkipsGokrazy`, same unread-unit assertion |
| TestCheckSystemdServiceSkipsContainer | moved, `TestCheckServiceInstallSkipsContainer`, same unread-unit assertion |
| TestCheckSystemdServiceRunsOnSystemd | moved, `TestCheckServiceInstallRunsOnSystemd`, same `doctor-service-executable` at error |
| TestParseServiceUnitLastExecStartWins | moved under the same name to `internal/plugins/systemd/doctor_test.go` |
| TestParseServiceUnitIgnoresNonServiceSection | moved under the same name to the same file |
| TestFirstSystemdCommandStripsAllPrefixes | moved under the same name to the same file, same eight inputs |
| TestCheckResolvConfMismatchSystemd | moved under the same name to `internal/component/config/system/doctor_test.go` |
| TestCheckResolvConfMismatchGokrazy | moved under the same name to the same file |
| TestCheckResolvConfMatchGokrazy | moved under the same name to the same file |
| TestCheckDNSResolvers_NoSystemBlock | moved under the same name to `internal/component/config/system/doctor_test.go`, and now also asserts the probe was never asked |
| TestCheckDNSResolvers_NoNameServers | moved under the same name to the same file |
| TestCheckDNSResolvers_UnreachableServer | moved under the same name to the same file, driven through a stubbed probe instead of a real timeout |
| TestDNSServerResponds_Unreachable | moved under the same name to the same file |
| TestDoctorUpdateCheckURL_Unreachable | moved under the same name to `internal/component/config/system/doctor_test.go` |
| TestDoctorUpdateCheckURL_NoConfig | moved under the same name to the same file, with a nil-tree case |
| TestDoctorGokrazyWarnsIgnoredConfig | moved under the same name to the same file |
| TestDoctorArchiveDestinations_HTTPUnreachable | moved under the same name to `internal/component/config/archive/doctor_test.go` |
| TestDoctorArchiveDestinations_FileSkipped | moved under the same name to the same file, and now also asserts the probe was never asked |
| TestDoctorConfigValidationBridge | moved to `internal/component/config/doctor_test.go` as `TestCheckSemanticsReportsAnInvalidMCPBlock`, same OAuth MCP tree and `config-mcp-invalid` |
| TestDoctorRegisteredCheckCodesHaveMetadata | kept in `internal/component/doctor/registry_test.go`; the assertion that every declared code carries a `doctor-` prefix left, because the semantics check now declares the `config-*` codes it emits and `internal/core/diagnostic/doctor_registry_test.go` `TestRegisterDoctorCheckAcceptsForeignNamespace` pins that the registry accepts them; the `diagnostic.Lookup` assertion stays and now covers those codes too |
