# Test weakenings this commit accepts

Every row is a doctor check that moved to the package owning the config it
reads. The test moved with it, case for case, and the doctor package kept no
copy because the check it drove no longer exists there. The commit is correct
without each row because the owner package holds the same assertion over the
same check body, now reached through `diagnostic.RegisterDoctorCheck`.

| Test | Reason |
|------|--------|
| checks_bgp_capture_test | the file was emptied in 130c8c54d7 when the capture-directory check moved to `internal/component/bgp/config` (`doctor_checks_test.go` holds every case, recorded row by row in that commit); an empty file carries no assertion, so deleting it removes nothing |
| checks_config_bgp_peer_test | emptied in 130c8c54d7 when the BGP peer-config and no-role checks moved to `internal/component/bgp/config`; holds no assertion |
| checks_reach_test | emptied in 130c8c54d7 when the RPKI and BMP reachability checks moved to their plugins and the probe timeout to `internal/core/diagnostic`; holds no assertion |
| checks_rir_sources_test | emptied in 130c8c54d7 when the RIR delegation-source check moved to `internal/component/resolve/cmd` (`doctor_rir_test.go`); holds no assertion |
| TestZZDumpProbe | a `fmt.Printf` probe from the Mac session that listed the registry and asserted nothing; the owner approved its deletion on 2026-09-14 and deleted the file himself on 2026-09-15 |
