| Test | Reason |
|------|--------|
| TestShowHoldsNoRowSetTheEngineCanAddress | It asserted that `show bgp adj-rib-in` holds no row set the engine can address. That was a fact about the READER, not about the answer, and this commit makes it false: `rowSet` now reads an identity map whose values share one shape, so the peer map is rows. The payload the test read is unchanged, and three tests assert more over the same producer, each observed RED before the fix: `TestShowAnswersRowsKeyedByPeer` (the `first 1` operator answers one peer's routes under that peer's address), `TestShowCountsThePeers` (the `count` operator answers the peer count rather than 1) and `TestShowRendersInEveryFormat` (the `json`, `yaml` and `table` operators each render the same payload). `TestStatusHoldsNoRowSet`, the sibling that pins the `doc` declaration on `show bgp adj-rib-in status`, is untouched. |
