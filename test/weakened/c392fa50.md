| Test | Reason |
|---|---|
| TestCIRootsRegistered | Owner decision D-8 (2026-09-25, spec-le-subject-first-command-tree): the harness no longer registers ze roots. The test moved to internal/le/harness_test.go under the same name and now reads the le test registrations, with `TestEveryHarnessCommandForwards` and `TestNoHarnessPackageRegistersARoot` beside it. |
| bigRunnerCIDirNames | Same move (D-8): the helper became `cli.BigRunnerCIDirs`, which the moved `TestCIRootsRegistered` calls. |
| coveredByBigRunner | Same move (D-8): its check is `suiteReached` in internal/le/harness_test.go. |
| dirHasCIFiles | Same move (D-8): the helper moved unchanged to internal/le/harness_test.go. |
| draftSuiteNames | Same move (D-8): the helper moved unchanged to internal/le/harness_test.go. |
| TestAnswerListsNativeActions | D-8 merges le test fixture into the harness fixture registry, so the le action table this test listed no longer exists. `TestFixtureCarriesTheStreamDrivers` pins that `dynamic` and `watchdog` are registered drivers, and the two sequence tests keep their byte-exact assertions. |
| register_stream_test | The file's assertion count changes with the row above; every stream test is unchanged. |
| TestBashHookAdmitsLeTest | D-8 removes the harness binary, so the hook's admission of it is deleted with it; there is no harness file left to admit. Heavy le commands stay admitted by `heavyArea`, pinned by `TestLossyPipeReadsTheTwoWordArea`. |
| bash_test | The file's assertion count changes with the row above. |
| TestBuildCommandsCarryTheTagsTheRunnerBuildsWith | D-8: test functional no longer builds a harness binary, so the harness build row this test pinned is gone; the ze and le build rows and their tags stay pinned by the same test. |
| TestTheUISetCarriesTheLEPersonality | D-8: every suite set now carries le (the Extras toggle is gone), so the ui-specific assertion became the general one the same test makes over the build set. |
| TestWebSessionBuildsLEForBareAndAliasVerbs | D-8: le is always built, so the le extra this test checked no longer exists; the verbs and suite label it pins are unchanged. |
| TestHarnessVariablesReadBothNames | D-8 removes `le.test.bin` and its retired spelling with the harness binary; the runner runs its own executable (`Runner.lePath`). `le.test.no.build` stays and is pinned in the runner package. |
| TestHelperBuildTagsCarryFeatureGates | D-8 removes the harness build and `testHelperBuildTags` with it; the le build carries the feature gates, pinned by `TestBuildCommandsCarryTheTagsTheRunnerBuildsWith`. |
| manifest_test | The file's assertion count changes with the row above. |
