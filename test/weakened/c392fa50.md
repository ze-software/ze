| Test | Reason |
|---|---|
| TestNoBuildReadsBothNames | Phase 3 (AC-17, AC-42, spec-le-subject-first-command-tree) deletes the retired `ze.test.no.build` fallback, so the test becomes `TestNoBuildReadsOnlyTheLeName`, which pins that only `le.test.no.build` is read and the retired spelling is ignored. |
