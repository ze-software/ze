| Test | Reason |
|---|---|
| TestStageLogNamesAreUnchangedByNamespacing | Owner decision 2026-09-24 (spec-le-subject-first-command-tree, AC-18) reverses the superseded spec's AC-14: stage log file names now follow the new command names. The test becomes `TestStageLogNamesFollowTheCommandName`, which pins the new name of every moved stage, so every stage keeps an exact file-name assertion. |
