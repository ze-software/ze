| Test | Reason |
|---|---|
| TestActionsCarryTheEightActionContracts | Renamed `TestActionsCarryTheirContracts` when the owner-approved `pty` action joined the table (2026-09-25, spec-le-subject-first-command-tree): the eight rows it pinned are still pinned, it gains the ninth `pty` row, and it adds a check that `pty` forwards its words. |
