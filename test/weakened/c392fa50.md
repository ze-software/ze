| Test | Reason |
|---|---|
| TestIsZePeerExec | Renamed `TestIsPeerExec` with its producer (`isZePeerExec` became `isPeerExec`) when the peer role moved to `le test peer` under D-8 (owner decision 2026-09-25, spec-le-subject-first-command-tree). It keeps the old spelling cases, which still resolve through the runner until phase 3, and adds the new spelling; `TestParseAndAdd_LeTestPeerIsThePeerRole` pins the role end to end. |
