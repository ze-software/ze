| Test | Reason |
|---|---|
| writeInstanceFixture | Owner deleted cmd/ze-gok on 2026-09-25 after `le build gokrazy` replaced it (spec-le-subject-first-command-tree, AC-14). The same test moved to internal/le/build/gokrazy/gokrazy_test.go with its assertions unchanged, as `writeInstanceFixture`. |
| TestZeGokPreparesParentDir | Owner deleted cmd/ze-gok on 2026-09-25 after `le build gokrazy` replaced it (spec-le-subject-first-command-tree, AC-14). The same test moved to internal/le/build/gokrazy/gokrazy_test.go with its assertions unchanged, as `TestBuildGokrazyPreparesTheInstance`. |
| TestZeGokLeavesMutatingSubcommandsAlone | Owner deleted cmd/ze-gok on 2026-09-25 after `le build gokrazy` replaced it (spec-le-subject-first-command-tree, AC-14). The same test moved to internal/le/build/gokrazy/gokrazy_test.go with its assertions unchanged, as `TestBuildGokrazyLeavesMutatingSubcommandsAlone`. |
| TestZeGokIdentifiesTheSubcommandNotAnyToken | Owner deleted cmd/ze-gok on 2026-09-25 after `le build gokrazy` replaced it (spec-le-subject-first-command-tree, AC-14). The same test moved to internal/le/build/gokrazy/gokrazy_test.go with its assertions unchanged, as `TestBuildGokrazyIdentifiesTheSubcommandNotAnyToken`. |
| TestZeGokFailsClosedOnUnpreparableParentDir | Owner deleted cmd/ze-gok on 2026-09-25 after `le build gokrazy` replaced it (spec-le-subject-first-command-tree, AC-14). The same test moved to internal/le/build/gokrazy/gokrazy_test.go with its assertions unchanged, as `TestBuildGokrazyFailsClosedOnUnpreparableParentDir`. |
| TestZeGokPassesKernelPackage | Owner deleted cmd/ze-gok on 2026-09-25 after `le build gokrazy` replaced it (spec-le-subject-first-command-tree, AC-14). The same test moved to internal/le/build/gokrazy/gokrazy_test.go with its assertions unchanged, as `TestBuildGokrazyPassesKernelPackage`. |
| repoRoot | Owner deleted cmd/ze-gok on 2026-09-25 after `le build gokrazy` replaced it (spec-le-subject-first-command-tree, AC-14). The same test moved to internal/le/build/gokrazy/gokrazy_test.go with its assertions unchanged, as `repoRoot`. |
| snapshotTracked | Owner deleted cmd/ze-gok on 2026-09-25 after `le build gokrazy` replaced it (spec-le-subject-first-command-tree, AC-14). The same test moved to internal/le/build/gokrazy/gokrazy_test.go with its assertions unchanged, as `snapshotTracked`. |
| TestZeGokLeavesTrackedTreeClean | Owner deleted cmd/ze-gok on 2026-09-25 after `le build gokrazy` replaced it (spec-le-subject-first-command-tree, AC-14). The same test moved to internal/le/build/gokrazy/gokrazy_test.go with its assertions unchanged, as `TestBuildGokrazyLeavesTrackedTreeClean`. |
| TestZeGokWithoutParentDirIsUntouched | Owner deleted cmd/ze-gok on 2026-09-25 after `le build gokrazy` replaced it (spec-le-subject-first-command-tree, AC-14). The same test moved to internal/le/build/gokrazy/gokrazy_test.go with its assertions unchanged, as `TestBuildGokrazyWithoutParentDirIsUntouched`. |
