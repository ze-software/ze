| Test | Reason |
|---|---|
| TestWebSessionBuildsChaosForBareAndAliasVerbs | Renamed `TestWebSessionBuildsLEForBareAndAliasVerbs` when the web suite stopped building a separate chaos binary and serves its dashboard through `le chaos run` (spec-le-subject-first-command-tree, owner-approved rename map). It pins the same two verbs and the same suite label; the flag it checks is now the le extra, since the chaos extra no longer exists. |
