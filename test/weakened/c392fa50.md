| Test | Reason |
|---|---|
| hostload_test | The harness now runs as `le test <name>`, so one table case is renamed from the retired binary to `le test` with the same load, counts and verdict (D-8, spec-le-subject-first-command-tree); two new tests pin the new process match (`TestIsHarnessCommand`, `TestHarnessSampleSeesALiveLeTest`). |
