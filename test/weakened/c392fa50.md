| Test | Reason |
|---|---|
| TestOnlyTheUpdateActionWrites | Owner decision D-5 (2026-09-24, spec-le-subject-first-command-tree, AC-29): `docs-to-code` becomes `doc index` with two verbs. The test becomes `TestOnlyTheWriteActionWrites` and pins exactly two actions, `check` (writes nothing) and `write` (writes). `TestDocIndexCheckCoversBothFilesAndAnchors` adds the proof that `check` judges both files and the anchors and that `write` regenerates both. |
| docstocode_test | Owner decision D-5 (2026-09-24, spec-le-subject-first-command-tree, AC-29): `docs-to-code` becomes `doc index` with the two verbs `check` and `write`, so the file loses the assertions of the retired verb names. `TestDocIndexCheckCoversBothFilesAndAnchors` and `TestOnlyTheWriteActionWrites` assert the two verbs, including exit 1 on an unresolved anchor and 3 on a stale file. |
