| Test | Reason |
|---|---|
| TestBuildAreaDeclaresThreeWritingActions | Spec-le-subject-first-command-tree rename map (2026-09-24, AC-4, AC-14): `build-artifacts` splits into `build installer` (actions `amd64`, `arm64`) and the bare command `build host-driver`. The host row, its reason text and its writes marker leave the action table because `host` is no longer an action. `TestInstallerDeclaresTwoWritingArchitectures` pins the two installer rows with their reasons and writes markers, `TestHostDriverRegistersAndRefusesAnArgument` pins the host-driver registration and its refusal of an argument, and the plan, environment and report tests in `internal/le/build/compile` still cover all three builds. |
| parity_test | The file moved from internal/le/buildartifacts to internal/le/build/compile when build-artifacts split into build installer and build host-driver (rename map, owner approval 2026-09-24), and its area test `TestBuildAreaDeclaresThreeWritingActions` left with the area. `TestInstallerDeclaresTwoWritingArchitectures` pins the two installer actions as writing, and `TestHostDriverRegistersAndRefusesAnArgument` pins host-driver registration. Every parity test is unchanged. |
| TestParseRequiresClosedKeywordsBeforeLabelAndCommand | Owner approved deleting internal/le/verify/lock on 2026-09-24 (spec-le-subject-first-command-tree): `verify lock` was `job run` without `quiet`, and the alias now runs it as `job run`. The job parser keeps its keyword and label guards, pinned by `TestALabelThatIsNotAPathComponentIsRefused` and `TestQuietIsReadBeforeTheCommandAndNowhereAfterIt` in internal/le/job. |
| TestConcurrentRunSharesOneAdmittedChildAndItsExit | Same deletion. Shared admission and exit propagation stay pinned in internal/le/job by `TestIdenticalWorkAttachesRatherThanQueueing`, `TestOneJobRunsAndTheOtherWaits` and `TestTheJobsOwnExitCodeReachesTheCaller`. |
| verifyLockHelperProcess | Same deletion: the helper process served only the deleted verify lock tests; internal/le/job has its own `TestHelperAdmit`. |
