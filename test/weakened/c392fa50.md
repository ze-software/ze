| Test | Reason |
|---|---|
| TestLeLauncherRefusesHarnessNames | Phase 3 of spec-le-subject-first-command-tree (owner-approved removal): the launcher's refusal of `--name test` existed only because a harness binary claimed `bin/le-test`, and D-8 deleted that binary. The test becomes `TestLeLauncherAcceptsTestNames`, which pins that the name is now accepted. |
| root_launcher_test | The file's assertion count changes with the row above. |
| chaosRun | The ze_chaos build that this helper drove is deleted in phase 3; the file moved to internal/le/chaos/run and drives `le chaos run` through `Answer` with the same cases. |
| TestRetiredNameAnswersAsItsNewCommand | Phase 3 deletes the dispatch alias this test pinned (owner-approved, time-bounded exception to no-layering, now ended). `TestEveryRetiredNameAnswersUnknownCommand` pins the replacement behavior for every map row. |
| TestLongestRetiredRowWins | Same alias removal; there is no rewrite left whose row choice could matter. |
| TestRetiredNameRunsItsOwnHandlerUntilItsFamilyMoves | Same alias removal; every family has moved. |
| withRenames | Helper of the alias tests above, removed with them. |
| registerVerdict | Helper of the alias tests above, removed with them. |
| dispatchStreams | Helper of the alias tests above, removed with them. |
| retired_dispatch_test | The file now holds `TestDispatchRewritesNoRetiredName`; its alias assertions went with the rows above. |
| TestEveryRetiredNameRunsItsNewCommand | Same alias removal: it becomes `TestEveryRetiredNameAnswersUnknownCommand` over the same map rows. |
| TestRenameMapRowsAreDisjoint | Moved unchanged to internal/le/doc/check with the rename map, which only the retired-name gate reads now. |
| TestRetiredHarnessWordsReachTheToolArea | Same alias removal: the retired harness words no longer reach any area. |
| TestSpecIsANamespaceAndSpecSessionRunsItsMembers | Same alias removal: bare `le spec` still lists its members (pinned by `TestBareNamespaceTokenListsItsMembers`); `spec session` no longer answers. |
| firstMember | Helper of the removed alias tests. |
| retired_test | The file's assertion count changes with the rows above. |
| TestRunnerUsesItsOwnExecutable | Phase 3 removes the runner's retired heads and shims, so the cases that rewrote them are gone; the own-executable case stays pinned by the same test. |
