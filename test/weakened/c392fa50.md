| Test | Reason |
|---|---|
| TestBuildCommandsAreExact | Owner decision D-8 (2026-09-25, spec-le-subject-first-command-tree): the terminal demo no longer builds a harness binary, so that build row left the exact list; every remaining build row keeps its exact argv and environment assertion. |
| TestL2TPScaleLocatesTheHarness | D-8: l2tp scale runs its own executable as `test l2tp-scale`, so there is no harness file to locate; `TestL2TPScaleRunsItsOwnExecutable` pins the argv. |
| fakeHarness | D-8 deleted `le test harness` and its producer (harness.go); the fake served only the tests below. Every harness command is `le test <name>`, pinned by `TestEveryHarnessCommandForwards`. |
| TestHarnessExecsLeTestWithTheTrailingArgv | D-8: no harness binary to exec. Forwarding of the trailing argv to the harness command is pinned by `TestEveryHarnessCommandForwards` and `TestAHelpWordAfterAForwardingCommandReachesTheProgram`. |
| TestHarnessBuildsOnEveryCall | D-8: there is no harness build; the harness is linked into le, which the runner and test functional build. |
| TestHarnessBuildUsesFeatureTags | D-8: no harness build; the le build carries the feature gates, pinned by `TestBuildCommandsCarryTheTagsTheRunnerBuildsWith`. |
| TestHarnessNoBuildRefusesAbsentHarness | D-8: no harness file exists to be absent; `le.test.no.build` still skips the ze build and is pinned in the runner package. |
| TestHarnessHonorsLeTestBin | D-8 deleted the harness path variable with the binary it named. |
| harness_test | The file keeps only a comment; its assertions went with the rows above. |
| TestQemuHarnessVariableHasOneDefault | D-8 deleted `le.qemu.test.bin`: the guest gets a linux le built by `buildGuestLe`, pinned by `TestQemuGuestRunsLinuxLe`. |
| TestQemuHarnessVariableReadsBothSpellings | Same deletion: no variable remains to read under two spellings. |
| harnessbin_test | The file now holds `TestQemuGuestRunsLinuxLe`; its variable assertions went with the rows above. |
