| Test | Reason |
|---|---|
| TestBuildArgvIsTheRetiredZePerfBuildRecipe | Owner decision D-2 (2026-09-24, spec-le-subject-first-command-tree, AC-26): the new perf path no longer builds a host ze-perf, so the recipe this test pinned is not produced by `perf run`. `TestPerfRunnerMountsLinuxLe` in internal/test/perfrunner pins the replacement build: GOOS=linux, CGO_ENABLED=0, output named `le`, mounted read-only, and no argv naming `bin/ze-perf`. |
| TestMeasureArgsAskTheRunnerToBuildImagesAndTest | Owner decision D-1 (same spec, AC-24): `perf run` hands the runner its steps as `step build` and `step test` keywords instead of `--build --test` argv. `TestPerfRunAcceptsRunnerOptionsAsKeywords` pins every keyword form, and `TestExecuteRefusesARunWithNoStep` pins the empty case. |
| TestRunBuildsThenMeasuresThenRecords | Same decision: with no host ze-perf build step, the sequence is measure then record. `TestRunMeasuresThenRecords` pins it, and `TestRunBuildOnlyRecordsNothing` pins the build-only step. |
| TestRunStopsWhenTheBuildFails | Same decision: the failing step that stops a run is now the measurement, pinned by `TestRunStopsWhenTheMeasurementFails`; the linux le build failure is refused inside the runner (`TestPerfRunnerRefusesALinuxBuildWithoutTags`). |
| build | The recorder's build method became `command` when the host ze-perf build left the run (D-2); it records the same argv, directory and environment. |
| bench_test | The assertion count changes with the four rows above; every remaining test is unchanged. |
