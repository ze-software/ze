# Test weakenings this commit accepts

Every row below is a relocation or a helper collapse; no behavior lost its
assertion. The five review rounds that produced this commit checked each one
at the producer (`tmp/review/storage-1-backend-parity-*.md`).

| Test | Reason |
|---|---|
| TestDoReloadPromotesCandidateOnSuccess | `storage.WritePointer`/`ParseVersionStamp` had no product caller and were removed; the seeded stamps now go through `setConfigPointer`, which writes the same key through `store.WriteFile` under the store's own guard, and the `require.NoError` lines on the deleted helpers went with them. Every promoted stamp is still read back and compared equal |
| TestDoReloadClearsCandidateOnFailure | Same collapse: the assertions that disappeared were `require.NoError` on `WritePointer`/`ParseVersionStamp`; the candidate-cleared assertion is unchanged |
| TestDoReloadRollsBackOnListenerMigrationFailure | Same collapse; rollback pointer and served config still asserted |
| TestClearStaleCandidateOnBoot | Same collapse; the stale candidate is still asserted gone |
| TestEnsureActivePointerProtectsFailedFirstSIGHUP | Same collapse; the active pointer is still asserted untouched |
| TestReadConfigWithStorage_BlobStorage | Same collapse (`ParseVersionStamp` + `WritePointer` seeding); the config read back is unchanged |
| TestReadConfigWithStorage_ReadsPromotedVersion | Same collapse for three seeded stamps; the promoted version read is unchanged |
| TestWireManagedCommitStagesCandidateAndPromotes | Same collapse; staged candidate and promotion still asserted |
| TestWireManagedCommitClearsCandidateOnReloadFailure | Same collapse; candidate cleared still asserted |
| mustParseManagedStamp | Helper: its one `require.NoError` wrapped the deleted `ParseVersionStamp`; it now parses with the same layout the product uses and fails the test on error |
| TestBootPowerUsersSaysSoWhenZefsIsUnreadable | Renamed to `TestBootPowerUsersWarnsWhenUsernameHasNoHash`: `usersFromStore` treats an absent username as no user by design (review round 1, I6), so the unreadable case it asserted is the username-without-hash case, which the renamed test asserts |
| TestUsersFromZefsDBFailsClosedOnMissingCreds | Split into `TestUsersFromZefsDBAbsentCredentialsIsNoUser` (absent username is no user, silent) and `TestUsersFromZefsDBFailsClosedOnMissingHash` (username present, hash absent, error); together they assert more than the deleted test |
| TestRootDispatchPassesRuntimeContext | Lost the assertion on `ResolveStorage`, a field deleted with the `SetRuntimeStorage`/`StorageAs` seam that had no product caller; the remaining fields are still asserted |
| TestModelExecutorAsksForTheDispatcherJSON | Its subject `modelExecutor` (and `dashboardPoller`) lost their only product caller in ba67a8b89c and are deleted; nothing reaches them |
| main_test | The file-level count dropped by that deleted test |
| TestSaveDraftAppliesToDraft | The `FileExists` check on a raw path is subsumed by the `store.Stat` assertion; the editor writes the tree store, not a loose file |
| TestStorageAsTypeAssertsRuntimeStorage | Subject `StorageAs` deleted with the seam (no product caller) |
| registry_test | The deleted test's one table case |
| TestImportRefusesCorruptSource | The `require.NoError(glob)` moved into the `archivesOf` helper, which fails the test on a glob error; the refusal assertion is unchanged |
| newBlobStorage | Test helper for the removed live blob backend; deleted with it |
| TestZeInitForce | `RunWithReaderForce` returned an always-nil error; the `if err != nil` check went with the return value; the exit code and store content assertions are unchanged |
| TestZeInitForceNoExisting | Same |
| newDaemonStateClient | Helper only: the always-nil result the linter flagged is replaced by `installDaemonState(t)` plus a `daemonStateClient{}` value; every assertion in the RFC-tagged tests that use it is unchanged, so no claim changed |
