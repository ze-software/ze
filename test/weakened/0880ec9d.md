# Test weakenings this commit accepts

Every row below is a relocation or a helper collapse; no behavior lost its
assertion. The five review rounds that produced this commit checked each one
at the producer (`tmp/review/storage-1-backend-parity-*.md`).

| Test | Reason |
|---|---|
| stringConstants | Test helper of `TestDispatchParity` that parsed literal string constants; replaced by a `packages.Load` type-check reading each case's constant value from `TypesInfo`, which resolves a selector constant too; the parity assertion is unchanged and now covers `subcmdShow = command.VerbShow` |
| mustUnquote | Helper of the deleted literal parser; nothing left to unquote |
