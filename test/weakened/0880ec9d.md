# Test weakenings this commit accepts

Every row below is a relocation or a helper collapse; no behavior lost its
assertion. The five review rounds that produced this commit checked each one
at the producer (`tmp/review/storage-1-backend-parity-*.md`).

| Test | Reason |
|---|---|
| TestDomainGroupRefreshFiresAtTTL | The two assertions (`found`, `due.After(now)`) moved inside one `require.Eventually`: the resolve callback returns before `resolveAndRecord` reaches `sched.arm`, so reading the schedule at that instant races the worker; the same two facts are still asserted, once the worker has written them |
