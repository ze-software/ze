# Test weakenings this commit accepts

Every row below is a relocation or a helper collapse; no behavior lost its
assertion. The five review rounds that produced this commit checked each one
at the producer (`tmp/review/storage-1-backend-parity-*.md`).

| Test | Reason |
|---|---|
| TestOpenFolderPrivateContainment | Asserted the ancestor-directory refusal the owner removed under AC-4 (2026-09-17); replaced by `TestOpenFolderKeepsContainingMode`, which asserts a 0775 parent is accepted and its mode left alone; root, frame, owner and symlink assertions are unchanged |
| open_test | The file-level count dropped by that replaced test |
| TestTreeIntegrityPrivateContainment | Both arms asserted the ancestor rule that no longer exists; the root/frame checks it shared are asserted by `TestOpenRejectsInsecureNodes` and the zefs frame tests |
| check_tree_unix_test | The file-level count dropped by that deleted test |
| TestStorageForResolution | Moved verbatim into `internal/core/resolve/resolve_test.go` beside the grandfathered `StorageFor` it tests; `storage_test.go` is deleted because the leaf tier may not import the storage component |
