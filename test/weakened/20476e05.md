| Test | Reason |
|------|--------|
| TestRunReportsDriverFailure | Nothing left the suite. The counted assertion is the `if err != nil { t.Fatal(err) }` guarding `os.Pipe()`, which moved into the new `readOneReport` helper in the same file. The helper is `t.Helper()`-marked and calls `t.Fatal` on the test's own goroutine, so a pipe failure still fails this test at the same point. The behavioural assertion is unchanged, character for character: exit code not 1, or the report not containing `observerFailure` followed by "broken observation". The pipe check now covers three call sites where it covered two, so the suite gained a site rather than losing one. |
| TestRunConvertsPanicToObserverFailure | Same move, same file, same helper. The behavioural assertion is unchanged: exit code not 1, or the report not containing `observerFailure` followed by "fixture panic: broken invariant". The helper also clears `reportedOnce`, which is why this test now passes in a full package run: `ReportFailure` emits the first failure of a process and drops every later one, so this case read an empty stream whenever `TestRunReportsDriverFailure` ran before it, and passed only when run alone. The change makes the test able to fail for its own reason, which is the opposite of a weakening. |
