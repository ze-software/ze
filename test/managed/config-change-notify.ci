# Test: config-changed notification protocol types
#
# VALIDATES: AC-3 -- config-changed notification carries version hash.
# PREVENTS: Stale version in notification causing fetch loop.
#
# Note: This exercises ze's config validation with server and client blocks
# through a compiled fixture.


cmd=foreground:seq=1:exec=ze-test fixture managed/config-change-notify:timeout=10s
expect=exit:code=0
