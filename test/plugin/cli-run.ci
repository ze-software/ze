# Test: ze run help lists all available commands
# VALIDATES: run command dispatch is wired and lists commands from YANG RPCs
# PREVENTS: run command dispatch broken or missing command list

cmd=foreground:seq=1:exec=ze run help
expect=exit:code=0
expect=stderr:contains=ze run
expect=stderr:contains=Available commands
