# Test: ze run prints deprecation error with migration hints
# VALIDATES: ze run is replaced by direct verb dispatch
# PREVENTS: users silently getting no output from removed command

cmd=foreground:seq=1:exec=ze run help
expect=exit:code=1
expect=stderr:contains=replaced
expect=stderr:contains=ze show
