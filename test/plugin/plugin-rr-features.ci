# plugin-rr-features - verify route-reflector plugin --features output
# Tests that ze plugin bgp-rr --features lists supported features

cmd=foreground:seq=1:exec=ze plugin bgp-rr --features
expect=exit:code=0
