# iface-registered - verify interface plugin is registered and discoverable
# Tests that ze recognises the interface plugin via CLI dispatch.

cmd=foreground:seq=1:exec=ze plugin interface --help
expect=exit:code=1
