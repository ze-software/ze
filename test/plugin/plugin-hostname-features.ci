# plugin-hostname-features - verify hostname plugin --features output
# Tests that ze plugin bgp-hostname --features lists supported features

cmd=foreground:seq=1:exec=ze plugin bgp-hostname --features
expect=exit:code=0
expect=stdout:contains=capa
