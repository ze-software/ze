# plugin-hostname-features - verify hostname plugin --features output
# Tests that ze bgp plugin hostname --features lists supported features

cmd=foreground:seq=1:exec=ze bgp plugin hostname --features
expect=exit:code=0
expect=stdout:contains=capa
