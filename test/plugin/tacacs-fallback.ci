# Test: an unreachable TACACS+ server falls back to local bcrypt.
# VALIDATES: AC-3 and AC-11.

stdin=ze-conf:terminator=EOF_CONF
bgp {
	peer peer1 {
		connection { remote { ip 127.0.0.1; } local { ip 127.0.0.1; accept false; } }
		session { asn { local 65533; remote 65533; } }
	}
}
system {
	authentication {
		user admin {
			password "$2a$04$UlwuiuH82Unfsq.XEMPGJeDkXwbm3KW.nvVaVXOd/JeFK8VjMjrQO"
			profile [ admin ]
		}
		tacacs {
			server 127.0.0.1 { port 1; key "ignored"; }
			timeout 1
		}
		tacacs-profile 15 { profile [ admin ]; }
	}
	authorization {
		profile admin { run { default-action allow; } edit { default-action allow; } }
	}
}
environment { ssh { enabled true; server main { ip 127.0.0.1; port $PORT2; } } }
plugin {
	external tacacs-fallback-test {
		run "ze-test fixture plugin/tacacs-fallback $PORT2"
		encoder json
	}
}
EOF_CONF

cmd=foreground:seq=1:exec=ze -:stdin=ze-conf:timeout=25s
expect=exit:code=0
expect=stderr:pattern=auth success.*source=local
reject=stderr:pattern=auth success.*source=tacacs
expect=stderr:pattern=OK: summary ran via local fallback
reject=stderr:pattern=ZE-OBSERVER-FAIL
