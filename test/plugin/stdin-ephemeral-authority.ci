# Test: stdin-ephemeral-authority
# VALIDATES: storage backend parity through the production binary and decoded persistent values.
# PREVENTS: silent loose-file fallback or loss of state at restart.
option=timeout:value=90s
cmd=foreground:seq=1:exec=le-test fixture storage/tree stdin-ephemeral-authority:timeout=90s:exit=0
expect=exit:code=0
expect=stdout:contains=OK: stdin-ephemeral-authority
