# Test: authorization denies restricted users for real commands outside the allow list.
# VALIDATES: restricted default deny rejects unmatched commands and names access control.
# PREVENTS: authorization checks being bypassed after command resolution.

cmd=foreground:seq=1:exec=ze-test fixture plugin/authz-deny:timeout=15s
expect=exit:code=0
