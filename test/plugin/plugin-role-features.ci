# plugin-role-features - verify role plugin --features output
# Tests that ze plugin bgp-role --features lists supported features

cmd=foreground:seq=1:exec=ze plugin bgp-role --features
expect=exit:code=0
expect=stdout:contains=capa
expect=stdout:contains=yang
