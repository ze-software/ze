# Test: the audit log survives daemon restart and records reloads.
# VALIDATES: daemon reload is audited and persisted records reload after restart.
# PREVENTS: audit entries being visible only in the process that created them.

option=timeout:value=30s
cmd=foreground:seq=1:exec=ze-test fixture plugin/audit-persistence:timeout=30s
expect=exit:code=0
