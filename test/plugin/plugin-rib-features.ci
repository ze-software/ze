# plugin-rib-features - verify rib plugin --features output
# Tests that ze bgp plugin bgp-rib --features lists supported features

cmd=foreground:seq=1:exec=ze bgp plugin bgp-rib --features
expect=exit:code=0
expect=stdout:contains=yang
