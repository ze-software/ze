# Test: an authenticated user without a profile is denied when assignments exist.
# VALIDATES: unassigned users fail closed.
# PREVENTS: unassigned users silently becoming admin-equivalent.

cmd=foreground:seq=1:exec=ze-test fixture plugin/authz-default:timeout=15s
expect=exit:code=0
