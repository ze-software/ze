# Test: SIGHUP reload applies a changed `hello-interval` to the
#       running subsystem's effective config.
#
# VALIDATES: spec-l2tp-7 AC-13 -- hello-interval is hot-applied by
#            Subsystem.Reload. Proves the diff-apply path (reactor
#            setHelloInterval + s.params update) is reached by a
#            real SIGHUP, and the new value surfaces through
#            EffectiveConfig to `show l2tp config`.
# PREVENTS:  regression where reload parses the new value but never
#            stores it (silent no-op Reload).
#
# Runs under test/plugin/ so the TLS plugin hub is configured; an
# L2TP-only SIGHUP scenario still needs a BGP peer in config to
# trigger hub setup.

option=env:var=ze.l2tp.skip-kernel-probe:value=true
stdin=peer:terminator=EOF_PEER
option=tcp_connections:value=1
option=timeout:value=20s
expect=bgp:conn=1:seq=1:hex=FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF00170200000000
EOF_PEER

# The second config text that the compiled `plugin/reload-hello-interval`
# fixture driver copies over ze-bgp.conf before SIGHUP. Only hello-interval changes (45 -> 90).
tmpfs=config2.conf:terminator=EOF_CONF2
plugin {
	external reload-hello-interval-test {
		run "ze-test fixture plugin/reload-hello-interval"
		encoder json
	}
}

environment {
	log {
		l2tp debug
	}
	l2tp {
		server main {
			ip 127.0.0.1
			port $PORT2
		}
	}
}

l2tp {
	enabled true
	hello-interval 90
}

bgp {
	peer peer1 {
		connection {
			remote {
				ip 127.0.0.1
			}
			local {
				ip 127.0.0.1
				accept false
			}
		}
		session {
			asn {
				local 1
				remote 1
			}
			router-id 1.2.3.4
			family {
				ipv4/unicast { prefix { maximum 10000; } }
			}
			capability {
				graceful-restart disable
			}
		}
		behavior {
			group-updates disable
		}
	}
}
EOF_CONF2


stdin=ze-conf:terminator=EOF_CONF
plugin {
	external reload-hello-interval-test {
		run "ze-test fixture plugin/reload-hello-interval"
		encoder json
	}
}

environment {
	log {
		l2tp debug
	}
	l2tp {
		server main {
			ip 127.0.0.1
			port $PORT2
		}
	}
}

l2tp {
	enabled true
	hello-interval 45
}

bgp {
	peer peer1 {
		connection {
			remote {
				ip 127.0.0.1
			}
			local {
				ip 127.0.0.1
				accept false
			}
		}
		session {
			asn {
				local 1
				remote 1
			}
			router-id 1.2.3.4
			family {
				ipv4/unicast { prefix { maximum 10000; } }
			}
			capability {
				graceful-restart disable
			}
		}
		behavior {
			group-updates disable
		}
	}
}
EOF_CONF

cmd=background:seq=1:exec=ze-peer --port $PORT:stdin=peer
cmd=background:seq=2:exec=ze-test fixture plugin/reload-hello-interval-trigger
cmd=foreground:seq=3:exec=ze -:stdin=ze-conf:timeout=20s
expect=exit:code=0
expect=stderr:contains=l2tp reload: hello-interval updated
