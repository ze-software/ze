# plugin-flowspec-features - verify flowspec plugin --features output
# Tests that ze bgp plugin bgp-flowspec --features lists supported features

cmd=foreground:seq=1:exec=ze bgp plugin bgp-flowspec --features
expect=exit:code=0
expect=stdout:contains=nlri
