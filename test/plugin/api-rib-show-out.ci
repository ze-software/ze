# Test: show bgp rib sent queries Adj-RIB-Out
# VALIDATES: show bgp rib sent dispatches through engine to bgp-rib plugin
# PREVENTS: rib show-out proxy handler broken or bgp-rib plugin unreachable

# ze-peer expectations
stdin=peer:terminator=EOF_PEER
option=tcp_connections:value=1
option=timeout:value=15s
expect=bgp:conn=1:seq=1:hex=FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF00170200000000
EOF_PEER

# Python test plugin: exercises dispatch-command for show bgp rib sent
tmpfs=rib-show-out-test.run:mode=755:terminator=EOF_RUN
#!/usr/bin/env python3
import sys
import time
from ze_api import API, result_text_data

def dispatch(api, command):
    resp = api._call_engine(
        'ze-plugin-engine:dispatch-command', {'command': command})
    if resp is None:
        return {'status': 'error', 'data': 'engine call returned None'}
    return resp.get('result', {})

def main():
    api = API()
    api.declare_done()
    api.wait_for_config()
    api.capability_done()
    api.wait_for_registry()
    api.ready()
    api.wait_for_post_startup(timeout=5.0)

    # Wait for the BGP session to establish, then quiesce so ze's End-of-RIB
    # reaches the wire before `request shutdown` closes the session. Without this
    # the peer's EOR expectation is unreachable by construction: the script can
    # shut ze down during the OPEN handshake. PendingSync stays set from
    # establishment (peer_run.go sendingInitialRoutes) until after the EOR is
    # sent (peer_initial_sync.go), and the bgp-peer-sync quiescer blocks on
    # exactly that, so this barrier cannot return early.
    established = api.dispatch_until(
        'show bgp peer peer1 detail',
        lambda r: r.get('status') == 'done' and 'established' in result_text_data(r).lower(),
        attempts=40,
    )
    if 'established' not in result_text_data(established).lower():
        print('FAIL: peer1 never reached established state', file=sys.stderr)
        dispatch(api, 'request shutdown')
        api.wait_for_shutdown()
        sys.exit(1)
    api.quiesce()

    try:
        result = dispatch(api, 'show bgp rib sent')
        if result.get('status') != 'done':
            print(f'FAIL: rib show-out status={result.get("status")} data={result.get("data")}', file=sys.stderr)
            sys.exit(1)
        print('OK: show bgp rib sent dispatched', file=sys.stderr)
    finally:
        dispatch(api, 'request shutdown')
        api.wait_for_shutdown()

if __name__ == '__main__':
    main()
EOF_RUN

# ze bgp configuration
stdin=ze-bgp:terminator=EOF_CONF
plugin {
	external rib-show-out-test {
		run ./rib-show-out-test.run
		encoder json
	}
	internal bgp-rib {
		use bgp-rib
	}
}

bgp {
	peer peer1 {
		connection {
			remote {
				ip 127.0.0.1
			}
			local {
				ip 127.0.0.1
				accept false
			}
		}
		session {
			asn {
				local 1
				remote 1
			}
			router-id 1.2.3.4
			family {
				ipv4/unicast { prefix { maximum 10000; } }
			}
			capability {
				graceful-restart disable
			}
		}
		behavior {
			group-updates disable
		}

		process rib-show-out-test {
		}
		process bgp-rib {
			receive [ state ]
			send [ update ]
		}
	}
}
EOF_CONF

# Process orchestration
cmd=background:seq=1:exec=ze-peer --port $PORT:stdin=peer
cmd=foreground:seq=2:exec=ze -:stdin=ze-bgp:timeout=15s
expect=exit:code=0
