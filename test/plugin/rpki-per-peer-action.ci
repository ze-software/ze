# Test: RPKI per-peer action override (AC-2, AC-10)
# VALIDATES: global action/invalid=reject, but peer1 overrides action/invalid=accept, so an
#            Invalid-origin route from peer1 is ACCEPTED; show bgp rpki status reports the override.
# PREVENTS: per-peer action being ignored (route rejected by global) or status not reflecting it.

# ze-peer: send route 10.0.1.0/24 from AS 65999 (VRP has AS 65001) => Invalid origin
stdin=peer:terminator=EOF_PEER
# option=linger holds the peer open past its own script. The observer reads the
# adj-RIB-in AFTER the exchange, and AdjRIBInManager.handleState
# (internal/component/bgp/plugins/adj_rib_in/rib.go) does `delete(r.ribIn,
# peerAddr)` the moment the session goes down, so a peer that exits on its last
# expect leaves the observer reading an empty RIB and reporting a drop that
# never happened.
option=linger:value=true
option=tcp_connections:value=1
option=timeout:value=15s
option=update:value=send-route:prefix=10.0.1.0/24:origin-as=65999:next-hop=10.0.0.1
# The FENCE. Same origin AS, so it takes the same Invalid verdict, the same
# per-peer override and the same path: pending, then promoted by the
# validator's batch-validate, which applies its decisions in slice order
# under one lock (adj_rib_in/rib_commands.go). The fence therefore cannot
# be installed before the route under test, so the observer waits for THIS
# one and then reads the other once. A route through the software is the
# synchronisation point; a poll on the route under test would be a sleep
# with a loop around it, and it would pass on a fast host and fail on a
# slow one.
option=update:value=send-route:prefix=10.0.99.0/24:origin-as=65999:next-hop=10.0.0.1
# EOR from ze
expect=bgp:conn=1:seq=1:hex=FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF00170200000000
EOF_PEER

# Native observer fixture: plugin/rpki-per-peer-action.

stdin=ze-bgp:terminator=EOF_CONF
plugin {
	hub {
		server local {
			ip 127.0.0.1;
			port 0;
			secret rpki-test-secret-key-01234567890;
		}
	}
	external rpki-per-peer-test {
		run "ze-test fixture plugin/rpki-per-peer-action"
		encoder json
	}
	internal rpki {
		use bgp-rpki
	}
	internal adj-rib-in {
		use bgp-adj-rib-in
	}
}

bgp {
	rpki {
		cache-server 127.0.0.1 {
			port $PORT2
		}
		action {
			invalid reject;
		}
	}
	peer peer1 {
		connection {
			remote {
				ip 127.0.0.1
			}
			local {
				ip 127.0.0.1
				accept false
			}
		}
		rpki {
			action {
				invalid accept;
			}
		}
		session {
			asn {
				local 1
				remote 1
			}
			router-id 1.2.3.4
			family {
				ipv4/unicast { prefix { maximum 10000; } }
			}
			capability {
				graceful-restart disable
			}
		}
		behavior {
			group-updates disable
		}

		attach process rpki-per-peer-test {
			receive [ update state ]
		}
		attach process rpki {
			receive [ update-received ]
		}
		attach process adj-rib-in {
			receive [ update-received state ]
		}
	}
}
EOF_CONF

# VRP: 10.0.0.0/8 maxlen=24 AS=65001 (route from AS 65999 = Invalid)
cmd=background:seq=1:exec=ze-test rtr-mock --port $PORT2 --vrp 10.0.0.0/8,24,65001
cmd=background:seq=2:exec=ze-peer --port $PORT:stdin=peer
cmd=foreground:seq=3:exec=ze -:stdin=ze-bgp:timeout=20s
