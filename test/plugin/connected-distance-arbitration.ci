# Test: an interface address and a BGP route name one prefix, and the connected
#       route takes it while Ze programs nothing for it.
# VALIDATES: spec-connected-static-reach-the-locrib AC-3/AC-6 -- a connected
#            prefix at the default distance 0 outranks a BGP path, and winning
#            means Ze installs NOTHING: the kernel already created the route when
#            the address was assigned, so there is no RTPROT_ZE entry for it.
# PREVENTS: a connected prefix being invisible to arbitration, which is what made
#           `rib { distance { connected N } }` decide nothing at all; and the
#           opposite failure, Ze programming a second entry for a prefix the
#           kernel already forwards on.
#
# The sibling test connected-distance-raised-loses.ci is the same configuration
# with `connected 250`, where BGP takes the prefix and Ze DOES program it. The two
# differ in one number, so what they prove is that the number decides.
#
# Requires Linux and CAP_NET_ADMIN: the driver creates a dummy device and assigns
# it an address WHILE the daemon runs, because the interface monitor emits
# `interface`/`addr-added` from a netlink event and an address that was already
# there at startup produces none. The check then reads the kernel routing table.
option=needs-linux:caps=net-admin

# ze-peer holds the session open so the BGP RIB reaches its initial-sync EOR; the
# route itself is injected by the fixture rather than announced, so no UPDATE is
# expected on the wire.
stdin=peer:terminator=EOF_PEER
option=tcp_connections:value=1
option=timeout:value=20s
expect=bgp:conn=1:seq=1:hex=FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF00170200000000
EOF_PEER

stdin=ze-bgp:terminator=EOF_CONF
plugin {
	external connected-distance-test {
		run "ze-test fixture plugin/connected-distance-arbitration"
		encoder json
	}
	internal bgp-rib {
		use bgp-rib
	}
	internal rib {
		use rib
	}
	internal fib-kernel {
		use fib-kernel
	}
}

interface {
	backend netlink
}

connected {
}

rib {
	distance {
		connected 0
	}
}

bgp {
	peer peer1 {
		connection {
			remote {
				ip 127.0.0.1
			}
			local {
				ip 127.0.0.1
				accept false
			}
		}
		session {
			asn {
				local 1
				remote 2
			}
			router-id 1.2.3.4
			family {
				ipv4/unicast { prefix { maximum 10000; } }
			}
			capability {
				graceful-restart disable
			}
		}
		behavior {
			group-updates disable
		}

		attach process bgp-rib {
			receive [ update state refresh ]
			send [ update ]
		}
	}
}
EOF_CONF

option=env:var=ze.log.connected:value=info
option=env:var=ze.log.rib:value=info

cmd=background:seq=1:exec=ze-peer --port $PORT:stdin=peer
cmd=foreground:seq=2:exec=ze -:stdin=ze-bgp:timeout=20s
expect=exit:code=0
expect=stderr:contains=OK: 10.9.0.0/24 is won by connected, ze-programmed=no
reject=stderr:pattern=panic
reject=stderr:pattern=fatal error
