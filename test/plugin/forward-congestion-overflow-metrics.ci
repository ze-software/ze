# Test: overflow metrics registered and visible via metrics values endpoint
#
# Flow:
# 1. BGP session establishes with telemetry enabled
# 2. compiled Go plugin dispatches "show metrics values" via dispatch-command
# 3. Verifies overflow-specific Prometheus metrics appear in output
# 4. Shuts down daemon
#
# VALIDATES: Phase 2 deferred — .ci functional test for ze_bgp_overflow_* metrics.
# PREVENTS: Overflow metrics not registered or not reachable via dispatch chain.

# ze-peer expectations
stdin=peer:terminator=EOF_PEER
option=tcp_connections:value=1
option=timeout:value=15s

# EOR
expect=bgp:conn=1:seq=1:hex=FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF00170200000000
EOF_PEER

# compiled Go test plugin: verifies overflow metrics appear in Prometheus output

# ze bgp configuration with telemetry enabled
stdin=ze-bgp:terminator=EOF_CONF
plugin {
	external overflow-metrics-test {
		run "ze-test fixture plugin/forward-congestion-overflow-metrics"
		encoder json
	}
}

telemetry {
	prometheus {
		enabled true
		server main {
			ip 127.0.0.1;
			port $PORT2;
		}
	}
}

bgp {
	peer peer1 {
		connection {
			remote {
				ip 127.0.0.1
			}
			local {
				ip 127.0.0.1
				accept false
			}
		}
		session {
			asn {
				local 1
				remote 1
			}
			router-id 1.2.3.4
			family {
				ipv4/unicast { prefix { maximum 10000; } }
			}
			capability {
				graceful-restart disable
			}
		}
		behavior {
			group-updates disable
		}
	}
}
EOF_CONF

# Process orchestration
cmd=background:seq=1:exec=ze-peer --port $PORT:stdin=peer
cmd=foreground:seq=2:exec=ze -:stdin=ze-bgp:timeout=15s
expect=exit:code=0
