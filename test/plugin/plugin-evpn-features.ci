# plugin-evpn-features - verify evpn plugin --features output
# Tests that ze plugin bgp-evpn --features lists supported features

cmd=foreground:seq=1:exec=ze plugin bgp-evpn --features
expect=exit:code=0
expect=stdout:contains=nlri
