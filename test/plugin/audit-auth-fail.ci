# Test: failed SSH authentication is visible through show audit.
# VALIDATES: failed authentication attempts emit auth-fail audit entries.
# PREVENTS: SSH login failures being visible only in logs.

option=timeout:value=25s
cmd=foreground:seq=1:exec=ze-test fixture plugin/audit-auth-fail:timeout=25s
expect=exit:code=0
