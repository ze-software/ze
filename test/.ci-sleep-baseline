# ci-sleep ratchet baseline -- composable delta form.
#
# The allowed time.sleep( ceiling for test/**/*.ci is the SUM of every
# signed-integer line below (this comment block is ignored). The first line is
# the origin count; each later change that removes sleeps appends its own signed
# delta line (e.g. `-3`) instead of editing a shared integer. Parallel removals
# land on distinct lines and never merge-conflict, yet the sum still ratchets
# down and a net rise (count above the sum) fails the gate. A `+N` line raises
# the ceiling and is the explicit-approval knob.
#
# Parsed by scripts/dev/verify_wiring_docs.py::parse_sleep_baseline.
125
-11
-12
-1
# test/policy/policy-reload.ci: the 0.5s post-boot "settle" and the 1.5s post-SIGHUP
# "hold" became wait_rules(), a bounded poll on the nft ruleset. Net -1, not -2:
# the helper keeps one poll-interval sleep, which is a granularity knob on a real
# condition wait rather than a guessed duration.
-1
# test/policy/*.ci: the whole suite, 12 -> 0 (spec-fixit-migrate-sleeps-infra
# P1+P3). Each driver's hand-rolled daemon-readiness loop became
# ze_api.wait_for_daemon_ready, and each blind post-apply "settle" became
# ze_api.wait_for_output -- a bounded poll on the same kernel readback the test
# already asserts on. The remaining poll-interval sleeps did not disappear; they
# moved into those two helpers, which the gate does not count because it scans
# test/**/*.ci, not test/scripts/ze_api.py.
#
# 006 reaches 0 too: its wait_rules() now delegates to the shared helper while
# keeping its own 12s deadline, derived per call from the budget still remaining.
#
# Verified on Linux via `make ze-netns-test ZE_NETNS_SUITES=policy` (not the
# darwin host this spec was previously stalled on): 3x 6/6, and two source
# mutations prove the waits are not vacuous -- skipping firewall.ApplyAll fails
# all 6, while skipping rm.applyAll (ip rules only) fails exactly 002 and 005,
# the two that read `ip rule show`.
-12
# test/firewall/*.ci: the whole suite, 8 -> 0 (spec-fixit-migrate-sleeps-infra
# P1+P3), same two helpers.
#   firewall-cli-show: the blind hold before connecting to the SSH CLI became a wait
#     on 127.0.0.1:2222 actually accepting. daemon.ready does NOT cover that
#     listener -- it binds separately from OnConfigure -- so this one could not
#     be deleted, only pointed at the right signal.
#   firewall-set-element-timeout: the post-apply settle became a poll on the set's
#     ELEMENTS, not on the set existing; nft prints an empty `elements = { }`
#     body for a declared-but-unpopulated set, which an existence check would
#     have accepted while both assertions still failed.
#   ddos-local-withdraw: three already-bounded polls moved into the helpers,
#     conditions and failure messages unchanged.
#
# Verified on Linux, 3x 23/23, against a zetest-tagged DUT (see below). One
# source mutation proves non-vacuity: dropping set elements in
# nft/lower_linux.go:191-192 fails exactly test 9 and nothing else.
#
# NOTE for whoever runs these next: `make ze-netns-test` reports
# ddos-local-withdraw as FAILING, and that is the target's own defect, not the
# test's. It depends on $(ZEBIN_ZE) (mk/test-integration.mk:137), the PRODUCTION
# binary, which mk/test-integration.mk:439 documents as carrying "neither zetest
# nor ze_test" -- so the ddos/fake config node this test needs does not exist and
# the daemon dies with "unknown field in ddos: fake". With a zetest-tagged DUT
# the same test passes in 653ms.
-8

# test/plugin/bgp-redistribute-{filtered-out,metrics}.ci: the blind 2s "drain"
# each held before shutdown is REPLACED by a real barrier plus a real assertion
# -- assert_peer_received_only_eor for filtered-out (the absence it claims) and
# assert_peer_received_beyond_eor for metrics (the four accepted operations).
# Both wait for the End-of-RIB and then `request quiesce`, so the counter they
# read is a settled total. Neither test could observe a leak before: a
# check-mode ze-peer closes as soon as its expectations are met, so the emits
# ran against a peer that was already gone (measured: state='connecting',
# connections-dropped=1). option=linger keeps the session up.
-1

# test/plugin/llgr-readvertise-multipeer.ci: 3 -> 0, and NO delta line, because
# this removal PAYS FOR a rise it did not cause. 6ec2a10bc added
# test/plugin/llgr-egress-state-unloaded.ci, a copy of this observer carrying the
# same three sleeps, and took the count from 79 to 82 with no baseline line. That
# file is RFC-tagged (RFC9494-4.3-3), so .claude/hooks/pretool-writeedit.py
# refuses an unapproved edit to it; its three sleeps stay until Thomas approves
# the same conversion there. Count and ceiling are both 79 again.
#   The 0.25s best-route poll became dispatch_until on the same predicate, whose
#     interval lives in ze_api and is not counted.
#   The 1.5s "let the reflection land" became wait_peer_eor_sent(2) plus a poll
#     on obsn's own updates-sent minus eor-sent -- the two writes obsn's expect
#     rules read, counted by IncrUpdatesSent after the bytes reach the socket.
#   The 2.0s "let the readvertise land" became the same counter, baselined
#     before mark-stale and waited past it.
#
# The conversion exposed what the sleeps were hiding, and the fix is in the test
# fixture, not in the waits: the source ze-peer is check-mode, so it closed the
# instant its End-of-RIB expect matched, and that close reached ze as a peer-down
# that WITHDREW 10.0.0.0/24 from obsn. obsn read the withdrawal as its seq=2 and
# the depreference never came. The blind 1.5s+2.0s was beating that withdrawal by
# under a second on this host. option=linger:value=true on the source holds the
# session open, which is what the header always claimed the test did: the stale
# transition is driven by mark-stale + clear-rib-out, never by a disconnect.
#
# Verified on Linux: 5x 5/5 (avg 5.5s, down from 11.3s). One source mutation
# proves the waits are not vacuous -- dropping the two mods.Op calls in the iBGP
# branch of LLGREgressFilter (gr/gr_egress.go) fails it 3x 0/3.

# test/plugin/llgr-egress-state-unloaded.ci: 3 -> 0. This is the rise 6ec2a10bc
# caused, now paid for at its source, so the ceiling drops 79 -> 76 and the
# comment above about a removal paying for someone else's rise is spent.
# The file is RFC-tagged (RFC9494-4.3-3), so Thomas approved the change on
# 2026-08-12. No expect, reject, hex or config line was touched: only the
# observer's waits, and option=linger on the source peer.
#   The 0.25s best-route poll became dispatch_until on the same predicate.
#   The 1.5s and 2.0s holds became wait_peer_eor_sent(2) and two polls on obsn's
#     updates-sent minus eor-sent, which IncrUpdatesSent bumps after the bytes
#     reach the socket.
#
# It had the SAME race as its sibling, and the barriers proved it rather than
# inheriting the finding: with the barriers in and option=linger commented out,
# 5x 0/5, obsn receiving UPDATE 001B WITHDRAWN 10.0.0.0/24 as its seq=2 instead
# of the depreferenced form. With linger, 5x 5/5 at avg 5.5s. Dropping the two
# mods.Op calls in the iBGP branch of LLGREgressFilter (gr/gr_egress.go) fails it
# 3x 0/3, so the barriers are not vacuous on this file either.
-3
-1

# test/plugin/{bfd-auth-mismatch,bfd-config-load,bfd-echo-multi-hop-reject,
# bfd-ipv6-dual-bind,bfd-transport-stage2,bgp-bfd-opt-in,
# fib-vpp-coexist-with-fib-kernel,fib-vpp-plugin-load}.ci: 8 -> 0. All eight
# carried a byte-identical hand-rolled poll over ze's stderr -- readline, echo,
# accumulate, match, sleep 0.05 on EOF -- and it is now one call to
# ze_api.wait_for_stderr_lines, which blocks in select on the pipe and keeps no
# poll interval at all. No expect, reject, config or assertion line changed:
# each driver still computes its own `missing` list from the returned text.
#
# The delta is -2 rather than -8 because 6 of the 8 PAY FOR a rise nobody had
# paid for. At the commit this landed on, test/**/*.ci held 81 time.sleep( calls
# against a ceiling of 75, so the ratchet was already failing for whoever ran it
# next. 73 is the count this change leaves, and the ceiling now says so.
#
# Non-vacuity, measured on two throwaway draft copies of fib-vpp-plugin-load.ci:
# making one REQUIRED needle unmatchable fails the test in 10.1s with "FAIL:
# missing required patterns", and passing echo=False fails it on "expect=stderr
# pattern not found" while the driver still exits 0 -- the echo is what the
# runner's expect=stderr rules read.
-2

# test/flow-export/{sampling-config,conntrack-config,collector-reload}.ci,
# test/reload/{background-ze-readiness,signal-quit}.ci,
# test/traffic/traffic-reload-apply.ci, test/encode/watchdog-encode.ci,
# test/plugin/bgp-redistribute-{withdraw,metrics}.ci: 16 -> 2.
#
# Nine files, three barriers, and one false comment that had kept the first six
# sleeps alive. Four of them said a backgrounded ze receives no ZE_READY_FILE
# marker. It does: zeReadyFileEnabled (internal/test/runner/runner_exec_util.go)
# returns true for modeBackground as well as modeForeground, and runner_exec.go
# waits for daemon.ready before it publishes daemon.pid on that path.
#   daemon readiness: the two flow-export blind holds and three hand-rolled
#     pid/ready polls became ze_api.wait_for_daemon_ready. daemon.ready appears
#     only after apiServer.WaitForStartupComplete returns (cmd/ze/hub/main.go),
#     so every plugin's Stage-2 configure has run -- which for flow-export is
#     startFlowSubsystems -> samplingWorker.Start / conntrackWorker.Start, each
#     logging the asserted line synchronously before returning. The two 0.3s
#     "let the initial apply finish" settles were deleted outright for the same
#     reason: nothing was left to settle.
#   End-of-RIB: signal-quit's 1.0s and watchdog-encode's 1.0s became
#     wait_peer_eor_sent, the per-peer eor-sent counter IncrEORSent bumps only
#     after the EOR write reached the socket.
#   updates-sent: the redistribute pacing sleeps became wait_peer_counter
#     baselined before each emit, and watchdog-encode's two 0.2s pacing sleeps
#     became wait_for_ack (`request quiesce`). Its loop, formerly unbounded on
#     os.getppid(), is now three cycles -- the exact six frames the peer binds,
#     since `withdraw true` sets initiallyAnnounced false (watchdog/config.go)
#     so peer-up sends nothing and each cycle emits one add and one del.
#
# The two sleeps left are the post-SIGHUP holds in the two reload drivers. Both
# carry sleep(no-signal): handleSIGHUPReload writes no file and answers no
# query, and neither driver holds an API connection.
#
# Two mutations prove the new waits are not vacuous. Restricting
# zeReadyFileEnabled to modeForeground times sampling-config out at 10s, while
# the same mutation left the pre-conversion file GREEN at 2.6s -- it never
# depended on the marker it claimed did not exist. Dropping every entry in the
# redistribute egress fan-out (redistribute_egress/redistribute.go) fails both
# plugin tests with "its UPDATE was never counted as sent".
-14

# test/install/{image-resolve-failure,dhcp-zero-listener,tftp-zero-listener}.ci
# and test/traffic/traffic-vpp-reject-dscp-filter.ci: 4 -> 0. Each held a blind
# `wait.py` hold, and each became one declarative `await=stderr:contains=` fence
# on a needle the test already asserted, so the whole tmpfs block went with the
# sleep. `expect=exit:code=0` went with it too, unreachable behind the fence arm
# in runner_exec.go, which is the shape test/plugin/as112-external-refuses.ci
# already carries. No expect= or reject= assertion was touched.
#
# One needle was NOT what an audit predicted, and the header that predicted it
# is corrected in the same change: the dscp rejection does not come from the vpp
# Verifier at all. It comes from dscp.Parse (internal/core/dscp/dscp.go) by way
# of parseFilterValue (internal/component/traffic/config.go), which is why the
# message reads `filter dscp: dscp value 64 out of range (0-63)`.
#
# Non-vacuity: pointing two of the four fences at a needle no producer emits
# fails each at its own timeout with `await=stderr: daemon stderr never
# contained ...`, rather than passing on a wait that never had to end.
#
# The ceiling this closes on is 55, which is the count. It began this series at
# 81 against a ceiling of 75 -- already failing, and failing for every session
# that carried a .ci path, since ze-doc-wiring-check is a structural gate.
-4
