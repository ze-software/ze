# RFC2181 -- enrolled (gated)

GENERATED by `make ze-rfc-index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc2181.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. Run `python3 scripts/dev/rfc_requirements.py --locate <requirement-id>` for the exact lines. The index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC2181-10.1-1` | MAY | 10.1 | -- | -- |  |
| `RFC2181-10.1-2` | SHOULD | 10.1 | -- | -- |  |
| `RFC2181-10.1-3` | MAY | 10.1 | -- | -- |  |
| `RFC2181-10.1-4` | MUST NOT | 10.1 | -- | -- | {not-applicable} ze serves no CNAME records -- geodns emits only A/AAAA/SRV at internal/plugins/geodns/record.go:10 and as112 only SOA/NS/TXT, so no CNAME can coexist with other data |
| `RFC2181-10.1.1-1` | MUST | 10.1.1 | -- | -- | {not-applicable} ze authors no CNAME records at internal/plugins/geodns/record.go:10, so there is no label-versus-canonical-name ambiguity for an implementation to resolve |
| `RFC2181-10.2-1` | MUST NOT | 10.2 | -- | -- | {not-applicable} ze authors no PTR records -- geodns serves A/AAAA/SRV and as112 reverse zones return NODATA with the SOA at internal/plugins/as112/zones.go:273 rather than any PTR, so no PTR value can be an alias |
| `RFC2181-10.2-2` | SHOULD NOT | 10.2 | -- | -- |  |
| `RFC2181-10.3-1` | MUST NOT | 10.3 | `internal/plugins/geodns/server_test.go` `TestRFC2181_NSCanonicalWithGlue` (unit/verify) | -- | {single-polarity} geodns synthesizes NS targets as canonical ns<n>.<zone> names at internal/plugins/geodns/server.go:154 and as112 uses fixed canonical names, so ze never emits a CNAME as an NS or MX value and serves no MX at all |
| `RFC2181-10.3-2` | MUST | 10.3 | `internal/plugins/geodns/server_test.go` `TestRFC2181_NSCanonicalWithGlue` (unit/verify) | -- | {single-polarity} geodns emits A glue for every synthesized NS target at internal/plugins/geodns/server.go:161, and as112 NS targets are canonical names whose address records are authoritative elsewhere, so the target name always has address records |
| `RFC2181-11-1` | MUST | 11 | `internal/plugins/geodns/server_test.go` `TestRFC2181_WireNameLimits` (unit/verify) | -- | {single-polarity} the DNS wire codec ze packs and unpacks through rejects a label of 64+ octets at vendor/github.com/miekg/dns/msg.go:281 and caps a name at 255, and geodns/as112 emit only short synthetic names, so no over-limit name is produced |
| `RFC2181-11-2` | MUST NOT | 11 | `internal/plugins/geodns/config_test.go` `TestRFC2181_LabelsUnrestricted` (unit/verify) | -- | {single-polarity} geodns applies no label-content restriction -- parseHost at internal/plugins/geodns/config.go:270 accepts any label characters and only requires a configured-zone suffix, so underscore and other non-hostname labels are served |
| `RFC2181-11-3` | MUST NOT | 11 | `internal/plugins/geodns/config_test.go` `TestRFC2181_LabelsUnrestricted` (unit/verify) | -- | {single-polarity} geodns never refuses a zone for questionable labels -- config parsing at internal/plugins/geodns/config.go:246 rejects only a missing zone suffix or an invalid IP, never label characters |
| `RFC2181-11-4` | MAY | 11 | -- | -- |  |
| `RFC2181-11-5` | SHOULD NOT | 11 | -- | -- |  |
| `RFC2181-4.1-1` | MUST | 4.1 | `internal/plugins/geodns/server_test.go` `TestRFC2181_UDPReplySourceAndPort` (unit/verify) | -- | {single-polarity} each UDP listener binds one specific IP at internal/core/dnsserver/manager.go:160 so the kernel sources every reply from the query destination address, and ze has no wildcard-bind or explicit-source path that could send from another address |
| `RFC2181-4.1-2` | MAY | 4.1 | -- | -- |  |
| `RFC2181-4.1-3` | SHOULD | 4.1 | -- | -- |  |
| `RFC2181-4.2-1` | MUST | 4.2 | `internal/plugins/geodns/server_test.go` `TestRFC2181_UDPReplySourceAndPort` (unit/verify) | -- | {single-polarity} the reply is written on the same socket the query arrived on via internal/core/dnsserver/handler.go:62 so miekg/dns directs it to the query source port, a property ze cannot violate |
| `RFC2181-4.2-2` | MUST | 4.2 | `internal/plugins/geodns/server_test.go` `TestRFC2181_UDPReplySourceAndPort` (unit/verify) | -- | {single-polarity} miekg/dns ServeUDP records the datagram source port and uses it as the reply destination for the write at internal/core/dnsserver/handler.go:62, so ze always answers to the query source port |
| `RFC2181-4.2-3` | SHOULD | 4.2 | -- | -- |  |
| `RFC2181-5-1` | SHOULD | 5 | -- | -- |  |
| `RFC2181-5.1-1` | MUST | 5.1 | -- | -- | {gap} geodns and as112 never set the TC bit or call miekg Truncate, and miekg WriteMsg at vendor/github.com/miekg/dns/server.go:747 packs and sends without auto-truncating, so an oversized RRSet would be sent unmarked |
| `RFC2181-5.2-1` | MUST | 5.2 | `internal/plugins/geodns/server_test.go` `TestRFC2181_RRSetEqualTTL` (unit/verify) | -- | {single-polarity} geodns assigns one TTL per host record set at internal/plugins/geodns/config.go:271 and as112 uses fixed per-zone TTL constants, so an emitted RRSet never carries unequal TTLs and no code path can produce one |
| `RFC2181-5.2-2` | SHOULD | 5.2 | -- | -- |  |
| `RFC2181-5.2-3` | SHOULD | 5.2 | -- | -- |  |
| `RFC2181-5.2-4` | SHOULD | 5.2 | -- | -- |  |
| `RFC2181-5.2-5` | SHOULD | 5.2 | -- | -- |  |
| `RFC2181-5.3.1-1` | MAY | 5.3.1 | -- | -- |  |
| `RFC2181-5.3.1-2` | MUST | 5.3.1 | -- | -- | {not-applicable} ze serves no SIG or DNSSEC records -- geodns emits only A/AAAA/SRV at internal/plugins/geodns/record.go:10 and as112 only SOA/NS/TXT, so there is no SIG RRSet to include |
| `RFC2181-5.3.1-3` | MUST | 5.3.1 | -- | -- | {not-applicable} ze's stub resolver extracts only answer-section A/AAAA/TXT/PTR/CNAME/MX/NS/SRV at internal/component/resolve/dns/resolver.go:299 and processes no SIG records, so there is no partial SIG RRSet to reason about |
| `RFC2181-5.3.1-4` | MUST NOT | 5.3.1 | -- | -- | {not-applicable} the resolver caches only records it extracted from the answer section at internal/component/resolve/dns/resolver.go:299 and never handles SIG, so an authority-section SIG can never be cached or returned |
| `RFC2181-5.3.2-1` | MAY | 5.3.2 | -- | -- |  |
| `RFC2181-5.4-1` | MUST NOT | 5.4 | `internal/component/resolve/dns/cache_test.go` `TestRFC2181_CacheReplacesRRSetNoMerge` (unit/verify) | -- | {single-polarity} the resolver cache replaces the whole RRSet for a name+type at internal/component/resolve/dns/cache.go:145 by removing the existing entry before storing the new records, so response RRs are never merged with cached ones |
| `RFC2181-5.4-2` | MUST | 5.4 | `internal/component/resolve/dns/cache_test.go` `TestRFC2181_CacheReplacesRRSetNoMerge` (unit/verify) | -- | {single-polarity} put discards the entire cached RRSet before storing the new answer at internal/component/resolve/dns/cache.go:145, taking the discard-cached branch of the rule rather than merging |
| `RFC2181-5.4-3` | MAY | 5.4 | -- | -- |  |
| `RFC2181-5.4-4` | SHOULD | 5.4 | -- | -- |  |
| `RFC2181-5.4.1-1` | SHOULD | 5.4.1 | -- | -- |  |
| `RFC2181-5.4.1-10` | SHOULD | 5.4.1 | -- | -- |  |
| `RFC2181-5.4.1-11` | MAY | 5.4.1 | -- | -- |  |
| `RFC2181-5.4.1-12` | SHOULD | 5.4.1 | -- | -- |  |
| `RFC2181-5.4.1-2` | SHOULD | 5.4.1 | -- | -- |  |
| `RFC2181-5.4.1-3` | SHALL | 5.4.1 | -- | -- | {not-applicable} ze's resolver is a stub forwarder to one configured upstream at internal/component/resolve/dns/resolver.go:263 with a single-source cache keyed by name+type, so it never ranks data from competing trustworthiness sources |
| `RFC2181-5.4.1-4` | SHOULD | 5.4.1 | -- | -- |  |
| `RFC2181-5.4.1-5` | SHOULD | 5.4.1 | -- | -- |  |
| `RFC2181-5.4.1-6` | SHOULD NOT | 5.4.1 | -- | -- |  |
| `RFC2181-5.4.1-7` | MAY | 5.4.1 | -- | -- |  |
| `RFC2181-5.4.1-8` | SHALL | 5.4.1 | -- | -- | {not-applicable} the stub resolver performs no per-record trust ranking -- it relies on a validating upstream returning SERVFAIL at internal/component/resolve/dns/resolver.go:99 rather than comparing authenticated against unauthenticated data |
| `RFC2181-5.4.1-9` | MUST | 5.4.1 | -- | -- | {not-applicable} ze's authoritative servers implement no DNSSEC signing so the DNSSEC-aware precondition does not hold; the AA bit is nonetheless always set at internal/core/dnsserver/handler.go:73 |
| `RFC2181-5.5-1` | SHOULD | 5.5 | -- | -- |  |
| `RFC2181-5.5-2` | MAY | 5.5 | -- | -- |  |
| `RFC2181-5.5-3` | SHOULD NOT | 5.5 | -- | -- |  |
| `RFC2181-5.5-4` | MUST | 5.5 | -- | -- | {not-applicable} ze serves no AXFR -- the geodns YANG notes this at internal/plugins/geodns/yang/ze-geodns-conf.yang:95 and no plugin emits an SOA twice in one message, so no duplicate RRSet arises |
| `RFC2181-6.1-1` | SHOULD NOT | 6.1 | -- | -- |  |
| `RFC2181-6.1-2` | SHOULD | 6.1 | -- | -- |  |
| `RFC2181-6.2-1` | MAY | 6.2 | -- | -- |  |
| `RFC2181-6.2-2` | SHOULD | 6.2 | -- | -- |  |
| `RFC2181-6.2-3` | SHOULD NOT | 6.2 | -- | -- |  |
| `RFC2181-7.1-1` | MAY | 7.1 | -- | -- |  |
| `RFC2181-7.2-1` | SHOULD NOT | 7.2 | -- | -- |  |
| `RFC2181-7.2-2` | MAY | 7.2 | -- | -- |  |
| `RFC2181-7.3-1` | SHOULD | 7.3 | -- | -- |  |
| `RFC2181-7.3-2` | SHOULD NOT | 7.3 | -- | -- |  |
| `RFC2181-8-1` | SHALL | 8 | `internal/plugins/geodns/config_test.go` `TestRFC2181_TTLSignBitBound` (unit/verify) | `internal/plugins/geodns/config_test.go` `TestRFC2181_TTLSignBitBound` (unit/verify) |  |
| `RFC2181-8-2` | SHOULD | 8 | -- | -- |  |
| `RFC2181-8-3` | MAY | 8 | -- | -- |  |
| `RFC2181-9-1` | SHOULD | 9 | -- | -- |  |
| `RFC2181-9-2` | SHOULD NOT | 9 | -- | -- |  |
| `RFC2181-9-3` | SHOULD | 9 | -- | -- |  |
| `RFC2181-9-4` | MAY | 9 | -- | -- |  |
| `RFC2181-9-5` | SHOULD | 9 | -- | -- |  |

