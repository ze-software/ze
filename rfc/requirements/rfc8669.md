# RFC8669 -- enrolled (gated)

GENERATED by `make ze-rfc-index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc8669.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. Run `python3 scripts/dev/rfc_requirements.py --locate <requirement-id>` for the exact lines. The index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC8669-1-1` | MAY | 1 | -- | -- |  |
| `RFC8669-3-1` | MUST | 3 | `internal/component/bgp/message/rfc8669_test.go` `TestRFC8669UnknownTLVIsIgnored` (unit/verify) | -- | {single-polarity} ze has no producer that rewrites a Prefix-SID TLV. validatePrefixSIDAttr walks TLV headers and reads the value of types 5 and 6 only, never writing any of them (internal/component/bgp/message/rfc7606.go:840-856), and no forward path edits the attribute value. Ze's one code-40 modification is coarser than this requirement rather than a counter-example to it: applyFactsNextHop drops the WHOLE attribute on every next-hop-changing readvertisement (internal/component/bgp/reactor/peer_forward_facts.go:241), taking any unknown TLV with it, so on that rail the attribute is not propagated at all. Where the attribute IS propagated its bytes are untouched, and no input can make a TLV come out modified, so there is nothing to drive negatively |
| `RFC8669-3.1-1` | MUST | 3.1 | -- | -- | {gap} neither the sender nor the receiver requires TLV type 1 for labeled unicast -- validatePrefixSIDAttr accepts a Prefix-SID with no Label-Index TLV (internal/component/bgp/message/rfc7606.go:837) and BuildLabeledUnicast attaches whatever bytes the route configuration produced, including an SRv6-only attribute (internal/component/bgp/message/update_build_labeled.go:189) |
| `RFC8669-3.1-2` | MUST | 3.1 | `internal/component/bgp/plugins/rib/pool/rfc8669_test.go` `TestRFC8669LabelIndexIgnoredOnNonLabeledUnicastFamily` (unit/verify) | -- | {single-polarity} ze has no receive-side Label-Index consumer for any family -- ExtractSRv6SIDFull steps over TLV type 1 by length (internal/component/bgp/plugins/rib/pool/srv6sid.go:47) and no other reader of TLV type 1 exists in internal/component/bgp or internal/core/bgp -- so the TLV is ignored on every AFI/SAFI and there is no label-index-driven behavior to drive negatively |
| `RFC8669-3.1-3` | MUST | 3.1 | `internal/component/bgp/config/rfc8669_test.go` `TestRFC8669LabelIndexTLVReservedAndFlagsClearOnTransmission` (unit/verify) | -- | {single-polarity} ParsePrefixSID hardcodes the Reserved octet to 0 on encode and no code path emits a non-zero value, so there is no negative input to reject (internal/component/bgp/config/routeattr_prefixsid.go:68) |
| `RFC8669-3.1-4` | MUST | 3.1 | `internal/component/bgp/message/rfc8669_test.go` `TestRFC8669LabelIndexReservedAndFlagsZeroAccepted` (unit/verify) | `internal/component/bgp/message/rfc8669_test.go` `TestRFC8669LabelIndexNonZeroReservedIgnored` (unit/verify) |  |
| `RFC8669-3.1-5` | MUST | 3.1 | `internal/component/bgp/config/rfc8669_test.go` `TestRFC8669LabelIndexTLVReservedAndFlagsClearOnTransmission` (unit/verify) | -- | {single-polarity} ParsePrefixSID hardcodes the Flags field to 0 on encode with no non-zero path to reject (internal/component/bgp/config/routeattr_prefixsid.go:69) |
| `RFC8669-3.1-6` | MUST | 3.1 | `internal/component/bgp/message/rfc8669_test.go` `TestRFC8669LabelIndexReservedAndFlagsZeroAccepted` (unit/verify) | `internal/component/bgp/message/rfc8669_test.go` `TestRFC8669LabelIndexNonZeroFlagsIgnored` (unit/verify) |  |
| `RFC8669-3.2-1` | MUST | 3.2 | `internal/component/bgp/config/rfc8669_test.go` `TestRFC8669OriginatorSRGBTLVFlagsClearOnTransmission` (unit/verify) | -- | {single-polarity} parsePrefixSIDWithSRGB hardcodes both Flags octets to 0 on encode with no non-zero path to reject (internal/component/bgp/config/routeattr_prefixsid.go:133) |
| `RFC8669-3.2-2` | MUST | 3.2 | `internal/component/bgp/message/rfc8669_test.go` `TestRFC8669SRGBFlagsZeroAccepted` (unit/verify) | `internal/component/bgp/message/rfc8669_test.go` `TestRFC8669SRGBNonZeroFlagsIgnored` (unit/verify) |  |
| `RFC8669-3.2-3` | MUST NOT | 3.2 | `internal/component/bgp/message/rfc8669_test.go` `TestRFC8669SRGBUnchangedThroughReceiveValidation` (unit/verify) | -- | {single-polarity} no producer writes into an Originator SRGB TLV. The receive validator reads TLV headers and never writes a value (internal/component/bgp/message/rfc7606.go:837-856). The forward path's only code-40 operation is a whole-attribute suppress on a next-hop change (internal/component/bgp/reactor/peer_forward_facts.go:241) -- that removes the Originator SRGB along with everything else in the attribute rather than changing it, so it is not a counter-example to "MUST NOT be changed" but it does mean the TLV survives propagation only on the rails that keep the attribute. On those rails the SRGB octets are byte-identical, and no input produces changed SRGB bytes to drive negatively |
| `RFC8669-3.2-4` | MUST | 3.2 | `internal/component/bgp/plugins/rib/pool/rfc8669_test.go` `TestRFC8669SRGBIgnoredOnNonLabeledUnicastFamily` (unit/verify) | -- | {single-polarity} ze has no receive-side Originator SRGB consumer for any family -- ExtractSRv6SIDFull steps over TLV type 3 by length (internal/component/bgp/plugins/rib/pool/srv6sid.go:47) and the only SRGB code in internal/component/bgp is the config-side encoder -- so the TLV is ignored on every AFI/SAFI and there is no SRGB-driven behavior to drive negatively |
| `RFC8669-3.2-5` | SHOULD | 3.2 | -- | -- |  |
| `RFC8669-3.2-6` | MAY | 3.2 | -- | -- |  |
| `RFC8669-4-1` | MUST | 4 | `internal/component/bgp/reactor/rfc8669_multi_test.go` `TestRFC8669PrefixSIDKeptPathsKeepExactlyOneCopy` (unit/verify), `internal/component/bgp/reactor/rfc8669_test.go` `TestRFC8669PrefixSIDFromEBGPAcceptedWhenConfigured` (unit/verify) | `internal/component/bgp/reactor/rfc8669_multi_test.go` `TestRFC8669PrefixSIDEveryOccurrenceDiscardedFromEBGP` (unit/verify), `internal/component/bgp/reactor/rfc8669_test.go` `TestRFC8669PrefixSIDFromEBGPDiscardedByDefault` (unit/verify) |  |
| `RFC8669-4-2` | SHOULD | 4 | -- | -- |  |
| `RFC8669-4.1-1` | MUST | 4.1 | -- | -- | {gap} the receive validator never scans for TLV type 1, so a labeled-unicast Prefix-SID with no Label-Index TLV is accepted as well formed (internal/component/bgp/message/rfc7606.go:837) |
| `RFC8669-4.1-10` | SHOULD | 4.1 | -- | -- |  |
| `RFC8669-4.1-11` | SHOULD NOT | 4.1 | -- | -- |  |
| `RFC8669-4.1-12` | SHOULD | 4.1 | -- | -- |  |
| `RFC8669-4.1-13` | SHOULD | 4.1 | -- | -- |  |
| `RFC8669-4.1-14` | SHOULD | 4.1 | -- | -- |  |
| `RFC8669-4.1-2` | MUST | 4.1 | -- | -- | {gap} ze has no "invalid" state for the Prefix-SID attribute -- validatePrefixSIDAttr returns nil for any attribute whose TLVs fit the declared bounds, whatever their types (internal/component/bgp/message/rfc7606.go:858) |
| `RFC8669-4.1-3` | MUST | 4.1 | -- | -- | {gap} ze keeps no label-index-to-prefix reverse index and reads no label index at all -- the only reader of the Prefix-SID TLV list is the SRv6 extractor, which skips TLV type 1 (internal/component/bgp/plugins/rib/pool/srv6sid.go:47), so no conflict can be detected |
| `RFC8669-4.1-4` | MUST | 4.1 | -- | -- | {gap} neither the invalid nor the conflicting state is computed, so the ignore action has no trigger -- the receive path's only Prefix-SID discard reasons are malformed TLV bounds (internal/component/bgp/message/rfc7606.go:842) and the EBGP boundary rule (internal/component/bgp/reactor/session_validation.go:107) |
| `RFC8669-4.1-5` | MUST | 4.1 | -- | -- | {gap} ze allocates no local label for a BGP prefix at all -- the label programmed for a labeled-unicast route is the one carried in the received NLRI (internal/component/bgp/plugins/rib/rib_bestchange.go:900), and the only MPLS ingress allocators are LDP and RSVP-TE (internal/plugins/ldp/fib.go:135, internal/plugins/rsvpte/fib.go:45) |
| `RFC8669-4.1-6` | MUST | 4.1 | -- | -- | {gap} the Section 6 error handling exists and fires for malformed TLV bounds and trailing bytes (internal/component/bgp/message/rfc7606.go:842,:859), but the "invalid" condition that would route a missing-Label-Index attribute into it is never computed (internal/component/bgp/message/rfc7606.go:837) |
| `RFC8669-4.1-7` | MUST | 4.1 | -- | -- | {gap} the received NLRI label is programmed as an MPLS encapsulation without any implicit-NULL exception -- validateMPLSLabels accepts label 3 like any other value (internal/plugins/fib/kernel/mpls.go:23) and buildMPLSEncap pushes whatever labels it is given (internal/plugins/fib/kernel/nexthop_linux.go:75), so an implicit-NULL labeled-unicast route is programmed as a push of label 3 rather than a pop |
| `RFC8669-4.1-8` | MUST | 4.1 | `internal/component/bgp/plugins/rib/pool/rfc8669_test.go` `TestRFC8669NLRILabelIsTheOutboundLabel` (unit/verify) | -- | {single-polarity} the label taken off the received NLRI is carried unchanged to the forwarding plane (internal/core/bgp/nlri/nlrisplit/labeled.go:110 to internal/component/bgp/plugins/rib/rib_bestchange.go:900 to internal/plugins/fib/kernel/nexthop_linux.go:75); the rule states which label to use, not a condition to reject, so there is no non-conforming input whose rejection could be asserted |
| `RFC8669-4.1-9` | SHOULD | 4.1 | -- | -- |  |
| `RFC8669-5-1` | SHOULD | 5 | -- | -- |  |
| `RFC8669-5-2` | SHOULD | 5 | -- | -- |  |
| `RFC8669-5-3` | MAY | 5 | -- | -- |  |
| `RFC8669-5.1-1` | MUST | 5.1 | -- | -- | {gap} BuildLabeledUnicastNLRIBytes writes the label taken from the route configuration into the NLRI (internal/component/bgp/message/update_build_labeled.go:278, fed by internal/component/bgp/reactor/peer_static_routes.go:86) and nothing programs a matching incoming MPLS entry -- the only emitters of MPLS ingress/transit entries are LDP and RSVP-TE (internal/plugins/ldp/fib.go:135, internal/plugins/rsvpte/fib.go:50) |
| `RFC8669-5.1-2` | SHOULD NOT | 5.1 | -- | -- |  |
| `RFC8669-5.1-3` | MAY | 5.1 | -- | -- |  |
| `RFC8669-6-1` | MUST | 6 | `internal/component/bgp/message/rfc8669_test.go` `TestRFC8669WellFormedAttributeAccepted` (unit/verify) | `internal/component/bgp/message/rfc8669_test.go` `TestRFC8669MalformedAttributeDiscardedAndNotAdvertised` (unit/verify), `internal/component/bgp/message/rfc8669_test.go` `TestRFC8669TrailingBytesDiscarded` (unit/verify) |  |
| `RFC8669-6-2` | SHALL | 6 | -- | -- | {gap} the processing half holds -- an already-seen non-MP attribute code is skipped without validation (internal/component/bgp/message/rfc7606.go:283) -- but nothing removes the duplicate from the bytes forwarded on. A valid first occurrence with a duplicate produces no DiscardEntry, and ApplyAttrDiscard returns the path attributes untouched when the entry list is empty (internal/component/bgp/message/attr_discard.go:73-75), so the second copy is re-advertised. When the FIRST occurrence is the malformed one, applyInPlace tombstones it through AttrFind, which returns only the first match (internal/component/bgp/message/attr_discard.go:111, internal/core/bgp/attribute/iterator.go:155), leaving the untouched duplicate on the wire |
| `RFC8669-6-3` | SHALL | 6 | `internal/component/bgp/plugins/rib/pool/rfc8669_test.go` `TestRFC8669DuplicateRecognizedTLVFirstWins` (unit/verify) | `internal/component/bgp/plugins/rib/pool/rfc8669_test.go` `TestRFC8669DuplicateRecognizedTLVCannotOverrideFirst` (unit/verify) |  |
| `RFC8669-8-1` | MUST | 8 | -- | -- | {gap} the AS boundary is gated on ingress only -- PeerSettings.AcceptSRv6PrefixSID controls acceptance from an EBGP peer (internal/component/bgp/reactor/session_validation.go:107) -- while on egress a locally originated or IBGP-learned Prefix-SID is advertised to EBGP peers with no configuration gate: applyFactsNextHop suppresses code 40 only when the next-hop changes (internal/component/bgp/reactor/peer_forward_facts.go:241) and no other suppression of code 40 exists in the forward path |
| `RFC8669-8-2` | SHOULD NOT | 8 | -- | -- |  |
| `RFC8669-8-3` | SHOULD | 8 | -- | -- |  |
| `RFC8669-9-1` | SHOULD | 9 | -- | -- |  |
| `RFC8669-9-2` | SHOULD | 9 | -- | -- |  |

