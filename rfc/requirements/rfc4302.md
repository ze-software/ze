# RFC4302 -- enrolled (gated)

GENERATED by `./le rfc index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc4302.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. The exact rows live in `rfc/requirements/rfc4302.md`, and the index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC4302-2-1` | SHALL | 2 | `internal/plugins/ospf/ipsec_install_test.go` `TestIPsecSAProtocolNumber` (unit/verify) | `internal/plugins/ospf/ipsec_install_test.go` `TestIPsecSAProtocolNumber` (unit/verify) |  |
| `RFC4302-2-2` | MUST | 2 | `internal/plugins/ospf/ipsec_rfc4302_test.go` `TestAHParametersComeFromConfiguration` (unit/verify) | `internal/plugins/ospf/ipsec_rfc4302_test.go` `TestAHParametersComeFromConfiguration` (unit/verify) |  |
| `RFC4302-2.3-1` | MUST | 2.3 | -- | -- | {lower-layer} Linux XFRM; internal/plugins/ospf/ipsec_install.go::buildIPsecSA installs the transport-mode AH SA, and the kernel's AH output builds every AH header from it, so no value Ze writes decides this field |
| `RFC4302-2.3-2` | SHOULD | 2.3 | -- | -- |  |
| `RFC4302-2.4-1` | MUST | 2.4 | `internal/plugins/ospf/ipsec_rfc4302_test.go` `TestAHSAIdentifiedBySPIAndProtocol` (unit/verify) | `internal/plugins/ospf/ipsec_rfc4302_test.go` `TestAHSAIdentifiedBySPIAndProtocol` (unit/verify) |  |
| `RFC4302-2.4-2` | MUST | 2.4 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams writes each SAD entry with its SPI and its address selector, and Linux XFRM maps an inbound AH datagram to an entry in the longest-match order this section gives; ze performs no SAD search of its own |
| `RFC4302-2.4-3` | MUST | 2.4 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams writes a state Linux XFRM keys by destination, SPI and protocol, and Linux XFRM de-multiplexes on that key, so a group SA and a unicast SA that share an SPI stay distinct entries |
| `RFC4302-2.4-4` | MUST | 2.4 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams installs the SAD entries, and Linux XFRM owns both the search and whatever hashing accelerates it, so the equivalence this sentence demands is the kernel's to keep |
| `RFC4302-2.4-5` | MUST | 2.4 | `internal/plugins/ospf/ipsec_rfc4302_test.go` `TestAHSAAddressMatchIndication` (unit/verify) | `internal/plugins/ospf/ipsec_rfc4302_test.go` `TestAHSAAddressMatchIndication` (unit/verify) |  |
| `RFC4302-2.4-6` | MUST NOT | 2.4 | `internal/plugins/ospf/ipsec_rfc4302_test.go` `TestAHSPIReservedRangeRefused` (unit/verify) | `internal/plugins/ospf/ipsec_rfc4302_test.go` `TestAHSPIReservedRangeRefused` (unit/verify) |  |
| `RFC4302-2.4-7` | MAY | 2.4 | -- | -- |  |
| `RFC4302-2.5-1` | MUST | 2.5 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams writes the XFRM state that carries the sequence counter, and the kernel increments it for each packet it sends on the SA; Ze sees no AH packet |
| `RFC4302-2.5-2` | MUST | 2.5 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams writes the XFRM state, and the kernel emits the Sequence Number on every AH packet whether or not the state carries a replay window |
| `RFC4302-2.5-3` | MUST | 2.5 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams writes the state for both directions of the SA, and the kernel generates the sequence numbers it sends and verifies the ones it receives |
| `RFC4302-2.5-5` | MUST | 2.5 | -- | -- | {gap} nothing resets the counters before the 2^32nd packet. The OSPFv3 SA is manually keyed and ze never rekeys it: setConfig installs one SPI and key per interface (internal/plugins/ospf/ipsec_install.go) and no code establishes a replacement SA, so an operator who enables anti-replay with the replay-window leaf gets an SA that stops sending at the rollover rather than one that is re-established. Disclosed in docs/features/rfc-status.md RFC 4302 row |
| `RFC4302-2.5.1-1` | MUST | 2.5.1 | -- | -- |  |
| `RFC4302-2.5.1-2` | SHOULD | 2.5.1 | -- | -- |  |
| `RFC4302-2.6-1` | MUST | 2.6 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams sets the integrity transform and its truncation length, and the kernel's AH output sizes the ICV field and pads it to the alignment the address family needs |
| `RFC4302-3.3.2-1` | MUST NOT | 3.3.2 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams writes the per-SA replay window the interface replay-window leaf asks for, and Linux XFRM refuses to send a packet on that state once the sequence number would cycle; ze emits no AH packet of its own |
| `RFC4302-3.3.3.1-1` | MUST | 3.3.3.1 | -- | -- | {lower-layer} Linux XFRM; internal/plugins/ospf/ipsec_install.go::buildIPsecSA installs the transport-mode AH SA, and the kernel's AH code zeroes the mutable fields and the ICV field before it computes the ICV |
| `RFC4302-3.3.3.2.2-1` | MUST | 3.3.3.2.2 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams names the integrity algorithm, and the kernel's crypto layer appends whatever implicit padding that algorithm's blocksize needs |
| `RFC4302-3.3.3.2.2-2` | MUST | 3.3.3.2.2 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams names the integrity algorithm, and the kernel's crypto layer chooses the value of the implicit padding octets |
| `RFC4302-3.3.3.2.2-3` | MUST | 3.3.3.2.2 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams names the integrity algorithm, and the kernel's implementation of that algorithm decides whether implicit padding is required |
| `RFC4302-3.3.3.2.2-4` | MUST | 3.3.3.2.2 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams names the integrity algorithm, and the kernel's implementation of that algorithm supplies the default when the algorithm gives no answer |
| `RFC4302-3.3.4-1` | MUST | 3.3.4 | -- | -- |  |
| `RFC4302-3.3.4-2` | MAY | 3.3.4 | -- | -- |  |
| `RFC4302-3.4.1-1` | MUST | 3.4.1 | -- | -- | {lower-layer} Linux XFRM; internal/plugins/ospf/ipsec_install.go::buildIPsecPolicies installs the inbound require-policy that puts OSPF traffic through the kernel's AH input, which is where a packet offered to AH that is an IP fragment is dropped |
| `RFC4302-3.4.2-1` | MUST | 3.4.2 | `internal/plugins/ospf/ipsec_rfc4302_test.go` `TestAHInboundRequirePolicyInstalled` (unit/verify) | `internal/plugins/ospf/ipsec_rfc4302_test.go` `TestAHInboundRequirePolicyInstalled` (unit/verify) |  |
| `RFC4302-3.4.3-1` | MUST | 3.4.3 | `internal/plugins/ospf/ipsec_install_test.go` `TestIPsecSAReplayWindow` (unit/verify) | `internal/plugins/ospf/ipsec_install_test.go` `TestIPsecSAReplayWindow` (unit/verify) |  |
| `RFC4302-3.4.3-2` | MUST | 3.4.3 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams writes the state and presets no replay counter, and Linux XFRM initializes the receive packet counter to zero as it creates the state |
| `RFC4302-3.4.3-3` | MUST | 3.4.3 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams writes the replay window the receiver asked for, and Linux XFRM checks every received sequence number against that window and rejects a duplicate |
| `RFC4302-3.4.3-4` | MUST | 3.4.3 | -- | -- | {lower-layer} Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams installs the integrity transform the kernel validates the ICV with, and the kernel discards the datagram when that validation fails; Ze holds no rejecting branch of its own |
| `RFC4302-3.4.3-5` | MUST | 3.4.3 | `internal/plugins/ospf/config_ipsec_test.go` `TestIPsecReplayWindowRange` (unit/verify) | `internal/plugins/ospf/config_ipsec_test.go` `TestIPsecReplayWindowRange` (unit/verify) |  |
| `RFC4302-3.4.3-6` | SHOULD | 3.4.3 | -- | -- |  |
| `RFC4302-3.4.3-7` | SHOULD | 3.4.3 | -- | -- |  |
| `RFC4302-3.4.3-8` | SHOULD | 3.4.3 | -- | -- |  |
| `RFC4302-3.4.3-9` | MAY | 3.4.3 | -- | -- |  |
| `RFC4302-4-1` | MUST | 4 | -- | -- | {gap} ze supports auditing for AH nowhere. readXfrmDropsPlatform (internal/plugins/ospf/ipsec_drops_linux.go) polls the node-global /proc/net/xfrm_stat counters into one OSPF metric, which carries no SPI, no per-SA identity, no AH-versus-ESP split and no administrator switch to enable or disable AH auditing. Disclosed in docs/features/rfc-status.md RFC 4302 row |
| `RFC4302-5-1` | MUST | 5 | -- | -- |  |
| `RFC4302-5-2` | MUST | 5 | -- | -- |  |
| `RFC4302-5-3` | SHOULD NOT | 5 | -- | -- |  |
| `RFC4302-5-4` | MAY | 5 | -- | -- |  |
| `RFC4302-A2-1` | MUST | A2 | -- | -- | {lower-layer} Linux XFRM; internal/plugins/ospf/ipsec_install.go::buildIPsecSA installs the transport-mode AH SA, and the kernel's IPv6 AH input skips a Fragmentation Extension Header the IP layer left in place and repairs the preceding Next Header |
| `RFC4302-A2-2` | MUST | A2 | -- | -- | {lower-layer} Linux XFRM; internal/plugins/ospf/ipsec_install.go::buildIPsecSA installs the transport-mode AH SA, and the kernel's IPv6 AH output skips a Fragmentation Extension Header the IP layer handed it and repairs the preceding Next Header |

