# RFC6286 -- enrolled (gated)

GENERATED by `make ze-rfc-index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc6286.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. Run `python3 scripts/dev/rfc_requirements.py --locate <requirement-id>` for the exact lines. The index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC6286-2.1-1` | MUST | 2.1 | `internal/component/bgp/reactor/config_test.go` `TestParsePeerFromTreeInvalid` (unit/verify) | -- | {single-polarity} the definition's other two properties are structural and have no failure mode a negative test could exercise -- the wire field is a uint32 (internal/component/bgp/message/open.go:55), and the value is read once at config load into reactor.Config.RouterID and used for every peer and every OPEN (internal/component/bgp/reactor/session_negotiate.go:160). The non-zero half IS enforced and tested: parseRouterID (internal/component/bgp/reactor/config.go) rejects 0.0.0.0 for both the global leaf and a per-peer override |
| `RFC6286-2.1-2` | SHOULD | 2.1 | `internal/component/bgp/reactor/routerid_unique_test.go` `TestRouterIDClaimConcurrentOnlyOneWins` (unit/verify) | -- | {single-polarity} the negative case -- ze accepting a duplicate -- is the operator-selected `bgp/session/allow-shared-router-id true` path, which is conformant precisely because the requirement is a lowercase "should", so there is no violation for a negative test to catch. Enforcement is proven by TestRouterIDClaimConcurrentOnlyOneWins and the TestRouterIDConflict* family; the opt-out by TestValidateOpenAllowSharedRouterID |
| `RFC6286-2.2-1` | MUST | 2.2 | `internal/component/bgp/message/open_test.go` `TestOpenValidateBGPIdentifier` (unit/verify), `internal/component/bgp/reactor/session_open_validation_test.go` `TestHandleOpenRejectsBadBGPIdentifier` (unit/verify), `internal/component/bgp/reactor/session_open_validation_test.go` `TestProcessOpenRejectsBadBGPIdentifier` (unit/verify) | `internal/component/bgp/message/open_test.go` `TestOpenValidateBGPIdentifier` (unit/verify) |  |
| `RFC6286-2.2-2` | MUST | 2.2 | `internal/component/bgp/message/open_test.go` `TestOpenValidateBGPIdentifier` (unit/verify), `internal/component/bgp/reactor/session_open_validation_test.go` `TestHandleOpenRejectsBadBGPIdentifier` (unit/verify) | `internal/component/bgp/message/open_test.go` `TestOpenValidateBGPIdentifier` (unit/verify), `internal/component/bgp/reactor/session_open_validation_test.go` `TestHandleOpenRejectsBadBGPIdentifier` (unit/verify) |  |
| `RFC6286-2.3-1` | MUST | 2.3 | `internal/component/bgp/reactor/collision_test.go` `TestDetectCollisionEqualIdentifierPrefersLargerAS` (unit/verify) | `internal/component/bgp/reactor/collision_test.go` `TestDetectCollisionEqualIdentifierPrefersLargerAS` (unit/verify) |  |

