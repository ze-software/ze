# RFC5575 -- enrolled (gated), superseded by RFC8955

GENERATED by `./le rfc index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc5575.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. The exact rows live in `rfc/requirements/rfc5575.md`, and the index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC5575-3-1` | SHOULD | 3 | -- | -- | {superseded: unextracted §3} RFC 8955 Section 3 states the obligation with a lowercase keyword, that standard BGP policy mechanisms such as UPDATE filtering by NLRI prefix and community matching must apply to the Flow specification defined NLRI-type. rfc/short/rfc8955.md declares no row for it |
| `RFC5575-4-1` | MUST | 4 | `internal/component/bgp/plugins/nlri/flowspec/plugin_test.go` `TestIPv4FlowSpecNegotiatesMultiprotocolCapability` (unit/verify) | -- | {single-polarity} the flowspec plugin unconditionally maps its declared ipv4/flow family to a Multiprotocol (Code 1) capability during OPEN, and there is no wrong input the negotiation path rejects (internal/component/bgp/plugins/nlri/flowspec/register.go:19-22, types.go:47) {superseded: restated RFC8955-4-1} RFC 8955 Section 4 keeps the sentence, that implementations wishing to exchange Flow Specification MUST use BGP's Capability Advertisement facility to exchange the Multiprotocol Extension Capability Code (Code 1) |
| `RFC5575-4-2` | MUST | 4 | `internal/component/bgp/plugins/nlri/flowspec/types_test.go` `TestFlowSpecIPv4Basic` (unit/verify), `internal/component/bgp/plugins/nlri/flowspec/types_test.go` `TestFlowSpecVPNFamily` (unit/verify) | -- | {single-polarity} the (AFI 1, SAFI 133/134) assignment is a family-registration constant, not an input guard, so only the positive assignment is assertable (internal/component/bgp/plugins/nlri/flowspec/types.go:47-49, encode.go:79-95) {superseded: restated RFC8955-4-2} RFC 8955 Section 4 makes the pair explicit rather than leaving it to the application: (AFI 1, SAFI 133) for IPv4 Flow Specification and (AFI 1, SAFI 134) for VPNv4 Flow Specification |
| `RFC5575-4-3` | MUST | 4 | `internal/component/bgp/plugins/nlri/flowspec/types_test.go` `TestFlowSpecComponentsAscendingOrder` (unit/verify), `internal/component/bgp/plugins/nlri/flowspec/types_test.go` `TestFlowSpecJoinsRepeatedTypeIntoOneComponent` (unit/verify) | `internal/component/bgp/plugins/nlri/flowspec/types_test.go` `TestParseFlowSpecRefusesRepeatedComponentType` (unit/verify) | {superseded: restated RFC8955-4.2-1} the NLRI value encoding moved from Section 4 to Section 4.2, which keeps the strict type ordering by increasing numerical order |
| `RFC5575-4-4` | MUST | 4 | `internal/component/bgp/plugins/nlri/flowspec/types_test.go` `TestFlowSpecComponentsAscendingOrder` (unit/verify) | `internal/component/bgp/plugins/nlri/flowspec/types_test.go` `TestParseFlowSpecRefusesDescendingComponentType` (unit/verify) | {superseded: restated RFC8955-4.2-2} RFC 8955 Section 4.2 keeps the rule that a component, if present, MUST precede any component of higher numeric type value |
| `RFC5575-4-5` | MUST | 4 | `internal/component/bgp/plugins/nlri/flowspec/types_test.go` `TestFlowSpecNumericOperatorReservedBitZero` (unit/verify) | -- | {single-polarity} the numeric operator byte is built as lenCode<<4 OR'd with only the comparison bits, so reserved bit 4 is never set on encode and decode ignores reserved bits (internal/component/bgp/plugins/nlri/flowspec/types_numeric.go:247-257) {superseded: restated RFC8955-4.2.1.1-3} RFC 8955 Section 4.2.1.1 keeps the numeric operator reserved bit at 0 on encoding and adds the receive half, that it MUST be ignored during decoding |
| `RFC5575-4-6` | MUST | 4 | `internal/component/bgp/plugins/nlri/flowspec/types_test.go` `TestFlowSpecBitmaskOperatorReservedBitsZero` (unit/verify) | -- | {single-polarity} bitmask components encode through the same operator builder using only match/not plus the len field, so reserved bits 4-5 are never set on encode (internal/component/bgp/plugins/nlri/flowspec/types_numeric.go:247-257) {superseded: restated RFC8955-4.2.1.2-1} RFC 8955 Section 4.2.1.2 keeps the bitmask operator reserved bits at 0 on encoding and adds the receive half, that they MUST be ignored during decoding |
| `RFC5575-4-7` | MUST | 4 | `internal/component/bgp/plugins/nlri/flowspec/types_test.go` `TestFlowSpecFragmentReservedHighNibbleZero` (unit/verify) | -- | {single-polarity} the Fragment value is assembled from the four low-nibble flag constants, so the reserved high-nibble bits are never set on encode (internal/component/bgp/plugins/nlri/flowspec/types.go:201-206) {superseded: restated RFC8955-4.2.2.12-2} RFC 8955 Section 4.2.2.12 keeps the Fragment bitmask reserved bits at 0 on encoding and adds the receive half, that they MUST be ignored during decoding |
| `RFC5575-6-1` | MUST | 6 | -- | -- | {gap} ze runs no eBGP leftmost-neighbor-AS enforcement; the only AS_PATH ingress guard is RFC 4271 Section 9 loop detection and firstASInPath is used solely for MED neighbor comparison (internal/component/bgp/reactor/filter/loop_metrics.go:31, internal/component/bgp/plugins/rib/bestpath.go:544) {superseded: restated RFC8955-6-2} RFC 8955 Section 6 keeps the eBGP leftmost-AS enforcement in the same words, and keeps the reason, that the rule is optional in the BGP specification and necessary here for security |
| `RFC5575-6-2` | MUST | 6 | -- | -- | {gap} ze decodes a received flowspec NLRI and lowers it to the firewall with no feasibility validation against the unicast RIB, the same absence disclosed for RFC8956-5-1 (internal/component/bgp/plugins/nlri/flowspec/types.go:351, internal/plugins/flowspec-firewall/translate.go:166) {superseded: restated RFC8955-6-1} RFC 8955 Section 6 keeps the feasibility rule and scopes it, adding that it applies in the absence of explicit configuration and that SAFI 133 validates against SAFI 1 while SAFI 134 validates against SAFI 128 |
| `RFC5575-6-3` | MUST | 6 | -- | -- | {gap} no code compares a flowspec's originator against the best-match unicast route's originator; this is part of the absent Section 6 validation procedure (internal/component/bgp/plugins/nlri/flowspec/) {superseded: restated RFC8955-6-1} originator matching is condition (b) of the RFC 8955 Section 6 list, in the same words, and RFC 8955 states the three conditions as one feasibility rule rather than three. Condition (b) is moot when condition (a) is relaxed by explicit configuration |
| `RFC5575-6-4` | MUST | 6 | -- | -- | {gap} no code walks more-specific unicast routes to compare neighboring AS, because the Section 6 validation procedure is unimplemented (internal/component/bgp/plugins/nlri/flowspec/) {superseded: restated RFC8955-6-1} the more-specific-route rule is condition (c) of the RFC 8955 Section 6 list, in the same words, and RFC 8955 states the three conditions as one feasibility rule rather than three. Condition (c) is moot when condition (a) is relaxed by explicit configuration |
| `RFC5575-7-1` | MUST | 7 | `internal/component/bgp/plugins/nlri/flowspec/types_test.go` `TestFlowSpecTrafficMarkingReservedBytesZero` (unit/verify) | -- | {single-polarity} the traffic-marking community is emitted with literal zero reserved bytes and only the trailing DSCP octet varies (internal/component/bgp/plugins/nlri/flowspec/encode.go:124-127) {superseded: restated RFC8955-7.5-1} the traffic-marking action moved from Section 7 to Section 7.5, which keeps the reserved bits at 0 on encoding and adds the receive half, that they MUST be ignored during decoding |
| `RFC5575-7-2` | MAY | 7 | -- | -- | {superseded: unextracted §7.6} RFC 8955 gives the paragraph its own section, 7.6, and keeps it word for word, that a user-defined community value can be mapped to platform-specific or network-specific behavior via user configuration. rfc/short/rfc8955.md declares no row for it |
| `RFC5575-9-1` | SHOULD | 9 | -- | -- | {superseded: restated RFC8955-9-1} RFC 8955 Section 9 keeps the SHOULD to provide a mechanism to log the packet header of filtered traffic |
| `RFC5575-9-2` | SHOULD | 9 | -- | -- | {superseded: restated RFC8955-9-2} RFC 8955 Section 9 keeps the SHOULD to provide a mechanism to count the number of matches for a given Flow Specification rule |

