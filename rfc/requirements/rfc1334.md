# RFC1334 -- enrolled (gated), superseded by RFC1994

GENERATED by `make ze-rfc-index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc1334.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. Run `python3 scripts/dev/rfc_requirements.py --locate <requirement-id>` for the exact lines. The index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC1334-1-1` | MUST | 1 | `internal/component/l2tp/ppp/auth_dispatch_test.go` `TestLocalCONFREQAdvertisesAuthMethod` (unit/verify) | `internal/component/l2tp/ppp/auth_dispatch_test.go` `TestAuthProtoRejectClearsMethod` (unit/verify) | {superseded: restated RFC1994-1-1} RFC 1994 Section 1 repeats the sentence word for word, that an implementation MUST specify the Authentication-Protocol Configuration Option during Link Establishment phase if authentication of the link is desired. The obligation is PPP-wide and binds neither authentication protocol in particular |
| `RFC1334-2.2-1` | MUST | 2.2 | -- | -- | {not-applicable} ze never issues a second PAP Authenticate-Request; the authenticator role only responds (internal/component/l2tp/ppp/pap.go:160) and the client issues exactly one request per session with a fixed Identifier and no reissue path (internal/component/l2tp/pppoeclient/session.go:230-241), so the change-Identifier-on-reissue obligation has no code path {superseded: dropped} RFC 1994 states no PAP obligation. Its Section 4.1 requires the Identifier to change each time a Challenge is sent, which binds the CHAP Challenge and not a reissued PAP Authenticate-Request |
| `RFC1334-2.2-2` | MAY | 2.2 | -- | -- | {superseded: dropped} RFC 1994 Section 4.1 obliges the authenticator to send further Challenges until a valid Response arrives or a retry counter expires, which is a CHAP obligation on the authenticator. RFC 1994 states nothing about a peer retrying a PAP Authenticate-Request |
| `RFC1334-2.3-1` | MUST | 2.3 | `internal/component/l2tp/ppp/pap_test.go` `TestPAPRequestEmitsEvent` (unit/verify) | `internal/component/l2tp/ppp/pap_test.go` `TestPAPRejectWritesNak` (unit/verify) | {superseded: dropped} RFC 1994 defines no PAP packet. Its Section 4.2 obliges a CHAP packet with Code 3 (Success) when the received Response Value equals the expected value, which is the CHAP handshake rather than the PAP Authenticate-Ack. PAP stays defined by RFC 1334, as this summary's forward Meta row records |
| `RFC1334-2.3-2` | MUST | 2.3 | `internal/component/l2tp/ppp/pap_test.go` `TestPAPRejectWritesNak` (unit/verify) | `internal/component/l2tp/ppp/pap_test.go` `TestPAPRequestEmitsEvent` (unit/verify) | {superseded: dropped} RFC 1994 Section 4.2 obliges a CHAP packet with Code 4 (Failure) when the Response Value does not match, and defines no PAP Authenticate-Nak. The PAP rule is still owed for as long as PAP peers are authenticated |
| `RFC1334-2.3-3` | MUST | 2.3 | `internal/component/l2tp/ppp/pap_test.go` `TestPAPRequestEmitsEvent` (unit/verify), `internal/component/l2tp/ppp/pap_test.go` `TestPAPRejectWritesNak` (unit/verify) | -- | {single-polarity} writePAPReply copies the request Identifier into the PAP reply (internal/component/l2tp/ppp/pap.go:138) and ze never validates or rejects on the Identifier, so there is no negative case {superseded: dropped} RFC 1994 Section 4.2 requires the Success or Failure Identifier to be copied from the Response which caused the reply, which binds CHAP. It states nothing about a PAP Authenticate-Ack or Authenticate-Nak |
| `RFC1334-2.3-4` | MUST NOT | 2.3 | `internal/component/l2tp/pppoeclient/rfc1334_pap_message_test.go` `TestPAPReplyMessageDoesNotAffectOutcome` (unit/verify) | `internal/component/l2tp/pppoeclient/rfc1334_pap_message_test.go` `TestPAPReplyMessageDoesNotAffectOutcome` (unit/verify) | {superseded: dropped} RFC 1994 Section 4.2 carries the same MUST NOT for the Message field of a CHAP Success or Failure packet. It states nothing about the PAP Ack and Nak Message field, which RFC 1334 still defines |
| `RFC1334-2.3-5` | SHOULD | 2.3 | -- | -- | {superseded: dropped} RFC 1994 Section 4.2 says a CHAP implementation SHOULD take action to terminate the link when it transmits a Failure. That binds the CHAP exchange, and RFC 1994 states nothing about a PAP Authenticate-Nak |
| `RFC1334-2.3-6` | MAY | 2.3 | -- | -- | {superseded: dropped} RFC 1994 Section 4.2 says the Message field of a CHAP Success or Failure is zero or more octets. It states nothing about the PAP Ack and Nak Message field |
| `RFC1334-x-1` | MUST | x | `internal/component/l2tp/ppp/auth_test.go` `TestDefaultAuthFallbackOrder` (unit/verify) | `internal/component/l2tp/ppp/auth_test.go` `TestSelectAuthFallback` (unit/verify) | {superseded: dropped} RFC 1994 defines CHAP alone and states no obligation to offer a stronger method before PAP. Its Security Considerations warn that authenticating one user name by several methods exposes the least secure of them, and recommend one method per user name, with no keyword. The rule is still owed for as long as PAP is offered |

