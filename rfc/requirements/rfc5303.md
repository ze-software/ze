# RFC5303 -- enrolled (gated)

GENERATED by `make ze-rfc-index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc5303.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. Run `python3 scripts/dev/rfc_requirements.py --locate <requirement-id>` for the exact lines. The index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC5303-3.1-1` | SHALL | 3.1 | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISP2PIIHCarriesThreeWayOption` (unit/verify) | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISP2PIIHCarriesThreeWayOption` (unit/verify) |  |
| `RFC5303-3.1-2` | SHALL | 3.1 | -- | -- | {not-applicable} this constrains a system that does NOT understand the option; Ze implements and processes it (packet/tlv_core.go DecodeP2PThreeWayTLV decodes it, circuit/runtime.go:143 consumes it), so the "does not understand" role never applies to Ze |
| `RFC5303-3.1-3` | SHALL NOT | 3.1 | -- | -- | {not-applicable} this constrains a non-supporting system's emission; Ze supports the mechanism and always emits TLV 240 in its point-to-point IIH (circuit/hello.go:158 threeWayTLV, circuit/hello.go:204 buildP2PHello), so the "does not understand" role never applies to Ze |
| `RFC5303-3.1-4` | MUST | 3.1 | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISP2PThreeWayStateFieldIncluded` (unit/verify) | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISP2PThreeWayStateFieldPresentWithoutNeighbor` (unit/verify) |  |
| `RFC5303-3.1-5` | SHOULD | 3.1 | -- | -- |  |
| `RFC5303-3.1-6` | SHALL | 3.1 | `internal/plugins/isis/adjacency/fsm_test.go` `TestISISThreeWayProceduresEngagedByOption` (unit/verify) | `internal/plugins/isis/adjacency/fsm_test.go` `TestISISThreeWayProceduresEngagedByOption` (unit/verify) |  |
| `RFC5303-3.2-1` | SHALL | 3.2 | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISThreeWayReportsCurrentState` (unit/verify) | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISThreeWayReportsCurrentState` (unit/verify) |  |
| `RFC5303-3.2-10` | SHALL | 3.2 | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISThreeWayNewAdjacencyStateDown` (unit/verify) | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISThreeWayNewAdjacencyStateDown` (unit/verify) |  |
| `RFC5303-3.2-11` | SHALL | 3.2 | -- | -- | {gap} Ze does not maintain a distinct three-way state nor the four-cell section 3.2 state table; bidirectionality is derived from the ISO 10589 adjacency state plus the System ID echo (adjacency/fsm.go:207 bidirectional), so the table's "Accept" and restart "Down" cells are not modeled |
| `RFC5303-3.2-12` | SHALL | 3.2 | -- | -- | {gap} the "Down"/"Neighbor restarted" action is absent; when our three-way state is Down and the neighbor reports Up Ze brings the adjacency Up (adjacency/fsm.go:189) rather than deleting it and emitting adjacencyStateChange(Down) |
| `RFC5303-3.2-13` | SHALL | 3.2 | `internal/plugins/isis/adjacency/fsm_test.go` `TestISISThreeWayInitializeAction` (unit/verify) | `internal/plugins/isis/adjacency/fsm_test.go` `TestISISThreeWayInitializeAction` (unit/verify) |  |
| `RFC5303-3.2-2` | SHALL | 3.2 | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISThreeWayDownWhenNoAdjacency` (unit/verify) | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISThreeWayDownWhenNoAdjacency` (unit/verify) |  |
| `RFC5303-3.2-3` | SHALL | 3.2 | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISThreeWayExtendedLocalCircuitID` (unit/verify) | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISThreeWayExtendedLocalCircuitID` (unit/verify) |  |
| `RFC5303-3.2-4` | SHALL | 3.2 | -- | -- | {gap} the Extended Local Circuit ID is derived as uint8(ifindex) (circuits.go:317), truncating the interface index to 8 bits; two circuits whose ifindexes collide modulo 256 emit the same value, so uniqueness among all circuits is not guaranteed and the 4-octet field never exceeds 255 |
| `RFC5303-3.2-5` | SHALL | 3.2 | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISThreeWayReportsNeighborSystemID` (unit/verify) | `internal/plugins/isis/circuit/rfc5303_threeway_test.go` `TestISISThreeWayReportsNeighborSystemID` (unit/verify) |  |
| `RFC5303-3.2-6` | SHALL | 3.2 | -- | -- | {gap} Ze does not track the neighbor's Extended Local Circuit ID; threeWayTLV echoes a constant 0 in the Neighbor Extended Local Circuit ID field (circuit/hello.go:168) and updateThreeWay never stores the neighbor's value (adjacency/fsm.go:232), so the neighbor's actual extended circuit ID is never reported |
| `RFC5303-3.2-7` | SHALL | 3.2 | -- | -- | {gap} the decoder validates only the TLV length, not the state value (packet/tlv_core.go DecodeP2PThreeWayTLV); an out-of-range Adjacency Three-Way State is folded into the FSM (adjacency/fsm.go:237) and treated as not-bidirectional rather than discarding the PDU, so the "discard, no further action" path is absent |
| `RFC5303-3.2-8` | SHALL | 3.2 | -- | -- | {gap} updateThreeWay examines only the Neighbor System ID (adjacency/fsm.go:240) to set neighborSawUs; the Neighbor Extended Local Circuit ID is decoded (packet/tlv_core.go DecodeP2PThreeWayTLV, the p2pThreeWayLenFull branch) but the FSM never examines it, so the required examination of both fields is incomplete |
| `RFC5303-3.2-9` | SHALL | 3.2 | -- | -- | {gap} on a Neighbor System ID that does not match ours Ze keeps the adjacency Initializing (adjacency/fsm.go:240) and still processes the PDU, arming the hold timer and recording the neighbor (adjacency/fsm.go:161); it neither discards the PDU nor checks the Neighbor Extended Local Circuit ID, so the loop-detection discard is absent |

