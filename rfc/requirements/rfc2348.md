# RFC2348 -- enrolled (gated)

GENERATED by `./le rfc index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc2348.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. The exact rows live in `rfc/requirements/rfc2348.md`, and the index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC2348-x-1` | MUST | x | `internal/plugins/tftpserver/rfc2348_blksize_test.go` `TestRFC2348BlksizeAckNotAboveRequest` (unit/verify) | `internal/plugins/tftpserver/rfc2348_blksize_test.go` `TestRFC2348BlksizeAckNotAboveRequest` (unit/verify) |  |
| `RFC2348-x-2` | MUST | x | -- | -- | {not-applicable} This requirement governs the TFTP CLIENT (it MUST use the OACK blocksize or send ERROR 8). Ze ships only a TFTP SERVER (internal/plugins/tftpserver/handler.go) with no TFTP client, so there is no client-side code path that consumes an OACK blocksize or emits ERROR 8. |
| `RFC2348-x-3` | MUST | x | `internal/plugins/tftpserver/rfc2348_blksize_test.go` `TestRFC2348BlksizeRangeEnforced` (unit/verify) | `internal/plugins/tftpserver/rfc2348_blksize_test.go` `TestRFC2348BlksizeRangeEnforced` (unit/verify) |  |
| `RFC2348-x-4` | MUST | x | `internal/plugins/tftpserver/handler_test.go` `TestTFTPReadLargeFile` (unit/verify) | `internal/plugins/tftpserver/handler_test.go` `TestTFTPReadExact512` (unit/verify) |  |
| `RFC2348-x-5` | MUST | x | `internal/plugins/tftpserver/handler_test.go` `TestTFTPReadExact512` (unit/verify) | `internal/plugins/tftpserver/handler_test.go` `TestTFTPReadLargeFile` (unit/verify) |  |

