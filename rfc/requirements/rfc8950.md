# RFC8950 -- enrolled (gated)

GENERATED by `make ze-rfc-index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc8950.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. The index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC8950-3-1` | MUST | 3 | `internal/core/bgp/attribute/mpnlri_test.go:338` (unit/verify), `internal/core/bgp/attribute/mpnlri_test.go:442` (unit/verify) | `internal/core/bgp/attribute/mpnlri_test.go:488` (unit/verify) |  |
| `RFC8950-3-2` | MUST | 3 | `internal/core/bgp/attribute/mpnlri_test.go:576` (unit/verify), `internal/core/bgp/attribute/mpnlri_test.go:674` (unit/verify) | -- | {single-polarity} on encode ze always writes an all-zero 8-byte Route Distinguisher before a VPN next-hop (internal/core/bgp/attribute/mpnlri.go:170-176) and on decode it skips the 8 RD bytes without validating their value (mpnlri.go:438-443), so ze never emits a nonzero RD and never rejects one -- there is no negative case |
| `RFC8950-4-1` | MUST | 4 | `internal/component/bgp/reactor/peer_test.go:1144` (unit/verify) | `internal/component/bgp/reactor/peer_test.go:1175` (unit/verify), `internal/component/bgp/reactor/peer_test.go:1199` (unit/verify) |  |
| `RFC8950-4-2` | MUST | 4 | `internal/core/bgp/capability/negotiated_test.go:274` (unit/verify) | `internal/core/bgp/capability/negotiated_test.go:313` (unit/verify) |  |
| `RFC8950-4-3` | MUST | 4 | `internal/core/bgp/capability/capability_test.go:502` (unit/verify), `internal/core/bgp/capability/capability_test.go:535` (unit/verify) | -- | {single-polarity} ze binds the Extended Next Hop Encoding capability to the constant CodeExtendedNextHop = 5 (internal/core/bgp/capability/capability.go:70); Code() returns it (capability.go:640) and WriteTo emits it (capability.go:644-646). The code is a fixed constant with no alternate-value code path, so there is no wrong-code case to reject as a negative |
| `RFC8950-5-1` | MUST NOT | 5 | `internal/component/bgp/reactor/forward_rr_test.go:230` (unit/verify) | -- | {single-polarity} on the reflection path ze rewrites the next-hop only under an explicit next-hop-self/explicit override (nhMode != nhModeNone); the default nhModeNone leaves the next-hop untouched (internal/component/bgp/reactor/peer_forward_facts.go:226-229) and the MP re-encode changes an attribute only when the NLRI framing differs between encoding contexts (internal/component/bgp/reactor/forward_body.go:217), so a reflected next-hop is carried verbatim and there is no ze code path that rewrites an unchanged-passthrough next-hop to assert as a negative. The positive is proven byte-identical in TestReactorForwardRRPreservesExtendedNextHop |
| `RFC8950-5-2` | SHOULD | 5 | -- | -- |  |
| `RFC8950-5-3` | MAY | 5 | -- | -- |  |

