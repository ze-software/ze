# RFC2347 -- enrolled (gated)

GENERATED by `make ze-rfc-index` -- do not edit. Requirement text is authored in `rfc/short/rfc2347.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. The index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC2347-x-1` | MUST NOT | x | `internal/plugins/tftpserver/option_negotiation_rfc2347_test.go:78` (unit/verify) | `internal/plugins/tftpserver/option_negotiation_rfc2347_test.go:82` (unit/verify) |  |
| `RFC2347-x-2` | MUST | x | -- | -- | {not-applicable} This requirement governs the TFTP CLIENT (it MUST use the options the server acknowledged). Ze ships only a TFTP SERVER (internal/plugins/tftpserver/handler.go) and has no TFTP client, so there is no client-side option-consumption code path to which this applies. |
| `RFC2347-x-3` | MUST | x | `internal/plugins/tftpserver/option_negotiation_rfc2347_test.go:103` (unit/verify) | `internal/plugins/tftpserver/option_negotiation_rfc2347_test.go:108` (unit/verify) |  |
| `RFC2347-x-4` | MUST NOT | x | -- | -- | {not-applicable} This requirement governs the TFTP CLIENT (it MUST NOT use options the server did not acknowledge). Ze has no TFTP client (only the server in internal/plugins/tftpserver/handler.go), so no client-side code path could use an unacknowledged option. |
| `RFC2347-x-5` | SHOULD | x | -- | -- |  |
| `RFC2347-x-6` | SHOULD | x | -- | -- |  |

