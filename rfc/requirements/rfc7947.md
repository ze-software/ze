# RFC7947 -- enrolled (gated)

GENERATED by `./le rfc index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc7947.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. The exact rows live in `rfc/requirements/rfc7947.md`, and the index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC7947-x-1` | SHOULD NOT | x | `internal/component/bgp/reactor/forward_rs_test.go` `TestReactorForwardRSTransparent` (unit/verify), `internal/component/bgp/reactor/reactor_api_relay_test.go` `TestRelayStoredRouteRSClientPreservesASPath` (unit/verify), `internal/le/interoplab/bgp/check_rfc.go` `checkRouteServerASPath` (interop/nightly), `test/plugin/bgp-rs-relay-aspath-transparency.ci` (functional/verify) | `internal/component/bgp/reactor/forward_rs_test.go` `TestReactorForwardRSEBGPPrepend` (unit/verify), `internal/component/bgp/reactor/reactor_api_relay_test.go` `TestRelayStoredRoutePlainEBGPPrependsLocalAS` (unit/verify), `test/plugin/bgp-rs-relay-aspath-transparency.ci` (functional/verify) |  |
| `RFC7947-x-2` | MUST NOT | x | `internal/component/bgp/reactor/forward_rs_test.go` `TestReactorForwardRSTransparent` (unit/verify) | -- | {single-polarity} NEXT_HOP transparency is not RS-specific -- every forwarded route preserves it by default (nhModeNone, internal/component/bgp/reactor/peer_forward_facts.go:147), so unlike x-1's AS-path prepend there is no "confined" negative where a comparable non-RS peer rewrites NEXT_HOP; the only rewrite is an explicit per-peer next-hop-self/explicit override, which exercises the override feature rather than the RS-transparency MUST-NOT. The positive is proven byte-identical in TestReactorForwardRSTransparent |
| `RFC7947-x-3` | SHOULD | x | `internal/component/bgp/reactor/forward_med_test.go` `TestForwardKeepsMEDForRouteServerClient` (unit/verify), `internal/component/bgp/reactor/forward_rs_test.go` `TestReactorForwardRSTransparent` (unit/verify), `test/plugin/med-not-propagated-across-as.ci` (functional/verify) | `test/plugin/med-not-propagated-across-as.ci` (functional/verify) |  |
| `RFC7947-x-4` | MUST | x | `internal/component/bgp/reactor/forward_rs_test.go` `TestReactorForwardRSFallback` (unit/verify) | `internal/component/bgp/reactor/forward_rs_test.go` `TestReactorForwardRSBasic` (unit/verify) |  |
| `RFC7947-x-5` | SHOULD | x | -- | -- |  |
| `RFC7947-x-6` | MAY | x | -- | -- |  |

