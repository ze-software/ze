# RFC7947 -- enrolled (gated)

GENERATED by `make ze-rfc-index` -- do not edit. Requirement text is authored in `rfc/short/rfc7947.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. The index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC7947-x-1` | SHOULD NOT | x | `internal/component/bgp/reactor/forward_rs_test.go:450` (unit/verify), `internal/component/bgp/reactor/reactor_api_relay_test.go:205` (unit/verify), `test/interop/scenarios/14-route-server-frr/check.py:8` (interop/nightly), `test/plugin/bgp-rs-relay-aspath-transparency.ci:4` (functional/verify) | `internal/component/bgp/reactor/forward_rs_test.go:327` (unit/verify), `internal/component/bgp/reactor/reactor_api_relay_test.go:241` (unit/verify), `test/plugin/bgp-rs-relay-aspath-transparency.ci:8` (functional/verify) |  |
| `RFC7947-x-2` | MUST NOT | x | `internal/component/bgp/reactor/forward_rs_test.go:452` (unit/verify) | -- | {single-polarity} NEXT_HOP transparency is not RS-specific -- every forwarded route preserves it by default (nhModeNone, internal/component/bgp/reactor/peer_forward_facts.go:147), so unlike x-1's AS-path prepend there is no "confined" negative where a comparable non-RS peer rewrites NEXT_HOP; the only rewrite is an explicit per-peer next-hop-self/explicit override, which exercises the override feature rather than the RS-transparency MUST-NOT. The positive is proven byte-identical in TestReactorForwardRSTransparent |
| `RFC7947-x-3` | MUST | x | `internal/component/bgp/reactor/forward_rs_test.go:454` (unit/verify) | -- | {single-polarity} MULTI_EXIT_DISC (attribute type 4) is never modified on any RS forwarding path -- no mods.Op(4, ...) exists in the reactor -- so MED rides through in the verbatim wire and there is no MED-rewrite behavior to assert as a negative. The positive is proven byte-identical in TestReactorForwardRSTransparent |
| `RFC7947-x-4` | MUST | x | `internal/component/bgp/reactor/forward_rs_test.go:250` (unit/verify) | `internal/component/bgp/reactor/forward_rs_test.go:162` (unit/verify) |  |
| `RFC7947-x-5` | SHOULD | x | -- | -- |  |
| `RFC7947-x-6` | MAY | x | -- | -- |  |

