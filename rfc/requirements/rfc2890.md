# RFC2890 -- enrolled (gated)

GENERATED by `make ze-rfc-index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc2890.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. Run `python3 scripts/dev/rfc_requirements.py --locate <requirement-id>` for the exact lines. The index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC2890-1.1-1` | SHOULD | 1.1 | -- | -- |  |
| `RFC2890-1.1-2` | SHOULD | 1.1 | -- | -- |  |
| `RFC2890-2.1-1` | MUST | 2.1 | -- | -- | {not-applicable} ze builds no GRE header; the netlink backend sets only the tunnel IKey/OKey config fields (internal/plugins/iface/netlink/tunnel_linux.go:129) and the VPP backend calls gre_tunnel_add_del (internal/plugins/iface/vpp/tunnel.go:70), so the kernel/VPP dataplane constructs the K flag and Key octets, not ze |
| `RFC2890-2.1-2` | MUST NOT | 2.1 | -- | -- | {not-applicable} ze emits no GRE header bytes; the K-bit-and-Key-absence invariant is enforced by the kernel, which sets the flag only when IKey/OKey are non-zero (internal/plugins/iface/netlink/tunnel_linux.go:128-139); ze has no header-flag code path |
| `RFC2890-2.2-1` | MUST | 2.2 | -- | -- | {not-applicable} ze exposes no GRE sequence-number configuration and constructs no Sequence Number field; grep for a sequence producer across the iface tunnel paths finds none |
| `RFC2890-2.2-2` | MUST NOT | 2.2 | -- | -- | {not-applicable} ze has no S-bit or Sequence Number code path; it configures kernel/VPP tunnels and builds no GRE header |
| `RFC2890-2.2-3` | MUST | 2.2 | -- | -- | {not-applicable} GRE receive-side sequence ordering is performed by the kernel/VPP datapath; ze has no GRE decapsulation or packet-parse code path |
| `RFC2890-2.2-4` | MUST | 2.2 | -- | -- | {not-applicable} ze has no GRE receiver buffer or OUTOFORDER_TIMER; it programs kernel/VPP tunnels and does not process GRE payloads |
| `RFC2890-2.2-5` | SHOULD | 2.2 | -- | -- |  |
| `RFC2890-2.2-6` | MAY | 2.2 | -- | -- |  |
| `RFC2890-2.2-7` | MAY | 2.2 | -- | -- |  |
| `RFC2890-2.2-8` | MAY | 2.2 | -- | -- |  |
| `RFC2890-3-1` | MUST | 3 | -- | -- | {not-applicable} ze applies no ESP or AH to GRE; IPsec is not wired to the GRE tunnel builders (internal/plugins/iface/netlink/tunnel_linux.go, internal/plugins/iface/vpp/tunnel.go) |

