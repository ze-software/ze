# RFC7854 -- enrolled (gated)

GENERATED by `make ze-rfc-index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc7854.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. Run `python3 scripts/dev/rfc_requirements.py --locate <requirement-id>` for the exact lines. The index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC7854-x-1` | MUST | x | `internal/component/bgp/plugins/bmp/header_test.go` `TestBMPCommonHeaderDecode` (unit/verify) | `internal/component/bgp/plugins/bmp/header_test.go` `TestBMPCommonHeaderDecode` (unit/verify), `internal/component/bgp/plugins/bmp/session_test.go` `TestBMPMalformedHeaderDrops` (unit/verify) |  |
| `RFC7854-x-10` | MUST | x | `internal/component/bgp/plugins/bmp/event_test.go` `TestHandleSenderStatePeerDown` (unit/verify), `internal/component/bgp/plugins/bmp/sender_test.go` `TestBMPSenderPeerDown` (unit/verify) | -- | {single-polarity} Peer Down is always written with a reason byte, so there is no valid Peer Down without one to assert against |
| `RFC7854-x-11` | MUST | x | `internal/component/bgp/plugins/bmp/sender_test.go` `TestBMPSenderTermination` (unit/verify) | -- | {single-polarity} Termination is produced unconditionally when the session is torn down, so there is no valid shutdown that omits it to reject |
| `RFC7854-x-12` | MUST | x | `internal/component/bgp/plugins/bmp/session_test.go` `TestBMPReceiverUnidirectional` (unit/verify) | -- | {single-polarity} the receiver loop (internal/component/bgp/plugins/bmp/bmp.go:441-492) issues only reads and the sender hold-loop (sender.go:207-237) reads only to detect close, so neither role writes toward the monitored router on a valid session and there is no reject case to construct |
| `RFC7854-x-13` | SHOULD | x | -- | -- |  |
| `RFC7854-x-14` | SHOULD | x | -- | -- |  |
| `RFC7854-x-15` | SHOULD | x | -- | -- |  |
| `RFC7854-x-16` | SHOULD | x | `internal/component/bgp/plugins/bmp/bmp_reconnect_test.go` `TestConcurrentDumpsStayAddressedToTheirOwnCollector` (unit/verify) | -- |  |
| `RFC7854-x-17` | SHOULD | x | `internal/component/bgp/plugins/bmp/bmp_reconnect_test.go` `TestConcurrentDumpsStayAddressedToTheirOwnCollector` (unit/verify) | -- |  |
| `RFC7854-x-18` | MAY | x | -- | -- |  |
| `RFC7854-x-19` | MAY | x | -- | -- |  |
| `RFC7854-x-2` | MUST | x | `internal/component/bgp/plugins/bmp/header_test.go` `TestBMPCommonHeaderEncode` (unit/verify) | `internal/component/bgp/plugins/bmp/header_test.go` `TestBMPCommonHeaderDecode` (unit/verify) |  |
| `RFC7854-x-20` | MAY | x | -- | -- |  |
| `RFC7854-x-3` | MUST | x | `internal/component/bgp/plugins/bmp/header_test.go` `TestHasPeerHeader` (unit/verify) | `internal/component/bgp/plugins/bmp/header_test.go` `TestHasPeerHeader` (unit/verify) |  |
| `RFC7854-x-4` | MUST | x | `internal/component/bgp/plugins/bmp/header_test.go` `TestBMPPeerHeaderDecode` (unit/verify), `internal/component/bgp/plugins/bmp/header_test.go` `TestBMPPeerHeaderEncode` (unit/verify) | -- | {single-polarity} the Peer AS is unconditionally a 4-octet field on every encode and decode, so there is no shorter-AS variant to reject and no negative case to construct |
| `RFC7854-x-5` | MUST | x | `internal/component/bgp/plugins/bmp/event_test.go` `TestParseIPIntoIPv4` (unit/verify) | `internal/component/bgp/plugins/bmp/event_test.go` `TestParseIPIntoIPv6` (unit/verify) |  |
| `RFC7854-x-6` | MUST | x | `internal/component/bgp/plugins/bmp/sender_test.go` `TestBMPSenderConnects` (unit/verify) | -- | {single-polarity} the sender always emits Initiation as the first message on a fresh connection, so there is no valid session in which another message precedes it to reject |
| `RFC7854-x-7` | MUST | x | `internal/component/bgp/plugins/bmp/sender_test.go` `TestBMPSenderInitiation` (unit/verify) | -- | {single-polarity} the Initiation the sender builds always includes the sysName TLV, so there is no valid Initiation omitting it to assert against |
| `RFC7854-x-8` | MUST | x | `internal/component/bgp/plugins/bmp/event_test.go` `TestHandleSenderStatePeerUp` (unit/verify), `internal/component/bgp/plugins/bmp/sender_test.go` `TestBMPSenderPeerUp` (unit/verify) | `internal/component/bgp/plugins/bmp/event_test.go` `TestBMPPeerUpSkippedOnCacheMiss` (unit/verify) |  |
| `RFC7854-x-9` | MUST | x | `internal/component/bgp/plugins/bmp/sender_test.go` `TestBMPSenderRouteMonitoring` (unit/verify) | -- | {single-polarity} Route Monitoring is only ever constructed around a complete BGP UPDATE PDU, so there is no valid Route Monitoring lacking one to reject |

