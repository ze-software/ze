# RFC2869 -- enrolled (gated)

GENERATED by `make ze-rfc-index` -- do not edit. Requirement text is authored in `rfc/short/rfc2869.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. The index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC2869-5.17-1` | SHOULD | 5.17 | -- | -- |  |
| `RFC2869-x-1` | MUST | x | -- | -- | {not-applicable} ze runs only the RADIUS accounting client (NAS) role; it has no accounting-server receive path (internal/component/radius/client.go binds only to receive responses to its own requests), so it never handles missing Gigawords on receipt |
| `RFC2869-x-2` | MUST | x | `internal/component/l2tp/plugins/authradius/acct_test.go:293` (unit/verify) | `internal/component/l2tp/plugins/authradius/accounting_rfc2869_test.go:21` (unit/verify) |  |
| `RFC2869-x-3` | MUST | x | `internal/component/l2tp/plugins/authradius/acct_test.go:295` (unit/verify) | `internal/component/l2tp/plugins/authradius/acct_test.go:257` (unit/verify) |  |
| `RFC2869-x-4` | MUST | x | -- | -- | {not-applicable} ze runs only the RADIUS accounting client (NAS) role; it has no accounting-server receive path (internal/component/radius/client.go binds only to receive responses to its own requests), so it never reconstructs a byte total from received Gigaword attributes |
| `RFC2869-x-5` | MUST | x | `internal/component/l2tp/plugins/authradius/acct_test.go:227` (unit/verify) | -- | {single-polarity} splitGigawords derives Gigawords directly from the 64-bit byte counter (internal/component/l2tp/plugins/authradius/acct.go:37-39) with no separate wrap counter; there is no reject path so no negative case exists |
| `RFC2869-x-6` | MAY | x | -- | -- |  |
| `RFC2869-x-7` | MAY | x | -- | -- |  |

