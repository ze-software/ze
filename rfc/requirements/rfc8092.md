# RFC8092 -- enrolled (gated)

GENERATED by `./le rfc index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc8092.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. The exact rows live in `rfc/requirements/rfc8092.md`, and the index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC8092-3-1` | MUST NOT | 3 | `internal/core/bgp/attribute/community_test.go` `TestLargeCommunitiesWriteToNoDuplicates` (unit/verify) | `internal/core/bgp/attribute/community_test.go` `TestLargeCommunities` (unit/verify) |  |
| `RFC8092-3-2` | MUST | 3 | `internal/core/bgp/attribute/community_test.go` `TestLargeCommunitiesDeduplication` (unit/verify) | `internal/core/bgp/attribute/community_test.go` `TestLargeCommunitiesParse` (unit/verify) |  |
| `RFC8092-3-3` | SHOULD | 3 | -- | -- |  |
| `RFC8092-3-4` | NOT RECOMMENDED | 3 | -- | -- |  |
| `RFC8092-4-1` | SHOULD | 4 | -- | -- |  |
| `RFC8092-5-1` | MUST NOT | 5 | `internal/core/bgp/attribute/text_append_test.go` `TestAppendText_LargeCommunityElement` (unit/verify) | -- | {single-polarity} the canonical rendering LargeCommunity.AppendText (internal/core/bgp/attribute/community.go:316) uses strconv.AppendUint, which never emits leading zeros and renders 0 as a single "0", so every value is canonical and there is no non-canonical rendering to assert as a negative |
| `RFC8092-5-2` | SHOULD | 5 | -- | -- |  |
| `RFC8092-6-1` | MUST NOT | 6 | `internal/component/bgp/message/rfc8092_test.go` `TestRFC8092LargeCommunityValidReservedASN` (unit/verify) | -- | {single-polarity} the malformed check validateLargeCommunityAttr (internal/component/bgp/message/rfc7606.go:620) inspects the attribute length only and never the Global Administrator value, so a reserved or unallocated ASN is never treated as malformed and there is no ASN-based rejection to assert as a negative |
| `RFC8092-6-2` | SHALL | 6 | `internal/component/bgp/message/rfc8092_test.go` `TestRFC8092LargeCommunityMalformedLength` (unit/verify) | `internal/component/bgp/message/rfc8092_test.go` `TestRFC8092LargeCommunityValidReservedASN` (unit/verify) |  |
| `RFC8092-6-3` | SHALL NOT | 6 | `internal/core/bgp/attribute/community_test.go` `TestLargeCommunitiesDeduplication` (unit/verify) | -- | {single-polarity} ParseLargeCommunities silently deduplicates and returns no error on duplicates (internal/core/bgp/attribute/community.go:422), and validateLargeCommunityAttr checks only length, so a duplicate is never treated as malformed and there is no duplicate-based rejection to assert as a negative |
| `RFC8092-6-4` | SHALL | 6 | `internal/component/bgp/message/rfc8092_test.go` `TestRFC8092LargeCommunityMalformedLength` (unit/verify) | `internal/component/bgp/message/rfc8092_test.go` `TestRFC8092LargeCommunityValidReservedASN` (unit/verify) |  |

