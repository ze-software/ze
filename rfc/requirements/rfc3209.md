# RFC3209 -- enrolled (gated)

GENERATED by `make ze-rfc-index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc3209.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. The index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC3209-2.5-1` | MUST | 2.5 | `internal/plugins/rsvpte/softstate_test.go:56` (unit/verify) | `internal/plugins/rsvpte/softstate_test.go:21` (unit/verify) |  |
| `RFC3209-4.1-1` | MUST | 4.1 | `internal/plugins/rsvpte/build_test.go:75` (unit/verify) | `internal/plugins/rsvpte/engine_test.go:363` (unit/verify) |  |
| `RFC3209-4.1-2` | MUST | 4.1 | `internal/plugins/rsvpte/wire_test.go:295` (unit/verify) | `internal/plugins/rsvpte/wire_test.go:296` (unit/verify) |  |
| `RFC3209-4.1-3` | MUST | 4.1 | `internal/plugins/rsvpte/fsm_test.go:66` (unit/verify) | -- | {single-polarity} the local label allocator starts at firstDynamicLabel=1000 and wraps back to 1000, so ze never hands out any label in 0-15 and there is no receive path that would allocate a reserved label (internal/plugins/rsvpte/fsm.go:184, :205, :215-217) |
| `RFC3209-4.2-1` | MUST | 4.2 | `internal/plugins/rsvpte/build_test.go:45` (unit/verify) | -- | {single-polarity} buildPath unconditionally appends a LABEL_REQUEST to every PATH it composes, and handlePath does not reject a received PATH lacking one, so only the positive is meaningful (internal/plugins/rsvpte/build.go:92) |
| `RFC3209-4.3.4-1` | MUST | 4.3.4 | `internal/plugins/rsvpte/engine_test.go:194` (unit/verify) | `internal/plugins/rsvpte/engine_test.go:402` (unit/verify) |  |
| `RFC3209-4.3.4.1-1` | MUST | 4.3.4.1 | -- | -- | {gap} nextHopFromERO takes rem[0] as next hop regardless of the L (loose/strict) bit and performs no adjacency check; the Loose flag is decoded and only used for display/config, so a non-adjacent strict hop is neither validated nor rejected (internal/plugins/rsvpte/wire.go:455, :467, engine.go:407-416) |
| `RFC3209-4.4-1` | MAY | 4.4 | -- | -- |  |
| `RFC3209-4.6.1-1` | MUST | 4.6.1 | `internal/plugins/rsvpte/wire_test.go:337` (unit/verify) | -- | {single-polarity} the encoder writes the SESSION reserved octet as 0 on every SESSION it emits, but the decoder never reads that octet, so a non-zero-reserved reject test is not meaningful (internal/plugins/rsvpte/wire.go:238, :243) |
| `RFC3209-4.6.1-2` | MUST | 4.6.1 | `internal/plugins/rsvpte/wire_test.go:336` (unit/verify) | -- | {single-polarity} encodeSessionIPv4 always stamps C-Type 7, but DecodeMessage dispatches SESSION by Class-Num only and never rejects a wrong C-Type, so no negative decode-reject exists (internal/plugins/rsvpte/wire.go:240, :746-752) |
| `RFC3209-4.6.2-1` | MUST | 4.6.2 | `internal/plugins/rsvpte/wire_test.go:362` (unit/verify) | -- | {single-polarity} the encoder zeroes the 2-byte reserved field before LSP ID on every SENDER_TEMPLATE, and decodeSenderTemplate never inspects it (internal/plugins/rsvpte/wire.go:270, :275-276) |
| `RFC3209-6-1` | MUST | 6 | `internal/plugins/rsvpte/reroute_test.go:41` (unit/verify) | `internal/plugins/rsvpte/admission_se_test.go:35` (unit/verify) |  |
| `RFC3209-6-2` | MUST | 6 | `internal/plugins/rsvpte/build_test.go:78` (unit/verify) | -- | {single-polarity} every RESV ze originates carries STYLE = Shared Explicit (18) and admission always applies SE sharing semantics; ze never emits a Fixed-Filter style for an LSP tunnel (internal/plugins/rsvpte/engine.go:272, build.go:126, wire.go:680) |
| `RFC3209-x-1` | MUST | x | -- | -- | {gap} the raw IPv4 socket (proto 46) is opened and used for Sendto with no IP_ROUTER_ALERT socket option and no per-packet IP option, so emitted PATH datagrams carry no Router Alert (the same transport gap as RFC2205-x-1) (internal/plugins/rsvpte/transport_linux.go:35-58, :60-69) |
| `RFC3209-x-2` | SHOULD | x | -- | -- |  |

