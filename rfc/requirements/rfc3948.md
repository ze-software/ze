# RFC3948 -- enrolled (gated)

GENERATED by `./le rfc index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc3948.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. The exact rows live in `rfc/requirements/rfc3948.md`, and the index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC3948-1-1` | MUST | 1 | `internal/component/ike/engine/child_test.go` `TestChildSAInstallsInDataplane` (unit/verify), `internal/component/ike/engine/child_test.go` `TestTunnelModeIsTheChildSADefault` (unit/verify) | `internal/component/ike/engine/child_test.go` `TestTunnelModeIsTheChildSADefault` (unit/verify) |  |
| `RFC3948-2.1-1` | MUST NOT | 2.1 | `internal/component/ike/engine/child_test.go` `TestGenerateESPSPI` (unit/verify) | `internal/component/ike/engine/child_test.go` `TestGenerateESPSPI` (unit/verify) |  |
| `RFC3948-2.1-2` | MUST | 2.1 | `internal/component/ike/engine/child_test.go` `TestChildSANATTEncapPorts` (unit/verify) | -- | {single-polarity} the Child SA install unconditionally sets both UDP-encap ports to the fixed IKE NAT-T port 4500 (internal/component/ike/engine/child.go:237-238,265-266); no ze code path produces a non-4500 encap port, so there is no mismatched-port case to reject |
| `RFC3948-2.1-3` | MUST | 2.1 | `internal/component/ike/transport/nat_test.go` `TestNonESPMarker` (unit/verify), `internal/component/ike/transport/nat_test.go` `TestIsNATKeepalive` (unit/verify) | `internal/component/ike/transport/nat_test.go` `TestNonESPMarkerESPPacket` (unit/verify), `internal/component/ike/transport/nat_test.go` `TestIsNATKeepalive` (unit/verify) |  |
| `RFC3948-2.1-4` | MUST NOT | 2.1 | -- | -- | {not-applicable} ze reads IKE/ESP demux from the OS UDP socket (internal/component/ike/engine/register.go:421) and never inspects the UDP checksum, so no ze code path can depend on it; ESP-in-UDP checksum handling is the kernel's |
| `RFC3948-2.1-5` | SHOULD | 2.1 | -- | -- |  |
| `RFC3948-2.2-1` | MUST | 2.2 | `internal/component/ike/transport/nat_test.go` `TestNonESPMarker` (unit/verify) | `internal/component/ike/transport/nat_test.go` `TestNonESPMarkerESPPacket` (unit/verify) |  |
| `RFC3948-2.3-1` | SHOULD | 2.3 | -- | -- |  |
| `RFC3948-2.3-2` | MUST | 2.3 | `internal/component/ike/transport/keepalive_test.go` `TestNATKeepalive` (unit/verify) | -- | {single-polarity} the keepalive payload is a fixed one-octet constant (internal/component/ike/transport/keepalive.go:14,48) that no input can vary, so there is no non-conforming sender case to drive |
| `RFC3948-3.1.1-1` | MUST | 3.1.1 | `internal/component/ike/engine/child_test.go` `TestChildSAInboundPolicyUsesNegotiatedTS` (unit/verify), `internal/component/ike/engine/rfc3948_inner_source_policy_test.go` `TestChildInboundPolicyDefinesTheValidInnerSourceSpace` (unit/verify) | -- | {single-polarity} ze takes the first option by installing the inbound policy with the negotiated remote traffic selector as its source selector (childPolicyParams, internal/component/ike/engine/child.go), and the drop of a mismatched inner packet is the kernel's, so ze holds no rejecting branch |
| `RFC3948-3.1.2-1` | MUST | 3.1.2 | `internal/le/interoplab/ipsec/checkers.go` `checkNATTTransportInnerChecksum` (interop/nightly) | `internal/le/interoplab/ipsec/checkers.go` `checkNATTTunnelInnerChecksum` (interop/nightly) | **nightly-only** |
| `RFC3948-3.1.2-2` | MUST | 3.1.2 | `internal/le/interoplab/ipsec/checkers.go` `checkNATTTunnelInnerChecksum` (interop/nightly) | `internal/le/interoplab/ipsec/checkers.go` `checkNATTTransportInnerChecksum` (interop/nightly) | **nightly-only** |
| `RFC3948-3.1.2-3` | MAY | 3.1.2 | -- | -- |  |
| `RFC3948-4-1` | MUST | 4 | `internal/component/ike/transport/keepalive_test.go` `TestNATKeepalive` (unit/verify), `internal/component/ike/transport/keepalive_test.go` `TestKeepaliveDefaultInterval` (unit/verify) | -- | {single-polarity} the keepalive interval is a fixed conservative 20s constant (internal/component/ike/transport/keepalive.go:13), well under a typical NAT UDP binding lifetime; there is no dynamic binding-timeout query to drive a negative |
| `RFC3948-4-2` | MAY | 4 | -- | -- |  |
| `RFC3948-4-3` | MUST NOT | 4 | `internal/component/ike/engine/rfc7296_natt_test.go` `TestNATKeepaliveReachesNoSA` (unit/verify) | `internal/component/ike/engine/rfc7296_natt_test.go` `TestNATKeepaliveReachesNoSA` (unit/verify), `internal/component/ike/transport/keepalive_test.go` `TestNATKeepaliveIsNeverDeliveredToASession` (unit/verify) |  |
| `RFC3948-5.1-1` | MUST | 5.1 | -- | -- | {gap} ze devises no such way, because it assigns no inner address at all. The section's RECOMMENDED remedy is to give each remote peer a locally unique address, and the allocator for it exists and is unreached: Pool.Allocate (internal/core/eap/pool.go) leases a unique address and refuses on exhaustion with ErrPoolExhausted, yet its only callers are pool_test.go and pool_release_test.go. registerIKE (internal/component/ike/engine/register.go) builds the pool from the remote-access config and then discards it at a bare `_ = ipPool`, so no lease ever reaches a peer. No engine code constructs a wire.PayloadCP either, so ze sends no CFG_REPLY and a client keeps whatever inner address it chose for itself. Two peers behind one NAT that both chose 10.1.2.3 would therefore present the gateway with the ambiguity the section names, and ze holds nothing that prevents it. Closing this is plan/spec-ike-virtual-ip-assignment.md |
| `RFC3948-5.2-1` | MUST | 5.2 | `internal/component/ike/dataplane/policy_owner_test.go` `TestPolicyOwnerSeparatesDistinctSelectors` (unit/verify) | `internal/component/ike/dataplane/policy_owner_test.go` `TestPolicyOwnerRefusesASecondPeerOnOneSelector` (unit/verify) |  |

