# RFC3579 -- enrolled (gated)

GENERATED by `./le rfc index-update` -- do not edit. Requirement text is authored in `rfc/short/rfc3579.md`; the test links are derived from `RFC requirement:` tags in the tests themselves. The exact rows live in `rfc/requirements/rfc3579.md`, and the index over every RFC is `ai/RFC-REQUIREMENTS.md`.

| Requirement | Level | § | Positive test | Negative test | Note |
|---|---|---|---|---|---|
| `RFC3579-1-1` | MUST NOT | 1 | -- | -- | {gap} ze declares no EAP-Message attribute today (`internal/component/radius/dict.go` names types 1-89 and no 79) and offers no EAP service, so the prohibition holds vacuously and no test asserts the pairing; plan/spec-radius-admin-eap.md adds the EAP service and the attribute together |
| `RFC3579-1-2` | MUST | 1 | -- | -- | {gap} `(*radiusAuthenticator).Authenticate` rejects an Access-Accept whose Service-Type is not Login-User (`AcceptedServiceType`, internal/component/radius/authenticator.go), which is the RFC 2865 form of this rule; it has no notion of an EAP method it cannot offer, so an Accept concluding an EAP conversation ze could not run is unreachable and untested; plan/spec-radius-admin-eap.md |
| `RFC3579-1.2-1` | MUST NOT | 1.2 | -- | -- | {gap} ze's RADIUS admin backend renders no displayable message. It carries no EAP Notification path and drops Reply-Message, so nothing asserts that a message text cannot steer the protocol; plan/spec-radius-admin-eap.md |
| `RFC3579-1.2-2` | MUST | 1.2 | -- | -- | {gap} no displayable-message path exists to encode or decode, so no test asserts the encoding; plan/spec-radius-admin-eap.md |
| `RFC3579-1.2-3` | SHOULD | 1.2 | -- | -- |  |
| `RFC3579-2-1` | MAY | 2 | -- | -- |  |
| `RFC3579-2.1-1` | MUST | 2.1 | -- | -- | {gap} `(*radiusAuthenticator).Authenticate` returns `aaa.ErrAuthRejected` on `CodeAccessReject` today, but there is no EAP conversation for the rule to end and no test tags this document's id; plan/spec-radius-admin-eap.md |
| `RFC3579-2.1-10` | MAY | 2.1 | -- | -- |  |
| `RFC3579-2.1-11` | SHOULD NOT | 2.1 | -- | -- |  |
| `RFC3579-2.1-2` | MUST NOT | 2.1 | -- | -- | {gap} ze builds no EAP packet on the RADIUS path at all, so it cannot manufacture one and nothing asserts the prohibition; plan/spec-radius-admin-eap.md |
| `RFC3579-2.1-3` | MUST | 2.1 | -- | -- | {gap} ze sends one Access-Request with the operator's typed User-Name and never a second one, so there is no subsequent request to carry the identity and no EAP-Response/Identity to copy from; plan/spec-radius-admin-eap.md adds the challenge loop this rule governs |
| `RFC3579-2.1-4` | SHOULD | 2.1 | -- | -- |  |
| `RFC3579-2.1-5` | MAY | 2.1 | -- | -- |  |
| `RFC3579-2.1-6` | MAY | 2.1 | -- | -- |  |
| `RFC3579-2.1-7` | SHOULD | 2.1 | -- | -- |  |
| `RFC3579-2.1-8` | SHOULD | 2.1 | -- | -- |  |
| `RFC3579-2.1-9` | SHOULD | 2.1 | -- | -- |  |
| `RFC3579-2.2-1` | MUST | 2.2 | -- | -- | {gap} ze decapsulates no EAP packet from RADIUS, so nothing validates an EAP header on this path. The EAP peer that will consume the packet parses its own header (`PeerSession.Process`, internal/core/eap), which is not the NAS-side check this requirement states; plan/spec-radius-admin-eap.md |
| `RFC3579-2.2-2` | MUST | 2.2 | -- | -- | {gap} ze reads no Error-Cause attribute and treats every Access-Challenge as a rejection, so this branch does not exist; plan/spec-radius-admin-eap.md |
| `RFC3579-2.2-3` | SHOULD | 2.2 | -- | -- |  |
| `RFC3579-2.6.3-1` | MUST | 2.6.3 | -- | -- | {gap} ze decides on the packet code today (`(*radiusAuthenticator).Authenticate` switches on `resp.Code`), but it reads no EAP-Message, so the rule that separates the two sources is untested and the EAP path that could violate it does not exist yet; plan/spec-radius-admin-eap.md |
| `RFC3579-2.6.3-2` | MUST NOT | 2.6.3 | -- | -- | {gap} no EAP-Message is parsed, so nothing asserts that an encapsulated EAP Success in an Access-Reject is ignored; plan/spec-radius-admin-eap.md |
| `RFC3579-2.6.3-3` | MUST NOT | 2.6.3 | -- | -- | {gap} ze emits no EAP packet on the RADIUS path, so the prohibition is unreached and untested; plan/spec-radius-admin-eap.md |
| `RFC3579-2.6.4-1` | MUST | 2.6.4 | -- | -- | {gap} ze processes Service-Type and the profile attributes inside the Accept branch, so the ordering this rule states is not implemented and no EAP-Message attribute is read at all; plan/spec-radius-admin-eap.md |
| `RFC3579-2.6.5-1` | MUST NOT | 2.6.5 | -- | -- | {gap} ze builds no packet carrying an EAP-Message, so the exclusion is unreachable and untested; plan/spec-radius-admin-eap.md |
| `RFC3579-2.6.5-2` | SHOULD | 2.6.5 | -- | -- |  |
| `RFC3579-3-1` | MUST | 3 | -- | -- | {gap} `(*radiusAuthenticator).Authenticate` always appends `AttrNASIdentifier`, so the value is on the wire today, but the Access-Request this document governs is the EAP-bearing one, which ze does not build, and no test tags this id; plan/spec-radius-admin-eap.md |
| `RFC3579-3-2` | SHOULD | 3 | -- | -- |  |
| `RFC3579-3.1-1` | MUST | 3.1 | -- | -- | {gap} ze encodes no EAP-Message attribute, so there is no fragmenter to order and no reassembler to require order; plan/spec-radius-admin-eap.md |
| `RFC3579-3.1-2` | MUST NOT | 3.1 | -- | -- | {gap} no EAP-Message encoder exists, so nothing enforces one EAP packet per RADIUS packet; plan/spec-radius-admin-eap.md |
| `RFC3579-3.1-3` | MUST | 3.1 | -- | -- | {gap} `internal/component/radius/packet.go` holds two Message-Authenticator verifiers and no signer, so ze cannot put the attribute on an outbound packet; plan/spec-radius-admin-eap.md adds the signer |
| `RFC3579-3.1-4` | MUST | 3.1 | -- | -- | {gap} `verifyResponseMessageAuthenticator` (internal/component/radius/packet.go) recomputes the value over every response and `(*Client).dispatchResponse` drops the datagram on a mismatch, which is the §3.2 half and is proven under RFC2869-5.14-1. Ze supports no EAP-Message attribute, so the §3.1 obligation this id anchors is unreached and no test tags it; plan/spec-radius-admin-eap.md |
| `RFC3579-3.1-5` | SHOULD | 3.1 | -- | -- |  |
| `RFC3579-3.1-6` | SHOULD | 3.1 | -- | -- |  |
| `RFC3579-3.2-1` | MUST | 3.2 | -- | -- | {gap} ze has no signer. The verifiers implement the same walk in reverse, so the algorithm is known, but nothing computes and inserts the value on an outbound packet; plan/spec-radius-admin-eap.md |
| `RFC3579-3.2-2` | MAY | 3.2 | -- | -- |  |
| `RFC3579-3.3-1` | MUST NOT | 3.3 | -- | -- | {gap} ze's accounting builder (`internal/component/l2tp/plugins/authradius/acct.go`) adds neither attribute, so the prohibition holds vacuously and no test asserts it against the EAP work that will introduce both; plan/spec-radius-admin-eap.md |
| `RFC3579-3.3-2` | MUST NOT | 3.3 | -- | -- | {gap} ze builds one credential attribute per Access-Request and has no EAP-Message to add, so the exclusive choice is not yet enforced across four types; plan/spec-radius-admin-eap.md |
| `RFC3579-4.2-1` | MUST | 4.2 | -- | -- | {gap} ze offers no way to run RADIUS over an IPsec SA and its RADIUS server configuration always carries a secret (`internal/component/radius/config.go`), so the zero-length case is unreachable; plan/spec-radius-admin-eap.md does not add it |
| `RFC3579-4.2-2` | MUST | 4.2 | -- | -- | {gap} ze has no RADIUS-over-IPsec profile, so nothing selects a transform for RADIUS traffic; plan/spec-radius-admin-eap.md does not add it |
| `RFC3579-4.2-3` | MUST | 4.2 | -- | -- | {gap} same absent RADIUS-over-IPsec profile; ze negotiates no IPsec SA for RADIUS traffic; plan/spec-radius-admin-eap.md does not add it |
| `RFC3579-4.2-4` | SHOULD | 4.2 | -- | -- |  |
| `RFC3579-4.2-5` | SHOULD | 4.2 | -- | -- |  |
| `RFC3579-4.3.2-1` | SHOULD | 4.3.2 | -- | -- |  |
| `RFC3579-4.3.4-1` | MUST NOT | 4.3.4 | -- | -- | {gap} ze holds both the admin backend's secret and the subscriber backend's secret and compares neither, so a configuration reusing one secret across an EAP NAS and a PAP NAS is accepted in silence; plan/spec-radius-admin-eap.md |
| `RFC3579-4.3.6-1` | MUST | 4.3.6 | -- | -- | {gap} ze runs no EAP conversation on the RADIUS admin path, so there is no unexpected type to detect and no login to fail on that ground; plan/spec-radius-admin-eap.md |
| `RFC3579-4.3.6-2` | MUST NOT | 4.3.6 | -- | -- | {gap} the `auth-method` leaf selects one method for the admin backend and no fallback path exists yet, because EAP is not one of its values; plan/spec-radius-admin-eap.md adds the value and owes the negative test |
| `RFC3579-4.3.6-3` | MUST | 4.3.6 | -- | -- | {gap} ze's PPP NAS offers PAP, CHAP-MD5 and MS-CHAPv2 and never EAP (`internal/component/l2tp/ppp`), so it cannot attempt to negotiate EAP for any session; plan/spec-radius-admin-eap.md |
| `RFC3579-4.3.6-4` | MUST | 4.3.6 | -- | -- | {gap} ze's admin login has no renegotiation path, and its PPP NAS never negotiates EAP, so nothing refuses a renegotiation; plan/spec-radius-admin-eap.md |
| `RFC3579-4.3.6-5` | MUST | 4.3.6 | -- | -- | {gap} ze's PPP NAS never negotiates EAP, so the branch that would send LCP-Terminate on an EAP-incapable server does not exist; plan/spec-radius-admin-eap.md |

