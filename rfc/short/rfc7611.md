# RFC 7611: BGP ACCEPT_OWN Community Attribute

## Meta

| Field | Value |
|-------|-------|
| RFC | 7611 |
| Title | BGP ACCEPT_OWN Community Attribute |
| Enrolment | enrolled |
| Enrolment reason | Ze recognizes ACCEPT_OWN and implements the Section 2.2 discard for families without a Route Distinguisher. Optional re-import under Section 2.1 is disabled; normal loop checks remain active and the Section 3 VPN-IP decision step is not invoked. Conditional requirement scope follows that disabled mode, not an exemption from the non-RD receive rule. |
| Implementation | ze |
| Implementation reason | Ze's receive path discards ACCEPT_OWN for non-RD families. Optional special acceptance and VPN-IP decision processing are disabled. |
| Support | - |

**Status:** Standards Track
**Date:** August 2015
**Authors:** J. Uttaro, P. Mohapatra, D. Smith, R. Raszuk, J. Scudder

## Overview

Defines the ACCEPT_OWN well-known community (0xFFFF0001). Configured VPN
re-import may accept a route carrying this community despite an ORIGINATOR_ID
or NEXT_HOP identifying the receiving speaker. It never bypasses an AS_PATH
loop check.

Primary use case: L3VPN environments where a PE needs to re-import a
route it originated into a different VRF on the same PE, via a route
reflector.

## Well-Known Community

| Community | Value | Hex |
|-----------|-------|-----|
| ACCEPT_OWN | 65535:1 | 0xFFFF0001 |

## Operation

### Normal BGP behavior (without ACCEPT_OWN)

A route identifying the receiver as its originator or next hop is rejected.

### With ACCEPT_OWN

1. Originating PE exports a VPN route with ACCEPT_OWN attached
2. Route reflector reflects the route back to the same PE
3. The configured PE verifies the source and destination VRFs, then applies the ORIGINATOR_ID/NEXT_HOP exception
4. PE imports the route into a different VRF (matched by Route Target)
5. When propagating routes into a VRF the community "SHOULD NOT be propagated", and it "SHOULD NOT be attached to any routes that cannot be associated with a source VRF" (§2.2). RFC 7611 states no rule about removing it before re-advertising to an eBGP peer

### Conditions for acceptance

§2.1: "A router MAY accept a route whose ORIGINATOR_ID or NEXT_HOP value matches that
of the receiving speaker if all of the following are true". The acceptance is a MAY, and
each condition below is quoted from that list. RFC 7611 states no iBGP-only restriction
and no AS_PATH condition.

| Condition | Requirement |
|-----------|-------------|
| Configuration | "Processing of the ACCEPT_OWN community is enabled by configuration" |
| Community | "The route in question carries the ACCEPT_OWN community" |
| Source VRF | "The route in question originated from a source VRF on the router", the VRF whose configured Route Distinguisher equals the route's |
| Destination VRF | "The route in question is targeted to one or more destination VRFs on the router (as determined by inspecting the Route Target(s))" |
| Different VRF | "At least one destination VRF is different from the source VRF" |

A route "MUST NOT ever be accepted back into its source VRF, even if it carries one or
more RTs that match that VRF" (§2.1).

## Key Requirements

- MUST NOT accept a route back into its source VRF, "even if it carries one or more RTs
  that match that VRF" (§2.1)
- MUST discard the community when the route arrives "in an address family that does not
  allow the source VRF to be looked up" (§2.2)
- SHOULD control processing by configuration, and it "MUST default to being disabled"
  where configuration controls it (§2.3)
- MUST NOT apply the §2.1 acceptance rules while ACCEPT_OWN is disabled (§2.3)
- MUST invoke the extra decision-process step only for VPN-IP best path selection, and
  not for imported IP routes in a VRF (§3)

The four bullets this section used to carry (honor only on iBGP, never accept from eBGP,
remove before advertising to eBGP, route reflectors propagate unchanged) are in no
sentence of RFC 7611 and were deleted on 2026-09-21.

## Security Considerations

- Misconfiguration can create forwarding loops between VRFs
- Should only be enabled where the PE/RR topology is well understood
- Configuration must not bypass AS_PATH loop prevention or permit import into the source VRF

## Implementation Notes (Ze context)

Ze parses and displays `accept-own`. On receive it removes that community from
routes in families without a Route Distinguisher, before storing or forwarding
the UPDATE. Other communities and routes remain intact. An UPDATE combining
such a family with a VPN family loses the shared ACCEPT_OWN value on both.
Optional VRF re-import under Section 2.1 is disabled. Ze applies no
special acceptance or decision-process step: normal originator, next-hop and
AS_PATH loop checks remain in force. Section 2.2's non-RD discard still applies.

<!-- source: internal/component/bgp/reactor/session_accept_own.go -- non-RD receive boundary -->

## Related RFCs

| RFC | Relationship |
|-----|--------------|
| RFC 1997 | Defines COMMUNITIES attribute |
| RFC 4271 | BGP-4 base (Section 9: AS_PATH loop detection) |
| RFC 4364 | BGP/MPLS IP VPNs (VRF import/export) |

## Compliance Checklist

- [ ] [RFC7611-2.1-1] [MUST NOT] A route MUST NOT ever be accepted back into its source VRF, even if it carries one or more RTs that match that VRF (§2.1)
- [ ] [RFC7611-2.2-1] [MUST] If a route carrying the ACCEPT_OWN community is received in an address family that does not allow the source VRF to be looked up, the ACCEPT_OWN community MUST be discarded (§2.2)
- [ ] [RFC7611-2.2-2] [SHOULD NOT] When propagating routes into a VRF, the ACCEPT_OWN community SHOULD NOT be propagated, and it SHOULD NOT be attached to any routes that cannot be associated with a source VRF (§2.2)
- [ ] [RFC7611-2.3-1] [MUST] ACCEPT_OWN handling SHOULD be controlled by configuration, and if controlled by configuration, it MUST default to being disabled (§2.3)
- [ ] [RFC7611-2.3-2] [MUST NOT] When ACCEPT_OWN is disabled by configuration, either explicitly or by default, the router MUST NOT apply the special route acceptance rules detailed in Section 2.1 (§2.3)
- [ ] [RFC7611-3-1] [MUST] The extra decision-process step MUST only be invoked during the best path selection process of VPN-IP routes, and MUST NOT be invoked for the best path selection of imported IP routes in a VRF (§3) {not-applicable: Optional ACCEPT_OWN re-import is disabled. Ze does not invoke the extra decision-process step; ordinary ORIGINATOR_ID, NEXT_HOP and AS_PATH loop checks remain active. The separate non-RD receive rule is implemented in internal/component/bgp/reactor/session_accept_own.go.}
- [ ] [RFC7611-2-1] [SHOULD] Processing of the ACCEPT_OWN community SHOULD be controlled by configuration, and the functionality SHOULD default to being disabled (§2)
- [ ] [RFC7611-5-1] [SHOULD] Where a BGP speaker maintains multiple routing contexts by an approach other than RFC 4364, "VRF" should be understood to mean whatever construct provides a routing context, "Route Distinguisher" whatever construct allows a route's originator to associate that route with its source context, and "Route Target" whatever construct allows a route to be targeted for import into a context other than its source (§5)
