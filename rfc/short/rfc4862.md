# RFC 4862 — IPv6 Stateless Address Autoconfiguration (SLAAC)

**Status:** Draft Standard
**Date:** September 2007
**Authors:** S. Thomson, T. Narten, T. Jinmei
**Obsoletes:** RFC 2462

## Purpose

Defines how an IPv6 host configures its own addresses without a stateful server:
it forms a link-local address, then, on receiving a Router Advertisement (RA)
carrying a Prefix Information Option with the Autonomous flag set, forms one or
more global addresses by combining the advertised /64 prefix with an
interface identifier. Each autoconfigured address carries the RA's preferred and
valid lifetimes and is refreshed or expired as the router re-advertises.

## Address lifecycle (§5.5, §5.5.3, §5.5.4)

| State | Meaning |
|-------|---------|
| Tentative | Address is undergoing Duplicate Address Detection (DAD); not yet usable (kernel IFA_F_TENTATIVE). |
| Preferred | Within its *preferred lifetime*: freely used for new connections. |
| Deprecated | Preferred lifetime elapsed but valid lifetime remains: used only for existing connections. |
| Invalid | Valid lifetime elapsed: the address is removed. |

A host **MUST** perform DAD on every unicast autoconfigured address before use
(§5.4). Preferred/valid lifetimes come from the RA's Prefix Information Option
(§5.5.3); a valid lifetime of 0xFFFFFFFF is infinity.

## Relation to other mechanisms

- **RFC 4941** adds *temporary* (privacy) addresses: additional SLAAC addresses
  with randomized interface identifiers and shorter lifetimes, marked by the
  kernel with IFA_F_TEMPORARY.
- **DHCPv6** (RFC 8415) is the stateful alternative; the RA's M/O flags tell a
  host whether to use it. DHCPv6-assigned addresses are also non-permanent but
  are not SLAAC.
- A *manually configured* (static) address has an infinite lifetime and is
  marked IFA_F_PERMANENT by the kernel.

## Compliance Checklist

RFC 4862 intentionally confines every RFC 2119 keyword to the protocol
specification (Section 5); §2.1 says so explicitly. Each line below is one
keyword occurrence from Section 5.

- [ ] [RFC4862-5.1-1] [MUST] A node MUST allow the DupAddrDetectTransmits autoconfiguration variable to be configured by system management for each multicast-capable interface (§5.1)
- [ ] [RFC4862-5.4-1] [MUST] Duplicate Address Detection MUST be performed on all unicast addresses prior to assigning them to an interface, regardless of whether they are obtained through stateless autoconfiguration, DHCPv6, or manual configuration (§5.4)
- [ ] [RFC4862-5.4-2] [MUST NOT] Duplicate Address Detection MUST NOT be performed on anycast addresses (§5.4)
- [ ] [RFC4862-5.4-5] [MUST NOT] New implementations MUST NOT skip DAD for a global address that reuses the link-local interface identifier (the DAD "optimization") (§5.4)
- [ ] [RFC4862-5.4.1-1] [MUST] A node MUST silently discard any Neighbor Solicitation or Advertisement message that does not pass the validity checks specified in [RFC4861] (§5.4.1)
- [ ] [RFC4862-5.4.2-1] [MUST] Before sending a Neighbor Solicitation, an interface MUST join the all-nodes multicast address and the solicited-node multicast address of the tentative address (§5.4.2)
- [ ] [RFC4862-5.4.2-4] [MUST] An interface MUST receive and process datagrams sent to the all-nodes multicast address or solicited-node multicast address of the tentative address during the delay period (§5.4.2)
- [ ] [RFC4862-5.4.3-1] [MUST NOT] In all cases, a node MUST NOT respond to a Neighbor Solicitation for a tentative address (§5.4.3)
- [ ] [RFC4862-5.4.5-1] [MUST NOT] A tentative address that is determined to be a duplicate MUST NOT be assigned to an interface (§5.4.5)
- [ ] [RFC4862-5.5-2] [MUST] The global-address creation processing described in this section MUST be enabled by default (§5.5)
- [ ] [RFC4862-5.5.3-2] [MUST] If the sum of the prefix length and interface identifier length does not equal 128 bits, the Prefix Information option MUST be ignored (§5.5.3)
- [ ] [RFC4862-5.5.4-3] [MUST] IP and higher layers (e.g., TCP, UDP) MUST continue to accept and process datagrams destined to a deprecated address as normal (§5.5.4)
- [ ] [RFC4862-5.5.4-5] [MUST] If an implementation prevents new communication from using a deprecated address, system management MUST have the ability to disable that facility (§5.5.4)
- [ ] [RFC4862-5.5.4-6] [MUST] The facility preventing new communication on a deprecated address MUST be disabled by default (§5.5.4)
- [ ] [RFC4862-5.5.4-7] [MUST NOT] An invalid address MUST NOT be used as a source address in outgoing communications (§5.5.4)
- [ ] [RFC4862-5.5.4-8] [MUST NOT] An invalid address MUST NOT be recognized as a destination on a receiving interface (§5.5.4)
- [ ] [RFC4862-5.4-3] [SHOULD] Each individual unicast address SHOULD be tested for uniqueness (§5.4)
- [ ] [RFC4862-5.4-4] [NOT RECOMMENDED] Performing DAD only for the link-local address and skipping the global address that reuses its interface identifier is NOT RECOMMENDED (§5.4)
- [ ] [RFC4862-5.4.2-2] [SHOULD] If the Neighbor Solicitation is the first message sent after interface (re)initialization, the node SHOULD delay joining the solicited-node multicast address by a random delay between 0 and MAX_RTR_SOLICITATION_DELAY (§5.4.2)
- [ ] [RFC4862-5.4.2-3] [SHOULD] Even when not the first message, the node SHOULD delay joining the solicited-node multicast address by a random delay between 0 and MAX_RTR_SOLICITATION_DELAY if the address being checked is configured by a multicasted router advertisement (§5.4.2)
- [ ] [RFC4862-5.4.5-2] [SHOULD] On DAD failure, the node SHOULD log a system management error (§5.4.5)
- [ ] [RFC4862-5.4.5-3] [SHOULD] If the duplicate link-local address is formed from a hardware-based interface identifier (e.g., EUI-64), IP operation on the interface SHOULD be disabled (§5.4.5)
- [ ] [RFC4862-5.5-1] [SHOULD] Creation of global addresses as described in this section SHOULD be locally configurable (§5.5)
- [ ] [RFC4862-5.5.4-1] [SHOULD] A deprecated address SHOULD continue to be used as a source address in existing communications (§5.5.4)
- [ ] [RFC4862-5.5.4-2] [SHOULD NOT] A deprecated address SHOULD NOT be used to initiate new communications if an alternate (non-deprecated) address of sufficient scope can easily be used instead (§5.5.4)
- [ ] [RFC4862-5.6-1] [SHOULD] If there is no security difference, the most recently obtained values SHOULD have precedence over information learned earlier (§5.6)
- [ ] [RFC4862-5.4.5-4] [MAY] If the duplicate link-local address is not formed from a hardware-based interface identifier, IP operation on the interface MAY be continued (§5.4.5)
- [ ] [RFC4862-5.5.3-1] [MAY] A node MAY wish to log a system management error when the preferred lifetime is greater than the valid lifetime (§5.5.3)
- [ ] [RFC4862-5.5.3-3] [MAY] An implementation MAY wish to log a system management error when the prefix length plus interface identifier length is not 128 bits (§5.5.3)
- [ ] [RFC4862-5.5.4-4] [MAY] An implementation MAY prevent any new communication from using a deprecated address (§5.5.4)

## How ze applies this (kernel-cooperating tracking)

ze does **not** run a userspace RA client or form SLAAC addresses itself — the
Linux kernel does that. ze *observes* the addresses the kernel autoconfigures
and classifies them so status/CLI can distinguish them from static ones
(`internal/plugins/iface/netlink`, spec followup-subsystem AC-6):

- `addrOrigin(isIPv6, IFA_F_*)` -> `static` (IFA_F_PERMANENT), `temporary`
  (IFA_F_TEMPORARY, RFC 4941), `slaac` (non-permanent IPv6), or `dynamic`
  (non-permanent IPv4 / DHCP).
- `AddrInfo.Origin` plus the normalized valid/preferred lifetimes ride the
  interface listing (`show interface`, web) and the netlink addr-event stream.
- Address churn is observed through the existing coalesced netlink monitor
  (RTM_NEWADDR/RTM_DELADDR), so a high-RA network needs no extra observer.
- IFA_F_TENTATIVE IPv6 addresses are dropped from the event stream until DAD
  completes (existing behavior), matching the Tentative state above.
