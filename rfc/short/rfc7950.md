# RFC 7950: The YANG 1.1 Data Modeling Language

## Meta

| Field | Value |
|-------|-------|
| RFC | 7950 |
| Title | The YANG 1.1 Data Modeling Language |
| Status | Standards Track (Proposed Standard) |
| Date | August 2016 |
| Author | M. Bjorklund (Tail-f Systems) |
| Obsoletes | - |
| Updates | - |
| Updated-by | RFC 8342, RFC 8526 |
| Enrolment | enrolled |
| Enrolment reason | The YANG 1.1 Data Modeling Language (ze config validator). The extraction walk of 2026-09-21 read all 227 MUST sites and rewrote this ledger: it added 67 MUST rows the checklist did not carry, covering module and submodule structure, identifier and prefix scoping, typedef and leaf definition rules, list keys and unique constraints, choice, grouping, augment, identity, feature, deviation, status and when rules, the Section 8.1 validity conditions and the Section 8.3 enforcement windows, the built-in type restrictions of Section 9, the module update rules of Section 11 and the YANG 1/1.1 coexistence rules of Section 12. On 2026-09-21 twelve of the added rows gained positive and negative tags (seven at the loader boundary in internal/component/config/yang/loader_rfc7950_test.go, decimal64 and default validation in the validator and the schema builder), one is single-polarity, and the forty-eight left carry {gap} rows naming their specs. Of the nine rows that stood before the walk, four are met with positive+negative tags in internal/component/config: 8.3.1-1 (a value violating a range, length, or pattern restriction is an error), 9.6-1 (an enum value must be one of the defined enums), 9.12-1 (a union value must match at least one member type), and 7.6.5-1 (a missing mandatory node is an error). Five are {not-applicable}: 7.5.3-1 (ze uses no must XPath statements and enforces cross-field constraints with Go validators instead), 9.2.4-1 (derived-type narrowing is resolved by goyang at module-processing time with no runtime surface), 9.4.5-1 (no ze type carries multiple pattern statements), x-1 (ze's models declare no yang-version and default to YANG 1.0, using no 1.1-only construct), and x-2 (no cross-revision position-stability tooling; critical enum values are pinned with explicit value statements). The walk excluded 32 sites as the NETCONF protocol binding, the XML encoding and the YIN syntax, none of which ze uses: ze serves its YANG-modeled config over gNMI and its own CLI, and no Go file in internal/ references NETCONF except a port-name table. Three further sites delegate module-name and namespace assignment to the IANA procedure of the earlier YANG specification they cite. |
| Implementation | ze |
| Implementation reason | Ze's own Go implements this document; each requirement row cites its producer. |
| Support | management 10 |
| Support area | YANG 1.1 data modeling |
| Support status | Partial |
| Support coverage | Config schema loaded from Ze's own YANG modules through goyang; value validation for string (length, pattern), the integer types (range, with negative bounds), decimal64 (lexical form, fraction-digits, range), boolean, enumeration and union; mandatory leaves; min-elements and max-elements; leaf and typedef defaults validated against their type at schema build; the loader refuses a bad escape, an unprefixed external reference, a leaf or typedef without a type, an extension with a non-YANG substatement, a descending or non-numeric range, a negative or descending length, a decimal64 without fraction-digits, and an enum value that is duplicated, beyond int32 or missing after the maximum. |
| Support remaining | Forty-eight MUST rows carry {gap}, each naming its spec. Ze's loader adds no check of its own where goyang accepts a violating module (circular imports, duplicate typedefs and groupings, a shared import prefix, a typedef named after a built-in type, a shared child name across choice cases, an action or notification under a keyless list, an augment adding a duplicate, an overlapping range, an empty enumeration or union, a restricted enumeration adding a name; a self-referencing grouping or identity and an augment targeting a leaf crash goyang instead of being refused): plan/pre-release/spec-config-yang-loader-structural-checks.md. The validator evaluates no when, unique or choice: plan/pre-release/spec-config-yang-when-unique-choice.md. A config list without a key is tolerated, leaf-list values are not deduplicated and leaf-list defaults are not applied: plan/pre-release/spec-config-yang-list-key-leaf-list.md. RPC input and output mandatory and defaults are read by no invocation path: plan/pre-release/spec-config-yang-rpc-mandatory-defaults.md. No repository check reads module namespaces or revisions: plan/spec-config-yang-authoring-checks.md. Ze offers no bits, leafref, identityref or instance-identifier type, no feature or if-feature, no deviation, no yang-version declaration and no submodule: one plan/spec-config-yang-*.md each, for the owner to decline or schedule. |

**Purpose:** Defines YANG 1.1, a data modeling language for configuration data, state data, RPCs, and notifications. Used primarily with NETCONF/RESTCONF but applicable to any management protocol.

**Scope:** Data modeling language — defines structure, types, constraints, and extensions for network management data. Ze uses YANG for config schema definition and validation.

**Ze usage:** Config schema (`ze-bgp-conf.yang`, `ze-types.yang`, `ze-extensions.yang`), type validation (range, pattern, enum, length), custom extensions (`ze:syntax`, `ze:key-type`). Ze uses `goyang` library (not `libyang`) for parsing — no XPath evaluation.

## Core Constructs

### Data Definition Nodes

| Construct | Purpose | Value? | Children? | Ze Usage |
|-----------|---------|--------|-----------|----------|
| `module` | Top-level unit, one per file | N/A | Yes | Each YANG file is a module |
| `container` | Groups related nodes | No | Yes | `bgp`, `peer`, `capability` blocks |
| `leaf` | Single typed value | Yes | No | `hold-time`, `router-id`, `peer-as` |
| `leaf-list` | Multiple typed values | Yes | No | `community`, `family` lists |
| `list` | Keyed collection of entries | No | Yes | `peer` list (keyed by address) |
| `choice` | Mutually exclusive alternatives | N/A | Yes | Not currently used in Ze |
| `grouping` | Reusable node collection | N/A | Yes | `route-attributes` grouping |
| `uses` | Instantiates a grouping | N/A | Yes | Used with `route-attributes` |
| `augment` | Adds nodes to existing schema | N/A | Yes | Plugin YANG augments peer config |
| `typedef` | Named derived type | N/A | N/A | `asn`, `ipv4-address`, `community` |

### Non-Data Constructs

| Construct | Purpose | Ze Usage |
|-----------|---------|----------|
| `rpc` | Remote procedure call definition | API schemas (`ze-bgp-api.yang`) |
| `action` | Operation on data node (new in 1.1) | Not used |
| `notification` | Async server-to-client event | Not used |
| `deviation` | Documents implementation differences | Not used |
| `extension` | Custom statement definition | `ze:syntax`, `ze:key-type`, `ze:validate` |

## Type System

### Built-in Types

| Type | Description | Ze Config Usage |
|------|-------------|-----------------|
| `string` | Character sequence | Most config values |
| `boolean` | true/false | Flags (`enable`, `disable`) |
| `int8`..`int64` | Signed integers | Not commonly used |
| `uint8`..`uint64` | Unsigned integers | `hold-time` (uint16), `local-as` (uint32) |
| `decimal64` | Fixed-point decimal | `decimal-2` typedef (`ze-types.yang`), used by the DDoS and anomaly detection modules; validated for lexical form, fraction-digits and range |
| `enumeration` | Named string constants | `origin` (igp/egp/incomplete), `connection` (both/passive/active) |
| `bits` | Named bit flags | Not used |
| `binary` | Base64-encoded data | Not used |
| `leafref` | Reference to another leaf | Not used (Ze resolves refs in Go code) |
| `identityref` | Reference to identity | Not used |
| `union` | Multiple type options | `ip-address` (IPv4 or IPv6 union) |
| `empty` | Presence-only, no value | Presence containers (e.g., `atomic-aggregate`) |
| `instance-identifier` | Path to data node | Not used |

### Type Restrictions (Section 9)

These are the validation constraints YANG can express natively on types.

| Restriction | Applies To | Syntax | Ze Enforcement |
|-------------|-----------|--------|----------------|
| `range` | Integer, decimal64 | `range "0 \| 3..65535"` | `yang.Validator` checks via `checkYangRange` |
| `length` | String, binary | `length "1..255"` | `yang.Validator` checks string length |
| `pattern` | String | `pattern '[0-9]+:[0-9]+'` | `yang.Validator` checks via `regexp.MatchString` |
| `pattern` + `modifier` | String (1.1 only) | `modifier invert-match` | Not used in Ze |
| `fraction-digits` | decimal64 | `fraction-digits 2` | Not used |

**Range syntax:** `"min..max"` with `min`/`max` keywords for type limits. Pipe `|` separates disjoint ranges. Example: `range "0 | 3..65535"` means 0 or 3-65535.

**Pattern syntax:** XSD regular expressions (similar to POSIX ERE). Anchored implicitly (full match). Multiple patterns on same type are AND-combined.

**Length syntax:** Same range format as `range` but applied to string byte length.

### Derived Types (typedef)

Typedefs create named types with optional restrictions. Ze uses these extensively in `ze-types.yang`:

| Ze Typedef | Base | Restriction |
|-----------|------|-------------|
| `asn` | uint32 | range "1..4294967295" |
| `asn2` | uint16 | range "1..65535" |
| `port` | uint16 | range "1..65535" |
| `ipv4-address` | string | pattern (dotted decimal) |
| `ipv6-address` | string | pattern (colon hex) |
| `ip-address` | union | ipv4-address or ipv6-address |
| `community` | string | pattern `[0-9]+:[0-9]+` |
| `large-community` | string | pattern `[0-9]+:[0-9]+:[0-9]+` |
| `extended-community` | string | pattern `(target\|origin):[0-9]+:[0-9]+` |
| `route-distinguisher` | string | pattern (Type 0 or Type 1) |

## Constraint Mechanisms

### Leaf/Container Constraints

| Constraint | Section | Purpose | Ze Usage |
|-----------|---------|---------|----------|
| `mandatory` | 7.6.5 | Requires node presence | `peer-as mandatory true`, `local-as mandatory true` |
| `default` | 7.6.4 | Default value when absent | `hold-time default 90` |
| `min-elements` | 7.7.5 | Minimum list/leaf-list entries | Not used |
| `max-elements` | 7.7.6 | Maximum list/leaf-list entries | Not used |
| `unique` | 7.8.3 | Unique leaf combinations in list | Not used |
| `ordered-by` | 7.7.7 | User vs system ordering | Not used |
| `presence` | 7.5.1 | Container carries meaning by existing | `atomic-aggregate`, `withdraw` |

### XPath-Based Constraints

| Constraint | Section | Purpose | Ze Usage |
|-----------|---------|---------|----------|
| `must` | 7.5.3 | XPath boolean condition on data | Not used (goyang does not evaluate XPath) |
| `when` | 7.21.5 | Conditional node applicability | Not used (goyang does not evaluate XPath) |

**must statement:** Defines constraints as XPath 1.0 expressions evaluated against the data tree. When the expression evaluates to false, the data is invalid. Supports `error-message` and `error-app-tag` substatements for error reporting.

**when statement:** Controls whether a node is valid based on other data. If the XPath expression is false, the node must not exist. Used for conditional augmentations and conditional leaves.

**Ze alternative:** Ze uses `ze:validate` extension to reference registered Go functions for constraints that would require `must`/`when` in standard YANG.

### Conditional Features

| Construct | Section | Purpose | Ze Usage |
|-----------|---------|---------|----------|
| `feature` | 7.20.1 | Declares optional capability | Not used |
| `if-feature` | 7.20.2 | Gates nodes on feature support | Not used |

YANG 1.1 extends `if-feature` to boolean expressions with AND, OR, NOT operators. Ze does not use features — plugin YANG is either loaded or not.

## Extension Mechanism (Section 7.19)

Extensions define custom statements that add semantics beyond standard YANG. The `extension` statement declares new keywords with optional arguments.

### Extension Definition

An extension is defined at module level with:
- `argument` — defines the parameter the extension takes
- `description` — human-readable purpose

### Extension Usage

Extensions are referenced via prefix in any YANG module that imports the defining module. Other YANG tools can parse modules containing extensions (they just don't execute them).

### Ze Extensions (ze-extensions.yang)

| Extension | Argument | Purpose |
|-----------|----------|---------|
| `ze:syntax` | mode (flex/freeform/inline-list/etc.) | Controls config parser behavior for special syntax |
| `ze:key-type` | type (string/prefix/ip/uint32) | Specifies key type for inline-list nodes |
| `ze:route-attributes` | (none) | Marks node as accepting standard BGP route attributes |
| `ze:allow-unknown-fields` | (none) | Container accepts arbitrary key-value pairs |
| `ze:validate` | function-name | References registered Go validation function (planned) |

**Key property:** Extensions are valid YANG — standard tools (pyang, libyang) parse Ze's schemas without error. They simply ignore the `ze:` extensions they don't understand.

## Enumeration Type (Section 9.6)

Enumerations restrict a leaf to a fixed set of named string values. Each enum value has an associated integer position (auto-assigned if omitted).

### YANG 1.1 Changes

- Enumerations can be subtyped (restrict enum set in derived type)
- `if-feature` allowed on individual enum values
- Position values preserved across subtyping

### Ze Usage

| Location | Enum Values |
|----------|-------------|
| `origin` attribute | igp, egp, incomplete |
| `connection` mode | both, passive, active |
| `role` type | provider, rs, rs-client, customer, peer |
| `encoding-mode` | text, hex, b64 |
| `wire-format` | hex, base64, parsed, full |
| `ack-mode` | sync, async |
| `afi` | ipv4, ipv6 |
| `safi` | unicast, multicast, mpls, mpls-vpn, flowspec |

## Key Changes from YANG 1.0 (RFC 6020)

### Relevant to Ze

| Change | Impact |
|--------|--------|
| `yang-version 1.1` mandatory | Ze modules must declare version |
| Enum/bits subtyping | Can restrict enum sets in derived types |
| Pattern modifier `invert-match` | Negative patterns available (not currently used) |
| `if-feature` on enum/bit values | Conditional enum values (not currently used) |
| Leaf-list defaults | Default values on leaf-lists now supported |
| `action` statement | Operations on data nodes (not used) |
| `must` in input/output | Constraints on RPC args (not used — no XPath eval) |
| `leafref require-instance` | Can relax reference validation (not used) |
| Multiple identity bases | Identity inheritance (not used) |

### Not Relevant to Ze

| Change | Why Not |
|--------|---------|
| `anydata` statement | Ze uses typed schemas, not arbitrary data |
| `empty` in unions | Ze doesn't use empty type in unions |
| Notification on data nodes | Ze uses IPC events, not YANG notifications |
| Module advertising via yang-library | Ze is not a NETCONF server |

## Error Handling

### YANG Validation Errors

| Error Type | Trigger | YANG Response |
|-----------|---------|---------------|
| Range violation | Value outside `range` bounds | `invalid-value` error-tag |
| Length violation | String outside `length` bounds | `invalid-value` error-tag |
| Pattern violation | String doesn't match `pattern` | `invalid-value` error-tag |
| Enum violation | Value not in enumeration | `invalid-value` error-tag |
| Mandatory missing | Required node absent | `missing-element` error-tag |
| Must constraint fail | XPath evaluates to false | `must-violation` error-tag |
| Unique violation | Duplicate unique key combo | `data-not-unique` error-tag |

### Ze Error Mapping

Ze maps YANG errors to `ValidationError` types in `internal/yang/validator.go`:

| YANG Error | Ze ErrorType |
|-----------|-------------|
| Range violation | `ErrTypeRange` |
| Length violation | `ErrTypeLength` |
| Pattern violation | `ErrTypePattern` |
| Enum violation | `ErrTypeEnum` |
| Mandatory missing | `ErrTypeMissing` |
| Type mismatch | `ErrTypeType` |

## XPath Functions (Section 10)

YANG 1.1 adds these XPath functions for use in `must`/`when` expressions:

| Function | Purpose |
|----------|---------|
| `current()` | Returns context node in must/when evaluation |
| `deref(node)` | Follows leafref/instance-identifier to target |
| `derived-from(identity, base)` | Tests identity inheritance |
| `derived-from-or-self(identity, base)` | Tests identity match or inheritance |
| `enum-value(node)` | Returns numeric position of enum value |
| `bit-is-set(bits, name)` | Tests if named bit is set |

**Ze note:** These functions are not available in Ze because goyang does not include an XPath evaluator. Ze uses `ze:validate` Go functions instead.

## MUST Requirements (Ze-Relevant)

### Type Validation

- "If a leaf data value does not match the type constraints for the leaf, including those defined in the type's range, length, and pattern properties, the server MUST reply with an invalid-value error" (Section 8.3.1)
- "An enumeration value MUST be one of the values specified in the type's enum statements" (Section 9.6)
- "A union value MUST match at least one member type" (Section 9.12)

### Mandatory Fields

- "If a mandatory node does not exist, the server MUST reply with a missing-element error" (Section 7.6.5)
- "If a leaf has a default value and the leaf is not set, the default value is used" (Section 7.6.4)

### Constraints

- "If a must expression evaluates to false, the data is not valid" (Section 7.5.3)
- "All range, length, and pattern restrictions MUST be more restrictive than or equal to the base type's restrictions" (Section 9.2.4)
- "Multiple pattern statements are combined as logical AND" (Section 9.4.5)

## Related RFCs

| RFC | Relationship |
|-----|--------------|
| RFC 6020 | YANG 1.0 (predecessor, effectively superseded) |
| RFC 6241 | NETCONF protocol (primary consumer of YANG models) |
| RFC 8040 | RESTCONF (HTTP-based YANG data access) |
| RFC 8342 | NMDA — updates datastore model |
| RFC 8407 | YANG module guidelines |
| RFC 8526 | NETCONF extensions for NMDA |
| RFC 7951 | JSON encoding of YANG data |
| RFC 7952 | YANG metadata annotations |

## Pitfalls

### For Ze Specifically

- **goyang limitations:** goyang parses YANG schemas and builds entry trees but does not evaluate XPath expressions. `must`/`when` constraints are parsed but not enforced — Ze must implement equivalent checks in Go code.
- **Pattern anchoring:** YANG patterns are implicitly anchored (full match). Ze's validator correctly adds `^` and `$` anchors via `regexp.MatchString("^"+p+"$", str)`.
- **Union type ordering:** Union member types are tried in declaration order. First match wins. Order matters when types overlap.
- **Typedef restriction inheritance:** Restrictions on typedefs are inherited by all uses. A `ze:validate` extension on a typedef should also propagate.
- **Augment from plugins:** Plugin YANG modules augment the base schema (e.g., `ze-graceful-restart.yang` augments `peer/capability`). Augmented nodes are only available after all modules are loaded and resolved.
- **Enum position stability:** Enum integer positions must not change across revisions. Adding new enum values should use explicit `value` statements to prevent position shifts.

## Errata

None relevant to Ze's usage.

## Compliance Checklist

- [ ] [RFC7950-8.3.1-1] [MUST] If a leaf data value does not match type constraints (range, length, pattern), server must reply with an invalid-value error (Section 8.3.1)
- [ ] [RFC7950-9.6-1] [MUST] An enumeration value must be one of the values specified in the type's enum statements (Section 9.6)
- [ ] [RFC7950-9.12-1] [MUST] A union value must match at least one member type (Section 9.12)
- [ ] [RFC7950-7.6.5-1] [MUST] If a mandatory node does not exist, server must reply with a missing-element error (Section 7.6.5)
- [ ] [RFC7950-7.5.3-1] [MUST] If a must expression evaluates to false, the data is not valid (Section 7.5.3) {not-applicable: ze uses zero YANG must statements (a grep of the .yang models finds must only inside a description); it parses via goyang which evaluates no XPath, and enforces cross-field constraints with Go ze:validate functions (internal/component/config/yang/validator.go:756 applyCustomValidators) instead, so there is no must-XPath code path}
- [ ] [RFC7950-9.2.4-1] [MUST] All range, length, and pattern restrictions must be more restrictive than or equal to the base type's restrictions (Section 9.2.4) {not-applicable: this is a schema-authoring-time narrowing constraint resolved by goyang during module processing (internal/component/config/yang/loader.go); ze authors valid narrowings and has no runtime enforcement surface for it}
- [ ] [RFC7950-9.4.5-1] [MUST] Multiple pattern statements on the same type are combined as logical AND (Section 9.4.5) {not-applicable: no ze type carries two or more pattern statements (a scan of the .yang models finds none); the validator loop (internal/component/config/yang/validator.go:268) AND-combines patterns if present, but the multi-pattern construct is unused}
- [ ] [RFC7950-x-1] [MUST] YANG modules must declare yang-version 1.1 (Core Constructs) {not-applicable: ze's YANG models declare no yang-version statement and default to YANG 1.0 in goyang; they use no YANG-1.1-only construct that would require the 1.1 declaration, so the version-declaration obligation has no applicable module}
- [ ] [RFC7950-x-2] [MUST] Enum integer positions must not change across revisions (Type System) {not-applicable: ze's modules carry no consumed revision statements and there is no cross-revision position-stability tooling; ze pins the values that matter with explicit value statements (e.g. role.yang, ze-types.yang afi/safi) but enforces no automated cross-revision check}
- [ ] [RFC7950-5.1-1] [MUST] Module names are unique within a server; a module includes all its submodules; a submodule is included only by the module it belongs to or by another submodule of that module, never imports its own module, and never includes a submodule of another module; a submodule includes the same submodule revisions its module includes and no submodule is included at two revisions; an external module is imported before its definitions are referenced, with no circular chains of imports; a reference to an external definition uses a locally defined prefix followed by a colon (Section 5.1, Section 7.1.6, Section 7.2.2) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-5.3-1] [MUST] Choose namespace URIs so they cannot collide with standard or other enterprise namespaces, for example by using the enterprise or organization name in the namespace (Section 5.3) {gap: no repository check reads the namespace or the revision history of the embedded modules; plan/spec-config-yang-authoring-checks.md}
- [ ] [RFC7950-5.5-1] [MUST] A scoped definition does not shadow a definition at a higher scope (Section 5.5) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-5.6.5-1] [MUST] Implement no more than one revision of a module, and where a supported augment or path statement uses a node from an imported module, implement a revision of that module that carries the node (Section 5.6.5) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-6.1.3-1] [MUST] In a double-quoted string a backslash is followed only by one of the characters the escape rules define (Section 6.1.3)
- [ ] [RFC7950-6.2-1] [MUST] Support identifiers up to 64 characters in length (Section 6.2) {single-polarity: positive; the RFC binds an implementation to accept an identifier of up to 64 characters and MAY accept longer, so no identifier length exists that Ze must refuse}
- [ ] [RFC7950-6.2.1-1] [MUST] All identifiers defined in one namespace are unique: a descendant node defines no typedef and no grouping whose name is already visible from an ancestor (Section 6.2.1) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-6.3.1-1] [MUST] Qualify an extension keyword with the prefix of the module that defines it, including inside that module, and process a supported extension in accordance with the specification governing it (Section 6.3.1) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-6.4-1] [MUST] Enforce the requirements the data model encodes, whether or not an XPath interpreter is implemented (Section 6.4) {gap: walkTree evaluates no when, unique or choice; plan/pre-release/spec-config-yang-when-unique-choice.md}
- [ ] [RFC7950-6.4-2] [MUST] XPath expressions are syntactically correct and every prefix they use is present in the XPath context (Section 6.4) {gap: walkTree evaluates no when, unique or choice; plan/pre-release/spec-config-yang-when-unique-choice.md}
- [ ] [RFC7950-6.5-1] [MUST] Qualify a reference to an identifier defined in an external module with the appropriate prefix (Section 6.5)
- [ ] [RFC7950-7.1.4-1] [MUST] All prefixes, the module's own included, are unique within the module or submodule, and where two imported modules define the same prefix at least one is imported under a different prefix (Section 7.1.4) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-7.3-1] [MUST] A typedef's argument is followed by a block of substatements, its type substatement is present, its name is not one of the YANG built-in types, and a top-level typedef name is unique within the module (Section 7.3, Section 7.3.2) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-7.3.4-1] [MUST] A typedef's default value is valid according to its type, and a derived type or leaf whose restrictions invalidate the inherited default specifies a new compatible default (Section 7.3.4)
- [ ] [RFC7950-7.6.1-1] [MUST] Use a leaf's default value when the leaf is not set and its ancestry allows it, and behave operationally as if the leaf were present in the data tree with that value (Section 7.6.1)
- [ ] [RFC7950-7.6.3-1] [MUST] A leaf's type statement is present (Section 7.6.3)
- [ ] [RFC7950-7.6.4-2] [MUST] A leaf's default value is valid according to the leaf's type, is absent where mandatory is true, and is not marked with an if-feature statement (Section 7.6.4)
- [ ] [RFC7950-7.7-1] [MUST] In configuration data the values in a leaf-list are unique, the definitions of default values carry no if-feature statement, and the values in the data tree are in canonical form (Section 7.7) {gap: yangToList tolerates a keyless config list and leaf-lists are neither deduplicated nor defaulted; plan/pre-release/spec-config-yang-list-key-leaf-list.md}
- [ ] [RFC7950-7.7.2-1] [MUST] Use a leaf-list's default values when it is not set and its ancestry allows it, and behave operationally as if the leaf-list were present in the data tree with those values (Section 7.7.2) {gap: yangToList tolerates a keyless config list and leaf-lists are neither deduplicated nor defaulted; plan/pre-release/spec-config-yang-list-key-leaf-list.md}
- [ ] [RFC7950-7.7.4-1] [MUST] A leaf-list's default value is valid according to its type and is absent where min-elements is one or more (Section 7.7.4)
- [ ] [RFC7950-7.7.5-1] [MUST] A valid leaf-list or list has at least min-elements entries (Section 7.7.5)
- [ ] [RFC7950-7.8.2-1] [MUST] A list that represents configuration carries a key statement; each key leaf identifier appears once, refers to a child leaf of the list, is given a value when a list entry is created, and has the same config value as the list (Section 7.8.2) {gap: yangToList tolerates a keyless config list and leaf-lists are neither deduplicated nor defaulted; plan/pre-release/spec-config-yang-list-key-leaf-list.md}
- [ ] [RFC7950-7.8.3-1] [MUST] A unique argument names descendant-form schema node identifiers that each refer to a leaf; where one referenced leaf represents configuration all of them do, and the combined values are unique across all list entries in which every referenced leaf exists (Section 7.8.3) {gap: walkTree evaluates no when, unique or choice; plan/pre-release/spec-config-yang-when-unique-choice.md}
- [ ] [RFC7950-7.9.2-1] [MUST] Child node identifiers are unique across all cases of a choice, a case identifier is unique within its choice, and a schema node identifier always includes the case node identifier (Section 7.9.2) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-7.9.3-1] [MUST] A choice carries no default statement where mandatory is true, and the default case holds no mandatory node (Section 7.9.3) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-7.9.4-1] [MUST] Where a choice is mandatory, at least one node from exactly one of its case branches exists (Section 7.9.4) {gap: walkTree evaluates no when, unique or choice; plan/pre-release/spec-config-yang-when-unique-choice.md}
- [ ] [RFC7950-7.12-1] [MUST] A grouping never references itself, directly or through a chain of groupings, and a top-level grouping identifier is unique within the module (Section 7.12) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-7.14.2-1] [MUST] In an RPC or action invocation a mandatory input leaf is present, the server uses the input defaults in the cases Sections 7.6.1 and 7.7.2 describe and behaves as if the defaulted node were present, and no node whose when statement evaluates to false is present (Section 7.14.2) {gap: no invocation or reply path reads mandatory or default on rpc input and output; plan/pre-release/spec-config-yang-rpc-mandatory-defaults.md}
- [ ] [RFC7950-7.14.3-1] [MUST] In an RPC or action reply a mandatory output leaf is present, the client uses the output defaults in the cases Sections 7.6.1 and 7.7.2 describe and behaves as if the defaulted node were present, and no node whose when statement evaluates to false is present (Section 7.14.3) {gap: no invocation or reply path reads mandatory or default on rpc input and output; plan/pre-release/spec-config-yang-rpc-mandatory-defaults.md}
- [ ] [RFC7950-7.15-1] [MUST] An action is not defined within an rpc, another action or a notification, and has no ancestor node that is a list without a key statement (Section 7.15) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-7.16-1] [MUST] A notification is not defined within an rpc, an action or another notification, has no ancestor node that is a list without a key statement, carries its mandatory leafs, and its receiver uses the notification's default values in the cases Sections 7.6.1 and 7.7.2 describe (Section 7.16) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-7.17-1] [MUST] An augment target is a container, list, choice, case, input, output or notification node; a top-level augment uses the absolute form of a schema node identifier and an augment under uses the descendant form; an augment adds no two nodes of the same name from the same module to one target; and an augment that adds mandatory configuration nodes to another module's target is made conditional with a when statement (Section 7.17) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-7.18.2-1] [MUST] A base argument names an identity defined in the current module or an included submodule, and an identity never references itself, directly or through a chain of identities (Section 7.18.2) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-7.19-1] [MUST] The substatements of an extension usage are YANG statements, extensions included, and follow the syntactical rules of Section 14 (Section 7.19)
- [ ] [RFC7950-7.20.1-1] [MUST] A feature never references itself, and a server that supports a feature supports every feature that feature depends on (Section 7.20.1) {gap: Ze evaluates no if-feature and declares no feature; plan/spec-config-yang-if-feature.md}
- [ ] [RFC7950-7.20.2-1] [MUST] An if-feature argument names a feature defined in the current module or an included submodule, and a leaf that is a list key carries no if-feature statement (Section 7.20.2) {gap: Ze evaluates no if-feature and declares no feature; plan/spec-config-yang-if-feature.md}
- [ ] [RFC7950-7.20.3-1] [MUST] A deviation is never part of a published standard, a server deviation is used only as a last resort, and the data model that results from applying all of a server's deviations in any order is still valid (Section 7.20.3) {gap: Ze reads no deviation; plan/spec-config-yang-deviation.md}
- [ ] [RFC7950-7.20.3.2-1] [MUST] A deviate add does not add a property that can appear only once and already exists in the target node; the properties a deviate replace names exist in the target node; and a deviate delete substatement matches the target node's keyword and argument string (Section 7.20.3.2) {gap: Ze reads no deviation; plan/spec-config-yang-deviation.md}
- [ ] [RFC7950-7.21.2-1] [MUST] A current definition references no deprecated or obsolete definition within the same module, and a deprecated definition references no obsolete definition within the same module (Section 7.21.2) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-7.21.5-1] [MUST] A leaf that is a list key carries no when statement and neither does a uses statement that brings a key leaf into a list; when expressions on referenced nodes are evaluated first; and there are no circular dependencies among when expressions (Section 7.21.5) {gap: walkTree evaluates no when, unique or choice; plan/pre-release/spec-config-yang-when-unique-choice.md}
- [ ] [RFC7950-8.1-1] [MUST] A valid data tree satisfies every Section 8.1 constraint: every leaf value matches its type constraints including range, length and pattern; all key leafs are present for every list entry; nodes are present for at most one case branch of a choice; no node tagged with an if-feature whose expression is false and no node tagged with a when whose condition is false is present; every path referential-integrity constraint is satisfied; every unique constraint is satisfied; mandatory is enforced for leafs and choices; and the constraint holds for configuration data, state data, notification content, and RPC or action input and output (Section 8.1) {gap: walkTree evaluates no when, unique or choice; plan/pre-release/spec-config-yang-when-unique-choice.md}
- [ ] [RFC7950-8.1-2] [MUST] The running configuration datastore is always valid (Section 8.1)
- [ ] [RFC7950-8.3-1] [MUST] Enforce configuration constraints in each of the three windows Section 8.3 defines (Section 8.3)
- [ ] [RFC7950-8.3.1-2] [MUST] Reject content that carries data for more than one case branch of a choice, or data for a node whose if-feature expression or when condition evaluates to false, answering with the error the management protocol defines (a bad-element or unknown-element error-tag under NETCONF) (Section 8.3.1) {gap: walkTree evaluates no when, unique or choice; plan/pre-release/spec-config-yang-when-unique-choice.md}
- [ ] [RFC7950-8.3.2-1] [MUST] While processing a datastore modification, detect data for a node whose if-feature expression or when condition evaluates to false and reject it (Section 8.3.2) {gap: walkTree evaluates no when, unique or choice; plan/pre-release/spec-config-yang-when-unique-choice.md}
- [ ] [RFC7950-8.3.3-1] [MUST] When datastore processing is complete the final contents obey every validation constraint, enforced at the end of the operation for the running and startup datastores (Section 8.3.3)
- [ ] [RFC7950-9.1-1] [MUST] Support all the lexical representations this specification defines, and where a type has no canonical form the value's format matches the type's lexical representation (Section 9.1)
- [ ] [RFC7950-9.2.4-2] [MUST] The values and ranges of a range restriction are disjoint and in ascending order, and every explicit value and range boundary matches the type being restricted or is one of the special values min and max (Section 9.2.4) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-9.3.2-1] [MUST] A decimal64 value carries at least one digit before and after the decimal point, with no leading or trailing zeros (Section 9.3.2)
- [ ] [RFC7950-9.3.4-1] [MUST] The fraction-digits statement is present where the type is decimal64 (Section 9.3.4)
- [ ] [RFC7950-9.4.4-1] [MUST] Length-restricting values are never negative, and the values and ranges of a length restriction are disjoint and in ascending order (Section 9.4.4)
- [ ] [RFC7950-9.5.1-1] [MUST] Represent a boolean value as the lowercase string true or false (Section 9.5.1)
- [ ] [RFC7950-9.6.4-1] [MUST] The enum statement is present where the type is enumeration; an assigned name is not zero-length and carries no leading or trailing whitespace; and all assigned names in an enumeration are unique (Section 9.6.4) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-9.6.4-2] [MUST] When an existing enumeration type is restricted, the new type's assigned names are a subset of the base type's and the value of an assigned name is not changed (Section 9.6.4) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-9.6.4.2-1] [MUST] An enum value is in the range -2147483648 to 2147483647 and unique within the enumeration type; once the highest value reaches 2147483647 a value is given explicitly for the enum substatements that follow; and a restricted enumeration either repeats the base type's value or omits the value statement (Section 9.6.4.2)
- [ ] [RFC7950-9.7-1] [MUST] When an existing bits type is restricted, the new type's assigned names are a subset of the base type's and the bit position of an assigned name is not changed (Section 9.7) {gap: Ze offers no bits type; plan/spec-config-yang-type-bits.md}
- [ ] [RFC7950-9.7.4-1] [MUST] The bit statement is present where the type is bits, and all assigned names in a bits type are unique (Section 9.7.4) {gap: Ze offers no bits type; plan/spec-config-yang-type-bits.md}
- [ ] [RFC7950-9.7.4.2-1] [MUST] A bit position is in the range 0 to 4294967295 and unique within the bits type; once the highest position reaches 4294967295 a position is given explicitly for the bit substatements that follow; and a restricted bits type either repeats the base type's position or omits the position statement (Section 9.7.4.2) {gap: Ze offers no bits type; plan/spec-config-yang-type-bits.md}
- [ ] [RFC7950-9.9-1] [MUST] Where require-instance is true the node a leafref refers to exists; a leafref that represents configuration refers to configuration; there are no circular chains of leafrefs; and a leafref to a feature-conditional leaf is itself conditional on at least the same features (Section 9.9) {gap: Ze offers no leafref type; plan/spec-config-yang-type-leafref.md}
- [ ] [RFC7950-9.9.2-1] [MUST] The path statement is present where the type is leafref, its argument refers to a leaf or leaf-list node, and where require-instance is true the node set it selects is non-empty (Section 9.9.2) {gap: Ze offers no leafref type; plan/spec-config-yang-type-leafref.md}
- [ ] [RFC7950-9.9.3-1] [MUST] Where require-instance is true, the instance being referred to exists for the data to be valid (Section 9.9.3) {gap: Ze offers no leafref type; plan/spec-config-yang-type-leafref.md}
- [ ] [RFC7950-9.10.2-1] [MUST] The base statement is present at least once where the type is identityref, and its argument names an identity defined in the current module or an included submodule (Section 9.10.2) {gap: Ze offers no identityref type; plan/spec-config-yang-type-identityref.md}
- [ ] [RFC7950-9.10.3-1] [MUST] An identityref value names an identity defined in the current module or one of its submodules (Section 9.10.3) {gap: Ze offers no identityref type; plan/spec-config-yang-type-identityref.md}
- [ ] [RFC7950-9.12-2] [MUST] The type statement is present where the type is union (Section 9.12) {gap: goyang accepts the violating module and Ze adds no check of its own; plan/pre-release/spec-config-yang-loader-structural-checks.md}
- [ ] [RFC7950-9.13-1] [MUST] An instance-identifier gives one equality-test predicate per key of a list entry; where it represents configuration and require-instance is true the node it refers to represents configuration; and the nodes it references exist for the data to be valid (Section 9.13) {gap: Ze offers no instance-identifier type; plan/spec-config-yang-type-instance-identifier.md}
- [ ] [RFC7950-11-1] [MUST] When a module is updated a new revision statement is placed in front of the existing ones, or added where none exist; the organization and contact metadata statements are updated as needed; the module name and the namespace statement are never changed; obsolete definitions are never removed from a published module; a change to the semantics of a definition is made through a new definition; and data definition substatements are never reordered (Section 11) {gap: no repository check reads the namespace or the revision history of the embedded modules; plan/spec-config-yang-authoring-checks.md}
- [ ] [RFC7950-12-1] [MUST] A YANG 1.1 module includes no YANG 1 submodule and a YANG 1 module includes no YANG 1.1 submodule, and a YANG 1 module or submodule does not import a YANG 1.1 module by revision (Section 12) {gap: no Ze module declares yang-version and none is a submodule; plan/spec-config-yang-version-submodule.md}
- [ ] [RFC7950-7.6.4-1] [SHOULD] If a leaf has a default value and the leaf is not set, the default value should be used (Section 7.6.4)
