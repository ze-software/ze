# RFC 9582: The Resource Public Key Infrastructure (RPKI) to Router Protocol, Version 2

## Meta

| Field | Value |
|-------|-------|
| RFC | 9582 |
| Title | A Profile for Route Origin Authorizations (ROAs) |
| Status | Standards Track |
| Date | May 2024 |
| Authors | J. Snijders (Fastly), B. Maddison (Workonline), M. Lepinski |
| Obsoletes | RFC 6482 |
| Obsoleted-by | - |
| Enrolment | enrolled |
| Enrolment reason | Two row sets live in this file. The twenty-two ids under Sections 3, 4 and 5 are the obligations RFC 9582 itself states, added by the 2026-09-21 extraction sign-off; fourteen are MUST level and none carries a test, because ze runs no relying-party validator (`Support coverage` below). The thirteen ids under Sections 5.12 and 7 describe ASPA PDUs and RTR version negotiation, which RFC 9582 does not contain: they belong to `draft-ietf-sidrops-8210bis` and are proven by tags in `internal/component/bgp/plugins/rpki/rtr_pdu_test.go` and `rtr_session_test.go`. Moving them is `plan/pre-release/spec-rfc-requirement-reattribution.md`, which the ratchets refuse until proof can follow an obligation to another document. |
| Implementation | third-party |
| Implementation reason | The RPKI cache validates the CMS and X.509 profile. Ze consumes an already-validated payload over the RTR protocol, internal/component/bgp/plugins/rpki/rtr_pdu.go. |
| Support | bgp-operations 70 |
| Support area | A Profile for Route Origin Authorizations (ROAs) |
| Support status | Unsupported |
| Support coverage | Nothing. RFC 9582 profiles the ROA signed object itself: a CMS envelope carrying a `RouteOriginAttestation`, an EE certificate bearing an RFC 3779 IP address delegation extension, and a certification path to a trust anchor. No ROA object exists anywhere in the ze process. `parsePrefixPDU` reads an already-validated payload off an RTR PDU at fixed byte offsets, so the decode, the extension reading and the path validation this RFC specifies all happen in the RPKI cache ze dials. <!-- source: internal/component/bgp/plugins/rpki/rtr_pdu.go -- parsePrefixPDU --> |
| Support remaining | Every obligation, and implementing them means a relying-party validator: a CMS/DER decoder (RFC 5652 and RFC 6488), an RFC 3779 extension reader, X.509 path validation to a trust anchor, and an RRDP or rsync fetcher. That is what Routinator and rpki-client are, and ze delegates it deliberately. **This row read `Supported` until 2026-09-01, and the claim was never about this RFC.** `rfc/short/rfc9582.md` still declares thirteen ids describing ASPA PDUs and RTR version negotiation, which are `draft-ietf-sidrops-8210bis`: RFC 9582 has no Section 5.12 and its Section 7 is IANA Considerations, so each id cites a section its own document does not have. The obligations are real and ze meets them; only the attribution is wrong. Correcting it is `plan/pre-release/spec-rfc-requirement-reattribution.md`, which the ledger's own ratchets refuse until they learn to let proof follow an obligation to another document. |

**Purpose:** Defines version 2 of the RPKI-to-Router protocol (RTR), adding support for ASPA (Autonomous System Provider Authorization) objects alongside existing ROA prefix and Router Key distribution.

**Scope:** Cache-to-router communication. Extends RTR v1 (RFC 8210) with new PDU types for ASPA records. Same transport, timing, and session management model.

## Changes from RFC 8210 (Version 1)

| Change | Section | Impact |
|--------|---------|--------|
| New ASPA PDU (Type 11) | 5.12 | Distributes ASPA customer-provider records to routers |
| Protocol version incremented from 1 to 2 | 5.1 | All PDUs carry version field = 2 |
| Version negotiation extended to v2 | 7 | Same pattern: client sends v2, server responds or downgrades |
| New error code for ASPA | 12 | Handling of unknown PDU type in older implementations |

## Wire Formats

### ASPA PDU (Type 11) -- NEW in Version 2

```
 0          8          16         24        31
.-------------------------------------------.
| Version  | Type=11  |        zero         |
+-------------------------------------------+
|                  Length                    |
+-------------------------------------------+
|  Flags   |   AFI    |       zero          |
+-------------------------------------------+
|            Customer AS Number             |
+-------------------------------------------+
|           Provider AS Number 1            |
+-------------------------------------------+
|           Provider AS Number 2            |
+-------------------------------------------+
|                   ...                     |
+-------------------------------------------+
|           Provider AS Number N            |
`-------------------------------------------'
```

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Version | 0 | 1 | uint8 | 2 |
| PDU Type | 1 | 1 | uint8 | 11 |
| zero | 2 | 2 | uint16 | Must be 0 |
| Length | 4 | 4 | uint32 | 16 + 4*N (N = provider count) |
| Flags | 8 | 1 | uint8 | Bit 0: 1=announce, 0=withdraw |
| AFI Flags | 9 | 1 | uint8 | 0=IPv4+IPv6, 1=IPv4-only, 2=IPv6-only |
| zero | 10 | 2 | uint16 | Must be 0 |
| Customer AS | 12 | 4 | uint32 | Customer ASN (1 to 2^32-2) |
| Provider ASNs | 16 | 4*N | []uint32 | Set of authorized provider ASNs |

**Fixed portion:** 16 bytes (header + flags + customer AS).
**Variable portion:** 4 bytes per provider ASN.
**Minimum length:** 20 bytes (one provider).

### AFI Flags Semantics

| Value | Meaning |
|-------|---------|
| 0 | ASPA applies to both IPv4 and IPv6 |
| 1 | ASPA applies to IPv4 unicast only |
| 2 | ASPA applies to IPv6 unicast only |

**Note:** Early drafts used per-AFI ASPA records. RFC 9582 simplified to a single provider set with AFI flags indicating applicability. Most deployments use AFI=0 (both).

### PDU Types (Complete for v2)

| Type | Name | Length | Direction | New in v2 |
|------|------|--------|-----------|-----------|
| 0 | Serial Notify | 12 | Cache->Router | No |
| 1 | Serial Query | 12 | Router->Cache | No |
| 2 | Reset Query | 8 | Router->Cache | No |
| 3 | Cache Response | 8 | Cache->Router | No |
| 4 | IPv4 Prefix | 20 | Cache->Router | No |
| 6 | IPv6 Prefix | 32 | Cache->Router | No |
| 7 | End of Data | 24 | Cache->Router | No |
| 8 | Cache Reset | 8 | Cache->Router | No |
| 9 | Router Key | variable | Cache->Router | No (v1) |
| 10 | Error Report | variable | Either | No |
| 11 | ASPA | variable | Cache->Router | Yes |

Type 5 remains unassigned.

## Protocol Version Negotiation (Extended for v2)

```
1. Router sends Reset Query or Serial Query with version = 2
2. If cache supports version 2: responds with version 2 PDUs (may include ASPA)
3. If cache only supports version 1:
   a. Cache sends Error Report with code 4 (Unsupported Protocol Version),
      max supported version in the version field of the error PDU
   b. Router downgrades: re-sends query with version = 1
   c. ASPA data unavailable at v1 (no Type 11 PDU)
4. If cache only supports version 0: same downgrade as v1->v0
5. Once negotiated, version is stable for session lifetime
```

**Key constraint:** ASPA PDUs are ONLY sent when the negotiated version is 2. A v1 session never contains ASPA data.

## MUST Requirements

### ASPA PDU

| Requirement | Section | Context |
|-------------|---------|---------|
| Provider AS set MUST contain at least one provider ASN | 5.12 | Empty provider set is malformed |
| Customer AS MUST NOT appear in its own provider set | 5.12 | Self-reference is invalid |
| Provider ASNs MUST be in ascending order within the PDU | 5.12 | Canonical form for deduplication |
| Cache MUST ensure one ASPA PDU per (Customer-AS, AFI) pair | 5.12 | Deduplication rule |
| Withdraw ASPA MUST match exact (Customer-AS, AFI) pair | 5.12 | Withdrawal semantics |
| Router MUST ignore ASPA PDUs with unknown AFI values | 5.12 | Forward compatibility |

### Version Negotiation (same as v1, extended)

| Requirement | Section | Context |
|-------------|---------|---------|
| Router starting a v2 session MUST send query with version=2 | 7 | Initiate negotiation |
| On Unsupported Protocol Version error, router MUST downgrade or disconnect | 7 | Graceful fallback |
| Cache receiving version it does not support MUST send error code 4 | 7 | Reject unknown version |

## SHOULD/MAY

| Type | Requirement | Section | Notes |
|------|-------------|---------|-------|
| [SHOULD] | Router SHOULD start at highest supported version | 7 | Start at v2, fall back if needed |
| [SHOULD] | Provider ASNs SHOULD be sorted ascending | 5.12 | Cache MUST sort; router SHOULD verify |
| [MAY] | Router MAY ignore AFI field and apply ASPA to all families | 5.12 | Simplification for routers |

## ASPA Cache Management

### Storage Model

For each (Customer-AS, AFI) tuple, store the complete set of authorized provider ASNs.

| Operation | Semantics |
|-----------|-----------|
| Announce (flags bit 0 = 1) | Replace entire provider set for (Customer-AS, AFI) |
| Withdraw (flags bit 0 = 0) | Remove ASPA record for (Customer-AS, AFI) |

**Important:** ASPA announce replaces the full provider set, not a delta. If Customer AS 64500 changes from providers {1, 2, 3} to {1, 2, 4}, the cache sends a single announce PDU with {1, 2, 4}.

### Delta Handling

Within a Cache Response / End of Data exchange:
- Multiple ASPA PDUs for same (Customer-AS, AFI): last one wins
- Withdraw followed by announce: net effect is the announce
- Announce followed by withdraw: net effect is removal

## Constants

| Name | Value | Usage |
|------|-------|-------|
| Protocol Version | 2 | Version 2 of RPKI-Router protocol |
| ASPA PDU Type | 11 | ASPA record distribution |
| ASPA PDU minimum length | 20 | Header (8) + flags/customer (8) + one provider (4) |
| ASPA PDU fixed portion | 16 | Before provider list |

## Algorithms

### Router ASPA Cache Update (on End of Data)

```
1. Accumulate ASPA PDUs between Cache Response and End of Data
2. For withdrawals: mark (Customer-AS, AFI) for removal
3. For announces: store complete provider set for (Customer-AS, AFI)
4. At End of Data: apply all changes atomically
5. Trigger re-validation of affected routes (routes with Customer-AS in AS_PATH)
```

## Pitfalls

### Edge Cases

- **Empty provider set:** PDU with Length=16 (no providers) is malformed; MUST have at least one provider
- **AFI=0 vs per-AFI:** If both AFI=0 and AFI=1 records exist for same customer, behavior is implementation-defined; caches SHOULD only emit one form
- **Provider ordering:** Providers MUST be sorted ascending in the PDU; unsorted PDUs are malformed
- **Large provider sets:** A customer with many providers (100+) produces a single large PDU; implementations must handle variable-length reads
- **Customer AS 0:** Reserved, MUST NOT appear; ignore if received

### Interop

- **v1 caches:** Will never send ASPA PDUs; router gets ROA-only validation
- **Mixed v1/v2 caches:** Different caches may provide different RPKI data subsets; track per-cache
- **PDU Type 11 in v1 session:** If received, this is a protocol violation; send Error Report code 5 (Unsupported PDU Type)

### Security

- **ASPA data integrity:** Same trust model as ROA; relies on transport security to cache
- **Provider set completeness:** An incomplete ASPA (missing a legitimate provider) causes false Invalid results for routes through that provider
- **Transition period:** During ASPA deployment, many ASes lack ASPA records; Unknown (not Invalid) is the expected result

## Compatibility

### With Version 1 (RFC 8210)

- All v1 PDU types valid in v2 (same type codes, same formats)
- ASPA PDU (Type 11) only in v2 sessions
- End of Data unchanged (24 bytes, same timing parameters)
- Error codes unchanged (0-8)
- Session ID and serial semantics unchanged

### Feature Availability by Version

| Feature | Version 0 | Version 1 | Version 2 |
|---------|-----------|-----------|-----------|
| IPv4/IPv6 Prefix PDUs | Yes | Yes | Yes |
| Router Key PDU | No | Yes | Yes |
| Timing parameters | No | Yes | Yes |
| ASPA PDU | No | No | Yes |

## Related RFCs

| RFC | Relationship |
|-----|--------------|
| RFC 8210 | Version 1 of this protocol (obsoleted by 9582) |
| RFC 6810 | Version 0 of this protocol |
| RFC 6811 | BGP Prefix Origin Validation (ROA consumer) |
| RFC 9319 | Updates to RFC 6811 |
| draft-ietf-sidrops-aspa-verification | ASPA path verification algorithm (consumer of ASPA data) |

## Errata

None reported for RFC 9582 as of May 2025.

## Compliance Checklist

- [ ] [RFC9582-5.12-1] [MUST] Provider AS set in ASPA PDU MUST contain at least one provider ASN (§5.12)
- [ ] [RFC9582-5.12-2] [MUST NOT] Customer AS MUST NOT appear in its own provider set (§5.12)
- [ ] [RFC9582-5.12-3] [MUST] Provider ASNs MUST be in ascending order within the PDU (§5.12)
- [ ] [RFC9582-5.12-4] [MUST] Cache MUST ensure one ASPA PDU per (Customer-AS, AFI) pair (§5.12) {not-applicable: this is a cache-side emission/deduplication guarantee; ze is the RTR router that only consumes ASPA PDUs (no ASPA PDU writer exists, only query writers) and never enforces or emits this pairing (internal/component/bgp/plugins/rpki/rtr_pdu.go:92, :103)}
- [ ] [RFC9582-5.12-5] [MUST] Withdraw ASPA MUST match exact (Customer-AS, AFI) pair (§5.12) {not-applicable: exact-(Customer-AS,AFI) withdraw matching binds the cache's emission and a per-AFI record model; ze consumes withdraws keyed on Customer-AS alone (the §5.12 router option to ignore AFI) and maintains no AFI dimension to match (internal/component/bgp/plugins/rpki/rtr_session.go:283, aspa_cache.go:114)}
- [ ] [RFC9582-5.12-6] [MUST] Router MUST ignore ASPA PDUs with unknown AFI values (§5.12)
- [ ] [RFC9582-5.12-7] [MUST NOT] Customer AS 0 is reserved, MUST NOT appear (§5.12)
- [ ] [RFC9582-7-1] [MUST] Router starting a v2 session MUST send query with version=2 (§7) {single-polarity: positive; ze constructs every session at rtrVersionMax and writes that version unconditionally into the initial query, so the emitted version byte is observable but there is no malformed input that yields a wrong-version query to test negatively}
- [ ] [RFC9582-7-2] [MUST] On Unsupported Protocol Version error, router MUST downgrade or disconnect (§7)
- [ ] [RFC9582-7-3] [MUST] Cache receiving a version it does not support MUST send error code 4 (§7) {not-applicable: this binds the RTR cache/server role; ze runs only an RTR client that dials out and reads error reports, with no listener and no error-report writer, so it never receives queries or sends error code 4 (internal/component/bgp/plugins/rpki/rtr_session.go:125)}
- [ ] [RFC9582-7-4] [SHOULD] Router SHOULD start at highest supported version (§7)
- [ ] [RFC9582-5.12-8] [SHOULD] Provider ASNs SHOULD be sorted ascending; cache MUST sort, router SHOULD verify (§5.12)
- [ ] [RFC9582-5.12-9] [MAY] Router MAY ignore AFI field and apply ASPA to all address families (§5.12)
- [ ] [RFC9582-3-1] [MUST] The ROA content-type OID 1.2.840.113549.1.9.16.1.24 MUST appear within both the eContentType in the encapContentInfo object and the content-type signed attribute in the signerInfo object (§3)
- [ ] [RFC9582-4.1-1] [MUST] The version number of the RouteOriginAttestation entry MUST be 0 (§4.1)
- [ ] [RFC9582-4.3.1-1] [MUST] addressFamily MUST be either 0001 or 0002 (§4.3.1)
- [ ] [RFC9582-4.3.1-2] [MUST NOT] IPv4 prefixes MUST NOT appear as IPv4-mapped IPv6 addresses (§4.3.1)
- [ ] [RFC9582-4.3.1-3] [MUST] There MUST be only one instance of ROAIPAddressFamily per unique AFI in the ROA (§4.3.1)
- [ ] [RFC9582-4.3.1-4] [MUST NOT] The ROAIPAddressFamily structure MUST NOT appear more than twice (§4.3.1)
- [ ] [RFC9582-4.3.2.2-1] [MUST] If present, the maxLength element MUST be an integer greater than or equal to the length of the accompanying prefix, and less than or equal to the maximum length in bits of an IP address in the applicable address family: 32 for IPv4 and 128 for IPv6 (§4.3.2.2)
- [ ] [RFC9582-5-1] [MUST] Before a Relying Party can use a ROA to validate a routing announcement, the Relying Party MUST first validate the ROA (§5)
- [ ] [RFC9582-5-2] [MUST] To validate a ROA, the Relying Party MUST perform all the validation checks specified in RFC 6488 as well as the ROA-specific validation steps of Section 5 (§5)
- [ ] [RFC9582-5-3] [MUST NOT] The EE certificate's IP address delegation extension MUST NOT contain "inherit" elements as described in RFC 3779 (§5)
- [ ] [RFC9582-5-4] [MUST NOT] The Autonomous System identifier delegation extension described in RFC 3779 is not used in ROAs and MUST NOT be present in the EE certificate (§5)
- [ ] [RFC9582-5-5] [MUST] If any of the Section 5 checks fail, the ROA in its entirety MUST be considered invalid (§5)
- [ ] [RFC9582-5-6] [MUST] The IP address delegation extension is present in the EE certificate and every IP address prefix in the ROA payload is contained within the set of IP addresses specified by that extension (§5)
- [ ] [RFC9582-5-7] [MUST] The ROA content fully conforms with all requirements specified in Sections 3 and 4 (§5)
- [ ] [RFC9582-6-1] [MUST] The integrity of a ROA MUST be established (§6)
- [ ] [RFC9582-6-2] [MUST] One MUST verify the signature on the ROA using an X.509 certificate issued under this PKI and check that the prefix or prefixes in the ROA are contained within those in the certificate's IP address delegation extension (§6)
- [ ] [RFC9582-4.3.2.2-2] [SHOULD NOT] The maxLength element SHOULD NOT be encoded if the maximum length is equal to the prefix length (§4.3.2.2)
- [ ] [RFC9582-4.3.2.2-3] [SHOULD] Certification Authorities SHOULD anticipate that future Relying Parties will become increasingly stringent in considering the presence of superfluous maxLength elements an encoding error (§4.3.2.2)
- [ ] [RFC9582-4.3.3-1] [SHOULD] The canonicalization process described in Section 4.3.3 SHOULD be used to ensure that information elements are unique with respect to one another and sorted in ascending order (§4.3.3)
- [ ] [RFC9582-4.3.3-2] [SHOULD] Certification Authorities SHOULD anticipate that future Relying Parties will impose a strict requirement for the ipAddrBlocks field to be in canonical form (§4.3.3)
- [ ] [RFC9582-5-8] [SHOULD] If any of the Section 5 checks fail, an error SHOULD be logged (§5)
- [ ] [RFC9582-4.3.2.3-1] [NOT RECOMMENDED] A ROA MAY contain two ROAIPAddress elements where the IP address prefix is identical in both cases, but this is NOT RECOMMENDED because the element with the shorter maxLength grants no additional privileges (§4.3.2.3)
