# RFC 9494: Long-Lived Graceful Restart for BGP

## Meta

| Field | Value |
|-------|-------|
| RFC | 9494 |
| Title | Long-Lived Graceful Restart for BGP |
| Enrolment | enrolled |
| Enrolment reason | Long-Lived Graceful Restart for BGP |
| Support | bgp-base 140 |
| Support area | Long-Lived Graceful Restart |
| Support status | Partial |
| Support coverage | Helper-side LLGR: capability code 71 declared only alongside GR and disregarded when GR is absent, per-family Long-Lived Stale Time decode and advertisement, GR-to-LLGR handover with per-family LLST timers, NO_LLGR deletion and LLGR_STALE attachment on LLGR entry, LLGR_STALE routes treated as least preferred in best-path, and partial deployment toward non-LLGR neighbors (NO_EXPORT plus LOCAL_PREF=0 for iBGP, withdrawal for eBGP) (internal/component/bgp/plugins/gr, internal/component/bgp/plugins/rib). Requirements bound per line in rfc/short/rfc9494.md. |
| Support remaining | Five MUST gaps annotated in rfc/short/rfc9494.md: LLGR_STALE is not protected on further advertisement -- a peer whose `send-community` omits `standard` (or is `none`) has the whole COMMUNITIES attribute suppressed on the readvertise rails (internal/component/bgp/reactor/peer_forward_facts.go, reactor_api_forward.go, forward_rs.go), and a `community-remove ffff0006` filter strips it through removeValues, which exempts no value (internal/component/bgp/plugins/filter_community/handler.go) (RFC9494-4.3-2); on a consecutive session drop ze applies the RFC 4724 purge of previously stale routes and re-arms a full LLST timer, so LLGR-marked routes are deleted early and the running timer is reset (RFC9494-4.2-7, 4.2-9); LLST timers are stopped at re-establishment, so an LLST elapsing during synchronization is not recorded and a subsequent reset removes nothing immediately (RFC9494-4.2-10); and long-lived-stale-time is configured per peer and applied to every negotiated family rather than per AFI/SAFI (RFC9494-5-2). |

**Status:** Standards Track
**Updates:** RFC 6368
**Date:** November 2023

## Overview

Extends BGP Graceful Restart (RFC 4724) to allow stale routes to be retained for much longer periods (hours/days vs minutes). Introduces the LLGR Capability and two well-known communities.

## Key Concepts

| Term | Meaning |
|------|---------|
| LLGR | Long-Lived Graceful Restart |
| LLST | Long-Lived Stale Time (24-bit, seconds) |
| Helper | Router that detects session failure and retains routes |
| Depreference | Reduce route preference to "least preferred" |

## LLGR Capability (Code 71)

**Wire Format:**

```
+--------------------------------------------------+
| Address Family Identifier (16 bits)              |
+--------------------------------------------------+
| Subsequent Address Family Identifier (8 bits)    |
+--------------------------------------------------+
| Flags for Address Family (8 bits)                |
+--------------------------------------------------+
| Long-Lived Stale Time (24 bits)                  |
+--------------------------------------------------+
| ... (repeated for each AFI/SAFI)                 |
+--------------------------------------------------+
```

**Flags:**

| Bit | Name | Description |
|-----|------|-------------|
| 0 | F | Forwarding state preserved during restart |
| 1-7 | Reserved | MUST be zero |

**Requirements:**
- MUST also advertise GR Capability (RFC 4724)
- If GR not present, LLGR MUST be ignored
- LLST can be up to 16,777,215 seconds (~194 days)

## Well-Known Communities

| Community | Value | Purpose |
|-----------|-------|---------|
| LLGR_STALE | 0xFFFF0006 | Marks routes retained beyond GR timer |
| NO_LLGR | 0xFFFF0007 | Routes should NOT be retained if stale |

## Operation Timeline

```
Session fails
    │
    ▼
┌─────────────────────────┐
│   GR Restart Time       │  Routes retained, preference unchanged
│   (0-4095 seconds)      │
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│   LLGR Period           │  Routes get LLGR_STALE, become least-preferred
│   (0-16,777,215 sec)    │  Routes withdrawn from non-LLGR peers
└─────────────────────────┘
    │
    ▼
Routes deleted (if session not re-established)
```

## Key Behaviors

### When LLGR Period Begins

1. **Start LLST timer** for each AFI/SAFI with nonzero LLST
2. **Attach LLGR_STALE** community to stale routes (requires readvertisement)
3. **Delete NO_LLGR routes** immediately
4. **Depreference** routes (treat as least preferred)

### LLGR_STALE Route Processing

- Treat as **least preferred** in route selection
- **SHOULD NOT advertise** to peers without LLGR capability
- **MUST NOT remove** LLGR_STALE when readvertising
- Only used as last resort (no better route available)

### Session Re-establishment

- RFC 4724 procedures apply for stale routes
- LLST timer continues running until EoR received
- If F bit clear or AFI/SAFI missing in new LLGR cap → delete stale routes
- If both GR and LLGR caps missing → delete all stale routes

## Partial Deployment (Section 4.6)

When advertising to IBGP peers without LLGR:

1. MUST be internal (IBGP/Confederation) neighbors
2. MUST attach NO_EXPORT community
3. MUST set LOCAL_PREF to zero

## VPN Considerations (Section 4.7)

### EBGP PE-CE

- MAY advertise stale routes to CE without LLGR (explicit config required)
- SHOULD attach NO_EXPORT by default
- All other LLGR rules still apply

### IBGP PE-CE (RFC 6368 Update)

When using ATTR_SET:
- LLGR_STALE from imported VPN route MUST be included in CE advertisement
- Even if not in ATTR_SET attribute

## Deployment Guidance

**MUST NOT enable by default** - requires explicit per-AFI/SAFI configuration.

### Safe to Use

- MPLS/tunneled forwarding networks
- Non-forwarding data (VPLS auto-discovery, FlowSpec)
- Route reflectors (control-plane only)

### Use with Caution

- Hop-by-hop forwarded networks (risk of forwarding loops)
- Conventional reachability information

### Security: Label Reuse

For MPLS VPNs, ensure:
- **Label reuse lower bound** > **LLST upper bound**
- Prevents VPN traffic isolation breach

## Implementation Notes

### Timer Interaction

| GR Restart Time | LLST | Behavior |
|-----------------|------|----------|
| 0 | nonzero | Skip GR, go straight to LLGR |
| nonzero | 0 | GR only, no LLGR |
| 0 | 0 | Neither GR nor LLGR (base BGP only) |
| nonzero | nonzero | GR then LLGR (serial) |

### F Bit Coordination

- If LLGR F bit set → GR F bit should also be set
- Otherwise GR will clear state before LLGR kicks in

### Route Selection

```
Normal routes (any preference)
    ↓ more preferred than
LLGR_STALE routes (least preferred)
```

Between two LLGR_STALE routes: normal tiebreaking applies.

## ZeBGP Implementation Considerations

### Capability

- Code: 71
- Variable length (7 bytes per AFI/SAFI tuple)
- Requires GR capability (code 64) or ignore

### Communities

```go
const (
    CommunityLLGRStale = 0xFFFF0006
    CommunityNoLLGR    = 0xFFFF0007
)
```

### State Machine Impact

- Add LLST timer per AFI/SAFI
- Track LLGR state separately from GR state
- Handle F bit in both GR and LLGR capabilities

## Compliance Checklist

- [ ] [RFC9494-3.1-1] [MUST] If the LLGR Capability is advertised, the Graceful Restart capability MUST also be advertised (§3.1, §4.1)
- [ ] [RFC9494-4.1-1] [MUST] If GR capability is not advertised alongside LLGR, the LLGR Capability MUST be disregarded (§4.1)
- [ ] [RFC9494-3.1-2] [MUST] Reserved bits in the Flags field MUST be set to zero by the sender and MUST be ignored by the receiver (§3.1)
- [ ] [RFC9494-4-1] [MUST] If configured to support LLGR procedures, a BGP speaker MUST use BGP Capabilities Advertisement to advertise the LLGR Capability (§4)
- [ ] [RFC9494-4.2-1] [MUST] After session goes down, stale routes for an AFI/SAFI MUST be retained for the sum of Restart Time and Long-Lived Stale Time (§4.2)
- [ ] [RFC9494-4.2-2] [MUST] Once the LLGR period begins, for each AFI/SAFI with nonzero LLST, the helper router MUST start a timer for that Long-Lived Stale Time (§4.2)
- [ ] [RFC9494-4.2-3] [MUST] If the LLST timer expires before session re-establishment, the helper MUST delete all stale routes of that AFI/SAFI from the neighbor (§4.2)
- [ ] [RFC9494-4.2-4] [MUST] The helper router MUST attach the LLGR_STALE community to stale routes being retained (§4.2)
- [ ] [RFC9494-4.2-5] [MUST NOT] Routes marked with NO_LLGR community MUST NOT be retained and MUST be removed per normal RFC 4271 operation (§4.2)
- [ ] [RFC9494-4.2-6] [MUST] The helper router MUST perform the LLGR_STALE route processing procedures (§4.2, §4.3)
- [ ] [RFC9494-4.2-7] [MUST NOT] In case of consecutive restarts, previously marked stale routes MUST NOT be deleted before the LLST timer expires (§4.2) {gap: ze applies the RFC 4724 Section 4.2 consecutive-restart rule unconditionally, including during an LLGR period -- every activation of a session-down dispatches "request bgp rib purge-stale <peer>" as its first step (internal/component/bgp/plugins/gr/gr.go:362, internal/component/bgp/plugins/gr/gr.go:532), and onSessionDown clears the prior peer state through clearPeerLocked (internal/component/bgp/plugins/gr/gr_state.go:125), so routes marked stale in the previous cycle are deleted at the new session drop rather than kept until their LLST timer expires}
- [ ] [RFC9494-4.2-8] [MUST] Once LLGR period begins, helper MUST immediately remove all stale routes if F bit is not set, AFI/SAFI is not listed, or LLGR+GR capabilities are not received in re-established session (§4.2)
- [ ] [RFC9494-4.2-9] [MUST NOT] A running LLST timer MUST NOT be updated (other than by manual intervention) until the peer has established and synchronized a new session (§4.2) {gap: a consecutive session drop while the peer is already in LLGR replaces the running timer -- onSessionDown calls clearPeerLocked, which stops every LLST timer via stopLLSTTimersLocked (internal/component/bgp/plugins/gr/gr_state.go:125, :457-465, :476-481), and enterLLGRLocked then arms a fresh time.AfterFunc for the family's full LLST (internal/component/bgp/plugins/gr/gr_state.go:384-387), so the remaining Long-Lived Stale Time is reset before the peer has established and synchronized a new session}
- [ ] [RFC9494-4.3-1] [MUST] A BGP speaker that advertised LLGR Capability MUST treat LLGR_STALE routes as least preferred in route selection (§4.3, §4.4)
- [ ] [RFC9494-4.3-2] [MUST NOT] The LLGR_STALE community MUST NOT be removed when the route is further advertised (§4.3) {gap: two egress paths strip the whole COMMUNITIES attribute with no LLGR_STALE (0xFFFF0006) exemption. applyFactsSendCommunity emits a whole-attribute suppress for code 8 whenever the peer's `send-community` is `none` or omits `standard` (internal/component/bgp/reactor/peer_forward_facts.go:249, mask set at :195-223), and it runs on both readvertise rails (internal/component/bgp/reactor/reactor_api_forward.go:516, internal/component/bgp/reactor/forward_rs.go:343). A `community-remove ffff0006` filter reaches the same attribute through AttrModRemove (internal/component/bgp/reactor/filter_delta.go:270), and removeValues drops every matching 4-octet value without checking which community it is (internal/component/bgp/plugins/filter_community/handler.go:120-131). The RIB-side attach path only appends (internal/component/bgp/plugins/rib/rib_commands_community.go:222-236), but that is not the path on which the community is lost}
- [ ] [RFC9494-4.4-1] [MUST] A least preferred route MUST be treated as less preferred than any route that is not also least preferred (§4.4)
- [ ] [RFC9494-4.5-1] [MUST] If LLGR Capability is received without accompanying GR Capability, the LLGR Capability MUST be ignored (§4.5)
- [ ] [RFC9494-4.6-1] [MUST] For partial deployment, neighbors receiving stale routes MUST be internal (IBGP or Confederation) neighbors (§4.6)
- [ ] [RFC9494-4.6-2] [MUST] For partial deployment, the NO_EXPORT community MUST be attached to the stale routes (§4.6)
- [ ] [RFC9494-4.6-3] [MUST] For partial deployment, stale routes MUST have their LOCAL_PREF set to zero (§4.6)
- [ ] [RFC9494-4.7.2-1] [MUST] When using ATTR_SET (IBGP PE-CE), PE MUST include LLGR_STALE community in CE advertisement if present in imported VPN route, even if not in ATTR_SET (§4.7.2) {not-applicable: ze has no ATTR_SET attribute and no RFC 6368 iBGP PE-CE model -- the path attribute code table stops at code 40 plus the provisional 252 and contains no code 128 (internal/core/bgp/attribute/attribute.go:46-66), `grep -rni "attr_set\|attrset" internal/core/` returns nothing, and `grep -rln "vrf" --include=*.go internal/component/bgp/` returns no file, so there is no PE that imports a VPN route into a CE session}
- [ ] [RFC9494-4.7.2-2] [MUST] When exporting CE route to VPN address family, PE MUST include LLGR_STALE community in VPN route if present in path attributes received from CE (§4.7.2) {not-applicable: ze has no PE export of a CE route into a VPN address family -- `grep -rln "vrf" --include=*.go internal/component/bgp/` returns no file, and the only route-target handling is parsing of the extended community and the RTC NLRI codec (internal/component/bgp/route/route_community.go:275, internal/component/bgp/plugins/nlri/rtc/), with no VRF import/export engine that could carry a CE route's LLGR_STALE into a VPN route}
- [ ] [RFC9494-5-1] [MUST NOT] Implementations MUST NOT enable LLGR procedures by default (§5)
- [ ] [RFC9494-5-2] [MUST] Implementations MUST require affirmative configuration per AFI/SAFI to enable LLGR (§5) {gap: ze requires affirmative configuration, but its granularity is the peer, not the AFI/SAFI -- parseLLGRCapValue reads a single long-lived-stale-time from the peer's (or group's) graceful-restart container and stamps that same LLST onto every negotiated family (internal/component/bgp/plugins/gr/gr_llgr.go:130-171), and the YANG leaf sits in the per-peer capability container with the description "Applied to all negotiated address families for the peer" (internal/component/bgp/plugins/gr/yang/ze-graceful-restart.yang), so an operator cannot enable LLGR for one address family and leave another off}
- [ ] [RFC9494-4.2-10] [MUST] If LLST timer expires during synchronization and session subsequently resets, remaining routes for that AFI/SAFI MUST be removed immediately (§4.2) {gap: ze holds no record of an LLST that elapsed during synchronization, because no LLST timer runs then -- onSessionReestablished stops every LLST timer and clears state.inLLGR the moment the session comes back (internal/component/bgp/plugins/gr/gr_state.go:194, :231, :476-481), so the expiry this requirement keys on cannot be observed and the next session reset starts an ordinary GR cycle with a full restart timer (internal/component/bgp/plugins/gr/gr_state.go:153-156) instead of removing the family's remaining routes immediately}
- [ ] [RFC9494-4.2-11] [SHOULD] The LLST timers received SHOULD be modifiable by local configuration (§4.2)
- [ ] [RFC9494-4.3-3] [SHOULD NOT] LLGR_STALE routes SHOULD NOT be advertised to any neighbor from which the LLGR Capability has not been received (§4.3)
- [ ] [RFC9494-4.7.1-1] [SHOULD] When advertising stale routes over PE-CE EBGP session, implementation SHOULD by default attach NO_EXPORT community (§4.7.1)
- [ ] [RFC9494-3.2-1] [MAY] An implementation MAY allow users to configure policies for LLGR_STALE community (§3.2)
- [ ] [RFC9494-3.3-1] [MAY] An implementation MAY allow users to configure policies for NO_LLGR community (§3.3)
- [ ] [RFC9494-4.2-12] [MAY] The value of LLST received from a neighbor MAY be reduced by local configuration (§4.2)
- [ ] [RFC9494-4.6-4] [MAY] For partial deployment, stale routes MAY be advertised to IBGP neighbors without LLGR Capability (§4.6)
- [ ] [RFC9494-4.7.1-2] [MAY] An implementation MAY advertise stale routes over a PE-CE session when explicitly configured (§4.7.1)
- [ ] [RFC9494-4.7.1-3] [MAY] The second rule of Section 4.3 MAY be disregarded for PE-CE VPN sessions (§4.7.1)
- [ ] [RFC9494-4.7.1-4] [MAY] Attachment of NO_EXPORT community MAY be disabled by explicit configuration for PE-CE (§4.7.1)
- [ ] [RFC9494-4.7.2-3] [MAY] For IBGP PE-CE when CE does not support LLGR, optional procedures of Section 4.6 MAY be followed, overriding LOCAL_PREF from ATTR_SET (§4.7.2)
