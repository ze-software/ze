# RFC 7858 — DNS over TLS (DoT)

## Meta

| Field | Value |
|-------|-------|
| RFC | 7858 |
| Title | Specification for DNS over Transport Layer Security (TLS) |
| Enrolment | enrolled |
| Enrolment reason | DNS over TLS / DoT (RFC 7858): server role. 4 MET (TLS-first handshake, DoT port refuses cleartext (2 facets), TLS 1.2/BCP 195 floor) + 3 single-polarity positive (listen/accept on 853, RFC 7766 two-octet length framing, robust to idle-connection close) + 1 gap (DoT listen-port 53 not rejected) + 11 not-applicable (9 DoT client-role MUSTs + 2 TCP-Fast-Open MUSTs) |
| Support | dns-telemetry 40 |
| Support area | DNS over TLS (DoT) |
| Support status | Partial |
| Support coverage | DoT server on the shared DNS harness: a TLS-wrapped TCP listener (miekg/dns RFC 7766 two-octet length-prefixed framing) on the DoT port, default 853, sharing the same dns.Handler as cleartext and DoH; TLS 1.2 minimum (BCP 195); cleartext queries to the DoT port are refused. Server role only -- ze is not a DoT client. |
| Support remaining | One MUST NOT gap (RFC7858-3.1-3): ze does not reject a DoT listen-port of 53 (internal/core/dnsserver/secure.go validates only 1..65535). The DoT client-role MUSTs (connect, response-matching, key-pinning, bootstrap alerting) and the TCP-Fast-Open MUSTs are not-applicable -- ze plays no DoT client and its DoT listener uses no TCP Fast Open. |

**Status:** Proposed Standard
**Date:** May 2016
**Authors:** Z. Hu, L. Zhu, J. Heidemann, A. Mankin, D. Wessels, P. Hoffman

## Purpose

Specifies how to run DNS message exchange over a TLS-encrypted TCP
connection, so on-path observers cannot read or tamper with queries and
answers. Only the transport is new: the DNS messages inside are exactly the
DNS-over-TCP framing of RFC 1035 / RFC 7766.

## Transport (§3.1, §3.2)

| Property | Value |
|----------|-------|
| Well-known port | **853** (TCP), IANA-assigned "domain-s" |
| Transport | TLS 1.2+ over TCP |
| Message framing | RFC 1035 §4.2.2 / RFC 7766: a 2-octet big-endian length prefix, then the DNS message |
| Session reuse | Connections are long-lived and reused (RFC 7766); pipelining and out-of-order responses are permitted |

A DoT client connects to TCP port 853, completes the TLS handshake, then
sends length-prefixed DNS messages. A server offering DoT MUST NOT use port
53 for TLS; port 53 stays cleartext DNS.

## Compliance Checklist

Every RFC 2119 keyword in RFC 7858's body maps to exactly one line below. The
keywords in §2 ("Key Words") are the boilerplate definitions and carry no
obligation. IDs anchor to the first section a line cites; ordinals are
per-section in document order.

- [ ] [RFC7858-3.1-1] [MUST] By default, a DNS server that supports DNS over TLS MUST listen for and accept TCP connections on port 853, unless it has mutual agreement with its clients to use another port (§3.1) {single-polarity: positive; affirmative listen mandate. Positive proof: a DoT listener accepts a TCP+TLS connection and answers (internal/core/dnsserver/secure_test.go TestDoTListener) with the default DoT port pinned to 853 (internal/core/dnsserver/secure.go:39, TestDefaultSecureConfig). A "must listen and accept" obligation has no rejecting counter-behavior to assert as a negative.}
- [ ] [RFC7858-3.1-2] [MUST] By default, a DNS client desiring privacy MUST establish a TCP connection to port 853 on the server, unless it has mutual agreement to use another port (§3.1) {not-applicable: ze is a DoT server only, not a DoT client. The querying resolver initiates the connection; ze's only DoT code path is the server listener bindDoT (internal/core/dnsserver/secure.go:307), and dnsserver/client.go is EDNS0 client-subnet resolution (client.go:21), not a DoT client.}
- [ ] [RFC7858-3.1-3] [MUST NOT] A mutually agreed alternative port MUST NOT be port 53 (§3.1) {gap: ze does not enforce this. ParseSecureLeaves validates the DoT listen-port only as 1..65535 (internal/core/dnsserver/secure.go:166-172) and does not reject 53, so an operator can configure the DoT listener on port 53.}
- [ ] [RFC7858-3.1-5] [MUST] The first data exchange on the TCP connection MUST be the client and server initiating a TLS handshake per RFC 5246 (§3.1)
- [ ] [RFC7858-3.1-6] [MUST NOT] DNS clients and servers MUST NOT use port 853 to transport cleartext DNS messages (§3.1)
- [ ] [RFC7858-3.1-7] [MUST NOT] DNS clients MUST NOT send cleartext DNS messages on any port used for DNS over TLS, including after a failed TLS handshake (§3.1) {not-applicable: constrains a DoT client's send behavior; ze is a DoT server only (internal/core/dnsserver/secure.go:307 bindDoT) and issues no DoT queries. The server-side counterpart (do not respond to cleartext on the DoT port) is RFC7858-3.1-8.}
- [ ] [RFC7858-3.1-8] [MUST NOT] DNS servers MUST NOT respond to cleartext DNS messages on any port used for DNS over TLS, including after a failed TLS handshake (§3.1)
- [ ] [RFC7858-3.3-1] [MUST] All messages (requests and responses) in the established TLS session MUST use the two-octet length field described in Section 4.2.2 of RFC 1035 (§3.3) {single-polarity: positive; the RFC 1035 4.2.2 two-octet length-prefixed framing is provided by the miekg/dns Server ze hands the TLS listener to (internal/core/dnsserver/secure.go:314-315). A successful DoT round trip (internal/core/dnsserver/secure_test.go TestDoTListener) proves conformant framing; ze has no code path that emits non-length-prefixed framing to exercise as a negative.}
- [ ] [RFC7858-3.3-4] [MUST] Clients MUST match pipelined responses to outstanding queries on the same TLS connection using the Message ID (§3.3) {not-applicable: a DoT client obligation to match pipelined responses to outstanding queries; ze is a DoT server only (internal/core/dnsserver/secure.go:307) and issues no DoT queries. dnsserver/client.go is EDNS0 client-subnet resolution (client.go:21), not a DoT client.}
- [ ] [RFC7858-3.3-5] [MUST] If a response contains a Question Section, the client MUST match the QNAME, QCLASS, and QTYPE fields (§3.3) {not-applicable: a DoT client response-matching obligation; ze is a DoT server only (internal/core/dnsserver/secure.go:307) and issues no DoT queries to match responses against.}
- [ ] [RFC7858-3.4-5] [MUST] Clients and servers that keep idle connections open MUST be robust to termination of an idle connection by either party (§3.4) {single-polarity: positive; server-side liveness property. Positive proof: after a DoT client abruptly closes an idle connection the server keeps serving and answers a fresh connection (internal/core/dnsserver/secure_test.go TestDoTRobustToIdleConnectionClose). "Remains robust" has no failure polarity to assert as a negative.}
- [ ] [RFC7858-3.4-7] [MUST] Clients MUST handle abrupt closes and be prepared to reestablish connections and/or retry queries (§3.4) {not-applicable: a DoT client reconnect/retry obligation; ze is a DoT server only (internal/core/dnsserver/secure.go:307) and opens no client connections to reestablish. The server-side robustness counterpart is RFC7858-3.4-5.}
- [ ] [RFC7858-3.4-8] [MUST] When using TCP Fast Open, the client and server MUST immediately initiate or resume a TLS handshake (§3.4) {not-applicable: ze's DoT listener does not use TCP Fast Open. bindDoT opens an ordinary TCP socket via lc.Listen (internal/core/dnsserver/secure.go:308-309) with no TCP_FASTOPEN, so the TFO-specific handshake obligation has no bearing.}
- [ ] [RFC7858-3.4-9] [MUST NOT] When using TCP Fast Open, cleartext DNS MUST NOT be exchanged (§3.4) {not-applicable: ze's DoT listener does not use TCP Fast Open (internal/core/dnsserver/secure.go:308-309 opens a plain TCP socket, no TCP_FASTOPEN), so the TFO-specific cleartext prohibition has no bearing.}
- [ ] [RFC7858-4.2-4] [MUST] The user MUST be alerted whenever possible that DNS is not private during such bootstrap network configuration (§4.2) {not-applicable: a DoT client bootstrap-configuration obligation (opportunistic-privacy user alerting); ze is a DoT server only (internal/core/dnsserver/secure.go:307) and performs no client bootstrap.}
- [ ] [RFC7858-4.2-5] [MUST] If no computed fingerprint matches a configured pin, the client MUST treat the SPKI validation failure as a non-recoverable error (§4.2) {not-applicable: a DoT client out-of-band key-pinning obligation; ze is a DoT server only (internal/core/dnsserver/secure.go:307) and pins no server SPKI. Client authentication policy is the querying resolver's concern.}
- [ ] [RFC7858-4.2-6] [MUST] Implementations of the key-pinned profile MUST support computing a fingerprint as the SHA-256 hash of the DER-encoded ASN.1 SubjectPublicKeyInfo of an X.509 certificate (§4.2) {not-applicable: a DoT client key-pinned-profile obligation; ze is a DoT server only (internal/core/dnsserver/secure.go:307) and implements no client-side SPKI pinning profile.}
- [ ] [RFC7858-4.2-7] [MUST] Implementations MUST support representing a SHA-256 fingerprint as a base64-encoded character string (§4.2) {not-applicable: a DoT client key-pinning representation obligation; ze is a DoT server only (internal/core/dnsserver/secure.go:307) and implements no client-side pin storage.}
- [ ] [RFC7858-8-1] [MUST] Clients and servers MUST adhere to the TLS implementation recommendations and security considerations of BCP 195 (§8)
- [ ] [RFC7858-3.1-9] [SHOULD] DNS clients SHOULD remember server IP addresses that do not support DNS over TLS (timeouts, connection refusals, TLS handshake failures) and not request DNS over TLS from them for a reasonable period (§3.1)
- [ ] [RFC7858-3.3-2] [SHOULD] DNS clients and servers SHOULD pass the two-octet length field and the message it describes to the TCP layer at the same time, e.g. in a single write system call (§3.3)
- [ ] [RFC7858-3.3-3] [SHOULD] Clients SHOULD pipeline multiple queries over a TLS session (§3.3)
- [ ] [RFC7858-3.4-1] [SHOULD] Clients SHOULD reuse a single TCP connection to the recursive resolver (§3.4)
- [ ] [RFC7858-3.4-2] [SHOULD NOT] Clients and servers SHOULD NOT immediately close a connection after each response (§3.4)
- [ ] [RFC7858-3.4-3] [SHOULD] Clients and servers SHOULD reuse existing connections for subsequent queries as long as they have sufficient resources (§3.4)
- [ ] [RFC7858-3.4-4] [SHOULD] An implementor of DNS over TLS SHOULD follow best practices for DNS over TCP per RFC 7766 (§3.4)
- [ ] [RFC7858-3.4-10] [SHOULD] DNS servers SHOULD enable fast TLS session resumption (§3.4)
- [ ] [RFC7858-3.4-11] [SHOULD] Fast TLS session resumption SHOULD be used when reestablishing connections (§3.4)
- [ ] [RFC7858-3.4-12] [SHOULD] When closing a connection, DNS servers SHOULD use the TLS close-notify to shift the TCP TIME-WAIT state to the client (§3.4)
- [ ] [RFC7858-4.2-1] [SHOULD] Client administrators SHOULD deploy a backup pin along with the primary pin (§4.2)
- [ ] [RFC7858-4.2-2] [SHOULD] After a change of keys on the server, an updated pin set SHOULD be distributed to all clients in some secure way (§4.2)
- [ ] [RFC7858-5-1] [SHOULD] To minimize server state and connection startup time, clients SHOULD minimize the creation of new TCP connections (§5)
- [ ] [RFC7858-3.1-4] [MAY] A mutually agreed alternative port MAY be from the "first-come, first-served" port range (§3.1)
- [ ] [RFC7858-3.1-10] [MAY] DNS clients following an out-of-band key-pinned privacy profile MAY be more aggressive about retrying DNS-over-TLS connection failures (§3.1)
- [ ] [RFC7858-3.4-6] [MAY] As with current DNS over TCP, DNS servers MAY close the connection at any time (§3.4)
- [ ] [RFC7858-4.2-3] [MAY] Techniques such as those used by DNSSEC-trigger MAY be used during network configuration, transitioning to the designated DNS provider after authentication (§4.2)
- [ ] [RFC7858-4.2-8] [MAY] Additional fingerprint types MAY also be supported (§4.2)
- [ ] [RFC7858-8-2] [MAY] Clients MAY discard cached information about server capabilities advertised in cleartext (§8)

## Authentication profiles (§4, refined by RFC 8310)

RFC 7858 defines two usage profiles; the authentication policy is the
client's choice, not the server's:

| Profile | Meaning |
|---------|---------|
| Opportunistic privacy | Encrypt when possible; do not fail if the server cannot be authenticated. Protects against passive observation only. |
| Out-of-band key-pinned | The client pins the server's SPKI (SubjectPublicKeyInfo) hash and refuses a mismatch. |

RFC 8310 later adds strict authentication by an Authentication Domain Name
(the server certificate must validate for a configured hostname).

## How ze applies this

- The authoritative DNS harness (`internal/core/dnsserver`) binds an optional
  DoT listener: a TCP listener wrapped with `crypto/tls` (`tls.NewListener`)
  handed to the miekg/dns `Server{Listener}`, which already speaks the RFC 7766
  length-prefixed framing. The same `dns.Handler` serves cleartext DNS, DoT,
  and DoH, so answer policy is identical across transports.
- The DoT port defaults to **853** and is operator-configurable per listener
  (`tls { listen-port }`). Cleartext UDP/TCP on port 53 (as112) or the geodns
  listener ports is unaffected when DoT is enabled.
- ze is the DoT **server**. Certificate material is operator-provided PEM
  (`tls { cert-file / key-file }`) or, absent that, a self-signed certificate
  from `internal/core/selfcert` (the same helper the web UI uses). A
  `ze doctor` check reports certificate validity and near-expiry.
- Client authentication policy (opportunistic vs pinned vs strict) is the
  querying resolver's concern; ze presents a normal server certificate and
  does not require client certificates.

## Notes

- DoT is TCP-only. There is no UDP variant; the harness binds only a TCP+TLS
  listener for DoT (no UDP counterpart), unlike cleartext DNS which binds both.
- Because self-signed material cannot be validated by a strict client, the
  operator either supplies a CA-issued cert or the client uses opportunistic /
  pinned mode.
