# RFC 8538 - Notification Message Support for BGP Graceful Restart

## Meta

| Field | Value |
|-------|-------|
| RFC | 8538 |
| Title | Notification Message Support for BGP Graceful Restart |
| Enrolment | blocked |
| Enrolment reason | Notification message support for BGP graceful restart. Written 2026-09-01 so the public row is declared by a summary. It is not enrolled because there is no source text at rfc/full/rfc8538.txt: fetch https://www.rfc-editor.org/rfc/rfc8538.txt, then extract. The row claims Unsupported, so no obligation here is gated. |
| Support | bgp-base 250 |
| Support area | Notification GR |
| Support status | Unsupported |
| Support coverage | Cease Hard Reset subcode (9) name is known. |
| Support remaining | The GR N-bit is decoded but not acted upon; routes are dropped on any NOTIFICATION, so RFC 8538 route retention is not implemented. |

## Overview

Specifies how a BGP speaker retains routes across a NOTIFICATION message or a hold-timer expiry, rather than only across the session resets RFC 4724 covers. It adds the N bit to the Graceful Restart capability and the Hard Reset Cease subcode that says a peer must NOT retain routes.

## Compliance Checklist

No requirement is extracted yet. The `Enrolment reason` above says why, and
names the fetch that unblocks it. An empty checklist gates nothing: it does not
claim conformance and it is not evidence of any.
