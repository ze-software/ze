# RFC 9830: BGP Extensions for SR Policy

## Meta

| Field | Value |
|-------|-------|
| RFC | 9830 |
| Title | BGP Extensions for the Advertisement of Segment Routing (SR) Policies |
| Status | Standards Track |
| Date | 2025 |
| Authors | S. Previdi, C. Filsfils, K. Talaulikar, P. Mattes, D. Jain |

**Purpose:** Defines the BGP encoding for advertising SR Policies: SAFI 73 assignment, SR Policy NLRI wire format, and Tunnel Encapsulation attribute sub-TLVs for SR Policy (Tunnel Type 15).

**Scope:** BGP advertisement and reception of SR Policies over AFI 1 (IPv4) and AFI 2 (IPv6) with SAFI 73. Architecture is in RFC 9256; segment types C-K are in RFC 9831.

## Wire Formats

### SR Policy NLRI (Section 2.1)

Carried in MP_REACH_NLRI (code 14) and MP_UNREACH_NLRI (code 15) with SAFI 73.

```
+---------------------------------------------------------+
| NLRI Length (1 octet, in bits)                          |
+---------------------------------------------------------+
| Distinguisher (4 octets)                                |
+---------------------------------------------------------+
| Color (4 octets)                                        |
+---------------------------------------------------------+
| Endpoint (4 octets for AFI 1, 16 octets for AFI 2)     |
+---------------------------------------------------------+
```

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Length | 0 | 1 | uint8 | Bit count: 96 for AFI 1, 192 for AFI 2 |
| Distinguisher | 1 | 4 | uint32 | Arbitrary; for uniqueness when multiple CPs share color+endpoint |
| Color | 5 | 4 | uint32 | Matches BGP Color Extended Community |
| Endpoint | 9 | 4 or 16 | IP addr | IPv4 or IPv6; 0.0.0.0 / :: means "any endpoint" |

**Fixed sizes per AFI:**
- AFI 1 (IPv4): Length=96, body=12 bytes (4+4+4)
- AFI 2 (IPv6): Length=192, body=24 bytes (4+4+16)

**Length field is in BITS, not bytes.** This differs from CIDR NLRI where the length is also in bits but represents prefix length. Here the length covers the entire NLRI body.

### Next-Hop Encoding (Section 2.1)

The next-hop in MP_REACH_NLRI for SAFI 73 follows standard RFC 4760 encoding:

| NH Length | Meaning |
|-----------|---------|
| 4 | IPv4 next-hop |
| 16 | IPv6 global next-hop |
| 32 | IPv6 global + link-local next-hop |

The next-hop AFI is independent of the SR Policy AFI. An IPv4 SR Policy (AFI 1) can have an IPv6 next-hop and vice versa.

### Tunnel Encapsulation Attribute (Section 2.2)

SR Policy candidate path content is encoded in the Tunnel Encapsulation Attribute (code 23, RFC 9012) using Tunnel Type 15 (SR Policy).

**Restriction:** Tunnel Type 15 is ONLY valid with SAFI 73. Any other tunnel type in a SAFI 73 UPDATE triggers treat-as-withdraw (RFC 7606).

Outer TLV structure (per RFC 9012):

```
+---------------------------------------------------------+
| Tunnel Type = 15 (2 octets)                             |
+---------------------------------------------------------+
| Length (2 octets)                                       |
+---------------------------------------------------------+
| SR Policy Sub-TLVs (variable)                           |
+---------------------------------------------------------+
```

### SR Policy Sub-TLVs (Section 2.4)

Sub-TLV length encoding per RFC 9012: types 0-127 use 1-byte length, types 128-255 use 2-byte length.

| Type | Name | Length field | Section |
|------|------|-------------|---------|
| 12 | Preference | 1 byte | 2.4.1 |
| 13 | Binding SID | 1 byte | 2.4.2 |
| 14 | Explicit NULL Label Policy (ENLP) | 1 byte | 2.4.5 |
| 15 | Priority | 1 byte | 2.4.6 |
| 20 | SRv6 Binding SID | 1 byte | 2.4.3 |
| 128 | Segment List | 2 bytes | 2.4.4 |
| 129 | SR Policy Candidate Path Name | 2 bytes | 2.4.7 |
| 130 | SR Policy Name | 2 bytes | 2.4.8 |

#### Preference Sub-TLV (Type 12)

```
+---------------------------------------------------------+
| Flags (1 octet, reserved)                               |
+---------------------------------------------------------+
| RESERVED (1 octet)                                      |
+---------------------------------------------------------+
| Preference (4 octets, uint32)                           |
+---------------------------------------------------------+
```

Value length: 6 bytes. Higher preference wins candidate path selection.

#### Binding SID Sub-TLV (Type 13, MPLS)

```
+---------------------------------------------------------+
| Flags (1 octet)                                         |
+---------------------------------------------------------+
| Reserved (1 octet)                                      |
+---------------------------------------------------------+
| MPLS Label (4 octets, label in top 20 bits)             |
+---------------------------------------------------------+
```

Value length: 2 bytes (no BSID) or 6 bytes (with BSID).

Flags: S bit (bit 0, 0x80) = Specified-BSID-Only; I bit (bit 1, 0x40) = Drop-Upon-Invalid.
Every other bit is unassigned and MUST be zero on transmission.

#### SRv6 Binding SID Sub-TLV (Type 20)

```
+---------------------------------------------------------+
| Flags (1 octet)                                         |
+---------------------------------------------------------+
| Reserved (1 octet)                                      |
+---------------------------------------------------------+
| SRv6 SID (16 octets)                                    |
+---------------------------------------------------------+
```

Value length: 18 bytes, or 26 bytes when the SRv6 Endpoint Behavior and SID
Structure is appended.

Flags: S bit (bit 0, 0x80), I bit (bit 1, 0x40), B bit (bit 2, 0x20: SRv6
Endpoint Behavior and SID Structure present).

#### Priority Sub-TLV (Type 15)

```
+---------------------------------------------------------+
| Priority (1 octet, uint8)                               |
+---------------------------------------------------------+
| Reserved (1 octet)                                      |
+---------------------------------------------------------+
```

Value length: 2 bytes.

#### Segment List Sub-TLV (Type 128)

Contains sub-sub-TLVs: Weight (type 9) and one or more Segment entries. Multiple Segment List sub-TLVs per tunnel TLV enable ECMP.

##### Weight Sub-Sub-TLV (Type 9)

```
+---------------------------------------------------------+
| Flags (1 octet)                                         |
+---------------------------------------------------------+
| RESERVED (1 octet)                                      |
+---------------------------------------------------------+
| Weight (4 octets, uint32)                               |
+---------------------------------------------------------+
```

Value length: 6 bytes.

##### Segment Type A (Type 1, MPLS Label)

```
+---------------------------------------------------------+
| Flags (1 octet)                                         |
+---------------------------------------------------------+
| Reserved (1 octet)                                      |
+---------------------------------------------------------+
| MPLS Label Stack Entry (4 octets)                       |
+---------------------------------------------------------+
```

Value length: 6 bytes. Label in top 20 bits of the 4-byte entry; TC, S and TTL
occupy the remaining 12 bits and the S bit MUST be zero on transmission.

Segment Flags (Section 2.4.4.2.3): V bit (bit 0, 0x80) = SID verification;
B bit (bit 3, 0x10) = SRv6 Endpoint Behavior and SID Structure present, which
applies to Segment Type B and MUST be ignored on Segment Type A.

##### Segment Type B (Type 13, SRv6 SID)

```
+---------------------------------------------------------+
| Flags (1 octet)                                         |
+---------------------------------------------------------+
| Reserved (1 octet)                                      |
+---------------------------------------------------------+
| SRv6 SID (16 octets)                                    |
+---------------------------------------------------------+
| [SRv6 Endpoint Behavior sub-sub-TLV, optional]          |
+---------------------------------------------------------+
```

Value length: 18 bytes without the endpoint behavior, 26 bytes with it.

##### SRv6 Endpoint Behavior and SID Structure (Section 2.4.4.2.4)

```
+---------------------------------------------------------+
| Endpoint Behavior (2 octets)                            |
+---------------------------------------------------------+
| Reserved (2 octets)                                     |
+---------------------------------------------------------+
| LB Length | LN Length | Fun. Length | Arg. Length       |
+---------------------------------------------------------+
```

8 bytes, appended inside the Segment Type sub-TLV (B, I, J, K) and inside the
SRv6 Binding SID sub-TLV; it is not a sub-TLV of its own. The four lengths MUST
total 128 or less.

#### SR Policy Candidate Path Name Sub-TLV (Type 129)

```
+---------------------------------------------------------+
| RESERVED (1 octet)                                      |
+---------------------------------------------------------+
| Candidate Path Name (variable, no NULL terminator)      |
+---------------------------------------------------------+
```

#### SR Policy Name Sub-TLV (Type 130)

```
+---------------------------------------------------------+
| RESERVED (1 octet)                                      |
+---------------------------------------------------------+
| SR Policy Name (variable, no NULL terminator)           |
+---------------------------------------------------------+
```

## Error Handling (Section 5)

All malformed SR Policy updates use treat-as-withdraw (RFC 7606):
- Wrong tunnel type with SAFI 73
- Malformed sub-TLV lengths
- Invalid NLRI length field

Unknown sub-TLVs within the SR Policy tunnel are silently ignored but preserved for propagation.

## IANA Registrations (Section 6)

| Registry | Value | Name |
|----------|-------|------|
| SAFI Values | 73 | SR Policy SAFI |
| BGP Tunnel Encapsulation Attribute Tunnel Types | 15 | SR Policy Type |
| SR Policy Sub-TLV Types | 12 | Preference |
| SR Policy Sub-TLV Types | 13 | Binding SID |
| SR Policy Sub-TLV Types | 14 | Explicit NULL Label Policy (ENLP) |
| SR Policy Sub-TLV Types | 15 | Priority |
| SR Policy Sub-TLV Types | 20 | SRv6 Binding SID |
| SR Policy Sub-TLV Types | 128 | Segment List |
| SR Policy Sub-TLV Types | 129 | SR Policy Candidate Path Name |
| SR Policy Sub-TLV Types | 130 | SR Policy Name |
| Segment Sub-TLV Types | 1 | Segment Type A (MPLS) |
| Segment Sub-TLV Types | 13 | Segment Type B (SRv6) |
| Segment Sub-TLV Types | 9 | Weight |

## Related RFCs

| RFC | Relationship |
|-----|-------------|
| 9256 | SR Policy Architecture (concepts, selection, steering) |
| 9831 | Segment Types C-K (extensions to segment sub-TLV types) |
| 9012 | Tunnel Encapsulation Attribute (code 23) base spec |
| 4760 | Multiprotocol BGP (MP_REACH/MP_UNREACH) |
| 7606 | Revised Error Handling (treat-as-withdraw) |

## Compliance Checklist

### MUST / MUST NOT

- [ ] [RFC9830-2.1-1] [MUST] The AFI used MUST be IPv4(1) or IPv6(2) (§2.1)
- [ ] [RFC9830-2.1-2] [MUST] The NLRI Length value MUST be 96 when AFI = 1 and 192 when AFI = 2 (§2.1)
- [ ] [RFC9830-2.1-3] [MUST] A BGP UPDATE carrying MP_REACH_NLRI or MP_UNREACH_NLRI with the SR Policy SAFI MUST also carry the BGP mandatory attributes (§2.1)
- [ ] [RFC9830-2.2-1] [MUST] Use of any Tunnel Type other than SR Policy with the SR Policy SAFI MUST be considered malformed and handled by treat-as-withdraw (§2.2) {gap: nothing rejects a non-SR-Policy tunnel type under SAFI 73. The RFC 7606 validator table has no entry for attribute 23 (internal/component/bgp/message/rfc7606.go:415-429), so no attribute-level check ever runs on a received Tunnel Encapsulation attribute, and the attribute itself is kept as raw TLV bytes parsed only on demand (internal/core/bgp/attribute/wire.go:346 and :418, internal/core/bgp/attribute/tunnel_encap.go:39). ze parses and re-advertises the attribute; it applies no treat-as-withdraw}
- [ ] [RFC9830-2.2-2] [MUST NOT] A Tunnel Encapsulation Attribute MUST NOT contain more than one TLV of type "SR Policy" (§2.2) {single-polarity: positive; buildTunnelEncap assembles every configured sub-TLV into a single Tunnel Type 15 TLV and has no path that appends a second one (internal/component/bgp/plugins/nlri/srpolicy/config.go:365-370), so no input to ze's encoder produces the forbidden encoding for a negative to assert. The receive-side obligation to treat two such TLVs as malformed is the separate RFC9830-2.2-3, which is annotated as a gap}
- [ ] [RFC9830-2.2-3] [MUST] Updates carrying more than one SR Policy TLV MUST be considered malformed and handled by treat-as-withdraw (§2.2) {gap: a Tunnel Encapsulation attribute holding two SR Policy TLVs is accepted. ParseTunnelEncap walks every TLV and appends each one without counting types (internal/core/bgp/attribute/tunnel_encap.go:41-54), and there is no RFC 7606 validator for attribute 23 (internal/component/bgp/message/rfc7606.go:415-429), so no treat-as-withdraw is applied. ze's own encoder emits exactly one such TLV, which is the separate RFC9830-2.2-2}
- [ ] [RFC9830-2.3-1] [MUST] If the Tunnel Egress Endpoint and Color sub-TLVs are present, a BGP speaker MUST ignore them (§2.3)
- [ ] [RFC9830-2.3-3] [MUST] Any other sub-TLVs without explicitly defined applicability to the SR Policy SAFI MUST be ignored by the BGP speaker (§2.3)
- [ ] [RFC9830-2.4-1] [MUST] For single-instance TLVs/sub-TLVs, only the first instance is used and the other instances MUST be ignored (§2.4)
- [ ] [RFC9830-2.4-2] [MUST NOT] The other (duplicate) instances of a single-instance TLV/sub-TLV MUST NOT be considered malformed (§2.4)
- [ ] [RFC9830-2.4.1-2] [MUST NOT] The Preference sub-TLV MUST NOT appear more than once in the SR Policy encoding (§2.4.1)
- [ ] [RFC9830-2.4.1-3] [MUST] The Preference sub-TLV Length value MUST be 6 (§2.4.1)
- [ ] [RFC9830-2.4.1-4] [MUST] The Preference Flags field MUST be set to zero on transmission (§2.4.1)
- [ ] [RFC9830-2.4.1-5] [MUST] The Preference Flags field MUST be ignored on receipt (§2.4.1)
- [ ] [RFC9830-2.4.1-6] [MUST] The Preference RESERVED field MUST be set to zero on transmission (§2.4.1)
- [ ] [RFC9830-2.4.1-7] [MUST] The Preference RESERVED field MUST be ignored on receipt (§2.4.1)
- [ ] [RFC9830-2.4.2-2] [MUST NOT] The Binding SID sub-TLV MUST NOT appear more than once in the SR Policy encoding (§2.4.2)
- [ ] [RFC9830-2.4.2-4] [MUST] The Binding SID Length value MUST be 18 when an SRv6 BSID is present, 6 when an SR-MPLS BSID is present, or 2 when no BSID is present (§2.4.2)
- [ ] [RFC9830-2.4.2-5] [MUST] The unassigned bits in the Binding SID Flags field MUST be set to zero upon transmission (§2.4.2) {gap: buildBindingSIDSubTLV writes 0x10 into the Binding SID Flags octet (internal/component/bgp/plugins/nlri/srpolicy/config.go:385). Section 2.4.2 assigns only S (bit 0, 0x80) and I (bit 1, 0x40) in that field, so bit 3 is unassigned and is set on transmission. The value is pinned as ExaBGP-interoperable in TestSRPolicyInteropExaBGPSubTLVBytes (internal/component/bgp/plugins/nlri/srpolicy/encode_test.go:118-124), which emits the same octet, so the two implementations agree with each other and not with the RFC}
- [ ] [RFC9830-2.4.2-6] [MUST] The unassigned bits in the Binding SID Flags field MUST be ignored upon receipt (§2.4.2)
- [ ] [RFC9830-2.4.2-7] [MUST] The Binding SID RESERVED field MUST be set to zero on transmission (§2.4.2)
- [ ] [RFC9830-2.4.2-8] [MUST] The Binding SID RESERVED field MUST be ignored on receipt (§2.4.2)
- [ ] [RFC9830-2.4.2-9] [MUST] The Binding SID Label TC, S, and TTL bits MUST be set to zero (§2.4.2)
- [ ] [RFC9830-2.4.2-10] [MUST] The Binding SID Label TC, S, and TTL bits MUST be ignored (§2.4.2)
- [ ] [RFC9830-2.4.2-11] [MUST NOT] The Binding SID Label field MUST NOT contain the reserved MPLS label values (0-15) (§2.4.2) {gap: the config parser accepts any 32-bit binding-sid label and range-checks nothing (internal/component/bgp/plugins/nlri/srpolicy/config.go:137-142), so a reserved MPLS label value 0-15 is encoded verbatim by buildBindingSIDSubTLV (internal/component/bgp/plugins/nlri/srpolicy/config.go:387-390). The TC, S and TTL bits ARE forced to zero on the same path, which is the separate RFC9830-2.4.2-9}
- [ ] [RFC9830-2.4.3-3] [MUST] The SRv6 Binding SID Length value MUST be 26 when the SRv6 Endpoint Behavior and SID Structure is present, else MUST be 18 (§2.4.3) {single-polarity: positive; buildSRv6BindingSIDSubTLV always writes the 18-octet form and never appends the SRv6 Endpoint Behavior and SID Structure (internal/component/bgp/plugins/nlri/srpolicy/config.go:409-416), so the 26-octet case has no producer and there is no contrasting length to assert. The same 18-versus-26 rule for the Type B segment sub-TLV, where ze DOES produce both, is covered with both polarities under RFC9830-2.4.4.2.2-1}
- [ ] [RFC9830-2.4.3-4] [MUST] The unassigned bits in the SRv6 Binding SID Flags field MUST be set to zero upon transmission (§2.4.3)
- [ ] [RFC9830-2.4.3-5] [MUST] The unassigned bits in the SRv6 Binding SID Flags field MUST be ignored upon receipt (§2.4.3)
- [ ] [RFC9830-2.4.3-6] [MUST] The SRv6 Binding SID RESERVED field MUST be set to zero on transmission (§2.4.3)
- [ ] [RFC9830-2.4.3-7] [MUST] The SRv6 Binding SID RESERVED field MUST be ignored on receipt (§2.4.3)
- [ ] [RFC9830-2.4.3-9] [MUST NOT] The SRv6 Endpoint Behavior and SID Structure MUST NOT be included when the SRv6 SID has not been included (§2.4.3) {single-polarity: positive; the SRv6 Binding SID sub-TLV ze writes always carries the 16-octet SID and never the Endpoint Behavior and SID Structure (internal/component/bgp/plugins/nlri/srpolicy/config.go:409-416), so the forbidden combination has no producer to drive a negative from. The parallel Type B rule, where an endpoint-behavior token without a preceding SRv6 SID IS refused, is covered with both polarities under RFC9830-2.4.4.2.2-4}
- [ ] [RFC9830-2.4.4-4] [MUST] The Segment List RESERVED field MUST be set to zero on transmission (§2.4.4)
- [ ] [RFC9830-2.4.4-5] [MUST] The Segment List RESERVED field MUST be ignored on receipt (§2.4.4)
- [ ] [RFC9830-2.4.4.1-2] [MUST NOT] The Weight sub-TLV MUST NOT appear more than once inside the Segment List sub-TLV (§2.4.4.1)
- [ ] [RFC9830-2.4.4.1-3] [MUST] The Weight sub-TLV Length value MUST be 6 (§2.4.4.1)
- [ ] [RFC9830-2.4.4.1-4] [MUST] The Weight Flags field MUST be set to zero on transmission (§2.4.4.1)
- [ ] [RFC9830-2.4.4.1-5] [MUST] The Weight Flags field MUST be ignored on receipt (§2.4.4.1)
- [ ] [RFC9830-2.4.4.1-6] [MUST] The Weight RESERVED field MUST be set to zero on transmission (§2.4.4.1)
- [ ] [RFC9830-2.4.4.1-7] [MUST] The Weight RESERVED field MUST be ignored on receipt (§2.4.4.1)
- [ ] [RFC9830-2.4.4.2.1-1] [MUST] The Type A Segment sub-TLV Length value MUST be 6 (§2.4.4.2.1)
- [ ] [RFC9830-2.4.4.2.1-2] [MUST] The Type A Segment RESERVED field MUST be set to zero on transmission (§2.4.4.2.1)
- [ ] [RFC9830-2.4.4.2.1-3] [MUST] The Type A Segment RESERVED field MUST be ignored on receipt (§2.4.4.2.1)
- [ ] [RFC9830-2.4.4.2.1-4] [MUST] The Type A Segment S bit MUST be zero upon transmission (§2.4.4.2.1)
- [ ] [RFC9830-2.4.4.2.1-5] [MUST] The Type A Segment S bit MUST be ignored upon reception (§2.4.4.2.1)
- [ ] [RFC9830-2.4.4.2.2-1] [MUST] The Type B Segment sub-TLV Length value MUST be 26 when the SRv6 Endpoint Behavior and SID Structure is present, else MUST be 18 (§2.4.4.2.2)
- [ ] [RFC9830-2.4.4.2.2-2] [MUST] The Type B Segment RESERVED field MUST be set to zero on transmission (§2.4.4.2.2)
- [ ] [RFC9830-2.4.4.2.2-3] [MUST] The Type B Segment RESERVED field MUST be ignored on receipt (§2.4.4.2.2)
- [ ] [RFC9830-2.4.4.2.2-4] [MUST NOT] The Type B SRv6 Endpoint Behavior and SID Structure MUST NOT be included when the SRv6 SID has not been included (§2.4.4.2.2)
- [ ] [RFC9830-2.4.4.2.3-1] [MUST] The unassigned bits in the Segment Flags field MUST be set to zero upon transmission (§2.4.4.2.3)
- [ ] [RFC9830-2.4.4.2.3-2] [MUST] The unassigned bits in the Segment Flags field MUST be ignored upon receipt (§2.4.4.2.3)
- [ ] [RFC9830-2.4.4.2.3-3] [MUST] If the B-Flag appears with Segment Type A, it MUST be ignored (§2.4.4.2.3)
- [ ] [RFC9830-2.4.4.2.4-2] [MUST] The SRv6 Endpoint Behavior and SID Structure Reserved field MUST be set to zero on transmission (§2.4.4.2.4)
- [ ] [RFC9830-2.4.4.2.4-3] [MUST] The SRv6 Endpoint Behavior and SID Structure Reserved field MUST be ignored on receipt (§2.4.4.2.4)
- [ ] [RFC9830-2.4.4.2.4-4] [MUST] The total of the locator block, locator node, function, and argument lengths MUST be less than or equal to 128 (§2.4.4.2.4) {gap: the four SID-structure lengths are parsed as independent octets with no sum check (internal/component/bgp/plugins/nlri/srpolicy/config.go:312-318) and written verbatim into the segment sub-TLV (internal/component/bgp/plugins/nlri/srpolicy/config.go:478-481), so a configuration whose locator block, locator node, function and argument lengths total more than 128 is encoded rather than refused}
- [ ] [RFC9830-2.4.5-2] [MUST NOT] The ENLP sub-TLV MUST NOT appear more than once in the SR Policy encoding (§2.4.5) {not-applicable: ze neither encodes nor interprets the ENLP sub-TLV, so it never writes a second instance. The SR Policy sub-TLV constant set holds no type 14 (internal/component/bgp/plugins/nlri/srpolicy/config.go:23-29), buildTunnelEncap has no ENLP branch (internal/component/bgp/plugins/nlri/srpolicy/config.go:339-363), and grep -rniE '\benlp\b|explicit.?null.?label' over the Go tree matches only the OSPF MPLS Explicit NULL label, an unrelated feature}
- [ ] [RFC9830-2.4.5-3] [MUST] The ENLP sub-TLV Length value MUST be 3 (§2.4.5) {not-applicable: ze writes no ENLP sub-TLV, so it declares no length for one. There is no type-14 constant in the SR Policy encoder (internal/component/bgp/plugins/nlri/srpolicy/config.go:23-29) and no ENLP branch in buildTunnelEncap (internal/component/bgp/plugins/nlri/srpolicy/config.go:339-363)}
- [ ] [RFC9830-2.4.5-4] [MUST] The ENLP Flags field MUST be set to zero on transmission (§2.4.5) {not-applicable: ze transmits no ENLP sub-TLV, so it has no ENLP Flags field to zero; the config keyword set has no ENLP spelling (internal/component/bgp/plugins/nlri/srpolicy/config.go:72-187) and buildTunnelEncap emits no type-14 sub-TLV (internal/component/bgp/plugins/nlri/srpolicy/config.go:339-363)}
- [ ] [RFC9830-2.4.5-5] [MUST] The ENLP Flags field MUST be ignored on receipt (§2.4.5) {not-applicable: ze decodes no ENLP sub-TLV, so it reads no ENLP Flags field. Preference is the only typed sub-TLV accessor (internal/core/bgp/attribute/tunnel_encap.go:145) and the sub-TLV type constants stop at Segment List (internal/core/bgp/attribute/tunnel_encap.go:87-92)}
- [ ] [RFC9830-2.4.5-6] [MUST] The ENLP RESERVED field MUST be set to zero on transmission (§2.4.5) {not-applicable: ze transmits no ENLP sub-TLV, so it has no ENLP RESERVED octet to zero; buildTunnelEncap emits no type-14 sub-TLV (internal/component/bgp/plugins/nlri/srpolicy/config.go:339-363)}
- [ ] [RFC9830-2.4.5-7] [MUST] The ENLP RESERVED field MUST be ignored on receipt (§2.4.5) {not-applicable: ze decodes no ENLP sub-TLV, so it reads no ENLP RESERVED octet; the only typed sub-TLV accessor is Preference (internal/core/bgp/attribute/tunnel_encap.go:145)}
- [ ] [RFC9830-2.4.5-8] [MUST] Implementations MUST ignore the ENLP sub-TLV with unrecognized values (other than 1 through 4) (§2.4.5) {not-applicable: ze interprets no ENLP value, recognized or not. The requirement presupposes a receiver that acts on values 1 through 4; ze has no ENLP decoder at all (internal/core/bgp/attribute/tunnel_encap.go:87-92, :145) and no Explicit NULL push driven by an SR Policy}
- [ ] [RFC9830-2.4.6-3] [MUST NOT] The Priority sub-TLV MUST NOT appear more than once in the SR Policy encoding (§2.4.6)
- [ ] [RFC9830-2.4.6-4] [MUST] The Priority sub-TLV Length value MUST be 2 (§2.4.6)
- [ ] [RFC9830-2.4.6-5] [MUST] The Priority RESERVED field MUST be set to zero on transmission (§2.4.6)
- [ ] [RFC9830-2.4.6-6] [MUST] The Priority RESERVED field MUST be ignored on receipt (§2.4.6)
- [ ] [RFC9830-2.4.7-5] [MUST NOT] The SR Policy Candidate Path Name sub-TLV MUST NOT appear more than once in the SR Policy encoding (§2.4.7)
- [ ] [RFC9830-2.4.7-6] [MUST] The SR Policy Candidate Path Name RESERVED field MUST be set to zero on transmission (§2.4.7)
- [ ] [RFC9830-2.4.7-7] [MUST] The SR Policy Candidate Path Name RESERVED field MUST be ignored on receipt (§2.4.7)
- [ ] [RFC9830-2.4.8-5] [MUST NOT] The SR Policy Name sub-TLV MUST NOT appear more than once in the SR Policy encoding (§2.4.8)
- [ ] [RFC9830-2.4.8-6] [MUST] The SR Policy Name RESERVED field MUST be set to zero on transmission (§2.4.8)
- [ ] [RFC9830-2.4.8-7] [MUST] The SR Policy Name RESERVED field MUST be ignored on receipt (§2.4.8)
- [ ] [RFC9830-3-3] [MUST] Upon reception, an implementation MUST treat Color-Only Type 3 (bits 11) like Type 0 (§3) {not-applicable: ze performs no color-based steering, so it never compares a route's Color Extended Community with an SR Policy and the Color-Only bits are never read. The eight octets are carried as an opaque extended community (internal/core/bgp/attribute/community.go, ParseExtendedCommunities) and grep -rniE 'color.?only|colorExtended' over the Go tree matches only test names, with no producer that decodes the CO field}
- [ ] [RFC9830-4.1-2] [MUST] If no route target is attached, the NO_ADVERTISE community MUST be attached to the SR Policy update (§4.1) {gap: ze attaches neither a route target nor NO_ADVERTISE to an SR Policy advertisement. parseConfigRoute builds exactly one attribute, the Tunnel Encapsulation attribute (internal/component/bgp/plugins/nlri/srpolicy/config.go:218-225), and deliberately ignores the pre-parsed attribute block that carries communities for other families (internal/component/bgp/plugins/nlri/srpolicy/config.go:43-44), so an SR Policy route ze originates carries no community at all}
- [ ] [RFC9830-4.2.1-1] [MUST] A BGP speaker MUST first perform validation based on the §4.2.1 rules in addition to the validation in §5 (§4.2.1) {gap: no SR Policy validation runs on receipt. validateMPNLRISyntax returns nil for every SAFI other than unicast and multicast, so SAFI 73 is skipped (internal/component/bgp/message/rfc7606.go:701-704), and the RFC 7606 validator table has no entry for attribute 23 (internal/component/bgp/message/rfc7606.go:415-429). A received SR Policy update reaches the RIB without any of the Section 4.2.1 checks}
- [ ] [RFC9830-4.2.1-2] [MUST] The SR Policy NLRI MUST include a Distinguisher, Color, and Endpoint field (§4.2.1)
- [ ] [RFC9830-4.2.1-3] [MUST] The length of the NLRI MUST be either 12 or 24 octets depending on the Endpoint address family (§4.2.1) {gap: SplitSRPolicy accepts any non-zero byte-aligned length that fits the buffer and never compares it with the AFI's mandated 12 or 24 octets (internal/component/bgp/plugins/nlri/srpolicy/split.go:22-33), and its error is discarded by the RIB walk (internal/component/bgp/plugins/rib/rib_structured.go:229). The encoder always writes the mandated length, which is the separate RFC9830-2.1-2}
- [ ] [RFC9830-4.2.1-4] [MUST] The SR Policy update MUST have either the NO_ADVERTISE community, at least one IPv4-address-format Route Target extended community, or both (§4.2.1) {gap: nothing inspects the communities of a received SR Policy update. There is no RFC 7606 validator for attribute 23 and none for the SAFI (internal/component/bgp/message/rfc7606.go:415-429, :701-704), and no code path reads NO_ADVERTISE or a route target for SAFI 73: grep for SAFISRPolicy outside the NLRI codec matches only the family registry and the next-hop length table (internal/core/bgp/attribute/mpnlri.go:277)}
- [ ] [RFC9830-4.2.1-5] [MUST] An SR Policy update with no Route Target extended communities and no NO_ADVERTISE community MUST be considered malformed (§4.2.1) {gap: an SR Policy update with neither a route target nor NO_ADVERTISE is accepted like any other. The malformed decision would have to come from an RFC 7606 validator for attribute 23 or for SAFI 73, and neither exists (internal/component/bgp/message/rfc7606.go:415-429, :701-704)}
- [ ] [RFC9830-4.2.1-6] [MUST] The Tunnel Encapsulation Attribute MUST be attached to the BGP UPDATE message (§4.2.1) {single-polarity: positive; parseConfigRoute attaches the Tunnel Encapsulation attribute to every SR Policy route it builds -- buildTunnelEncap always returns at least the 4-octet TLV header, so the len(tunnelEncapValue) > 0 guard never fails (internal/component/bgp/plugins/nlri/srpolicy/config.go:209-225, :365-370) -- leaving no update-without-the-attribute for a negative to observe. The receive-side obligation to call such an update malformed is the separate RFC9830-4.2.1-8, which is annotated as a gap}
- [ ] [RFC9830-4.2.1-7] [MUST] The Tunnel Encapsulation Attribute MUST have a Tunnel Type TLV set to SR Policy (code point 15) (§4.2.1)
- [ ] [RFC9830-4.2.1-8] [MUST] A router receiving an update not valid according to these criteria MUST treat the update as malformed (§4.2.1) {gap: no receive-side validity criteria are evaluated, so none can drive a malformed verdict. SAFI 73 is excluded from the NLRI syntax check (internal/component/bgp/message/rfc7606.go:701-704) and attribute 23 has no validator (internal/component/bgp/message/rfc7606.go:415-429)}
- [ ] [RFC9830-4.2.1-9] [MUST NOT] An invalid SR Policy CP MUST NOT be passed to the SRPM (§4.2.1) {not-applicable: ze has no SRPM. grep -rni '\bsrpm\b' over the Go tree matches nothing, the SR Policy plugin registers only an NLRI codec and a config route encoder (internal/component/bgp/plugins/nlri/srpolicy/register.go:29-40), and no candidate path is ever handed to a policy manager, valid or otherwise}
- [ ] [RFC9830-4.2.2-1] [MUST] If route targets are present, at least one MUST match the BGP Identifier of the receiver for the update to be usable (§4.2.2) {not-applicable: ze never makes an SR Policy locally usable, so there is no eligibility decision for a route target to gate. There is no SRPM (grep -rni '\bsrpm\b' over the Go tree matches nothing) and no SAFI 73 consumer outside the NLRI codec and the family registry (internal/component/bgp/plugins/nlri/srpolicy/register.go:29-40, internal/core/family/family.go:93)}
- [ ] [RFC9830-4.2.2-2] [MUST] The Route Target extended community MUST be of the same format (4-octet, unsigned, non-zero) as the BGP Identifier (§4.2.2) {not-applicable: ze compares no route target against its BGP Identifier for SAFI 73, because it makes no SR Policy locally usable; there is no SRPM and no local-use path (internal/component/bgp/plugins/nlri/srpolicy/register.go:29-40), so the format constraint governs a comparison ze never performs}
- [ ] [RFC9830-4.2.2-5] [MUST] When an update results in the SR Policy NLRI becoming unusable, BGP MUST delete its corresponding SR Policy CP from the SRPM (§4.2.2) {not-applicable: ze holds no SR Policy candidate path to delete. grep -rni '\bsrpm\b' over the Go tree matches nothing and the SR Policy plugin keeps no state beyond the NLRI codec (internal/component/bgp/plugins/nlri/srpolicy/register.go:29-40); withdrawal of a SAFI 73 route removes the RIB entry and nothing else}
- [ ] [RFC9830-4.2.3-1] [MUST NOT] SR Policy NLRIs that have the NO_ADVERTISE community MUST NOT be propagated (§4.2.3) {gap: the egress gate consults only the operator-configured export filter chain and never inspects community values (writeUpdateGated, internal/component/bgp/reactor/session_write.go:263-283), so an SR Policy NLRI carrying NO_ADVERTISE is propagated unless an operator filter happens to match it. The same omission is disclosed for RFC 1997}
- [ ] [RFC9830-4.2.3-2] [MUST NOT] By default, a BGP node receiving an SR Policy NLRI MUST NOT propagate it to any EBGP neighbor (§4.2.3) {gap: propagation of SAFI 73 is family-generic with no eBGP default. The reactor's forwarding path keys on the negotiated families and the per-peer export filter, and grep for SAFISRPolicy over internal/component/bgp/reactor matches nothing, so a received SR Policy NLRI is forwarded to an eBGP neighbor that negotiated the family like any other route}
- [ ] [RFC9830-4.2.3-6] [MUST NOT] A BGP node MUST NOT alter the SR Policy information carried in the Tunnel Encapsulation Attribute during propagation (§4.2.3)
- [ ] [RFC9830-5-1] [MUST] A BGP speaker MUST perform syntactic validation of the SR Policy NLRI (per-NLRI length, total MP_REACH_NLRI/MP_UNREACH_NLRI length, and consistency of NLRI length with the AFI and endpoint) to determine if malformed (§5) {gap: the syntactic validation is partial and its verdict is discarded. SplitSRPolicy checks the framing -- zero length, byte alignment, buffer overrun (internal/component/bgp/plugins/nlri/srpolicy/split.go:22-33) -- but never the consistency of the length with the AFI and endpoint, and every caller drops its error (internal/component/bgp/plugins/rib/rib_structured.go:229). The MP attribute's own NLRI check skips SAFI 73 outright (internal/component/bgp/message/rfc7606.go:701-704)}
- [ ] [RFC9830-5-2] [MUST] When the error allows skipping the malformed NLRI(s) and continuing, the router MUST handle such malformed NLRIs as treat-as-withdraw (§5) {gap: no SR Policy NLRI is ever treated as withdrawn for being malformed. SAFI 73 is excluded from validateMPNLRISyntax (internal/component/bgp/message/rfc7606.go:701-704), which is the only path that turns an NLRI-level error into an RFC 7606 action, and the splitter's error is discarded (internal/component/bgp/plugins/rib/rib_structured.go:229)}
- [ ] [RFC9830-5-4] [MUST] The router MUST perform session reset when the session is only used for SR Policy or when AFI/SAFI disable is not possible (§5) {gap: no SR Policy error path exists, so no session reset can be reached from one. The RFC7606ActionSessionReset verdict is produced only by the checks in internal/component/bgp/message/rfc7606.go, and SAFI 73 reaches none of them (internal/component/bgp/message/rfc7606.go:701-704, :415-429)}
- [ ] [RFC9830-5-5] [MUST] The validation of the TLVs/sub-TLVs defined in Section 2.4 MUST be performed to determine if they are malformed or invalid (§5) {gap: only one Section 2.4 sub-TLV is validated. TunnelTLV.Preference checks the mandated 6-octet value length before reading it (internal/core/bgp/attribute/tunnel_encap.go:151); every other sub-TLV is walked for framing only by TunnelTLV.SubTLVs (internal/core/bgp/attribute/tunnel_encap.go:104-131) and no length, flag or field of the Binding SID, SRv6 Binding SID, Priority, Segment List, Weight, Segment or name sub-TLVs is checked}
- [ ] [RFC9830-5-6] [MUST] The validation of the Tunnel Encapsulation Attribute and other TLVs/sub-TLVs (RFC 9012 Section 13) MUST be done as described in that document (§5) {gap: the RFC 9012 Section 13 structural validation exists but is never driven at reception. ParseTunnelEncap is registered as the parser for attribute 23 (internal/core/bgp/attribute/wire.go:418) and rejects broken TLV framing (internal/core/bgp/attribute/tunnel_encap.go:41-54), but attributes are parsed lazily, only when something reads them (internal/core/bgp/attribute/wire.go:346), and nothing reads attribute 23 for a SAFI 73 route; there is no RFC 7606 validator that would force the parse (internal/component/bgp/message/rfc7606.go:415-429)}
- [ ] [RFC9830-5-7] [MUST] In case of any error detected at the attribute or its TLV/sub-TLV level, the treat-as-withdraw strategy MUST be applied (§5) {gap: an error at the attribute or sub-TLV level produces no treat-as-withdraw. The RFC 7606 validator table has no entry for attribute 23 (internal/component/bgp/message/rfc7606.go:415-429), so the ParseTunnelEncap and SubTLVs errors (internal/core/bgp/attribute/tunnel_encap.go:43, :109) are only ever seen by a caller that chose to parse, never by the UPDATE validation path}
- [ ] [RFC9830-5-8] [MUST] An SR Policy update determined not valid per Section 4.2.1 MUST be handled by treat-as-withdraw (§5) {gap: the Section 4.2.1 validity criteria are not evaluated at all (see RFC9830-4.2.1-1), so no update can be handled as treat-as-withdraw for failing them; SAFI 73 reaches neither the NLRI check nor an attribute validator (internal/component/bgp/message/rfc7606.go:701-704, :415-429)}
- [ ] [RFC9830-5-9] [MUST NOT] A BGP implementation MUST NOT perform semantic verification of the individual TLV/sub-TLV fields, nor consider the SR Policy update invalid or not usable based on such validation (§5)

### SHOULD / SHOULD NOT / RECOMMENDED

- [ ] [RFC9830-2.1-5] [RECOMMENDED] When several CPs of the same SR Policy are signaled via BGP, it is RECOMMENDED that each NLRI use a different distinguisher (§2.1)
- [ ] [RFC9830-2.4.2-3] [RECOMMENDED] It is RECOMMENDED that the SRv6 Binding SID sub-TLV be used when signaling an SRv6 BSID for an SR Policy CP (§2.4.2)
- [ ] [RFC9830-2.4.7-2] [RECOMMENDED] It is RECOMMENDED that the size of the symbolic name for the CP be limited to 255 bytes (§2.4.7)
- [ ] [RFC9830-2.4.8-2] [RECOMMENDED] It is RECOMMENDED that the size of the symbolic name for the SR Policy be limited to 255 bytes (§2.4.8)
- [ ] [RFC9830-3-2] [SHOULD NOT] Color-Only Type 3 (bits 11) is reserved for future use and SHOULD NOT be used (§3)
- [ ] [RFC9830-4.1-1] [SHOULD] One or more route targets SHOULD be attached to the advertisement (§4.1)
- [ ] [RFC9830-4.2.2-3] [SHOULD NOT] When the SR Policy tunnel type includes any unrecognized or unsupported sub-TLV, the update SHOULD NOT be considered usable (§4.2.2)
- [ ] [RFC9830-4.2.3-4] [SHOULD NOT] By default, a BGP node receiving an SR Policy NLRI SHOULD NOT remove the Route Target extended community before propagation (§4.2.3)
- [ ] [RFC9830-5-3] [SHOULD] Where the error prevents processing the UPDATE, the router SHOULD handle such malformed NLRIs as AFI/SAFI disable when other AFI/SAFIs share the session (§5)
- [ ] [RFC9830-5-10] [SHOULD] An implementation SHOULD log any errors found during the above validation (§5)

### MAY / OPTIONAL

- [ ] [RFC9830-2.1-4] [MAY] The BGP UPDATE message MAY also contain any of the BGP optional attributes (§2.1)
- [ ] [RFC9830-2.3-2] [MAY] A BGP speaker MAY remove the Tunnel Egress Endpoint and Color sub-TLVs from the Tunnel Encapsulation Attribute during propagation (§2.3)
- [ ] [RFC9830-2.3-4] [MAY] Other sub-TLVs without defined applicability to the SR Policy SAFI MAY be removed from the Tunnel Encapsulation Attribute during propagation (§2.3)
- [ ] [RFC9830-2.4.1-1] [OPTIONAL] The Preference sub-TLV is OPTIONAL (§2.4.1)
- [ ] [RFC9830-2.4.2-1] [OPTIONAL] The Binding SID sub-TLV is OPTIONAL (§2.4.2)
- [ ] [RFC9830-2.4.3-1] [OPTIONAL] The SRv6 Binding SID sub-TLV is OPTIONAL (§2.4.3)
- [ ] [RFC9830-2.4.3-2] [MAY] More than one SRv6 Binding SID sub-TLV MAY be signaled in the same SR Policy encoding (§2.4.3)
- [ ] [RFC9830-2.4.3-8] [MAY] The SRv6 Binding SID value 0 MAY be used to indicate the desired behavior without specifying the BSID (§2.4.3)
- [ ] [RFC9830-2.4.4-1] [OPTIONAL] The Segment List sub-TLV is OPTIONAL (§2.4.4)
- [ ] [RFC9830-2.4.4-2] [MAY] The Segment List sub-TLV MAY appear multiple times in the SR Policy encoding (§2.4.4)
- [ ] [RFC9830-2.4.4-3] [MAY] The Segment List sub-TLV MAY contain a Weight sub-TLV (§2.4.4)
- [ ] [RFC9830-2.4.4.1-1] [OPTIONAL] The Weight sub-TLV is OPTIONAL (§2.4.4.1)
- [ ] [RFC9830-2.4.4.2-1] [OPTIONAL] The Segment sub-TLVs are OPTIONAL (§2.4.4.2)
- [ ] [RFC9830-2.4.4.2-2] [MAY] The Segment sub-TLVs MAY appear multiple times in the Segment List sub-TLV (§2.4.4.2)
- [ ] [RFC9830-2.4.4.2.1-6] [MAY] The receiver MAY override the originator's TC and/or TTL values, as determined by local policy (§2.4.4.2.1)
- [ ] [RFC9830-2.4.4.2.4-1] [MAY] The Segment Type sub-TLVs MAY contain the SRv6 Endpoint Behavior and SID Structure encoding (§2.4.4.2.4)
- [ ] [RFC9830-2.4.5-1] [OPTIONAL] The ENLP sub-TLV is OPTIONAL (§2.4.5)
- [ ] [RFC9830-2.4.5-9] [MAY] The behavior signaled in the ENLP sub-TLV MAY be overridden by local configuration (§2.4.5)
- [ ] [RFC9830-2.4.6-1] [MAY] An operator MAY set the SR Policy Priority sub-TLV to indicate recomputation order upon topological change (§2.4.6)
- [ ] [RFC9830-2.4.6-2] [OPTIONAL] The Priority sub-TLV is OPTIONAL (§2.4.6)
- [ ] [RFC9830-2.4.7-1] [MAY] An operator MAY set the SR Policy Candidate Path Name sub-TLV to attach a symbolic name to the SR Policy CP (§2.4.7)
- [ ] [RFC9830-2.4.7-3] [MAY] Implementations MAY choose to truncate long CP names to 255 bytes when signaling via BGP (§2.4.7)
- [ ] [RFC9830-2.4.7-4] [OPTIONAL] The SR Policy Candidate Path Name sub-TLV is OPTIONAL (§2.4.7)
- [ ] [RFC9830-2.4.8-1] [MAY] An operator MAY set the SR Policy Name sub-TLV to associate a symbolic name with the SR Policy (§2.4.8)
- [ ] [RFC9830-2.4.8-3] [MAY] Implementations MAY choose to truncate long SR Policy names to 255 bytes when signaling via BGP (§2.4.8)
- [ ] [RFC9830-2.4.8-4] [OPTIONAL] The SR Policy Name sub-TLV is OPTIONAL (§2.4.8)
- [ ] [RFC9830-3-1] [MAY] The Color Extended Community MAY be carried in any BGP UPDATE message whose AFI/SAFI is one of the families listed (§3)
- [ ] [RFC9830-3-4] [MAY] One or more Color Extended Communities MAY be associated with a BGP route update (§3)
- [ ] [RFC9830-4.2.2-4] [MAY] An implementation MAY provide an option for ignoring unsupported sub-TLVs (§4.2.2)
- [ ] [RFC9830-4.2.3-3] [MAY] An implementation MAY provide explicit configuration to override the EBGP default and enable propagation to specific EBGP neighbors (§4.2.3)
- [ ] [RFC9830-4.2.3-5] [MAY] An implementation MAY provide support for configuration to filter and/or remove the Route Target extended community before propagation (§4.2.3)
