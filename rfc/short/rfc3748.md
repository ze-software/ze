# RFC 3748 - Extensible Authentication Protocol (EAP)

## Meta

| Field | Value |
|-------|-------|
| RFC | 3748 |
| Title | Extensible Authentication Protocol (EAP) |
| Status | Standards Track |
| Date | June 2004 |
| Authors | B. Aboba, L. Blunk, J. Vollbrecht, J. Carlson, H. Levkowetz (Ed.) |
| Obsoletes | RFC 2284 |
| Updated by | RFC 5247, RFC 7057 |
| Enrolment | enrolled |
| Enrolment reason | Extensible Authentication Protocol / EAP (RFC 3748): IKEv2-carried EAP peer + co-located authenticator (EAP-TLS, EAP-MSCHAPv2, local termination, no pass-through). Both roles are gated: the packet format and its length handling, the Identifier and Type rules that bind a Request to its Response, one method per conversation, method completion -> Success/Failure, the 4-octet terminal packet, the peer's end of an unsuccessful method, and four silent discards -- a Code outside 1-4 by either role, and by the peer a canned Success, a Success the method does not permit yet, and a Failure after both ends indicated success. A non-Response carrying a DEFINED Code is still answered with EAP-Failure; a Code outside 1-4 is discarded, which corrects a row that claimed the first behavior for every non-Response (2026-09-01). Lower-layer ordering, error detection, MTU and duplicate detection are the IKEv2 carrier's, pass-through and Expanded Types are out of scope, and no EMSK is derived. |
| Support | ipsec 70 |
| Support area | EAP |
| Support status | Supported in IPsec |
| Support coverage | EAP framework inside IKEv2 IKE_AUTH, Success and Failure handling, and the Section 4.2 discards that stop a rogue authenticator bypassing the method: the peer reads an EAP-Success only once the method conversation concluded, drops an EAP-Failure once both ends indicated success, and both roles drop a Code outside 1-4 (`PeerSession.Process` and `Session.Process`, `internal/core/eap`). The peer answers Type 1 (Identity), Type 2 (Notification) and Type 3 (Nak): a Notification Request draws a five-octet Notification Response and its message reaches the operator log, and a Request for an authentication Type ze does not run draws a six-octet legacy Nak naming the configured method, until the peer has answered a method Request and Section 2.1 closes the Nak (`PeerSession.handleRequest`, `internal/core/eap/peer.go`). A Type-254 Request draws the same legacy Nak, which is what Section 5.7 prescribes for a peer not equipped to interpret an Expanded Type. Type 4 (MD5-Challenge) runs on both roles and is the `authentication { mode eap-md5 }` an operator selects. It is never a default, and adopting a configuration that names it writes one warning quoting the RFC 7296 Section 2.16 sentence that discourages a method establishing no shared key (`warnKeylessEAPModes`, `internal/component/ike/engine/eap_auth.go`). |
| Support remaining | Ze reads no Expanded Type (254), which Section 5 states as a SHOULD, and answers a Type-254 Request with the legacy Nak Section 5.7 prescribes rather than composing an Expanded Nak. Ze's authenticator sends no Notification Request, which RFC 3748 Section 5.2 states as an option ("An authenticator MAY send a Notification Request to the peer at any time when there is no outstanding Request, prior to completion of an EAP authentication method") and which the owner declined on 2026-09-01; Ze's peer answers one, which is the mandatory half. Two further features are absent by decision rather than by omission, and neither is a conformance gap. Ze's authenticator terminates every EAP method locally and does not act as a pass-through agent for a backend authentication server; Section 2 says "Support for pass-through is optional". Ze offers neither the One Time Password method (Type 5) nor the Generic Token Card method (Type 6), which Section 5 leaves to the implementation ("Implementations MAY support other Types defined here or in future RFCs"). A later scope decision can revisit any of the three. The Type 4 (MD5-Challenge) deviation authorized on 2026-08-30 was WITHDRAWN by the owner on 2026-09-01, who ordered the method implemented; both roles now run it. |

**Purpose:** Defines a flexible authentication framework that supports multiple authentication mechanisms (methods) over various link layers. EAP provides a standard exchange of authentication credentials between a peer and an authenticator, with method negotiation via type codes. It does not select a specific mechanism at the link layer; instead, authentication is deferred to a type-specific exchange.

**Ze context:** Ze implements EAP as part of its native IKEv2 engine for authenticating road warrior clients. EAP runs inside IKE_AUTH exchanges (RFC 7296 Section 2.16): the responder authenticates first with a certificate, then the initiator authenticates via EAP. Ze supports EAP-TLS (Type 13, RFC 5216) and EAP-MSCHAPv2 (Type 26) for Windows client compatibility.

## EAP Packet Format (Section 4)

| Field | Size | Description |
|-------|------|-------------|
| Code | 1 octet | Packet type: 1=Request, 2=Response, 3=Success, 4=Failure |
| Identifier | 1 octet | Matches Responses to Requests; unchanged on retransmission |
| Length | 2 octets | Total packet length including Code, Identifier, Length, and Data |
| Type | 1 octet | Authentication method (present only in Request/Response, Codes 1-2) |
| Type-Data | Variable | Method-specific data (present only in Request/Response) |

Success (Code 3) and Failure (Code 4) packets contain only Code, Identifier, and Length (4 octets total, no Type field).

Minimum Length: 4 octets. Packets shorter than Length are silently discarded. Octets beyond Length are ignored.

## Code Values (Section 4)

| Code | Name | Direction | Contains Type |
|------|------|-----------|---------------|
| 1 | Request | Authenticator to Peer | Yes |
| 2 | Response | Peer to Authenticator | Yes |
| 3 | Success | Authenticator to Peer | No |
| 4 | Failure | Authenticator to Peer | No |

EAP defines Codes 1-4 and nothing else, so a packet carrying any other Code is silently discarded, by an authenticator and by a peer alike.

## EAP Exchange Model (Sections 1.3, 2, 2.1)

### Roles

| Role | Description |
|------|-------------|
| Peer | End system being authenticated (supplicant). Sends Responses. |
| Authenticator | System requesting authentication. Sends Requests, Success, Failure. |
| EAP Server | Back-end entity terminating the EAP method. May be co-located with the authenticator or accessed via AAA (RADIUS). |

### Exchange Flow

```
Peer                              Authenticator
----                              -------------
                           <--    Request/Identity
Response/Identity          -->
                           <--    Request/Method
Response/Method            -->
                           ...    (method-specific exchanges)
                           <--    Success or Failure
```

### Key Rules

| Rule | Section | Detail |
|------|---------|--------|
| Lock-step | 2 | One outstanding Request at a time. Authenticator must receive a valid Response before sending a new Request. |
| Retransmission | 4.1 | Authenticator only. Peer never retransmits on timer; it re-sends its last Response if a duplicate Request arrives. |
| One method | 2.1 | Only one authentication method (Type >= 4) per conversation. After the method completes, authenticator sends Success or Failure. |
| Success/Failure | 4.2 | Not retransmitted, not acknowledged. Peer must handle loss (e.g., treat lower-layer success as implicit EAP Success). |
| NAK rejects | 2.1 | Peer sends NAK (Type 3) to reject a proposed method and suggest alternatives. Not permitted after first non-NAK Response. Ze reads that boundary as the peer's first Response to an authentication METHOD Request, not the Identity Response, because Section 5.4 describes a Nak sent after the Identity Response. |
| Notification | 5.2 | The peer answers a Type-2 Request with a five-octet Type-2 Response and never with a Nak. The message is advisory and changes no peer state. |
| Peer discards | 4.2 | The peer reads a Success only once the method conversation concluded, which is what stops a rogue authenticator bypassing the method, and it drops a Failure once both ends indicated success. |

### Pass-Through Model (Section 2.3)

The authenticator may act as a pass-through, forwarding EAP packets between the peer and a back-end EAP server via AAA (RADIUS/Diameter). The authenticator examines only Code, Identifier, and Length for routing. It must forward packets of any Type without interpreting method-specific data.

## Method Types (Sections 5, 6.2)

### Initial Types (defined in RFC 3748)

| Type | Name | Section | Notes |
|------|------|---------|-------|
| 1 | Identity | 5.1 | Query/declare peer identity. Sent in cleartext. |
| 2 | Notification | 5.2 | Display message to user. Advisory only. |
| 3 | NAK (Legacy) | 5.3 | Response only. Rejects proposed type, lists alternatives. |
| 4 | MD5-Challenge | 5.4 | CHAP-like challenge-response. No mutual auth, no key derivation. |
| 5 | OTP | 5.5 | One-Time Password (RFC 2289). |
| 6 | GTC | 5.6 | Generic Token Card. |
| 254 | Expanded Types | 5.7 | Vendor-specific or extended IETF namespace. |
| 255 | Experimental | 5.8 | Local testing only. |

### Types Relevant to Ze

| Type | Name | Defined in | Notes |
|------|------|------------|-------|
| 1 | Identity | RFC 3748 S5.1 | Answered by the peer, and sent by the authenticator to open every exchange. |
| 2 | Notification | RFC 3748 S5.2 | Answered by the peer. The message reaches the operator log. Ze's authenticator sends none. |
| 3 | Nak (Legacy) | RFC 3748 S5.3.1 | Sent by the peer to refuse an authentication Type it does not run, naming the configured method. Read by the authenticator, which records the Types the peer asked for and ends the exchange. |
| 4 | MD5-Challenge | RFC 3748 S5.4 | Both roles. Derives no key, so RFC 7296 Section 2.16 builds the IKEv2 AUTH payloads from SK_pi and SK_pr rather than from an MSK. |
| 13 | EAP-TLS | RFC 5216 | Certificate-based mutual authentication. Derives MSK/EMSK. |
| 26 | EAP-MSCHAPv2 | RFC 2759 + vendor | Password-based. Microsoft extension, widely deployed for Windows VPN clients. |
| 254 | Expanded Types | RFC 3748 S5.7 | NOT read. A Type-254 Request draws the legacy Nak Section 5.7 prescribes. |

### Expanded Type Format (Section 5.7)

When Type = 254, the packet carries additional fields for a larger namespace:

| Field | Size | Description |
|-------|------|-------------|
| Type | 1 octet | Set to 254 |
| Vendor-Id | 3 octets | 0 = IETF, other = SMI Network Management Private Enterprise Code |
| Vendor-Type | 4 octets | Method within vendor namespace |
| Vendor-Data | Variable | Method-specific |

## EAP over IKEv2 (RFC 7296 Section 2.16)

EAP within IKEv2 modifies the standard IKE_AUTH exchange:

```
Initiator                          Responder
-----------                        -----------
HDR, SK {IDi, [CERTREQ]}  -->                        IKE_AUTH request 1
                           <--  HDR, SK {IDr, [CERT],
                                  AUTH, EAP}          Responder authenticates with cert,
                                                      starts EAP
HDR, SK {EAP}              -->                        EAP Response
                           <--  HDR, SK {EAP}         EAP Request (method-specific)
                           ...                        (repeat as needed)
HDR, SK {EAP}              -->                        Final EAP Response
                           <--  HDR, SK {EAP(Success)} EAP Success
HDR, SK {AUTH}             -->                        Initiator AUTH (derived from EAP MSK)
                           <--  HDR, SK {AUTH, SAr2,
                                  TSi, TSr}           Responder final AUTH + Child SA
```

| Aspect | Detail |
|--------|--------|
| Responder authenticates first | Certificate-based AUTH before EAP begins |
| Initiator defers AUTH | No AUTH payload in first IKE_AUTH request; AUTH sent after EAP Success |
| AUTH from EAP keys | Initiator's AUTH payload uses key material derived from EAP MSK |
| EAP method must derive keys | Methods that do not generate MSK (e.g., MD5-Challenge) cannot be used with IKEv2 |
| One method per IKE_AUTH | Consistent with EAP's one-method-per-conversation rule |

## Key Derivation (Section 7.10)

| Key | Minimum size | Purpose | Visibility |
|-----|-------------|---------|------------|
| MSK (Master Session Key) | 64 octets | Exported to authenticator for link/SA protection | Shared with authenticator |
| EMSK (Extended MSK) | 64 octets | Reserved for future use, method-specific extensions | Not shared with authenticator |

For IKEv2, the MSK is used to derive the AUTH payload that completes initiator authentication after EAP Success.

Methods that do not derive keys (Identity, Notification, MD5-Challenge, OTP, GTC) are unsuitable for IKEv2.

## Lower Layer Transport (Section 3)

| Lower Layer | Encapsulation | Section |
|-------------|---------------|---------|
| PPP | Protocol 0xC227 | 3.2 |
| IEEE 802.1X | EAPOL frames | 3.3 |
| IKEv2 | EAP payload inside SK{} | RFC 7296 2.16 |
| RADIUS | EAP-Message attribute | RFC 3579 |

### Lower Layer Requirements (Section 3.1)

| Requirement | Detail |
|-------------|--------|
| Unreliable transport | Acceptable; EAP handles retransmission |
| Ordering | Required; EAP depends on in-order delivery |
| Error detection | Required (CRC, checksum, MIC) |
| Minimum MTU | 1020 octets for EAP packets |
| Duplicate detection | Required to avoid method confusion |

## Security Considerations (Section 7)

### Threat Model (Section 7.1)

| Threat | Description |
|--------|-------------|
| Identity exposure | Identity (Type 1) is sent in cleartext; vulnerable to eavesdropping |
| Spoofing | Success/Failure packets are unauthenticated; lower layer or method must protect |
| Dictionary attacks | Methods using passwords without per-packet integrity are vulnerable |
| Man-in-the-middle | Sequential method negotiation is vulnerable; tunneled methods must include MITM protection |
| Downgrade | Attacker may force weaker method via NAK manipulation |
| Replay | Methods must include replay protection |

### Method Security Properties (Section 7.2)

| Property | Methods providing it |
|----------|---------------------|
| Mutual authentication | EAP-TLS; not MD5-Challenge, OTP, GTC |
| Key derivation (MSK/EMSK) | EAP-TLS, EAP-MSCHAPv2; not MD5-Challenge, OTP, GTC |
| Protected identity | EAP-TLS (inside TLS tunnel); not Identity type |
| Integrity protection | EAP-TLS; not base types |
| Replay protection | EAP-TLS (TLS nonces); not MD5-Challenge alone |
| Dictionary attack resistance | EAP-TLS (no password); EAP-MSCHAPv2 (limited) |

### Implementation Guidance for Ze

| Concern | Guidance |
|---------|----------|
| Method selection | Only use methods that derive MSK (required for IKEv2 AUTH) |
| Identity privacy | Use EAP-TLS or tunnel the identity inside the method; avoid sending real identity in initial Identity Response |
| Certificate validation | EAP-TLS requires full chain validation, CRL/OCSP checking |
| Fragmentation | EAP-TLS messages can exceed MTU; fragment at the EAP method layer (RFC 5216 Section 2.1.5) |
| Result indication | Peer should not grant access until both EAP Success and IKEv2 AUTH are verified |

## Section Index

| Section | Topic |
|---------|-------|
| 1 | Introduction |
| 1.3 | Applicability |
| 2 | Protocol Overview (lock-step, roles) |
| 2.1 | One method per conversation |
| 2.2 | EAP multiplexing model |
| 2.3 | Pass-through behavior |
| 3 | Lower layer interface |
| 3.1 | Lower layer requirements |
| 3.2 | EAP usage within PPP |
| 3.3 | EAP usage within IEEE 802 |
| 4 | EAP packet format |
| 4.1 | Request and Response |
| 4.2 | Success and Failure |
| 4.3 | Retransmission |
| 5 | Initial EAP Request/Response Types |
| 5.1 | Identity |
| 5.2 | Notification |
| 5.3 | NAK |
| 5.4 | MD5-Challenge |
| 5.5 | One-Time Password (OTP) |
| 5.6 | Generic Token Card (GTC) |
| 5.7 | Expanded Types |
| 6 | IANA Considerations |
| 6.2 | Method type allocation |
| 7 | Security Considerations |
| 7.1 | Threat model |
| 7.2 | Security claims |
| 7.4 | Man-in-the-middle attacks |
| 7.10 | Key derivation |
| 7.15 | Channel binding |

## Compliance Checklist

- [ ] [RFC3748-4-1] [MUST] Packets shorter than the Length field indicates MUST be silently discarded (S4)
- [ ] [RFC3748-4-2] [MUST] Minimum EAP packet length is 4 octets (S4)
- [ ] [RFC3748-2-1] [MUST] The authenticator MUST operate in lock-step: only one outstanding Request at a time (S2) {single-polarity: positive; the authenticator Session emits one packet per call -- Begin returns a single Request (internal/core/eap/eap.go:164) and each Process returns a single *Packet (eap.go:176), so a new Request cannot precede its Response and no path leaves two outstanding, giving no negative case}
- [ ] [RFC3748-2-2] [MUST] The authenticator MUST receive a valid Response before sending a new Request (S2)
- [ ] [RFC3748-2.1-1] [MUST] Only one authentication method (Type >= 4) per EAP conversation (S2.1)
- [ ] [RFC3748-2.1-2] [MUST] After the method completes, the authenticator MUST send Success or Failure (S2.1)
- [ ] [RFC3748-2.1-3] [MUST] The peer MUST NOT send a NAK (Type 3) after the first non-NAK Response in a conversation (S2.1, S5.3)
- [ ] [RFC3748-4.1-1] [MUST] Only the authenticator retransmits on timer; the peer MUST NOT retransmit on its own timer (S4.1) {single-polarity: positive; the EAP peer (PeerSession.Process, internal/core/eap/peer.go:97) is a synchronous request-driven transform with no timer -- it emits a Response only when handed a Request and never self-retransmits; lower-layer retransmission is IKEv2's (internal/component/ike/engine/fsm.go:138), so there is no forbidden self-timer to test negatively}
- [ ] [RFC3748-4.1-2] [MUST] The peer MUST re-send its last Response when a duplicate Request arrives (S4.1) {not-applicable: duplicate-Request handling is delegated to the IKEv2 carrier -- on timeout the initiator (EAP peer) re-sends its last IKE_AUTH message, which carries its last EAP Response (sa.LastSentMsg, internal/component/ike/engine/fsm.go:138); the EAP peer state machine holds no per-Request retransmission state}
- [ ] [RFC3748-4.2-1] [MUST] Success and Failure packets MUST NOT be retransmitted (S4.2) {single-polarity: positive; the authenticator Session emits Success or Failure once and then enters a terminal state where Process returns nil (internal/core/eap/eap.go:186), so the EAP layer never retransmits them; there is no negative case}
- [ ] [RFC3748-4.2-2] [MUST] Success and Failure contain only Code, Identifier, and Length (4 octets, no Type field) (S4.2)
- [ ] [RFC3748-4.2-4] [MUST] The Identifier of a Success or Failure MUST match the Identifier of the Response it answers (S4.2) {single-polarity: positive; this obligation is on the SENDER. The authenticator's terminal packets are produced by Session.failure and the result.Done arm of Session.handleMethod (internal/core/eap/eap.go), and both now stamp the answered Response's Identifier. A negative case would need a RECEIVER that discards a mismatched Success or Failure, which Section 4.2 does not require of a sender and which ze's PeerSession.Process (internal/core/eap/peer.go) does not do: it switches on request.Code alone}
- [ ] [RFC3748-2.3-1] [MUST] A pass-through authenticator MUST forward packets of any Type without interpreting method-specific data (S2.3) {not-applicable: ze's authenticator always terminates the EAP method locally (eap.Session, internal/core/eap/eap.go:130); there is no AAA/RADIUS back end in the IKE engine, so it never operates as a pass-through}
- [ ] [RFC3748-3.1-1] [MUST] Lower layer MUST provide in-order delivery for EAP (S3.1) {not-applicable: in-order delivery is a lower-layer obligation; ze carries EAP only inside IKEv2, whose message-ID sequencing delivers each IKE_AUTH request/response in order (RFC 7296 Section 2.3; internal/component/ike/engine/msgid.go:76), so the EAP framework code neither provides nor can violate it}
- [ ] [RFC3748-3.1-2] [MUST] Lower layer MUST provide error detection (CRC, checksum, or MIC) (S3.1) {not-applicable: bit-error detection is a lower-layer obligation; ze's EAP packets travel inside the IKEv2 SK payload whose AEAD/ICV check rejects any corrupted frame on decrypt (internal/component/ike/engine/fsm.go:658), so the EAP framework code adds no CRC of its own}
- [ ] [RFC3748-3.1-3] [MUST] Lower layer MUST support a minimum MTU of 1020 octets for EAP packets (S3.1) {not-applicable: the 1020-octet minimum MTU is a lower-layer obligation; ze carries EAP inside IKEv2 SK payloads over UDP, which admit EAP packets far larger than 1020 octets, and EAP-TLS method data is itself fragmented in 1024-octet chunks (internal/core/eap/eap_tls.go:28)}
- [ ] [RFC3748-3.1-4] [MUST] Lower layer MUST provide duplicate detection (S3.1) {not-applicable: duplicate detection is a lower-layer obligation; IKEv2 detects a duplicated message by message ID and replays the cached response (internal/component/ike/engine/msgid.go:79; responder.go:81), so the EAP framework code neither provides nor can violate it}
- [ ] [RFC3748-5.7-1] [MUST] When Type = 254 (Expanded Types), Vendor-Id 0 = IETF namespace (S5.7) {not-applicable: ze offers MD5-Challenge (4), EAP-TLS (13) and EAP-MSCHAPv2 (26); NewSession rejects every other type (NewSession, internal/core/eap/eap.go) and the codec never encodes or parses an Expanded Type (254) packet or its Vendor-Id field, so no Vendor-Id namespace rule can bind. The peer reads TypeExpandedEAP only to route a Type-254 Request to the legacy Nak that Section 5.7 prescribes for a peer not equipped to interpret it (PeerSession.naks, internal/core/eap/peer.go), and that Nak carries no Vendor-Id}
- [ ] [RFC3748-7.10-1] [MUST] MSK MUST be at least 64 octets (S7.10) {single-polarity: positive; the MSK is the compile-time fixed [64]byte returned by DeriveMSK (internal/core/eap/mschapv2.go:206) and by the EAP-TLS ExportKeyingMaterial(...,64) (eap_tls.go:262), so the 64-octet floor cannot be undershot and has no negative case}
- [ ] [RFC3748-7.10-2] [MUST] EMSK MUST be at least 64 octets (S7.10) {not-applicable: ze derives only the MSK (exported for the IKEv2 AUTH payload) and never derives or consumes the EMSK -- no EMSK is produced anywhere in internal/core/eap, so there is no EMSK to size-check}
- [ ] [RFC3748-7.10-3] [SHOULD NOT] An EAP method that establishes no shared key, such as MD5-Challenge, SHOULD NOT be used with IKEv2 (RFC 7296 S2.16 states the rule; S7.10 anchors this id)
- [ ] [RFC3748-4-4] [MUST] Octets outside the range of the Length field MUST be ignored upon reception (S4, S4.1)
- [ ] [RFC3748-4.1-3] [MUST] A retransmitted Request MUST carry the same Identifier value, and a new (non-retransmission) Request MUST carry one different from the previous Request (S4.1)
- [ ] [RFC3748-4.1-4] [MUST] The Identifier field of a Response MUST match that of the currently outstanding Request (S4.1)
- [ ] [RFC3748-4.1-5] [MUST] The Type field of a Response MUST match that of the Request, or be a legacy or Expanded NAK (S4.1)
- [ ] [RFC3748-4.2-5] [MUST] Once the method completes unsuccessfully, the peer MUST terminate the conversation and indicate failure to the lower layer (S4.2)
- [ ] [RFC3748-4.2-6] [MUST NOT] Where the authenticator has sent no result indication, the peer MUST NOT silently discard the Success or Failure it waits for (S4.2)
- [ ] [RFC3748-4-5] [MUST] EAP packets carrying a Code outside 1-4 MUST be silently discarded by both authenticators and peers (S4)
- [ ] [RFC3748-4.2-7] [MUST] By default, an EAP peer MUST silently discard a "canned" Success packet, one sent immediately upon connection (S4.2)
- [ ] [RFC3748-4.2-8] [MUST] A peer receiving a Success or Failure packet where sending one is not explicitly permitted MUST silently discard it (S4.2)
- [ ] [RFC3748-4.2-9] [MUST] On the peer, after success result indications have been exchanged by both sides, a Failure packet MUST be silently discarded (S4.2)
- [ ] [RFC3748-2-3] [MUST NOT] The authenticator MUST NOT send a Success or Failure packet when retransmitting or when it fails to get a response from the peer (S2)
- [ ] [RFC3748-2.2-1] [MUST NOT] The Success, Failure, Nak Response and Notification Request/Response messages MUST NOT be used to carry data destined for delivery to other EAP methods (S2.2)
- [ ] [RFC3748-4.1-6] [MUST] Additional Request packets MUST be sent until a valid Response packet is received, an optional retry counter expires, or a lower layer failure indication is received (S4.1)
- [ ] [RFC3748-4.1-7] [MUST] The peer MUST send a Response packet in reply to a valid Request packet (S4.1)
- [ ] [RFC3748-4.1-8] [MUST] Requests MUST be processed in the order that they are received, and MUST be processed to their completion before inspecting the next Request (S4.1)
- [ ] [RFC3748-4.1-9] [MUST] A single Type MUST be specified for each EAP Request or Response (S4.1)
- [ ] [RFC3748-4.1-10] [MUST] An authenticator receiving a Response whose Identifier value does not match that of the currently outstanding Request MUST silently discard the Response (S4.1)
- [ ] [RFC3748-4.1-11] [MUST] An EAP server receiving a Response whose Type is neither the outstanding Request's nor a legacy Nak MUST silently discard it (S4.1)
- [ ] [RFC3748-2.1-4] [MUST] A peer receiving a Request of a Type other than the one under way MUST silently discard it, because an authenticator MUST NOT send a Request of a different Type before the method's final round completes (S2.1)
- [ ] [RFC3748-4.2-15] [MUST] The peer MUST silently discard a Success packet that arrives after the peer has ended the session (S4.2)
- [ ] [RFC3748-4.2-10] [MUST NOT] Success and Failure packets MUST NOT be sent by an EAP authenticator if the specification of the given method does not explicitly permit the method to finish at that point (S4.2)
- [ ] [RFC3748-4.2-11] [MUST] A peer MUST allow for the circumstance that a Success or Failure packet, being unacknowledged, can be lost (S4.2)
- [ ] [RFC3748-4.2-12] [MUST] After the authenticator sends a failure result indication to the peer, regardless of the response from the peer, it MUST subsequently send a Failure packet (S4.2)
- [ ] [RFC3748-4.2-13] [MUST] After the authenticator sends a success result indication to the peer and receives a success result indication from the peer, it MUST subsequently send a Success packet (S4.2)
- [ ] [RFC3748-4.2-14] [MUST] If the peer attempts to authenticate to the authenticator and fails to do so, the authenticator MUST send a Failure packet and MUST NOT grant access by sending a Success packet (S4.2)
- [ ] [RFC3748-7.10-5] [MUST NOT] The MSK and EMSK MUST NOT be used directly to protect data (S7.10)
- [ ] [RFC3748-7.10-6] [MUST] The EMSK MUST remain on the EAP peer and EAP server where it is derived, and MUST NOT be transported to, shared with, or used to derive keys for additional parties (S7.10)
- [ ] [RFC3748-7.10-7] [MUST] EAP peers, authenticators and authentication servers MUST be prepared for situations in which one of the parties discards the key state, which remains valid on another party (S7.10)
- [ ] [RFC3748-5-1] [MUST NOT] NAK (Type 3) and Expanded NAK (Type 254) MUST NOT be sent in a Request (S5)
- [ ] [RFC3748-5-2] [MUST] All EAP implementations MUST support Types 1-4, which are defined in this document (S5)
- [ ] [RFC3748-5.2-1] [MUST] The peer MUST respond to a Notification Request with a Notification Response, unless the EAP authentication method specification prohibits the use of Notification messages (S5.2)
- [ ] [RFC3748-5.2-2] [MUST NOT] A Nak Response MUST NOT be sent in response to a Notification Request (S5.2)
- [ ] [RFC3748-5.3.1-1] [MUST] Where a peer receives a Request for an unacceptable authentication Type (4-253,255), or a peer lacking support for Expanded Types receives a Request for Type 254, a Nak Response (Type 3) MUST be sent (S5.3.1, S5.7)
- [ ] [RFC3748-5.3.1-2] [MUST] The Type-Data field of the Nak Response (Type 3) MUST contain one or more octets indicating the desired authentication Type(s), one octet per Type, or the value zero (0) to indicate no proposed alternative (S5.3.1)
- [ ] [RFC3748-5.3.1-3] [MUST] The Identifier field of a legacy Nak Response MUST match the Identifier field of the Request packet that it is sent in response to (S5.3.1)
- [ ] [RFC3748-5.3.1-4] [MUST NOT] The legacy Nak MUST NOT be used as a general purpose error indication, such as for communication of error messages or negotiation of method-specific parameters (S5.3.1)
- [ ] [RFC3748-5.4-1] [MUST] A Response MUST be sent in reply to an MD5-Challenge Request (S5.4)
- [ ] [RFC3748-5.4-2] [MUST] EAP peer and EAP server implementations MUST support the MD5-Challenge mechanism (S5.4)
- [ ] [RFC3748-5.1-2] [MUST NOT] The Identity Response field MUST NOT be null terminated (S5.1)
- [ ] [RFC3748-7.5-1] [MUST] Where an EAP method employs a per-packet MIC, the peer and an authenticator not in pass-through mode MUST validate it (S7.5)
- [ ] [RFC3748-7.10-4] [MUST] EAP methods deriving keys MUST provide for mutual authentication between the EAP peer and the EAP server (S7.10)
- [ ] [RFC3748-5.1-1] [SHOULD] Identity (Type 1) SHOULD NOT be relied upon for authentication; it is sent in cleartext (S5.1, S7.1)
- [ ] [RFC3748-7.4-1] [SHOULD] EAP methods used in environments subject to MITM attacks SHOULD provide mutual authentication (S7.4)
- [ ] [RFC3748-4.2-3] [SHOULD] Peer SHOULD treat lower-layer success as implicit EAP Success if EAP Success is lost (S4.2)
- [ ] [RFC3748-2.3-2] [MAY] The authenticator MAY act as a pass-through, forwarding EAP to a back-end server via AAA (S2.3)
- [ ] [RFC3748-4-3] [MAY] Octets beyond the Length field MAY be ignored (S4)

Correction 2026-09-01: `RFC3748-7.10-3` was extracted at MUST NOT strength, cited S7.10 of
this document, and named IKEv2. Section 7.10 states no such rule. It states what a method
that derives keys owes, and it opens by making the derivation itself optional: "It is
possible for the peer and EAP server to mutually authenticate and derive keys." RFC 3748
mentions IKE in a lower-layer list and in two references, and in no obligation at all. The
rule the row carries comes from the carrier, RFC 7296 Section 2.16 at
`rfc/full/rfc7296.txt` line 2958, and that document states it as a SHOULD NOT: "EAP methods
that do not establish a shared key SHOULD NOT be used, as they are subject to a number of
man-in-the-middle attacks". The sentence after it says what an implementation does when
such a method IS used, which a MUST NOT would leave unreachable. Same requirement id,
corrected text, level and citation. The id keeps its S7.10 anchor, because an id is
permanent and is parsed from the section its line cites.

Ze acts on the corrected level rather than around it. `eap-md5` is an
`authentication { mode ... }` value an operator can select, it is never a default, and
adopting a configuration that names it writes one warning quoting the Section 2.16 sentence
(`warnKeylessEAPModes`, `internal/component/ike/engine/eap_auth.go`). RFC 7296 Section 2.16
then keys the AUTH payloads from SK_pi and SK_pr rather than from an MSK (`eapAuthSecret`,
same file).
