# RFC 7947 -- Internet Exchange BGP Route Server (short)

## Meta

| Field | Value |
|-------|-------|
| RFC | 7947 |
| Title | Internet Exchange BGP Route Server |
| Enrolment | enrolled |
| Enrolment reason | BGP Route Server: two MUST-level transparency requirements over the reactor-native RS forwarding path (internal/component/bgp/reactor/forward_rs.go), plus the advisory-level RFC7947-x-1, which the gate does not require but which is proven anyway. RFC7947-x-1 (SHOULD NOT prepend own AS for RS clients -- Section 2.2.2.1 calls it "a recommendation rather than a requirement", so it is not MUST-level and the gate does not require it) has both polarities: TestReactorForwardRSTransparent proves an RS client's forwarded body is byte-identical (no prepend), TestReactorForwardRSEBGPPrepend proves a non-RS eBGP peer DOES get the local AS prepended, so the no-prepend is confined to RS clients. RFC7947-x-2 (MUST NOT rewrite NEXT_HOP) is {single-polarity: positive}: the byte-identical transparency test proves it, and it has no natural negative, NEXT_HOP transparency not being RS-specific. RFC7947-x-3 is NOT MUST-level and the gate does not require it: Section 2.2.3 reads "this attribute SHOULD be propagated to other route server clients, and the route server SHOULD NOT modify its value", so the row is [SHOULD] and the earlier MUST here misquoted it. The same transparency test proves Ze meets it anyway: medPropagationAllowedTo (internal/component/bgp/reactor/forward_med.go) never applies the automatic RFC 4271 Section 5.1.4 strip to a route server client. An operator's own `del { med; }` policy still removes the metric upstream of that predicate (test/plugin/med-removal-configured.ci, against an rs-client receiver), which is Section 5.1.4's required removal mechanism rather than an exception to this recommendation. RFC7947-x-4 (per-client import/export policy applied on each redistribution) has both polarities: TestReactorForwardRSFallback proves an export-filtered client is routed to the per-client policy path, TestReactorForwardRSBasic proves an unfiltered client is forwarded directly. The x-5 SHOULD (path-hiding mitigation) and x-6 MAY (add-path) are not gated. |
| Support | bgp-base 230 |
| Support area | BGP Route Server |
| Support status | Supported |
| Support coverage | Transparent RS forwarding for RS clients by default: no AS_PATH prepend, no NEXT_HOP rewrite, MED preserved (verbatim wire forwarding); per-client import/export policy applied on redistribution. Sections 2.2.2.1 and 2.2.3 state the AS_PATH and MULTI_EXIT_DISC rules as recommendations, so an operator policy may override each. On the client side of the same document, Section 2.2.2.2 (RFC7947-2.2.2.2-1) asks that a leftmost-AS check be possible to disable; ze runs no ingress leftmost-AS check at all, so a route ze receives from a route server whose leftmost AS_PATH AS is another client's is accepted in every configuration (`LoopIngress`, `internal/component/bgp/reactor/filter/loop.go`). The walk that bounds this row against the RFC's own text is `rfc/extraction/rfc7947.json`. |
| Support remaining | Optional per-peer next-hop override and path-hiding mitigation (add-path) are operator-configured. A modify policy with `as-path-prepend` or `del { med; }` also changes what an RS client receives. Section 2.1 requires a route server to accept every UPDATE it receives from a client for inclusion in its Adj-RIB-In, and ze does not: `notifyMessageReceiver` (`internal/component/bgp/reactor/reactor_notify.go`) returns at the ordered ingress step loop's non-accept branch, ahead of the cache add, ahead of `reactorForwardRS` and ahead of the plugin dispatch, so an UPDATE a filter denied is absent from `show bgp adj-rib-in`. `LoopIngress` denies with no operator configuration, so the path is reachable by default. Thomas ruled on 2026-08-30 that ze stores such an UPDATE first and marks the entry filtered; that work is homed in `plan/immediate/spec-rfc7947-adj-rib-in-accepts-filtered-updates.md`, which reserves the id RFC7947-2.1-1, and site 2.1:1 of the extraction sign-off records the relocation. |

## Scope

Specifies the operation of a BGP route server (RS): a BGP speaker at an
Internet Exchange Point (IXP) that exchanges NLRI between participating
routers over a multilateral peering fabric, without itself being a transit
router. Complements RFC 7948 (operational requirements).

## Key Requirements

| Area | Behavior |
|------|----------|
| Forwarding model | Re-advertise routes received from each client to every other client, subject to per-client policy. The RS is not in the forwarding path. |
| AS_PATH | SHOULD NOT prepend its own AS, nor modify AS_PATH in any other way (Section 2.2.2.1). Client peerings appear as direct eBGP between clients, not transit via the RS. The RFC states this is a recommendation rather than a requirement, and that it deliberately differs from RFC 4271 Section 5.1.2, which REQUIRES the prepend; the weaker form exists only for backwards compatibility with clients that cannot accept a non-adjacent leftmost AS (Section 2.2.2.2). |
| NEXT_HOP | MUST NOT rewrite. Clients receive the original advertised NEXT_HOP (the advertising client's IP). |
| MED | SHOULD be propagated to other route server clients, and the RS SHOULD NOT modify its value (Section 2.2.3). The RFC states this contrary to RFC 4271 Section 5.1.4, which would otherwise have an external speaker strip a received metric. Like AS_PATH above, the obligation is a recommendation, not a requirement. |
| Client AS_PATH check | A route server client accepts an UPDATE whose leftmost AS_PATH AS is not the sending route server's AS (Section 2.2.2.2). Ze implements no such check on ingress, so there is none to allow to be disabled: `LoopIngress` (`internal/component/bgp/reactor/filter/loop.go`) is the whole ingress loop-detection surface and tests the local AS in AS_PATH, ORIGINATOR_ID and CLUSTER_LIST, none of them this equality. |
| Client policy | Per-client import/export policy applied on each redistribution. A route may be hidden from some clients and sent to others. |
| Path hiding | A naive RS hides alternative paths (single best-path per client-family). Mitigations: multi-RIB per client or add-path (RFC 7911). RFC 7947 flags path hiding as a known issue, not a protocol mandate. |
| Looping | Because AS_PATH is not prepended, loop detection across the RS relies on client AS numbers present elsewhere in the path. |
| Session model | One eBGP session per client pair (client -- RS). No client-to-client session. |

## Implementation Notes (Ze context)

- bgp-rs is the in-engine plugin implementing the forward-all model.
- AS_PATH non-prepending and NEXT_HOP preservation are enforced by default
  forwarding policy (`selectForwardTargets` + downstream filters).
- Path hiding is not yet mitigated in ze; add-path support exists at the
  session layer but RS path-hiding mitigations (per-client RIBs) are out
  of scope for the rs-fastpath umbrella.
- RFC 7947 places no normative requirement on forwarding throughput; the
  umbrella targets are driven by operational need, not by the RFC.

## References Consumed By Ze

- RFC 4271 -- base BGP-4 UPDATE processing.
- RFC 7911 -- ADD-PATH (capability already handled by engine).
- RFC 7948 -- companion operational document (not summarised here).

## Compliance Checklist

> Correction 2026-08-15: `RFC7947-x-3` was extracted at MUST strength. RFC 7947
> Section 2.2.3 states it as a recommendation: the attribute "SHOULD be propagated
> to other route server clients, and the route server SHOULD NOT modify its value".
> The stronger form was a misquote. Same requirement id, corrected text and level.
> The recommendation is stated against RFC 4271 Section 5.1.4, which otherwise has an
> external speaker discard a metric received from one neighboring AS rather than pass
> it to another. Section 5.1.4 separately REQUIRES a configured removal mechanism, so
> a route server that honors this recommendation by default and still lets an operator
> remove the metric explicitly satisfies both sections.

> Correction 2026-08-14: `RFC7947-x-3` read `single-polarity: positive` on the
> ground that no `mods.Op(4, ...)` existed in the reactor, so no MED-rewrite
> behavior could be asserted as a negative. One exists now. RFC 4271 Section
> 5.1.4 forbids relaying a received metric to another neighboring AS, and
> `applyFactsMED` (internal/component/bgp/reactor/forward_med.go) enforces it,
> with `medPropagationAllowedTo` gating the strip on the exact condition RFC 7947
> Section 2.2.3 exempts. The confining negative is therefore real: one UPDATE,
> two external receivers, the route server client keeping the metric and the
> ordinary eBGP neighbor losing it (test/plugin/med-not-propagated-across-as.ci).

> Correction 2026-07-23: `RFC7947-x-1` was previously extracted at MUST-NOT
> strength. RFC 7947 Section 2.2.2.1 says "SHOULD NOT" and states outright that
> it "is a recommendation rather than a requirement", existing only for backwards
> compatibility with clients that cannot accept a non-adjacent leftmost AS
> (Section 2.2.2.2). The stronger form was a misquote; same requirement id,
> corrected text and level.

- [ ] [RFC7947-x-1] [SHOULD NOT] Route server SHOULD NOT prepend its own AS to AS_PATH nor modify it in any other way, per RFC 7947 Section 2.2.2.1 (Key Requirements)
- [ ] [RFC7947-x-2] [MUST NOT] Route server must not rewrite NEXT_HOP (Key Requirements) {single-polarity: positive; NEXT_HOP transparency is not RS-specific -- every forwarded route preserves it by default (nhModeNone, internal/component/bgp/reactor/peer_forward_facts.go:147), so unlike x-1's AS-path prepend there is no "confined" negative where a comparable non-RS peer rewrites NEXT_HOP; the only rewrite is an explicit per-peer next-hop-self/explicit override, which exercises the override feature rather than the RS-transparency MUST-NOT. The positive is proven byte-identical in TestReactorForwardRSTransparent}
- [ ] [RFC7947-x-3] [SHOULD] MULTI_EXIT_DISC applied to an NLRI UPDATE sent to a route server SHOULD be propagated to other route server clients, and the route server SHOULD NOT modify its value, per RFC 7947 Section 2.2.3 (Key Requirements)
- [ ] [RFC7947-x-4] [MUST] Per-client import/export policy must be applied on each redistribution (Key Requirements)
- [ ] [RFC7947-x-5] [SHOULD] Route server should mitigate path hiding (e.g., multi-RIB per client or add-path) (Key Requirements)
- [ ] [RFC7947-x-6] [MAY] Route server may use ADD-PATH (RFC 7911) to distribute multiple paths to clients (Key Requirements)
- [ ] [RFC7947-2.2-1] [SHOULD NOT] A route server SHOULD NOT by default update the well-known or optional BGP attributes it receives from a route server client, and SHOULD pass them on unchanged to its other clients (S2.2)
- [ ] [RFC7947-2.2.2.2-1] [MUST] A route server client BGP implementation that has implemented the RFC 4271 Section 6.3 leftmost-AS check MUST allow that check to be disabled, so an UPDATE whose leftmost AS in AS_PATH is not the AS of the route server that sent it is accepted (S2.2.2.2)
