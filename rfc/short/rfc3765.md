# RFC 3765: NOPEER Community for BGP

**Status:** Informational
**Date:** April 2004
**Authors:** G. Huston

## Overview

Defines the NOPEER well-known community (0xFFFFFF04). Routes tagged with
this community should not be advertised to bilateral peers. Complements
NO_EXPORT (not outside confederation) and NO_ADVERTISE (not to any peer)
from RFC 1997 by adding a finer-grained control: suppress redistribution
to peers while still allowing propagation to customers and upstream transit.

## Well-Known Community

| Community | Value | Hex |
|-----------|-------|-----|
| NOPEER | 4294967044 | 0xFFFFFF04 |

## Semantics

A route carrying NOPEER:

| Relationship | Advertise? |
|--------------|------------|
| Customer (downstream) | Yes |
| Transit (upstream) | Yes |
| Bilateral peer | No |

The RFC defines "peer" narrowly as a bilateral peering relationship
(settlement-free interconnection), not as a generic BGP neighbor.
Routes may still be sent to customers (who pay for transit) and to
upstream providers.

## Constants

| Name | Value | Usage |
|------|-------|-------|
| NOPEER | 0xFFFFFF04 (4294967044) | Well-known advisory transitive community, registered by IANA with global significance (§4) |

## Key Requirements

RFC 3765 uses **no RFC 2119 keyword at any point** and carries no
"Specification of Requirements" boilerplate section. It describes its own
mechanism as an *advisory qualification*, and both §2 and §4 phrase the
behaviour as a permission ("permitting an AS not to readvertise"), never as an
obligation. What the RFC actually states:

- A route carrying NOPEER **may** be withheld from all external bilateral peer
  neighbour ASes. The RFC permits this; it does not require it.
- A receiver **may** additionally filter prefixes arriving across a session it
  regards as a bilateral peering session.
- NOPEER is a well-known **transitive** community, so it travels with the route.
  The obligation to pass an unrecognised transitive community through unchanged
  belongs to RFC 1997 (COMMUNITIES attribute), not to this document.
- NOPEER does not affect route selection, only redistribution.

## Use Cases

- Preventing route leaking at IXPs where bilateral peering occurs
- Allowing a multihomed customer to signal that certain prefixes should
  not traverse peering links
- Finer-grained control than NO_EXPORT when confederation boundaries
  do not align with peering boundaries

## Implementation Notes (Ze context)

- Constant `CommunityNoPeer` (0xFFFFFF04) defined in `attribute/community.go`
- Parsed as `nopeer` / `no-peer` / `no_peer` in `attribute/text.go`
- Displayed as `nopeer` in `attribute/text_append.go`
- The route server plugin (`bgp-rs`) does not currently enforce NOPEER
  semantics; the RS forwards to all clients regardless. This is correct
  per RFC 7947: the RS is not a peer in the bilateral sense.

## Related RFCs

| RFC | Relationship |
|-----|--------------|
| RFC 1997 | Defines COMMUNITIES attribute and NO_EXPORT/NO_ADVERTISE |
| RFC 7947 | IXP Route Server (RS is not a "peer" for NOPEER purposes) |

## No MUST-level obligation (declared, with evidence)

**This RFC contains zero gated (MUST / MUST NOT / SHALL / REQUIRED) obligations,
and that absence is a property of the document, not a gap in this summary.**
Evidence:

| Check | Result |
|-------|--------|
| Category | Informational (not Standards Track) |
| RFC 2119 boilerplate section | Absent -- the document never invokes RFC 2119 |
| Capitalised RFC 2119 keywords in `rfc/full/rfc3765.txt` | **0** occurrences of MUST, MUST NOT, SHALL, SHALL NOT, REQUIRED, SHOULD, SHOULD NOT, RECOMMENDED, MAY or OPTIONAL |
| How the document describes its own mechanism | "an advisory qualification to readvertisement of a route prefix" (§2, restated verbatim in §4) |
| Grammatical mood of the semantics | Permissive: "permitting an AS not to readvertise", "an AS may filter" (lowercase) |
| Wire format defined here | None -- NOPEER is a community value carried in RFC 1997's COMMUNITIES attribute |

No row below is upgraded to reach a non-zero gated count. The predecessor version of
this checklist recorded the core semantic as `[SHOULD NOT]`; the RFC text does not
support that level, so it is recorded at the permissive level the document actually
uses. The two rows are anchored to §2, whose text §4 repeats verbatim.

## Compliance Checklist

- [ ] [RFC3765-2-1] [MAY] The semantics of this attribute is to allow an AS to interpret the presence of this community as an advisory qualification to readvertisement of a route prefix, permitting an AS not to readvertise the route prefix to all external bilateral peer neighbour AS's (§2, §4)
- [ ] [RFC3765-2-2] [MAY] It is consistent with these semantics that an AS may filter received prefixes that are received across a peering session that the receiver regards as a bilateral peer sessions (§2, §4)
