# RFC 2349 - TFTP Timeout Interval and Transfer Size Options

## Meta

| Field | Value |
|-------|-------|
| RFC | 2349 |
| Title | TFTP Timeout Interval and Transfer Size Options |
| Status | Standards Track |
| Date | May 1998 |
| Author | G. Malkin (Bay Networks), A. Harkin (Hewlett Packard) |
| Updates | 1350 |
| Obsoletes | 1784 |

**Purpose:** Defines two TFTP options: "timeout" (retransmission interval) and
"tsize" (transfer size). Uses the OACK mechanism from RFC 2347.

**Scope:** Two independent option definitions.

## Timeout Option (Timeout Interval Option Specification section)

| Field | Value |
|-------|-------|
| Option name | `timeout` (case-insensitive) |
| Option value | ASCII decimal, seconds before retransmit |
| Valid range | 1 to 255, inclusive |

- Server's acknowledged value MUST match the client's requested value
  (no counter-proposal).

## Transfer Size Option (Transfer Size Option Specification section)

| Field | Value |
|-------|-------|
| Option name | `tsize` (case-insensitive) |
| Option value | ASCII decimal, file size in octets |

### Read Request behavior

- Client sends `tsize` with value `0` in the RRQ.
- Server determines the file size and returns it in the OACK.
- "In Read Request packets, a size of '0' is specified in the request
  and the size of the file, in octets, is returned in the OACK."
- If the file is too large for the client, it aborts with ERROR code 3
  (disk full / allocation exceeded).

### Write Request behavior

- Client sends `tsize` with the actual file size in the WRQ.
- Server echoes the size back in the OACK.
- If the file is too large for the server, it aborts with ERROR code 3.

## Wire Format in RRQ/WRQ

```
    2 bytes    string   1B   string   1B  string  1B  string  1B
    -------------------------------------------------------------
   | opc   | filename |  0 |  mode  |  0 | tsize |  0 |  size |  0 |
    -------------------------------------------------------------
```

## MUST Requirements

- Timeout: server's value MUST match client's value exactly.
- Tsize in RRQ: client's value MUST be "0"; server returns actual size.
- Tsize in WRQ: server's OACK value MUST echo client's value.

## Constants

| Name | Value | Usage |
|------|-------|-------|
| TIMEOUT_MIN | 1 | Minimum timeout in seconds |
| TIMEOUT_MAX | 255 | Maximum timeout in seconds |

## Pitfalls

- **tsize=0 is a query, not "zero-byte file":** In a Read Request,
  `tsize` value `0` means "tell me the file size." The server stats the
  file and returns the real size in the OACK. This is the standard UEFI
  PXE probe before download.

- **tsize requires stat, not transfer:** The server must be able to
  determine file size (e.g., via stat/fstat) without reading the entire
  file, since this is a pre-transfer query.

## Compliance Checklist

- [ ] [RFC2349-x-1] [MUST] Timeout: server's acknowledged value MUST match the client's requested value exactly (Timeout Interval Option Specification) {not-applicable: Ze does not implement the RFC 2349 timeout option. Its RRQ parser recognizes only blksize, tsize, and windowsize (internal/plugins/tftpserver/handler.go:121-131 parseRRQ); a requested timeout option falls through and is ignored per RFC 2347 (an unacknowledged option is treated as never requested, gated as RFC2347-x-3). Ze never places timeout in an OACK, so it has no code path that could acknowledge a mismatched timeout value.}
- [ ] [RFC2349-x-2] [MUST] Timeout valid range MUST be 1 to 255 seconds inclusive (Timeout Interval Option Specification) {not-applicable: Ze does not implement the timeout option (parseRRQ recognizes only blksize/tsize/windowsize, internal/plugins/tftpserver/handler.go:121-131), so it has no timeout value to range-check against 1-255 seconds.}
- [ ] [RFC2349-x-3] [MUST] Tsize in RRQ: client's value MUST be "0"; server returns actual file size in OACK (Transfer Size Option Specification)
- [ ] [RFC2349-x-4] [MUST] Tsize in WRQ: server's OACK value MUST echo the client's specified file size (Transfer Size Option Specification) {not-applicable: Ze is a read-only TFTP server: it rejects every write request (WRQ) with an Illegal-Operation error (internal/plugins/tftpserver/handler.go:226-227 "write not supported"; TestTFTPWriteRejected). With no WRQ transfer ever accepted, Ze has no code path that would echo a client-specified file size in a WRQ OACK.}
- [ ] [RFC2349-x-5] [MAY] If the file is too large for the client (RRQ), it MAY abort with ERROR code 3 (Transfer Size Option Specification)
- [ ] [RFC2349-x-6] [MAY] If the file is too large for the server (WRQ), it MAY abort with ERROR code 3 (Transfer Size Option Specification)
