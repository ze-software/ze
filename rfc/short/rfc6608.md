# RFC 6608: Subcodes for BGP Finite State Machine Error

## Meta

| Field | Value |
|-------|-------|
| RFC | 6608 |
| Title | Subcodes for BGP Finite State Machine Error |
| Status | Standards Track |
| Date | May 2012 |
| Updates | RFC 4271 |
| Errata | None |
| Enrolment | enrolled |
| Enrolment reason | Subcodes for BGP Finite State Machine Error: three MUSTs requiring a code-5 FSM Error NOTIFICATION with subcode 1/2/3 (plus the 1-octet message type) on an unexpected message in OpenSent/OpenConfirm/Established. All three are {gap}: ze defines and decodes the subcodes (message/notification.go:110-113, format/decode.go:316-320) but never originates them -- the reactor dispatches received messages by type with no FSM-state guard (reactor/session_read.go:260-273) and no path emits these subcodes. Disclosed in the docs/features/rfc-status.md RFC 6608 row (Partial). |
| Support | bgp-base 180 |
| Support area | BGP FSM Error subcodes |
| Support status | Partial |
| Support coverage | The RFC 6608 FSM Error subcodes (1 OpenSent, 2 OpenConfirm, 3 Established) are defined and decoded for display of a received NOTIFICATION. |
| Support remaining | Three MUST gaps, gated in `rfc/short/rfc6608.md`: ze does not originate a code-5 FSM Error NOTIFICATION with these subcodes on an unexpected message; the reactor dispatches received messages by type without an FSM-state guard. |

**Purpose:** Defines subcodes for the BGP FSM Error (Error Code 5) to provide more diagnostic information about which state received an unexpected message.

## Constants

### FSM Error Subcodes (Error Code 5)

| Value | Name | Section |
|-------|------|---------|
| 0 | Unspecified Error | 3 |
| 1 | Receive Unexpected Message in OpenSent State | 3 |
| 2 | Receive Unexpected Message in OpenConfirm State | 3 |
| 3 | Receive Unexpected Message in Established State | 3 |

### NOTIFICATION Data Field

When sending FSM Error with subcodes 1-3, the Data field contains:

```
 0
 0 1 2 3 4 5 6 7
+-+-+-+-+-+-+-+-+
| Message Type  |
+-+-+-+-+-+-+-+-+
```

| Field | Size | Type | Description |
|-------|------|------|-------------|
| Message Type | 1 octet | uint8 | Type of the unexpected message received |

## MUST Requirements

### Tx: Sending NOTIFICATION

**Section 4:**

> "If a BGP speaker receives an unexpected message (e.g., KEEPALIVE/UPDATE/ROUTE-REFRESH message) on a session in OpenSent state, it MUST send to the neighbor a NOTIFICATION message with the Error Code Finite State Machine Error and the Error Subcode 'Receive Unexpected Message in OpenSent State'."

> "If a BGP speaker receives an unexpected message (e.g., OPEN/UPDATE/ROUTE-REFRESH message) on a session in OpenConfirm state, it MUST send a NOTIFICATION message with the Error Code Finite State Machine Error and the Error Subcode 'Receive Unexpected Message in OpenConfirm State' to the neighbor."

> "If a BGP speaker receives an unexpected message (e.g., OPEN message) on a session in Established State, it MUST send to the neighbor a NOTIFICATION message with the Error Code Finite State Machine Error and the Error Subcode 'Receive Unexpected Message in Established State'."

## Error Handling

| State | Unexpected Messages | Subcode | Data |
|-------|---------------------|---------|------|
| OpenSent | KEEPALIVE, UPDATE, ROUTE-REFRESH | 1 | Message type |
| OpenConfirm | OPEN, UPDATE, ROUTE-REFRESH | 2 | Message type |
| Established | OPEN | 3 | Message type |

**Note:** "Unexpected" means messages not valid for that FSM state per RFC 4271.

## State Machine Integration

| Current State | Valid Messages | Unexpected → Subcode |
|---------------|----------------|----------------------|
| OpenSent | OPEN, NOTIFICATION | All others → 1 |
| OpenConfirm | KEEPALIVE, NOTIFICATION | OPEN, UPDATE, ROUTE-REFRESH → 2 |
| Established | KEEPALIVE, UPDATE, NOTIFICATION, ROUTE-REFRESH | OPEN → 3 |

## Security Considerations

**Section 5:**

> "Specification, implementation, and deployment of the proposed BGP FSM Error subcodes could make BGP implementation fingerprinting easier and probably more accurate."

Operators should consider this when deciding whether to implement detailed subcodes.

## Implementation Notes

1. **Subcode 0 (Unspecified):** Use when the specific condition doesn't match subcodes 1-3, or for backwards compatibility
2. **Data field:** Always include the 1-octet message type for subcodes 1-3
3. **Message type values:** Per RFC 4271: OPEN=1, UPDATE=2, NOTIFICATION=3, KEEPALIVE=4, ROUTE-REFRESH=5

## Compliance Checklist

- [ ] [RFC6608-4-1] [MUST] On receiving an unexpected message in OpenSent state, MUST send NOTIFICATION with Error Code 5 (FSM Error) and Subcode 1 ("Receive Unexpected Message in OpenSent State"), with Data field containing the 1-octet message type (Section 4) {gap: ze defines and decodes the RFC 6608 subcodes (internal/component/bgp/message/notification.go:110-113, internal/component/bgp/format/decode.go:316-320) but never sends them -- the reactor dispatches received messages by type with no FSM-state guard (internal/component/bgp/reactor/session_read.go:260-273), and no code path emits a code-5 FSM Error NOTIFICATION with subcode 1 on an unexpected message in OpenSent. Disclosed in the docs/features/rfc-status.md RFC 6608 row}
- [ ] [RFC6608-4-2] [MUST] On receiving an unexpected message in OpenConfirm state, MUST send NOTIFICATION with Error Code 5 (FSM Error) and Subcode 2 ("Receive Unexpected Message in OpenConfirm State"), with Data field containing the 1-octet message type (Section 4) {gap: same root cause as 4-1 -- ze decodes the RFC 6608 subcodes but no reactor path sends a code-5 FSM Error NOTIFICATION with subcode 2 on an unexpected message in OpenConfirm (session_read.go:260-273). Disclosed in the docs/features/rfc-status.md RFC 6608 row}
- [ ] [RFC6608-4-3] [MUST] On receiving an unexpected message in Established state, MUST send NOTIFICATION with Error Code 5 (FSM Error) and Subcode 3 ("Receive Unexpected Message in Established State"), with Data field containing the 1-octet message type (Section 4) {gap: same root cause as 4-1 -- ze decodes the RFC 6608 subcodes but no reactor path sends a code-5 FSM Error NOTIFICATION with subcode 3 on an unexpected message in Established (session_read.go:260-273). Disclosed in the docs/features/rfc-status.md RFC 6608 row}
