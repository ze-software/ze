# RFC 1877 - PPP Internet Protocol Control Protocol Extensions for Name Server Addresses

## Meta

| Field | Value |
|-------|-------|
| RFC | 1877 |
| Title | PPP Internet Protocol Control Protocol Extensions for Name Server Addresses |
| Status | Informational |
| Date | December 1995 |
| Author | S. Cobb |
| Obsoletes | - |
| Updates | - (informationally extends RFC 1332; does not formally update it) |
| Updated by | - |
| Enrolment | enrolled |
| Enrolment reason | PPP IPCP Extensions for Name Server Addresses (DNS options): four MUST-level requirements. RFC1877-x-1 (link usable for IPv4 with or without DNS) both polarities via existing tests: TestIPCPDNSRejectAbsorbed (a peer Configure-Reject of the DNS options is absorbed and the session survives, still negotiating the IPv4 address) and TestIPCPIPAddressRejectIsFatal (rejecting the IP-Address IS fatal, so DNS not the address is the optional part). RFC1877-x-3 (Configure-Ack echoes the option Data verbatim when acceptable) both polarities via TestRFC1877ConfigureAckEchoesAcceptable: an acceptable request is Acked byte-for-byte (sendNCPConfigureAck writes req.Data unchanged, ncp.go:567) while an unacceptable IP-Address draws a Nak not an Ack. RFC1877-x-4 (Configure-Reject echoes the offending unsupported option) both polarities via TestRFC1877ConfigureRejectEchoesUnsupportedOnly: copyUnknownOptions (ncp.go:619) echoes an unknown type-99 option verbatim and skips the recognized IP-Address option. RFC1877-x-2 (option with Length other than 6 must be Configure-Rejected) is {gap}: Ze validates the length (errIPCPBadOptionLen) but answers a bad-length known-type DNS option with a Configure-Nak rather than a Configure-Reject (its reject path keys on unknown option type, not length); disclosed in the docs/features/rfc-status.md RFC 1877 row. The x-5 MAY is not gated. |
| Support | access-aaa 70 |
| Support area | IPCP DNS options |
| Support status | Partial |
| Support coverage | Primary and secondary DNS option parsing and negotiation; the Configure-Ack echoes acceptable options verbatim, the Configure-Reject echoes unsupported ones, and the IPv4 link stays usable with or without DNS. Tests bound per requirement in `rfc/requirements/rfc1877.md`. |
| Support remaining | Carries the L2TP and PPPoE Partial status. One MUST gap gated in `rfc/short/rfc1877.md`: a DNS option with a Length other than 6 is answered with a Configure-Nak (correcting the value) rather than a Configure-Reject, because Ze's reject path is keyed on unknown option TYPE, not option length. |

**Purpose:** Adds four optional IPCP Configuration Options that let the
peer learn addresses for the primary and secondary DNS and NBNS
(NetBIOS Name Server) servers it should use while the PPP link is up.

**Scope:** Only four option types are defined. They do not alter the
IPCP packet format, do not change the state machine, and are
informational in the sense that either side may ignore them by
Configure-Reject; the link MUST still be usable for IPv4 traffic with
or without DNS assignment.

## Relationship to IPCP

RFC 1877 rides entirely on top of RFC 1332's IPCP. It reuses the LCP
packet shape (Code, Identifier, Length, Data), the IPCP state machine,
and the IPCP Protocol field `0x8021`. Only the Data field (the option
list inside a Configure-Request / Ack / Nak / Reject) carries new type
values.

## Configuration Options

Each option has the standard TLV shape: one-byte Type, one-byte
Length (including the TLV header), four-byte IPv4 address.

| Type | Length | Name |
|------|--------|------|
| 129 | 6 | Primary-DNS-Address |
| 130 | 6 | Primary-NBNS-Address |
| 131 | 6 | Secondary-DNS-Address |
| 132 | 6 | Secondary-NBNS-Address |

A length value other than 6 is a malformed option; the receiver MUST
Configure-Reject it.

## Negotiation Semantics

The negotiation pattern mirrors the IP-Address option from RFC 1332:

| Request value | Meaning |
|---------------|---------|
| `0.0.0.0` | "I do not know this server; please tell me" (client request) |
| Non-zero | "I believe this is the server address" |

The peer responds:

| Condition | Response |
|-----------|----------|
| Requested value is `0.0.0.0` and we have a value to suggest | Configure-Nak carrying our suggestion |
| Requested value is non-zero and we accept it | Configure-Ack (echo the Data verbatim) |
| Requested value is non-zero and we disagree | Configure-Nak carrying our counter-proposal |
| We do not support the option | Configure-Reject (echo the offending option) |

## How Ze Uses These Options

Ze runs as an LNS (Section 1.1 of RFC 5072, server role). On session
start the NCP coordinator emits `EventIPRequest{Family: ipv4}` on the
IPEventsOut channel; the external IP handler (l2tp-pool plugin in
production) returns `Local`, `Peer`, `DNSPrimary`, `DNSSecondary` in
its `IPResponse`. Ze then builds its initial IPCP Configure-Request
with:

- IP-Address = `Local` (ze's PPP-link address, RFC 1332 Section 3.3)
- Primary-DNS-Address = `DNSPrimary` if non-zero
- Secondary-DNS-Address = `DNSSecondary` if non-zero

Ze does NOT implement options 130 / 132 (NBNS). NBNS is a
Microsoft-specific service that modern clients ignore; the handoff
from the peer to a DHCP-based NBNS discovery or a dedicated `nmbd`
lookup is the operator's responsibility.

If the peer Configure-Rejects Primary-DNS-Address or Secondary-DNS-
Address, ze clears the corresponding field and omits the option from
its next Configure-Request. Configure-Reject of IP-Address (RFC 1332
type 3) is fatal because it removes the only option ze needs to
negotiate (spec-l2tp-6c-ncp AC-16).

## Security Considerations

RFC 1877 Section 3 observes that because IPCP is not authenticated,
the DNS addresses carried in these options are as trustworthy as the
PPP peer. Ze assumes the peer is a subscriber CPE that will forward
user DNS queries, so a malicious peer can only harm its own user; a
malicious LAC could push a hostile resolver on ze, which is why ze's
`l2tp-pool` plugin is the single source of DNS values and the reactor
ignores any peer-originated Primary/Secondary-DNS Naks that conflict
with policy.

## Source Files in Ze

<!-- source: internal/component/l2tp/ppp/ipcp.go — IPCPOptIPAddress, IPCPOptPrimaryDNS, IPCPOptSecondaryDNS -->
<!-- source: internal/component/l2tp/ppp/ipcp.go — ParseIPCPOptions, WriteIPCPOptions -->
<!-- source: internal/component/l2tp/ppp/ncp.go — runNCPPhase, handleIPCPPacket, absorbIPCPReject -->

## Compliance Checklist

- [ ] [RFC1877-x-1] [MUST] Link must still be usable for IPv4 traffic with or without DNS assignment (Scope)
- [ ] [RFC1877-x-2] [MUST] Option with Length other than 6 must be Configure-Rejected (Configuration Options) {gap: A DNS option (Primary/Secondary DNS, type 129/131) with a Length other than 6 is validated by parseIPCPv4Option (internal/component/l2tp/ppp/ipcp.go:100-102, errIPCPBadOptionLen) and flags the Configure-Request as bad in evalIPCPRequest (internal/component/l2tp/ppp/ncp.go:397-400), but Ze responds with a Configure-Nak carrying its own DNS values rather than a Configure-Reject of the malformed option. buildNakOrReject (ncp.go:586-601) takes the Reject branch only for UNKNOWN option TYPES: ipcpHasUnknownOption (ipcp.go:142-158) checks the type, not the length, so a known-type option with a bad length falls to the Nak branch. Disclosed in docs/features/rfc-status.md}
- [ ] [RFC1877-x-3] [MUST] Configure-Ack must echo the option Data verbatim when value is acceptable (Negotiation Semantics)
- [ ] [RFC1877-x-4] [MUST] Configure-Reject must echo the offending option when option is not supported (Negotiation Semantics)
- [ ] [RFC1877-x-5] [MAY] Either side may ignore DNS/NBNS options by Configure-Reject (Scope)
