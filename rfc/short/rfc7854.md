# RFC 7854 -- BGP Monitoring Protocol (BMP)

## Meta

| Field | Value |
|-------|-------|
| RFC | 7854 |
| Title | BGP Monitoring Protocol (BMP) |
| Enrolment | enrolled |
| Enrolment reason | Ze implements the BMP sender and receiver under internal/component/bgp/plugins/bmp. Wire-message and session carriers cover the common and per-peer headers, OPEN exchange, current per-peer replay and completion, identity, ordered information, termination and statistics. Requirement coverage and discrimination remain subject to the current gate result. |
| Implementation | ze |
| Implementation reason | Ze's own Go implements this document; each requirement row cites its producer. |
| Support | bgp-operations 10 |
| Support area | BMP v3 |
| Support status | Partial |
| Support coverage | BMP receiver and sender, peer lifecycle, current per-NLRI replay with per-family End-of-RIB, received and sent route monitoring, CLI and configuration. Initiation carries the administrative system identity; receiver information strings retain order and duplicates. Sender shutdown carries Reason 0 and closes; bounded transmit queues reset stalled sessions. |
| Support remaining | Focused execution, producer discrimination and third-party pmacct acceptance of the current changes remain unrun. Loc-RIB monitoring is defined under RFC 9069 and Adj-RIB-Out under RFC 8671. |

## Purpose

BMP enables routers to stream BGP routing state to external monitoring
stations over TCP. Unidirectional: router to collector. Covers Adj-RIB-In,
peer state changes, and statistics.

## Wire Format

### Common Header (6 bytes, all messages)

| Offset | Size | Field |
|--------|------|-------|
| 0 | 1 | Version (must be 3) |
| 1 | 4 | Message Length (total including header) |
| 5 | 1 | Message Type (0-6) |

### Per-Peer Header (42 bytes, types 0-3 and 6)

| Offset | Size | Field |
|--------|------|-------|
| 0 | 1 | Peer Type: 0=Global, 1=L3VPN, 2=Local, 3=Loc-RIB |
| 1 | 1 | Flags: bit7=V(IPv6), bit6=L(post-policy), bit5=A(2-byte-AS), bit4=O(Adj-RIB-Out) |
| 2 | 8 | Peer Distinguisher (RD for VRF peers) |
| 10 | 16 | Peer Address (IPv4 in final four bytes, first twelve zero) |
| 26 | 4 | Peer AS (always 4-byte) |
| 30 | 4 | Peer BGP ID |
| 34 | 4 | Timestamp seconds |
| 38 | 4 | Timestamp microseconds |

### TLV Format (4-byte header)

Type (2) + Length (2) + Value (variable).

## Message Types

| Type | Name | Per-Peer Header | Content |
|------|------|-----------------|---------|
| 0 | Route Monitoring | Yes | BGP UPDATE message |
| 1 | Statistics Report | Yes | Count (4) + stat TLVs |
| 2 | Peer Down | Yes | Reason (1) + optional NOTIFICATION/FSM |
| 3 | Peer Up | Yes | Local addr (16) + ports (4) + sent OPEN + recv OPEN + optional TLVs |
| 4 | Initiation | No | TLVs: sysName (2), sysDescr (1), string (0) |
| 5 | Termination | No | Optional string (0), required two-byte reason (1) |
| 6 | Route Mirroring | Yes | BGP message (0, last), Information (1: errored PDU or messages lost) |

## Peer Down Reasons

| Code | Meaning | Data |
|------|---------|------|
| 1 | Local closed, NOTIFICATION sent | BGP NOTIFICATION PDU |
| 2 | Local closed, no NOTIFICATION | 2-byte FSM event code |
| 3 | Remote closed, NOTIFICATION recv | BGP NOTIFICATION PDU |
| 4 | Remote closed, no data | None |
| 5 | Peer de-configured | None |

## Statistics Types (Section 4.8)

Types 0-13 are defined. Types 0-6 are 32-bit counters for policy rejections,
duplicates, and loop detection. Types 7-8 are 64-bit Adj-RIB-In and Loc-RIB route
counts. Types 9-10 are per-AFI/SAFI variants (11-byte: AFI(2)+SAFI(1)+value(8)).
Types 11-13 cover treat-as-withdraw and duplicate updates.

## Session Lifecycle

1. TCP connection established (IANA port 11019)
2. Initiation message with sysName/sysDescr
3. For each established peer: Peer Up with OPEN messages
4. Route Monitoring: current per-peer RIB dump, per-family End-of-RIB, then incremental updates
5. Periodic Statistics Reports
6. Peer Down on session loss
7. Termination on BMP session close

## Reconnection

RFC suggests minimum 30 seconds, maximum 720 seconds between reconnection
attempts with exponential backoff.

## Related RFCs

- RFC 8671: Adj-RIB-Out support (O flag in per-peer header)
- RFC 9069: Local RIB support (peer type 3, F flag)
- RFC 9736: Peer Up TLV namespace clarification

## Ze Implementation

- Receiver: `internal/component/bgp/plugins/bmp/` -- accepts TCP, parses all 7 types
- Sender: same package -- connects to collectors, streams peer state + updates
- YANG config: `ze-bmp-conf.yang` under `bgp { bmp { receiver { ... } sender { ... } } }`

## Compliance Checklist

- [ ] [RFC7854-x-1] [MUST] Common Header Version field must be 3 (Wire Format)
- [ ] [RFC7854-x-2] [MUST] Common Header must be 6 bytes: Version (1) + Message Length (4) + Message Type (1) (Wire Format)
- [ ] [RFC7854-x-3] [MUST] Per-Peer Header must be present for message types 0-3 and 6 (Wire Format)
- [ ] [RFC7854-x-4] [MUST] Peer AS field must always be encoded as 4-byte AS number (Wire Format) {single-polarity: positive; the Peer AS is unconditionally a 4-octet field on every encode and decode, so there is no shorter-AS variant to reject and no negative case to construct}
- [ ] [RFC7854-x-5] [MUST] IPv4 Peer Address occupies the least significant 4 bytes of the 16-byte field, "with the 12 most significant bytes zero-filled" (§4.2, Per-Peer Header)
- [ ] [RFC7854-x-6] [MUST] Initiation message must be sent immediately after TCP connection establishment (Session Lifecycle) {single-polarity: positive; the sender always emits Initiation as the first message on a fresh connection, so there is no valid session in which another message precedes it to reject}
- [ ] [RFC7854-x-7] [MUST] Initiation message must include sysName TLV (type 2) (Session Lifecycle) {single-polarity: positive; the Initiation the sender builds always includes the sysName TLV, so there is no valid Initiation omitting it to assert against}
- [ ] [RFC7854-x-8] [MUST] Peer Up message must include both sent and received OPEN messages (Message Types)
- [ ] [RFC7854-x-9] [MUST] Route Monitoring messages must contain a BGP UPDATE message (Message Types) {single-polarity: positive; Route Monitoring is only ever constructed around a complete BGP UPDATE PDU, so there is no valid Route Monitoring lacking one to reject}
- [ ] [RFC7854-x-10] [MUST] Peer Down must include the reason code (1 byte) (Message Types) {single-polarity: positive; Peer Down is always written with a reason byte, so there is no valid Peer Down without one to assert against}
- [ ] [RFC7854-x-11] [MAY] Termination message may be sent before the BMP session is closed: "The router MAY send a Termination message prior to closing the session." (Session Lifecycle) {single-polarity: positive; Termination is produced unconditionally when the session is torn down, so there is no valid shutdown that omits it to reject}
- [ ] [RFC7854-x-12] [MUST] BMP is unidirectional: router to collector only (Session Lifecycle) {single-polarity: positive; the receiver loop (internal/component/bgp/plugins/bmp/bmp.go:441-492) issues only reads and the sender hold-loop (sender.go:207-237) reads only to detect close, so neither role writes toward the monitored router on a valid session and there is no reject case to construct}
- [ ] [RFC7854-4.9-1] [MUST] Peer Down must carry the Data field when the Reason is 1, 2 or 3: the BGP NOTIFICATION PDU for reason 1 and reason 3, and the 2-byte FSM event code for reason 2 (§4.9, Peer Down Notification)
- [ ] [RFC7854-4.5-2] [MUST] The monitoring station must close the TCP session after receiving a termination message (§4.5, Termination Message)
- [ ] [RFC7854-x-13] [SHOULD] Minimum 30 seconds between reconnection attempts (Reconnection)
- [ ] [RFC7854-x-14] [SHOULD] Maximum 720 seconds between reconnection attempts with exponential backoff (Reconnection)
- [ ] [RFC7854-x-15] [MAY] Statistics Reports may be sent periodically: "It MAY periodically send Stats Reports or even new Initiation messages, according to configuration." The Stats Reports text imposes no timing either: "This specification does not impose any timing restrictions on when and on what event these reports have to be transmitted." (Session Lifecycle)
- [ ] [RFC7854-x-16] [SHOULD] Peer Up message should be sent for each established peer (Session Lifecycle)
- [ ] [RFC7854-x-17] [SHOULD] Initial RIB dump via Route Monitoring should follow Peer Up (Session Lifecycle)
- [ ] [RFC7854-x-18] [MUST] Initiation message must include the sysDescr TLV (type 1): "The sysDescr and sysName Information TLVs MUST be sent, any others are optional." (Message Types)
- [ ] [RFC7854-x-19] [MAY] Peer Up message may include optional TLVs (Message Types)
- [ ] [RFC7854-x-20] [MAY] Route Mirroring messages may be used to mirror BGP messages verbatim (Message Types)
- [ ] [RFC7854-3.2-1] [MUST] Retries of a failed connection must be subject to some variety of backoff: "Retries MUST be subject to some variety of backoff." (§3.2, Connection Establishment and Termination)
- [ ] [RFC7854-3.2-2] [MUST] The router must restrict the rate at which BMP sessions may be established: "The router MUST also restrict the rate at which sessions may be established." (§3.2, Connection Establishment and Termination)
- [ ] [RFC7854-3.3-1] [MUST] Once all the routes for a given peer have been sent, an End-of-RIB message must be sent for that peer: "Once it has sent all the routes for a given peer, it MUST send an End-of-RIB message for that peer" (§3.3, Lifecycle of a BMP Session, and restated in the Route Monitoring section)
- [ ] [RFC7854-4.1-1] [MUST] Unrecognized message types must be ignored on receipt: "A BMP implementation MUST ignore unrecognized message types upon receipt." (§4.1, Common Header)
- [ ] [RFC7854-4.2-1] [MUST] The reserved per-peer flag bits must be transmitted as 0 and their values ignored on receipt: "They MUST be transmitted as 0 and their values MUST be ignored on receipt." (§4.2, Per-Peer Header)
- [ ] [RFC7854-4.3-1] [MAY] The string TLV may be included multiple times in an Initiation message: "The string TLV MAY be included multiple times." (§4.3, Initiation Message)
- [ ] [RFC7854-4.4-1] [MUST] When multiple strings are included in a string TLV, their ordering must be preserved when they are reported: "If multiple strings are included, their ordering MUST be preserved when they are reported." (§4.4, Information TLV)
- [ ] [RFC7854-4.4-2] [MUST] The sysDescr TLV Information field must equal the MIB-II sysDescr object: "The Information field contains an ASCII string whose value MUST be set to be equal to the value of the sysDescr MIB-II [RFC1213] object." (§4.4, Information TLV)
- [ ] [RFC7854-4.4-3] [MUST] The sysName TLV Information field must equal the MIB-II sysName object: "The Information field contains an ASCII string whose value MUST be set to be equal to the value of the sysName MIB-II [RFC1213] object." (§4.4, Information TLV)
- [ ] [RFC7854-4.5-1] [MUST] After sending a termination message the router must close the TCP session and send no further messages: "Once the router has sent a termination message, it MUST close the TCP session without sending any further messages." (§4.5, Termination Message)
- [ ] [RFC7854-4.5-3] [MUST] A Termination message must carry the Reason TLV (type 1): "Inclusion of this TLV is REQUIRED." (§4.5, Termination Message)
- [ ] [RFC7854-4.7-1] [MUST] A BGP Message TLV in a Route Mirroring message must occur last in the list of TLVs: "If the BGP Message TLV occurs in the Route Mirroring message, it MUST occur last in the list of TLVs." (§4.7, Route Mirroring)
- [ ] [RFC7854-4.7-2] [MUST] A Route Mirroring message carrying Information code 0 (Errored PDU) must also carry a BGP Message TLV: "A BGP Message TLV MUST also occur in the TLV list." (§4.7, Route Mirroring)
- [ ] [RFC7854-4.8-1] [MUST] Unrecognized stat types and unexpected Stat Data must be ignored on receipt: "A BMP implementation MUST ignore unrecognized stat types on receipt, and likewise MUST ignore unexpected data in the Stat Data field." (§4.8, Stats Reports)
- [ ] [RFC7854-4.8-2] [MUST] A transmitted Stats Report must carry at least one statistic: "However, if an SR message is transmitted, at least one statistic MUST be carried in it." (§4.8, Stats Reports)
- [ ] [RFC7854-5-1] [MUST] Pre-policy routes must have the L flag clear and post-policy routes must have it set: "Pre-policy routes MUST have their L flag clear in the BMP header (see Section 4), post-policy routes MUST have their L flag set." (§5, Route Monitoring)
- [ ] [RFC7854-5-2] [MUST] Where the time a route was installed is not available, the BMP Timestamp field must be set to 0: "Otherwise, the BMP Timestamp field MUST be set to 0, indicating that time is not available." (§5, Route Monitoring)
- [ ] [RFC7854-5-3] [MUST] A withdraw must carry the L flag of the announcement it withdraws, and must be sent twice where the route was announced both pre-policy and post-policy: "The withdraw MUST have its L flag set to correspond to that of any previous announcement; if the route in question was previously announced with L flag both clear and set, the withdraw MUST similarly be sent twice, with L flag clear and set." (§5, Route Monitoring)
- [ ] [RFC7854-8.2-1] [MUST] Where no transport session exists, the Local Port and Remote Port fields of a Peer Up message must be set to 0: "Since in this case no transport session actually exists, the Local and Remote Port fields of the Peer Up message MUST be set to 0." (§8.2, Peer Up Notification)
