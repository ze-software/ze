# RFC 8707 - Resource Indicators for OAuth 2.0

## Meta

| Field | Value |
|-------|-------|
| RFC | 8707 |
| Title | Resource Indicators for OAuth 2.0 |
| Status | Proposed Standard |
| Date | February 2020 |
| Authors | B. Campbell, J. Bradley, H. Tschofenig |
| Related | RFC 9728 (resource metadata), RFC 6749 (OAuth 2.0 core) |
| Enrolment | enrolled |
| Enrolment reason | Thomas selected "Keep present OAuth roles" on 2026-09-21: MCP resource server and external-AS metadata consumer, without an AS, protected-resource discovery client, response signing or signed metadata. JWT audience comparison is enforced from RFC 7519 §§2 and 4.1.3. The historical RFC8707-5-1 and RFC8707-5-2 identifiers retain their tests but explicitly cite that actual source; RFC 8707 §5 is IANA considerations and states neither obligation. |
| Implementation | ze |
| Implementation reason | Ze's own Go implements this document; each requirement row cites its producer. |
| Support | - |

**Purpose:** Adds a `resource` parameter to OAuth authorization and token requests. Section 2 recommends audience-restricted tokens and permits the authorization server to map a resource value to a broader URI or abstract identifier.

**Scope:** Partial implementation. No authorization/token request producer or
AS token issuer is offered under the 2026-09-21 scope decision. Local audience
verification remains implemented and testable; its retained historical IDs are
not evidence of normative text in RFC 8707 §5.

The absolute-URI constraint is recorded as RFC8707-2-6. Producing OAuth
authorization or token requests remains an absent feature; the source mapping
does not claim that Ze implements or has verified that request parameter.

## Client Behavior (Section 2)

| Requirement | Item |
|-------------|------|
| `resource` parameter on authorization request | An absolute URI identifying the target resource. |
| MAY appear on token endpoint | When present, the AS uses it to select the audience. |
| Multiple `resource` values | MAY appear; AS policy decides. |

## Authorization Server Behavior (Sections 2 and 2.1)

| Requirement | Item |
|-------------|------|
| Validate `resource` against registered metadata | AS decides which values it issues tokens for. |
| Set the token audience | The AS SHOULD audience-restrict tokens to the indicated resources (§2). |
| Reject unacceptable resources | Section 2.1 recommends the `invalid_target` error. |

## Resource Server Behavior (RFC 7519 Sections 2 and 4.1.3)

This is the rule the MCP resource server enforces:

| Requirement | Item |
|-------------|------|
| Verify `aud` on every token | Reject a token whose audience does not identify this resource server. |
| String equality | Compare case-sensitive strings with no transformations or canonicalization (RFC 7519 §2). |
| Multiple audiences | RFC 7519 §4.1.3 permits a string or an array of strings. At least one must identify this resource server. |

RFC 8707 does not define URL canonicalization for JWT audience comparison.
Distinct trailing slashes, queries, escaped path segments and Unicode spellings
remain distinct audience identifiers. Section 2 prohibits fragments in the
`resource` request parameter and recommends omitting queries, but explicitly
allows useful query components.

## Ze Implementation

- Audience comparison: `internal/component/mcp/jwt.go` `audClaim.Matches` exact-match on string or array-form `aud`.
- Operator config: `environment.mcp.oauth.audience` is the exact audience identifier. It must be set explicitly and never comes from `r.Host`.
- Rejection path: `oauthAuthenticator.mapVerifyError` maps `errJWTAudienceMismatch` to `error="invalid_token", error_description="invalid audience"` per RFC 6750 vocabulary.

## Key Security Property

Audience restriction prevents a token intended only for resource A from being
accepted by resource B. RFC 8707 communicates requested resource targets to an
authorization server; RFC 7519 defines how a JWT recipient checks `aud`. A JWT
audience can be assigned and checked even when no RFC 8707 `resource` request
parameter was used.

## Section Index

| Section | Topic |
|---------|-------|
| 1 | Introduction |
| 2 | Resource Parameter |
| 2.1 | Authorization Request |
| 2.2 | Access Token Request |
| 3 | Security Considerations |
| 4 | Privacy Considerations |
| 5 | IANA Considerations |
| 6 | References |

## Compliance Checklist

- [ ] [RFC8707-3-1] [SHOULD] The authorization server SHOULD audience-restrict issued access tokens to the resource(s) indicated by the resource parameter (RFC 8707 §2; historical section-3 ID) {not-applicable: §2 explicitly addresses "The authorization server". Thomas excluded the AS role on 2026-09-21. internal/component/mcp/streamable_auth.go::buildAuthForMode constructs a verifier for external tokens; internal/component/mcp/streamable.go::ServeHTTP has no token-issuance endpoint}
- [ ] [RFC8707-3-2] [SHOULD] If the authorization server cannot parse the provided resource value(s) or does not consider them acceptable, it should reject the request with the error code invalid_target (RFC 8707 §2.1; historical section-3 ID; lowercase should in the source) {not-applicable: §2.1 addresses the authorization server's response to the resource request parameter. Thomas excluded the AS role on 2026-09-21. internal/component/mcp/streamable.go::ServeHTTP publishes no authorization or token endpoint; internal/component/mcp/streamable_auth.go::buildAuthForMode verifies external tokens without processing an OAuth resource request parameter}
- [ ] [RFC8707-5-1] [MUST] A principal processing a JWT whose `aud` is present MUST reject the JWT if no audience value identifies that principal (actual source: RFC 7519 §4.1.3, with exact StringOrURI comparison from §2; historical ID retained, not an RFC 8707 §5 requirement)
- [ ] [RFC8707-5-2] [MUST] Each principal intended to process the JWT MUST identify itself with a value in `aud`; the general form is an array of case-sensitive StringOrURI values, and the one-audience form MAY be a string (actual source: RFC 7519 §4.1.3; historical ID retained, not an RFC 8707 §5 requirement)
- [ ] [RFC8707-2-1] [MUST] The `resource` request parameter URI MUST NOT include a fragment component (§2) {feature-declined: "In requests to the authorization server, a client MAY indicate the protected resource (a.k.a. resource server, application, API, etc.) to which it is requesting access by including the following parameter in the request."; Thomas's 2026-09-21 selection retains the resource-server and AS-metadata-consumer roles only. internal/component/mcp/streamable_auth.go::buildAuthForMode verifies external tokens and emits no authorization/token request or resource request parameter}
- [ ] [RFC8707-2-2] [SHOULD NOT] The `resource` request parameter URI SHOULD NOT include a query component, though a query component can be a useful and necessary part of the parameter (§2) {feature-declined: "In requests to the authorization server, a client MAY indicate the protected resource (a.k.a. resource server, application, API, etc.) to which it is requesting access by including the following parameter in the request."; Thomas's 2026-09-21 selection retains resource-server and AS-metadata-consumer roles only. internal/component/mcp/streamable_auth.go::buildAuthForMode verifies tokens without sending this request parameter; audience values are not automatically canonical resource URLs}
- [ ] [RFC8707-2-4] [MAY] The `resource` parameter MAY be included in an access token request to the token endpoint (§2, §2.2)
- [ ] [RFC8707-2-5] [MAY] Multiple `resource` values MAY appear; AS policy decides (§2)
- [ ] [RFC8707-2-6] [MUST] The `resource` request parameter value MUST be an absolute URI, as specified by RFC 3986 §4.3 (§2) {feature-declined: "In requests to the authorization server, a client MAY indicate the protected resource (a.k.a. resource server, application, API, etc.) to which it is requesting access by including the following parameter in the request."; the 2026-09-21 scope retains the resource-server and AS-metadata-consumer roles. internal/component/mcp/streamable_auth.go::buildAuthForMode verifies external tokens and emits no authorization/token request or resource request parameter}
