# RFC 8707 - Resource Indicators for OAuth 2.0

## Meta

| Field | Value |
|-------|-------|
| RFC | 8707 |
| Title | Resource Indicators for OAuth 2.0 |
| Status | Proposed Standard |
| Date | February 2020 |
| Authors | B. Campbell, J. Bradley, H. Tschofenig |
| Related | RFC 9728 (resource metadata), RFC 6749 (OAuth 2.0 core) |
| Enrolment | enrolled |
| Enrolment reason | Resource Indicators for OAuth 2.0 (ze as an OAuth resource server): seven MUST-level requirements. Three are met with positive+negative tags in internal/component/mcp: 5-1 (reject a token whose audience does not match the resource), 5-2 (accept an array audience when at least one entry matches), and 2-3 (canonicalize the audience so a trailing-slash divergence does not mismatch). 3-1 (set the token audience when minting) and 3-2 (return invalid_target for an unknown resource) are {not-applicable}: those are authorization-server token-endpoint behaviors and ze issues no tokens. 2-1 (no fragment) and 2-2 (no query) are {not-applicable}: they govern the client-formed canonical resource request parameter, which a resource server neither emits nor consumes. |
| Support | - |

**Purpose:** Adds a `resource` parameter to OAuth 2.0 authorization / token endpoints so the client can tell the authorization server which resource a requested access token is for. The AS encodes this as the `aud` claim on the issued JWT. Resource servers then REJECT any token whose `aud` does not match their own identity -- defeating the token-passthrough attack where a token stolen from one resource is replayed against another.

## Client Behavior (Section 2)

| Requirement | Item |
|-------------|------|
| `resource` parameter on authorization request | An absolute URI identifying the target resource. |
| MAY appear on token endpoint | When present, the AS uses it to select the audience. |
| Multiple `resource` values | MAY appear; AS policy decides. |

## Authorization Server Behavior (Section 3)

| Requirement | Item |
|-------------|------|
| Validate `resource` against registered metadata | AS decides which values it issues tokens for. |
| Set `aud` on the JWT | Access token's `aud` claim MUST be the canonical resource URL(s). |
| Reject unknown resources | AS MUST respond with `invalid_target` per RFC 8693 when it will not issue a token for the requested resource. |

## Resource Server Behavior (Section 5)

This is the rule the MCP resource server enforces:

| Requirement | Item |
|-------------|------|
| Verify `aud` on every token | MUST reject if `aud` does not match the resource's own canonical URL. |
| Canonicalisation | Compare after RFC 6749 Section 2.3.1 canonicalisation: scheme lowercase, default ports elided, no fragment, no query, no trailing slash. |
| Multiple audiences | If `aud` is a JSON array, at least one entry MUST match; array-shape decoding is required. |

## Canonical Resource URL (Section 2)

| Rule | Detail |
|------|--------|
| scheme | lowercase, typically `https` |
| host | lowercase, IDN normalised via IDNA / NFC |
| port | default for scheme elided (443 for https, 80 for http) |
| path | MAY be present; preserved as-is |
| fragment | MUST be absent |
| query | MUST be absent from the canonical form |
| trailing slash | operator decision; MUST be consistent on both sides |

## Ze Implementation

- Audience comparison: `internal/component/mcp/jwt.go` `audClaim.Matches` exact-match on string or array-form `aud`.
- Operator config: `environment.mcp.oauth.audience` is the canonical resource URL. MUST be set explicitly; the resource server never derives it from `r.Host`.
- Rejection path: `oauthAuthenticator.mapVerifyError` maps `errJWTAudienceMismatch` to `error="invalid_token", error_description="invalid audience"` per RFC 6750 vocabulary.

## Key Security Property

Without RFC 8707: a token issued for resource A can be replayed against resource B if both trust the same AS. B has no way to tell the token was not meant for it.

With RFC 8707: B validates `aud == B`. A token issued for A has `aud = A`. Replaying it against B fails closed.

## Section Index

| Section | Topic |
|---------|-------|
| 1 | Introduction |
| 2 | Authorization Request |
| 3 | Authorization Server Response |
| 4 | Token Endpoint |
| 5 | Access Token Request |
| 6 | Introspection |
| 7 | Security Considerations |

## Compliance Checklist

- [ ] [RFC8707-3-1] [MUST] Access token's `aud` claim MUST be the canonical resource URL(s) (§3) {not-applicable: setting the token audience is an authorization-server obligation performed when minting a token; ze is an OAuth resource server (internal/component/mcp) and never issues or signs tokens, so it has no audience-setting code path}
- [ ] [RFC8707-3-2] [MUST] AS MUST respond with `invalid_target` when it will not issue a token for the requested resource (§3) {not-applicable: the invalid_target error is an authorization-server token-endpoint behavior; ze has no token or authorization endpoint (grep for invalid_target in internal/component/mcp finds none)}
- [ ] [RFC8707-5-1] [MUST] Resource server MUST reject any token whose `aud` does not match the resource's own canonical URL (§5)
- [ ] [RFC8707-5-2] [MUST] If `aud` is a JSON array, at least one entry MUST match; array-shape decoding is required (§5)
- [ ] [RFC8707-2-1] [MUST] Fragment MUST be absent from the canonical resource URL (§2) {not-applicable: this governs the canonical resource URI a client forms and sends as the resource request parameter; ze is a resource server that never emits or consumes that parameter (internal/component/mcp validates the token audience only)}
- [ ] [RFC8707-2-2] [MUST] Query MUST be absent from the canonical resource URL form (§2) {not-applicable: this governs the client-formed canonical resource URI; ze (resource server) never emits or consumes the resource request parameter, validating only the token audience}
- [ ] [RFC8707-2-3] [MUST] Trailing slash usage MUST be consistent on both sides (§2)
- [ ] [RFC8707-2-4] [MAY] `resource` parameter MAY appear on the token endpoint (§2, §4)
- [ ] [RFC8707-2-5] [MAY] Multiple `resource` values MAY appear; AS policy decides (§2)
