# RFC 4552 - Authentication/Confidentiality for OSPFv3

## Meta

| Field | Value |
|-------|-------|
| RFC | 4552 |
| Title | Authentication/Confidentiality for OSPFv3 |
| Status | Standards Track |
| Date | June 2006 |
| Authors | M. Gupta, N. Melam |
| Obsoletes | None |
| Updates | None |
| Enrolment | enrolled |
| Enrolment reason | Authentication/Confidentiality for OSPFv3 (RFC 4552): OSPFv3 IPsec SA/policy installer over kernel XFRM. 4 MET (authentication support, confidentiality via ESP not AH, stream-cipher prohibition, hexadecimal key configuration) + 11 single-polarity positive (transport-mode SA + policies, per-interface SPD selection, source/dest/proto/direction selectors, inbound interface tagging, manual keying, shared inbound/outbound SA, IPsec barrier around all OSPF, protect + bypass SPD rules) + 5 gap (virtual-link IPsec: distinct SA, source/destination LA-bit selection, transit-area SPD rules) + 6 lower-layer (per-packet AH/ESP discard, ESP encapsulation and multi-SA storage performed by Linux XFRM on the state ze installs) + 1 not-applicable (the §6 clause that imports RFC 4305 algorithm keyword levels) |
| Support | igp-mpls 180 |
| Support area | OSPFv3 IPsec authentication |
| Support status | Experimental |
| Support coverage | Manual AH/ESP IPsec config for OSPFv3 IPv6 interfaces, XFRM readiness check, SA and policy lifecycle. |
| Support remaining | Manual keying only; feature remains under OSPF experimental status. |

**Purpose:** Specifies how OSPFv3 can use IPv6 IPsec AH and ESP for
authentication and confidentiality. OSPFv3 removed the OSPFv2 AuType and
Authentication header fields, so this RFC originally supplied the security
mechanism for OSPFv3.

**Scope for Ze:** This is an out-of-scope marker for the first `ospfv3`
follow-up. Use RFC 7166 authentication trailer first. IPsec support requires
kernel policy, Security Association management, multicast key handling, and
operator workflow that are larger than base OSPFv3.

## Required Capabilities

| Area | RFC 4552 requirement |
|------|----------------------|
| Mode | Transport mode Security Associations must be supported; tunnel mode may also be supported (§2) |
| Authentication | Implementations must support OSPFv3 authentication; ESP must be supported and AH may be supported (§3) |
| Confidentiality | Implementations should support confidentiality; if confidentiality is provided, ESP must be used (§4) |
| Packet drops | When auth or confidentiality is enabled, unprotected packets and failed checks are silently discarded (§3, §4) |
| OSPF version | OSPFv2 and OSPFv3 both use protocol 89, but IP version distinguishes them for IPsec selectors (§5) |
| SPD | Multiple Security Policy Databases with interface-based selection are required (§6) |
| Selectors | Source, destination, protocol, direction, and interface tagging are required (§6) |
| Manual keys | Manual keying must be available for the specified traffic (§6, §7) |

## Key Management Constraint (§7)

OSPFv3 uses multicast and unicast. For broadcast links, authentication is a
one-to-many problem. IKE is not suitable for the multicast group case in this
RFC, so manual keying is required. The RFC explains why per-peer inbound and
outbound SAs do not scale on broadcast links, then uses shared SA parameters
for inbound and outbound group traffic.

This is the main reason Ze should not treat RFC 4552 as the first auth path:
base OSPFv3 should not require users to configure kernel IPsec policy and
manual multicast group keys before an adjacency can form.

## SA Granularity and Selectors

The implementation should let operators choose whether one SA is shared among
multiple interfaces or each interface has a unique SA. Selectors must be able
to distinguish direction, source, destination, protocol, and interface. Virtual
links have additional dynamic policy needs.

## Out-of-Scope Decision for First Ze OSPFv3

Initial Ze OSPFv3 should implement RFC 7166 trailer authentication instead of
RFC 4552 IPsec. Reasons:

- RFC 7166 is carried in OSPFv3 packet processing and can share Ze key-chain
  patterns.
- RFC 4552 needs OS IPsec policy management, manual group keys, and SPD
  interface selection.
- FRR interoperability for base OSPFv3 can be proven without implementing
  kernel IPsec control first.

## Compliance Checklist

Every RFC 2119 keyword in RFC 4552 becomes one line below (the §1.1 conventions
boilerplate is the only occurrence excluded; the §6 sentence that imports the
RFC 4305 keyword levels is captured as RFC4552-6-7). Grouped MUST/MUST NOT, then
SHOULD/SHOULD NOT, then MAY; ordered by section within each group. IDs anchor to
the first section cited.

- [ ] [RFC4552-2-2] [MUST] All implementations conforming to this specification MUST support transport mode SA to provide required IPsec security to OSPFv3 packets (§2) {single-polarity: positive; the installer only ever builds a transport-mode SA (buildIPsecSA Mode=ModeTransport, ipsec_install.go), so there is no tunnel-mode reject path to exercise}
- [ ] [RFC4552-3-1] [MUST] Implementations conforming to this specification MUST support authentication for OSPFv3 (§3)
- [ ] [RFC4552-3-2] [MUST] In order to provide authentication to OSPFv3, implementations MUST support ESP (§3) {single-polarity: positive; an esp interface always installs an SA with Proto=ProtoESP (ipsecProtoNumber, ipsec_install.go); "supporting ESP" has no reject path of its own}
- [ ] [RFC4552-3-4] [MUST] When OSPFv3 authentication is enabled, OSPFv3 packets that are not protected with AH or ESP MUST be silently discarded (§3) {lower-layer: Linux XFRM; internal/plugins/ospf/ipsec_install.go::buildIPsecPolicies installs the inbound require-policy (SADirIn, transport mode, upper-protocol 89, scoped to the arrival ifindex), and Linux XFRM discards an OSPFv3 packet that arrives on that interface without the required AH or ESP transform. Ze only samples the resulting counters (readXfrmDropsPlatform, ipsec_drops_linux.go)}
- [ ] [RFC4552-3-5] [MUST] When OSPFv3 authentication is enabled, OSPFv3 packets that fail the authentication checks MUST be silently discarded (§3) {lower-layer: Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams installs the integrity transform and its truncation length, and Linux XFRM discards a packet whose ICV does not verify. Ze holds no verifying branch of its own and reads only the XfrmInIntegFailures/XfrmInStateProtoError counters}
- [ ] [RFC4552-4-2] [MUST] If confidentiality is provided, ESP MUST be used (§4)
- [ ] [RFC4552-4-3] [MUST] When OSPFv3 confidentiality is enabled, OSPFv3 packets that are not protected with ESP MUST be silently discarded (§4) {lower-layer: Linux XFRM; internal/plugins/ospf/ipsec_install.go::buildIPsecPolicies installs the inbound require-policy on a confidentiality-enabled interface, and Linux XFRM discards an OSPFv3 packet that arrives there without the required ESP transform}
- [ ] [RFC4552-4-4] [MUST] When OSPFv3 confidentiality is enabled, OSPFv3 packets that fail the confidentiality checks MUST be silently discarded (§4) {lower-layer: Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams installs the ESP encryption transform beside the integrity one, and Linux XFRM discards a packet whose decryption or integrity check fails}
- [ ] [RFC4552-6-1] [MUST] IPsec in transport mode MUST be supported (§6) {single-polarity: positive; every installed policy is transport mode (buildIPsecPolicies Mode=ModeTransport, ipsec_install.go); there is no non-transport policy to reject}
- [ ] [RFC4552-6-2] [MUST] The implementation MUST support multiple SPDs with an SPD selection function that chooses a specific SPD based on interface (§6) {single-polarity: positive; each interface's policies are scoped by IfIndex (buildIPsecPolicies IfIndex, ipsec_install.go), so the per-interface policy set is the interface-selected SPD; there is no reject path}
- [ ] [RFC4552-6-3] [MUST] The implementation MUST be able to use source address, destination address, protocol, and direction as selectors in the SPD (§6) {single-polarity: positive; every policy carries source, destination, upper-protocol 89, and direction selectors (buildIPsecPolicies, ipsec_install.go); a selector set is emitted, never rejected}
- [ ] [RFC4552-6-4] [MUST] The implementation MUST be able to tag inbound packets with the ID of the (physical or virtual) interface via which they arrived (§6) {single-polarity: positive; the inbound require-policy is scoped to the arrival ifindex (buildIPsecPolicies SADirIn IfIndex, ipsec_install.go); the kernel does the per-packet tagging, ze only supplies the interface selector}
- [ ] [RFC4552-6-5] [MUST] Manually configured keys MUST be able to secure the specified traffic (§6) {single-polarity: positive; the SA is keyed from the statically configured SPI+key with no IKE (buildIPsecSA, ipsec_install.go); manual keying has no reject path}
- [ ] [RFC4552-6-6] [MUST NOT] The implementation MUST NOT allow the user to choose stream ciphers as the encryption algorithm for securing OSPFv3 packets (§6)
- [ ] [RFC4552-6-7] [MUST] The algorithm key words (MUST, MUST NOT, REQUIRED, SHOULD, SHOULD NOT) that appear in RFC 4305 [N6] are to be interpreted per RFC 2119 for OSPFv3 support as well, except when in conflict with the stream-cipher prohibition (§6) {not-applicable: an interpretation clause importing RFC 4305 [N6] algorithm keywords; RFC 4552 defines no independent behavior here. The concrete algorithm conformance lives in the config enums (ipsecAuthKeyLen and ipsecEncKeyLen, internal/plugins/ospf/config.go) and the stream-cipher carve-out is RFC4552-6-6}
- [ ] [RFC4552-6-9] [MUST] IP encapsulation of ESP packets MUST be supported (§6) {lower-layer: Linux XFRM; internal/plugins/ospf/ipsec_install.go::buildIPsecSA installs the transport-mode ESP SA and asks for no UDP encapsulation, so xfrmStateFromParams writes no Encap, and Linux XFRM carries every ESP packet directly in IP}
- [ ] [RFC4552-6-11] [MUST] The IPsec implementation MUST support the establishment and maintenance of multiple SAs with the same selectors between a given sender and receiver (§6) {lower-layer: Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams writes each state with its own SPI, and Linux XFRM keys the SAD by destination, SPI and protocol, so several SAs carrying one selector set coexist between a given sender and receiver}
- [ ] [RFC4552-7-1] [MUST] The implementations MUST use manually configured keys with the same SA parameters (SPI, keys, etc.) for both inbound and outbound SAs (§7) {single-polarity: positive; one manually configured SPI/key drives the single shared SA that both protects egress and verifies ingress (buildIPsecSA installed once, ipsec_install.go); there is no reject path}
- [ ] [RFC4552-9-1] [MUST] A different SA than the SA of the underlying interface MUST be provided for virtual links (§9) {gap: OSPFv3 virtual-link IPsec is unimplemented; the installer consumes only configured-interface IPsec blocks (setConfig over cfg.Interfaces, ipsec_install.go) and no virtual link ever installs an SA. Disclosed in docs/features/rfc-status.md RFC 4552 row}
- [ ] [RFC4552-9-2] [MUST] Routers that implement this specification MUST change the way source and destination addresses are chosen for packets exchanged over virtual links when IPsec is enabled (§9) {gap: ze never enables IPsec on a virtual link, so it does not switch virtual-link source/destination selection when IPsec is on; the endpoints stay RFC 5340 §2.9 routed globals (v6ResolveVirtualEndpointLocked, virtuallink_v6.go:28-35). Disclosed in docs/features/rfc-status.md RFC 4552 row}
- [ ] [RFC4552-9-3] [MUST] The first IPv6 address with the "LA-bit" set in prefixes advertised in intra-area-prefix-LSAs in the transit area MUST be used as the source address for packets exchanged over the virtual link (§9) {gap: virtual-link IPsec is unimplemented, so the RFC 4552 §9 rule of using the first LA-bit intra-area-prefix address as the source is not applied; ze selects the source from RFC 5340 routed globals (v6RouterGlobalAddr, virtuallink_v6.go:42-81). Disclosed in docs/features/rfc-status.md RFC 4552 row}
- [ ] [RFC4552-9-4] [MUST] The first IPv6 address with the "LA-bit" set in prefixes received in intra-area-prefix-LSAs from the virtual neighbor in the transit area MUST be used as the destination address for packets exchanged over the virtual link (§9) {gap: the matching destination rule (first LA-bit intra-area-prefix address of the virtual neighbor) is likewise unapplied because virtual-link IPsec is unimplemented (virtuallink_v6.go:28-35). Disclosed in docs/features/rfc-status.md RFC 4552 row}
- [ ] [RFC4552-11-1] [MUST] The IPsec protection barrier MUST be around the OSPF protocol so that all inbound and outbound OSPF traffic goes through IPsec processing (§11) {single-polarity: positive; the out/in/fwd proto-89 policies put the IPsec barrier around all OSPF traffic on the interface (buildIPsecPolicies, ipsec_install.go); the barrier is installed, never rejected}
- [ ] [RFC4552-11-2] [MUST] The SPD selection function MUST return an SPD with the bypass rule for all interfaces that have OSPFv3 authentication/confidentiality disabled (§11) {single-polarity: positive; a disabled interface installs no require-policy (setConfig adds only interfaces with an IPsec block, ipsec_install.go), so its OSPF is bypassed by the kernel default; the bypass has no reject path}
- [ ] [RFC4552-11-3] [MUST] The SPD selection function MUST return an SPD with the protect rules for all interfaces that have OSPFv3 authentication/confidentiality enabled (§11) {single-polarity: positive; an enabled interface installs the out/in/fwd protect (require) policies (installLocked, ipsec_install.go); the protect rules are installed, never rejected}
- [ ] [RFC4552-11-4] [MUST] The virtual-link SPD rules MUST be installed in the SPD for the interfaces that are connected to the transit area for the virtual link (§11) {gap: no virtual-link SPD rules are installed on transit-area interfaces because virtual-link IPsec is unimplemented (ipsec_install.go installs only configured-interface policies). Disclosed in docs/features/rfc-status.md RFC 4552 row}
- [ ] [RFC4552-12-1] [MUST] The implementations MUST allow the administrator to configure the cryptographic and authentication keys in hexadecimal format (§12)
- [ ] [RFC4552-4-1] [SHOULD] Implementations conforming to this specification SHOULD support confidentiality for OSPFv3 (§4)
- [ ] [RFC4552-6-8] [SHOULD] The routing module SHOULD be able to configure, modify, and delete IPsec rules on the fly (mainly for securing virtual links) (§6)
- [ ] [RFC4552-6-10] [SHOULD NOT] For simplicity, UDP encapsulation of ESP packets SHOULD NOT be used (§6)
- [ ] [RFC4552-8-1] [SHOULD] The user SHOULD be given the choice of sharing the same SA among multiple interfaces or using a unique SA per interface (§8)
- [ ] [RFC4552-10-1] [SHOULD] To maintain the security of a link, the authentication and encryption key values SHOULD be changed periodically (§10)
- [ ] [RFC4552-10.1-1] [SHOULD] The three-step rekey procedure SHOULD be provided to rekey the routers on a link without dropping OSPFv3 protocol packets or disrupting the adjacency (§10.1)
- [ ] [RFC4552-10.3-1] [SHOULD] The encryption and authentication keys SHOULD be changed at least every 90 days (§10.3)
- [ ] [RFC4552-2-1] [MAY] Two hosts MAY establish a tunnel mode SA between themselves (§2)
- [ ] [RFC4552-2-3] [MAY] Implementations MAY also support tunnel mode SA to provide required IPsec security to OSPFv3 packets (§2)
- [ ] [RFC4552-3-3] [MAY] In order to provide authentication to OSPFv3, implementations MAY support AH (§3)
- [ ] [RFC4552-11-5] [MAY] The virtual-link SPD rules MAY alternatively be installed on all the interfaces (§11)

## Ze Scoping Notes (not RFC requirements)

- Add an explicit OSPFv3 IPsec feature gate separate from RFC 7166 trailer auth.
- Add FRR `ospf6d` IPsec interop before marking support complete.
