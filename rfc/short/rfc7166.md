# RFC 7166 - Supporting Authentication Trailer for OSPFv3

## Meta

| Field | Value |
|-------|-------|
| RFC | 7166 |
| Title | Supporting Authentication Trailer for OSPFv3 |
| Status | Standards Track |
| Date | March 2014 |
| Authors | M. Bhatia, V. Manral, A. Lindem |
| Obsoletes | RFC 6506 |
| Updates | None |

**Purpose:** Defines a non-IPsec authentication trailer for OSPFv3 so
OSPFv3 can authenticate protocol packets without depending only on AH/ESP.
It reuses HMAC-SHA algorithms and key rollover concepts familiar from
OSPFv2 cryptographic authentication, but the wire framing is an OSPFv3
trailer, not an OSPF header field.

**Scope for Ze:** Use this for the first OSPFv3 authentication follow-up.
Do not add OSPFv2 AuType fields to the OSPFv3 header. IPsec from RFC 4552
is a later optional path.

## Authentication Trailer Placement (§2)

The Authentication Trailer is appended after the OSPFv3 packet and optional
LLS data block. Its length is not included in the OSPFv3 packet header
length, but is included in the IPv6 payload length. The digest calculation
covers the OSPFv3 packet, any LLS data block, selected trailer fields, and
the IPv6 source address protection input from §2.3 and §4.5.

| Length | Includes |
|--------|----------|
| OSPFv3 packet length | OSPFv3 header and OSPFv3 packet body only |
| LLS data block length | Optional LLS data block when the L-bit is set |
| Authentication length | Fixed 16-byte trailer plus digest |
| IPv6 payload length | OSPFv3 packet, optional LLS data, and Authentication Trailer |

## AT-Bit (§2.1)

The AT-bit in the OSPFv3 Options field indicates Authentication Trailer
use. Routers using the trailer must set the AT-bit in Hello and Database
Description packets. For packet types without an Options field, the neighbor
state learned from Hello/DD determines whether a trailer is expected.

Implementation rule: once a neighbor is operating with the AT-bit, every
OSPFv3 packet from that neighbor is expected to include a valid trailer.
Missing, expired, or invalid Security Association state is a drop.

## Trailer Format (§4.1)

| Field | Width | Rule |
|-------|-------|------|
| Authentication Type | 16 bits | 0 reserved, 1 HMAC Cryptographic Authentication |
| Auth Data Len | 16 bits | Length in octets of the whole Authentication Trailer, including the fixed 16-byte header and digest |
| Reserved | 16 bits | Send zero, ignore on receive |
| Security Association ID | 16 bits | Selects the configured algorithm and key |
| Cryptographic Sequence Number | 64 bits | Strictly increasing anti-replay value |
| Authentication Data | variable | HMAC digest |

Sequence numbers must be strictly greater than the last accepted sequence
number for the same neighbor and OSPFv3 packet type. Packet-type separation
is required because OSPF packet prioritization can reorder different packet
types. The 64-bit value should preserve monotonicity across restarts, for
example by using high-order 32 bits as a persistent boot or wrap counter.

## Security Association (§3)

An OSPFv3 Security Association contains:

| SA item | Requirement |
|---------|-------------|
| SA ID | 16-bit configured identifier carried in the trailer |
| Authentication algorithm | HMAC-SHA-1, HMAC-SHA-256, HMAC-SHA-384, or HMAC-SHA-512 |
| Authentication key | Secret key for the SA |
| Accept/generate lifetimes | KeyStartAccept, KeyStartGenerate, KeyStopGenerate, KeyStopAccept |

A receiver chooses the active SA from the SA ID in the packet. The algorithm
is not carried on the wire and must not be inferred only from digest length,
because future algorithms can share the same output length.

For smooth rollover, accept lifetime should start before generate lifetime,
and generate lifetime should stop before accept lifetime. If no valid key
exists for an interface, the router must not transmit unauthenticated OSPFv3
packets.

## Checksum Handling (§2.2, §4.2)

When the Authentication Trailer is used, OSPFv3 header checksum calculation
and LLS data block checksum calculation are omitted. The LLS data block, if
present, is included in the authentication digest instead. This is a key
difference from unauthenticated RFC 5340 packets.

## IPv6 Source Address Protection (§2.3, §4.5)

OSPFv3 identifies neighbors by Router ID, but learns the neighbor IPv6 source
address from Hellos. The IPv6 source address is included in the digest
calculation to prevent an attacker from changing the source address while
leaving the Router ID unchanged.

## Cryptographic Procedure (§4.3 through §4.6)

The supported algorithms are HMAC-SHA-1, HMAC-SHA-256, HMAC-SHA-384, and
HMAC-SHA-512. The implementation must use the protocol-specific key derivation
from §4.5, include the source address protection input, include the LLS block
when present, and compare digests in constant time.

Received packets are dropped if the trailer is absent when expected, if the
SA is unknown or expired, if the sequence number is not strictly greater than
the last accepted value for that neighbor and packet type, or if digest
verification fails.

## Compliance Checklist

- [ ] [RFC7166-2.1-1] [MUST] OSPFv3 routers set the AT-bit in all OSPFv3 Hello and Database Description packets that contain an Authentication Trailer (§2.1) {gap: no AT-bit exists in the OSPFv3 Options bitset -- internal/plugins/ospf/v3/types/options.go:14-23 defines only V6/E/N/R/AF, and the Hello/DD encoders internal/plugins/ospf/v3/packet/hello.go:88 and dbdesc.go:73 never set one, so no Authentication Trailer is emitted}
- [ ] [RFC7166-3-3] [MUST] When a new key replaces an old key, the new key's KeyStartGenerate be less than or equal to the old key's KeyStopGenerate (§3) {gap: ze has no OSPFv3 Security Association carrying the RFC 7166 KeyStartAccept/KeyStartGenerate/KeyStopGenerate/KeyStopAccept lifetimes -- the only key model is the OSPFv2 send-lifetime pair in internal/plugins/ospf/auth_keystore.go:29-32, and validateKeyRollover internal/plugins/ospf/config.go:1007 orders OSPFv2 send-lifetimes only}
- [ ] [RFC7166-3-6] [MUST NOT] Transmit an OSPFv3 packet unauthenticated when the last key associated with an interface has expired (§3) {gap: no OSPFv3 Authentication Trailer is ever produced -- internal/plugins/ospf/auth_wiring.go:28 signPacket appends the OSPFv2 trailer via internal/plugins/ospf/packet/auth_verify.go:157, so there is no OSPFv3 authenticated-transmit path whose key expiry this could govern; the never-revert selection in auth_keystore.go:281-286 is OSPFv2-only}
- [ ] [RFC7166-4.1-2] [MUST] Ignore the Reserved field when receiving protocol packets (§4.1) {gap: no OSPFv3 trailer decoder exists -- the only receive-side auth verify internal/plugins/ospf/auth_wiring.go:56 verifyPacket delegates to internal/plugins/ospf/auth_keystore.go:330 which decodes an OSPFv2 common header, so an OSPFv3 trailer Reserved field is never parsed}
- [ ] [RFC7166-4.1-3] [MUST] Increment the 64-bit cryptographic sequence number for every OSPFv3 packet sent (§4.1) {gap: the 64-bit ESN counter in internal/plugins/ospf/auth_keystore.go:304-314 feeds the OSPFv2 packet.Sign trailer, not an OSPFv3 trailer; no OSPFv3 Cryptographic Sequence Number field is ever written}
- [ ] [RFC7166-4.1-4] [MUST] On reception, require the sequence number to be greater than that of the last accepted OSPFv3 packet of the same packet type from the sending neighbor (§4.1) {gap: the per-packet-type strictly-greater replay check internal/plugins/ospf/auth_keystore.go:352-361 runs only inside the OSPFv2 verify path; there is no OSPFv3 trailer receive path to which it applies}
- [ ] [RFC7166-4.1-5] [MUST] Use available mechanisms to preserve the sequence number's strictly increasing property for the router's deployed life, including cold restarts (§4.1) {gap: the persisted boot-count mechanism internal/plugins/ospf/auth_keystore.go:115 loadOSPFBootCount seeds the OSPFv2 ESN sequence only; no OSPFv3 trailer sequence is emitted for it to protect}
- [ ] [RFC7166-4.1.1-1] [MUST] Reset all keys before the 64-bit sequence number can wrap, to avoid replay attacks (§4.1.1) {gap: no OSPFv3 trailer or its 64-bit sequence exists to wrap -- internal/plugins/ospf/auth_keystore.go:311-314 only bumps bootCount on the OSPFv2 ESN low-word wrap and performs no key reset}
- [ ] [RFC7166-4.2-3] [MUST] Omit OSPFv3 header checksum verification for received packets that include an Authentication Trailer (§4.2) {gap: the receive path never detects an OSPFv3 Authentication Trailer, so it cannot conditionally omit header-checksum verification; only the transmit-side checksum-omission hook exists at internal/plugins/ospf/v3/packet/checksum.go:165-167 and internal/plugins/ospf/v3/packet/header.go:277-280}
- [ ] [RFC7166-4.2-4] [MUST] Omit LLS data block checksum verification for received packets that include an Authentication Trailer (§4.2) {gap: the OSPFv3 codec implements no LLS data block -- there is no LLS type under internal/plugins/ospf/v3/packet/ -- and no trailer receive path, so there is no LLS checksum to conditionally omit}
- [ ] [RFC7166-4.3-1] [MUST] Include support for at least HMAC-SHA-256 (§4.3) {gap: the HMAC-SHA-256 primitive exists only for the OSPFv2 trailer at internal/plugins/ospf/packet/auth_verify.go:69 and :120; no OSPFv3 Authentication Trailer wires any algorithm}
- [ ] [RFC7166-4.3-4] [MUST] Use HMAC-SHA-256 as the default authentication algorithm (§4.3) {gap: no OSPFv3 trailer means no OSPFv3 default algorithm; the OSPFv2 keychain algorithm is operator-selected per key at internal/plugins/ospf/auth_keystore.go:239-252 with no HMAC-SHA-256 default}
- [ ] [RFC7166-4.4-1] [MUST] Append the two-octet OSPFv3 Cryptographic Protocol ID to the Authentication Key prior to use; other protocols sharing common keys similarly append their own IDs (§4.4) {gap: only the OSPFv2 Cryptographic Protocol ID 0x0001 is appended, and only for the OSPFv2 AuType-3 trailer at internal/plugins/ospf/packet/auth_verify.go:38 and :195; there is no OSPFv3 Cryptographic Protocol ID constant or OSPFv3 trailer to apply it to}
- [ ] [RFC7166-4.6-1] [MUST] Minimally support examining the L-bit in the OSPFv3 Options and using the LLS data block length to access the Authentication Trailer (§4.6) {gap: the OSPFv3 codec parses no LLS block, defines no L-bit in internal/plugins/ospf/v3/types/options.go:14-23, and defines no trailer, so it cannot locate an Authentication Trailer past an LLS block}
- [ ] [RFC7166-4.6-3] [MUST] Drop a packet whose cryptographic sequence number is less than or equal to the last accepted value for that neighbor and OSPFv3 packet type (§4.6) {gap: the seq<=last drop at internal/plugins/ospf/auth_keystore.go:357-359 is reached only via the OSPFv2 verify path; no OSPFv3 trailer receive path exists}
- [ ] [RFC7166-4.6-5] [MUST] Discard the packet when the computed digest does not match the received Authentication Data (§4.6) {gap: the constant-time digest compare at internal/plugins/ospf/packet/auth_verify.go:242 and :263 verifies the OSPFv2 trailer only; there is no OSPFv3 trailer digest to verify}
- [ ] [RFC7166-4.6-6] [MUST] After successful authentication, store the 64-bit cryptographic sequence number for each OSPFv3 packet type received from the neighbor (§4.6) {gap: the recvSeq high-water store keyed by packet type at internal/plugins/ospf/auth_keystore.go:352-361 records OSPFv2-trailer sequences only; no OSPFv3 trailer sequence is ever received to store}
- [ ] [RFC7166-3-1] [SHOULD] Set KeyStartAccept less than KeyStartGenerate for smooth key transition (§3)
- [ ] [RFC7166-3-2] [SHOULD] Set KeyStopGenerate less than KeyStopAccept for smooth key transition (§3)
- [ ] [RFC7166-3-4] [SHOULD] Persist key storage across warm or cold system restarts (§3)
- [ ] [RFC7166-3-5] [SHOULD] Notify the network operator when the last key associated with an interface expires (§3)
- [ ] [RFC7166-4.1-1] [SHOULD] Set the Reserved field to 0 when sending protocol packets (§4.1)
- [ ] [RFC7166-4.2-1] [SHOULD] Set the OSPFv3 header checksum to 0 before computing the Authentication Trailer digest on transmit (§4.2)
- [ ] [RFC7166-4.2-2] [SHOULD] Set the LLS data block checksum to 0 before computing the Authentication Trailer digest on transmit (§4.2)
- [ ] [RFC7166-4.3-2] [SHOULD] Include support for HMAC-SHA-1 (§4.3)
- [ ] [RFC7166-4.6-2] [RECOMMENDED] Fully support OSPFv3 Link-Local Signaling (§4.6)
- [ ] [RFC7166-4.6-4] [SHOULD] Log an error event when an OSPFv3 packet is dropped for replay or digest-verification failure (§4.6)
- [ ] [RFC7166-5-1] [SHOULD] Migrate all OSPFv3 routers on a link to authentication at the same time (§5)
- [ ] [RFC7166-6-1] [SHOULD] Use sufficiently long and random values for the Authentication Key (§6)
- [ ] [RFC7166-6-2] [RECOMMENDED] Have Authentication Keys incorporate at least 128 pseudorandom bits (§6)
- [ ] [RFC7166-6-3] [SHOULD] Support hexadecimal input of Authentication Keys in management systems (§6)
- [ ] [RFC7166-4.3-3] [MAY] Include support for HMAC-SHA-384 and HMAC-SHA-512 (§4.3)
- [ ] [RFC7166-5-2] [MAY] Implement a transition mode that adds the Authentication Trailer to transmitted packets without verifying received packets (§5)
