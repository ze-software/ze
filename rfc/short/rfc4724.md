# RFC 4724: Graceful Restart Mechanism for BGP

## Meta

| Field | Value |
|-------|-------|
| RFC | 4724 |
| Title | Graceful Restart Mechanism for BGP |
| Status | Standards Track |
| Date | January 2007 |
| Updates | RFC 4271 (verified errata 7915) |
| Purpose | Minimize routing disruption during BGP restart by preserving forwarding state and deferring route deletion |

## Wire Formats

### Graceful Restart Capability

Type code: 64 (Capability)

```
+--------------------------------------------------+
| Restart Flags (4 bits)                           |
+--------------------------------------------------+
| Restart Time in seconds (12 bits)                |
+--------------------------------------------------+
| Address Family Identifier (16 bits)              |
+--------------------------------------------------+
| Subsequent Address Family Identifier (8 bits)    |
+--------------------------------------------------+
| Flags for Address Family (8 bits)                |
+--------------------------------------------------+
| ...                                              |
+--------------------------------------------------+
| Address Family Identifier (16 bits)              |
+--------------------------------------------------+
| Subsequent Address Family Identifier (8 bits)    |
+--------------------------------------------------+
| Flags for Address Family (8 bits)                |
+--------------------------------------------------+
```

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Restart Flags | 0 | 4 bits | bitfield | See Restart Flags below |
| Restart Time | 4 bits | 12 bits | uint | 0-4095 seconds |
| AFI | 2 + (n*4) | 2 bytes | uint16 | Per IANA AFI registry |
| SAFI | 4 + (n*4) | 1 byte | uint8 | Per IANA SAFI registry |
| Address Family Flags | 5 + (n*4) | 1 byte | bitfield | See AF Flags below |

Length calculation: `2 + (4 * num_afi_safi_tuples)` bytes

**Restart Flags (4 bits):**
```
 0 1 2 3
+-+-+-+-+
|R|Resv.|
+-+-+-+-+
```

| Bit | Name | Meaning |
|-----|------|---------|
| 0 (MSB) | R | Restart State - speaker has restarted |
| 1-3 | Reserved | MUST be zero on send, ignored on receive |

**Address Family Flags (8 bits):**
```
 0 1 2 3 4 5 6 7
+-+-+-+-+-+-+-+-+
|F|   Reserved  |
+-+-+-+-+-+-+-+-+
```

| Bit | Name | Meaning |
|-----|------|---------|
| 0 (MSB) | F | Forwarding State preserved for this AFI/SAFI |
| 1-7 | Reserved | MUST be zero on send, ignored on receive |

### End-of-RIB Marker

**IPv4 Unicast:**
UPDATE message with minimum length per RFC 4271:
- Withdrawn Routes Length: 0
- Total Path Attribute Length: 0
- No NLRI

**Other AFI/SAFI:**
UPDATE message containing only MP_UNREACH_NLRI (type 15) with:
- AFI: target address family
- SAFI: target sub-address family
- Withdrawn Routes: empty (length 0)

## Encoding Rules

- Restart Flags and Restart Time share first 2 bytes (big-endian)
- AFI/SAFI tuples: 0 to 63 tuples allowed
- Empty capability (no AFI/SAFI tuples): indicates support for receiving speaker procedures only, no forwarding state preservation

## Decoding Rules

1. Parse Restart Flags (4 bits) and Restart Time (12 bits) from first 2 bytes
2. Remaining bytes / 4 = number of AFI/SAFI tuples
3. For each tuple: parse AFI (2), SAFI (1), Flags (1)
4. If capability length < 2: malformed
5. If (capability length - 2) % 4 != 0: malformed

## Validation

| Check | Valid | Invalid Action |
|-------|-------|----------------|
| Capability length | >= 2, (len-2) % 4 == 0 | Treat as malformed capability |
| Reserved bits in Restart Flags | Any value | Ignore (Section 3) |
| Reserved bits in AF Flags | Any value | Ignore (Section 3) |
| Multiple GR capabilities | N/A | Use last instance only (Section 3) |
| Restart Time | 0-4095 | Always valid |
| AFI/SAFI tuple count | 0-63 | > 63 would exceed capability max length |

## MUST Requirements

### Tx (Sender)

| Requirement | Section | Context |
|-------------|---------|---------|
| "A BGP speaker MUST NOT include more than one instance of the Graceful Restart Capability in the capability advertisement" | 3 |
| "The remaining bits are reserved and MUST be set to zero by the sender" | 3 | Restart Flags |
| "The remaining bits are reserved and MUST be set to zero by the sender" | 3 | AF Flags |
| "The End-of-RIB marker MUST be sent by a BGP speaker to its peer once it completes the initial routing update" | 4 |
| "Unless allowed via configuration, the 'Forwarding State' bit for an address family in the capability can be set only if the forwarding state has indeed been preserved" | 4.1 |

### Rx (Receiver)

| Requirement | Section | Context |
|-------------|---------|---------|
| "its peer MUST NOT wait for the End-of-RIB marker from the speaker before advertising routing information" | 3 | When R bit set |
| "If more than one instance of the Graceful Restart Capability is carried in the capability advertisement, the receiver MUST ignore all but the last instance" | 3 |
| "it MUST treat the subsequent open connection from the peer as an indication of the termination of the old TCP session" | 4.2 | When GR capability received |
| "the previous TCP session MUST be closed, and the new one retained" | 4.2 |
| "it MUST retain the routes received from the peer for all the address families that were previously received in the Graceful Restart Capability and MUST mark them as stale" | 4.2 |
| "a route (from the peer) previously marked as stale MUST be deleted" | 4.2 | Consecutive restarts |

### Restarting Speaker

| Requirement | Section | Context |
|-------------|---------|---------|
| "it MUST retain, if possible, the forwarding state for the BGP routes in the Loc-RIB and MUST mark them as stale" | 4.1 |
| "It MUST NOT differentiate between stale and other information during forwarding" | 4.1 |
| "the Restarting Speaker MUST set the 'Restart State' bit in the Graceful Restart Capability of the OPEN message" | 4.1 |
| "it MUST defer route selection for an address family until it either (a) receives the End-of-RIB marker from all its peers... or (b) the Selection_Deferral_Timer... has expired" | 4.1 |
| "the forwarding state of the speaker MUST be updated and any previously marked stale information MUST be removed" | 4.1 | After route selection |
| "Once the initial update is complete... the End-of-RIB marker MUST be sent" | 4.1 |
| "an implementation MUST support a (configurable) timer that imposes this upper bound" | 4.1 | Selection_Deferral_Timer |

### Receiving Speaker

| Requirement | Section | Context |
|-------------|---------|---------|
| "The router MUST NOT differentiate between stale and other routing information during forwarding" | 4.2 |
| "The 'Restart State' bit... MUST NOT be set unless the Receiving Speaker has restarted" | 4.2 |
| "the Receiving Speaker MUST delete all the stale routes from the peer" | 4.2 | If session not re-established within Restart Time |
| "the Receiving Speaker MUST immediately remove all the stale routes" | 4.2 | If F bit not set or AFI/SAFI missing or no GR capability on re-establish |
| "The Receiving Speaker MUST send the End-of-RIB marker once it completes the initial update" | 4.2 |
| "The Receiving Speaker MUST replace the stale routes by the routing updates received from the peer" | 4.2 |
| "Once the End-of-RIB marker... is received from the peer, it MUST immediately remove any routes from the peer that are still marked as stale" | 4.2 |

### Error Handling

| Requirement | Section | Context |
|-------------|---------|---------|
| "the normal BGP procedures MUST be followed when the TCP session terminates due to the sending or receiving of a BGP NOTIFICATION message" | 4 | No GR on NOTIFICATION |

## SHOULD/MAY

| Type | Requirement | Section | Notes |
|------|-------------|---------|-------|
| [RECOMMENDED] | "the practice is recommended" | 2 | Sending End-of-RIB even without GR |
| [RECOMMENDED] | "it is still recommended that the speaker advertise the Graceful Restart Capability" | 4 | Even without forwarding preservation |
| [MAY] | "A BGP speaker MAY advertise the Graceful Restart Capability for an address family to its peer if it has the ability to preserve its forwarding state" | 4 |
| [MAY] | "the speaker MAY delete all the stale routes from the peer" | 4.2 | If peer forwarding state determined non-viable (e.g., via BFD) |
| [MAY] | "an implementation MAY support a (configurable) timer that imposes this upper bound" | 4.2 | Stale route retention timer |
| [SUGGESTED] | "A suggested default for the Restart Time is a value less than or equal to the HOLDTIME" | 4 |

## State Machine Changes

RFC 4724 modifies the BGP FSM defined in RFC 4271 Section 8.2.2.

### Idle State Modification

**Original:**
> initializes all BGP resources for the peer connection

**Modified:**
> initializes all BGP resources for the peer connection, other than those resources required in order to retain routes according to section "Procedures for the Receiving Speaker"

### Established State - Collision Handling (Event 16/17)

**Without GR Capability:**
Track second connection until it sends OPEN (original behavior)

**With GR Capability (one or more AFI/SAFI received):**

| Step | Action |
|------|--------|
| 1 | Retain all routes per "Procedures for the Receiving Speaker" |
| 2 | Release all other BGP resources |
| 3 | Drop TCP connection associated with ESTABLISHED session |
| 4 | Initialize BGP resources (except route retention resources) |
| 5 | Set ConnectRetryCounter to zero |
| 6 | Start ConnectRetryTimer with initial value |
| 7 | Change state to Connect |

### Established State - NOTIFICATION/TcpConnectionFails (Event 18/24/25)

**On NOTIFICATION (Event 24/25) OR TcpConnectionFails without GR Capability:**

| Step | Action |
|------|--------|
| 1 | Set ConnectRetryTimer to zero |
| 2 | Delete all routes associated with connection |
| 3 | Release all BGP resources |
| 4 | Drop TCP connection |
| 5 | Increment ConnectRetryCounter by 1 |
| 6 | Change state to Idle |

**On TcpConnectionFails (Event 18) with GR Capability:**

| Step | Action |
|------|--------|
| 1 | Set ConnectRetryTimer to zero |
| 2 | Retain routes per "Procedures for the Receiving Speaker" |
| 3 | Release all other BGP resources |
| 4 | Drop TCP connection |
| 5 | Increment ConnectRetryCounter by 1 |
| 6 | Change state to Idle |

### State Machine Summary Table

| State | Event | GR Received? | Route Action | Next State |
|-------|-------|--------------|--------------|------------|
| Established | Event 16/17 (TCP up) | No | Track 2nd conn | Established |
| Established | Event 16/17 (TCP up) | Yes | Retain, mark stale | Connect |
| Established | Event 24/25 (NOTIF) | Any | Delete all | Idle |
| Established | Event 18 (TCP fail) | No | Delete all | Idle |
| Established | Event 18 (TCP fail) | Yes | Retain, mark stale | Idle |
| Idle | Init | Any | Preserve retained routes | - |

## Timers

| Name | Default | Range | Behavior |
|------|---------|-------|----------|
| Restart Time | <= HOLDTIME (suggested) | 0-4095 seconds | Receiving speaker deletes stale routes if session not re-established within this time |
| Selection_Deferral_Timer | Implementation-defined | Configurable | Restarting speaker defers route selection until EoR from all peers or timer expires |
| Stale Route Retention Timer | Implementation-defined | Configurable (optional) | Upper bound on retaining stale routes |

## Constants

| Name | Value | Usage |
|------|-------|-------|
| Capability Code | 64 | Graceful Restart Capability type |
| Max AFI/SAFI tuples | 63 | Implied by capability length limit |
| Min capability length | 2 bytes | Restart Flags + Restart Time only |

## Algorithms

### Restarting Speaker Procedure

```
1. On restart:
   a. Retain forwarding state for routes in Loc-RIB
   b. Mark all retained routes as stale
   c. Continue forwarding (no stale differentiation)

2. Re-establish session:
   a. Set Restart State (R) bit in GR Capability
   b. Set Forwarding State (F) bit per AFI/SAFI if state preserved

3. After session established:
   a. Receive and process BGP messages
   b. Defer route selection per AFI/SAFI until:
      - End-of-RIB received from all peers (excluding R-bit peers, non-GR peers)
      - OR Selection_Deferral_Timer expires

4. After route selection:
   a. Update forwarding state
   b. Remove stale routes
   c. Advertise Adj-RIB-Out
   d. Send End-of-RIB for each AFI/SAFI
```

### Receiving Speaker Procedure

```
1. On TCP session termination (peer has GR Capability):
   a. Retain routes for all AFI/SAFI in peer's GR Capability
   b. Mark retained routes as stale
   c. Delete any previously stale routes from this peer
   d. Start Restart Time timer

2. On new connection while existing session (peer has GR Capability):
   a. Close previous TCP session (no NOTIFICATION)
   b. Accept new connection
   c. Retain routes, mark as stale

3. On session re-establishment:
   a. Check new GR Capability:
      - If no GR Capability: delete all stale routes
      - If AFI/SAFI missing: delete stale routes for that AFI/SAFI
      - If F bit not set: delete stale routes for that AFI/SAFI
   b. Do NOT set R bit unless self has restarted

4. During session:
   a. Replace stale routes with new updates
   b. On End-of-RIB for AFI/SAFI:
      - Delete remaining stale routes for that AFI/SAFI

5. On Restart Time expiry (session not re-established):
   a. Delete all stale routes from peer
```

### End-of-RIB Detection

```
For IPv4 Unicast:
  UPDATE where:
    - Withdrawn Routes Length == 0
    - Total Path Attribute Length == 0
    - No NLRI

For other AFI/SAFI:
  UPDATE containing:
    - MP_UNREACH_NLRI attribute only
    - Withdrawn Routes in MP_UNREACH_NLRI is empty
    - Correct AFI/SAFI for target address family
```

## Pitfalls

### Edge Cases

- **Consecutive restarts:** Must delete previously stale routes before marking current routes as stale (prevents stale route accumulation)
- **R-bit deadlock avoidance:** When R bit is set, peer MUST NOT wait for End-of-RIB before sending routes (prevents mutual waiting)
- **Empty GR Capability:** Valid - indicates support for receiving speaker procedures only, Restart Time is irrelevant
- **NOTIFICATION terminates GR:** Normal BGP procedures apply - no route retention on NOTIFICATION

### Interop

- **Non-GR peers:** Exclude from End-of-RIB wait list during selection deferral
- **R-bit peers:** Exclude from End-of-RIB wait list (they're also restarting)
- **Capability duplication:** Use only last instance if multiple GR capabilities received

### Security

- **DoS via spurious connections:** New connections can terminate existing sessions; mitigate with TCP MD5 (RFC 2385)
- **Route injection window:** Attacker connection replaced by legitimate peer reconnection (brief window)
- **Errata 6217 (held):** Potential capability downgrade via NOTIFICATION for permanent hijacking - consider in security-sensitive deployments

## Compatibility

### With Non-GR Peers

- No route retention on session failure
- Standard BGP behavior applies
- Can still send End-of-RIB marker (recommended even without GR)

### Feature Negotiation

- GR Capability exchanged in OPEN
- Per-AFI/SAFI granularity for forwarding state
- Absence of AFI/SAFI tuple = no forwarding preservation for that family
- Empty capability = receiving speaker support only

### Fallback Behavior

| Scenario | Behavior |
|----------|----------|
| Peer lacks GR Capability | Standard BGP (delete routes on session down) |
| Peer's GR lacks AFI/SAFI | Delete stale routes for that AFI/SAFI immediately |
| Peer's F bit = 0 | Delete stale routes for that AFI/SAFI immediately |
| NOTIFICATION received | Standard BGP (delete all routes) |

## Errata

| ID | Status | Impact |
|----|--------|--------|
| 7915 | Verified | Metadata: RFC updates 4271 (FSM changes) |
| 6217 | Held | Security: capability downgrade attack vector |
| 4193 | Held | Editorial: Section 4.2 references "Section 8" should be "Section 5" |

## Compliance Checklist

- [ ] [RFC4724-3-1] [MUST] A BGP speaker MUST NOT include more than one instance of the Graceful Restart Capability in the capability advertisement (Section 3) {single-polarity: positive; ze's GR sender emits exactly one code-64 declaration per peer (internal/component/bgp/plugins/gr/gr.go:703 extractGRCapabilities appends one CapabilityDecl per peer) and the encoder writes a single TLV (internal/core/bgp/capability/capability.go:553 WriteTo); there is no code path that emits two instances, so the more-than-one case cannot be constructed to test negatively}
- [ ] [RFC4724-3-2] [MUST] Reserved bits in Restart Flags MUST be set to zero by the sender (Section 3)
- [ ] [RFC4724-3-3] [MUST] Reserved bits in Address Family Flags MUST be set to zero by the sender (Section 3)
- [ ] [RFC4724-3-4] [MUST] If more than one instance of the Graceful Restart Capability is received, the receiver MUST ignore all but the last instance (Section 3)
- [ ] [RFC4724-3-5] [MUST] When R bit is set, peer MUST NOT wait for End-of-RIB marker from the speaker before advertising routing information (Section 3) {not-applicable: ze advertises its Adj-RIB-Out and per-family End-of-RIB immediately on reaching Established (internal/component/bgp/reactor/peer_initial_sync.go:277,334) and has no receive-side mechanism that gates advertisement on a peer's End-of-RIB; the only End-of-RIB timer (internal/component/bgp/reactor/session_health.go:114 startEORTimer) raises a health warning and never defers advertisement, so there is no wait state for the R bit to override}
- [ ] [RFC4724-4-1] [MUST] The End-of-RIB marker MUST be sent by a BGP speaker to its peer once it completes the initial routing update for an address family (Section 4)
- [ ] [RFC4724-4-2] [MUST] Normal BGP procedures MUST be followed when the TCP session terminates due to sending or receiving a NOTIFICATION message (Section 4)
- [ ] [RFC4724-4.1-1] [MUST] Restarting Speaker MUST retain, if possible, the forwarding state for BGP routes in Loc-RIB (Section 4.1) {gap: ze's Restarting Speaker path implements only GR signaling -- it writes a restart marker and sets the R bit (internal/component/bgp/grmarker/grmarker.go, internal/component/bgp/reactor/peer.go:574) -- and does not retain its own in-memory Loc-RIB forwarding state across a process restart within the bgp packages; the Loc-RIB is rebuilt from scratch on restart}
- [ ] [RFC4724-4.1-2] [MUST] Restarting Speaker MUST mark retained forwarding state as stale (Section 4.1) {gap: because ze does not retain its own Loc-RIB across a restart (see RFC4724-4.1-1), there is no retained own-forwarding state to mark stale; the stale-marking machinery (internal/component/bgp/plugins/rib/rib_commands.go:817 markStaleCommand) applies to routes received from a restarting peer, not to ze's own routes on ze's restart}
- [ ] [RFC4724-4.1-3] [MUST NOT] Restarting Speaker MUST NOT differentiate between stale and other information during forwarding (Section 4.1) {gap: this governs forwarding over ze's own retained stale Loc-RIB, which ze does not build on restart (see RFC4724-4.1-1); the generic non-differentiation of level-1 stale in best-path selection (internal/component/bgp/plugins/rib/bestpath.go:308) is the Receiving Speaker path for peer routes, not ze's own routes as a Restarting Speaker}
- [ ] [RFC4724-4.1-4] [MUST] Restarting Speaker MUST set the "Restart State" (R) bit in the Graceful Restart Capability of the OPEN message (Section 4.1)
- [ ] [RFC4724-4.1-5] [MUST] Restarting Speaker MUST defer route selection per address family until End-of-RIB from all peers or Selection_Deferral_Timer expires (Section 4.1) {gap: ze runs best-path selection as updates arrive and has no selection-deferral path keyed on End-of-RIB; the only End-of-RIB timer (internal/component/bgp/reactor/session_health.go:114 startEORTimer) raises a health warning and does not gate route selection}
- [ ] [RFC4724-4.1-6] [MUST] After route selection, forwarding state MUST be updated and stale information MUST be removed (Section 4.1) {gap: this is the completion step of the deferred-selection cycle ze does not run (see RFC4724-4.1-5); with no own-Loc-RIB stale state (RFC4724-4.1-1) there is no post-selection stale removal on ze's own restart}
- [ ] [RFC4724-4.1-7] [MUST] Once initial update is complete, the End-of-RIB marker MUST be sent (Section 4.1)
- [ ] [RFC4724-4.1-8] [MUST] An implementation MUST support a configurable Selection_Deferral_Timer (Section 4.1) {gap: ze exposes no Selection_Deferral_Timer configuration -- the GR YANG model (internal/component/bgp/plugins/gr/yang/ze-graceful-restart.yang) carries restart-time and long-lived-stale-time only, and no code defers selection (see RFC4724-4.1-5)}
- [ ] [RFC4724-4.2-1] [MUST] Receiving Speaker MUST treat subsequent open connection from peer as termination of old TCP session when GR Capability was received (Section 4.2) {gap: ze follows plain RFC 4271 Section 6.8 collision detection -- a new inbound connection while the session is Established is rejected with Cease/Connection Collision (internal/component/bgp/reactor/reactor_connection.go:134-136), with no GR-capability branch that treats the new OPEN as terminating the old session}
- [ ] [RFC4724-4.2-2] [MUST] Previous TCP session MUST be closed, and the new one retained (Section 4.2) {gap: the same RFC 4271 collision path closes the NEW connection and keeps the existing Established session (internal/component/bgp/reactor/reactor_connection.go:134-136 rejectConnectionCollisionWithSettings), the opposite of the RFC 4724 Section 4.2 override that closes the previous session and retains the new one}
- [ ] [RFC4724-4.2-3] [MUST] Receiving Speaker MUST retain routes from peer for all address families previously in Graceful Restart Capability and MUST mark them as stale (Section 4.2)
- [ ] [RFC4724-4.2-4] [MUST] On consecutive restarts, previously stale routes from the peer MUST be deleted (Section 4.2)
- [ ] [RFC4724-4.2-5] [MUST NOT] Receiving Speaker MUST NOT differentiate between stale and other routing information during forwarding (Section 4.2)
- [ ] [RFC4724-4.2-6] [MUST NOT] The "Restart State" bit in the Receiving Speaker's OPEN MUST NOT be set unless the Receiving Speaker has itself restarted (Section 4.2)
- [ ] [RFC4724-4.2-7] [MUST] If session not re-established within Restart Time, Receiving Speaker MUST delete all stale routes from the peer (Section 4.2)
- [ ] [RFC4724-4.2-8] [MUST] If F bit not set, or AFI/SAFI missing, or no GR capability on re-establishment, Receiving Speaker MUST immediately remove all stale routes for that address family (Section 4.2)
- [ ] [RFC4724-4.2-9] [MUST] Receiving Speaker MUST send End-of-RIB marker once it completes the initial update (Section 4.2)
- [ ] [RFC4724-4.2-10] [MUST] Receiving Speaker MUST replace stale routes by routing updates received from the peer (Section 4.2)
- [ ] [RFC4724-4.2-11] [MUST] Once End-of-RIB is received from peer, Receiving Speaker MUST immediately remove any routes still marked as stale for that address family (Section 4.2)
- [ ] [RFC4724-2-1] [RECOMMENDED] Sending End-of-RIB upon completion of initial update is recommended even without GR (Section 2)
- [ ] [RFC4724-4-3] [RECOMMENDED] Advertising Graceful Restart Capability even without forwarding preservation ability is recommended (Section 4)
- [ ] [RFC4724-4-4] [MAY] A BGP speaker MAY advertise the Graceful Restart Capability for an address family if it can preserve forwarding state (Section 4)
- [ ] [RFC4724-4.2-12] [MAY] Receiving Speaker MAY delete all stale routes if peer's forwarding state is determined non-viable (e.g., via BFD) (Section 4.2)
- [ ] [RFC4724-4.2-13] [MAY] An implementation MAY support a configurable stale route retention timer (Section 4.2)
