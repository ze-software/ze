# RFC 9256: Segment Routing Policy Architecture

## Meta

| Field | Value |
|-------|-------|
| RFC | 9256 |
| Title | Segment Routing Policy Architecture |
| Status | Standards Track |
| Date | July 2022 |
| Authors | C. Filsfils, K. Talaulikar, D. Voyer, A. Bogdanov, P. Mattes |
| Enrolment | enrolled |
| Enrolment reason | Segment Routing Policy Architecture |
| Support | bgp-attributes 310 |
| Support area | Segment Routing Policy Architecture |
| Support status | Partial |
| Support coverage | Ze is an SR Policy originator, not a headend: it builds the SR Policy identification tuple into the NLRI key (internal/component/bgp/plugins/nlri/srpolicy/types.go), parses it back (types.go), and encodes a candidate path's preference, priority, MPLS and SRv6 binding SID, weighted segment lists of Type A and Type B segments, and the policy and candidate-path names into the Tunnel Encapsulation attribute (config.go). Symbolic names stay out of the NLRI key, and the segment encoder admits only Type A and Type B. Requirements bound per line in rfc/short/rfc9256.md. |
| Support remaining | Six MUST gaps annotated in rfc/short/rfc9256.md, all from ze holding no SR Policy state: RFC9256-2.6-1 -- the only candidate-path identity is the RFC 9830 distinguisher in the NLRI key and no candidate-path store resolves add, delete or modify; RFC9256-4-6 -- no code turns a segment list into a label stack or an SRv6 SID list, and a received Tunnel Encapsulation attribute stays raw TLV bytes with only the Preference sub-TLV decoded (internal/core/bgp/attribute/tunnel_encap.go); RFC9256-5.1-2 and RFC9256-5.1-5 -- parseSegmentList determines no segment-list validity (empty list, weight 0 and mixed SR-MPLS/SRv6 lists are all accepted) and ze resolves no SID, so first-SID reachability is never established; and RFC9256-6.1-3 and RFC9256-6.2-4 -- the binding SID is encoded from configuration with no allocation table, no SRLB availability check and no alert. Headend obligations (composite candidate paths, Originator sub-TLV, candidate-path selection, active-path forwarding, dynamic candidate paths, Specified-BSID-only, policy state reporting) are annotated not-applicable: ze instantiates no policy. |

**Purpose:** Defines the architecture for Segment Routing (SR) Policies, including policy identification, candidate path selection, segment list structure, and traffic steering. This is the conceptual architecture document; BGP encoding is in RFC 9830.

**Scope:** SR-MPLS and SRv6 data planes. Defines the <Headend, Color, Endpoint> identification tuple and the segment type taxonomy (Types A through K).

## Key Concepts

### SR Policy Identification (Section 2.1)

An SR Policy is identified by the tuple:
- **Headend:** the node where the policy is instantiated
- **Color:** a 32-bit value used with BGP Color Extended Community for steering
- **Endpoint:** the destination of the policy (IPv4 or IPv6 address, or unspecified 0.0.0.0/::)

### Candidate Paths (Section 2.9)

An SR Policy has one or more candidate paths. Each is identified by:
- **Protocol origin:** LOCAL (10), PCEP (20), BGP (30)
- **Originator:** ASN + node address of the source
- **Discriminator:** 32-bit value for uniqueness

Selection uses: preference (highest wins), then protocol origin tiebreak.

### Segment Types (Section 4)

| Type | Code | Description |
|------|------|-------------|
| A | 1 | SR-MPLS label |
| B | 13 | SRv6 SID |
| C | 3 | IPv4 Node + SR Algorithm (+ optional MPLS SID) |
| D | 4 | IPv6 Node + SR Algorithm (+ optional MPLS SID) |
| E | 5 | IPv4 Node + Local Interface ID (+ optional MPLS SID) |
| F | 6 | IPv4 Adjacency (+ optional MPLS SID) |
| G | 7 | IPv6 Link-local Adjacency + Interface IDs (+ optional MPLS SID) |
| H | 8 | IPv6 Adjacency (+ optional MPLS SID) |
| I | 14 | IPv6 Node + SR Algorithm (+ optional SRv6 SID + Endpoint Behavior) |
| J | 15 | IPv6 Link-local Adjacency + Interface IDs + Algorithm (+ optional SRv6 SID + EB) |
| K | 16 | IPv6 Adjacency + SR Algorithm (+ optional SRv6 SID + EB) |

Types C-K are defined in RFC 9831.

### Binding SID (Section 6)

A Binding SID (BSID) is bound to an SR Policy. Packets arriving with the BSID as top label/SRv6 SID are steered into the policy. BSID can be:
- SR-MPLS label (20-bit)
- SRv6 SID (128-bit)

### Weight-Based ECMP (Section 2.10)

Multiple segment lists within a candidate path can carry a weight for ECMP distribution.

## Compliance Checklist

Every RFC 2119 keyword occurrence in RFC 9256 becomes one line below. Duplicate
obligations restated across sections are merged and anchored to the first section
that states them. Lines are grouped MUST-level first, then SHOULD-level, then
MAY-level, and ordered by section within each group. RFC 9256 is an architecture
document (it "does not define any new protocol extensions", §10); most obligations
govern headend behavior (identification, validation, selection, BSID handling,
steering) rather than wire encoding, which lives in RFC 9830/9831.

### MUST-level

- [ ] [RFC9256-2.1-1] [MUST] An SR Policy MUST be identified through the tuple <Headend, Color, Endpoint> (§2.1) {not-applicable: the three-element tuple is headend-scoped identification, and ze holds no headend. SRPolicy carries distinguisher, color, endpoint and afi and no headend field (internal/component/bgp/plugins/nlri/srpolicy/types.go:35-40), and grep -rni headend --include=*.go over internal/ and pkg/ matches only internal/component/bgp/plugins/nlri/srpolicy/rfc9256_test.go. What ze does implement -- identification by <Color, Endpoint> within one headend's context -- is the separate RFC9256-2.1-2, which the NLRI key does satisfy; this line is the same not-applicable shape as RFC9256-2.1-3, RFC9256-2.9-1 and RFC9256-7-1, which already record that ze instantiates no policy at a headend}
- [ ] [RFC9256-2.1-2] [MUST] In the context of a specific headend, an SR Policy MUST be identified by the <Color, Endpoint> tuple (§2.1) {single-polarity: positive; every <Color, Endpoint> pair identifies a policy, so no conforming input exists that identification must REFUSE, and a counter-pole would have to assert a non-identification that the requirement never mandates. The discrimination against a key that ignores its inputs is inside the positive test: a different color and a different endpoint each change the key, while a changed preference does not}
- [ ] [RFC9256-2.1-3] [MUST] The headend is specified as an IPv4 or IPv6 address and MUST resolve to a unique node in the SR domain (§2.1) {not-applicable: ze originates and parses SR Policy NLRI but instantiates no policy at a headend; grep for headend or head-end across internal/component/bgp and internal/component/mpls matches nothing, and the SR Policy plugin carries no headend field to specify or resolve (internal/component/bgp/plugins/nlri/srpolicy/types.go:35)}
- [ ] [RFC9256-2.2-1] [MUST] The endpoints of the constituent SR Policies and the parent SR Policy MUST be identical (§2.2) {not-applicable: composite candidate paths have no producer; grep -rniE composite over internal, pkg and cmd matches only report subjects and capability sub-components, and parseConfigRoute accepts no constituent-policy keyword (internal/component/bgp/plugins/nlri/srpolicy/config.go:72)}
- [ ] [RFC9256-2.2-2] [MUST] The colors of each of the constituent SR Policies and the parent SR Policy MUST be different (§2.2) {not-applicable: composite candidate paths have no producer; parseConfigRoute's keyword switch has no constituent-policy or composite spelling, so ze never forms a parent policy whose constituents could share a color (internal/component/bgp/plugins/nlri/srpolicy/config.go:72)}
- [ ] [RFC9256-2.2-3] [MUST NOT] The constituent SR Policies MUST NOT use composite candidate paths (§2.2) {not-applicable: composite candidate paths have no producer; grep -rniE composite over internal, pkg and cmd finds no SR Policy match, so ze forms no constituent policy that could carry one (internal/component/bgp/plugins/nlri/srpolicy/config.go:72)}
- [ ] [RFC9256-2.4-1] [MUST] For the Originator ASN, if 2-byte ASNs are in use the low-order 16 bits MUST be used and the high-order bits MUST be set to 0 (§2.4) {not-applicable: ze encodes no Originator sub-TLV; the SR Policy sub-TLV constant set is preference, binding-sid, priority, srv6-binding-sid, segment-list and the two name sub-TLVs only, and grep for ProtocolOrigin or OriginatorASN over internal, pkg and cmd matches nothing (internal/component/bgp/plugins/nlri/srpolicy/config.go:23)}
- [ ] [RFC9256-2.4-2] [MUST] For the Originator node address, IPv4 addresses MUST be encoded in the lowest 32 bits and the high-order bits MUST be set to 0 (§2.4) {not-applicable: ze encodes no Originator sub-TLV, so it writes no originator node address; buildTunnelEncap emits only the preference, BSID, priority, segment-list and name sub-TLVs (internal/component/bgp/plugins/nlri/srpolicy/config.go:339)}
- [ ] [RFC9256-2.6-1] [MUST] The identity of a candidate path MUST be uniquely established in the context of an SR Policy <Headend, Color, Endpoint> to handle add, delete, or modify operations unambiguously (§2.6) {gap: the only candidate-path identity ze produces is the RFC 9830 distinguisher in the NLRI key, written by (*SRPolicy).WriteTo (internal/component/bgp/plugins/nlri/srpolicy/types.go:91); ze keeps no candidate-path store, so an add, delete or modify is never resolved against a candidate-path identity, and neither a protocol origin nor an originator is carried}
- [ ] [RFC9256-2.6-3] [MUST NOT] Symbolic candidate-path names MUST NOT be considered as identifiers for a candidate path (§2.6)
- [ ] [RFC9256-2.9-1] [MUST] Whenever a new path is learned, an active path is deleted, an existing path's validity changes, or an existing path is changed, the candidate-path selection process MUST be re-executed (§2.9) {not-applicable: ze runs no candidate-path selection; there is no candidate-path store and no preference comparison anywhere (grep for protocol-origin or active candidate over internal matches nothing), and the SR Policy plugin registers only an NLRI codec and a config route encoder (internal/component/bgp/plugins/nlri/srpolicy/register.go:29)}
- [ ] [RFC9256-2.11-1] [MUST] Only the active candidate path MUST be used for forwarding traffic steered onto that policy, except in scenarios such as fast reroute where a backup candidate path may be used (§2.11) {not-applicable: no SR Policy reaches forwarding; SAFI 73 appears only in the family registry and the NLRI codec (internal/core/family/family.go:93, internal/component/bgp/plugins/nlri/srpolicy/types.go:74), and grep for SAFISRPolicy over internal/plugins/fib and internal/component/resolve matches nothing}
- [ ] [RFC9256-4-6] [MUST] When building the label stack or SRv6 SID list, the node instantiating the policy MUST interpret the first segment as the topmost MPLS label / first SRv6 SID and the last segment as the bottommost label / last SID (§4) {gap: no code converts an SR Policy segment list into a label stack or an SRv6 SID list; buildSegmentListSubTLV only serializes the configured segments in their configured order (internal/component/bgp/plugins/nlri/srpolicy/config.go:428), and a received Tunnel Encapsulation attribute is kept as raw TLV bytes of which only the Preference sub-TLV is ever decoded (internal/core/bgp/attribute/tunnel_encap.go:39 and :137)}
- [ ] [RFC9256-5.1-2] [MUST] A segment list of an explicit candidate path MUST be declared invalid when it is empty, its weight is 0, it mixes SR-MPLS and SRv6 segment types, the headend cannot resolve the first SID to outgoing interface(s)/next-hop(s), it cannot resolve any non-first SID of type C-K to an MPLS label or SRv6 SID, or verification fails for any SID for which verification was requested (§5.1) {gap: parseSegmentList builds a segment list with no validity determination; it accepts a list with zero segments, accepts weight 0, and appends Type A and Type B segments into one list without a data-plane check (internal/component/bgp/plugins/nlri/srpolicy/config.go:267), and ze resolves no SID to an outgoing interface or next hop}
- [ ] [RFC9256-5.1-4] [MUST] Types A or B MUST be used for the SIDs whose reachability cannot be verified (§5.1) {single-polarity: positive; the segment encoder accepts only type-a and type-b and returns an error for every other segment spelling (internal/component/bgp/plugins/nlri/srpolicy/config.go:276), so every SID ze emits already carries its value outright; ze holds no SID verification state whose failure could produce a violating segment to reject}
- [ ] [RFC9256-5.1-5] [MUST] The first SID MUST always be reachable regardless of its type (§5.1) {gap: ze resolves no SID; the first segment of a segment list is encoded straight from configuration with no reachability lookup (internal/component/bgp/plugins/nlri/srpolicy/config.go:276), and grep for SAFISRPolicy over internal/component/resolve and internal/plugins/fib matches nothing, so first-SID reachability is never established}
- [ ] [RFC9256-5.2-1] [MUST] If no solution is found to the optimization objective and constraints, the dynamic candidate path MUST be declared invalid (§5.2) {not-applicable: ze signals only explicit segment lists and has no dynamic candidate path; parseSegmentList takes literal MPLS labels and SRv6 SIDs (internal/component/bgp/plugins/nlri/srpolicy/config.go:276) and grep for OptimizationObjective over internal, pkg and cmd matches nothing}
- [ ] [RFC9256-6.1-3] [MUST NOT] Candidate paths of different SR Policies MUST NOT have the same BSID (§6.1) {gap: buildBindingSIDSubTLV encodes whatever label the configuration names (internal/component/bgp/plugins/nlri/srpolicy/config.go:381) and ze keeps no BSID allocation table, so two SR Policies configured with the same BSID are both encoded and advertised}
- [ ] [RFC9256-6.2-4] [MUST] When the specified BSID is not available (optionally not in the SRLB), an alert message MUST be generated via mechanisms like syslog (§6.2) {gap: ze performs no BSID availability check; buildBindingSIDSubTLV encodes the configured label unconditionally (internal/component/bgp/plugins/nlri/srpolicy/config.go:381) and the only SRLB allocator in ze belongs to OSPF Adj-SIDs (internal/plugins/ospf/sr_adjsid.go:115), which no SR Policy code consults, so no alert is raised}
- [ ] [RFC9256-6.2.3-3] [MUST] Under Specified-BSID-only behavior, when a candidate path has an unspecified or unavailable BSID it is considered invalid and an alert MUST be triggered via mechanisms like syslog (§6.2.3) {not-applicable: ze implements no Specified-BSID-only behavior; grep for specified-bsid or SpecifiedBSID over internal, pkg and cmd matches nothing and parseConfigRoute's keyword switch has no spelling for it (internal/component/bgp/plugins/nlri/srpolicy/config.go:72)}
- [ ] [RFC9256-6.2.3-4] [MUST] Under Specified-BSID-only behavior, other candidate paths MUST then be evaluated for becoming the active candidate path (§6.2.3) {not-applicable: ze implements no Specified-BSID-only behavior and no candidate-path selection, so there is no evaluation of other candidate paths to perform; the SR Policy plugin registers only an NLRI codec and a config route encoder (internal/component/bgp/plugins/nlri/srpolicy/register.go:29)}
- [ ] [RFC9256-7-1] [MUST] The SR Policy state MUST also reflect the reason when a policy and/or its candidate path is not active due to validation errors or not being preferred (§7) {not-applicable: ze keeps no SR Policy state; grep for SRPolicyState or policyState over internal, pkg and cmd matches nothing, grep for sr-policy over internal/component/cli matches nothing, and an SR Policy route is an ordinary BGP NLRI carried in the RIB (internal/component/bgp/plugins/nlri/srpolicy/register.go:29)}

### SHOULD-level

- [ ] [RFC9256-2.1-4] [SHOULD] The endpoint is specified as an IPv4 or IPv6 address and SHOULD resolve to a unique node in the domain (§2.1)
- [ ] [RFC9256-2.3-1] [RECOMMENDED] The RECOMMENDED default Protocol-Origin values are PCEP=10, BGP SR Policy=20, and Via Configuration=30 (§2.3)
- [ ] [RFC9256-2.7-1] [RECOMMENDED] It is RECOMMENDED that each candidate path of a given SR Policy has a different Preference (§2.7)
- [ ] [RFC9256-2.8-1] [RECOMMENDED] The RECOMMENDED candidate-path validity criterion is the validity of at least one of its constituent segment lists (§2.8)
- [ ] [RFC9256-4-4] [SHOULD] When the algorithm is not specified for SID types that allow it, the headend SHOULD use the Strict Shortest Path algorithm if available and otherwise SHOULD use the default Shortest Path algorithm (§4)
- [ ] [RFC9256-4.1-1] [SHOULD] When an "IPv6 Explicit NULL label" is not present as the bottom label, the headend SHOULD automatically impose one (§4.1)
- [ ] [RFC9256-6.1-2] [SHOULD] Candidate paths of the same SR Policy SHOULD have the same BSID (§6.1)
- [ ] [RFC9256-6.2-6] [SHOULD] Dynamically bound BSIDs SHOULD use an available SID outside the SRLB (§6.2)
- [ ] [RFC9256-6.2-9] [SHOULD NOT] The BSID SHOULD NOT be used as an identification of an SR Policy (§6.2)

### MAY-level

- [ ] [RFC9256-2.1-5] [MAY] An implementation MAY allow the assignment of a symbolic name of printable ASCII characters to an SR Policy for debugging/troubleshooting (§2.1)
- [ ] [RFC9256-2.1-6] [MAY] The SR Policy name MAY also be signaled along with a candidate path of the SR Policy (§2.1)
- [ ] [RFC9256-2.1-7] [MAY] An SR Policy MAY have multiple names associated with it when different names arrive with different candidate paths (§2.1)
- [ ] [RFC9256-2.3-2] [MAY] Implementations MAY allow modifications of the default Protocol-Origin values, similar to a routing administrative distance (§2.3)
- [ ] [RFC9256-2.4-3] [MAY] When provisioning is via configuration, the Originator ASN and node address MAY be set to either the headend or the provisioning controller/node ASN and address (§2.4)
- [ ] [RFC9256-2.6-2] [MAY] Candidate paths MAY also be assigned or signaled with a symbolic name of printable ASCII characters for debugging/troubleshooting (§2.6)
- [ ] [RFC9256-2.12-1] [MAY] An implementation MAY provide a per-policy priority configuration governing the order in which policies are re-computed (§2.12)
- [ ] [RFC9256-2.12-2] [MAY] A candidate path MAY be signaled with a priority value (§2.12)
- [ ] [RFC9256-4-1] [MAY] A Type A SID MAY be any MPLS label, including special-purpose labels such as explicit-null (§4)
- [ ] [RFC9256-4-2] [MAY] For SRv6 SID types (B, I, J, K), the SRv6 SID behavior and structure MAY also be provided for the headend to validate the SID (§4)
- [ ] [RFC9256-4-3] [MAY] For SID types that resolve to a Prefix/Adjacency SID (C, D, I, J, K), the SR Algorithm to be used MAY also be provided (§4)
- [ ] [RFC9256-4-5] [MAY] For SID types C through K, a SID value MAY also be optionally provided to the headend for verification (§4)
- [ ] [RFC9256-5.1-1] [MAY] An explicit candidate path MAY consist of a single explicit segment list containing only an implicit-null label to indicate pop-and-forward behavior (§5.1)
- [ ] [RFC9256-5.1-3] [MAY] Implementations MAY provide a local configuration option to enable SID verification on a global, per-policy, or per-candidate-path basis (§5.1)
- [ ] [RFC9256-5.1-6] [MAY] A segment list MAY be declared invalid when its last segment is neither a Prefix SID advertised by the endpoint node nor an Adjacency SID of a link terminating on the endpoint node (§5.1)
- [ ] [RFC9256-5.1-7] [MAY] An explicit candidate path MAY be declared invalid when its constituent segment lists use segment types of different SR data planes (§5.1)
- [ ] [RFC9256-6.1-1] [MAY] Each candidate path MAY be defined with a BSID (§6.1)
- [ ] [RFC9256-6.2-1] [MAY] In the case of SR-MPLS, SRv6 BSIDs (e.g., End.BM) MAY be associated with the SR Policy in addition to the MPLS BSID (§6.2)
- [ ] [RFC9256-6.2-2] [MAY] In the case of SRv6, multiple SRv6 BSIDs (e.g., End.B6.Encaps and End.B6.Encaps.Red) MAY be associated with the SR Policy (§6.2)
- [ ] [RFC9256-6.2-3] [MAY] A headend MAY optionally check that the BSID is available within the given SID range, i.e., the SRLB (§6.2)
- [ ] [RFC9256-6.2-5] [MAY] When no BSID is available, the SR Policy MAY dynamically bind a BSID to itself (§6.2)
- [ ] [RFC9256-6.2-7] [MAY] When a new active path has no specified or available BSID, the SR Policy MAY keep the previous BSID (§6.2)
- [ ] [RFC9256-6.2-8] [MAY] The association of an SR Policy with a BSID MAY change over the life of the SR Policy (e.g., upon active path change) (§6.2)
- [ ] [RFC9256-6.2.1-1] [MAY] When all candidate paths have an unspecified BSID, a BSID MAY be dynamically bound to the SR Policy as soon as the first valid candidate path is received (§6.2.1)
- [ ] [RFC9256-6.2.3-1] [MAY] An implementation MAY support configuring the Specified-BSID-only restrictive behavior for all or individual SR Policies (§6.2.3)
- [ ] [RFC9256-6.2.3-2] [MAY] The Specified-BSID-only restrictive behavior MAY also be signaled on a per-SR-Policy basis to the headend (§6.2.3)
- [ ] [RFC9256-6.4-1] [MAY] An implementation MAY choose to associate a Binding SID with any type of interface or tunnel (§6.4)
- [ ] [RFC9256-7-2] [MAY] Implementations MAY support an administrative state to control locally provisioned policies via mechanisms like CLI or NETCONF (§7)
- [ ] [RFC9256-8.2-1] [MAY] An SR Policy MAY be enabled for the Drop-Upon-Invalid behavior (§8.2)
- [ ] [RFC9256-8.4-1] [MAY] In a BGP multi-path scenario, the BGP route MAY be resolved over a mix of paths steered over SR Policies and paths resolved via normal BGP next-hop resolution (§8.4)
- [ ] [RFC9256-8.4-2] [MAY] Implementations MAY provide options to prefer one type of path over the other, or other local policy, to select the paths (§8.4)
- [ ] [RFC9256-8.6-1] [MAY] A headend MAY support options to apply per-flow steering only for traffic matching specific prefixes (§8.6)
- [ ] [RFC9256-8.7-1] [MAY] Headend H MAY be configured with a local routing policy that overrides any BGP/IGP path and steers a specified packet on an SR Policy (§8.7)
- [ ] [RFC9256-9.3-1] [MAY] A lower-preference candidate path MAY be designated as the backup for a specific or all active candidate path(s) (§9.3)
- [ ] [RFC9256-9.3-2] [MAY] The headend MAY compute a priori and validate backup candidate paths and provision them into the forwarding plane as backup for the active path (§9.3)
- [ ] [RFC9256-9.3-3] [MAY] A fast-reroute mechanism MAY be used to trigger sub-50 msec switchover from the active to the backup candidate path (§9.3)
- [ ] [RFC9256-9.3-4] [MAY] Mechanisms like BFD MAY be used for fast detection of such failures (§9.3)

## Ze-Relevant Notes

- The NLRI and attribute wire formats are NOT in this RFC; see RFC 9830.
- Tunnel Encapsulation attribute (code 23) carries SR Policy sub-TLVs; see RFC 9012 for the attribute frame and RFC 9830 for SR Policy sub-TLV encoding.
- Segment types C-K wire encoding is in RFC 9831.
- SAFI 73 is assigned in RFC 9830, not here.

## Related RFCs

| RFC | Relationship |
|-----|-------------|
| 9830 | BGP extensions for SR Policy (NLRI wire format, SAFI 73, sub-TLVs) |
| 9831 | Segment types C-K encoding |
| 9012 | Tunnel Encapsulation attribute (code 23) base specification |
| 8402 | Segment Routing Architecture |
| 4760 | Multiprotocol BGP (MP_REACH/MP_UNREACH carrying SR Policy NLRI) |
