# RFC 5881 - BFD for IPv4 and IPv6 (Single Hop)

## Meta

| Field | Value |
|-------|-------|
| RFC | 5881 |
| Title | Bidirectional Forwarding Detection (BFD) for IPv4 and IPv6 (Single Hop) |
| Status | Standards Track |
| Date | June 2010 |
| Authors | D. Katz, D. Ward (Juniper Networks) |
| Base | RFC 5880 |
| Related | RFC 5082 (GTSM), RFC 5882 (Generic Application) |
| Enrolment | enrolled |
| Enrolment reason | BFD for IPv4/IPv6 Single Hop (RFC 5881): 12 MET (Control 3784 / Echo 3785 ports, TTL=255 transmit + GTSM receive gate, first-packet + Your-Discriminator demux, Active role, per-protocol sessions, stable transmit destination) + 3 single-polarity positive (fixed single-socket source port, echo reaches remote, on-subnet transmit destination) + 6 gap (source port 3784 not ephemeral 49152-65535, echo uses application reflection not self-addressed/forward-back/redirect-avoiding, point-to-point any-source initial demux unmodelled) + 2 not-applicable (separate L3 path is operator topology, Echo ingress-filtering is host policy) |
| Support | bgp-operations 90 |
| Support area | BFD single-hop |
| Support status | Partial |
| Support coverage | Single-hop UDP 3784 Control sessions, TTL/GTSM receive gate (TTL=255) plus TTL=255 transmit, first-packet demux by remote address/interface/protocol, Your-Discriminator demultiplexing, Active-role default, stable transmit destination, per-protocol sessions, and echo on UDP 3785; MUST-level requirements bound per requirement in `rfc/requirements/rfc5881.md`. |
| Support remaining | Six MUST gaps, gated in `rfc/short/rfc5881.md`: RFC5881-4-2 -- the control socket transmits from port 3784 rather than an ephemeral 49152-65535 source port (single-socket design, internal/component/bfd/transport/udp.go); RFC5881-2-4/4-6/4-7 -- the echo function is application-level ZEEC reflection to the peer (internal/component/bfd/engine/echo.go) rather than the RFC self-addressed, forwarding-plane-looped echo, so self-addressing, forward-back destination, and redirect-avoiding source are unimplemented; and RFC5881-6-3/6-4 -- point-to-point links are not modelled distinctly, so the first-packet demux keys on the peer source address (internal/component/bfd/engine/loop.go) instead of accepting any initial source. IPv6 dual-bind and wider deployment proof remain tracked with BFD. |

**Purpose:** Specifies how BFD is carried and demultiplexed for liveness detection across a single IP hop between directly-connected systems. Covers UDP encapsulation, port assignments, interface binding, and TTL/Hop Limit security.

**Scope:** IPv4 and IPv6, single-hop only. Tunnels that do not decrement the TTL may reuse this mechanism.

## Wire Format

### Encapsulation

BFD Control packets are UDP over IPv4 or IPv6. The BFD payload is the mandatory section (and optional authentication section) defined in RFC 5880.

| Role | UDP Destination Port |
|------|---------------------:|
| Control | 3784 |
| Echo | 3785 |

| Role | UDP Source Port |
|------|----------------:|
| Control | 49152-65535 (ephemeral), same across all packets of a session |
| Echo | Implementation choice (not specified) |

The same UDP source port MUST be reused for all Control packets in a session and SHOULD be unique across sessions on the system. With more than 16384 concurrent sessions, source ports MAY be reused, but the number of distinct sessions per source port SHOULD be minimised.

### TTL / Hop Limit Rules (Section 5)

| Condition | Transmit | Receive |
|-----------|----------|---------|
| Authentication NOT in use | MUST be 255 | MUST be discarded if != 255 |
| Authentication in use | MUST be 255 | MAY be discarded if != 255 (check MAY be done before crypto to save CPU) |

This is the Generalized TTL Security Mechanism (GTSM, RFC 5082): any packet received with TTL != 255 has been routed through a remote system and cannot have originated from the direct neighbour.

"Authentication in use" means the system is transmitting with A=1 and discarding unauthenticated packets demultiplexed to the session.

## States and Transitions

RFC 5881 adds no states. All state handling is inherited from RFC 5880.

## Demultiplexing and Session Identity (Section 3, 6)

### Active/Active Roles

Both endpoints MUST take the Active role (both send initial Control packets with Your Discriminator = 0). This is because single-hop sessions are fully described by the interface and the address pair; no passive discovery is needed.

### First-Packet Rule

A received packet with Your Discriminator = 0 MUST be associated with the session bound to:

- The remote system's IP address
- The ingress interface
- The data protocol (IPv4 or IPv6)

Once the peer's My Discriminator has been learned, Your Discriminator alone demultiplexes all subsequent packets (source address and even interface MAY change without breaking the session, subject to the TTL=255 rule).

### Per-Protocol Sessions

A separate BFD session MUST be established for each data protocol over the same link. A dual-stack link therefore requires two sessions: one IPv4, one IPv6.

### Addressing Rules

| Topology | Source Address | Destination Address |
|----------|----------------|---------------------|
| Multi-access | From the local interface's subnet | On the peer's interface in that subnet |
| Point-to-point | MAY vary across packets (any source accepted initially; subsequent packets demultiplex via Your Discriminator) | Address configured at session creation |

On a point-to-point link, the initial source address does NOT identify the session. The local system MUST NOT change its outgoing destination to track a changed peer source; it continues using the destination configured at session creation. Applications MAY be notified that the peer's source address changed.

## Echo Function

### Encapsulation

- UDP destination port 3785
- UDP source port is unspecified
- Source IP address MUST be chosen so the peer does not send ICMP/ND Redirects (typically NOT from the subnet bound to the egress interface, NOT an IPv6 link-local)
- Destination IP address MUST be chosen so the peer forwards the packet back to the local system (typically the local system's own address)

On multi-access media, the L2 destination address MUST be the remote system's MAC so the packet physically reaches the remote before the IP layer reflects it.

### Constraints

- Ingress filtering (BCP 38) is incompatible with Echo: implementations MUST either exempt Echo packets from ingress filtering or not enable Echo on filtered interfaces.
- The echo originator typically bypasses normal forwarding lookup to send a packet addressed to itself. This may require platform APIs that are not universally available.

## Algorithms

### TTL/Hop Limit Check

```
on receive Control packet:
    if bfd_auth_in_use:
        if ttl != 255:
            MAY discard (check MAY run before auth to save CPU)
    else:
        if ttl != 255:
            MUST discard
    continue to RFC 5880 reception procedure
```

### UDP Source Port Allocation

```
on session create:
    pick src_port in [49152, 65535] unique across local sessions if possible
    bind all Control packets in this session to that src_port
```

## Pitfalls

### Edge Cases

- **One session per data protocol:** dual-stack requires two sessions (one IPv4, one IPv6), not a single session carrying both.
- **Identical sessions on the same link:** MUST traverse distinct network-layer paths in both directions; the spec forbids multiple sessions sharing a single L3 path.
- **Tunnels that decrement TTL:** cannot use this mechanism. Only tunnels that preserve the inner TTL qualify.
- **Echo source address in the local subnet:** may trigger ICMP/ND Redirects from the peer, breaking echo.
- **Echo on a multi-access network:** the L2 destination MUST match the peer; L3 alone is not enough.
- **Changing peer source address on point-to-point:** the local system continues using the original destination; apps MAY be notified but BFD does not switch.

### Interop Considerations

- **Source port uniqueness** aids the peer's demultiplexing on the initial packet but is advisory. Ultimately Your Discriminator is authoritative.
- **TTL=255 is the default security perimeter** for single-hop BFD. Any firewall or overlay that rewrites TTL breaks this.
- **Same-interface constraint:** the session is bound to one interface. If routing moves traffic to a different interface, the session breaks by design.

### Security

- TTL=255 on both transmit and receive provides equivalent anti-spoofing to other infrastructure protocols (OSPF, ISIS) without cryptographic cost.
- Authentication remains optional; when combined with TTL=255, the check may be done before the cryptographic computation to reduce DoS exposure.
- Echo function does not need authentication per this RFC because the peer never parses the packet (it is looped at IP); the originator is responsible for matching returning packets to sessions.

## Compatibility

### With the Base Protocol

RFC 5881 is a companion to RFC 5880 and reuses all state machine, timer, discriminator, and FSM rules. It adds only encapsulation, port assignment, and TTL semantics.

### Related RFCs

| RFC | Relationship |
|-----|--------------|
| RFC 5880 | Base BFD protocol |
| RFC 5082 | GTSM: the TTL=255 rule |
| RFC 5882 | Generic application guidance for client protocols |
| RFC 5883 | Multi-hop BFD (uses UDP 4784, no GTSM) |
| RFC 2827 (BCP 38) | Ingress filtering, conflict with Echo |

## IANA

| Port | Role |
|------|------|
| 3784 | BFD Control (single-hop) |
| 3785 | BFD Echo |

## Compliance Checklist

- [ ] [RFC5881-2-1] [MUST] Each BFD session between a pair of systems must traverse a separate network-layer path in both directions (§2) {not-applicable: ze cannot select or guarantee the network-layer path a session's packets take; the datagram is handed to the kernel FIB by internal/component/bfd/transport/udp.go:226 (conn.WriteToUDP), and separating two sessions onto distinct L3 paths is an operator topology property the BFD plugin does not control}
- [ ] [RFC5881-2-2] [MUST] A separate BFD session must be established for each protocol (IPv4 and IPv6) over a link (§2)
- [ ] [RFC5881-2-3] [MUST] Implementations supporting Echo function must ensure ingress filtering is not used on the Echo interface, or make an exception for Echo packets (§2) {not-applicable: ingress filtering (BCP 38) is host and network policy; the echo transport opens a plain UDP socket at internal/component/bfd/bfd.go:393 (newEchoTransport) and the BFD plugin neither configures nor exempts kernel ingress filters}
- [ ] [RFC5881-2-4] [MUST] A system implementing Echo must be capable of sending packets to its own address (§2) {gap: ze's echo does not address packets to its own address; sendEchoLocked (internal/component/bfd/engine/echo.go:96) sets the datagram destination to the peer (To: PeerAddr) and relies on the peer's application-level ZEEC reflection (internal/component/bfd/engine/echo.go:203), so the RFC 5881 self-addressed echo is unimplemented}
- [ ] [RFC5881-3-1] [MUST] Both sides of a session must take the Active role (§3)
- [ ] [RFC5881-3-2] [MUST] A received packet with Your Discriminator = 0 must be associated with the session bound to the remote system, interface, and protocol (§3)
- [ ] [RFC5881-4-1] [MUST] BFD Control packets must be transmitted in UDP with destination port 3784 (§4)
- [ ] [RFC5881-4-2] [MUST] UDP source port must be in the range 49152-65535 (§4) {gap: the control transport binds one UDP socket to port 3784 (internal/component/bfd/bfd.go:356,360) and reuses it for TX (internal/component/bfd/transport/udp.go:225, conn.WriteToUDP), so the source port of transmitted Control packets is 3784, not a value in the ephemeral 49152-65535 range}
- [ ] [RFC5881-4-3] [MUST] The same UDP source port must be used for all Control packets in a session (§4) {single-polarity: positive; every Control packet in a session leaves from the one UDP socket the (vrf,mode) loop binds (internal/component/bfd/bfd.go:355-367, internal/component/bfd/transport/udp.go:218-228), so the source port is a fixed socket property; there is no per-packet source-port selection that could vary it, hence no negative state to exercise}
- [ ] [RFC5881-4-4] [MUST] Ultimately, RFC 5880 mechanisms must be used to demultiplex incoming packets to the proper session (§4)
- [ ] [RFC5881-4-5] [MUST] BFD Echo packets must be transmitted in UDP with destination port 3785 (§4)
- [ ] [RFC5881-4-6] [MUST] Echo destination address must cause the remote system to forward the packet back (§4) {gap: the echo destination is the peer address (internal/component/bfd/engine/echo.go:96, To: PeerAddr), reflected by the peer's ze application (internal/component/bfd/engine/echo.go:203), not an address chosen so the peer's forwarding plane loops the packet back, so the RFC 5881 echo dest-addressing rule is unimplemented}
- [ ] [RFC5881-4-7] [MUST] Echo source address must preclude the remote system from generating ICMP or ND Redirect messages (§4) {gap: sendEchoLocked (internal/component/bfd/engine/echo.go:83-101) sets no source address on the echo datagram (the kernel selects it) and applies no redirect-avoidance, because ze's echo is peer-addressed and application-reflected rather than looped by the peer's forwarding plane}
- [ ] [RFC5881-4-8] [MUST] Echo packets must be transmitted so they are received by the remote system (e.g., correct L2 destination on multiaccess media) (§4) {single-polarity: positive; ze transmits echo datagrams to the peer's echo port and the peer receives and reflects them (proven by the round-trip in internal/component/bfd/engine/echo_test.go:68); ze relies on the kernel for L2 delivery and has no path that would stop a well-formed echo reaching the remote, so there is no negative polarity to exercise}
- [ ] [RFC5881-5-1] [MUST] If authentication is not in use: TTL/Hop Limit must be 255 on transmit (§5)
- [ ] [RFC5881-5-2] [MUST] If authentication is not in use: received packets must be discarded if TTL/Hop Limit != 255 (§5)
- [ ] [RFC5881-5-3] [MUST] If authentication is in use: TTL/Hop Limit must be 255 on transmit (§5)
- [ ] [RFC5881-6-1] [MUST] All BFD Control packets must be transmitted over the one-hop path being protected (§6)
- [ ] [RFC5881-6-2] [MUST] On multiaccess networks, Control packets must be transmitted with source and destination addresses on the subnet (§6) {single-polarity: positive; ze transmits single-hop Control packets to the operator-configured peer (internal/component/bfd/engine/loop.go:232, To: PeerAddr) and the kernel selects the interface source; subnet membership is set by operator config and routing, and no ze code rewrites either address off-subnet, so there is no negative polarity}
- [ ] [RFC5881-6-3] [MUST NOT] On point-to-point links, source address must not be used to identify the session (§6) {gap: ze does not model point-to-point links separately; the first-packet demux keys firstPacketKey on the source address in.From (internal/component/bfd/engine/loop.go:88), so an initial packet whose source differs from the configured peer is not associated with the session, whereas RFC 5881 forbids using the source to identify a point-to-point session}
- [ ] [RFC5881-6-4] [MUST] On point-to-point links, initial BFD packet must be accepted with any source address (§6) {gap: the first-packet demux requires the source to equal the configured peer (internal/component/bfd/engine/loop.go:88-94, byKey lookup on in.From), so ze does not accept a point-to-point initial packet bearing an arbitrary source address; once a discriminator is learned, subsequent packets are demuxed by Your Discriminator alone (internal/component/bfd/engine/loop.go:82) as RFC5881-6-5 requires}
- [ ] [RFC5881-6-5] [MUST] On point-to-point links, subsequent packets must be demultiplexed solely by Your Discriminator (§6)
- [ ] [RFC5881-6-6] [MUST NOT] If received source address changes on point-to-point, local system must not use that address as the destination; must continue using the address configured at session creation (§6)
- [ ] [RFC5881-4-9] [SHOULD] UDP source port number should be unique among all BFD sessions on the system (§4)
- [ ] [RFC5881-4-10] [SHOULD NOT] Echo source address should not be part of the subnet bound to the egress interface (§4)
- [ ] [RFC5881-4-11] [SHOULD NOT] Echo source address should not be an IPv6 link-local address (§4)
- [ ] [RFC5881-7-1] [SHOULD] BFD authentication mechanism should be used for tunnels (§7)
- [ ] [RFC5881-5-4] [MAY] If authentication is in use: received packets may be discarded if TTL/Hop Limit != 255 (§5)
- [ ] [RFC5881-5-5] [MAY] TTL/Hop Limit check may be done before cryptographic authentication to save CPU (§5)
- [ ] [RFC5881-4-12] [MAY] If more than 16384 sessions, UDP source ports may be reused on multiple sessions (§4)
- [ ] [RFC5881-4-13] [SHOULD] If source ports are reused, the number of distinct uses of the same port should be minimized (§4)
- [ ] [RFC5881-4-14] [MAY] An implementation may use UDP source port to aid in demultiplexing (§4)
- [ ] [RFC5881-6-7] [MAY] An implementation may notify the application that the neighbor's source address has changed (§6)
