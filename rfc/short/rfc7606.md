# RFC 7606 - Revised Error Handling for BGP UPDATE Messages

## Meta

| Field | Value |
|-------|-------|
| RFC | 7606 |
| Title | Revised Error Handling for BGP UPDATE Messages |
| Status | Standards Track |
| Date | August 2015 |
| Updates | RFC 1997, 4271, 4360, 4456, 4760, 5543, 5701, 6368 |

**Purpose:** Minimize routing impact from malformed UPDATE messages by avoiding session resets when possible, instead using "treat-as-withdraw" or "attribute discard" approaches.

**Problem Solved:** The original BGP spec required session reset for any malformed attribute. This is problematic because:
- Affects all routes on the session, not just the bad one
- Optional transitive attributes may be "tunneled" through non-recognizing routers
- A single malformed attribute can fan out and reset many sessions

## Error-Handling Approaches

Listed from strongest to weakest action (Section 2):

| Approach | Action | When to Use |
|----------|--------|-------------|
| **Session reset** | Send NOTIFICATION, terminate session | Last resort; structural errors |
| **AFI/SAFI disable** | Ignore all subsequent routes for that AFI/SAFI | Per RFC 4760 Section 7 |
| **Treat-as-withdraw** | Process UPDATE as if routes were withdrawn | Preferred for most attribute errors |
| **Attribute discard** | Discard attribute, continue processing UPDATE | Only for attributes with no route selection effect |

### Critical Constraints

- **Treat-as-withdraw** requires successful parsing of NLRI field and/or MP_REACH/MP_UNREACH attributes
- **Attribute discard** MUST NOT be used for attributes affecting route selection
- When multiple errors exist with different approaches, use the **strongest** action

## Validation Rules

### UPDATE Message Structure (Section 3)

| Check | Valid | Invalid Action |
|-------|-------|----------------|
| Withdrawn Routes Length + Total Attribute Length + 23 > Message Length | No | Session reset (Malformed Attribute List) |
| MP_REACH_NLRI appears > 1 time | No | Session reset (Malformed Attribute List) |
| MP_UNREACH_NLRI appears > 1 time | No | Session reset (Malformed Attribute List) |
| Other attribute appears > 1 time | Yes | Discard all but first occurrence |
| Missing well-known mandatory attribute | No | Treat-as-withdraw |
| Attribute Flag O/T bits conflict with spec | No | Treat-as-withdraw (unless spec says otherwise) |

### Attribute Length Errors (Section 4)

| Check | Valid | Invalid Action |
|-------|-------|----------------|
| Last attribute length exceeds Total Attribute Length | No | Treat-as-withdraw |
| < 3 bytes remain (or < 4 if Extended Length) when parsing next attribute | No | Treat-as-withdraw |
| Attribute length = 0 (except AS_PATH, ATOMIC_AGGREGATE) | No | Treat-as-withdraw |

### NLRI Field Syntax (Section 5.3)

**IPv4 NLRI/Withdrawn Routes:**

| Check | Valid | Invalid Action |
|-------|-------|----------------|
| Any NLRI prefix length > 32 | No | Session reset (cannot parse NLRI) |
| Last NLRI length exceeds remaining data | No | Session reset (cannot parse NLRI) |

**MP_REACH_NLRI / MP_UNREACH_NLRI:**

| Check | Valid | Invalid Action |
|-------|-------|----------------|
| NLRI length inconsistent with AFI/SAFI (e.g., IPv4 > 32, IPv6 > 128) | No | AFI/SAFI disable or session reset |
| Last NLRI length exceeds remaining attribute data | No | AFI/SAFI disable or session reset |
| Attribute flags inconsistent with RFC 4760 | No | AFI/SAFI disable or session reset |
| MP_UNREACH_NLRI length < 3 | No | AFI/SAFI disable or session reset |
| MP_REACH_NLRI length < 5 | No | AFI/SAFI disable or session reset |
| MP_REACH Next Hop length inconsistent with AFI/SAFI | No | AFI/SAFI disable or session reset |

## Per-Attribute Error Handling (Section 7)

### Well-Known Mandatory Attributes

| Attribute | Type | Malformed If | Action |
|-----------|------|--------------|--------|
| ORIGIN (1) | Well-known mandatory | Length != 1 OR undefined value | Treat-as-withdraw |
| AS_PATH (2) | Well-known mandatory | See AS_PATH rules below | Treat-as-withdraw |
| NEXT_HOP (3) | Well-known mandatory | Length != 4 | Treat-as-withdraw |

### AS_PATH Malformation Rules (Section 7.2)

AS_PATH is malformed if any of:
- Unrecognized segment type
- Segment length would exceed attribute length (overrun)
- Only 1 byte remains after last segment (underrun - can't fit segment header)
- Segment has Path Segment Length = 0

**Leftmost AS check:** If implementation checks that leftmost AS = peer AS:
- SHOULD use treat-as-withdraw
- MAY use session reset if configured

### Well-Known Discretionary Attributes

| Attribute | Type | Malformed If | Action |
|-----------|------|--------------|--------|
| MULTI_EXIT_DISC (4) | Well-known discretionary | Length != 4 | Treat-as-withdraw |
| LOCAL_PREF (5) from EBGP | Well-known discretionary | Received from external peer | Attribute discard |
| LOCAL_PREF (5) from IBGP | Well-known discretionary | Length != 4 | Treat-as-withdraw |
| ATOMIC_AGGREGATE (6) | Well-known discretionary | Length != 0 | Attribute discard |
| AGGREGATOR (7) without 4-byte AS | Optional transitive | Length != 6 | Attribute discard |
| AGGREGATOR (7) with 4-byte AS | Optional transitive | Length != 8 | Attribute discard |

### Optional Attributes

| Attribute | Type | Malformed If | Action |
|-----------|------|--------------|--------|
| Community (8) | Optional transitive | Length = 0 OR length % 4 != 0 | Treat-as-withdraw |
| ORIGINATOR_ID (9) from EBGP | Optional non-transitive | Received from external peer | Attribute discard |
| ORIGINATOR_ID (9) from IBGP | Optional non-transitive | Length != 4 | Treat-as-withdraw |
| CLUSTER_LIST (10) from EBGP | Optional non-transitive | Received from external peer | Attribute discard |
| CLUSTER_LIST (10) from IBGP | Optional non-transitive | Length = 0 OR length % 4 != 0 | Treat-as-withdraw |
| MP_REACH_NLRI (14) | Optional non-transitive | See NLRI section | Session reset or AFI/SAFI disable |
| MP_UNREACH_NLRI (15) | Optional non-transitive | See NLRI section | Session reset or AFI/SAFI disable |
| Extended Community (16) | Optional transitive | Length = 0 OR length % 8 != 0 | Treat-as-withdraw |
| IPv6 Extended Community (25) | Optional transitive | Length = 0 OR length % 20 != 0 | Treat-as-withdraw |
| Traffic Engineering (24) | Optional transitive | Implementation-defined | Treat-as-withdraw |
| ATTR_SET (128) | Optional transitive | Implementation-defined | Treat-as-withdraw |

**Note:** Unrecognized Extended Community Type/Sub-Type MUST NOT be treated as error.

## MUST Requirements

### Tx (Sender)

- "The MP_REACH_NLRI or MP_UNREACH_NLRI attribute (if present) SHALL be encoded as the very first path attribute in an UPDATE message." (Section 5.1)
- "An UPDATE message MUST NOT contain more than one of the following: non-empty Withdrawn Routes field, non-empty Network Layer Reachability Information field, MP_REACH_NLRI attribute, and MP_UNREACH_NLRI attribute." (Section 5.1)

### Rx (Receiver)

- "Since older BGP speakers may not implement these restrictions, an implementation MUST still be prepared to receive these fields in any position or combination." (Section 5.1)
- "A BGP speaker advertising support for such a typed address family MUST handle routes with unrecognized NLRI types within that address family by discarding them." (Section 5.4)

### Validation

- Missing well-known mandatory attributes: "treat-as-withdraw" MUST be used (Section 3.d)
- Attribute Flag O/T conflicts: "treat-as-withdraw" approach MUST be used (Section 3.c)
- Multiple MP_REACH/MP_UNREACH: NOTIFICATION MUST be sent (Section 3.g)
- Multiple errors with same approach: specified approach MUST be used (Section 3.h)
- Multiple errors with different approaches: strongest action MUST be used (Section 3.h)

### Errors

- Withdrawn Routes Length + Total Attribute Length + 23 > Message Length: Error Subcode MUST be Malformed Attribute List (Section 3.b)
- UPDATE with path attributes but no reachable NLRI and any error other than attribute discard: "session reset" MUST be used (Section 5.2)

### State

- When treat-as-withdraw is used: routes MUST be removed from Adj-RIB-In per RFC 4271 (Section 2)

## SHOULD/MAY

- [SHOULD] Handle leftmost-AS check violations with treat-as-withdraw - maintains session stability
- [MAY] Use session reset for leftmost-AS check if configured - allows strict policy enforcement
- [MAY] Use AFI/SAFI disable per RFC 4760 Section 7 - alternative to session reset for MP errors

## Error Handling Decision Tree

```
1. Can NLRI be parsed?
   NO  --> Session reset (or AFI/SAFI disable for MP)
   YES --> Continue

2. Is it a structural error (length fields inconsistent)?
   YES --> Session reset
   NO  --> Continue

3. Is MP_REACH or MP_UNREACH duplicated?
   YES --> Session reset (Malformed Attribute List)
   NO  --> Continue

4. Is other attribute duplicated?
   YES --> Discard all but first, continue processing
   NO  --> Continue

5. Is well-known mandatory attribute missing?
   YES --> Treat-as-withdraw
   NO  --> Continue

6. Is attribute malformed?
   YES --> Use attribute-specific action from table
   NO  --> Process normally

7. Multiple attribute errors?
   Same approach for all --> Use that approach
   Different approaches --> Use strongest (session reset > AFI/SAFI disable > treat-as-withdraw > attribute discard)
```

## Special Cases

### UPDATE with Attributes but No NLRI (Section 5.2)

End-of-RIB (RFC 4724) can be:
- Empty UPDATE message
- UPDATE with only MP_UNREACH_NLRI containing no NLRI

For any other UPDATE with attributes but no reachable NLRI:
- If any error other than "attribute discard" is encountered
- MUST use "session reset" (cannot confirm NLRI parsed correctly)

### Typed NLRI Address Families (Section 5.4)

For AFI/SAFIs with typed NLRI (MCAST-VPN, MCAST-VPLS, EVPN):
- Unrecognized NLRI types: discard the route
- Do NOT reset session for unknown types

### Attributes Not Discussed

These attributes already have RFC 7606-consistent error handling:
- AS4_PATH (17)
- AS4_AGGREGATOR (18)
- PMSI_TUNNEL (22)
- Tunnel Encapsulation (23)
- AIGP (26)
- PE Distinguisher Labels (27)
- BGP-LS Attribute (29)

These attributes are deprecated:
- DPA (11)
- ADVERTISER (12)
- RCID_PATH/CLUSTER_ID (13)
- SAFI Specific Attribute (19)
- Connector Attribute (20)
- AS_PATHLIMIT (21)
- BGP Entropy Label Capability (28)

## Operational Considerations (Section 6)

### IBGP Inconsistency Risk

Using treat-as-withdraw on IBGP sessions can cause:
- Inconsistent routing within AS
- Forwarding loops
- Black holes

**Mitigation:** Trace malformed attributes to ingress router, apply filter there.

### Debugging Requirements

Implementation MUST provide debugging facilities:
- Log error with affected NLRI
- Log entire malformed UPDATE message
- Enable root cause analysis

## Pitfalls

### Edge Cases

1. **Attribute at end of UPDATE**: If parsing the last attribute would exceed Total Attribute Length, use treat-as-withdraw but rely on Total Attribute Length to find NLRI start

2. **Partial attribute header**: If < 3 bytes (or < 4 with Extended Length) remain, treat-as-withdraw

3. **Zero-length attributes**: Only AS_PATH and ATOMIC_AGGREGATE may have zero length; all others are malformed

4. **EBGP attributes on IBGP**: LOCAL_PREF, ORIGINATOR_ID, CLUSTER_LIST have different handling based on session type

5. **Next Hop length mismatch**: If MP_REACH Next Hop length is wrong, NLRI cannot be located - must session reset or AFI/SAFI disable

### Interop

- Old implementations may send MP_REACH/MP_UNREACH in any position
- Old implementations may send multiple NLRI-bearing fields
- Must accept these but SHOULD NOT send them

### Security

- Malformed optional transitive attributes can be "tunneled" through non-recognizing routers
- Fan-out amplifies impact
- Treat-as-withdraw limits blast radius vs session reset

## Errata

### Errata 5941 (Held for Document Update)

**Section 7.16** - Editorial only: "treat as withdraw" should be "treat-as-withdraw" for consistency. No implementation impact.

## Implementation Checklist

- [ ] Attribute flag O/T bit validation with treat-as-withdraw
- [ ] Missing well-known mandatory detection with treat-as-withdraw
- [ ] Duplicate MP_REACH/MP_UNREACH detection with session reset
- [ ] Duplicate other attribute handling (keep first)
- [ ] Per-attribute length validation per table
- [ ] Per-attribute malformation checks per table
- [ ] EBGP vs IBGP handling for LOCAL_PREF, ORIGINATOR_ID, CLUSTER_LIST
- [ ] AS_PATH segment validation (type, length, overrun, underrun)
- [ ] Community/Extended Community length validation (multiple of 4/8/20)
- [ ] NLRI syntax validation
- [ ] MP_REACH/MP_UNREACH minimum length validation
- [ ] Typed NLRI discard for unknown types
- [ ] Multiple error handling (use strongest)
- [ ] Logging of malformed UPDATEs with NLRI and full message

## Compliance Checklist

**Keyword convention:** §1.1 invokes RFC 2119, under which SHALL and MUST are synonymous.
SHALL-based obligations are therefore tagged `[MUST]` throughout. Do not "correct" this.

**IDs are permanent.** `RFC7606-<section>-<n>` is referenced by `RFC requirement:` tags in
the tests that enforce each line. The id is anchored to the section it cites, and the two
must agree (`make ze-rfc-check` enforces it). Never renumber, never reuse a retired ordinal;
a new requirement takes the next free ordinal IN ITS SECTION (see `ai/skills/ze-rfc.md`).

§3.a and the six §5.3 criteria were added after an audit against `rfc/full/rfc7606.txt`
found them uncaptured; because ids are section-anchored they sit with their siblings rather
than being appended to the end of the file.

- [ ] [RFC7606-2-1] [MUST] When treat-as-withdraw is used, the UPDATE message MUST be treated as though all contained routes had been withdrawn (§2)
- [ ] [RFC7606-2-2] [MUST] Attribute discard: malformed attribute MUST be discarded and UPDATE processing continues (§2)
- [ ] [RFC7606-2-3] [MUST NOT] Attribute discard MUST NOT be used for attributes affecting route selection or installation (§2)
- [ ] [RFC7606-3.b-1] [MUST] If Withdrawn Routes Length + Total Attribute Length + 23 exceeds Message Length, Error Subcode MUST be Malformed Attribute List (§3.b)
- [ ] [RFC7606-3.c-1] [MUST] If Optional or Transitive flag bits conflict with specification, attribute MUST be treated as malformed with treat-as-withdraw, unless the specification for the attribute mandates different handling for incorrect Attribute Flags (§3.c)
- [ ] [RFC7606-3.d-1] [MUST] If any well-known mandatory attributes are missing, treat-as-withdraw MUST be used (§3.d)
- [ ] [RFC7606-3.e-1] [MUST] Treat-as-withdraw MUST be used for malformed ORIGIN, AS_PATH, NEXT_HOP, MULTI_EXIT_DISC, or LOCAL_PREF (§3.e)
- [ ] [RFC7606-3.f-1] [MUST] Attribute discard MUST be used for malformed ATOMIC_AGGREGATE or AGGREGATOR (§3.f)
- [ ] [RFC7606-3.g-1] [MUST] If MP_REACH_NLRI or MP_UNREACH_NLRI appears more than once, a NOTIFICATION MUST be sent with Malformed Attribute List (§3.g)
- [ ] [RFC7606-3.h-1] [MUST] When multiple attribute errors exist with same approach, the specified approach MUST be used (§3.h) {single-polarity: negative; "multiple attribute errors exist" has no conforming instance -- an UPDATE with zero errors is not a case of this rule, it is the absence of the rule}
- [ ] [RFC7606-3.h-2] [MUST] When multiple attribute errors exist with different approaches, the strongest action MUST be used (§3.h) {single-polarity: negative; as 3.h-1: the premise of the rule is that errors exist, so no positive case can be constructed}
- [ ] [RFC7606-3.i-1] [MUST] The Withdrawn Routes field MUST be checked for syntactic correctness in the same manner as the NLRI field (§3.i)
- [ ] [RFC7606-3.j-1] [MUST] If NLRI/MP_REACH/MP_UNREACH cannot be successfully parsed, session reset (or AFI/SAFI disable) MUST be followed (§3.j)
- [ ] [RFC7606-4-1] [MUST] In attribute length conflicts, treat-as-withdraw MUST be used, unless some other more severe error dictates a stronger approach (§4)
- [ ] [RFC7606-5.1-1] [MUST] MP_REACH_NLRI or MP_UNREACH_NLRI (if present) SHALL be encoded as the very first path attribute (§5.1) {gap: Ze intentionally emits MP_UNREACH first and MP_REACH last, treating them as NLRI-carrying wire structures rather than descriptive attributes; deliberate design decision recorded in docs/architecture/wire/mp-nlri-ordering.md and disclosed publicly in the docs/features/rfc-status.md RFC 7606 row}
- [ ] [RFC7606-5.1-2] [MUST NOT] An UPDATE message MUST NOT contain more than one of: non-empty Withdrawn Routes, non-empty NLRI, MP_REACH_NLRI, MP_UNREACH_NLRI (§5.1)
- [ ] [RFC7606-5.1-3] [MUST] Implementation MUST still be prepared to receive MP_REACH/MP_UNREACH in any position or combination from older speakers (§5.1) {single-polarity: positive; the obligation is to ACCEPT MP attributes in any position, so the only conforming assertion is acceptance -- a negative would assert a rejection the RFC forbids}
- [ ] [RFC7606-5.2-1] [MUST] For an UPDATE containing path attributes OTHER THAN MP_UNREACH_NLRI that encodes no reachable NLRI, if any path attribute error specifies an approach other than attribute discard, session reset MUST be used. The "other than MP_UNREACH_NLRI" clause exempts End-of-RIB, which §5.2 defines as an UPDATE carrying only an MP_UNREACH_NLRI encoding no NLRI (§5.2)
- [ ] [RFC7606-5.4-1] [MUST] A BGP speaker advertising typed address family support MUST discard routes with unrecognized NLRI types, unless the relevant specification for that address family specifies otherwise (§5.4) {gap: DELIBERATE divergence, ruled by Thomas 2026-07-20, not an oversight. Unrecognized typed NLRI is retained and propagated opaquely. Correcting the earlier mechanism recorded here: ParseEVPN/ParseMVPN are NOT on the RIB or forwarding path at all (their only non-test callers are the display/JSON decoders, evpn/plugin.go:444). Retention is emergent from two byte-opaque paths that never read the route type: FamilyRIB.Insert keys on the raw NLRI bytes via insertOpaque (rib/storage/familyrib.go:143-158), and buildFwdBody forwards the payload verbatim when the ContextID matches (reactor/forward_body.go:51-65). Rationale: ze has no EVPN forwarding plane (no MAC-VRF, no MAC learning, no DF election -- see the RFC 7432 row in docs/features/rfc-status.md), so it is only ever a control-plane relay for these routes. Discarding would remove function from the PEs on either side while improving nothing locally, and RFC 7606 Section 6 warns that exactly this kind of asymmetric dropping inside an AS causes 'long-lived forwarding loops and black holes'. Precedent: RFC 9552 Section 5.1 uses 5.4's own 'unless the relevant specification specifies otherwise' clause to require preserve-and-propagate for BGP-LS; RFC 7432 never did so for EVPN, which is why this stays a disclosed divergence rather than conformance by another route. Survey of the implementations on hand: ExaBGP does the same (GenericEVPN, opaque re-advertise), GoBGP session-resets on an unknown type -- the reading 5.4 exists to prevent, and one it triggers against its own I-PMSI originator. Neither cites 5.4. Disclosed in the docs/features/rfc-status.md RFC 7606 row}
- [ ] [RFC7606-7.1-1] [MUST] ORIGIN: malformed if length != 1 or undefined value; treat-as-withdraw (§7.1)
- [ ] [RFC7606-7.2-1] [MUST] AS_PATH: malformed if unrecognized segment type, overrun, underrun, or zero-length segment; treat-as-withdraw (§7.2)
- [ ] [RFC7606-7.3-1] [MUST] NEXT_HOP: malformed if length != 4; treat-as-withdraw (§7.3)
- [ ] [RFC7606-7.4-1] [MUST] MULTI_EXIT_DISC: malformed if length != 4; treat-as-withdraw (§7.4)
- [ ] [RFC7606-7.5-1] [MUST] LOCAL_PREF from eBGP: attribute discard (§7.5)
- [ ] [RFC7606-7.5-2] [MUST] LOCAL_PREF from iBGP: malformed if length != 4; treat-as-withdraw (§7.5)
- [ ] [RFC7606-7.6-1] [MUST] ATOMIC_AGGREGATE: malformed if length != 0; attribute discard (§7.6)
- [ ] [RFC7606-7.7-1] [MUST] AGGREGATOR: malformed if length != 6 (without 4-byte AS) or != 8 (with 4-byte AS); attribute discard (§7.7)
- [ ] [RFC7606-7.8-1] [MUST] Community: malformed if length is zero or not a multiple of 4; treat-as-withdraw (§7.8)
- [ ] [RFC7606-7.9-1] [MUST] ORIGINATOR_ID from eBGP: attribute discard (§7.9)
- [ ] [RFC7606-7.9-2] [MUST] ORIGINATOR_ID from iBGP: malformed if length != 4; treat-as-withdraw (§7.9)
- [ ] [RFC7606-7.10-1] [MUST] CLUSTER_LIST from eBGP: attribute discard (§7.10)
- [ ] [RFC7606-7.10-2] [MUST] CLUSTER_LIST from iBGP: malformed if length is zero or not a multiple of 4; treat-as-withdraw (§7.10)
- [ ] [RFC7606-7.11-1] [MUST] MP_REACH_NLRI with inconsistent Next Hop length: session reset or AFI/SAFI disable (§7.11)
- [ ] [RFC7606-7.13-1] [MUST] Traffic Engineering path attribute if malformed: treat-as-withdraw (§7.13)
- [ ] [RFC7606-7.14-1] [MUST] Extended Community: malformed if length is zero or not a multiple of 8; treat-as-withdraw (§7.14)
- [ ] [RFC7606-7.14-2] [MUST NOT] Unrecognized Extended Community Type or Sub-Type MUST NOT be treated as error (§7.14) {single-polarity: positive; the rule is a prohibition (MUST NOT treat an unrecognized type as an error), not a detection duty -- the only conforming observation is acceptance, and a negative would assert the rejection the RFC forbids}
- [ ] [RFC7606-7.15-1] [MUST] IPv6 Extended Community: malformed if length is zero or not a multiple of 20; treat-as-withdraw (§7.15)
- [ ] [RFC7606-7.15-2] [MUST NOT] Unrecognized IPv6 Extended Community Type or Sub-Type MUST NOT be treated as error (§7.15) {single-polarity: positive; the rule is a prohibition (MUST NOT treat an unrecognized type as an error), not a detection duty -- the only conforming observation is acceptance, and a negative would assert the rejection the RFC forbids. Now met BY DESIGN rather than by omission: validateIPv6ExtCommunityAttr takes the attribute value as `_` and tests length alone, so no Type or Sub-Type can reach a rejection}
- [ ] [RFC7606-7.16-1] [MUST] ATTR_SET if malformed: treat-as-withdraw (§7.16)
- [ ] [RFC7606-4-2] [MUST] For all path attributes other than those specified as having an attribute length that may be zero, a zero attribute length SHALL be a syntax error handled as a malformed attribute. Of the attributes considered in RFC 7606, only AS_PATH and ATOMIC_AGGREGATE may validly have zero length; the RFC leaves this open for future attributes (§4)
- [ ] [RFC7606-3.g-2] [MUST] Duplicate non-MP attributes: discard all but first occurrence (§3.g)
- [ ] [RFC7606-8-1] [MUST] A new BGP attribute specification MUST define what constitutes malformation and how to handle it (§8) {not-applicable: an obligation on the authors of FUTURE attribute specifications, not on an implementation; Ze has no code path that could satisfy or violate it}
- [ ] [RFC7606-6-1] [MUST] Implementation must provide debugging facilities, at minimum logging an error listing the NLRI involved and containing the entire malformed UPDATE. NOTE: §6 uses lowercase "must", outside the RFC 2119 keyword set scoped by §1.1; kept at MUST level as a Ze policy choice, not an RFC 2119 obligation (§6)
- [ ] [RFC7606-7.2-2] [SHOULD] Leftmost-AS check violations SHOULD use treat-as-withdraw (§7.2)
- [ ] [RFC7606-8-2] [SHOULD] New attribute specifications SHOULD provide consideration of debugging facilities (§8)
- [ ] [RFC7606-7.2-3] [MAY] Leftmost-AS check violations MAY use session reset if configured (§7.2)
- [ ] [RFC7606-2-4] [MAY] Where session reset is specified, a speaker MAY instead use the RFC 4760 §7 "AFI/SAFI disable" approach: ignore all subsequent routes with that AFI/SAFI. §7.11 mandates one of the two approaches; the permission to choose is in §2 (§2)

<!-- Added 2026-07-16 after auditing the checklist against rfc/full/rfc7606.txt. -->

- [ ] [RFC7606-3.a-1] [MUST] An error detected while processing an UPDATE for which a session reset is specified MUST be indicated by sending a NOTIFICATION with Error Code "UPDATE Message Error"; the subcode elaborates on the specific nature (§3.a)
- [ ] [RFC7606-2-5] [MUST] When treat-as-withdraw is used, the affected routes MUST be removed from the Adj-RIB-In per the procedures of RFC 4271 (§2)
- [ ] [RFC7606-5.3-1] [MUST] The NLRI or Withdrawn Routes field SHALL be considered syntactically incorrect if the length of any included NLRI is greater than 32 (§5.3)
- [ ] [RFC7606-5.3-2] [MUST] The NLRI or Withdrawn Routes field SHALL be considered syntactically incorrect if, when parsing NLRI, the length of the last NLRI found exceeds the unconsumed data remaining in the field (§5.3)
- [ ] [RFC7606-5.3-3] [MUST] MP_REACH_NLRI/MP_UNREACH_NLRI SHALL be considered incorrect if the length of any included NLRI is inconsistent with the given AFI/SAFI (§5.3)
- [ ] [RFC7606-5.3-4] [MUST] MP_REACH_NLRI/MP_UNREACH_NLRI SHALL be considered incorrect if, when parsing NLRI in the attribute, the length of the last NLRI found exceeds the unconsumed data remaining in the attribute (§5.3)
- [ ] [RFC7606-5.3-5] [MUST] MP_REACH_NLRI/MP_UNREACH_NLRI SHALL be considered incorrect if the attribute flags are inconsistent with those specified in RFC 4760. NOTE: this routes via §3.j to session reset / AFI-SAFI disable, a STRONGER action than the generic flag-conflict treat-as-withdraw of R005 (§5.3)
- [ ] [RFC7606-5.3-6] [MUST] MP_REACH_NLRI/MP_UNREACH_NLRI SHALL be considered incorrect if the MP_UNREACH_NLRI length is less than 3, or the MP_REACH_NLRI length is less than 5 (§5.3)
- [ ] [RFC7606-2-6] [MUST] When multiple errors dictate different approaches, the strongest action MUST be used; the strength ordering is session reset > AFI/SAFI disable > treat-as-withdraw > attribute discard, as listed in §2 (§2, §3.h)
