# RFC 6810: The Resource Public Key Infrastructure (RPKI) to Router Protocol

## Meta

| Field | Value |
|-------|-------|
| RFC | 6810 |
| Title | The Resource Public Key Infrastructure (RPKI) to Router Protocol |
| Status | Standards Track |
| Date | January 2013 |
| Authors | R. Bush (Internet Initiative Japan), R. Austein (Dragon Research Labs) |
| Obsoleted-by | RFC 8210 (protocol version 1) |
| Purpose | Deliver cryptographically validated prefix origin data from RPKI caches to routers for BGP origin validation |

## Wire Formats

### Common PDU Header

All PDUs share an 8-byte header:

```
 0          8          16         24        31
.-------------------------------------------.
| Protocol |   PDU    |  Session ID /       |
| Version  |   Type   |  reserved / Error   |
|    0     |   0-10   |  Code               |
+-------------------------------------------+
|                                           |
|                  Length                   |
|                                           |
`-------------------------------------------'
```

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Protocol Version | 0 | 1 byte | uint8 | 0 for this RFC |
| PDU Type | 1 | 1 byte | uint8 | See PDU Types table |
| Session ID / Reserved / Error Code | 2 | 2 bytes | uint16 | Varies by PDU type |
| Length | 4 | 4 bytes | uint32 | Total PDU length including header |

### PDU Types

| Type | Name | Length | Direction | Header Byte 2-3 |
|------|------|--------|-----------|------------------|
| 0 | Serial Notify | 12 | Cache -> Router | Session ID |
| 1 | Serial Query | 12 | Router -> Cache | Session ID |
| 2 | Reset Query | 8 | Router -> Cache | reserved = zero |
| 3 | Cache Response | 8 | Cache -> Router | Session ID |
| 4 | IPv4 Prefix | 20 | Cache -> Router | reserved = zero |
| 6 | IPv6 Prefix | 32 | Cache -> Router | reserved = zero |
| 7 | End of Data | 12 | Cache -> Router | Session ID |
| 8 | Cache Reset | 8 | Cache -> Router | reserved = zero |
| 10 | Error Report | variable | Either | Error Code |

Note: PDU types 5 and 9 are unassigned in version 0.

### Serial Notify (Type 0)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=0  |     Session ID      |
+-------------------------------------------+
|                Length=12                  |
+-------------------------------------------+
|               Serial Number               |
`-------------------------------------------'
```

Cache -> Router. Unsolicited notification that new data is available.

### Serial Query (Type 1)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=1  |     Session ID      |
+-------------------------------------------+
|                Length=12                  |
+-------------------------------------------+
|               Serial Number               |
`-------------------------------------------'
```

Router -> Cache. Request incremental update from specified serial number.

### Reset Query (Type 2)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=2  |   reserved = zero   |
+-------------------------------------------+
|                Length=8                   |
`-------------------------------------------'
```

Router -> Cache. Request complete dataset (full reset).

### Cache Response (Type 3)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=3  |     Session ID      |
+-------------------------------------------+
|                Length=8                   |
`-------------------------------------------'
```

Cache -> Router. Acknowledges query, followed by zero or more prefix PDUs, then End of Data.

### IPv4 Prefix (Type 4)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=4  |   reserved = zero   |
+-------------------------------------------+
|                Length=20                  |
+-------------------------------------------+
|  Flags   |  Prefix  |   Max    |   zero   |
|          |  Length   |  Length  |          |
+-------------------------------------------+
|              IPv4 Prefix (4 bytes)        |
+-------------------------------------------+
|         Autonomous System Number          |
`-------------------------------------------'
```

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Flags | 8 | 1 byte | uint8 | Bit 0: 1=announce, 0=withdraw |
| Prefix Length | 9 | 1 byte | uint8 | 0-32 |
| Max Length | 10 | 1 byte | uint8 | >= Prefix Length, 0-32 |
| zero | 11 | 1 byte | uint8 | Must be zero |
| IPv4 Prefix | 12 | 4 bytes | IPv4 addr | Network byte order |
| ASN | 16 | 4 bytes | uint32 | 32-bit AS number |

### IPv6 Prefix (Type 6)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=6  |   reserved = zero   |
+-------------------------------------------+
|                Length=32                  |
+-------------------------------------------+
|  Flags   |  Prefix  |   Max    |   zero   |
|          |  Length   |  Length  |          |
+-------------------------------------------+
|                                           |
|           IPv6 Prefix (16 bytes)          |
|                                           |
|                                           |
+-------------------------------------------+
|         Autonomous System Number          |
`-------------------------------------------'
```

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Flags | 8 | 1 byte | uint8 | Bit 0: 1=announce, 0=withdraw |
| Prefix Length | 9 | 1 byte | uint8 | 0-128 |
| Max Length | 10 | 1 byte | uint8 | >= Prefix Length, 0-128 |
| zero | 11 | 1 byte | uint8 | Must be zero |
| IPv6 Prefix | 12 | 16 bytes | IPv6 addr | Network byte order |
| ASN | 28 | 4 bytes | uint32 | 32-bit AS number |

### End of Data (Type 7)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=7  |     Session ID      |
+-------------------------------------------+
|                Length=12                  |
+-------------------------------------------+
|               Serial Number               |
`-------------------------------------------'
```

Cache -> Router. Signals end of data transmission. Router updates its serial to this value.

### Cache Reset (Type 8)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=8  |   reserved = zero   |
+-------------------------------------------+
|                Length=8                   |
`-------------------------------------------'
```

Cache -> Router. Informs router that incremental update is unavailable.

### Error Report (Type 10)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  | Type=10  |     Error Code      |
+-------------------------------------------+
|                  Length                   |
+-------------------------------------------+
|       Length of Encapsulated PDU          |
+-------------------------------------------+
~        Copy of Erroneous PDU              ~
+-------------------------------------------+
|           Length of Error Text            |
+-------------------------------------------+
~       Error Diagnostic Message            ~
`-------------------------------------------'
```

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Error Code | 2 | 2 bytes | uint16 | See Error Codes table |
| Length of Encapsulated PDU | 8 | 4 bytes | uint32 | 0 if no erroneous PDU |
| Erroneous PDU | 12 | variable | bytes | Copy (possibly truncated) of offending PDU |
| Length of Error Text | 12+N | 4 bytes | uint32 | 0 if no diagnostic text |
| Error Text | 16+N | variable | UTF-8 | Diagnostic message |

## Encoding Rules

- All multi-byte fields: big-endian (network byte order)
- Protocol Version: always 0 for this RFC
- Reserved/zero fields: MUST be zero on transmission, MAY be ignored on receipt
- Flags lowest bit: 1 = announce, 0 = withdraw
- Max Length MUST NOT be less than Prefix Length
- Serial Number arithmetic per RFC 1982 (wraps from 2^32-1 to 0)

## Decoding Rules

1. Read 8-byte header: Version (1), Type (1), Session ID or reserved or Error Code (2), Length (4)
2. Validate Version == 0 (or send Error Code 4)
3. Validate PDU Type is known (or send Error Code 5)
4. Read remaining (Length - 8) bytes
5. For prefix PDUs: parse Flags, Prefix Length, Max Length, Prefix, ASN
6. For Error Report: parse encapsulated PDU length, PDU copy, error text length, error text

## Validation

| Check | Valid | Invalid Action |
|-------|-------|----------------|
| Protocol Version | 0 | Error Code 4 (Unsupported Protocol Version) |
| PDU Type | Known type | Error Code 5 (Unsupported PDU Type) |
| Max Length >= Prefix Length | Yes | Error Code 0 (Corrupt Data) |
| Session ID matches | Yes | Flush all data from cache, reset session |
| Duplicate {Prefix, Len, Max-Len, ASN} | Not already active | Error Code 7 (Duplicate Announcement) |
| Withdraw unknown {Prefix, Len, Max-Len, ASN} | Already active | Error Code 6 (Withdrawal of Unknown Record) |
| Error Report for Error Report | Never | Drop session |
| Reset Query response all announce | Flags = 1 | Error Code 0 (Corrupt Data) |

## MUST Requirements

### Tx (Sender)

| Requirement | Section | Context |
|-------------|---------|---------|
| Fields with unspecified content MUST be zero on transmission | 5 | All reserved/zero fields |
| Max Length MUST NOT be less than Prefix Length | 5.1 | IPv4/IPv6 Prefix PDUs |
| An Error Report PDU MUST NOT be sent for an Error Report PDU | 5.10 | Error handling |
| If error text is present, it MUST be UTF-8 | 5.10 | Error Report text |
| If Erroneous PDU field is empty, Length of Encapsulated PDU MUST be zero | 5.10 | Generic errors |
| If diagnostic text not present, Length of Error Text MUST be zero | 5.10 | Error Report |
| In response to Reset Query, withdraw/announce field MUST be 1 (announce) | 5.5 | Cache Response to Reset |
| Session ID in End of Data MUST be same as corresponding Cache Response | 5.8 | End of Data |
| Cache MUST rate limit Serial Notifies to no more than one per minute | 6.2 | Serial Notify |
| Cache MUST ensure one and only one IPvX PDU for unique {Prefix, Len, Max-Len, ASN} | 5.6 | Duplicate prevention |

### Rx (Receiver)

| Requirement | Section | Context |
|-------------|---------|---------|
| If Session ID mismatch, MUST flush all data learned from that cache | 5.1 | Session management |
| Router MUST poll no less frequently than once an hour | 6.1 | Keep-alive / polling |
| Router MUST choose most preferred cache by configuration | 4 | Cache selection |
| If Cache Reset received and no preferred caches, router MUST issue Reset Query | 6.3 | Fallback |
| If No Data Available and no other caches, router MUST issue periodic Reset Queries | 6.4 | Recovery |

### Transport

| Requirement | Section | Context |
|-------------|---------|---------|
| Caches and routers MUST implement unprotected TCP on port rpki-rtr (323) | 7 | Mandatory transport |
| If unprotected TCP, cache and routers MUST be on same trusted network | 7 | Security |
| Cache servers supporting SSH MUST accept RSA and DSA authentication | 7.1 | SSH transport |
| TLS clients MUST present client-side certificates | 7.2 | TLS transport |
| TLS routers MUST verify cache server certificate | 7.2 | TLS transport |
| TCP MD5 MUST support key lengths of at least 80 printable ASCII bytes | 7.3 | TCP MD5 |
| TCP-AO MUST support key lengths of at least 80 printable ASCII bytes | 7.4 | TCP-AO |

## SHOULD/MAY

| Type | Requirement | Section | Notes |
|------|-------------|---------|-------|
| [SHOULD] | Cache SHOULD send Notify when serial changes | 6.2 | Analogous to DNS NOTIFY |
| [SHOULD] | Router receiving duplicate announcement SHOULD raise Error 7 | 5.6 | Duplicate detection |
| [SHOULD] | Caches and routers SHOULD use TCP-AO, SSHv2, TCP MD5, or IPsec | 7 | Protected transport |
| [SHOULD] | Both SHOULD enable transport keep-alives | 7 | Session liveness |
| [SHOULD] | Router SHOULD attempt more preferred caches on Cache Reset | 6.3 | Failover |
| [SHOULD] | Router SHOULD attempt other caches on No Data Available | 6.4 | Failover |
| [SHOULD] | Client SHOULD retain data from previous cache during switchover | 8 | Data continuity |
| [SHOULD] | Client SHOULD delete data from unreachable cache after 2x polling period | 8 | Stale data cleanup |
| [SHOULD] | Cache SHOULD be topologically close to router | 11 | Security |
| [SHOULD] | Erroneous Error Report PDU received: session SHOULD be dropped | 5.10 | Error handling |
| [MAY] | Router MAY start with Serial Query if it has unexpired data | 6.1 | Session resumption |
| [MAY] | Truncated Erroneous PDU MAY be included for oversized PDUs | 5.10 | Error Report |
| [MAY] | Routers MAY use SSHv2, TCP MD5, IPsec, or TLS transport | 7 | Transport options |

## Error Codes

| Code | Name | Fatal | Description |
|------|------|-------|-------------|
| 0 | Corrupt Data | Yes | Received PDU believed corrupt |
| 1 | Internal Error | Yes | Internal error unrelated to protocol |
| 2 | No Data Available | No | Cache has no useful data (temporary) |
| 3 | Invalid Request | Yes | Client request believed invalid |
| 4 | Unsupported Protocol Version | Yes | Unknown protocol version |
| 5 | Unsupported PDU Type | Yes | Unknown PDU type |
| 6 | Withdrawal of Unknown Record | Yes | Withdraw for non-existent {Prefix, Len, Max-Len, ASN} |
| 7 | Duplicate Announcement Received | Yes | Announce for already-active {Prefix, Len, Max-Len, ASN} |

Fatal errors SHOULD cause session to be dropped.

## Constants

| Name | Value | Usage |
|------|-------|-------|
| Protocol Version | 0 | Version 0 defined by this RFC |
| rpki-rtr port (TCP) | 323 | Unprotected TCP transport |
| rpki-rtr-tls port | 324 | TLS transport |
| SSH subsystem name | "rpki-rtr" | SSH transport subsystem |
| Max polling interval | 1 hour | Router MUST query at least this often |
| Max Notify rate | 1 per minute | Cache rate limit for Serial Notify |
| Serial Number range | 0 to 2^32-1 | Wraps, arithmetic per RFC 1982 |

## Algorithms

### Router Startup / Session Establishment

```
1. Connect to most preferred cache (by configuration preference)
2. If router has unexpired data from previous session with same cache:
   a. Send Serial Query with previous Session ID and last known Serial Number
   b. If Cache Response received: process incremental updates
   c. If Cache Reset received: go to step 3
3. Otherwise (new session or cache reset):
   a. Send Reset Query
   b. Receive Cache Response
   c. Receive zero or more IPv4/IPv6 Prefix PDUs (all announce, Flags=1)
   d. Receive End of Data with new Session ID and Serial Number
   e. Replace all data from this cache with received dataset
```

### Incremental Update (Typical Exchange)

```
1. Cache sends Serial Notify (optional, unsolicited)
2. Router sends Serial Query with current Session ID and Serial Number
3. Cache responds:
   a. If incremental available:
      - Cache Response
      - Zero or more IPv4/IPv6 Prefix PDUs (announce and withdraw)
      - End of Data with new Serial Number
   b. If no changes: End of Data with same Serial Number
   c. If incremental unavailable: Cache Reset (router must Reset Query or switch cache)
   d. If no data at all: Error Report (Code 2: No Data Available)
4. Router updates its Serial Number to value in End of Data
```

### Session ID Handling

```
1. On Cache Response to Reset Query: adopt new Session ID
2. On Cache Response to Serial Query: verify Session ID matches
3. On mismatch: flush all data from this cache, treat as new session
4. Cache generates unique Session ID on startup (pseudo-random, clock-based, or persistent)
```

## Pitfalls

### Edge Cases

- **Serial Number wrap-around:** Serial Numbers wrap from 2^32-1 to 0. Comparison uses RFC 1982 serial number arithmetic, not simple integer comparison.
- **Session ID reuse after cache restart:** If cache erroneously reuses a Session ID, router may become confused. Cache SHOULD use persistent storage or clock-based generation to avoid collisions.
- **Duplicate announcements:** Cache MUST ensure at most one active PDU per unique {Prefix, Len, Max-Len, ASN}. Router SHOULD report Error 7 on duplicates.
- **Error Report for Error Report:** MUST NOT send. If received, SHOULD drop session (prevents infinite error loops).
- **Truncated erroneous PDU:** Allowed in Error Reports when the offending PDU is excessively long.

### Interop

- **Version negotiation:** This RFC defines version 0 only. RFC 8210 defines version 1 with additional PDUs. Unsupported version triggers Error Code 4.
- **Multiple caches:** Router MAY hold data from multiple caches but MUST track data by source. Data from different caches MUST NOT be distinguished during validation (RFC 6811).
- **Cache Reset recovery:** Router SHOULD try more preferred caches first before issuing Reset Query to current cache.

### Security

- **Transport trust:** Router cannot validate RPKI cryptography itself. The cache-to-router link is the weakest point. Transport authentication is critical.
- **Cache spoofing:** Without authentication, a rogue cache could inject invalid ROA data. Use SSH, TLS, TCP-AO, or restrict to trusted network.
- **DoS via routers:** Cache should authenticate connecting routers to prevent load from unauthorized clients.

## Compatibility

### With RFC 8210 (Version 1)

- RFC 8210 obsoletes this RFC and defines protocol version 1
- Version 1 adds Router Key PDU (Type 9) for BGPsec
- Version 1 adds Refresh, Retry, and Expire interval fields to End of Data PDU
- Version 0 implementations interoperate with version 1 caches that support both versions

### Feature Negotiation

- No capability negotiation within the protocol
- Version mismatch detected by Protocol Version field in PDU header
- Unsupported version triggers Error Code 4

## Related RFCs

| RFC | Relationship |
|-----|--------------|
| RFC 1982 | Serial Number Arithmetic (used for serial comparison) |
| RFC 6480 | RPKI architecture overview |
| RFC 6481 | RPKI repository structure |
| RFC 6811 | BGP Prefix Origin Validation (consumer of RTR data) |
| RFC 8210 | Obsoletes this RFC (protocol version 1) |
| RFC 7313 | Not directly related but analogous incremental update pattern |
| RFC 5925 | TCP-AO (expected future mandatory transport) |

## Errata

None reported.

## Compliance Checklist

- [ ] [RFC6810-5-1] [MUST] Fields with unspecified content MUST be zero on transmission (Section 5)
- [ ] [RFC6810-5.1-1] [MUST] Max Length MUST NOT be less than Prefix Length (Section 5.1)
- [ ] [RFC6810-5.6-1] [MUST] Cache MUST ensure one and only one IPvX PDU for a unique {Prefix, Len, Max-Len, ASN} at any one point in time (Section 5.6) {not-applicable: cache-side emission guarantee -- ze is the RTR router; it only parses Prefix PDUs (internal/component/bgp/plugins/rpki/rtr_pdu.go:114 parsePrefixPDU) and has no Prefix PDU writer, so it never emits or enforces one-PDU-per-VRP}
- [ ] [RFC6810-5.5-1] [MUST] In response to a Reset Query, the withdraw/announce field in payload PDUs MUST have the value 1 (announce) (Section 5.5) {not-applicable: cache-side emission obligation -- ze reads the announce/withdraw flag on receipt (internal/component/bgp/plugins/rpki/rtr_pdu.go:132) but sends no Prefix PDUs, so it never sets this field on transmission}
- [ ] [RFC6810-5.8-1] [MUST] Session ID in End of Data MUST be the same as the corresponding Cache Response (Section 5.8) {not-applicable: cache-side emission obligation -- ze reads End of Data for its serial only (internal/component/bgp/plugins/rpki/rtr_session.go:291) and emits neither Cache Response nor End of Data PDUs whose Session IDs it would have to match}
- [ ] [RFC6810-5.10-1] [MUST] If the Erroneous PDU field is empty, Length of Encapsulated PDU MUST be zero (Section 5.10) {not-applicable: Error Report sender obligation -- ze only reads received Error Reports (internal/component/bgp/plugins/rpki/rtr_session.go:363) and has no Error Report writer, so it never encodes Length of Encapsulated PDU}
- [ ] [RFC6810-5.10-2] [MUST NOT] An Error Report PDU MUST NOT be sent for an Error Report PDU (Section 5.10) {not-applicable: Error Report sender obligation -- ze has no Error Report writer (it only reads them at internal/component/bgp/plugins/rpki/rtr_session.go:363), so it can never emit an Error Report about an Error Report}
- [ ] [RFC6810-5.10-3] [MUST] If error text is present, it MUST be UTF-8 encoded (Section 5.10) {not-applicable: Error Report sender obligation -- ze emits no Error Report PDU (it only reads them at internal/component/bgp/plugins/rpki/rtr_session.go:363), so it never encodes error text}
- [ ] [RFC6810-5.10-4] [MUST] If diagnostic text is not present, Length of Error Text MUST be zero (Section 5.10) {not-applicable: Error Report sender obligation -- ze emits no Error Report PDU (it only reads them at internal/component/bgp/plugins/rpki/rtr_session.go:363), so it never sets Length of Error Text}
- [ ] [RFC6810-2-1] [MUST] Incoming data associated with new serial MUST NOT be sent until the fetch is complete (Section 2, Serial Number definition) {not-applicable: cache-side transmission obligation on serial-associated data -- ze is the consumer; it buffers a delta in pendingVRPs and applies it atomically only at End of Data (internal/component/bgp/plugins/rpki/rtr_session.go:307), and sends no serial-tagged data}
- [ ] [RFC6810-5.1-2] [MUST] If Session ID mismatch, MUST completely drop the session and flush all data learned from that cache (Section 5.1) {gap: ze does not detect a Session ID change -- handlePDU adopts the Cache Response Session ID unconditionally (internal/component/bgp/plugins/rpki/rtr_session.go:233, s.sessionID = hdr.SessionID) with no comparison to the prior value and no cache flush}
- [ ] [RFC6810-4-1] [MUST] The router MUST choose the most preferred cache by configuration (Section 4) {gap: ze does not select the most preferred cache -- startSessions launches a Run goroutine for every configured cache concurrently (internal/component/bgp/plugins/rpki/rpki.go:291-298) and the parsed preference is only surfaced for display (rpki.go:1026), never used to order or choose caches}
- [ ] [RFC6810-6.1-1] [MUST] Router MUST send Serial Query or Reset Query no less frequently than once an hour (Section 6.1, Section 6.2) {single-polarity: positive; the re-query cadence is Run's post-sync wait of s.retryInterval (internal/component/bgp/plugins/rpki/rtr_session.go:106-110), defaulting to 600s (rtr_session.go:81) which is below the one-hour ceiling; there is no malformed input that yields a too-infrequent negative case}
- [ ] [RFC6810-6.2-1] [MUST] Cache MUST rate limit Serial Notifies to no more than one per minute (Section 6.2) {not-applicable: cache-side rate limit on Serial Notify emission -- ze receives Serial Notify and ignores it during sync (internal/component/bgp/plugins/rpki/rtr_session.go:359) and never sends one}
- [ ] [RFC6810-6.3-1] [MUST] If Cache Reset received and no more preferred caches, router MUST issue a Reset Query (Section 6.3)
- [ ] [RFC6810-6.4-1] [MUST] If No Data Available and no other caches, router MUST issue periodic Reset Queries (Section 6.4)
- [ ] [RFC6810-3-1] [MUST] A relying party MUST have a trust relationship with, and a trusted transport channel to, any authoritative caches it uses (Section 3) {not-applicable: operator deployment obligation -- the trust relationship and trusted channel are established by network placement; ze dials the operator-configured cache over unprotected TCP (internal/component/bgp/plugins/rpki/rtr_session.go:127) and has no in-band trust mechanism to produce or violate}
- [ ] [RFC6810-4-2] [MUST] Cache servers' clocks MUST be correct to a tolerance of approximately an hour (Section 4) {not-applicable: cache-server clock obligation -- ze is the RTR router client that dials out (internal/component/bgp/plugins/rpki/rtr_session.go:125 connectAndSync); it runs no cache and holds no clock this binds}
- [ ] [RFC6810-7-1] [MUST] Caches and routers MUST implement unprotected TCP transport on port rpki-rtr (323) (Section 7) {single-polarity: positive; ze implements the unprotected TCP transport by dialing tcp (internal/component/bgp/plugins/rpki/rtr_session.go:127) and defaults the RTR port to 323 (rpki_config.go:174); it never declines to offer unprotected TCP, so there is no negative case}
- [ ] [RFC6810-7-2] [MUST] If unprotected TCP is the transport, cache and routers MUST be on the same trusted and controlled network (Section 7) {not-applicable: operator deployment obligation -- when unprotected TCP is used the trusted-network placement is the operator's; ze dials plain TCP (internal/component/bgp/plugins/rpki/rtr_session.go:127) with no code path to enforce network topology}
- [ ] [RFC6810-7-3] [MUST] If available, caches and routers MUST use one of the more protected protocols (Section 7) {gap: ze implements no protected RTR transport -- connectAndSync dials only unprotected TCP (internal/component/bgp/plugins/rpki/rtr_session.go:127) and the plugin has no SSH, TLS, TCP-MD5, or TCP-AO client, so no more protected protocol is available to select}
- [ ] [RFC6810-7.1-1] [MUST] Cache servers supporting SSH MUST accept RSA and DSA authentication (Section 7.1) {not-applicable: cache-server SSH obligation -- ze runs no RTR SSH server and no SSH client (connectAndSync dials plain TCP, internal/component/bgp/plugins/rpki/rtr_session.go:127), so it performs no SSH authentication}
- [ ] [RFC6810-7.1-2] [MUST] SSH user authentication MUST be supported (Section 7.1) {not-applicable: SSH-transport obligation -- ze implements no SSH RTR transport (connectAndSync dials plain TCP, internal/component/bgp/plugins/rpki/rtr_session.go:127), so SSH user authentication has no code path}
- [ ] [RFC6810-8-1] [MUST] Client MUST keep data marked as to source, as later updates MUST affect the correct data (Section 8) {gap: ze does not mark VRPs by source cache -- every RTR session writes one shared ROACache (internal/component/bgp/plugins/rpki/rpki.go:292) and vrpEntry carries only MaxLength and ASN (roa_cache.go:12-15) with no source field, so a withdraw from one cache can remove a VRP another announced}
- [ ] [RFC6810-8-2] [MUST NOT] If data from multiple caches are held, implementations MUST NOT distinguish between data sources when performing validation (Section 8)
- [ ] [RFC6810-7.2-1] [MUST] TLS client routers MUST present client-side certificates (Section 7.2) {not-applicable: TLS-transport obligation -- ze implements no TLS RTR client (connectAndSync dials plain TCP, internal/component/bgp/plugins/rpki/rtr_session.go:127), so the client-certificate requirement has no code path}
- [ ] [RFC6810-7.2-2] [MUST] TLS certificates MUST include subjectAltName extension with iPAddress identities (Section 7.2) {not-applicable: TLS-transport obligation -- ze implements no TLS RTR transport (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it issues and validates no rpki-rtr TLS certificates}
- [ ] [RFC6810-7.2-3] [MUST] Cache MUST check IP address of TLS connection against iPAddress identities (Section 7.2) {not-applicable: cache-side TLS obligation -- ze runs no RTR TLS server (it is a client that dials out, internal/component/bgp/plugins/rpki/rtr_session.go:125), so it checks no connecting IP against iPAddress identities}
- [ ] [RFC6810-7.2-4] [MUST] Routers MUST verify the cache's TLS server certificate using subjectAltName dNSName identities (Section 7.2) {not-applicable: TLS-transport obligation -- ze implements no TLS RTR client (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it verifies no cache server certificate}
- [ ] [RFC6810-7.2-5] [MUST] DNS-ID identifier type support is REQUIRED in rpki-rtr TLS implementations (Section 7.2) {not-applicable: TLS-transport obligation -- ze has no rpki-rtr TLS implementation (internal/component/bgp/plugins/rpki/rtr_session.go:127 dials plain TCP), so DNS-ID identifier support does not apply}
- [ ] [RFC6810-7.2-6] [MUST] DNS-ID identifier type MUST be present in rpki-rtr server certificates (Section 7.2) {not-applicable: cache-server-certificate obligation under TLS -- ze runs no RTR TLS server and issues no server certificate (it is a plain-TCP client, internal/component/bgp/plugins/rpki/rtr_session.go:127)}
- [ ] [RFC6810-7.2-7] [MUST NOT] rpki-rtr TLS implementations MUST NOT use CN-ID identifiers for authentication (Section 7.2) {not-applicable: TLS-transport obligation -- ze has no rpki-rtr TLS implementation (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it uses no certificate identifiers, CN-ID or otherwise}
- [ ] [RFC6810-7.2-8] [MUST] The client router MUST set its "reference identifier" to the DNS name of the rpki-rtr cache (Section 7.2) {not-applicable: TLS-transport obligation -- ze implements no TLS RTR client (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it sets no TLS reference identifier}
- [ ] [RFC6810-7.3-1] [MUST] TCP MD5 implementations MUST support key lengths of at least 80 printable ASCII bytes (Section 7.3) {not-applicable: TCP-MD5 transport obligation -- ze implements no TCP-MD5 for RTR (connectAndSync dials plain TCP, internal/component/bgp/plugins/rpki/rtr_session.go:127), so it holds no MD5 keys}
- [ ] [RFC6810-7.3-2] [MUST] TCP MD5 implementations MUST support hexadecimal sequences of at least 32 characters (Section 7.3) {not-applicable: TCP-MD5 transport obligation -- ze implements no TCP-MD5 for RTR (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it parses no MD5 key material}
- [ ] [RFC6810-7.4-1] [MUST] TCP-AO implementations MUST support key lengths of at least 80 printable ASCII bytes (Section 7.4) {not-applicable: TCP-AO transport obligation -- ze implements no TCP-AO for RTR (connectAndSync dials plain TCP, internal/component/bgp/plugins/rpki/rtr_session.go:127), so it holds no AO keys}
- [ ] [RFC6810-7.4-2] [MUST] TCP-AO implementations MUST support hexadecimal sequences of at least 32 characters (Section 7.4) {not-applicable: TCP-AO transport obligation -- ze implements no TCP-AO for RTR (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it parses no AO key material}
- [ ] [RFC6810-7.4-3] [MUST] TCP-AO MAC lengths of at least 96 bits MUST be supported (Section 7.4) {not-applicable: TCP-AO transport obligation -- ze implements no TCP-AO for RTR (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it negotiates no MAC length}
- [ ] [RFC6810-7.4-4] [MUST] TCP-AO cryptographic algorithms described in RFC 5926 MUST be supported (Section 7.4) {not-applicable: TCP-AO transport obligation -- ze implements no TCP-AO for RTR (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it supports no RFC 5926 algorithms}
- [ ] [RFC6810-6.2-2] [SHOULD] Cache SHOULD send a Notify PDU when its serial changes (Section 6.2)
- [ ] [RFC6810-5.6-2] [SHOULD] Router receiving a duplicate announcement SHOULD raise Duplicate Announcement Received error (Section 5.6)
- [ ] [RFC6810-7-4] [SHOULD] Caches and routers SHOULD use TCP-AO, SSHv2, TCP MD5, or IPsec transport (Section 7)
- [ ] [RFC6810-7-5] [SHOULD] Both caches and routers SHOULD enable transport keep-alives (Section 7)
- [ ] [RFC6810-6.3-2] [SHOULD] Router SHOULD attempt more preferred caches on Cache Reset (Section 6.3)
- [ ] [RFC6810-6.4-2] [SHOULD] Router SHOULD attempt other caches on No Data Available (Section 6.4)
- [ ] [RFC6810-8-3] [SHOULD] Client SHOULD retain data from previous cache during switchover (Section 8)
- [ ] [RFC6810-8-4] [SHOULD] Client SHOULD delete data from unreachable cache after 2x polling period (Section 8)
- [ ] [RFC6810-11-1] [SHOULD] Cache SHOULD be topologically close to router (Section 11)
- [ ] [RFC6810-5.10-5] [SHOULD] If erroneous Error Report PDU received, session SHOULD be dropped (Section 5.10)
- [ ] [RFC6810-7.1-3] [SHOULD] Cache servers supporting SSH SHOULD accept ECDSA authentication (Section 7.1)
- [ ] [RFC6810-7.1-4] [SHOULD] Client routers SHOULD verify the public key of the cache to avoid MITM attacks (Section 7.1)
- [ ] [RFC6810-7.2-9] [SHOULD] TLS cache SHOULD reject connections if no iPAddress identities match (Section 7.2)
- [ ] [RFC6810-7.2-10] [SHOULD NOT] DNS names in rpki-rtr server certificates SHOULD NOT contain the wildcard character "*" (Section 7.2)
- [ ] [RFC6810-7-6] [SHOULD] Operators SHOULD use procedural means (e.g., ACLs) to reduce exposure to authentication issues when using unprotected TCP (Section 7)
- [ ] [RFC6810-7.3-3] [SHOULD] Cache servers SHOULD support RFC 4808 for TCP MD5 key rollover (Section 7.3)
- [ ] [RFC6810-8-5] [SHOULD] The client SHOULD attempt to maintain at least one set of data regardless of cache changes (Section 8)
- [ ] [RFC6810-11-2] [SHOULD] The identity of the cache server SHOULD be verified and authenticated by the router client, and vice versa (Section 11)
- [ ] [RFC6810-11-3] [SHOULD] Protocols that provide integrity and authenticity SHOULD be used for cache-router transport (Section 11)
- [ ] [RFC6810-10-1] [SHOULD] Fatal errors SHOULD cause the session to be dropped (Section 10)
- [ ] [RFC6810-6.1-2] [MAY] Router MAY start with Serial Query if it has unexpired data from a previous session (Section 6.1)
- [ ] [RFC6810-5.10-6] [MAY] Truncated Erroneous PDU MAY be included for oversized PDUs (Section 5.10)
- [ ] [RFC6810-5-2] [MAY] Fields with unspecified content MAY be ignored on receipt (Section 5)
- [ ] [RFC6810-7-7] [MAY] Routers MAY use SSHv2, TCP MD5, IPsec, or TLS transport (Section 7)
- [ ] [RFC6810-7.1-5] [MAY] SSH host authentication MAY be supported (Section 7.1)
- [ ] [RFC6810-7.1-6] [MAY] SSH implementations MAY support password authentication (Section 7.1)
- [ ] [RFC6810-8-6] [MAY] A client MAY drop the data from a particular cache when it is fully in sync with other caches (Section 8)
- [ ] [RFC6810-3-2] [MAY] There MAY be mechanisms for the router to assure authenticity of the cache (Section 3)
