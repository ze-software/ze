# RFC 4301 - Security Architecture for the Internet Protocol

## Meta

| Field | Value |
|-------|-------|
| RFC | 4301 |
| Title | Security Architecture for the Internet Protocol |
| Status | Standards Track |
| Date | December 2005 |
| Authors | S. Kent, K. Seo |
| Obsoletes | RFC 2401 |
| Updated by | RFC 6040, RFC 6864, RFC 7619 |
| Enrolment | enrolled |
| Enrolment reason | Security Architecture for IP (RFC 4301): native control-plane SPD/SAD projected to kernel XFRM. The extraction walk of 2026-09-21 read all 89 normative sites in `rfc/full/rfc4301.txt` and grew the checklist from 24 rows to 81 (72 MUST, 6 MUST NOT, 2 SHOULD, 1 MAY). 14 rows carry an annotation from the 2026-08-30 pass: 1 gap (port/ICMP selectors), 7 single-polarity positive (tunnel/transport modes, SPD present, negotiated selectors, manual+automated keying), 6 not-applicable (per-packet datapath kernel-delegated). The 57 rows added on 2026-09-21 carried no annotation and no test when added; the ruling pass of the same day classified every one (Support remaining). |
| Implementation | mixed |
| Implementation reason | Ze's Go builds and installs the security policy and association, internal/component/ike/dataplane/xfrm_linux.go::InstallSA and InstallPolicy. Linux XFRM performs the per-packet policy and association lookup. |
| Support | ipsec 20 |
| Support area | IPsec architecture |
| Support status | Partial |
| Support coverage | Native control-plane SPD/SAD model projected to kernel XFRM. The SPD carries all three dispositions of Section 4.4.1: a negotiated Child SA produces the PROTECT entries, and the operator `vpn ipsec policy` list produces the BYPASS and DISCARD entries, each with a selector, a direction and a total order. A selector holds the local and remote prefixes, the next-layer protocol, and a local and a remote port as an any-or-one-exact value. IKEv2 Child SAs install tunnel mode, or transport mode when USE_TRANSPORT_MODE is negotiated and echoed, and the RFC 4552 OSPFv3 path installs manual transport-mode ESP or AH. SAD entries carry the SPI, the address pair, the mode, the protocol, the algorithms, the replay window and a time-based lifetime. Per-packet SPD and SAD processing is kernel-delegated to XFRM. The 2026-09-21 walk widened the checklist to the whole document, so this paragraph now describes the covered part of a much larger row set rather than most of it. |
| Support remaining | Lowered from `Supported` on 2026-08-30, after an extraction walk of `rfc/full/rfc4301.txt` found architecture obligations Ze does not meet. The implementation work is `plan/immediate/spec-rfc4301-architecture-gaps.md`, one phase per block. Section 6, ICMP processing: no control lets an administrator accept or reject unauthenticated ICMP error messages per ICMP type, and no check compares a protected transit ICMP error message payload header against the traffic selectors of the SA that carried it. Section 8, DF bit and PMTU: the DF treatment of a tunnel-mode SA is not configurable, and no per-SA PMTU value is held or aged. Section 4.4.2.1, SAD lifetimes: `newLifetimeState` (internal/component/ike/engine/rekey.go) assigns a time lifetime only, `softBytes` is never assigned, and the byte-count arm of `softExpired` is unreachable in a running daemon; `plan/spec-ipsec-lifetime-volume.md` designs the byte-count lifetime. Section 5.1.2.1, outer header: the DSCP value of the outer tunnel header is not mapped for the domain the packet enters. Section 4.4.1.1, selectors: a port selector holds any port or one exact port rather than the range the section defines, and `SPParams` carries no ICMP type or code field. Each of those blocks is now gated by a checklist row of its own, added by the 2026-09-21 extraction walk: RFC4301-6.1.1-1 and RFC4301-6.1.1-2 for the unauthenticated-ICMP control, RFC4301-6.2-1 for the ICMP error payload check, RFC4301-8.1-1 and RFC4301-8.1-2 for the DF bit, RFC4301-8.2.2-1 for PMTU ageing, RFC4301-4.4.2.1-2 for the byte-count lifetime, and RFC4301-5.1.2.1-1 for the outer-header DSCP mapping. RFC4301-4.4.1.1-1 remains the gated selector row and its reason text is stale about ports. The same walk added 49 further rows, for obligations no earlier pass had recorded at all: the Section 3 connectivity and ESP-support rows, the Section 4.4.1 SPD management interface and decorrelation rows, the Section 4.4.1.1 OPAQUE and ICMP-range selector rows, the Section 4.4.2.1 SAD data items, the Section 4.4.3 PAD rows, the Section 4.5.2 key-splitting rule, the Section 5 processing-step rows, and the Section 7 fragment-handling rows. None of them carried a test on 2026-09-21. The ruling pass of that day implemented the Section 5 catch-all (`vpn ipsec unmatched`, RFC4301-5-1), proved the Section 3.1, 4, 4.1, 4.4, 4.4.1, 4.4.1.1, 4.4.2.1, 5.1, 5.2 and 7.3 obligations the kernel performs on state Ze installs at the XFRM boundary, and scheduled every remaining MUST as a `{gap}` naming its spec: the Section 6 ICMP block, the Section 8 DF bit and PMTU block, the outer-header DSCP mapping and the ICMP type/code selector in `plan/immediate/spec-rfc4301-architecture-gaps.md`; the byte-count lifetime in `plan/spec-ipsec-lifetime-volume.md`; OPAQUE ports in the operator list in `plan/spec-ipsec-opaque-selector-port-mask.md`; multiple SPDs, the user override control and the name selector in `plan/spec-ipsec-spd-selection.md`; the dual-family PAD range in `plan/spec-ipsec-pad-dual-family-range.md`; and NON_FIRST_FRAGMENTS_ALSO in `plan/spec-ipsec-non-first-fragments-also.md`. |

**Purpose:** Defines the overall security architecture for IPsec: the Security Policy Database (SPD), Security Association Database (SAD), and the inbound/outbound packet processing model. Ze delegates IKEv2 negotiation to strongSwan/charon and dataplane encryption to the Linux XFRM subsystem; this RFC is the conceptual model behind both. RFC 4303 (ESP wire format) and RFC 7296 (IKEv2) implement the pieces; this RFC defines how they fit together.

## Architecture Overview (Section 3)

IPsec protects IP traffic using two protocols:

| Protocol | IP Proto | Service |
|----------|----------|---------|
| AH (Authentication Header) | 51 | Integrity, data-origin authentication |
| ESP (Encapsulating Security Payload) | 50 | Confidentiality, integrity, data-origin authentication, anti-replay |

Ze uses ESP only (protocol 50). AH is not used in the strongSwan VTI deployment.

Three databases govern processing:

| Database | Purpose |
|----------|---------|
| SPD (Security Policy Database) | Ordered policy rules: which traffic to PROTECT, BYPASS, or DISCARD |
| SAD (Security Association Database) | Per-SA parameters: keys, SPI, algorithms, lifetimes, tunnel endpoints |
| PAD (Peer Authorization Database) | Links IKE peer identity to SPD entries for SA authorization |

On Linux, SPD maps to XFRM policies (`ip xfrm policy`), SAD maps to XFRM states (`ip xfrm state`), and PAD is managed by strongSwan's credential subsystem.

## Tunnel Mode vs Transport Mode (Section 4.1)

| Mode | Description | When required |
|------|-------------|---------------|
| Tunnel | Outer IP header (IPsec endpoints) encapsulates entire inner IP packet (original src/dst). Security protocol header sits between outer and inner headers. | MUST be used whenever either SA endpoint is a security gateway (two exceptions below). |
| Transport | Security protocol header inserted between IP header and next-layer protocol. Protects payload only (ESP) or payload plus selected IP header fields (AH). | Host-to-host only. Security gateways MAY use transport mode only when acting as a host (e.g. SNMP management traffic). |

**Ze uses tunnel mode exclusively** for site-to-site VPN via VTI.

### Tunnel Mode Packet Layout

```
+---------------------------------------------+
| Outer IP Header (src/dst = IPsec endpoints) |
+---------------------------------------------+
| ESP Header (SPI + Sequence Number)          |
+---------------------------------------------+
| Inner IP Header (src/dst = original hosts)  |
+---------------------------------------------+
| Original Payload (TCP, UDP, etc.)           |
+---------------------------------------------+
| ESP Trailer (Padding + Pad Length + NH)      |
+---------------------------------------------+
| ICV (Integrity Check Value)                 |
+---------------------------------------------+
```

Outer header Protocol/Next Header = 50 (ESP). Inner header is the complete original packet carried unchanged. Mixed IP versions permitted (IPv4-in-IPv6, IPv6-in-IPv4).

### Requirements (Section 4.1)

| Requirement | Detail |
|-------------|--------|
| Host MUST support both modes | Transport and tunnel |
| Security gateway MUST support tunnel mode | MAY support transport mode (host-acting-as-host only) |
| SA pairs MUST use same mode | IKE creates paired SAs; both transport or both tunnel |
| Multicast-capable MUST support multicast SAD lookup | Three-step algorithm: SPI+dst+src, then SPI+dst, then SPI alone |

## Security Policy Database (Section 4.4.1)

### Three Processing Actions

| Action | Meaning |
|--------|---------|
| PROTECT | Afford IPsec protection using specified protocols, modes, algorithms |
| BYPASS | Allow traffic to cross IPsec boundary without protection |
| DISCARD | Drop traffic; not allowed to traverse IPsec boundary |

Ze produces all three. A site-to-site peer's negotiated Child SA produces the PROTECT
entries (`childPolicyParams`, `internal/component/ike/engine/child.go`). The
`vpn ipsec policy` list produces the BYPASS and DISCARD entries, which no negotiation
can produce because neither names a transform or a peer (`internal/component/ike/ipsec/spd_policy.go`).
On Linux a DISCARD entry is `XFRM_POLICY_BLOCK` and a BYPASS is `XFRM_POLICY_ALLOW`
with no template; on VPP they are `IPSEC_API_SPD_ACTION_DISCARD` and
`IPSEC_API_SPD_ACTION_BYPASS`. A backend that cannot express a disposition refuses
the install rather than substituting another (`xfrmPolicyAction`, `vppSPDAction`).

### SPD Subdivisions

| Partition | Contains | Direction |
|-----------|----------|-----------|
| SPD-S | Entries for traffic subject to IPsec protection | Both (creates SAs) |
| SPD-O | Entries for outbound BYPASS or DISCARD traffic | Outbound |
| SPD-I | Entries for inbound BYPASS or DISCARD traffic | Inbound |

The SPD is an ordered database (like ACLs). Entries may overlap; first match wins. Every implementation MUST have at least one SPD. A final default entry SHOULD discard unmatched traffic.

The order is the OPERATOR'S. `policy-priority` under an IPsec peer ranks that peer's entries, lower first, and it defaults to `dataplane.PriorityChildSA` (2000) so an unordered configuration installs what it installed before the leaf existed. `vpn ipsec policy <name> order` ranks an operator-authored entry the same way and defaults to 1000, between the IKE bypass and the peers. `dataplane.PriorityIKEBypass` (100) holds the IKE control-plane bypass, and `ValidatePolicyOrder` refuses a peer rank at or above it, because such an entry captures the IKE exchange that builds and rekeys the SA it protects.

### Selectors (Section 4.4.1.1)

All implementations MUST support:

| Selector | Source | Notes |
|----------|--------|-------|
| Remote IP Address | Dst (outbound) / Src (inbound) | Single, range, or list of ranges |
| Local IP Address | Src (outbound) / Dst (inbound) | Single, range, or list of ranges |
| Next Layer Protocol | IPv4 Protocol / IPv6 Next Header | Specific value, ANY, or OPAQUE |
| Local Port | TCP/UDP/SCTP src port | List of ranges; OPAQUE for fragments |
| Remote Port | TCP/UDP/SCTP dst port | List of ranges; OPAQUE for fragments |
| ICMP Type/Code | 16-bit (type in high 8, code in low 8) | If Next Layer Protocol is ICMP |

Special values: **ANY** (wildcard, encompasses OPAQUE) and **OPAQUE** (field not available due to fragmentation or prior encryption).

### SPD Entry Structure (Section 4.4.1.2)

Each PROTECT entry contains:

| Field | Description |
|-------|-------------|
| Selector sets (1..N) | Traffic matching criteria per selector above |
| PFP flags | Per-selector: derive SA value from packet (true) or from SPD entry (false) |
| IPsec mode | Tunnel or transport |
| Tunnel addresses | Local and remote (tunnel mode only) |
| IPsec protocol | AH or ESP |
| Algorithms | Ordered list by priority |
| Extended Sequence Number flag | Enable 64-bit sequence numbers |
| Stateful fragment checking flag | Maintain reassembly state |
| DF bit bypass | Tunnel mode: copy/set/clear DF from inner header |
| DSCP bypass/mapping | Tunnel mode: how to handle DSCP in outer header |

### XFRM Mapping

| SPD concept | XFRM equivalent |
|-------------|-----------------|
| SPD entry (PROTECT) | `ip xfrm policy` with `dir out` or `dir in`, action `allow`, template referencing SA |
| SPD entry (BYPASS) | `ip xfrm policy` with `dir out/in`, action `allow`, no template |
| SPD entry (DISCARD) | `ip xfrm policy` with `dir out/in`, action `block` |
| Selector (remote addr + port) | Policy selector `src`/`dst` + `proto` + `sport`/`dport` |
| SPD-S/SPD-O/SPD-I | Policy direction (`dir out`, `dir in`, `dir fwd`) |
| PFP flags | Implicit in how strongSwan negotiates traffic selectors |

## Security Association Database (Section 4.4.2)

### SA Definition (Section 4.1)

An SA is a simplex (one-way) connection providing security services via AH or ESP (not both). Bidirectional communication requires a pair of SAs. IKE creates SA pairs explicitly.

### SA Identification

| Traffic type | Lookup key |
|--------------|------------|
| Unicast (inbound) | SPI alone (or SPI + protocol as local matter) |
| Multicast (inbound) | Step 1: SPI + dst + src. Step 2: SPI + dst. Step 3: SPI alone. |
| Outbound | SPD-S cache entry points to SAD entry |

### SAD Data Items (Section 4.4.2.1)

| Field | Size/Type | Description |
|-------|-----------|-------------|
| SPI | 32-bit | Selected by receiver to identify the SA. Outbound: placed in AH/ESP header. Inbound: maps packet to SA. |
| Sequence Number Counter | 64-bit | Generates Sequence Number field. First packet uses value 1. 32-bit also supported if negotiated. |
| Sequence Counter Overflow | flag | If set, overflow generates auditable event and prevents further transmission. If clear, rollover permitted. |
| Anti-Replay Window | 64-bit counter + bitmap | Detects replayed inbound packets. Ignored if anti-replay disabled (e.g. manual keying). |
| AH Auth Algorithm | algorithm + keys | If AH SA (not used by Ze). |
| ESP Encryption Algorithm | algorithm + keys | Cipher, key material, IV requirements. |
| ESP Auth Algorithm | algorithm + keys | Integrity algorithm and key. For AEAD (e.g. AES-GCM): combined mode. |
| IPsec Protocol | AH or ESP | Which protocol this SA uses. |
| IPsec Mode | tunnel or transport | Encapsulation mode. |
| SA Lifetime | time + bytes | Soft limit (rekey trigger), hard limit (SA deletion), idle timeout. |
| Tunnel Header (tunnel mode) | IP addresses | Local tunnel address (outer src), remote tunnel address (outer dst). |
| Traffic Selectors | per selector | Negotiated values for verifying inbound packets match policy. |
| Path MTU | integer | Current PMTU for this SA's path. |
| Stateful Fragment Checking | flag | Maintain fragment reassembly state. |
| Extended Sequence Numbers | flag | 64-bit ESN enabled. |

### SA Lifetime (Section 4.4.2.1)

| Lifetime | Trigger | Action |
|----------|---------|--------|
| Soft (time or bytes) | Threshold reached | Initiate rekeying via IKE. SA remains usable during rekey. |
| Hard (time or bytes) | Threshold reached | Delete SA. No further traffic permitted. |
| Idle (time) | No traffic for duration | Delete SA to reclaim resources. |

### XFRM Mapping

| SAD concept | XFRM equivalent |
|-------------|-----------------|
| SAD entry | `ip xfrm state` |
| SPI | `spi` field in xfrm state |
| ESP encryption algorithm + key | `enc` attribute |
| ESP auth algorithm + key | `auth` attribute (or `aead` for combined mode) |
| Tunnel addresses | `src`/`dst` in xfrm state |
| SA lifetime (soft/hard) | `limit` attributes: `time-soft`, `time-hard`, `byte-soft`, `byte-hard` |
| Anti-replay window | `replay-window` attribute |
| Mode | `mode tunnel` or `mode transport` |
| XFRM mark | Binds SA to VTI interface (not in RFC, Linux extension) |

## Packet Processing Model (Section 5)

### Outbound Processing (Section 5.1)

Direction: protected side to unprotected side.

```
Outbound Packet
     |
     v
[SPD Lookup] -- match selectors against SPD-S and SPD-O
     |
     +---> DISCARD: drop packet
     |
     +---> BYPASS: forward without IPsec
     |
     +---> PROTECT:
              |
              v
         [SAD Lookup] -- find existing SA via SPD-S cache
              |
              +---> No SA exists: trigger IKE negotiation, queue packet
              |
              +---> SA found:
                       |
                       v
                  [Apply ESP/AH] -- encrypt, authenticate per SA parameters
                       |
                       v
                  [Tunnel Header] -- construct outer IP header (tunnel mode)
                       |
                       v
                  [Fragment Check] -- PMTU handling per Section 8
                       |
                       v
                  Forward to network
```

### Inbound Processing (Section 5.2)

Direction: unprotected side to protected side.

```
Inbound Packet
     |
     v
[IPsec Header?] -- check for AH (51) or ESP (50) protocol
     |
     +---> No IPsec header:
     |        |
     |        v
     |   [SPD-I Lookup] -- check BYPASS or DISCARD policy
     |        |
     |        +---> BYPASS: forward to protected side
     |        +---> DISCARD: drop
     |        +---> No match: drop (MUST discard)
     |
     +---> IPsec header present:
              |
              v
         [SAD Lookup] -- SPI (+ dst addr for multicast)
              |
              +---> No SA: discard (auditable event)
              |
              +---> SA found:
                       |
                       v
                  [Anti-Replay Check] -- sequence number in window?
                       |
                       v
                  [Verify Integrity] -- ICV check
                       |
                       v
                  [Decrypt] -- if ESP with encryption
                       |
                       v
                  [Strip Tunnel Header] -- extract inner packet (tunnel mode)
                       |
                       v
                  [Verify Selectors] -- inner header fields match SAD traffic selectors
                       |
                       v
                  Forward to protected side
```

### SPD Consultation Requirement

All traffic crossing the IPsec boundary MUST be checked against SPD/SAD, including IPsec management traffic (IKE). No exception.

## SA and Key Management (Section 4.5)

| Method | Requirement | Notes |
|--------|-------------|-------|
| Manual | MUST support | Administrator configures SA parameters and keys directly into SAD. No rekeying, no anti-replay (window ignored). |
| Automated (IKEv2) | MUST support | IKEv2 (RFC 7296, originally RFC 4306) negotiates SAs, manages rekeying, handles DPD. |

Ze delegates all SA negotiation to strongSwan/charon via its VICI control interface. The kernel XFRM subsystem receives SA/SP installations from charon. Ze manages the lifecycle (start, monitor, restart on DPD failure) but never touches keys or negotiation directly.

### IKE Responsibilities

| Function | Owner |
|----------|-------|
| SA parameter negotiation | IKE (strongSwan) |
| Key generation and exchange | IKE (strongSwan) |
| SA installation into kernel | IKE -> XFRM netlink |
| Rekeying on soft lifetime | IKE (strongSwan) |
| Dead Peer Detection (DPD) | IKE (strongSwan) |
| SA deletion on hard lifetime | IKE (strongSwan) |
| Policy (SPD) installation | IKE -> XFRM netlink (or Ze via `ip xfrm policy`) |

## SA Bundles (Section 4.3)

RFC 4301 removes the "SA bundle" concept from RFC 2401. Nested SAs (e.g. ESP inside AH) are no longer a first-class construct. They can still be achieved through SPD configuration and forwarding table entries, but this is outside the IPsec module proper.

Ze does not use nested SAs. A single ESP tunnel SA per peer is the deployment model.

## Peer Authorization Database (Section 4.4.3)

The PAD links IKE peer identity to SPD entries:

| PAD component | Purpose |
|---------------|---------|
| Entry IDs and matching rules (4.4.3.1) | Map peer identity (DN, FQDN, IP) to PAD entry |
| IKE peer authentication data (4.4.3.2) | Credentials: certificates, PSK, EAP |
| Child SA authorization data (4.4.3.3) | Which traffic selectors this peer may negotiate |

Ze's native IKEv2 engine holds the PAD. One `remote-id` leaf per peer is the entry ID, `remote-id-type` pins the one ID type the peer can assert, and `ca-certificate` plus `certificate` carry the authentication data. `remoteIDMatches` (`internal/component/ike/engine/remote_id.go`) is the matching rule and `certificateCarriesIdentity` is the binding against the credential.

### Entry ID Matching (Section 4.4.3.1)

An entry names one peer or a sub-tree of peers. A sub-tree is written with the hierarchy separator in front, and it admits the identities BELOW it and never its own text.

| Entry form | Example | Admits |
|------------|---------|--------|
| Exact domain name | `vpn.example.com` | that name alone |
| Domain sub-tree | `.example.com` | `vpn.example.com`, `gw.branch.example.com` |
| Exact mail address | `gateway@example.com` | that address alone |
| Mail sub-tree | `@example.com` | `gateway@example.com` |
| Exact distinguished name | `CN=hq,O=Example` | that DN alone |
| DN sub-tree | `,ST=MA,C=US` | every DN whose top RDNs are `ST=MA,C=US` |
| Address | `10.0.0.1` | that address alone |
| Address range | `10.0.0.0/24`, `2001:db8::/32` | every address in the prefix |
| Key ID | `branch-42` | exact match only (§4.4.3.1 requires no other) |

RFC 4514 renders the most significant RDN last, so a DN sub-tree is a suffix of every member and the leading comma is what makes the comparison land on an RDN boundary. The address range syntax is the `zt:ip-prefix` the SPD traffic-selector leaves use, which is what "the same address range syntax used for SPD entries" binds Ze to.

The certificate binding stays per peer whichever form the entry takes: a peer admitted by a sub-tree must still assert an identity its own certificate carries.

## Fragment Handling (Section 7)

| Rule | Detail |
|------|--------|
| Transport mode: no fragments | AH/ESP MUST NOT be applied in transport mode to IPv4 fragments. Use tunnel mode. |
| Tunnel mode: fragments OK | Tunnel mode may encapsulate fragments. The inner packet may be a fragment. |
| PMTU | Encapsulator SHOULD perform Path MTU Discovery and adjust tunnel MTU. |
| BYPASS/DISCARD fragments | Plaintext fragments on protected side are discarded if policy requires port matching beyond ANY/OPAQUE. |

### Stateful fragment checking (Section 7.4), and where it is discharged

Two separate claims, and they answer different questions.

**The check cannot be an XFRM policy.** `XfrmSelector`
(`vendor/github.com/vishvananda/netlink/nl/xfrm_linux.go`) carries `Daddr`,
`Saddr`, `Dport`, `DportMask`, `Sport`, `SportMask`, `Family`, `PrefixlenD`,
`PrefixlenS`, `Proto`, `Ifindex` and `User`. There is no fragment field, so no
policy Ze installs can match on "is a non-initial fragment". This says nothing
about whether such a check is needed.

**It is not needed on the directions Ze installs, because Linux orders
reassembly ahead of the policy check there.** Read in Linux v6.8:

| Direction | Ordering | Producer |
|---|---|---|
| IN, IPv4 | `ip_defrag` runs before the `LOCAL_IN` hook, so `xfrm4_policy_check` reads a reassembled datagram | `ip_local_deliver`, `net/ipv4/ip_input.c:242`; the check is in `ip_protocol_deliver_rcu` |
| IN, IPv6 | the Fragment header carries `INET6_PROTO_NOPOLICY`, so the check is skipped for it and applied to the transport protocol after reassembly | `frag_protocol`, `net/ipv6/reassembly.c:413`; the check is in `ip6_protocol_deliver_rcu` |
| OUT | `ip_fragment` runs after the routing-time XFRM lookup, so the decision is made on the whole datagram | `__ip_finish_output`, `net/ipv4/ip_output.c` |
| FWD | **no reassembly**: a non-initial fragment IS classified on ports the kernel could not read | `ip_forward` calls `xfrm4_policy_check(XFRM_POLICY_FWD)` |

So the obligation is met through the stack, and what Ze owes is the property
that puts its own entries inside that guarantee: **every port-scoped BYPASS Ze
can install is confined to IN and OUT.** `ikeBypassPolicies`
(`internal/component/ike/engine/bypass.go`) installs no forward policy, and the
operator `vpn ipsec policy` list offers `out`, `in` and `both` and no forward
direction. The per-socket exemption in `espform_linux.go` constrains no port, so
Section 7.4 does not bind it.

`TestPortScopedBypassNeverReachesTheForwardPath` and
`TestIKEBypassIsPortScopedSoSection74Binds`
(`internal/component/ike/engine/rfc4301_fragment_test.go`) assert that property
over what Ze builds, and
`TestXFRMPortScopedBypassIsNeverStoredOnTheForwardPath`
(`internal/component/ike/dataplane/rfc4301_fragment_integration_linux_test.go`)
asserts it over what the kernel stored.

## Key Behavior

| Section | Requirement |
|---------|-------------|
| 4.1 | Host implementations MUST support both transport and tunnel mode. Security gateways MUST support tunnel mode. |
| 4.1 | SAs between a security gateway and any peer MUST use tunnel mode (two narrow exceptions for gateway-as-host). |
| 4.1 | IKE-created SA pairs MUST use the same mode (both tunnel or both transport). |
| 4.1 | Implementation MUST permit multiple SAs between same endpoints with same selectors (QoS differentiation). |
| 4.2 | MUST NOT instantiate an ESP SA with both NULL encryption and no integrity algorithm. Auditable event. |
| 4.4.1 | Implementation MUST have at least one SPD. SPD MUST be consulted for ALL traffic crossing IPsec boundary. |
| 4.4.1 | The operator MUST be able to order the SPD entries, and the management interface MUST support total ordering of them. |
| 4.4.1 | SPD SHOULD have a default final entry that discards unmatched traffic. |
| 4.4.1.1 | All implementations MUST support the defined selectors: remote/local IP, next-layer protocol, ports, ICMP type/code. |
| 4.4.2 | Inbound SAD entries MUST be populated with negotiated selector values for packet verification. |
| 4.5 | Implementations MUST support both manual and automated (IKEv2) key management. |
| 5.1 | Outbound: SPD match determines action. PROTECT triggers SA lookup or IKE negotiation. |
| 5.2 | Inbound: packets not matching SPD-I MUST be discarded. |
| 5.2 | Inbound: after decapsulation, inner packet selectors MUST be verified against SAD traffic selectors. |

## Section Index

| Section | Topic |
|---------|-------|
| 1 | Introduction |
| 2 | Design Objectives |
| 3 | System Overview |
| 3.1 | What IPsec Does |
| 3.2 | How IPsec Works |
| 3.3 | Where IPsec Can Be Implemented |
| 4 | Security Associations |
| 4.1 | Definition and Scope |
| 4.2 | SA Functionality |
| 4.3 | Combining SAs |
| 4.4 | Major IPsec Databases |
| 4.4.1 | Security Policy Database (SPD) |
| 4.4.1.1 | Selectors |
| 4.4.1.2 | Structure of an SPD Entry |
| 4.4.2 | Security Association Database (SAD) |
| 4.4.2.1 | Data Items in the SAD |
| 4.4.3 | Peer Authorization Database (PAD) |
| 4.4.3.1 | PAD Entry IDs and Matching Rules |
| 4.5 | SA and Key Management |
| 4.6 | SAs and Multicast |
| 5 | IP Traffic Processing |
| 5.1 | Outbound IP Traffic Processing |
| 5.2 | Inbound IP Traffic Processing |
| 6 | ICMP Processing |
| 7 | Handling Fragments |
| 8 | Path MTU/DF Processing |
| 9 | Auditing |
| 10 | Conformance Requirements |
| A | Glossary |
| B | Decorrelation |
| C | ASN.1 for SPD Entry |
| D | Fragment Handling Rationale |
| E | Nested SAs via SPD and Forwarding |

## Compliance Checklist

- [ ] [RFC4301-4.1-1] [MUST] Host implementations MUST support both transport and tunnel mode (§4.1) {single-polarity: positive; ze projects both modes -- IKE child SAs install tunnel-mode ESP and OSPFv3 RFC 4552 installs transport-mode ESP/AH; a capability-presence MUST has no meaningful negative (internal/component/ike/engine/child.go:224, :253, internal/plugins/ospf/ipsec_install.go:413, :441)}
- [ ] [RFC4301-4.1-2] [MUST] Security gateways MUST support tunnel mode (§4.1) {single-polarity: positive; ze (a security gateway) installs tunnel-mode ESP for every IKE-negotiated peer SA and policy (internal/component/ike/engine/child.go:224, :253, :281, :295)}
- [ ] [RFC4301-4.1-3] [MUST] SAs between a security gateway and any peer MUST use tunnel mode (two narrow exceptions for gateway-as-host) (§4.1) {single-polarity: positive; the IKE child-SA path hardcodes tunnel mode for every peer SA, so a peer SA can never be transport (internal/component/ike/engine/child.go:40, :224, :253)}
- [ ] [RFC4301-4.1-4] [MUST] IKE-created SA pairs MUST use the same mode (both tunnel or both transport) (§4.1). The source sentence is indicative: "IKE creates pairs of SAs, so for simplicity, we choose to require that both SAs in a pair be of the same mode, transport or tunnel." {single-polarity: positive; the inbound and outbound child SAs of a pair are both built with modeTunnel, so the pair is always same-mode (internal/component/ike/engine/child.go:224, :253)}
- [ ] [RFC4301-4.1-5] [MUST] Implementation MUST permit multiple SAs between same endpoints with same selectors (QoS differentiation) (§4.1) {not-applicable: multiple SAs with identical selectors coexist by SPI in the kernel XFRM SAD and ze's SPI-keyed installs do not forbid this; the DSCP classification that selects among them for QoS is a datapath function ze does not model (internal/component/ike/dataplane/dataplane.go:81-108)}
- [ ] [RFC4301-4.2-1] [MUST NOT] MUST NOT instantiate an ESP SA with both NULL encryption and no integrity algorithm (§4.2)
- [ ] [RFC4301-4.4.1-1] [MUST] Implementation MUST have at least one SPD (§4.4.1) {single-polarity: positive; ze maintains an SPD policy model (SPParams) and installs at least the PROTECT entries into the kernel SPD via both the IKE and OSPFv3 paths (internal/component/ike/dataplane/dataplane.go:145, engine/child.go:276, :290, internal/plugins/ospf/ipsec_install.go:432-452)}
- [ ] [RFC4301-4.4.1-2] [MUST] SPD MUST be consulted for ALL traffic crossing IPsec boundary, including IKE management traffic (§4.4.1, §5) {not-applicable: per-packet SPD consultation for every crossing packet is a kernel XFRM datapath function; ze populates the kernel SPD but does not process packets}
- [ ] [RFC4301-4.4.1.1-1] [MUST] All implementations MUST support the defined selectors: remote/local IP, next-layer protocol, ports, ICMP type/code (§4.4.1.1) {gap: ze's policy model carries IP-prefix and next-layer-protocol selectors only; the IKE traffic-selector negotiation discards ports/protocol into an address-only net.IPNet, and SPParams has no port or ICMP-type/code field (internal/component/ike/dataplane/dataplane.go:110-130 exists; ports/ICMP missing at internal/component/ike/engine/sa.go:128-129, engine/initiator.go:325)}
- [ ] [RFC4301-4.4.2-1] [MUST] Inbound SAD entries MUST be populated with negotiated selector values for packet verification (§4.4.2) {single-polarity: positive; ze captures the RFC 7296-narrowed negotiated traffic selectors and projects them into the inbound require-policy; the per-packet check against them is kernel-enforced (internal/component/ike/engine/child.go:155-164, :276-288)}
- [ ] [RFC4301-4.5-1] [MUST] Implementations MUST support both manual and automated (IKEv2) key management (§4.5) {single-polarity: positive; ze implements automated keying via its native IKEv2 engine and manual keying via the OSPFv3 RFC 4552 config, both installing SAs through the same dataplane seam (internal/component/ike/engine/child.go:199-307, internal/plugins/ospf/ipsec_install.go:295-342)}
- [ ] [RFC4301-5.2-1] [MUST] Inbound packets not matching SPD-I MUST be discarded (§5.2) {not-applicable: dropping inbound packets that fail the SPD-I match is a kernel XFRM datapath function; ze installs the inbound require-policies but does not process packets (internal/component/ike/engine/child.go:276)}
- [ ] [RFC4301-5.2-2] [MUST] After decapsulation, inner packet selectors MUST be verified against SAD traffic selectors (§5.2) {not-applicable: post-decapsulation inner-selector verification against the SAD is a kernel XFRM datapath function; the kernel verifies the inner packet against the inbound policy ze installs}
- [ ] [RFC4301-4.1-6] [MUST] Multicast-capable implementations MUST support multicast SAD lookup (three-step: SPI+dst+src, SPI+dst, SPI alone) (§4.1) {not-applicable: the three-step inbound SAD lookup (SPI+dst+src / SPI+dst / SPI) is a per-packet kernel XFRM function; ze performs no inbound SAD lookups}
- [ ] [RFC4301-7-1] [MUST NOT] AH/ESP MUST NOT be applied in transport mode to IPv4 fragments; use tunnel mode (§7). The obligation is stated indicatively and the cited section only restates it: Section 4.1 says "AH and ESP cannot be applied using transport mode to IPv4 packets that are fragments. Only tunnel mode can be employed in such cases", and Section 7 recalls it as "In Section 4.1, transport mode SAs have been defined to not carry fragments (IPv4 or IPv6)" {not-applicable: ze applies transport mode only to IPv6 OSPFv3 traffic (IPv4-family IPsec is rejected at config), so transport-mode IPsec on an IPv4 fragment structurally cannot arise, and per-packet fragment handling is kernel-delegated (internal/plugins/ospf/config_ipsec.go:21-22)}
- [ ] [RFC4301-4.4.1-3] [SHOULD] SPD SHOULD have a default final entry that discards unmatched traffic (§4.4.1)
- [ ] [RFC4301-7-2] [SHOULD] Encapsulator SHOULD perform Path MTU Discovery and adjust tunnel MTU (§7). The sentence behind this row is in Section 8.2.2 of RFC 4301, not in Section 7 of it: "Implementations SHOULD use the approach described in the Path MTU Discovery document ..., which suggests periodically resetting the PMTU to the first-hop data-link MTU and then letting the normal PMTU Discovery processes update the PMTU as necessary. The period SHOULD be configurable." (§7)
- [ ] [RFC4301-4.1-7] [MAY] Security gateways MAY support transport mode only when acting as a host (e.g., SNMP management traffic) (§4.1)
- [ ] [RFC4301-4.4.3.1-1] [MUST] Sub-tree matching MUST be supported for distinguished names, domain names and RFC 822 e-mail addresses in PAD entries (§4.4.3.1)
- [ ] [RFC4301-4.4.3.1-2] [MUST] For IPv4 and IPv6 addresses in PAD entries, the same address range syntax used for SPD entries MUST be supported (§4.4.3.1)
- [ ] [RFC4301-4.4.1-4] [MUST] A user or administrator MUST be able to order the SPD entries, and the management interface MUST support (total) ordering of them as seen via that interface (§4.4.1)
- [ ] [RFC4301-7.4-1] [MUST] All implementations MUST support DISCARDing of fragments using the normal SPD packet classification mechanisms (§7.4)
- [ ] [RFC4301-7.4-2] [MUST] All implementations MUST support stateful fragment checking to accommodate BYPASS traffic for which a non-trivial port range is specified (§7.4; Appendix D.4 restates it as "implementations MUST support fragment reassembly for BYPASS/DISCARD traffic when port fields are specified")
- [ ] [RFC4301-3.1-1] [MUST] A compliant host implementation MUST support (a) and (c) and a compliant security gateway must support all three of these forms of connectivity, since under certain circumstances a security gateway acts as a host (§3.1)
- [ ] [RFC4301-3.2-1] [MUST] IPsec implementations MUST support ESP and MAY support AH (§3.2) {single-polarity: positive; "support ESP" has no violating input, every IKE Child SA installs ESP (internal/component/ike/engine/child.go::installChildSA) and AH is the MAY Ze offers on the OSPFv3 manual path only}
- [ ] [RFC4301-4-1] [MUST] All implementations of AH or ESP MUST support the concept of an SA as described below (§4)
- [ ] [RFC4301-4.1-8] [MUST] The indication of whether source and destination address matching is required to map inbound IPsec traffic to SAs MUST be set either as a side effect of manual SA configuration or via negotiation using an SA management protocol, e.g., IKE or GDOI (§4.1)
- [ ] [RFC4301-4.1-9] [MUST] The receiver MUST process the packets from the different SAs (multiple SAs with the same selectors) without prejudice (§4.1)
- [ ] [RFC4301-4.1-10] [MUST NOT] The DSCP value MUST NOT be checked as part of SA/packet validation, because it is not employed for SA selection and can change en route in transport mode (§4.1) {lower-layer: Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams writes no DSCP into the state and xfrmPolicyFromParams none into the selector, so no value Ze installs is read against DSCP}
- [ ] [RFC4301-4.1-11] [MUST] In IPv6 the security protocol header MUST appear before next layer protocols (e.g., TCP, UDP, SCTP) (§4.1) {lower-layer: Linux XFRM; internal/component/ike/dataplane/xfrm_linux.go::xfrmStateFromParams installs the ESP state and the kernel output path builds every header, so no value Ze writes decides where the ESP header sits}
- [ ] [RFC4301-4.4-1] [MUST] The external behavior of implementations MUST correspond to the externally observable characteristics of the processing model of Section 4.4 in order to be compliant (§4.4)
- [ ] [RFC4301-4.4.1-5] [MUST] If an implementation supports multiple SPDs, then it MUST include an explicit SPD selection function that is invoked to select the appropriate SPD for outbound traffic processing (§4.4.1) {gap: Ze holds one SPD and no selection function; plan/spec-ipsec-spd-selection.md}
- [ ] [RFC4301-4.4.1-6] [MUST] The SPD MUST permit a user or administrator to specify policy entries as SPD-I, SPD-O and SPD-S, covering the DISCARD, BYPASS and PROTECT dispositions (§4.4.1)
- [ ] [RFC4301-4.4.1-7] [MUST] For every IPsec implementation, there MUST be a management interface that allows a user or system administrator to manage the SPD (§4.4.1)
- [ ] [RFC4301-4.4.1-8] [MUST] The system administrator MUST be able to specify whether or not a user or application can override (default) system policies (§4.4.1) {gap: no user or application override control exists in the SPD model; plan/spec-ipsec-spd-selection.md}
- [ ] [RFC4301-4.4.1-9] [MUST] When an SPD entry is decorrelated all the resulting entries MUST be linked together, so that all members of the group derived from an individual SPD entry can all be placed into caches and into the SAD at the same time (§4.4.1)
- [ ] [RFC4301-4.4.1-10] [MUST] If the SPD is not decorrelated, caching is not allowed and an ordered search of the SPD MUST be performed to verify that inbound traffic arriving on an SA is consistent with the access control policy expressed in the SPD (§4.4.1)
- [ ] [RFC4301-4.4.1.1-2] [MUST] A value of OPAQUE also MUST be supported for the Local and Remote port selectors, because ports may be unavailable on a fragment or encrypted by IPsec (§4.4.1.1) {gap: the operator SPD list refuses OPAQUE and the XFRM selector cannot express port 0 mask 0xffff; plan/spec-ipsec-opaque-selector-port-mask.md}
- [ ] [RFC4301-4.4.1.1-3] [MUST] If the SA requires a port value other than ANY or OPAQUE, an arriving fragment without ports MUST be discarded (§4.4.1.1)
- [ ] [RFC4301-4.4.1.1-4] [MUST] Given a policy entry with a range of ICMP Types and a range of Codes, an implementation MUST test for a match using the Type and Code of the arriving ICMP packet as the stated composite comparison (§4.4.1.1) {gap: SPParams carries no ICMP type or code selector; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-4.4.1.1-5] [MUST] All IPsec implementations MUST support the use of names (DNS name, RFC 822 address, X.500 distinguished name, other ID type) as a Local or Remote identity selector (§4.4.1.1) {gap: no name-valued selector selects an operator SPD entry during IKE; plan/spec-ipsec-spd-selection.md}
- [ ] [RFC4301-4.4.1.2-1] [MUST NOT] Until IKE provides a facility that conveys the semantics expressed in the SPD via selector sets, users MUST NOT include multiple selector sets in a single SPD entry unless the access control intent aligns with the IKE "mix and match" semantics (§4.4.1.2) {single-polarity: positive; internal/component/ike/ipsec/spd_policy.go::SPDPolicy holds one local and one remote prefix leaf, so no input can carry a second selector set}
- [ ] [RFC4301-4.4.2.1-1] [MUST] The data items listed in Section 4.4.2.1 (SPI, sequence number counter, anti-replay window, AH and ESP algorithm state, lifetime, mode, tunnel header, PMTU) MUST be in the SAD (§4.4.2.1)
- [ ] [RFC4301-4.4.2.1-2] [MUST] A compliant implementation MUST support both types of SA lifetime (time and byte count) and MUST support a simultaneous use of both (§4.4.2.1) {gap: newLifetimeState sets a time lifetime only and never a byte count; plan/spec-ipsec-lifetime-volume.md}
- [ ] [RFC4301-4.4.2.1-3] [MUST] Implementations MUST be able to handle having the counters at the ends of an SA get out of synch, e.g., because of packet loss (§4.4.2.1)
- [ ] [RFC4301-4.4.3.1-3] [MUST] For the "other ID type" PAD name type, only exact-match syntax MUST be supported, since there is no explicit structure for this ID type (§4.4.3.1)
- [ ] [RFC4301-4.4.3.2-1] [MUST] Implementations MUST provide a means for an administrator to require a match between an asserted IKE ID and the subject name or subject alt name in a certificate (§4.4.3.2)
- [ ] [RFC4301-4.4.3.3-1] [MUST] A peer may be authorized for both address types, so there MUST be provision for both a v4 and a v6 address range in the PAD child SA authorization data (§4.4.3.3) {gap: one remote-id string holds one address range, not a v4 and a v6 range; plan/spec-ipsec-pad-dual-family-range.md}
- [ ] [RFC4301-4.5.2-1] [MUST] The encryption keys MUST be taken from the first (left-most, high-order) bits of the keying material string and the integrity keys MUST be taken from the remaining bits (§4.5.2)
- [ ] [RFC4301-4.5.3-1] [MUST] An IPsec-supporting host or security gateway MUST have an administrative interface that allows the user/administrator to configure the address of one or more security gateways for ranges of destination addresses that require its use (§4.5.3)
- [ ] [RFC4301-5-1] [MUST] If no policy is found in the SPD that matches a packet (for either inbound or outbound traffic), the packet MUST be discarded (§5)
- [ ] [RFC4301-5.1-1] [MUST] IPsec MUST perform the steps of Section 5.1 when processing outbound packets (§5.1)
- [ ] [RFC4301-5.1-2] [MUST] For an outbound packet matched to a BYPASS entry whose return traffic must also bypass, there MUST be an entry in the SPD-I database that permits inbound bypassing of the packet, otherwise the packet will be discarded (§5.1)
- [ ] [RFC4301-5.1-3] [MUST] With regard to determining and enforcing the PMTU of an SA, the IPsec system MUST follow the steps described in Section 8.2 (§5.1) {gap: no per-SA PMTU is held or aged; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-5.1.2.1-1] [MUST] If the packet will immediately enter a domain for which the DSCP value in the outer header is not appropriate, that value MUST be mapped to an appropriate value for the domain (§5.1.2.1) {gap: the outer-header DSCP is not mapped for the domain entered; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-5.2-3] [MUST] IPsec MUST perform the steps of Section 5.2 when processing inbound packets (§5.2)
- [ ] [RFC4301-5.2-4] [MUST] IKE traffic MUST have an explicit BYPASS entry in the SPD (§5.2)
- [ ] [RFC4301-5.2-5] [MUST] As with all outbound traffic that is to be bypassed, an inbound-bypassed packet whose return traffic is also bypassed MUST be matched against an SPD-O entry (§5.2)
- [ ] [RFC4301-6-1] [MUST] Disposition of non-error ICMP messages that are not addressed to the IPsec implementation itself MUST be explicitly accounted for using SPD entries (§6) {gap: no ICMP type or code selector and no default entry accounts for non-error ICMP; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-6.1.1-1] [MUST] A compliant IPsec implementation MUST permit a local administrator to configure it to accept or reject unauthenticated ICMP traffic (§6.1.1) {gap: no control accepts or rejects unauthenticated ICMP; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-6.1.1-2] [MUST] That accept/reject control MUST be at the granularity of ICMP type and MAY be at the granularity of ICMP type and code (§6.1.1) {gap: no ICMP-type granularity exists for the control; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-6.1.2-1] [MUST] Implementers MUST provide controls to allow local administrators to constrain the processing of ICMP error messages received on the protected side of the boundary and directed to the IPsec implementation (§6.1.2) {gap: no control constrains ICMP error processing at the IPsec boundary; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-6.2-1] [MUST] An IPsec implementation MUST be configurable to check that the ICMP error message payload header information is consistent with the SA via which it arrives (§6.2) {gap: no check compares an ICMP error payload header against the SA selectors; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-6.2-2] [MUST] IPsec senders and receivers MUST support the Section 6.2 processing for ICMP error messages that are sent and received via SAs (§6.2) {gap: the Section 6.2 ICMP error processing is absent; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-6.2-3] [MUST] If no SA and no SPD entry would carry an outbound ICMP error message, an IPsec implementation MUST map the message to the SA that would carry the return traffic associated with the packet that triggered it (§6.2) {gap: whether the kernel maps an outbound ICMP error to the return SA is unverified and Ze holds no producer for it; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-6.2-4] [MUST] If an inbound ICMP error message arrives on an SA and its IP and ICMP headers do not match the traffic selectors for that SA, the receiver MUST process the received message in the special fashion Section 6.2 describes (§6.2) {gap: no special processing of a mismatching inbound ICMP error exists; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-6.2-5] [MUST NOT] If the ICMP error message payload check fails, the IPsec implementation MUST NOT forward the ICMP message to the destination (§6.2) {gap: no payload check exists, so nothing refuses to forward on its failure; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-7.1-1] [MUST] All implementations MUST support tunnel mode SAs that are configured to pass traffic without regard to port field (or ICMP type/code or Mobility Header type) values (§7.1)
- [ ] [RFC4301-7.1-2] [MUST] If such an SA will carry traffic for specified protocols, the selector set for the SA MUST specify the port fields (or ICMP type/code or Mobility Header type) as ANY (§7.1)
- [ ] [RFC4301-7.1-3] [MUST] If such an SA will carry traffic without regard to a specific protocol value, the port field values are undefined and MUST be set to ANY as well (§7.1)
- [ ] [RFC4301-7.2-1] [MUST] Receivers MUST perform a minimum offset check on IPv4 non-initial fragments to protect against overlapping fragment attacks when separate tunnel mode SAs for non-initial fragments are employed (§7.2) {lower-layer: Linux XFRM; internal/component/ike/engine/child.go::childPolicyParams builds one policy pair per Child SA and no separate non-initial-fragment SA, so approach #2 of Section 7.2 is never taken and no value Ze installs decides the offset check the kernel reassembly performs}
- [ ] [RFC4301-7.2-2] [MUST] These separate fragment-carrying SAs MUST have non-trivial protocol selector values, otherwise approach #1 (Section 7.1) MUST be used (§7.2) {lower-layer: Linux XFRM; internal/component/ike/engine/child.go::childPolicyParams builds no fragment-carrying SA, so the protocol selector this sentence constrains is never written by Ze}
- [ ] [RFC4301-7.3-1] [MUST] Implementations that will transmit non-initial fragments on a tunnel mode SA that makes use of non-trivial port (or ICMP type/code or MH type) selectors MUST notify a peer via the IKE NOTIFY NON_FIRST_FRAGMENTS_ALSO payload (§7.3) {gap: Ze never sends NON_FIRST_FRAGMENTS_ALSO; plan/spec-ipsec-non-first-fragments-also.md}
- [ ] [RFC4301-7.3-2] [MUST] The peer MUST reject the NON_FIRST_FRAGMENTS_ALSO proposal if it will not accept non-initial fragments in this context (§7.3) {gap: Ze never parses or answers NON_FIRST_FRAGMENTS_ALSO; plan/spec-ipsec-non-first-fragments-also.md}
- [ ] [RFC4301-7.3-3] [MUST NOT] If an implementation does not successfully negotiate transmission of non-initial fragments for such an SA, it MUST NOT send such fragments over the SA (§7.3)
- [ ] [RFC4301-7.3-4] [MUST] A receiver MUST discard non-initial fragments that arrive on an SA with non-trivial port (or ICMP type/code or MH type) selector values unless this feature has been negotiated (§7.3)
- [ ] [RFC4301-7.3-5] [MUST] The receiver MUST discard non-initial fragments that do not comply with the security policy applied to the overall packet (§7.3)
- [ ] [RFC4301-8.1-1] [MUST] All IPsec implementations MUST support the option of copying the DF bit from an outbound packet to the tunnel mode header that it emits, when traffic is carried via a tunnel mode SA (§8.1) {gap: no option copies the DF bit into the tunnel header per SA; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-8.1-2] [MUST] It MUST be possible to configure the implementation's treatment of the DF bit (set, clear, copy from inner header) for each SA (§8.1) {gap: the DF treatment of an SA is not configurable; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-8.2.1-1] [MUST] When traffic arrives that would exceed the updated PMTU value, the traffic MUST be handled as Section 8.2.1 describes (fragment before or after IPsec, or discard and send a PMTU ICMP message, per the DF bit) (§8.2.1) {gap: no per-SA PMTU exists to exceed; plan/immediate/spec-rfc4301-architecture-gaps.md}
- [ ] [RFC4301-8.2.2-1] [MUST] In all IPsec implementations, the PMTU associated with an SA MUST be "aged" and some mechanism is required to update the PMTU in a timely manner (§8.2.2) {gap: no per-SA PMTU is held or aged; plan/immediate/spec-rfc4301-architecture-gaps.md}
