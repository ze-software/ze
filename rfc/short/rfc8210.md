# RFC 8210: The Resource Public Key Infrastructure (RPKI) to Router Protocol, Version 1

## Meta

| Field | Value |
|-------|-------|
| RFC | 8210 |
| Title | The Resource Public Key Infrastructure (RPKI) to Router Protocol, Version 1 |
| Status | Standards Track |
| Date | September 2017 |
| Authors | R. Bush (Internet Initiative Japan), R. Austein (Dragon Research Labs) |
| Updates | RFC 6810 |
| Obsoletes | - |
| Obsoleted-by | - |
| Enrolment | enrolled |
| Enrolment reason | RPKI to Router Protocol, Version 1 |
| Support | bgp-operations 50 |
| Support area | RPKI RTR v1 |
| Support status | Partial |
| Support coverage | ze is the RTR router (client). It opens every connection with a Reset or Serial Query (internal/component/bgp/plugins/rpki/rtr_session.go), speaks version 2 with a downgrade to version 1 (rtr_pdu.go, rtr_session.go), parses IPv4/IPv6 Prefix, Cache Response, the 24-byte v1 End of Data with its Refresh/Retry/Expire parameters, Cache Reset, Serial Notify and Error Report PDUs, enforces Max Length >= Prefix Length and ignores reserved Flags bits on receipt (rtr_pdu.go), applies each delta atomically at End of Data (rtr_session.go), ignores Serial Notify during startup, re-issues a Reset Query on Cache Reset and on No Data Available, and validates origins against the merged VRP cache (validate.go). |
| Support remaining | Twelve MUST-level gaps, each annotated in rfc/short/rfc8210.md: RFC8210-5.1-3, RFC8210-5.1-4 -- no Session ID mismatch detection, no Error Report code 0, no cache flush (rtr_session.go adopts the Session ID unconditionally); RFC8210-5.1-5 -- a version downgrade reuses the previous version's Session ID and serial (rtr_session.go); RFC8210-7-3, RFC8210-7-4, RFC8210-7-7 -- handlePDU never reads hdr.Version (rtr_session.go), so no received-version check, downgrade or session drop happens; RFC8210-12-1 -- Error Code 4 is excluded from isFatalError (rtr_pdu.go) and at rtrVersionMin the session survives it (rtr_session.go); RFC8210-5.10-2 -- Router Key PDUs are discarded (rtr_session.go), so no Subject Public Key comparison exists; RFC8210-6-2 -- VRPs outlive the Expire Interval (ROACache.Clear, roa_cache.go, has no production caller); RFC8210-4-1 -- no most-preferred-cache selection (rpki.go runs every configured cache concurrently); RFC8210-10-2 -- VRPs are not marked by source cache (rpki.go, roa_cache.go); RFC8210-9-3 -- no protected RTR transport (rtr_session.go dials only unprotected TCP). |

**Purpose:** Defines version 1 of the protocol by which routers receive validated prefix origin data (ROAs) and router keys from trusted RPKI caches, enabling BGP origin validation (RPKI-ROV).

**Scope:** Cache-to-router communication. Not BGP wire format itself, but the protocol that feeds RPKI validation data to BGP speakers.

## Changes from RFC 6810 (Version 0)

| Change | Section | Impact |
|--------|---------|--------|
| New Router Key PDU (Type 9) | 5.10 | Supports BGPsec router key distribution |
| Explicit timing parameters in End of Data PDU | 5.8, 6 | Cache controls Refresh, Retry, Expire intervals |
| Protocol version incremented from 0 to 1 | 5.1 | All PDUs carry version field = 1 |
| Protocol version negotiation added | 7 | Router and cache agree on version at connection start |
| New error code 8: Unexpected Protocol Version | 12 | Fatal error for version mismatch after negotiation |

## Wire Formats

### Common PDU Header

All PDUs share an 8-byte header:

```
 0          8          16         24        31
.-------------------------------------------.
| Protocol |   PDU    |   Session ID /      |
| Version  |   Type   |   Flags / zero      |
+-------------------------------------------+
|                                           |
|                  Length                    |
`-------------------------------------------'
```

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Protocol Version | 0 | 1 | uint8 | 1 (for this version) |
| PDU Type | 1 | 1 | uint8 | 0-10 (see PDU Types table) |
| Session ID / Flags / zero | 2 | 2 | uint16 | Depends on PDU type |
| Length | 4 | 4 | uint32 | Total PDU length including header |

### PDU Types

| Type | Name | Length | Direction | Bytes 2-3 Usage |
|------|------|--------|-----------|-----------------|
| 0 | Serial Notify | 12 | Cache->Router | Session ID |
| 1 | Serial Query | 12 | Router->Cache | Session ID |
| 2 | Reset Query | 8 | Router->Cache | zero |
| 3 | Cache Response | 8 | Cache->Router | Session ID |
| 4 | IPv4 Prefix | 20 | Cache->Router | zero |
| 6 | IPv6 Prefix | 32 | Cache->Router | zero |
| 7 | End of Data | 24 | Cache->Router | Session ID |
| 8 | Cache Reset | 8 | Cache->Router | zero |
| 9 | Router Key | variable | Cache->Router | Flags (byte 2), zero (byte 3) |
| 10 | Error Report | variable | Either | Error Code |

Note: PDU Type 5 is intentionally skipped (not assigned).

### Serial Notify (Type 0)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=0  |     Session ID      |
+-------------------------------------------+
|                 Length=12                  |
+-------------------------------------------+
|               Serial Number               |
`-------------------------------------------'
```

| Field | Offset | Size | Type |
|-------|--------|------|------|
| Session ID | 2 | 2 | uint16 |
| Length | 4 | 4 | uint32 (fixed: 12) |
| Serial Number | 8 | 4 | uint32 |

### Serial Query (Type 1)

Same format as Serial Notify. Router sends to request incremental updates since specified serial.

### Reset Query (Type 2)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=2  |        zero         |
+-------------------------------------------+
|                 Length=8                   |
`-------------------------------------------'
```

No payload beyond header. Router requests full data set.

### Cache Response (Type 3)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=3  |     Session ID      |
+-------------------------------------------+
|                 Length=8                   |
`-------------------------------------------'
```

Begins a sequence of zero or more payload PDUs followed by End of Data.

### IPv4 Prefix (Type 4)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=4  |        zero         |
+-------------------------------------------+
|                 Length=20                  |
+-------------------------------------------+
|  Flags   |  Prefix  |   Max    |   zero   |
|          |  Length   |  Length  |          |
+-------------------------------------------+
|              IPv4 Prefix (4 bytes)        |
+-------------------------------------------+
|         Autonomous System Number          |
`-------------------------------------------'
```

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Flags | 8 | 1 | uint8 | Bit 0: 1=announce, 0=withdraw |
| Prefix Length | 9 | 1 | uint8 | 0-32 |
| Max Length | 10 | 1 | uint8 | >= Prefix Length, 0-32 |
| zero | 11 | 1 | uint8 | Must be 0 |
| IPv4 Prefix | 12 | 4 | [4]byte | Network byte order |
| ASN | 16 | 4 | uint32 | 32-bit AS number |

### IPv6 Prefix (Type 6)

Same structure as IPv4 Prefix but with 16-byte prefix. Fixed length: 32 bytes.

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Flags | 8 | 1 | uint8 | Bit 0: 1=announce, 0=withdraw |
| Prefix Length | 9 | 1 | uint8 | 0-128 |
| Max Length | 10 | 1 | uint8 | >= Prefix Length, 0-128 |
| zero | 11 | 1 | uint8 | Must be 0 |
| IPv6 Prefix | 12 | 16 | [16]byte | Network byte order |
| ASN | 28 | 4 | uint32 | 32-bit AS number |

### End of Data (Type 7)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=7  |     Session ID      |
+-------------------------------------------+
|                 Length=24                  |
+-------------------------------------------+
|               Serial Number               |
+-------------------------------------------+
|              Refresh Interval             |
+-------------------------------------------+
|               Retry Interval              |
+-------------------------------------------+
|              Expire Interval              |
`-------------------------------------------'
```

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Session ID | 2 | 2 | uint16 | Must match Cache Response |
| Length | 4 | 4 | uint32 (fixed: 24) | |
| Serial Number | 8 | 4 | uint32 | Current cache serial |
| Refresh Interval | 12 | 4 | uint32 | 1-86400 seconds |
| Retry Interval | 16 | 4 | uint32 | 1-7200 seconds |
| Expire Interval | 20 | 4 | uint32 | 600-172800 seconds |

**Note:** In version 0 (RFC 6810), End of Data was only 12 bytes (no timing parameters). The three interval fields are new in version 1.

### Cache Reset (Type 8)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=8  |        zero         |
+-------------------------------------------+
|                 Length=8                   |
`-------------------------------------------'
```

Sent by cache when it cannot provide incremental update from the requested serial.

### Router Key (Type 9) -- NEW in Version 1

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=9  |  Flags   |   zero   |
+-------------------------------------------+
|                  Length                    |
+-------------------------------------------+
|                                           |
|        Subject Key Identifier (20 bytes)  |
|                                           |
+-------------------------------------------+
|              AS Number (4 bytes)          |
+-------------------------------------------+
|                                           |
|       Subject Public Key Info (variable)  |
|                                           |
`-------------------------------------------'
```

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Flags | 2 | 1 | uint8 | Bit 0: 1=announce, 0=withdraw |
| zero | 3 | 1 | uint8 | Must be 0 |
| Length | 4 | 4 | uint32 | 32 + len(SubjectPublicKeyInfo) |
| Subject Key Identifier | 8 | 20 | [20]byte | SKI per RFC 6487 |
| AS Number | 28 | 4 | uint32 | 32-bit ASN |
| Subject Public Key Info | 32 | variable | bytes | Full ASN.1 DER encoding per RFC 8208 |

### Error Report (Type 10)

```
 0          8          16         24        31
.-------------------------------------------.
| Version  |  Type=10 |     Error Code      |
+-------------------------------------------+
|                  Length                    |
+-------------------------------------------+
|       Length of Encapsulated PDU          |
+-------------------------------------------+
|            Erroneous PDU (variable)       |
+-------------------------------------------+
|           Length of Error Text            |
+-------------------------------------------+
|        Error Diagnostic Message (UTF-8)   |
`-------------------------------------------'
```

| Field | Offset | Size | Type | Constraints |
|-------|--------|------|------|-------------|
| Error Code | 2 | 2 | uint16 | See Error Codes table |
| Length | 4 | 4 | uint32 | Total PDU length |
| Encapsulated PDU Length | 8 | 4 | uint32 | 0 if no erroneous PDU |
| Erroneous PDU | 12 | variable | bytes | Copy of offending PDU (may be truncated) |
| Error Text Length | 12 + encap_len | 4 | uint32 | 0 if no text |
| Error Text | 16 + encap_len | variable | UTF-8 string | Diagnostic message |

## Protocol Timing Parameters

| Parameter | Default | Min | Max | Purpose |
|-----------|---------|-----|-----|---------|
| Refresh Interval | 3600s (1 hour) | 1s | 86400s (1 day) | Time between normal cache polls |
| Retry Interval | 600s (10 min) | 1s | 7200s (2 hours) | Time between retries after failed poll |
| Expire Interval | 7200s (2 hours) | 600s (10 min) | 172800s (2 days) | Data validity without successful poll |

Constraint: Expire Interval MUST be larger than both Refresh Interval and Retry Interval.

## Protocol Version Negotiation

```
1. Router starts connection by sending Reset Query or Serial Query with version 1
2. If cache supports version 1: responds with version 1 PDUs
3. If cache only supports version 0:
   a. Cache may terminate (possibly with v0 Error Report) -> router MAY retry with v0
   b. Cache may reply with v0 response -> router MUST downgrade to v0 or terminate
4. If cache supports v1 but receives v0 query:
   cache MUST downgrade to v0 OR send v1 Error Code 4 and terminate
5. Once negotiated, version is stable for session lifetime
6. Version mismatch after negotiation -> Error Code 8 (Unexpected Protocol Version)
```

**During negotiation:** Router MUST ignore any Serial Notify PDUs received from cache. Caches SHOULD NOT send Serial Notify before negotiation completes.

## Error Codes

| Code | Name | Fatal | Description |
|------|------|-------|-------------|
| 0 | Corrupt Data | Yes | PDU believed corrupt |
| 1 | Internal Error | Yes | Internal error unrelated to protocol |
| 2 | No Data Available | No | Cache has no useful data (temporary) |
| 3 | Invalid Request | Yes | Client request invalid |
| 4 | Unsupported Protocol Version | Yes | Unknown protocol version |
| 5 | Unsupported PDU Type | Yes | Unknown PDU type |
| 6 | Withdrawal of Unknown Record | Yes | Withdraw for non-existent record |
| 7 | Duplicate Announcement Received | Yes | Announce for already-active record |
| 8 | Unexpected Protocol Version | Yes | Version differs from negotiated (new in v1) |

## MUST Requirements

### Session Management

| Requirement | Section | Context |
|-------------|---------|---------|
| "Reserved fields MUST be zero on transmission and MUST be ignored on receipt" | 5 | All PDUs |
| "the party which detects the mismatch MUST immediately terminate the session with an Error Report PDU with code 0" | 5.1 | Session ID mismatch after negotiation |
| "the router MUST flush all data learned from that cache" | 5.1 | On Session ID mismatch |
| "routers MUST treat sessions with different Protocol Version fields as separate sessions" | 5.1 | Even if same Session ID |
| "Max Length MUST NOT be less than the Prefix Length" | 5.1 | IPv4/IPv6 Prefix PDUs |

### Version Negotiation

| Requirement | Section | Context |
|-------------|---------|---------|
| "A router MUST start each transport connection by issuing either a Reset Query or a Serial Query" | 7 | Initiates version negotiation |
| "the cache MUST downgrade to protocol version 0 or send a version 1 Error Report PDU with Error Code 4" | 7 | Cache receives v0 query |
| "the router MUST either downgrade to version 0 or terminate the connection" | 7 | Router receives v0 response |
| "the router MUST ignore any Serial Notify PDUs it might receive from the cache during this initial startup period" | 5.2, 7 | During version negotiation |
| "that party MUST drop the session" | 7 | Version mismatch after negotiation |

### Cache Behavior

| Requirement | Section | Context |
|-------------|---------|---------|
| "the cache MUST return the minimum set of changes" | 5.3 | Reply to Serial Query |
| "the cache MUST merge those changes to present the simplest possible view" | 5.3 | Multiple changes to same prefix/key |
| "the withdraw/announce field in the payload PDUs MUST have the value 1 (announce)" | 5.5 | Reply to Reset Query |
| "The cache server MUST ensure one and only one IPvX PDU for a unique {Prefix, Len, Max-Len, ASN}" | 5.6 | Deduplication |
| "The cache server MUST ensure one and only one Router Key PDU for a unique {SKI, ASN, Subject Public Key}" | 5.10 | Deduplication |
| "implementations MUST compare Subject Public Key values as well as SKIs when detecting duplicate PDUs" | 5.10 | SKI hash collision |
| "Caches MUST set Expire Interval to a value larger than either Refresh Interval or Retry Interval" | 6 | Timing constraints |
| "MUST rate-limit Serial Notifies to no more frequently than one per minute" | 8.2 | Notify rate limiting |

### Router Behavior

| Requirement | Section | Context |
|-------------|---------|---------|
| "The router MUST choose the most preferred cache" | 4 | Cache selection |
| "the router MUST send either a Serial Query or a Reset Query periodically" | 8.1, 8.2 | Keep-alive / poll |
| "The router MUST NOT retain the data past the time indicated by [Expire Interval]" | 6 | Data expiry |
| "it MUST issue a Reset Query and get an entire new load from the cache" | 8.3 | After Cache Reset, no preferred caches |

### Error Handling

| Requirement | Section | Context |
|-------------|---------|---------|
| "An Error Report PDU MUST NOT be sent for an Error Report PDU" | 5.11 | Prevents error loops |
| "Erroneous PDU field MUST be empty and Length of Encapsulated PDU field MUST be zero" | 5.11 | Generic errors |
| "Errors which are considered fatal MUST cause the session to be dropped" | 12 | Fatal error codes |

### Transport

| Requirement | Section | Context |
|-------------|---------|---------|
| "Caches and routers MUST implement unprotected transport over TCP using port rpki-rtr (323)" | 9 | Baseline transport |
| "the cache and routers MUST be on the same trusted and controlled network" | 9 | When using plain TCP |
| "caches and routers MUST use one of the [protected] protocols" | 9 | When protection available |

## SHOULD/MAY

| Type | Requirement | Section | Notes |
|------|-------------|---------|-------|
| [SHOULD] | "The router SHOULD NOT poll the cache sooner than indicated by [Refresh Interval]" | 6 | Overridden by Serial Notify |
| [SHOULD] | "The router SHOULD NOT retry sooner than indicated by [Retry Interval]" | 6 | Overridden by version downgrade |
| [SHOULD] | "caches and routers SHOULD enable keep-alives" | 9 | Detect dropped sessions |
| [SHOULD] | "Caches and routers SHOULD use TCP-AO transport" | 9 | Preferred protected transport |
| [SHOULD] | "cache servers SHOULD NOT use the same Session ID across multiple protocol versions" | 5.1 | Reduce confusion |
| [SHOULD] | "Caches SHOULD NOT send Serial Notify PDUs before version negotiation completes" | 7 | Backwards compat with v0 caches |
| [SHOULD] | "the router client SHOULD raise a Duplicate Announcement Received error" | 5.6, 5.10 | On duplicate active record |
| [SHOULD] | "router SHOULD attempt to connect to any more-preferred caches" | 8.3 | On Cache Reset |
| [SHOULD] | "the erroneous Error Report PDU session SHOULD be dropped" | 5.11 | Error-on-error |
| [MAY] | "the router MAY issue an immediate Serial Query or Reset Query" | 5.2 | On Serial Notify receipt |
| [MAY] | "Caches and routers MAY use SSH, TCP MD5, IPsec, or TLS transport" | 9 | Alternative transports |
| [MAY] | "the Erroneous PDU field MAY be truncated" | 5.11 | For excessively long PDUs |
| [MAY] | "the router MAY retry the connection using protocol version 0" | 7 | On v0 cache termination |
| [MAY] | "it MAY send the first Serial Query or Reset Query immediately" | 6 | After version downgrade |

## Protocol Sequences

### Start or Restart

```
Router -> Cache:  Reset Query (or Serial Query with previous Session ID)
Cache  -> Router: Cache Response (with Session ID)
Cache  -> Router: Payload PDUs (zero or more IPv4/IPv6 Prefix, Router Key)
Cache  -> Router: End of Data (with Serial Number + timing parameters)
```

### Typical Exchange (Incremental Update)

```
Cache  -> Router: Serial Notify (optional, with current serial)
Router -> Cache:  Serial Query (with last known serial + Session ID)
Cache  -> Router: Cache Response
Cache  -> Router: Payload PDUs (announcements and withdrawals since serial)
Cache  -> Router: End of Data (with new serial + timing parameters)
```

### No Incremental Update Available

```
Router -> Cache:  Serial Query
Cache  -> Router: Cache Reset
Router -> Cache:  Reset Query (after trying preferred caches)
Cache  -> Router: Cache Response + Payload PDUs + End of Data
```

### Cache Has No Data

```
Router -> Cache:  Serial Query or Reset Query
Cache  -> Router: Error Report (Code 2: No Data Available)
Router:           Retry periodically using Retry Interval
```

## Session ID and Serial Number Handling

| Concept | Rule |
|---------|------|
| Session ID scope | Specific to a particular protocol version; (version, session_id) pair identifies a session |
| Serial Number scope | Commensurate only within same (version, session_id) pair |
| Serial Number arithmetic | 32-bit unsigned, wraps from 2^32-1 to 0; comparison per RFC 1982 |
| Session ID generation | Pseudorandom, strictly increasing, or timestamp-based (e.g., POSIX time()) |
| Session ID mismatch | Terminate with Error Code 0, router flushes all data from that cache |
| Cross-version Session ID | Even if numerically same, sessions with different versions are separate |

## Transport

| Transport | Port | Auth | Notes |
|-----------|------|------|-------|
| TCP (unprotected) | 323 (rpki-rtr) | None | MUST implement; MUST be same trusted network |
| TCP-AO | 323 (rpki-rtr) | Shared key | SHOULD use; expected future mandatory-to-implement |
| SSH (SSHv2) | 22 | RSA (MUST), ECDSA (SHOULD) | MAY use; subsystem "rpki-rtr" |
| TLS | 324 (rpki-rtr-tls) | Client+server certs | MAY use; requires client-side certs |
| TCP MD5 | 323 (rpki-rtr) | Shared key | MAY use; obsoleted by TCP-AO |
| IPsec | 323 (rpki-rtr) | Per IPsec config | MAY use |

### TLS Requirements

- Client routers MUST present client-side certificates
- Client certs MUST include subjectAltName with iPAddress identities
- Cache MUST check client IP against iPAddress identities
- Router MUST verify cache cert using subjectAltName dNSName per RFC 6125
- MUST NOT use Common Name (CN-ID) for authentication
- DNS names SHOULD NOT contain wildcard "*"

### SSH Requirements

- Cache MUST accept RSA, SHOULD accept ECDSA
- User authentication MUST be supported
- Client SHOULD verify cache public key

## Constants

| Name | Value | Usage |
|------|-------|-------|
| Protocol Version | 1 | Version 1 of RPKI-Router protocol |
| rpki-rtr port | 323 | TCP, TCP-AO, TCP MD5, IPsec |
| rpki-rtr-tls port | 324 | TLS transport |
| SSH subsystem name | "rpki-rtr" | SSH transport |
| Max Serial Notify rate | 1 per minute | Cache rate limiting |

## Algorithms

### Router Startup

```
1. Choose most preferred cache from configured list
2. Establish transport connection
3. Send Reset Query (or Serial Query if resuming with known Session ID)
4. Version negotiation occurs implicitly via query/response versions
5. Receive Cache Response -> Payload PDUs -> End of Data
6. Store Session ID, Serial Number, and timing parameters
7. Set Refresh timer from End of Data
```

### Router Polling Loop

```
1. Wait for Refresh Interval (or Serial Notify receipt)
2. Send Serial Query with current (Session ID, Serial Number)
3. On success (Cache Response + data + End of Data):
   - Apply announcements and withdrawals
   - Update Serial Number from End of Data
   - Update timing parameters from End of Data
   - Restart Refresh timer
4. On Cache Reset:
   - Try more-preferred caches
   - If none: send Reset Query for full data
5. On Error (No Data Available):
   - Retry after Retry Interval
6. On connection failure:
   - Retry after Retry Interval
   - If Expire Interval exceeded: flush all data from this cache
```

### Cache Incremental Response

```
For Serial Query requesting serial S:
1. If S is too old or unknown: send Cache Reset
2. If S is current: send Cache Response + End of Data (empty update)
3. Otherwise, for each prefix/router key changed since S:
   - Merge all changes into minimal set
   - At most one withdrawal + one announcement per record
   - If changes cancel out: omit entirely
4. Send Cache Response
5. Send merged payload PDUs
6. Send End of Data with current serial + timing parameters
```

## Pitfalls

### Edge Cases

- **Serial Number wrap-around:** Comparison MUST use RFC 1982 arithmetic, not simple integer comparison
- **Session ID reuse by cache:** If cache erroneously reuses Session ID after restart, router may become confused; mitigate with persistent storage or clock-based Session IDs
- **Empty incremental update:** Cache Response followed immediately by End of Data is valid (no changes since requested serial)
- **Router Key SKI collision:** Multiple distinct public keys can hash to same SKI; compare full public key, not just SKI
- **Multiple caches:** Data from different caches is not commensurate; track data source separately per cache

### Interop

- **Version 0 caches:** Router must handle downgrade gracefully; v1 features (Router Key, timing parameters) unavailable in v0
- **End of Data length:** v0 End of Data is 12 bytes; v1 is 24 bytes (timing parameters added); implementations must check version before parsing
- **PDU Type 5 gap:** Type 5 is not assigned; implementations must handle gracefully
- **Error-on-error:** Never send Error Report in response to Error Report PDU; drop session instead

### Security

- **Cache spoofing:** Critical risk; cache feeds data directly to routing decisions; always use authenticated transport when possible
- **Last-hop trust:** Unlike inter-cache RPKI object security, cache-to-router link relies on transport security only
- **No Data Available is non-fatal:** Router should not flush existing data on this error; retry and use existing data until Expire Interval

## Compatibility

### With Version 0 (RFC 6810)

- All v0 PDU types valid in v1 (same type codes)
- Router Key (Type 9) reserved but unused in v0
- End of Data is longer in v1 (24 vs 12 bytes)
- Error Code 8 (Unexpected Protocol Version) new in v1
- Version negotiation allows interop between v0 and v1 implementations

### Feature Availability by Version

| Feature | Version 0 | Version 1 |
|---------|-----------|-----------|
| IPv4/IPv6 Prefix PDUs | Yes | Yes |
| Serial/Reset Query | Yes | Yes |
| Router Key PDU | No | Yes |
| Timing parameters in End of Data | No | Yes |
| Version negotiation | No | Yes |
| Error Code 8 | No | Yes |

### Related RFCs

| RFC | Relationship |
|-----|--------------|
| RFC 6810 | Version 0 of this protocol (updated by 8210) |
| RFC 6480 | RPKI architecture |
| RFC 6811 | BGP Prefix Origin Validation (consumes RPKI data) |
| RFC 8208 | BGPsec Algorithms (Router Key subject public key format) |
| RFC 6487 | RPKI Certificate Profile (SKI format) |
| RFC 1982 | Serial Number Arithmetic (for serial comparison) |
| RFC 5925 | TCP-AO (recommended transport authentication) |
| RFC 4252 | SSH Authentication Protocol (SSH transport) |
| RFC 5246 | TLS (TLS transport) |
| RFC 6125 | TLS Server Identity Verification |

## Errata

None reported for RFC 8210.

## Compliance Checklist

- [ ] [RFC8210-5-1] [MUST] Reserved fields must be zero on transmission and ignored on receipt (§5)
- [ ] [RFC8210-5.1-1] [MUST] Flags field bits other than bit 0 must be zero on transmission and ignored on receipt (§5.1)
- [ ] [RFC8210-5.1-2] [MUST] Max Length must not be less than Prefix Length in IPv4/IPv6 Prefix PDUs (§5.1)
- [ ] [RFC8210-5.1-3] [MUST] Party detecting Session ID mismatch must immediately terminate with Error Report PDU code 0 (§5.1) {gap: ze detects no Session ID mismatch -- handlePDU adopts the Cache Response Session ID unconditionally (internal/component/bgp/plugins/rpki/rtr_session.go:233, s.sessionID = hdr.SessionID) with no comparison against the prior value, and the plugin has no Error Report writer to emit code 0}
- [ ] [RFC8210-5.1-4] [MUST] Router must flush all data learned from a cache on Session ID mismatch (§5.1) {gap: no cache flush on a Session ID change -- the mismatch is never detected (internal/component/bgp/plugins/rpki/rtr_session.go:233) and ROACache.Clear (internal/component/bgp/plugins/rpki/roa_cache.go:317) has no production caller, so VRPs learned under the old Session ID stay in the cache}
- [ ] [RFC8210-5.1-5] [MUST] Routers must treat sessions with different Protocol Version fields as separate sessions even if same Session ID (§5.1) {gap: sessions are not separated by protocol version -- on a version downgrade Run reconnects without clearing sessionID or serial (internal/component/bgp/plugins/rpki/rtr_session.go:96-99), so connectAndSync re-sends the previous version's Session ID and Serial Number in the new version's Serial Query (internal/component/bgp/plugins/rpki/rtr_session.go:172)}
- [ ] [RFC8210-5.3-1] [MUST] Cache must return the minimum set of changes needed to bring router into sync on Serial Query reply (§5.3) {not-applicable: cache-side response obligation -- ze is the RTR router: it parses Prefix PDUs (internal/component/bgp/plugins/rpki/rtr_pdu.go:114 parsePrefixPDU) and writes only Reset and Serial Queries (internal/component/bgp/plugins/rpki/rtr_pdu.go:92, 103), so it computes and sends no change set}
- [ ] [RFC8210-5.3-2] [MUST] Cache must merge multiple changes to same prefix/key into simplest possible view (§5.3) {not-applicable: cache-side response obligation -- ze emits no payload PDUs at all (the only writers are writeResetQuery and writeSerialQuery, internal/component/bgp/plugins/rpki/rtr_pdu.go:92, 103), so it merges no changes for transmission}
- [ ] [RFC8210-5.5-1] [MUST] Withdraw/announce field in payload PDUs must have value 1 (announce) when replying to Reset Query (§5.5) {not-applicable: cache-side emission obligation -- ze reads the announce/withdraw flag on receipt (internal/component/bgp/plugins/rpki/rtr_pdu.go:132) and has no Prefix PDU writer, so it never sets this field on transmission}
- [ ] [RFC8210-5.6-1] [MUST] Cache must ensure one and only one IPvX PDU for a unique {Prefix, Len, Max-Len, ASN} at any point in time (§5.6) {not-applicable: cache-side emission guarantee -- ze only parses Prefix PDUs (internal/component/bgp/plugins/rpki/rtr_pdu.go:114 parsePrefixPDU) and has no Prefix PDU writer, so it never emits or enforces one-PDU-per-VRP}
- [ ] [RFC8210-5.10-1] [MUST] Cache must ensure one and only one Router Key PDU for a unique {SKI, ASN, Subject Public Key} at any point in time (§5.10) {not-applicable: cache-side emission guarantee -- ze has no Router Key PDU writer; the only Router Key code is the discard arm in handlePDU (internal/component/bgp/plugins/rpki/rtr_session.go:377-379), so it emits no Router Key PDU whose uniqueness it would have to ensure}
- [ ] [RFC8210-5.10-2] [MUST] Implementations must compare Subject Public Key values as well as SKIs when detecting duplicate PDUs (§5.10) {gap: ze performs no Router Key duplicate detection -- handlePDU discards every Router Key PDU (internal/component/bgp/plugins/rpki/rtr_session.go:377-379) and stores neither SKI nor Subject Public Key, so no Subject Public Key comparison happens anywhere}
- [ ] [RFC8210-5.8-1] [MUST] End of Data Session ID and Protocol Version must match the corresponding Cache Response (§5.8) {not-applicable: cache-side emission obligation -- ze reads End of Data for its serial and timing parameters only (internal/component/bgp/plugins/rpki/rtr_session.go:291-306) and emits neither Cache Response nor End of Data PDUs whose Session ID and version it would have to match}
- [ ] [RFC8210-5.11-1] [MUST] Error Report PDU must not be sent for an Error Report PDU (§5.11) {not-applicable: Error Report sender obligation -- ze only reads received Error Reports (internal/component/bgp/plugins/rpki/rtr_session.go:363) and has no Error Report writer, so it can never emit an Error Report about an Error Report}
- [ ] [RFC8210-5.11-2] [MUST] Erroneous PDU field must be empty and Length of Encapsulated PDU must be zero for generic errors (§5.11) {not-applicable: Error Report sender obligation -- ze emits no Error Report PDU (it only reads them at internal/component/bgp/plugins/rpki/rtr_session.go:363), so it never encodes the Erroneous PDU field or its length}
- [ ] [RFC8210-5.11-3] [MUST] Error text, if present, must be UTF-8 encoded (§5.11) {not-applicable: Error Report sender obligation -- ze emits no Error Report PDU (it only reads them at internal/component/bgp/plugins/rpki/rtr_session.go:363), so it never encodes error text}
- [ ] [RFC8210-5.11-4] [MUST] Length of Error Text field must be zero if no diagnostic text present (§5.11) {not-applicable: Error Report sender obligation -- ze emits no Error Report PDU (it only reads them at internal/component/bgp/plugins/rpki/rtr_session.go:363), so it never sets Length of Error Text}
- [ ] [RFC8210-6-1] [MUST] Caches must set Expire Interval to a value larger than either Refresh Interval or Retry Interval (§6) {not-applicable: cache-side timing obligation -- ze is the router: it adopts whatever intervals the End of Data PDU carries (internal/component/bgp/plugins/rpki/rtr_session.go:298-306) and publishes none of its own, so it sets no Expire Interval}
- [ ] [RFC8210-6-2] [MUST] Router must not retain data past the time indicated by Expire Interval (§6) {gap: ze retains VRPs past the Expire Interval -- expireInterval only sets the socket read deadline (internal/component/bgp/plugins/rpki/rtr_session.go:188) and Run simply reconnects after retryInterval (internal/component/bgp/plugins/rpki/rtr_session.go:106-110); ROACache.Clear (internal/component/bgp/plugins/rpki/roa_cache.go:317) has no production caller, so cached VRPs outlive an expired session}
- [ ] [RFC8210-7-1] [MUST] Router must start each transport connection by issuing either a Reset Query or Serial Query (§7) {single-polarity: positive; connectAndSync writes a Reset Query or a Serial Query as the first bytes of every connection (internal/component/bgp/plugins/rpki/rtr_session.go:165-176) before entering readLoop, and no received input can make the router open a connection without a query, so there is no negative case}
- [ ] [RFC8210-7-2] [MUST] Cache receiving v0 query must downgrade to v0 or send v1 Error Report with Error Code 4 and terminate (§7) {not-applicable: cache-side obligation -- ze runs no RTR cache server; RTRSession dials out to a configured cache (internal/component/bgp/plugins/rpki/rtr_session.go:125 connectAndSync) and receives no queries to downgrade or reject}
- [ ] [RFC8210-7-3] [MUST] Router receiving v0 response must either downgrade to v0 or terminate the connection (§7) {gap: ze never inspects a received PDU's Protocol Version -- handlePDU switches on hdr.Type alone (internal/component/bgp/plugins/rpki/rtr_session.go:229-383) and reads hdr.Version nowhere, so a version 0 response is processed as if it matched the negotiated version; ze also cannot downgrade below version 1 (rtrVersionMin, internal/component/bgp/plugins/rpki/rtr_pdu.go:16)}
- [ ] [RFC8210-5.2-1] [MUST] Router must ignore any Serial Notify PDUs received during version negotiation startup period (§5.2, §7)
- [ ] [RFC8210-7-4] [MUST] Party receiving PDU for different Protocol Version after negotiation must drop the session (§7) {gap: no post-negotiation version check -- handlePDU compares no received PDU version against the negotiated s.version (internal/component/bgp/plugins/rpki/rtr_session.go:229-383); the one version-sensitive guard (internal/component/bgp/plugins/rpki/rtr_session.go:271) keys off the ASPA PDU type, not the header version field}
- [ ] [RFC8210-4-1] [MUST] Router must choose the most preferred cache by configuration (§4) {gap: ze selects no most-preferred cache -- startSessions launches a Run goroutine for every configured cache concurrently (internal/component/bgp/plugins/rpki/rpki.go:291-298) and the parsed preference is only rendered for display (internal/component/bgp/plugins/rpki/rpki.go:1026)}
- [ ] [RFC8210-8.1-1] [MUST] Router must send either a Serial Query or Reset Query periodically (§8.1, §8.2) {single-polarity: positive; the poll cadence is Run's post-sync wait of s.retryInterval (internal/component/bgp/plugins/rpki/rtr_session.go:106-110) followed by a fresh connectAndSync query, seeded at 600s (internal/component/bgp/plugins/rpki/rtr_session.go:81); no received input produces a non-conformant never-poll case}
- [ ] [RFC8210-8.3-1] [MUST] Router must issue Reset Query and get entire new load after Cache Reset when no preferred caches available (§8.3)
- [ ] [RFC8210-12-1] [MUST] Errors which are considered fatal must cause the session to be dropped (§12) {gap: Error Code 4 is fatal per RFC 8210 Section 12 but isFatalError excludes it (internal/component/bgp/plugins/rpki/rtr_pdu.go:167-169); once the session already sits at rtrVersionMin the downgrade branch is skipped and handlePDU logs the code and keeps the session alive (internal/component/bgp/plugins/rpki/rtr_session.go:366-375), so this fatal error drops no session}
- [ ] [RFC8210-8.2-1] [MUST] Cache must rate-limit Serial Notifies to no more frequently than one per minute (§8.2) {not-applicable: cache-side rate limit on Serial Notify emission -- ze receives Serial Notify and ignores it (internal/component/bgp/plugins/rpki/rtr_session.go:359-361) and has no Serial Notify writer}
- [ ] [RFC8210-9-1] [MUST] Caches and routers must implement unprotected transport over TCP using port rpki-rtr (323) (§9) {single-polarity: positive; ze implements the unprotected TCP transport by dialing tcp (internal/component/bgp/plugins/rpki/rtr_session.go:127) and defaults each cache server to port 323 (internal/component/bgp/plugins/rpki/rpki_config.go:174); it never declines to offer unprotected TCP, so there is no negative case}
- [ ] [RFC8210-9-2] [MUST] Cache and routers must be on the same trusted and controlled network when using unprotected TCP (§9) {not-applicable: operator deployment obligation -- ze dials plain TCP (internal/component/bgp/plugins/rpki/rtr_session.go:127) with no code path that can observe or enforce network topology}
- [ ] [RFC8210-9-3] [MUST] Caches and routers must use one of the protected protocols when available (§9) {gap: ze implements no protected RTR transport -- connectAndSync dials only unprotected TCP (internal/component/bgp/plugins/rpki/rtr_session.go:127) and the package contains no SSH, TLS, TCP-MD5 or TCP-AO client, so no protected protocol is available to select}
- [ ] [RFC8210-9.1-1] [MUST] Cache servers supporting SSH must accept RSA authentication (§9.1) {not-applicable: cache-server SSH obligation -- ze runs no RTR SSH server and no SSH client (connectAndSync dials plain TCP, internal/component/bgp/plugins/rpki/rtr_session.go:127), so it performs no SSH authentication}
- [ ] [RFC8210-9.1-2] [MUST] SSH user authentication must be supported (§9.1) {not-applicable: SSH-transport obligation -- ze implements no SSH RTR transport (connectAndSync dials plain TCP, internal/component/bgp/plugins/rpki/rtr_session.go:127), so SSH user authentication has no code path}
- [ ] [RFC8210-9.2-1] [MUST] Client routers using TLS must present client-side certificates (§9.2) {not-applicable: TLS-transport obligation -- ze implements no TLS RTR client (connectAndSync dials plain TCP, internal/component/bgp/plugins/rpki/rtr_session.go:127), so it presents no client certificate}
- [ ] [RFC8210-9.2-2] [MUST] TLS client certificates must include subjectAltName with iPAddress identities (§9.2) {not-applicable: TLS-transport obligation -- ze implements no TLS RTR client (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it holds and presents no rpki-rtr client certificate}
- [ ] [RFC8210-9.2-3] [MUST] Cache must check TLS client IP against iPAddress identities (§9.2) {not-applicable: cache-side TLS obligation -- ze runs no RTR TLS server (it dials out, internal/component/bgp/plugins/rpki/rtr_session.go:125), so it checks no connecting client IP against iPAddress identities}
- [ ] [RFC8210-9.2-4] [MUST] Routers must verify cache TLS server certificate using subjectAltName dNSName per RFC 6125 (§9.2) {not-applicable: TLS-transport obligation -- ze implements no TLS RTR client (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it verifies no cache server certificate}
- [ ] [RFC8210-9.2-5] [MUST NOT] TLS implementations must not use Common Name (CN-ID) for authentication (§9.2) {not-applicable: TLS-transport obligation -- ze has no rpki-rtr TLS implementation (internal/component/bgp/plugins/rpki/rtr_session.go:127 dials plain TCP), so it uses no certificate identifiers, CN-ID or otherwise}
- [ ] [RFC8210-9.2-6] [MUST] DNS-ID identifier type must be present in rpki-rtr server certificates (§9.2) {not-applicable: cache-server-certificate obligation under TLS -- ze runs no RTR TLS server and issues no server certificate (it is a plain-TCP client, internal/component/bgp/plugins/rpki/rtr_session.go:127)}
- [ ] [RFC8210-9.3-1] [MUST] TCP MD5 implementations must support key lengths of at least 80 printable ASCII bytes (§9.3) {not-applicable: TCP-MD5 transport obligation -- ze implements no TCP-MD5 for RTR (connectAndSync dials plain TCP, internal/component/bgp/plugins/rpki/rtr_session.go:127), so it holds no MD5 keys}
- [ ] [RFC8210-9.4-1] [MUST] TCP-AO implementations must support key lengths of at least 80 printable ASCII bytes and MAC lengths of at least 96 bits (§9.4) {not-applicable: TCP-AO transport obligation -- ze implements no TCP-AO for RTR (connectAndSync dials plain TCP, internal/component/bgp/plugins/rpki/rtr_session.go:127), so it holds no AO keys and negotiates no MAC length}
- [ ] [RFC8210-10-1] [MUST] Data from multiple caches must not be distinguished between when performing BGP validation (§10)
- [ ] [RFC8210-4-2] [MUST] Servers' clocks must be correct to a tolerance of approximately an hour (§4) {not-applicable: server-clock obligation -- ze is the RTR router client that dials out (internal/component/bgp/plugins/rpki/rtr_session.go:125 connectAndSync); it runs no cache server and holds no clock this binds}
- [ ] [RFC8210-4-3] [MUST] Serial Number comparison must take wrap-around into account per RFC 1982 (§4) {not-applicable: ze performs no Serial Number ordering comparison -- it stores the serial the cache reports (internal/component/bgp/plugins/rpki/rtr_session.go:297) and tests only serial == 0 to choose between a Reset Query and a Serial Query (internal/component/bgp/plugins/rpki/rtr_session.go:166); an equality test against zero is unaffected by wrap-around, and grep over the package finds no other serial comparison}
- [ ] [RFC8210-5.1-6] [SHOULD] Cache servers should not use the same Session ID across multiple protocol versions (§5.1)
- [ ] [RFC8210-5.6-2] [SHOULD] Router should raise Duplicate Announcement Received error on duplicate active record (§5.6, §5.10)
- [ ] [RFC8210-5.11-5] [SHOULD] Erroneous Error Report PDU session should be dropped (§5.11)
- [ ] [RFC8210-6-3] [SHOULD NOT] Router should not poll the cache sooner than indicated by Refresh Interval (§6)
- [ ] [RFC8210-6-4] [SHOULD NOT] Router should not retry sooner than indicated by Retry Interval (§6)
- [ ] [RFC8210-7-5] [SHOULD NOT] Caches should not send Serial Notify PDUs before version negotiation completes (§7)
- [ ] [RFC8210-8.3-2] [SHOULD] Router should attempt to connect to more-preferred caches on Cache Reset (§8.3)
- [ ] [RFC8210-9-4] [SHOULD] Both caches and routers should enable keep-alives when available (§9)
- [ ] [RFC8210-9-5] [SHOULD] Caches and routers should use TCP-AO transport (§9)
- [ ] [RFC8210-9.1-3] [SHOULD] Cache servers supporting SSH should accept ECDSA authentication (§9.1)
- [ ] [RFC8210-9.1-4] [SHOULD] Client routers should verify the public key of the cache (§9.1)
- [ ] [RFC8210-9.2-7] [SHOULD NOT] DNS names in rpki-rtr server certificates should not contain wildcard "*" (§9.2)
- [ ] [RFC8210-5.2-2] [MAY] Router may issue an immediate Serial Query or Reset Query upon receipt of Serial Notify (§5.2)
- [ ] [RFC8210-5.11-6] [MAY] Erroneous PDU field may be truncated for excessively long PDUs (§5.11)
- [ ] [RFC8210-7-6] [MAY] Router may retry connection using protocol version 0 on v0 cache termination (§7)
- [ ] [RFC8210-6-5] [MAY] Router may send first Serial Query or Reset Query immediately after version downgrade (§6)
- [ ] [RFC8210-9-6] [MAY] Caches and routers may use SSH, TCP MD5, IPsec, or TLS transport (§9)
- [ ] [RFC8210-7-7] [MUST] If either party receives a PDU with unrecognized Protocol Version during negotiation, it MUST either downgrade to a known version or terminate (§7) {gap: ze validates no received Protocol Version -- handlePDU never reads hdr.Version (internal/component/bgp/plugins/rpki/rtr_session.go:229-383), so an unrecognized version in a received PDU triggers neither a downgrade nor a termination; the downgrade at internal/component/bgp/plugins/rpki/rtr_session.go:366-370 fires only on an Error Report carrying code 4}
- [ ] [RFC8210-7-8] [MUST] Routers MUST handle Serial Notify PDUs received before version negotiation completes (by ignoring them) (§7)
- [ ] [RFC8210-9.2-8] [MUST] Client router using TLS MUST set its reference identifier to the DNS name of the rpki-rtr cache (§9.2) {not-applicable: TLS-transport obligation -- ze implements no TLS RTR client (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it sets no TLS reference identifier}
- [ ] [RFC8210-10-2] [MUST] Router MUST keep data from multiple caches marked as to source, as later updates MUST affect the correct data (§10) {gap: VRPs carry no source marking -- every RTR session writes one shared ROACache (internal/component/bgp/plugins/rpki/rpki.go:292) and vrpEntry holds only MaxLength and ASN (internal/component/bgp/plugins/rpki/roa_cache.go:12-15), so a withdraw from one cache removes a VRP a different cache announced}
- [ ] [RFC8210-9.3-2] [MUST] TCP MD5 implementations MUST support hexadecimal sequences of at least 32 characters (128 bits) (§9.3) {not-applicable: TCP-MD5 transport obligation -- ze implements no TCP-MD5 for RTR (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it parses no MD5 key material}
- [ ] [RFC8210-9.4-2] [MUST] TCP-AO implementations MUST support hexadecimal sequences of at least 32 characters (128 bits) (§9.4) {not-applicable: TCP-AO transport obligation -- ze implements no TCP-AO for RTR (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it parses no AO key material}
- [ ] [RFC8210-9.4-3] [MUST] The cryptographic algorithms and associated parameters described in RFC 5926 MUST be supported for TCP-AO (§9.4) {not-applicable: TCP-AO transport obligation -- ze implements no TCP-AO for RTR (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it supports no RFC 5926 algorithms}
- [ ] [RFC8210-9.2-9] [MUST] CAs issuing rpki-rtr server certificates MUST support the DNS-ID identifier type (§9.2) {not-applicable: certification-authority obligation -- ze issues no rpki-rtr server certificates and runs no TLS RTR transport (internal/component/bgp/plugins/rpki/rtr_session.go:127 dials plain TCP)}
- [ ] [RFC8210-9.2-10] [MUST NOT] CN field in TLS certificate MUST NOT be used for authentication (§9.2) {not-applicable: TLS-transport obligation -- ze has no rpki-rtr TLS implementation (internal/component/bgp/plugins/rpki/rtr_session.go:127), so it reads no certificate CN field for authentication}
- [ ] [RFC8210-8.1-2] [MUST] When transport first established, router MUST send Reset Query or Serial Query (§8.1) {single-polarity: positive; the first bytes connectAndSync writes on a freshly established transport are a Reset Query when the serial is 0 and a Serial Query otherwise (internal/component/bgp/plugins/rpki/rtr_session.go:165-176); no received input can produce a connection that carries no opening query, so there is no negative case}
- [ ] [RFC8210-8.4-1] [MUST] If cache cannot supply update and no other caches available, router MUST issue periodic Reset Queries (§8.4)
- [ ] [RFC8210-7-9] [SHOULD] Party dropping session after version mismatch SHOULD send Error Report with error code 8 (§7)
- [ ] [RFC8210-10-3] [SHOULD] Client SHOULD attempt to maintain at least one set of data regardless of cache changes (§10)
- [ ] [RFC8210-10-4] [SHOULD] Client switching to new cache SHOULD retain data from previous cache until fully synced (§10)
- [ ] [RFC8210-9.3-3] [SHOULD] Cache servers supporting TCP MD5 SHOULD support RFC 4808 for key rollover (§9.3)
- [ ] [RFC8210-8.4-2] [SHOULD] Router receiving "No Data Available" error SHOULD attempt to connect to other caches in preference order (§8.4)
- [ ] [RFC8210-11-1] [SHOULD] Cache identity SHOULD be verified and authenticated (§11)
- [ ] [RFC8210-8.2-2] [SHOULD] Cache SHOULD send Notify PDU with current Serial Number when cache serial changes (§8.2)
