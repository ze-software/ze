# RFC 4035 — Protocol Modifications for the DNS Security Extensions (DNSSEC)

**Status:** Proposed Standard
**Date:** March 2005
**Authors:** R. Arends, R. Austein, M. Larson, D. Massey, S. Rose
**Related:** RFC 4033 (DNSSEC introduction/requirements), RFC 4034 (resource
records: DNSKEY, RRSIG, NSEC, DS)

## Purpose

Defines how DNS resolvers and servers use the DNSSEC resource records (RFC 4034)
to authenticate DNS data: signing zones, serving signatures, and validating the
chain of trust so a resolver can tell whether an answer is Secure, Insecure
(unsigned), Bogus (signatures do not verify), or Indeterminate.

## EDNS0 header bits used by a resolver (§3.2, §4.9)

| Bit | Set by | Meaning |
|-----|--------|---------|
| **DO** (DNSSEC OK, EDNS0 flags) | client query | "Send me DNSSEC RRs (RRSIG/NSEC/DS)." A server only includes DNSSEC data when DO=1 (§3.2.1). |
| **CD** (Checking Disabled, message header) | client query | CD=1 tells a validating resolver "do NOT validate, give me the raw data even if Bogus." CD=0 (default) asks the upstream validating resolver to validate (§3.2.2, §4.9.2). |
| **AD** (Authenticated Data, message header) | validating server | The server sets AD=1 in a response when the answer (and authority) RRsets it returns were validated as Secure (§3.2.3). A stub MUST NOT trust AD unless the channel to the validating resolver is trusted. |

## Validation outcomes (§5, RFC 4033 §5)

| State | Meaning | Response |
|-------|---------|----------|
| Secure | Chain of trust verifies to a trust anchor | NOERROR, AD=1 (from a validating resolver) |
| Insecure | Provably unsigned delegation (no DS) | NOERROR, AD=0 -- a legitimate answer, not a failure |
| **Bogus** | Signatures present but do not verify, or missing where required | A validating resolver **MUST return RCODE 2 (SERVFAIL)** to a CD=0 query (§5.5) |
| Indeterminate | No trust anchor covers the name | Treated as Insecure by most resolvers |

## Requirements relevant to a stub / forwarding resolver

- A security-aware **non-validating** stub relies on a trusted validating
  resolver: it sends CD=0 and DO=1, and reads the AD bit / SERVFAIL result
  (§4.9.1-§4.9.3). It MUST NOT set CD=1 if it wants the upstream to validate.
- A validating resolver **MUST** set RCODE=SERVFAIL for data it determines is
  Bogus when the querier did not set CD (§5.5). SERVFAIL under DO=1/CD=0 is the
  wire signal of a broken chain.
- A resolver **MUST NOT** treat an Insecure (AD=0, unsigned) NOERROR answer as a
  failure -- unsigned zones are legitimate.

## Compliance Checklist

Every RFC 2119 keyword occurrence in RFC 4035 is captured below as one line.
MUST-level obligations (MUST, MUST NOT, REQUIRED) come first, then SHOULD-level,
then MAY-level; within each group lines are ordered by section. Requirement IDs
are permanent: `RFC4035-<section>-<ordinal>`.

### MUST-level

- [ ] [RFC4035-2.1-2] [MUST] A zone key DNSKEY RR MUST have the Zone Key bit of the flags RDATA field set (§2.1) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.1-4] [MUST NOT] Public keys stored in DNSKEY RRs that are not marked as zone keys MUST NOT be used to verify RRSIGs (§2.1) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.1-5] [MUST] For a signed zone usable other than as an island of security, the zone apex MUST contain at least one DNSKEY RR to act as a secure entry point (§2.1) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.2-1] [MUST] For each authoritative RRset in a signed zone there MUST be at least one RRSIG record whose owner, class, Type Covered, Original TTL, TTL, Labels, and Signer's Name match the RRset and identify an apex zone key (§2.2) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.2-3] [MUST NOT] An RRSIG RR itself MUST NOT be signed (§2.2) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.2-4] [MUST] The NS RRset that appears at the zone apex name MUST be signed (§2.2) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.2-5] [MUST NOT] The NS RRsets that appear at delegation points MUST NOT be signed (§2.2) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.2-6] [MUST NOT] Glue address RRsets associated with delegations MUST NOT be signed (§2.2) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.2-7] [MUST] There MUST be an RRSIG for each RRset using at least one DNSKEY of each algorithm in the zone apex DNSKEY RRset (§2.2) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.2-8] [MUST] The apex DNSKEY RRset MUST be signed by each algorithm appearing in the DS RRset at the delegating parent, if any (§2.2) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.3-1] [MUST] Each owner name in the zone that has authoritative data or a delegation point NS RRset MUST have an NSEC resource record (§2.3) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.3-3] [MUST NOT] An NSEC record and its associated RRSIG RRset MUST NOT be the only RRset at any particular owner name (§2.3) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.3-4] [MUST NOT] The signing process MUST NOT create NSEC or RRSIG RRs for owner name nodes that were not the owner name of any RRset before the zone was signed (§2.3) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.3-5] [MUST] The type bitmap of every NSEC RR MUST indicate the presence of both the NSEC record itself and its corresponding RRSIG record (§2.3) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.3-6] [MUST] In the NSEC bitmap at a delegation point, bits for the delegation NS RRset and any RRsets for which the parent zone has authoritative data MUST be set (§2.3) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.3-7] [MUST] In the NSEC bitmap at a delegation point, bits for any non-NS RRset for which the parent is not authoritative MUST be clear (§2.3) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.4-3] [MUST] All DS RRsets in a zone MUST be signed (§2.4) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.4-4] [MUST NOT] DS RRsets MUST NOT appear at a zone's apex (§2.4) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.5-1] [REQUIRED] If a CNAME RRset is present at a name in a signed zone, appropriate RRSIG and NSEC RRsets are REQUIRED at that name (§2.5) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.5-2] [MUST NOT] Types other than CNAME, its RRSIG and NSEC, and a KEY RRset for secure dynamic update MUST NOT be present at a CNAME name (§2.5) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-2.6-1] [REQUIRED] At the parental side of a zone cut, NSEC RRs are REQUIRED at the owner name (§2.6) {not-applicable: ze creates no DNSKEY, RRSIG, NSEC, or DS record for this rule to constrain -- ze signs no zone: grep -rnE 'TypeRRSIG|TypeDNSKEY|TypeNSEC|TypeDS' --include=*.go internal/ pkg/ cmd/ matches only the RFC 4035 conformance tests, and answerQuestions synthesizes A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-3-1] [MUST] A security-aware name server MUST support the EDNS0 message size extension (§3) {gap: ze's authoritative servers give the EDNS0 message size extension no support -- the only SetEdns0 producer in ze is the stub resolver (internal/component/resolve/dns/resolver.go:261); the server path reads an OPT only for the client-subnet address (internal/core/dnsserver/client.go:23) and writes a reply built by msg.SetReply with no OPT pseudo-RR and no requestor-payload-size handling (internal/core/dnsserver/handler.go:55)}
- [ ] [RFC4035-3-2] [MUST] A security-aware name server MUST support a message size of at least 1220 octets (§3) {gap: the UDP listener accepts at most 512 octets -- dns.Server is constructed with UDPSize left zero (internal/core/dnsserver/manager.go:165), which miekg defaults to MinMsgSize 512 (vendor/github.com/miekg/dns/server.go:287), so a query above 512 octets is never read whole and no reply advertises a larger size}
- [ ] [RFC4035-3-5] [MUST] A name server receiving a query without the EDNS OPT pseudo-RR or with the DO bit clear MUST treat the RRSIG, DNSKEY, and NSEC RRs as it would any other RRset (§3) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3-6] [MUST NOT] Such a name server MUST NOT perform any of the DNSSEC additional processing (§3) {single-polarity: positive; ze performs no DNSSEC additional processing for any query -- answerQuestions builds A/AAAA/SRV/SOA/NS records only (internal/plugins/geodns/server.go:168) and never consults the DO bit, so no processing path exists to drive negatively}
- [ ] [RFC4035-3-8] [MUST] A security-aware name server MUST copy the CD bit from a query into the corresponding response (§3)
- [ ] [RFC4035-3-9] [MUST] A security-aware name server MUST ignore the setting of the AD bit in queries (§3)
- [ ] [RFC4035-3.1-1] [MUST] Upon a DO-set query to a signed zone, RRSIG RRs that can be used to authenticate the response MUST be included (§3.1) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1-2] [MUST] NSEC RRs that provide authenticated denial of existence MUST be included in the response automatically (§3.1) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1-3] [MUST] Either a DS RRset or an NSEC RR proving that no DS RRs exist MUST be included in referrals automatically (§3.1) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.1-3] [MUST] When placing a signed RRset in the Answer section, the name server MUST also place its RRSIG RRs in the Answer section (§3.1.1) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.1-4] [MUST] If space does not permit inclusion of the RRSIG RRs that must accompany a signed RRset, or of a mandatory NSEC or DS RRset and its RRSIGs, the name server MUST set the TC bit (§3.1.1) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.1-5] [MUST] When placing a signed RRset in the Authority section, the name server MUST also place its RRSIG RRs in the Authority section (§3.1.1) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.1-6] [MUST] When placing a signed RRset in the Additional section, the name server MUST also place its RRSIG RRs in the Additional section (§3.1.1) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.1-8] [MUST NOT] The name server MUST NOT set the TC bit solely because RRSIG RRs did not fit in the Additional section (§3.1.1) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.2-3] [MUST] If there is not enough space for the apex DNSKEY RRset and its RRSIGs, the name server MUST omit them (§3.1.2) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.2-4] [MUST NOT] The name server MUST NOT set the TC bit solely because the apex DNSKEY and RRSIG RRs did not fit (§3.1.2) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.3-1] [MUST] When responding to a DO-set query, the name server MUST include NSEC RRs in the No Data, Name Error, Wildcard Answer, and Wildcard No Data cases (§3.1.3) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.3.1-1] [MUST] For a No Data response, the name server MUST include the NSEC RR for the queried name and its RRSIGs in the Authority section (§3.1.3.1) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.3.2-1] [MUST] For a Name Error response, the name server MUST include, with their RRSIGs, an NSEC RR proving no exact match and an NSEC RR proving no wildcard match (§3.1.3.2) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.3.3-1] [MUST] For a Wildcard Answer response, the name server MUST include the wildcard-expanded answer and its wildcard-expanded RRSIG RRs in the Answer section (§3.1.3.3) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.3.3-2] [MUST] For a Wildcard Answer response, the name server MUST include in the Authority section an NSEC RR and its RRSIGs proving that no closer match exists (§3.1.3.3) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.3.4-1] [MUST] For a Wildcard No Data response, the name server MUST include, with their RRSIGs, an NSEC RR proving no matching type at the wildcard owner name and an NSEC RR proving no closer match (§3.1.3.4) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.4-1] [MUST] If a DS RRset is present at the delegation point, the name server MUST return the DS RRset and its RRSIGs in the Authority section with the NS RRset (§3.1.4) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.4-2] [MUST] If no DS RRset is present, the name server MUST return the NSEC RR proving the DS RRset is absent and its RRSIGs with the NS RRset (§3.1.4) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.4-3] [MUST] The name server MUST place the NS RRset before the NSEC RRset and its RRSIGs (§3.1.4) {not-applicable: ze's authoritative servers hold no RRSIG, NSEC, DS, or DNSKEY record to place in any section: answerQuestions builds A/AAAA/SRV/SOA/NS answers from local host-sets (internal/plugins/geodns/server.go:168) and as112 answers only negatively (internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.1.4.1-1] [MUST] When authoritative for the child zone but not the parent and not offering recursion, on a DS query at the zone cut the name server MUST return an authoritative no-data response (§3.1.4.1)
- [ ] [RFC4035-3.1.5-2] [MUST NOT] A name server performing its own zone validation MUST NOT selectively reject some RRs and accept others (§3.1.5) {not-applicable: ze serves no zone transfer: grep -rniE 'axfr|ixfr' --include=*.go internal/ finds no producer, and no ze DNS server holds DNSSEC records to transfer}
- [ ] [RFC4035-3.1.5-3] [MUST] The DS RRset MUST be included in zone transfers of the parent zone in which it is authoritative data (§3.1.5) {not-applicable: ze serves no zone transfer: grep -rniE 'axfr|ixfr' --include=*.go internal/ finds no producer, and no ze DNS server holds DNSSEC records to transfer}
- [ ] [RFC4035-3.1.5-4] [MUST] NSEC RRs MUST be included in zone transfers of the zone in which they are authoritative data (§3.1.5) {not-applicable: ze serves no zone transfer: grep -rniE 'axfr|ixfr' --include=*.go internal/ finds no producer, and no ze DNS server holds DNSSEC records to transfer}
- [ ] [RFC4035-3.1.5-5] [MUST] The parental NSEC RR at a zone cut MUST be included in zone transfers of the parent zone (§3.1.5) {not-applicable: ze serves no zone transfer: grep -rniE 'axfr|ixfr' --include=*.go internal/ finds no producer, and no ze DNS server holds DNSSEC records to transfer}
- [ ] [RFC4035-3.1.5-6] [MUST] The NSEC at the zone apex of the child zone MUST be included in zone transfers of the child zone (§3.1.5) {not-applicable: ze serves no zone transfer: grep -rniE 'axfr|ixfr' --include=*.go internal/ finds no producer, and no ze DNS server holds DNSSEC records to transfer}
- [ ] [RFC4035-3.1.5-7] [MUST] RRSIG RRs MUST be included in zone transfers of the zone in which they are authoritative data (§3.1.5) {not-applicable: ze serves no zone transfer: grep -rniE 'axfr|ixfr' --include=*.go internal/ finds no producer, and no ze DNS server holds DNSSEC records to transfer}
- [ ] [RFC4035-3.1.6-2] [MUST NOT] A security-aware name server MUST NOT set the AD bit in a response unless it considers all RRsets in the Answer and Authority sections to be authentic (§3.1.6) {single-polarity: positive; no ze code sets AuthenticatedData on a reply -- the single non-test AuthenticatedData reference reads the upstream's bit in the stub resolver (internal/component/resolve/dns/resolver.go:278) -- so an AD-asserting response cannot be produced to reject}
- [ ] [RFC4035-3.1.6-4] [MUST NOT] The name server MUST NOT treat authoritative-zone data as authentic unless it obtained the zone via secure means (§3.1.6) {single-polarity: positive; ze takes its zone data from the local running configuration and still serves it with AD clear, because no reply-building path sets AuthenticatedData (internal/core/dnsserver/handler.go:55, internal/plugins/geodns/server.go:168); there is no AD-setting path to drive negatively}
- [ ] [RFC4035-3.1.6-5] [MUST NOT] The name server MUST NOT treat authoritative-zone data as authentic unless this behavior has been configured explicitly (§3.1.6) {single-polarity: positive; ze has no configuration leaf that marks local zone data authentic, so the AD bit stays clear whatever the operator configures (the geodns YANG carries no such leaf and no server path writes AuthenticatedData, internal/plugins/geodns/server.go:168)}
- [ ] [RFC4035-3.1.6-6] [MUST] A security-aware name server that supports recursion MUST follow the recursive-server CD and AD bit rules for data obtained via recursion (§3.1.6) {not-applicable: ze's DNS servers never recurse and have no resolver side: shapeAuthoritative clears RecursionAvailable on every reply (internal/core/dnsserver/handler.go:74) and each AnswerFunc answers from local state alone, forwarding nothing upstream (internal/plugins/geodns/server.go:221, internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.2.1-1] [MUST] The resolver side of a security-aware recursive name server MUST set the DO bit when sending requests (§3.2.1) {not-applicable: ze's DNS servers never recurse and have no resolver side: shapeAuthoritative clears RecursionAvailable on every reply (internal/core/dnsserver/handler.go:74) and each AnswerFunc answers from local state alone, forwarding nothing upstream (internal/plugins/geodns/server.go:221, internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.2.1-2] [MUST] If the DO bit in an initiating query is not set, the name server side MUST strip any authenticating DNSSEC RRs from the response (§3.2.1) {not-applicable: ze's DNS servers never recurse and have no resolver side: shapeAuthoritative clears RecursionAvailable on every reply (internal/core/dnsserver/handler.go:74) and each AnswerFunc answers from local state alone, forwarding nothing upstream (internal/plugins/geodns/server.go:221, internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.2.1-3] [MUST NOT] The name server side MUST NOT strip any DNSSEC RR types that the initiating query explicitly requested (§3.2.1) {not-applicable: ze's DNS servers never recurse and have no resolver side: shapeAuthoritative clears RecursionAvailable on every reply (internal/core/dnsserver/handler.go:74) and each AnswerFunc answers from local state alone, forwarding nothing upstream (internal/plugins/geodns/server.go:221, internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.2.2-1] [MUST] The name server side MUST pass the state of the CD bit to the resolver side along with the initiating query (§3.2.2) {not-applicable: ze's DNS servers never recurse and have no resolver side: shapeAuthoritative clears RecursionAvailable on every reply (internal/core/dnsserver/handler.go:74) and each AnswerFunc answers from local state alone, forwarding nothing upstream (internal/plugins/geodns/server.go:221, internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.2.2-4] [MUST] If the CD bit is not set and the query matches a BAD cache entry, the name server side MUST return RCODE 2 (server failure) (§3.2.2) {not-applicable: ze's DNS servers never recurse and have no resolver side: shapeAuthoritative clears RecursionAvailable on every reply (internal/core/dnsserver/handler.go:74) and each AnswerFunc answers from local state alone, forwarding nothing upstream (internal/plugins/geodns/server.go:221, internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-3.2.3-2] [MUST] The resolver side MUST determine whether the RRs are authentic by following the RFC's authentication procedure (§3.2.3) {not-applicable: ze's DNS servers never recurse and have no resolver side: shapeAuthoritative clears RecursionAvailable on every reply (internal/core/dnsserver/handler.go:74) and each AnswerFunc answers from local state alone, forwarding nothing upstream (internal/plugins/geodns/server.go:221, internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-4.1-1] [MUST] A security-aware resolver MUST include an EDNS OPT pseudo-RR with the DO bit set when sending queries (§4.1) {single-polarity: positive; the DO bit is emitted rather than parsed -- SetEdns0(4096, validating) sets it whenever dnssec-validation is permissive or strict (internal/component/resolve/dns/resolver.go:261) -- so there is no non-conformant input to reject; off mode is a plain non-security-aware stub, which this requirement does not govern}
- [ ] [RFC4035-4.1-2] [MUST] A security-aware resolver MUST support a message size of at least 1220 octets (§4.1) {single-polarity: positive; the resolver advertises 4096 octets and miekg sizes the receive buffer from that advertisement (vendor/github.com/miekg/dns/client.go:201), so a response above 1220 octets arrives whole; a message-size floor has no non-conformant input to reject}
- [ ] [RFC4035-4.1-4] [MUST] A security-aware resolver MUST use the sender's UDP payload size field in the EDNS OPT pseudo-RR to advertise the message size it will accept (§4.1) {single-polarity: positive; the sender's UDP payload size field is emitted, not parsed -- SetEdns0 writes 4096 on every query (internal/component/resolve/dns/resolver.go:261) -- so no malformed input exists to reject}
- [ ] [RFC4035-4.1-5] [MUST] A security-aware resolver's IP layer MUST handle fragmented UDP packets correctly whether received via IPv4 or IPv6 (§4.1) {not-applicable: UDP fragment reassembly belongs to the host IP stack, which ze neither implements nor bypasses -- the resolver exchanges datagrams through the ordinary miekg client socket (internal/component/resolve/dns/resolver.go:263) and grep -rniE 'fragment|reassembl' --include=*.go internal/component/resolve internal/core/dnsserver finds no producer}
- [ ] [RFC4035-4.2-1] [MUST] A security-aware resolver MUST support the signature verification mechanisms the RFC describes (§4.2) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-4.2-3] [MUST] A resolver's signature verification support MUST include verification of wildcard owner names (§4.2) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-4.2-5] [MUST] When retrieving missing NSEC RRs on the parental side of a zone cut, an iterative-mode resolver MUST query the parent zone name servers, not the child (§4.2) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-4.2-6] [MUST] When retrieving a missing DS, an iterative-mode resolver MUST query the parent zone name servers, not the child (§4.2) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-4.3-1] [MUST] A security-aware resolver MUST be able to determine whether it should expect a particular RRset to be signed, distinguishing Secure, Insecure, Bogus, and Indeterminate (§4.3) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-4.4-1] [MUST] A security-aware resolver MUST be capable of being configured with at least one trusted public key or DS RR (§4.4) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-4.6-2] [MUST] A security-aware resolver MUST clear the AD bit when composing query messages (§4.6) {single-polarity: positive; the AD bit of an outgoing query is emitted, not parsed -- the query is composed by new(mdns.Msg) plus SetQuestion (internal/component/resolve/dns/resolver.go:255) and no ze code sets AuthenticatedData on a query -- so no negative input exists}
- [ ] [RFC4035-4.6-3] [MUST] A resolver MUST disregard the meaning of the CD and AD bits in a response unless it was obtained over a secure channel or the resolver was configured to trust them (§4.6)
- [ ] [RFC4035-4.7-2] [MUST] A resolver that implements a BAD cache MUST take steps to prevent the cache being used as a denial-of-service amplifier (§4.7) {not-applicable: ze keeps no BAD cache: Resolve caches only a non-empty successful answer (internal/component/resolve/dns/resolver.go:192) and a SERVFAIL yields an uncached empty result (internal/component/resolve/dns/resolver.go:285)}
- [ ] [RFC4035-4.7-3] [MUST] Since RRsets that fail to validate lack trustworthy TTLs, the implementation MUST assign a TTL (§4.7) {not-applicable: ze keeps no BAD cache: Resolve caches only a non-empty successful answer (internal/component/resolve/dns/resolver.go:192) and a SERVFAIL yields an uncached empty result (internal/component/resolve/dns/resolver.go:285)}
- [ ] [RFC4035-4.7-7] [MUST NOT] A resolver MUST NOT return RRsets from the BAD cache unless it is not required to validate their signatures (§4.7) {not-applicable: ze keeps no BAD cache: Resolve caches only a non-empty successful answer (internal/component/resolve/dns/resolver.go:192) and a SERVFAIL yields an uncached empty result (internal/component/resolve/dns/resolver.go:285)}
- [ ] [RFC4035-4.8-1] [MUST] A validating resolver MUST treat the signature of a valid signed DNAME RR as also covering unsigned CNAME RRs synthesizable from it, at least by not rejecting the message solely for containing them (§4.8) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-4.9-1] [MUST] A security-aware stub resolver MUST support the DNSSEC RR types, at least by not mishandling responses that contain them (§4.9)
- [ ] [RFC4035-4.9.1-2] [MUST] A validating security-aware stub resolver MUST set the DO bit (§4.9.1) {single-polarity: positive; ze's stub sets the DO bit under permissive and strict (internal/component/resolve/dns/resolver.go:261) and leaves the signature check to the upstream; the bit is emitted, not parsed, so there is no non-conformant input to reject}
- [ ] [RFC4035-4.9.3-2] [MUST NOT] A security-aware stub resolver MUST NOT place any reliance on signature validation performed on its behalf except when it obtained the data from a trusted recursive name server over a secure channel (§4.9.3) {gap: ze's stub rests a security decision on validation performed for it over an unauthenticated channel -- strict mode rejects an answer solely because the configured upstream returned SERVFAIL (internal/component/resolve/dns/resolver.go:103-106) while the client speaks plain UDP with no TLS, TSIG, or other authentication of that server (internal/component/resolve/dns/resolver.go:81)}
- [ ] [RFC4035-5-1] [MUST] To authenticate an apex DNSKEY RRset with an initial key, the resolver MUST verify that the initial DNSKEY RR appears in the apex DNSKEY RRset and has the Zone Key Flag set (§5) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5-2] [MUST] To authenticate an apex DNSKEY RRset with an initial key, the resolver MUST verify that some RRSIG RR covers the apex DNSKEY RRset and that it together with the initial DNSKEY authenticates the RRset (§5) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5-3] [MUST NOT] The absence of DNSSEC data in a response MUST NOT by itself be taken as an indication that no authentication information exists (§5) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.2-1] [MUST] A security-aware resolver MUST query the parent zone name servers for the DS RRset if a referral includes neither a DS RRset nor an NSEC RRset proving the DS RRset does not exist (§5.2) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.2-3] [MUST] A security-aware resolver MUST use the parent NSEC RR when attempting to prove that a DS RRset does not exist (§5.2) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.1-1] [MUST] The RRSIG RR and the RRset MUST have the same owner name and the same class (§5.3.1) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.1-2] [MUST] The RRSIG RR's Signer's Name field MUST be the name of the zone that contains the RRset (§5.3.1) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.1-3] [MUST] The RRSIG RR's Type Covered field MUST equal the RRset's type (§5.3.1) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.1-4] [MUST] The number of labels in the RRset owner name MUST be greater than or equal to the value in the RRSIG RR's Labels field (§5.3.1) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.1-5] [MUST] The validator's notion of the current time MUST be less than or equal to the RRSIG RR's Expiration field (§5.3.1) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.1-6] [MUST] The validator's notion of the current time MUST be greater than or equal to the RRSIG RR's Inception field (§5.3.1) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.1-7] [MUST] The RRSIG RR's Signer's Name, Algorithm, and Key Tag fields MUST match the owner name, algorithm, and key tag of some DNSKEY RR in the zone's apex DNSKEY RRset (§5.3.1) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.1-8] [MUST] The matching DNSKEY RR MUST be present in the zone's apex DNSKEY RRset (§5.3.1) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.1-9] [MUST] The matching DNSKEY RR MUST have the Zone Flag bit set (§5.3.1) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.1-10] [MUST] If more than one DNSKEY RR matches, the validator MUST try each until the signature validates or the matching keys are exhausted (§5.3.1) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.2-1] [MUST NOT] If the RRSIG Labels field is greater than the RRset's label count, the RRSIG did not pass validation and MUST NOT be used to authenticate the RRset (§5.3.2) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.2-2] [MUST NOT] When reconstructing the parent-zone NSEC RRset at a delegation, its NSEC RRs MUST NOT be combined with NSEC RRs from the child zone (§5.3.2) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.2-3] [MUST NOT] When reconstructing the child-apex NSEC RRset, its NSEC RRs MUST NOT be combined with NSEC RRs from the parent zone (§5.3.2) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.3-1] [MUST] When the RRSIG Labels field does not equal the owner name's label count, the resolver MUST verify that wildcard expansion was applied properly before considering the RRset authentic (§5.3.3) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.3.3-2] [MUST] On accepting an RRset as authentic, the validator MUST set the RRSIG RR and each RR's TTL to no greater than the minimum of the RRset TTL, the RRSIG TTL, the RRSIG Original TTL, and the time until the RRSIG expires (§5.3.3) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.4-1] [MUST] A security-aware resolver MUST authenticate the NSEC RRsets that comprise a denial-of-existence proof (§5.4) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.4-2] [MUST] If the complete set of necessary NSEC RRsets is not present in a response, the resolver MUST resend the query to obtain them (§5.4) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.4-3] [MUST] The resolver MUST bound the work it puts into answering any particular query (§5.4) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.4-4] [MUST] A validator MUST ignore the settings of the NSEC and RRSIG bits in an NSEC RR (§5.4) {not-applicable: ze runs no local validator: the resolver's whole DNSSEC surface is dnssecDecision (internal/component/resolve/dns/resolver.go:99), which inspects the rcode alone -- no signature verification, trust-anchor store, DS chain, or NSEC proof exists in ze}
- [ ] [RFC4035-5.5-2] [MUST] When validation was done to service a recursive query, the name server MUST return RCODE 2 to the originating client (§5.5) {not-applicable: ze's DNS servers never recurse and have no resolver side: shapeAuthoritative clears RecursionAvailable on every reply (internal/core/dnsserver/handler.go:74) and each AnswerFunc answers from local state alone, forwarding nothing upstream (internal/plugins/geodns/server.go:221, internal/plugins/as112/server.go:86)}
- [ ] [RFC4035-5.5-3] [MUST] The name server MUST return the full response if and only if the original query had the CD bit set (§5.5) {not-applicable: ze's DNS servers never recurse and have no resolver side: shapeAuthoritative clears RecursionAvailable on every reply (internal/core/dnsserver/handler.go:74) and each AnswerFunc answers from local state alone, forwarding nothing upstream (internal/plugins/geodns/server.go:221, internal/plugins/as112/server.go:86)}

### SHOULD-level

- [ ] [RFC4035-2.1-1] [SHOULD] For each private key used to create RRSIGs in a zone, the zone SHOULD include a zone DNSKEY RR containing the corresponding public key (§2.1)
- [ ] [RFC4035-2.3-2] [SHOULD] The TTL value for any NSEC RR SHOULD be the same as the minimum TTL value field in the zone SOA RR (§2.3)
- [ ] [RFC4035-2.4-1] [SHOULD] A DS RRset SHOULD be present at a delegation point when the child zone is signed (§2.4)
- [ ] [RFC4035-2.4-5] [SHOULD] A DS RR SHOULD point to a DNSKEY RR that is present in the child's apex DNSKEY RRset (§2.4)
- [ ] [RFC4035-2.4-6] [SHOULD] The child's apex DNSKEY RRset SHOULD be signed by the private key corresponding to the DS-referenced DNSKEY (§2.4)
- [ ] [RFC4035-2.4-7] [SHOULD] The TTL of a DS RRset SHOULD match the TTL of the delegating NS RRset (§2.4)
- [ ] [RFC4035-3-3] [SHOULD] A security-aware name server SHOULD support a message size of 4000 octets (§3)
- [ ] [RFC4035-3-4] [SHOULD] A security-aware name server SHOULD ensure UDP datagrams it transmits over IPv6 are fragmented at the minimum IPv6 MTU when necessary, unless the path MTU is known (§3)
- [ ] [RFC4035-3-10] [SHOULD NOT] A name server that synthesizes CNAME RRs from DNAME RRs SHOULD NOT generate signatures for the synthesized CNAME RRs (§3)
- [ ] [RFC4035-3.1.1-1] [SHOULD] When responding to a DO-set query, the name server SHOULD attempt to send RRSIG RRs a resolver can use to authenticate the response (§3.1.1)
- [ ] [RFC4035-3.1.1-2] [SHOULD] The name server SHOULD make every attempt to keep an RRset and its associated RRSIGs together in a response (§3.1.1)
- [ ] [RFC4035-3.1.2-2] [SHOULD NOT] The name server SHOULD NOT include the apex DNSKEY RRset unless there is enough space for both it and its associated RRSIGs (§3.1.2)
- [ ] [RFC4035-3.1.3.2-2] [SHOULD] If a single NSEC RR proves both required points, the name server SHOULD include that NSEC RR and its RRSIGs only once in the Authority section (§3.1.3.2)
- [ ] [RFC4035-3.1.3.4-2] [SHOULD] If a single NSEC RR proves both required points, the name server SHOULD include that NSEC RR and its RRSIGs only once in the Authority section (§3.1.3.4)
- [ ] [RFC4035-3.1.6-1] [SHOULD] A security-aware name server SHOULD clear the CD bit when composing an authoritative response (§3.1.6)
- [ ] [RFC4035-3.2.2-2] [SHOULD] When the CD bit is set, the recursive name server SHOULD, if possible, return the requested data even if its local policy would reject the records (§3.2.2)
- [ ] [RFC4035-3.2.2-3] [SHOULD] If the CD bit is set and the query matches a BAD cache entry, the name server side SHOULD return the data from the BAD cache (§3.2.2)
- [ ] [RFC4035-3.2.3-1] [SHOULD] The name server side SHOULD set the AD bit if and only if the resolver side considers all Answer RRsets and any relevant negative Authority RRs authentic (§3.2.3)
- [ ] [RFC4035-4.1-3] [SHOULD] A security-aware resolver SHOULD support a message size of 4000 octets (§4.1)
- [ ] [RFC4035-4.2-2] [SHOULD] A security-aware resolver SHOULD apply the signature verification mechanisms to every received response, except the enumerated cases (§4.2)
- [ ] [RFC4035-4.4-2] [SHOULD] A security-aware resolver SHOULD be capable of being configured with multiple trusted public keys or DS RRs (§4.4)
- [ ] [RFC4035-4.4-3] [SHOULD] A security-aware resolver SHOULD have a reasonably robust mechanism for obtaining trust anchor keys when it boots (§4.4)
- [ ] [RFC4035-4.5-1] [SHOULD] A security-aware resolver SHOULD cache each response as a single atomic entry containing the entire answer and its associated DNSSEC RRs (§4.5)
- [ ] [RFC4035-4.5-2] [SHOULD] The resolver SHOULD discard the entire atomic cache entry when any RR contained in it expires (§4.5)
- [ ] [RFC4035-4.7-4] [SHOULD] The TTL assigned to a failed-validation RRset SHOULD be small, to limit the effect of caching an attack's results (§4.7)
- [ ] [RFC4035-4.7-5] [SHOULD] Resolvers SHOULD track queries that result in validation failures (§4.7)
- [ ] [RFC4035-4.7-6] [SHOULD] Resolvers SHOULD answer from the BAD cache only after the failure count for the query exceeds a threshold value (§4.7)
- [ ] [RFC4035-4.9.2-1] [SHOULD NOT] A non-validating security-aware stub resolver SHOULD NOT set the CD bit unless requested by the application layer (§4.9.2)
- [ ] [RFC4035-4.9.2-2] [SHOULD] A validating security-aware stub resolver SHOULD set the CD bit (§4.9.2)
- [ ] [RFC4035-4.9.3-3] [SHOULD NOT] A validating security-aware stub resolver SHOULD NOT examine the setting of the AD bit (§4.9.3)
- [ ] [RFC4035-5-4] [SHOULD] A resolver SHOULD expect authentication information from signed zones (§5)
- [ ] [RFC4035-5-5] [SHOULD] A resolver SHOULD believe a zone is signed if it is configured with the zone's public key, or the parent is signed and the delegation contains a DS RRset (§5)
- [ ] [RFC4035-5.1-1] [SHOULD] If a validator cannot obtain an initial authenticated key for an island of security, it SHOULD operate as if the island's zones are unsigned (§5.1)
- [ ] [RFC4035-5.2-4] [SHOULD] If the resolver supports none of the algorithms in an authenticated DS RRset, it SHOULD treat the child zone as if it were unsigned (§5.2)
- [ ] [RFC4035-5.5-1] [SHOULD] If none of the RRSIGs can be validated, the response SHOULD be considered BAD (§5.5)

### MAY-level

- [ ] [RFC4035-2.1-3] [MAY] Public keys associated with other DNS operations MAY be stored in DNSKEY RRs that are not marked as zone keys (§2.1)
- [ ] [RFC4035-2.2-2] [MAY] An RRset MAY have multiple RRSIG RRs associated with it (§2.2)
- [ ] [RFC4035-2.4-2] [MAY] A DS RRset MAY contain multiple records, each referencing a public key in the child zone (§2.4)
- [ ] [RFC4035-3-7] [MAY] For explicit security-RR-type queries matching more than one served zone, as long as responses stay self-consistent, the name server MAY return one of the enumerated response forms (§3)
- [ ] [RFC4035-3.1.1-7] [MAY] When both a signed RRset and its RRSIGs will not fit in the Additional section, the name server MAY retain the RRset and drop the RRSIG RRs (§3.1.1)
- [ ] [RFC4035-3.1.2-1] [MAY] When a DO-set query requests the SOA or NS RRs at a signed zone's apex, the name server MAY return the apex DNSKEY RRset in the Additional section (§3.1.2)
- [ ] [RFC4035-3.1.5-1] [MAY] An authoritative name server MAY reject an entire zone transfer if the zone fails to meet the signing requirements (§3.1.5)
- [ ] [RFC4035-3.2.3-3] [MAY] For backward compatibility, a recursive name server MAY set the AD bit when a response includes unsigned CNAME RRs demonstrably synthesizable from an authentic DNAME RR also in the response (§3.2.3)
- [ ] [RFC4035-4.2-4] [MAY] Security-aware resolvers MAY query for missing security RRs in an attempt to perform validation (§4.2)
- [ ] [RFC4035-4.6-1] [MAY] A security-aware resolver MAY set a query's CD bit to indicate that it takes responsibility for authentication (§4.6)
- [ ] [RFC4035-4.7-1] [MAY] Security-aware resolvers MAY cache data with invalid signatures, subject to restrictions (§4.7)
- [ ] [RFC4035-4.8-2] [MAY] A resolver MAY retain synthesized CNAME RRs in its cache or in the answers it hands back (§4.8)
- [ ] [RFC4035-4.9.1-1] [MAY] A non-validating security-aware stub resolver MAY include the DNSSEC RRs returned by a recursive name server in the data it hands back to the application (§4.9.1)
- [ ] [RFC4035-4.9.3-1] [MAY] A non-validating security-aware stub resolver MAY examine the AD bit to see whether the recursive name server claims to have verified the data (§4.9.3)
- [ ] [RFC4035-5.2-2] [MAY] If an authenticated NSEC proves that no DS exists, an initial DNSKEY or DS RR for the child zone or a delegation below it MAY be used to re-establish an authentication path (§5.2)

## How ze applies this

- `internal/component/resolve/dns` is a stub/forwarding resolver, not a full
  local validator. Its `dnssec-validation` mode (`system { dns {
  dnssec-validation } }`, default `off`) follows the stub model:
  - `off` -- DO bit clear, behavior unchanged.
  - `permissive` / `strict` -- set the EDNS0 DO bit (CD stays 0), so a validating
    upstream signs/validates and returns SERVFAIL on a Bogus chain.
  - `strict` rejects a SERVFAIL-under-validation answer as an error (fail-closed);
    `permissive` logs a warning and returns the empty result (fail-open).
  - A NOERROR answer is accepted whether AD=1 (Secure) or AD=0
    (Insecure/unsigned), per the MUST above.
- Full local chain validation (building trust from the root DNSKEY and verifying
  RRSIGs in-process) is intentionally not implemented: ze delegates validation to
  the configured upstream, which is the standard security-aware stub deployment.
- ze's authoritative side (`internal/plugins/geodns`, `internal/plugins/as112`,
  on the shared `internal/core/dnsserver` harness) serves synthesized, unsigned
  zones: it holds no DNSKEY, RRSIG, NSEC, or DS record, so the RFC's
  zone-signing, signed-response, zone-transfer, and recursive-server rules govern
  nothing ze produces. The header-bit rules it does meet are the CD bit copied
  from the query into the reply (`msg.SetReply`,
  `internal/core/dnsserver/handler.go:56`), the query's AD bit ignored and never
  asserted in a reply, and a DS query inside a served zone answered as
  authoritative no-data (`internal/plugins/geodns/server.go:205`).
- Two name-server EDNS0 obligations are unmet and tracked as gaps in
  `docs/features/rfc-status.md`: the server emits no OPT pseudo-RR and honors no
  requestor payload size (RFC4035-3-1), and its UDP listener reads at most 512
  octets because `dns.Server.UDPSize` stays zero
  (`internal/core/dnsserver/manager.go:165`, RFC4035-3-2).
