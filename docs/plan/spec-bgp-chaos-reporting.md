# Spec: bgp-chaos-reporting (Phase 5 of 5) — SKELETON

**Master design:** `docs/plan/spec-bgp-chaos.md`
**Previous spec:** `spec-bgp-chaos-families.md`
**Next spec:** None (final phase)

**Status:** Skeleton — to be fleshed out after Phase 4 completes.

## Post-Compaction Recovery

**Re-read these after context compaction:**
1. This spec file
2. `docs/plan/spec-bgp-chaos.md` - master design (dashboard mockup, summary format)
3. Phase 1-4 done specs - learnings and actual data structures
4. `.claude/rules/planning.md` - workflow rules

## Task

Add production-quality reporting and remaining polish to `ze-bgp-chaos`: live terminal dashboard, JSON event log, Prometheus metrics, and iBGP/eBGP mixed-mode validation refinements.

**Scope:**
- Live terminal dashboard with per-peer status table (escape codes, in-place updates)
- Fallback to line-based output when not a TTY
- JSON structured event log (for post-mortem analysis)
- Prometheus metrics endpoint (for grafana during long runs)
- iBGP + eBGP mixed validation refinements (AS_PATH handling)
- Connection collision handling
- `--quiet` and `--verbose` modes

## Required Reading

### Architecture Docs
- [ ] `docs/plan/spec-bgp-chaos.md` - master design (dashboard mockup, exit summary format, metrics)
  → Decision: Dashboard uses terminal escape codes with TTY fallback
  → Decision: JSON log uses NDJSON format (one object per line)
- [ ] `docs/architecture/core-design.md` - connection collision (RFC 4271 Section 6.8)
  → Constraint: Higher BGP identifier wins collision resolution

### Source Code
- [ ] Phase 1-4 implementation files (paths TBD)
  → Constraint: Must integrate with existing event bus and data structures

**Key insights:**
- Per-peer `Families []string` available on `PeerProfile` and `SimProfile` — can be displayed in dashboard peer table
- `EventEORSent.Count` now reports total routes across all families (not just IPv4)
- Non-unicast families (VPN, EVPN, FlowSpec) are count-tracked only — no per-route validation
- IPv4/IPv6 unicast have full prefix-level validation via `netip.Prefix`
- `familyToNLRI` and `familyToAFISAFI` maps exist for family string lookups
- Route counts per family: unicast families get full `RouteCount`, others get `RouteCount/4`
- Chaos withdrawals currently only affect IPv4 unicast routes (extending to other families is future work)

**Phase 3 chaos insights:**
- Chaos event log: events emitted as `peer.EventChaosExecuted` with `ChaosAction` string field (e.g. "tcp-disconnect", "reconnect-storm")
- Chaos stats in exit summary: `ChaosEvents`, `Reconnections`, `Withdrawn` counters in `report.Summary`
- Summary already conditionally shows chaos section when `ChaosEvents > 0` (see `report/summary.go`)
- Events channel carries all lifecycle events: `EventEstablished`, `EventDisconnected`, `EventChaosExecuted`, `EventReconnecting`, `EventWithdrawalSent`, etc.
- Chaos scheduler logs to stderr: `"ze-bgp-chaos | scheduler | <action-type> -> peer <N>"`
- 10 action types with kebab-case string names (tcp-disconnect, notification-cease, hold-timer-expiry, partial-withdraw, full-withdraw, disconnect-during-burst, reconnect-storm, connection-collision, malformed-update, config-reload)

## Current Behavior (MANDATORY)

**Source files read:** (to be re-read after Phase 4 completes)
- [ ] `cmd/ze-bgp-chaos/main.go` — CLI entry point, flag parsing
- [ ] `cmd/ze-bgp-chaos/orchestrator.go` — multi-peer lifecycle coordination
- [ ] `cmd/ze-bgp-chaos/peer/simulator.go` — per-peer goroutine with event reporting
- [ ] `cmd/ze-bgp-chaos/peer/session.go` — TCP + OPEN + KEEPALIVE exchange
- [ ] `cmd/ze-bgp-chaos/validation/model.go` — expected state model
- [ ] `cmd/ze-bgp-chaos/chaos/scheduler.go` — seed-based event scheduling
- [ ] `cmd/ze-bgp-chaos/report/summary.go` — exit summary (Phase 2)

**Behavior to preserve:**
- All Phase 1-4 functionality
- Basic stdout output from Phase 1

**Behavior to change:**
- Replace basic stdout with full dashboard
- Add JSON log, Prometheus, verbose/quiet modes

## Data Flow (MANDATORY)

### Entry Point
- Reporter receives events from orchestrator and peer simulators
- Dashboard, JSON log, and Prometheus all consume the same event stream

### Transformation Path
1. Events generated by peers/chaos/validation → event channel
2. Reporter goroutine reads events
3. Dashboard: renders to terminal (escape codes or line-based)
4. JSON log: serializes to NDJSON file
5. Prometheus: updates gauges/counters/histograms
6. On shutdown: final summary rendered

### Boundaries Crossed
| Boundary | How | Verified |
|----------|-----|----------|
| Peers/Orchestrator ↔ Reporter | Go channel (event stream) | [ ] |
| Reporter ↔ Terminal | stdout with escape codes | [ ] |
| Reporter ↔ JSON file | NDJSON writes | [ ] |
| Reporter ↔ Prometheus | HTTP /metrics endpoint | [ ] |

### Integration Points
- Phase 2 convergence data (for metrics)
- Phase 3 chaos event data (for chaos log section)
- Phase 4 per-family stats (for family breakdown)

### Architectural Verification
- [ ] Reporter is read-only consumer of events
- [ ] No coupling between dashboard and validation logic

## Acceptance Criteria

| AC ID | Input / Condition | Expected Behavior |
|-------|-------------------|-------------------|
| AC-1 | TTY output | Live dashboard with per-peer table, updates in-place |
| AC-2 | Piped output (not TTY) | Line-based output, no escape codes |
| AC-3 | `--log events.json` | NDJSON file with one event per line |
| AC-4 | `--metrics :9090` | Prometheus endpoint at /metrics |
| AC-5 | `--quiet` | Only errors and final summary |
| AC-6 | `--verbose` | Extra debug output per BGP message |
| AC-7 | iBGP + eBGP peers | Validation accounts for AS_PATH differences |
| AC-8 | Connection collision | Handled gracefully, one connection wins |
| AC-9 | 30-minute run | No memory growth, dashboard responsive |
| AC-10 | `Ctrl-C` during dashboard | Clean shutdown, summary printed |

## 🧪 TDD Test Plan

### Unit Tests

| Test | File | Validates | Status |
|------|------|-----------|--------|
| `TestDashboardRender` | `report/dashboard_test.go` | Produces correct terminal output | |
| `TestDashboardFallback` | `report/dashboard_test.go` | Line-based output when not TTY | |
| `TestJSONLogFormat` | `report/jsonlog_test.go` | Valid NDJSON output | |
| `TestJSONLogEventTypes` | `report/jsonlog_test.go` | All event types serialized correctly | |
| `TestMetricsEndpoint` | `report/metrics_test.go` | Prometheus /metrics returns valid format | |
| `TestMetricsGauges` | `report/metrics_test.go` | Gauges update correctly | |
| `TestSummaryFormat` | `report/summary_test.go` | Exit summary has all required sections | |
| `TestQuietMode` | `report/dashboard_test.go` | Only errors and summary | |
| `TestVerboseMode` | `report/dashboard_test.go` | Extra per-message output | |
| `TestCollisionHandling` | `peer/session_test.go` | One connection wins, other closed | |

_Additional tests to be identified after Phase 4._

### Boundary Tests (MANDATORY for numeric inputs)

| Field | Range | Last Valid | Invalid Below | Invalid Above |
|-------|-------|------------|---------------|---------------|
| metrics port | 1024-65535 | 65535 | 1023 | 65536 |

### Functional Tests

| Test | Location | End-User Scenario | Status |
|------|----------|-------------------|--------|
| `chaos-full-run` | `test/chaos/full-run.ci` | Full 5-phase tool, all features, 30s run | |

## Files to Create

- `cmd/ze-bgp-chaos/report/dashboard.go` - live terminal output
- `cmd/ze-bgp-chaos/report/dashboard_test.go`
- `cmd/ze-bgp-chaos/report/jsonlog.go` - NDJSON event log
- `cmd/ze-bgp-chaos/report/jsonlog_test.go`
- `cmd/ze-bgp-chaos/report/metrics.go` - Prometheus endpoint
- `cmd/ze-bgp-chaos/report/metrics_test.go`

## Files to Modify

- `cmd/ze-bgp-chaos/main.go` - wire reporting options
- `cmd/ze-bgp-chaos/orchestrator.go` - feed events to reporter
- `cmd/ze-bgp-chaos/peer/session.go` - connection collision handling
- `cmd/ze-bgp-chaos/validation/model.go` - iBGP/eBGP-aware checks
- `cmd/ze-bgp-chaos/report/summary.go` - enhance with per-family and chaos stats

### Integration Checklist

| Integration Point | Needed? | File |
|-------------------|---------|------|
| YANG schema | No | N/A |
| Makefile | No | Already added in Phase 1 |

## Implementation Steps

1. **Read Phase 1-4 learnings** - understand event types and data structures
   → Review: What events exist? What data is available for display?

2. **Write dashboard tests** - rendering, TTY detection, fallback
   → Run: Tests FAIL

3. **Implement dashboard** - terminal escape codes, in-place updates
   → Run: Tests PASS

4. **Write JSON log tests** - NDJSON format, all event types
   → Run: Tests FAIL

5. **Implement JSON log** - file writing, serialization
   → Run: Tests PASS

6. **Write metrics tests** - Prometheus format, gauge updates
   → Run: Tests FAIL

7. **Implement Prometheus metrics** - HTTP endpoint, gauges/counters/histograms
   → Run: Tests PASS

8. **Implement connection collision** - RFC 4271 Section 6.8
   → Run: Tests PASS

9. **Add iBGP/eBGP validation refinements** - AS_PATH, LOCAL_PREF checks

10. **Implement quiet/verbose modes**

11. **Verify** - `make lint && make test`

12. **Update master design doc** (Spec Propagation Task)

## Spec Propagation Task

**MANDATORY at end of this phase (final phase):**

Instead of updating follow-on specs, complete these:

1. **Update `docs/plan/spec-bgp-chaos.md`** (master design) with:
   - Final architecture as-built vs as-designed
   - Deviations and reasons
   - Performance observations
   - Known limitations and future work

2. **Update `docs/architecture/`** if architectural insights discovered

3. **Consider adding to MEMORY.md:**
   - Patterns that worked well
   - Common pitfalls
   - Testing patterns for long-lived processes

## Mistake Log

### Wrong Assumptions
| What was assumed | What was true | How discovered | Impact |
|------------------|---------------|----------------|--------|

### Failed Approaches
| Approach | Why abandoned | Replacement |
|----------|---------------|-------------|

### Escalation Candidates
| Mistake | Frequency | Proposed rule | Action |
|---------|-----------|---------------|--------|

## Implementation Audit

### Requirements from Task
| Requirement | Status | Location | Notes |
|-------------|--------|----------|-------|
| Live terminal dashboard | | | |
| TTY fallback | | | |
| JSON event log | | | |
| Prometheus metrics | | | |
| Quiet/verbose modes | | | |
| iBGP/eBGP validation | | | |
| Connection collision | | | |

### Acceptance Criteria
| AC ID | Status | Demonstrated By | Notes |
|-------|--------|-----------------|-------|
| AC-1 | | | |
| AC-2 | | | |
| AC-3 | | | |
| AC-4 | | | |
| AC-5 | | | |
| AC-6 | | | |
| AC-7 | | | |
| AC-8 | | | |
| AC-9 | | | |
| AC-10 | | | |

### Tests from TDD Plan
| Test | Status | Location | Notes |
|------|--------|----------|-------|

### Files from Plan
| File | Status | Notes |
|------|--------|-------|

### Audit Summary
- **Total items:**
- **Done:**
- **Partial:**
- **Skipped:**
- **Changed:**

## Checklist

### Goal Gates (MUST pass)
- [ ] AC-1..AC-10 demonstrated
- [ ] Tests pass (`make test`)
- [ ] No regressions (`make functional`)

### Quality Gates (SHOULD pass)
- [ ] `make lint` passes
- [ ] Master design doc updated
- [ ] Implementation Audit completed

### 🧪 TDD
- [ ] Tests written
- [ ] Tests FAIL
- [ ] Implementation complete
- [ ] Tests PASS
- [ ] Boundary tests for numeric inputs

### Completion
- [ ] Spec Propagation Task completed (master doc + architecture)
- [ ] Spec updated with Implementation Summary
- [ ] Spec moved to `docs/plan/done/NNN-bgp-chaos-reporting.md`
