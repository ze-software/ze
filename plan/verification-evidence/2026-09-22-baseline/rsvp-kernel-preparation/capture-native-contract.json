{
  "artifact_retention": "plan/verification-evidence/2026-09-22-baseline/artifact-retention.json",
  "status": "prepared-not-run",
  "adapter": "plan/verification-evidence/2026-09-22-baseline/rsvp-kernel-preparation/capture-native-guest.py",
  "attempt": "attempt-008",
  "host_cwd": "/home/thomas/Code/github.com/ze-software/ze/main",
  "proposed_host_argv": [
    "env", "CGO_ENABLED=0",
    "/home/thomas/Code/github.com/ze-software/ze/main/tmp/session/2026-09-21-97650314-2770-467e-8578-1739803540cf/scratch/baseline-runtime-accepted/bin/le",
    "le", "qemu", "run", "kernel", "tmp/kernel/build/vmlinuz",
    "packages", "iproute2 python3 tcpdump", "timeout", "8m", "command",
    "python3 /workspace/plan/verification-evidence/2026-09-22-baseline/rsvp-kernel-preparation/capture-native-guest.py /workspace/tmp/session/2026-09-21-97650314-2770-467e-8578-1739803540cf/scratch/baseline-runtime-fixed/bin/rsvpte.test /workspace/tmp/session/2026-09-21-97650314-2770-467e-8578-1739803540cf/scratch/baseline-runtime-fixed/bin/ze /workspace/plan/verification-evidence/2026-09-22-baseline/rsvp-kernel-preparation/attempt-008"
  ],
  "proposed_host_command": "env CGO_ENABLED=0 /home/thomas/Code/github.com/ze-software/ze/main/tmp/session/2026-09-21-97650314-2770-467e-8578-1739803540cf/scratch/baseline-runtime-accepted/bin/le le qemu run kernel tmp/kernel/build/vmlinuz packages 'iproute2 python3 tcpdump' timeout 8m command 'python3 /workspace/plan/verification-evidence/2026-09-22-baseline/rsvp-kernel-preparation/capture-native-guest.py /workspace/tmp/session/2026-09-21-97650314-2770-467e-8578-1739803540cf/scratch/baseline-runtime-fixed/bin/rsvpte.test /workspace/tmp/session/2026-09-21-97650314-2770-467e-8578-1739803540cf/scratch/baseline-runtime-fixed/bin/ze /workspace/plan/verification-evidence/2026-09-22-baseline/rsvp-kernel-preparation/attempt-008'",
  "inputs": {
    "arguments": ["absolute executable test binary", "absolute executable daemon", "new absolute output directory below rsvp-kernel-preparation"],
    "identity": "Reuse the exact baseline-runtime-fixed inputs selected for attempt-007 and untraced-experiment-001. Do not rebuild or substitute as part of this invocation.",
    "native_test_flags": ["-test.v", "-test.run=^TestRSVPNativeProducer$", "-test.timeout=4m", "-rsvp-producer-daemon", "the supplied absolute daemon path"],
    "execution": "Direct native test child; no strace, SSH observer, readiness wait, route/sysctl writes, namespace deletion, packet injection or source/kernel change. Existing native QEMU action remains the runner.",
    "guest_packages": ["iproute2", "python3", "tcpdump"]
  },
  "capture": {
    "namespace_selection": "Only /var/run/netns/rsvp-<direct native test child PID>-h, -m and -e; exact-path stat every 100ms, with timestamped discovery outcomes. No broad netns list or foreign namespace matching.",
    "startup": "Start each passive tcpdump immediately on first observation of its namespace; never wait for tcpdump readiness before allowing the test to progress. Early-packet coverage is not guaranteed; startup timestamps and errors are retained.",
    "tcpdump_options": ["-i", "any", "-p", "-n", "-s", "0", "-U", "-B", "2048", "-Z", "root", "-w", "namespace.pcap"],
    "filter": "ether proto 0x8847 or ether proto 0x8848 or arp or (ip and (udp or ip proto 46 or ip proto 89 or icmp))",
    "filter_boundary": "All MPLS unicast/multicast frames, ARP, IPv4 UDP, RSVP, OSPF and ICMP. Plain TCP/SSH streams are excluded; MPLS frames are retained regardless of inner protocol. Complete packets, no rotation or truncation.",
    "sampling": "First sample immediately after discovery, then at most one sample per namespace every 2 seconds; no overlapping samples within a namespace, at most three sampling workers and 121 samples per namespace. Each command has a 2-second timeout and file-backed complete output.",
    "state": ["all native IPv4 route tables with details", "native MPLS route table with details", "IPv4 rules", "link details and statistics", "IPv4 neighbors and statistics", "IPv4 addresses", "/proc/net/snmp", "/proc/net/netstat", "/proc/net/dev", "IPv4 ip_forward", "MPLS platform_labels", "actual per-interface MPLS input", "actual all/default/per-interface rp_filter"],
    "read_failures": "Every command retains argv, start/end wall and monotonic timestamps, stdout, stderr, numeric exit code or null on spawn failure, and timeout/spawn status. Proc helper stdout contains timestamped file reads and directory enumeration; each value/error is explicit, and any failed read/list exits 1. Expected-but-not-yet-created interface files are recorded as errors, not invented zero values.",
    "shutdown": "After native exit, SIGINT each owned tcpdump and wait up to 5s; escalate to SIGTERM for 2s then SIGKILL only if needed. Retain signals, exact exit code and final pcap/stdout/stderr. The native test alone owns its normal namespace cleanup. Adapter interruption is forwarded to the native child and recorded. Forced VM termination may leave only partial evidence, never a pass."
  },
  "collection": {
    "host_owner": "Main alone invokes QEMU. Before execution retain actual SHA-256 identities for le, tmp/kernel/build/vmlinuz, adapter, test and daemon; retain exact host argv, start/end times, complete action stdout/stderr and exit status in new host-invocation-008 artifacts. A host-action timeout is not a guest result.",
    "guest_directory": "plan/verification-evidence/2026-09-22-baseline/rsvp-kernel-preparation/attempt-008",
    "guest_outputs": {
      "invocation.json": "Exact native/adapter argv, child PID, kernel uname, cwd, before/after test and daemon hashes, copied source hashes and timestamps. Copies are workspace interpretation context, not proof of executable build provenance.",
      "sources/": "Copied carrier, relevant RSVP/MPLS implementation, MPLS architecture contract and this exact adapter.",
      "carrier.json": "Native child argv, start/end, PID, exact exit code and output paths; any adapter termination is explicit.",
      "producer.stdout and producer.stderr": "Separate, complete native outputs; no syscall interception.",
      "namespace-discovery.jsonl": "Timestamped exact-name existence checks, inode/device when present and explicit errors.",
      "captures/": "Per-namespace pcap plus complete tcpdump stdout/stderr and lifecycle JSON, including final packet/loss statistics in stderr.",
      "samples/": "Per-namespace, numbered bounded live snapshots; each command has its own JSON/stdout/stderr. No overwriting older samples.",
      "observations/": "ip/tcpdump versions and guest process capabilities with argv/times/outputs/status.",
      "result.json": "Separate test_status and collection_status, exact test PASS/SKIP/FAIL marker observations, native and adapter exit codes, collection errors and sample counts."
    },
    "adapter_exit_codes": {"0": "exact native pass and no recorded collection errors; still requires provenance and packet review", "1": "native failure or no exact pass/skip result, regardless of collection status", "2": "exact native skip with native exit zero, regardless of collection status", "3": "exact native pass with collection errors"},
    "interpretation": "Missing result.json means an incomplete attempt. Preserve all files on every outcome; never delete/reuse attempt-008. Mere captures or successful installed-route readback do not prove forwarding. Review loose-native payload chronology across H/M/E against routes, neighbors, MPLS input/platform labels, rp_filter and SNMP counters. Kernel in-label 1000 EEXIST is the deliberate foreign-label negative control, not a product defect."
  },
  "preparation_verification": "Not executed, syntax-checked, formatted, tested, built or run in QEMU/Docker by this worker. Main owns runtime verification. This contract is a proposal, not evidence of a pass."
}
