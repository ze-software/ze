# Deferrals: fixit-tombstone-ebgp-transitive

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/deferral-tracking.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-16 | F4 shard (agents, tests 83 / 365 / 368) | **The repo now handles RFC-invalid LOCAL_PREF two contradictory ways.** Three tests fed LOCAL_PREF on an EBGP session, which RFC 4271 Section 5.1.5 forbids ("MUST NOT"); ze correctly discards it per RFC 7606 Section 7.5 (`message/rfc7606.go:442-449`, `validateLocalPrefAttr`: `!isIBGP` -> `AttributeDiscard`) and marks it in place as ATTR_253 (`message/attr_discard.go:96-115`). `bgp-rs-fastpath-ebgp-shared.ci` and `remove-private-as-replace-peer.ci` removed the invalid attribute from the INPUT frame; `remove-private-as-export.ci:49` instead expects the marker on the wire (`C0FD0405010000`). All three pass. Pick one precedent and apply it to all three | Two agents fixing sibling tests in parallel reached opposite conclusions; both are defensible and neither is wrong on its own. The input-side fix is the better precedent (it keeps a test about AS_PATH rewriting free of an orthogonal RFC-7606 concern), but blessing the marker is only wrong if ze should NOT propagate it -- which is exactly the open question the destination spec owns. **RULED by Thomas 2026-07-16: take the input-side precedent** (remove the invalid LOCAL_PREF from `remove-private-as-export.ci`'s source frame), **explicitly as the interim answer "until we re-engineer how we deal with attributes"** -- adopted on the test-hygiene argument, NOT as a settled verdict on attribute handling. **Row stays live: the pick is made, the edit is not applied.** It goes `done` when the `.ci` changes, not when the decision is recorded (`ai/rules/deferral-tracking.md`: "Close a row when the work is implemented, not when it is filed"). The change is byte-mechanical and its target is proven: `remove-private-as-replace-peer.ci:39` already carries the exact frame it produces (`length=0x0037`, `attr-len=0x001C`, same AS_PATH, no LOCAL_PREF). Both `:21` and `:49` shrink by the 7 LOCAL_PREF bytes; the load-bearing AS_PATH assertion `[65000 64496 64497]` stays byte-identical. Recorded with the before/after hex in the destination spec | `plan/spec-tombstone-forwarding-policy.md` (re-homed 2026-07-22: the original destination `spec-fixit-tombstone-ebgp-transitive.md` closed as `plan/learned/1239` with this edit still unapplied -- `test/plugin/remove-private-as-export.ci:51` still carries the marker bytes `C0FC0405010000`, and learned 1237 later changed only the code byte `C0FD`->`C0FC`. The ruling and hex table survive in git history of the closed spec) | open |
| 2026-07-16 | spec-fixit-tombstone-ebgp-transitive (second bug found while implementing) | **Ze has TWO wire code points for ONE attribute.** `attribute.AttrTombstone = 252` (`internal/core/bgp/attribute/attribute.go:66`) is what `wireu.WriteTombstone` emits (`tombstone.go:77`); `attrCodeAttrDiscard = 253` (`internal/component/bgp/message/attr_discard.go:30`) is what the receive path stamps. `ExtractUpstreamAttrDiscard` (`:135`) searches only for 253, so a 252 marker ZE ITSELF WROTE is invisible to its own upstream-merge and draft Section 5.1's merge rule silently fails against ze's own egress marker. The `attr_discard` to `attr-tombstone` rename left two half-implementations that do not recognise each other | The live bug is CLOSED in this commit by recognising both codes, so nothing is broken meanwhile. Unifying the code points and following the rename through the file and symbol names is mechanical but wide: 5 files plus hex fixtures plus any `.ci` carrying `FD` bytes, and it wants its own spec and its own red-to-green rather than riding a conformance fix | `plan/learned/1237-fixit-tombstone-code-point-split.md` (implemented and closed: code point unified to the single `attribute.AttrTombstone = 252`, `attrCodeAttrDiscard` and the wireu dual-recognition shim deleted, `.ci` wire expectation moved `C0FD`->`C0FC`) | done |
| 2026-07-16 | spec-fixit-tombstone-ebgp-transitive (Known Limitations) | eBGP RS-clients bypass the prepend funnel, so draft Section 5.3's Transitive-clear does not reach them: `forward_rs.go:351-360` and `reactor_api_forward.go:526-535` hand out `update.WireUpdate`, the received wire, with no per-destination buffer. Clearing the bit there would corrupt the shared wire for every other peer | Honouring 5.3 for RS-clients needs a third pooled slot mirroring `ebgpSlotASN4` plus release plumbing at `recent_cache.go:461,527`, which trades RS zero-copy for conformance on marker-bearing UPDATEs. That is a performance-versus-conformance decision for Thomas, not an implementation detail | `plan/spec-tombstone-forwarding-policy.md` (re-homed 2026-07-22: the original destination closed as `plan/learned/1239` with this item still listed as a deferred Known Limitation. The forwarding-policy spec owns Section 5.3 semantics and already holds the sibling policy row; still needs Thomas's ruling before it can move) | deferred |
| 2026-07-16 | spec-fixit-tombstone-ebgp-transitive (draft Section 5.3 SHOULD) | The configurable forwarding policy the draft says implementations SHOULD provide (inherit / strip / propagate, per neighbor or peer-group). Ze now implements only the default, "inherit". Two draft ambiguities ride along: 5.3 says a non-transitive marker is "not forwarded" under inherit, which ze cannot do without a rebuild; and the Partial bit is ambiguous for a recognizing speaker under inherit (5.3's inherit bullet says transitive markers are forwarded with Partial set, its propagate bullet says a recognizing forwarder MUST clear it), and ze sets Partial nowhere | Needs a YANG config surface design, and "strip" needs a rebuild rather than an in-place mask. The draft ambiguities are Thomas's to resolve as its author before an implementation can be correct | `plan/spec-tombstone-forwarding-policy.md` (skeleton; BLOCKED on the two draft ambiguities, recorded there as D-1 and D-2 with the draft text quoted. They are Thomas's to resolve as the draft's author, not an implementer's: the spec cannot leave `skeleton` until both are answered, and the first deliverable may be a draft revision rather than a Go change) | deferred |

