# Ad-hoc deferrals -- 2026-07-27 (session dd843d81)

Found while closing `spec-fixit-bgp-session-fsm-lifecycle` AC-6/AC-7 and fixing
`test/plugin/reactor-bus-subscribe.ci`. Each was verified at the producing function or
in a captured run log, and each is separable from the goal the surrounding work existed
to achieve (`ai/rules/completion.md`).

Evidence for every row below is in this session's captured suite runs: a clean-tree
baseline (`tmp/runlogs/plugin-suite-baseline.log`) plus three runs with the reactor
change applied (`plugin-suite.log`, `plugin-suite-fixed2.log`, `bus-fix2.log`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-27 | ad-hoc (fixing `reactor-bus-subscribe.ci`) | `test/plugin/eor-per-family.ci` carries the same latent race that made `reactor-bus-subscribe.ci` red, and is FLAKY on it: PASS in four suite runs, FAIL in the fifth with `RPC error from ze-plugin-engine:update-route: no established peers to send to`. The peer harness's own `cmd=api` lines (`eor.ci`, `:8`) already drive both End-of-RIB markers that the `expect=bgp` lines assert; the observer then sends the SAME two markers itself after a bare `ready()` with no establishment barrier. ze-peer is check-mode: it exits the moment its expectations are met, so whichever of the two producers loses the race dispatches into an empty peer set. | Separable, and the fix needs a decision this session has no basis to make: which producer is authoritative. Deleting the observer's sends removes `.ci` test lines (the `test-weakening` hook blocks it, correctly -- it cannot tell a duplicate from a lost assertion), and deleting the harness's `cmd=api` lines changes what the expectations are proving. The bus-subscribe fix did not need that choice, because there the redundant dispatch had NO second producer to weigh against: ze emits that marker itself from `sendInitialRoutes`. Destination already owns this family: it root-caused the same shape ("The real cause is a **teardown race**", `:236`) and documents check-mode ze-peer's accept-one-and-exit semantics (`:324`, citing `internal/test/peer/peer.go`). Re-homed 2026-08-23 when that spec closed. Its root-cause finding survives in `docs/architecture/testing/ci-format.md`, and one premise of this row is now known WRONG: `cmd=api` executes nothing in any suite, so the peer block's own lines drive no EOR and the observer may be the only producer. | plan/future/spec-ci-peer-harness-contract-backlog.md | deferred |
| 2026-07-27 | ad-hoc (plugin suite baseline for the AC-6/AC-7 change) | `test/plugin/as112-community-choice.ci` is STABLY red, on a clean tree at `404b65cca` as well as with the reactor change: it expects an End-of-RIB (len 23) as the first message and receives the route UPDATE instead (`ORIGIN/AS_PATH/NEXT_HOP/MED/LOCAL_PREF/COMMUNITIES 65535:65281/NLRI 192.175.48.0/24`). The daemon logs `end-of-rib suppressed: peer is still draining its initial route sync ... marker will be emitted by sendInitialRoutes when the drain completes`, so the marker is deliberately reordered behind the drain and the fixture still encodes the old order. | Observed, not diagnosed. Deciding whether the ORDER or the FIXTURE is wrong is an RFC 4724 question about when a speaker may emit End-of-RIB relative to its initial announcements, and the fixture may not be changed without owner approval (`ai/rules/testing.md`: never edit test data to make a failing test pass). It is also not this change's regression -- it is red identically at HEAD. Destination is the existing home for exactly this ordering question (initial-sync drain vs the markers and routes that ride it). | `689ce5eca`, `3a9bd8983a` | done 2026-09-05: the fixture asserts the End-of-RIB group unordered, and `Peer.forwardOrderHold` (`internal/component/bgp/reactor/peer.go`) fixed the ordering the test was pinning |

## Not defects: environment-dependent reds in `./le functional plugin`

Recorded so the next session does not spend a round chasing them. All are red on a clean
tree here and all name a missing capability in their own output; this host runs the suite
without root (`warning: running without root; missing capabilities: CAP_NET_BIND_SERVICE,
CAP_NET_RAW, CAP_NET_ADMIN`).

Written as a list, not a table: every table row under `plan/deferrals/` is parsed as
a six-cell deferral row, and a shorter row is reported as malformed ("a row this
gate cannot read is a row it cannot enforce").

- `teardown-session`, `teardown-session-all` -- L2TP, not BGP: `l2tp: kernel tunnel
  create failed ... genl tunnel create: operation not permitted`, after which the
  observer reports `session never established`.
- `l2tp-history-show`, `l2tp-session-detail-show`, `l2tp-sessions-show` -- the same
  L2TP kernel path, 60 s timeouts.
- `system-kernel-log-show` -- reads the kernel log.
- `appliance` vpp -- needs VPP on the host.

Separately, `bfd-echo-handshake`, `bfd-echo-multi-hop-reject`, `role-otc-unicast-scope`,
`rpf-multicast`, `aaa-radius-admin` and `aaa-radius-fallback` each flipped between PASS
and FAIL across the five runs WITHOUT any relevant tree change (`role-otc-unicast-scope`
failed on the clean baseline and passed with the reactor change applied, which no causal
story explains). They are flaky, not regressions, and are not counted against any change
made this session.
