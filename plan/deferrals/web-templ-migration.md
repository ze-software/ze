# Deferrals: web-templ-migration

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

No rows yet. The spec's phase 5 (the 143 lines of Go-literal markup across 18
files, chiefly `cli_terminal.go`, `page_system.go`, `page_interfaces.go`) is
the one item declared optional up front; it earns a row here only if a session
reaches phase 4 and stops.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
