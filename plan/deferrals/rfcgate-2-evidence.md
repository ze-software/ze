# Deferrals -- spec-rfcgate-2-evidence

Source: `plan/spec-rfcgate-2-evidence.md`. Format: `ai/rules/planning.md`.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-29 | spec-rfcgate-2-evidence | Wire `test/interop-ipsec/` (10 scenarios) into an automated pipeline, then change its `CARRIERS` row from `TIER_UNRUN` to a real tier | The suite has a runner (`./le integration interop-ipsec`) but no automated caller, so a tag there would be evidence nothing executes. Refused by the scanner today (AC-7), which is the correct interim state, not a fix **CLOSED 2026-09-03 after verifying the producer rather than the row: the suite runs in a pipeline and its carrier is tiered.** `.github/workflows/evidence-nightly.yml` runs `./le integration interop-ipsec`, and `interopCarriers` (`internal/le/rfc/carriers.go`) derives `tierNightly` from exactly that workflow, so the carrier is no longer untiered.** | `plan/spec-rfcgate-2-deferred-unrun-interop-trees.md` | done |
| 2026-07-29 | spec-rfcgate-2-evidence | Wire `test/interop-l2tp/` (3 scenarios) into an automated pipeline and tier its carrier | Same as above **CLOSED 2026-09-03 after verifying the producer rather than the row: the suite runs in a pipeline and its carrier is tiered.** `.github/workflows/evidence-nightly.yml` runs `./le deployment docker-l2tp-ppp-test`, and `interopCarriers` (`internal/le/rfc/carriers.go`) derives `tierNightly` from exactly that workflow, so the carrier is no longer untiered.** | `plan/spec-rfcgate-2-deferred-unrun-interop-trees.md` | done |
| 2026-07-29 | spec-rfcgate-2-evidence | Wire `test/interop-pppoe/` (1 scenario) into an automated pipeline and tier its carrier | Same as above **CLOSED 2026-09-03 after verifying the producer rather than the row: the suite runs in a pipeline and its carrier is tiered.** `.github/workflows/evidence-nightly.yml` runs `./le deployment docker-pppoe-accel-test`, and `interopCarriers` (`internal/le/rfc/carriers.go`) derives `tierNightly` from exactly that workflow, so the carrier is no longer untiered.** | `plan/spec-rfcgate-2-deferred-unrun-interop-trees.md` | done |
| 2026-07-29 | spec-rfcgate-2-evidence | Back-fill non-unit evidence for the ~2571 requirements bound only to `*_test.go` (measured: `unit/verify 2571, functional/verify 6, editor/verify 0, interop/nightly 0`). `ai/rules/testing.md` "Back-Fill New Test Types" requires the applicable set be named or the remainder recorded; the classifier's estimate (at least 76% of 2720 gated MUSTs wire-visible) is a sizing input only and may never gate (A-4) | This spec adds the carriers, the tiers and the ratchet; re-binding the existing corpus is the fleet drain. ~~`plan/spec-rfcgate-0-umbrella.md`~~ **Re-homed 2026-07-29:** the umbrella is not a destination for this. Its own D4 says "This spec set is MACHINERY ONLY. The fleet drain is named as follow-on work, **not scoped here**", and Constraint(D4) adds "no child may open the backlog". A destination that disclaims the work is not a home (`ai/rules/planning.md`), so a dedicated spec was created | `plan/spec-rfcgate-2-deferred-nonunit-evidence-backfill.md` | deferred |
| 2026-07-29 | spec-rfcgate-2-evidence | Bind `RFC7606-5.1-3 positive` to `test/interop/scenarios/bgp-rfc7606-relay-shape-frr/check.py` (retired; now `internal/le/interoplab/bgp/`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> (AC-16 interop clause, AC-17 interop half) | BLOCKED, not skipped -- but ~~because the route server prepends its own AS on the replay rail~~ **for a different reason (corrected 2026-07-29, see the row below).** The scenario fails at Path 1's `check_route_no_as("10.0.0.0/24", "65001")` because that assertion tests RFC 7947 route-server transparency against a peer that is NOT configured as an RS-client: `rs-client` defaults to `false` (`internal/component/bgp/plugins/rs/yang/ze-rs-conf.yang`) and no interop scenario sets it. Ze prepends correctly there. Either the scenario config gains `rs-client` or the assertion is wrong; that decision needs the evidence work in the destination spec. AC-17 forbids landing a binding with no recorded RED, and a binding whose scenario is RED for an unrelated reason would publish evidence for a failing test | `spec-rfcgate-2-evidence` (closed; LANDED 2026-07-29: the binding is on disk at `bgp-rfc7606-relay-shape-frr/check.py`, Docker-verified RED then GREEN; the blocker was a misdiagnosis, not a defect) | done |
| 2026-07-29 | spec-rfcgate-2-evidence | ~~Fix RS AS-path transparency on the adj-rib-in replay-on-peer-up path~~ **SUPERSEDED 2026-07-29: the defect does not exist.** Verified against the producing code, not the symptom: ONE prepend gate serves both rails (`if facts.isEBGP && !facts.rsClient`, `internal/component/bgp/reactor/reactor_api_forward.go`), reached by `RelayStoredRoute` via `reactor_api_relay.go` and by `ForwardUpdate` via `reactor_api_forward.go`; `facts.rsClient` comes only from the `session/rs-client` leaf (`reactor/peer_forward_facts.go` <- `reactor/config.go`), defaults `false`, and no interop scenario sets it. The scenario's earlier green depended on an RFC 4271 Section 5.1.2 bug that `8bb55e509` fixed on 2026-07-25; the red that followed was misread as a new route-server defect | Superseded by the row below, which carries the real gap. Recorded rather than deleted so the refutation survives (`ai/rules/never-destroy-work.md`) | `plan/spec-rfcgate-2-deferred-rs-replay-evidence.md` | done |
| 2026-07-29 | spec-rfcgate-2-evidence | ~~Give `RFC7947-x-1` non-unit evidence, and add an rs-client **relay** test pinning AS_PATH byte-identity through `RelayStoredRoute`~~ **NARROWED 2026-07-29: the evidence half LANDED; the relay test did NOT.** What remains is ONLY the rs-client relay test pinning AS_PATH byte-identity through `RelayStoredRoute`. Verified at closure: `internal/component/bgp/reactor/reactor_api_relay_test.go` is unmodified and a grep for either rsClient or rs-client over it returns nothing, so nothing pins the transform on the relay rail to an RS-client destination. The evidence half is discharged by the interop binding at `test/interop/scenarios/bgp-route-server-frr/check.py` (retired; now `internal/le/interoplab/bgp/`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->. Recorded as a narrowing rather than a close because this row bundled two separable items and only one landed: marking it `done` wholesale would have dropped a real test silently | The real gap under the refuted defect above, and it is evidence rather than behavior. `RFC7947-x-1` is bound to unit tests only (`ai/RFC-REQUIREMENTS.md`: positive `forward_rs_test.go`, negative `forward_rs_test.go`, both `unit/verify`), and the one relay test (`internal/component/bgp/reactor/reactor_api_relay_test.go`) proves only that the relay reaches the forward pool and releases its buffers -- it asserts nothing about the AS_PATH transform, and its destination peer is never an RS-client. Note the requirement is a SHOULD NOT, not a MUST (`rfc/short/rfc7947.md`), so the earlier framing as a "violation" overstated it. A daemon-behavior test, outside a tooling spec's scope and its review model | **DONE 2026-08-03.** The relay test landed as `TestRelayStoredRouteRSClientPreservesASPath` and its plain-eBGP twin (`internal/component/bgp/reactor/reactor_api_relay_test.go`), both setting and asserting `forwardFacts().rsClient` as a precondition, plus `test/plugin/bgp-rs-relay-aspath-transparency.ci`. `RFC7947-x-1` now carries `functional/verify` evidence in BOTH polarities. Landed by spec-rfcgate-2-deferred-rs-replay-evidence, closed in `15dac5bc4` | done |
| 2026-07-29 | spec-rfcgate-2-evidence (incidental finding) | **Ze sends no Cease / Administrative Shutdown NOTIFICATION on SIGTERM, and the code that would send one is unreachable.** `Session.Close()` builds it (`internal/component/bgp/reactor/session_connection.go`) and has NO reachable production caller: `runOnce`'s deferred `p.session = nil` (`peer_run.go`) runs before `p.cleanup()` tests `if p.session != nil` in the same goroutine in strict program order, and `closeConn` has already nil'd `s.conn`, so the second guard would skip the send anyway. A peer observes a bare TCP FIN | Found while implementing this spec (an agent observed zero NOTIFICATIONs on SIGTERM), then traced hop by hop against the producing code by a second agent. NOT a MUST violation and therefore NOT an owner-escalation case: RFC 4271 §8.2.2's action list is indicative with no RFC 2119 keyword (`rfc/full/rfc4271.txt:3940-3943`), and the only governing keyword is §6.7's **MAY** (`:1919-1923`); RFC 4486's two extracted requirements are both MAY. Separable from this spec's goal, which is evidence widening and is met without it, so this is a deferral rather than parking (`ai/rules/completion.md`: the goal still holds). It is filed rather than fixed here because the fix changes daemon shutdown ordering and needs its own race testing. The operational reason it still matters: a bare FIN is what a GR-capable peer reads as "neighbour restarted, retain routes as stale" rather than "withdraw", and Ze advertises GR -- UNVERIFIED against Ze's GR plugin or a real peer, and that is the destination spec's AC-7 | `spec-fixit-bgp-shutdown-cease-notification` (created 2026-07-29, CLOSED 2026-08-11 in commit e4be51e5a; carries the full twelve-hop trace, the MAY-level finding, and the separate note that no `RFC4271-8.2.2-*` requirement is extracted at all despite RFC 4271 being enrolled) | done 2026-08-23: re-read at the producer. `Reactor.Stop` calls `stop(true)`, which calls `notifyPeersShutdown`, which calls `Peer.ShutdownNotify`, which calls `Teardown(NotifyCeaseAdminShutdown)` (`reactor.go`, `peer.go`). `StopForRestart` takes the silent path deliberately, per the RFC 4724 Section 5 rationale written on it |
