# Deferrals: doc-claims-are-checked-not-just-resolved

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-09 | `plan/spec-problem-journal.md` | Every doc-freshness gate verifies reference integrity and none verifies claim truth: 82 of 1611 anchors named a symbol absent from the file they point at, all passing green | Found while moving 1155 `// Design:` pointers. The goal of that work did not depend on the gate being stronger, so it gets a spec rather than a fix folded into that commit (`ai/rules/completion.md`) | spec-doc-claims-are-checked-not-just-resolved | done: the symbol check and the marker-reason check are armed, and both run inside `make ze-verify` |
| 2026-08-09 | `plan/spec-problem-journal.md` | Full audit of whether the rule corpus keeps design documentation current, beyond the gates read while working | Owner asked for the sweep to run at the end of the work in hand, not during it | this spec's research phase | resolved: 14 findings, 2 folded into this spec (the gated home for the symbol check, the false "folded into ze-doc-test" sentence), 3 specs written for the rest |
| 2026-08-09 | spec-doc-claims-are-checked-not-just-resolved | Code can land with no design doc: 85 package directories under `internal/` carry no anchor, and the one check that looks reads one registry against one table | Found by the audit above. This spec's goal does not depend on it, so it gets a spec rather than a fix folded in (`ai/rules/completion.md`) | `plan/spec-code-can-land-with-no-design-doc.md` | open |
| 2026-08-09 | spec-doc-claims-are-checked-not-just-resolved | 451 citations of `plan/learned/<id>-*.md` over 172 distinct paths, none resolving, in files `check_markdown` does not walk | Found by phase 4, which repaired the 3 that unreasoned markers hid. The other 448 are outside that phase's reach and do not block this spec's goal | spec-dead-learned-citations-outside-the-walked-corpus | done: `26684a4bc` repaired all but 3 of the dead paths, this spec repaired those 3, and `check_tracked_citations` now sweeps every tracked file so the class cannot return |
