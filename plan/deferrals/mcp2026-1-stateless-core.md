# Deferrals: mcp2026-1-stateless-core

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/deferral-tracking.md`).

Owner directive 2026-07-29: work this spec authored as a deferral is in scope
for the goal and is implemented, not homed elsewhere. Every row this spec
authored is therefore closed with evidence rather than routed away.

The two rows under **Live** are the exceptions, and neither is work this spec
authored: one is a diagnosis this session RETRACTED after reading the producer,
and one is a defect in another subsystem discovered incidentally. Both name a
real destination spec and cite the producing function.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-29 | spec-mcp2026-1-stateless-core | Fix the MCP auth fail-open: `ExtractMCPConfig` returned `ok=false` unless the block was `enabled true` with a ported server, so `cmd/ze/hub/main.go` and `service_mcp.go` discarded `auth-mode`, `token`, `BearerList` and `OAuth`, and `NewStreamable` inferred `AuthNone`. An operator who configured bearer auth and started `ze --mcp <port>` got an unauthenticated listener | **IMPLEMENTED 2026-07-29, not deferred.** Root cause was one flag answering two unrelated questions. Split: `extractMCPBlock` parses the block and reports "exists" and "asks for a listener" separately; `ExtractMCPConfig` keeps its contract for its five existing callers; new `ExtractMCPSettings` returns auth/TLS whenever the block exists. `main.go` now takes addresses only from a listener-asking config but settings from any block, and the boot-path `Validate()` is gated on the listener actually binding so a dormant block cannot refuse boot. Evidence: four unit tests in `internal/component/config/loader_extract_test.go` and functional `test/plugin/mcp-cli-listener-honors-config-auth.ci`, all mutation-verified — restoring the `enabled` gate turns the unit tests red and the functional test returns `status=200` with the whole tool list leaking | implemented in this spec | done |
| 2026-07-29 | spec-mcp2026-1-stateless-core | ~~Fix `ze:task-support forbidden` being inert~~ | **WITHDRAWN 2026-07-29: the premise was false and I should have caught it before recording it.** The row claimed `groupTaskSupport` never reports `forbidden` because all four YANG declarations sit in mixed groups, and that a task-augmented `clear bgp rib in` is accepted and returns a taskId. Neither is true. `tools/list` carries `taskSupport: "forbidden"` on both `ze_clear_bgp` and `ze_request_bgp`, and a task-augmented call to `ze_clear_bgp` is refused: `status=200 code=-32602 message=tool ze_clear_bgp does not support task-augmented calls`. Advertisement and enforcement cannot diverge because both read the same `g.taskSupport` field — `buildToolDef` publishes it (`internal/component/mcp/tools.go:374`) and `lookupTaskSupport` returns it (`internal/component/mcp/streamable_tools.go:224`), both set by `groupTaskSupport`. **The failure was mine**: I recorded a subagent's claim as fact without reading the producing function, which is exactly what `ai/rules/no-fabrication.md` forbids. `test/plugin/task-forbidden.ci` now asserts BOTH directions of the gate and its stale "KNOWN GAP" comment is replaced with the evidence, so the claim cannot silently return | no defect exists | done |
| 2026-07-29 | spec-mcp2026-1-stateless-core | Implement the terminal-task cap: `defaultMaxTerminalTasks = 128` was assigned and never read, so `sweep()` reaped on TTL alone and nothing bounded retained terminal tasks — while spec decision D-1 cited that cap as load-bearing justification for deleting the session caps | **IMPLEMENTED 2026-07-29, not deferred.** `evictTerminalOverCap` evicts oldest-terminal-first, per principal, with the scoping reasoning in its godoc (a global cap would hand every caller a cross-principal eviction primitive; per-principal bounds the map at `principals x cap`, and the auth mode sets the multiplier). Non-terminal tasks are never evicted and `byIdentity` is kept consistent. Evidence: four tests in `internal/component/mcp/tasks_test.go`, all four mutation-verified red with eviction disabled. One of them initially passed under mutation — the index is trivially consistent when nothing is removed — and was strengthened to assert eviction ran first | implemented in this spec | done |

## Live

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-29 | spec-mcp2026-1-stateless-core | Emit the required `CacheableResult` fields `ttlMs` and `cacheScope` on `tools/list`, `resources/list`, `resources/read` and `server/discover`. Both are non-optional in the schema and changelog minor #5 requires them | In progress in this same session as part of the umbrella series, not routed away. `plan/spec-mcp2026-4-caching-apps.md` carries the completed design (two compile-time constants, 60 s for registry-derived surfaces and 1 h for embedded assets, plus an explicit `cacheScope` security decision). Recorded here so the obligation is visible until the code lands | implemented in this spec series (Phase 4) | done |
| 2026-07-29 | spec-mcp2026-1-stateless-core | Fix `contains=` assertions being silently truncated at the first colon, and re-arm the 203 `.ci` assertions across 15 suites that are currently weakened by it | Found by the Phase 4 agent when two of its own drafts passed for the wrong reason, then verified at the producer: `ParseKVPairs` (`internal/test/ci/ciformat.go:14-44`) preserves colon-bearing values for exactly four keys -- `json=`, `text=`, `hex=`, `pattern=` (`:21`) -- and everything else goes through `strings.SplitSeq(joined, ":")` (`:37`), so a `contains=` needle containing a colon is cut at that colon. `expect=stdout:contains=error: image path ... escapes appliance directory` actually asserts only `error`. A tree-wide sweep separates 203 PROSE-colon cases (silently weakened) from 33 DIRECTIVE-colon cases (`:timeout=5`, the documented engine-step form, correct as-is): parse 37, ui 35, plugin 22, ospf-wire 17, isis 17, isis-wire 10, ipsec 9, firewall 9, ospf 7, static 4, vrrp 2, policy 2, managed/install/appliance 1 each. **This is the "test that cannot fail" class**, so it is not deferred -- it is queued behind the spec series only because the fix belongs at the owning layer (split on `:` only when the next segment matches a `key=` token, which repairs all 203 at once without touching the 33), and landing a shared-parser change while phase agents are running tests would corrupt their evidence. Expect some of the 203 to turn RED when re-armed; each such test was passing vacuously and is a real finding | implemented in this session | done |
| 2026-07-29 | spec-mcp2026-1-stateless-core | Reproduce, then fix, one observed failure of `test/plugin/forward-overflow-two-tier.ci` | **DIAGNOSIS RETRACTED 2026-07-29 -- it was recorded before being verified, which is the same error this session made once already.** The row previously asserted that `option=timeout:value=20s` (`:86`) is a fixed wall-clock bound that blows under load, and proposed rewriting the runner's ordered-needle wait to be progress-bounded. Reading the producer first would have caught it: `runner_exec.go:111` passes every resolved per-test timeout through `withParallelHeadroom` (`internal/test/runner/runner_exec_util.go:189`), which multiplies by `ParallelTimeoutHeadroom = 3` (`internal/test/runner/parallel.go:29`) whenever concurrency > 1. So the effective budget is **60s** against a ~4.2s uncontended runtime, about 14x headroom, and that helper exists precisely for this class -- its godoc cites "fixed startup deadlines fail under CPU oversubscription, resolved 2026-07-25 by this helper". A failure at 60s is something stuck, not something slow, so "fixed duration bounding 50 sequential matches" does not explain it. **What actually happened is unestablished.** One failure was observed in a full suite run while other agents were concurrently rebuilding binaries; a run of mine under the same conditions produced `plugin bgp failed during startup at stage Config: mux conn read error: EOF`, which is a binary-swapped-underneath artifact and not a timing bug at all. The obligation that remains is to REPRODUCE it with `scripts/dev/stress-repro.py` on a quiet tree (explicit `ZE_BIN`/`ZE_TEST_BIN`, `--any-failure`) and fix whatever it actually is -- not to rewrite shared test infrastructure used by every `.ci` on the strength of an unverified story. Still not a known-failure shard (`ai/rules/fix-dont-record.md`) | `plan/spec-fixit-migrate-sleeps-infra.md` | deferred |
| 2026-07-30 | spec-mcp2026-1-stateless-core | Fix the route-server establishment race behind an intermittent `test/plugin/bgp-rs-community-strip-multi-fastpath` failure: `update-route failed ... "no established peers to send to"`, produced by `AnnounceEOR` (`internal/component/bgp/reactor/reactor_api_forward.go:147-149`) when `sentCount == 0` because no peer had established yet at EOR-announce time | Observed once in three full `make ze-plugin-test` runs; passes in isolation. **Not a known-failure shard** (`ai/rules/fix-dont-record.md`). Distinct from the `wait_rs_replayed` fixed-deadline defect this session DID fix -- that one was the observer's flat 30 s wall-clock bound and is now progress-bounded; this one fails earlier, at route push, and the observer timeout is a consequence rather than the cause. Nothing in this series touches `internal/component/bgp/`, and the destination spec is explicitly the P0 establishment investigation (Status `in-progress`, Phase 4/6) that owns this exact shape; another session also has staged work in `internal/component/bgp/reactor/session*.go`, so fixing it here would collide | `plan/spec-fixit-redistribute-establishment-stall.md` | deferred |
| 2026-07-30 | spec-mcp2026-1-stateless-core | Bring the MCP documentation prose into ASD-STE100 compliance: 537 findings across `docs/architecture/mcp/overview.md` (run-ons 11 -> 60), `docs/functional-tests.md` (152 -> 171), `ai/digests/mcp.md` (34 -> 77), `docs/architecture/api/commands.md` (37 -> 52), `docs/features/mcp-integration.md`, `docs/comparison.md`, `docs/features.md`, `docs/architecture/testing/ci-format.md`, `cmd/ze/hub/main.go` and the five `plan/learned/129x-130x` summaries | Owner granted a bypass for the closing commit, 2026-07-30. The gate is real but **not yet part of the project**: `ai/rules/simplified-technical-english.md` and `scripts/dev/ste_check.py` are both untracked, added by the concurrent RFC session, and the enforcement in `scripts/dev/commit_helper.py:1469` is that session's uncommitted edit. Rewriting 537 findings against a rule its own author may still be changing risks doing the work twice. Tree-wide the gate is already red on ~72 files this session never touched, so the MCP prose is not an outlier. Do it once the rule and checker are committed and stable. Note the honest trade-off recorded at decision time: STE's 25-word sentence cap works against the explanatory density that makes a learned summary worth reading later, so `plan/learned/` may warrant an exemption rather than a rewrite -- that is a question for the rule's author | `plan/spec-mcp2026-4-caching-apps.md` (the last spec of the series to close; retarget if the STE rule lands first with its own backfill spec) | deferred |
