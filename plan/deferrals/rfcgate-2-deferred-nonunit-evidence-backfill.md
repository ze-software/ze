# Deferrals: spec-rfcgate-2-deferred-nonunit-evidence-backfill

Deferred work from `plan/spec-rfcgate-2-deferred-nonunit-evidence-backfill.md`.

The source spec's Task section is explicit that the corpus is drained in
batches, so a tranche landing is not a claim that the backlog is finished. These
rows are the remainder. Every live row was re-homed on 2026-08-03: each named
its own source spec as its Destination, which is not a home at all, because
closure deletes that spec and the row would dangle.

The source spec is still OPEN. Its closure was attempted on 2026-08-03 and
stopped at the Review Gate: the independent review found that
`test/l2tp/rfc2661-emitted-control-shape.ci` is unfenced against the daemon
(last row below), the file is RFC-tagged, and only Thomas can approve an edit to
it. When that spec does close, repoint the two rows below that cite it.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-02 | spec-rfcgate-2-deferred-nonunit-evidence-backfill | Backfill the IKE and EAP unit-only cluster (254 gated MUSTs: rfc7296 211, rfc3748 12, rfc5216 10, rfc2759 9, rfc4301 8, rfc4303 3, rfc7427 1) at `functional/verify` via the `ipsec` suite | Rank 1 by measured defect density: five defects shipped 2026-08-01, all unit-green for months. NOT taken in the source spec because a concurrent session was reviewing `internal/component/ike/**`, and editing under a live review costs more than it buys. Re-homed 2026-08-03 | `plan/spec-rfc-evidence-deferred-ike-eap-tranche.md` | deferred |
| 2026-08-02 | spec-rfcgate-2-deferred-nonunit-evidence-backfill | Backfill the IS-IS (52), RSVP-TE (25) and LDP (7) unit-only requirements; runnable suites already exist (`isis`, `isis-wire`, `rsvpte`, `ldp`) | Rank 4, and unexamined. Apply the selection rule's test 1 (oracle independence) as a scan across each RFC before picking, rather than reasoning from protocol family. Re-homed 2026-08-03 | `plan/spec-rfc-evidence-deferred-isis-rsvpte-ldp-tranche.md` | deferred |
| 2026-08-02 | spec-rfcgate-2-deferred-nonunit-evidence-backfill | Decide what to do about 242 gated MUSTs whose subsystem no `ze-functional-test` suite boots (BFD 98, VRRP 80, dhcpserver 28, geodns 18, dnsserver 18) | Blocked on an owner decision, raised as Q2 in the source spec: add suites, accept nightly-only tier, or leave unit-only by decision. `carrier_for` resolves any `.ci` there to `TIER_UNRUN` and the scanner refuses the tag, so no test can fix this alone. Re-homed 2026-08-03 | `plan/spec-rfc-evidence-deferred-unbootable-suite-musts.md` | deferred |
| 2026-08-02 | spec-rfcgate-2-deferred-nonunit-evidence-backfill | Fix RFC2661-24.10-1: ze silently drops an SCCRQ whose Assigned Tunnel ID is 0 rather than replying with StopCCN | Blocked on an owner decision, raised as Q1 in the source spec. The RFC permits the reply (section 4.4.3) but the current silent drop avoids answering an unauthenticated datagram, which is a reflection-amplification consideration. Which way to fix is Thomas's call. Re-homed 2026-08-03 to the spec that already carries the full trace and the question. The 2026-08-02 reproducer under `test/draft/l2tp/` is GONE (that tree is gitignored and was session-local); the destination spec records that it must be written, and names the committed file whose shape to copy. RESOLVED 2026-08-12: Thomas answered Route C on 2026-08-10 (reply, under a rate bound), the destination spec implemented and closed it, and the reproducer was written as `test/l2tp/rfc2661-sccrq-tunnel-id-zero.ci` | `spec-fixit-l2tp-sccrq-tunnel-id-zero`, closed. `answerZeroTunnelIDSCCRQ` and `sendUnassociatedStopCCN` (`internal/component/l2tp/reactor.go`) | done |
| 2026-08-02 | spec-rfcgate-2-deferred-nonunit-evidence-backfill | Regenerate `ai/RFC-REQUIREMENTS.md` (`make ze-rfc-index`) so the three new `functional/verify` bindings appear in the ledger | LANDED. Verified 2026-08-03 at `HEAD:ai/RFC-REQUIREMENTS.md`: the rows for `RFC2661-4.1-1`, `RFC2661-4.1-2` and `RFC2661-x-1` each carry `test/l2tp/rfc2661-emitted-control-shape.ci` as `(functional/verify)`. `git log -S` puts the regen in `e585c963a`, the same commit that landed the test, so the row was stale rather than outstanding | `e585c963a` | done |
| 2026-08-02 | spec-rfcgate-2-deferred-nonunit-evidence-backfill | Establish what ze does with a received AVP whose reserved bits 2-5 are set (RFC2661-4.1-1 receive side) | A probe produced neither a ze log line nor a reply, and the cause was not isolated. Not claimed as a finding in either direction. Needs a deliberate reproduction before it can be called conformant or not. Re-homed 2026-08-03 to the L2TP residual-coverage spec, which now carries it as a work item | `plan/spec-finish-l2tp.md` | deferred |
| 2026-08-03 | spec-rfcgate-2-deferred-nonunit-evidence-backfill (closure review) | Fence `test/l2tp/rfc2661-emitted-control-shape.ci` on the daemon's acceptance line: the peer script sends the SCCCN and exits without reading a reply, so the runner can reach teardown before ze verifies the digest and writes `tunnel now established` | Found by the closure review. It fails CLOSED (a missing line reddens the test), so it is a flake risk under load rather than a vacuity, and the evidence it publishes is real. BLOCKED on Thomas: the file is RFC-tagged, so `.claude/hooks/pretool-writeedit.py` refuses any edit without his recorded approval, and self-approval is banned. The obvious fix (`await=stderr:contains=tunnel now established`) also needs design: `runner_exec.go` takes the `awaitStderrSW != nil` switch arm BEFORE the exit-code arm, so adding the fence changes which process the runner waits on | `plan/spec-finish-l2tp.md` | deferred |
