# Deferrals: ad-hoc-2026-07-13-09cefa

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/deferral-tracking.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-13 | ad-hoc (fixed-buffer-overflow class, found while fixing the MRT record overflow) | `internal/component/l2tp/ppp/ipv6_service.go:145` (also :181/:213/:238/:250): `var buf [512]byte; n := BuildDHCPv6Reply(buf[:], …)` then `buf[:n]` — a fixed 512-byte stack buffer with no `n > cap` guard. Today only small DHCPv6-PD replies flow; latent slice-bounds panic if a reply ever exceeds 512 bytes | Low severity (not reachable with today's payloads). Same class fix as the IKE row. RESOLVED 2026-07-21: added `CheckedBuildDHCPv6Reply`/`CheckedBuildDHCPv6StatusReply` (length helpers + capacity check); all 5 `ipv6_service.go` call sites use them and drop with an error (routed to the DHCPv6 server loop's existing Warn-and-skip) instead of panicking or truncating | `plan/learned/1235-fixit-fixed-buffer-overflow.md` | done |

