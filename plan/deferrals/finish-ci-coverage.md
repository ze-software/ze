# Deferrals: finish-ci-coverage

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-16 | plan/learned/HOOK-FRICTION.md F2 | The spec validator rejects the citation form `ai/rules/evidence.md` MANDATES: its Current Behavior regex needs the backticked path to END in `go`/`py`/`rs`/`ts`/`js`, so a trailing line number defeats the match, and `.sh` and the retired `Makefile` (current producers: `internal/le/` native action tables) are absent from the set entirely. It also reads only the first 30 lines of the section, so long specs carry a permanent spurious warning that trains readers to ignore validator output. The rule and its gate demand opposite things, and the gate wins | Filed with evidence, not fixed: the regex must accept the mandated form WITHOUT accepting prose, which needs a must-not-fire test, and the filing task already carried three other hook changes. The check itself is correct and must not be weakened | `spec-fixit-agent-tooling-misleads` (T-1, closed) | resolved |
| 2026-07-16 | plan/learned/HOOK-FRICTION.md F3 | `.claude/rules/session-start.md` makes an LSP ToolSearch the unconditional first action, with a table of "banned excuses" for skipping it, but LSP is genuinely absent for subagents: a filing agent's own first action returned "No matching deferred tools found". The gate lifts on the QUERY TEXT rather than on a successful load, so an agent passes the gate while having no LSP. The rule is not blocking anyone; it is lying to them | Filed, not fixed: the decision is whether the rule gains a subagent carve-out or the gate learns to tell a load from a query, which is a rule change needing Thomas. LSP's absence may be a harness property that changes without notice, so a fix must not hard-code today's behaviour | `spec-fixit-agent-tooling-misleads` (T-2, closed) | resolved |
| 2026-07-16 | plan/learned/HOOK-FRICTION.md F4 | The commit gate's structural-gate refusal tells the reader to re-run the changed-lint target to refresh its failure record. That target does not write the file; only a full verify run does. Following the instruction leaves the agent stuck with no signal and no next step. Same class as `doctor-vpp-lcp-netns` recommending a config that breaks LCP, fixed in `287aa411e` | Filed, not fixed: the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`) was explicitly out of scope for the filing task and had already been changed twice today by other agents. The wider question is that `ai/rules/cli.md` requires an error to say what to do next but does not yet require the advice to be TRUE | `spec-fixit-agent-tooling-misleads` (T-3, closed) | resolved |
| 2026-08-07 | spec-finish-ci-coverage (`test/pppoe/` orphan repair; same class as the PPPoE `ExtractParameters` defect fixed there) | SECURITY: a ONE-prefix anomaly allowlist parses empty, so the self-lockout guard is absent. `ParseConfig` (`internal/plugins/anomaly/shape/config.go`) reads `m["allowlist"].([]any)`, and the node is `leaf-list allowlist` (`internal/plugins/anomaly/shape/yang/ze-anomaly-shape-conf.yang`). `Tree.ToMap` (`internal/component/config/tree.go`) collapses a single-member leaf-list to a bare string, so one prefix arrives as a JSON string, the assertion fails, and `Config.Allowlist` stays empty. `handleEvent` (`internal/plugins/anomaly/shape/responder.go`) then never reaches its `allowlisted(entity, r.cfg.Allowlist)` skip, so an armed responder can install a limit or drop against the very source the operator protected, including its own management source. Two or more prefixes parse correctly, so the defect appears only at the smallest and most likely allowlist | Deferred, not parked: the pppoe repair does not depend on it (`ai/rules/rule-precedence.md` -- a defect the goal does not need is fixed AFTER closing, so the closing commit keeps its single focus and its gates). Homed rather than fixed inline because the fix is a shared reader for both leaf-list shapes, not a per-caller patch, and each caller needs a discriminating one-member test | `plan/spec-review-typed-config-decode.md` (its Task is exactly this: type information created at the YANG edge is destroyed in the middle and re-derived by hand at each consumer) | deferred |
| 2026-08-07 | spec-finish-ci-coverage (`test/pppoe/` orphan repair; same class as the PPPoE `ExtractParameters` defect fixed there) | Four more readers assert a config shape `Tree.ToMap` does not emit. (1) `parseFullPoolConfig` (`internal/component/l2tp/plugins/pool/register.go`) reads `named-pool` and `named-ipv6-pool` as `[]any`; both are `list ... key "name"` (`internal/component/l2tp/plugins/pool/yang/ze-l2tp-pool-conf.yang`) and `ToMap` emits a keyed list as a map of key to entry, so `parseNamedPools` and `parseNamedIPv6Pools` are unreachable and every named pool is silently dropped. The consequence is a REFUSAL, not a fallback: with `namedPools` nil, `handleIPRequest` (same file) answers a RADIUS `Framed-Pool` with `Accept: false`, `named pool <x> not configured`, so every subscriber whose profile names a pool is denied an address. (2) `parseRouterInformation` (`internal/plugins/ospf/config.go`) reads `scope`, `leaf-list scope` (`ze-ospf-conf.yang`). (3) `parseIfaceConfig` (`internal/component/iface/config.go`) reads bridge `member`, `leaf-list member` (`ze-iface-conf.yang`). (4) `parseFlowtable` (`internal/component/firewall/config.go`) reads `device`, `leaf-list device` (`ze-firewall-conf.yang`). All four take JSON, so 2 to 3 survive their multi-member case and lose only the single-member one; the taker must re-derive per reader whether it is fed JSON or `ToMap` directly, because a reader fed `ToMap` gets `[]string` and loses the multi-member case as well | Same as the row above: the pppoe repair does not depend on any of them, and folding four unrelated component fixes into its closing commit would cost that commit its single focus and restart its gates (`ai/rules/rule-precedence.md`). Each needs its own one-member functional test, because the surviving unit tests hand-build the slice shape no producer emits -- which is exactly how the PPPoE and pool defects both survived | `plan/spec-review-typed-config-decode.md` | deferred |
| 2026-08-07 | spec-finish-ci-coverage (`test/pppoe/` orphan repair, review finding) | the retired `ze-qemu-pppoe-test` (current: `./le qemu pppoe-test`) is named by no aggregate target and no workflow, so only a hand-typed command ever runs the repaired suite. The three `.ci` declare `needs-linux`, so `.github/workflows/qemu-nightly.yml` DISCOVERS them, but `ze-qemu-needs-linux-test` sets no `ZE_TEST_NETNS`, so `applyNetnsLinkGate` (`internal/test/runner/caps.go`) skips all three and the nightly reports skips rather than results. This is the orphan condition in a weaker form: the suite is reachable, and nothing reaches for it. The same is true today of every sibling lab (`ze-qemu-pppoe-accel-test`, `ze-qemu-netns-test`, `ze-qemu-vrrp-keepalived-test`), so the fix is one CI decision for the class, not a row per lab | Deferred, not parked: the repair itself is complete and proven (three tests pass in QEMU and each was shown to discriminate), and the fix is CI wiring in `.github/workflows/qemu-nightly.yml` plus the workflow-set assertions in the retired `scripts/dev/github_workflows_test.go` (current producer: `internal/le/`), both outside this phase's file scope. Whether the netns labs get a scheduled home, and whether it blocks or reports, is an owner decision about runner cost (`TestCapabilityGatedTestsHaveAQemuHome` already encodes the principle that a gated test needs a CI home) | `plan/future/spec-finish-appliance-qemu-evidence.md` | resolved 2026-08-12 in `dee3b9aae`, as one CI decision for the class rather than a row per lab. `.github/workflows/qemu-nightly.yml` gains jobs `protocol-labs` and `runtime-kernel-labs`; `ze-qemu-pppoe-test` runs in the second, and the target itself sets `ZE_NETNS_QEMU_SUITES=pppoe`, so the three `.ci` execute rather than skip. `TestQemuAndInteropTargetsHaveACaller` (the retired `scripts/dev/github_workflows_test.go` (current producer: `internal/le/`)) refuses a repeat. `ze-qemu-netns-test` sits outside that guard's derived class (it is `-qemu-test`, not `ze-qemu-*-test`) and still has no caller |
| 2026-08-03 | spec-finish-ci-coverage (op-1 Tier-1 phase) | `tmpfs=<path>:mode=<octal>` is parsed, validated and then DISCARDED. `parsingRunner.setupWorkDir` (`internal/test/runner/parsing.go`) writes every tmpfs file `0o644`; the orchestrated path re-derives the mode from the file extension in `Tmpfs.AddFile` (`internal/test/tmpfs/tmpfs.go`). Both read `map[string][]byte`, so the mode is already gone. A fixture that must carry a specific name (`wg`, for a PATH lookup) can never be executable. Second limit found with it: a parse-suite helper script cannot invoke `ze`, because `runOneCommand` builds the child environment with `childEnv` and never adds `Runner.childPathEnv` | The goal held without it: the success path moved to `TestRunWgKeypair_PipesGenkeyIntoPubkey` (`internal/plugins/diag/diag_test.go`), which shims `wg` on PATH and is fully discriminating, and the `.ci` still drives the command end to end through the real ze binary. Fixing it means threading a mode map through `Record`, `parsingTest` and both writers, which is a runner-wide change that would take this commit's single focus and restart its gates (`ai/rules/rule-precedence.md`, closing comes first) | SPLIT 2026-08-15 at the closure of `spec-fixit-ci-peer-block-silent-directives`. **Half one is HALF done**: `parsingRunner.setupWorkDir` (`internal/test/runner/parsing.go`) now materialises through `test.Tmpfs.WriteTo(workDir)`, so a declared `mode=` reaches disk in the PARSE suite; landed in `dc591ec72`, not by that spec. The orchestrated `.ci` path still flattens the block to `Record.TmpfsFiles map[string][]byte` and re-derives the mode from the extension, so `mode=` is still dropped there; that half is recorded in `plan/journal/helper-bypassed-by-an-open-coded-copy.md` (row of 2026-08-14) with the 8 call sites it touches. **Half two is HOMED as of 2026-08-16**: `parsing.go` still builds the child environment with `childEnv(test.EnvVars...)` and never adds a PATH entry for `filepath.Dir(r.zePath)`, so a parse-suite helper script still gets `ze: not found`. The closing spec named it in its Task and gave it no AC, so it was neither implemented nor covered. It now has its own spec, `spec-fixit-parse-suite-helper-cannot-invoke-ze`, which carries the acceptance criteria this row never had and asks the design question the row was waiting on: whether the orchestrated runner's shim-dir producer can be reused, or whether the two runners' environment builders must be merged so there is one answer to "where is ze" | homed (half two) |

