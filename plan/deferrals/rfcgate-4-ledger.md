# Deferrals: rfcgate-4-ledger

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/deferral-tracking.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-29 | spec-rfcgate-4-ledger (OR-3) | **Write the 32 missing `docs/features/rfc-status.md` rows.** 166 RFCs are enrolled and 157 rows exist, of which 23 key non-enrolled stems, leaving 32 enrolled entries with no public row at all (verified by driving `load_enrolled` and `parse_status_ledger`; all 32 are of the form `rfcNNNN`, no drafts, no `sflow-v5`). Several are exactly what the page's own preamble says it exists for -- a deliberate non-implementation a user would look up, such as `rfc7611` ACCEPT_OWN recognised for display only and `rfc7440` TFTP windowsize whose option name is parsed and value discarded | Two reasons, in order. Writing them is 32 separate product judgements about Status plus source-anchored prose, which is authoring rather than machinery, and this spec ships machinery. More importantly the judgement that their absence is currently SAFE rests entirely on none of the 32 carrying a `{gap}` annotation -- and `ai/rules/rfc-compliance.md:53` voided every `{gap}`/`{not-applicable}` as authority on 2026-07-27, so writing the rows before that re-derivation would bake a void basis into the public page. The gate this spec does ship keeps them under pressure without reddening the build: row presence is a git-HEAD ratchet (a newly enrolled RFC must bring a row, and an existing row may not vanish while its RFC stays enrolled), and the backlog is DERIVED as `enrolled - rows` and rendered into `ai/RFC-REQUIREMENTS.md` rather than kept as a second hand-maintained list (`ai/rules/derive-not-hardcode.md`). Distinct from the four unproven-support stems (rfc1035, rfc3765, rfc4486, rfc5301), which are NOT deferred: those are extracted and enrolled by this spec's phase 6, because a public support claim over a zero-requirement checklist is a different severity from a missing row over an annotated one | `plan/spec-followup-rfc-enrollment.md` (owns the annotation re-derivation this work sequences behind; the rows are written once their basis is re-derived, never before) | deferred |
