# Deferrals: rfcgate-4-ledger

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/deferral-tracking.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-29 | spec-rfcgate-4-ledger (OR-3) | **Write the 32 missing `docs/features/rfc-status.md` rows.** 166 RFCs are enrolled and 157 rows exist, of which 23 key non-enrolled stems, leaving 32 enrolled entries with no public row at all (verified by driving `load_enrolled` and `parse_status_ledger`; all 32 are of the form `rfcNNNN`, no drafts, no `sflow-v5`). Several are exactly what the page's own preamble says it exists for -- a deliberate non-implementation a user would look up, such as `rfc7611` ACCEPT_OWN recognised for display only and `rfc7440` TFTP windowsize whose option name is parsed and value discarded | Two reasons, in order. Writing them is 32 separate product judgements about Status plus source-anchored prose, which is authoring rather than machinery, and this spec ships machinery. More importantly the judgement that their absence is currently SAFE rests entirely on none of the 32 carrying a `{gap}` annotation -- and `ai/rules/rfc-compliance.md:56` voided every `{gap}`/`{not-applicable}` as authority on 2026-07-27, so writing the rows before that re-derivation would bake a void basis into the public page. The gate this spec does ship keeps them under pressure without reddening the build: row presence is a git-HEAD ratchet (a newly enrolled RFC must bring a row, and an existing row may not vanish while its RFC stays enrolled), and the backlog is DERIVED as `enrolled - rows` and rendered into `ai/RFC-REQUIREMENTS.md` rather than kept as a second hand-maintained list (`ai/rules/derive-not-hardcode.md`). Distinct from the four unproven-support stems (rfc1035, rfc3765, rfc4486, rfc5301), which are NOT deferred: those are extracted and enrolled by this spec's phase 6, because a public support claim over a zero-requirement checklist is a different severity from a missing row over an annotated one | `plan/spec-followup-rfc-enrollment.md` (owns the annotation re-derivation this work sequences behind; the rows are written once their basis is re-derived, never before) | deferred |
| 2026-07-30 | spec-rfcgate-4-ledger (OR-1, OR-1a, OR-1b) | **Enrol `rfc1035`.** Its extraction landed. The summary now declares 27 MUST-level obligations, read from the document's pre-RFC-2119 indicative prose. The full 73-section walk is signed off in `rfc/extraction/rfc1035.json`. What remains is proving those obligations, and the walk showed that cannot finish without owner rulings. Five have no code path in Ze at all. The 512-octet UDP bound is never enforced and the TC bit is never set: `internal/core/dnsserver/handler.go:62` writes whatever the answer function produced. A response TTL is never raised to the zone SOA MINIMUM (`internal/plugins/geodns/server.go:106`). An unsupported inverse query draws no Not Implemented reply. Ze performs no zone transfer of any kind. About six more admit only a POSITIVE polarity, because `github.com/miekg/dns` owns the wire codec and no change to Ze can break them. | The extraction half is done, so this is not extraction work sequenced behind anything. It is a set of conformance decisions, plus real DNS protocol work if the owner chooses to fix rather than annotate. `ai/rules/rfc-compliance.md` reserves both to Thomas. Writing `{gap}`, `{not-applicable}` or `{single-polarity}` on an implementer's own authority is banned, and OR-1a pre-authorised none of them. OR-1b named this scoping conversation as PART of phase 6 rather than a surprise inside it. A-10's "unknowable tail" is now measured instead of guessed. Meanwhile `rfc/not-enrolled.txt` declares it `backlog`, which renders as DEBT, asserts nothing about conformance and silences nothing (OC-4). `docs/features/rfc-status.md` discloses the unmet obligations in its Remaining cell. The spec must not close while this row is live. | `plan/spec-fixit-dns-rfc1035-conformance.md` (**re-homed 2026-07-30 at closure.** The old Destination was `plan/spec-rfcgate-0-umbrella.md`, which closes in the same change set as this child, so the row would have dangled. Owner Ruling OR-C names this spec as the home, and its own acceptance criteria include the enrolment) | deferred |
| 2026-07-30 | spec-rfcgate-4-ledger (OR-1, OR-1a) | **Enrol `rfc5301`.** Its extraction landed too. Section 3's indicative prose yields 7 MUST-level obligations, and the walk is signed off in `rfc/extraction/rfc5301.json`. Three are unmet in the code. The emitted TLV 137 value is not restricted to 7-bit ASCII. `internal/plugins/isis/lsdb/encode.go:60` writes a bare `[]byte(name)`. The YANG leaf at `internal/plugins/isis/yang/ze-isis-conf.yang:69` carries only `length "1..255"`, with no `pattern`. So a UTF-8 hostname goes on the wire as 8-bit octets. The value is not validated as a domain name per RFC 2181. And no ToASCII or ToUnicode handling exists anywhere on the IS-IS path. | Same shape as the rfc1035 row, and the same rule. The first two obligations are one YANG `pattern` away. But that narrows a config surface operators can already be using, so it is a decision rather than a cleanup. The third turns on whether an interface that REFUSES Unicode satisfies a requirement whose antecedent is an interface that PERMITS it. Either reading is honest. `ai/rules/rfc-compliance.md` ("Implement Full Compliance. Ask Thomas Only Before Doing LESS") makes the narrower answer his to give. Declared `backlog` in `rfc/not-enrolled.txt` meanwhile, and its Remaining cell on the public page discloses all three. | `plan/spec-fixit-isis-hostname-ascii.md` (**re-homed 2026-07-30 at closure**, same reason as the row above. Owner Ruling OR-B names this spec as the home, and it owns the YANG pattern plus its RFC 2181 correction) | deferred |
