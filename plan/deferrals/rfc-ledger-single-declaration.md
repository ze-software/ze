# Deferrals -- rfc-ledger-single-declaration

Work this spec touched and deliberately did not do. Every row names where it goes
rather than only that it was left, because a deferral with no destination is an
absence with a table around it.

Nothing here is a conformance decision. The two scope rulings this spec carried
to the owner were answered on 2026-09-01 and are recorded where they bind, in
`rfc/short/rfc6514.md` and `rfc/short/rfc8362.md`, not here.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-09-01 | rfc-ledger-single-declaration | Repoint the published prose in `internal/le/site/rfccompliance.go`, `internal/le/site/rfcdetail.go`, `internal/le/site/rfcledger.go` (whose field-source comments name the two retired files while `rfcLedgerStemOf` in the same file says they are generated) and `internal/le/site/facts.go` (whose `factsFromRFCLedger` publishes `internal/le/rfc.Collect, over rfc/short/*.md and rfc/enrolled.txt` into the site facts, and `Collect` no longer reads that file), which names `rfc/enrolled.txt`, `rfc/not-enrolled.txt` and `docs/features/rfc-status.md` as AUTHORED inputs, plus the golden at `internal/le/site/testdata/published-rfc-compliance-body.html`. One sentence in `rfcComplianceBody` also tells a reader that `checkStatusCompleteness` refuses a tree with a missing public row, and that check no longer exists. | AC-7 of this spec, and it is NOT dropped: the site session owns every one of those files and was editing them while this work ran, so a second author touching them would have collided. The replacement wording was handed to that session over the message bus on 2026-09-01, including what the three files became and where a reader now looks for the authored fact. | the site session's own commit | done: verified at closure on 2026-09-02. `rfcLedgerStemOf` (`internal/le/site/rfcledger.go`) says the three files are now GENERATED from the Meta tables, `factsFromRFCLedger` (`internal/le/site/facts.go`) publishes `internal/le/rfc.Collect, over rfc/short/*.md` with a comment naming the removed input, and `rfcNoPublicRowWhy` (`internal/le/site/rfcdetail.go`) names the summary as the author. No rendered string in the package names any of the three as an authored input. The golden needed no edit: `internal/le/site/testdata/published-rfc-compliance-body.html` is a frozen capture of the RETIRED published page, and the sections its comparison still reads exclude the gate-inputs table |
| 2026-09-01 | rfc-ledger-single-declaration | Give `checkGapCountAgreement` a reader for a gap count written in DIGITS, or for one separated from MUST by intervening words. | Out of scope and unchanged by this spec: the limit predates it, the generated page states it in its own header, and widening the reader changes which rows the check judges, which is a ledger decision rather than a migration one. This spec moved where the Remaining cell is authored and did not touch how it is read. **MEASURED 2026-09-03, and widening the regex is a TRAP: `gapCountRE` must not simply gain `[0-9]+`.** The population is real: six `Support remaining` cells write the count in digits and the check skips every one of them, including the two largest claims on the ledger, rfc9012's "51 MUST-level gaps annotated" and rfc9830's "20". But the cells are PROSE, and three of the six would go falsely red under a naive widening. rfc6514 reads "declares all 133 MUST-level obligations: 132 have no producer in Ze", so the pattern takes 133 as the gap claim; another cell reads "All 5 MUST-level requirements implemented and extracted", where 5 is the opposite of a gap. The spelled-number restriction is not an oversight, it is a smaller surface for the same hazard. The real answer is the one `ai/rules/principles.md` names: a count parsed out of English cannot be checked against a checklist, and the number should be DERIVED from the `{gap}` annotations and rendered into the cell rather than written by hand beside them. That is a ledger design change on a gated surface, and it is the owner's to authorise. | a spec of its own | deferred |
| 2026-09-01 | rfc-ledger-single-declaration | Write a public row for the 32 enrolled RFCs that have none, or decide they should not have one. | Named as a Known Limitation of this spec rather than an omission. They were grandfathered before the move and are grandfathered after it: `Support` set to `-` is now their explicit declaration, so the state is a decision a reader can see rather than an absence. Deciding whether each deserves a row is editorial work on the public page, not ledger plumbing. | a spec of its own | deferred |
| 2026-09-01 | rfc-ledger-single-declaration | Reconcile `TestSupportedRowsHaveDerivableScope`, which asserts 52 support-promising rows against a corpus holding 50. | Not this spec's number to move. It was already red at HEAD before this work began, at 51, and fell to 50 when another session lowered `rfc/short/rfc3748.md` from `Supported in IPsec` to `Partial in IPsec`. The test's own comment reserves that delta to the spec that owns it, and absorbing it here would hide two other sessions' changes inside a migration commit. **CLOSED 2026-09-03 after verifying the producer rather than the row: the test was reconciled.** `check_test.go` now asserts 44 support-promising rows (35 exact, 9 scope-qualified, 0 'Yes') with its rationale inline, and it passes.** | `plan/spec-rfcgate-6-supported-extraction-signoff.md` | done |
