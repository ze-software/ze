# Ad-hoc deferrals -- 2026-07-27 (session 423eaa77)

Defects found while closing every document in `plan/handover/`. None of them was a
handover item: each surfaced underneath one, was verified at the producing function,
and is separable from the goal the surrounding work existed to achieve
(`ai/rules/no-parking.md` -- "the goal still works; this is a distinct, larger,
separable feature"). Each names a spec that exists on disk.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-27 | ad-hoc (handover 19, web editor list-entry delete) | Under zefs blob storage `/config/diff` under-reports pending structural ops: after a list-entry delete it renders `Review changes (1)` with an empty body, where filesystem storage yields `- bgp peer london` and a count of 2. `Editor.listChangeFiles` (`internal/component/cli/editor.go:919-937`) calls `store.List(filepath.Dir(originalPath))`, which for blob storage resolves through `blobStorage.List` -> `ReadDir(resolveKey(prefix))` (`internal/component/config/storage/blob.go:186-204`). Leaf edits are unaffected because they are also in the in-memory `e.meta`; structural ops live only in the change file. `/config/rename/` is affected identically. | The delete itself is correct -- tree, peer list and commit all land -- so the handover-19 goal is met. This is a separate review-surface defect in the storage/editor layer with its own test surface, and it predates the delete fix. | plan/spec-fixit-zefs-diff-structural-ops.md | deferred |
| 2026-07-27 | ad-hoc (handover 22, OTC role-resolution holes) | A peer created from a dynamic group receives no per-peer plugin config at all: it establishes with a real address but has no `peer` list entry, so `configjson.ForEachPeer` (`internal/component/config/configjson/traverse.go:37-39`, `:57-59`) never visits it. For `bgp-role` that means no Role capability advertised and no RFC 9234 OTC gate; `bgp-rpki` has the identical limitation. | Distinct from the keying bug that WAS fixed (config keyed by name while readers key by address). Here the config never reaches the delivery layer, so there is no key to get wrong. A real fix changes the shape of delivered config, not its contents, and must be fixed at the delivery layer rather than per-plugin. | plan/spec-fixit-dynamic-group-peer-config.md | deferred |
| 2026-07-27 | ad-hoc (ze-ci-dispatch-check, built while sweeping stale command call sites) | `validateCommandArgs` Phase 2 (`internal/component/plugin/server/command.go:716-750`) positionally matches only `ArgEnum`/`ArgUnion`/`ArgString`, so a mandatory non-string leaf can never be filled positionally; and where a command's noun is the terminal key token, `matchCommandTokens` extracts no selector and the `RequiresSelector` guard (`command.go:570`) rejects it. Verified live: `show tcp-check 127.0.0.1 1 timeout 2s` -> `required argument missing: port`; `delete bgp peer 127.0.0.1` -> `requires a selector`; `show interface rate <name>` -> usage text. | Both fixes alter matching for all 410 registered commands, so this needs its own spec, a mutation-verified test per shape and a full functional pass. A narrower fix special-casing these three would leave the next typed leaf broken identically. `test/plugin/api-peer-remove.ci` is red on this root cause; three other `.ci` tests currently assert only that the command resolves, with the defect documented inline. | plan/spec-fixit-positional-arg-matching.md | deferred |
| 2026-07-27 | ad-hoc (rfc6286 review NIT-1 follow-through) | `parsePeersFromTree` (`internal/component/bgp/reactor/reactor_api.go:701-782`) reads a flat peer shape the YANG no longer produces -- it expects `remote`/`local`/`receive-hold-time`/`router-id` directly under the peer, while `grouping peer-fields` (`internal/component/bgp/yang/ze-bgp-conf.yang:290-805`) nests them under `connection`/`session`/`timer`. On a YANG tree it dies at `missing required remote container` (`reactor_api.go:703`). Reachable via `ze -` (stdin), because `internal/component/bgp/config/loader.go:93-96` guards both `SetConfigPath` and `SetReloadFunc` on `configPath != "" && configPath != "-"`. | Realigning the shape rewrites a function whose entire existing test surface encodes the flat form. Doing that silently under a green bar is the test-rewrite-as-replacement trap (`ai/rules/no-test-deletion.md`); it needs its own review, not a drive-by. | plan/spec-fixit-peers-from-tree-stale-shape.md | deferred |
