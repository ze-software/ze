# Ad-hoc deferrals -- 2026-07-27 (session 423eaa77)

Defects found while closing every document in `plan/handover/`. None of them was a
handover item: each surfaced underneath one, was verified at the producing function,
and is separable from the goal the surrounding work existed to achieve
(`ai/rules/completion.md` -- "the goal still works; this is a distinct, larger,
separable feature"). Each names a spec that exists on disk.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-27 | ad-hoc (handover 19, web editor list-entry delete) | Under zefs blob storage `/config/diff` under-reports pending structural ops: after a list-entry delete it renders `Review changes (1)` with an empty body, where filesystem storage yields `- bgp peer london` and a count of 2. `Editor.listChangeFiles` (`internal/component/cli/editor.go`) calls `store.List(filepath.Dir(originalPath))`, which for blob storage resolves through `blobStorage.List` -> `ReadDir(resolveKey(prefix))` (`internal/component/config/storage/blob.go`). Leaf edits are unaffected because they are also in the in-memory `e.meta`; structural ops live only in the change file. `/config/rename/` is affected identically. | The delete itself is correct -- tree, peer list and commit all land -- so the handover-19 goal is met. This is a separate review-surface defect in the storage/editor layer with its own test surface, and it predates the delete fix. | plan/spec-fixit-zefs-diff-structural-ops.md | deferred |
| 2026-07-27 | ad-hoc (handover 22, OTC role-resolution holes) | A peer created from a dynamic group receives no per-peer plugin config at all: it establishes with a real address but has no `peer` list entry, so `configjson.ForEachPeer` (`internal/component/config/configjson/traverse.go`, `:57-59`) never visits it. For `bgp-role` that means no Role capability advertised and no RFC 9234 OTC gate; `bgp-rpki` has the identical limitation. | Distinct from the keying bug that WAS fixed (config keyed by name while readers key by address). Here the config never reaches the delivery layer, so there is no key to get wrong. A real fix changes the shape of delivered config, not its contents, and must be fixed at the delivery layer rather than per-plugin. | plan/spec-fixit-dynamic-group-peer-config.md | deferred |
| 2026-07-27 | ad-hoc (handover 22 spec-closure audit) | `spec-fixit-bgp-session-fsm-lifecycle` AC-6 and AC-7 have no code and no test. AC-6: `session_read.go` returns `nil` after `closeConn` with no `setCloseReason` and no `errChan` signal; `closeConn` nils `s.conn`, so `Run` reaches `session.go`, finds no close reason at `:884`, and spins the 10 ms sleep at `:887` FOREVER. AC-7: `grep 'defer s.closeConn'` matches nowhere in `reactor/`, leaving all three owner-approved leak sites returning without closing (`session_handlers.go`, `runOpenValidator` at `session_open_validation.go`, `session_handlers.go`) and the cancel goroutine exiting on `<-s.done` without closing (`session.go`). Also missing: `deadpeer-holddown.ci`, the dead-peer interop scenario, and both owner-approved Prometheus counters. | The spec is its own home and stays OPEN. The `fixit-bgp-session-fsm-lifecycle` closure record is a FALSE closure signal: it scopes itself to the FSM-package slice and names the reactor consumers as out of scope, and the reactor half then arrived piecemeal in three differently-titled commits. The spec carries a 5-step "to close this" list. Not started here because `internal/component/bgp/reactor/session*.go` was owned by the buffer-guard work for the whole session. | plan/spec-fixit-bgp-session-fsm-lifecycle.md | deferred |
| 2026-07-27 | ad-hoc (round-2 review, RFC compliance) | 55 of 162 enrolled RFCs have no full text under `rfc/full/` -- including `rfc7854` and `rfc9069`, the two the BMP work was done under. `scripts/dev/rfc_requirements.py` already emits "no source text under `rfc/full/` -- cannot judge", and `ai/skills/ze-rfc.md` step 2 says to read a file that for a third of enrolled RFCs has never existed, so `ze-rfc-check` validates those summaries against themselves. This permits both a requirement the RFC does not contain (one instance found and corrected this session) and -- invisibly, because the ledger reads complete -- a requirement it does contain that was never extracted. | Fetching 55 texts is mechanical; re-auditing 55 summaries against their sources is the real work and needs its own spec and an owner decision on whether enrolment should REQUIRE the full text (which would fail the gate for 55 RFCs the day it lands). Recorded rather than started, with the owner asked directly. | plan/spec-release-evidence-gate.md | **half done 2026-07-27 (session dd843d81)** |

**Update, session dd843d81.** The owner ruled "backfill first, then require", and both halves of
that ruling are now done: all 58 stems that the gate could not resolve were fetched
(55 RFCs into `rfc/full/`, 2 drafts into `rfc/drafts/`, plus `sflow-v5`), so all 166
enrolled stems resolve, and `check_enrolment` (`scripts/dev/rfc_requirements.py`) now FAILS
an enrolment with no source text (mutation-verified: hiding `rfc/full/rfc9069.txt` reproduces
the violation). `make ze-rfc-check` is green at 2720 gated requirements across 166 RFCs.
Note the original count was slightly off: `draft-abraitis-idr-addpath-paths-limit` already had
its text under `rfc/drafts/`, which the gate accepts (`rfc_requirements.py` searches
`full` AND `drafts`), so 58 were missing rather than 55 of 162.

**What REMAINS deferred is the part that always was the real work:** re-auditing those 58
summaries against the sources now sitting beside them. The backfill makes the audit possible;
it does not perform it. The one automated signal available today is clean -- the ledger's
"Summaries declaring no MUST-level requirement" table lists only `rfc1035` and `rfc6987`,
both with 0 MUST-level keywords in source, i.e. consistent -- but that table only catches a
summary that captured NOTHING, never one that captured SOME of its obligations.
| 2026-07-27 | ad-hoc (ze-ci-dispatch-check, built while sweeping stale command call sites) | `validateCommandArgs` Phase 2 (`internal/component/plugin/server/command.go`) positionally matches only `ArgEnum`/`ArgUnion`/`ArgString`, so a mandatory non-string leaf can never be filled positionally; and where a command's noun is the terminal key token, `matchCommandTokens` extracts no selector and the `RequiresSelector` guard (`command.go`) rejects it. Verified live: `show tcp-check 127.0.0.1 1 timeout 2s` -> `required argument missing: port`; `delete bgp peer 127.0.0.1` -> `requires a selector`; `show interface rate <name>` -> usage text. | Both fixes alter matching for all 410 registered commands, so this needs its own spec, a mutation-verified test per shape and a full functional pass. A narrower fix special-casing these three would leave the next typed leaf broken identically. `test/plugin/api-peer-remove.ci` is red on this root cause; three other `.ci` tests currently assert only that the command resolves, with the defect documented inline. | plan/spec-fixit-positional-arg-matching.md | **done 2026-07-28 (session dd843d81), with one row-item disproven** |

**Update, session dd422581/dd843d81.** Both matcher defects are FIXED in
`internal/component/plugin/server/command.go`: the positional loop now offers a token to
every `ArgKind` (mandatory defs first) via `positionalDef`, and a lone spare
positional token is adopted as the selector for a terminal-noun command, fenced on
`RequiresSelector` + no out-of-band `ctx.Peer` + exactly one spare token. That third fence
is load-bearing: `announce` is a single-token command that also carries `leaf selector
mandatory` (`ze-cli-announce-cmd.yang`), so a greedy version would have announced to a
peer named "unicast". Plugin suite before: `failed 8 [20, 45, 459, 460, 461, 476, 515,
516]`; after: `failed 6 [459, 460, 461, 476, 515, 516]` -- test 45 (aspath-filter-reject)
was fixed as a side effect and NOTHING regressed. `make ze-validate-commands` produces an
identical row set (the diff is Go map iteration order; sorted, both logs are byte-identical).

**The third reproduction in the row above is WRONG and is withdrawn.** `show interface rate
<name>` is not a matcher defect: the matcher hands the handler `args=["eth0"]` correctly.
`show interface rate` is a registered ALIAS path for wire method `ze-show:interface`, so the
key consumes `rate`, while `handleShowInterface` (`internal/component/iface/cmd/show_interface.go`)
switches on `args[0]` expecting to find it there. The same contract bug hits `show interface
brief` (returns ALL interfaces, not brief) and `show interface errors`. It needs a way for a
handler to learn which alias invoked it, which is a different piece of work from argument
matching and remains OPEN and unhomed.
| 2026-07-28 | ad-hoc (spec-fixit-migrate-sleeps-infra, verifying the firewall bucket on Linux) | `make ze-netns-test` cannot pass any `zetest`-gated test. The target depends on `$(ZEBIN_ZE)` (`mk/test-integration.mk`) and runs the suites with `ZE_TEST_NO_BUILD=1`, but `$(ZEBIN_ZE)` is the PRODUCTION binary -- `mk/test-integration.mk` says so outright ("the real $(ZEBIN_ZE) has neither zetest nor ze_test"), while the functional-test DUT is built with `-tags '... zetest ...'` (`mk/test-functional.mk`). So `test/firewall/ddos-local-withdraw.ci`, whose config carries the `ddos/fake` node that only `internal/test/plugins/fakeddos/yang/ze-fakeddos-conf.yang` augments in, dies at `parse config: line 5: unknown field in ddos: fake` and reports as a TEST failure. Verified both ways on this host: 22/23 via `ze-netns-test`, and 23/23 (ddos-local-withdraw in 653ms) running the same suite against a DUT built with `ze_core ze_distro ze_setup zetest $(ZE_FEATURES)`. A second, independent breakage of the same target: it cannot run on-session at all, because make writes `bin/ze-<session-id>` (`mk/session.mk`) while the runner resolves the binary by BARE name (`internal/test/sessionpath/sessionpath.go`) and `.ci` tests exec `ze` unsuffixed. | Both are defects in the VEHICLE, not in any test, and fixing them is a build-graph change (which binary the netns target depends on, and how the session suffix reaches an exec-by-bare-name harness) that touches a target other suites and other sessions share. Doing it inside a sleep-conversion spec would hide a build-infra change under a test-hygiene diff. The sleep work did not need it fixed: the firewall bucket was verified against a hand-built zetest DUT and the result recorded in `test/.ci-sleep-baseline`. | plan/spec-fixit-netns-test-dut-tags.md | deferred |
| 2026-07-27 | ad-hoc (rfc6286 review NIT-1 follow-through) | `parsePeersFromTree` (`internal/component/bgp/reactor/reactor_api.go`) reads a flat peer shape the YANG no longer produces -- it expects `remote`/`local`/`receive-hold-time`/`router-id` directly under the peer, while `grouping peer-fields` (`internal/component/bgp/yang/ze-bgp-conf.yang`) nests them under `connection`/`session`/`timer`. On a YANG tree it dies at `missing required remote container` (`reactor_api.go`). Reachable via `ze -` (stdin), because `internal/component/bgp/config/loader.go` guards both `SetConfigPath` and `SetReloadFunc` on `configPath != "" && configPath != "-"`. | Realigning the shape rewrites a function whose entire existing test surface encodes the flat form. Doing that silently under a green bar is the test-rewrite-as-replacement trap (`ai/rules/testing.md`); it needs its own review, not a drive-by. | plan/spec-fixit-peers-from-tree-stale-shape.md | deferred |
