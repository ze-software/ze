# Deferrals: wire-edit tail, ad-hoc 2026-08-02

Everything found while completing the wire-edit spec set that is NOT part of it.
Filed rather than fixed, deliberately: this session repeatedly turned "I found
it" into "I must fix it now", which is how closure kept receding. `no-parking.md`
requires fixing a defect that BLOCKS THE GOAL the work exists to achieve. None of
these did.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-02 | wire-edit closure | Two broken `// Design:` refs to the deleted `plan/spec-wire-edit-5-fanout-dedup.md`, in `internal/component/bgp/filterapi/fingerprint.go` and `internal/component/bgp/reactor/forward_dedup.go`. `make ze-verify-wiring-docs` reports them. | Caused BY this session's own closure commit B, which `git rm`'d the spec while two files still cited it. This is finishing my own work, not an old issue, and it is a two-line edit — but it is filed rather than done because the session was told to stop expanding scope. It should be the FIRST thing the next session does. | `plan/spec-fixit-wire-edit-orphaned-design-refs.md` | deferred |
| 2026-08-02 | wire-edit closure | `plan/spec-wire-edit-2-edit-apply.md` planned three `.ci` files that were never created. Closure recorded substitutes (`modify-oversize-suppress.ci`, `wire-edit-api-origin-order.ci`, `TestModifyPathZeroAlloc`) plus the reason the RR case cannot discriminate merge-insert. | If the substitution is not accepted, child 2 has an unmet wiring row and should not have closed. That is Thomas's call, not the closing agent's. Raised in the closure report and here. | `plan/spec-wire-edit-2-deferred-ci-substitution.md` | deferred |
| 2026-08-02 | independent review | `reject=bgp:` is not implemented ANYWHERE. `parseReject` (`internal/test/runner/record_parse.go`) handles stderr, syslog and stdout and defaults to "unknown reject type"; inside a `stdin=peer` block it is silently dropped, since that loop parses only `expect=` and `action=` and `ConsumesLine` returns false for reject. Three live sites. | Wider than first recorded: it is not merely dropped in peer blocks, the `bgp` type does not exist at all. This makes child 5's AC-1 and AC-2 inexpressible with today's directives. The fix is a runner GUARD that hard-errors, matching the existing `option=env:` precedent, plus an audit of the sites once it fires. Harness work, not forward-path work. | `plan/spec-fixit-ci-peer-block-silent-directives.md` | deferred |
| 2026-08-02 | independent review | `test/plugin/filter-family-export-flowspec.ci` is VACUOUS: its `cmd=api:` injections are inert (`cmd=` is documentation-only to ze-peer), it has no `tmpfs` plugin script, and its one live assertion is the `ipv4/flow` End-of-RIB ze sends at initial sync anyway. | Belongs to nobody; surfaced by a reviewer. It advertises coverage it does not provide. | `plan/spec-fixit-vacuous-functional-tests.md` | deferred |
| 2026-08-02 | independent review | `test/plugin/logging-level-filter.ci` has never enforced its own subject, because its `reject=stderr:` sits inside the peer block where it is silently dropped. | Same root cause as the `reject=` row above; listed separately because it is a distinct test whose stated purpose is unproven. | `plan/spec-fixit-vacuous-functional-tests.md` | deferred |
| 2026-08-02 | wire-edit child 3 | `EBGPWire`, `ebgpSlotASN4`, `ebgpSlotASN2` and `ebgpWireSlot` (`reactor/received_update.go`) have zero non-test callers after the AS-path fold, but were not deleted. Child 3's AC-9 is recorded Partial. | Dead code, no behavioural effect. Deleting it is a separate, reviewable change. | `plan/spec-wire-edit-3-deferred-ac9-dead-code.md` | deferred |
| 2026-08-02 | ze-verify | `TestFwdPool_BackpressureBehavior` fails under `make ze-race-reactor` (`-count=20`), passes 5/5 isolated, 0 data races. | Per `ai/rules/fix-dont-record.md` this is a BROKEN TEST, not a flake: it waits on a duration instead of a condition, and "passes in isolation" is a banned excuse. It reproduces, so it must be fixed rather than shelved — but it is not wire-edit work. | `plan/spec-fixit-fwdpool-backpressure-timing.md` | deferred |
| 2026-08-02 | wire-edit child 4 | The `NN-wire-edit-api-origin` BIRD interop scenario and the `bgp_announce_dropped_oversize_total` counter were never written. The oversize drop itself is implemented and tested; only its metric surface and the interop proof are missing. | Already homed in `plan/deferrals/wire-edit-4-api-origin.md`; repeated here so the tail is readable in one place. | `plan/deferrals/wire-edit-4-api-origin.md` | deferred |
