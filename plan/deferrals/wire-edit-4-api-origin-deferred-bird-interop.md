# Deferrals: wire-edit-4-api-origin-deferred-bird-interop

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-05 | spec-wire-edit-4-api-origin-deferred-bird-interop (adjacent defect, found at the round 4 review gate) | Five interop scenarios are red at HEAD and untracked: `05-routes-from-frr`, `06-routes-from-bird`, `09-route-withdrawal-frr`, `10-ipv6-ebgp-frr`, `11-addpath-frr`. Plus the harness helper that hides two of them: `Ze.rib_count` (`test/interop/interop.py`) returns 0 when its verb does not resolve, so "unknown command" reports as "0 routes received" | Found while attributing reds for this spec's shared-harness change; each fails identically with HEAD's `interop.py`, so none is caused by this work. `06` reproduced directly by the main thread. OUT of scope: my goal does not depend on those five scenarios or on `rib_count`, and the fix needs a clean-tree re-run first to exclude other sessions' uncommitted BGP edits as a cause | plan/spec-interop-suite-red.md | deferred |
| 2026-08-05 | spec-wire-edit-4-api-origin-deferred-bird-interop (adjacent defect, found at the round 2 review gate) | The interop harness namespaces containers per run (`_SUFFIX`, `test/interop/interop.py`) but builds a bare shared `ze-interop` image tag (`build_images`, `test/interop/run.py`), so two concurrent runs race on the image and the loser runs the winner's daemon | Measured twice on 2026-08-05: a mutation run PASSED when it should have failed, because another session's build landed between build and container start, and a mutated daemon then sat in the shared tag after the source tree was clean. A false GREEN from a mutation run is how a vacuous test gets certified as discriminating. OUT of this spec's scope: the fix touches every interop scenario and the `NO_BUILD=1` iterate-fast path, which is a design decision with its own blast radius | plan/spec-interop-image-tag-race.md | deferred |
| 2026-08-05 | spec-wire-edit-4-api-origin-deferred-bird-interop | Live-peer proof of ascending attribute type-code order (RFC 4271 Section 5) for an API-originated route. The property is produced by `(*announceAttrs).emit` (`internal/component/bgp/reactor/announce_build.go`) and pinned at the byte level by `test/plugin/wire-edit-api-origin-order.ci`, but no interop scenario can observe it | BIRD 2.15.1 accepts any attribute order and `birdc show route ... all` prints in BIRD's own canonical order, so reverting `emit` leaves the route dump identical: a route-dump assertion cannot discriminate (`ai/rules/interop-and-goal-validation.md`, vacuity traps). `test/interop/` has no packet capture of any kind, measured 2026-08-05. Thomas ruled on 2026-08-05 to land the acceptance scenario first and home the capture capability separately | plan/spec-interop-wire-capture.md | deferred |
