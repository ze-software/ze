# Deferrals: gokrazy-init-bump

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-10 | spec-gokrazy-init-bump AC-6 | Full QEMU gokrazy L2TP appliance proof (build + boot + xl2tpd/pppd session) | The two appliance build/config-flow bugs that first blocked it (daemonRunning false-positive vs host sshd:22; ze init active-config shadows the build template) are FIXED, per `plan/learned/1103`. The residual blocker is the host environment: the run needs root, `/dev/ppp` and PPPoL2TP. The bump itself is verified via the image build. Reason corrected 2026-08-03 at source-spec closure; the boot proof is `make ze-vpp-hugepages-qemu-test` plus `ze-deployment-gokrazy-l2tp-ppp-test`, never `test/appliance/serial-login.ci`, which boots nothing | `plan/spec-finish-appliance-qemu-evidence.md` (work item added; re-homed 2026-07-16 -- the original destination was closed in `f42c2ccb2` with `plan/learned/1103`, whose :69-73 records that AC-3's end-to-end qemu run 'remains to be executed on a root host'. The two bugs that blocked it ARE fixed; only the run remains) | deferred |
| 2026-08-03 | spec-gokrazy-init-bump (closure review) | Make the make-path credential injection fail closed: `debugfs -w -R` at `mk/gokrazy.mk` exits 0 on failure and its only error report goes to a discarded stderr, so an image whose `/perm` database was never written builds green | Found by the closure review of the E2FS guard. Out of scope for the bump, whose goal does not depend on this path (`ai/rules/rule-precedence.md`: name it, home it, close, then fix). The guard itself was hardened in the closing commit to require `debugfs` as well as `mkfs.ext4`; what remains is the silent failure of a tool that IS present | `plan/spec-gokrazy-builddir-tmp-deferred-build-flow-unification.md` (work item added under "Added 2026-08-03") | deferred |

