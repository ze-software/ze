# Deferrals: gokrazy-init-bump

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-10 | spec-gokrazy-init-bump AC-6 | Full QEMU gokrazy L2TP appliance proof (build + boot + xl2tpd/pppd session) | The two appliance build/config-flow bugs that first blocked it (daemonRunning false-positive vs host sshd:22; ze init active-config shadows the build template) are FIXED, per the closure record of `spec-fixit-appliance-evidence-config`. The residual blocker is the host environment: the run needs root, `/dev/ppp` and PPPoL2TP. The bump itself is verified via the image build. Reason corrected 2026-08-03 at source-spec closure; the boot proof is `make ze-qemu-vpp-hugepages-test` plus `ze-deployment-gokrazy-l2tp-ppp-test`, never `test/appliance/serial-login.ci`, which boots nothing | RESOLVED 2026-08-05 at source-spec closure -- the run happened here, so nothing is homed elsewhere for this row. `make ze-qemu-vpp-hugepages-test` -> `VPP-HUGEPAGES-QEMU: PASS cmdline has hugepages=64, hugepages-total=64`. `scripts/evidence/effective-gokrazy-l2tp-ppp.py` -> exit 0, `OK: gokrazy Ze appliance completed real L2TP PPP/IPCP with Ze ppp0 and LAC ppp0, dataplane ping, route inject, and clean teardown`. The residual blocker this row recorded (root, `/dev/ppp`, PPPoL2TP) was re-measured and is gone: `make ze-deployment-preflight` reports that prerequisite line `ok`. `plan/spec-finish-appliance-qemu-evidence.md` keeps its own second row (iface-absent-link-graceful AC-3) and stays open on that | done |
| 2026-08-03 | spec-gokrazy-init-bump (closure review) | Make the make-path credential injection fail closed: `debugfs -w -R` at `mk/build-gokrazy.mk` exits 0 on failure and its only error report goes to a discarded stderr, so an image whose `/perm` database was never written builds green | Found by the closure review of the E2FS guard. Out of scope for the bump, whose goal does not depend on this path (`ai/rules/rule-precedence.md`: name it, home it, close, then fix). The guard itself was hardened in the closing commit to require `debugfs` as well as `mkfs.ext4`; what remains is the silent failure of a tool that IS present | `plan/spec-gokrazy-builddir-tmp-deferred-build-flow-unification.md` (work item added under "Added 2026-08-03") | deferred |

