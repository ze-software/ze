# Deferrals: fixit-web-auth-deleted-user-survives-reload

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-07 | spec-fixit-web-auth-deleted-user-survives-reload | Rebuild the live AAA bundle on config reload, so RADIUS and TACACS SERVER changes (address, secret, timeout, backend added or removed) apply without a daemon restart. The bundle's LOCAL backend is no longer stale: it reads the running config per call, which is this spec's fix. What remains is the remote backends, which are constructed from the boot tree and cannot re-read anything | `swapAAABundle` has two non-test callers: `infraSetup` (`cmd/ze/hub/infra_setup.go`), reached only through `infra.Run`, whose sole non-test call site is `CreateReactorFromTree` (`internal/component/bgp/config/loader_create.go`); and `runYANGConfig` (`cmd/ze/hub/main.go`) on the ssh-standalone startup path. `CreateReactorFromTree` runs from the BGP plugin's `p.OnConfigure` (`internal/component/bgp/plugin/register.go`), which the plugin protocol calls at stage 2 of plugin STARTUP; that plugin registers no apply or verify handler, and `(*Server).ReloadConfig` (`internal/component/plugin/server/reload.go`) drives verify/apply RPCs only. `go list -deps ./internal/component/plugin/server` carries no `bgp/config` at all, so no reload path can reach the rebuild. The comment in `cmd/ze/hub/aaa_lifecycle.go` claiming the bundle swaps "on each config reload" is its author's belief, not a decision record. Fixing it needs a reload-time rebuild seam next to `ListenerMigrator.ReloadListeners`, which another session is actively changing | `plan/spec-fixit-mgmt-listener-auth-guard-deferred-reload-auth-rebuild.md` | deferred |
| 2026-08-07 | spec-fixit-web-auth-deleted-user-survives-reload | Resolve the web server's BOOT-TIME serve-or-not user list independently of the `environment { ssh { } }` block | `ExtractSSHConfig` (`internal/component/config/infra/ssh.go`) returns early when the tree carries no `environment` or no `environment/ssh` container, so `cfg.Users` stays empty. `ServiceDeps.ConfigUsers` is that field (`cmd/ze/hub/main.go`), and `startWebServer` refuses to serve when the merged list is empty. A config that declares `system.authentication.user` but no ssh block therefore disables the web server with "no authenticatable users" even though users are configured. Only that boot check remains coupled: this spec's per-login reader calls `infra.ExtractAuthUsers` on the `system` root directly, so AUTHENTICATION is already independent of the ssh block. The goal (a deleted user cannot log in) holds without this fix | `plan/spec-fixit-mgmt-listener-auth-guard.md` | done 2026-08-08: `ServiceDeps.ConfigUsers` is DELETED. `startWebServer` (`cmd/ze/hub/service_web.go`) asks `localUsersLive` once at construction, the same source the login path answers from, so the serve-or-not answer can no longer differ from who may log in and neither depends on the ssh block. It came out of the round-2 fix for the double zefs read: one producer for the live list left no second list for this check to consult. `startWebServer` now fails closed on a nil or erroring source instead of reading an empty snapshot as "nobody configured". Evidence: `TestWebServerUsesTheCallersCredentialsWhenZefsIsUnreadable` (`cmd/ze/hub/service_web_users_test.go`) |
| 2026-08-08 | spec-fixit-web-auth-deleted-user-survives-reload | Resolve the API's per-user gate independently of the `environment { ssh { } }` block, the same coupling just closed for web | `apiUsers` (`cmd/ze/hub/main.go`) is `mergeAuthUsers(zefsUsers, sshCfg.Users)`, and `sshCfg` comes from `infra.ExtractSSHConfig`, which returns nothing without an `environment/ssh` container. `apiAuthReloader` (`cmd/ze/hub/mgmt_auth_reload.go`) reads the same extractor. So a config declaring `system.authentication.user` and an `api-server` block, with no ssh block and no token, gives `buildUserAuthenticator` an empty list, which returns nil, and the REST listener gates nothing. It is not remotely reachable: `apiAuthed` is then false and `checkMgmtListeners` refuses any non-loopback API listener at boot, so the exposure is a loopback listener the operator believes is per-user gated. NOT this spec's goal, which is that a REMOVED user stops authenticating, and that holds: with no per-user gate there is no per-user access to remove. Fixing it means giving the API the same treatment web just got, a live merged source in place of the snapshot pair | `plan/spec-fixit-mgmt-listener-auth-guard.md` | deferred |
| 2026-08-07 | spec-fixit-web-auth-deleted-user-survives-reload | SSH public-key authentication reads the startup `Users` snapshot (`sshBuildInputs.Users`, `cmd/ze/hub/infra_setup.go`), so a public key removed by a reload keeps working until restart | SSH PASSWORD authentication goes through `bundle.Authenticator`, whose local backend this spec made live, so that half is fixed. The public-key path is a different credential store read at a different point: `sshBuild` receives the merged list once, and the ssh server binds it into its own key checker. Making it live needs the ssh server to accept a user source rather than a list, which is an ssh-package change this spec does not otherwise touch | `plan/spec-ssh-fido2-keys.md` | done 2026-08-08: implemented in this spec as AC-11. `(*Server).authenticatePublicKey` (`internal/component/ssh/pubkey.go`) reads `(*Server).users()`, which prefers `Config.UsersFunc` over the boot snapshot, and both `zessh.NewServer` sites set it (`cmd/ze/hub/service_ssh.go`). Evidence: `TestPublicKeyAuthFollowsRunningConfig`, `TestPublicKeyAuthDeniesWhenRunningConfigUnreadable` (`internal/component/ssh/pubkey_test.go`), `TestInfraSetupSSHPublicKeyFollowsRunningConfig`, `TestSSHStandaloneBuildPublicKeyFollowsRunningConfig` (`cmd/ze/hub/ssh_pubkey_live_test.go`) |
