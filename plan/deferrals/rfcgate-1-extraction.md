# Deferrals: rfcgate-1-extraction

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-29 | spec-rfcgate-1-extraction (phase 8) | **The rfc7296 pilot, in full: re-authoring `rfc/short/rfc7296.md` with the 214 unextracted MUST-level obligations the 2026-07-29 walk found, proving the 88 that are already implemented, IMPLEMENTING the 108 that are not, resolving the 18 uncertain rows, and signing off `rfc/extraction/rfc7296.json`.** Sizing is measured, not estimated: 289 MUST/MUST NOT/SHALL keyword lines read in context across all 92 numbered sections, 43 restatement sites collapsed, 8 judged non-normative, leaving 214 distinct obligations against the 18 MUST rows the summary holds today (roughly 8% of the RFC's MUST surface). Triage: 63 `implemented-and-testable`, 25 `implemented-untested`, 108 NOT IMPLEMENTED, 18 `uncertain`. Verified against the producing code by the supervising session: `RFC7296-2.2-1` IS implemented (`internal/component/ike/engine/fsm.go` resends `sa.LastSentMsg` byte-identically, so the Message ID is reused by construction), `RFC7296-2.2-2` is NOT (`NextMsgID` is a bare `uint32` at `internal/component/ike/engine/sa.go` and every mutation is an unchecked `++` at `dpd.go`, `inbound.go` and `inbound.go`, so the SA silently wraps to 0 instead of closing or rekeying), and DPD probes are sent unencrypted (`internal/component/ike/engine/dpd.go`, admitted in the comment at `:77-78`) | Owner ruling (Thomas, 2026-07-29), taken in answer to the walk's count exactly as the spec's Failure Routing requires ("Phase 8a's count is larger than this spec can carry -> STOP and take the COUNT to Thomas for scoping"). He ruled twice, and the two rulings compose: **fix all 108 inside the spec that owns them**, and **split phase 8 into its own spec** so the rfcgate machinery set is not serialised behind an IKEv2 compliance workstream. This is a change of HOME, not a reduction of scope: the obligations are extracted, implemented and proven in the destination spec, and no annotation may absorb any of them. `ai/rules/rfc-compliance.md` voids `{gap}` / `{not-applicable}` / `partial` as authority, and the owner has now ruled in the opposite direction explicitly. D7 of the umbrella ("found means owed") continues to bind: these obligations are CONFIRMED unextracted, so they escape the grandfather by definition and the debt is paid in the destination spec rather than written off | **DONE 2026-08-03.** Landed by the rfcgate-1b pilot, which closed in commit `ff8f13fea`. The summary carries 227 rows / 222 gated, the signed `rfc/extraction/rfc7296.json`, and the four MUSTs the walk found that no gate could see | done |
| 2026-07-29 | spec-rfcgate-1-extraction (A-7) | **A closure decision on assumption A-7**, which the implementation phase left `unvalidated`: whether the derived-site inventory stays stable against future churn in the RFC source texts. It is a claim about future source changes and cannot be validated inside one session, which is why no evidence closed it | Every other assumption (A-1 through A-6, A-9) was re-derived against the live 166-RFC corpus and moved to `confirmed`. A-7 is not a gap in the machinery: the `source-sha` field already fails an artifact closed the moment its source text changes (`plan/spec-rfcgate-1-extraction.md` AC-4), so the risk A-7 names is detected rather than silently absorbed. What remains is the judgement of whether that detection rate is tolerable once the fleet drain is signing off at volume, and only measured throughput answers it | `plan/spec-followup-rfc-enrollment.md` (owns the fleet drain, which is the first activity that produces the sign-off volume A-7 is a claim about) | deferred |
