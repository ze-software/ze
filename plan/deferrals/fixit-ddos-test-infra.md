# Deferrals: fixit-ddos-test-infra

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-16 | spec-fixit-ddos-test-infra (Design Insights, found while validating A-4) | `test/pppoe/` is ORPHANED DEAD CODE: its 3 `.ci` files use `option=netns:veth=`, which `parseOption` rejects as `unknown option type` (`internal/test/runner/record_parse.go`), and no suite registers pppoe (`internal/test/cli/register.go`). They cannot parse and nothing runs them, so the PPPoE Access feature row in `docs/features.md` has no functional test behind it | Thomas chose REPAIR on 2026-08-07, voiding the Option D (delete) decision the `fixit-pppoe-orphaned-tests` closure recorded. `option=netns:veth=` was never implemented, so the repair extends the existing `netns-link` option with `peer=` (veth pair) and `vlan=` (802.1Q sub-interface per end) instead of adding a second directive for the same job; `registerCIRoot("pppoe", ...)` roots the suite and `make ze-qemu-pppoe-test` runs it under the netns launch mode on ze's runtime kernel (stock Alpine has no `CONFIG_PPPOE`, and `handlePADR` opens AF_PPPOX before PADS). Running them exposed why the feature had no test: `ExtractParameters` read the `interface` list as a `[]any` while `Tree.ToMap` emits a keyed YANG list as a map, so `Interfaces` was always empty and the subsystem was never registered from any real config | `spec-finish-ci-coverage` (work item "`test/pppoe/` orphan", DONE 2026-08-07) | done |
| 2026-07-16 | spec-fixit-ddos-test-infra (AC-10 transit proof) | Proving packets are FORWARDED (not merely admitted) needs a receiver on the far veth end counting arrivals; the current shape can prove the drop but not the forward | Out of scope for the test's rework onto `wait_until`: it needs new test topology, not a wait-primitive change | `plan/future/spec-ci-coverage-remaining-surfaces.md` (AC-10, tracked in-spec) | deferred |
| 2026-07-19 | spec-fixit-ddos-test-infra functional-proof | QEMU run of the two .ci is the AC-1/4/5/6 proof, deferred to CI | live-server/QEMU constraint, deferred to CI | plan/spec-finish-appliance-qemu-evidence.md | deferred |

