# Deferrals: fixit-bgp-per-family-prefix-enforcement

Deferral rows for this source. `/ze-status` folds the aggregate live backlog on
read from `plan/deferrals/`. Nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-04 | spec-fixit-bgp-per-family-prefix-enforcement, review gate run 3 | The `ze` CLI client inside the interop image resolves NO BGP show verbs: `docker exec <ze-container> ze show bgp rib status` answers `error: unknown command`, and so does the older `show rib status`. `Ze.rib_count` (`test/interop/interop.py`) therefore returns 0 for every caller, and `make ze-interop-test INTEROP_SCENARIO=05-routes-from-frr` is RED on `Ze RIB has 0 received routes (expected >= 3)`. Reproduced four times. PARTLY FIXED here: the helper asked for `show rib status`, and the verb is `show bgp rib status` (`internal/component/bgp/plugins/cmd/rib/yang/ze-rib-cmd.yang`, compiled by `BuildVerbCommandTree` in `internal/component/cli/client/main.go`). The verb is corrected; the client still resolves nothing | PRE-EXISTING and not introduced by this spec. Scenario 46 no longer depends on it: the RIB assertion it briefly carried was removed, because an upper-bound assertion over a helper that answers 0 on failure is worth less than no assertion. The per-family goal is proven by the log-line and session-survival assertions against FRR, by `TestPrefixTeardownPerFamilyEnforcement` asserting `drop=true`, and by `test/plugin/prefix-per-family-teardown.ci` | `plan/spec-fixit-test-harness-fail-open-guards.md`. It owns harness guards that answer a plausible value on failure, and `rib_count`'s 0-on-failure is one. NEXT STEP, and it is NOT the Dockerfile: verb resolution is CLIENT-LOCAL with no daemon in the path (`cmdutil.RunCommand` to `cli.BuildVerbCommandTree`), and the same `unknown command` reproduces on the HOST with this repo's own `bin/ze` for `show bgp rib status`, `show bgp peer list` and `show isis neighbor`, while `ze show help` lists `bgp` and `ze show interface` works. Start at `cliWireToPaths = yang.WireMethodToPaths(cliLoader)` (`internal/component/cli/client/main.go`). Fixing it un-reds scenarios 05, 13, 35 and ospf-gr, and only then is a RIB assertion worth adding to 46 | deferred |
| 2026-08-04 | spec-fixit-bgp-per-family-prefix-enforcement, review gate run 2 | In warn-only mode the prefix counter counts routes Ze never installs. `applyPrefixCheck` (`internal/component/bgp/reactor/session_prefix.go`) adds the delta BEFORE it decides, and `processMessage` (`internal/component/bgp/reactor/session_read.go`) then drops the whole UPDATE, so an over-limit UPDATE of N prefixes leaves the count at N with the RIB holding none. Every later announce for that family is dropped until the peer withdraws routes Ze never had | PRE-EXISTING on the AC-27 drop path, untouched by this spec, and self-correcting: the peer still holds those routes in its own Adj-RIB-Out, so a withdraw decrements the count. The enforcement goal holds without it. Deciding it needs an answer this spec has no standing to give: does a prefix limit count what the peer OFFERED or what Ze INSTALLED? RFC 4271 Section 6.7 does not say, and the answer changes the meaning of every prefix counter and metric | `plan/spec-bgp-per-peer-received-counter.md`. It owns per-peer received accounting, the same question one layer up. Whoever takes it must put the offered-versus-installed choice to Thomas before changing the counter | deferred |
| 2026-08-04 | spec-fixit-bgp-per-family-prefix-enforcement, review gate | A config change that touches ONLY the prefix `updated` dates never reaches a running peer, so the prefix-stale warning and `ze_bgp_prefix_stale` cannot clear until the daemon restarts. `peerSettingsEqual` (`internal/component/bgp/reactor/reactor_api.go`) nils `PrefixUpdated` on both copies before comparing, so `reconcilePeersJournaled` reads the peer as unchanged and neither swaps nor restarts it, and `RaisePrefixStale` is called only from `Reactor.AddPeer` (`internal/component/bgp/reactor/reactor_peers.go`). A PeeringDB refresh that bumps only the dates therefore leaves the alarm raised | PRE-EXISTING and unchanged by this spec: the field was a peer-wide string with the same exclusion and the same AddPeer-only raise before it became a map, so the behavior is identical on both sides of the change. The enforcement goal this spec exists to achieve holds without it, which makes it separable rather than parked (`ai/rules/rule-precedence.md`). Fixing it needs three coordinated edits: stop excluding `PrefixUpdated` from `peerSettingsEqual`, classify it swappable in `hotSwappableSettings` (`internal/component/bgp/reactor/peer_settings_apply.go`), and re-raise the staleness warning on the swap. That is reload-path work, not prefix-limit work | `plan/spec-bgp-peer-settings-reload-ignored.md`. It exists on disk and owns exactly this surface: a settings change the running peer never sees | deferred |
| 2026-07-30 | spec-fixit-bgp-per-family-prefix-enforcement | Expose per-family prefix staleness dates on an operator surface. The spec stores `updated` per family (`ze-bgp-conf.yang`). It then aggregates to the oldest date for the existing `prefix-updated` JSON key (`internal/component/bgp/plugins/cmd/peer/peer.go`) and for the staleness report bus (`internal/component/bgp/reactor/reactor_peers.go`). An operator cannot see WHICH family is stale | A per-family output field changes an external JSON surface and `plugin.PeerInfo` (`internal/component/plugin/types_bgp.go`). That is a new output capability. It is not part of correcting the parse defect. The enforcement goal holds without it, so this is separable per `ai/rules/completion.md` | `plan/spec-bgp-peer-metric-labels.md`. It owns the per-peer output and label surface, where a per-family staleness field belongs. The reviewer judgement this row asked for was made at closure: the field is NOT cheap, because it adds an output field to `plugin.PeerInfo` (`internal/component/plugin/types_bgp.go`), the plugin SDK surface | deferred |
