# Deferrals: kernel-compose-make-q-assertion-is-vacuous

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-09 | the kernel-profile-fixtures-leak-into-registry spec (commit `1acf1a627`), review gate | `test/install/kernel-compose.ci` deletes the built image, then asserts `make -q` reports work to do. `$(OUT)/Image` in `tools/installer-kernel/Makefile` lists the phony `FORCE` as a prerequisite. `make -q` therefore always exits non-zero, the branch is unreachable, and the assertion stays green with the rebuild trigger broken | Two independent review lenses each checked that the fixture-isolation diff lowered no assertion. Each reported this one. `FORCE` sits on that line at `1acf1a627^`, so the vacuity predates the work in hand. That work does not depend on the assertion being real, so it gets a spec rather than a fix in the closing commit (`ai/rules/completion.md`) | `plan/spec-kernel-compose-make-q-assertion-is-vacuous.md` | done |
