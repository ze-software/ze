# Deferrals: ad-hoc-2026-08-08-031d68b3

**This shard is an INDEX and carries no rows.** Its seven rows moved to one shard
per issue on 2026-08-08, on the owner's instruction that each identified issue
gets its own file. They were moved verbatim, not rewritten, so every trace,
reason and destination is intact in the shard named below.

The aggregate live backlog is folded on read from `plan/deferrals/` by
`/ze-status` (`ai/rules/planning.md`), so the split changes what a reader opens
and not what the backlog contains. The list below is deliberately NOT a table:
the commit gate parses a deferral row as exactly six cells, and an index table
here would present itself as unreadable rows.

Nothing here is fixed. Every one was found while closing other work, and
`ai/rules/rule-precedence.md` puts a defect the goal does not depend on AFTER
the work in hand, never on the way to it.

- `fixit-interop-bare-verb-guard.md` — no mechanical guard stops a new interop
  scenario calling the bare `ze <verb>` form, which cannot authenticate and
  whose failure `docker_exec_quiet` turns into an empty string.
- `fixit-attribute-length-from-family.md` — three sibling attributes size from an
  address family while writing `AsSlice()`, so a size query and a write can
  disagree. One writes 16 octets into an 8-octet region.
- `fixit-declared-commands-without-leaves.md` — 30 declared command paths carry a
  `<value>` placeholder with no YANG leaf, so they have no completion and no
  daemon-side argument validation.
- `fixit-ospf-sr-missing-label-passes.md` — that scenario's step 3 downgrades a
  missing MPLS label 16100 to `log_info` and passes.
- `fixit-mpreach-split-undercounts-rd.md` — `SplitMPReachNLRIWithAddPath` derives
  its next-hop overhead from `Is4()` and omits the RD, under-counting 8 octets
  per next hop under SAFI 128.
- `fixit-commit-rail-nexthop-unvalidated.md` — a third rail lost the same
  accidental size-mismatch guard. Dead today, and whether the set of such checks
  is CLOSED is the open question.
- `fixit-boilerplate-split-loses-obligation.md` — the splitter loses an
  obligation when the fused chunk has no terminator after the boilerplate match,
  and never cuts a preceding head at all.
- `fixit-learned-1366-has-no-files-section.md` — `./le doc check verify` is RED at
  HEAD: a committed learned summary with no `## Files` section takes the
  staleness corpus one over its ceiling.

Four more shards came out of the independent review of `2eb6a3dda`, the
prefix-set counter. They are recorded here for the same reason and each carries
its own file: `fixit-prefix-count-installed-unproven-against-a-real-peer.md`,
`fixit-prefix-config-lookup-scans-per-update.md` and
`fixit-installed-count-memory-magnitude-undisclosed.md`.
