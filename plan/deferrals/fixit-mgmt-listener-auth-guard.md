# Deferrals: fixit-mgmt-listener-auth-guard

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-17 | spec-fixit-mgmt-listener-auth-guard (Known Limitations) | Auth-mode changes on a SIGHUP reload do not take effect: management servers are constructed once, and reload migrates addresses only. Verified at the producer rather than inherited from the spec's prose -- `ReloadListeners` (`cmd/ze/hub/listener_migrate.go`) builds its change set exclusively from addresses (`endpointsToAddrs` for web/lg/mcp `:80-102`, `apiListenToAddrs` for rest/grpc `:104-117`), every path funnels into `buildChange(name, srv, newAddrs)` whose only per-service input is `newAddrs`, and the diff it drives (`listenerDiff`, `:190-213`) compares address lists. No auth field is read, compared, or applied | Not fixed in the source spec: its AC-7 stops a running UNAUTHENTICATED listener migrating onto a non-loopback address, which is the security-relevant half and needs no rebuild. Turning auth on for an already-running server is a lifecycle change (drain/replace or a widened `Reconfigurable`), which is larger and independent. Thomas confirmed AC-7's boot-plus-migration scope as shipped, 2026-07-17 | Fixed 2026-08-07 through `plan/spec-fixit-mgmt-listener-auth-guard-deferred-reload-auth-rebuild.md`. The trace held: `ReloadListeners` read no auth field. `AuthUpdatable` (`cmd/ze/hub/listener_migrate.go`) now widens the seam alongside `TLSUpdatable`, and `(*rest.RESTServer).UpdateAuth` / `(*grpc.GRPCServer).UpdateAuth` rebuild credentials on a running server without rebinding. `ReloadListeners` rebuilds auth BEFORE any address moves, re-runs the exposure classifier over the resulting pair through `runningAuth` (which asks the server, replacing the boot-time `unauth` snapshot), and restores every server's prior credentials on any later failure. A surface whose auth is fixed at construction (web, mcp) FAILS the reload from `checkAuthRebuildable`, before anything is applied, so `runReload` discards the candidate instead of promoting it: an ERROR log alone let `ze config commit` report success over a still-unauthenticated web server (review finding, corrected 2026-08-07). Reloaders live in `cmd/ze/hub/mgmt_auth_reload.go` so the flag/env/config precedence has one implementation. Both directions proven RED with the change reverted (`TestReloadListenersRebuildsAuthenticationOn` / `...Off`, `TestRESTUpdateAuthTurnsAuthenticationOn` / `...Off`, `TestGRPCUpdateAuthTurnsAuthenticationOn` / `...Off`). The gNMI token-over-plaintext concern stays UNCLAIMED and is now verified real at the producer: `(*gnmi.Server).Serve` adds the token interceptor and the TLS credentials from independent conditions, so a token with no cert/key serves the token in cleartext, and `gnmiBuildImpl` (`cmd/ze/hub/service_gnmi.go`) downgrades an unreadable cert path to plaintext with only a warning | done |
| 2026-07-19 | spec-fixit-mgmt-listener-auth-guard functional-proof | AC-5 LG TLS + AC-6 gnmi validate wiring + functional/QEMU refusal test deferred | live-server/QEMU constraint, deferred to CI | plan/spec-fixit-mgmt-listener-auth-guard.md | done |
| 2026-08-03 | spec-fixit-mgmt-listener-auth-guard (AC-6 residual) | `TestDoctorDependencyInventory` (`internal/component/doctor/doctor_test.go`) needs a `listener/gnmi` row and `expectedTotal` 55 -> 56. The gnmi listener doctor check and its `config-gnmi-invalid` diagnostic both landed and are tested; only the hand-maintained inventory listing lacks the entry, so `ze doctor`'s dependency inventory does not mention the gnmi listener (`ai/rules/repo-maintenance.md` discovery surface) | Not a red: `total` is folded from the same hand-maintained map as `expectedTotal`, so the test cannot detect a new dependency and stayed green. Left because `doctor_test.go` carries a CONCURRENT session's uncommitted work (`ze.config.dir` / statfs / checkDiskSpace, alongside `checks_storage.go`); adding a row would entangle this commit with theirs, which `ai/rules/git-safety.md` forbids. Separable: the guard refuses and the doctor check fires without it | Fixed 2026-08-07. `TestDoctorDependencyInventory` (`internal/component/doctor/doctor_test.go`) now DERIVES the listener half of the inventory from `config.DiscoverListenerServices` (`internal/component/config/listener.go`), the same `ze:listener` schema walk `collectSchemaListeners` probes, so a listener service nobody lists fails the test instead of passing on a self-folded count. It went red on SIX unlisted services, not the one this row named. `expectedTotal` 55 -> 61 | done |

