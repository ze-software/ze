# Deferrals: fixit-private-asn-leak

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-16 | spec-fixit-private-asn-leak (risk R1) | `r.api == nil` is a silent fail-open on the BGP egress filter path: a peer WITH export filters and no API server sends unfiltered, with no log, no metric, no error. R1 names two sites; the homing trace found **five**: `filter_ordered.go` (`runEgressPolicyChain`, R1's citation, exact), `:222` (`runEgressPolicyChainASN4`, the shared body `exportFilterForBody` actually reaches -- the one that matters), `egress_inject_filter.go` (`exportFilterForBody`'s own guard, with a second fail-open `facts == nil` fused in), `filter_ordered.go` (ingress, same defect), and `reactor.go` (`SetPluginServerAny` silently leaves api nil on a failed type assertion) | **Ruled by Thomas 2026-07-16: trace reachability first, then decide fix vs fail-closed.** Left unfixed in the source spec because the path was never traced to a producer and `ai/rules/evidence.md` forbids inventing a fix for an untraced path. The trace is now largely done and reframes the work: reachability is the WEAKER argument. The same condition already fails CLOSED with a Warn at `filter_chain.go` (marked `// fail-closed`) and `peer_initial_sync.go` -- but `policyFilterFunc` is only reached through `PolicyFilterChain` at `filter_ordered.go`/`:232`, both AFTER the early returns at `:196`/`:222`, so when api is nil the correct guard is UNREACHABLE, pre-empted by the wrong one. One package, one condition, two loud denials, one silent accept | `plan/spec-fixit-private-asn-leak-deferred-nil-api-fail-open.md` (skeleton; the trace is recorded there in full so nobody redoes it. Not reachable in a successfully-started reactor in either mode -- but standalone safety rests on an undocumented, incidental `r.mu` barrier over a genuinely inverted ordering: listeners bind at `reactor.go`/`:1012` BEFORE `startAPIServer()` at `:1017`. Reachability for `&Reactor{}` literals built outside `StartWithContext` is NOT established, and ~10 unit tests are exactly that: they pass while silently skipping the egress chain. **RESOLVED 2026-07-21:** the fused guard was split at all five sites (`filter_ordered.go` ingress `:139` + egress `:196`/`:222`, `egress_inject_filter.go` already landed under `1fb231afb`, `reactor.go` `SetPluginServerAny`); each miss now suppresses AND `slog.Warn`/`Error`s, matching the two loud siblings. RED-first unit tests then green; A-1 held so this was hygienic, not a live leak. The `r.mu` barrier over the inverted ordering `reactor.go` is NOT fixed -- recorded as R-2) | done |
| 2026-07-16 | spec-fixit-private-asn-leak (Known Limitations: EBGP prepend on originate) | Whether RFC 4271 S9.1.2 prepend applies to API-originated routes on an EBGP session. Flagged for Thomas there, deliberately unanswered: orthogonal to the private-ASN MUST and not traced to a producer | **Answered by Thomas 2026-07-16 with a design, not a yes/no:** *"we should have an 'auto-added' filter added to the chain of ebgp peer to add our ASN to the peer when sending and another one to remove local pref."* So prepend happens, but as an auto-added entry on the EBGP peer's export chain rather than a wire-path special case. `config/peers.go` `prependDefaultFilters` is the precedent -- it auto-prepends loop-detection entries, but to `ImportFilters` ONLY; the export chain is 100% user config today. Both filters are expressible in today's vocabulary with no new ops (`AttrModPrepend` on AS_PATH, `AttrModSuppress` on LOCAL_PREF, whose handler is already registered at `filter_delta_handlers.go`) | `plan/spec-fixit-private-asn-leak-deferred-ebgp-auto-filters.md` (skeleton; SEQUENCES against three in-progress specs that all stand on `wireu.rewriteASPathPrepend`, the seam this design retires: `spec-fixit-as4path-missing-on-rewrite` owns its inside, `spec-perf-next-1-ebgp-wire-lockfree` owns the `EBGPWire` cache that memoizes its output, and `spec-fixit-tombstone-ebgp-transitive` is adding per-destination EBGP logic into it. Ordering is Thomas's call and gates the spec leaving `skeleton`) | deferred |
| 2026-07-16 | spec-fixit-private-asn-leak (found while homing the EBGP-prepend ruling; NOT in that spec's scope) | **A live RFC 4271 S5.1.5 MUST NOT violation: LOCAL_PREF is not stripped on the forwarded EBGP egress path.** Enforced on ingress (`message/rfc7606.go`, discard from an external neighbor) and on origination (`update_build_plugin.go`, `rib/commit.go`, both IBGP-gated), but `grep -i localpref internal/component/bgp/wireu/*.go` returns ZERO non-test hits and no egress strip exists anywhere in `reactor/*.go`. So an **IBGP-sourced route carrying LOCAL_PREF, forwarded to an EBGP destination, keeps LOCAL_PREF on the wire**. Ingress discard hides this for EBGP-sourced routes only, which is why it has gone unnoticed. `attribute/builder.go` carries the comment "LOCAL_PREF (filtered at send time for eBGP)" above a function that appends it unconditionally -- the send-time filter it defers to does not exist | Not fixed on discovery: it is not `spec-fixit-private-asn-leak`'s subject (the RFC 6996 private-ASN MUST), that spec's code was already complete, and Thomas's auto-filter ruling gives it a proper home where it is a first-class AC rather than a drive-by. **No test covers it today** -- the destination spec's first deliverable is the RED functional test, since the finding is grep-established, not test-established (recorded there as assumption A-3, unvalidated) | **RESOLVED 2026-08-07: the violation is closed and the row was stale.** `applyFactsLocalPref` (`internal/component/bgp/reactor/forward_local_pref.go`) records `AttrModSuppress` on LOCAL_PREF for every destination `localPrefAllowedTo` refuses, which is every external peer, and it guards the filter-set case too through `modsTouchLocalPref`. It is WIRED on both forward rails: `reactor_api_forward.go` (`forwardUpdateCore`) and `forward_rs.go` (`reactorForwardRS`). The row's "no test covers it" is also stale: `internal/component/bgp/reactor/forward_local_pref_test.go` covers the unit, `test/plugin/local-pref-strip-ebgp.ci` covers the daemon. The grep the row quotes still returns nothing because the enforcement lives in `reactor/`, not `wireu/`. The prepend half of the same design stays live in the row above | done |

