# Deferrals: spec-fixit-rs-community-strip-arity

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-28 | spec-fixit-rs-community-strip-arity (RF-1, found by three independent reviewers over commit `4730deb84`) | `removeValues` (`internal/component/bgp/plugins/filter_community/handler.go`) scans the removal set once per retained value. Both operands are peer-controlled on the route-server path: `data` is the peer's own COMMUNITY attribute, `toRemove` is derived from that same attribute by `StripControlCommunities`, not from configuration. Measured 874-889 ms of CPU per destination peer for a 16383-value all-control attribute, multiplied by the fan-out. The arity fix removed the old `len(toRemove) != valueSize` guard, which was the defect AND, accidentally, the cost cap. A replacement must: deduplicate at the producer (`StripControlCommunities` already walks the payload once per UPDATE, and duplicates are what make a set representation pathological); hoist the set out of the per-destination loop, since `communityStripBytes` is fan-out-invariant while `buildModifiedPayload` runs per peer; and threshold on `min(len(data), len(set))` rather than on set size alone | Owner direction 2026-07-28: this handler is being replaced wholesale, so a half-finished cost trade is not worth shipping. A candidate fix was written and reverted; it is preserved at `backups/work-20260728-valueset-fix.patch`. The reviewers showed it was 326x better against the worst shape but 16.5x worse and 0 to 1 MB on a duplicate-heavy attribute, and that its regression guard was decorative (reordering the membership test restores the quadratic with the suite green, and the benchmark meant to catch it is executed by no gate)  **RESOLVED 2026-08-05.** The quadratic is gone, and it survived the handler rewrite that deleted `removeValues` itself: the per-value call moved to `removedByAny` and stayed inside the loop over source values. `newRemovalSet` (`internal/component/bgp/plugins/filter_community/handler.go`) now picks the membership representation ONCE, above that loop, and answers from a map above the threshold. The map collapses duplicates as it is built, which is the property the reverted candidate lacked. Threshold is on `min(source values, removal values)`, so a large removal set against a two-value attribute still scans. Mutation-verified twice: forcing the scan fails `TestRemovalSetIndexesOnlyAboveThreshold` at `both one above` and `the measured attack shape`, and thresholding on the removal count alone fails it at `large removals against a two-value attribute`. The guard pins the REPRESENTATION, not elapsed time, so it cannot go decorative and cannot flake on a loaded host. Proof: package green, `make ze-lint-changed` 0 issues, `make ze-plugin-test` 597 of 597 including `bgp-rs-community-strip-multi` and its fastpath sibling| `plan/spec-fixit-rs-community-strip-arity-deferred-removevalues-quality.md` | done |
| 2026-07-28 | spec-fixit-rs-community-strip-arity (RF-2 remainder) | Three stale `file:line` citations inside the `removeValues` doc comment: `handler.go` names `community.go` (now `:158`), and `:161` names `reactor_api_forward.go` and `forward_rs.go` (now `:643` and `:347`). No gate catches this class: `scripts/dev/spec-citation-check.py` scans only `plan/` and `plan/learned/`, and `scripts/dev/check_doc_links.py --design-only` follows only `// Design:` references, so comments in `.go` and `.ci` files are unchecked | They sit inside the reverted RF-1 hunk, so correcting them now would reintroduce part of a change that was deliberately backed out. The `.ci` half of RF-2 IS fixed and committed  **RESOLVED before 2026-08-05, verified today.** `removeValues` and its doc comment no longer exist: the handler plans retained runs instead. No `file:line` citation remains in the file| `plan/spec-fixit-rs-community-strip-arity-deferred-removevalues-quality.md` | done |
| 2026-07-28 | spec-fixit-rs-community-strip-arity (reviewer NOTE, test quality) | The only test of the refusal message is vacuous: `assert.Contains(t, out, "3")` (`internal/component/bgp/plugins/filter_community/handler_test.go`) matches the `slog` RFC 3339 timestamp regardless of the message, so deleting `"buffer-length", len(op.Buf)` from the handler leaves it green. `assert.Contains(t, out, "buffer-length=3")` is the real check. Separately, the map representation had no coverage at widths 8 and 12, because `TestRemoveValuesAllWidths` only ever passes two-value sets | Test-only, no behaviour impact today, and the function is being rewritten  **RESOLVED before 2026-08-05, verified today.** The assertions now name KEY=VALUE pairs (`buffer-length=3`, `attribute-code=8`, `value-size=4`) and carry a comment recording why the bare `Contains(out, "3")` matched the slog timestamp| `plan/spec-fixit-rs-community-strip-arity-deferred-removevalues-quality.md` | done |
