# Deferrals -- spec-rfcgate-1b-rfc7296-pilot

Source: `plan/spec-rfcgate-1b-rfc7296-pilot.md`. Format: `ai/rules/deferral-tracking.md`.

Every row below records the same owner decision, taken 2026-07-31: work package WP-9
("Configuration payload and remote access", phase list item 15) was SPLIT. The rows that
need the virtual-address-assignment feature move to `plan/spec-ipsec-remote-access.md`,
which already owns that surface and is named as its owner by
`internal/component/ike/engine/config.go:115`. The rows that are already conformant, and
the two live defects, stay in the pilot.

`ai/rules/deferral-tracking.md` prefers an existing spec that covers the topic over a new
deferral holder, so no `spec-rfcgate-1b-deferred-*` spec was created.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-01 | spec-rfcgate-1b-rfc7296-pilot | Land `RFC7296-3.13.1-3` (a system that wishes to indicate OPAQUE ports, but not ANY ports, MUST encode start 65535 / end 0). The ENCODER is implemented and unit-proven (`ipsec.PortSelector.Wire`, mutation 13 reddens it), but the row is NOT gated | Ze never wishes to indicate OPAQUE, so the MUST has no reachable antecedent. No dataplane backend can program an opaque-port policy exactly: the vendored netlink derives the port mask from the port value (`selFromPolicy`, vendor/github.com/vishvananda/netlink/xfrm_policy_linux.go), so an exact match on port 0 is inexpressible and would install as any-port -- WIDER than OPAQUE, which RFC 7296 Section 2.9 forbids. Ze therefore refuses OPAQUE at commit and narrows it away at negotiation. Tagging a test would claim a sender obligation production never exercises. `ai/rules/rfc-compliance.md` forbids a subagent classifying a row away: full compliance needs an OPAQUE-capable dataplane, and that is Thomas's call. The §3.13.1 ordinal is NOT stranded -- `-1` and `-2` landed, so `-3` stays free. RESOLVED 2026-08-01 by owner ruling. The row LANDS as encoder-proven. The refusal to emit OPAQUE is Ze's conformant behavior under `ai/rules/exact-or-reject.md` and not a hole, and the tag says so. It landed as `RFC7296-3.13.1-3` with a tagged pair over `TestPortEncodingFollowsSection3131` (`internal/component/ike/engine/ts_narrow_test.go`). Mutations 9 and 10 redden it | `plan/spec-rfcgate-1b-rfc7296-pilot.md` | done |
| 2026-08-01 | spec-rfcgate-1b-rfc7296-pilot | Prove `RFC7296-2.24-1` (tunnel encapsulators and decapsulators for all tunnel mode SAs created by IKEv2 MUST support the ECN full-functionality option) on the VPP backend | The row LANDED, proven on the XFRM backend, which is the default and the path that ships: no config leaf and no negotiated parameter reaches an ECN knob, and the netlink state Ze fills carries no general flags field, so `XFRM_STATE_NOECN` is unreachable and the kernel's propagation stands. It is NOT proven on VPP for one reason only: VPP cannot program a security association at all. All four IPsec messages declare a CRC of `00000000` and GoVPP resolves a message ID from name and CRC together, so every send returns `UnknownMsgError`. There is no VPP Child SA whose ECN behavior could be asserted until that is fixed. No `{gap}` and no `{not-applicable}` was written, because the obligation is met on the shipping path | `plan/spec-fixit-vpp-ipsec-inoperable.md` | deferred |
| 2026-08-01 | spec-rfcgate-1b-rfc7296-pilot | Prove `RFC7296-2.24-2` (tunnel encapsulators and decapsulators MUST implement the tunnel encapsulation and decapsulation processing specified in IPSECARCH to prevent discarding of ECN congestion indications) on the VPP backend | Identical to the `2.24-1` row above and recorded separately so neither id can go quiet behind the other. Proven on XFRM, unprovable on VPP while VPP programs no security association | `plan/spec-fixit-vpp-ipsec-inoperable.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-2.19-2` (the CP payload MUST be inserted before the SA payload). Needs a CP entry between AUTH and SAr2 in the responder's IKE_AUTH response chain | Owner decision 2026-07-31 splitting WP-9. The row needs the address-assignment feature, not a test. Ze builds no CP payload at all today | `plan/spec-ipsec-remote-access.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-2.19-3` (CP payloads MUST be inserted in the messages containing the SA payloads, when there are multiple IKE_AUTH exchanges). Needs CP handling at all three drop sites, including the post-EAP-Success response | Owner decision 2026-07-31 splitting WP-9. EAP is exactly that variation and Ze implements it, so the row is live rather than hypothetical | `plan/spec-ipsec-remote-access.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-2.19-5` (the responder MUST NOT send a CFG_REPLY when no CP(CFG_REQUEST) arrived first). Needs the fail-closed request guard that checks payload count, CFG type, attribute presence and profile policy | Owner decision 2026-07-31 splitting WP-9. Vacuously true today because Ze sends no CFG_REPLY. It becomes the feature's primary authorization guard, and `ai/rules/fail-closed-guards.md` governs it | `plan/spec-ipsec-remote-access.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-2.19-6` (IRAS MUST fail the request and end Child SA creation with FAILED_CP_REQUIRED when its configuration requires CP for an identity and the IRAC sent none). Needs a new YANG policy leaf, a resolved-flag policy lookup, the first use of notify 37, and a short-circuit before the Child SA install | Owner decision 2026-07-31 splitting WP-9. No config leaf expresses the policy, and `NotifyFailedCPRequired` (`internal/component/ike/wire/payload_notify.go:44`) has zero referents | `plan/spec-ipsec-remote-access.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-3.15.1-1` (only one netmask is allowed in request and response messages, and it MUST be used only with an INTERNAL_IP4_ADDRESS attribute). Needs send-side cardinality and pairing in the CFG_REPLY builder, and receive-side tolerance | Owner decision 2026-07-31 splitting WP-9. `ReadFrom` (`internal/component/ike/wire/payload_cp.go:65`) enforces no cardinality of any kind, and no consumer applies the rule | `plan/spec-ipsec-remote-access.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-3.15.1-3` (SUPPORTED_ATTRIBUTES MUST be zero-length within a Request). CONDITIONAL on owner item OI-3: the send half binds the requester, which Ze is not, so only the responder-answering half needs code. Attribute type 14 is not a declared constant | Owner decision 2026-07-31 splitting WP-9. Answering the query is strictly more compliance and needs no permission. Declining it is narrower and needs the owner's answer (`ai/rules/rfc-compliance.md`) | `plan/spec-ipsec-remote-access.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-3.15.1-4` (unrecognized or unsupported attributes MUST be ignored in both requests and responses). Needs the attribute dispatcher, and it is BLOCKED on the RESERVED-bit codec fix that stays in the pilot | Owner decision 2026-07-31 splitting WP-9. Nothing ignores and nothing acts, because there is no consumer. The negative half of its tagged pair IS the reserved-bit case, so the two specs are coupled here | `plan/spec-ipsec-remote-access.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-4-2` (an implementation supporting these requests MUST parse the CFG_REQUEST CP payload in the first IKE_AUTH message and recognize INTERNAL_IP4_ADDRESS or INTERNAL_IP6_ADDRESS). Must land BEFORE the pilot's `RFC7296-4-4`, see the id-allocation row below | Owner decision 2026-07-31 splitting WP-9. Vacuously conformant today because Ze does not support responding. This spec makes the antecedent true | `plan/spec-ipsec-remote-access.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-4-3` (an implementation supporting leasing MUST return a CFG_REPLY containing an address of the requested type). Needs type fidelity: an IPv6-only request against an IPv4-only pool gets no address of the wrong family. Must land BEFORE the pilot's `RFC7296-4-4` | Owner decision 2026-07-31 splitting WP-9. Vacuously conformant today, same antecedent as `4-2` | `plan/spec-ipsec-remote-access.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-1.7-1` (implementations MUST ignore proposals that have configuration attribute type 5, the old value for INTERNAL_ADDRESS_EXPIRY), landing as `RFC7296-1.7-3` because Section 1.7's high-water mark is 2. Implements the semantics corrected by erratum 5056: ignore the ATTRIBUTE, never the proposal. Also settles that `1.7-1` does NOT duplicate `3.15.1-4` (`plan/spec-rfcgate-1b-rfc7296-pilot.md:1289`) | Owner decision 2026-07-31 splitting WP-9. The constant for type 5 is not declared, and the dispatcher that must drop it does not exist | `plan/spec-ipsec-remote-access.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Honor Section 4 id-allocation order across the two specs. `check_id_allocation` (`scripts/dev/rfc_requirements.py:477-510`) refuses an ordinal at or below its section's high-water mark. Section 4 has THREE claimants and no mark yet: `4-1` (pilot WP-12, item 7), `4-2` and `4-3` (this deferral), `4-4` (pilot WP-10, item 14). Section 4 must land ASCENDING. So the pilot defers `4-4` until `4-2` and `4-3` land, or all four land in one commit | Owner decision 2026-07-31 splitting WP-9. The pilot's own phase list schedules WP-10 (item 14) before WP-9 (item 15), which strands `4-1`, `4-2` and `4-3` permanently. Measured 2026-07-31: no `RFC7296-4-*` id exists at HEAD or in the working tree, so no mark is set yet and the order is still free | `plan/spec-ipsec-remote-access.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Write `test/ipsec/ipsec-child-rekey-no-proposal.ci`, the end-to-end proof that a refused CHILD REKEY draws NO_PROPOSAL_CHOSEN and leaves the IKE SA established. WP-3's design named it | BLOCKED, not chosen. A static config cannot make IKE_AUTH succeed and the later rekey fail. A disjoint `esp-group` fails `selectResponderESP` at IKE_AUTH, so the SA never establishes. And `respondChildRekey` matches against `ESPGroup.Proposals[0]`, which `selectResponderESP` has already narrowed to the negotiated suite. Needs a way to change `esp-group` on a live SA, or a seam that narrows `ESPGroup` after establishment. The handler is covered by `TestErrRefusedChildRekeyIsAnswered`, which drives the real handler and reads the real datagram off a real UDP socket. Detail: `plan/handover/04-wp3-residuals.md`. REHOMED 2026-08-02 at pilot closure: the pilot is closed and its shard is deleted, so a row naming the pilot would take its work with it | `plan/spec-rfcgate-1b-rfc7296-pilot-deferred-child-rekey-refusal-proof.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Write `test/ipsec-interop/scenarios/19-error-notifications/`, the strongSwan proof that a refused CHILD REKEY draws NO_PROPOSAL_CHOSEN and the IKE SA stays up. WP-3's design named it | BLOCKED on the same fact as the row above, against strongSwan instead of a second ze. Also gated by `plan/spec-rfcgate-2-deferred-unrun-interop-trees.md`, because no automated caller runs that tree so the gate refuses a tag placed there. REHOMED 2026-08-02 at pilot closure, beside the ze-to-ze carrier it shares a blocker with | `plan/spec-rfcgate-1b-rfc7296-pilot-deferred-child-rekey-refusal-proof.md` | deferred |
| 2026-08-01 | spec-rfcgate-1b-rfc7296-pilot | Implement the piggyback of RFC 7296 Section 2.21.2. The responder sends IDr, CERT and AUTH beside the error notify on an IKE_AUTH Child SA failure, and keeps the IKE SA alive. WP-3 sends the notify where it once sent silence. It still sets `StateDead` | ANSWERED. The owner ruled on 2026-08-01 that the piggyback lands in a FOLLOW-UP spec, and not in the pilot, which is near closure. The question this row once held is closed, and the work it names is now homed. The clause is a MAY, so Ze stays conformant and no gated row is unproven. `RFC7296-2.21.2-2` binds the INITIATOR. It is proven in both polarities (`ai/RFC-REQUIREMENTS.md:3697`, `internal/component/ike/engine/rfc7296_errornotify_test.go:409`). The value is operational. Cost, re-verified 2026-08-01: THREE structural gaps, and only one sits in the IKE_AUTH code. `finishResponderEstablish` (`internal/component/ike/engine/responder.go:818`) is the only responder path to `StateEstablished` and requires an installed Child SA. `runEstablished` (`internal/component/ike/engine/established.go:56-64`) returns `errInvalidMessage` when the Child SA is nil. `handleCreateChildSAOwned` (`internal/component/ike/engine/inbound.go:441-448`) refuses every new-Child-SA request, so nothing can attach one later | `plan/spec-ipsec-auth-piggyback.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-2.22-1` (these payloads MUST NOT occur in messages that do not contain SA payloads). Needs the IPCOMP_SUPPORTED notify, CPI allocation, Child SA binding and both dataplane backends, none of which exist | Owner decision 2026-07-31: create a spec to fully implement IPComp, and do not implement it in this session. RFC 7296 Section 2.22 makes offering and accepting IPComp a MAY. The row is conformant today by non-participation, and the owner chose a real feature over four tests over an absence | `plan/spec-ipsec-ipcomp.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-2.22-2` (an implementation MUST NOT accept an IPComp algorithm that was not proposed). Needs the IPCOMP_SUPPORTED notify, CPI allocation, Child SA binding and both dataplane backends, none of which exist | Owner decision 2026-07-31: create a spec to fully implement IPComp, and do not implement it in this session. RFC 7296 Section 2.22 makes offering and accepting IPComp a MAY. The row is conformant today by non-participation, and the owner chose a real feature over four tests over an absence | `plan/spec-ipsec-ipcomp.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-2.22-3` (an implementation MUST NOT accept more than one IPComp algorithm). Needs the IPCOMP_SUPPORTED notify, CPI allocation, Child SA binding and both dataplane backends, none of which exist | Owner decision 2026-07-31: create a spec to fully implement IPComp, and do not implement it in this session. RFC 7296 Section 2.22 makes offering and accepting IPComp a MAY. The row is conformant today by non-participation, and the owner chose a real feature over four tests over an absence | `plan/spec-ipsec-ipcomp.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Implement and prove `RFC7296-2.22-4` (an implementation MUST NOT compress using an algorithm other than one proposed and accepted in the setup of the Child SA). Needs the IPCOMP_SUPPORTED notify, CPI allocation, Child SA binding and both dataplane backends, none of which exist | Owner decision 2026-07-31: create a spec to fully implement IPComp, and do not implement it in this session. RFC 7296 Section 2.22 makes offering and accepting IPComp a MAY. The row is conformant today by non-participation, and the owner chose a real feature over four tests over an absence | `plan/spec-ipsec-ipcomp.md` | deferred |
| 2026-08-01 | spec-rfcgate-1b-rfc7296-pilot | Give `RFC7296-3.13.1-3` a PRODUCTION SENDER. Make an exact match on port 0 expressible in the XFRM policy selector. Ze can then PROGRAM the OPAQUE port form instead of refusing it. The config-commit refusal (`checkPortProgrammable`, `internal/component/ike/ipsec/traffic_selector.go:305`) and the negotiation refusal (`programmableSelector`, `internal/component/ike/engine/ts_narrow.go:282`) both relax. This is NOT a duplicate of the `done` row above. That row records the checklist row LANDING as encoder-proven. This row carries the dataplane work the landing left open | Owner ruling of 2026-08-01, in two steps. Step one: the row lands as encoder-proven. Its tag states that Ze refuses OPAQUE at config commit and at negotiation, because the dataplane cannot express it exactly. Step two: the owner ruled that the derived mask must be fixed. He named the fix vendored territory. He required its own spec and a QEMU proof on a real kernel. He also required an upstream patch, if one is needed. His verbatim words are quoted in the Provenance block of the destination spec. The upstream patch is part of the ruling, and the destination spec must keep it. The defect is a modelling fault in a vendored library rather than a kernel limit. The kernel `xfrm_selector` carries `DportMask` and `SportMask` (`vendor/github.com/vishvananda/netlink/nl/xfrm_linux.go:193`, `:195`). The public `XfrmPolicy` declares neither (`vendor/github.com/vishvananda/netlink/xfrm_policy_linux.go:82-96`). `selFromPolicy` re-derives each one from its port value (`:119-124`), and `parseXfrmPolicy` reads neither back (`:318-333`). An exact port-0 match is therefore neither expressible nor observable. Upstream is unaware, verified 2026-08-01. `main` still carries the defect. `v1.3.1` is both the vendored version and the newest release. A repository search for `DportMask OR SportMask OR selFromPolicy OR "port mask"` finds no issue and no pull request about it. Ze stays CONFORMANT while this row is open, because refusal is correct when exact expression is impossible (`ai/rules/exact-or-reject.md`) | `plan/spec-ipsec-opaque-selector-port-mask.md` | deferred |

## Section 2.19 and Section 3.15.1 ordinals: read this before you allocate an id

The pilot's half of WP-9 landed `RFC7296-2.19-1`, `-2.19-4`, `-2.20-1`, `-3.15.1-2`, `-5`,
`-6` and `-7` at their Appendix A ordinals. The marks were measured first. No
`RFC7296-2.19-*`, `RFC7296-2.20-*` or `RFC7296-3.15.1-*` id existed at HEAD. No mark was set,
and every ordinal was free.

Those rows set the high-water mark to **4 for Section 2.19** and **7 for Section 3.15.1**.
`check_id_allocation` (`scripts/dev/rfc_requirements.py`) refuses a NEW id at or below its
section's mark. Five of the deferred rows above can therefore no longer take their
Appendix A ordinal:

Written as a list rather than a table on purpose. Every pipe-delimited line in this file
is read by the deferral gate as a six-cell row, and a row it cannot parse is a row it
cannot enforce (`scripts/dev/commit_helper.py`). Reference tables live as lists here.

- `RFC7296-2.19-2`, Appendix A ordinal -2: NOT free, the mark is 4. Needs -7 or higher.
- `RFC7296-2.19-3`, ordinal -3: NOT free, the mark is 4. Needs -7 or higher.
- `RFC7296-2.19-5`, ordinal -5: free.
- `RFC7296-2.19-6`, ordinal -6: free.
- `RFC7296-3.15.1-1`, ordinal -1: NOT free, the mark is 7. Needs -8 or higher.
- `RFC7296-3.15.1-3`, ordinal -3: NOT free, the mark is 7. Needs -8 or higher.
- `RFC7296-3.15.1-4`, ordinal -4: NOT free, the mark is 7. Needs -8 or higher.

This is the accepted cost of the package split. It is a renumbering rather than a loss,
because the obligations still land and stay gated. `plan/spec-ipsec-remote-access.md` must
allocate the five ids above their section mark. It must also record the Appendix A ordinal
each one came from, so a reader can still match the row to Appendix A. The same precedent
applies to `RFC7296-1.7-1`. Section 10 of `plan/handover/03-design-wp9.md` renumbers it to
`RFC7296-1.7-3`, because the mark for Section 1.7 is 2.

**Recompute the mark at the moment you land a row. Never hardcode it from this table**:

    git show HEAD:rfc/short/rfc7296.md | grep -o 'RFC7296-2\.19-[0-9]*' | sort -V | tail -1

~~Section 4 is untouched. The pilot's half of WP-9 took no Section 4 ordinal. So `4-1`, `4-2`,
`4-3` and `4-4` are all still free, and the ascending-order rule above still governs them.~~

**Superseded 2026-08-01. Section 4's mark is now 4, and `4-2` and `4-3` must renumber.**

Two commits moved it. `RFC7296-4-1` landed in `6ae8fb55d`, setting the mark to 1. WP-10 then
landed `RFC7296-4-4` at its Appendix A ordinal, which is legal (4 > 1) and sets the mark to 4.

`check_id_allocation` therefore refuses ordinals 2 and 3 from here on. The two rows deferred
to `plan/spec-ipsec-remote-access.md` above must land as:

- `RFC7296-4-2`, Appendix A ordinal -2: land it as `RFC7296-4-6` or higher.
- `RFC7296-4-3`, ordinal -3: land it as `RFC7296-4-7` or higher.

**Superseded again 2026-08-02. The mark is now 5.** The extraction walk landed
`RFC7296-4-5` for site `4:4` (the four-message IKE_SA_INIT and IKE_AUTH capability), which
is why the two numbers above moved up by one. This is the third time this paragraph has
been recomputed, which is the reason the instruction below says never to hardcode it.

This is a renumbering rather than a loss. Section 2.19 and Section 3.15.1 above took the
same cost. `plan/spec-ipsec-remote-access.md` must record the Appendix A ordinal each row
came from, so a reader can still match the row to Appendix A.

The ascending rule stated above was written when no Section 4 id existed at all. Commit
`6ae8fb55d` overtook it before WP-10 ran. WP-10 was therefore unable to honor it by landing
later.

**Recompute at the moment you land. Never hardcode from this table:**

    git show HEAD:rfc/short/rfc7296.md | grep -o 'RFC7296-4-[0-9]*' | sort -V | tail -1

## Work package WP-11 left the pilot whole

The four rows above are the entire WP-11 package (phase list item 13). No IPComp row stays
in the pilot. `plan/spec-ipsec-ipcomp.md` carries all four. Section 2.22 has no high-water
mark in `rfc/short/rfc7296.md`, so the four land at `-1` through `-4` in one commit. A
partial landing sets the mark and strands the rest.
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Express UDP encapsulation on the VPP dataplane. `vppBackend.InstallSA` (`internal/component/ike/dataplane/vpp.go`) hardcodes `UDPSrcPort: 0, UDPDstPort: 0` and never reads `p.UDPEncap`, and its hand-rolled `ipsecSAEntry` has no `Flags` field, so `IPSEC_API_SAD_FLAG_UDP_ENCAP` cannot be sent at all | The VPP backend programs NOTHING today: every `InstallSA`, `RemoveSA`, `InstallPolicy` and `RemovePolicy` declares a CRC of `"00000000"` and fails at the first request. The missing encapsulation flag is a symptom of that larger defect, which already has its own spec. WP-8 scoped its work to the Linux/XFRM dataplane Ze ships | `plan/spec-fixit-vpp-ipsec-inoperable.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Land `RFC7296-2.23-6` (reserved ordinal `-2.23-10`) and Appendix A's `RFC7296-2.23-8` (reserved `-2.23-11`): receive and process BOTH ESP forms at any time, and process encapsulated ESP with no NAT detected | MEASURED blocker, not a scope choice. `TestEncapKernelBindsOneESPFormPerState` shows a Linux XFRM inbound state accepts bare ESP or UDP-encapsulated ESP, never both. Two states on one SPI do not help either. CORRECTED 2026-08-02: that part was reasoning from the state lookup key, and it is now measured, with a different mechanism. `TestEncapTwoStatesOneSPI` records that the kernel REFUSES to install the second state (`file exists`), both with identical addresses and with a differing source, because the uniqueness key and the lookup key are the same tuple. The second state never exists, so no lookup ever chooses between two. Full compliance is on the table, so `ai/rules/rfc-compliance.md` reserves the decision for Thomas. Raised as OR-WP8-4. No `{gap}` and no `partial` was written. Writing one IS the decision. RESOLVED 2026-08-01 by owner ruling on OR-WP8-4. Both rows LAND, gated and recorded as a platform limit. Ze receives both ESP forms across its SAs, and it chooses the form per SA from its NAT verdict and the IKE SA's port. Either condition alone selects the encapsulated form. A form change on one established SA is not handled, and `docs/features/rfc-status.md` says so plainly. The rows landed as `RFC7296-2.23-10` and `-2.23-11` with tagged pairs in `internal/component/ike/engine/rfc7296_natt_bothforms_test.go`. Mutations 6, 7 and 8 redden them | `plan/spec-rfcgate-1b-rfc7296-pilot.md` | done |
| 2026-08-01 | spec-rfcgate-1b-rfc7296-pilot | Lift the platform limit that bounds `RFC7296-2.23-10` and `-2.23-11`, so ONE established Child SA receives both ESP forms at any time. Today one XFRM inbound state carries one encapsulation decision (`internal/component/ike/engine/child.go:249`, applied to both directions at `:348-352` and `:376-380`), so a peer that changes form on an established SA is not served | Step TWO of the owner's two-step ruling of 2026-08-01 on OR-WP8-4. Step one landed both rows in the pilot with tags recording a measured platform limit, and the row above records it as `done`. Step two creates this destination spec, so the limit does not become permanent (`ai/rules/no-parking.md`: a recorded limit is not an addressed limit). This row is NOT a duplicate of the row above. That row's work was the LANDING, which is complete. This row's work is the residual capability, which nothing owned until now. Ze is CONFORMANT-AS-BOUNDED today and this is not an outstanding violation: both rows are gated in both polarities and the bound is disclosed at `docs/features/rfc-status.md:229`. Four routes are open and none is measured, so the destination spec is a skeleton whose phase 1 is evidence | `plan/spec-ipsec-esp-dual-form-receive.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Meet the source-ADDRESS half of `RFC7296-2.11-3` on a multi-homed host. Both IKE listeners bind the wildcard by default. `WriteToUDP` therefore lets the route table choose the source address. `pkt.LocalAddr` is captured and no sender reads it | WP-8 implemented and tagged the PORT half only. Its tag says so. The address half needs `IP_PKTINFO` plus a `sendmsg` path, or one socket per local address. Both change listener lifecycle. The choice is the owner's (OR-WP8-1). REHOMED 2026-08-02 at pilot closure: the pilot is closed and its shard is deleted, so a row naming the pilot would take its work with it | `plan/spec-rfcgate-1b-rfc7296-pilot-deferred-ike-source-address.md` | deferred |
| 2026-07-31 | spec-rfcgate-1b-rfc7296-pilot | Fix defect 2 of `plan/spec-fixit-ike-responder-natt-port-float.md`: `handleResponderEAP` sets `StateDead` on a retransmitted IKE_AUTH instead of replaying the cached response (RFC 7296 Section 2.1) | WP-8 subsumed defect 1 of that spec and NARROWED it rather than closing it. Defect 2 touches none of WP-8's producers and has no row among the pilot's 113, so absorbing it would have shipped an untagged protocol fix (OR-WP8-3) | `plan/spec-fixit-ike-responder-natt-port-float.md` | deferred |

## Two items the design documents name, and neither is deferred

`plan/handover/03-design-wp10.md` (R-WP10-16) and `plan/handover/03-design-wp7.md` (R-WP7-6)
each discuss whether a finding is in scope. Both are recorded here as IN SCOPE, so no reader
mistakes the discussion for a deferral.

All three are FIXED inside this pilot. They are listed rather than tabled for the reason
given above: a pipe-delimited line here is read as a deferral row.

**1. The `dh-group` leaf accepted groups 1 to 31 with no implementability gate, while only
14, 19 and 20 exist.** FIXED 2026-07-31, in WP-10, inside this pilot. It surfaced while
`RFC7296-3.3.4-1` was established, and `ai/rules/no-parking.md` makes finding it the reason
to fix it. `DHGroupImplemented` (`internal/component/ike/ipsec/algorithm_support.go`) now
gates it from `parseIKEProposal`, mirroring `EncryptionImplemented` and `HashImplemented`.
Proven by `TestIKESuitePolicyRejectsAnUnhonourableSuite` row 3.

**2. `storeRemoteCerts` accepted an UNBOUNDED certificate chain on receive, while
`buildCertPayloads` capped the send side at two.** FIXED 2026-08-01, in WP-10, inside this
pilot. `certificate-count` (default 4, the figure RFC 7296 Section 3.6 names) now bounds
BOTH directions. `storeRemoteCerts` (`internal/component/ike/engine/cert_payload.go`)
REFUSES an over-long chain and stores nothing, so a refused message leaves the SA empty
rather than half-filled. The send cap is gone: `CertificateEntry.RawIntermediates` is a
slice, and `ValidateCertificateChains` refuses at commit a peer whose count is smaller than
its PKI chain. Proven by `TestCcnCertificateCountReachesFourInBothDirections`,
`TestCcnCertificateCountIsBoundedAndConfigurable` and
`TestCcnOverlongChainKillsTheSAOnBothRoles`.

**3. Traffic selectors had no config surface, so there was nothing for `RFC7296-2.9-2` to
narrow against.** FIXED 2026-08-01, in WP-7, inside this pilot. The policy IS the antecedent
of the row. Narrowing written against a hardcoded policy is dead on arrival.

## Findings measured on 2026-08-02, homed at closure

Seven findings surfaced while the pilot was closing. Thomas ruled on 2026-08-02 that the
codebase carries too many known issues to fix each one where it is found, so each gets a
home and a row rather than a fix in this session. Every destination below was confirmed to
exist on disk on 2026-08-02. The rows carry no header of their own: a header line would be
parsed as a six-cell row whose Status is not terminal.

| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Fix IPsec interop scenario `08-responder-eap-mschapv2`, which is RED. It fails at EAP identity inside IKE_AUTH, before any Child SA exists | MEASURED pre-existing on 2026-08-02: a ze built at HEAD and a ze built from the working tree fail identically, so the pilot did not cause it and did not mask it. The destination spec ALREADY owns this: it names this scenario as its own AC-4 proof, and its defect 2 is exactly the mechanism a code read implicates. `handleResponderInbound` (`internal/component/ike/engine/responder.go`) passes EVERY IKE_AUTH straight to `handleResponderEAP` in its `StateEAPInProgress` arm, with no retransmit classification, while its `StateEstablished` arm explicitly replays a cached response. So a retransmitted EAP-Response/Identity is REPROCESSED: the session is already past identity, the MSCHAPv2 method sees an Identity type it does not expect, and the responder answers a successful identity round with EAP-Failure and marks the SA dead. Ze DOES support EAP-MSCHAPv2 in both roles (`NewEAPSession`, `internal/component/ike/engine/eap_auth.go`), so this is not a missing feature. The row at line 111 above already defers that same defect 2 to the same spec; this row records the interop RED as its measured symptom, so the scenario is not mistaken for untested infrastructure. The mechanism is READ, not measured against a capture: a packet capture showing a single Identity response on the wire would overturn it | `plan/spec-fixit-ike-responder-natt-port-float.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Fix IPsec interop scenario `10-clear-reestablish`, which is RED because `ze cli -c "clear vpn ipsec sa"` exits 1 | NOT an IKE defect: the handler `handleClearIPsecSA` is never reached, because the client returns before an SSH connection is constructed. The EARLIER diagnosis is WRONG at its root and is superseded, verified 2026-08-02. It named `internal/component/ssh/client/client.go` `readCredentials` failing against `ZE_STORAGE_BLOB=false`. That path does not exist (`internal/component/ssh/` is the SSH server; the function is at `internal/core/ssh/client/client.go:302`), and `readCredentials` never reads `ze.storage.blob` at all, whose only producing consumer is `resolve.Storage` selecting the daemon CONFIG backend. The real cause is that nothing ever provisioned an SSH credential: the lab runs no `ze init` and sets no username or password, and scenario 10's `ze.conf` carries no `system` block, so no login user exists either. `readCredentials` is a CORRECT fail-closed guard and must not be weakened. Scenario 10 is the only `ze cli` caller in the whole interop tree, which is why no other scenario shows it. A NEW spec was created because no spec owned this; the 2026-07-19 row in `plan/deferrals/fixit-ipsec-clear-reestablish.md` deferred the scenario to CI and is ANSWERED by this measurement rather than duplicated | `plan/spec-fixit-ipsec-interop-cli-credentials.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Report two strongSwan 5.9.14 defects upstream, and remove Ze's workaround when upstream ships a fix. Both sit in `write_certificate_authorities` (`src/libtls/tls_server.c`): the trust-anchor enumerator is called with a hardcoded `KEY_RSA` key type, so an EC CA is never enumerable; and the extension body is written unconditionally, so an empty list is emitted as `002f 0002 0000` | The peer is wrong, not Ze. RFC 8446 Section 4.2.4 declares `DistinguishedName authorities<3..2^16-1>`, so 3 octets is the shortest legal list and a zero-length list has no legal encoding. Go's `crypto/tls` refuses it in `certificateRequestMsgTLS13.unmarshal` and answers a decode error, which reaches Ze as a TLS decode failure. Ze already works around it by using an RSA CA in scenario `06-eap-tls13`, and both the reasoning and the 2026-08-01 measurement are recorded in that scenario's `pki/gen-pki.sh` and `strongswan.conf` header comments. What had NO home is the upstream report, which is why a spec exists rather than a comment: the precedent is `plan/spec-ipsec-opaque-selector-port-mask.md`, which carries a vendored-netlink defect, its minimal patch and the obligation to send it. The destination spec carries ready-to-file text for BOTH issues, and a stated removal condition so the RSA CA does not become permanent by default. Whether either defect survives on strongSwan `master`, and whether an upstream report already exists, are BOTH unchecked and are recorded as assumptions to resolve before filing. NOT filed yet, so this row is live: the report IS the deliverable | `plan/spec-fixit-strongswan-tls13-certreq-authorities.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Remove the dead `ze_has_xfrm` helper from `test/ipsec-interop/scenarios/02-ipsec-bgp-redistribute-frr/check.py` | ALREADY REMOVED, in commit `1963345b4`, and verified absent from the live file on 2026-08-02. It had ZERO callers for its whole life: the surviving copies are all under `tmp/` and a worktree. Its logic was also always-true in practice. The guard read "XFRM absent OR 'not supported' absent", an OR, so it returned false only when the output held the uppercase literal and the lowercase phrase together, which `ip xfrm state` never produces, and its exception arm was unreachable because the helper it called swallows failures and returns a string. Recorded so the finding is not re-raised as live. Nothing remains to do | `1963345b4` | done |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Reconcile `plan/spec-fixit-child-sa-rekey-policy.md` with the code, then finish its residual work. Nine concrete items, listed in the spec's own "Audit 2026-08-02" section | The spec is NOT complete and must NOT be closed, although the tunnel-level defect it describes IS fixed and interop-proven. Commit `1963345b4` added the spec file and the fix together, and the fix took the alternative the spec's Key Design Decisions section explicitly REJECTS. There is no session-owned policy record. What shipped is an upsert (`netlink.XfrmPolicyUpdate`, `internal/component/ike/dataplane/xfrm_linux.go:205`, with no `XfrmPolicyAdd` left in production code) plus a shared-selector guard on removal (`samePolicySelector` and `removeChildSAExcept`, `internal/component/ike/engine/child.go`). So AC-1 is literally FALSE today: `installChildSA` still calls `InstallPolicy` unconditionally at `child.go:467` and `:492`, while AC-1 asserts no such call is made. AC-4's second clause is false by design. AC-5 has no test at all, which leaves assumption A-1 unvalidated and still load-bearing, because the upsert design has ONE policy serve successive states. A residual hazard was also read but NOT measured: two pending-child teardown paths in `engine/established.go` remove with no `keep`, so a pending child sharing the live child's selector can strip the LIVE pair's policy, which is the bug class AC-3 fixes on a path the fix did not reach | `plan/spec-fixit-child-sa-rekey-policy.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Finish phase 5 of `plan/spec-ipsec-esp-dual-form-receive.md`, and either land the AC-4 test or narrow the public claim that currently outruns it | The spec is NOT complete. Phases 1 to 4 landed, including rekey inheritance, and AC-1, AC-2, AC-3, AC-6 and AC-7 are proven while AC-5 is not-applicable by measurement and says so. AC-4 has NO proof of any kind. The finding that matters is not the missing test but the DISCLOSURE: `docs/features/rfc-status.md` states the platform limit is lifted and that a peer changing ESP form on an established SA is served, while the test that exists measures one state accepting both forms and never changes form on an established SA. Necessary, not sufficient. Either `test/ipsec/ipsec-esp-form-change.ci` lands or that sentence narrows to what is measured; leaving a public claim ahead of its evidence misleads a reader rather than merely leaving work open. The spec names FIVE residuals, not the two its own summary suggests, and its declared deferral shard did not exist, so its Goal Gate "Deferral shard resolved" could not be evaluated at all. The shard was created on 2026-08-02 with the three genuinely-deferred rows; the rest stay live in the spec | `plan/spec-ipsec-esp-dual-form-receive.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Settle AC-4 and AC-5 of `plan/spec-rfcgate-2-deferred-unrun-interop-trees.md` by measuring whether a GitHub `ubuntu-latest` runner provides the `l2tp_ppp` and `pppoe` kernel modules | The destination spec ALREADY owns both, they are its only live ACs, and two live rows in `plan/deferrals/rfcgate-2-evidence.md` already name it. This row adds the EVIDENCE the blocker was waiting on. Measured 2026-08-02 on the dev host: `pppoe` and `/dev/ppp` are PRESENT, and `l2tp_ppp` is ABSENT. That settles neither AC by itself, because the dev host is not the runner, but it does show the two modules must be judged separately rather than as one question. Nothing in the repository asserts what a runner image carries: a search of the workflows, the make files and the evidence scripts finds privilege and timing claims about `ubuntu-latest` but not one module claim. What would settle it is a single privileged run of the two labs' existing strict preflight probes on `ubuntu-latest`, which already modprobe and then check the module and device paths. The two QEMU targets do NOT sidestep the question: they run the evidence scripts rather than the trees' own checks, so crediting them would award a tier the carrier has not earned, and neither target has an automated caller either. One more gap: the destination spec forward-references an owner-question section in four places and that section does NOT exist, so the blocker's stated escape route is unwritten | `plan/spec-rfcgate-2-deferred-unrun-interop-trees.md` | deferred |

## Fourteen more findings measured on 2026-08-02, homed the same way

A second closing pass found fourteen more. Thomas's ruling of 2026-08-02 applies unchanged:
the codebase carries too many known issues to fix each one where it is found, so each gets a
home and a row rather than a fix in this session.

They were homed as SIX destinations rather than fourteen. Findings sharing a root cause were
grouped, and `ai/rules/deferral-tracking.md` prefers an existing spec over a new holder, so
two existing specs took five of the fourteen. Four new skeleton specs were created for the
rest, each from `plan/TEMPLATE.md` at `Status | skeleton`. Every destination was confirmed
to exist on disk on 2026-08-02, and every finding was verified against the producing
function before its row was written.

The rows carry no header of their own, for the reason the earlier block gives: a header line
would be parsed as a six-cell row whose Status is not terminal.

| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Fix `RemoveSA` (`internal/component/ike/dataplane/xfrm_linux.go:163`) leaking an ESP form entry. It returns the `netlink.XfrmStateDel` error at `:169`, above the `b.espForms.Forget(spi)` call at `:175`, so a failed state delete never forgets the SPI and a host-wide raw ESP socket outlives the last tunnel | READ, not measured. The reachable trigger is the documented double-remove in `internal/component/ike/engine/delete.go`: the second remove finds no state, the delete fails, and that is the ordinary case rather than an exotic one. Grouped with two sibling leaks under one destination because all three are one rule broken three ways, a resource acquired on the way in that is not released on every way out. Phase 1 of the destination spec is the measurement, so no fix lands on a read alone | `plan/spec-fixit-ike-resource-lifetime-leaks.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Fix `p.Run` (`internal/component/ike/engine/register.go`) leaking four node-wide XFRM bypass policies. It returns 1 at `:451`, above the `removeIKEBypass(dataplane.Get(), log)` call at `:483`, so every error exit above that line leaves the IKE control-plane bypass installed after the process is gone | READ, not measured. The policies are node-wide rather than per-peer, so the leak is not bounded by peer count, and a bypass that survives its installer keeps IKE traffic out of IPsec processing for a daemon that is no longer running. Risk R-3 of the destination spec guards the fix: removal must not strip a bypass another peer still needs, and that must be settled before the fix lands | `plan/spec-fixit-ike-resource-lifetime-leaks.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Pair the initiator `SAUp` with an `SADown`. `runInitiator` emits `SAUp` at `internal/component/ike/engine/fsm.go:238` and then returns `ps.runEstablished(...)` at `:249`, and nothing on that path emits the down event | Established by reading both paths. `runResponder` is the in-tree control and does it correctly, emitting `SAUp` at `:305` and `emitSADown(bus, sa, log)` at `:324` once `runEstablished` returns. The initiator has the emit and not the pair, so this is an omission rather than a design choice. The reconnect-on-peer-Delete path re-enters `runInitiator`, so the imbalance repeats once per reconnect cycle rather than once per process, and any subscriber counting SAs up against SAs down drifts without bound | `plan/spec-fixit-ike-resource-lifetime-leaks.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Audit the three IKE_AUTH rollback sites in `internal/component/ike/engine/responder.go` (`:810`, `:821`, `:846`), each calling `removeChildSA` with no `keep`, so a rollback can remove a policy a surviving pair still answers to | ALREADY OWNED, and recorded so the finding is not raised a third time. The destination spec's own "Audit 2026-08-02" residual list already carries the line "Audit the three responder rollback sites for the same hazard", so no new spec was minted and nothing was appended for this item. `removeChildSA` is `removeChildSAExcept(child, nil, ...)` (`internal/component/ike/engine/child.go:599`), which is the bug class AC-3 of that spec fixes, on a path the shipped fix did not reach. The row stays live because the audit has not been done | `plan/spec-fixit-child-sa-rekey-policy.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Fix `samePolicySelector` (`internal/component/ike/engine/child.go:585`) flipping orientation on a peer-initiated rekey. `selectorPort` (`:725`) chooses the selector side from `child.LocalIsInitiator` at `:732`, and `newRekeyedChild` resets that field, so one selector compares unequal across the rekey | REFUTED as a policy drop, and real as an ORPHANED policy. An incorrect false makes `removeChildSAExcept` drop a policy it should keep, which the upsert then reinstalls, so no traffic is lost. What remains is a policy left behind because the guard did not recognise the pair that still needs it. Exercising it needs a peer-proposed port narrowing, which no current test produces. READ, not measured. Appended to the destination spec's audit section on 2026-08-02 | `plan/spec-fixit-child-sa-rekey-policy.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Reconcile Child SA policy removal with installation. Install passes `SPParams` carrying `Proto`, `IfID`, `ReqID`, `Action` and `Priority` (`internal/component/ike/dataplane/dataplane.go:296`), while removal is the three-argument `RemovePolicy(src, dst *net.IPNet, dir SADir)` (`dataplane.go:400`), carrying no ports, no protocol and no interface id | PRE-EXISTING and not a regression, recorded because the new `keep` design depends on it. A guard deciding that a selector is still in use is only as good as the identity the removal call can express, so a removal keyed on fewer fields than the install can remove a policy the guard meant to protect. Appended to the destination spec's audit section on 2026-08-02 | `plan/spec-fixit-child-sa-rekey-policy.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Settle whether `resolvePendingAfterOwnerLoop` (`internal/component/ike/engine/fsm.go:370`, called at `:330`) strands a pending child. It returns early when `pendingSA` is nil, before it can consider a non-nil `pendingChild` holding the only claim on a shared policy | READ, not measured, and the destination spec says so in those words. What would settle it is a case producing a nil `pendingSA` beside a non-nil `pendingChild`. If that combination cannot arise the finding dissolves, and proving it cannot arise is the cheaper answer, so this row may close on evidence rather than on code. Distinct from the `reapStalePending` and `cleanupPendingSA` line already in that spec's residual list, which names different functions. Appended to the destination spec's audit section on 2026-08-02 | `plan/spec-fixit-child-sa-rekey-policy.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Close the lint gate hole that hides every `integration`-tagged Go file from `make ze-lint`. `.golangci.yml` sets `build-tags` to `ze_core` plus every feature-gate tag and has no `integration` entry, so golangci-lint never loads those packages and reports nothing about them | MEASURED over tracked files on 2026-08-02: 68 tracked `.go` files carry `//go:build integration` across about 25 packages, and 56 of them are named `*_integration_linux_test.go`. The largest clusters are `internal/component/iface` at 15, `internal/component/ike/dataplane` at 7 and `internal/plugins/iface/netlink` at 5. This is a gate hole rather than a style nit, because `ai/rules/qemu-testing.md` makes those files the mandatory home for kernel-facing tests, so the least-linted code in the tree is the code that talks to netlink, nftables and XFRM. A NEW spec was minted rather than folding this into the IKE evidence spec, because the fix is repo-wide tooling with a different gate and a different owner. The obvious one-line fix is wrong twice, which is the substance of the spec: that `build-tags` list is GENERATED by `scripts/codegen/feature_tags.go` (header at `:6`, list at `:53` and `:56`, rewrite at `:68`), so a hand edit is reverted by `make generate` and fails the `dep_audit.py --check` drift check in between, and `integration` is a test capability rather than a compile-out feature so it must not enter `feature-gates.txt`. The reported count of about eleven accumulated findings is UNVERIFIED, and phase 1 of the destination spec measures the real one | `plan/spec-fixit-lint-blind-to-integration-tag.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Write a test that reddens when the RFC 2759 one-octet guard is reverted. `mschapv2Method.Process` (`internal/component/ike/eap/eap_mschapv2.go:82`) refuses an empty response at `:89`, guarding the `response.TypeData[0]` read at `:93` | MEASURED on 2026-08-02: reverting the guard leaves the whole `eap` package green, so no test drives an MSCHAPv2 response with empty `TypeData` and nothing reaches the panic the guard prevents. Grouped with three sibling items under one destination because all four are the same failure, a check that reports green whether or not the behavior it names is present, which is the discrimination standard in `ai/rules/interop-and-goal-validation.md`. The guard itself is CORRECT and must not change: this row is missing coverage, not a defect | `plan/spec-fixit-ike-test-discrimination.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Write a test that reddens when the `pendingRekey.clear()` defer is reverted. `runEstablished` (`internal/component/ike/engine/established.go:56`) clears the session rekey slot in a deferred call at `:71` | MEASURED on 2026-08-02: reverting the defer leaves the whole `engine` package green. This is UNPROVEN HARDENING rather than a false claim, and the distinction is recorded so nobody narrows a compliance row for it. `RFC7296-2.12-1` is tagged over `forgetKeys` and those tags describe honestly what they prove, so no public claim outruns its evidence here. What is missing is a test for the clear itself | `plan/spec-fixit-ike-test-discrimination.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Extend IPsec interop scenarios `04-eap-tls` and `06-eap-tls13` to assert that ESP is accepted. Both `check.py` files stop at `wait_xfrm_sa` for each container and then call `log_pass`, which proves the SAs were installed and never that traffic crosses them | READ, not measured. Scenario `07-responder-psk` is the in-tree control and shows the fix already exists: it pings across the tunnel and then reads the ESP counters, with a comment recording which counter advances and which does not. Scenario `02` follows the same pattern by carrying BGP routes through the tunnel. So this is a back-fill of a pattern the tree already has rather than a new technique. Check each scenario for an `RFC requirement:` tag BEFORE editing, because the rfc-tagged-test hook blocks a behavior change to a tagged carrier and only the user may authorize one | `plan/spec-fixit-ike-test-discrimination.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Record, where a probe author will read it, that a loopback egress moves NO xfrm counter, so any future QEMU probe built on `lo` is a blind instrument and needs a positive control known to move the counter | CONSTRAINT rather than a defect: nothing in the tree is wrong today, and the row exists so the next author of an xfrm probe meets it before choosing an interface. It is the absence-assertion vacuity trap named in `ai/rules/interop-and-goal-validation.md`, where deleting the mechanism leaves the same zero reading. Recorded in the destination spec, which owns the xfrm probe surface, and deliberately NOT written into `ai/rules/qemu-testing.md`: that rule is arguably the better long-term home, but editing it is a rule change and belongs to the owner. Risk R-3 of the destination spec tracks the danger that the constraint dies at closure, and routes it to the learned summary at minimum | `plan/spec-fixit-ike-test-discrimination.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Make `vppBackend.InstallPolicy` (`internal/component/ike/dataplane/vpp.go:141`) honor the `SPParams.Action` and `SPParams.Priority` contract. It hardcodes `Priority: 100` at `:153` and `Policy: 3` at `:158`, reading neither field | The destination spec ALREADY owns the function and already names the hardcoded `Policy: 3` as its SA-id gap, so no new spec was minted. `Action` and `Priority` were added to `SPParams` after that spec was written, and an "Added 2026-08-02" subsection now records them there. Both fields carry explicit instructions in their own declarations: `Action` (`dataplane.go:310`) states that a backend which cannot express `SPActionBypass` MUST reject the install rather than fall back to protecting, which is exactly what a hardcoded PROTECT does and what `ai/rules/exact-or-reject.md` forbids, and `Priority` (`dataplane.go:344`) names `PriorityIKEBypass` at 100 and `PriorityChildSA` at 2000, so a hardcoded 100 gives every Child SA policy the IKE bypass precedence and inverts the ranking those constants exist to express. LATENT today, masked only by `vppUnsupportedSelector(p)` running first at `:142`, and that mask disappears the moment selector support widens | `plan/spec-fixit-vpp-ipsec-inoperable.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Answer the owner question on `RFC2661-24.10-1`, then align the code and the published claim. An SCCRQ carrying Assigned Tunnel ID 0 is dropped in silence, while the requirement says reject with StopCCN | ESCALATED to the owner, not classified, per `ai/rules/rfc-compliance.md`. Measured 2026-08-02 and worse than first framed. `parseSCCRQ` (`internal/component/l2tp/tunnel_fsm.go:321`) rejects a zero Assigned Tunnel ID, and `Reactor.handlePacket` (`internal/component/l2tp/reactor.go:353`) logs at Debug at `:355` and returns at `:357`, sending nothing. `rfc2661` is ENROLLED (`rfc/enrolled.txt:162`) so the MUST is gated, the requirement at `rfc/short/rfc2661.md:479` names SCCRQ AND SCCRP, and the published coverage at `:433` claims the Reactor rejects with StopCCN Result Code 2. But BOTH tags sit on the SCCRP half (`internal/component/l2tp/tunnel_initiator_test.go:107` and `:135`, ledger row `ai/RFC-REQUIREMENTS.md:1361`), so the SCCRQ half is neither implemented nor proven while the id reads as fully gated in both polarities. The ledger counts polarities per requirement id, so it cannot see one id covering two message types. Three routes are live and a subagent may choose none of them, because each either lowers what Ze owes or adds an unauthenticated reply surface: send StopCCN and become a spoofable reflector, keep the drop and narrow the published claim under RFC 2661 Section 4.4.3, or bound the reply per source. A reproducer exists in `test/draft/l2tp/`. No `{gap}`, `partial` or `{not-applicable}` was written, because writing one IS the decision | `plan/spec-fixit-l2tp-sccrq-tunnel-id-zero.md` | deferred |

| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Close `RFC5216-2.1.3-1` honestly: the fix that makes `tlsMethod.Process` read `handshakeError` and send the fatal TLS alert (`internal/component/ike/eap/eap_tls.go`) satisfies the requirement, but three records still call it a gap (`rfc/short/rfc5216.md`, `docs/features/rfc-status.md`, `rfc/enrolled.txt`) and NO test carries the tag. The requirement's own direction, a peer sending the alert that rejects the SERVER, is untested | Closing the row claims compliance nothing proves, so the tag must land first. `check_gap_count_agreement` also needs the spelled count corrected when it closes | `plan/spec-fixit-ike-test-discrimination.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Tag the espform receiver test against `RFC7296-2.23-10`. `espform_run_linux_test.go` drives the real receiver and three mutations of `run` kill it, but it carries no `RFC requirement:` tag, while the tagged proof (`engine/rfc7296_natt_bothforms_test.go` `TestBfmBothESPFormsAreReachable`) asserts only the `UDPEncap` boolean on installed SA params and never drives the receiver | The strongest evidence for the row is invisible to `ze-rfc-check`, and the evidence it does count is flag-only | `plan/spec-fixit-ike-test-discrimination.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Reach or retire `TestEAPTLSProcessRefusesUnboundedPeerBuffer`. It sets `finished=true, err=nil, handshaked=false`, and `runTLSServer` sets err OR handshaked on every exit, so the state it drives cannot occur. After the `handshakeError` fix the failed path terminates in the round it appends, capping `peerBuf` at 2x64 KiB | Reasoned, not measured. The 128 KiB ceiling is defense in depth and should stay; it must not be credited with closing the live unbounded-growth path, which `handshakeError` closes | `plan/spec-fixit-ike-test-discrimination.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Assert the two length fields of `writeESPForm` from a fixture rather than from the producer. `TestESPFormRunRepresentsWatchedSPI` computes `want` by calling `writeESPForm`, so `bytes.Equal` cannot fail for any change to it; a TTL mutation survived at package scope | No merge-gate regression: the untagged sibling `espform_test.go` already covers those fields, and all three mutations of the diffed `run` were killed. It is the oracle shape this session kept finding, in a new test | `plan/spec-fixit-ike-test-discrimination.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Pass `firstSharingSelector(old, child, getPendingChild())` on the superseded arm of `closeDesignatedChildSAs` (`internal/component/ike/engine/delete.go`), which still passes a bare `child` | Harmless today, because a non-nil `supersededChild` implies a sharing live child, but it is inconsistent with the arm beside it that was just fixed. An invariant holding by accident is the shape that produced the original defect | `plan/spec-fixit-child-sa-rekey-policy.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Close a Child SA when a peer Delete names the PENDING child's SPI. It falls to the `default:` arm of `closeDesignatedChildSAs` and closes nothing, while RFC 7296 Section 1.4.1 says the recipient MUST close the designated SAs | A gated MUST with a reachable path that does nothing. Found while fixing the live-child arm of the same function | `plan/spec-fixit-child-sa-rekey-policy.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Prove `samePolicySelector`'s protocol and two port terms (`internal/component/ike/engine/child.go`). Deleting any of the three leaves every test green | The `spdDP` fake keys only on source, destination and direction, so it cannot express the difference the real kernel selector makes. A guard whose terms no test can distinguish is three unproven conditions | `plan/spec-fixit-ike-test-discrimination.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Decide what two Child SAs differing only in `Mode` should do. They share one kernel policy whose template belongs to whichever installed last | Tunnel and transport mode install different templates against the same selector, so the second silently rebinds the first's traffic. Reasoned from the selector fields, not measured | `plan/spec-fixit-child-sa-rekey-policy.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Route the 3-arg `RemovePolicy` through the same `xfrmPolicyDel` seam as `RemovePolicyParams` (`internal/component/ike/dataplane/xfrm_linux.go`) | The seam that makes the ownership guard testable from its entry point covers one of the two delete sites, so the other is untestable by the same means | `plan/spec-fixit-ike-test-discrimination.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Give `make ze-qemu-integration-test` an automated caller. Every kernel-level proof this pilot rests on runs only there, and `ai/rules/qemu-testing.md` records that nothing runs it | The dual-form receive, the IKE bypass, the policy-owner guard and the peer-Delete policy retention are all proven ONLY in QEMU. None of that evidence is merge-gated, so a regression in any of them reaches main green | `plan/spec-rfcgate-2-deferred-unrun-interop-trees.md` | deferred |
| 2026-08-02 | spec-rfcgate-1b-rfc7296-pilot | Raise the QEMU boot timeout default, or make it adaptive. The 60s default times out under host load around 10, and `ZE_QEMU_BOOT_TIMEOUT=300` was needed | A timeout that fails on a loaded developer machine reads as a product red. `ai/rules/fix-dont-record.md` treats a fixed duration where a condition exists as the defect | `plan/spec-fixit-ike-test-discrimination.md` | deferred |
