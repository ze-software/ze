# Deferrals — spec-fixit-bgp-egress-rail-divergence

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-24 | spec-fixit-bgp-egress-rail-divergence | `OTCEgressFilter`'s Gao-Rexford safety net reads `meta["src-role"]` and treats a MISSING key as "no restriction", so it goes unevaluated for any caller without ingress metadata (the Adj-RIB-In relay path, and any future one). The value is recoverable exactly — `OTCIngressFilter` writes `meta["src-role"] = cfg.role` from the same `getFilterConfig(src)` lookup the egress filter already performs — so the fix is a config-derived fallback when meta is absent. | Not required by any AC of this spec: RFC 9234 egress suppression on the replay path already works, because the ingress filter stamps OTC into the WIRE bytes before the route is stored and the wire-bytes rule (`checkOTCEgress`, `role/otc.go`) sees it on the reconstruction. Verified by removing the fallback and re-running 394/395 — both still pass. Landing it here would also require editing `TestOTCEgressNoStampProvider`, which carries `RFC requirement: RFC9234-5-4 negative` and may only be changed with explicit user approval (`ai/rules/testing.md` RFC-Tagged Tests). | plan/spec-fixit-otc-src-role-meta-fallback.md | done |

| 2026-07-25 | spec-fixit-bgp-egress-rail-divergence | Normalize the stored NLRI framing so ADD-PATH (RFC 7911) routes can be replayed. The structured ingest strips the 4-byte path-id (`nlri/iterator.go` `Next`, stored verbatim by `installStructuredNLRIs`) while the legacy ingest prepends it (`prefixToWireHex`, and only when non-zero though path-id 0 is legal), so the stored bytes carry one of two framings and nothing records which. | Emitting either framing under the source's add-path context corrupts the destination session, so the relay refuses add-path sources outright (`errRelayAddPath`) rather than guess. Refusing loses the replay; guessing loses the session. Spec assumption A-3, Phase 5. | plan/spec-fixit-stored-route-relay-hardening.md | deferred |
| 2026-07-25 | spec-fixit-bgp-egress-rail-divergence | `test/plugin/adj-rib-in-replay-on-peerup.ci` replays to `10.0.0.99`, which is not a configured peer, so `RelayStoredRoute` returns at destination resolution and the test asserts on the SELECTED route count. It passes with the relay entirely dead (fails the mutation check in `ai/rules/testing.md`). | Found by independent review; making it gate needs a second source peer and wire-byte assertions, which is test authoring beyond this spec's fix. The relay path is covered meanwhile by the four target `.ci` tests plus the reactor unit tests. **DONE 2026-08-07.** The vacuity was reproduced before it was fixed: with `return nil` as the first statement of `RelayStoredRoute`, the old test still reported PASS. The rewrite replays to an ESTABLISHED second peer on 127.0.0.2 and asserts exact wire bytes twice. The second peer is the DESTINATION rather than the second SOURCE this row predicted: one source is enough, because what the old test lacked was a destination the relay could resolve. The dest's seq=1 pins the LIVE forward (the control) and seq=2 pins the RELAYED copy (the subject), both to the same 51-byte UPDATE, which is the direct statement of learned 1271's one-egress-transform invariant. Nothing else can produce the second copy: no RIB plugin is loaded, and adj-rib-in is given `state` for the source peer only, so its peer-up self-replay cannot fire for the dest. Mutation-verified in both directions: RED with the relay dead (`ZE-OBSERVER-FAIL: dest peer was never sent the relayed copy of the stored route`, dest wire shows one UPDATE, and the plugin still reported `replay selected 1 route(s)`), GREEN restored. `make ze-functional-plugin-test` 558/558. | plan/spec-fixit-stored-route-relay-hardening.md | done |
| 2026-07-25 | spec-fixit-bgp-egress-rail-divergence | Four smaller relay gaps: RFC 2545 32-byte next hop (global + link-local) truncated to 16. **CITATION CORRECTED 2026-08-07, verified at the producer: it is NOT `nhopHexFromAddr`,** which takes one `netip.Addr` and truncates nothing. The link-local is dropped one layer earlier by `MPReachWire.NextHop` (`internal/component/bgp/wireu/mpwire.go`), whose `afi == 2 && nhLen >= 16` branch copies `nhBytes[:16]` under the comment "may have link-local after, take first". Start the fix there and at the stored representation. RFC 2545 is now enrolled (`rfc/short/rfc2545.md`), and Section 3 binds this: the link-local "shall be included ... if and only if" the speaker shares a subnet with both the next-hop entity and the peer. Today the loss is observability only, because `RawRoute.NHopHex` is read at two command-output sites and never re-encoded onto the wire; it becomes a Section 3 violation the moment a stored route is RELAYED in the route-server case, which is the one deployment that sends the 32-octet form at all; complex families (VPN/EVPN/Flowspec) store the WHOLE MP_REACH NLRI block so a replay re-announces every NLRI of the originating UPDATE; no backpressure on in-flight relays (each pins a read-pool buffer); `Coordinator.RelayStoredRoute` has no test. | Each is real but none is reachable from the four failures this spec fixes, and each needs its own fixture work. | plan/spec-fixit-stored-route-relay-hardening.md | deferred |
| 2026-07-25 | spec-fixit-bgp-egress-rail-divergence | Replay ownership is process-global (`replayOwned`) while event delivery is per-peer per-plugin, so a peer whose `process` block gives `state` to adj-rib-in but not bgp-rs is replayed by NOBODY. Separately the claim does not survive an adj-rib-in respawn (`SendPostStartup` has one call site, inside `signalStartupComplete`). | Both found by independent review Run 2. Scoping ownership per-peer and re-delivering post-startup on respawn are lifecycle changes wider than this spec's fix. The startup ORDERING race they compound is NOT fixed here: making `sendPostStartupToAll` wait before `StartPeers` was tried and reverted because it deadlocks (a post-startup handler that waits on peer activity blocks the peers it waits for; three functional tests failed), so the function still fans out without waiting (`plugin/server/startup.go`) and the claim can lose to a peer that establishes immediately. Corrected 2026-07-25 by a later review: the earlier text here claimed the wait had landed, which the code contradicts. | plan/spec-fixit-stored-route-relay-hardening.md | deferred |
| 2026-07-25 | spec-fixit-bgp-egress-rail-divergence | `relayChunkSize` chunks by route COUNT, which does not bound BYTES: `AttrHex` is hex (~2x the attribute block), so 4096 routes x a 4 KB block already exceeds the 16 MB `rpc.MaxMessageSize` frame ceiling for a forked adj-rib-in. | Fails closed (the error propagates), so availability rather than corruption. Needs a byte-budget accumulator plus a `last-index` contract assertion across chunks. | plan/spec-fixit-stored-route-relay-hardening.md | deferred |

| 2026-07-25 | spec-fixit-bgp-egress-rail-divergence | Cover the remaining zero-dispatch FAILURE branches of `forwardUpdateCore` with tests: read-buffer pool exhaustion in the EBGP wire build and in the two transcode acquisitions, `buildFwdBody` failure, and both dispatch attempts failing on a stopped pool. Today only two failure branches are covered (nil forward facts, and a failing egress step). | Found by independent review Run 4. None is reachable from a unit fixture without a pool-injection seam, and the classification they feed is now correct by construction (`suppressedCount` is incremented ONLY at the three policy sites, so a new branch defaults to "drop"). The risk is a future edit adding `suppressedCount++` to a failure branch, which no current test would catch. | plan/spec-fixit-stored-route-relay-hardening.md | deferred |

| 2026-07-25 | spec-fixit-bgp-egress-rail-divergence | Gate the two remaining non-decided `PolicyReject` producers with tests: the filter IPC error under a fail-closed `FilterOnError`, and the AC-13 undeclared-attribute override. Both now set `Failed` correctly, but neither has a test that would fail if the flag were removed. | Found by independent review Run 5. `Reactor.api` is a concrete `*pluginserver.Server`, not an interface, so driving either producer needs a live plugin server or an injection seam. The other two producers (nil API server, filter panic) ARE gated and mutation-verified. | plan/spec-fixit-stored-route-relay-hardening.md | deferred |

| 2026-07-25 | spec-fixit-bgp-egress-rail-divergence | **The in-process egress half has no failure channel at all.** `filterapi.EgressFilterFunc` returns a bare `bool` (`filterapi/filterapi.go`), so `safeEgressFilter` can only report the failure it causes itself (a recovered panic); every other way an in-process filter can fail to evaluate is indistinguishable from a policy decision. This is already live in one filter: `OTCEgressFilter` rejects when the destination's role is not in the source's export set (`role/otc.go`), and the role it reads comes only from `filterRemoteRoles`, whose empty value means BOTH "the peer advertised no role" (a decision) and "we never recorded it" (a failure). Producers of the second: a validate-open RPC timeout (`bgp/server/validate.go` logs WARN and `continue`s, session proceeds), the plugin conn not yet up, no process manager, and `setFilterState` nulling the map on every `OnConfigure` (`role/role.go`), which a plugin respawn triggers while `OnValidateOpen` only fires on a NEW OPEN. Fix needs both halves: a failure channel on the filter signature, and role recording that separates "no role" from "not recorded". | Found by independent review Run 6. Pre-existing and separable: the zero-value trap in the role filter and the silent validate-open skip both predate this spec, and the relay's own goal (one egress transform, drops visible for everything that HAS a failure channel) is met. Closing it means changing a filter signature used by three plugins AND the role plugin's recording semantics, which is its own AC set, not a line fix. Config-gated: needs a source with a `role export` set plus a destination whose role went unrecorded. | plan/spec-fixit-stored-route-relay-hardening.md | deferred |

## Detail

The fixture `TestOTCEgressNoStampProvider` (`internal/component/bgp/plugins/role/otc_test.go`)
configures local role `customer` toward the source — meaning the SOURCE peer is a
Provider (`otc.go`) — with a Provider destination, and asserts `accept == true`.
RFC 9234 Section 5 requires a Provider-learned route NOT be propagated to a Provider,
so that assertion only holds because `meta` is nil and the safety net is skipped.

Closing the gap therefore needs the fixture to change so the route legitimately
reaches the stamp block (local role `provider` toward the source, i.e. the source is
a Customer and transit is allowed), preserving the RFC9234-5-4 negative proof, plus a
NEW test asserting the Provider→Provider leak is suppressed with `meta == nil`.

That is a strengthening rewrite, not a relaxation — but the RFC-tagged-test hook
cannot tell the two apart, and correctly demands user approval first.

**RESOLVED 2026-07-27.** Thomas approved the RFC-tagged test change, and the fix
landed exactly as prescribed above:

- `resolveSrcRole` (`role/otc.go`) recovers our role for the source peer from
  `srcCfg.role` when meta carries no usable `src-role`. It is an exact recovery,
  not a guess: `OTCIngressFilter` only ever copied that same config field into
  meta. An unusable value (present but not a string) takes the fallback too, so
  a malformed input is never more permissive than a missing one.
- `TestOTCEgressNoStampProvider` keeps its `RFC9234-5-4 negative` tag and now
  configures local role `provider` toward the source, so the route legitimately
  reaches the stamp block and the tag still proves stamping scope. Flipping its
  assertion instead would have made it vacuous: a suppressed route returns
  before the stamping code, so "no stamp" would have held for the wrong reason.
- `TestOTCEgressSuppressProviderLearnedWithoutMeta` is the new test carrying the
  old fixture with the correct expectation. Mutation-verified: disabling the
  fallback turns it red.
- `meta_wrong_type_not_suppressed` moved to a config-less source (10.0.0.99).
  Its old source had config role `provider`, which the fallback would have made
  the reason it passed, hiding whether the type check still worked.
- The stale comment at `reactor_api_relay.go` that documented this gap as open
  is updated to record it closed.
