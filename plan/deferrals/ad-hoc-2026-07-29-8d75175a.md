# Deferrals: ad-hoc-2026-07-29-8d75175a

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/deferral-tracking.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-29 | ad-hoc (found while fixing the `commandMetaSource` duplicate-command union) | Hidden plugin commands reach the MCP tool list and the API command list. `CommandRegistry.All` returns every registration including hidden ones (`internal/component/plugin/server/command_registry.go:147`) and `Hidden` is referenced nowhere in the command-metadata path (`cmd/ze/hub/command_meta.go`, `cmd/ze/hub/api.go`, `internal/component/mcp/tools.go`); `VisibleCommandEntries` skips them (`command_registry.go:445-447`) but feeds only the completion tree (`cmd/ze/hub/main.go:972`, `cmd/ze/hub/session_factory.go:167`). A command marked `Hidden: true` is therefore suppressed from completion and help while still advertised as an MCP tool, the opposite of what `ai/rules/cli-patterns.md` documents the flag to mean | Distinct from the duplicate-command defect it was found beside: different key (visibility, not identity), different consumers affected, and folding it into that fix would have widened a targeted dedupe into a behavior change on the always-on API command list. The goal the dedupe existed to serve (one entry per command, deterministic order) holds without it, so this is separable work rather than a parked blocker (`ai/rules/no-parking.md`). FIXED 2026-07-30. The home spec named here was closed with the series while this item was still open, which left the row dangling and the work undone. The deferrals review found both. `buildCommandMeta` now skips a hidden plugin command at the top of its loop (`cmd/ze/hub/command_meta.go:163`). That one site covers both the MCP tool list and the API command list, because it is their shared source. The dispatcher loop needs no equivalent, because `pluginserver.Command` has no `Hidden` field. Proven by mutation: with the skip disabled the wire payload carries the concealed command, and `test/plugin/mcp-tools-list-hidden-command.ci` goes red | implemented 2026-07-30, `cmd/ze/hub/command_meta.go` plus `test/plugin/mcp-tools-list-hidden-command.ci` | done |
