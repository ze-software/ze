# Ad-hoc deferrals -- 2026-07-27 (session 2c83641a)

Rows rehomed out of `plan/spec-fixit-sleeps-cli-harness.md` when it was closed as stale.
The spec's core (an SSH CLI harness and three blind-sleep conversions) was verified dead,
but an independent check found its A-4 obligation still live, so it is homed here rather
than dying with the file.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-27 | spec-fixit-sleeps-cli-harness (closed as stale) | Wire the `static` functional suite into an actually-run Linux path. `test/static/static-show.ci` and `test/static/static-table-interface.ci` carry `option=needs-linux`, but `static` is absent from `all_suites` in `mk/test-functional.mk` and from the `fsuite` list in `scripts/evidence/qemu-all-tests.sh`, so neither the default verify gate nor the QEMU Linux-only target reaches them. They run nowhere. | This was the closing spec's A-4 obligation and its only live item; the rest was verified stale. The work is suite-runner wiring, which is `spec-release-evidence-gate`'s own subject, not CLI-harness work. | plan/spec-release-evidence-gate.md | deferred |
| 2026-07-27 | spec-fixit-sleeps-cli-harness (closed as stale) | Add `caps=net-admin` to `test/static/static-show.ci` and `test/static/static-table-interface.ci`. Both run `ip link add` from `setup.py` while declaring only `option=needs-linux`; `internal/test/runner/record_parse.go` documents that shape as fail-open, so on an unprivileged Linux host they hang or fail instead of skipping honestly. | Only observable once the suite actually runs somewhere, so it travels with the wiring item above. | plan/spec-release-evidence-gate.md | deferred |
| 2026-07-27 | spec-fixit-sleeps-cli-harness (closed as stale) | `test/vpp/vpp-fib-route-lookup.ci` cites `plan/spec-vpp-fib-query.md` on its `// Design:` line; that spec does not exist. | Cosmetic dead reference the closing spec had listed as "fix opportunistically". It would die silently with that spec, so it is recorded rather than lost. | plan/spec-release-evidence-gate.md | deferred |
