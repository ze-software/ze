# Ad-hoc deferrals -- 2026-07-27 (session 2c83641a)

Rows rehomed out of `plan/spec-fixit-sleeps-cli-harness.md` when it was closed as stale.
The spec's core (an SSH CLI harness and three blind-sleep conversions) was verified dead,
but an independent check found its A-4 obligation still live, so it is homed here rather
than dying with the file.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-27 | spec-fixit-sleeps-cli-harness (closed as stale) | Wire the `static` functional suite into an actually-run Linux path. `test/static/static-show.ci` and `test/static/static-table-interface.ci` carry `option=needs-linux`, but `static` is absent from `all_suites` in the retired `mk/test-functional.mk` (current producer: `internal/le/functional/suites.go`) and from the `fsuite` list in the retired `scripts/evidence/qemu-all-tests.sh` (current producer: `internal/le/qemu/alltests.go`) **CLOSED 2026-09-03 after verifying both producers: the suite is wired in both places this row says it is absent from.** `suiteStatic` is a row of `Suites` (`internal/le/functional/suites.go`) carrying its own `Args`, and `"static"` is a row of `vmSuites` (`internal/le/qemu/alltests.go`), marked `Concurrency: serial`. The hand-written `all_suites` line and the `fsuite` shell list the row names are both gone, and with them the second declaration the defect lived in., so neither the default verify gate nor the QEMU Linux-only target reaches them. They run nowhere. | This was the closing spec's A-4 obligation and its only live item; the rest was verified stale. The work is suite-runner wiring, which is `spec-release-evidence-gate`'s own subject, not CLI-harness work. | plan/spec-release-evidence-gate.md | done |
| 2026-07-27 | spec-fixit-sleeps-cli-harness (closed as stale) | Add `caps=net-admin` to `test/static/static-show.ci` and `test/static/static-table-interface.ci`. Both run `ip link add` from `setup.py` while declaring only `option=needs-linux`; `internal/test/runner/record_parse.go` documents that shape as fail-open, so on an unprivileged Linux host they hang or fail instead of skipping honestly. **CLOSED 2026-09-03 after verifying the producers: both files carry the capability now.** `test/static/static-show.ci` and `test/static/static-table-interface.ci` each declare `option=needs-linux:caps=net-admin`, so an unprivileged host skips them rather than failing open. | Only observable once the suite actually runs somewhere, so it travels with the wiring item above. | plan/spec-release-evidence-gate.md | done |
| 2026-07-27 | spec-fixit-sleeps-cli-harness (closed as stale) | `test/vpp/vpp-fib-route-lookup.ci` cites `plan/spec-vpp-fib-query.md` on its `// Design:` line; that spec does not exist. **FIXED 2026-09-03 in `d69ce9aa48`.** The header now names `docs/features/interfaces.md`, which documents `RouteLookup via IPRouteLookupV2` and carries a source line for `internal/plugins/iface/vpp/fib.go`. The sibling `test/ui/dash-stdio.ci` had the same defect and was repointed in the same commit; the class is recorded in `plan/journal/closure-deletes-a-cited-document.md`, because a `# Design:` header in a `.ci` is read by no gate. | Cosmetic dead reference the closing spec had listed as "fix opportunistically". It would die silently with that spec, so it is recorded rather than lost. | plan/spec-release-evidence-gate.md | done |
