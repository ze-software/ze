# Deferrals: ad-hoc-2026-07-22-f27dc80f

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-22 | ad-hoc (bmp-locrib / functional test 97 shared-scratch race fix) | Stop `BMPPlugin.handleBestChange` (`internal/component/bgp/plugins/bmp/bmp_locrib.go`) doing blocking socket I/O on an EventBus subscriber goroutine. Engine subscribers fire synchronously from `deliverEvent` (`internal/component/plugin/server/engine_event.go` `SubscribeEngineEvent`) and `pkg/ze/eventbus.go` `EventBus.Subscribe` says a handler "MUST NOT block on I/O"; the handler loops `len(batch.Changes) x len(senders)` blocking `conn.Write` calls bounded only by `writeTimeout` (10s), so a wedged collector stalls RIB best-change publication. Wanted: a bounded per-session send queue | Pre-existing, introduced with RFC 9069 Loc-RIB support, and NOT a regression from the `writeMu` fix that prompted the review -- concurrent `conn.Write` calls already serialize on the socket's own write lock (`internal/poll.FD.Write` takes `fd.writeLock()` and holds it across the whole call, EAGAIN waits included), so a producer already waited out any in-flight write. It does not block the scratch-corruption fix's goal, and the remedy is a design decision (queue depth, and drop-vs-stall when full) rather than a local edit. The obvious shortcut -- `TryLock`-and-drop -- silently loses Route Monitoring messages, which is worse for a monitoring protocol, so it must not be taken casually. Recorded at the site as a KNOWN DEFECT comment | `plan/spec-fixit-bmp-sender-blocking-and-reload.md` | deferred |
| 2026-07-22 | ad-hoc (bmp-locrib / functional test 97 shared-scratch race fix) | Make `BMPPlugin.startSender` (`internal/component/bgp/plugins/bmp/bmp.go`) idempotent across config reloads: it appends to `bp.senders` and starts a goroutine per collector with no preceding `stopSenders`, so a re-delivered `OnConfigure` doubles the sender set (duplicate BMP streams per collector, leaked sockets and goroutines). Its call site sits next to a comment stating "startLocRIB is idempotent across reloads", so the asymmetry looks unintentional | Reachability is UNVERIFIED and must be settled before fixing: `deliverConfig` is called from the 5-stage startup driver (`internal/component/plugin/server/startup_driver.go`) and no reload re-delivery path was traced, so this may be latent rather than live. Reported by independent review of the test-97 fix; out of that fix's scope (different file, different failure mode, no bearing on the framing corruption) and cheap to fix correctly once the reload semantics are known | `plan/spec-fixit-bmp-sender-blocking-and-reload.md` | deferred |
