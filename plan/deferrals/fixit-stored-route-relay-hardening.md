# Deferrals — spec-fixit-stored-route-relay-hardening

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-03 | spec-fixit-stored-route-relay-hardening | `LLGREgressFilter` returns ACCEPT when `egressState.Load()` is nil (`internal/component/bgp/plugins/gr/gr_egress.go`), so a stale route is advertised unmodified while the GR plugin's egress state is not yet stored. The nil state withholds exactly the fact the filter needs, the destination's LLGR capability, so RFC 9494 Section 4.3's "SHOULD NOT be advertised to peers that have not advertised the LLGR capability" is not applied, and Section 4.6's iBGP depreference is skipped too. | The same fail-open family as R6-1, found while reading every registered egress filter for the AC-7 reassessment, but outside R6-1 and R6-2: it is a GR-plugin startup-ordering question (is the window even reachable?) plus an RFC 9494 fail-open/fail-closed decision, not the stored-route relay's egress classification. Fixing it inside this spec would put an RFC 9494 behavior change in a commit about the relay. **DONE 2026-08-07.** The window is REACHABLE and is not closed by ordering: `filterapi.Register` puts `LLGREgressFilter` in the egress pipeline from `init()` (`internal/component/bgp/plugins/gr/register.go`), which runs for every `ze_bgp` build via `internal/component/plugin/all/all_ze_bgp.go`, while the only non-test store of `egressState` is `setEgressState` inside `RunGRPlugin`'s `OnConfigure` callback (`internal/component/bgp/plugins/gr/gr.go`). Nothing sequences the two, and if the GR plugin engine never runs in this process the state stays nil for its whole lifetime while the filter stays registered and called. Fixed fail-CLOSED: the stale-level fast path now runs FIRST (it needs no state), and an unloaded state resolves to `hasLLGR=false` rather than to an early ACCEPT, so the destination takes the same RFC 9494 treatment as a peer known not to have advertised the capability (withdraw for EBGP, Section 4.6 NO_EXPORT + LOCAL_PREF=0 for IBGP). This is the literal reading of Section 4.3, which keys on whether the capability "has been received": with no state, it has not been received from anyone. A latched WARN (`egressStateMissingWarned`) says so once per process, matching `recordDrop`'s shape in the sibling `role` plugin; the fail-closed behavior does not depend on that log landing. `TestLLGREgressFilter_NilState` asserted the violation (stale route, EBGP dest, `mods.Len()==0`) and was replaced. Proof: `TestLLGREgressFilter_NilStateWithdrawsEBGP` (RFC9494-4.3-3 positive), `TestLLGREgressFilter_StateLoadedStillAdvertisesToLLGRPeer` (RFC9494-4.3-3 negative), `TestLLGREgressFilter_NilStateDepreferencesIBGP` (4.6-2 / 4.6-3 positives), `TestLLGREgressFilter_NilStateDoesNotSuppressFreshRoute` (bounds the fix to stale routes). RFC9494-4.3-3 went from unproven (`--` / `--` in `ai/RFC-REQUIREMENTS.md`) to both polarities. Both nil-state tests verified RED with the fix reverted. | plan/spec-fixit-egress-filter-non-decision-channel.md | done |
| 2026-08-03 | spec-fixit-stored-route-relay-hardening | `safeEgressFilter` (`internal/component/bgp/reactor/reactor_notify.go`) returns `(accept, panicked)`, and two of its three call sites throw the second value away: the route-server fanout (`internal/component/bgp/reactor/forward_rs.go`) and `decideStaleReadvertise` (`internal/component/bgp/reactor/reactor_api_batch.go`). On those rails a recovered filter panic is reported as a policy suppression. `forwardUpdateCore` reads both and classifies correctly, which is why R6-1's chain was only ever visible on the forward rail. | Reading an existing return, not adding one, so it is small — but it is a behavior change on two rails this spec does not otherwise touch, and it belongs with the LLGR row above and with whatever Thomas rules for AC-7. Homed together so the seam is fixed once rather than three times. **NOT deferred after all: Thomas ruled on 2026-08-03 that AC-7 be REPLACED by this work, so it landed in `plan/spec-fixit-stored-route-relay-hardening.md` (AC-7), not in the destination named here.** | plan/spec-fixit-stored-route-relay-hardening.md | done |
| 2026-08-03 | spec-fixit-stored-route-relay-hardening | `decideStaleReadvertise` (`internal/component/bgp/reactor/reactor_api_batch.go`) still returns `staleSuppress` on its OTHER failure branch: `buildModifiedPayload` refused to apply the LLGR depreference mods. The filter DID decide there; realizing the decision failed. AC-7 gave the rail a `staleFailed` token for exactly that class, so this branch now reports a speaker-side failure as a policy suppression while the branch four lines above reports it correctly, and `AnnounceNLRIBatch` turns it into `ErrNoPeersAcceptedFamily` — a cause that is untrue and that `DispatchNLRIGroups` downgrades to a warning. | Found while implementing AC-7, in the same function. **NOT deferred after all: fixed here on 2026-08-03, in `plan/spec-fixit-stored-route-relay-hardening.md` (AC-7), on the reviewer's direction.** Leaving it would have left half of one function conflating "could not" with "decided not to", and making Ze more correct needs no further ruling. The branch now returns `staleBuildFailed`; the sentinel was renamed to `errStaleReadvertiseWithheld` with `...FilterPanic` / `...BuildFailed` wrapping it, since `Filter` was false for a build failure. `recordModifyFailureAddr` is untouched, so the producer-side count and reason still fire. Two adjacent nil-build branches came with it and now report `errAnnounceTooLarge` / `errWithdrawTooLarge`, matching the non-stale rail. Proof: `TestDecideStaleReadvertiseFailureIsNotPolicy`, `TestAnnounceNLRIBatchStaleFailureIsNotFamilyMismatch`, and the renamed `TestDecideStaleReadvertiseWithholdsOnModifyFailure`, each mutation-verified. | plan/spec-fixit-stored-route-relay-hardening.md | done |
| 2026-08-03 | spec-fixit-stored-route-relay-hardening | RFC 2545 has neither a summary (`rfc/short/rfc2545.md`) nor full text (`rfc/full/rfc2545.txt`) in the repository, while `docs/features/rfc-status.md` already claims it Supported for IPv6 next-hop handling. Produce both through `/ze-rfc`, which also owes enrolment in `rfc/enrolled.txt`, an `rfc/extraction/rfc2545.json` sign-off, and a re-checked status row. | This spec's I-6 needs the 32-byte next hop (global plus link-local), which `nhopHexFromAddr` (`internal/component/bgp/plugins/adj_rib_in/rib.go`) truncates to 16. It cannot be implemented against memory of the RFC (`ai/rules/rfc-compliance.md`), so the summary is a precondition, not part of the fix. The spec's Required Reading was reworded to keep the obligation visible rather than cite a file that does not exist; this row is what tracks it. **DONE 2026-08-07.** `rfc/full/rfc2545.txt` fetched from the RFC Editor, `rfc/short/rfc2545.md` written from that text, `rfc2545` enrolled in `rfc/enrolled.txt`, and `rfc/extraction/rfc2545.json` signed off at register `prose` (10 sites, 10 sections; 4 mapped, 4 excluded, 2 of the excluded naming another role and 2 naming another document). All four SHALL-level requirements are Section 3 and all four are proven in BOTH polarities at `functional/verify` tier by `test/encode/new-v6.ci` and the two `test/exabgp-compat/encoding/conf-llnh-*.ci`. `make ze-rfc-check` reports 2954 gated requirements across 169 enrolled RFCs with 3277 tags resolved. The `docs/features/rfc-status.md` row was re-checked and rewritten. Carried forward for I-6: the truncation is in `MPReachWire.NextHop()` (`internal/component/bgp/wireu/mpwire.go`), not in `nhopHexFromAddr`; see the report on this row. | plan/spec-followup-rfc-enrollment.md | done |

## Detail

**AC-7 is NOT in this shard, and is now closed.** Thomas ruled on 2026-08-03:
"REPLACE AC-7 with the work the evidence supports." The AC was rewritten in
`plan/spec-fixit-stored-route-relay-hardening.md` to fix the two discarding rails
(row 2 above, now `done` in that spec), and it is DONE rather than retired or
deferred (`ai/rules/no-partial-completion.md` — owed, done, or retired by the
owner, and no fourth state).

The `filterapi.EgressFilterFunc` signature change is therefore owed by nobody, and
neither are the `otc_test.go` / `config_test.go` edits it would have dragged in.
`plan/spec-fixit-egress-filter-non-decision-channel.md` survives for ROW 1 ALONE:
the `LLGREgressFilter` nil-state answer, an RFC 9494 fail-open decision plus a
GR-plugin startup-ordering question, which this work did not touch. Rows 2 and 3
both landed here instead. That is the whole of its remaining scope.

**Q-1 is closed, not deferred.** Thomas ruled it on 2026-08-03: an unrecorded
destination role KEEPS matching an explicit `export { unknown }`. The ruling is
recorded in the source spec's Key Design Decisions and Q-1 section, at
`OTCEgressFilter`'s export-set block, at `filterapi.EgressFilterFunc`, and in
`TestExportSetUnrecordedStillMatchesExplicitUnknown`
(`internal/component/bgp/plugins/role/role_recorded_test.go`), whose assertions
did not move.
