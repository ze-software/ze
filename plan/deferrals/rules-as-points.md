# Deferrals: rules-as-points

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-07 | spec-rules-as-points | Make `DemoLockTest` in `demos/terminal/test_render.py` portable, or make its skip guard capability-aware. Its two lock holders start with `flock -F <path> sleep`, and `-F` (`--no-fork`) exists in util-linux `flock` but NOT in the portable `flock` on this machine, whose usage lists only `-s -x -u -n -w -o -c -E --verbose`. The holder exits at once, `demo-run.lock` is never created, and `wait_for` fails on its absence in `test_a_run_told_the_lock_is_held_does_not_take_it_again` and `test_the_harness_lock_refuses_a_tree_another_run_holds`. The guard `@unittest.skipIf(shutil.which("flock") is None)` checks that flock EXISTS and never that it supports the flag the test depends on, so on macOS the test neither skips nor works. Preferred fix: hold the lock from a Python child using `fcntl.flock`, as `render.py` itself does, which removes the dependency on flock(1)'s feature set entirely and makes `kill()` reach the holder with no fork to orphan | Found while verifying phase 2 of rules-as-points, on a surface this spec does not touch (`demos/terminal/` is unmodified in this working tree; last written by `256c58881`, 2026-08-05). The rules-as-points goal does not depend on it, so `ai/rules/rule-precedence.md` puts the fix after this spec closes rather than on the way to closing it. It is not a typo fix: the correct remedy is a portability change plus a capability-aware guard, and it needs verifying on both Linux and macOS | Fixed 2026-08-07 by `db493f22a`, outside this spec. `demos/terminal/test_render.py` no longer runs `flock -F`: its lock holders are a Python child that takes `fcntl.flock` on the file and writes a ready marker, which is the preferred fix this row named. the closure record of that fix carries the reasoning | done |
| 2026-08-07 | spec-rules-as-points | Teach `check_hook_names` in the retired `scripts/dev/check_doc_links.py` (current producer: `internal/le/doc/check/links.go`) to see a dead check name in a rule file, then delete the stale `c_model_phase` prose it would then report. The function was REMOVED (`ai/rules/points/planning/work-phases/implementation-carries-no-model-requirement.md` says so, and it is in no dispatcher), yet `ai/rules/points/planning/work-phases/how-the-model-phase-gates-work-and-where-they-stop.md` still describes it as a live BLOCKING gate and the Hook-to-Rule Mapping table still carries its row. The guard misses BOTH: `NAME_LINT_FILES` covers `ai/rules/repo-maintenance.md`, where the name sits UNBACKTICKED in a table cell, and does not cover `ai/rules/planning.md`, where it is backticked | A guard that reports nothing on a name that resolves to nothing is fail-open (`ai/rules/evidence.md`), and it left two rule points describing a gate that cannot fire. Found while reading the `Enforces` column as phase 4's seed, on a surface phase 4 does not write. The gate-map goal does not depend on it, so `ai/rules/rule-precedence.md` puts the fix after this spec closes. The mapping-table row is phase 5's to remove, because phase 5 generates that table from the bindings; the planning.md point and the guard are neither phase's file | `plan/future/spec-harness-fail-open-guard-backlog.md` | deferred |
| 2026-08-07 | spec-rules-as-points | Decide and act on `c_check_existing_tests` in `.claude/hooks/pretool-writeedit.py` (retired; now `internal/le/hookruntime/writeedit.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->: its body is `return None` with no other statement, under a comment reading "Warning-only hook: prints to stderr but always exit 0". It prints nothing. It is registered in the `CHECKS` tuple, so the dispatcher invokes it on every Write and Edit and it enforces nothing at all. Either implement the warning the comment promises, or remove the function and its `CHECKS` entry. The comment is stale either way (`ai/rules/stale-comments.md`) | Surfaced by phase 4: binding every check to the point it enforces is what forced the question "what does this one enforce", and the answer is nothing. A guard that cannot fire is fail-open (`ai/rules/evidence.md`), and it is pre-existing, not introduced here. Choosing between implementing it and deleting it is a judgement about the original intent rather than a mechanical fix, so it is not folded into this spec. The rules-as-points goal does not depend on it, which is what `ai/rules/rule-precedence.md` uses to put the fix after closure rather than on the way to it | `plan/future/spec-harness-fail-open-guard-backlog.md` | deferred |
| 2026-08-07 | spec-rules-as-points | Add an authored `title:` field to the point frontmatter, for the roughly 800 instruction-bearing points that no check binds, so `gate_map --ungated` (the retired `scripts/dev/rules_points.py` (current producer: `internal/le/rules/points.go`)) prints a scannable inventory. The 47 BOUND slugs were re-authored in phase 5 because a hook comment is the one place a human reads a slug raw. The other 1541 are read only through generated output, so re-authoring their ids would churn 800 filenames, 27 manifests and every `git log` trail for no reader. A `title:` beside `kind:` and `level:` gets the same result and touches no id | A slug is a KEY: it is opaque, it is stable, and handover D2 makes the path the id precisely so a binding survives an edit. A title is a NAME: it is authored, it is improvable, and it may be rewritten whenever the wording gets better. One `slugify` pass in `rules_points.py` had to produce both from the block's first line, which is why the two are conflated today and why `architecture/anti-pattern-instead` was named after a table header row. Separating them is a schema change to the frontmatter plus a renderer and lint update, not a rename, and the rules-as-points goal does not depend on it, so `ai/rules/rule-precedence.md` puts it after this spec closes | `plan/future/spec-harness-fail-open-guard-backlog.md` | deferred |
| 2026-08-07 | spec-rules-as-points | Decide if a `Binding` must carry a digest of the point BODY it names, in the shape of `check_audit_freshness` in the retired `scripts/dev/rfc_requirements.py` (current producer: `internal/le/rfc/rfc.go`): the digest sits on the BINDING, never on the id, and a body rewritten under the same slug then leaves the binding stale until a reader re-affirms it. Today `gate_map` in the retired `scripts/dev/rules_points.py` (current producer: `internal/le/rules/points.go`) joins on `Binding.ref` alone, so a reword keeps its gate. The third of the three problems in the spec Task table is therefore answered in review rather than by a machine, and `docs/contributing/rule-authoring.md` plus `ai/rules/points/rule-format/rationale/one-instruction-one-file.md` now say so | Handover D2 chose an authored filename plus `git log` over a content hash deliberately, and that decision stands: a hash as the ID changes on every edit and breaks every binding on a typo fix. A digest on the BINDING is a different design and was never put to the owner. It needs a staleness policy, a re-affirm step and a place to hold the digest, so it is a design question rather than a mechanical fix. The rules-as-points goal does not depend on it, which is what `ai/rules/rule-precedence.md` uses to put it after this spec closes | `plan/future/spec-harness-fail-open-guard-backlog.md` | deferred |
| 2026-08-07 | spec-rules-as-points | Teach `is_test_path` in the retired `scripts/dev/audit-test-relaxation.py` (current producer: `internal/le/testweakened/audit.go`) to see a Python test. It matches `_test.go` and, under `test/`, `.ci`/`.et`, so a change set that is almost entirely Python gets ZERO signal from the test-weakening audit: deleting an assertion from the retired `scripts/dev/learned_staleness_test.py` (current producer: `internal/le/`) still reports clean. The repository's own tooling tests are the retired `scripts/dev/*_test.py` (current producer: `internal/le/`) and its hook fixtures are the retired `scripts/dev/hook-fixture-check.py` (current producer: `internal/le/`), and neither is a test path to this guard | A guard that reports clean on a weakened test is fail-open (`ai/rules/evidence.md`), and it was silent through the rules-as-points review rounds, which changed 3 Python files and 1,580 markdown ones. Pre-existing and not caused here: `is_test_path` predates this spec and no phase of it touched that file. The rules-as-points goal does not depend on the audit seeing Python, so `ai/rules/rule-precedence.md` puts the fix after this spec closes rather than on the way to closing it | `plan/future/spec-harness-fail-open-guard-backlog.md` | deferred |
