# Deferrals: fixit-rfc4486-data-carries-count-not-upper-bound

One issue, recorded not fixed (owner instruction, 2026-08-08). The aggregate
live backlog is folded on read from `plan/deferrals/` by `/ze-status`. Nothing
stores it (`ai/rules/planning.md`).

**Issue:** the Cease/MaxPrefixes Data field carries the count, where RFC 4486
Figure 1 defines the prefix upper bound

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-08 | spec-bgp-per-peer-received-counter, while reading RFC 4486 Section 4 for the prefix-count rewrite | `reportPrefixExceeded` (`internal/component/bgp/reactor/session_prefix.go`) passes `current` -- the count that crossed the maximum -- to `buildPrefixNotification`, which writes it into the last 4 octets of the NOTIFICATION Data. RFC 4486 Section 4 Figure 1 labels those octets "Prefix upper bound", and the sentence introducing the figure says the message "MAY optionally include the Address Family information and the upper bound in the Data field". A peer decoding the field per the RFC therefore reads ze's current count as ze's configured maximum. `rfc/short/rfc4486.md` records the divergence as a MAY decision ("Include AFI+SAFI+count"), which `ai/rules/rfc-compliance.md` voids as authority: a recorded decision pointing away from the RFC text is not a ruling | The obligation is a MAY (`RFC4486-4-10`), so ze owes no Data field at all; what is wrong is the VALUE in a field the RFC defines. That makes it a conformance question rather than a defect I may settle: `ai/rules/rfc-compliance.md` requires the question to go to Thomas, and the fix needs an edit to `rfc/short/rfc4486.md`'s MAY-decision row, which this session was told not to touch. It affects BOTH count modes and is not caused by the `installed` rewrite, whose goal holds without it. **Ask Thomas which way:** write the configured `maximum` and correct the summary row, keep the count and record WHY against the RFC text, or drop the optional Data field. Two pinned assertions move with the answer: `TestPrefixCountInstalledTeardownStillStopsTheSession` and `TestPrefixCountInstalledStopsGrowingAtTheMaximum` (`internal/component/bgp/reactor/session_prefix_count_test.go`), plus `buildPrefixNotification`'s own tests | fixed in place, no destination spec needed | done: Thomas answered on 2026-08-24, "we want RFC compliance, so why is it even a question". `reportPrefixExceeded` now passes the family's configured `maximum`, `buildPrefixNotification`'s parameter is named `upperBound`, and `rfc/short/rfc4486.md`'s MAY-decision row carries the `Correction 2026-08-23` paragraph. The three pinned assertions moved with the answer: `TestPrefixExceedTeardown` (`session_prefix_test.go`, approved in `test/rfc-changed.md`), `TestPrefixCountInstalledTeardownStillStopsTheSession` and `TestPrefixCountInstalledStopsGrowingAtTheMaximum` (`session_prefix_count_test.go`). `TestPrefixNotificationDataCarriesTheConfiguredUpperBound` proves `RFC4486-4-10` with a maximum of 3 against an UPDATE of 8, so neither number can be read as the other. Restoring the defect fails it, measured |
