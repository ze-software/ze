# Deferrals: ad-hoc-2026-07-30-38f52d7b

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`. Nothing stores it (`ai/rules/deferral-tracking.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-30 | ad-hoc (found during the deferrals review that closed the MCP 2026-07-28 series) | The MCP listener answers `tools/list` before the plugin command registry freezes, so an early client caches a tool set that is missing commands. `buildServices` binds and serves MCP at `cmd/ze/hub/main.go:961`, `cr.Freeze()` runs at `internal/component/plugin/server/startup.go:206`, and the daemon waits for that freeze only at `cmd/ze/hub/main.go:1123`. `commandMetaSource` rebuilds the list per call from `d.Registry().All()` (`cmd/ze/hub/command_meta.go:95`), so two calls inside the window legitimately differ | Pre-existing and not caused by the MCP series. The measured rate is 2 failures in 10 concurrent runs of `test/plugin/mcp-tools-list-deterministic-order.ci` on a HEAD-equivalent tree. The new hidden-command test was withdrawn from that run, so it cannot interfere. The direction is a real design choice rather than a formality. The reason is that the product fix delays the port bind and turns an incomplete answer into a refused connection. That trade is for Thomas. The fix is also unverifiable right now: a startup-ordering change needs a full `make ze-verify`, and a concurrent session holds the verify lock (`ai/rules/git-safety.md`, one verify at a time repo-wide) | `plan/spec-fixit-mcp-serves-before-registry-freeze.md` | deferred |
