# Deferrals: fixit-redistribute-establishment-stall

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-16 | F4 shard (agent, test 506) | **Audit the 40 `test/plugin/*.ci` that use `cmd=api`, where it is INERT.** `cmd=api` is an encode-suite directive: `record_parse.go` sets `msg.Cmd`, and the ONLY reader is the reporter (`report.go`); ze-peer discards it outright (`internal/test/peer/expect.go`, literally `case "cmd": // Ignore - documentation only`). In `test/encode` the runner drives ze's API from it; in `test/plugin` nothing does. `test-pipe-first-last.ci` was silently testing an EMPTY RIB for this reason -- `count == 0` was the correct answer to a question nobody asked. `grep -rl "cmd=api" test/plugin/` returns **40 files**; how many RELY on it to inject (vacuous) versus carry it as harmless documentation alongside real injection is UNKNOWN and needs a file-by-file audit | F4's spec diagnoses this for the ONE file its shard covered but does not size or close the class. The 40 are a distinct vacuity class from F1 (peer never bound) and F2 (peer declared nothing): here the peer may bind and assert correctly while the test's PREMISE -- that routes exist -- was never established. Not urgent (a vacuous test is not a broken product) but every one is a test believed to cover something it does not | `plan/spec-fixit-redistribute-establishment-stall.md` (owns the harness contract; add as an F6 item alongside F5) | open |
| 2026-07-16 | F4 shard (agent, test 219) | **The F2 guard's own remedy text creates a vacuous green.** `validatePeerBlocks` tells the author to "run the peer with `--mode sink`". Doing so makes `hasCheckPeer` false (`internal/test/runner/peer_contract.go`); if the file also has `expect=exit:code=0`, `isSelfValidated` then returns true, and `runner_exec.go` gates the whole BGP branch on `!isSelfValidated(...)` -- so `validateJSON` (`runner_exec.go`, comment "peer path only") is SKIPPED ENTIRELY. Demonstrated on `forward-mpreach-nexthop-self-two-peer.ci`: it turns PASS in 8.2s while asserting nothing. A guard written to fail closed hands out a fail-open remedy (`ai/rules/evidence.md`). Fix options: make the remedy text name the `expect=json` consequence, or evaluate `validateJSON` outside the peer branch | Found while fixing test 219, not caused by it. Fixing the guard's semantics is a harness change owned by the spec that introduced F1/F2, not by a single test's shard. NOT urgent for the 8 tests sinked in `4ce173e32`/`282663671`: all 8 were checked and NONE declares `expect=json`, and their remaining assertions were proven live by breaking one and observing red | `plan/spec-fixit-redistribute-establishment-stall.md` (owns `peer_contract.go`; add as an F5 item) | open |
| 2026-07-16 | spec-fixit-redistribute-establishment-stall (F4) | Fix the 9 json-only `.ci` whose peer block is F2-rejected: `bgp-redistribute-{announce,burst,explicit-nhop,nexthop-self,withdraw}.ci`, `redistribute-l2tp-{announce,withdraw,multi-peer-nexthop}.ci`, `forward-mpreach-nexthop-self-two-peer.ci`. Each needs a ze-peer-consumed expectation added (so the peer binds), `expect=exit:code=0` dropped, and the `expect=json` corrected -- it has NEVER matched the UPDATE ze sends (missing `local-preference`, proven at E8/E9 in the spec) | F1/F2/F3 (the harness fix) landed first by design: `ai/rules/completion.md` requires the reds be exposed before the tests are corrected. Fanning F4 across agents is the user's stated plan (2026-07-16) | `plan/spec-fixit-redistribute-establishment-stall.md` (F4) | open |
| 2026-07-16 | spec-fixit-redistribute-establishment-stall (F4) | Fix the 21 F2-rejected `.ci` whose check-mode peer declares NO expectation at all and is pure scaffolding: `bgp-gtsm-reject.ci`, `bmp-{lg-bestpath-isolation,lg-disconnect,lg-ingest,receiver-messages,receiver-session}.ci`, `family-no-plugin-failure.ci`, `policy-chain-plain-names.ci`, `policy-test-{as4path-suppress,configured-export,configured-import,errors,reject-bad-hex,remove-private-as}.ci`, `rest-peer-set-delete-lifecycle.ci`, `rib-{best-selection,graph,graph-best,graph-filtered}.ci`, `show-bmp-sessions.ci`, `show-rr-status.ci`. Expected fix is `--mode sink` (the peer exists only so ze has a dial target; `rib-graph.ci`'s own peer block says `# No wire expectations from ze-peer (routes injected via API)`), NOT re-adding a fake expectation | The harness change (F2) only makes them parse-fail; deciding sink-vs-real-assertion per file is per-test judgement and is the F4 fan-out | `plan/spec-fixit-redistribute-establishment-stall.md` (F4) | open |
| 2026-07-16 | spec-fixit-redistribute-establishment-stall (F4) | Fix the 33 F1 runtime reds in `test/plugin` whose check-mode peer DOES bind but whose declared wire expectation never happens. All 33 declare ONLY the ipv4/unicast EOR (`FFFF..00170200000000`) while their `.run` plugin calls `request shutdown` at post-startup, so ze closes the session during the OPEN handshake and the EOR is unreachable by construction (`api-rib-inject` peer output: `open recv`/`open sent` then `connection closed before completion`, while the run-script prints `OK: rib inject + show verified`). Full list in the session report; includes `api-rib-*`, `adj-rib-in-*`, `rr-basic`, `nexthop-self*`, `multipath-basic`, `gr-cli-restart`, `rib-*`. Fix is per-file: either `--mode sink` (peer is scaffolding) or make the test reach Established before shutting down (the stronger test) | Each is a pre-existing false green unmasked by F1, not a regression. Choosing sink-vs-wait per file is judgement the F4 agents must make with the test's VALIDATES line in hand | `plan/spec-fixit-redistribute-establishment-stall.md` (F4) | open |
| 2026-07-16 | spec-fixit-redistribute-establishment-stall (F4, HIGHEST VALUE) | Triage the 6 F1 runtime reds whose peer validates REAL wire content and found a CONTENT mismatch -- these may be genuine product bugs, not test bugs: `bgp-rs-fastpath-ebgp-shared.ci` (expects an UPDATE announcing 10.0.0.0/24 with ORIGIN/AS_PATH/NEXT_HOP/LOCAL_PREF, peer received a WITHDRAW of 10.0.0.0/24; note its log also shows a failing external IRR lookup, so the environment dependency must be excluded first), `remove-private-as-{export,replace-peer}.ci`, `api-route-refresh.ci`, `rib-pipe-filter.ci`, `test-pipe-first-last.ci` | These are the only reds where the BGP assertion was substantive and is now enforced for the first time. Diagnosing announce-vs-withdraw in the route-server fastpath is a product investigation, not a test edit, and must not be closed by editing the expectation to match observed behavior (`ai/rules/completion.md`) | `plan/spec-fixit-redistribute-establishment-stall.md` (F4) | open |
| 2026-07-16 | spec-fixit-redistribute-establishment-stall (harness scope) | ~~7 `test/plugin` tests are load-sensitive under the default `-p 20`: `config-edit-ssh`, `ipv6`, `show-l2tp-{session-detail,statistics,tunnels}`, `sysctl-{list,show-describe}`~~ **2026-07-26: 6 of 7 FIXED** (`25f9c7b50`). Not load-sensitivity at all: each observer ended with a SYNCHRONOUS `request shutdown` RPC that raced ze's own socket close, so `_call_engine` blocked its 30s TLS read and raised after every assertion had already passed. `ipv6` remains and has a DIFFERENT mechanism (its peer asserts announce -> EOR -> withdraw, but the peer is established and has sent its EORs before the plugin announces, so the order is only reachable by winning a race) | Superseded. `ai/rules/completion.md` (owner directive 2026-07-26) bans "load-sensitive" as an outcome and bans `plan/known-failures/` as a deferral destination: a test that fails only on a busy host asserts on elapsed time and gets fixed | `plan/spec-fixit-redistribute-establishment-stall.md` (F4 -- `ipv6` only; its ordering expectation needs the RFC 4724 read, tracked with the same question in `as112-healthcheck-withdraw`) | open |

