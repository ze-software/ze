# Deferrals: knowledge-0-umbrella

Deferred work from the knowledge-system spec set. Format and status vocabulary:
`ai/rules/planning.md`.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-01 | spec-knowledge-0-umbrella | Decide whether `ai/rules/planning.md`'s Opus 4.8 implementation model is still deliberate, and fix the three gates keyed on `REVIEW_TIER` in `scripts/dev/running_model.py` if it is not | Owner answered "unsure, treat as out of scope" when the spec set was scoped. The finding is real and verified: on an Opus 5 session `c_model_phase` refuses every edit to `.go`, `.py`, `.sh`, `.ci`, `.et`, `.yang`, `.mk`, `.tmpl`, `.rego` outside `tmp/` unless `tmp/session/.model-ack-<sid>` exists | `plan/spec-knowledge-0-umbrella-deferred-model-selection-policy.md` (re-homed 2026-08-03: the original destination `spec-knowledge-0-umbrella` closed without doing it) | deferred |
| 2026-08-01 | spec-knowledge-0-umbrella | Remove the model-era justification sentences from `ai/INSTRUCTIONS.md` "STANDING REQUEST" and `ai/rules/planning.md` Enforcement, keeping the delegation counter-measures themselves | The harness guard those counter-measures defeat is verifiably still present in an Opus 5 session, so the rules stay. Only the sentence blaming "those models" is stale, and it invites a future session to delete a live rule | `plan/spec-knowledge-0-umbrella-deferred-model-selection-policy.md` (re-homed 2026-08-03, same reason) | deferred |
| 2026-08-01 | spec-knowledge-1-corpus | Fix the ~1,725 dead `## Files` paths that remain after band 1-400 is retired | The decay gate lands with a shrink-only baseline instead, following the `plan/.citation-baseline` precedent. Fixing every remaining dead path first is a much larger, separate piece of work | `plan/future/spec-fixit-learned-dead-paths.md` (re-homed 2026-08-03: that spec IS this work, and cites the same predecessor; the original destination `spec-knowledge-1-corpus` closed without doing it) | deferred <!-- doc-links: ignore (the holder spec was deleted as obsolete in cfeaa7f28) --> |
| 2026-08-01 | spec-knowledge-1-corpus | `TestFamiliesSentEmpty` (`internal/component/bgp/reactor/peer_test.go`) is vacuous: it builds an empty map, asserts it is empty, and calls no production code. `familiesSent` does not exist in production code. It passes against any implementation while its name and comment claim it validates RFC 4724 EOR behaviour. Siblings in the same file may share the shape | Found while extracting summary 033 during the band 1-400 consolidation. Out of scope for the knowledge-system spec set, which changes no BGP code. The producing function `peer_initial_sync.go` is CONFORMANT (sends EOR for all negotiated families, quoting RFC 4724 "including the case when there is no update to send"), so this is a test defect, not a conformance gap | `plan/spec-fixit-vacuous-eor-family-tests.md` (re-homed 2026-08-03: that spec already names `TestFamiliesSentEmpty`; the original destination `spec-knowledge-0-umbrella` closed without doing it). Delivered 2026-08-09: all three vacuous tests removed, replaced by `TestInitialSyncEORReachesTheSilentFamilyToo` and `test/encode/eor-silent-family.ci`, and `peer_initial_sync.go` was confirmed conformant with no change | done |
