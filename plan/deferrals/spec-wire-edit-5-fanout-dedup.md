# Deferrals: spec-wire-edit-5-fanout-dedup

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-02 | `spec-wire-edit-5-fanout-dedup` | A `reject=bgp:` directive inside a `stdin=peer` block is a SILENT NO-OP. Neither the runner's peer-block parser nor `internal/test/peer/expect.go` consumes it, so the line asserts nothing while reading as a guard. Three live sites: `test/plugin/rfc7606-54-discard-unrecognized-nlri.ci`, `test/plugin/filter-family-export-flowspec.ci`, `test/plugin/logging-level-filter.ci`. The fix is a runner GUARD that hard-errors on `reject=` in a peer block, matching the precedent already in place for `option=env:`, plus an audit of the three sites once it fires. Patching the three call sites alone would leave the next one silent. | Found by an independent review of wire-edit children 1-5 on 2026-08-02. It is a test-harness defect, not a wire-edit defect: the surrounding `expect=bgp:...contains=` adjacency assertion does still prove the discard in the observed framing, so no RFC claim rests on the dead line. Fixing the runner touches every functional suite and belongs with the harness owner, not inside a forward-path spec. | `plan/spec-fixit-ci-peer-block-silent-directives.md` | deferred |
| 2026-08-02 | `spec-wire-edit-5-fanout-dedup` | `test/plugin/wire-edit-fanout-dedup.ci` is unfinished and sits in `test/draft/plugin/`, which is gitignored, so it is in no commit and no suite. Child 5's behaviour has mutation-verified Go coverage but no socket-level proof. Two blockers are written into its header: `community { send none }` did not suppress in the fixture, and the `contains=` value came back as a hex-decode error. | The behaviour is proven at the Go level, including the cross-peer leak the design exists to prevent. The missing piece is end-to-end framing, which needs the fixture blockers resolved rather than more design work. | `plan/spec-wire-edit-5-fanout-dedup.md` | deferred |
| 2026-08-02 | `spec-wire-edit-5-fanout-dedup` | AC-11 is not met at fan-out 2 with 2 groups: +3.3% where the spec says "no worse at 2", against -28.6% at 100 destinations in 2 groups. | No adaptive threshold was added on purpose. Any cutoff L silently disables sharing for G >= L, which is the silent cap `ai/rules/no-parking.md` and the umbrella both ban. The trade is +3% worst case for -29% best case, measured by interleaved A/B in one process. Recording it rather than hiding it behind a threshold. | `plan/spec-wire-edit-5-fanout-dedup.md` | deferred |
