# Deferrals: ad-hoc-2026-08-08-ci-31225029268

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`. Nothing stores it (`ai/rules/planning.md`).

Source for every row: the repair of GitHub Actions run 31225029268, which failed
7 of 24 `ze-verify` stages on `main` at `7996eb2f0`. Each row is a defect found
while repairing those stages, separable from them, and recorded rather than
fixed on owner instruction (2026-08-08).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-08 | ad-hoc (found while repairing the CI plugin-suite reds) | `test/plugin/rib-graph.ci` times out at 20s run ALONE on an unloaded box, with every one of its own assertions reporting OK and every expected message received. A test that finishes its work and never terminates is a defect in whatever should signal completion: a barrier that never fires, an observer that never exits, or a shutdown that never arrives. The producing function is not yet identified | Separable from the CI repair: `rib-graph` was not among the 7 failing stages, and it was found only because the plugin suite was re-run to verify unrelated fixes. Raising the timeout is explicitly NOT the fix, since the assertions already pass; the hang is the finding | `plan/spec-fixit-rib-graph-ci-never-terminates.md` | deferred |
| 2026-08-08 | ad-hoc (found while re-running the plugin suite to verify the peer-establishment gate fix) | `test/plugin/forward-overflow-two-tier.ci` failed ONCE with an `ordered:` needle against a 246-byte `fwdBucketMerge` UPDATE. Not reproduced since, and not attributed to any change in this tree | Unattributed and unreproduced, so it does not meet the bar for a known-failures shard either: that path requires a reproduction attempt on the record (`ai/rules/completion.md`). The next holder should first establish whether it reproduces at all, alone and with `ZE_PLUGIN_PARALLEL` well above the core count, before treating it as a defect | `plan/spec-fixit-forward-overflow-two-tier-flake.md` | deferred |
| 2026-08-08 | ad-hoc (closing review of the opaque-NLRI withdrawal fix) | The route-server opaque (BGP-LS) withdrawal fix in `internal/component/bgp/plugins/rs` is proven by UNIT tests only. `test/plugin/rfc7606-54-bgpls-override-propagates.ci` CANNOT catch the defect on any platform: it was run with the fix disabled and still passed, because its assertions stop at the announcement and no route-server `.ci` ever closes a peer. Only a running daemon proves the daemon (`ai/rules/interop-and-goal-validation.md`) | The fix itself is landed and its unit tests are proven to discriminate, so the wire-visible defect is closed; what is missing is functional proof. Separable, and it is its own package: an attempt hit two traps worth inheriting. The `.ci` phase counter is SHARED by every connection of one `ze-peer`, so an assertion at seq=2 holds the phase below an `action=close` at seq=3 and the test deadlocks. On darwin the two peers need 15s or more to establish because of the local bind of 127.0.0.2, which is plausibly the same asymmetry that made this class of failure linux-only in CI | `plan/spec-fixit-bgpls-withdrawal-functional-proof.md` | deferred |
| 2026-08-08 | ad-hoc (found while repairing the `ze-doc-test` learned-staleness stage) | `path_problem` (`scripts/dev/learned_staleness.py`) tests a citation with `os.path.exists` against the FILESYSTEM, so the dead-reference count is a property of the checkout it runs in rather than of the corpus. A developer tree holds gitignored build outputs and generated files a fresh CI clone never has, so `--write-baseline` from a dev checkout records a ceiling CI cannot reach. Measured: 317 locally against 330 for `origin/main` extracted with `git archive`, and all 13 extra findings are citations to never-tracked paths (`test/perf/results/*.json`, `CLAUDE.md`, three directories under `.claude/`) | NOT deferred for lack of a fix: this row exists so the finding is not lost. The work was handed to another session on 2026-08-08 with a reproduction recipe. It also collides with a blocking rule, since `ai/rules/planning.md` states the ceiling is "drained, never removed and never widened" while `--raise-baseline` is literally a widening. The route that avoids the collision is to drain those 13 citations first, after which the tracked-set count lands at 317 and no raise is needed | `plan/spec-fixit-learned-staleness-measurement.md` (assigned to another session 2026-08-08) | assigned |
