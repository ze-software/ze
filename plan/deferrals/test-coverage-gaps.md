# Deferrals: test-coverage-gaps

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-10 | spec-test-coverage-gaps AC-3 (ipsec-sa-installed.ci + ipsec-sa-show.ci + ipsec-peer-show.ci + ipsec-clear-sa.ci + ipsec-child-rekey.ci) | Assert the `vpn-ipsec/sa-up` (+ child-up/child-rekey) EVENTS via the `.ci` engine-step executor (all five `.ci`s prove two-ze IKEv2 establishment via `show vpn ipsec sa`/`peer` gated on the negotiated aes-cbc algorithm; clear-sa exercises `clear vpn ipsec sa`; child-rekey polls the SA `rekey-count` after a short-lifetime rekey; the event assertions were dropped). Blocked on two real product gaps, not test infra: (1) `clear vpn ipsec sa` does not re-establish against a live responder (initiator re-sends IKE_SA_INIT, responder holding the stale SA drops it — 2 sent / 1 accepted); (2) startup event subscriptions are namespace-locked to bgp (`registerSubscriptions`→`DefaultEventNamespace()`, `plugin/server/dispatch.go`) and delivered events carry no ns/name envelope (`payloadToJSON`, `dispatch.go`) | Two engine bugs surfaced by the two-ze IKE test; event delivery itself proven by `deliver_subscriber_test.go` **HALF THE BLOCKER IS DEAD, verified at the producer 2026-09-03.** The namespace half is gone: `resolveSubscriptionNamespace` (`internal/component/plugin/server/dispatch.go`) returns `events.LookupNamespaceID(requested)` for any requested namespace `events.IsValidNamespace` accepts, so a subscription is no longer locked to bgp. Three `.ci` headers still state the opposite and are stale on that point: `ipsec-sa-show.ci`, `ipsec-peer-show.ci` and `ipsec-status-show.ci` each say the only pre-establishment subscribe path is namespace-locked to bgp. The `expect=event:namespace=<ns>:name=<name>` directive exists and parses (`parseEngineExpectEvent`, `internal/test/runner/engine_steps.go`), and NO `.ci` in the tree uses it yet. NOT verified here, and what remains: the timing half, whether the initial `sa-up` still fires before any subscription can be placed. Settle that before rewriting the three headers. | `plan/future/spec-ci-coverage-remaining-surfaces.md` | deferred |
| 2026-07-10 | spec-test-coverage-gaps AC-3 (ipsec-dpd-timeout.ci, DELETED) | End-to-end DPD liveness-teardown `.ci`: prove the initiator tears down the SA (child-down/sa-down) when the responder goes silent. Un-runnable: DPD fires only on peer death, and the `.ci` runner has no primitive to stop a background daemon mid-test (its `Process.Kill()` calls are teardown/timeout only, `runner_exec.go`). DPD timer/probe logic is unit-tested (`engine/dpd_test.go`, `established_test.go`); only the kill-and-observe leg is blocked. The deleted `.ci` was a single non-establishing daemon asserting the bgp-locked sa-down/child-down events. | Needs a runner "stop background process at step N" primitive **ATTEMPTED 2026-09-03. The stated blocker is DEAD and a SECOND one stands, unstated until now.** Dead: the runner does have a stop primitive. `cmd=stop:seq=N:name=NAME[:signal=kill **SHARPENED 2026-09-03, after chasing the one escape that looked like it avoided a runner change.** `request shutdown` IS a real daemon command (`internal/component/plugin/server/system.go`), dispatchable by a plugin, and several `.ci` already use it, so a daemon CAN be made to exit at a chosen moment from inside a test. It cannot be aimed at the responder here: the engine-step list is RECORD-GLOBAL. `command=` and `expect=` lines become `rec.EngineSteps`, the runner marshals them into ONE `engine-steps.json` in the shared work directory (`EngineStepsFileName`, `internal/test/runner/engine_steps.go`), and only the daemon whose config declares the `external engine-steps` plugin executes them. Two daemons in one record cannot carry different step lists, so the responder cannot be given "wait, then shut yourself down" while the initiator watches. Inverting the roles does not help either: `expect=stderr:` assertions ride the FOREGROUND process, so making the responder foreground so a `timeout=` kills it aims every assertion at the wrong daemon. That leaves the two answers already stated: Go fixtures driving two background daemons, or a runner change. **UNBLOCKED 2026-09-03. The primitive was there all along, parsed and thrown away.** `cmd=background` has documented `timeout=DUR` in its own format comment since it was written (`record_parse_cmd.go`), the parser sets `rc.Timeout` for every mode, and the only reader was `resolveOrchestratedTimeout`, gated on `cmd.Mode == modeForeground`. A background `timeout=` therefore reached nothing: accepted, validated, silent. `startBackgroundLifetime` (`internal/test/runner/runner_exec.go`) now gives it the meaning the syntax advertises, a LIFETIME, and stops the process that long after it starts. Kill rather than a signal, because the point is a peer that STOPS ANSWERING and a graceful stop would let it say goodbye on the wire and prove nothing. That is the controlled death this row needs and `cmd=stop` cannot give: a stop fires only between `cmd=` steps, so it cannot reach a peer while a foreground daemon is still running its own, and a declared lifetime can. The shape is now writable with no further runner work: responder `cmd=background:...:timeout=20s`, initiator `cmd=foreground` carrying the engine-steps executor, so `expect=stderr:` stays on the initiator where the teardown is observed. **DONE 2026-09-03: `test/ipsec/ipsec-dpd-timeout.ci` exists and the suite is 19/19.** The responder is backgrounded with `timeout=20s`, so it is KILLED mid-test rather than stopped gracefully, which is the distinction the test rests on: a graceful stop sends a Delete and proves nothing about liveness, a kill makes the peer stop ANSWERING. The initiator is foreground so `expect=stderr:` reads the daemon whose verdict matters, and carries the engine-steps executor so its `command=` steps run against its own state. `action clear` rather than `restart`, so the SA's absence is stable instead of a window between a teardown and its retry. Four assertions, each load-bearing: `aes-cbc` present first so what follows is a teardown and not a failure to connect, `aes-cbc` absent after so the SA is gone, `dpd: peer dead` by name, and `dpd: sent probe` without which the SA could have vanished for any other reason. DISCRIMINATION MEASURED both ways: removing the responder's lifetime, so the peer stays alive and keeps answering, turns it RED at 123.5s; restoring it turns it GREEN at 28.9s. |term]` reaches `stopNamedBackground` (`internal/test/runner/runner_exec.go`), `test/runner/stop-background.ci` proves the wiring, and four ipsec `.ci` already use it. Live: the ASSERTION mechanism cannot observe a mid-test stop. The sibling `ipsec-dpd-holds-tunnel.ci` asserts through `command=` steps polling a FOREGROUND daemon, `cmd=` steps run in sequence, and a foreground daemon occupies its step for the whole run, so a `cmd=stop` scheduled after it fires only once the daemon has already exited. Both daemons must therefore be BACKGROUND and named, with the driving done by a Go fixture, which is the `ipsec-peer-reload-leaves-tunnel-alone` shape. That costs two new fixtures in `internal/test/fixture/`, and the existing ipsec fixtures there read kernel XFRM state through `ip xfrm`, which this test must not do: it runs `ze_test_ike_dataplane=noop` precisely to stay unprivileged. A fixture would have to reach the daemon's own `show vpn ipsec sa`, and the one fixture that talks to a running ze does it over SSH after `ze init` (`netfilter_fixture_firewall.go`), which would make a DPD test depend on the SSH CLI path. The cheaper answer is a runner feature rather than a test: let `command=` polling interleave with a `cmd=stop` against a background daemon. | `plan/future/spec-ci-coverage-remaining-surfaces.md` (re-homed 2026-08-17 from `spec-finish-ci-coverage`, which closed against its op-1 phase alone. That skeleton was CREATED 2026-07-16 for this row, which had previously said "new when picked up" -- not a destination. Verified at the producer: `runner_exec.go` tracks `bgProcs` and `:431-436` kills them only in a deferred teardown, so no mid-test stop primitive exists) | done |
| 2026-07-10 | spec-test-coverage-gaps AC-3 (ipsec-monitor.ci, DELETED) | `monitor vpn ipsec` streaming `.ci`: prove the monitor surface emits sa-up/child-up as they occur. Un-runnable: lifecycle events reach subscribers only via the startup event subscription, namespace-locked to bgp (`dispatch.go registerSubscriptions`), and sa-up/child-up fire at establishment before the engine-steps stream can attach. The deleted `.ci` was a single non-establishing daemon. | Same subscription gap as the dropped sa-up event assertions (spec-fixit-plugin-event-subscription unblocks both) | `plan/future/spec-ci-coverage-remaining-surfaces.md` | deferred |
| 2026-07-10 | spec-test-coverage-gaps W-3..W-9 + ipsec AC-3 remainder | mrt, gokrazy proxy (+gokrazyutil AuthHeader bug), static/vpp, aigp, telemetry collectors, functional-gate misses, cmd/core unit deserts, FSM fuzz, VPP renames, linux back-fill; plus the 6 remaining ipsec `.ci` on the two-ze + engine-step pattern | Resolved 2026-07-11: W-1..W-9 all committed and spec closed. The 5 genuinely-blocked sub-legs are tracked in their own rows below + named fixit specs | `spec-test-coverage-gaps` (closed) | done |
| 2026-07-10 | spec-test-coverage-gaps W-3 static/vpp wiring | Interface-only static next-hops on the VPP data plane (map a logical interface name to a VPP `sw_if_index` so `toVPPRoute` can emit interface-scoped paths instead of erroring). Address/recursive next-hops, blackhole and reject work; interface-only next-hops currently return an error rather than program an index-0 path | Needs the iface/vpp name→sw_if_index resolver threaded into the static backend; address next-hops (the common static-default case) already work, so this is a bounded follow-up not a blocker | `spec-finish-ci-coverage` (groups the VPP leg with the linux `iface.Resolve` leg found 2026-07-15) | done |

