# Deferrals -- spec-rfcgate-6-supported-extraction-signoff

Source: `plan/spec-rfcgate-6-supported-extraction-signoff.md`. Format: `ai/rules/planning.md`.

The spec's own findings live in its `## Walk Findings (AC-8)` section, not here. This shard
carries only what leaves the spec: work a walk uncovered that this spec does not do, and the
decisions an owner owes.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-08-31 | spec-rfcgate-6, rfc5176 walk | RFC 5176 Section 3.4: the Message-Authenticator and Request Authenticator computations are inverted in both directions, so no conformant Dynamic Authorization Client can authenticate to Ze | NOT deferred. `ai/rules/rfc-compliance.md` makes a reachable conformance fix something to do and report, never to ask about, and rung 2 of `ai/rules/rule-precedence.md` puts that above this spec's scope. Verified at both producers (`VerifyMessageAuthenticator` and `VerifyCoARequestAuth`, `internal/component/radius/packet.go`) and against `rfc/full/rfc5176.txt` before dispatch | fixed in this spec's session | in progress |
| 2026-08-31 | spec-rfcgate-6, rfc5176 walk | The other nine RFC 5176 defect classes: Service-Type never read; attributes not treated as mandatory; only the first matching session acted on; Proxy-State and State not echoed; a stale Event-Timestamp NAK'd where Section 6.3 says silently discard; CoA state changes not atomic; Termination-Action State echo absent | A conformance programme, not a walk. `rfc/extraction/rfc5176.json` cannot sign until they resolve, because 23 of 72 sites state obligations Ze does not meet and no exclusion kind in the closed set honestly covers an unmet obligation **FOUR OF THE NINE ARE DEAD, verified at the producer 2026-09-03 in `internal/component/l2tp/plugins/authradius/coa.go` (this row's `internal/component/radius/coa.go` is the wrong path).** (1) Service-Type IS read: the CoA path tests `pkt.FindAttr(radius.AttrServiceType)` and quotes Section 3.2's Authorize-Only rule above the branch. (2) Attributes ARE treated as mandatory: two allow-listed attribute sets gate what a CoA-Request and a Disconnect-Request may carry. (3) Proxy-State and State ARE echoed: the response builder copies every attribute whose type is `AttrProxyState` or `AttrState`. (4) A stale Event-Timestamp is SILENTLY DISCARDED, not NAK'd: the `eventTimestampStale` arm logs and returns with no answer, quoting Section 6.3 verbatim, and the comment gives the reason a NAK would be wrong, that it tells a replaying sender the secret is right. An absent Event-Timestamp draws `ErrorCauseInvalidRequest`, which is Section 6.3's SHOULD-be-configurable-to-discard read as refuse-rather-than-honor. WHAT REMAINS, and all this row still owes: only the first matching session acted on; CoA state changes not atomic; Termination-Action State echo absent. | needs a spec; Thomas decides whether it runs | open |
| 2026-08-31 | spec-rfcgate-6, rfc5176 walk | RFC 5176 Section 3.2 "Authorize Only" is an explicit OPTIONAL | A MAY clause. `ai/rules/rfc-compliance.md` is explicit that an agent is not authorized to pick: implement it, decline it, or make it a config option. Declining still owes the Section 2.2 CoA-NAK for an unsupported Service-Type | Thomas | open |
| 2026-08-31 | spec-rfcgate-6, rfc2865 walk | RFC 2865 Section 5.25, Class attribute: "The client MUST NOT interpret the attribute locally", against Ze's shipped `profile-attribute class` feature (`mapProfiles`, `internal/component/radius/authenticator.go`; documented in `docs/guide/radius.md`, "Profile mapping") | Not a slip and not an agent's call. A documented, shipped feature sits directly on a MUST NOT, so the answer is either to drop the option or to authorise the deviation and record it as a `plan/journal/` row carrying the RFC section and the reason, which `ai/rules/rfc-compliance.md` requires of an authorised deviation | Thomas | open |
| 2026-08-31 | spec-rfcgate-6, rfc2865 walk | Seven ordinary RFC 2865 conformance fixes: Request Authenticator not regenerated per Identifier on failover (Section 4.1); Access-Challenge not treated as Access-Reject on the admin path (Section 4.4); empty shared secret accepted (Section 3); Service-Type written but never read (Sections 5.6 and 1.1); Access-Request built with no credential attribute (Section 4.1); zero-length User-Name reaching the wire (Section 5) | NOT deferred, only queued. `internal/component/radius/client.go` and `internal/component/radius/packet.go` are held by two other agents in this checkout, so dispatching now would collide. The failover and empty-secret rows were verified at `SendToServers` and `ExtractConfig` by the main thread **CLOSED 2026-09-03. Every item verified at its producer, and each carries the RFC sentence in the code beside it.** Request Authenticator on failover: `(*Client).SendToServers` (`internal/component/radius/client.go`) takes a new Identifier per server and calls `RandomAuthenticator()`, quoting Section 4.1's "The Request Authenticator value MUST be changed each time a new Identifier is used" and Section 2.5, and noting that writing it also re-derives the User-Password keystream. Access-Challenge on the admin path: `(*radiusAuthenticator).Authenticate` (`authenticator.go`) has a `CodeAccessChallenge` arm quoting Section 4.4's "MUST treat an Access-Challenge as though it had received an Access-Reject", and it returns `ErrAuthRejected` rather than a plain error so the chain does not read a challenge as an infrastructure failure. Empty shared secret: refused at `client.go` on `len(secret) == 0`, with `config.go` recording that RFC 2865 forbids it outright so there is no partial form. Service-Type read: `AcceptedServiceType` (`attr.go`). Credential attribute: the Access-Request attrs always carry `AttrUserPassword`. Zero-length User-Name: `AppendTextAttr` omits the attribute entirely, quoting Section 5's "Text of length zero (0) MUST NOT be sent; omit the entire attribute instead" and noting NAS-Identifier already meets Section 4.1's MUST. The row's title says seven and lists six; the seventh, `profile-attribute class`, is the sibling row above, where `profileAttrType` now discards its argument and returns `AttrFilterID`. | dispatched as one package once those files are released | done |
| 2026-08-31 | spec-rfcgate-6, rfc2865 walk | Add the 28 new MUST-level ids `rfc/short/rfc2865.md` does not declare, with two tagged tests each, and finish the remaining 35 sites | `rfc/short/rfc2865.md` declares 13 ids for a 76-page base protocol carrying 74 normative sites. The gap is structural, and the ids cannot land before the conformance rows above resolve: a new MUST id without its tests reds the corpus for every session in this shared checkout | follows the conformance package | open |
| 2026-08-31 | spec-rfcgate-6 | Partial walk artifacts preserved at `plan/deferrals/rfcgate-6-partial-walks/rfc2865.json` (39 of 74 sites, 77 of 77 sections) and `rfc5176.json` (26 of 72 sites, 28 of 28 sections) | They were written to `tmp/session/.../scratch/`, which is cleaned on a cadence. That is the decay this session filed a row about in `plan/journal/claim-outlives-the-evidence-it-cites.md` on the same day, so leaving ~65 hand classifications there would have been an instance of the class the row describes. Copied into a tracked path the RFC gate does not read, confirmed by running `./le rfc check` after the copy | resumed by whichever spec finishes the two stems | preserved |

| 2026-09-01 | spec-rfcgate-6, rfc3748 second walk | Fourteen RFC 3748 sites state obligations Ze MEETS that `rfc/short/rfc3748.md` does not declare: `2:1`, `2.2:1`, `4.1:1`, `4.1:3`, `4.1:6`, `4.1:13`, `4.2:4`, `4.2:7`, `4.2:8`, `4.2:9`, `4.2:14`, `7.10:3`, `7.10:10`, `7.10:11`. The reason each is met, with its producer, is the `RESIDUAL` table in `plan/deferrals/rfcgate-6-partial-walks/classify-rfc3748.py` | Not a classification. A met-and-undeclared site has no honest exclusion kind, so its disposition is `mapped`, which needs the row; and a MUST-level row of an enrolled RFC is gated the moment it is written, so the row needs a positive and a negative tagged test and a discrimination record for each. Fourteen rows, up to 28 tagged tests and 28 records. Several are genuinely single-polarity (`4.1:13` and `2:1` are met by construction, with no negative case), so the test count is a ceiling. `4.2:8` is the sharp one: `stateLastWord` (`internal/core/eap/eap.go`) exists solely to satisfy it and quotes the sentence in its own doc comment, and no row records it | needs a spec; Thomas decides whether it runs | open |
| 2026-09-01 | spec-rfcgate-6, rfc3748 second walk | Two reachable RFC 3748 conformance gaps. Section 2.1: a peer receiving a Request of a Type the method did not ask for "MUST treat them as invalid, and silently discard them", where `handleMSCHAPv2Request` (`internal/core/eap/peer.go`) answers with an error that kills the SA (site `2.1:2`). Section 4.1: "An authenticator receiving a Response whose Identifier value does not match that of the currently outstanding Request MUST silently discard the Response", and `Session.Process` (`eap.go`) never compares `response.Identifier` against `s.identifier` (site `4.1:10`) | NOT deferred, only queued behind the row package above, because each fix owes the same row plus two tagged tests that the package defines. Both are small: the discard mechanism (`peerDiscard`, an explicit `PeerResult.Discarded`) landed in this session's Section 4.2 work | follows the row package | open |
| 2026-09-01 | spec-rfcgate-6, rfc3748 second walk | Two RFC 3748 obligations whose fix turns an RFC-tagged test red. Section 4.1: "An EAP server receiving a Response not meeting these requirements MUST silently discard it", against the `RFC3748-2.1-1` NEGATIVE arm of `TestRFC3748OneMethodPerConversation` (`rfc3748_test.go`), which requires an EAP-Failure for a Type-mismatched Response by name (site `4.1:16`). Section 4.2: "The peer MUST silently discard Success packets" once the method completes unsuccessfully, against the `RFC2759-x-7` NEGATIVE arm of `TestRFC2759PeerEndsSessionOnBadAuthenticatorResponse` (`rfc2759_authenticator_response_test.go`), which requires `errSessionEnded` by name (site `4.2:11`) | Not an agent's call. `ai/rules/rfc-compliance.md` says a test that pins non-conformant behavior is fixed at the code and then corrected, and `ai/rules/testing.md` routes correcting an RFC-tagged test through the owner and a row in `test/rfc-changed.md`. The question is which way to fix each, never whether to skip it. Neither is a security defect: the peer hands out no MSK in either case, and the divergence is that Ze reports an error where the RFC asks for silence | Thomas | open |
| 2026-09-01 | spec-rfcgate-6, rfc3748 second walk | UPDATED 2026-09-01: three of the four sites are dispositioned and ONE remains. D-1 was answered by Thomas, who withdrew the 2026-08-30 deviation and ordered MD5-Challenge implemented, so `5:2`, `5.4:1` and `5.4:2` are now `mapped` to `RFC3748-5-2`, `RFC3748-5.4-1` and `RFC3748-5.4-2`. No exclusion kind for an authorized deviation was ever needed. `5.2:4` alone still waits, on D-2 of `plan/spec-eap-notification-and-nak.md` (whether Ze's authenticator ever SENDS a Notification Request, which is a MAY an agent may not pick) | The walk itself is done: 81 of 103 sites carry a disposition, and every one of the 22 that do not carries a recorded reason and a named blocker rather than an annotation. The classified artifact is preserved at `plan/deferrals/rfcgate-6-partial-walks/rfc3748.json` and its replay is `classify-rfc3748.py` beside it | the row package, then D-1 and D-2 | open |
| 2026-09-01 | spec-rfcgate-6, rfc3748 second walk | Eighteen rfc3748 sites moved from `binds-another-role` to `feature-out-of-scope`, and the three absent features are now disclosed in `docs/features/rfc-status.md`: pass-through operation ("Support for pass-through is optional", Section 2), the OTP and GTC methods, and the Expanded Type namespace ("Implementations MAY support other Types defined here or in future RFCs", Section 5) | Not deferred, corrected. `binds-another-role` is presumed wrong (`ai/rules/CORE.md`) and it was wrong on all eighteen: each names a role Ze DOES play declining an option the RFC states in its own words, so the old label published "not our problem" where the truth is "our problem, declined". Sixteen keep the kind, and every reason now names the role as a document-authoring one and cites the producer that would act as it if Ze did | done in this session | closed |

## Note on the two blocked stems

`rfc5176` and `rfc2865` are the first two Tier 1 walks and both are `Supported` on
`docs/features/rfc-status.md`. Neither can sign. That is the spec's premise landing rather
than failing: the artifact exists to bound a checklist against the RFC's own text, and both
checklists turned out to be bounded by what somebody happened to write down.

The consequence for `plan/deferrals/rfcgate-0-umbrella.md` row D5, which wants a measured
drain rate before the quota is armed: Tier 1 throughput is not that measurement. A Tier 1
sign-off is gated on conformance work rather than on walking effort, so timing it would
measure the wrong thing. Tier 4 is the tier to measure.
