# Deferrals: rfcgate-0-umbrella

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-29 | spec-rfcgate-0-umbrella (D4) | **The drain itself: extraction sign-offs for the 166 enrolled RFCs, audit verdicts for the 930 tested-but-unaudited requirements, and re-derivation of the 1376 `{gap}`/`{not-applicable}` annotations that `ai/rules/rfc-compliance.md` voided as authority on 2026-07-27.** The `rfcgate` set ships the gates, the ratchets, the artifact formats and the published counters; it drains none of the backlog. Sizing, all independently verified: 2720 gated MUST-level requirements across 166 enrolled RFCs, of which 974 have both polarities tested; 44 of 974 carry an audit verdict (4.5%), all in `rfc/audit/rfc7606.json`; an estimated 1200-1500 obligations were never extracted at all | Owner decision D4 (Thomas, 2026-07-29): machinery now, then a large parallel fleet. The order is load-bearing rather than a preference. Until child 1 lands there is no artifact format for a sign-off to be written into, no ratchet to make a signed-off count monotonic, and no published denominator, so a fleet dispatched first would produce records the gate cannot read and counts nothing can hold. The one exception the owner carved is already in scope and not deferred: rfc7296 is re-authored by child 1 phase 8, because two of its unextracted MUSTs are confirmed (`rfc/full/rfc7296.txt:1397` and `:1439`) and a confirmed unextracted MUST escapes the grandfather by definition (D7, "found means owed") | `plan/spec-followup-rfc-enrollment.md` (already owns `rfc/enrolled.txt` and the coverage rollup; its own content is stale and is rewritten once child 1 makes the backlog derivable, per the umbrella's "Relationship to spec-followup-rfc-enrollment" section) | deferred |
| 2026-07-29 | spec-rfcgate-0-umbrella (D5, R-2) | **Arming the drain quota.** The forcing function ships at rate 0, so the machinery lands inert and the floor comparison (implemented by child 1, `check_drain_floor`) is satisfied trivially until a rate is set. Arming is a one-line edit to the authored policy file plus the commit that carries it | The repository has no release concept to anchor "N per release" against: `git tag --list` is empty, there is no VERSION file, and `ze-release-evidence-verify` (`mk/test-release.mk`) is an unscheduled manual target. A guessed rate is the one failure mode that loses the forcing function permanently, because `ai/rules/rfc-compliance.md` says a rule that reds the gate on unrelated work gets removed rather than obeyed. Owner ruling (Thomas, 2026-07-29): ship at 0 and arm it after the first fleet batch measures real throughput. Recorded as an ACCEPTED risk, not a forgotten one -- a quota that ships inert is also a quota that may never arm, and only the owner can close that | `plan/spec-followup-rfc-enrollment.md` (**re-homed 2026-07-30 at closure.** The old Destination was this shard's own source spec, which closes now, so the row would have dangled. That spec already owns `rfc/enrolled.txt` and the coverage rollup, and arming the quota is the same programme as draining the backlog the quota measures. The authored policy stays `rfc/drain-budget.txt` and the comparison stays child 1's `check_drain_floor`) | deferred |
