# Deferrals: fixit-forward-readbuf-leak

Deferral rows for this source. The aggregate live backlog is folded on
read from `plan/deferrals/` by `/ze-status`; nothing stores it (`ai/rules/planning.md`).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-07-21 | spec-fixit-forward-readbuf-leak (adjacent observation) | Outgoing peer-pool MOD buffer leak on the body-build failure path: `forwardUpdateCore` acquires a mod buffer (`reactor_api_forward.go`) then `continue`s at :627-628 when `buildFwdBody` fails, dropping the item without `Return`; same shape in `forward_rs.go`/:436-437. Distinct from the read-pool leak this spec fixed (different pool, failure path only) | Found while verifying the read-pool fix; explicitly OUT of this spec's scope ("flag for triage, do not fix here without approval"); recorded in the spec Notes which vanish on closure, so homed here + in learned 1234 | **DONE 2026-08-07.** Re-confirmed live against the post-fan-out-dedup tree, on BOTH rails. Fixed by a call to `fwdPool.releaseItem` before the `continue` in `forwardUpdateCore` (`reactor_api_forward.go`) and `reactorForwardRS` (`forward_rs.go`). `TestForwardUpdateCoreReturnsModBufOnBodyFailure` and `TestForwardRSReturnsModBufOnBodyFailure` (`forward_modbuf_leak_test.go`) both go RED with the fix reverted, on all twenty repetitions of `make ze-race-reactor`, and report a free count of 63 where 64 is required. `TestForwardModBufTakenOnRebuild` is the control that proves the pool did lend the buffer, so the two assertions are not vacuous. An independent reviewer re-walked every exit on both rails and confirmed the enumeration. Spec: plan/spec-fixit-forward-readbuf-leak-deferred-pool-release-paths.md | done |
| 2026-07-21 | spec-fixit-forward-readbuf-leak (adjacent observation) | Pool-stopped `DispatchOverflow` calls `done()` but not `releaseItem` (`forward_pool.go`,:617-623) -- shutdown-window-only leak of item resources | Found while verifying the read-pool fix; OUT of this spec's scope (forward_pool.go untouched per D-1); shutdown-window only. Recorded in learned 1234 | **DONE, and not by this work.** Commit `027f6b0b3` already released the item on both stopped branches and landed `forward_pool_stopped_release_test.go`. The function was re-read on 2026-08-07 and both branches call `releaseItem` before `done()`. The test file records its own coverage limit: only the first of the two stopped branches is asserted, because the second needs a synchronization seam the forwarding hot path does not have. Spec: plan/spec-fixit-forward-readbuf-leak-deferred-pool-release-paths.md | done |
| 2026-08-07 | spec-fixit-forward-readbuf-leak-deferred-pool-release-paths (gate failure, not caused by it) | `TestForwardAdoptedHandleHeldUntilLastWrite` (`forward_readbuf_leak_test.go`) fails under `make ze-race-reactor` at roughly the fifteenth of its twenty repetitions: `len(update.fwdHandles)` is 0 where the test requires 1, so `fwdUpdateForDestination` borrowed no read-pool handle and fell back to `make`. Reproduces on the same repetition WITH and WITHOUT this spec's change, so it is not this spec's; passes at `-count=20` when run alone, so it needs the whole package's traffic through the package-global `bufMuxStd`. Mechanism NOT determined: the shared byte budget was ruled out (the three tests that set it restore it with `defer`), leaving the collapse probe and a possible read-pool leak elsewhere in the package unexamined | Found while running this spec's own required gate. Attributed with a baseline run rather than assumed. Not folded into this work: the goal here is the Outgoing Peer Pool release, a different pool and a different path, and `ai/rules/rule-precedence.md` puts a defect the goal does not depend on AFTER closing rather than inside the closing commit | plan/spec-fixit-forward-readbuf-leak-deferred-pool-release-paths.md (that spec owns read-pool and peer-pool lifetime in this package, so it is the right home; this row is about a handle NOT borrowed, where its landed work was about a buffer not released). Reproduce with `make ze-race-reactor`. Start at `withCollapseProbe` / `BufMux.collapseLocked` (`bufmux.go`) and at whether `bufMuxStd` in-use drifts across repetitions | open |

