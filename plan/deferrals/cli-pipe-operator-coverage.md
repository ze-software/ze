# Deferrals: spec-cli-pipe-operator-coverage

Rows deferred from spec-cli-pipe-operator-coverage, which is in progress.
Each row names where the work goes, so nothing is recorded without a
destination.

The spec runs in six phases and its Failure Routing says what lands here: a
defect found in a phase that does NOT block that phase gets a spec and an ask,
and its row goes here. A defect that blocks the phase is fixed in it.

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|

Nothing deferred yet. Phases 1 to 3 each fixed what they found.
