# Spec: a related ICMP error belongs to the GTSM session it quotes, whoever sent it

| Field | Value |
|-------|-------|
| Status | ready |
| Scope | protocol |
| Depends | - |
| Phase | - |
| Handoff | - |
| Updated | 2026-09-15 |

Scope is protocol: the change is to what the RFC 5082 related-message filter
claims. No config leaf, no CLI command.

Recovery after compaction: `.claude/rules/post-compaction.md`.

## Task

RFC 5082 Section 3 sorts a packet reaching the control plane into Unknown,
Trusted and Dangerous, where Trusted and Dangerous are "packets that have been
identified as belonging to one of the GTSM-enabled sessions", and extends the
TTL rule to "the related ICMP error handling messages". Ze's IPv4 receive half
of that rule is an nftables input table (`peerTerms`,
`internal/component/gtsm/gtsm.go`) whose terms match an ICMP error by four
things: one of the five related ICMP types, the OUTER source address equal to
the peer, the quoted TCP header carrying the BGP port, and the outer TTL below
the peer's floor. The outer-source condition is the defect. An ICMP error is
generated by whichever router met the problem, never necessarily by the peer,
and the kernel delivers it to the session by the header it QUOTES
(`tcp_v4_err`, `net/ipv4/tcp_ipv4.c`, looks the socket up by the quoted 4-tuple
and never reads the outer source). So a forged error from any address but the
peer's, quoting the session's TCP header and arriving with a low outer TTL, is
not claimed by ze's table, reaches `tcp_v4_err`, and is judged there on the
TTL the forger wrote inside the quote. That is the attack Section 6.1 names:
"related messages provide a significant attack vector to e.g., reset protocol
sessions".

A survey of FRR (`bgp_set_socket_ttl`), BIRD (`bgp_connect`), GoBGP
(`SetTCPMinTTLSockopt`) and OpenBGPD (`session_setup_socket`) found that none
does anything for related messages beyond the min-TTL socket option, and that
every kernel path those options drive (Linux `tcp_v4_err`, `tcp_v6_err`,
OpenBSD `tcp_ctlinput`) associates the error with the session by the quoted
header and constrains the outer source nowhere. Owner ruling, 2026-09-15:
association is by the quoted header, any outer source (reading 2).

The goal: a term claims an ICMP error when its quoted IP header's destination
is the peer (the error is about a packet ze sent to the peer), its quoted TCP
header carries the BGP port, and its type is a related type; the outer source
is not read. The outer TTL floor is unchanged. An error quoting another
destination, or another port, stays Unknown and is delivered whatever its TTL
and source (RFC5082-3-4).

## Required Reading

### Architecture Docs
- [ ] `docs/DESIGN.md` - what GTSM covers for a session's related ICMP messages
  → Constraint: the paragraph describing the filter names the association basis and changes with this spec
- [ ] `docs/architecture/firewall/table-ownership-and-shutdown-flush.md` - the `ze_gtsm` owner row
  → Constraint: a daemon-owned table is published through `firewall.RegisterTables` and withdrawn with its configuration; the term shape is the owner's to change
- [ ] `docs/architecture/core-design.md` - the design page the firewall model and the gtsm component declare
  → Constraint: a component holds its own domain and reaches another through the registered seam; gtsm publishes a table through `firewall.RegisterTables` and never speaks nftables
- [ ] `docs/architecture/testing/interop.md` - the vacuity traps
  → Constraint: a receive proof that reads a counter placed BEFORE its matches proves evaluation, not a match (`plan/journal/counter-counts-the-wrong-packets.md`); the proofs read `IcmpMsg InType3` and `TCPMinTTLDrop` from `/proc/thread-self/net`
- [ ] `docs/contributing/rfc-conformance-gates.md` - a changed tagged unit owes a discrimination record in the same change, and a changed tag owes an owner row in `test/rfc-changed/`
  → Constraint: three tagged units change here; the rows are the owner's and the records are re-observed natively on this Linux host

### RFC Summaries (Scope: protocol)
- [ ] `rfc/short/rfc5082.md` - `RFC5082-3-2` (the related-message rule) and `RFC5082-3-4` (never drop Trusted or Unknown)
  → Constraint: "MUST NOT drop (as part of GTSM processing) packets classified as Trusted or Unknown" (Section 3): an error quoting another destination or another port is Unknown and is delivered
  → Constraint: the Enrolment reason and Support remaining cells describe the match and change with this spec
- [ ] `rfc/full/rfc5082.txt` - Section 3 (the three classes), 5.3 (on-link attackers), 6.1 (why related messages matter)
  → Decision: Section 5.5 describes the single-hop case only; on a single-hop session the peer is the only legitimate sender within one hop, so reading 2 claims no legitimate error that reading 1 delivered

**Key insights:** (minimal context to resume after compaction)
- The lowering reads the quoted header at fixed offsets from the transport header base: ICMP header is 8 octets, so the quoted IPv4 header starts at offset 8; its version/IHL byte is at 8 (must be 0x45), its protocol byte at 17, its destination address at 24 (8 + 16), and the quoted TCP ports at 28 and 30 (`lowerICMPErrorQuotedTCPPort`, `internal/plugins/firewall/nft/lower_linux.go`)
- Every raw read is behind `nfprotoGuard(family, unix.NFPROTO_IPV4)` so it cannot reach the IPv6 family
- `peerTerms` builds `len(relatedICMPTypes) * len(quotedPortSides)` terms per peer; the outer-source match is one element of each term's match list

## Current Behavior (MANDATORY)

**Source files read:** (must read BEFORE you write this spec)
- [ ] `internal/component/gtsm/gtsm.go` - `peerTerms` builds each term's matches: `MatchICMPType`, `MatchSourceAddress{Prefix: host}` (the peer /32), `MatchICMPErrorQuotedTCPPort{Port, Side}`, `MatchIPv4TTLBelow{Floor}`; `termName` names a term by peer, type and side
- [ ] `internal/component/firewall/model.go` - `MatchICMPErrorQuotedTCPPort` (daemon-only, IPv4-only, needs a `MatchICMPType` beside it), `MatchIPv4TTLBelow`, the `matchMarker` list
- [ ] `internal/component/firewall/validate.go` - `validateMatch` refuses the quoted-port match outside family `ip`/`inet`, with no side, or with port 0
- [ ] `internal/plugins/firewall/nft/lower_linux.go` - `lowerMatch` dispatch; `lowerICMPErrorQuotedTCPPort` emits the nfproto guard, the 0x45 check at offset 8, the protocol check at offset 17, the port read at 28 or 30
- [ ] `internal/component/gtsm/gtsm_rfc5082_linux_test.go` - `TestGTSMDropsADangerousQuotedICMPError` (source = peer, quoted port 179, TTL below floor: dropped), `TestGTSMDeliversAQuotedICMPErrorAtTTL255`, `TestGTSMDeliversAnICMPErrorNoSessionClaims` (quotes another port: delivered); the frames come from `packet_linux_test.go`
- [ ] `test/firewall/gtsm-related-icmp.ci` and `internal/test/fixture/netfilter_fixture_gtsm.go` - the functional test asserts `nft list table inet ze_gtsm` carries `ip saddr 192.0.2.2 ... drop`
- [ ] `rfc/discrimination/rfc5082.json` - the records for the three receive proofs

**Behavior to preserve:** (unless the user explicitly said to change it)
- The transmit half (the hop-limit host route), the IPv6 receive half (`IPV6_MINHOPCOUNT`), the five related ICMP types, the quoted-port match on both sides, the TTL floor from the peer's MinTTL, the interface-scoped table and its withdrawal
- An error quoting a TCP header on any other port is delivered (RFC5082-3-4), whatever its source and TTL
- An error at or above the floor is delivered

**Behavior to change:** (only what the user asked for)
- The term no longer reads the outer source address. It claims the error by its quoted destination address being the peer, beside the quoted port
- An error from ANY source quoting the session's destination and port, below the floor, is dropped

## Data Flow (MANDATORY - see `ai/rules/architecture.md`)

### Entry Point
- A BGP peer with `connection ttl max N` reaches the reactor's peer reconcile, which publishes the peer set to `gtsm.SetPeers` (unchanged)
- On the wire: an ICMPv4 error datagram arriving on any interface, whose payload quotes an IPv4 header and the first 8 octets of a TCP header

### Transformation Path
1. `peerTerms` (`gtsm.go`) builds each term with `MatchICMPType`, `MatchICMPErrorQuotedDestination{Addr: p.Addr}` in place of `MatchSourceAddress`, `MatchICMPErrorQuotedTCPPort`, `MatchIPv4TTLBelow`
2. `validateMatch` (`validate.go`) accepts the new match in family `ip`/`inet` only, with a valid IPv4 unicast address, and refuses it elsewhere
3. `lowerICMPErrorQuotedDestination` (`lower_linux.go`) emits: `nfprotoGuard(NFPROTO_IPV4)`, the 0x45 check at transport offset 8, a 4-octet read at transport offset 24 compared against the peer address; it does not repeat the protocol check, which the quoted-port match beside it performs
4. The kernel evaluates the term on input; a match with outer TTL below the floor drops before the TCP stack sees it; everything else continues to `tcp_v4_err` as today

### Boundaries Crossed
| Boundary | How | Verified |
|----------|-----|----------|
| gtsm component ↔ firewall model | a new daemon-only `Match` type | No |
| firewall model ↔ nft lowering | `lowerMatch` dispatch case | No |
| lowering ↔ kernel | raw payload read at transport-header offset 24, guarded by nfproto and the 0x45 byte | No |

### Integration Points
- `firewall.Match` marker list and `validateMatch` switch: the new type registers there as `MatchICMPErrorQuotedTCPPort` does
- `lowerMatch`: one new case

### Architectural Verification
| Check | Holds? | Evidence |
|-------|--------|----------|
| No bypassed layers (data flows through the intended path) | Yes | the match goes through the same model, validation and lowering as its sibling |
| No unintended coupling (components stay isolated) | Yes | gtsm holds no nftables knowledge; the lowering holds no BGP knowledge |
| No duplicated functionality (extends existing, does not recreate) | Yes | the 0x45 guard is shared with the sibling through one helper if the lowering already factors it, else the two-line guard is repeated with a comment naming the sibling |
| Zero-copy preserved where applicable (refs, not copies) | N-A | control-plane term construction |
| Registration over hardcoding, outbound | N-A | no new registry surface |
| Registration over hardcoding, inbound | Partly | `validateMatch` and `lowerMatch` are type switches that gain one arm each; that is the existing shape for every `Match` type and the `matchMarker` compile-time list guards a missing arm at the model, while a missing lowering arm is caught by `TestLowerICMPErrorQuotedDestinationReadsTheQuotedAddressUnderAnNfprotoGuard` |

## Risks & Assumptions

### Assumptions
| ID | Assumption | Basis (file/doc/user statement) | If wrong | Validated by | Status |
|----|-----------|--------------------------------|----------|--------------|--------|
| A-1 | An ICMP error ze receives about a GTSM session quotes a packet ZE sent, so the quoted destination is the peer | RFC 792: the error carries "the internet header + 64 bits of the original datagram's data" of the packet that caused it, and a router returns it to that packet's source | the term would need the quoted source too | `TestGTSMDropsADangerousQuotedICMPError` from a third source | unvalidated |
| A-2 | `PayloadBaseTransportHeader` offset 24 is the quoted destination for a 20-octet quoted header | `lowerICMPErrorQuotedTCPPort` reads ports at 28 and 30 for the same layout | the match reads the wrong bytes and drops nothing | the lowering unit test and the kernel proof | unvalidated |
| A-3 | The three receive proofs' frames (`packet_linux_test.go`) can carry an arbitrary outer source | the builder takes addresses as parameters | the proof needs a builder change | reading `packet_linux_test.go` at Phase 1 | unvalidated |

### Risks
| ID | Risk | Early signal | Mitigation / fallback |
|----|------|--------------|----------------------|
| R-1 | A legitimate error from an intermediate router on a multi-hop `ttl max N` session is dropped | `IcmpMsg InType3` advances while `ze_gtsm` counters advance too on a lab with a router in the path | its TTL is 255 minus its hop distance, which the floor 255-N+1 admits for any router within N hops; this is the RFC's rule, not a regression |
| R-2 | The `.ci` and the rendered-ruleset assertion go stale | `gtsm-related-icmp.ci` red on `ip saddr` | the fixture asserts the new term shape (`@th,192,32` or however nft renders the quoted read) |
| R-3 | The tag claims on three proofs stale their records | `./le rfc check` names them | re-observe natively, owner rows in `test/rfc-changed/` |

## Blast Radius

| Question | Answer |
|----------|--------|
| What breaks if this is wrong? | A GTSM peer's related ICMP errors: a wrong offset drops nothing (the hole stays) or drops errors that quote a different destination (a session's PMTU signal lost) |
| How is it reverted? | One commit revert; the table is rebuilt from config on the next reconcile |
| Who else touches this path? | Nobody in flight: `internal/component/gtsm` and the nft lowering are quiet; `spec-copp-*` and `spec-firewall-*` in `plan/` do not touch the quoted-header matches |

## Wiring Test (MANDATORY -- NOT deferrable)

| Entry Point | → | Feature Code | Test |
|-------------|---|--------------|------|
| `gtsm.SetPeers` with one peer | → | `peerTerms` carrying `MatchICMPErrorQuotedDestination` and no `MatchSourceAddress` | `TestGTSMPublishesTheICMPFilterTableForAPeer` (existing, assertion changed) |
| A term carrying the new match reaches the nft backend | → | `lowerICMPErrorQuotedDestination` | `TestLowerICMPErrorQuotedDestinationReadsTheQuotedAddressUnderAnNfprotoGuard` |
| An ICMPv4 error from a third source quoting the session, TTL below floor | → | the kernel drops it before `tcp_v4_err` | `TestGTSMDropsADangerousQuotedICMPError` (case added) |

## Acceptance Criteria

| AC ID | Input / Condition | Expected Behavior |
|-------|-------------------|-------------------|
| AC-1 | A GTSM peer published | Each `ze_gtsm` term matches the quoted destination = peer and the quoted BGP port; no term reads the outer source |
| AC-2 | An ICMPv4 error from a source other than the peer, quoting dst = peer and the BGP port, outer TTL below the floor | Dropped before the TCP stack; `TCPMinTTLDrop` unchanged, `IcmpMsg InType3` unchanged |
| AC-3 | The same error from the peer's own address | Dropped (unchanged) |
| AC-4 | The same error from any source with outer TTL 255 | Delivered |
| AC-5 | An error from any source below the floor quoting dst = peer but a TCP port other than the BGP port | Delivered (RFC5082-3-4) |
| AC-6 | An error from any source below the floor quoting the BGP port but a destination other than the peer | Delivered (RFC5082-3-4) |
| AC-7 | A quoted header carrying IP options (version/IHL not 0x45) | Matches nothing; delivered |
| AC-8 | The new match in a table of family `ip6` | Refused by `validateMatch` |

## End-to-End User Stories

| # | User does | Path through system | Test proving it works |
|---|-----------|--------------------|-----------------------|
| 1 | Configures `connection ttl max 1` and expects a forged port-unreachable from an off-link host, quoting the session, not to reach TCP | config → reactor reconcile → `gtsm.SetPeers` → `peerTerms` → nft input drop on the quoted destination | `TestGTSMDropsADangerousQuotedICMPError` third-source case |
| 2 | Runs `show firewall ruleset ze_gtsm` and sees terms keyed on the quoted destination | config → `ze_gtsm` table → rendered ruleset | `test/firewall/gtsm-related-icmp.ci` |

## 🧪 TDD Test Plan

### Unit Tests
| Test | File | Validates | Status |
|------|------|-----------|--------|
| `TestGTSMPublishesTheICMPFilterTableForAPeer` | `internal/component/gtsm/gtsm_test.go` | the term shape: quoted destination, no outer source (existing test, assertion changed) | |
| `TestLowerICMPErrorQuotedDestinationReadsTheQuotedAddressUnderAnNfprotoGuard` | `internal/plugins/firewall/nft/lower_linux_test.go` | nfproto guard, the 0x45 check, the 4-octet read at offset 24 | |
| `TestValidateRefusesQuotedDestinationOutsideIPv4` | `internal/component/firewall/validate_test.go` | AC-8, and a zero address refused | |
| `TestGTSMDropsADangerousQuotedICMPError` | `internal/component/gtsm/gtsm_rfc5082_linux_test.go` | AC-2 and AC-3: RFC5082-3-2 receive IPv4 positive, now from the peer AND from a third source | |
| `TestGTSMDeliversAQuotedICMPErrorAtTTL255` | same | AC-4: RFC5082-3-2 receive IPv4 negative, from a third source too | |
| `TestGTSMDeliversAnICMPErrorNoSessionClaims` | same | AC-5, AC-6, AC-7: the RFC5082-3-4 guard, three cases | |

### Boundary Tests (numeric inputs)
| Field | Range | Last Valid | Invalid Below | Invalid Above |
|-------|-------|------------|---------------|---------------|
| Quoted destination | any IPv4 unicast address | 255.255.255.254 | the unspecified address 0.0.0.0 is refused | N/A |
| Quoted version/IHL byte | 0x45 only | 0x45 | 0x44 matches nothing | 0x46 matches nothing |

### Functional Tests
| Test | Location | End-User Scenario | Status |
|------|----------|-------------------|--------|
| `gtsm-related-icmp` | `test/firewall/gtsm-related-icmp.ci` | The rendered `ze_gtsm` terms are keyed on the quoted destination (existing test, assertion changed) | |

### Interop Tests (Scope: protocol)
| Scenario | Directory | Peer Daemon | What It Proves | Status |
|----------|-----------|-------------|----------------|--------|
| `gtsm-related-icmp-ttl` | `test/interop/scenarios/` | FRR | Unchanged: IPv6, exercises the transmit half. The IPv4 receive half has no peer-visible behavior (a dropped forged error is invisible to the peer), so the kernel proofs above are its evidence, and this row records that no new scenario is owed | |

## Files to Modify
- `internal/component/gtsm/gtsm.go` - `peerTerms`, `termName` if the name carried the source
- `internal/component/firewall/model.go` - `MatchICMPErrorQuotedDestination`, marker
- `internal/component/firewall/validate.go` - the new arm
- `internal/plugins/firewall/nft/lower_linux.go` - `lowerICMPErrorQuotedDestination` and the `lowerMatch` case
- `internal/component/gtsm/gtsm_rfc5082_linux_test.go`, `packet_linux_test.go` - the three proofs and their frame builder
- `internal/component/gtsm/gtsm_test.go`, `internal/plugins/firewall/nft/lower_linux_test.go`, `internal/component/firewall/validate_test.go`
- `internal/test/fixture/netfilter_fixture_gtsm.go` - the rendered-term assertion
- `rfc/short/rfc5082.md` - Enrolment reason and Support remaining cells: the association basis
- `rfc/discrimination/rfc5082.json` - re-observed records for the three changed units
- `test/rfc-changed/<session>.md` - the owner's rows for the three tag changes
- `docs/DESIGN.md` - the GTSM paragraph
- `docs/architecture/firewall/table-ownership-and-shutdown-flush.md` - the `ze_gtsm` row if it names the match

## Files to Create
- None

### Integration Checklist
| Integration Point | Applies? | File / reason |
|-------------------|----------|---------------|
| YANG schema (new RPCs/config) | No | No leaf; the match is daemon-only |
| YANG validation constraints | No | No leaf |
| YANG custom validators | No | No leaf |
| CLI commands/flags | No | The table is visible through the existing `show firewall ruleset` |
| CLI grammar (keyword before value) | N-A | No command |
| Editor autocomplete | No | No leaf |
| Functional test for new RPC/API | No | No RPC |
| Pipe completeness | N-A | No command |
| Env var registration | No | No environment leaf |
| Doctor check for runtime dependencies | No | `gtsm-kernel-state` already reports a missing `ze_gtsm` table; the term shape does not change what it checks |
| Prometheus counters/metrics | No | The firewall component counts rule hits |
| BGP family surface (new SAFI / capability / attribute) | N-A | No family change |

### Documentation Update Checklist (BLOCKING)
| # | Question | Applies? | File to update |
|---|----------|----------|---------------|
| 1 | New user-facing feature? | No | Behavior change inside an existing feature; `docs/features.md` GTSM line is re-read |
| 2 | Config syntax changed? | No | - |
| 3 | CLI command added/changed? | No | - |
| 4 | API/RPC added/changed? | No | - |
| 5 | Plugin added/changed? | No | - |
| 6 | Has a user guide page? | Yes | `docs/guide/firewall.md` "Tables another feature installs" names the `ze_gtsm` term shape |
| 7 | Wire format changed? | No | - |
| 8 | Plugin SDK/protocol changed? | No | - |
| 9 | RFC behavior implemented, changed, or newly proven? | Yes | `rfc/short/rfc5082.md`; `docs/features/rfc-status.md` is rendered |
| 10 | Test infrastructure changed? | No | - |
| 11 | Affects daemon comparison? | Yes | `docs/comparison.md` GTSM row, if it describes related messages |
| 12 | Internal architecture changed? | Yes | `docs/DESIGN.md` GTSM paragraph |
| 13 | Route metadata keys added/changed? | No | - |
| 14 | Prometheus counters added/changed? | No | - |
| 15 | Registered plugin, event type, send type, command, capability, or inventory changed? | No | - |
| 16 | Any changed source file referenced by existing doc source anchors? | Yes | DERIVED: `./le spec citation anchors spec plan/immediate/spec-gtsm-related-icmp-quoted-destination.md` |
| 17 | Existing docs show config/CLI/API examples for this area? | Yes | The `ze_gtsm` ruleset excerpt in `docs/guide/firewall.md` is re-read against the new rendering |

## Implementation Steps

1. **Phase: Wiring (MANDATORY FIRST)** -- the term carries the new match
   - Tests: `TestGTSMPublishesTheICMPFilterTableForAPeer` (assertion changed), `TestValidateRefusesQuotedDestinationOutsideIPv4`
   - Files: `model.go`, `validate.go`, `gtsm.go`
   - Verify: the term builds and validates; the lowering refuses the unknown match until Phase 2, so the kernel proofs are red
2. **Phase: the lowering** -- the quoted destination read
   - Tests: `TestLowerICMPErrorQuotedDestinationReadsTheQuotedAddressUnderAnNfprotoGuard`
   - Files: `lower_linux.go`
   - Verify: the guard precedes the read; the read is 4 octets at transport offset 24
3. **Phase: the proofs** -- the three receive proofs and their records
   - Tests: the three tagged tests, each with its new case; `sudo -n -E env "PATH=$PATH" ./le integration gtsm`
   - Files: `gtsm_rfc5082_linux_test.go`, `packet_linux_test.go`, `rfc/discrimination/rfc5082.json`, `test/rfc-changed/<session>.md` (owner)
   - Verify: red with the outer-source match restored, green with the quoted-destination match; records observed natively; `./le rfc check` names no rfc5082 row
4. **Phase: the rendered surface and the pages** -- the `.ci`, `rfc5082.md`, `docs/DESIGN.md`, `docs/guide/firewall.md`
   - Tests: `test/firewall/gtsm-related-icmp.ci` through its registered runner under `ZE_TEST_NETNS=1`
   - Verify: the fixture asserts the quoted-destination term; the pages say the association is by the quoted header

### Critical Review Checklist
| Check | What to verify for this spec |
|-------|------------------------------|
| Completeness | Every AC-N has an implementation at file:line |
| Feature completeness | Every user story has a working path |
| Correctness | Offset 24 is the quoted destination only after the 0x45 guard; no term reads the outer source; the floor is unchanged |
| Naming | The match type's name says it reads the QUOTED destination, not the packet's |
| Data flow | gtsm holds no nftables knowledge; the lowering holds no BGP knowledge |
| Rule: `ai/rules/rfc-compliance.md` | Each tag's claim states what the test body checks: the third-source case is in the body before the claim says "any source" |

### Deliverables Checklist
| Deliverable | Verification method |
|-------------|---------------------|
| No `ze_gtsm` term reads the outer source | `grep -n MatchSourceAddress internal/component/gtsm/gtsm.go` returns nothing |
| The three receive proofs pass with the third-source cases | `sudo -n -E env "PATH=$PATH" ./le integration gtsm` green for `internal/component/gtsm` |
| RFC5082-3-2 stays proven in both polarities | `./le rfc check` names no rfc5082 row; `./le rfc discriminate stem rfc5082` lists nothing stale |

### Security Review Checklist
| Check | What to look for |
|-------|-----------------|
| Input validation | The quoted header is attacker-chosen bytes inside the ICMP payload; the 0x45 guard keeps offset 24 meaningful, and a quoted header with options matches nothing rather than the wrong bytes |
| Fail closed | A term that fails to lower refuses the whole table apply, as today, and the doctor check reports the missing table |
| Denial of service | The drop stays in the kernel before the TCP stack; the added read is one 4-octet compare per term |

### Failure Routing

| Failure | Route To |
|---------|----------|
| Compilation error | Fix in the phase that introduced it |
| Test fails for the wrong reason | Fix the test assertion or setup |
| Test fails on behavior mismatch | Re-read the source in Current Behavior. If misunderstood → RESEARCH |
| Lint failure | Fix inline. If architectural → DESIGN |
| Functional test fails | Check the AC: wrong AC → DESIGN, correct AC → IMPLEMENT |
| Audit finds a missing AC | Back to the relevant phase and implement |
| 3 fix attempts failed | STOP. Report all 3 approaches. Ask the user |

## Design Insights
- The kernel and every surveyed daemon agree that an ICMP error belongs to the session it quotes. Ze's outer-source condition came from the spec that introduced the filter, which modeled the attacker of Section 5.3 (one who spoofs the peer's address) and not the one of Section 6.1 (one who forges an error from anywhere).

## Key Design Decisions
| Decision | Alternatives Considered | Rationale |
|----------|------------------------|-----------|
| Match the quoted DESTINATION, not the quoted source | Quoted source = ze's local address | An error ze receives is about a packet ze sent, whose destination is the peer; ze's local address per peer is not always configured, and the peer address is |
| Keep the quoted-port match beside it | Drop it, since the destination now identifies the peer | The port is what ties the error to the BGP session rather than to any other flow toward the peer (RFC5082-3-4) |
| No new interop scenario | One against FRR | A dropped forged error is invisible to the peer; the kernel proofs are the only observer |
| The owner writes the `test/rfc-changed/` rows | The implementer writes them | The rows record the owner's decision; this spec is that decision, taken 2026-09-15, and the rows quote it |

## Known Limitations
- The filter claims BGP sessions only. BFD and VRRP GTSM sessions quote no TCP header, so their related messages stay Unknown, as before this spec.

## RFC Documentation (Scope: protocol)

Add `// RFC NNNN Section X.Y: "<quoted requirement>"` above enforcing code.
MUST document: validation rules, error conditions, state transitions, timer
constraints, message ordering, and every MUST/MUST NOT.

## Checklist

### Pre-Spec Verification (before the design is presented)
- [ ] Metadata table present, with a valid Status, Depends, Phase and Updated
- [ ] `ai/INDEX.md` keyword table checked
- [ ] An `rfc/short/` summary exists for every RFC referenced
- [ ] Template format followed: the 🧪 emoji, tables rather than prose, `[ ]` never `[x]`
- [ ] No code snippets
- [ ] Files to Modify names feature code, not only tests
- [ ] Current Behavior and Data Flow sections completed
- [ ] AC-N rows carry testable assertions
- [ ] Every assumption has a Basis and a validation method; every failure mode is a risk row
- [ ] Required Reading carries `→ Decision:` / `→ Constraint:` checkpoints
- [ ] Integration Checklist marks "CLI grammar" when a command is added, "Doctor check" when a runtime dependency is

### Goal Gates (MUST pass)
- [ ] AC-1..AC-N all demonstrated
- [ ] Every user story has a working path and a passing test
- [ ] Wiring Test table complete: every row a concrete test name, none deferred
- [ ] `./le verify worktree` passes. It runs every stage against a COMMIT in a throwaway worktree, which is the pre-commit gate (`ai/rules/git-safety.md`). An in-place `./le verify current` is void the moment the tree moves under it
- [ ] Feature code integrated (`internal/*`, `cmd/*`), not library-only
- [ ] Integration and Documentation checklists answered Yes/No/N-A with evidence
- [ ] Architectural Verification table filled, including registration over hardcoding
- [ ] Critical Review passes (all 6 checks in `ai/rules/quality.md`)
- [ ] Every A-N confirmed or broken, none `unvalidated`
- [ ] Every item this spec did not do is a spec of its own, named here, in its own bucket

### TDD
- [ ] Tests written
- [ ] Tests FAIL (paste output)
- [ ] Tests PASS (paste output)
- [ ] Boundary tests for all numeric inputs
- [ ] Functional `.ci` tests for end-to-end behavior
- [ ] Interop tests for protocol features (or N-A with a reason)

### Closure
- [ ] Append `plan/TEMPLATE-CLOSURE.md` and complete every section in it
- [ ] `/ze-review` gate clean, recorded via `internal/le/spec/session/review.go`
- [ ] Learned summary written to `plan/learned/NNN-<name>.md`
- [ ] **Commit A:** code + tests + docs + spec + learned summary
- [ ] **Commit B:** `git rm plan/<spec>` only (commit A preserves the spec in history)
