# Deferrals

Tracked deferrals from implementation sessions. Every decision to not perform in-scope work
must be recorded here with a destination (receiving spec or explicit cancellation).

| Date | Source | What | Reason | Destination | Status |
|------|--------|------|--------|-------------|--------|
| 2026-03-23 | spec-forward-congestion Phase 2 | `updatePeriodicMetrics()` unit test with mock registry verifying gauge values are set | Pre-existing peer.go/types.go build break blocks reactor-level test compilation | reactor_metrics_test.go:TestUpdatePeriodicMetrics_SetsOverflowGauges | done |
| 2026-03-23 | spec-forward-congestion Phase 2 | ForwardUpdate -> RecordForwarded/RecordOverflowed end-to-end wiring test | Pre-existing peer.go/types.go build break blocks reactor-level test compilation | reactor_metrics_test.go:TestForwardDispatch_RecordForwarded_UpdatesMetrics | done |
| 2026-03-23 | spec-forward-congestion Phase 2 | .ci functional test for metrics endpoint showing ze_bgp_overflow_* metrics | Pre-existing peer.go/types.go build break blocks functional test build | test/plugin/forward-congestion-overflow-metrics.ci | done |
| 2026-03-23 | spec-forward-congestion Phase 2 | Test verifying exact Prometheus metric names match registration | Pre-existing peer.go/types.go build break blocks reactor-level test compilation | reactor_metrics_test.go:TestMetricNames_MatchRegistration | done |
| 2026-03-23 | spec-forward-congestion Phase 3 | Wire ReadThrottle.ThrottleSleep into session read loop | Superseded: spec replaced sleep-between-reads with buffer denial (2026-03-23) | cancelled | cancelled |
| 2026-03-23 | spec-forward-congestion Phase 3 | Register ze.fwd.throttle.enabled env var in reactor.go | Superseded: ReadThrottle removed, buffer denial is the backpressure mechanism | cancelled | cancelled |
| 2026-03-24 | CLI autocomplete session | Sanitize non-printable characters (ANSI escapes) in config values before terminal rendering | Low severity: attacker must be authenticated user editing config; lipgloss passes through raw escape codes | model_render.go:sanitizeForDisplay | done |
| 2026-03-24 | CLI autocomplete session | Clear searchCache on commit/discard/rollback to prevent stale search results | Info severity: cache rebuilds on next Dirty() change, staleness window is between commit and next edit | ad-hoc (fixed inline) | done |
| 2026-03-25 | spec-role-otc (learned/401) | OTC egress stamping: add OTC = local ASN when route without OTC sent to Customer/Peer/RS-Client | EgressFilterFunc returns bool only, cannot return modified payload; needs applyMods framework | spec-apply-mods | done |
| 2026-03-25 | spec-role-otc (learned/401) | Unicast-only scope enforcement: `isUnicastFamily` defined but not called from OTC filters | Needs family extraction from payload (MP_REACH_NLRI parsing) | spec-apply-mods | done |
| 2026-03-25 | spec-role-otc (learned/401) | `resolveExport` hot-path allocation: allocates per UPDATE per peer, should pre-compute at config time | Performance optimization, not correctness | spec-apply-mods | done |
| 2026-03-26 | spec-peer-local-remote | AC-5: Validate duplicate `remote > ip` across peers in same list | goyang does not support YANG `unique` constraint; needs custom post-parse validation | resolve.go:checkDuplicateRemoteIPs + cli/validator.go:checkDuplicateRemoteIPs | done |
| 2026-03-26 | make ze-verify | Editor `.et` test race conditions: 8 tests fail with `race detected` (lifecycle, session, pipe categories) | Pre-existing race in TUI framework, not related to config changes | Fixed: headless.go SettleWait snapshot isolation + 15s deadline (was 3s), settle snapshot isolation, stale comment fix in runner.go | done |
| 2026-03-26 | make ze-verify | Perf benchmark failures: TestBenchmarkEndToEnd, TestRunSmallBenchmark, TestBenchmarkIterDelayZero (bind port 179 permission denied, dial timeout) | Pre-existing infrastructure issue, requires privileged port or test environment changes | Fixed: PassiveListen flag gates port 179 listen, removed t.Parallel from heavy benchmark tests | done |
| 2026-03-26 | spec-monitor-1-event-stream review | FormatMonitorLine in `bgp/plugins/cmd/monitor/format.go` is dead code: defined and tested but never called from production code. Decide whether to wire it into a visual/text output mode or remove it | Discovered during spec review against current code; not blocking monitor-1 implementation | Wired into CLI streaming (ProcessPipesDefaultFunc) and TUI monitor (FormatFunc) via registered formatter | done |
| 2026-03-27 | spec-route-loop-detection | `allow-own-as N` configuration to skip AS loop detection for specific peers | Implemented: loop-detection YANG with allow-own-as 0-10, per-peer via PeerFilterInfo (spec-policy-2) | spec-policy-0-umbrella | done |
| 2026-03-27 | spec-route-loop-detection | Explicit `cluster-id` configuration (override Router ID as Cluster ID) | Implemented: loop-detection YANG with cluster-id leaf, ClusterID in PeerFilterInfo (spec-policy-2) | spec-policy-0-umbrella | done |
| 2026-03-27 | deep-review | Metrics behavioral tests: spy registry tests for RIB route churn counters, config reload counters, wire bytes counters | updatePeerStateMetric and IncrNotification* tests done (peer_stats_test.go); RIB, config reload, and wire counters still untested | spec-prometheus-plugin-health | open |
| 2026-03-27 | deep-review | notifSent/notifRecv per-peer Prometheus label cleanup on peer removal | Notification code/subcode combinations create per-peer labels that are never deleted in RemovePeer; needs per-peer tracking of observed (code, subcode) pairs | Fixed: reactor_peers.go enumerates notifCodeNames (1-7) x notifSubcodeNames (0-14) for cleanup | done |
| 2026-03-27 | deep-review | GR plugin per-peer metric label cleanup on peer removal (staleRoutes, timerExpired gauges) | GR plugin has no peer-removal callback; reactor cleans its own metrics but plugin metrics persist | spec-prometheus-plugin-health | open |
| 2026-03-27 | spec-prometheus-deep | Plugin health metrics (ze_plugin_status, ze_plugin_restarts_total, ze_plugin_events_delivered_total) | Requires changes to internal/plugin/ infrastructure, not just reactor wiring | spec-prometheus-plugin-health | done |
| 2026-03-27 | spec-prometheus-deep | `ze_rib_eor_received_total` per peer+family counter at RIB level | EOR already tracked at reactor level as type=eor on ze_peer_messages_received_total; per-family counter would require RIB to subscribe to EOR events | user-approved-drop | cancelled |
| 2026-03-28 | spec-redistribution-filter AC-18 | Default named filter `rfc:no-self-as` active by default for all peers | Implemented: loop-detection filter auto-populates in peer import chains (spec-policy-2 + spec-policy-3) | spec-policy-0-umbrella | done |
| 2026-03-28 | spec-redistribution-filter AC-19/20 | Override mechanism for default filters (per-peer and per-group) | Implemented: user controls filter chain directly, inactive: prefix deactivates (spec-policy-3) | spec-policy-0-umbrella | done |
| 2026-03-28 | spec-redistribution-filter AC-21 | Mandatory filter cannot be overridden (override targeting mandatory is ignored) | Implemented: OTC stays in-process (not in named system), loop-detection deactivatable via inactive: prefix | spec-policy-0-umbrella | done |
| 2026-03-28 | spec-redistribution-filter AC-10 | Export filter modify dedicated functional test (.ci) | Implemented: test/plugin/redistribution-export-modify.ci | spec-redistribution-filter-phase2 | done |
| 2026-03-28 | spec-redistribution-filter AC-13 | Reject modify of undeclared attribute | Already implemented in original spec: filter_chain.go validateModifyDelta() + TestValidateModifyDelta | spec-redistribution-filter-phase2 | done |
| 2026-03-28 | spec-redistribution-filter AC-15 | raw=true delivers hex wire bytes in filter-update | Already implemented in original spec: filter_chain.go policyFilterFunc() hex-encodes when wantsRaw | spec-redistribution-filter-phase2 | done |
| 2026-03-29 | spec-redistribution-filter | Wire-level dirty tracking: re-encode only modified attributes into wire UPDATE bytes | Implemented: filter_delta.go textDeltaToModOps + generic handlers + wiring in import/export paths | spec-redistribution-filter-phase2 | done |
| 2026-03-29 | spec-redistribution-filter | TestAttributeAccumulation, TestDirtyTracking, TestFilterModifyOnlyDeclared unit tests | Implemented: filter_delta_test.go (20 subtests covering encoding, round-trip, declared-attribute validation) | spec-redistribution-filter-phase2 | done |
| 2026-03-29 | spec-redistribution-filter | redistribution-override.ci functional test | Superseded: redistribution format replaced by filter chain (spec-policy-3). Override mechanism replaced by direct list management with inactive: prefix | spec-policy-0-umbrella | done |
| 2026-04-09 | spec-policy-0-umbrella | Filter name validation at parse time | Implemented: plain names (no colon) validated against policy registry at parse time. Colon names (external plugins) deferred to runtime. | spec-policy-filter-validation | done |
| 2026-04-11 | spec-api-2-grpc (learned/544) | gRPC functional .ci test exercising daemon via wire protocol | grpcurl not installed in dev env; Python grpcio not vendored. Go unit tests (server_test.go) cover full request/response end-to-end via the grpc library, which is a minor coverage gap only for the daemon startup path. REST .ci test (rest-api-commands.ci) covers that path since both transports use the same startAPIServers entry. | spec-api-grpc-ci-test | open |
| 2026-04-17 | spec-l2tp-6c-ncp AC-19 | Backend-error injection test: fakeBackend.addAddrP2PErr -> EventSessionDown + LCP Terminate-Request | Mechanism present in `ncp.go:onNCPOpened` (s.fail on any AddAddressP2P/AddRoute/SetAdminUp error), but no test exercises it. Low risk: the error propagation is a single-line pattern identical to afterLCPOpen's. | spec-l2tp-7-subsystem | open |
| 2026-04-17 | spec-l2tp-6c-ncp AC-20 | Renegotiation-after-Opened test: peer CR arriving at Opened state triggers state fallback to AckSent and re-completion | handleFrame dispatch wires the path; FSM transition is table-driven. Uncovered by a direct test because the LNS is not expected to receive spontaneous peer renegotiation in normal operation. | spec-l2tp-7-subsystem | open |
| 2026-04-17 | spec-l2tp-6c-ncp AC-20 | IPCP Configure-Reject of Primary-DNS / Secondary-DNS absorb test | `absorbIPCPReject` clears `s.dnsPrimary` / `s.dnsSecondary` on Reject so the next CONFREQ omits the option. Not exercised by a test; low value because the code path is a one-line field clear. | spec-l2tp-7-subsystem | open |
| 2026-04-17 | spec-l2tp-6c-ncp Phase 7 | `.ci` functional test against accel-ppp or bngblaster peer validating full L2TP + PPP + NCP session and subscriber route injection | Requires the L2TP subsystem glue and l2tp-pool plugin, both out of umbrella 6c scope. Go-level net.Pipe tests in ncp_test.go cover the driver API end-to-end. | spec-l2tp-7-subsystem | open |
| 2026-04-17 | make ze-verify-fast (Darwin dev host) | Two VPP external-mode tests fail on macOS because `t.TempDir()` produces sun_path > 104 bytes (Darwin limit) | Pre-existing Darwin environmental issue, not caused by spec-l2tp-6c-ncp. Logged in `plan/known-failures.md`. Likely fix: `//go:build linux` tag since VPP is Linux-only. | spec-vpp-test-darwin | open |
| 2026-04-11 | spec-iface-tunnel design | ERSPAN and ip6erspan encapsulation kinds | vishvananda/netlink has no first-class type; would require raw rtnetlink IFLA_INFO_DATA attrs or library fork. User confirmed out of scope: Cisco-interop only, no current demand. | user-approved-drop | cancelled |
| 2026-04-11 | spec-iface-tunnel design | GRE keepalives (Cisco/Junos compatible) | Linux kernel does not implement; requires userspace daemon (~500 LOC, raw socket, rp_filter interaction). User confirmed out of scope: BFD over the tunnel is the operator-consensus liveness mechanism and `internal/plugins/bfd/` is already in flight. | user-approved-drop | cancelled |
| 2026-04-11 | spec-iface-tunnel design | VRF underlay (`tunnel routing-instance destination`) and overlay leaves on tunnel interfaces | ze has no VRF model today; reserving visible-but-unimplemented config has bitten the project before. User confirmed out of scope: revisit when VRF lands in iface. | user-approved-drop | cancelled |
| 2026-04-11 | spec-iface-tunnel design | 6rd attributes on `sit` (`6rd-prefix`, `6rd-relay-prefix`) | Niche IPv6 transition deployment scenario, no current demand. | user-approved-drop | cancelled |
| 2026-04-11 | spec-iface-tunnel implementation | `ignore-df` leaf on gretap/ip6gretap | vishvananda/netlink v1.3.1 (vendored) does not expose `IgnoreDf` on its `Gretap` Go type. Adding requires raw `IFLA_GRE_IGNORE_DF` netlink attr or library upgrade; spec said "no new third-party imports without asking the user." | user-approved-drop | cancelled |
| 2026-04-11 | spec-iface-tunnel implementation | `encaplimit` leaf on ip6gre/ip6gretap (kept on ip6tnl/ipip6) | vishvananda/netlink v1.3.1 exposes `EncapLimit` only on the `Ip6tnl` Go type, not on `Gretun`/`Gretap`. The kernel ip6_gre module supports IFLA_GRE_ENCAP_LIMIT but the high-level lib does not surface it. | user-approved-drop | cancelled |
| 2026-04-11 | spec-iface-tunnel implementation | `ze config validate` invoking plugin OnConfigVerify callbacks. The iface plugin's parseTunnelEntry checks (mandatory encapsulation, mutually-exclusive local-address/local-interface, missing local) only run at daemon reload via OnConfigVerify, not at static `ze config validate`. Two parser-rejection .ci file slots repurposed: iface-tunnel-invalid-no-encap.ci now tests hoplimit-on-gre rejection, iface-tunnel-invalid-both-locals.ci now tests encaplimit-on-sit rejection. The Go-side checks are exercised by Go unit tests (TestParseTunnelMissingEncapsulation, TestParseTunnelMultipleCases, TestParseTunnelBothLocals, TestParseTunnelMissingLocal). | user-approved-drop | cancelled |
| | | | | | |
| 2026-04-13 | spec-gokrazy-4-resilience | AC-3: Configurable route priority per interface (YANG leaf, metric in AddRoute) | Not gokrazy-specific; belongs in iface spec. User agreed. | spec-iface-route-priority.md | open |
| **2026-04-04 audit of deleted specs -- significant** | | | | | |
| 2026-04-04 | spec-rib-inject | Functional tests api-rib-inject.ci, api-rib-withdraw.ci | Spec deleted with TODO items unfinished; no .ci test proves feature reachable | spec-rib-inject-ci (to be created) | open |
| 2026-04-04 | spec-rib-inject | docs/features.md + docs/architecture/api/commands.md updates for RIB inject | Listed as TODO in spec, never done | spec-rib-inject-ci (to be created) | open |
| 2026-04-04 | spec-rib-inject | RFC 5549 extended next-hop for injected routes (PackContext.ExtendedNextHop check) | Noted as future work | spec-rib-inject-rfc5549 (to be created) | open |
| 2026-04-04 | spec-apply-mods | v2 ACs (AC-11 through AC-18): progressive build pipeline, AttrOp struct, pooled buffer | Core purpose of spec; status was "v2" not "done" when deleted | spec-apply-mods-v2 (to be created) | open |
| 2026-04-04 | spec-apply-mods | NLRI structural modification via ModAccumulator | Listed as future separate field | spec-apply-mods-v2 (to be created) | open |
| 2026-04-04 | spec-exabgp-bridge-muxconn | Bridge internal plugin registration for .ci tests and production | Bridge uses stdin/stdout only; cannot run as external plugin through process manager | spec-exabgp-bridge-internal (to be created) | open |
| 2026-04-04 | spec-exabgp-bridge-muxconn | SetWriteDeadline degradation on non-TCP transports (os.File, SSH) | Write deadline silently skipped; writes may block indefinitely | spec-exabgp-bridge-internal (to be created) | open |
| 2026-04-04 | spec-fleet-config | Config rollback mechanism | Deferred to "config-archive spec" which now exists as spec-config-2-archive | spec-config-2-archive | open |
| 2026-04-04 | spec-monitor-2-bgp-dashboard | All 17 ACs: entire BGP dashboard spec | Deleted as "complete" but nothing was implemented; audit was empty | spec-bgp-dashboard (to be created) | open |
| 2026-04-04 | spec-command-inventory | Entire spec: make ze-command-list, StreamingPrefixes accessor | Skeleton status when deleted | spec-command-inventory (to be created) | open |
| 2026-04-04 | spec-llgr-4-readvertisement | Route metadata infrastructure (meta map, ModAccumulator) | Dependency spec-route-metadata never created | spec-route-metadata (to be created) | open |
| 2026-04-04 | spec-llgr-4-readvertisement | Per-family ribOut infrastructure | Dependency spec-rib-family-ribout never created | spec-rib-family-ribout (to be created) | open |
| 2026-04-04 | spec-llgr-4-readvertisement | Multi-peer partial deployment .ci test for LLGR readvertisement | Requires multi-peer .ci infrastructure | spec-llgr-readvertisement (to be created) | open |
| 2026-04-04 | spec-filter-community | bgp-filter-prefix plugin | Planned as separate spec, never written | spec-filter-prefix (to be created) | open |
| 2026-04-04 | spec-filter-community | bgp-filter-irr plugin (library exists at internal/component/bgp/irr/) | Planned as separate spec, never written | spec-filter-irr (to be created) | open |
| | | | | | |
| **2026-04-04 audit of deleted specs -- medium** | | | | | |
| 2026-04-04 | spec-iface-0-umbrella | macOS/BSD interface plugins (_darwin.go, _bsd.go) | Platform-specific; Linux-only for now | spec-iface-darwin-bsd (to be created) | open |
| 2026-04-04 | spec-iface-0-umbrella | Phase 4: DHCP client, make-before-break, traffic mirroring, SLAAC | Later phase of iface umbrella | spec-iface-phase4 (to be created) | open |
| 2026-04-04 | spec-iface-5-vm-tests | Packet-level mirror verification, DHCPv6 PD test, SLAAC test | Advanced VM-based testing | spec-iface-phase4 (to be created) | open |
| 2026-04-04 | spec-looking-glass | Alice-LG e2e integration test, large RIB perf test, TLS, pagination | Advanced LG features | spec-looking-glass-v2 (to be created) | open |
| 2026-04-04 | spec-web-0-umbrella | RBAC, i18n, mobile layout, config upload/download, plugin web extensions | Later phases of web umbrella | spec-web-phase2 (to be created) | open |
| 2026-04-04 | spec-shell-completion-v2 | Flag-value completion (--family \<TAB\>), config section completion | Advanced completion features | spec-shell-completion-v3 (to be created) | open |
| 2026-04-04 | spec-port-defaults | Range-vs-single port conflict detection, YANG-default lint check | Edge case detection and tooling | spec-port-defaults-v2 (to be created) | open |
| 2026-04-04 | spec-multipeer-ci | LLGR egress suppress test, multi-peer route reflection test | Requires multi-peer .ci infrastructure | spec-multipeer-ci-v2 (to be created) | open |
| 2026-04-04 | spec-gr-marker | F-bit per-family signaling, Selection Deferral Timer, supervisor crash recovery | Advanced GR features | spec-gr-advanced (to be created) | open |
| 2026-04-04 | spec-prefix-limit | CLI warning count on login, ze bgp warnings command, staleness banner | Visibility features for prefix-limit | spec-prefix-limit-visibility (to be created) | open |
| 2026-04-04 | spec-cli-dispatch | Functional tests for ze set interface create, ze update peeringdb, ze validate config | .ci tests for CLI dispatch commands | spec-cli-dispatch-ci (to be created) | open |
| 2026-04-04 | spec-prometheus-deep | Process/runtime metrics (phase 6), bgp_as_path_loop_detected_total, RPKI/ASPA metrics | Later phases of prometheus work | spec-prometheus-phase6 (to be created) | open |
| 2026-04-04 | spec-prometheus-deep | Max-prefix metrics (may now be unblocked since prefix-limit is implemented) | Was blocked on prefix-limit; may now be feasible | spec-prometheus-plugin-health | open |
| 2026-04-04 | spec-llgr-0-umbrella | Restarting Speaker procedures (RFC 9494/4724), VPN ATTR_SET (RFC 6368), full hard-reset N-bit | Advanced LLGR/GR features | spec-gr-advanced (to be created) | open |
| 2026-04-04 | spec-dns | DNS-over-TLS, DNS-over-HTTPS, DNSSEC validation | Secure DNS transport features | spec-dns-secure (to be created) | open |
| 2026-04-04 | spec-role-otc | Private AS removal filter, AS Confederation OTC (RFC 9234 Section 5) | Advanced OTC features | spec-role-otc-v2 (to be created) | open |
| 2026-04-04 | spec-decorator | Reverse DNS, community name, RPKI status decorators | Additional decorator types | spec-decorator-v2 (to be created) | open |
| | | | | | |
| **2026-04-04 audit of deleted specs -- low (property/chaos/perf tests)** | | | | | |
| 2026-04-04 | spec-listener-0-umbrella | Property test: random listener conflict detection symmetric+transitive | Property testing | spec-property-tests (to be created) | open |
| 2026-04-04 | spec-listener-7-migrate | Property test: round-trip migration | Property testing | spec-property-tests (to be created) | open |
| 2026-04-04 | spec-forward-backpressure | Property tests for overflow ordering under concurrent access | Property testing | spec-property-tests (to be created) | open |
| 2026-04-04 | multiple iface specs | Chaos tests: rapid flapping, DHCP server failure | Chaos testing | spec-chaos-iface (to be created) | open |
| 2026-04-04 | spec-redistribution-filter | Property test: random UPDATEs through filter chains; perf benchmarks | Property testing and benchmarks | spec-property-tests (to be created) | open |
| 2026-04-04 | spec-web-3-config-edit | Concurrent multi-user editing stress test | Stress testing | spec-web-stress (to be created) | open |
| 2026-04-04 | spec-fleet-config | Performance test with >100 concurrent clients | Performance testing | spec-fleet-config-perf (to be created) | open |
| 2026-04-04 | spec-fib-admin-distance AC-5 | Config reload re-evaluates existing routes with new admin distance | Requires OnConfigure re-walk of stored routes in sysrib; config reload wiring not yet scoped | spec-fib-admin-distance-reload (to be created) | open |
| 2026-04-07 | spec-config-tx-bus-native AC-14 | External Python plugins receive config transaction bus events over TLS connection | Requires generic bus-over-wire RPCs (`bus-subscribe`/`bus-publish`/`bus-deliver`) and per-plugin engine-side bridge -- not config-tx-specific, benefits all bus events | spec-plugin-bus-access | cancelled |
| 2026-04-07 | spec-config-tx-bus-native review | Open namespace on stream system: a reserved namespace (e.g. `open`) where the engine does not validate event types, plus optional binary `Payload []byte` field, so unintegrated plugins can coordinate via opaque messages without registry updates | No concrete consumer today. YAGNI per `rules/design-principles.md` (three implementations before abstracting). Recorded so future plugin-to-plugin coordination needs land here instead of recreating a parallel bus. The answer for "I need plugin coordination" is open-namespace stream, NOT a new bus | spec-stream-open-namespace (to be created when a concrete consumer appears) | open |
| 2026-04-07 | spec-report-bus Phase 11 | `test/plugin/show-errors-empty.ci` (functional test for empty report bus error ring via `ze show errors`) | A draft of this `.ci` test timed out at the test plugin's `wait_for_shutdown` step. The same dispatch+shutdown pattern works in `show-warnings.ci` and the populated error tests. Cause unknown; suggests an empty-bus daemon-shutdown timing or IPC bug. Unit tests `TestHandleShowErrorsEmpty` and `TestHandleShowWarningsEmpty` cover the handler-level empty case deterministically. Parked draft at `tmp/show-errors-empty.ci.parked`. | spec-report-bus Phase 11 follow-up | open |
| 2026-04-07 | spec-report-bus Phase 11 | Investigate empty-bus daemon-shutdown timeout: a `.ci` test plugin that dispatches `show errors` (against an empty bus) followed by `daemon shutdown` and `wait_for_shutdown` hangs until the test runner timeout, even though the dispatch succeeds and the populated-bus version works. Could be a `wait_for_shutdown` race, an IPC FD lifecycle issue, or something in how the daemon handles shutdown when no bus state exists. Worth a focused debugging session before more `.ci` tests are added that depend on the same path. | Unknown root cause; reproduces only in the empty-bus case. Affects future `.ci` test patterns | spec-report-bus Phase 11 follow-up | open |
| 2026-04-07 | spec-report-bus Phase 11 | `test/plugin/show-errors-session-dropped.ci` (functional test for the session-dropped error producer) | The session-dropped producer fires only when an Established BGP session ends WITHOUT a NOTIFICATION exchange (TCP loss, hold-timer with no notification, peer FIN). `ze-peer` doesn't expose an "abrupt close" action that I can find, so triggering the path requires either adding such an action or finding another simulation path. Unit test `TestRaiseSessionDropped` covers the helper directly. | spec-report-bus Phase 11 follow-up | open |
| 2026-04-07 | spec-config-tx-protocol Phase 7 | Wire `internal/component/plugin/server/reload.go` SIGHUP/CLI commit path through `transaction.TxCoordinator.Execute` | Closed 2026-04-08: reload.go now calls `runTxCoordinator`, which wires the orchestrator to plugin RPC callbacks via `config_tx_bridge.go` so in-process and external plugins both participate without needing an SDK-level stream subscription path. | plugin/server/{reload.go,reload_tx.go,config_tx_bridge.go} | done |
| 2026-04-07 | spec-config-tx-protocol AC-17 | External Python plugin participates in config transactions over TLS via stream `subscribe-events`/`emit-event` RPCs | Closed 2026-04-08: the RPC bridge dispatches verify/apply over the existing plugin transport (MuxConn), which is identical for in-process plugins and external plugins running over TLS. `test/reload/test-tx-protocol-external-plugin.ci` exercises an external `ze plugin bgp-watchdog` process participating in a bgp-root transaction through the same code path. | test/reload/test-tx-protocol-external-plugin.ci | done |
| 2026-04-07 | spec-config-tx-protocol Phase 11 | `test/reload/test-tx-protocol-external-plugin.ci` functional test | Closed 2026-04-08: test written and passing in `make ze-verify`. The test launches bgp-watchdog as an external plugin via `ze plugin bgp-watchdog` and triggers a reload via SIGHUP; the orchestrator publishes verify-bgp-watchdog/apply-bgp-watchdog and the bridge dispatches through the external plugin connection. | test/reload/test-tx-protocol-external-plugin.ci | done |
| 2026-04-07 | spec-config-tx-protocol bus migration chain 2 | Migrate `internal/plugins/iface/netlink/monitor_linux.go`, `internal/component/iface/migrate_linux.go`, `internal/component/bgp/reactor/reactor.go`, `internal/component/bgp/reactor/reactor_iface.go`, `internal/component/bgp/reactor/reactor_bus.go` (delete), and `internal/component/bgp/plugin/register.go` from `pkg/ze.Bus` to `pkg/ze.EventBus` | Closed 2026-04-07: migrated in commit 6730989e. | commit 6730989e | done |
| 2026-04-07 | spec-config-tx-protocol bus migration chain 3 | Migrate `internal/plugins/iface/dhcp/dhcp_linux.go`, `internal/plugins/fib/kernel/monitor.go`, `internal/component/bgp/server/events.go`, `internal/component/bgp/server/event_dispatcher.go`, and `internal/component/iface/register.go` (interface/rollback publisher) from `pkg/ze.Bus` to `pkg/ze.EventBus` | Closed 2026-04-07: migrated in commit 6730989e. | commit 6730989e | done |
| 2026-04-07 | spec-config-tx-protocol bus migration chain 4 | Delete `pkg/ze/bus.go` (Bus, Topic, Consumer, Event, Subscription types), `internal/component/bus/` (entire package), `Registration.ConfigureBus` field, `SetBus`/`GetBus`/`busInstance` in registry, `bus.NewBus()` + `registry.SetBus(b)` in `cmd/ze/hub/main.go`, `ConfigureBus` invocation in `inprocess.go` (with the `//nolint:staticcheck` markers added in chain 1), and any remaining `pkg/ze/bus.go` consumers | Closed 2026-04-08: staged in commit script tmp/commit-d98156d2.sh; runs after ze-verify passes. | tmp/commit-d98156d2.sh | done |
| 2026-04-08 | spec-report-bus Phase 7 unit tests | Unit tests for config commit error producers (`TestCommitAbortRaisesReportError`, `TestCommitRollbackRaisesReportError`, `TestCommitSaveFailedRaisesReportError`) | Closed 2026-04-08: added to `internal/component/config/transaction/orchestrator_test.go`. Each test uses the testGateway fake + new findReportError helper to assert producer -> report bus path. | orchestrator_test.go (3 tests) | done |
| 2026-04-08 | spec-report-bus Phase 11 | `test/plugin/show-errors-config-abort.ci` (functional test for commit-aborted producer) | Closed 2026-04-08: added via new Python test plugin pattern. Extended `test/scripts/ze_api.py` with `on_config_verify/apply/rollback` handlers, then wrote the plugin that rejects verify on reload, triggers SIGHUP via the runner's `action=sighup`, and polls `show errors` for the commit-aborted entry. | test/plugin/show-errors-config-abort.ci | done |
| 2026-04-08 | spec-report-bus Phase 11 | `test/plugin/show-warnings-clear.ci` (functional test for prefix-threshold clear after withdraw below threshold) | `.ci` infra has no way to make ze-peer announce enough routes to cross a prefix-count threshold and then withdraw. Unit test `TestSessionPrefixThresholdRaisesAndClearsReport` covers the raise+clear logic deterministically at the reactor level. | spec-report-bus Phase 11 follow-up | open |
| 2026-04-08 | spec-report-bus Phase 11 | `test/plugin/show-warnings-stale.ci` | Skipped as a duplicate: the existing `test/plugin/show-warnings.ci` already exercises the prefix-stale path (it sets `updated 2024-01-01` and asserts `code=prefix-stale`). A separate stale .ci would test the same code path with the same assertions. | user-approved-drop | cancelled |
| 2026-04-08 | spec-report-bus Phase 11 | `test/plugin/show-warnings-empty.ci` | Shares the empty-bus daemon-shutdown hang that is already tracked for `show-errors-empty.ci` (row 87 above). Unit test `TestHandleShowWarningsEmpty` covers the handler-level empty case deterministically. | spec-report-bus Phase 11 follow-up | open |
| 2026-04-08 | spec-report-bus Phase 11 | `test/plugin/show-errors-config-rollback.ci` (functional test for commit-rollback producer) | Requires a .ci test plugin that accepts verify on reload but rejects apply mid-transaction while another plugin accepts apply. The current Python test-plugin pattern can only toggle on one phase; implementing multi-phase toggle needs state tracking across RPC calls and a companion plugin that applies cleanly. Unit test `TestCommitRollbackRaisesReportError` covers the producer deterministically. | spec-report-bus Phase 11 follow-up | open |
| 2026-04-08 | spec-report-bus Phase 11 | `test/plugin/show-errors-config-save.ci` (functional test for commit-save-failed producer) | Requires the .ci runner to support a read-only filesystem (or to intercept the config file write) so that `writeConfigFile` returns an error after a successful apply. Neither mechanism exists today. Unit test `TestCommitSaveFailedRaisesReportError` covers the producer deterministically using a ConfigWriter stub that returns `errConfigWriteFailed`. | spec-report-bus Phase 11 follow-up | open |
| 2026-04-10 | (was spec-fwd-auto-sizing) AC-14 | `test/chaos/no-congestion-initial.ci` (4-peer chaos: no false congested warnings during initial convergence) | Needs ze-chaos infrastructure with multi-peer orchestration and log assertion for absence of warnings. ze-chaos tests are orchestrated differently from plugin .ci tests. | spec-chaos-testing | open |
| 2026-04-11 | spec-cmd-2 default-originate /ze-review #6 | Raw-mode filter support for `default-originate-filter` -- filters declared `raw=true` receive empty hex because no wire bytes exist for the synthetic route | Two options: (a) config validator rejects raw-mode filters bound to `default-originate-filter`; (b) `defaultOriginateFilterAccepts` pre-encodes the synthetic UPDATE to wire bytes before calling the filter. Not blocking the text-mode path which is the common case. | spec-cmd-2 follow-up | open |
| 2026-04-11 | spec-cmd-1 IPv6 nexthop /ze-review #7 | `RewriteASPath` single-ASN slice allocation -- refactor allocates `[]uint32{localASN}` per call via the shared `rewriteASPathPrepend` helper | Not in the hot path today (EBGPWire cache amortizes). If profiling later shows it matters, either add a single-ASN fast path that skips the slice or use a sync.Pool for `[]uint32` with capacity 1/2. | this session: stack-allocated [1]uint32 array avoids heap alloc | done |
| 2026-04-11 | spec-cmd-1 IPv6 nexthop /ze-review #9 | End-to-end `.ci` test that exercises MP_REACH next-hop rewriting through the full forwarding pipeline (receive from peer A, forward to peer B with next-hop-self) | ze-peer now supports prefix=/contains= matching and bind=ADDR, but two-peer forwarding tests are blocked by non-deterministic connection-to-peer mapping (conn=1/conn=2 can't be assigned to specific peers). Unit test `TestBuildModifiedPayload_MPReachNextHopSelf` + 6 handler tests cover the wire rewriting logic. | spec-test-infra-multi-peer (future) | open |
| 2026-04-11 | spec-cmd-3 phase 3 | Realtime best-change event delivery for N-way multipath: `checkBestPathChange` still emits single-best entries on the `(rib, best-change)` topic. FIB plugins programming the kernel need atomic "N nexthops for prefix P" delivery, replay semantics across restarts, and a consumer-side protocol for ECMP route groups. | Defer to a dedicated spec so the FIB consumer contract is designed before wire format changes. The algorithm (`SelectMultipath`) and the CLI surface (`bgp rib show best` multipath-peers field) are already wired. | spec-fib-ecmp | open |
| 2026-04-11 | spec-record-parse-peer-block-hardening | Fix typo `ze.bgp.log.server` -> `ze.log.bgp.server` in `test/plugin/logging-level-filter.ci`, `logging-stderr.ci`, `logging-syslog.ci`. The three logging tests use the wrong env var name (the registered convention is `ze.log.<subsystem>` per `slogutil.go:45`), so the variable was silently dropped twice: first by the parser (fixed by this spec), then by the env registry on the ze side. The tests currently pass because their stderr/syslog patterns match messages at the default log level. | Renamed in commit; tests still pass with the corrected name. The rename is purely cosmetic until the orchestrated-mode validateLogging bug below is fixed -- see the next row. | done in same commit | done |
| 2026-04-11 | spec-record-parse-peer-block-hardening (follow-up) | `runOrchestrated` (`internal/test/runner/runner_exec.go:825-834`, the success branch) does NOT call `validateLogging`. As a result, every `expect=stderr:pattern=`, `reject=stderr:pattern=`, and `expect=syslog:pattern=` directive on a test that uses `cmd=background`/`cmd=foreground` orchestration is silently SKIPPED. Confirmed empirically: setting `ze.log=disabled` (silencing all ze logging) on `logging-stderr.ci` does not fail the test, even though it asserts `expect=stderr:pattern=stageTransition\|deliverConfig\|handleRegistrationLine`. The non-orchestrated path at line 329 calls `validateLogging` correctly; the orchestrated success path was missed when the new format was added. The legacy `expect=stderr:contains=` form (`ExpectStderrMatch`) IS checked at line 805 for the `ExpectExitCode` branch only. | Wired `validateLogging` into both orchestrated branches (success + ExpectExitCode) in the ze-review resolve pass. Added syslog server setup to the orchestrated path and corrected the `ze.log.bgp.backend`/`ze.log.bgp.destination` typo to `ze.log.backend`/`ze.log.destination`. Four `.ci` files updated to match reality: logging-stderr, logging-syslog, logging-level-filter, logging-config-file. One new real bug surfaced (see next row -- hub reload). | done in same commit | done |
| 2026-04-11 | ze-review resolve pass | `hub-reload-no-change.ci` fails after `validateLogging` is wired in the orchestrated ExpectExitCode branch: stderr contains `reload error: reload: read config: read file/active/hub.conf: file does not exist` which matches the test's `reject=stderr:pattern=reload error`. The test sends SIGHUP to a ze hub process started with `ze hub.conf`; on reload the hub resolves the config path as `file/active/hub.conf` (a managed-mode/active-config layout) instead of re-reading the original `hub.conf`. Root cause unknown -- could be managed-mode path layering interacting with the tmpfs test harness working directory, or a regression in the hub reload handler. | Fixed in `internal/component/hub/reload.go`: `Orchestrator.Reload` now falls back to `os.ReadFile(configPath)` when `o.store.ReadFile` fails and the store is blob-backed. Mirrors the initial-load fallback that already existed in `cmd/ze/hub/main.go:107-113` and the BGP config reload fallback at `cmd/ze/hub/main.go:336-351`. hub-reload-no-change and the full 20/20 reload suite now green. | done in same commit | done |
| 2026-04-11 | ze-review resolve pass (follow-up) | `spec-plugin-relay-config-file-coverage`: deferred from the first pass because the plugin.relay chain appeared to not deliver plugin DEBUG stderr to the engine in the test harness. Root cause turned out to be a false assumption: `external gr { use bgp-gr }` runs the bgp-gr plugin as an INTERNAL (in-process) runner, not as a forked subprocess, so its slog handler writes directly to the engine's `os.Stderr` -- the plugin.relay hop is never involved. The missing piece was a registry-name vs config-key mismatch: `ConfigureEngineLogger` passes `"bgp-gr"` (registry name, with hyphen) to `slogutil.Logger`, so the effective env var is `ze.log.bgp-gr`, not `ze.log.gr`. | First pass: patched the .ci file to use `bgp-gr debug` and assert on `subsystem=bgp-gr`. Second pass: fixed the underlying design issue in `internal/component/plugin/inprocess.go`. `GetInternalPluginRunner` now transforms the registry name via a new `CanonicalSubsystemName` helper (`bgp-gr` -> `bgp.gr`, `bgp-filter-community` -> `bgp.filter.community`, etc.) before passing it to the plugin's `ConfigureEngineLogger` callback. Every plugin using the `SetLogger(slogutil.Logger(loggerName))` pattern (25+ plugins: gr, rib, rs, role, softver, aigp, healthcheck, hostname, llnh, rpki, rpki-decorator, persist, adj-rib-in, watchdog, route-refresh, all bgp-nlri-*, iface, iface-dhcp, fib-kernel, fib-p4, sysrib) now registers under the canonical dot-separated name. Unit test `TestCanonicalSubsystemName` in `inprocess_test.go` covers the transform. `test/plugin/logging-config-file.ci` rewritten to use the intuitive `bgp.gr debug` key and `subsystem=bgp.gr` assertion, with the earlier inert exploratory `option=env:var=ze.test.unused.*` lines removed. | done in same commit | done |
| 2026-04-11 | spec-cmd-4-prefix-filter | Per-prefix nlri rewriting: when an UPDATE carries multiple prefixes and only some match the prefix-list, return `action=modify` with a new `nlri` text containing only the matching prefixes instead of strict whole-update reject. v1 ships strict mode (any denied prefix rejects whole UPDATE). | Shipped in `plan/learned/552-cmd-4-prefix-filter-phase2.md`: filter_prefix now runs per-prefix partition in `partitionUpdate` and returns `action=modify` with a delta containing only the accepted prefixes. Engine-side `extractLegacyNLRIOverride` re-encodes the accepted subset as wire NLRI bytes and `buildModifiedPayload` writes them into the payload tail via a new `nlriOverride` parameter. IPv4-unicast legacy NLRI only in v1 -- MP_REACH rewriting for IPv6/labeled families stays deferred (filter plugins needing per-NLRI decisions on non-CIDR families declare raw=true per dest-2). Functional test `test/plugin/prefix-filter-modify-partial.ci` covers the mixed-prefix path; unit tests cover `partitionUpdate` and `extractLegacyNLRIOverride` end-to-end. | plan/learned/552-cmd-4-prefix-filter-phase2.md | done |
| 2026-04-11 | spec-cmd-4-prefix-filter | Documentation sweep: `docs/guide/configuration.md` (prefix-list config examples), `docs/guide/plugins.md` (bgp-filter-prefix plugin), `docs/comparison.md` (prefix-list filtering parity), `docs/features.md` (user-facing feature listing) | cmd-4 subset shipped in `plan/learned/553`: `features.md` gained a prefix-list filters bullet; `docs/guide/plugins.md` grew a "Prefix-List Filter" subsection covering chain-ref forms, action matrix, and the cmd-4 phase-2 modify path; `docs/guide/configuration.md` grew a "Prefix-List Filter" subsection with the full `bgp { policy { prefix-list } }` syntax, entry field table, chain-ref forms, and multi-prefix modify behaviour; `docs/comparison.md` flipped the `Prefix matching (ge/le)` row from No to Yes and enumerated the built-in filter plugins in the policy intro. cmd-5/6/7 sweeps remain open as they add their own filter types. | plan/learned/553-cmd-4-docs-sweep.md | done |
| 2026-04-11 | spec-cmd-4-prefix-filter /ze-review resolve | bgp-rpki `OnStarted` callback dispatches `adj-rib-in enable-validation` before adj-rib-in's commands are reachable via the engine dispatcher, causing bgp-rpki to exit code 1 during post-startup. This blocks session establishment because the crashed plugin's `validate-open` callback is still pending when a peer OPEN arrives, and `notifyMessageReceiver` never fires for the subsequent UPDATE. | Fixed in `plan/learned/549-plugin-startup-dispatcher-barrier.md`: added a new `ze-plugin-callback:post-startup` RPC sent by the engine after `signalStartupComplete` freezes both registries. New SDK method `OnAllPluginsReady(fn func() error)` dispatches via the event loop so cross-phase `DispatchCommand` is guaranteed to resolve. `bgp-rpki` moved from `OnStarted` to `OnAllPluginsReady`; workaround deleted. The original "Stage 4 share-registry barrier" hypothesis was wrong: the race is CROSS-PHASE, not intra-tier (bgp-rpki auto-loads Phase 1 via `ConfigRoots: ["bgp"]`, bgp-adj-rib-in loads Phase 2). | plan/learned/549-plugin-startup-dispatcher-barrier.md | done |
| 2026-04-11 | spec-cmd-4-prefix-filter /ze-review resolve | `.ci` test-infrastructure false positive: the Python observer pattern (`dispatch daemon shutdown; api.wait_for_shutdown(); sys.exit(1)`) is a no-op. The runner checks only ze's exit code. `daemon shutdown` causes ze to exit 0 cleanly, and the observer's subsequent `sys.exit(1)` does not propagate back to the runner. Every `test/plugin/*.ci` that relies on an observer assertion to signal failure (community-*, redistribution-*, and the initial cmd-4 accept/reject tests) is vulnerable. | Framework fix shipped in plan/learned/550: added `ze_api.runtime_fail(message)` that emits a slog-formatted `ZE-OBSERVER-FAIL` sentinel on stderr (bypasses the default WARN relay filter because ERROR-level passes unconditionally) then dispatches `daemon shutdown` and `sys.exit(1)`. Added `checkObserverSentinel` in `internal/test/runner/runner_validate.go` and wired it into both `runner_exec.go` paths BEFORE the timeout check, so runtime_fail takes precedence over timeout classification. Per-test conversion (community-*, redistribution-*, etc.) deferred because converting each test exposes multiple pre-existing bugs (e.g., community-tag exposes the `value 65000:100` leaf-list parser bug fixed in same commit AND a separate route-delivery issue). | plan/learned/550-ci-observer-exit-code-fix.md | done (framework); per-test conversion open |
| 2026-04-11 | spec-cmd-4-prefix-filter /ze-review resolve | Non-unicast families (EVPN, flowspec, VPN, BGP-LS, MVPN) are not emitted in the filter text protocol by `FormatUpdateForFilter`. A filter plugin attached to an EVPN/flowspec session will see attrs but no `nlri` block, and the prefix-list filter treats the absence as "no prefixes to evaluate" and accepts. | Fixed via the hybrid emit strategy in `plan/learned/551`: `FormatUpdateForFilter` now classifies families via `isCIDRFamily` (AFI IPv4/IPv6 x SAFI unicast/multicast/mpls-label). CIDR families emit `nlri <family> <op> <prefix>...` inline; non-CIDR families emit a marker-only `nlri <family> <op>` block so text-mode filters at least learn the family is present. Filters that need per-NLRI decisions on non-CIDR families MUST declare `raw=true` -- this contract is documented in `FilterRegistration`, `process-protocol.md`, and `ai/rules/plugin-design.md`. | plan/learned/551-filter-non-cidr-families.md | done |
| 2026-04-11 | spec-bfd-1-wiring (closes bfd-skeleton row) | `RunBFDPlugin` real implementation: parse YANG `bfd` section in `OnConfigure`, build `engine.NewLoop(transport.UDP, clock.RealClock{})` per VRF, drive `Service.EnsureSession`/`ReleaseSession` from `single-hop-session` and `multi-hop-session` config lists. Plus: `make generate` to add to `internal/component/plugin/all/all.go`, bump `TestAllPluginsRegistered`, register `ze.log.bfd` env var, delete safety comment in `internal/plugins/bfd/register.go`, add a `test/plugin/bfd/01-standalone-session.ci` so the wiring is provable per `rules/integration-completeness.md`. | Stage 1 wiring landed in `plan/learned/556-bfd-1-wiring.md`. The `ze.log.bfd` env var registration was unnecessary because `slogutil.go:45` registers `ze.log.<subsystem>` as a prefix wildcard. The functional test landed at the conventional `test/plugin/bfd-config-load.ci` flat path (Python orchestrator drives ze and asserts four lifecycle log patterns) plus a CLI-discovery `test/plugin/bfd-features.ci`. The hidden additional fix was loading `ze-bfd-conf` in `internal/component/config/yang_schema.go YANGSchemaWithPlugins` so the parser accepts `bfd { ... }` as a top-level keyword -- the same hard-coded list also omits `ze-rib-conf` so sysrib's `rib { ... }` still does not parse, tracked separately. | plan/learned/556-bfd-1-wiring.md | done |
| 2026-04-11 | bfd-skeleton (e5a4add9) | Production transport hardening: GTSM TTL extraction (`IP_RECVTTL` / `IPV6_RECVHOPLIMIT` via `ReadMsgUDPAddrPort`) so `Inbound.TTL` carries the real value; engine enforcement of TTL=255 single-hop and `>= MinTTL` multi-hop; outbound TTL=255 setsockopt; `SO_BINDTODEVICE` for single-hop; RFC 5880 §6.8.7 0-25% TX jitter (one-call addition in `engine/loop.go:tick`). | Stage 2 landed in `plan/learned/559-bfd-2-transport-hardening.md`: IPv4 GTSM (single-hop TTL==255 gate, multi-hop min-TTL), `IP_TTL=255` outbound, `SO_BINDTODEVICE` via `net.ListenConfig.Control` for single-hop interface and multi-VRF binding, and RFC 5880 §6.8.7 TX jitter (0-25%, clamped [10%, 25%) when DetectMult==1) via `engine.Loop.applyJitter`. IPv6 dual-bind deliberately deferred -- see the new `spec-bfd-2b-ipv6-transport` row below. | plan/learned/559-bfd-2-transport-hardening.md | done |
| 2026-04-11 | spec-bfd-2-transport-hardening | IPv6 dual-bind for the BFD UDP transport: second socket bound to `::0` (or a replaced `transport.Dual` wrapping v4 + v6 `UDP` pair), `IPV6_RECVHOPLIMIT` cmsg extraction to populate `Inbound.TTL` for v6 traffic, `IPV6_UNICAST_HOPS=255` outbound option. Requires a `transport.Dual` refactor or per-family loop keying; Stage 2 deliberately kept the scope IPv4-only to avoid the refactor. | Stage 2b landed in `plan/learned/564-bfd-2b-ipv6-transport.md`: new `transport.Dual` wrapper pairs an IPv4 UDP with an optional IPv6 UDP per (vrf, mode) loop; `applySocketOptionsV6` Linux helper sets `IPV6_RECVHOPLIMIT` + `IPV6_UNICAST_HOPS=255` + `SO_BINDTODEVICE`; `parseReceivedTTL` recognizes both `IP_TTL` and `IPV6_HOPLIMIT` cmsgs so a single readLoop populates `Inbound.TTL` for both families; `UDP.Start` branches on `Bind.Addr().Is6()` to pick `udp4`/`udp6` network + address formatting; new top-level `bfd { bind-v6 true }` YANG leaf opts in; `newTransport` dispatches to `Dual` or bare `UDP` based on config; the `.ci` test `bfd-ipv6-dual-bind` drives a pinned v6 single-hop session and asserts the lifecycle logs. Mixed v4/v6 pinned sessions share one Dual per loop; metrics remain unchanged because the merger keeps Inbounds family-agnostic. | plan/learned/564-bfd-2b-ipv6-transport.md | done |
| 2026-04-11 | bfd-skeleton (e5a4add9) | BGP plugin opt-in via YANG augment for `bgp peer { bfd { ... } }`: schema sketch in `docs/guide/bfd.md`; needs `ze:augment` (cross-component) per `rules/config-design.md`. BGP reactor calls `Service.EnsureSession` on peer startup, subscribes to state changes, tears the peer down on Down. | Stage 3 landed in `plan/learned/560-bfd-3-bgp-client.md`: new `internal/plugins/bfd/api/registry.go` exposes `SetService`/`GetService` via an `atomic.Pointer[Service]`; the bfd plugin publishes a `pluginService` wrapper on `OnStarted` and clears it on shutdown. BGP gains a YANG augment `bgp peer connection bfd { enabled, mode, profile, min-ttl, interface }` and a per-peer `bfdClient` struct driven by `peer_run.go`'s FSM callback: `startBFDClient` fires on `StateEstablished`, a long-lived subscriber goroutine translates BFD Down events into `Peer.Teardown` with RFC 9384 Cease subcode 10 ("BFD Down"), and `stopBFDClient` releases the handle on exit from Established. Unit tests exercise the happy path, nil-service fallback, enabled-false no-op, single-hop/multi-hop request shape, subscriber-driven teardown, and release idempotency. Functional wiring test `test/plugin/bgp-bfd-opt-in.ci` proves the YANG augment parses and the plugin publishes its Service. FRR interop and the sub-2s failover test remain deferred -- see `spec-bfd-3b-frr-interop` below. | plan/learned/560-bfd-3-bgp-client.md | done |
| 2026-04-11 | spec-bfd-3-bgp-client | FRR `bfdd` interop scenario under `test/interop/scenarios/bgp-bfd-frr/`: two netns, ze on one side, FRR `bgpd` + `bfdd` on the other, assert BGP session reaches Established, BFD session reaches Up, link failure drops BGP in < 2 s (vs 90 s hold timer). Highest-value test for catching wire-format regressions but depends on FRR interop scaffolding and a second BFD-capable peer. | Stage 3b landed in `plan/learned/565-bfd-3b-frr-interop.md`: new `test/interop/scenarios/33-bfd-frr/` with `ze.conf` (YANG-form BGP peer with `connection { bfd { enabled true; mode single-hop; profile fast } }`), `frr.conf` (FRR `bfd { peer 172.30.0.2 }` + `router bgp { neighbor 172.30.0.2 bfd }`), and `check.py` that calls `frr.wait_session(ZE_IP)` followed by `frr.wait_bfd_up(ZE_IP)`. New `FRR.wait_bfd_up` helper in `test/interop/interop.py` polls `vtysh -c "show bfd peers"` for `Status: up`. `test/interop/daemons` toggles `bfdd=yes` globally so every scenario can use BFD without a runner change. The scenario runs under `python3 test/interop/run.py 33-bfd-frr` (requires Docker + FRR image). Link-failure sub-2s regression is not asserted yet because the scenario does not induce a failure; a follow-up row can add a link-down trigger once the test is stable in CI. | plan/learned/565-bfd-3b-frr-interop.md | done |
| 2026-04-11 | bfd-skeleton (e5a4add9) | Operator UX: `show bfd sessions`, `show bfd profile`, `show bfd session <peer>` CLI commands plus Prometheus gauges (`ze_bfd_sessions{state}`, `ze_bfd_transitions_total{diag}`, `ze_bfd_detection_expired_total`). UX shape sketched in `docs/guide/bfd.md` Observing-state section. | Stage 4 landed in `plan/learned/561-bfd-4-operator-ux.md`: new `internal/plugins/bfd/api/snapshot.go` (`SessionState`, `TransitionRecord`, `ProfileState`); `Loop.Snapshot`/`SessionDetail` copy live state under `l.mu`; `sessionEntry.recordTransition` keeps a bounded 8-entry ring; engine `MetricsHook` fires from `makeNotify`/TX/RX; plugin-level `metricsHook` + `bindMetricsRegistry` publish `ze_bfd_sessions`, `ze_bfd_transitions_total`, `ze_bfd_detection_expired_total`, `ze_bfd_tx_packets_total`, `ze_bfd_rx_packets_total`; `refreshSessionsGauge` populates the gauge from `pluginService.Snapshot`. New `ze-bfd-api.yang` module declares RPCs; new `internal/component/cmd/bfd/` package augments `clishowcmd:show` with `bfd { sessions, session, profile }` and the handlers call `api.GetService()` directly (in-process, zero IPC hop). Four `.ci` tests in `test/plugin/bfd-show-*.ci` and `test/plugin/bfd-metrics.ci` plus Go unit tests cover snapshot, handlers, and metrics. The `OnStarted` hook rebinds the metrics registry with the live reactor-provided instance because plugin Phase 1 runs before the BGP loader sets up the Prometheus server. | plan/learned/561-bfd-4-operator-ux.md | done |
| 2026-04-11 | bfd-skeleton (e5a4add9) | FRR `bfdd` interop scenario under `test/interop/scenarios/`: highest-value test for catching wire-format regressions. Two namespaces, ze on one side, FRR on the other, asserts session reaches Up and detects induced failure. Closed 2026-04-12: `test/interop/scenarios/33-bfd-frr/` shipped in Stage 3b (`plan/learned/565-bfd-3b-frr-interop.md`) with BGP+BFD handshake assertions, and the `wg-handover` session (commit `e1cfd3f8`) extended `check.py` with `FRR.break_link()` (iptables -I OUTPUT -p udp --dport 3784 -j DROP) + a monotonic-clock teardown assertion (< 2 s). | plan/learned/565-bfd-3b-frr-interop.md + commit e1cfd3f8 | done |
| 2026-04-11 | bfd-skeleton (e5a4add9) | Authentication digest verification (RFC 5880 §6.7) -- parser exists in `internal/plugins/bfd/packet/auth.go` (Type+Len header only); keyed-SHA1 verifier and sequence-number persistence across restart are TODO. First-pass scope rejects every authenticated packet with `ErrAuthMismatch`. | Stage 5 landed in `plan/learned/562-bfd-5-authentication.md`: new `internal/plugins/bfd/auth/` subpackage with a generic `digestSigner`/`digestVerifier` shared between all four keyed variants (Keyed MD5, Meticulous Keyed MD5, Keyed SHA1, Meticulous Keyed SHA1); `SeqState` enforces replay protection with meticulous strict-increase; `SeqPersister` coalescing writer persists TX sequence across restart; `session.AuthPair`/`Machine.Sign`/`Machine.Verify`/`Machine.AdvanceAuthSeq`/`Machine.CloseAuth` wire the signer + verifier into the FSM; `engine.sendLocked` signs after `WriteTo`, `engine.handleInbound` verifies before the reception procedure, `ze_bfd_auth_failures_total` counter increments on every rejection; YANG profile grows a presence `auth { type key-id secret }` block and a top-level `persist-dir` leaf; Simple Password is refused at parse time with an RFC 5880 §6.7.2 citation; three `.ci` tests (`bfd-auth-sha1`, `bfd-auth-mismatch`, `bfd-auth-meticulous-persist`) plus the Go unit tests cover the happy path, simple-password rejection, and sequence persistence. `ze config show` secret redaction (AC-11) and a dedicated fuzz target for the auth body are deferred as follow-up rows. | plan/learned/562-bfd-5-authentication.md | done |
| 2026-04-11 | spec-bfd-5-authentication | `ze config show` secret redaction: the BFD plugin never logs secrets, but a full `ze config show` round-trip that demonstrates the secret field renders redacted belongs to the core config pipeline. Closed 2026-04-12 by the `wg-handover` session: the `ze:sensitive` YANG extension was already defined in `ze-extensions.yang` and already wired through `yang_schema.hasSensitiveExtension` + `LeafNode.Sensitive` + `config.SensitiveKeys` + `cmd/ze/config/cmd_dump.go maskMapValues`/`maskValue` (JunOS `$9$` encoding by default, `--strip-private` renders `/* SECRET-DATA */`), but `internal/plugins/bfd/schema/ze-bfd-conf.yang` was the only password/secret/token/key leaf across the project that had not imported `ze-extensions` and marked its secret leaf. Fix: one `import ze-extensions { prefix ze; }` line + one `ze:sensitive;` tag on `leaf secret`. Audited every other plugin YANG (`internal/component/api`, `internal/component/ssh`, `internal/component/mcp`, `internal/component/bgp`, `internal/component/plugin`); all were already correct. Verified end-to-end via `bin/ze config dump --json` (plaintext renders as `$9$...`) and `bin/ze config dump --json --strip-private` (renders as `/* SECRET-DATA */`). | this session | done |
| 2026-04-11 | spec-bfd-5-authentication | Dedicated fuzz target for the BFD authentication body parser (`FuzzAuthDigest`): the existing `packet/fuzz_test.go` covers `ParseControl` but not the digest body. Closed 2026-04-12 by the `wg-handover` session: new `internal/plugins/bfd/auth/fuzz_test.go` seeds the corpus with a round-trip Sign/Verify pair for each of the four keyed variants (Keyed MD5, Meticulous Keyed MD5, Keyed SHA1, Meticulous Keyed SHA1) plus three hand-crafted short-body rows; the fuzz function drives `Verifier.Verify` with adversarial `data`, `authType`, `keyID`, `secret` tuples and asserts the replay floor never advances on a rejected packet. Ran `go test -fuzz=FuzzAuthDigest -fuzztime=10s` locally: 5.7M executions in 10 s, no panics, no new crashers. | this session | done |
| 2026-04-11 | bfd-skeleton (e5a4add9) | Echo mode (RFC 5880 §6.4 + RFC 5881 §5): UDP 3785 socket, RTT measurement, slowed control TX. Single-hop only -- RFC 5883 §4 prohibits multi-hop echo. | Stage 6 landed the config + wire-advertisement half in `plan/learned/563-bfd-6-echo-mode.md`: new `packet.Echo` with a 16-byte `ZEEC` envelope codec; `session.Vars` gains `DesiredMinEchoTxInterval`, `RequiredMinEchoRxInterval`, `RemoteMinEchoRxInterval`; `Machine.Build` populates the outgoing Control packet's `RequiredMinEchoRxInterval`; `Machine.EchoEnabled` / `EchoInterval` expose the negotiated cadence; YANG profile grows a presence `echo { desired-min-echo-tx-us }` container; the config parser refuses echo on multi-hop sessions with an RFC 5883 §4 citation; `ze_bfd_echo_tx_packets_total` and `ze_bfd_echo_rx_packets_total` metric families plus `MetricsHook.OnEchoTx` / `OnEchoRx` are registered; two `.ci` tests (`bfd-echo-config`, `bfd-echo-multi-hop-reject`) cover the happy path and the rejection. The transport half (second UDP socket on 3785, per-session TX scheduler, RX demux + reflect + RTT histogram, detection-time switchover, async slow-down) is tracked as `spec-bfd-6b-echo-transport`. | plan/learned/563-bfd-6-echo-mode.md | done |
| 2026-04-11 | spec-bfd-6-echo-mode | Phase A of the BFD Echo transport: second UDP socket on port 3785 (single-hop loops only), per-session echo TX scheduler integrated with the express loop, echo RX demux + reflect path for peer-originated echoes, self-carried TimestampMs-based RTT measurement, `ze_bfd_echo_rtt_us` histogram. Closed 2026-04-12 by the `wg-handover` session: new `internal/plugins/bfd/engine/echo.go` (`echoTickLocked`, `handleEchoInbound`, `reflectEchoLocked`, `recordEchoRTTLocked`), `transport.UDPPortEcho` reused on a second `*transport.UDP` wired via `bfd.newEchoTransport`/`loopFor`, `engine.NewLoopWithEcho` constructor, `MetricsHook.OnEchoRTT(mode, time.Duration)` + `echoRTTUs` HistogramVec in `internal/plugins/bfd/metrics.go`, `Machine.PrimeEcho`/`AdvanceEcho`/`NextEchoTxDeadline`/`NextEchoSequence`/`RecordEchoRTT`/`LastEchoRTT`/`ClearEchoSchedule` on `internal/plugins/bfd/session/timers.go`, and an `internal/plugins/bfd/engine/echo_test.go` loopback round-trip test. | this session, Phase A | done |
| 2026-04-12 | spec-bfd-6-echo-mode | Phase B.1+B.2 of the BFD Echo transport: (1) outstanding echo-ID ring per session to track echoes that never reflect, (2) echo-mode detection timer that declares the session Down with DiagEchoFailed when the oldest outstanding entry is older than `DetectMult * EchoInterval()`. Closed 2026-04-12 by the `wg-handover` session: `internal/plugins/bfd/session/session.go` grows a fixed 16-slot `echoOutstanding [echoOutstandingCap]echoEntry` ring; `internal/plugins/bfd/session/timers.go` grows `RegisterEchoTx`, `MatchEchoRx`, `EchoDetectInterval`, `EchoDetectionExpired`, `EchoFail`; `ClearEchoSchedule` now wipes the ring so a session that flaps down does not carry stale entries back into Up; `internal/plugins/bfd/engine/echo.go` registers every TX into the ring, matches RX against it for RTT (falling back to the ZEEC `TimestampMs` envelope value on ring evictions), and calls `EchoFail` from `echoTickLocked` when `EchoDetectionExpired` fires. New `TestEchoDetectionSwitchover` in `internal/plugins/bfd/engine/echo_test.go` drives the full detection path via a `dropEchoTransport` that swallows every outbound echo and asserts a Down+DiagEchoFailed transition within 2 s. Race-clean under `-race -count=10` (stress). | this session, B.1+B.2 | done |
| 2026-04-12 | spec-iface-wireguard (learned/566) | Dynamic YANG-walk listener collector: replace the hardcoded `knownListenerServices` list in `internal/component/config/listener.go` with a YANG-schema walk that discovers every `ze:listener`-marked node dynamically | Out of scope for the wireguard spec; the minimal extension (adding wireguard to the hardcoded list + Protocol field) is sufficient for now | plan/learned/568-listener-dynamic-walk.md | done |
| 2026-04-12 | spec-iface-wireguard (learned/566) | Per-case tunnel MAC handling: move `mac-address` from `interface-l2` (list-level) into the per-case YANG containers for gretap/ip6gretap only, so L3 tunnel cases (gre/ipip/sit/ip6tnl) no longer carry a vestigial `mac-address` leaf | Would expand the D2 grouping split scope significantly; tunnel already ships on `interface-l2` and users have not reported confusion | plan/learned/567-iface-tunnel-mac-per-case.md | done |
| 2026-04-12 | spec-iface-wireguard (learned/566) | AC-9 duplicate peer name rejection: the config tree `AddListEntry` auto-renames duplicate list keys with `#N` suffixes (`tree.go:227`), so `parseWireguardEntry` never sees a true duplicate. Enforcing rejection requires a YANG-parser-level change to reject duplicate keys instead of auto-renaming | YANG parser change is cross-cutting and affects every list in the config, not just wireguard peers | no receiving spec yet | open |
| 2026-04-12 | spec-iface-wireguard (learned/566) | `ze config validate` invoking plugin `OnConfigVerify`: currently only runs YANG schema + BGP/hub-specific checks. Iface parser-level rejections (wireguard missing private-key, bad public-key, tunnel missing encapsulation, both-locals) only fire at daemon reload, not at static validation time | Pre-existing architectural gap affecting all plugin validators, not just wireguard | no receiving spec yet | open |
| 2026-04-12 | spec-bfd-6-echo-mode | Phase B.3 (async control slow-down) and Unsubscribe race fix: both closed 2026-04-12. B.3: `ApplyEchoSlowdown` raises `DesiredMinTxInterval` and `RequiredMinRxInterval` to `max(1s, configured)` with `PollOutstanding=true`; `ClearEchoSchedule` calls `revertEchoSlowdownLocked` to restore + re-Poll; idempotent via `echoSlowdownApplied` flag; `echoTickLocked` calls `ApplyEchoSlowdown` when Up+EchoEnabled. `TestEchoSlowdown` unit test. Unsubscribe: `trySendStateChange` now uses `recover()` guard; `Unsubscribe` no longer closes the channel (Loop.Stop handles cleanup post-loop). | this session, B.3+unsub | done |
| 2026-04-12 | spec-bfd-6-echo-mode | Phase B.4 `.ci` integration test `bfd-echo-handshake.ci`. Closed 2026-04-12: Python BFD Control speaker + echo reflector bound to 127.0.0.2 (specific-IP bind takes kernel routing precedence over ze's wildcard 0.0.0.0). Helper drives three-way handshake with `RequiredMinEchoRxInterval=50000`; observer scrapes telemetry for `echo_tx_packets > 0` and `echo_rtt_us_bucket` present. `bfd-echo-failover.ci` and `bfd-echo-metrics.ci` remain open as follow-up. | this session, B.4 partial | done |
| 2026-04-12 | spec-cmd-7-route-modify | AS-path prepend via modify filter: `textDeltaToModOps` explicitly skips `as-path` (filter_delta.go:198) to avoid clobbering EBGP AS prepend at wire layer. `AttrModPrepend` action type exists (registry_bgp_filter.go:95) but no handler is registered for AS_PATH (code 2). Needs a dedicated prepend mechanism, not text delta. | Architectural gap in the text delta pipeline | this session | done |
| 2026-04-12 | spec-cmd-7-route-modify | Export-path modify: egress `PolicyFilterChain` call in `reactor_api_forward.go:420` discards the delta (underscore placeholder). Import path has the full `textDeltaToModOps` + `buildModifiedPayload` wiring at `reactor_notify.go:395-410`. Export needs the same wiring. | Framework gap, not plugin gap | this session | done |
| 2026-04-11 | bfd-skeleton (e5a4add9) | Demand mode (RFC 5880 §6.6) and S-BFD / Micro-BFD on LAG / Multipoint BFD (RFC 7880, 7881, 7130, 8562). | Niche features. None of the listed reference implementations (FRR, BIRD 3.x) deploy these widely. Skip until concretely requested. | user-approved-drop | cancelled |
| 2026-04-11 | spec-bfd-1-wiring | `internal/component/config/yang_schema.go YANGSchemaWithPlugins` hard-codes the list of YANG modules. Closed 2026-04-12: replaced 14 hardcoded blocks with a loop over `loader.ModuleNames()` filtered by `-conf` suffix. `Schema.Define` merges same-named containers. Any future `*-conf` module is picked up automatically. All config tests pass. | this session | done |
| 2026-04-14 | spec-vpp-0-umbrella design review | vpp-3 MPLS label operations: sysRIB event payload (`outgoingChange`) has no `labels` field. Needs sysRIB label extension spec before vpp-3 can proceed. | sysRIB `outgoingChange` struct has Action/Prefix/NextHop/Protocol only, no labels. Backward-compatible extension (JSON omitempty) but needs explicit design. | spec-sysrib-labels (not yet written) | open |
| 2026-04-14 | spec-vpp-0-umbrella design review | vpp-5 ACL feature: VPP ACLs dropped from vpp-5. Owned by spec-fw-6-firewall-vpp (firewall Backend interface pattern). | Firewall component already has Backend interface. ACL is a firewall backend, not a standalone VPP feature. | spec-fw-6-firewall-vpp | open |
| 2026-04-14 | spec-l2tp-1-wire AC-14 | Oversize-buffer contract for AVP writers. The package contract is "caller sizes the buffer" (same as BGP wire helpers); no runtime assertion is enforced. Fuzz targets already cover parse-side bounds. | Adding an internal bounds check would violate buffer-first (allocation or branch on hot path) and would be inconsistent with the rest of the ze wire code. | user-approved-drop | cancelled |
| 2026-04-14 | spec-l2tp-1-wire | L2TP-wire ze-test runner category to pick up `test/l2tp-wire/*.ci` automatically. Phase 1 uses Go integration tests in `cmd/ze/l2tp/decode_test.go` instead; `.ci` files exist on disk as spec artifacts. | The existing ze-test `parse` runner is specialized for config parsing (`stdin=config`) and rejects `stdin=payload:hex=`. Writing a new runner is out of scope for a pure library phase. | spec-l2tp-3-tunnel | done |
| 2026-04-15 | spec-l2tp-4-session | Incoming call LAC side: detect call, send ICRQ, receive ICRP, send ICCN (wait-tunnel state) | Requires LAC-initiated tunnel creation (sending SCCRQ), not implemented in phase 3 | spec within l2tp set (before set closes) | open |
| 2026-04-15 | spec-l2tp-4-session | Outgoing call LNS side: send OCRQ, receive OCRP, receive OCCN (wait-tunnel state) | Requires LAC-initiated tunnel creation (sending SCCRQ), not implemented in phase 3 | spec within l2tp set (before set closes) | open |
| 2026-04-15 | spec-l2tp-5-kernel | `docs/guide/configuration.md` note about l2tp_ppp/pppol2tp kernel module requirement | Phase 5 has no user-visible config knob; the operator-facing note belongs alongside Phase 7's L2TP config section | spec-l2tp-7-subsystem | open |
| 2026-04-15 | spec-l2tp-6a-lcp-base | `.ci` functional test of LCP-Opened + MTU set against accel-ppp peer | Requires kernel `l2tp_ppp` + `/dev/ppp` + root + accel-ppp; same constraint as Phase 5. PPP code path is exercised by Go tests using `net.Pipe` substitute for chan/unit fds | spec-l2tp-7-subsystem | open |
| 2026-04-15 | spec-l2tp-6a-lcp-base | LCP Restart timer + IRC/ZRC action bookkeeping (RFC 1661 §4.6). 6a's `performAction` treats `LCPActIRC`/`LCPActZRC` as no-ops; the 30s `defaultNegoTimeout` covers the "silent peer" case at the session level but does NOT implement per-CR retransmit with exponential backoff. A hostile peer that ACKs the 1st CR then never responds again will hang for 30s per stall rather than 3/6/12s retransmits culminating in teardown. | Full retransmit machinery requires a per-session timer integration; landing it now would couple PPP to a timer abstraction the spec set hasn't yet established. Phase 7 (subsystem wiring) is the natural home because it owns the engine lifecycle that timers attach to | spec-l2tp-7-subsystem | open |
| 2026-04-16 | spec-l2tp-6a-lcp-base review | `docs/architecture/core-design.md` PPP component entry: component-imports table has no `ppp` row. 6a deferred the cross-component doc update to keep the spec focused on code. | Phase 7 owns cross-component documentation rewrite (YANG leaves, operator-facing config sections, and component-imports additions land together). | spec-l2tp-7-subsystem | open |
| 2026-04-16 | spec-l2tp-6a-lcp-base review | Buffer-first violation in `internal/component/l2tp/reliable.go:298,311`: `append([]byte(nil), body...)` when queueing and `make([]byte, length)` per message in `send()`. The engine retains these bytes in `rtmsQueue` for retransmission, so per-call pool borrow/return does not fit; a dedicated retransmit pool (`sync.Pool` of 1023-byte slabs released on `processNr` ACK) is required. | Newly-strengthened `design-principles.md` "No make where pools exist" rule applies. Fix is a focused refactor of the engine's ownership model; belongs in a dedicated commit alongside the broader pool work the pool-resolution agent is running. | b39b8eb4 refactor(l2tp): pool retransmit buffers in ReliableEngine | done |
| 2026-04-16 | make-pool audit (learned/603) | `internal/component/bgp/message/update_build.go` allocations (`attrBytes`, `inlineNLRI`, `result`) escape via the returned `*Update` struct. Pooling them requires either caller-owned buffers (API rewrite) or a `Release()` lifecycle on Update (caller protocol change). | Substantial API redesign — out of scope for the audit's "1 subsystem = 1 commit" cadence. Needs its own spec covering Update lifetime semantics across the reactor, RIB cache, and per-peer dispatch. | learned/604-update-pool | done |
| 2026-04-16 | make-pool audit (learned/603) | BGP NLRI builders in `internal/component/bgp/message/update_build_{vpn,labeled,evpn,flowspec,mup,mvpn,vpls}.go`: `attrBytes := make([]byte, attribute.AttributesSize(attrs))` per build. Same blocker as `update_build.go` — output slice stored in returned `*Update`. | Same API redesign required. Will land together with the `update_build.go` migration once the Update lifetime spec is approved. | learned/604-update-pool | done |
| 2026-04-16 | spec-l2tp-6a-lcp-base review | Hidden-AVP `FlagHidden` bit is extracted by `AVPIterator.Next` but ignored by every FSM parser. Inbound AVPs with `H=1` are treated as plaintext. No auth bypass today (hidden ChallengeResponse verification fails), but proxy-auth AVPs and informational AVPs (HostName, CalledNumber) are garbled. | Low current impact (auth paths fail closed). Originally scheduled to land with 6b's proxy-auth consumption; 6b dropped proxy-auth (see 2026-04-16 drop deferral below) so this moves to the next spec that actually parses a hidden-sensitive AVP, or to 7-subsystem as defence-in-depth. | spec-l2tp-7-subsystem | open |
| 2026-04-16 | spec-l2tp-6b-auth scope | Proxy-authentication consumption (`trust-proxy` config, `ProxyAuth*` fields on `ppp.StartSession`, proxy-auth short-circuit, `EventAuthSuccess` on proxy path, AC-11/12, `TestProxyAuth*`, Phase 7 of the old plan) | Three-implementation survey: accel-ppp (`l2tp.c:3590-3594`), mpd5 (`src/l2tp_avp.c:714-737`), xl2tpd (`avp.c:54-58` + `avp.c:383-388` labels it "a ridiculous security threat"). Every production RFC 2661 LNS parses-and-ignores these AVPs; none has shipped a trust-proxy knob. Adding one yields a credential-forwarding surface with no offsetting benefit. `L2TPSession.proxyAuthen*` fields stay populated for logs but PPP consumes none of them. | user-approved-drop | cancelled |
| 2026-04-16 | spec-l2tp-6a-lcp-base review | No default cap on `MaxTunnels` / `MaxSessions` — both default to `0 = unbounded`. An unauthenticated (or now-blocked-by-auth-fix) peer flooding SCCRQ can exhaust memory before auth denies them. | Requires YANG default decision + operator-visible doc update. Risk reduced by the 6a auth-bypass fix (peer must now produce a valid CHAP response before a session is added), but a non-zero hard default is still the defence-in-depth stance. | spec-l2tp-7-subsystem | open |
| 2026-04-15 | spec-tacacs AC-13 | `ze show tacacs` CLI surfacing per-server reachability and auth stats | Operational tooling, not blocking core auth path; the four `.ci` wiring tests cover AC-1/AC-2/AC-3/AC-5/AC-6/AC-8 end-to-end, and operators can verify reachability today via `ze cli -c "summary"` against the daemon log (`auth success ... source=tacacs` vs `source=local`). Best landed alongside other observability work (per-server probe + counters surfaced via metrics) rather than as a standalone CLI | cmd/ze/tacacs/main.go + test/plugin/tacacs-show.ci | done |
| 2026-04-15 | spec-tacacs AC-9/AC-10 | TACACS+ authorization functional `.ci` tests (per-command pass/fail) | Authorization wiring exists (`tacacs.NewTacacsAuthorizer` + `Dispatcher.SetAuthorizer`); functional .ci coverage requires a mock that distinguishes commands and a wiring assertion that the chosen command actually consults the authorizer. Unit tests in `authorizer_test.go` cover the bridge (PassAdd/Fail/Repl/Error/Fallback). Tracking as separate scope rather than block spec closure | test/plugin/tacacs-author.ci (mock `--author-deny` flag) | done |
| 2026-04-15 | spec-tacacs AC-16 | Single-connect mode (RFC 8907 §4.4): persistent TCP across sessions when server echoes flag 0x04 | Mock and client always open a fresh TCP per session; saves a TCP handshake per auth but no functional regression and Cisco TACACS+ servers commonly disable single-connect for security | client.go connection pool + tacacs-singleconnect.ci | done |
| 2026-04-17 | spec-l2tp-6b-auth Phase 7 | LCP Configure-Nak / Configure-Reject handling for the Auth-Protocol option: when a peer rejects our advertised Auth-Protocol, the FSM resends CONFREQ without it and reaches Opened, but `s.negotiatedAuthMethod = s.configuredAuthMethod` still fires. The session enters the configured handler and times out waiting for a wire message the peer will not send. | Phase 7 only covers dispatch; NAK/Reject fallback was always scoped to Phase 8 (AC-13). Production path is proxy LCP where the LAC already negotiated with the peer, so the over-promise is hidden there. Non-proxy LNS deployments must wait for Phase 8 before this gap closes. | spec-l2tp-6b-auth (Phase 8) | done |
| 2026-04-17 | spec-vpp-7 (learned/610) | `test/vpp/003-fib-withdraw.ci`, `004-vpp-restart.ci`, `007-coexist.ci` functional tests | Phase 1 delivered stub + runner + `vpp.external` leaf; these tests require Phase 2 (real VPP API dispatch) and Phase 3 (FIB plugin wiring) | spec-vpp-7 Phase 3 | open |
| 2026-04-17 | spec-vpp-7 (learned/610) | `test/vpp/005-mpls-push.ci` MPLS label push functional test | Blocked on spec-vpp-3 (MPLS label operations), which itself is blocked on sysRIB label extension (`outgoingChange` has no `labels` field) | spec-vpp-3 (blocked on spec-sysrib-labels) | open |
| 2026-04-17 | spec-vpp-7 (learned/610) | `test/vpp/006-iface-create.ci` VPP interface creation functional test | Blocked on spec-vpp-4 (iface-vpp), which owns VPP-side interface lifecycle | spec-vpp-4 | open |
| 2026-04-17 | spec-vpp-7 (learned/610) | Stats-segment emulation in `vpp_stub.py` (shared-memory stat counters) | Phase 1 stub covers binary API only; stats-segment is a separate shared-memory protocol needing its own emulation layer | spec-vpp-6 or spec-vpp-7 Phase 4 | open |
| 2026-04-17 | spec-vpp-7 (learned/610) | Fault injection (`--inject "ip_route_add_del:retval=-1:after=3"`) for error-path testing | Requires stub enhancement to return configurable error retvals on specific messages after N successes | spec-vpp-7 Phase 4 | open |
| 2026-04-17 | spec-vpp-7 (learned/610) | `ip_route_dump` streaming handler in `vpp_stub.py` (return installed routes on request) | Phase 1 stub handles `ip_route_add_del` only; dump requires maintaining state across calls | spec-vpp-7 Phase 4 | open |
| 2026-04-17 | spec-vpp-6-telemetry (learned/612) | `test/vpp/012-telemetry.ci` functional test for stats segment metrics | Stats segment emulation not yet in `vpp_stub.py`; all 14 ACs covered by unit tests. fibvpp metrics and stats_up gauge testable once stub gains stats socket emulation | spec-vpp-7 Phase 4 (stats-segment emulation) | open |
| 2026-04-17 | spec-fmt-0-append | Migrate `format/text_update.go` (UPDATE-path `FormatMessage`, `formatFromFilterResult`, `formatRawFromResult`, `formatParsedFromResult`, `formatFullFromResult`, `formatEmptyUpdate`, `formatNonUpdate`, `formatSummary`, `FormatSentMessage`, `FormatNegotiated`) off `fmt.Sprintf`/`fmt.Fprintf`/`strings.Builder`/`strings.Replace` onto AppendText | fmt-0-append scope is bounded to non-UPDATE formatters to keep the diff auditable; UPDATE-path rewrite needs its own Append pipeline for `bgpfilter.FilterResult`, raw-components, and ADD-PATH rendering | spec-fmt-1-text-update | done |
| 2026-04-17 | spec-fmt-0-append | Migrate `format/format_buffer.go` (`FormatASPathJSON`, `FormatCommunitiesJSON`, `FormatOriginJSON`, `FormatMEDJSON`, `FormatLocalPrefJSON`) off `io.Writer` + `fmt.Fprintf` onto `AppendXxx(buf []byte, ...) []byte` | Re-scoped at SCOPE gate: repo-wide grep found zero production callers of the five Format*JSON helpers. fmt-2-json-append deleted `format_buffer.go` + test, migrated the actually-used cold-path sites in `decode.go` and `json.go` off `fmt.Sprintf`/`fmt.Fprintf`, and graduated the hand-maintained banned-pattern list to `block-format-alloc.sh` over 9 files. | spec-fmt-2-json-append | done |
| 2026-04-17 | spec-fmt-0-append | Switch plugin IPC from `FilterUpdateInput.Update string` (JSON) to a length-prefixed raw-bytes wire format so the final `string(scratch)` at the IPC boundary can be dropped | Breaks every external filter plugin via SDK; changes pkg/plugin/rpc contract; needs its own design doc and migration window | spec-plugin-ipc-raw-bytes | open |
| 2026-04-17 | spec-vpp-4-iface | AC-5 CreateTunnel (vxlan): wire `VxlanAddDelTunnelV3` in ifacevpp | `go.fd.io/govpp/binapi/vxlan` is not in `vendor/`; needs `make vendor-pull` and user approval (`go-standards.md` dependencies rule) | spec-vpp-4b-tunnels | open |
| 2026-04-17 | spec-vpp-4-iface | AC-6 CreateTunnel (gre, ipip): wire `GreTunnelAddDel` / `IpipAddTunnel` in ifacevpp | `go.fd.io/govpp/binapi/gre` and `/ipip` are not in `vendor/`; same third-party dependency gate | spec-vpp-4b-tunnels | open |
| 2026-04-17 | spec-vpp-4-iface | AC-11 GetStats: interface rx/tx/drop counters via VPP stats segment | Stats API is a separate library (`go.fd.io/govpp/core/stats`), not in `vendor/`; needs user approval to vendor | spec-vpp-6b-iface-stats | open |
| 2026-04-17 | spec-vpp-4-iface | AC-12 LCP pair management: Linux TAP shadow of VPP interface for BGP TCP bind | `go.fd.io/govpp/binapi/lcp` is not in `vendor/`; needs user approval to vendor the package and to design the TAP netns model | spec-vpp-4c-lcp | open |
| 2026-04-17 | spec-vpp-4-iface | Mirror/SPAN: wire `SpanEnableDisableL2` in SetupMirror / RemoveMirror | `go.fd.io/govpp/binapi/span` is not in `vendor/`; VPP version compatibility also uncertain | spec-vpp-5-features | open |
| 2026-04-17 | spec-vpp-4-iface | Wireguard netdev via VPP wireguard plugin | `go.fd.io/govpp/binapi/wireguard` is not in `vendor/`; additionally requires the wireguard VPP plugin at runtime (not a default VPP install) | spec-vpp-5-features | open |
| 2026-04-17 | spec-vpp-4-iface | Iface-component reconciliation (`list interfaces for reconciliation`) runs before vpp component connects; with lazy-channel ifacevpp, `ListInterfaces` returns `VPP connector not available` and the iface config-apply errors at startup | Reconciliation ordering is an iface-component problem (deliverConfigRPC timing vs vpp OnStarted), not an ifacevpp fix. Backend-load itself succeeds -- the failure is post-load. | plan/learned/617-iface-vpp-ready-gate.md | done |
| 2026-04-17 | spec-vpp-4-iface | test/vpp/006-iface-create.ci only covers AC-1 (backend registers and loads) plus AC-13 (populateNameMap runs without hanging). AC-2..AC-4, AC-7..AC-10, AC-14..AC-16 exercised by unit tests with mock GoVPP channel, not by a live stub-driven `.ci`. | vpp_stub.py does not yet handle `create_loopback`, `sw_interface_set_flags`, `sw_interface_add_del_address`, `sw_interface_dump`, `sw_interface_event`; extending the stub is substantial Python work better scoped as its own spec. | spec-vpp-stub-iface-api | open |
| 2026-04-17 | spec-l2tp-7 Phase 8 | Remaining 16 of 17 planned `.ci` tests. Delivered 13 of 16 in spec-l2tp-7b-ci-coverage: show-l2tp-config, show-l2tp-tunnels, show-l2tp-tunnel-detail, show-l2tp-statistics, show-l2tp-sessions, show-l2tp-session-detail, teardown-tunnel, teardown-tunnel-all, teardown-session, teardown-session-all, reload-shared-secret, reload-hello-interval, reload-listener-rejected. Deferred: offline-show-tunnels (needs SSH cred plumbing in test harness), redistribute-inject + redistribute-withdraw (need spec-l2tp-7c RIB inject path). Side deliverable: hub SIGHUP now fans `engine.Reload` to every subsystem so L2TP Subsystem.Reload actually fires. | plan/learned/622-l2tp-7b-ci-coverage.md | done |
| 2026-04-17 | spec-l2tp-7b-ci-coverage | offline-show-tunnels .ci -- `ze l2tp show tunnels` against a live daemon | forwarder at `cmd/ze/l2tp/show.go` dispatches via `sshclient.ExecCommand` which requires the ZEFS DB (SSH creds) resolvable from the subprocess's `ze.config.dir`. Wiring the test harness to share the daemon's ZEFS dir with a shell subprocess and making the creds round-trip work is out-of-scope for a test-only spec. Low value because the Go code is a one-line `forwardToDaemon(strings.Join(...))`. | follow-up ci-harness-ssh-creds spec | open |
| 2026-04-17 | spec-l2tp-7 Phase 6 | `/32` + `/128` redistribution into BGP UPDATEs on session IP-up / withdraw on session-down | spec-l2tp-7 lands source registration + RouteObserver + counters; the advertise path was originally envisaged as a programmatic RIB inject. Design review (2026-04-18) reframed redistribute as filtered EventBus subscription: producers (L2TP, future static/connected/ospf) emit `(<proto>, route-change)` events; a new consumer plugin `bgp-redistribute` filters via `redistribute.Global()` and dispatches `update-route` to BGP peers with attrs synthesised at egress (`origin=incomplete`, empty AS-path, `next-hop=self` resolved per-peer by the existing reactor `resolveNextHop`). Split: spec-bgp-redistribute builds the consumer; spec-l2tp-7c-redistribute adds the L2TP producer + end-to-end tests. Existing intra-BGP `IngressFilter` is unchanged (still an ingress ACL on iBGP/eBGP sources). | spec-bgp-redistribute + spec-l2tp-7c-redistribute | open |
| 2026-04-17 | spec-l2tp-7 Phase 8 | Reload applies to live reactors: the subsystem façade reads `s.params` at lookup time but the reactors each carry their own `ReactorParams`. | Closed by spec-l2tp-7b-ci-coverage: `cmd/ze/hub/main.go` `handleSIGHUPReload` / `doReload` now fan `engine.Reload` out to every registered subsystem on SIGHUP, after refreshing the shared ConfigProvider from the freshly-loaded tree. `test/plugin/reload-hello-interval.ci` + `reload-shared-secret.ci` + `reload-listener-rejected.ci` exercise the path end-to-end. | spec-l2tp-7b-ci-coverage | done |
| 2026-04-17 | plan/learned/618-rib-bestpath-pack.md | AC-1 1M-prefix stress-profile (fringe-node heap 56.5 MB -> under 12 MB) + AC-10 GC-CPU share drop (27% -> under 22%) | `make ze-stress-profile` requires root + network namespaces; not runnable in the implementation session. Go benchmark `BenchmarkBestPathRecordHeapFootprint` (default BART tag) gives 27.53 bytes/entry at 1M as mechanical lower bound, but the authoritative pprof comparison table is the deferred item. | privileged-stress-session | open |
| 2026-04-17 | spec-fw-9-traffic-lifecycle | `ze:backend` annotations on tc-only traffic-control YANG nodes (qdisc `clsact`/`ingress`, tc-specific filter types) | Superseded 2026-04-18 by spec-fw-7-traffic-vpp Decision 2 revision: YANG gate cannot express per-enum-value annotations, so rejection now lives in `traffic.RunVerifier` invoked from OnConfigVerify. No YANG annotation change needed. | user-approved-drop | cancelled |
| 2026-04-17 | spec-fw-9-traffic-lifecycle | `.ci` tests `traffic-vpp-rejects-tc-only.ci` and `traffic-tc-accepts-htb.ci` | Same intent delivered by `test/traffic/011-vpp-reject-hfsc.ci` (added by spec-fw-7-traffic-vpp) which asserts the Verifier's rejection message under the vpp backend. | spec-fw-7-traffic-vpp | done |
| 2026-04-17 | spec-fw-9-traffic-lifecycle | Kernel-state assertion (`tc qdisc show` verifying the configured qdisc type is present on `test/traffic/001-boot-apply.ci` + `002-reload-apply.ci`) | The shared `.ci` runner does not grant CAP_NET_ADMIN, so programming a real HTB qdisc on an interface fails with EPERM. fw-9 covers the reactor wiring via `traffic-control config applied` / `traffic-control config reloaded` log-line assertions; the tc-command assertion belongs in a privileged integration test. | spec-traffic-privileged-ci | open |
| 2026-04-18 | spec-rs-fastpath-1-profile | `test/plugin/bgp-rs-batch-flush.ci` (single-UPDATE forward-latency `.ci` exercising end-to-end) | AC-4 is an internal timing property (onDrained fires for partial batches). `TestBatchForwardSingleFlushOnDrain` already asserts one forward-command per single UPDATE via `updateRouteHook`, and every multi-peer rs-forward `.ci` already exercises the batching path end-to-end. A dedicated `.ci` would duplicate existing coverage. | user-approved-drop | cancelled |
| 2026-04-18 | spec-rs-fastpath-1-profile | Engine-side fix: `CommandRegistry.All()` allocates a new `[]*RegisteredCommand` on every RPC dispatch (`internal/component/plugin/server/command.go:465` + `:479` + `:213`). Found via Phase 1 profile: 126 MB / 4.91 % of allocation pressure, 111 MB flat. | Superseded by rs-fastpath-3 (learned/630): `rs.plugin.ForwardCached` -> `DirectBridge.ForwardCached` -> `handleForwardCachedDirect` -> `s.forwardCached` bypasses the command registry entirely. Verified 2026-04-18: all three `registry.All()` call sites are CLI-input dispatch (`HasCommandPrefix`, `dispatchPlugin`, no-match debug log), none on the forward hot path. | plan/learned/630-rs-fastpath-3-passthrough.md | done |
| 2026-04-18 | spec-rs-fastpath-1-profile | Scaling sweep at 10k/25k/50k/75k (with new `maxBatchSize=500`) | Run at rs-fastpath-0 umbrella close (2026-04-18): 10k=294k rps / 25k=338k / 50k=431k / 75k=395k / 100k=407k rps. Spread -21%/+16% from mean (within AC-3 +/-25%). Evidence in `test/perf/results/ze-*k.json`. | plan/learned/631-rs-fastpath-0-umbrella.md | done |
| 2026-04-18 | spec-fw-7-traffic-vpp | `test/traffic/010-vpp-boot-apply.ci` exercising a real VPP daemon (policer + DSCP filter end-to-end via `ze cli traffic show`) | Requires a VPP infrastructure hook in the shared `.ci` runner (start VPP, provision sockets, verify policer state via `vppctl`). Same infra gap as a future `test/fib-vpp/*.ci`. Wiring under test is exercised by `011-vpp-reject-hfsc.ci` (verify path) and `012-vpp-not-connected.ci` (apply path), so Phase 4 functional proof is complete without the full end-to-end run. | spec-vpp-ci-infrastructure | open |
| 2026-04-18 | spec-fw-7-traffic-vpp | VPP-native metadata-match primitive to honor `filter mark` under the vpp backend | VPP's `ClassifyAddDelSession` matches packet-header bytes, not Linux SKB metadata. A faithful translation requires introducing a VPP-specific classify feature (e.g. match on `vnet_buffer` opaque slot) or reframing `filter mark` semantics to match VPP's capabilities. For now, `Verify` rejects `filter mark` under the vpp backend so operators get an explicit error rather than a silent wiring gap. | spec-fw-7b-vpp-metadata-filter | open |
| 2026-04-18 | spec-fw-7-traffic-vpp | `qdisc prio` translation to VPP QoS egress map | Initial translation (`Outputs[class_index] = class.Priority` on the IP-source row) does not correspond to any prio-qdisc semantic: the row is indexed by input DSCP value, and using class index makes the resulting map arbitrary. A proper mapping needs an explicit operator-facing DSCP-to-class binding, which is a design decision this spec does not make. Verifier rejects `qdisc prio` under vpp until the design lands. `egressMapFromPrioClasses` is retained in `translate.go` as the skeleton for the follow-up. | spec-fw-7b-prio-mapping | open |
| 2026-04-18 | spec-fw-7-traffic-vpp | IPv6 next-header matching for `filter protocol` under the vpp backend | Classify pipeline is fully deferred (see the row below); IPv6 is one part of what that follow-up spec has to address. Retained here as an explicit reminder that IPv4 + IPv6 both need wiring when the classify pipeline lands. | spec-fw-7b-vpp-classify-pipeline | open |
| 2026-04-18 | spec-fw-7-traffic-vpp | End-to-end `filter protocol` support under the vpp backend (classify table creation + attachment to interface + session management) | Pass-3 review of fw-7 found the first implementation programmed classify sessions into a table that was never attached to any interface via `ClassifySetInterfaceIPTable`. The match bytes also indexed at packet offset 9 which is inside the Ethernet header, not the IPv4 Protocol byte at packet offset 23. Feature would have shipped as silent no-op. Per `rules/exact-or-reject.md` the verifier now rejects `filter protocol` under vpp; reintroducing it requires designing the full classify attachment chain and handling the per-interface table lifecycle. | spec-fw-7b-vpp-classify-pipeline | open |
| 2026-04-18 | spec-fw-7-traffic-vpp | End-to-end `filter dscp` support under the vpp backend (ingress QoS record + egress map + mark) | Pass-3 review found the first implementation called `QosEgressMapUpdate` + `QosMarkEnableDisable` without the prerequisite `QosRecordEnableDisable` on ingress. VPP's mark op reads a stored DSCP that was never captured, so every packet would get the output value for input 0 regardless of its actual DSCP. Feature would have shipped as silent no-op. Verifier now rejects `filter dscp` under vpp; reintroducing it requires wiring the full three-step VPP QoS pipeline and deciding on map-ID allocation semantics. | spec-fw-7b-vpp-qos-pipeline | open |
| 2026-04-18 | spec-fw-7-traffic-vpp | Multi-class HTB/TBF shaping under the vpp backend | Pass-5 review found that the first implementation bound every class's policer to VPP's output feature arc via `PolicerOutput`. VPP runs all registered features on every packet in series, so N policers at different rates become `min(rates)` instead of per-class shaping -- silently divergent from netlink's "default class catches unfiltered traffic, siblings idle" behavior. Per-class shaping requires filter-based classification steering packets to distinct policers; that needs the classify pipeline deferred above. For now the verifier rejects qdiscs with 0 or >1 classes; single-class HTB/TBF works as a simple interface rate limit. | spec-fw-7b-vpp-classify-pipeline | open |
| 2026-04-18 | spec-fw-7-traffic-vpp | Cross-run orphan-policer cleanup at trafficvpp backend startup | Pass-6 review noted that a fresh `trafficvpp` backend instance (ze process restart, or `backend tc` -> `backend vpp` switch) starts with an empty `interfaceOutputPolicers` tracker. Policers created by a previous ze run and still present in VPP (crashed ze, switched backend, etc.) are invisible to reconcile; VPP's upsert semantics handle same-name reprogramming, but a stale policer whose name is no longer in the new config would leak. Needs a startup scan: `PolicerDump`, filter by `ze/` prefix, compare to current desired, delete orphans. Not critical (operators can `vppctl show policer` and clean up manually); worth its own small spec so the design (what prefix to match? what if user renames intentionally?) is explicit. | spec-fw-7b-vpp-orphan-scan | open |
| 2026-04-18 | spec-fw-7-traffic-vpp | Mockable-channel integration tests for trafficvpp Apply / reconcileRemovals / undo semantics | Pass-7 review observed the Apply code path has zero automated test coverage (translate_test.go covers pure functions; verify_test.go covers the verifier; register_test.go covers init side-effects). The undo list, the create-vs-update distinction, the orphan-policer PolicerDel path, and the reconcile-across-Apply behavior all live in code without tests. Delivered by spec-fw-7b-backend-hardening as a narrow unexported `vppOps` interface (4 methods: dumpInterfaces, policerAddDel, policerDel, policerOutput), a stateless `govppOps{ch api.Channel}` production adapter, and a scripted `fakeOps` test double. 8 tests in `internal/plugins/traffic/vpp/apply_test.go` cover create, update, undo-on-partial-failure, reconcile-drops, reconcile-orphan-still-dels, and the warn-on-VPP-delete-error branch. See `plan/learned/629-fw-7b-backend-hardening.md`. | plan/learned/629-fw-7b-backend-hardening.md | done |
| 2026-04-18 | spec-fw-7-traffic-vpp | `PolicerOutput(apply=true)` idempotency on an already-bound (policer, interface) pair | Pass-7 review flagged this VPP-semantic question that cannot be answered without a running VPP instance. Current mitigation: `applyInterface` skips `PolicerOutput(apply=true)` for UPDATE operations (name in prior state), calling it only for CREATE. Trade-off: if VPP auto-unbinds a policer between ze Applies (VPP restart, manual intervention) and the operator then re-applies the same config, ze treats the class as UPDATE and never rebinds -- the policer is in VPP but not in the feature arc. Adequate proof requires a scripted VPP scenario or an operator-facing "ze traffic resync" command. fw-7b added the `vppOps` seam (learned/629) but fakeOps cannot reproduce VPP's feature-arc state, so the question stays open pending `spec-vpp-ci-infrastructure` or `spec-fw-7b-vpp-orphan-scan` (which would introduce the resync command). | spec-vpp-ci-infrastructure | open |
| 2026-04-18 | spec-bgp-redistribute | `Evaluator.AcceptFamily(family.Family, ...)` API to avoid the per-batch `Family.String()` allocation on the bgp-redistribute hot path | Cross-cutting change: touches `internal/component/config/redistribute/evaluator.go` and both subscribers (the new egress plugin AND the existing `IngressFilter`). Single string allocation per batch is negligible at steady state; landing the API now enlarges this spec. | spec-redistribute-family-api | open |
| 2026-04-18 | spec-env-cleanup | `.ci` tests for openwait/announce-delay/pid-file/pprof/l2tp-log-level/bridge-ack-{default,disabled,config}/migration-env-{surviving,dropped} | Go unit tests cover the plumbing (`TestApplyEnvConfig*`, `TestPIDFile*`, `TestAck*`, `TestEnvSurvivingKey`, `TestEnvDroppedComment`) and the feature code reads the env vars directly; the `.ci` integration runs would add ~12 new test files that exercise the same path as the unit tests via the functional runner. Deferring to avoid blocking the compat-surface deletion on test-runner plumbing for each new knob. | spec-env-cleanup-ci-coverage | open |
| 2026-04-18 | spec-op-1-easy-wins | `show firewall group <name>` command | Ze's firewall config model has no named-set primitive (no `address-group` / `network-group` / `port-group` YANG container, no parser). Adding the command alone would have nothing to read. The prerequisite is a YANG + parser extension adding the three group types plus their inclusion in `MatchSourceAddress` / `MatchDestinationAddress` / `MatchSourcePort` / `MatchDestinationPort` / `MatchInputInterface` / `MatchOutputInterface` as group-name alternatives to literal values. | spec-firewall-groups | open |
| 2026-04-18 | spec-op-1-easy-wins | `.ci` functional tests for all 10 shipped Tier-1 commands (system memory/cpu/date, interface type/errors, bgp summary family, ping/traceroute/generate-wireguard-keypair) | Unit tests cover the handlers in-process; wiring is verified by handler registration + YANG containers. End-to-end `.ci` runs that drive the commands through `ze cli -c` or the offline subcommand entry would add 10 new test files exercising the same code paths. Deferred to avoid gating the implementation batch on test-runner plumbing for each command. | spec-op-1-ci-coverage | open |
| 2026-04-18 | spec-host-0-inventory | Repo-wide stdio-error-check hardening for CLI render helpers (every `fmt.Printf`/`fmt.Println` to `os.Stdout` in render paths across `internal/component/cli/`, `cmd/show/`, `cmd/ze/host/`, etc.) | `/ze-review` pass 2 flagged that host-0's new text renderers in `cmd/ze/host/host.go` ignore stdout write errors. The pattern is project-wide (grep `internal/component/cli/editor.go`, every `cmd/show/` handler) and changing host alone would create inconsistency. A single follow-up spec introducing a shared `renderWriter` (or equivalent bufio-backed error-capturing wrapper) and migrating all call sites at once keeps style uniform. | spec-cli-stdio-hardening | open |
| 2026-04-18 | spec-host-0-inventory | `show system uptime` hardware enrichment (AC-14 in host-0) | The `ze-show:system-uptime` RPC handler does not yet exist in `internal/component/cmd/show/system.go`; it is in op-1's pending task list. Once op-1 lands the handler, adding a `hardware` nested object driven by `host.DetectHost()` is a three-line copy of the existing cpu/memory enrichment. Tracked so the follow-up is not lost. | spec-op-1-easy-wins | open |
| 2026-04-19 | spec-bgp-redistribute | `Evaluator.AcceptFamily(family.Family, ...)` typed-family API to remove the per-batch `Family.String()` allocation in the bgp-redistribute-egress consumer hot path | The current `Evaluator.Accept(route, importingProtocol)` API takes `RedistRoute.Family` as a string. Adding a typed counterpart touches the existing intra-BGP `IngressFilter` caller and the redistribute config types; out of scope for the egress spec. Negligible at steady state, surfaces under sustained MB/s burst rates. | spec-redistribute-acceptfamily | open |
| 2026-04-19 | spec-bgp-redistribute | Central per-protocol RIB store (engine-owned) so consumers (bgp-redistribute, FIB plugins, looking glass, cross-protocol show) read by reference instead of subscribing to deltas | The shipped event-bus model is correct against the spec; the user noted post-implementation that the original mental model was a central store (sysrib-style). Spec rejected `*RouteSource` pointers across plugin boundaries but never separately considered engine-owned shared state. Move when a second consumer beyond bgp-redistribute makes the lack-of-source-of-truth painful. See learned/634 "Open Question" section for the full comparison. | spec-protorib-0 | open |
| 2026-04-19 | spec-fw-10-linux-gaps | Kernel-backed `.ci` tests for every AC that needs real nft state | Initially deferred because the `ze-test` sandbox does not grant CAP_NET_ADMIN. Reversed in the same session on user directive: tests MUST exist even if they require privileged infrastructure to pass. Delivered as `test/firewall/001..011.ci`, each driving ze via `ze -`, snapshotting kernel state via `nft list table` / `nft list set`, and asserting on the rendered rules / set members / timeouts / limit types. Tests that run against an unprivileged sandbox fail loudly rather than silently skip. | test/firewall/001..011.ci | done |
| 2026-04-19 | spec-mcp-1-streamable-http AC-3 | Exercise the POST-to-SSE upgrade path with a real method that needs server-initiated frames | No Phase 1 method requires it. The transport scaffolding is in place (`handlePOST` accepts stream upgrade), but until a consumer emits intermediate notifications mid-tool-call, there is nothing to stream. First such consumer is `elicitation/create` on a tool call. | spec-mcp-3-elicitation | open |
| 2026-04-19 | spec-mcp-1-streamable-http | Standalone `.ci` test for GET /mcp SSE stream | Unit test `TestStreamableGETOpensSSEStream` covers server-side; no Phase 1 production consumer writes onto the stream from a user-triggered flow. Once elicitation or task-status notifications exist, the `.ci` test becomes meaningful. | spec-mcp-3-elicitation | open |
| 2026-04-19 | spec-mcp-1-streamable-http | Delete legacy `internal/component/mcp/handler.go` (no-layering) | `tools_test.go` contains ~26 HTTP tests using the old `Handler()` factory. Rewriting them for the Streamable transport requires session plumbing plus a broader `tools.go` refactor that is already scheduled for Phase 4 (task-augmented `tools/call`). Production already uses `NewStreamable`. | spec-mcp-4-tasks | open |
| 2026-04-19 | spec-mcp-1-streamable-http | Session registry GC goroutine tied to server Shutdown | `sessionRegistry.Close` is idempotent but not called by `cmd/ze/hub/mcp.go` on daemon shutdown; the goroutine outlives `http.Server.Shutdown` until process exit. Benign in practice (session state is in-RAM, process-bounded) but worth tying into the shutdown path when Phase 2 adds a wrapping server struct. | spec-mcp-2-remote-oauth | open |
| 2026-04-19 | design-rib-rs-fastpath | BGP producer populates `Change.Forward` on `InsertForward`: thread the reactor forward-pool handle through StructuredEvent delivery into `rib_bestchange.checkBestPathChange` | Wired via `ribForwardHandle` (`internal/component/bgp/plugins/rib/forward_handle.go`): sync.Once copy-on-AddRef over `RawMessage.RawBytes`, exposed via the `locrib.ForwardBytes` optional interface. Created once per UPDATE in `handleReceivedStructured`, passed through `checkBestPathChange` to `locrib.InsertForward`. Subscriber-free UPDATEs pay one handle struct alloc and zero byte copies; retaining subscribers pay one bounded copy. | internal/component/bgp/plugins/rib/forward_handle.go | done |
| 2026-04-19 | design-rib-rs-fastpath | First real subscriber that consumes `Change.Forward` in production | Producer side is now fully wired; the infrastructure (handle, refcount, Bytes accessor, ForwardBytes optional interface) is ready. The first consumer will be a `locrib.OnChange` subscriber that calls `c.Forward.AddRef()` inside the handler and reads `c.Forward.(locrib.ForwardBytes).Bytes()` from its worker (e.g. sysrib byte-mirror, route-archive plugin, RR cluster-list extractor). NOT for forwarders -- RS / RR forward via the receive-path trigger and `reactor_api_forward.go` per-peer egress, not via locrib events. | design-rib-rs-fastpath-state-tracker (future) | open |
| 2026-04-20 | design-rib-rs-fastpath | "Retire the receive-path forward trigger" | Cancelled. The receive-path is the entry point of the inbound filter pipeline (`reactor_notify.go:302-353` in-process ingress filters with `IngressFilterFunc.modifiedPayload` copy-on-modify; `reactor_notify.go:357+` external-plugin import policy filter chain via `PolicyFilterChain`). Filtering and modify happen there, before caching, before any consumer sees the bytes. Two triggers (StructuredEvent for forwarders, `locrib.OnChange` for state trackers) coexist by design -- forwarder needs (per received UPDATE) and state-tracker needs (per best-change) do not collapse. See `docs/architecture/core-design.md` "Two-Trigger Model" and `plan/design-rib-rs-fastpath.md`. | cancelled | cancelled |
| 2026-04-20 | design-rib-rs-fastpath | "Per-peer best-path Change events" prerequisite for retiring the trigger | Cancelled. False premise: ze's RS is forward-all (`rs/server_forward.go:34-56`), it does not compute per-peer best-paths. Per-peer egress (filters, RFC 4456 RR injection, next-hop, AS-override, EBGP prepend) lives in `reactor_api_forward.go:380-540` and runs at forward time. Locrib correctly tracks one global best per (family, prefix); turning it into a per-received-path event source would mean storing every received path per peer, which is not what a Loc-RIB is. | cancelled | cancelled |
