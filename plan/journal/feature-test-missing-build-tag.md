| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-17 | storage-1-backend-parity (found in the unit stage of the verify on `381b43c652`) | `internal/component/mtu/cmd/overhead.go`, always-on, imports `internal/component/ike/crypto`, which `feature-gates.txt` places under `ze_ike` | `TestBuildTag_Gate12_AbsentBinaryDropsSymbols` (`cmd/ze/hub`) fails: the bare `ze_core` binary retains `ike/crypto.(*EncryptionID).IsAEAD`. `go list -deps -tags ze_core ./cmd/ze` names `mtu/cmd` as the one importer, through `overhead.go` and the `ikeprobe` leaf. `665a070dda` (2026-09-16, `show mtu` sizes each IPsec tunnel) added both. Same shape as the 2026-09-09 row: a feature import with no tag guard in code every build carries | not fixed: not this spec's producer, and the fix is a design choice. Either `overhead.go` and the IPsec sizing move behind `//go:build ze_ike` with the tunnel overhead registered from the ike side, or the transform-id table `overhead.go` reads moves to a leaf that is not gated |
| 2026-09-13 | le-publishes-its-command-surface (found clearing the reds for the first green push) | ten `internal/component/web/testdata/handler/*.txt` golden captures, recaptured by `fda38d5415` | The first occurrence of this class where the wrong tag set is FROZEN INTO A FIXTURE, so it does not fail a run: it passes one, against a recording of a half-built product. The capture was taken from a binary compiled with the feature gates off, and `get-show-bgp-finder.txt` fell from 14,814 bytes to 10,322. Measured here by node set rather than by size: the committed golden carries 17 top-level `show` paths where the pre-regression capture at `c3b6433ba8` carries 36, and the 19 missing are exactly the gated features (`anomaly bfd class-of-service control-plane-protection ddos exabgp flow-export isis l2tp ldp mrt ospf policy pppoe rsvp-te service telemetry vpn vpp`). So `TestWebHandlerGoldenOutput` was asserting that the web config finder shows an operator 17 of 36 feature trees, and was green | fixed: regenerated with `Toolchain.TestTags`, core plus every gate. The new capture's node set is identical to `c3b6433ba8`'s, plus the label and aria work of the two commits since. What this occurrence adds to the eight below is that a GOLDEN outlives the run that produced it: every other row is a run that went red or silently narrow once, where this one republishes the wrong answer on every later run and carries the authority of a passing test. A capture-time tag set is a build input, and nothing beside the fixture records which one made it |
| 2026-09-13 | le-publishes-its-command-surface (found while sweeping for the first green push) | the sweep itself: `./le job run label le-full-sweep command go test -tags ze_le ./internal/le/...` | The eighth occurrence, and the row dated 2026-08-29 in this same file had already written the practice down: "a red seen outside the registered test action is not evidence until it is reproduced INSIDE it. Reproduce first, diagnose second." `-tags ze_le` sets ONE tag, not the 36 in `feature-gates.txt`, so the sweep answered 12 red of 112 packages and FOUR were the missing tags alone. Re-run with `ze_core` plus the 36: `config/claims`, `command/list` and `wikicatalog` pass, and docvalid's `TestEveryYANGCommandHasAHandler` with them. The cost was nearly real work: `config claims` reported that operator config written to `environment`, `pki` and `redistribute` "is stored and delivered to nobody", which reads as a serious product defect and is an artifact of a registry that never populated. Its own non-vacuity floors caught it and said so in plain words, "the plugin registry is not populated, so every root would look unclaimed" | not fixed as code, because there is no defect: the gate and its floors both worked. What failed is the session's method, twice over. `ai/rules/commands.md` says to use the registered `./le` action so feature tags are preserved, and the 2026-09-02 row above records that `job run ... command go test` passes its argv through unchanged and therefore adds none. So the sanctioned-looking recipe is the banned one wearing an admission wrapper, which is exactly what this session reached for. The durable repair is the one that row already names and nobody has built: give the `go test` child the tag environment `internal/le/gotoolchain` computes, so the wrapper cannot silently drop them |
| 2026-09-05 | fixit-send-names-its-destination (found while vetting the tree) | `codes` declared twice in package `doctor`: `internal/component/doctor/checks_redistribute_test.go:29` and `checks_mpls_linux_test.go:188` | The doctor package's tests do not build on Linux. `checks_mpls_linux_test.go` carries `//go:build linux` and `checks_redistribute_test.go` carries none, so on a Linux host both are in the package and `go vet -tags ze_core,ze_test,<feature tags> ./...` answers `codes redeclared in this block`. A package whose test binary does not compile reports no verdict at all, so every check over `internal/component/doctor` is silently absent rather than failing. Landed today in 3c71ff73e / f93b11649, which is another session's work | unfixed, and deliberately: the tree is another session's and the fix is not one line. The helper belongs in an untagged test file that both callers share, because deleting either copy leaves one platform's tests without it |
| 2026-09-03 | none (took it as the next piece of work after the hook binding) | `TestRFC9552BGPLSAttributeWellFormedIsKept` (`internal/component/bgp/reactor/rfc9552_test.go`), an RFC-TAGGED test on the public conformance ledger | Same class as the row below, one step further out: the missing thing was not a build tag but a PLUGIN IMPORT, and the test that degraded was reporting on the RFC ledger. The test drives the receive path and asserts a well-formed BGP-LS Attribute survives it. `publishBase` (`session_read.go`) strips an optional non-transitive attribute ze does not recognize, which RFC 4271 Section 5 requires, and code 29 is recognized only because the ls plugin calls `attribute.RegisterName(29, "BGP_LS")` in its init (`internal/component/bgp/plugins/nlri/ls/register.go`). A shipped ze links that plugin through the generated composition root; the reactor test binary linked nothing, so in THAT binary ze correctly held no meaning for the attribute and correctly dropped it. The test read the correct behaviour as a conformance failure and stayed red for five days carrying two `RFC requirement:` tags, so `./le rfc check` published a gap the product does not have. Its sibling tests in the same file passed throughout, because they assert the tombstone code 252, which core registers | FIXED: the test file blank-imports the plugin, with a comment saying why, and the fix was proven by swapping `RegisterName` for `RegisterNameOnly` under a Go overlay, which reddens it again. The design is NOT the defect and was deliberately left alone: without the plugin ze holds no meaning for the TLVs, so RFC 4271 Section 5 is right to drop the attribute, and moving recognition into core beside the RFC 9552 Section 8.2.2 validator would make ze claim a meaning it does not hold. The transferable point: a red that names a REQUIREMENT is read as a product verdict, so a test whose subject reaches a REGISTRY must link what fills it, and an RFC-tagged one owes that more than any other test, because its failure is published rather than merely seen |
| 2026-09-02 | - | `./le job run label unit-pkg quiet command go test <package>`, the focused-test recipe `ai/rules/testing.md` prescribes | The recipe passes everything after `command` to the child unchanged, so it adds NO build tags, and the rule that names it says a bare `go test` must not be used because it omits them. Both are true at once: the sanctioned route is the banned one with an admission wrapper. Measured on `./internal/component/plugin/server/`, whose tests no `./le test-unit` group covers: the recipe answered 40-plus failures, `TestBuiltinPathsResolveToTheirOwnHandler` among them saying `[]string(nil) does not contain "show bgp"`, which is an empty registry rather than a defect. The same package with `-tags "ze_core <every tag in feature-gates.txt>"` answered `ok ... 16.531s` | not fixed. Two candidates: `job run` gives a `go test` child the toolchain environment `internal/le/gotoolchain` already computes, or the rule stops naming a recipe that cannot carry the tags and names a package-scoped native action instead. A third gap is next to it: `internal/le/testunit/groups.go` has no group for `./internal/component/plugin/...`, so that package has no focused action at all and reaches a verdict only inside the whole verify gate |
| 2026-08-29 | - | a guard test run without the feature tags its registry needs | A test whose subject is a REGISTRY degrades rather than fails when the tag that populates it is absent: the registry is empty, every lookup misses, and the counters the test compares invert. The failure message then describes the product accurately and blames the wrong thing, so it reads as a regression in a surface that never changed. An investigation can eliminate cause after cause and never reach the tag, because the tag is not a cause inside the code | closed as not a defect: the test passes through the registered action that supplies the tags. The general practice, twice-learned: a red seen outside the registered test action is not evidence until it is reproduced INSIDE it. Reproduce first, diagnose second. A guard over a registry SHOULD also state its emptiness as its own assertion, so an absent tag reports the tag rather than the symptom |
| 2026-08-24 | - | the retired `ze-qemu-l2tp-ppp-test` (current: `./le deployment l2tp-ppp-test`), the mandated L2TP QEMU evidence | The test builds the daemon it puts in the VM with `go build -tags "ze_core,ze_distro"` (the retired `scripts/evidence/effective-l2tp-ppp.py` (current producer: `internal/le/deployment/l2tp.go`)), then feeds it a config whose first line is `l2tp {`. That keyword is registered by `cmd/ze/hub/register_l2tp.go`, which carries `//go:build ze_l2tp`, so the binary cannot know it. The daemon exits at the `load config` stage with `unknown top-level keyword: l2tp` before any kernel feature is reached, and the target reports `FAIL: ze L2TP listener did not start`. Deterministic, not flaky. The two commits that made it so are both in history: `50d6d1bea` (2026-06-07) set that tag list when it was correct, and `b7bfbac23` (2026-07-24) then moved L2TP registration behind a NEW `ze_l2tp` tag without updating the evidence script. No make target passes `ze_l2tp` either: grep over the retired `mk/` (current producer: `internal/le/`) and the retired `Makefile` (current producers: `internal/le/` native action tables) returns nothing. So the test has been unable to pass for a month. Found while validating a 7.1.4 to 7.2 kernel bump, which `ai/rules/platform-linux.md` requires to BOOT, and which this test is the named proof of | not fixed. The kernel bump it was found by does not depend on it: the failure is upstream of every kernel interaction, and the one kernel-side reading the run did produce is positive, `/proc/net/pppol2tp` reporting `PPPoL2TP driver info, V2.0` on both containers under 7.2. What the row is for is the month, not the tag. `ai/rules/platform-linux.md` names this test as the proof that L2TP works against real kernel modules, and the retired `mk/build-appliance.mk` (current producer: `internal/appliance/cmd_build.go`) names it as a consumer of every staged runtime kernel, so every kernel change since 2026-07-24, including the 7.1.4 pin itself, was accepted with its mandated evidence silently unavailable. A test nobody runs reports nothing, which is why a month passed. `ze-qemu-pppoe-accel-test` is built the same way from the same `./le build-artifacts host` dependency and may carry the same hole; NOT checked |
| 2026-08-16 | weakened-followups | reload unit test | authentication fixture ran in the bare `ze_core` pass and failed because its YANG modules were compiled out | moved the unchanged test to a file guarded by its `ze_bgp` and `ze_ssh` feature tags |
| 2026-09-09 | subscriber-reader-loops-retry-a-failing-socket-without-backoff (found while running `./le tier check` to confirm the new pacer leaf's placement) | `internal/le/interoplab/pppoe/check_ipv6cp.go`, `check_padr_replay.go`, `check_service_name.go` | Each imports `internal/component/l2tp/pppoe`, a compile-out-able feature, with no `//go:build ze_l2tp` guard, so `./le tier check` fails: "disableable feature(s) imported by always-on code -- breaks compile-out". None of the three files were touched by this session; `git log -1` puts them in an earlier commit | FIXED 2026-09-12, in the standalone commit named at the end of this row. The three files carry `//go:build ze_l2tp`, and the four scenarios they implement move out of the always-on `checkers()` map into `checkers_l2tp.go`, which fills a `wireCheckers` map from its `init()` behind the same tag. The gate's other remedy, a construction registry, was refused: `checkers()` IS the table, and a registry beside it would be a second declaration of one map. The wider remedy of adding `internal/le/` to `DisableableNonProdPrefixes` (`internal/le/tier/gates.go`) was also refused, though the tree meets that list's own criterion of not being the production daemon: it would lift the guard over every `internal/le` package at once, and `internal/le/docvalid/contract_bgp.go` and `internal/le/wikicatalog/catalog_*.go` already answer this case with the tag. Nothing stops running: `le` is built from `feature-gates.txt` in the `le` script's `build_le`, and every CI build of the personality is `-tags "ze_le $FEATURES"` with `ze_l2tp` in `$FEATURES`, so every build that runs this lab carries the tag. `interoplab.Discover` also fails closed on a scenario directory with no checker ("scenario <name> has no Go checker"), so a tagless build would error rather than skip |
