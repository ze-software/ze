# A status field is a constant, so it reports nothing

A field in a status payload is written as a literal rather than read from the
state it names. It carries no information, and it states the opposite of what
the daemon did whenever the state it names is conditional. The tell is a status
key whose writer names a runtime property and whose value has no producer.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-11 | path-mtu-diagnostic | `show vpn ipsec sa` | `PeerInfo.ESPEncryption` and `ESPIntegrity` are read from `ps.espGroup.Proposals[0]` (`internal/component/ike/engine/reconcile.go`), and `ps.espGroup` is the CONFIGURED group, assigned once in `startPeerSession` from `newCfg.ESPGroups[peer.ESPGroup]`. Negotiation narrows a different value: `selectResponderESP` (`responder.go`) and the initiator path in `fsm.go` both overwrite `sa.ESPGroup.Proposals` with the single accepted proposal. So a peer that negotiates the second configured proposal is reported as running the first. The field names the negotiated transform and reads configuration, which is undetectable whenever a group declares one proposal (the common case) and wrong whenever it declares two | fixed 2026-09-16 by spec-path-mtu-diagnostic phase 2 (AC-15): `Info` now reads `child.ESPGroup.Proposals[0]`, the group every negotiation and rekey path narrows to the accepted proposal. `TestPeerInfoReportsNegotiatedTransform` (`reconcile_test.go`) went red on the old read (`esp encryption = "aes256", want the negotiated "aes128gcm"`), and the `mtu-negotiated-transform` interop scenario asserts the same against strongSwan accepting the second of two proposals |
| 2026-08-22 | - | bgp rpki commands | `summaryCommand` writes `"validation-enabled":true` and `statusCommand` writes `"running":true`, both as literals (`internal/component/bgp/plugins/rpki/rpki.go`). Validation is conditional: `startSessions` clears `active` when the config names no cache server, and the `OnAllPluginsReady` handler then skips the `request bgp adj-rib-in enable-validation` dispatch and logs that it skipped it. So a daemon with the RPKI plugin loaded and no cache server configured answers `show bgp rpki summary` with `validation-enabled true` while no route is validated. `sessions-total 0` is the only field that hints at it, and it answers a different question | not fixed. Reading the fields from `active` changes what `show bgp rpki summary` answers, which AC-13 of `plan/spec-plugin-registers-pipe-operations.md` holds unchanged for phase 5. Found while giving the bare `show bgp rpki` command the aggregate half that `show bgp rpki summary` reports, which made the two answers share one writer and put both literals in one place FIXED 2026-09-20: validationEnabled derives the field from syncedSessions, so it answers true only while a cache has delivered a set, and `running` reads the same active flag the per-prefix work reads. A daemon with the plugin loaded and no cache reachable now reports both honestly, and `synced` false beside `running` true is the state an operator could not otherwise see. |
| 2026-09-21 | - (RFC 4090 triage) | `internal/plugins/rsvpte/frr.go::rroProtectionFlags`, read by `engine.go::handleResvTransit` | The PLR's RRO subobject reports "local protection available" (0x01) from `lsp.Bypass != nil`, which `handlePathTransit` sets from CONFIG (a bypass whose merge point matches) and never from the bypass LSP's state. A bypass still in path-sent, or one that failed to come up, is reported available. RFC 4090 Section 6: "If no established one-to-one backup LSP or bypass tunnel exists, or if the one-to-one LSP and the bypass tunnel is in 'DOWN' state, the PLR MUST clear the 'local protection available' flag" (RFC4090-6-3, 6-4). The same function also set "bandwidth protection" (0x04) whenever the head-end asked, although `setupBypass` reserves no bandwidth; that half is fixed in the same triage | not fixed: the fix is small (read the bypass LSP state outside `lsp.mu` in `handleResvTransit` and pass it in) but `TestPLRArmsBypass` (tags RFC4090-3.2-2, 4.4-1) asserts the available bit with the bypass never brought up, so the fix reddens a tagged test and needs the owner's `./le rfc approve`. Put to the owner in the rfc4090 triage report |
