# lint contract not applied

A test or helper changes in a way that violates an existing lint contract. The
contract is correct, and the failure is fixed at the source rather than bypassed.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-26 | - | `make ze-lint`, whose population is `./...` (`ZE_LINT_PKGS` in the Makefile) | `golangci-lint run -j 4 ./scripts/status/...` is RED at HEAD on five findings, so the `ze-lint` stage of `make ze-precommit-verify` fails for every session in this checkout. Two are `goconst` (`"group"` at nine sites and `"subcheck"` at three, in `classifiedGroups` and its classifiers, `scripts/status/verify_run.go`), two are `modernize` `errorsastype` (`execStage` in the same file, and `verifyStatusCheck` in `verify_run_test.go`), and one is `noctx` (`leGateScripts`, `verify_run_test.go`, which reaches `le gates --json` with a bare `exec.Command`). The last was introduced the same day by `da417ecc4`, which routed the functional-suite list through `le` and added the call. Found while porting this directory, not by any gate report: the file is `package main` with no build tag, so it has been in the linter's population all along and nothing said so | fixed on the spot, all five, because they stop a gate. `parallelGroup` and `kindSubcheck` are named beside `wholeStage` and replace eleven literals; the two `errors.As` sites become `errors.AsType[*exec.ExitError]`; `leGateScripts` takes `t.Context()`. `scripts/status` lints clean afterwards and its 62 tests stay green. The trap to carry forward: a directory whose files read like scripts is not outside the linter, and a red that nobody's `-changed` run selects can sit at HEAD indefinitely, because the changed mode only lints what a commit touched |
| 2026-08-19 | spec-verify-scope-2-change-set-selector | ci-dispatch call-site gate | `TestNoDeadDispatchCommands` (`scripts/checks/ci_dispatch_commands_test.go`) is red at HEAD in a clean `internal/component/cli/` tree: `ci-dispatch-check: FAIL (0 dead, 2 unverifiable)`. `80c9fbcf7` routed two emitters through a helper, so `cliClient.Execute` and `cliClient.executeWithTranscript` (`internal/component/cli/client/main.go`) now call `SendCommand(commandWithFormat(command, format))`, and the checker cannot resolve that to a registered command. The contract it prints is the fix: make the command path literal, or mark the line `ze-dispatch-check: dynamic -- <reason>` when it is genuinely uncheckable | not fixed. It blocks nothing in this spec, whose whole surface is `scripts/checks/verify_scope_selector.go` and one Makefile target, and both emitters belong to a subsystem another session is editing now. Whoever fixes it decides one thing: whether a command `commandWithFormat` builds is uncheckable by nature, or should reach `SendCommand` literally |
| 2026-08-17 | spec-rfc4271-med-ibgp-readvertisement | scripts/checks staticcheck feature matrix tests | `TestStaticcheckFeatureMatrixCompletedVerdictWinsExpiredContext` called `t.Parallel` inside subtests but not on the parent test, so `make ze-lint-changed` failed with `tparallel` before the MED commit could be prepared | added top-level `t.Parallel` to the parent test |
| 2026-08-17 | spec-fixit-zefs-diff-structural-ops | bgp message RFC 7606 receive test | `internal/component/bgp/message/rfc7606_section51_receive_test.go` was committed un-gofmt'd at line 26, so `make ze-lint-changed` failed with `gofmt` for every session whose changed set reached the package. The file is clean in the working tree, so the red comes from HEAD, not from another session | ran `gofmt -w` on the file |
| 2026-08-17 | none (owner directive: a Go commit owes a full verify) | verify runner tests | `scripts/status/verify_run_test.go` is un-gofmt'd in HEAD at line 82, so `make ze-lint-changed` failed with `gofmt` as soon as any edit put the file in the changed set. Third row of this shape today: an un-gofmt'd file reaches HEAD and lies dormant until somebody else's change pulls its package into the changed set | ran `gofmt -w` on the file |
| 2026-08-19 | fixit-iface-selector-ignored-by-apply | ssh session command dispatch | `internal/component/ssh/ssh.go` is un-gofmt'd in HEAD at line 835, a `//nolint` comment left over-aligned after the sibling line above it changed length. It arrived 40 minutes earlier in `80c9fbcf7`, and `make ze-lint-changed` then failed with `gofmt` for a session whose own changed set is entirely under `internal/component/iface`. Fourth row of this shape: an un-gofmt'd file reaches HEAD and lies dormant until somebody else's change pulls its package into the changed set | ran `gofmt -w` on the file |
| 2026-08-19 | none (nft address-match family guard) | scripts/evidence l2tp diag tools | `make ze-lint-changed` widened to `./...` because `tmp/ze-verify.status` records no green commit, and reported 8 findings at HEAD: errcheck on `unix.SetsockoptInt` and on `fmt.Sscanf`, gosec G103 on four `unsafe.Pointer` conversions, gosec G204 and noctx on `exec.Command`, all in `scripts/evidence/l2tp-pppox-diag/main.go` and `scripts/evidence/l2tp-tunnel-diag/main.go`. Both files are clean in the working tree, so the red is HEAD's. Fifth row of this shape, and the first where the widened scope is what surfaced it | not fixed. Outside the problem in hand and blocking nothing in it |
