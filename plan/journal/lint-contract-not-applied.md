# lint contract not applied

A test or helper changes in a way that violates an existing lint contract. The
contract is correct, and the failure is fixed at the source rather than bypassed.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `golangci-lint run ./internal/component/l2tp/plugins/authradius/...`, `setupCapturingAuth` (`internal/component/l2tp/plugins/authradius/rfc2865_nas_obligations_test.go`) | one `unparam` finding, "attrs always receives nil", red at HEAD and the only finding in the package: all six call sites pass `nil`, and `git status` reports the file unmodified in a checkout where five sessions are editing, so no working-tree change produces it. Same shape as the 2026-08-30 `writeResetQuery` row of this class. Found by the scoped lint run this session owed for its own change to `acct.go` and `config.go` in the same package | not fixed, and unusually it is not the finding that blocks the fix. Dropping the parameter and its six `nil` arguments is a one-line change, but the file carries `RFC requirement:` tags, so the write hook (`weakened.Proposed`, `internal/le/hookruntime/writeedit.go`) refuses every edit to it until `test/rfc-changed.md` names the test, and that row records an OWNER approval an author may not write for themselves. So a tagged test file cannot have its lint debt cleared by the session that meets it. Whoever asks Thomas about this file carries the parameter removal with them |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `./le verify lint run scope ./internal/component/radius/...`, `internal/component/radius/rfc2865_response_auth_test.go:198` | one `gofmt` finding, red at HEAD and the only one in the two packages: the `//nolint:gosec` comment on `h := md5.New()` is not aligned with the four `h.Write` comments below it, so gofmt's comment-column rule wants two more spaces. `git status` reports the file unmodified, so no working-tree edit produces it. Unlike the 2026-08-29 and 2026-08-31 rows of this class, `gofmt -l` DOES report this one, because it is column alignment rather than import grouping. Found by the scoped lint run this session owed for its own change, which the red would otherwise have failed | fixed on the spot with `gofmt -w`, because it stops the gate over the package this session edited: the run goes 1 issue to 0 in all three flavours. The trap to carry forward: a `//nolint` comment added to the first line of a commented block re-columns the whole block, and a session that writes one without running gofmt leaves a red every later session in the package inherits |
| 2026-08-31 | spec-fixit-rfc-drain-quota-never-armed | `./le verify lint run scope ./internal/le/rfc/...`, `carriers.go` | one `goimports` finding, red at HEAD and the only one in the package: `internal/le/leroot` sits in the standard-library group, and `gofmt` does not check grouping under the local prefix, so every `gofmt -l` over the package reports it clean. The 2026-08-29 row of this class already named this file among the four that persisted across two measurements | not fixed. Moving the import to the local group takes the scope to 0 issues in both flavours, and it also changes the digest `TestNativeImplementationFixture` pins over every non-test file in the package, so the fix owes a re-seal of that constant. The constant is red right now from a second session's in-flight `check_status.go`, so re-sealing it here would pin a tree neither session is committing. Whoever re-seals it next carries this one-line move with them |
| 2026-08-30 | spec-fixit-peer-pending-sync-settles-too-early | `./le verify lint run`, `capability` flavour, `writeResetQuery` (`internal/component/bgp/plugins/rpki/rtr_pdu.go`) | one `unparam` finding, "off always receives 0", red at HEAD: `git status` reports the file unmodified in a checkout where six sessions are editing, so no working-tree change produces it. It fires only in the `capability` flavour (tags `debug,race,live,stress,maprib,fleetperf,zetest,gokrazy,ze_test,ze_perf,ze_analyze,ze_chaos,ze_le,integration,ze_docvalid_fixture`), which is why a session linting the default flavour alone never sees it. Beside it the same run reported four typecheck errors in `internal/le/site/producer.go`, which ARE a live edit and will clear themselves | not fixed. Outside the problem in hand and blocking nothing in it. The `capability` flavour is the only failing one, and the whole of `internal/component/bgp/` is otherwise clean in every flavour |
| 2026-08-29 | spec-site-renderers-in-go (phase 1) | `./le verify lint run`, host flavour | 16 findings, none in the changed set: 10 `goimports` "File is not properly formatted", 5 `misspell` (`behaviour`, `defence`, `summarised`), 1 `unused` (`uint16ToBytes`, `internal/component/bgp/plugins/nlri/ls/types.go:537`). Every `goimports` finding points at a `github.com/ze-software/ze/internal/le/...` import line and `gofmt -l` lists none of the files, so it is import GROUPING under the local prefix, which gofmt does not check. Measured TWICE, 30 minutes apart: the count held at 10 both times but the FILE SET moved. Four persisted (`le/plugin/boundary/pluginboundary.go` and its test, `le/repository/generate.go`, `le/rfc/carriers.go`); six dropped (`le/doc/check/actions.go`, `doc/check/links.go`, `le/group_test.go`, `le/hookruntime/agent.go`, `le/verify/status/answer.go`, `le/verify/summary/answer.go`) and six arrived, all under `internal/le/verify/`. So this is not one static red: a persistent core sits at HEAD while a live edit front churns around it, and a reader chasing the file list of any single run will find half of it already clean | not fixed. Outside the problem in hand and blocking nothing in it: `./le site check` is not a verify stage (`internal/le/verify/engine/stages.go` registers `site facts check`), so phase 1 does not depend on this gate |
| 2026-08-28 | - (goconst fixed-point pass over `internal/le/interoplab/`) | `./le verify lint run`, `observeRPKI` (`internal/le/interoplab/bgp/helper.go`) | two `unconvert` findings sat at HEAD, invisible: `sdk.(*Plugin).DispatchCommand` (`pkg/plugin/sdk/sdk_engine.go:212`) already returns `json.RawMessage`, so both `json.RawMessage(...)` conversions on the line are no-ops. golangci-lint keeps one issue per line by default (`issues.uniq-by-line`), and a `goconst` finding for `"rpki"` held that line, so removing the literal is what let the older red through | fixed on the spot, because it stops a gate: both conversions dropped, the map now carries `statusData` and `routesData` unchanged. The trap to carry forward: a lint red can be hidden by a second finding on the same line, so clearing one linter can turn a package red on another |
| 2026-08-26 | - | `./le verify lint run`, whose population is `./...` (`ZE_LINT_PKGS` in the retired Makefile (current producers: `internal/le/` native action tables)) | `golangci-lint run -j 4 ./scripts/status/...` is RED at HEAD on five findings, so the `./le verify lint run` stage of `./le verify current mode full` fails for every session in this checkout. Two are `goconst` (`"group"` at nine sites and `"subcheck"` at three, in `classifiedGroups` and its classifiers, the retired `scripts/status/verify_run.go` (current producer: `internal/le/verify/engine/run.go`)), two are `modernize` `errorsastype` (`execStage` in the same file, and `verifyStatusCheck` in `verify_run_test.go`), and one is `noctx` (`leGateScripts`, `verify_run_test.go`, which reaches `le gates --json` with a bare `exec.Command`). The last was introduced the same day by `da417ecc4`, which routed the functional-suite list through `le` and added the call. Found while porting this directory, not by any gate report: the file is `package main` with no build tag, so it has been in the linter's population all along and nothing said so | fixed on the spot, all five, because they stop a gate. `parallelGroup` and `kindSubcheck` are named beside `wholeStage` and replace eleven literals; the two `errors.As` sites become `errors.AsType[*exec.ExitError]`; `leGateScripts` takes `t.Context()`. the retired `scripts/status` (current producer: `internal/le/`) lints clean afterwards and its 62 tests stay green. The trap to carry forward: a directory whose files read like scripts is not outside the linter, and a red that nobody's `-changed` run selects can sit at HEAD indefinitely, because the changed mode only lints what a commit touched |
| 2026-08-19 | spec-verify-scope-2-change-set-selector | ci-dispatch call-site gate | `TestNoDeadDispatchCommands` (the retired `scripts/checks/ci_dispatch_commands_test.go` (current producer: `internal/le/repository/`)) is red at HEAD in a clean `internal/component/cli/` tree: `ci-dispatch-check: FAIL (0 dead, 2 unverifiable)`. `80c9fbcf7` routed two emitters through a helper, so `cliClient.Execute` and `cliClient.executeWithTranscript` (`internal/component/cli/client/main.go`) now call `SendCommand(commandWithFormat(command, format))`, and the checker cannot resolve that to a registered command. The contract it prints is the fix: make the command path literal, or mark the line `ze-dispatch-check: dynamic -- <reason>` when it is genuinely uncheckable | not fixed. It blocks nothing in this spec, whose whole surface is the retired `scripts/checks/verify_scope_selector.go` (current producer: `internal/le/changed/selector.go`) and one the retired Makefile (current producers: `internal/le/` native action tables) target, and both emitters belong to a subsystem another session is editing now. Whoever fixes it decides one thing: whether a command `commandWithFormat` builds is uncheckable by nature, or should reach `SendCommand` literally |
| 2026-08-17 | spec-rfc4271-med-ibgp-readvertisement | scripts/checks (retired; current producer: `internal/le/`) staticcheck feature matrix tests | `TestStaticcheckFeatureMatrixCompletedVerdictWinsExpiredContext` called `t.Parallel` inside subtests but not on the parent test, so `./le changed scope` failed with `tparallel` before the MED commit could be prepared | added top-level `t.Parallel` to the parent test |
| 2026-08-17 | spec-fixit-zefs-diff-structural-ops | bgp message RFC 7606 receive test | `internal/component/bgp/message/rfc7606_section51_receive_test.go` was committed un-gofmt'd at line 26, so `./le changed scope` failed with `gofmt` for every session whose changed set reached the package. The file is clean in the working tree, so the red comes from HEAD, not from another session | ran `gofmt -w` on the file |
| 2026-08-17 | none (owner directive: a Go commit owes a full verify) | verify runner tests | the retired `scripts/status/verify_run_test.go` (current producer: `internal/le/verify/engine/verifyengine_test.go`) is un-gofmt'd in HEAD at line 82, so `./le changed scope` failed with `gofmt` as soon as any edit put the file in the changed set. Third row of this shape today: an un-gofmt'd file reaches HEAD and lies dormant until somebody else's change pulls its package into the changed set | ran `gofmt -w` on the file |
| 2026-08-19 | fixit-iface-selector-ignored-by-apply | ssh session command dispatch | `internal/component/ssh/ssh.go` is un-gofmt'd in HEAD at line 835, a `//nolint` comment left over-aligned after the sibling line above it changed length. It arrived 40 minutes earlier in `80c9fbcf7`, and `./le changed scope` then failed with `gofmt` for a session whose own changed set is entirely under `internal/component/iface`. Fourth row of this shape: an un-gofmt'd file reaches HEAD and lies dormant until somebody else's change pulls its package into the changed set | ran `gofmt -w` on the file |
| 2026-08-19 | none (nft address-match family guard) | scripts/evidence (retired; current producer: `internal/le/`) l2tp diag tools | `./le changed scope` widened to `./...` because `tmp/ze-verify.status` records no green commit, and reported 8 findings at HEAD: errcheck on `unix.SetsockoptInt` and on `fmt.Sscanf`, gosec G103 on four `unsafe.Pointer` conversions, gosec G204 and noctx on `exec.Command`, all in the retired `scripts/evidence/l2tp-pppox-diag/main.go` (current producer: `internal/le/deployment/l2tpdiag_linux.go`) and the retired `scripts/evidence/l2tp-tunnel-diag/main.go` (current producer: `internal/le/deployment/l2tpdiag_linux.go`). Both files are clean in the working tree, so the red is HEAD's. Fifth row of this shape, and the first where the widened scope is what surfaced it | not fixed. Outside the problem in hand and blocking nothing in it |
