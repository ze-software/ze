| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `validateID` (`internal/le/rfc/summary.go`), reached through `./le rfc index-update` and `./le rfc check` | A requirement id whose cited section disagrees with its own number does not warn, it BLOCKS THE ENTIRE INDEX WRITE: `rfc-requirements: refusing to write: a summary did not parse, so its requirements are absent from this render and the prune would delete that RFC's file`. One malformed row in one summary therefore stops `ai/RFC-REQUIREMENTS.md` and all 179 shards from regenerating, for every session sharing the checkout. The refusal is correct -- a partial render would prune the unparsed RFC's file -- but its blast radius is corpus-wide while its cause is one line, and the second-order symptom is what a reader meets first: every test tagged against that summary reports `unknown RFC requirement`, because the check reads the render. This session met eight such lines and diagnosed them as a stale index before finding the one row that caused them. The row was `RFC9069-x-6` cited as `(Per-Peer Header, §5.1)` after an agent rewrote its text to the Section 5.1 obligation and kept the `x-` id, which is the corpus's own form for a requirement with no section. Two attempts were needed to clear it, because the parser reads a PROSE "Section 5.1" as the citation too, not only the `§` form -- so removing the `§5.1` from the parenthetical left the row still failing, with the same message, for a reason nothing in the message names | not fixed, and the repair is a message rather than a mechanism. The refusal already names the file, the line and the expected id shape, which is more than most gates give. What it does not say is the two things that cost the time: that the failure is total rather than per-row, so `unknown RFC requirement` elsewhere is a SYMPTOM of this and not a second defect; and that the section is parsed from prose as well as from `§`, so an author who removes the obvious citation and re-runs meets an identical message and reasonably concludes the check is wrong. The general practice this class states, pointed at a PARSER: a refusal that names one cause and has two teaches the author to disbelieve it on the second attempt, and disbelieving a gate is the habit every other rule here exists to prevent |
| 2026-08-31 | fixit-rfc-drain-quota-never-armed | `./le commit audit` against the refusal in `internal/le/commit/rfcchange.go`, both reading `internal/le/rfc/goscope.go` | The audit names the tag CARRIER and the commit gate wants the tag UNIT, so an author who writes `test/rfc-changed.md` rows from the audit's own output is refused by the gate, every time, one refusal cycle per row. First-hand: the audit reported `test: rfc5880_test` for two BFD files and the gate then demanded `TestRFC5880UnsupportedAuthTypesRejected` and `TestRFC5880KeyManagementRejectsIncompleteConfig`. A second session hit it the same night on four FlowSpec files: the audit gave four file stems, the gate named four FUNCTIONS across THREE files, added `flowspec-firewall/bridge_test.go` which the audit had not mentioned, and dropped `route_parse_test` which it had. The two are not a superset and a subset of each other, so no rule of thumb converts one into the other. The file-scope fallback applies where a file carries no tagged function and the function unit applies where it does, which is why the same tool pair disagrees per file rather than uniformly | not fixed. The remedy an author needs is one sentence and neither tool prints it: write the rows from the GATE's refusal, never from the audit. The audit is a survey of what changed; only the refusal states the names the ledger is matched on. This class is about a message that teaches the wrong move, and here the message is correct about its own question and wrong as guidance for the only task anybody runs it for. Either the audit prints the unit the gate matches, or it says outright that its names are not row names |
| 2026-08-31 | fixit-rfc-drain-quota-never-armed | `checkAnnotatedButTested` in `internal/le/rfc/`, whose message reads "`RFC4271-6.1-3` is annotated `{gap}` but IS tested (`rfc7313_error_scope_test.go`); the annotation is stale -- remove it" | The condition the check detects has TWO causes and it names the remedy for one. Either the annotation is stale, or a test carries a tag for a requirement it does not prove. Removing a truthful `{gap}` on the strength of a mistagged test converts an honest disclosure into a false public compliance claim, which is what `ai/rules/rfc-compliance.md` exists to prevent, reached by OBEYING a gate rather than by ignoring one. It happened: a second session read the message, believed the annotation stale, and reported it to Thomas as such. The gap was truthful. `ParseHeader` (`internal/component/bgp/message/header.go`) returns a bare `ErrInvalidLength` sentinel for a header Length below 19, and `readMessage` (`internal/component/bgp/reactor/session_read.go`) turns it into `logFSMEvent(fsm.EventBGPHeaderErr)` and returns, so nothing reaches the wire and `RFC4271-6.1-3`'s "Error Subcode MUST be Bad Message Length; Data field MUST contain the erroneous Length field" is genuinely unmet for that condition. The other length conditions DO answer conformantly through `ValidateLength` and `ValidateLengthWithMax`, which is exactly what the annotation said: five conditions, four reported. The tags were mine and wrong, on tests driving the ROUTE-REFRESH body length rather than the header Length field | tags deleted, annotation kept. Not fixed in the check, and that is the row's point: the remedy sentence is the defect, not the detection. A check that cannot distinguish its own two causes MUST NOT name one remedy in the imperative. It should state both causes and ask which, the way a which-way question does, because the reader who trusts it is the reader who loses a requirement. The general shape reaches every gate that pairs one detection with one instruction: before writing the remedy, ask what ELSE produces this condition, and whether the instruction harms that case |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `rfc/extraction/README.md`, over `createExtraction` (`internal/le/rfc/extraction_create.go`) | Re-running `./le rfc extraction-create stem <stem>` during a FIRST walk silently overwrites the classified scratch file with an all-`null` skeleton, destroying the walk with no warning, no prompt and no backup. `createExtraction` loads its `previous` document from `treePath(tree, extractionRel+"/"+stem+".json")` and from nowhere else, so it never sees the scratch copy the command itself just told the reviewer to edit. For a first sign-off that path does not exist, `previous` is nil, and `newExtractionDocument` regenerates every disposition as null. The README teaches the destructive action in the sentence that describes the safe one: it says the artifact reaches `rfc/extraction/` "on a refresh whose decisions all carry forward", which reads as an invitation to re-run and check your work, and the action-description for the command repeats "preserving authored classifications only where the same locator still carries the same sentence". Both are true of a RE-sign, where a signed artifact exists to carry forward from, and both are false of the first walk, which is the only kind this spec performs 46 times. Found when a sibling agent lost a fully classified rfc1997 skeleton to it and recovered only because it had classified through a script it could re-run; an agent hand-editing the JSON would have lost the walk with nothing to say so. TWO independent agents hit it within the hour, which is the measurement that matters: the second (rfc9234, 21 sites and 15 sections zeroed) re-ran the command for the reason the README invites, "to check my classified scratch file", and the warning broadcast to all four live agents reached it one tool call too late. It survived only because it still held the whole classification in context, which is luck and does not scale. Two hits in one hour, from agents who had each read the README, is a rate that says the hazard is in the tool rather than in the reader | not fixed here, and the repair is a decision rather than a string edit. Three candidates, in increasing order of what they cost: the README and the action description say plainly that carry-forward reads `rfc/extraction/<stem>.json` and that a first walk has nothing to carry, so re-running restarts it; or the writer refuses to overwrite an existing scratch file whose dispositions are not all null, which makes the safe path the default rather than the documented one; or `previous` also reads the scratch copy, which makes the advertised behaviour true. The second is the one that cannot be lost to a reader who does not open the README, and `ai/rules/never-destroy-work.md` is the rule it would satisfy: a writer that discards a reviewer's uncommitted work needs the reviewer to say so, not a paragraph warning them not to ask. The general practice this class already states, pointed at a WRITER rather than a message: a command whose documentation describes its safe case as though it were the general one teaches the destructive case to everyone whose situation is the other one |
| 2026-08-10 | - | hooks | 351 loose scratch files at the `tmp/` root in one day: the two hooks that correct a path (`check_pipe_tail`, `check_system_tmp`) named `tmp/out-$$.log` and `tmp/<subfolder>/` in their own remediation text, so the enforcement layer taught the shape `ai/rules/commands.md` bans | remediation text now names the retired `scripts/dev/session-scratch.sh` (current producer: `internal/le/`); `check_scratch_path` and `c_scratch_path_we` refuse a fixed name at the `tmp/` root on Bash and on Write, deciding it in one shared module; the same line went into the subagent contract |
| 2026-08-16 | - | bgp cache command | `handleBgpCacheForward` (`internal/component/bgp/plugins/cmd/cache/cache.go`) answered a missing selector with `usage: cache forward <id> <selector>`. The verb-first migration removed that spelling: `ze-cli-cache-cmd.yang` registers paths under `show cache` and `request cache` alone, and `command_test.go` asserts the command tree holds no `cache` child. Its four siblings in the same file, and the RPC entry point for the same action, all say `request cache ...`. So the one batch path told an operator to type a command the dispatcher refuses | fixed the string to `usage: request cache forward <id> <selector>`. Found while correcting the same removed spellings across the wiki CLI pages. the retired `ze-unit-pkg-test PKG=./internal/component/bgp/plugins/cmd/cache` (current: `go test -race ./internal/component/bgp/plugins/cmd/cache`) passes |
| 2026-08-19 | verify-scope-1-shared-checkout-freshness | job admission, `check_raw_test_invocation` remediation text | The refusal for a raw test run advises the retired `scripts/dev/ze-run.sh <label> <command>` (current producer: `internal/le/job/job.go`) when no make target fits, and that route admits FOUR TIMES fewer jobs than the one it replaces. `ze-run.sh` reads `SLOTS="${ZE_RUN_SLOTS:-1}"`, and the only producer of a real value is the retired `Makefile:166` (current producers: `internal/le/` native action tables), which derives cores/`GO_TEST_PROCS` (4 on this 32-core box) and exports it to make's children alone. A Bash tool call arrives without it, so the advised command silently runs the box at one slot. Measured: the retired `scripts/dev/ze-run.sh vscope-pytest3 python3 scripts/dev/commit_helper_test.py` invocation (current job route: `./le job run`; current test producer: `internal/le/commit/`), a 7.9s file, sat behind another session's `ze-qemu-needs-linux-test` for 621s and had not started when abandoned; the same file run under the `ZE_ADMIT_RAW` escape finished in 7.9s. The comment on the default calls it "for a caller that arrives without make", so the DEFAULT is deliberate and the ADVICE is what disagrees with it | not fixed. It does not block the spec in hand, and the repair is a decision covering both surfaces rather than a string edit: either the remediation text sets `ZE_RUN_SLOTS` in the command it prints, or `ze-run.sh` derives the slot count itself the way the retired `Makefile:166` (current producers: `internal/le/` native action tables) does instead of defaulting to one. The second is the one that cannot drift, since it removes make as the sole producer. Deriving it in the script also fixes every other direct caller, which no string edit reaches |
| 2026-08-19 | - | the RFC-tagged-test refusal, `.claude/hooks/pretool-writeedit.py` (retired; now `internal/le/hookruntime/writeedit.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> | The refusal dictates a PERMANENT artifact in the file it is guarding: "Once the USER has approved, record what they approved on the changed test: `// rfc-test-change-approved: <date> ...`", followed by "PUT IT IN THE LINES YOU ARE WRITING". An agent meeting that message complies, because complying is the only way past the gate, so the guard mints one permanent in-file justification per approval forever. The tree holds 255 of them across 120 test files. This repo has ALREADY ruled the other way for the same problem: `test/weakened.md` replaced the `test-relax:` in-file comment with a ledger REPLACED PER COMMIT, and its own prose gives the reason -- "A justification explains one diff ... After the commit lands, it explains a change the reader of the test file can no longer see. Storing that record permanently is what built the pile", measured then at 601 justifications across 413 files. The class is wider than one token: `rfc-test-change-approved:` 255, `test-relax:` 27, `test-asserts-nothing:` 6. The 27 are the tell, because that is the mechanism `test/weakened.md` says it REPLACED, still in the tree beside its own replacement: a reform with no sweep and no ratchet does not finish. Found by the owner reading a marker this session wrote, on `test/encode/new-v6.ci`, after the same session had read `test/weakened.md` in full and written two rows into it without connecting the two | not fixed, and the ORDER matters: the sweep MUST NOT go first. `audit-test-relaxation.py` states that `grep -rn 'rfc-test-change-approved'` is "the only backstop" against a forged token, and while the hook still demands an in-file marker a rule forbidding one leaves every author refused by one gate for obeying another. The repair is four pieces landing together: a ledger replaced per commit, a `commit_helper.py` gate reading it the way the weakening gate does, the hook message rewritten to point at the ledger, and only then the sweep of all three tokens. A ratchet over the total is what stops the next token accumulating. The hook half needs the owner: this session's edits to `.claude/hooks/` were refused twice by the harness classifier |
| 2026-08-24 | website-asciinema-terminal-demos | `rowOperatorRefusal` (`internal/component/command/pipe.go`) | The several-lists branch refuses a row operator with "it holds several lists (edges, nodes), so select one first", and nothing in the CLI can select one. No operator in `pipe_catalog.go` addresses a named list inside a document: `path` is not an operator at all, it is `ArgPath`, a FILESYSTEM path taken only by `save`. `ze config graph` accepts no selector either (`internal/component/config/cli/cmd_graph.go`). So the advice names an action the surface does not offer, and an operator who follows it literally cannot proceed. Reached by `ze config graph <file> then ze pipe match peer/upstream`, which the site demonstrated until this spec found it | not fixed. It does not block the spec in hand: the demo now reads `then ze pipe text then ze pipe match <selector>`, because a format operator applies to the whole answer and `match` over non-JSON keeps whole lines. The repair is a decision rather than a string edit, and it is one of two: either an operator that selects a named list, which makes the existing advice true, or advice that names what the surface actually offers. The first is the one an operator would expect from the sentence as written |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `validateTag` (`internal/le/commit/script.go`) against `tagPattern` (`internal/le/commit/input.go`) | `tagPattern` enforces two independent things, a character class and a `{0,31}` length bound, and one message spoke for both: "tag must start with an alphanumeric character and contain only alnum, dot, underscore, or dash". A 33-character tag of entirely legal characters drew it, which sends the author hunting a bad character that is not there. Met with `rfc5176-message-authenticator-may`, whose every character the class permits | fixed. The length is reported first and on its own, naming the count, the excess and the limit. `tagMaxLength` is now the one declaration and `tagPattern` is BUILT from it, so the bound cannot drift between the check and the regexp. `TestValidateTagReportsLengthSeparatelyFromCharacters` asserts both messages and derives its boundary from the constant; it was forced red by deleting the length branch and failed with the character-class text |
