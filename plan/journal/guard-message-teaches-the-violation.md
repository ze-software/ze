| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-10 | - | hooks | 351 loose scratch files at the `tmp/` root in one day: the two hooks that correct a path (`check_pipe_tail`, `check_system_tmp`) named `tmp/out-$$.log` and `tmp/<subfolder>/` in their own remediation text, so the enforcement layer taught the shape `ai/rules/commands.md` bans | remediation text now names `scripts/dev/session-scratch.sh`; `check_scratch_path` and `c_scratch_path_we` refuse a fixed name at the `tmp/` root on Bash and on Write, deciding it in one shared module; the same line went into the subagent contract |
| 2026-08-16 | - | bgp cache command | `handleBgpCacheForward` (`internal/component/bgp/plugins/cmd/cache/cache.go`) answered a missing selector with `usage: cache forward <id> <selector>`. The verb-first migration removed that spelling: `ze-cli-cache-cmd.yang` registers paths under `show cache` and `request cache` alone, and `command_test.go` asserts the command tree holds no `cache` child. Its four siblings in the same file, and the RPC entry point for the same action, all say `request cache ...`. So the one batch path told an operator to type a command the dispatcher refuses | fixed the string to `usage: request cache forward <id> <selector>`. Found while correcting the same removed spellings across the wiki CLI pages. `make ze-unit-pkg-test PKG=./internal/component/bgp/plugins/cmd/cache` passes |
