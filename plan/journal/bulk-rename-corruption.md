| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-28 | - | a package rename crossing into an unrelated package | A package was split and its old name swept repository-wide. The sweep matched a LOCAL VARIABLE that happened to carry the old name in a file with no connection to the package, renaming its five uses and not its declaration, so the package stopped compiling. Nothing in the renamed package was wrong; the damage was entirely in a file the rename had no business touching. Found by a build in another session, not by the sweeping one, because the broken package is a test fixture tree whose failure reads as `typecheck` in a lint run rather than as a compile error | reverted the five uses to the declared name. General practice: a rename of a PACKAGE identifier must be scoped to import sites and qualified references, never applied as a bare token substitution across the tree, because a short package name is also a plausible local variable name. Where a sweep is textual, the compile must be run over the whole affected build before it is handed on, since a partially renamed identifier fails only where it is read |
| 2026-03-21 | - | refactor | bulk sed renamed map-literal string keys alongside variable names | reverted and edited file-by-file |
| 2026-04-07 | - | refactor | family rename hit slog kv pairs and GetContainer arguments | reverted and renamed selectively |
| 2026-08-10 | fixit-l2tp-sccrq-tunnel-id-zero | lint | unexporting an exported-but-uncalled helper makes `unused` see it, so `golangci-lint ./internal/component/l2tp/...` is red at HEAD on `writeAVPUint8` and `readAVPUint8` (`internal/component/l2tp/avp.go`, unexported by 3c9644e15). Every package the refactor touched can carry the same shape | not fixed here: the owning spec is fixit-unexport-package-private-symbols and its session is live. Run `golangci-lint run ./cmd/ze/... ./internal/... ./pkg/...` over the renamed set and delete or call each helper the pass reveals |
| 2026-08-10 | fixit-unexport-package-private-symbols | refactor | `gopls rename` refuses a rename that breaks a reference. It runs untagged on the host GOOS. A reference under `//go:build ze_core` is invisible to it, so the refusal never fires. Unexporting `AvailableInternalPlugins` broke `cmd/ze`. Its only caller is `cmd/ze/main_test.go`, under `ze_core`. A per-package `go vet` misses it too, because it never compiles the other package | whole-tree `go vet` over all four build views after every package is processed, then rename each caught symbol back |
| 2026-08-29 | fixit-lint-blind-to-every-other-build-tag | the repository-wide sweep that retired `make` and `scripts/` (`eae282592`), where it reached `plan/` | The sweep rewrote a RECORD, and a record is a statement about a run that already happened. Four evidence cells came out citing `./le verify lint run-population-check`, a command that exists under no spelling, and those cells exist to be re-runnable. Prose came out mangled the same way ("the native action tables under `internal/le/`"). The same migration deleted `uncompiledTreeReaders` while two wiring rows kept naming it, and retired three guard tests whose names the spec still gave as its evidence | fixed at closure with a Post-Migration Correction table naming each retired spelling beside the producer that answers today, every one read from the tree rather than trusted from the sweep. Transferable: a sweep MUST NOT edit a record of a measurement. A rewritten command is a claim nobody can re-run, and it reads as evidence. When a sweep does reach a record, the closing session re-verifies each cited producer and states what moved, instead of reading the new name as the old fact |
