| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-03 | local-ca | `(*Root).IssueLeaf` (`internal/component/pki/ca.go`) and its two callers, `NewHubAcceptor` (`internal/component/plugin/acceptor.go`) and `managedCertificate` (`internal/component/plugin/server/managed_serve.go`) | `leafValidity` is 24 hours, and the comment above it states the assumption behind that number, "a component reissues on every start". A ze daemon is not restarted daily. Both callers issue ONCE at construction: `managedCertificate` runs from `NewManagedServer`, which `gopls references` answers as its only caller, and `NewHubAcceptor` runs from `(*Manager).ensureAcceptor`. Nothing holds a timer, and `tls.Config.GetCertificate` is set on neither listener, so the certificate a hub serves after 24 hours of uptime is expired. Every plugin connect-back and every managed-client reconnect after that point fails path validation, while the operator's config is unchanged, so nothing points at the cause. AC-7 of the spec names the expiry-then-RESTART case and is satisfied; the expiry-without-restart case is not in the spec. Found while writing this spec's phase 3-4 documentation, which cannot state the leaf lifetime without stating what happens at the end of it | fixed 2026-09-03, the first of the two: `ServingLeaf` (`internal/component/plugin/leaf.go`) holds the served leaf and reissues it once two thirds of its life is spent, and `StartListeners` (`internal/component/plugin/ipc/tls.go`) now takes a `tls.Config.GetCertificate` func rather than a fixed `Certificates` entry. `NewHubAcceptor` and `managedCertificate` both serve through one; a certificate the operator NAMED is answered unchanged, because that material is theirs. `leafValidity` is untouched at 24 hours. Proven by `TestHubAcceptorReissuesBeforeTheLeafExpires` and `TestHubAcceptorIssuesOneLeafForConcurrentHandshakes` (`internal/component/plugin/leaf_test.go`), which move a `sim.FakeClock` past the leaf's life and dial with `tls.Config.Time` set to it. The recorded RED, with the renewal check removed: `tls: failed to verify certificate: x509: certificate has expired or is not yet valid` |
