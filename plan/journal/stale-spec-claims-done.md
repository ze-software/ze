| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-21 | - | `test/runner/verify-scope-wiring-attribution.ci` | Red at HEAD and deterministic: identical failure in two consecutive full `make ze-precommit-verify` runs. Its SETUP script builds a scratch repo, commits `mine.go`, and expects `commit_helper.py` to allow it with the note `structural gate(s) red for another session only`. It never reached that assertion, because a different gate refused first: `error: this commit carries Go and no test: mine.go ... or pass --no-test`. The fixture stated no `--no-test` reason for a fixture file that needs none, so a gate unrelated to its subject decided its verdict | fixed, all three `commit_helper.py` invocations in the fixture now carry `--no-test` with a reason naming `mine.go` as a scratch-repo fixture. **This row first recorded it as another session's to fix, and that was wrong**: the spec owning the file is `in-progress`, which says nothing about who is working, and one owner works this tree. A spec's status is not an ownership claim, and reading it as one leaves a deterministic red sitting for whoever runs the gate next |
| 2026-04-14 | - | workflow | spec said 'Phase N remains' but code was fully landed | audited spec against code and updated status |
| 2026-04-14 | - | workflow | four cmd-series specs all had stale What Remains sections | audited and closed each spec |
| 2026-08-14 | fixit-bgp-egress-rail-divergence | bgp replay ownership, plugin startup | The spec was `blocked` on an owner decision about AC-5, written 2026-07-25 when the ownership claim rode the unordered post-startup fan-out. The ordering fix had landed since: the claim is declared in `rs/register.go` and delivered on Stage 2 by `advertiseClaims`. A triage pass then over-corrected and offered "AC-5 is met" flat. The closure review found two mid-life paths the claim still misses | Closed AC-5 on the STARTUP path only. The two mid-life paths went into the destination spec as criteria, not as prose. Also swept the citations a closure breaks. Six Go comments named the spec by full path, and `make ze-spec-citation-check` cannot see them, because it reads `plan/spec-*.md` citers only |
| 2026-08-14 | fixit-ipsec-interop-cli-credentials | ipsec interop lab | Triaged as "already fixed, close it": the lab does provision the credential and scenario 10 passes live against strongSwan. AC-3 was NOT met, and the evidence first offered for it was a mis-grep. `grep -l authentication` over the scenario configs returns all sixteen because every scenario carries an IKE `authentication` block; only two carry the `system authentication user` account | Kept the spec OPEN on one deliverable. A grep that returns every file is a signal to read one of them, not a result |
| 2026-08-15 | fixit-mirror-clsact-ownership | iface traffic mirroring, QEMU integration tests | the spec's Functional Tests table marked ten tests `pass` while three of them asserted the OPPOSITE of the shipped code. AC-3 was relaxed at the end of implementation (teardown stopped deleting the shared clsact qdisc), and the relaxation reached the code, the commit message and the spec's AC row, but not the tests, the journal row, or `docs/features/interfaces.md`. Nothing caught it: the tests are `//go:build integration && linux`, so a darwin workstation compiles them and runs nothing, and the QEMU run that would have failed was made BEFORE the relaxation. Closure ran them and got three reds, exactly the three assertions naming the qdisc | fixed. The assertions are INVERTED rather than dropped, so a teardown that starts deleting the qdisc again reds each one, and the doc plus the journal Fix cell now state what the code does. The lesson is that the last decision of an implementation is the one nobody re-proves: a QEMU result cited in a spec is a claim about the tree it ran on, and the relaxation made that tree obsolete |
| 2026-08-12 | fixit-bgp-session-fsm-lifecycle | bgp session FSM and timers | The spec carried a header reading THIS SPEC CANNOT BE CLOSED, written on 2026-07-27. Every item it listed as owed had landed since. Its own AC tables also cited two tests as evidence that a later owner ruling had deleted, `TestGracedHoldExpiryIncrementsCounter` and `TestRFC4271GracedHoldExpirySendsNoNotification`, both removed with the hold-timer grace on 2026-08-03 | Verified every AC at its producing function instead of trusting the spec text, then retired the stale verdict and repointed the dead test citations in the closure commit. A dated audit verdict in a spec is a claim about the day it was written. It is never a claim about today |
| 2026-08-17 | fixit-stored-route-relay-hardening | bgp forward rail, RFC 7911 path identifiers | The spec parked an RFC MUST as an open owner question and made a later implementation step depend on it. The requirement had been implemented and both polarities tagged three days after the spec text was written, so the blocker survived only as prose. Acting on it would have spent an owner decision on a question the tree already answered | Re-read the producer before acting on the parked question, corrected the spec where the stale text lived, and unblocked the step. Root cause: a parked question is a dated observation, and nothing re-reads it when the step waiting on it resumes. General practice: re-verify a parked question at its producer before it reaches the owner or gates work |
| 2026-08-16 | fixit-peer-process-event-filter | runtime event subscription confinement and route-server replay fixtures | The implementation and its closure prose called `request subscribe` a live override delivered even when the peer config denied the type, while the spec's Deliverables and Security Review checklists required every subscription producer to stay inside the configured receive grant. The migrated route-server fixtures also named `bgp-rs` but omitted its optional `bgp-adj-rib-in` replay provider, so source-before-destination ordering lost the retained route even though assertions still expected replay | Kept runtime subscriptions as live additions to process capability but intersected them with `DeliveryGraph.Receivers` in `PeerScopedProcs`. Added the replay provider and its source/destination attachments to all 19 dependent fixtures without changing assertions or RFC outcomes. The shared lesson is that a configured process edge is an authorization and dependency boundary: every producer must stay inside it, and every fixture must explicitly provide each optional process its assertion depends on |
| 2026-08-17 | fixit-qemu-runtime-kernel, fixit-firewall-concurrency-deadlock, fixit-lint-blind-to-integration-tag, fixit-redistribute-establishment-stall | spec text vs tree | Four open specs each carried a claim about the tree that the tree had stopped holding. The four claims were a make default, a file count, an AC inventory, and the scope of a finished conversion. Two specs cited the SAME stale default. One had quoted the other's research instead of the Makefile. Root cause: a spec records a measurement taken the day it was written. Nothing re-reads that measurement when the spec resumes. A spec that quotes another spec carries the staleness forward with no second reading. The cost is a stale FRAME, not a wrong fact. Nothing contradicts a frame, so the next session plans competently inside a picture the tree abandoned | Re-verified every claim at its producing function. Struck each stale sentence in place and stated what is true now beside it, with the producer named. A deletion would hide that the frame moved. General practice: treat a spec's claim about the tree as dated evidence, and re-read its producer before it gates work. When a spec needs another spec's measurement, cite the PRODUCER rather than the quoting spec |
| 2026-08-19 | verify-scope-4-suite-budget-and-ci | `plan/spec-functional-suites-missing-their-make-target.md`, Status `ready` since 2026-08-11 | The spec is right that `ldp`, `rsvpte` and `install` have no make entry point, and WRONG about the naming it derives from. Its Task says "The other 21 names in the `all_suites` line resolve", and its AC-1, AC-2, AC-3, AC-6 and AC-7 name the new targets `ze-ldp-test`, `ze-rsvpte-test` and `ze-install-test`. Measured against the make database (`make -p -q` over a probe target, so no recipe runs): **zero** targets exist matching `ze-<suite>-test` for any of the 24 gating suites, and 21 exist matching `ze-functional-<suite>-test`. The 2026-08-11 measurement quoted in the spec is sound for the three missing suites and does not support the premise drawn beside it: those 21 never resolved under the spelling the ACs adopt. Implemented as written it would add three targets in a scheme nothing else uses, and keep a failure hint that is wrong for the other 21 | the DEFECT is fixed, under this spec rather than that one, and with the sibling naming: `ze-functional-ldp-test`, `ze-functional-rsvpte-test` and `ze-functional-install-test` exist, the failure report prints `make ze-functional-%s-test`, `functionalSuiteRerun` (`scripts/status/verify_run.go`) prints the same, and `TestEverySuiteCanBeRerun` (`scripts/dev/functional_suite_test.py`) fails when an `all_suites` member has no target. The SPEC is untouched: its ACs contradict the tree now, and rewriting an approved spec's acceptance criteria is the owner's call, not a passing session's. Raised with Thomas on 2026-08-19 |
| 2026-08-22 | - | spec metadata Status field against the tree | A spec's Status said the work had not started while that work had shipped and was committed. The inventory a session reads to choose its next piece of work reported the spec as unstarted, so a later session spent a full agent run re-deriving code the tree already held. Root cause: Status is an assertion maintained by hand at a moment nobody revisits, and no check compares it against the artifact it describes. The two records drift in silence, and the drift is visible only to a reader who opens both | Not fixable by editing the affected specs, because the next one drifts the same way. General practice: a hand-maintained field that describes another artifact will drift, so either derive it from that artifact or gate it with a check that compares the two. Until such a check exists, read the tree before you trust a Status, and advance the Status in the same change that lands the work rather than as a later step |
