| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-04-14 | - | workflow | spec said 'Phase N remains' but code was fully landed | audited spec against code and updated status |
| 2026-04-14 | - | workflow | four cmd-series specs all had stale What Remains sections | audited and closed each spec |
| 2026-08-14 | fixit-bgp-egress-rail-divergence | bgp replay ownership, plugin startup | The spec was `blocked` on an owner decision about AC-5, written 2026-07-25 when the ownership claim rode the unordered post-startup fan-out. The ordering fix had landed since: the claim is declared in `rs/register.go` and delivered on Stage 2 by `advertiseClaims`. A triage pass then over-corrected and offered "AC-5 is met" flat. The closure review found two mid-life paths the claim still misses | Closed AC-5 on the STARTUP path only. The two mid-life paths went into the destination spec as criteria, not as prose. Also swept the citations a closure breaks. Six Go comments named the spec by full path, and `make ze-spec-citation-check` cannot see them, because it reads `plan/spec-*.md` citers only |
| 2026-08-14 | fixit-ipsec-interop-cli-credentials | ipsec interop lab | Triaged as "already fixed, close it": the lab does provision the credential and scenario 10 passes live against strongSwan. AC-3 was NOT met, and the evidence first offered for it was a mis-grep. `grep -l authentication` over the scenario configs returns all sixteen because every scenario carries an IKE `authentication` block; only two carry the `system authentication user` account | Kept the spec OPEN on one deliverable. A grep that returns every file is a signal to read one of them, not a result |
| 2026-08-12 | fixit-bgp-session-fsm-lifecycle | bgp session FSM and timers | The spec carried a header reading THIS SPEC CANNOT BE CLOSED, written on 2026-07-27. Every item it listed as owed had landed since. Its own AC tables also cited two tests as evidence that a later owner ruling had deleted, `TestGracedHoldExpiryIncrementsCounter` and `TestRFC4271GracedHoldExpirySendsNoNotification`, both removed with the hold-timer grace on 2026-08-03 | Verified every AC at its producing function instead of trusting the spec text, then retired the stale verdict and repointed the dead test citations in the closure commit. A dated audit verdict in a spec is a claim about the day it was written. It is never a claim about today |
