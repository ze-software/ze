| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-23 | - | `plan/spec-finish-l2tp.md` and `plan/deferrals/startup-resilience.md`, against `internal/component/l2tp/plugins/authradius/yang/ze-l2tp-auth-radius-conf.yang` | Both records state that `coa-port` is read by the authradius parser with no YANG leaf anywhere in the tree, so the CoA listener branch in `register.go` is unreachable from production config. The leaf has existed since commit 64d160ace on 2026-07-16, and `TestParseConfigCoAPortProductionPath` has pinned the parse path since the same commit. A sweep re-derived the defect from the two records rather than from the tree and commissioned work already done. The half of those records that was still true is the end-to-end proof: nothing exercised a running daemon starting the listener from an operator config | fixed. Both records now say the leaf landed and name the commit, and `test/l2tp/radius-coa-listener.ci` closes the proof they asked for: it starts the daemon from a config naming `coa-port` and asserts the listener answers an RFC 5176 Disconnect-Request with a Disconnect-NAK carrying Error-Cause 503. Deleting the leaf takes it red with the original symptom, unknown field in radius: coa-port |
| 2026-08-21 | - | `test/runner/verify-scope-wiring-attribution.ci` | Red at HEAD and deterministic: identical failure in two consecutive full `./le verify current mode full` runs. Its SETUP script builds a scratch repo, commits `mine.go`, and expects `commit_helper.py` to allow it with the note `structural gate(s) red for another session only`. It never reached that assertion, because a different gate refused first: `error: this commit carries Go and no test: mine.go ... or pass --no-test`. The fixture stated no `--no-test` reason for a fixture file that needs none, so a gate unrelated to its subject decided its verdict | fixed, all three `commit_helper.py` invocations in the fixture now carry `--no-test` with a reason naming `mine.go` as a scratch-repo fixture. **This row first recorded it as another session's to fix, and that was wrong**: the spec owning the file is `in-progress`, which says nothing about who is working, and one owner works this tree. A spec's status is not an ownership claim, and reading it as one leaves a deterministic red sitting for whoever runs the gate next |
| 2026-04-14 | - | workflow | spec said 'Phase N remains' but code was fully landed | audited spec against code and updated status |
| 2026-04-14 | - | workflow | four cmd-series specs all had stale What Remains sections | audited and closed each spec |
| 2026-08-14 | fixit-bgp-egress-rail-divergence | bgp replay ownership, plugin startup | The spec was `blocked` on an owner decision about AC-5, written 2026-07-25 when the ownership claim rode the unordered post-startup fan-out. The ordering fix had landed since: the claim is declared in `rs/register.go` and delivered on Stage 2 by `advertiseClaims`. A triage pass then over-corrected and offered "AC-5 is met" flat. The closure review found two mid-life paths the claim still misses | Closed AC-5 on the STARTUP path only. The two mid-life paths went into the destination spec as criteria, not as prose. Also swept the citations a closure breaks. Six Go comments named the spec by full path, and `./le spec citation anchors` cannot see them, because it reads `plan/spec-*.md` citers only |
| 2026-08-14 | fixit-ipsec-interop-cli-credentials | ipsec interop lab | Triaged as "already fixed, close it": the lab does provision the credential and scenario clear-reestablish passes live against strongSwan. AC-3 was NOT met, and the evidence first offered for it was a mis-grep. `grep -l authentication` over the scenario configs returns all sixteen because every scenario carries an IKE `authentication` block; only two carry the `system authentication user` account | Kept the spec OPEN on one deliverable. A grep that returns every file is a signal to read one of them, not a result |
| 2026-08-15 | fixit-mirror-clsact-ownership | iface traffic mirroring, QEMU integration tests | the spec's Functional Tests table marked ten tests `pass` while three of them asserted the OPPOSITE of the shipped code. AC-3 was relaxed at the end of implementation (teardown stopped deleting the shared clsact qdisc), and the relaxation reached the code, the commit message and the spec's AC row, but not the tests, the journal row, or `docs/features/interfaces.md`. Nothing caught it: the tests are `//go:build integration && linux`, so a darwin workstation compiles them and runs nothing, and the QEMU run that would have failed was made BEFORE the relaxation. Closure ran them and got three reds, exactly the three assertions naming the qdisc | fixed. The assertions are INVERTED rather than dropped, so a teardown that starts deleting the qdisc again reds each one, and the doc plus the journal Fix cell now state what the code does. The lesson is that the last decision of an implementation is the one nobody re-proves: a QEMU result cited in a spec is a claim about the tree it ran on, and the relaxation made that tree obsolete |
| 2026-08-12 | fixit-bgp-session-fsm-lifecycle | bgp session FSM and timers | The spec carried a header reading THIS SPEC CANNOT BE CLOSED, written on 2026-07-27. Every item it listed as owed had landed since. Its own AC tables also cited two tests as evidence that a later owner ruling had deleted, `TestGracedHoldExpiryIncrementsCounter` and `TestRFC4271GracedHoldExpirySendsNoNotification`, both removed with the hold-timer grace on 2026-08-03 | Verified every AC at its producing function instead of trusting the spec text, then retired the stale verdict and repointed the dead test citations in the closure commit. A dated audit verdict in a spec is a claim about the day it was written. It is never a claim about today |
| 2026-08-17 | fixit-stored-route-relay-hardening | bgp forward rail, RFC 7911 path identifiers | The spec parked an RFC MUST as an open owner question and made a later implementation step depend on it. The requirement had been implemented and both polarities tagged three days after the spec text was written, so the blocker survived only as prose. Acting on it would have spent an owner decision on a question the tree already answered | Re-read the producer before acting on the parked question, corrected the spec where the stale text lived, and unblocked the step. Root cause: a parked question is a dated observation, and nothing re-reads it when the step waiting on it resumes. General practice: re-verify a parked question at its producer before it reaches the owner or gates work |
| 2026-08-16 | fixit-peer-process-event-filter | runtime event subscription confinement and route-server replay fixtures | The implementation and its closure prose called `request subscribe` a live override delivered even when the peer config denied the type, while the spec's Deliverables and Security Review checklists required every subscription producer to stay inside the configured receive grant. The migrated route-server fixtures also named `bgp-rs` but omitted its optional `bgp-adj-rib-in` replay provider, so source-before-destination ordering lost the retained route even though assertions still expected replay | Kept runtime subscriptions as live additions to process capability but intersected them with `DeliveryGraph.Receivers` in `PeerScopedProcs`. Added the replay provider and its source/destination attachments to all 19 dependent fixtures without changing assertions or RFC outcomes. The shared lesson is that a configured process edge is an authorization and dependency boundary: every producer must stay inside it, and every fixture must explicitly provide each optional process its assertion depends on |
| 2026-08-17 | fixit-qemu-runtime-kernel, fixit-firewall-concurrency-deadlock, fixit-lint-blind-to-integration-tag, fixit-redistribute-establishment-stall | spec text vs tree | Four open specs each carried a claim about the tree that the tree had stopped holding. The four claims were a make default, a file count, an AC inventory, and the scope of a finished conversion. Two specs cited the SAME stale default. One had quoted the other's research instead of the retired Makefile (current producers: `internal/le/` native action tables). Root cause: a spec records a measurement taken the day it was written. Nothing re-reads that measurement when the spec resumes. A spec that quotes another spec carries the staleness forward with no second reading. The cost is a stale FRAME, not a wrong fact. Nothing contradicts a frame, so the next session plans competently inside a picture the tree abandoned | Re-verified every claim at its producing function. Struck each stale sentence in place and stated what is true now beside it, with the producer named. A deletion would hide that the frame moved. General practice: treat a spec's claim about the tree as dated evidence, and re-read its producer before it gates work. When a spec needs another spec's measurement, cite the PRODUCER rather than the quoting spec |
| 2026-08-19 | verify-scope-4-suite-budget-and-ci | `spec-functional-suites-missing-their-make-target` (closed 2026-09-05, cancelled by the owner), Status `ready` since 2026-08-11 | The spec is right that `ldp`, `rsvpte` and `install` have no make entry point, and WRONG about the naming it derives from. Its Task says "The other 21 names in the `all_suites` line resolve", and its AC-1, AC-2, AC-3, AC-6 and AC-7 name the new targets `ze-ldp-test`, `ze-rsvpte-test` and `ze-install-test`. Measured against the make database (`make -p -q` over a probe target, so no recipe runs): **zero** targets exist matching `ze-<suite>-test` for any of the 24 gating suites, and 21 exist matching `ze-functional-<suite>-test`. The 2026-08-11 measurement quoted in the spec is sound for the three missing suites and does not support the premise drawn beside it: those 21 never resolved under the spelling the ACs adopt. Implemented as written it would add three targets in a scheme nothing else uses, and keep a failure hint that is wrong for the other 21 | the DEFECT is fixed, under this spec rather than that one, and with the sibling naming: `ze-functional-ldp-test`, `ze-functional-rsvpte-test` and `ze-functional-install-test` exist, the failure report prints `make ze-functional-%s-test`, `functionalSuiteRerun` (the retired `scripts/status/verify_run.go` (current producer: `internal/le/verify/engine/run.go`)) prints the same, and `TestEverySuiteCanBeRerun` (the retired `scripts/dev/functional_suite_test.py` (current producer: `internal/le/`)) fails when an `all_suites` member has no target. The SPEC is untouched: its ACs contradict the tree now, and rewriting an approved spec's acceptance criteria is the owner's call, not a passing session's. Raised with Thomas on 2026-08-19 |
| 2026-08-22 | - | spec metadata Status field against the tree | A spec's Status said the work had not started while that work had shipped and was committed. The inventory a session reads to choose its next piece of work reported the spec as unstarted, so a later session spent a full agent run re-deriving code the tree already held. Root cause: Status is an assertion maintained by hand at a moment nobody revisits, and no check compares it against the artifact it describes. The two records drift in silence, and the drift is visible only to a reader who opens both | Not fixable by editing the affected specs, because the next one drifts the same way. General practice: a hand-maintained field that describes another artifact will drift, so either derive it from that artifact or gate it with a check that compares the two. Until such a check exists, read the tree before you trust a Status, and advance the Status in the same change that lands the work rather than as a later step |
| 2026-08-24 | fixit-rfc-gate-has-no-level-ratchet | RFC gate ratchets, the retired `scripts/dev/rfc_requirements.py` (current producer: `internal/le/rfc/rfc.go`) | The spec's Status read `skeleton` and its Task described a hole in the RFC gate that had been closed the day after the spec was written. `check_level_ratchet`, `parse_corrections`, `authorising_correction` and `_git_baseline_levels` all shipped in `41c44438c`, whose subject reads "fix(rfc): stop telling readers to run a command that does not exist", and the rule and skill halves shipped in `c22b64f02` and `449d71402`. No commit subject names the spec, so `git log` over the spec path shows only plan commits and the connection is invisible from either side. The spec's own TDD tables already read `done` in every row while Status stayed `skeleton`: the session that did the work updated the evidence and not the field the backlog reads | Closed as ALREADY SATISFIED, with no product code written. Every AC was re-derived at the producer in a `git clone --local` of this repository, where one real row was demoted, corrected, promoted and deleted in turn. General practice: a commit that satisfies a spec must name the spec stem in its subject or body. When it does not, the only surviving pointer is the Status field, which is the one thing nobody moves, and the spec then commissions work already done |
| 2026-08-24 | fixit-lint-blind-to-every-other-build-tag | `spec-fixit-lint-blind-to-every-other-build-tag` Files to Modify and AC-7, `test/weakened.md`, against `cmd/ze/hub` | Three statements described work that never happened, and each was written in the same edit as the work it was describing rather than after a run. The spec said `cmd/ze/hub/bgp_decode_nolink_test.go` <!-- doc-links: ignore (the test file this row records was never created) --> "is gone and `bgpDecodeLinked` with it": the file is 557 bytes on disk and still holds the constant. It said `forceExitOnSignal` moved from `main.go` into `service_web.go`: only `resolveConfigPath` moved, and `c_os_exit` refuses the destination for anything carrying `os.Exit`. AC-7 said the `compile-out` lint row "now reports 0": it reports 2, at HEAD, and both are the symbols above. `test/weakened.md` carried the first claim a second time. The commit messages of `98f4297eb` and `448740364` are ACCURATE about all three, so the tree held the correct account and the spec held the intended one, and only the spec is what a closing session reads | corrected at closure, one edit each, and the spec was held OPEN because the two `unused` findings were real and each needed an owner ruling. Thomas ruled both on 2026-08-24 and the fixes landed at `89c35d8ed` and `d580c0870`, so the intended account became the true one a day later, by doing the work rather than by editing the claim. Round 4 then found a FOURTH statement of the same shape in the same spec, a count of `//nolint` suppressions written as four where the diff carries eleven. It was corrected to TEN, which was wrong again: the correction omitted the `gosec` on the modprobe call in `internal/plugins/flowexport/conntrack_setup_appliance_linux.go`, one of the nine `587f86ad4` adds. Three values for one number, and the third was found on 2026-08-25 by deriving it from the eight commits rather than reading the enumeration. That is the point the row exists to make: the habit outlives one correction, and it outlives two when the correction is itself hand-counted. Transferable: a closure record must be written from a run, never from the edit that was about to make it true. The cheapest guard is the one that caught this -- re-run the gate the AC names, over the exact scope it names, and paste the exit code -- because an AC that says "reports 0" is a command, and a command that was not run is not evidence |
| 2026-08-29 | wire-edit-3-deferred-ac9-dead-code | `plan/` backlog against `internal/component/bgp/reactor/received_update.go` and `internal/perf/allocgate.go` | The spec sat `in-progress` with `Handoff: verify` while every deliverable had landed 12 days earlier. `df44d8d27` deleted `EBGPWire`, `ebgpSlot`, `ebgpWireSlot`, `ebgpMu`, `errEbgpWireBufferExhaustedPoolAt`, the two `recent_cache.go` slot fields with their four readers, six tests and two benchmark files; `467d99165` removed the `AllocCeilings` string key and rewrote the two architecture pages. Neither subject names the spec or its stem, and `df44d8d27` opens NOT THIS SESSION'S WORK: it is a salvage commit, made by a session that found another's finished tree and landed it on the owner's instruction. A salvage commit is the case where the stem is most likely to be dropped, because the committing session did not do the work and does not know which spec it satisfies. Nothing then moved Status to `verification`, so the backlog reported the delete as open work for 12 days | closed as ALREADY SATISFIED, with no product code written. Every AC was re-derived at the producer: the tree-wide grep returns zero matches for all five symbols, `TestForwardDoesNotRetranscodeASN2RewrittenWire` still asserts AS_PATH 65000 then 65001 and builds its fixture with `wireu.RewriteASPath`, and `./le verify deps alloc` is green over six registered benchmarks while `TestAllocGateMissingFailsClosed` still holds the fail-closed branch that made the stale key catchable. General practice unchanged from the 2026-08-24 row above and reinforced by the salvage case: a commit that satisfies a spec must name the spec stem, and when it cannot, the session that lands it must move the Status in the same commit |
| 2026-09-04 | radius-acct-session-attributes | `internal/component/radius`, the spec's own Deliverables Checklist and TDD table | The Deliverables Checklist row for the honest delay on retransmit named `TestAcctDelayTimeUpdatesOnRetransmit` and read "green (phase 5)" for a test that was never written, and the TDD table's Status cell for the same name was left EMPTY, which is the tell. AC-7 and AC-8 rested on `TestRFC2866AccountingRetransmitTakesANewIdentifier`, which watches the Identifier alone and would pass against a client that moved the Identifier and left a stale Acct-Delay-Time behind, which is the osvbng constant-zero shape the spec exists to avoid | fixed at closure. `internal/component/radius/acct_delay_time_retransmit_test.go` reads the datagrams off the wire and asserts the delay is 0 on the first attempt and at least 1 after a 1.1s retry, a distinct Identifier, and a Request Authenticator that matches an independent MD5 of the second datagram. Its red phase was forced: `seconds = uint32(elapsed / time.Second)` set to a constant zero fails it with "the retransmission reported Acct-Delay-Time 0 after a 1.1s wait". General practice: an EMPTY Status cell in a TDD table is read as unwritten, whatever the Deliverables Checklist beside it claims |
