# Green that could not have been red

An observation that cannot come out otherwise is not evidence. Every row here is
a signal that read green while carrying no information, and in each one the
greenness was structural rather than earned: nothing about the state of the
product could have changed it.

This is a sharper class than "vacuous test", because the mechanisms differ and
only the consequence is shared. Ten are known, and they fail at ten different
points.

Where the stimulus never reaches the defect's path, the green says the test
exercised something else. Where the assertion names a spelling the surface never
emits, it says the test asserted nothing. Where the run never happened, it says
the tool answered for a previous tree. Where the reason is produced and then
discarded before a reader, the failure is real and merely unattributable. That
fourth one is the only shape here that is not a false green. Where the MUTATION
never happened, nothing was tested at all, and the artifact of the attempt is
byte-identical to success.

The sixth is the odd one, because the signal itself was sound. The other five
RAN and carried no information. This one never ran where it mattered: a skip
kept it off the machine the work happens on, and the suite's aggregate still
read green. A coverage hole in a rarely-run suite costs little. A hole over the
one signal that CAN fail, on the one machine everybody uses, costs the whole
value of the test.

The seventh is not about the test at all. It is about the THRESHOLD the gate
compares against. When that threshold is an editable value, raising it is the
cheapest route from red to green, cheaper than fixing what went red. A passing
run prints neither the measurement nor the threshold. The raise therefore leaves
no trace in the output. A relaxation audit that reads assertions does not see it
either, because a value in a map is not an assertion. A gate that reads a tunable
and reports only its verdict needs a second reader for the tunable, or the
tunable IS the escape hatch.

The eighth is the only one where nothing is wrong with the signal. Every other
mechanism here is a signal that FAILED to carry information. This one carries
perfect information, accurately, about something you did not ask about: a real
exit status, belonging to a different process from the one whose verdict you
wanted. A wrapper's status is the wrapper's. Nothing in the output looks wrong
because nothing IS wrong, except the referent.

It is also the one no checker can reach. A tool can notice a bare `ok` with no
duration, or a mutation that did not apply, or a threshold nobody read. Nothing
can notice that a true statement answers a different question, because every
part of it is true. What caught the one instance was a discipline rather than a
tool: the reds were being read name by name, and seven were expected where six
appeared. That is consistent with it being uncheckable, and it is the reason to
read a gate's verdict from the gate's own last lines rather than from whatever
reported it.

The ninth is a lock-ordering bug, and it is the only one here whose test was
written for exactly its class and still could not fail. Nested readers on a Go
`RWMutex` deadlock only when a writer QUEUES between the two acquisitions, so
the interleaving that fails is one a concurrency test has to produce by luck.
It produced the others and not that one. A test proves the interleavings it
runs, never the ones it could have run, and no amount of re-running changes
which window it happens to hit. Reading the lock structure is what catches it.

The tenth is a property of the test DOUBLE rather than of the test. The test is
well formed, it reaches the code under test, and it asserts a real thing. The
double collapses two cases the test exists to tell apart, so the distinction is
gone before the assertion runs. That is what separates it from the first four:
the stimulus does reach the defect's path, the assertion can fail, and the
fixture is a shape the producer really emits. What cannot vary is the double's
answer. A fake that cannot represent the situation under test is a green that
could not have been red.

The tell is a constant standing in for a collaborator whose real answer VARIES
per input, and the check is to ask what the double returns for the two inputs
the test is about, then whether the test could tell them apart. Such a double
can be harmless for as long as one ordering holds and become wrong the instant
that ordering changes, so a row here can record a LATENT green rather than a
failed one. Fix it when you find it, not when it bites.

The habit that catches the first four is the same, and it is cheap. Before you
trust a green, break the thing it judges and watch it go red. A red you cannot
produce on demand is a green you cannot rely on. Where no red can be produced at
all, say so in the test. Do not leave the reader to assume coverage.

That habit does not reach the sixth either, and for a plainer reason than the
fifth: you cannot break a thing and watch it go red on a machine where it does
not run. What reaches it is reading the skip. A skip with a stated reason is a
coverage boundary a reader can audit. A skip with none is an assertion nobody
can check. The two read identically, in the file and in the suite output alike.

**The fifth mechanism defeats that habit, which is why it is listed apart.** It
is the habit itself failing silently. The break is attempted, the patch does not
apply, the test runs against unmodified source, and it passes. The proof then
reads exactly like a successful one.

So the habit needs a guard of its own, and the guard is cheaper than the run it
protects. VERIFY THAT THE MUTATION APPLIED, between the patch and the run.

The two ways to do that are not equal, and the weaker one looks sufficient. A
non-empty diff proves that SOMETHING changed. It does not prove the right thing
changed. A patch that lands at the wrong offset gives a non-empty diff. So does a
`sed` that matches twice. Either one leaves a mutation that never disabled what
the test judges.
Assert the specific text instead. Grep for the mutated string and require the
count you expect, which is usually exactly one. A count is what separates "it
matched" from "it matched once, where I meant it".

The restore half is already habit and the applied half is not. Every agent one
session ran on 2026-08-22 saved a copy and restored it by hash, and not one
confirmed that the change had landed. That is an observation over those runs
rather than a survey of the repository. The reason it is worth stating anyway:
the interesting moment feels like the run, so the patch is what goes unchecked.

The header above names the consequence. One phrasing names the mechanism: a
signal whose shape does not change when the thing it reports changes. Three
spellings of that one property are known. An empty saved log directory reads as
"logs saved". A bare `ok` carries no duration. A `cleared` is written from a
cached run. The last is the worst placed, because it lands in the artifact a
later reader opens to check.

The stale-cache row's reach was measured on 2026-08-22. The boundary needs both
of its halves, or it becomes the next wrong frame.

- It does NOT reach a gate run in a FRESH worktree at a NEW path. `go test` keys
  a cached verdict on the absolute paths in its testlog, so such a run starts
  cold whatever the module and build caches hold. Measured on `./scripts/status/`:
  1.674s then `(cached)` in the main tree, 2.384s in a fresh worktree at the same
  sha with that package byte-identical, then `(cached)` again inside it.
- It DOES reach those same gates run over the shared working tree. There the path
  is stable, so a warm verdict outlives an edit to a producer the test only execs.

The timestamped path in `verify_worktree.py` is load-bearing for a reason nobody
chose it for. Stabilizing it to save the checkout cost re-arms the class.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-23 | interop-image-tag-race | `build_images` (`test/interop/run.py`), the nine image references in `test/interop/interop.py` | The harness namespaced its CONTAINERS per run with a PID suffix and its IMAGES not at all. Every container started from the shared tag `ze-interop`, and a second run's `docker build -t ze-interop` rebinds that tag between the first run's build and its container start, so the first run goes on to test a daemon it never built. Measured twice on 2026-08-05 inside one review: a mutation build PASSED, which is the exact inversion of the expected result, and a second reviewer's runs silently used the first reviewer's mutated image. This is the one entry here that can CERTIFY another vacuity rather than being one. Reverting the behaviour and watching the interop test fail is what `ai/rules/interop-and-goal-validation.md` requires before an interop test may be called discriminating, and that procedure is worth exactly what the guarantee that the container ran the reverted build is worth | fixed. `docker build -q` prints an image ID, `build_images` reads it, and `set_images` points every reference at it, so a concurrent build can take the tag and cannot take the ID. `NO_BUILD=1` still falls back to the tag because a run that builds nothing has no ID to pin. Two probe modes drive both halves and two Go tests assert on argv ELEMENTS rather than substrings, since a volume mount carries a repository path that a substring match would answer. Reverting one reference to the literal tag reddens the first test in 0.26s. Proven against real Docker as well: the 2026-08-23 run built five images and printed a distinct sha256 for each. The four peer images are pinned the same way and the pulled FRR version tag is left alone |
| 2026-08-23 | - | `docker_rm` in `test/interop-ipsec/lab.py`, `test/interop-l2tp/lab.py` and `test/interop-pppoe/lab.py` | Three siblings of one helper, each a bare `subprocess.run` with no returncode read and no exception handling. The same removal runs at two moments that owe opposite answers, and all three answered the same way at both. `Scenario.setup` calls it to PRE-CLEAN, where a silent failure leaves the scenario running beside an earlier run's daemon on the same address, and nothing downstream catches that: a container the scenario starts collides by name and is reported, a stale peer it never starts is invisible, and `docker network create` accepts a network that already exists. `test/interop/interop.py` had carried both contracts since the same defect was fixed there | fixed by mirroring that helper rather than inventing a fourth. The default contract still raises nothing, because teardown runs from the runner's `finally` and an exception there discards the summary of every scenario the suite already finished. `strict=True` denies on all three failure shapes, and `setup` passes it. `scripts/dev/lab_docker_rm_test.py` drives the pre-clean through `Scenario.setup`, its real entry point, for all three labs and asserts the reference lab agrees. Both halves mutation-proven: dropping `strict=True` at the pppoe call site reds three subtests, and deleting the exit-code branch in the ipsec helper reds two and leaves setup running on for 30s to a charon-not-ready. The inline network removal beside it keeps the old silence and is not covered here |
| 2026-08-23 | - | the seven `.ci` in `test/static` and two in `test/vrrp`, `option=needs-linux` with no caps | Each runs `ip link add` from a `tmpfs=setup.py` and declared `option=needs-linux` alone. `caps=net-admin` is what `record_parse.go` documents for that, and without it the parser records no skip, the test RUNS on an unprivileged Linux host, `ip` is refused, the daemon never reaches the asserted state, and the red names a route or an interface rather than the missing capability. The inverse of this file's usual shape: not a green that could not be red, but a red that is not a defect and that misattributes itself. The prompt named two files and a sweep found nine | fixed in all nine, and the class is now checked. `TestCIPrivilegedIPDeclaresNetAdmin` (`internal/test/runner/caps_declaration_lint_test.go`) walks the `.ci` corpus for iproute2 mutations in either the shell or the Python-helper spelling, reads the option LINE rather than searching the file for the token, and refuses a declaration that appears in prose only. Reverting one file's declaration reds it by name. Its bound is stated in the file: it sees iproute2 and not `nft`, `tc` or a raw socket, so it cannot tell a test that also needs net-raw from one that does not. The two vrrp files are one of those cases and were given net-admin alone |
| 2026-08-23 | - | `parseWBOption` (`internal/component/web/testing/parser.go`), `runWBTestCase` (`internal/component/web/testing/runner.go`) | `option=timeout` parsed into `WBTestCase.Timeout` and nothing read it. `pr.Run` (`internal/test/runner/parallel.go`) gives each web test the suite's context and no deadline of its own, so every `.wb` that declared 30s or 90s declared nothing, and a web test that stopped making progress hung the whole suite instead of failing one test. The runner's own doc comment said so, which is the part worth noticing: the defect was written down beside the code for as long as it existed and that changed nothing | fixed. The field is a `time.Duration` now, the parser refuses a value it cannot read instead of falling back to the default, and `runWBTestCase` checks the deadline before each step. That bounds a test at its budget plus one step, and every step is itself bounded by `agentTimeout`, by a retry deadline, or by the millisecond count the file asked for. A zero Timeout means UNSPECIFIED and takes the default, because the only way to reach the runner with zero is a struct literal built in code, and reading it literally failed an existing test the moment the bound was added. Removing the check reds the new test in 0.53s |
| 2026-08-23 | - | `check()` in `test/interop/scenarios/ospf-sr-frr/check.py` and its OSPFv3 twin | Both looked for the label the scenario exists to prove, and on not finding it called `log_info` and fell through to `log_pass`. A scenario that could not find its label reported success. Then the re-run said something the raise alone would not have: the scenario had never run. Steps 1 and 2 pass, the adjacency reaches Full and Ze originates its SRGB and node Prefix-SID, and step 3 cannot be observed at all. `mpls` is not installed in `test/interop/Dockerfile.ze`, and the Docker Desktop kernel exposes no `/proc/sys/net/mpls`, so neither the tool nor the forwarding table exists on the host the Docker harness runs on. `docker_exec_quiet` returns an empty string for a command that could not run, so the readback could not tell a missing instrument from a missing route | the raise is in, in both files, and the failure text now reports the readback, whether the `mpls` binary is present, and whether the kernel has MPLS at all, so the red says which of the three it is. The scenario is RED on this host and it is NOT made green: it proves the ORIGINATION half only, and its docstring now records that measurement instead of claiming the reception half. What it still needs is an instrument a kernel with AF_MPLS can answer, which is a harness question rather than an assertion to relax |
| 2026-08-23 | - | `is_test_path` (`scripts/dev/audit-test-relaxation.py`), `_test_weakening_errs` (`.claude/hooks/pretool-writeedit.py`) | The relaxation audit matched `_test.go`, and `.ci` or `.et` under test/. Nothing else. So the whole tooling suite reported CLEAN however it was weakened, and that suite is the hooks, the commit helper, the rules pipeline, the verify runner and this audit's own tests. A guard that cannot see the tests guarding it. Two halves, and fixing one alone buys nothing: the predicate never admitted a Python path, and the shared detector carried Go and `.ci` shapes only, so a path admitted without the arms would be walked past in silence. The detector already read Python comments and already carried Python tautology arms, so the gap was narrower than it looked and completely invisible | fixed at both. The predicate takes the two file shapes `pythonTestGlobs` discovers, so the audit and the runner cannot name different populations. The detector gains three arms mirroring the Go ones: every `def test_` gone, a skip added in any of unittest's or pytest's five spellings, and an advisory assertion-count drop. Four cases in `scripts/dev/audit_relaxation_test.py`, one of them the bound that a Python file which is not a test stays unjudged. Mutation-proven three ways, and the third mutation found a vacuity in the new test itself: deleting a test function also deletes its assertions, so that case passed on the advisory arm with the function arm removed, and it now asserts the MESSAGE. The hook's own `is_test` is deliberately not widened, because this hook refuses an edit while the audit reports one |
| 2026-08-23 | fixit-redistribute-establishment-stall | `runOrchestrated` (`internal/test/runner/runner_exec.go`), `validatePeerBlocks` (`internal/test/runner/peer_contract.go`) | The guard added to stop one vacuous green prescribed another. `validatePeerBlocks` refuses a check-mode ze-peer that declares nothing it consumes and tells the author to run the peer with `--mode sink`; sinking clears `hasCheckPeer`, `isSelfValidated` then returns true for any record carrying an exit-code or output assertion, and `validateJSON` was inside the branch that predicate gates. So the remedy silently deleted every `expect=json` in the file while the test still reported PASS. One predicate was carrying two questions: does anything validate the wire, and does anything validate the file's own assertions. Measured rather than argued: with the pre-fix gating restored from HEAD, a `.ci` asserting an NLRI ze never announces passes in 2.3s | `validateJSON` moved to the common tail, gated only on `hasJSONExpectations`, because `expect=json` is a runner-side assertion that LoadExpectFile never forwards to ze-peer. `test/runner/json-assertion-survives-sink-peer.ci` pins it and the same mutation is RED with the fix in. The remedy text was left alone: with no consequence to warn about, warning about it would have been the workaround |
| 2026-08-23 | fixit-shutdown-waits-out-a-deadlock | `test/plugin/shutdown-is-prompt.ci`, the spec's only functional proof | The file carried `option=skip-os:value=darwin` with no reason recorded, on a spec whose whole premise is that the defect it fixes is invisible to every gate. Every clean daemon stop waited out a 3.0s plus 0.5s deadlock that two timeouts broke, so nothing hung and nothing was red. The `.ci` written to make that visible reported `pass 0/0 100.0% skip 1` on darwin, which is the primary development machine, so the one signal able to fail could not fail where the work happens. A sixth mechanism for this file: the run was neither absent nor misdirected nor vacuous, it was declared out of scope for this OS. Nothing in the test is platform-specific: the cycle lives in `Reactor.cleanup` and `ProcessManager.Stop`, neither of which has a build-tagged variant, and the daemon it drives runs on darwin like every other plugin `.ci`. Corrected 2026-08-23: this cell claimed "18 of 694 plugin `.ci` files carry the skip and the siblings that carry it have a real reason, `system-cpu-show.ci` needing Linux `/proc` CPU stats". Both halves were false and are struck. The 18 came from an unanchored grep. It matches the string inside comment blocks that DISCUSS the directive. Anchored, `grep -lE '^option=skip-os:value=darwin' test/plugin/*.ci` returns 8. And `system-cpu-show.ci` is the counterexample rather than the good example. The three comment lines above its skip are VALIDATES and PREVENTS about handler shape and a partial map, with nothing about the OS. No replacement count is given, because a count in a row nobody re-measures ages the same way a count in a header does. The durable statement is that the skip directive and its justification have no defined relationship, so neither a reader nor a checker can tell a considered boundary from an unexplained one | fixed in the closure, one line removed. Measured before removing it: 5 of 5 pass on darwin in 1.1s to 2.3s against the file's own 30s budget, and with both ownership guards reverted the same darwin run fails on `reject=stderr:pattern=may be left behind` 3.001s after the shutdown request, with the warning logged twice for `plugins=bgp`. So it passes here and it discriminates here. The file now carries that measurement and says not to skip it here, because the next reader otherwise re-adds the line for the same unstated reason. The general check this file wants: a skip with no recorded reason is a claim that the test cannot run, and on the machine you develop on that claim is one run away from being tested |
| 2026-08-22 | fixit-link-event-drop-loses-carrier-state | the functional-test check in `.claude/hooks/validate-spec.sh` | The check refuses a spec whose Functional Tests section names no `.ci`, `.et` or `.wb`, and its message says a Go unit test is NOT a substitute. It looks for the TOKEN, anywhere in the section, so a sentence about the missing file satisfies it. Measured on this spec: the section named only a Go integration test and the check was a hard error, then a row was added that says the proof is OPEN and names the `.ci` extension while describing its absence, and the same check dropped to a warning. Nothing about the spec's evidence changed. The token also appears in a path, a make target and any prose about test types, so the greenness is available to every spec that discusses functional testing without doing any | not fixed, and the row it went green on says OPEN in its own text, so this spec's record is not the thing at risk. The check should read the spec's declared test PATHS and test that each names a file, rather than matching an extension inside free text. It is the same shape as a doc gate satisfied by a mention: the artifact is asserted, never resolved |
| 2026-08-22 | - | `go test` result cache over an exec'd producer | The cache keys a verdict on the files the TEST BINARY opened. A producer the test runs through `exec` is not one of them, so mutating it leaves the verdict cached and the tool answers `ok (cached)` for a run that never happened. Reproduced independently in two packages on the same day, once over a `//go:build ignore` Go program compiled by the test and once over a Python script the test execs through `python3`. The tell is a bare `ok` with no duration. Worse than a caveat, because the standard discrimination proof is exactly "mutate the producer, re-run, observe red": that proof silently degrades to "mutate the producer, re-run nothing, observe the old green". Earlier reds under this pattern had been real only because each ran under a different `-run` filter, which is a cache miss for an unrelated reason | fixed at the two helpers that own it, using the pattern `scripts/status/verify_run_test.go` already carried. Eleven further exec sites in `scripts/dev` still carry it and are named in `plan/journal/test-cache-stale.md`. The general practice: a test that execs its producer must defeat the cache (`-count=1`) or run outside it, and a discrimination proof must state whether its re-run was cached or real |
| 2026-08-22 | fixit-flowspec-protocol-name-drift | `test/plugin/flowspec-fw-protocol-sctp.ci`, `test/plugin/flowspec-fw-untranslatable-keeps-others.ci` | Both drivers polled for the literal `sctp` in `nft list ruleset`. nft prints the lowered value as `meta l4proto 132` and reserves symbolic names for the protocols its own `inet_proto` table carries, so the predicate never matched the surface it was written against. Each file's `expect` lines had already hedged, accepting the number or the name, so the two halves of the same file disagreed about what the kernel emits and nothing said so. A `reject=` on a digits spelling sat beside them and could never fire either: with the defect present, lowering refuses and `Apply` returns before its `Flush`, so no rule of any spelling reaches the kernel | fixed. Each driver now matches what nft prints, and each `expect` asserts one full rule line carrying every condition the route announced, so a dropped match reddens it as well as a missing one. The unfireable `reject` is replaced by the positive assertion that can fail, with a comment stating why its predecessor could not. Both mutation-proven against the pre-fix translator |
| 2026-08-22 | fixit-child-sa-rekey-policy | `test/interop-ipsec/scenarios/child-rekey` | The suite held a strongSwan Child SA rekey scenario throughout, and it could not fail for the defect the spec fixed. The defect was a responder-role Ze spending the peer's IKE_AUTH id on its own request counter, and in scenario 05 Ze is the connection initiator, whose counter was always right. Every other responder scenario has Ze ANSWERING an exchange charon started. No scenario had a responder-role Ze speak first, so a wire-visible change shipped with the interop box ticked and no scenario that reverting it reddens | fixed by adding the missing direction, `responder-raises-child-rekey`, mutation-verified in a throwaway worktree: with the old counter restored, charon parses no REKEY_SA request at all. The general practice: an interop scenario that EXISTS for a feature is not one that FAILS for the defect, and which role the daemon plays decides which defects the scenario can see |
| 2026-08-22 | - | `_observer_excepthook` (`test/scripts/ze_api.py`), the observer relay | `test/plugin/attach-process-reload.ci` fails deterministically, 4 of 4 standalone, and the only thing that reaches the runner is `ZE-OBSERVER-FAIL: uncaught observer exception: json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)`. The traceback IS produced: `_observer_excepthook` writes it to the observer's own stderr. The relay carries only the sentinel, so the throwing call never reaches anybody who could act on it, and the observer's last printed checkpoint is the only bound on where it threw. This is the one row here whose failure is REAL rather than a green: the defect is visible, its location is not | not fixed, and the sequencing is diagnostic first. Carry the traceback through the relay, then read the defect it names, rather than guessing at a decoder that is handed an empty string. It was also mishandled before it was understood: it was called a known pre-existing red across seven agent briefings, and pre-existing is not a disposition. `ai/rules/completion.md` reserves the recording route for a failure actively tried and NOT reproducible, and this one reproduces every time |
| 2026-08-22 | fixit-vacuous-functional-tests | `test/plugin/task-forbidden.ci` | `reject=stderr:pattern=does not support task-augmented calls` cannot fire on the current build. It was live once. A 2026-07-29 build refused a task-augmented call with that exact text. The cutover to the server-directed model then replaced the refusal with a revert. `callTool` (`internal/component/mcp/streamable_tools.go`) now reads `lookupTaskSupport` only for `TaskSupportRequired` and carries no TaskSupportForbidden branch at all. The phrase survives in one doc comment (`TaskSupportLevel`, `internal/component/mcp/tools.go`) and in no format string, so no build can put it on either stream. An assertion that outlives the behavior it names is the shape here: the reject stayed green because it stopped being about anything. The same file had already been corrected once for the same shape, a `code -32021` spelling only stdout could carry, which is what makes this a class rather than a slip | fixed. The dead line is gone and its claim is carried by the live pair beside it: the probe answers `code=ok` with a result, and that result spells `"resultType":"complete"` and no taskId. A comment states why the removed spelling could never match. Found by a mechanical sweep over all 616 `reject=stderr` and `reject=stdout` sites in the `.ci` corpus, 107 distinct non-boilerplate literals, each longest literal run checked for a producer outside an assertion line. It was the only survivor. No gate does this today |
| 2026-08-22 | - | `main` and the new `save_logs` (`scripts/dev/verify_worktree.py`) | The runner printed `verify-worktree: logs under <wt>/tmp/verify/` and then removed the worktree in its `finally` block, so the stage logs of a 25-to-53-minute red were destroyed before any reader reached them. The message named a path that no longer existed, and its advice to re-run with `--keep` only helps somebody who already knew the run would go red. Two sessions hit this on the same day, and one recovered stage detail only because it had redirected the whole run into its own scratchpad by hand. This is the same shape as the observer-relay row above rather than a false green: the reason is produced and then discarded, so the failure is real and merely unattributable | fixed. `save_logs` copies `tmp/verify` out to `tmp/verify-worktree-logs/<stamp>-<sha12>` before the removal, on red runs only, because an hour of stage output from a green run changes nothing about what the operator does next. A run that died before its first stage prints a distinct line, so an empty directory never reads as logs saved. Proven to discriminate rather than asserted: replacing the `save_logs` call with `None` failed exactly the end-to-end red test and nothing else, and the file was restored byte-identical afterwards. Covered by `scripts/dev/verify_worktree_test.py`, which `python_tests_test.go` globs into `make ze-unit-test` with no further wiring |
| 2026-08-22 | - | `make ze-lint-changed` over a partially reclaimed `GOCACHE` | Sharper than the rest of this file, because the two channels actively DISAGREED rather than one being absent. The first bare run after a cache reclaim printed `0 issues.` on stdout and exited 7. golangci-lint's own index still referenced entries the clean had removed, so it emitted `typechecking error: open cache/go-cache/fc/fc408e...-a: no such file or directory` and then reported a clean count anyway. Every row above has a green that carried no information; this one has a green sitting beside a red, in the same output, from the same command, and only the exit code was true. Anything reading the printed count rather than the status would have recorded it as clean. The second half is why it is worth a row rather than a shrug: it HEALED on the next run, and a defect that heals is the one most likely to be dismissed as noise by whoever meets it next | not fixed, and it is upstream rather than ours. What is ours is the reading habit: a tool's printed summary is a claim, its exit status is the verdict, and a wrapper that judges one while reporting the other will pass a broken run. Anything in this repository that shells out to a checker takes the status. The related toolchain fault the same reclaim exposed IS fixed, by deriving `GOTOOLCHAIN` from the `go.mod` toolchain line in the `Makefile` rather than leaving each caller to pass it |
| 2026-08-22 | fixit-ike-resource-lifetime-leaks | closure review, discrimination by source mutation | The MUTANT read green and that green carried no information, so it nearly became a vacuity BLOCKER against a test that is in fact sound. `TestStopWaitsForAPluginReleaseThatOutlastsItsReadLoop` (`internal/component/plugin/process/manager_stop_wait_test.go`) sleeps 700ms in its plugin engine and its comment says that is "longer than the 500ms this wait used to run for". The reviewer reverted by setting `pluginStopGrace` back to 500ms, the test passed, and the obvious reading was that the margin is fake. It is not. The fix ADDED a stage. `ProcessManager.Stop` had ONE wait of 500ms, and it now runs an engine wait of `pluginStopGrace` followed by that same 500ms group wait. Restoring the old constant leaves the new stage in place. The effective budget becomes 1000ms, which no 700ms release can exceed. The true revert is grace 0, where the effective budget is the residual 500ms, and the test reddens there in 0.50s | fixed in the reviewer rather than in the tree, because the test was already right. The general practice: when a fix adds a STAGE in front of an existing one, restoring its constant does not revert the change. The mutation must remove the stage, or drive the constant to the value that makes the stage inert. Name the effective budget before you pick the mutant. A mutation that cannot redden reports vacuity that is not there, and a false BLOCKER costs a closure as much as a missed one |
| 2026-08-22 | - | a discrimination proof over `message_text` (`scripts/dev/commit_helper.py`) | The proof mutated nothing and reported a pass. The patch script raised `substring not found` from `str.index`, wrote no file, and exited before its own report line. The test then ran against UNMODIFIED source, passed, and the runner printed `probe exit=0`, which is byte-identical to what a real proof prints. This is worse than the stale-cache row above rather than another instance of it. A cached verdict at least ran once against real code. Here nothing was ever tested. It also defeats the habit this file prescribes for the other four, because the break WAS attempted and the attempt is what failed. Found only by reading the output rather than the exit code, and the tell was an unrelated traceback sitting above a green | fixed in the proof, not in a tool. The second attempt patched by slicing between two located anchors. It asserted the marker count was 1 before it ran the test. Exactly the two tests that assert the new content then failed, and the boundary test stayed green, which is the correct split. The guard is now in the header above: verify that the mutation APPLIED, with a non-empty diff or a grep for the mutated text, between the patch and the run. Nothing enforces it. Every session here saves a copy and restores by hash already, so the restore half is habit and the applied half is not |
| 2026-08-23 | fixit-link-event-drop-loses-carrier-state | `test/plugin/iface-link-flap-during-commit.ci`, and AC-5's stated observable | A LATER phase of this spec repaired the thing an EARLIER phase's acceptance criterion asserts. The criterion stopped being able to fail. AC-5 says the default route metric matches live carrier after a commit. Phase 3 then added the carrier resync, which compares acted-on metric state against live carrier on every rate-tracker tick. It repairs a contradiction inside a second (`resyncCarrierState`, `internal/component/iface/link_queue.go`, fed by `SubscribeCollectNotify` in `register.go`). Measured on the arm64 QEMU VM. The queue was restored to the pre-fix 16-deep drop-on-full channel, and six rounds of 101 carrier transitions during real commits still ended with the route at the right metric in every round. The only difference from HEAD was that `ze_iface_carrier_resyncs_total` had risen by 4. A test asserting AC-5's own words would have passed against the defect it exists to catch. Two further measurements bound such a test, and neither is about the queue. A SIGHUP over an UNCHANGED config never reaches the iface component's apply, so it holds `dhcpMu` for no time and stalls no worker: 96 signals, zero applies. Above about 400 transitions the kernel's own netlink socket drops notifications before ze sees them, which makes any no-loss assertion judge the socket | fixed in the test rather than in the product, because the self-heal is wanted. The `.ci` asserts three things. The metric reaches 254 plus 1024. `ze_iface_carrier_resyncs_total` never moves, which is the fact the two producers differ on. And `ze_iface_link_events_coalesced_total` rose, so a drain that later keeps up cannot make the run vacuous in silence. It reads `/proc/net/netlink` and fails when the kernel dropped. It flips a DHCP hostname before each SIGHUP so the commit is a real change. Six rounds, because the pre-fix producer loses the final transition in about two rounds in three: a burst ending leaves the buffer room again, so one burst is not a proof. Red and green measured both ways, 39.9s green at HEAD and red on the mutant. The general practice: when a spec adds a self-heal, re-read every acceptance criterion written before it. Ask whether the self-heal now satisfies it. Where it does, the criterion's test must also assert that the self-heal did NOT have to fire |
| 2026-08-23 | record-answers-3-zero-alloc | `TestShowPipelineWalkSurvivesConcurrentUpdates`, the test written for exactly this defect class | Converting `show bgp rib` to stream made each source take `peerMu` for itself inside `Next`. `showProtocolPipeline` still held `peerMu` across its whole drain, so the two nested a reader inside a reader. Go's `RWMutex` makes a later `RLock` wait behind a QUEUED WRITER, so nested readers deadlock only when a writer arrives between them, and never otherwise. Every unit test passed, the package was green under `-race`, and the concurrency test that exists to catch a wedged walk passed too: its writer does not happen to arrive in that window. In production the first UPDATE during a dump would have wedged the walk and the writer together. What found it was `unused: protocolInboundSource.release`, a lint finding about DEAD CODE, which is the only signal that pointed at the function at all | fixed by giving `showProtocolPipeline` the same structure as `showPipeline`: `peerMu` for the map reads and source construction, released before the drain. Two functional tests, `prefix-teardown-holds-peer-down` and `prefix-teardown-reconnect-backoff`, had been failing and cleared with it, which is the only reason the defect had any external signal at all. The general practice: a concurrency test proves the interleavings it PRODUCES, and a lock-ordering bug whose window is "a writer queues between two reader acquisitions" is not one a test produces by luck. Reading the lock structure is what catches it, and a tidiness finding is sometimes the only thing that makes you read it |
| 2026-08-23 | - | `make ze-verify-worktree` read through a background-task notification | The gate died mid-run on `no space left on device`, inside `ze-staticcheck-feature-matrix-check`, and then could not write its own stage log either. `make` reported Error 2. The harness notification that surfaced the run reported **exit code 0**, because that is the status of the OUTER shell the command was launched in, not of `make`. I read the notification, said the gate had passed over the commit, and believed it for about ten minutes. Distinct from the `ze-lint-changed` row above rather than another instance of it: there the tool printed a summary that disagreed with its own exit status, so both channels came from the same process and one was true. Here the status was real, accurate, and belonged to a DIFFERENT process from the one whose verdict I wanted. No output was wrong; the wrong thing was read | fixed in the reading, and nothing was pushed on the false green. The general practice: a wrapper's exit status is the wrapper's, and a gate's verdict has to be read from the gate's own last lines. The run's tail said `ze-precommit-verify exit=2` two lines above the notification's 0. The tell that broke it was unrelated: I only re-read the output because the disk had filled elsewhere. Worth pairing with the disk finding it exposed, which is that `mk/schedule-cadence.mk:76-79` cleans both Go caches, in the right order, with a comment naming the exact trap that `go env GOCACHE` from a shell resolves to the user cache while make resolves to the repository's, and NOTHING schedules it: no crontab entry, no LaunchAgent. The verify run that would have said what failed was the casualty of a cleanup that exists and never runs |
| 2026-08-23 | - | `TestPipeHelpNamesEveryOperator` (`cmd/ze/pipe_surfaces_test.go`), written for spec-cli-pipe-operator-coverage phase 1 and caught while it was still being written | The test asserts that every operator in the catalog is NAMED by `ze pipe help`, and its first draft asked with `strings.Contains(out, op.Name)`. It reported `raw` as published while `raw` was absent from that page, because `raw` is a substring of nothing there but `json` is a substring of `ndjson`, and the same weakness covered every name that sits inside another. The test ran, the assertion evaluated, and for those names it could not have distinguished present from absent. It was caught only because the surrounding red was being read name by name rather than as a count: six names were reported missing and I expected seven | fixed before the test was committed, by matching WORDS rather than substrings: the page is split on anything outside `[A-Za-z0-9-]` and the operator name is looked for among the tokens. Re-running then reported `raw` missing too, which is the truth. The general practice, which is the transferable half: whenever a test asserts that a NAME is published, it must match the name as a token. A containment test over a set of names is sound only if no name is a substring of another, which is a property of the data and not of the test, and nothing checks it. The same applies to a `.ci` `expect=` pattern over a name |
| 2026-08-23 | record-answers-3-zero-alloc | ze-alloc-check, the ceiling registry | `AllocCeilings` (`internal/perf/allocgate.go`) is the number the gate compares against, so raising it is the cheapest route from a red gate to a green one: cheaper than removing the allocation, and invisible in the gate's own output, which prints the measurement and never the ceiling it passed. Measured by mutation: `BenchmarkRecordAnswerRows` moved from 0 to 5 left `make ze-alloc-check` green, `go test ./internal/perf/` green, and `scripts/dev/audit-test-relaxation.py` silent, because the audit reads assertion counts in test files and a map literal is neither. `TestAllocGateCoversRecordPath` was the one check written to keep the registry honest and it asserted only that an entry EXISTS. The entry's whole point is that it was registered RED as a goal a phase before the optimization landed, and nothing held it to that goal afterwards | fixed for this spec's entry. `recordPathCeiling` (`internal/perf/allocgate_test.go`) spells the goal beside the registry and `TestAllocGateCoversRecordPath` fails when the two disagree, proven by moving the registered value to 5 and watching it red by name. It is deliberately ONE entry rather than a general ratchet over the map: the other ceilings are measurements with headroom, where a raise can be the honest answer, and this one is a goal, where it never is. The general practice: a gate that reads a tunable and prints only the measured value needs a second reader for the tunable, or the tunable is the escape hatch |
| 2026-08-23 | fixit-fwdpool-backpressure-timing | `TestFwdPool_BackpressureBehavior` and `TestForwardPoolBackpressurePropagation` (`internal/component/bgp/reactor/forward_pool_test.go`) | Both proved backpressure with `require.Never` over "the fourth dispatch has not returned yet", polled for 100ms. That is the same green a dispatcher the scheduler never ran produces, so the assertion could not tell a parked dispatcher from an unstarted one. Measured by mutation: with `fwdPool.Dispatch`'s blocking send replaced by a dropping `default:` arm, and the fourth dispatch delayed 150ms, both pre-fix tests PASS in 0.15s with backpressure deleted. The reported symptom was elsewhere and hid this one. Each test also carried a 2s `require.Eventually` on completion, and that is where the flake landed: 4 failures in 43 runs of `-test.count=20` under 32 CPU burners on 16 cores at `GOMAXPROCS=2`, each at 2.10s to 2.17s, which is the budget expiring while the code behaves correctly. A red at the `require.Never` also hung the whole package to the `go test` timeout, because the handler was released by a bare close on the success path only | both waits now read state the product already keeps. The park wait polls `w.pending.Load() == 1` against a full `w.ch`, which `fwdPool.Dispatch` raises before the blocking send and lowers in both arms of the select after it, and a non-blocking receive then states that the dispatch has not returned. The completion wait is a bare receive on the dispatch goroutine's own channel and carries no deadline. `forward_pool.go` was not modified, because `TestFwdPool_StopUnblocksDispatch` already read `pending` that way. Re-measured at closure: the rewritten pair FAILS in 1.00s under the same mutation, with and without the 150ms delay, and 43 post-fix runs under the same load reported 0 failures in 860 executions. The general practice, which is the transferable half: `require.Never` over "X has not happened yet" asserts nothing whenever the thing that would make X happen may not have started. Assert the state that says the subject is BLOCKED, never the absence of a result |
| 2026-08-23 | - | `TestLenMatchesWriteTo` (`internal/core/bgp/attribute/len_writeto_test.go`), the invariant guarding AGGREGATOR, AS4_AGGREGATOR and ORIGINATOR_ID | The invariant compares the COUNT a size query reports against the COUNT the write returns, and for these attributes both are the same literal, so it stayed green while the write overran its declared region by twelve octets. `(*Aggregator).WriteTo` copied `a.Address.AsSlice()` into a field RFC 4271 Section 5.1.7 fixes at four octets, and returned 8 whatever it had copied. An IPv6 Address clobbered the next twelve octets of the attribute block, and the test could not see it, because a count-only invariant is blind to where the bytes LANDED. Measured during this fix: reverting it left every AGGREGATOR row of that table green | The write is bounded to the declared width. The new tests fill the buffer with a canary, write at a non-zero offset, and assert the canary survives on BOTH sides of the region. A size-against-size assertion needs a companion that reads the bytes outside the region, or it certifies nothing about the write |
| 2026-08-23 | fixit-config-list-readers-assert-the-wrong-shape | `TestParseConfigLeaves` and `TestParseConfig_StringValuedDelivery` (`internal/plugins/anomaly/shape/config_test.go`), `TestParseNamedPoolConfig` and `TestParseNamedPoolMissingName` (`internal/component/l2tp/plugins/pool/pool_test.go`), `TestParseNamedIPv6PoolConfig` (`internal/component/l2tp/plugins/pool/pool_v6_test.go`) | A fixture built by hand in a shape the producer never emits certifies a reader production cannot reach. `(*Tree).ToMap` collapses a one-member leaf-list to a bare string and renders a YANG list as a map of list key to entry at every count. Every fixture above wrote a JSON ARRAY instead: an array of one for the leaf-list, an array of entries each carrying a name field for the list. So the readers asserted `[]any`, agreed with their own fixtures, and discarded the operator's value in every real config. The allowlist one is a security defect: one allowlisted prefix armed a shaping action against the network it protected. `pppoe.ExtractParameters` had already recorded this exact mechanism in its doc comment, about the same producer, and it did not stop the next four | Five readers now call `configvalue.LeafList` or `configvalue.ListEntries` (`internal/core/configvalue`), and the five local coercion spellings were deleted so no wrong model survives to be copied. Every corrected fixture uses the producer's shape. A fixture is a second claim about the producer, so derive it from the producer or assert the producer's contract separately: `TestToMapLeafListMemberCountShapes` (`internal/component/config/tomap_shape_test.go`) is that assertion |
| 2026-08-23 | - | `parsePrefixListEntries` (`internal/component/bgp/plugins/filter_prefix/config.go`), `parseAsPathEntries` (`filter_aspath/config.go`), `parseCommunityEntries` (`filter_community_match/config.go`), and the one multi-entry case in each of their `config_test.go` | Each reader takes a slice of entries or refuses more than one. Their only multi-entry test passes `"entry"` a JSON array, and no producer emits that: the chain is `(*Tree).ToMap` then `ExtractConfigSubtree` then marshal then `configjson.ParseBGPSubtree`, which is a plain unmarshal, so a list arrives as a map of key to entry at every count. So the slice branch is unreachable and the map branch refuses two or more entries to protect first-match-wins order. A prefix-list with two entries therefore cannot load, and only the array fixture ever exercised the multi-entry path. The root is upstream of all three: `entry` is declared `ordered-by user`, `ToMap` lowers every list to an unordered map, and eight other lists share that declaration, `firewall` `term` and `element` and the `server` list of `radius`, `tacacs` and `authradius` among them. Both tests that RUN the plugin, `test/plugin/policy-list-show.ci` and `test/interop/scenarios/bgp-policy-import-export-frr`, use exactly one entry | Not fixed here. Sorting the map by key would substitute a lexical order for the configured one and turn a loud refusal into a silently wrong evaluation order, and deleting the unreachable branch would delete the only multi-entry coverage. The fix belongs at the producer, which must carry an ordered list's order through lowering. Sorting the map instead is not theoretical harm: `serverEntries` (`internal/component/l2tp/plugins/authradius/config.go`) already sorts a keyed map by name for what its comment calls deterministic failover ordering, over a `list server` declared `ordered-by user`, so RADIUS failover follows alphabetical order rather than the operator's |
| 2026-08-23 | fixit-vrrp-parent-ignores-the-selector | `fakePlatform.parentIfindex` (`internal/plugins/vrrp/engine_test.go`) | The fake returned a constant 2 for every parent. The real one answers `iface.Resolve(parent).Ifindex`, which differs per device, and `deviceName` composes the macvlan name from it (`ComposeOwnedDeviceName`, `internal/plugins/vrrp/engine.go`). So under the fake two DIFFERENT parents produced the SAME macvlan name, and a device MOVE looked like a device replacing itself. Every engine test was well formed, reached the code and asserted a real thing; the double had removed the only property that distinguishes two parents, which is the one the move tests are about. It was harmless for as long as `teardown` preceded `build` in the move path, and it would have become wrong the instant that order was reversed to fix review finding R2-1: the predecessor's teardown deletes by device name, so it would have deleted the REPLACEMENT's registry entry under the shared name, and the move test would then have failed for a reason that exists only in the fake. Found while reversing that order, not by a failing test | fixed: the fake allocates each parent its own index on first sight, as the real one does. No assertion referenced the old constant, so nothing changed meaning; what changed is that a move can be REPRESENTED at all. The lesson generalises past this fake: when a double returns a constant, ask what the real collaborator's answer depends on, and whether the test's two cases differ in exactly that |
| 2026-08-23 | fixit-vrrp-parent-ignores-the-selector | `fakePlatform.parentIfindex` again, its ERROR arm this time | THIRD instance of the row above in one session, and the recurrence is the point. The real `parentIfindex` errors whenever `iface.Resolve` does, which is the entire transient class: no backend loaded, a failed interface listing, a selector answering nothing this pass. The fake could not error at all. A review round then asked for a branch keyed on exactly that failure, and the branch shipped with an error arm nothing could exercise -- it folded the failure in with the rename case and adopted a parent the instance's macvlan was not on, which points `parentReady` at an absent device and resigns the master. Same shape all three times: round 3 the fake could not express a RENAME, round 4 it could not express a FAILURE, and both times the code change was written before the double could describe the situation that code creates | fixed: `ifindexErr` beside the existing `macvlanErr`, and the test was written and watched FAIL before the fix went in rather than after. The lesson is an ordering, not a fact: before writing a branch, ask what the double returns for the two inputs the branch distinguishes, and if the answer is the same for both, fix the double first. Three instances in one session says the rule is right and the habit is not automatic yet, which is worth counting rather than hiding |
| 2026-08-24 | fixit-eap-tls-escape-hatch-kills-the-daemon | `is_test_path` (`scripts/dev/audit-test-relaxation.py`), interop scenario checks | The relaxation audit admits `_test.go`, `.ci` and `.et` under `test/`, and the two Python test-file shapes `pythonTestGlobs` discovers. No interop scenario `check.py` matches any of the three: they are named `check.py`, so neither the `_test.py` suffix nor the `test_` prefix arm sees them. Four whole suites are outside the population -- `test/interop`, `test/interop-ipsec`, `test/interop-l2tp`, `test/interop-pppoe` -- and those are the tests with a foreign peer, which is to say the only tests that can see a wire-form defect two copies of ze would share. The repository has already been bitten there twice: the row in `plan/journal/balance-assertion-vacuous-without-a-loan.md` records both EAP-TLS scenarios stopping at `wait_xfrm_sa` and calling `log_pass`, and scenario 04's own check carried an `except (AssertionError, Exception): log_pass(...)` that reported success whatever the dataplane did. The 2026-08-23 row above widened this same predicate to Python and closed the tooling half; the interop half stayed open because the file-shape rule it borrowed does not describe how these files are named | not fixed. It blocks no goal of the spec that found it: this scenario's discrimination was proven by mutation instead, with `eapTLS12ExportRefused` reverted to the bare wrap and the scenario watched go red. The repair is one arm in `is_test_path` admitting `check.py` under an interop scenario directory, and the question it opens first is whether the shared detector's `.ci` assertion-count arm says anything useful about a Python file whose assertions are `assert`, `raise` and a helper that raises. The general practice: a population rule written from file NAMES misses a suite that names its files by ROLE |
| 2026-08-23 | - | a grep whose pattern was never proven able to find, reported as absence | Three zeros in one session, each reported to a peer or to the owner as fact, and each from a pattern its own author composed. A journal row was called missing on a search for `harness notification`, `outer shell` and `wrong process`, all invented, while the row said `background-task notification`. A second session withdrew a CORRECT statement on the strength of that report. A `skip-os:value=darwin` population was reported as 18, from a grep matching the string anywhere, comment blocks that DISCUSS the directive included. Anchored to `^option=` it is 8. A commit gate was reported to name its remedy and not its signer, on a search for `add a row`, while the refusal says `The OWNER approves this, never the author`. A search that cannot match is a zero that could not have been a hit, and nothing about the zero says which one you have | the point `evidence/claims-about-project-state/prove-a-search-can-find-before-you-report-a-zero` requires the pattern to be run against a case known to exist before a zero is reported as absence. That control is this class's own habit of breaking the thing a signal judges and watching it go red |
| 2026-08-24 | - | cross-session correction about a hook symbol | **FOURTH instance, and the first from a second session, which is what makes it a class rather than one agent's habit.** A peer was told that `_SKIP_PAT`, the Go skip arm of `_test_weakening_errs`, is applied with no path gate and so fires on `.py`, `.ci` and `.et`. It searched `scripts/dev/*.py`, found no such symbol, and reported that the arm therefore was not in HEAD and the claim must belong to unlanded work. The refusal to endorse a symbol it could not open was correct. The conclusion drawn FROM that failure was not: "I could not find it" and "it is not there" are different statements, and only the first had evidence. The search could not have found it, because there is exactly one detector and `scripts/dev/` does not hold it: `audit-test-relaxation.py` and `check_weakened_tests.py` each IMPORT `_test_weakening_errs` from `.claude/hooks/pretool-writeedit.py`, which the audit's own docstring says is deliberate, "so the audit and the hook can never drift apart". So the design that made the search fail is the same design that made the original claim true. Settled by one command, `git show a7b3cbd87~1:.claude/hooks/pretool-writeedit.py`, which shows the ungated line at :2652 before either hook commit that night | not a code fix: the row exists to count the recurrence. The rule is `ai/rules/evidence.md`, "prove a search can find before you report a zero", whose own originating case moved into this file the same day. The control it asks for costs one command here: run the pattern against a case you KNOW exists, or name the file you expect the symbol in and open it. The sibling failure is in `plan/journal/reference-checked-claim-unchecked.md`, where the recipient of a correction adopts it without checking; both ends of one exchange met their own half of it, and both were caught by opening the producer |
| 2026-08-24 | fixit-vpp-lcp-reachability | vpp lcp doctor check registration test | `TestVPPLCPPluginCheckIsRegistered` was named for CHECK registration, and its failure messages talked about it, but its body asserted only against the diagnostic CODE registry through `RegisterBuiltinCodes` and `Lookup`. `internal/core/diagnostic/codes.go` registers the code independently of `registerDoctorChecks` (`internal/plugins/iface/vpp/doctor.go`), so deleting the `vpp-lcp-plugin` entry left the test green while `ze doctor` would never run the check. The green was structural: no state of the registration could change it | fixed: the test locates the entry in `diagnostic.DoctorChecksForPhase(DoctorPhasePostConfig)`, asserts its `Codes`, and CALLS `entry.Check` with a fake probe, so a check swapped for another function is caught as well as a missing registration. Mutation-proved both ways. The habit: a registration test asserts against the registry the RUNNER reads, and a second registry that happens to carry the same name proves nothing about the first |
| 2026-08-24 | - | diagnosing a held `.git/index.lock` | **The eighth mechanism, met live: a true statement about a different question, which cost four sessions an hour of blocked commits.** A zero-byte `.git/index.lock` blocked every session. Three signals said orphaned, and all three were CORRECT: `pgrep git` found no git process anywhere on the box, `fuser` and `lsof` reported no holder, and zero bytes is what a lock that was created and abandoned looks like. The owning session then answered "it is mine and the process is ALIVE, do not remove it", which was TRUE and IRRELEVANT. Its `ze-verify-debt-clear` was alive, and queued behind another session's job in `ze-run.sh`'s admission queue: **a queued process holds no file descriptor.** What had actually happened was that a git invocation inside that run created the lock at 14:10:19 and died on ENOSPC seconds later, with the run's own log carrying `compile: writing output: no space left on device`. The run survived the stage that died. So "my process is alive" and "my process holds the lock" were two different claims sharing one subject, and the second was never checked | not a code fix; the row exists because the reasoning is reusable and both parties got it wrong in turn. **The diagnostic that works is to ask who owns the WORK, not who holds the FILE**, here `pgrep -af debt-clear`. The trap generalises past git: any lock held by a process that shells out in BURSTS is unowned between bursts, so a holder check samples an instant and reports it as a state. The sharper lesson is about the override: three independent signals agreed and were talked down by a fourth that was true, confidently asserted by the one person with standing to know, and about something else. A claim from the owner of a thing is evidence about the thing they own, never about the question you asked. Verify the LINK between them, which is the step nobody took for an hour |
| 2026-08-24 | spec-website-asciinema-terminal-demos | `demos/terminal/render_test.py`, discovered by `TestPythonUnitTests` (`scripts/dev/python_tests_test.go`) | Two assertions that no runner ever evaluates. The file holds `test_definition_digest_changes_only_for_vhs_definition` and `test_definition_check_ignores_non_vhs_source_digest`, both pytest-style: they take the `tmp_path` fixture and the file carries no `if __name__ == "__main__"` block. `TestPythonUnitTests` runs each match as `python3 <basename>` and judges the exit code alone, so this file defines two functions, calls neither, and exits 0. Measured 2026-08-24: `python3 demos/terminal/render_test.py` prints nothing and exits 0. Pytest does not reach it either, because the one pytest invocation in the build globs `website/tools/test_*.py` (`Makefile` target `ze-site-generate`). The sibling `demos/terminal/test_render.py` in the same directory is unittest-style with `unittest.main()` and does run, which is what hides this: the root contributes real coverage, so the empty-root guard stays green | fixed at both ends on 2026-08-24. The two cases are unittest methods of `DefinitionDigestTest`, with `tempfile.TemporaryDirectory` in place of pytest's `tmp_path` and every assertion preserved; both pass. The root cause is that an exit code cannot tell "every test passed" from "no test ran", so `pythonTestRunGap` (`scripts/dev/python_tests_test.go`) now reads the count out of the run's own output and requires it to EQUAL the count of `def test...` the file declares. A file whose run legitimately reaches more cases, a mixin base class or a case built at run time, declares that with a `# python-tests: generated-cases: N` comment line, whose N the run must equal; no file in the corpus needs one. One reporter shape is read, unittest's `Ran N tests`, and `test/interop/speaker/test_engine.py` prints that line from its custom `_main` rather than the gate carrying a second shape for it. Proven to discriminate by restoring the pytest-style file: the gate fails it by name in 0.04s. The judgement is a pure function of two byte slices, so `TestPythonTestRunGap` drives it with the SHAPE of a file rather than with a file; a real `*_test.py` written under `pythonTestRoots` to prove this would be globbed by every other session running the gate |
| 2026-08-24 | spec-website-asciinema-terminal-demos | two classes named `TestRealTree` (`scripts/dev/rfc_requirements_test.py`, lines 7951 and 12592) | Found by the gate in the row above, on its first run over the corpus. Python binds a class name like any other, so the second definition replaced the first and took its one case with it: `TestRealTree.test_all_summaries_parse_or_report` was collected nowhere from the day the RFC 7296 pilot class landed. The file reported `Ran 820 tests` against 821 declared. Nothing about that output looks wrong, which is the point of the class this file collects: 820 passing tests is a fine-looking number, and the missing one is invisible without something that knows how many the file offers. A duplicate METHOD inside one class shadows the same way and reads the same way | fixed on the spot, because it reds the gate this session added. The first class is `TestRealTreeSummaryCorpus` now, named for its subject rather than for the tree it reads, and 821 of 821 pass in 81s. Its docstring was corrected in the same edit: it claimed every summary must parse, and the body counts files. Where the parse claim IS asserted is now named in the docstring (`TestRealTreeIsGreen.test_run_check_exits_zero_on_the_real_tree`, which runs the whole gate over the committed tree). Proven to discriminate by restoring the duplicate name: `declares 821 test(s) but ran 820` |
| 2026-08-26 | - | QEMU hugepage evidence, the kernel command line assertion | `boot_and_assert` (`scripts/evidence/effective-vpp-hugepages-qemu.py`) tests three required kernel arguments with `if arg not in cmdline`. It searches the command line as one string. `hugepagesz=2M` is a substring of `default_hugepagesz=2M`. Thus, the first argument's presence always satisfies the second assertion. A kernel-argument path can emit the default and omit the per-size argument, but the test still passes. The gate exists to prove this exact derived-instance-config path. On 2026-08-26, `default_hugepagesz=2M hugepages=64` reproduced the false pass. The third argument has the same defect because `hugepages=64` is a substring of `hugepages=640` | fixed in the port only (`hasToken`, `qemu/boot.go` in the former top-level tool tree). Each required argument must be a whole whitespace-separated kernel command-line argument. `TestAKernelArgumentIsMatchedAsAWholeArgument` (`qemu/boot_test.go` in the former top-level tool tree) pins both traps. The script keeps the defect until the swap step of the le migration. `TestThePortRefusesTheCmdlineTheScriptCannotJudge` (`scripts/evidence/hugepages_parity_test.go`) verifies that the script still accepts the partial command line. Thus, the test becomes red if somebody repairs the Python |
| 2026-08-26 | - | L2TP PPP evidence, the negotiated address assertion, in TWO scripts | Two more evidence scripts have the same substring defect as the row above. Their ports exposed it. `verify_ppp_address` exists in `scripts/evidence/effective-l2tp-ppp.py` and `scripts/evidence/effective-gokrazy-l2tp-ppp.py`. Each function tests both negotiated endpoints with `if local not in out or peer not in out`. It searches the output of `ip -o addr show dev pppN` as one string. The proof's own address pool defeats the test. Its gateway is 10.100.0.1, and the pool contains 10.100.0.2 through 10.100.0.10. Thus, an interface with 10.100.0.10 satisfies the gateway assertion. Both endpoints have this defect, so the wrong pool endpoints can pass the complete check. On 2026-08-26, `10.100.0.10 peer 10.100.0.20` reproduced the false pass and an OK result. The general rule is short: dotted-quad addresses and kernel arguments are tokens, not substring-safe text | fixed in the port only (`addressPresent`, `deployment/pppstate.go` in the former top-level tool tree). Each address must be a complete whitespace-separated field, either bare or with its prefix length. This is how `ip -o addr show` writes the endpoints of a peer pair. `TestAnAddressIsMatchedWholeRatherThanAsASubstring` and `TestTheAddressAssertionNeedsBothEnds` (`deployment/pppstate_test.go` in the former top-level tool tree) pin both traps. The script keeps the defect until the swap step of the le migration. Two parity tests verify that each script still accepts the shifted pair. They are `TestThePortRefusesTheAddressPairTheScriptAcceptsAsASubstring` (`scripts/evidence/l2tp_ppp_parity_test.go`) and `TestTheAppliancePortRefusesTheAddressPairTheScriptAcceptsAsASubstring` (`scripts/evidence/gokrazy_l2tp_parity_test.go`). Thus, either test becomes red when somebody repairs its Python file. Three instances in two days across three scripts identify a script-wide habit instead of one author's mistake. When you port a remaining evidence driver, grep for `not in` over command output. Do this before you trust its result |
