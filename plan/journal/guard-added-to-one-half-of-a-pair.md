| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-10 | fixit-route-removal-protocol-blind | iface netlink routes | the delete side now matches on rtm_protocol, so it removes only routes the interface layer installed. The add side cannot: `RouteReplace` matches on destination, metric and table and takes no protocol, so `(*netlinkBackend).AddRoute` still overwrites an operator's `proto static` default that shares that key, and re-stamps it 253. Measured in an ephemeral netns: `ip route replace default via 192.0.2.2 metric 5 proto 253` turned a `proto static` route into a `proto 253` one. The link-up handler in `internal/component/iface/register.go` runs that replace, so a bounce hands ownership of a static default to the interface layer, and the NEXT lease expiry then deletes it legitimately | not fixed. The cause is narrower than first recorded: the two routes collide only because both land at metric 0, since `route-priority` and the static plugin's `metric` both default to 0 and `pppoe_client.go` passes a literal 0. Measured on a real kernel, defaults at DIFFERENT metrics coexist, the lower wins, and deleting it promotes the other, so a distinct default metric removes the collision with no ownership check and no read before the write. Cisco stamps a DHCP-learned default AD 254 and Junos ranks it below static, so a learned default losing to an operator static is also what the vendors do. The root cause is narrower still and is now its own spec, `spec-admin-distance-reaches-the-kernel`: Ze computes an administrative distance in sysrib and discards it at `BestChangeEntry`, which carries no distance field, so the kernel gets a flat metric and one `proto` stamp for every protocol. `rib admin-distance static` and `connected` are settable and inert because neither the static plugin nor iface goes through sysrib. The narrow fix here gives learned routes a non-zero default metric; that spec supersedes it. The narrow fix LANDED on 2026-08-11: `route-priority` defaults to 254 on a unit and on a pppoe-client, `defaultLearnedRouteMetric` (`internal/component/iface/config.go`) applies it when the leaf is absent, and `PPPoEClientConfig.RoutePriority` carries it to the AddRoute that passed a literal 0. The collision is gone because the metrics differ; the ownership question the add side cannot ask stays open and stays with `spec-admin-distance-reaches-the-kernel` |
| 2026-08-11 | fixit-bgp-shutdown-cease-notification | bgp reactor shutdown | `Reactor.stop` sends the Cease before it cancels, and round 3 added `Peer.stopping` so a notified peer stops re-dialing. That closed the OUTBOUND rail only. `Listener.acceptLoop` reaches `Peer.AcceptConnection` on the listener goroutine and reads neither `p.stopping` nor `p.ctx`, and the listeners are stopped in `Reactor.cleanup`, which `monitor()` reaches only after the cancel. So for the whole notify budget an inbound OPEN can drive an already-notified peer back to OpenSent, and the cancel then kills it silently: the same RFC4271-8.2.2-18 miss the outbound guard had just fixed. `Reactor.stop` also releases `r.mu` before marking, so a peer built by `tryCreateDynamicPeer` inside the window never gets the flag | fixed by sealing inside `Reactor.stop`, and that fix was ALSO incomplete: see the row below |
| 2026-08-11 | fixit-bgp-shutdown-cease-notification | bgp reactor shutdown | the same class a THIRD time in one spec, and the shape is now clear. `Reactor.stop` must keep every peer context live in order to notify, so every guard saying "we are leaving" has to be re-read by anything that can produce a NEW SOCKET in that window. Four producers exist and rounds 3 and 4 gated the two in front of them: `Peer.run`'s loop top and `tryCreateDynamicPeer`. Still open after round 4 were `startListenerForAddressPort`, which `handleAddrAddedPayload` calls on a netlink address-added event with no seal check so a fresh listener starts mid-shutdown, and `Session.Connect`, which unlike `Session.Accept` reads no `tearingDown` flag, so a `runOnce` already in flight dials, reaches OpenSent and sends its OPEN before the cancel kills it with a bare FIN. Both guards were keyed to a decision point read only BETWEEN attempts, while the window they protect is INSIDE one, and the fix itself widens that window to a full second | fixing by enumerating the producers of a new socket and gating each at its own producer rather than at each caller. The per-rail patch is what failed twice |
| 2026-08-11 | traffic-vpp-deferred-reply-timeout | vpp binary-API reply deadline | `newGovppOps` (`internal/plugins/firewall/vpp/timeout_linux.go`) is the ONLY production `SetReplyTimeout` call in the tree (grep over `internal/`, 2026-08-11). Four other plugins take govpp channels off the same pooled `Connection` and bind nothing: `internal/plugins/traffic/vpp` through `(*backend).Apply`, `internal/plugins/iface/vpp` through `ensureChannel`, `internal/plugins/fib/vpp` through its plugin registration, and `internal/plugins/static/vpp`. `core.DefaultReplyTimeout` is 0 and `receiveReplyInternal` reads that as `maxInt64`, so a VPP that accepts a request and goes quiet blocks each of them forever, with no context arm to cancel it. Each of the four already carries a test fake whose `SetReplyTimeout` is an empty method, so the seam was there and nobody used it | traffic is fixed by `plan/spec-traffic-vpp-deferred-reply-timeout.md`. iface, fib and static are NOT fixed and have no spec: they have different callers, different locks and different blast radii, so the bound belongs at each backend rather than in `(*Connector).NewChannel`, and the choice is owed to Thomas |
| 2026-08-23 | traffic-vpp-deferred-reply-timeout | ike ipsec vpp dataplane | a FIFTH unbounded govpp consumer, named by neither the row above nor `plan/spec-vpp-reply-deadline-iface-fib-static.md` (which covers iface, fib and static only). `newVPPBackend` (`internal/component/ike/dataplane/vpp.go`, `//go:build ze_vpp`) calls `conn.NewChannel()` and returns `&vppBackend{conn: conn, ch: ch}` with no `SetReplyTimeout`. Unlike the four above it KEEPS that channel for the backend's lifetime rather than per apply, and every SA and SPD operation waits on it: `SendMultiRequest` in the SA dump, and `SendRequest(req).ReceiveReply(reply)` in the SA and policy writers. `core.DefaultReplyTimeout` is 0 and `receiveReplyInternal` reads that as `maxInt64`, so a VPP that accepts one of those and goes quiet blocks the IKE dataplane with no context arm. `capturingChannel` (`vpp_message_test.go`) already carries an empty `SetReplyTimeout`, so the seam is there and unused, the same tell as the other four | not fixed, and not in scope for the traffic spec, which the deferral row scoped to the one backend it named. Belongs with the same owner decision the row above is waiting on: it is a sixth backend with its own caller and its own blast radius, and the sibling spec's Files to Modify would have to grow to reach it |
| 2026-08-11 | runtime-kernel-carries-every-tunnel-kind | iface config apply, tunnel create | `applyConfig` (`internal/component/iface/config_apply.go`) gives five of its six create steps one branch: when the create fails and `GetInterface` finds the name, keep the link and leave `created` false so the rollback cannot delete it. Dummy, veth, wireguard, xfrm and bridge carry it and the tunnel step returned the error instead. **The measurement first recorded in this row was wrong and is corrected here.** The QEMU failures that prompted the branch were a TEST COLLISION, not that defect: `test/plugin/iface-tunnel-kinds.ci` had taken the endpoint pairs of `test/reload/tx-iface-tunnel-create.ci`, it ran first in the shared VM and left its links behind, and the kernel answers EEXIST on a duplicate local/remote pair whatever the device is named. So `tgretap` and `tgrerm0` failed on links a DIFFERENT test had created, and moving the endpoints made all three tests pass with no product change. `tgrerm0` was never "a link an earlier run had left in the kernel", and the branch would not have fixed it. The defect the branch DOES fix is a ze restart, which no test had reached: `netlink.LinkAdd` sends `NLM_F_CREATE` with `NLM_F_EXCL` (`vendor/github.com/vishvananda/netlink/link_linux.go`), nothing in `internal/component/iface` deletes tunnels when the daemon stops, and `OnConfigure` runs `applyConfig(cfg, nil, b)` at every plugin start, so a second start meets EEXIST over ze's own netdevs, fails the whole interface apply, and the fail-closed startup cascade then refuses to start the daemon. The five sibling steps already survived that; tunnels did not | fixed at the tunnel step, and the FIRST version of that fix carried the very shape this file collects: it kept any device that held the name, so a dummy, a bridge or a physical NIC named `tgre0` passed the guard and Phases 2, 2c and 3 pushed the tunnel's MTU, admin state and addresses onto it. `reconcileOwnedDevices`, in the same file, already failed closed on that state for macvlans (`name occupied by a non-owned %s device`). The step now reads the link type of the device it found (`kernelLinkTypes`, `internal/component/iface/tunnel.go`) and keeps it only when it is a tunnel of the configured kind; anything else fails the apply, the operator who edits `encapsulation` while ze is down included. `fakeBackend.CreateTunnel` (`internal/component/iface/config_test.go`) now answers `file exists` for a name it already holds and reports the link type the kernel reports, which is why no unit test saw either half. The cross-test endpoint constraint is enforced by `TestCITunnelEndpointsAreUniqueAcrossTests` (`internal/test/runner/tunnel_endpoint_lint_test.go`) rather than by a comment in one `.ci`. What stays open, for tunnels and xfrm equally: a kept link's parameters (local, remote, key, ttl) are never compared, because `InterfaceInfo` carries no encapsulation field, so a parameter edit made while ze was down reaches the link's addresses and MTU and not its encapsulation. **What also stays open is the wider half of this class: the five sibling steps keep the link and remain KIND-BLIND.** Each spells its read as `if _, getErr := b.GetInterface(...); getErr != nil` and throws the answer away: the steps around `CreateDummy`, `CreateVeth`, `CreateWireguardDevice`, `CreateXFRM` and `CreateBridge`, and the one around `CreateVLAN` beside them. Only the tunnel step binds the `InterfaceInfo` and compares its `Type`. So a gre tunnel, a bridge or a physical NIC holding a dummy's name is kept, and Phases 2, 2c and 3 push that entry's MTU, admin state and addresses onto it -- the defect the tunnel step was just fixed for, in six more places. Round 2 of the review judged it separable and strictly pre-existing: no step this spec touched has it, the fix at each site is the read-back the tunnel step now does, and it wants its own spec rather than six edits folded into a closing commit |
| 2026-08-11 | runtime-kernel-carries-every-tunnel-kind | iface interface classification, tunnel prune | `kernelLinkTypes` (`internal/component/iface/tunnel.go`) maps `TunnelKindVxlan` to the kernel's `"vxlan"` link type and `kernelTunnelKinds` (`internal/component/iface/discover.go`), the same mapping the other way, never listed it. So Ze could CREATE a vxlan and could not DELETE one: `zeManageable` (`config_apply.go`) reads that map and answered false, and the Phase 4 prune in `applyConfig` skips any link type it answers false for, so a vxlan the operator removed from the config stayed on the box. The second consequence was in `infoToZeType` (`discover.go`), whose tunnel branch it also gates. A vxlan netdev is ethernet-shaped and carries a hardware address, which `linkToInfo` (`internal/plugins/iface/netlink/show_linux.go`) copies into `InterfaceInfo.MAC`, so the unmatched vxlan fell through to the MAC fallback at the end of that function and Ze called it an ETHERNET port rather than dropping it: measured `infoToZeType({Name:vx0 Type:vxlan MAC:aa:bb:cc:dd:ee:04}) = "ethernet"`. `EmitConfig` (`internal/component/iface/emit.go`) then writes an `ethernet <name> {}` stanza for it in `ze init` output, while every other tunnel kind has no case there and is skipped | fixed by adding `"vxlan"` to `kernelTunnelKinds`, and by pinning the two maps to each other with `TestKernelTunnelKindsCoversEveryModeledKind` (`internal/component/iface/discover_test.go`), which fails on a name in either map and not the other. Adding a tenth kind now fails that test rather than shipping half-wired. The prune stays safe for a vxlan the OPERATOR made: Phase 4 also requires `removedManaged[name]`, and `removedManagedNames` reads `cfg.previousManaged`, which `rememberPreviousManaged` fills from `previous.desiredState()` -- only names Ze's own config declared, so a device Ze never configured is never a deletion candidate |
| 2026-08-12 | - | geodns config TTL bounds | `parseTTL` (`internal/plugins/geodns/config.go`) bounds `default-ttl` and every record `ttl` to `maxTTL = 2147483647` (RFC 2181 section 8). `parseSOA`, in the same file, reads `minimum` with a bare `strconv.ParseUint(v, 10, 32)` and bounds nothing, and the YANG leaf is a bare `uint32`. `buildSOA` (`internal/plugins/geodns/server.go`) already puts that value on the wire as the SOA's own TTL and as `Minttl`, so a configured `minimum` above 2147483647 emits a TTL with the sign bit set, on every negative answer | not fixed. It is spec AC-8, which also owns the YANG `range`, and it is not what this package was cut for. The blast radius was briefly wider: an RFC 1035 section 3.3.13 TTL floor implemented on 2026-08-12 carried `minimum` onto every RR of the zone, and it was reverted the same day because RFC 2308 section 4 deprecates that floor |
| 2026-08-12 | - | dns negative answers | RFC 2308 section 3: "Name servers authoritative for a zone MUST include the SOA record of the zone in the authority section of the response when reporting an NXDOMAIN or indicating that no data of the requested type exists." Both responders satisfy the NODATA half and neither satisfies the NXDOMAIN half. `answerQuestions` (`internal/plugins/geodns/server.go`) appends `buildSOA` on every no-answer branch, then ends `if !found { msg.SetRcode(r, dns.RcodeNameError) }` with nothing appended to `msg.Ns`; `internal/plugins/as112/zones.go` has the same shape. The only route to `found == false` is `matchZone` returning "", so the NXDOMAIN is for a name outside every served zone -- where Ze holds no zone whose SOA it could include, which makes the sharper question whether an authoritative-only server should answer NXDOMAIN there at all rather than REFUSED | not fixed, and not this spec's RFC. Found while reverting the RFC 1035 section 3.3.13 floor; needs its own home |
| 2026-08-12 | - | dns reply header shape (found under spec-fixit-dns-rfc1035-conformance, which does not close here) | `shapeAuthoritative` (`internal/core/dnsserver/handler.go`) exists so no answer func can leave a reply non-authoritative, recursion-advertising or compressed, and it held three fields of the header and not the fourth. RFC 1035 section 4.1.1's Z field was untouched, and RFC 4035 section 3.1.6's AD bit with it, so `msg.Zero = true` or `msg.AuthenticatedData = true` in any AnswerFunc reached the wire. `SetReply` copies neither, which is why no query could produce it and no test caught it; `TestRFC4035_CDCopiedADIgnored` (`internal/core/dnsserver/rfc4035_test.go`) asserts "never asserts AD" against an answer func that never sets it, so the claim was a convention rather than the invariant the function's own doc comment promises | fixed: `shapeAuthoritative` now zeroes `Zero` and `AuthenticatedData` beside `Authoritative`, `RecursionAvailable` and `Compress`. `TestRFC1035_ReservedZFieldIsZero` (`internal/core/dnsserver/rfc1035_header_test.go`) drives an answer func that sets both, and it goes red when either assignment is removed. The wider half stays open and is what this class is for: nothing enforces that a field ADDED to `dns.Msg` is considered here, so the next reserved bit is missed the same way |
| 2026-08-12 | - | dns negative answers (found under spec-fixit-dns-rfc1035-conformance, which does not close here) | the sibling of the 2026-08-12 row above, and the reason `RFC1035-4.1.1-3` has no honest positive test. RFC 1035 section 4.1.1: "3 Name Error - Meaningful only for responses from an authoritative name server, this code signifies that the domain name referenced in the query does not exist." Neither responder ever emits that code for a name in a zone it serves. `answerQuestions` (`internal/plugins/geodns/server.go`) answers a name that is in the zone and has no record with NOERROR plus the zone SOA, which RFC 2308 section 2 reserves for a name that EXISTS with no data of the requested type, and `internal/plugins/as112/zones.go` has the same shape. So Ze's only RCODE 3 is the out-of-zone one, where `shapeAuthoritative` also stamps AA=1 on it, and the two defects compound: the reply claims authority for a name Ze serves no zone for and asserts that name does not exist anywhere | fixed the same day. Thomas answered "fix any issues - the code must be RFC compliant", so both halves landed. A name a served zone owns with no record of the type asked for stays NOERROR, a name inside a served zone that the zone does not own answers RCODE 3, and a name under no served zone answers RCODE 5 with AA clear. The client-dependence resolved the other way from the concern: existence is a property of the ZONE, so a host configured in another host-set still EXISTS and its empty view is no-data. `resolverState.names` holds that set and `nameExists` (`internal/plugins/geodns/server.go`) reads it. as112 needed no set because every RFC 7534 zone holds its records at the apex, and all three RFC 7534 rows kept their polarities. The AA half moved into `shapeAuthoritative` (`internal/core/dnsserver/handler.go`), which now reads the RCODE, so neither responder special-cases it |
| 2026-08-14 | spec-web-templ-migration | web commit bar | `commit_bar` (`templates/component/commit_bar.html`) wraps its whole body in `if not .ReadOnly`, so a read-only user gets no `div id=commit-bar` at all. `oob_save_ok` (`templates/component/oob_save.html`) replaces that same element through `hx-swap-oob=outerHTML`. It carries no ReadOnly branch. The OOB path therefore emits the commit and discard buttons the page template withholds. Seen side by side in the captured `testdata/golden/component/commit_bar--readonly.html`, which is empty, and `oob_save_ok--changes.html`, which is not | FIXED 2026-08-15 in the interface defect pass. `saveOKData` (`internal/component/web/view_fragment.go`) carries `ReadOnly` and `oobSaveOK` renders nothing when it is set, which is what `commitBar` does for the same element. One producer fills it: `saveOK` (`rbac.go`) reads `canEdit`, so no call site can forget. `HandleConfigChanges` (`handler_config_commit.go`) took no authorizer at all, and it is the live path. `refreshCommitBar` (`assets/cli.js`) fetches `/config/changes` on every page load. It swaps the answer over `#commit-bar`, so a read-only session recovered the buttons one fetch after the page rendered. It takes one now. `TestCommitBarHalvesAgreeOnReadOnly` and `TestConfigChangesHidesTheCommitBarFromAReadOnlySession` cover both |
| 2026-08-14 | - | web security headers | three responses reach the browser with no security header at all. `/`, `/favicon.ico` and `/assets/` bypass the wrapper that `addSecurityHeaders` (`internal/component/web/auth.go`) applies. They carry no Content-Security-Policy, no X-Frame-Options, no X-Content-Type-Options and no HSTS. Every other page carries all four. `/` serves the root document, so the CSP that constrains every sibling page does not constrain it. The handler golden capture recorded this: `testdata/handler/get-root.txt`, `get-favicon.txt` and `get-assets-ze-svg.txt` hold zero header lines, while `get-admin.txt` holds the set | FIXED 2026-08-15 in the interface defect pass, and it was FIVE responses, not three. `SecurityHeaders` (`internal/component/web/auth.go`) wraps the whole mux and `NewWebServer` (`server.go`) installs it, so every route carries Content-Security-Policy, X-Frame-Options, X-Content-Type-Options and HSTS whatever served it. `addSecurityHeaders` keeps the two that belong to an authenticated page, `Cache-Control: no-store` and the version header, so a static asset stays cacheable. The capture found the two the row missed: `get-assets-missing.txt` and `post-login-ok.txt`, the second being the redirect that hands out the session cookie. `TestWebServerSetsSecurityHeadersOnEveryRoute` drives `NewWebServer` over a route with no auth wrapper. Proven to discriminate: unwrapping the mux reds four assertions |
| 2026-08-14 | - | lg AS-path rendering | the templ port fixed the browser column and left its sibling. `scalarString` (`internal/component/lg/handler_ui.go`) prints a decoded JSON number with `FormatFloat` and precision -1, so AS 4200000000 renders its digits. `formatASPathPlain`, in the same file, still called `fmt.Sprint` on the same `float64`, so the Download CSV button wrote `4.2e+09`. No test saw either half: every ASN in every fixture is 65002 or less, where the two forms agree | fixed. `formatASPathPlain` calls `scalarString`. `TestASPathRendersALargeASN` (`internal/component/lg/handler_ui_test.go`) asserts both paths, and reverting either one reds it by name |
| 2026-08-14 | fixit-ike-responder-natt-port-float | ike responder cached-response replay | `handleResponderInbound` (`internal/component/ike/engine/responder.go`) consults the retransmission cache from two pre-adoption arms. Only one of them was guarded. The `StateEstablished` arm carried `carriesSKPayload` and a token bucket, because somebody had reasoned about amplification there. The `StateEAPInProgress` arm, four lines above it, consulted nothing. It called `handleResponderEAP`, which decrypts a duplicate that carries neither an EAP nor an AUTH payload. Six of that function's paths end in `StateDead`. The unguarded arm was the one with the WORSE exposure, and that is what makes this a class and not an oversight. It holds the window open across several EAP round trips, where the sibling holds one narrow pre-adoption moment. Its failure was also worse: not amplification but a dead SA. A 28-byte forged header, built from one observed datagram, killed a live IKE SA, because `decryptAndParse` failure sets `StateDead`. strongSwan's responder EAP exchange died there, and interop scenario responder-eap-mschapv2 never passed. The cache was already populated on the EAP path. `cacheResponse` (`engine/msgid.go`) is called by both `startResponderEAP` and `sendResponderEAP`, so only the CONSULT was missing | fixed. The guarded body is lifted into `replayCachedResponse` and called from BOTH arms, so a consult site cannot exist without the guards. Proven at two tiers, and both proofs discriminate. `TestEapRtxResponderReplaysCachedResponseMidEAP` asserts byte-equality of the replay against the first response. A rebuild cannot satisfy that, because every build draws a fresh random CBC IV. Scenario responder-eap-mschapv2 blacks out ze's port 4500, so charon retransmits of its own accord. Review round 1 found the same shape one level up, in the RECORD. The comment justifying the guards cited RFC 7296 Section 2.21.4 as a MUST NOT over "an unprotected message". Section 2.21.4 says the opposite for an unprotected REQUEST: the node "MAY send a response". Its MUST NOTs cover a message marked as a response, and a peer receiving an unprotected INVALID_IKE_SPI Notify. The governing obligation is `RFC7296-2.4-12`. Both call sites now cite it. Both also state that refusing is a choice the RFC permits, and not one it compels |
| 2026-08-14 | fixit-mirror-clsact-ownership | iface netlink traffic mirroring | the clsact qdisc at handle ffff: is shared, and only one of its two users knew it. `SetupSampling` and `RemoveSampling` (`internal/plugins/flowexport/sampling/tc_linux.go`) tolerate `EEXIST` on the qdisc add and remove only their own priority-100 filter, and say so in their doc comments. `SetupMirror` and `RemoveMirror` (`internal/plugins/iface/netlink/mirror_linux.go`) did neither: `RemoveMirror` called `QdiscDel` on the whole qdisc, so removing a mirror deleted every sampling filter on that interface, and `setupClsactMirror` failed outright when sampling had created the qdisc first. Its rollback deleted the qdisc it had found rather than the filter it had added. A third defect fell out of the same intolerance: a mirror with a different destination per direction needs two `SetupMirror` calls, the first installed the older ingress qdisc, and the second could not add an egress filter to it | fixed. Teardown removes the mirror's own filters and LEAVES the shared qdisc, because a teardown cannot know who created it and the filter set can change between reading it and acting on it. Only `undoMirrorSetup` deletes, gated on having created the qdisc moments earlier. Setup tolerates `EEXIST` and rollback undoes only what it did. Every mirror now uses clsact, so `setupIngressMirror` is deleted and the two-destination case works. Proven in QEMU with a foreign filter really attached, and each guard reverted in turn to see its own test go red |
| 2026-08-15 | - | bgp reactor egress, RFC 4271 Section 5.1.3 | commit `480897faf` added `egressNextHopIsPeerOwn` (`internal/component/bgp/reactor/forward_next_hop.go`) and claimed RFC4271-5.1.3-1 in `rfc/short/rfc4271.md` and `docs/features/rfc-status.md`. It is called from TWO sites, both FORWARD rails: `forwardUpdateCore` (`reactor_api_forward.go`) and `reactorForwardRS` (`forward_rs.go`), which relay a route another speaker sent. The requirement's own sentence names the other half: "A route ORIGINATED by a BGP speaker SHALL NOT be advertised to a peer using an address of that peer as NEXT_HOP." Five rails originate a NEXT_HOP and none of them asked: configured static routes and default-originate (`peer_initial_sync.go`), the RIB op-queue drain (`buildRIBRouteUpdate`, `peer_rib_routes.go`), the announce batch (`buildBatchAnnounceUpdate`, `reactor_api_batch.go`) and the RFC 9494 stale re-advertise beside it. Every refusal those rails already carried is about ENCODABILITY (`ErrNextHopUnencodable`, `errAnnounceNextHopUnencodable`, `ValidateNextHops`), never about WHOSE address it is. Measured with the guard stubbed to false: a peer at 127.0.0.1 was sent `4003047F000001 180A0100`, its own address as the next hop for 10.1.0.0/24. The ledger therefore claimed a conformance Ze had for relayed routes only | fixed by asking the question at the two WRITERS rather than at the five producers. `originatedNextHopIsPeerOwn` (same file) reads the built body, and `writeUpdateGated` and `Session.SendAnnounce` (`session_write.go`) call it: `gate == true` already separated originated from relayed for the export chain, so no second discriminator was needed, and the hot forward path (gate false, and `writeRawUpdateBody`) is untouched. A sixth originating rail inherits the guard rather than being born without it. Proven by `test/plugin/originated-nexthop-peer-own.ci` and two unit pairs, all four tagged, and all four shown to go red with the guard stubbed. THE GENERAL LESSON, which is why this row is long: the unit to guard is the WRITER, not the producer. A per-producer guard is N copies to keep in step and an N+1th producer with none, and the count of producers is not visible from the one you happen to be editing |
| 2026-08-15 | spec-web-templ-migration | config placeholder guard, read half against write half | the display mask widened to every secret leaf and the write guard did not. `RejectMaskedBcryptLeaves` (`internal/component/config/mask.go`) keyed on `leaf.Bcrypt`. It is the guard the config loader, `ze config validate` and the web upload handler all call. `ze config dump --strip` writes `/* SECRET-DATA */` for a `ze:sensitive` leaf. A dump-then-upload round trip therefore stored 17 bytes of C comment as the API bearer token, and the daemon then ran with it. A test proved the old code overwrote the stored file with that value | FIXED 2026-08-15. The guard reads `config.LeafHoldsSecret`, the one predicate the mask reads. Its walk also descends a flex node and an inline list, which the bcrypt walk never did. RENAMED 2026-08-15 to `RejectMaskedSecretLeaves`, with its diagnostic code, which is now `config-secret-masked` and is registered in `internal/core/diagnostic/codes.go`. The old name and the old code recorded the bcrypt half the guard covered first, and a code is a contract surface. `TestUploadedConfigWithAMaskedSecretIsRefused` (`internal/component/web/secret_test.go`) drives the real validator through `HandleConfigUpload` and asserts the stored file is unchanged |
| 2026-08-17 | spec-hub-deferred-api-auth-independent-of-ssh-block | insecure web authorization | `startWebServer` selected `InsecureMiddleware` when `--insecure-web` disabled authentication, but still passed the live AAA authorizer to every edit guard. After AAA became independent of SSH, an empty accepted authorization store made an insecure web request return HTTP 403. The mode bypassed one access gate and retained its paired gate | fixed. `startWebServer` clears the authorizer for insecure mode before it constructs handlers. `api-user-removed-by-reload` still exercises strict API authorization, and `web-commit-reject` proves insecure web edits remain available |
| 2026-08-17 | spec-hub-deferred-api-auth-independent-of-ssh-block | API reload exposure guard | `runReloadContext` rebuilt and published the candidate API identity even when `environment.api-server` was absent, but `apiAuthReloader` returned no intent for that same case. Removing the block and final user could leave an environment-started non-loopback gRPC listener running after authentication became optional | `apiAuthReloader` now classifies absent blocks from candidate users and the retained accepted token. It shares `candidateAPIToken` with final identity publication. `TestAPIAuthReloaderAbsentBlockUsesCandidateUsersAndRetainedToken` and `TestAPIAuthReloaderAbsentBlockWithoutCredentialsIsKnownUnauthenticated` cover the paired decision |
| 2026-08-17 | spec-hub-deferred-api-auth-independent-of-ssh-block | command accounting and response delivery | `Dispatcher.BeginAccounting` emitted STOP when dispatch returned, before REST, gRPC, web, MCP, LG, CLI, or SSH had delivered the rendered response. Shutdown could close the producer or socket after STOP while the accepted action's observable answer was still in flight | dispatch now returns a completion carrier. Every direct and socket-backed surface calls `TransportComplete` only after delivery, and the carrier retains the accountant that emitted START so STOP cannot cross an accepted AAA bundle swap. Focused dispatcher, plugin-server, transport, and surface tests cover both halves |
| 2026-08-17 | fixit-ike-resource-lifetime-leaks | ike engine start, runEngine error exit | AC-2 put the IKE bypass removal and `dataplane.CloseBackend` on a defer beside `installIKEBypass`, so every exit of `runEngine` (`internal/component/ike/engine/register.go`) releases them. Everything ELSE that function acquires is still released only by the clean tail below `p.Run`: the peer sessions (`ps.Stop` plus `cleanupPendingSA`), the two UDP transports, and the metrics ticker goroutine that `close(metricsStop)` ends. An in-process plugin exit is not a process exit -- `startInternal` (`internal/component/plugin/process/process.go`) logs the non-zero code and the daemon runs on -- so those outlive the engine, and the bypass is now removed while an orphaned peer goroutine may still hold a tunnel, which is spec R-3 | not fixed. The spec scopes AC-2 to the bypass and the work package was scoped to three missing calls and their lifetimes rather than to runEngine's resource ownership. The sibling releases want their own spec |
| 2026-08-17 | fixit-ike-resource-lifetime-leaks | as112 QEMU integration tests | the 2026-08-12 RFC 2308 / RFC 1035 RCODE fix recorded two rows above changed what a no-data name, an out-of-zone name and a loopback query answer, and the four tests in `internal/plugins/as112/integration_linux_test.go` still assert the old answers: `TestIntegration_ReverseZoneNoData` and `TestIntegration_EmptyAS112Arpa` want NOERROR and get NXDOMAIN, `TestIntegration_OutOfZoneNXDOMAIN` wants NXDOMAIN and gets REFUSED, `TestIntegration_LoopbackAlwaysPermittedOverWire` wants NOERROR and gets NXDOMAIN. They carry `integration && linux`, so `make ze-qemu-integration-test` is the only gate that runs them and no native gate could see them. Measured in that run on 2026-08-17 against committed code, with no uncommitted change in `internal/plugins/as112`, `internal/core/dnsserver` or `internal/plugins/geodns` | not fixed here. The tests are wrong about what they assert, and the four expectations have to be re-derived from the RFC 2308 section 2 and RFC 1035 section 4.1.1 reading that changed the code, plus RFC 7534 for the AS112 zones. That is the DNS spec's ruling to restate rather than this package's, and folding it into an IKE commit would cost both their scope |
| 2026-08-18 | fixit-link-event-drop-loses-carrier-state | iface resolver link-event fan-out | the spec replaced the cap-16 drop in `internal/component/iface/register.go` with a coalescing queue, and `(*resolver).onLinkEvent` (`internal/component/iface/resolve.go`) keeps the older shape beside it: a non-blocking send that drops the event when a subscriber channel is full, and a `GetInterface` backend call made on the monitor's read loop when a `mac/match` selector exists. Its comment states the drop is recoverable ("the consumer recovers on the next event or a re-Resolve"); that claim was not verified against the subscribers | not fixed. It does not hold `dhcpMu`, so it does not produce the commit-window loss the spec closed. Whether the recovery claim holds is the open question, and it is the thing to establish before touching it |
| 2026-08-19 | none (nft address-match family guard) | firewall nft lowering | the `meta nfproto` guard now precedes every family-specific network-header READ in `internal/plugins/firewall/nft/lower_linux.go`: `lowerAddrMatch`, `lowerMatchInSet` and `lowerDSCPMatch`. Two siblings in the same file still read a header without one. `lowerSetDSCP` WRITES the IPv4 TOS byte at network-header offset 1 with an IPv4 checksum fixup at offset 10, so in an inet table an IPv6 packet gets its traffic-class and flow-label byte rewritten and a checksum recomputed over its source address; its own doc comment already says the lowering is IPv4-only. `lowerICMPTypeMatch` reads transport-header offset 0 with no `meta l4proto` guard, and `lowerMatch` routes both `MatchICMPType` and `MatchICMPv6Type` to it, so an ICMPv4 type match also matches an ICMPv6 packet carrying that first byte. `validateMatch` (`internal/component/firewall/validate.go`) states the false premise behind both: it calls inet safe because "its chains dispatch per packet to the IPv4 header layout", which a raw payload read does not do | BOTH fixed 2026-08-19, and the decision the row asked for turned out to be unnecessary. An inline action guard does gate every later expression, so it was never the answer; `lowerTerm` now returns `[][]expr.Any` and emits ONE RULE PER FAMILY when `familySplitNeeded` sees a term that writes the network header, each rule behind its own `nfprotoGuard`. `applyChain` already looped per term and called `AddRule` itself, so nothing had to be plumbed. `setDSCPv4` keeps the old body byte for byte and `setDSCPv6` writes the traffic class across bytes 0 and 1 with mask `F0 3F`, preserving the version nibble, the two ECN bits and the flow label, and setting no checksum because IPv6 has no header checksum. `NFPROTO_UNSPEC` is rejected rather than defaulted. Nothing is lost for either family. Measured under `unshare -rn` with nft 1.0.9, 6 ICMPv6 packets per phase, against `dscp-set 46`: today's code marked dscp 2 on all 6, forced ECN to CE on all 6 and polluted the flow label on all 6; the fix marks dscp 46 on all 6 and leaves ECN and the flow label alone. The false ECN congestion signal was not predicted by this row and is arguably worse than the wrong DSCP. `lowerICMPTypeMatch(l4proto, icmpType)` now prepends `meta l4proto` and `lowerMatch` passes `IPPROTO_ICMP` or `IPPROTO_ICMPV6`, which needed the two match kinds told apart as this row said. `validateMatch`'s false premise is corrected, and so is the same claim inside `TestValidateSetDSCPInet`. One defect the fix would have introduced was caught with it: `handleShowFirewallRuleset` keyed counters by term name last-write-wins, so a split term would have reported only its IPv6 packets, and `GetCounters` now sums through `mergeRuleCounters`. Still open and NOT journalled separately: `readTables` reports a split term twice in `ListTables`, judged an honest kernel view, and `MatchDSCP` in an inet table still matches IPv4 only, which narrows rather than corrupts |
| 2026-08-19 | cli-order-pipe | `ze cli` interactive startup | `NewCommandModel` (`internal/component/cli/model.go`) builds a Model with NO config completer, which is what that constructor is for. Three of the type's methods guard `m.completer != nil` before they read it. `SetCommandCompleter` did not, and called `m.completer.Backends()` on the nil. Every interactive session died with a SIGSEGV before it drew a prompt, on `ze cli` and on `ze start --cli` alike. Nothing caught it because `ze cli -c` exits before it builds a Model, and every `.ci` in the tree uses `-c`. `test/ui/display-fill-completion.ci` found it, the first test that drives the interactive client over a pseudo-terminal | guarded `SetCommandCompleter`, and its sibling `refreshCompleter`, which reads the same field on the same assumption. `TestCommandModelTakesACompleterWithNoEditor` (`internal/component/cli/model_test.go`) pins it, and it first asserts that `NewCommandModel` still leaves the field nil, so the test cannot pass by the case going away |
| 2026-08-19 | - | commit_helper deferral scan, its two fixture suites | `deferral_in_diff_problems` (`scripts/dev/commit_helper.py`) gained an early return for a repository holding no `plan/` tree (`8f2ae417a`). Such a repository cannot record a deferral, so the gate had no reachable satisfying action there. Two suites build fixture repositories for that gate. `commit_helper_test.py` was updated to model a source checkout. `scripts/dev/hook-fixture-check.py` was not, and its `_init_repo` builds a repository with no `plan/` tree. So `commit-gate-deferral-in-diff`, `commit-gate-deferral-in-diff-prose-still-caught` and `commit-gate-create-blocks-deferral` asserted a refusal the gate now exempts. `make ze-unit-hook-test` was red at HEAD for everyone, 540/543, reproduced against a clean `git archive HEAD` tree | fixed. `_init_repo` creates `plan/deferrals/`, so every commit-gate fixture models a source checkout, and its docstring says why the directory is load-bearing. 543/543, and `commit-gate-deferral-in-diff-logged-ok` still passes, so the cases did not go green by the gate going away |
| 2026-08-20 | cli-dispatch-child-guard | CLI command resolution | Ze resolves a typed command by longest registered prefix TWICE, once in the client (`LookupLocal`, `internal/component/command/registry/registry.go`) and once in the daemon (`matchBuiltinTokens`, `internal/component/plugin/server/command.go`). Only the client refused a match when a LONGER prefix of the input was itself a declared command. The daemon handed the whole subtree below its match to that match's handler as arguments, and four `show bgp` subtrees are plugin names the daemon reaches only after the builtin lookup fails, so one `ze:command` on `container bgp` would have sent `show bgp rpki status` to the summary handler to be rejected as an address family called rpki. No test could see it: the guarded half had the tests, and the unguarded half had no caller that needed it until now | fixed here: `longerCommandPath` and `isCommandPath` give the daemon the same rule, reading all three registries `Dispatch` resolves from, proven against plugin and subsystem names rather than synthetic builtin keys. The two stay separate implementations because they read different registries. The question a twin lookup owes is which of the two carries the guard, and what happens to the other one's callers |
| 2026-08-21 | - | CLI table and text rendering | `cellFromValue` (`internal/component/command/pipe_table.go:353`) carried `case nil: return cellFromString("")`, so a null INSIDE a table cell rendered as blank. `renderValue`, the top of the same file's dispatch and the entry `applyTableStyled` calls, carried no such case, so a WHOLE answer of `null` fell through both type switches to `fmt.Sprint(formatNumber(v))` and an operator read `<nil>`. The file already knew the rule and applied it to one half of the pair. Reached by every command answering with an empty payload on a human-facing surface, which is every request-style command that succeeds with nothing to report: the default format pipe is table or text (`ProcessPipesDefaultFormatChecked`, `pipe.go:940`), and only the json, yaml and raw renderings want the literal null | fixed on the spot, one case: `renderValue` returns `emptyMarker` for nil, the marker the same function already returns for an empty map and an empty list. Machine renderings are untouched, so a program still parses `null` (owner directive, 2026-08-21: returning nil is fine, printing it is not). `TestApplyTableNullIsEmptyNotGoNil` covers `null`, `{}` and `[]` through table and text and asserts no `<nil>` reaches an operator; mutating the case away turns it red with `got "<nil>\n"`. Found while reading this spec's own diff, which routed more answers through the empty payload than before and made a latent defect reachable more often |
| 2026-08-22 | - | fleet hub auth line reader | the owner ruled on 2026-08-22 that a line of this wire ends with exactly one newline and that no reader strips a trailing carriage return, because a stripped byte leaves a counted field's stated width disagreeing with the bytes that arrived. Four readers were changed to refuse it: `FrameReader` (`pkg/plugin/rpc/framing.go`), `readAnswerFrame` and `ExecCommandStream` (`internal/core/ssh/client`), and the ExaBGP bridge's stdin scanner. A fifth still strips it. `readLine` (`internal/component/managed/client.go`) reads the hub's auth response byte by byte and drops a trailing carriage return with the comment `Strip trailing \r for CRLF compatibility`, and `TestReadLine` (`internal/component/managed/client_test.go`) pins that as intended with a case named `CRLF stripped`. It speaks the same `#<id> <verb> [json]` grammar as the plugin wire | not fixed. It carries no counted field, so nothing is corrupted by the strip today, and the ruling named three speakers rather than this transport. Fixing it means changing a test that documents the behavior as deliberate, so the decision is owed to the owner |
| 2026-08-22 | fixit-child-rekey-answer-vs-installed-selectors | ike engine child rekey initiator | the IKE_AUTH initiator adopts the responder's narrowed traffic selectors: `adoptAuthResponseNegotiation` (`internal/component/ike/engine/transport_mode.go`) calls `recordInitiatorSelectors` and installs what came back, so both ends program the same traffic. The CHILD REKEY initiator does neither. `applyChildRekeyResponse` (`internal/component/ike/engine/rekey.go`) never reads TSi or TSr from the response, and `newRekeyedChild` (same file) inherits `old.Selectors`, so Ze installs the retired pair's scope whatever the responder answered. The design rests on the peer obeying RFC 7296 Section 2.9.2, and strongSwan 5.9.14 does not: driven from the Docker lab with `ze_test_ike_rekey_ts_local=10.2.0.0/25`, charon accepted a rekey proposal narrower than the scope in use, logged `inbound CHILD_SA ze-child{2} established with SPIs ... and TS 10.1.0.0/24 === 10.2.0.0/25`, and programmed that pair, while Ze's kernel kept `10.2.0.0/24 <-> 10.1.0.0/24` and `show vpn ipsec sa` kept reporting `ts-local 10.2.0.0/24`. Traffic from 10.2.0.128/25 is then protected by Ze and dropped by the peer, with no notification and no log line, which is the divergence the spec names, one exchange role over | fixed here, after the spec was widened to carry the initiator half as AC-5 and AC-6. `applyChildRekeyResponse` now captures `*wire.PayloadTS` in its payload walk, refuses a response missing either payload, and adopts the answer through `recordInitiatorSelectors` BEFORE it derives any key. `newRekeyedChild` takes the negotiated set as a parameter and derives `Selectors`, `SelectorsLocalIsTSi`, `TSLocal` and `TSRemote` from it, so both roles install what THIS exchange agreed. The design question this row posed was answered by the RFC and not by the owner: Section 2.9.2's first sentence binds the NEW SA, so it binds the end that INSTALLS one as much as the end that answers, and Section 2.21.3 tells the initiator to send no error notify for a bad response. Ze drops the pending rekey and keeps the SA in use. `test/interop-ipsec/scenarios/initiator-rekey-answer-narrows` is the committed reproduction: PASS against strongSwan 5.9.14, and RED with the floor removed, where Ze programmed 10.2.0.0/25. The general practice: when one role of a pair carries an adoption guard, ask what the OTHER role INSTALLS, not only what it checks |
| 2026-08-22 | - | write-time hook and commit-time gate | a rule about file CONTENT was enforced in the commit half of the pair alone. The write half read nothing, so the defect landed at the edit and the author met the rule as a refusal when the commit was prepared, one re-edit later | the write half now reads the same parser. The general practice: a rule a write-time check CAN read is enforced in both halves. A rule enforced only when the commit is prepared is a rule discovered by being rejected, and the cost is paid again on every occurrence |
| 2026-08-22 | fixit-zefs-diff-structural-ops | web pending-change review, `/config/diff` | The count and the body of one overlay come from two producers, and only one of them has a fallback. `EditorManager.ChangeCount` (`internal/component/web/editor.go`) returns `len(us.editor.PendingChanges(sid))`, then falls back to `1` when that is empty and `us.editor.Dirty()` is true. `EditorManager.Diff`, twenty lines above it, reads the same `PendingChanges(sid)` and returns `""` for an empty result with no fallback of its own. `diffModalOpen` (`component_diff_modal.templ`) renders the two side by side, so an editor that is dirty with nothing pending draws `Review changes (1)` over an empty `pre`. That is the exact symptom this spec was written for: a blob-backed `List` hid the structural op, `PendingChanges` went empty, and the fallback supplied the `(1)` the operator saw | not fixed. The spec's goal is met without it: the op is found again, so `ChangeCount` never reaches the fallback for this cause, and both halves read one value. The shape survives for any other cause of dirty-with-nothing-pending, and no test asserts that the count and the body of `/config/diff` agree. The general practice: when a screen shows a count beside the thing counted, one value produces both, or a test holds them against each other |
| 2026-08-22 | - | bgp rib show pipelines | the fix for one read lock taken twice on one path landed at all three show sources and was guarded at one. `PeerRIB.AddPathFamilies` replaced the in-callback `IsAddPath` in `inboundSource.Next` and `protocolInboundSource.Next` (`internal/component/bgp/plugins/rib/rib_pipeline.go`) and in `newBestSource`'s collect (`rib_pipeline_best.go`), and only the third carried a test. `TestBestPipelineWalkSurvivesConcurrentUpdates` seeds through `seedBestPathRows` and drives the best-path walk, so it never reaches the two sources in `rib_pipeline.go`, and reverting either of them left the whole package green. A deadlock reachable from `show bgp rib` on any peer that is still sending was therefore unguarded | `TestShowPipelineWalkSurvivesConcurrentUpdates` and `TestShowProtocolPipelineWalkSurvivesConcurrentUpdates` (`rib_show_concurrency_test.go`) drive the other two walks beside a writer that removes the routes they read, under a deadline that reports the wedge instead of waiting out a suite timeout. Each was proven by reverting its own site and watching it go red. The general practice: when one fix lands at N call sites, the reversion has to be tried at each one, because a test over the site the author happened to instrument says nothing about the other N minus 1 |
| 2026-08-23 | fixit-shutdown-waits-out-a-deadlock | `ProcessManager.Stop` re-entry guard, `internal/component/plugin/process/manager.go` | The guard is a `stopping atomic.Bool` that `Stop` sets so a second entry does not re-wait for the same engines, and it is correct only while the flag is FALSE whenever the manager holds a live process. `startConfigs` cleared it and its comment claimed "Every spawn goes through here". Three sites write `pm.processes`, not one: `startConfigs`, `Respawn` and `AddProcess`. `Respawn` is reachable in production from `Server.restartPlugin` (`internal/component/plugin/server/reload_tx.go`) when a reload rollback reports a broken plugin. `Stop` cancels `pm.ctx` before it waits, and `Process.startInternal` (`internal/component/plugin/process/process.go`) never reads that context: it makes the pipe, sets `running` and starts the engine goroutine whatever the context says. So a respawn landing after a `Stop`'s engine wait left a LIVE engine behind a set flag, and the next `Stop` took the second-entry path and returned before that engine released what it installed. The guard added to prevent a leak could cause one, and the false sentence in its own comment is what would stop the next reviewer asking | fixed in the closure at all three sites, three lines. The comment now names all three and states why `startInternal` ignoring the cancel is what makes the respawn site live rather than theoretical. `TestARespawnedPluginStillGetsItsEngineWait` drives `Start`, `Stop`, `Respawn`, `Stop` and asserts the release landed; it reds in 0.70s against the unfixed `Respawn`, the second stop returning before the 700ms release. The config needs `RespawnEnabled: true` or `Respawn` returns nil having done nothing, which is the first thing the test got wrong. The general check: a guard whose correctness depends on a flag being cleared owes an enumeration of every writer of the state that flag describes, and the enumeration belongs in the test plan, not only in the comment |
| 2026-08-23 | record-answers-3-zero-alloc | bgp rib best-path walk, pool references | phase 5 gave the best-path walk a reference where a lock never covered it, and the guard covers ONE consumer shape. `bestSource.Next` (`internal/component/bgp/plugins/rib/rib_pipeline_best.go`) gives the previous item's reference back on every pull, which is what makes a walk of a million rows affordable, and it is correct only while nothing downstream holds an item across a pull. `lastFilter.drain` (`rib_pipeline.go`) holds them: it pulls the WHOLE walk into a ring of N and emits from the ring afterwards. So `show bgp rib best last N` built every row but the final one from handles no reference covered, and `last` is a registered operator-facing pipe filter (`command.PipeFilter{Name: "last"}`, `internal/component/bgp/plugins/cmd/rib/rib.go`). Measured: with the walk seeded at 3 routes and `last 3` in the chain, `pool.ASPath.Get` on the first buffered row's handle answered `handle references dead slot` after one withdrawal, and the row rendered with its as-path missing. In a release build that slot is re-interned, so the row carries another route's attributes rather than losing one. It is not a regression: before phase 5 no row of that command was covered either, and `bestSource`'s own doc said every dereference downstream was covered, which is the sentence that stopped it being asked | fixed in the review of phase 5. `applyBestStages` (`rib_pipeline_best.go`) builds the chain for both the streamed walk and the terminal branch, sets `lastFilter.retain` on every holder it constructs, and returns the release that chain owes. The ring takes its own reference for each item it keeps, gives it back on eviction and gives the rest back at the end, so the cost is bounded by N where retaining the whole walk would not be. `TestBestPathRowsHandleSafeUnderLastFilter` and `TestBestPathRowsLastFilterReleasesItsRing` are the regressions, each proven by its own mutation: `retain = false` reds the first naming the lost AS path, dropping the release reds the second. The general practice, and it is the reason this row is here: a lifetime stated as "valid until the next pull" is a contract on every CONSUMER, and a pipeline hands the value to consumers the author of the source never enumerated. Ask which stages HOLD, not which stages read |
| 2026-08-23 | - | `scripts/dev/commit_helper_test.go` | `message_text` (`scripts/dev/commit_helper.py`) gained a subject-length refusal that names the length, the overage and the subject, so a caller retries once instead of twice. `commit_helper_test.py` was updated with it. The Go twin over the same CLI was not, so `TestCommitHelperRejectsLongSubject` kept asserting the message the code stopped emitting, and the package was red at HEAD | fixed. The Go assertion pins the message the Python test pins, and its comment names the twin. The general practice: a user-visible string asserted by two suites at different levels has two assertions, so changing the string is a change to both, and the suite that is slower to run is the half that gets forgotten |
| 2026-08-23 | fixit-firewall-irr-term-fails-validation | firewall-irr table terms, IPv4/IPv6 set pairing | Two producers derived one PAIR by different rules. `expandIRRTermV6` (`internal/component/firewall/config.go`) emits an IPv6 twin of every IRR term unconditionally, because the config parser cannot see the prefix data. `buildSets` (`internal/component/firewall/plugins/irr/sets.go`) emitted a set only for a family that announced prefixes. So an ASN or AS-SET with IPv4 space and no IPv6, which is the ordinary case, left the twin naming `irr_v6_<name>` that no owner declared, `dropTablesMissingAProvidedSet` (`internal/component/firewall/registry.go`) removed the operator's WHOLE table from the applied snapshot, and the commit reported success with nothing in the kernel. Every test agreed because the AS-TEST fixture answers both families, so the asymmetric input was unreachable from any test in the tree | fixed by `buildTermSets`, which declares BOTH set names for any entry the plugin holds data for and gives the family that announced nothing a set with no elements, which is what its term must read. An entry announcing nothing at all still yields no set, so a cold cache keeps holding the table back. `buildIfaceTables` keeps `buildSets` because it emits an accept term per family and never names a set it did not declare. The fixture gap is closed at its source: `internal/test/mock/irr/irr.go` now serves AS-V4ONLY, whose `!a6` answers key-not-found. Proven by `TestBuildTermSetsPairsTheFamilies`, `TestConfigureAcceptsIRRTableTermWithOneFamily` and the AS-V4ONLY half of `test/plugin/firewall-irr-table-term-commit.ci`, which reds with the fix reverted on `firewall: table "wan" not found` |
| 2026-08-23 | fixit-ipsec-interop-cli-credentials | ipsec interop lab, ESP SPI snapshots | W-2 made scenario clear-reestablish's `before` snapshot fail closed and left scenario responder-accepts-reinit, which reads the same `StrongSwan.xfrm_state` and makes the same `now - before` comparison. An empty `before` is satisfied on the FIRST poll by the SA that already existed, so `responder-accepts-reinit` passed with Ze having ignored strongSwan's re-init entirely. Converting the READER to raise (`docker_exec` rather than `docker_exec_quiet`) does not close it: a kernel holding no ESP state answers empty on exit 0, so the reader cannot tell that case from a real one. The unit test written to hold the fix, `test_scenario_10_refuses_an_empty_before_snapshot`, named one of the two scenarios that share the pattern | fixed. Scenario responder-accepts-reinit refuses an empty snapshot before it breaks the link, and `test_scenario_11_refuses_an_empty_before_snapshot` (`test/interop-ipsec/lab_test.py`) holds it; `load_scenario_10` became `load_scenario(name)` so the next scenario costs one line. Mutation-verified: with the guard removed the test fails on `docker_exec` having been called, after the poll runs its full 30 second bound. The lesson is that a fail-closed READER and a fail-closed ASSERTION are different guards, and converting the reader reads like finishing the job |
| 2026-08-23 | fixit-stored-route-relay-hardening | `CommandRegistry` frozen snapshot, `internal/component/plugin/server/command_registry.go` | `Unregister` and `UnregisterAll` both call `republishFrozen`, so a removal reaches the snapshot `Lookup` reads. `Register` and `registerDeprecated` did not. Freeze happens once, in `signalStartupComplete` (`startup.go`), and registration does NOT stop there: `autoLoadForNewConfigPaths` starts plugins on a config reload and a restart re-runs the handshake, and both reach `engineStartupSink.onReady`, which calls `Register`. So every command declared after startup was written to the mutable map and answered nowhere: the plugin came up, reported its commands registered, and no operator or sibling plugin could dispatch one. Nothing caught it because the removal half had always been right, and the pair is only ever read one side at a time | fixed, two lines: a `defer r.republishFrozen()` under the existing `defer r.mu.Unlock()` in both adders, so it runs while the lock is still held. `TestCommandRegistryRegisterAfterFreezeIsVisible` and `...DeprecatedAliasAfterFreezeIsVisible` redden against the unfixed adders with a nil lookup. The general check: when a mutation pair keeps a derived snapshot, ask which SIDE refreshes it, because the side that is exercised at shutdown gets tested and the side exercised only after startup does not |
| 2026-08-23 | - | ddos-detect startup grace, the PPS and BPS trigger pair | `applyTick` (`internal/plugins/ddos/detect/detector.go`) opened with a startup-grace block that returned `d.drainPending()` whenever `maxPps` was below `AbsoluteFloor*5`, and `bpsAbove` was computed AFTER that block, so during grace the bandwidth trigger was never evaluated. The escape was written for the packet-rate half of the pair and was never extended when the BPS half arrived. Amplification is precisely the class that is low packet rate and high bandwidth, so the one attack shape grace could not see is the one the BPS path exists to catch. It bites hardest after a restart, where `(*baseline).restore` can leave the bandwidth baseline Ready at tick 1 while grace still discards the next 90 ticks at the default `startup-grace` | `bpsAbove` moved above the grace block, and the escape now reads both halves: a tick is discarded only when the packet rate is quiet AND the bandwidth trigger is not firing. `TestApplyTick_BpsTriggerFiresDuringStartupGrace` restores a warm BPS baseline from the store, then floods 50 pps at 4 Mbps inside the grace window; it reddens against the original ordering |
| 2026-08-24 | fixit-ordered-list-loses-its-order | `runYANGConfig` Phase 2 (`cmd/ze/hub/main.go`) against `rollbackReload` (`cmd/ze/hub/main_reload.go`) | The spec gave plugins a new lowering, `Tree.ToPluginMap`, that carries a list's entry order, and converted four of the five producers of a plugin's config map. The fifth was the hub seeding the ConfigProvider, still on `Tree.ToMap`. The two halves only ever meet on the ROLLBACK path: a failed reload replays the provider snapshot into the plugin server through `snapshotProvider` -> `snapshotToLoadedTree` -> `Server.ReloadConfig`, so the recovery from a bad config handed every plugin a multi-entry list with no order, which `configorder.Entries` refuses. Boot green, reload green, and the divergence invisible until a reload FAILED. The spec's own A-2 assumption asserted two producers and was never revisited when three more were converted during implementation | `lowerForPlugins` lowers once and seeds the provider from that same map, so there is one lowering and no second half to forget. `TestLowerForPluginsGivesTheProviderTheOrderARollbackReplays` asserts the order on both the coordinator's map and the map `rollbackReload` replays; it reddens against `tree.ToMap()`. The shape to look for is a new representation adopted by the forward path while the ROLLBACK, snapshot or restore path keeps building the old one |
| 2026-08-26 | - | the vendored-web pair: `run` (`scripts/vendor/check_web.go`) against `sync` (`scripts/vendor/sync_web.go`) | The check half fails closed on an empty population: "FAIL CLOSED. A run that read nothing has proven nothing, so it must not report what a run that read every copy reports". Its writing twin never got the same guard. `sync` warns and continues past every unreadable source, so a run pointed at a tree with no `third_party/web/` reads nothing, writes nothing, leaves `changed` at 0, prints "all consumer copies are up to date" and exits 0 -- byte-identical to the answer a genuinely synced tree gets. `make generate` runs it, so the state it reports success over is the state where every consumer copy is whatever it was. The check half cannot catch the follow-on either: with no source directory it errors for its OWN reason, and with a source directory present but unreadable files it reports UNREADABLE, so the two halves never meet on the case that produced the false success. Found while porting both to `vendorweb` in the former top-level tool tree for the le migration, not while looking for it | fixed IN THE PORT only, because the script is deleted at that spec's step 14 and a fix in both places would have to be kept in agreement for the fortnight between. `SyncReport.Readable` counts the sources one run read and `Sync` (`vendorweb/sync.go` in the former top-level tool tree) refuses a run that read none; `SyncReport.Text` drops the up-to-date line for the same case. `TestSyncFailsClosedWhenNoVendoredAssetIsReadable` reddens against the unguarded port, and `TestScriptSyncStillFailsOpenOnAnEmptyVendorTree` (`scripts/vendor/parity_test.go`) asserts the SCRIPT still fails open, so it goes red the day somebody fixes the script -- which is the signal to delete the script and that test together. The shape to look for: a read-only gate and its generator twin are a pair, and the empty-population guard is the one a generator's author does not think to write, because "nothing to do" and "nothing found" spell the same success |
