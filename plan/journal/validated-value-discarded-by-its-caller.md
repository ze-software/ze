# Validated value discarded by its caller

A validator that answers "yes, and here is the thing that matched" is only as
good as what the caller does with the second half. Drop it and the caller
re-derives the value from somewhere else, usually the first element of the list
it just searched. The check passes, the wrong value is used one line later, and
the failure looks like success. Distrust a verifier whose only output is an
error: what it learned has nowhere to go.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-03 | command-help-and-description | `announce ... for <duration>`, `announceAndTrack` (`internal/component/bgp/plugins/cmd/announce/announce.go`) | `parseTrailingOpts` parses and validates the lifetime, refusing a non-positive one and a seconds count that would wrap a `time.Duration`. `announceAndTrack` then returns on `opts.tagKey == ""` BEFORE it calls `reg.Announce`, which is the only site that arms the expiry timer. So `announce unicast 10.0.0.0/24 for 300s`, with no `tag`, announces the route and never withdraws it. The operator is told nothing: the answer is `announced: 1` exactly as for a lifetime that WAS armed. The two options are independent `ze:modifier "once"` siblings in `ze-cli-announce-cmd.yang`, so the model does not tie them either | not fixed. The `ze:help` written for the `for` container states the behavior as it is today, so the operator is not misled by the model. The fix needs a decision. Three answers exist: refuse `for` without `tag`, arm a timer for an untagged announcement, or make `for` imply a generated tag |
| 2026-09-03 | command-help-and-description | `-cmd.yang` and `-api.yang` leaf `description` | An author writes a `description` on a command ARGUMENT leaf and no operator ever sees it. `argDefFor` (`internal/component/config/yang/command.go:630`) builds the leaf's `command.ArgDef` from `leaf.Type` alone, and `ArgDef` (`internal/component/command/node.go:32`) declares nine fields, none of which holds text. So the description is dropped at the tree boundary, silently, and the author has no way to learn it. A config leaf does NOT share this: `entryDescription` (`internal/component/cli/completer.go:1023`) reads `entry.Description` off the goyang entry and puts it on the completion row, so the same statement in a config module renders and in a command module vanishes. Two producers, two answers, and nothing states the difference. Found while an agent was shortening command leaf descriptions to fit a row they never reach. Corroborating evidence: `internal/plugins/as112/yang/ze-as112-cmd.yang` `leaf target` carries a semicolon and no full stop, and `./le docvalid help-shape` reports zero refusals over it, which it could not do if that leaf were in the tree it walks | not fixed. The work in hand is the two-text split for the surfaces that DO render, and this is the opposite defect: text that renders nowhere. The fix is a decision, not an edit, because either `ArgDef` grows a text field and a renderer shows it, or the gate refuses a `description` on a command leaf as a declaration nothing reads. Both are larger than this spec and neither blocks it |
| 2026-08-12 | fixit-ike-negotiation-conformance | ike engine | `verifyAcceptedOffer` (`engine/initiator.go`) matched the peer's accepted offer against every ESP proposal ze had sent, and returned the one it agreed with. Two of its four call sites wrote `if _, err := verifyAcceptedOffer(...)` and then keyed from `Proposals[0]`. A peer that accepted the second proposal therefore passed the check, and ze installed the first proposal's cipher and key length. The tunnel establishes and black-holes ESP, which is worse than a refused negotiation because it reads as success. The responder half had been narrowing correctly for months | `acceptedOffer.ESPConfig` carries the configured proposal, resolved by `espConfigForAccepted`. `applyChildRekeyResponse` keys from it and `handleAuthResponse` narrows `sa.ESPGroup.Proposals` to it, which is what `selectResponderESP` already did on the other side. `TestNegInitiatorInstallsAcceptedESPSuite` drives a peer that accepts the second of two proposals and asserts the algorithms of the states ze programs |
| 2026-08-12 | - | bgp rpki rtr session | `handlePDU` (`internal/component/bgp/plugins/rpki/rtr_session.go`) parses the End of Data PDU's Refresh Interval and stores it in `s.refreshInterval`, which is then read only by `Snapshot`. `Run` waits `s.retryInterval` after a SUCCESSFUL sync as well as after a failure, so RFC 8210 Section 6 ("The router SHOULD NOT poll the cache sooner than indicated by [Refresh Interval]") is not honoured. Measured against StayRTR, which sends refresh 3600 and retry 600: ze re-polls every 600 seconds | fixed 2026-08-12: `Run` waits `pollDelay(err == nil)`, which returns the refresh interval after a completed sync and the retry interval after a failed query, the split RFC 8210 Section 6 states ("countdown ... starts upon receipt of the containing End Of Data PDU" against "starts upon failure of the query"). `pollDelay` takes the session lock, so the Run loop no longer reads an interval `handlePDU` writes under it. `TestRunPollsOnRefreshAfterSuccessRetryAfterFailure` (`rtr_poll_test.go`) drives `Run` against a listener that answers with an End of Data carrying refresh 1 and retry 7200, then against one that answers nothing with the pair inverted, and measures when the router re-connects. Each branch goes red under the other's constant. Found by the StayRTR RTR interop scenario |
| 2026-08-18 | fixit-firewall-irr-term-fails-validation | firewall-irr command forwarders | `matchCommandTokens` (`internal/component/plugin/server/command.go`) treats a key token that also names an ArgDef as a keyword introducing a value, so `update firewall irr asn 65001` binds `65001` into `ctx.Selectors` and hands the handler empty args. The four forwarders in `cmd_irr.go` passed `args` alone, so the plugin answered `usage: update firewall irr asn <asn>` for every invocation. No operator could fetch a prefix list at all, and the whole IRR feature was unusable. `show firewall irr prefix <name>` was unaffected because its leaf is `name`, which no key token spells. Every `firewall-irr-*.ci` failed on the first command it issued, unseen because all nine carry `caps=net-admin` and skip on an unprivileged host | fixed: `argsOrSelector` reads `ctx.Selector(leaf)` when args is empty, the pattern `iface/cmd/manage.go` and `l2tp/cmd/l2tp.go` already use. `TestArgsOrSelectorRecoversTheBoundValue` covers both leaves, and seven of the nine `.ci` now pass |
| 2026-08-18 | fixit-firewall-irr-term-fails-validation | firewall-irr reload | `OnConfigApply` read `pendingRefs` from the verify hook and threw it away on the next line (`_ = refs`), then called `applyTables`, which builds from `plug.config`. `plug.config` was only ever written by `OnConfigure`, so a reload applied the configuration the daemon started with: a term, an interface binding, a server or a refresh interval added by a commit never reached the plugin. The commit reported success and the sets were never built | fixed: `configure(cfg)` holds what `OnConfigure` did, `OnConfigVerify` parks the parsed config in `pendingCfg`, and `OnConfigApply` installs it. `test/plugin/firewall-irr-table-term-commit.ci` fails without it |
| 2026-08-23 | fixit-eap-tls-escape-hatch-kills-the-daemon | ike eap authenticator | `handleMethod` (`internal/component/ike/eap/eap.go`) reads `result.Err` from the method, uses it only as a boolean, and returns `s.failure(response)`. `Session` keeps no field for it and exposes no reader, so the authenticator half of every EAP method drops its own diagnosis. `handleResponderEAP` (`internal/component/ike/engine/responder_eap.go`) then logs `ike: EAP authentication failed` with the peer name and nothing else. The initiator half does not have this: `PeerSession.Process` returns `PeerResult.Err` and `handleEAPResponse` (`engine/fsm.go`) logs it. So the same refusal is diagnosable when ze initiates and invisible when ze responds, and every EAP-MSCHAPv2 authenticator failure is invisible too | fixed 2026-08-23: `Session.err` records `result.Err` in `handleMethod`, `Session.Err()` reads it, and `handleResponderEAP` logs it beside its own failure line. An EAP-Failure packet carries no reason (RFC 3748 Section 4.2), so the field is the only place one exists. `TestEAPTLSAuthenticatorKeepsItsRefusalReason` drives the real authenticator against an untrusted client chain and goes red when the assignment is removed. The first attempt SKIPPED this on the belief that `responder_eap.go` was held by another session; it was unmodified, and the held files were its siblings in the same directory. A `--file` list carries paths, not directories, so the belief was wrong twice over |
| 2026-08-26 | - | go-extract, the file-split tool agents run | `declSymbol` (the retired `scripts/dev/go_extract.go` (current producer: `internal/le/goextract/goextract.go`)) answers the name it matched, and its one caller writes `_ = name` on the next line. With the name gone, `main` cannot tell WHICH of the requested symbols it found, so its only guard is `len(extractRanges) == 0`. Ask for three declarations and mistype one, and the other two move, the file is rewritten, and the run exits 0 saying `extracted 2 symbols` -- a count nobody compares against the three that were asked for. The developer is left with a half-split file and a message saying it worked, and the missing declaration is discovered later by the compiler or not at all | fixed in the PORT only (`goextract` in the former top-level tool tree). `match` keeps the matched name, deletes it from the wanted set, and refuses the whole move naming every symbol that is not declared, sorted so two runs answer one thing. The name it kept is also the payload's `symbol` field, so what the script computed and threw away is now what `le go-extract` answers. `TestASymbolThatIsNotDeclaredRefusesTheWholeMove` drives it and asserts nothing was written. The SCRIPT keeps the defect until the swap step of the le migration, and `TestScriptGoExtractStillMovesAPartialSetOfSymbols` (the retired `scripts/dev/go_extract_parity_test.go` (current producer: `internal/le/`)) asserts that it still does |
| 2026-08-26 | - | le setup, the dev-toolchain installer | `vendor_go_deps` (the retired `scripts/le/devtools/install.py` (current producer: `internal/le/`)) reports whether `go mod tidy` and `go mod vendor` both succeeded. But `action` (the retired `scripts/le/application/setup.py` (current producer: `internal/le/`)) calls it as a bare statement. The next line derives the exit code from `report.summarise()`. Thus, a failed vendor operation changes neither the page nor the status. The run ends with `Setup complete` and exit 0, although vendor/ differs from go.mod and the tree cannot build. The module header identifies this exact defect shape as the problem that it prevents. But a fresh-box setup reports success when this failure occurs | fixed in the port only, under the 2026-08-26 rule for defects in code under migration. `(*Setup).Run` (`devsetup/setup.go` in the former top-level tool tree) reads the result from `VendorGoDeps`, records the reason, and answers 1. `TestAFailedVendorFailsTheRun` (`devsetup/devsetup_test.go` in the former top-level tool tree) pins the fix. `TestTheScriptStillReportsSuccessAfterAFailedVendor` (the retired `scripts/le/setup_parity_test.go` (current producer: `internal/le/`)) verifies that the script still fails open. Thus, the test becomes red if somebody repairs the Python |
| 2026-08-29 | fixit-linger-rejection-reaches-no-verdict | `internal/test/peer/reject.go` and `internal/test/runner/peer_contract.go`, judged by mutating a daemon filter under `test/plugin/filter-family-export-flowspec.ci` | A producer that must speak BEFORE it knows the answer makes its announcement unwithdrawable, and every later finding is then discarded in silence. `(*Peer).completed` prints the success token BEFORE the `option=linger` loop, for a correct reason: teardown is a kill, so a post-run print is lost. `failedCheckPeers` decided each check peer with a bare `strings.Contains` for that token, so a rejection the linger loop found was detected, returned by `(*Peer).rejected`, and then never consulted. Every negative assertion held open by linger was vacuous, and the published contract in `docs/architecture/testing/ci-format.md` told authors to reach for it. The vacuous population was NINE fixtures pairing `option=linger` with `reject=bgp`, not the 62 pairing linger with any `reject=`: `reject=bgp` is the only reject type ze-peer reads, and the rest are runner-side checks over the client's output that the linger loop never touched. The assumption the spec wrote down was also wrong in an instructive direction: it guessed no caller acted on the failing Result, and `zeTestPeerCommand` does act on it and exits 1. The deafness is one layer further out, in `drainPeers`, whose `_ = peers[idx].proc.Wait()` throws the peer's exit status away | fixed at `715a54fad`. The announcement stays early and the same channel gains a RETRACTION: `(*Peer).rejected` prints `RejectionMarker`, and the verdict reads it after the token and lets it win. One definition, exported from the peer package and aliased by the runner, so the halves cannot drift on a literal. Proven by mutation on 2026-08-29 rather than by a new fixture, because the runner cannot express a run that MUST fail: `handleFilterUpdate` was changed to stop stripping the matched MP attribute, the fixture went red on `filtered-peer` naming the pattern, and reverting returned it to green. The general shape to carry: when a producer must speak before it knows the answer, the consumer needs a second token whose absence is not silence |
| 2026-08-30 | flowspec-states-its-own-grammar | `handleAnnounceFlowspec` (`internal/component/bgp/plugins/cmd/announce/announce.go`) against `route.ParseExtendedCommunities` | The parser answers `(communities, consumed, error)` and every action form returns a fixed count: 1 for `discard`, 3 for `redirect`, 3 or 4 for `traffic-rate`. The caller wrote `ecs, _, err :=`. That was unreachable while the action was SYNTHESIZED, because `splitFlowspecArgs` built exactly the tokens the parser would read. This spec added `community <action>`, which hands the parser the operator's own tokens, so the discard became reachable at the moment the feature landed: `announce flowspec destination 192.0.2.0/24 community redirect 65001 100 junk` announced the redirect and dropped `junk` with no word, and the operator read success for a rule they did not describe. The pre-existing discard was not a defect of this spec and the entry point that reached it was | fixed in the same work: `consumed < len(actionArgs)` answers `errFlowspecActionExtraTokens` naming the leftover tokens, and nothing is announced. `TestFlowspecActionAcceptsACommunity` covers a word after a complete `redirect` and after a complete `traffic-rate`, and both subtests go red when the guard is disabled. The general shape to carry: a parser that answers how much it read is stating that its caller owns the rest, so a `_` in that position is a promise the caller never keeps |
