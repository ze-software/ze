| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-04 | radius-admin-interop-freeradius | `buildZe` in `internal/le/interoplab/radius/radius.go`, and the identical line in `internal/le/interoplab/ipsec/ipsec.go:288` | Both interop labs cross-compile the ze daemon with `command.Env = append(os.Environ(), "CGO_ENABLED=0", "GOOS=linux")`, so GOCACHE resolves to the MACHINE default (`~/Library/Caches/go-build` here) rather than to the checkout's `cache/go-cache`. That cache is outside everything this repository manages: `./le scratch cache-clean` does not empty it, the full-disk discipline does not measure it, and the `cache/` symlink reasoning in the rows below does not address it. Measured twice within ten minutes while another process trimmed it: the cross-compile failed with a wave of `could not import internal/race`, `package math/bits is not in std` and `no such file or directory` against Go's own standard library, which reads as a broken toolchain rather than as a cache being emptied underneath the build. The repository was at 96% on a single 927G volume at the time | fixed in the RADIUS lab only: `buildZe` now takes its environment from `gotoolchain.New(root).Environment(gotoolchain.EnvOptions{GOOS: "linux"})`, which sets GOCACHE inside the checkout and pins GOTOOLCHAIN to the one `go.mod` names. The IPsec lab carries the same line and was left alone because a peer session held that file; it is the other half of this defect and needs the same one-line change |
| 2026-09-03 | local-ca | three phase agents at once, and what a full device does to a DISCRIMINATION BREAK | Same machine and same day as the row below, and it names a cost none of the nine rows below reaches: a full device can leave the product BROKEN rather than merely unjudged. A phase agent had forced a discrimination break into `managedCertificate` (`internal/component/plugin/server/managed_serve.go`), replacing issuance with a self-signed certificate to watch its own test go red, which is exactly what `ai/rules/testing.md` asks of a new test. The device filled between the break and the revert, so every write and every Bash call failed and the agent could neither restore the file nor run `./le scratch cache-clean`. It reported the break and its pristine copy's path instead, which is the only correct move available, and the main thread restored it. Had that report been lost, the tree would carry a silent fail-open in the exact function this spec exists to make fail-closed, with the agent's own test still green because the break was reverted in the SECOND agent's imagination and not on disk. Two sibling agents died at the same moment: one lost its documentation package before writing a line, one could not write its handoff into `tmp/session/.../state/` and carried it in its report. The device then had 33.9G recovered from `cache/go-cache` alone by `./le scratch cache-clean`, so this occurrence WAS the documented remedy's population, unlike the row below | not fixed as a class. What this occurrence adds is a practice with a cheap mechanism: a forced break saves the pristine file BEFORE it edits, and reports that path if it cannot restore. Both were done here and are why the repair cost one `cp`. What would end it is an overlay: `./le rfc discriminate-record` already applies its break through a Go overlay precisely so no file on disk is modified, and `plan/journal/concurrent-session-corruption.md` (2026-09-01) asks for that route to be opened to an UNTAGGED test for a different reason. A device that fills mid-break is the second reason, and it is the one that survives a single-session machine |
| 2026-09-03 | lg-pki-certificate | the agent harness's own tool-result path under `/private/tmp/claude-502/`, and then the checkout itself | The first occurrence whose filling takes away the ability to diagnose it. The harness writes every Bash tool result to a file before the command runs, so a full device fails the CALL rather than the command: `ENOSPC ... open '.../tasks/<id>.output'`. A phase agent probed eight times, `true`, `date` and `echo hi` among them, and every one failed identically; the main thread then failed the same way on `df -h /private/tmp`. `Edit` failed too, on its own temp file beside the target, so a journal row could not be written either and a phase agent could not append its handoff to `tmp/session/.../state/` and had to carry it in its report. Only `Read` still worked. So `df`, `stat -f`, `findmnt -T` and `./le scratch cache-clean` were all unreachable, and the 2026-09-01 row's general practice, read the device the failing path is on before reaching for the documented remedy, is correct and cannot be followed: reading it requires a command. Distinct from every row below, each of which filled a device a BUILD writes to and left the agent able to run diagnostics. Measured after the owner freed space: one volume, `/dev/disk3s1s1`, 927G at 98%, carrying `/`, `/private/tmp` and the checkout alike, so on this machine the three-devices reasoning collapses to one and `/private/tmp` is not a separate device at all | not fixed, and not fixable from inside a session: freeing the device needs a command and no command runs. The owner freed 21G. Two things the class should take from it. The per-session subagent transcripts under `/private/tmp/claude-502/<checkout>/<session>/tasks/` are a large, wholly disposable consumer that nothing prunes, and this session alone wrote seven of them. And the first diagnostic has to be chosen by WHICH path failed: a path under `/private/tmp/claude-502/` belongs to the harness, so `cache-clean` and the `cache/` symlink reasoning do not address it, and on a single-volume machine no amount of cache discipline protects it from anything else on the disk |
| 2026-09-03 | spec-fixit-full-disk-verdict | `./le verify worktree`, its own disk use and its own verdict | The gate is one of the largest consumers on the machine it judges, and it never said so. Every run linked no `cache/` in its worktree, so `Overrides` resolved GOCACHE to `<worktree>/cache/go-cache` and each run built a private Go build cache from cold, measured at 7.4 GiB and 6.7 GiB in the two worktrees present, against a 0.6 GiB source tree, then deleted it unread. Beside them sat an 8.27 GiB abandoned worktree the sweep preserves by design because it is dirty, named in the report by directory name alone, with no path, age, size or shape of its dirt. When the device then filled, the run said `verify-worktree: full exit=2` and did not exit 2: `report.Code` was printed and then overwritten to 1 by the log-save branch and again by the deferred cleanup branch. One layer down, `runMode` flattened every non-zero stage code to 1, destroying the staticcheck matrix's deliberate 2 for "could not be judged". So a run that judged NOTHING answered with the code a red answers, twice over | The worktree links its `cache/` to the shared per-user target at extraction (`sharedCacheLink` in `internal/le/verify/lifecycle.go`, calling the new cache-only `EnsureCache` in `internal/le/scratch`), and `tmp/` is deliberately not linked with it. `runMode` stops flattening: a stage that answers 2 makes the run UNJUDGED, exit code 3, and a stage that judged and failed still outranks it. A full device is classified by `errors.Is(err, syscall.ENOSPC)` at the write sites that hold the typed error (`Defeated`, `internal/le/verify/engine/run.go`), never by matching text, because no stage output reaches a `Report`. The verdict line moved into the first deferred call, so it is the last thing printed and states the status the process exits with. A preserved worktree is now reported with its path, its age, its size and whether its dirt is modified or untracked content rather than deletions alone; the 8.27 GiB one was 264 deletions, 0 modified, 0 untracked. The occurrence count that four surfaces carried by hand is gone: this file is the count |
| 2026-09-03 | bcp194-7-transit-asn | `./le integration interop`, every scenario | Seventh occurrence, and the sequel to the row below: the colima datadisk did not merely stop growing, it took the guest filesystem down with it. `docker build` answers `mkdir /var/lib/docker/tmp/docker-builder...: input/output error`, `docker images` and `docker system df` answer blob-not-found on the containerd content store, and `docker ps -a` reports a container Up that `docker exec` says is not running. The guest kernel explains all three: `EXT4-fs error (device vdb1): ext4_journal_check_start: Detected aborted journal`, preceded by write I/O errors on both vda and vdb, timestamped about seven hours before this session. `mount` still says `rw`, which is why the state reads as Docker corruption rather than as a dead filesystem; the one-line test that settles it is `colima ssh -- sudo dd if=/dev/zero of=/var/lib/docker/tmp/probe bs=1M count=64`, which answers `Input/output error`. So no interop or IPsec scenario can run in this checkout until the VM restarts and ext4 replays | RECOVERED WITHOUT A RESTART, and this correction is the point of the row. The first diagnosis called `colima restart` the only repair and held the interop work back for it, on the reasoning that the aborted journal survives until the filesystem is unmounted. Measured a few hours later: no restart was run, the `ze-kernel-builder` container had exited on its own, and the same `dd` probe wrote 8MB at 1.2 GB/s. `docker build` then succeeded and `bgp-path-asn-leak-frr` ran four times, green and red as instructed. So an aborted ext4 journal inside colima CAN clear once the writer that filled the disk exits, and the recovery costs nothing. What the class should take from this: run the `dd` probe again before you report the VM as needing owner action, because the probe is one line and the wrong report costs a session its interop evidence |
| 2026-09-02 | none (walked into running the QEMU proof for the never-guess-a-NIC work) | `ze appliance kernel --target runtime`, `kernelbuilder.dockerArgs` (`internal/appliance/kernelbuilder/driver.go:262`) | Sixth occurrence, and the first where the store that filled is NOT a Go cache. The kernel builder mounts named Docker volumes (`ze-kernel-build:/tmp/kbuild`, `ze-kernel-work:/build`), so the build tree lands inside the colima VM, whose disk is ONE SPARSE FILE on the host (`~/.colima/_lima/_disks/colima/datadisk`, apparent 100G, allocated 45G) that grows on guest writes and never shrinks without fstrim. Two kernel builds took `/` from workable to 119M free of 927G. The failure then surfaced as Docker corruption, not as ENOSPC: `write /var/lib/containerd/io.containerd.metadata.v1.bolt/meta.db: input/output error` and blob-not-found on the content store, because the sparse file could no longer grow. `docker system df` could not even list images. Nothing had checked free space before starting a build that can consume 40G+ | NOT fixed. The bound this class has asked for since the 2026-08-18 row still does not exist, and this row widens what it must cover: the colima datadisk is a second unbounded store, and unlike a Go cache it does not release space when its contents are deleted. `freeBytes` (`internal/le/scratch/cacheclean.go:201`) is the primitive a preflight would use and today it only REPORTS inside cache-clean, gating nothing. Producers read 2026-09-02 |
| 2026-09-01 | rfc-ledger-single-declaration | `./le verify worktree`, twice | The lint matrix reported `could not import os` and `could not import internal/bytealg` under `internal/core/crashlog`, which is the whole-toolchain false red this class collects, and golangci-lint logged `Failed to persist facts to cache: ... no space left on device`. The first run had already died differently, on `copy stage logs: mkdir tmp/verify-worktree-logs/...: no space left on device`. What makes this row worth a line rather than a fifth tally mark: the documented remedy does NOT apply. `CLAUDE.md` and `docs/contributing/running-commands.md` both send a reader to `stat -f cache/go-cache` and `./le scratch cache-clean`, on the reasoning that `cache/` is a symlink onto another filesystem. That reasoning held and the disk was innocent: `cache/go-cache` had 210 GB free of 294. The filesystem that filled was the one under `tmp/`, at 5.9 GB free of 98, and `cache-clean` empties neither of its two consumers -- `tmp/session` at 6.8 GB across 27 per-session scratch directories, 17 of them from previous days, and `tmp/golangci-lint-cache` at 5.9 GB | not fixed, and deliberately not: both consumers are shared, three sessions were active in this checkout, and one was running golangci-lint against that very cache. Deleting another session's scratch or a cache a live run depends on is exactly what `ai/rules/never-destroy-work.md` reserves to the owner. Reported instead. The repair this class is still missing is a `./le scratch` action for the OTHER filesystem: `cache-clean` names the two Go build caches and nothing prunes `tmp/session` by age, so a checkout accumulates one scratch directory per session forever and the first symptom is an unrelated package failing to import `os`. The general practice this row sharpens: `no space left on device` names a DEVICE, and this repository has three under one working directory -- the checkout, the `cache/` symlink target and the `tmp/` symlink target. Read the one the failing path is on before reaching for the documented remedy, because the documented remedy addresses only one of the three |
| 2026-07-30 | - | build | `./le verify current mode full` reported build failures across unrelated packages | ran `go clean -cache` and `docker builder prune -f` |
| 2026-08-18 | - | `./le repository tracked-build check` | The check ran twice after a commit deleting a Go package and failed both times, which reads exactly like the deletion broke the build. It did not: `go vet ./internal/component/bgp/rib/...` exits 0 on the same tree. The real failure is at the bottom of its log, where the check untars the tracked population into a clean directory: `tar: website: Cannot mkdir: No space left on device`, then `exit status 2`. `/` was at 100% with 128M free. The gate's own output makes this hard to see, because tar reports one line per unreachable path first, so the log opens with dozens of `Cannot open: No such file or directory` lines naming website content and closes with the one line that explains them. Reading the tail is what finds it | mitigated, not fixed. `find ~/.cache/ze/go-cache -type f -mtime +2 -delete`, the remedy the 2026-08-15 row records, freed 586M and left 714M free against a shared cache that `du` still reports as 106G. That is the third occurrence of this class and the second where a full disk was first read as a code defect. The trim is a remedy an agent runs after losing time, not a bound: nothing caps that cache, so it grows until it fills the disk again |
| 2026-08-20 | - | `./le verify current mode full`, twice | Eight stages red at once: the staticcheck matrix, tracked-build, doc-verify, hook-test, govulncheck, unit, functional and exabgp. Every message was `no space left on device` or `cache entry not found` against `cache/go-cache`. `df -h /` said 24G free and 75% used, which is why the first read was a code defect again. The disk that was full is a DIFFERENT one: `~/.cache` is its own filesystem (`/dev/sdb`, 295G) and `cache/` is a symlink into it, so `df` on the repository path answers about the wrong device. `stat -f` on `~/.cache` reported `blocks_free=0` and an 8MB `dd` probe wrote 0 bytes. `~/.cache/ze/go-cache` held 261G of a 295G disk. Fourth occurrence of this class, and the third time it was first read as a code defect | `GOCACHE="$PWD/cache/go-cache" go clean -cache` freed 256G. The bound this class asked for in the 2026-08-18 row EXISTS and did not run: `ze-cadence-daily-run` (the retired `mk/schedule-cadence.mk` (current producer: `internal/le/testhealth/modes.go`)) empties both Go caches, and its own comment calls it "run this one every morning", so it is advisory and nothing schedules it. `make clean` runs the same pair; `make clean-all` deliberately does NOT (the retired `Makefile` (current producers: `internal/le/` native action tables), the comment above `clean-all`: "It NEVER touches the durable cache/"), so the bigger-sounding target does less for the cache that fills. Two diagnostics belong in the first minute: `stat -f` on the path that failed rather than `df` on the repository, and `findmnt -T` to learn which device that path is even on |
| 2026-08-15 | fixit-peer-process-event-filter | plugin functional suite, linker | The root filesystem reached 100% (544M free of 927G) mid-session. the retired `ze-functional-plugin-test` (current: `./le functional plugin`) reported 54 failures and 9 timeouts, and the runner could not link a daemon at all (`link: mapping output file failed: no space left on device`). The failures read exactly like a behaviour regression, and a whole suite run was spent judging them before the disk was looked at | freed 4G by deleting entries older than two days from the shared cache (`find ~/.cache/ze/go-cache -type f -mtime +2 -delete`), which is what the toolchain's own trimmer does and costs only recompilation. The suite then gave a real verdict. `df -h .` belongs in the first minute of diagnosing any wave of unrelated functional failures |
| 2026-09-04 | local-ca | `go test ./internal/component/pki/`, the toolchain's own packages | One package test came back as a wave of import failures inside the standard library: `could not import cmp`, `could not import sync`, `could not import internal/runtime/math`, each naming a missing `~/Library/Caches/go-build/<xx>/<hash>-d` file, plus `invalid operation: x[i] == target (incomparable types in type set)` from `slices/sort.go`. Nothing in the diff touches any of those. `stat -f cache/go-cache` read 0 blocks available; the root filesystem was at 98%, 25G free of 927G | fixed by `./le scratch cache-clean`, which freed 16.7G on the checkout cache and left 40.3G free. The ambient cache REFUSED (`go: unlinkat /Users/thomas/Library/Caches/go-build/03: directory not empty`), a concurrent session writing into it while the clean walked it, and the checkout half was enough. The tell was the toolchain's own packages failing to import, which no repository change can cause |
