| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-15 | the-fix-pass-for-restated-registries | the 230 rows `./le enumeration report` answered on 2026-09-14, across every corpus: doctor checks, plugin names, family names, CLI verbs, YANG enumerations | Two traps caught the pass, and both are about which side DECLARES. First, the closed corpus flags a Go table that holds every value of a YANG enumeration and says "the two must agree", and in 64 of 72 rows the Go side is the declaration (it carries the wire value, the IANA number, the kernel name or the handler) while the model is the copy, so "derive from the registry" is the WRONG repair there: it would move a wire constant into a schema. Second, a family name derived in a package that links no NLRI plugin renders `afi-1/safi-128`, because only four families register outside the `ze_bgp` tag (`internal/core/family/registry.go`), and `kernelcap.labeledFamilies` (`internal/component/kernelcap/predicate.go`) decides whether the kernel is asked for an AF_MPLS table, so a registry miss there answers "no MPLS needed": a derivation would have turned a literal into a fail-open guard | 64 rows carry an agreement test in the owning package that loads the module, reads the enum at the leaf path and compares both ways, and the gate learned `enumeration: gated by TestX`, honoured only on a closed-corpus row, only when the test is declared in the unit's own package (or the spelled `<dir>:TestX`), red where it names nothing (`readGatedMarker`, `internal/le/enumeration/enumeration.go`). The 7 family rows stay findings until the standard families register outside the tag (`plan/spec-standard-families-register-outside-the-bgp-tag.md`). The lesson: before deriving, ask which side holds the fact the other cannot; when the literal does, the fix is a test that pins the copy to it, never a derivation |
| 2026-09-09 | pppoe-discovery-omits-the-mandatory-service-name-tag | `netnsSelections[netnsPPPoE]` (`internal/le/qemu/netns_linux.go`) against `test/pppoe/*.ci` | The one verb that can run a PPPoE `.ci` test, `./le qemu pppoe-test`, selects its tests from a hand-written name slice rather than from the directory the tests live in: `runNetnsSuite` passes the slice verbatim to `ze-test pppoe -p 1 <names...>`. A new `.ci` under `test/pppoe/` therefore runs nowhere and reports nothing until somebody edits a file in another area, and no gate names the omission. `vmSuites` (`internal/le/qemu/alltests.go`) already keys the same suites by name, so the directory is the only declaration that cannot go stale | not fixed here: one row, per `ai/rules/completion.md`. The spec's own `pppoe-service-name` entry WAS added to the slice, so this work is wired; the class is the slice. The fix reads `test/pppoe/*.ci` for the selection, the way the suite directory already declares its own membership |
| 2026-09-08 | vrrp-deferred-accept-mode-dataplane | the `vrrp-keepalived-test` action's `Why` and its `scenarios` parameter placeholder (`internal/le/qemu/actions.go`) | Both enumerate three scenarios, `QS-1`, `QS-2` and `QS-3`, while `vrrpScenarioNames` (`internal/le/qemu/guestlabs.go`) holds four: `parseClosedCSV` accepts `tracked-uplink-hands-the-vip-to-keepalived` and the action's own help never names it, so a reader of `./le qemu` cannot discover the scenario added on 2026-09-08. The placeholder is a hand-written copy of a list one file away, and `runVRRPHere` already reads that list. Walked into while running the scenario for the first time | not fixed here: one row, per `ai/rules/completion.md`, and `actions.go` sits outside this work package's file scope. FIXED the same day, in the work that found it: the `Value` is now `strings.Join(vrrpScenarioNames, ",")` and the `Why` states what the lab proves without listing its scenarios |
| 2026-09-07 | connected-static-reach-the-locrib | `internal/plugins/ospf/register.go:117`, `internal/plugins/isis/register.go:141` | Both route producers name `fib-kernel` in `Dependencies`, so an OSPF or IS-IS deployment whose `interface { backend }` is `vpp` loads the KERNEL writer and programs the Linux kernel of a box whose data plane is VPP. The registry now answers the question they hardcode: `Registration.NeedsDataPlane` on the producer plus `Registration.DataPlane` on the writer, resolved against the configured backend (`appendDataPlaneWriter`, `internal/component/plugin/server/startup_autoload.go`) | not fixed here: one row, per `ai/rules/completion.md`. The fix is `NeedsDataPlane: true` in place of the `fib-kernel` dependency on both, and a test that a vpp-backend config loads `fib-vpp` |
| 2026-08-31 | yang-short-and-long-command-help | `internal/le/docvalid/register.go` `Description` | The command's one-line description enumerates three of the gates the action table now holds (`command-contract`, `doc-drift`, the operator table), so `usage-contract` and `help-shape` are invisible to a reader of `le --list`. The same sentence is copied into the generated `ai/PACKAGE-MAP.md` and `ai/INDEX.md` rows for `internal/le/docvalid`, so correcting it costs a regeneration of two indexes another session holds modified. `SubsFunc: Subs` in the same `init()` already derives the verb list from the table | not fixed here: one row, per `ai/rules/completion.md`. The fix is a description that states what the area does without naming its actions, plus `./le discovery-index update` |
| 2026-04-03 | - | tests | adding a plugin broke `TestAllPluginsRegistered` | updated `all/all_test.go` |
| 2026-04-11 | - | tests | fixing one plugin test list broke the other | updated both files in the same commit |
| 2026-08-10 | fixit-vpp-ipsec-inoperable | tests | the retired `scripts/evidence/effective-vpp.py` (current producer: `internal/le/deployment/vppevidence.go`) built its `ze` with a hardcoded `ze_core,ze_distro`, so every gated feature was missing and `./le deployment vpp-test` died on `unknown top-level keyword: bgp`; its firewall fixture had also drifted from the YANG grammar unnoticed underneath that | `feature_tags()` derives the tags from `feature-gates.txt`, the source `ZE_FEATURES` reads, and the fixture keywords were corrected |
| 2026-08-26 | le-is-a-ze-binary | le migration census and parity tests | Two hand-written lists became stale as soon as a port moved a gate. Each failure reached the next person who ran the tests. `forkedToday` (`census_test.go` in the former standalone composition) still named `./le deployment l2tp-ppp-test` after `deployment/actions.go` in the former top-level tool tree gave it a Go answer. `TestEveryIntegrationGateOfTheScriptIsServedSomewhere` (the retired `scripts/le/functional_parity_test.go` (current producer: `internal/le/`)) read three areas for gates absent from `integration` in the former top-level tool tree. But `qemu` in the former top-level tool tree had owned `./le qemu vpp-hugepages-test` for three hours | both fixed here. The stale row was removed. The served set now reads deployment, evidence, and qemu from one loop over their action tables. `forkedToday` intentionally remains asserted. Derivation from the table that the census reads would make both sides agree by construction |
