| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-04-03 | - | tests | adding a plugin broke `TestAllPluginsRegistered` | updated `all/all_test.go` |
| 2026-04-11 | - | tests | fixing one plugin test list broke the other | updated both files in the same commit |
| 2026-08-10 | fixit-vpp-ipsec-inoperable | tests | `scripts/evidence/effective-vpp.py` built its `ze` with a hardcoded `ze_core,ze_distro`, so every gated feature was missing and `make ze-deployment-vpp-test` died on `unknown top-level keyword: bgp`; its firewall fixture had also drifted from the YANG grammar unnoticed underneath that | `feature_tags()` derives the tags from `feature-gates.txt`, the source `ZE_FEATURES` reads, and the fixture keywords were corrected |
| 2026-08-26 | le-is-a-ze-binary | le migration census and parity tests | Two hand-written lists became stale as soon as a port moved a gate. Each failure reached the next person who ran the tests. `forkedToday` (`census_test.go` in the former standalone composition) still named `ze-deployment-l2tp-ppp-test` after `deployment/actions.go` in the former top-level tool tree gave it a Go answer. `TestEveryIntegrationGateOfTheScriptIsServedSomewhere` (`scripts/le/functional_parity_test.go`) read three areas for gates absent from `integration` in the former top-level tool tree. But `qemu` in the former top-level tool tree had owned `ze-qemu-vpp-hugepages-test` for three hours | both fixed here. The stale row was removed. The served set now reads deployment, evidence, and qemu from one loop over their action tables. `forkedToday` intentionally remains asserted. Derivation from the table that the census reads would make both sides agree by construction |
