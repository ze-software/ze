| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-22 | - | `runReloadContext` (`cmd/ze/hub/main_reload.go`) and RADIUS `runPlugin` (`internal/component/l2tp/plugins/authradius/register.go`) | Inner config commit retired accounting before outer listener migration and candidate promotion could reject the reload. Rollback restored a provider but had already lost active accounting sessions; synchronous removal also waited on network Stops during apply. | Preserve sessions through tentative removal and retire asynchronously after matching whole-reload acceptance. Source-reviewed actual-entry RED/GREEN used UDP accounting and a failing listener bind, without a daemon or live PPP peer. |
| 2026-09-22 | - | `seedReloadTransactions` and `recordReloadTransaction` (`internal/component/plugin/server/reload_tx.go`) | An unrelated participant transaction lost the pending RADIUS removal's acceptance ownership, so successful outer acceptance did not retire its accounting. | Preserve per-participant transaction ownership and clone it for independent outer scopes. Source review and actual-entry RED/GREEN were observed. |
| 2026-09-22 | - | RADIUS acceptance and rollback callbacks in `runPlugin` (`internal/component/l2tp/plugins/authradius/register.go`) | Acceptance of saved prior state was forgotten while a newer apply remained tentative. Rolling back the newer apply restored that state without retiring the accounting already accepted for removal. | Remember prior-state acceptance and consume it on restoration without retiring the tentative replacement. Source-reviewed actual-entry RED/GREEN was observed. |
| 2026-08-17 | spec-hub-deferred-api-auth-independent-of-ssh-block | management listener reload | `listenerMigrator.migrateListeners` tried to restore applied listener moves after a later move failed, but returned only an error when that restore also failed. `runReloadContext` therefore received a no-op undo and restored accepted API credentials while a listener could remain on its candidate address | `migrateListeners` now returns a retryable undo for the applied set. `runReloadContext` keeps API authentication staged unless that outer restore succeeds. `TestRunReloadInternalAndRetryListenerRollbackFailureStaysFailClosed` covers both failed restore attempts |
| 2026-08-21 | - | `cleanupProcess` and `rollbackStartupProcess` (`internal/component/plugin/server/dispatch.go`, `startup.go`), `PluginRegistry.Register` (`internal/component/plugin/registration.go:190`) | the two teardown paths clean different registries. `rollbackStartupProcess` calls `s.registry.Unregister`; `cleanupProcess`, which is what a crashed process reaches, clears the dispatcher registry and leaves the `PluginRegistry` row standing. `Register` then refuses any command already in its table and has no exemption for the same plugin re-registering, so it answers `command conflict: %q already registered by %s` naming the plugin against itself. Both halves are read and verified | NOT verified, and stated as the reason the row is not stronger: whether a respawn re-enters the 5-stage handshake and reaches `Register` at all was not traced. `ProcessManager.Respawn` builds a new process and calls `StartWithContext`, and its one caller is `restartPlugin` (`reload_tx.go:410`) on a CodeBroken ack. If it does re-enter, a crashed plugin can never come back. Next step: drive `restartPlugin` against a plugin that declares a command and see whether Stage 1 refuses it. Found while adding alias removal, which deliberately did NOT hook `cleanupProcess`, because that would make the alias the one registration leaving on a path the others stay on |
| 2026-08-25 | - | `migrate` (the retired `scripts/dev/ensure-links.py` (current producer: `internal/le/scratch/scratch.go`)), `./le scratch migrate` | `migrate` converts a real `tmp/` into a symlink by moving entries one at a time with `shutil.move`, and it has no undo. Across devices, and `tmp/` on `/dev/sda2` against `~/.cache` on `/dev/sdb` always is, `shutil.move` is copytree-then-rmtree, so a failure at entry N leaves N-1 entries moved, entry N copied AND partly deleted at the source, and the rest in place. Nothing records how far it got. It failed twice in one day. The first run died on root-owned paths under `tmp/evidence` left by sudo-run VPP and gokrazy tests; the second on `PermissionError: 'asm'` while removing `tmp/qemu`. Each time the entries it had already moved included every session's `tmp/commit-session-id-*` and every prepared `tmp/commit-*.sh`, so `commit_helper.py session` on a shared checkout stopped resolving and no session could commit until the move was undone by hand. The second run also carried `tmp/kernel` out, which is where `_ze-qemu-*-test-impl` reads `vmlinuz`. The root cause is not the permissions it happened to trip on: **Go writes `gomodcache` directories mode 0555 on purpose**, 4005 of them under `tmp/qemu`, and `rmtree` cannot unlink a child of a 0555 directory, so a cross-device move of any tree holding a module cache cannot complete. The `go mod download all` recipe in the retired `mk/build-gokrazy.mk` (current producer: `internal/appliance/cmd_build.go`) already sets `GOFLAGS=-modcacherw` for its own downloads for this reason | not fixed. `~/bin/./le scratch migrate-when-quiet` now refuses BEFORE moving anything: it tests deletability (every directory writable and executable) rather than readability, which is the check that would have caught this, and its message names the module cache. That was a wrapper, not a repair. FIXED in the script itself on 2026-08-25, by the second of the two routes this row named: `first_undeletable_dir` (the retired `scripts/dev/ensure-links.py` (current producer: `internal/le/scratch/scratch.go`)) walks the tree before `migrate` moves anything and refuses when a directory is not writable and searchable, naming the directory and the module cache. Unlinking a file needs write and execute on its PARENT, never any permission on the file, so the check asks about directories; asking about readability answers a different question, which is the mistake the wrapper's first version made. `migrate` still has no rollback and still no record of its progress, and that is deliberate rather than deferred: the realistic failure is now impossible to start, and a stage-and-swap would not have helped, because the source `rmtree` is the half that fails. `MigrateRefusesAnUndeletableTreeTest` (the retired `scripts/dev/ensure_links_test.py` (current producer: `internal/le/`)) holds both directions, and it discriminates: disabling the pre-flight turns it red with `migrated tmp: moved 2 entries`, the script reporting success over a tree it cannot move. Still open, and still worth an answer before anyone runs this: whether `tmp/` is worth migrating at all, since `~/.cache/go-build` already lives on the second drive and `tmp/` is scratch four sessions rewrite continuously |
| 2026-09-22 | - | RPKI registered configuration callbacks | A rollback before the current transaction applied could restore the previous successful transaction's policy. | Clear the saved rollback candidate at the start of verification. The callback regression passed with the fix and failed when a Go overlay removed that reset. |
| 2026-09-23 | - | VRRP shared-sysctl setup and teardown (`internal/plugins/vrrp/dataplane_linux.go`) | Failed restoration discarded the original values at zero group references, so retry could save a leftover recipe value as the original. | Retain only failed keys independently of group references; retry before recapture and on unreferenced teardown. Added rollback-failure, teardown-retry and active-parent regressions in `dataplane_failure_linux_test.go`; not run by this worker. |
