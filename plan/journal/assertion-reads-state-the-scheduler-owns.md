| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-04 | spec-radius-acct-session-attributes | `TestRFC2869InterimLoopKeepsOneGenerationOutstanding` (`internal/component/l2tp/plugins/authradius/rfc2869_interim_generation_test.go`) | The same case, a third, fourth and fifth time, on a checkout carrying six concurrent agent sessions: 14 datagrams in a plain run, 20 under `-race`, and 16 under `-race` again in a later session whose own change cannot reach this loop (it emits the session-down that STOPS the interim loop, so it can only lower the count). All against the same limit of 12, and it passed in the plain run either side of the first two failures. Confirms the 2026-08-31 row's reading: the bound is wall-clock and the box's load moves it | not fixed, and the reason has not changed: the file carries `RFC requirement:` tags, so `internal/le/commit/rfcchange.go` and the write hook both refuse an edit without an owner row in `test/rfc-changed.md`, which no author writes for themselves. Verified as not mine the same way: the retransmission generation this case measures is produced by `(*Client).SendToServers` (`internal/component/radius/client.go`), which this spec's phases 1 to 3 do not touch, and `interimLoop` is byte-identical to HEAD apart from the two attributes `buildAcctPacket` now appends |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `TestRFC2869InterimLoopKeepsOneGenerationOutstanding` (`internal/component/l2tp/plugins/authradius/rfc2869_interim_generation_test.go`) | The case bounds the interim send rate by the retransmission budget: at a 10ms tick with a 100ms timeout and 2 retries it admits at most 12 datagrams in one second, and one generation is meant to occupy the queue for about 300ms. It failed twice at 14 and at 18 datagrams while the box carried a load average above 17, then passed five runs out of five ten minutes later on the same tree. 18 datagrams is six generations in a second, so a generation finished in about 166ms rather than 300ms. A starved goroutine meets per-attempt deadlines that have already passed, and each attempt then returns at once, so CPU starvation makes the sender look FASTER and drives the count up. The bound is written as a ceiling on datagrams, which is the one direction load can break | not fixed. The file carries `RFC requirement:` tags, so the write hook refuses every edit to it without an owner row in `test/rfc-changed.md`, and this session may not write that row for itself. Whoever asks Thomas about this file carries the fix: bound the rate against the OBSERVED budget rather than against wall-clock time, or give the case a clock it owns. Verified as not mine: `interimLoop`'s body is byte-identical to HEAD, and the case never reaches `acctInterval`, `onSessionIPAssigned` or `parseConfigFromTree` |
| 2026-08-22 | - | `TestWriteAnswerUsesPooledBuffer`, `TestWriteAnswerReleasesBufferOnError` (`internal/component/plugin/dispatch_test.go`) | A test proved a pooled buffer's round trip by state. It put a marked buffer into the `sync.Pool`, wrote one answer, then asked the pool again. It passed alone and failed one run in three for the whole package under `-race`, on a different subtest each time, with the returned buffer full of zeros. A `sync.Pool` is per-P and its private slot cannot be stolen. A goroutine the scheduler moves between the release and the next Get therefore reads an empty pool, through no fault of the writer. A pool of its own did not fix it, and neither did `debug.SetGCPercent(-1)`. The scheduler owns which P answers, so no state read of a pool says anything about the code that used it. Found in phase 2 of spec-record-answers-3-zero-alloc | measured the answer's ALLOCATION COUNT instead, with `testing.AllocsPerRun`. It pins GOMAXPROCS for its runs and truncates its average to an integer. A buffer the writer allocates, or fails to return, then reads as exactly one more, and a stray collection reads as nothing. Five of five whole-package runs under `-race` were green after the change. The race detector allocates on its own account, up to two on these answers, so the ceilings are exact on the pass without it and `answerAllocRaceOverhead` gives the changed-group pass that slack |
