# documentation shows config the parser refuses

An operator copies what a guide prints. A guide that prints config `ze config
validate` rejects has published a defect, and nothing in this repository parses
a config example, so it survives until a reader tries it.

The second reader of a refused example is not an operator, it is a review. A
review lens cited invalid guide lines as evidence that a config shape was live
in the tree, and those lines had never parsed. Inside this repository a refused
example is false evidence about the product, and whoever reasons quickly quotes
it as a producer.

| Date | Spec | Surface | Symptom | Fix |
| 2026-08-31 | - | `docs/contributing/running-commands.md`, "Testing one package while you develop it" | The page publishes `./le job run label unit-pkg command go test PKG=<pattern>` as "the supported route" for testing one package, with `RUN=<regexp>` and `RACE=0` beside it, and claims the route carries the feature tags, the timeout and the process ceiling. None of it holds. `parseRun` (`internal/le/job/answer.go`) takes everything after the `command` keyword as the child's argv verbatim, and `Admission.Run` (`internal/le/job/job.go`) streams that argv unchanged: no tags, no `-race`, no timeout, no interpretation of `PKG=` or `RUN=`. Both documented example lines fail on the first try with `malformed import path "PKG=./internal/component/ike/eap": trailing dot in path element`. The `PKG=` spelling is real, but it belongs to `./le fuzz`, which declares it as an argument alias (`Aliases: []string{"PKG"}`, `internal/le/fuzz/actions.go`). Found on the first inner-loop test run of this spec, by copying the page | fixed on the spot, under `ai/rules/documentation.md`: the section now states that argv passes through unchanged, names the two producing functions, says where `PKG=` really belongs, and its examples pass `-race`, `-tags "$tags"` and `-run` in the spellings `go test` reads. The transferable half: a page that documents a wrapper is checked by nothing, so a wrapper that stops interpreting its arguments leaves the page describing a grammar that no longer exists |
| 2026-08-24 | plugin-declares-answer-shape | plugin IPC protocol documentation | `docs/architecture/api/ipc_protocol.md` publishes a table of text-protocol verbs whose rows include `register command "<name>" description "<help>" [args "<usage>"] [completable] [timeout <dur>]` and `unregister command "<name>"`, and `docs/architecture/api/architecture.md` prints the same line as a worked example. The one Go reference to that grammar is a tokenizer CASE in `internal/component/plugin/server/command_test.go`, so nothing in the shipped engine parses it. The same page states two paragraphs earlier that there is no separate text mode and that Stage 1 registration travels in `ze-plugin-engine:declare-registration`, which is the mechanism that does exist. A plugin author reading the table would write a registration the daemon never reads. Found while adding the answer-shape fields to the Stage 1 documentation, because the field table had to go on the page that describes registration and two pages described it two ways | not fixed. It blocks no acceptance criterion of the spec that found it, and it predates that spec by a long way: the text form is either a removed mechanism whose documentation survived it or one that never shipped, and telling those apart needs the history rather than the tree. The fix is a deletion or a restoration and the choice is not the finder's to make. Recorded here rather than repaired inside a documentation phase whose subject was a different channel |
|------|------|---------|---------|-----|
| 2026-08-24 | website-asciinema-terminal-demos | published demo `config-graph`, its tape, its validator and its transcript | `demos/terminal/config-graph/validate.sh` runs `ze config graph router.conf then ze pipe match peer/upstream` and the CLI answers `pipe error: match needs rows, and this answer has none: it holds several lists (edges, nodes), so select one first`, exit 1. So the demo cannot be rendered at all: `render.py --demo config-graph` fails in validation before any recorder starts. The refusal is deliberate and is the newer behaviour: `rowOperatorRefusal` (`internal/component/command/pipe.go`) arrived in `6b0eb49e3` on 2026-08-23, and its own comment states the reason, which is that an operator the answer cannot support is refused by name because answering something plausible is worse. `ze config graph` emits one object holding `nodes` and `edges`, which is exactly the shape that refusal names. The published `config-graph.webm` is dated 2026-08-19, four days before the change, so the site shows a session the current CLI will not produce. Three artifacts encode the old grammar and all three move together: the tape's three `ze config graph ... then ze pipe match ...` lines, the validator's three assertions, and `transcript.txt` lines 6 to 8, which quote the same three commands | FIXED the same day. The owner lifted the spec's own no-tape-edits rule for this one demo (D-8), and tape, validator and transcript moved together. The three views read `ze config graph router.conf then ze pipe text then ze pipe match <selector>`: `then text` renders both lists as aligned rows and `match` keeps whole lines, so each answer now shows a complete relationship where the old grep over pretty JSON showed lone fields. The demo renders and its cast passes the transcript gate. What the refusal ASKS for still does not exist: no pipe operator selects one list out of a document, so a caller meeting this message has to render the answer before filtering it |
| 2026-08-15 | fixit-peer-process-event-filter | docs/guide/plugins.md | attach and plugin examples written as one-line blocks without the closing semicolon, which the parser refuses (automatic semicolon insertion fires at a newline); one example also named peers by IP address, which the peer name validator refuses | rewritten multi-line and verified with a built `ze config validate` |
| 2026-08-15 | fixit-peer-process-event-filter | docs/guide/ospf.md | same one-line block form outside the plugin surface: `ospf { extended-prefix true }`, `nssa { default-originate true }`, and `redistribute { destination ospf { import connected static bgp } }` are each refused | recorded, not fixed |
| 2026-08-15 | fixit-peer-process-event-filter | docs/architecture/api/process-protocol.md | `plugin { external my-traffic { declares ipv4/flow } }` names a leaf the external list does not have, so the example is refused whatever its formatting | recorded, not fixed |
| 2026-08-15 | fixit-peer-process-event-filter | docs/guide/flowspec-protected-router.md | the copy-paste example sets `ddos flowspec allowlist`, a leaf removed on 2026-07-12 and replaced by the shared ddos/detect traffic policy, so the whole example is refused with unknown field allowlist | recorded, not fixed |
| 2026-08-15 | fixit-peer-process-event-filter | docs/guide/rpki.md | main peer example written in the retired shape `peer { remote { } local { } router-id }`, refused with `unknown field in peer: remote`; the current schema puts them under `connection` and `session`. This spec had edited the attach block INSIDE that example, which repairs one line of a config nobody can load | fixed and verified: the block extracted verbatim from the page answers `configuration valid` |
| 2026-08-15 | fixit-peer-process-event-filter | 15 changed files under docs/ | the same retired peer shape, counted rather than sampled: DESIGN.md, architecture/config/syntax.md, architecture/overview.md, architecture/testing/ci-format.md, config-reference.md, features/interfaces.md, functional-tests.md, guide/configuration.md, guide/graceful-restart.md, guide/ipsec.md, guide/irr-filtering.md, guide/plugins.md, guide/quickstart.md, guide/route-reflection.md, guide/rpki.md. It predates this spec (the shape is at origin/main) and is a documentation sweep of its own size | recorded, not fixed; rpki.md alone is fixed above |
| 2026-08-15 | bgp-decode-render | docs/guide/logging.md | the runtime log-level section prints `ze cli -c "bgp log set bgp.fsm debug"` and `ze cli -c "bgp log levels"` four times over. Neither is a command. The YANG registers `request log level <logger> <level>` and `show log levels` (`internal/plugins/log/yang/ze-log-cmd.yang`, containers `request/log/level` and `show/log/levels`), and `ze-bgp:log-set` is the internal wire method, not an operator spelling. The root `bgp` handler (`Run`, `internal/component/bgp/cli/main.go`) accepts only decode, encode, plugin and help, so the documented form answers `unknown command: log`. This is a CLI surface rather than config, so `ze config validate` was never going to catch it | to be fixed by `plan/spec-bgp-decode-render.md`, which edits this page anyway to add the `bgp.wire` subsystem row |
| 2026-08-18 | - | docs/guide/firewall.md | The IRR operator workflow's step 3, "Commit config with `source-asn 13335` in a term's from-block", is a config no operator can commit. `irrSetMatch` emits a `MatchInSet` naming `irr_v4_AS13335` and no `Set`, because the sets belong to the `firewall-irr` plugin and are merged only at `ApplyAll`, while `ValidateTables` runs at verify and configure over the parsed tables alone and `collectSetNames` reads just the table's own sets. A tree-built binary answers `match references unknown set "irr_v4_AS13335"`, and `source-as-set` fails the same way. The page also promises a rejection naming the missing IRR entry and the fetch command, which this error is not. Unlike the earlier rows this is not a stale example: the documented shape is the intended one and the code refuses it | fixed by `spec-fixit-firewall-irr-term-fails-validation`, which carried the doc correction as AC-7 and closed on 2026-08-23. The stem is bare because the spec is not in the tree any more. `MatchInSet.ProvidedType` (`internal/component/firewall/model.go`) states the type the supplying owner will register, and `validateMatch` reads it. The guide's step 3 now commits. The page also states the errors an operator can see and that the table family must be `inet`. Proven end to end by `test/plugin/firewall-irr-table-term-commit.ci` |
| 2026-08-16 | - | docs/config-reference.md | Two blocks on the canonical config-syntax page had never parsed. The Minimal Example wrote `local { as 65000; }`, which answers `unknown field in bgp: local`, because a peer keeps transport under `connection` and protocol under `session`; the same block wrote `internal bgp-rib;`, which answers `internal plugin requires use`. The PKI Certificate Store block named `certificate-file`, `private-key-file` and `ca-certificate`, none of which exists in any YANG module in the tree, and called the values PEM. The real schema is `pki { ca <n> { certificate } certificate <n> { certificate; intermediate; private { key } } }` (`internal/component/pki/yang/ze-pki-conf.yang`, `list ca`, `list certificate`, `container private`) and the values are base64-encoded DER | fixed both during the wiki sync. Every runnable block on the page was then machine-checked with `bin/ze config validate` in its parent context: 13 validate, 0 fail, 9 skipped as placeholders, prose artifacts, or Linux-gated |
| 2026-08-21 | - | docs/guide/command-catalogue.md, docs/research/comparison/freertr/07-cli-philosophy.md | The catalogue's Ze column for the two BGP summary rows named `show summary` and `show summary <afi/safi>`, and its notes named the wire method `ze-bgp:summary`. The freeRtr comparison named `show bgp neighbor` as a Ze operational command. None of the three is a dispatcher key: `./le command list` prints 356 lines and names neither `show summary` nor `show bgp neighbor`. Both pages predate the removal of `show bgp summary` and were wrong before it | fixed on the spot, since both sites sat on the lines this docs sweep was editing: the Ze cells read `show bgp` and `show bgp <afi/safi>`, the note names `ze-bgp:overview`, and the comparison names `show bgp` and `show bgp peer list` |
| 2026-08-23 | - | `test/interop/scenarios/ospfv3-sr-frr/ze.conf` | The scenario puts `router-information` inside `address-family ipv6`, and the daemon refuses to boot on it: `load config: parse config: line 12: unknown field in ipv6: router-information`. The OSPF schema declares that container exactly once, directly under `ospf`, never under `address-family` (`internal/plugins/ospf/yang/ze-ospf-conf.yang`). So the scenario has never exercised RFC 7770 router-information on OSPFv3, and it fails as `containers not healthy` rather than naming the config, which reads like an environment problem. A refused config is not documentation here, it is a shipped test that tests nothing | Not fixed here. Found while running the three OSPF scenarios that declare a single `scope` member, to check a config-reader change against them. The edit looks like three lines moving up to the `ospf` level, where the working `ospfv3-ri-frr` scenario already puts them, and that appearance is why it is worth saying why it was left. Moving them makes the scenario exercise OSPFv3 router-information against FRR for the FIRST time, so what it then reports is unknown, and its OSPFv2 twin already shows the next step failing on an instrument the host does not carry. That is a scenario to bring up deliberately, not a typo to absorb into a config-reader commit. Its unit-test cover models the same impossible nesting (`v6RIConfig` in `internal/plugins/ospf/origination_v6_ri_test.go` builds `address-family.ipv6.router-information` and hands it straight to the resolver, bypassing the schema that refuses it), so the shape looked plausible from both sides |
| 2026-08-28 | - | `plan/TEMPLATE.md` against `validateSpec` (`internal/le/hookruntime/lifecycle.go`) | The template's Goal Gates line read ``./le verify current mode full`` and the validator refuses any spec whose text lacks ``./le verify worktree``, so a spec copied from the template failed its own gate on the first edit anybody made to it. Found while editing one field of `plan/spec-le-is-a-ze-binary.md`: the edit landed and the hook reported two errors, neither about the edit. The validator is the half that is right. The owner directive of 2026-08-21 (`ai/rules/git-safety.md`) requires the pre-commit gate to run against a COMMIT in a throwaway worktree, and states that an in-place run is void the moment the tree moves under it. Both verbs are live (`le verify` offers `worktree`, `current`, `list`), so nothing failed loudly; the template simply named the wrong one. The same spec was also missing `## Implementation Steps`, which it carried under the name `## Historical implementation record` | fixed on the spot in both places. The template now names `./le verify worktree` and states why an in-place run is void, and the spec's section is renamed to the heading the template and the validator both use. The transferable half: a template and the gate that judges its output are two spellings of one contract, and nothing in this repository compares them, so a template edit that drifts is invisible until somebody edits a document made from it |
| 2026-08-30 | rfcgate-6-supported-extraction-signoff | `docs/contributing/running-commands.md`, `ai/rules/testing.md` | Both print `./le job run label unit-pkg command go test PKG=<pattern>` as the supported route for testing one package. `job run` passes its argv straight to the command, so `go test` reads `PKG=./internal/component/ike/eap` as an import path and answers `malformed import path ... trailing dot in path element`, exit 1, twice over for `RUN=` as well. `PKG` is a keyword of the FUZZ action alone (`internal/le/fuzz/actions.go`), and the rows in `plan/journal/hardcoded-count-in-test.md` already record `ze-unit-pkg-test PKG=...` as retired with `go test ./<pkg>` as its successor. The working form is `./le job run label unit-pkg command go test ./internal/component/ike/eap -run <regexp> -count=1` | recorded, not fixed: it blocked no goal here, and the rule file's edit needs `./le rules condensed-update` to regenerate `ai/rules/TRIGGERS.md` and `CORE.md`, which other sessions are reading |
| 2026-09-03 | spec-bcp194-7-transit-asn | `docs/guide/bgp-peering.md`, `docs/guide/route-reflection.md` | A new config rule made two published examples UNLOADABLE. `validateLeakFilterObligations` (`internal/component/bgp/config/peers.go`) refuses a peer whose declared RFC 9234 role implies the transit-leak check and whose bound chains name no discharging filter, and `leakFilterByRole["rs"]` binds both chains. Both pages showed `role { import rs }` with no `filter` block, so an operator copying either one gets a daemon that will not start. The spec's own Documentation Update Checklist had NAMED both pages, in row 17, and the implementing phase edited the pages it was already touching instead of the pages the row named. Four more pages the checklist marked Yes (`docs/config-reference.md`, `docs/guide/bgp-policy.md`, `docs/guide/command-catalogue.md`, `docs/comparison.md`) carried no mention of the feature at all | FIXED at closure. Both examples gained a `policy { reject-asn ... }` block, both chains, and a sentence naming the obligation and the `inactive:` opt-out; the four missing pages were written. Lesson for the class: a checklist row that names a page is a CLAIM about that page, so the phase that owns the row greps the page, and a row saying a page is WRONG needs the same grep as a row saying it is MISSING |
