| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-03 | command-help-and-description | `bgp/update/watchdog/withdraw` and `bgp/peer/connection/local/port` (`ze-bgp-conf.yang`) | Two config leaves whose declared meaning their producer does not implement, found by an agent writing their `ze:help`: an honest explanation would have had to state the bug, so it wrote none. FIRST, `withdraw` is declared a boolean and is keyed on PRESENCE. `collectWatchdogRoutes` (`internal/component/bgp/plugins/watchdog/config.go:145`) reads `_, wdWithdraw := wdMap["withdraw"]`, so `withdraw false;` starts the route withdrawn exactly as `withdraw true;` does. An operator who writes the false value gets the opposite of what the schema promises, with no error and no log line, which is the silently-wrong-value class `ai/rules/principles.md` names. SECOND, `connection/local/port` says "Local bind port" and binds nothing. `peerSettingsFrom` (`internal/component/bgp/reactor/config.go:258`) writes it to `ps.Port`, and line 267 writes `connection/remote/port` to the SAME field nine lines later, so the remote value wins. The only consumer is the dial target: `net.JoinHostPort(s.settings.Address.String(), strconv.Itoa(int(s.settings.Port)))` (`session_connection.go:53`). So the leaf either does nothing or silently changes the port Ze DIALS, and no code binds a local port anywhere | not fixed. Both are product behavior in the BGP reactor and the watchdog plugin, and this spec's diff is help text and a documentation gate. The `withdraw` repair is a decision rather than an edit: reading the value changes what an existing config does, so a config that says `withdraw false` today and behaves as true would flip on upgrade. The `local/port` repair is either to bind it or to remove the leaf, and either one is a config-surface change |
| 2026-09-01 | - | `rfc/not-enrolled.txt` entry for `iso-iec-10589` | `./le rfc check` rejects the entry because its `source-restricted` kind is outside the allowed `backlog`, `blocked`, and `non-normative` vocabulary. The generated ledger therefore carries a value its reader refuses. | CLOSED, and the observation was an artifact of a stale binary rather than a defect in the tree. `bin/le` was six hours old and a bare `./le` runs it without rebuilding, so the reader that refused the kind was the retired `parseDispositions`, deleted from the source hours before the run. Under a rebuilt binary (`./le --name <name> rfc check`) the kind is accepted: `enrolmentKinds` (`internal/le/rfc/meta.go`) is the one declaration of the closed set and `source-restricted` is a member. The proposed repair would have been wrong twice over: `backlog` means the extraction is OWED, and ISO/IEC 10589's text can never be fetched, which is the state `source-restricted` exists to record. The general practice this row is worth keeping for: a verdict from `./le` is a verdict from whatever `bin/le` was last built from, so a gate result that contradicts the source is a stale-binary hypothesis before it is a code one. |
| 2026-09-01 | rfc-ledger-single-declaration | the seven `LSTypeE*` constants in `internal/plugins/ospf/v3/types/lsa.go`, and the comment above them | Every OSPFv3 Extended LSA Ze originates carries the U-bit CLEAR. RFC 8362 Section 2 states "For backward compatibility, the U-bit MUST be set in the LS Type so that the LSAs will be flooded by OSPFv3 routers that do not understand them", and the table on the same page assigns 0xA021, 0xA022, 0xA023, 0xA024, 0xC025, 0xA027, 0x8028 and 0xA029. Ze declares 0x2021, 0x2022, 0x2023, 0x4025, 0x2027, 0x0028 and 0x2029: each is its RFC value less 0x8000. RFC 5340 Section 4.4.1 makes U=0 mean an LSA a router does not recognise is flooded only link-locally, so Ze's Extended LSAs stop at the first non-supporting OSPFv3 router and its RFC 8666 Prefix-SIDs never reach the far side of a mixed area. The comment above the constants makes the bug read as a decision: it asserts "The U-bit is 0: an Extended LSA a router does not understand is flooded only within its scope, per RFC 8362 §3", and Section 3 says nothing about the U-bit while Section 2 requires the opposite. The sibling constants twelve lines below get it right and say why, citing RFC 5340 Section 4.4.1 and RFC 7770 Section 2.2 for U=1 on the Router Information LSA. Found while auditing what Ze implements per OSPF RFC, and confirmed against `rfc/full/rfc8362.txt` rather than against a summary | not fixed. The edit itself is seven constants and a comment, and `Scope()` (same file) masks 0x6000 so the U-bit does not disturb scope derivation -- 0xC025 still derives AS scope and 0x8028 still derives link-local. What stops it being a drive-by is what sits on top: the values are wire format under RFC-tagged tests, and `ai/rules/testing.md` reserves editing an RFC-tagged test to the owner, so the change and its tests are one owner decision rather than two edits. A second question rides with it: `test/interop/scenarios/ospfv3-sr-frr` is recorded as proving Ze decodes FRR's E-Intra-Area-Prefix-LSA, and FRR's ospf6d implements neither RFC 8362 nor RFC 8666, so what that scenario actually exercises needs establishing before it is trusted as the regression guard for this fix. The general practice this class states, pointed at a CONSTANT: a wire value with a comment citing the section that licenses it is checked by grepping the RFC for the claim, which costs one command, and a citation naming the wrong section converts a typo into a design |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | the `Uncommitted:` section of a per-spec session state file, `tmp/session/<date>-<sid>/state/session-state-<spec-stem>-<sid>.md` | The file is named for one session and one spec, and the section is headed `Uncommitted:`, so a reader takes its list as that session's own uncommitted work. It is a `git status` dump of the WHOLE shared tree. Verified by reading the state file of `fixit-rfc-drain-quota-never-armed`: it lists `ai/RFC-REQUIREMENTS.md`, `docs/features/rfc-status.md` and `docs/architecture/l2tp/bng-1-radius-attributes.md`, all of which belong to a different session, alongside `ai/INDEX.md` and `cmd/ze/hub/startup_gate_test.go` which a third session had edited in the preceding twenty minutes. With six sessions in one checkout the list is very nearly the same in every session's file, so it selects for who wrote a state file most recently rather than for who is doing the work. The harm is attribution, and it is the expensive kind: a session hunting the owner of a change reads a file that looks authoritative, is per-session by its own path and heading, and names an innocent party. One session proposed exactly this method for finding a file's owner tonight and a second checked it before using it, which is the only reason it did not misdirect a third time -- three separate ownership hunts ran in this checkout in one evening, and the two that succeeded both worked from an owner's own testimony about its own chunks | not fixed. The narrow repair is the heading: `Uncommitted (whole tree, not this session):` costs one line and removes the false reading entirely, since the dump is genuinely useful as tree context for post-compaction recovery, which is what `.claude/rules/post-compaction.md` uses it for. The wider one is to record the session's own footprint, which nothing in the tree can currently derive -- git tracks no author for an uncommitted hunk, so a per-session footprint has to be accumulated as the session writes rather than sampled at the end. The general practice this class states, pointed at a HEADING: a section label inside a per-session artifact is read as scoped to that session, whatever the data behind it actually is, and a label that has to be disbelieved is worse than an absent one |
| 2026-08-30 | rfcgate-6-supported-extraction-signoff | `runMSCHAPv2AuthPhase` (`internal/component/l2tp/ppp/mschapv2.go`) and `extractMSCHAP2Success` (`internal/component/l2tp/plugins/authradius/handler.go`) | Found while establishing what Ze implements for RFC 2759, to settle which of two contradictory RFC 2759 rows on `docs/features/rfc-status.md` is true. The PPP MS-CHAPv2 authenticator declares it needs the RAW 20-octet Authenticator-Response: `mschapv2AuthenticatorResponseLen = 20`, and `runMSCHAPv2AuthPhase` fails the session when `len(decision.authResponseBlob) != mschapv2AuthenticatorResponseLen`, hex-encoding the 20 bytes itself into the `S=` field. Its only supplier that does not refuse the method is `authradius`, whose `extractMSCHAP2Success` returns the MS-CHAP2-Success vendor attribute's data VERBATIM. RFC 2548 Section 2.3.3 defines that data as `Ident` (1 octet) followed by "The 42-octet authenticator string", so the supplier hands over 43 octets of `Ident` plus ASCII `S=<40 hex>` where the consumer requires 20 raw octets. The length guard therefore fires on every RADIUS-backed PPP MS-CHAPv2 session and tears it down. The other handler, `authlocal`, refuses the method outright (`localAuth.handle`, `internal/component/l2tp/plugins/authlocal/auth.go`, "MS-CHAPv2 not supported by local auth"), so no PPP MS-CHAPv2 path in Ze can complete. No test covers it: no `RFC2759` tag exists anywhere under `internal/component/l2tp/`, and `internal/le/interoplab/pppoe/check_ac.go` configures the peer pppd with `refuse-mschap` and `refuse-mschap-v2`. This is why the "Supported within PPP and IPsec EAP" row for RFC 2759 on `docs/features/rfc-status.md` was false; only the IKEv2 EAP path implements RFC 2759. The owner removed that row on 2026-08-30, leaving the accurate `Partial` row, which also cleared a silent overwrite: the page carried two RFC 2759 rows and `parseStatusLedger` (`internal/le/rfc/ledger.go`) keeps the last, so no ledger check could see the support claim | not fixed. Recorded under the owner-directive route: the defect does not block this spec's goal, which is extraction sign-offs. The fix is at the supplier: `extractMSCHAP2Success` must strip the `Ident` octet and hex-DECODE the 40 hex characters of the `S=` field into the 20 raw octets its consumer declares, and it owes a test at the `runMSCHAPv2AuthPhase` entry point plus a PPPoE interop scenario that does not refuse the method |
| 2026-08-30 | - | `FlowSpec.writeComponentsSorted` and `ParseFlowSpec` (`internal/component/bgp/plugins/nlri/flowspec/types.go`) | Found auditing whether RFC-tagged tests can fail. RFC 8955 Section 4.2 states "A given component type MAY (exactly once) be present in the Flow Specification", and the same section states "An NLRI value not encoded as specified here, including an NLRI that contains an unknown component type, is considered malformed and error handling according to Section 10 is performed". `writeComponentsSorted` writes EVERY component of each type, so `port >80 <100 port >443 <500` leaves ze as two Type 4 components in one NLRI: wire `0c 04 02 50 c4 64 04 12 01 bb d4 01 f4`. A conforming receiver treats that NLRI as malformed. The correct encoding is one Type 4 component whose operator list joins the two groups with the AND bit clear, which is what Section 4.2.1.1 defines the bit for. `ParseFlowSpec` accepts the same shape on receive, so ze is non-conformant in both directions, and the `{single-polarity}` annotations on RFC8955-4.2-1, RFC8955-4.2-2, RFC5575-4-3 and RFC5575-4-4 cite the decoder's silence as the reason no negative test exists | fixed 2026-08-30, both directions in one change, and the cost of leaving it was larger than the row said. The fix is in the component MODEL rather than at either boundary: `FlowSpec.AddComponent` returns an error and MERGES a component whose type is already present, through `numericComponent.mergeMatches`, clearing the AND bit of the joined group's leading operator per Section 4.2.1.1; a second prefix component has no operator list to join and is refused with `ErrFlowSpecDuplicateType`. So a FlowSpec cannot hold two components of one type, and `writeComponentsSorted` emits each type once by construction. Both decode walks share `FlowSpec.parseComponents`, which refuses a repeat (`ErrFlowSpecDuplicateType`) and a descent (`ErrFlowSpecTypeOrder`); `ParseFlowSpecVPN` had a second open-coded copy of that walk and now calls the shared one. The interop lab DID run: `bgp-flowspec-gobgp` announces `port >80 <100 port >443 <500` and GoBGP 3.31 installs `[destination: 10.99.2.0/24][port: >80&<100 >443&<500]`. Reverting the merge makes GoBGP log `ipv4-flowspec nlri violate strict type ordering` and RESET the session, so the old encoding did not merely lose the rule, it flapped the peer. The four `{single-polarity}` annotations are removed and each row now carries a negative test |
| 2026-08-26 | - | the retired `ze-command-ownership --json` (current: `./le command ownership`, whose JSON is the `json` pipe operator on the same action), whose gate row declares `json_flag='--json'` | Found while porting this gate to `le`. `command_ownership.go` writes the findings as JSON to stdout and then writes `command-ownership: FAILED, N problem(s)` to the SAME stream, unconditionally. So the declared format holds only while the gate has nothing to report: a clean run's `--json` parses and a failing run's does not, which is the one case a caller asked for JSON in order to read. Demonstrated on the built script over a four-violation fixture tree: `invalid character 'c' after top-level value`. The sibling `config_claims.go` prints its verdict to stderr, which is what makes this a defect rather than a convention | fixed in the port rather than in the script, because the port replaces it: an `le` tool answers a PAYLOAD and `leroot.Run` renders it, so nothing but the document reaches stdout and the verdict is part of the page a person reads. `TestOwnershipScriptStillCorruptsItsOwnJSON` (the retired `scripts/checks/parity_test.go` (current producer: `internal/le/repository/`)) asserts the SCRIPT's `--json` still fails to parse over a failing tree, so it reddens the day somebody fixes the script and this row can be closed. The script is deleted at the swap step of the le migration |
| 2026-08-15 | bgp-pcap-decode | diag pcap export | `exportBGPPcap` declares `LinkTypeRaw`, but writes only BGP bodies. `notifyMessageReceiver` receives the body after the 19-byte header. Wireshark reads it as IPv4 and displays invalid packets. The capture omits the message type and boundaries | `plan/spec-bgp-pcap-decode.md` fixes this defect. It adds IP and TCP framing around complete BGP messages. This owner decision dates 2026-08-15. The defect blocked pcap input for `ze bgp decode` because Ze did not read its own capture |
| 2026-08-19 | - | cli response payload | `RawJSON.MarshalJSON` (`internal/component/plugin/types.go`) accepts a payload its own type name refuses: on `!json.Valid` it returns `json.Marshal(string(r))`, so finished text becomes a quoted JSON string rather than an error. The invariant it defeats is `ai/rules/cli.md`, "a command's response payload MUST be structured data", which `ResponseData` otherwise holds by refusing a bare string in `Response.Data`. Quoting is not the whole hole either: a plugin returning a Go string marshals to a valid JSON string with no invalid bytes anywhere, and `applyYAML` (`internal/component/command/pipe.go`) and `applyTableStyled` (`pipe_table.go`) each unmarshal it to a scalar and print it back, so `json`, `yaml` and `table` stop being three renderings of one payload. No test covered the command population: `TestBGPDecodePayloadRendersInEveryFormat` pins one command | partly fixed. `TestShowCommandPayloadsAreStructured` (`internal/component/plugin/server/response_format_audit_test.go`) walks the `ze-show:` namespace and holds the invariant that bites, which is that the top-level JSON value is an object or an array; 56 payloads judged under the feature tags, and the census names the 109 with no offline payload and the 13 needing a daemon. The walk is scoped to the read verb because judging a payload means CALLING the handler, and `handleDaemonQuit` (`internal/component/plugin/server/system.go`) is a registered RPC that quits the daemon. Then fixed at the type in `1ecd24039`, on the owner's answer to the contract question this row had left open: `RawJSON.MarshalJSON` returns an error rather than quoting, since a quoted payload is a guard producing a valid-looking answer. No production call site changed, because all ten pass `json.Marshal` output or a `json.RawMessage` validated at an RPC boundary. The test fakes did change: `internal/component/api/engine_test.go` had documented text-through-`RawJSON` as the shape for plain-text commands on the REST and gRPC surfaces, and its AC-2 wording is superseded, recorded in `test/weakened.md`. `TestRawJSONRefusesNonJSON` and `TestEngineExecuteRefusesNonJSONPayload` hold the refusal |
| 2026-08-19 | - | flowspec-firewall bridge | `handleEvent` (`internal/plugins/flowspec-firewall/engine.go`) unmarshals a FLAT envelope: `type` at the top level, `peer.remote.address` at depth two, and it switches on `type` being `state` or `update`. The daemon emits the ze-bgp envelope instead, `{"type":"bgp","bgp":{"message":{"type":"update"},"peer":{},"update":{"nlri":{"ipv4/flow":[]}}}}`, whose producers are `appendFilterResultJSON` and `appendStateChangeJSON` (`internal/component/bgp/format/text_json.go`), each appending that literal prefix. That envelope is the Ze native format (`docs/architecture/api/json-format.md`), not the exabgp one: `ZebgpToExabgpJSON` (`internal/exabgp/bridge/bridge_event.go`) converts it TO the flat exabgp shape, which carries `exabgp` and `neighbor.address.peer`. An earlier reading of this row named the nested shape exabgp and had the direction backwards. `envelope.Type` is therefore `bgp`, which matches neither case, and the switch has no default, so every FlowSpec update is dropped in silence. Four mismatches, not one: the envelope type, the peer depth, the family nesting, and the traffic action arriving as raw `attr-16` hex where `parseExtendedCommunities` expects decoded `rate-limit:0` strings. The plugin has never worked end to end. Consequence for coverage: `test/plugin/flowspec-fw-protocol-sctp.ci` and `flowspec-fw-untranslatable-keeps-others.ci` cannot pass, and a port-0 `.ci` drafted for the 2026-08-19 translation fix was proven unpassable and deleted rather than promoted red. Found during the port-0 widening repair, whose own fix is unit-proven and unaffected | mostly fixed, 2026-08-19. The fourth mismatch had a root cause none of this row's earlier readings named: the four community JSON formatters asserted a POINTER while `knownAttrParsers` (`internal/core/bgp/attribute/wire.go`) boxes the parser's return VALUE, so `appendExtCommunitiesJSON` returned nil and `appendAttributeJSON` fell back to `attr-16` hex. No community of any kind had ever rendered on any surface. Fixed in `2fe792839`, with `ExtendedCommunity.AppendDecoded` (`internal/core/bgp/attribute/extcomm_decoded.go`) giving the decode direction the shared home the encode keyword table already had, so the event now carries `rate-limit:0` and the RFC 8955 Section 7.5 DSCP mask lands in one place; `c0b32c116` retired the stale `{gap}` on RFC8955-7.5-1 and the public gap count. The first three mismatches are fixed in the working tree, UNCOMMITTED: `handleEvent` goes through `bgp.ParseEvent`, which was itself reading `attributes` where every writer emits `attr`, and the switch gained the `default` whose absence made the drop silent. That work is held because correcting `TestRFC8955NextHopIgnoredForFlowSpec` needs the owner's approval on an RFC-tagged test, and until it lands the package does not type-check. The 20s-late-at-shutdown observation is RETIRED as an artifact of this very bug: `deliveryLoop` (`internal/component/plugin/process/delivery.go`) blocks on `eventChan`, drains non-blockingly through `drainBatch` and delivers at once, with no ticker, flush interval or batching delay anywhere in the path, so nothing there defers an event. With every update silently dropped there was no per-event trace to see, which is the likelier reading of what was observed |
| 2026-08-23 | - | bgp prefix limit NOTIFICATION | RFC 4486 Section 4 Figure 1 labels the last four octets of the Cease/MaxPrefixes Data field the "Prefix upper bound". `reportPrefixExceeded` (`internal/component/bgp/reactor/session_prefix.go`) passed `current`, the count that crossed the maximum, into `buildPrefixNotification`, which wrote it there. So a conforming peer read ze's current count as ze's configured limit. The two numbers differ by construction: the count is always at least maximum+1. `test/plugin/prefix-maximum-enforce.ci` pinned the wrong bytes (`00000003` over a `maximum 2`). `rfc/short/rfc4486.md` recorded the divergence as a deliberate MAY decision ("Include AFI+SAFI+count"), and the retired `scripts/dev/rfc_requirements_test.py` (current producer: `internal/le/`) asserted the same hex. So three surfaces agreed with each other and none of them with the RFC | fixed on Thomas's 2026-08-23 ruling to send the correct value rather than drop the optional field. `reportPrefixExceeded` passes `maximum`, the write carries an RFC comment naming Section 4 Figure 1, and `buildPrefixNotification`'s parameter is `upperBound`. `TestPrefixNotificationDataCarriesTheConfiguredUpperBound` sets `maximum 3` against an 8-prefix UPDATE, so neither number can be read as the other. Restoring `uint32(current)` turns it red with `00000008` for `00000003`. The three agreeing surfaces were corrected with it. `TestPrefixCountInstalledStopsGrowingAtTheMaximum` had used the Data field as its only observable for the maximum+1 growth bound. It now reads the set `applyInstalledPrefixSection` leaves behind, which is that bound's producer |
| 2026-08-24 | cli-show-bgp-answer-shapes | cli resolve and origin operators | `RegisterAddressFields` (`internal/component/command/answer_shape.go`) declares WHICH fields of an answer hold an IP address, and the two transforms that act on them never read it. `resolveJSON` (`internal/component/command/pipe_resolve.go`) and `originJSON` (`pipe_origin.go`) walk every value of the payload and decorate any string that passes `netip.ParseAddr`. The declaration is read only by `validateDeclaredShape` (`pipe.go`), which decides ADMISSION, and by `operatorsFor` (`cmd/ze/help_command.go`), which publishes. So a command that declares one address field has every address-shaped value in its answer decorated once the operator is admitted. Running `resolve` over `show bgp peer detail` is the case. The row's `router-id` and `cluster-id` are 4-octet identifiers rendered as dotted quads. RFC 6286 Section 2.1 and RFC 4456 Section 7 make them integers rather than addresses, and both get a PTR lookup and a `-name` sibling. The gap runs the other way too: a declared field the walk cannot reach is admitted and decorates nothing. That is why `show bgp irr` declares none although its rows hold peer addresses inside an ARRAY. It is also why `show bgp rib best` declares `best-peer` and `next-hop` rather than the `prefix` and `multipath-peers` the spec's own table named | not fixed. Phase 4 of `plan/spec-cli-show-bgp-answer-shapes.md` worked around it by declaring only fields the walk decorates. `TestDeclaredAddressFieldsHoldAnAddress` (`internal/component/bgp/plugins/cmd/peer/peer_shape_test.go`) holds each declared name to being a bare address string in a row. The fix is for the two transforms to take the declared list as their SELECTOR, which needs the list threaded to `ApplyPipes` |
| 2026-08-24 | - | rib ribOut event attributes, flowspec-firewall rate limit | Found reviewing `2fe792839` for its verification-debt row. That commit made `appendExtCommunitiesJSON` (`internal/component/bgp/plugins/filter_community/json.go`) emit the NAMED form, `target:65000:1`, where the key had never been emitted at all. Its only wire-direction consumer is `ParseExtCommunityStrings` (`internal/component/bgp/route.go`), which runs `hex.DecodeString` and keeps a value only when it decodes to exactly 8 octets, so every named string is dropped in silence. `packEventAttrs` (`internal/component/bgp/plugins/rib/ribout_entry.go`) then guards on `len(event.ExtendedCommunities) > 0` rather than on the length of what the parser returned, and calls `appendAttr` with `make([]byte, 8*len(ecs))`, so a length-0 EXTENDED_COMMUNITIES attribute reaches the interned ribOut bytes replayed on reconnect. RFC 4360 Section 2 defines the attribute as a set of 8-octet communities and RFC 7606 makes a zero-length one treat-as-withdraw or NOTIFICATION. A second consumer disagrees about units rather than format: `parseExtendedCommunities` (`internal/plugins/flowspec-firewall/translate.go`) matches `rate-limit:` by prefix and stops its `parseUint32` at the colon, so the `:packets` suffix `AppendDecoded` renders for sub-type 0x800c is discarded, and `actionToFirewall` always builds `firewall.Limit{Dimension: firewall.RateDimensionBytes}`. RFC 8955 Section 7.2 defines traffic-rate-packets in packets per second, so a peer asking for 1000 pkt/s is given a 1000 byte/s limit. **Correction:** the review that found this, and the first version of this row, named that function `buildActions`. No such symbol exists; the builder is `actionToFirewall`, found when a test written against the reported name failed to compile | fixed on Thomas's instruction, 2026-08-25, and the fix was wider than the row described. The zero-length attribute was never specific to extended communities: `parseCommunityStrings` and `parseLargeCommunityStrings` drop unreadable members the same way, and all three blocks of `packEventAttrs` guarded on the length of the EVENT slice rather than on what parsed. `TestUnparsableCommunityStringsPackNoAttribute` walks the packed bytes and refuses any zero-length attribute; against the old guard it reports types 8, 16 AND 32, so the defect stood in three places and the row named one. Each block now guards on the parsed slice. For the vocabulary, the consumer moved rather than the producer, because reverting `appendExtCommunitiesJSON` to hex would re-break the FlowSpec bridge that `2fe792839` fixed: `parseSingleExtCommunity` is exported as `ParseSingleExtCommunity` and `ParseExtCommunityStrings` now tries hex first and the named form second, so both spellings round-trip. The FlowSpec unit is fixed at `parseExtendedCommunities`, which reads the `:packets` suffix into a new `rateInPackets` field that `actionToFirewall` turns into `RateDimensionPackets`; `TestTrafficRatePacketsInstallsAPacketDimension` pins both directions and fails when the dimension is hardcoded back |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff (phase 4, rfc8671) | BMP sender Peer Down for a BGP peer | Found while implementing the RFC 8671 Section 7.2 peer bounce next to it. `handleSenderState` (`internal/component/bgp/plugins/bmp/bmp_events.go`) answers a peer-down event with `ss.writePeerDown(peer, reason, nil)`, and `peerDownReasonFromString` returns 1, 2, 3 or 5. RFC 7854 Section 4.9 makes the Data field mandatory for three of those: reason 1 and reason 3 are "Following the Reason is a BGP PDU containing a BGP NOTIFICATION message", and reason 2 is "Following the reason code is a 2-byte field containing the code corresponding to the Finite State Machine (FSM) Event ... Two bytes both set to 0 are used to indicate that no relevant Event code is defined". Ze sends the reason byte alone, so a collector decoding to the letter reads past the end of the message for every ordinary peer down. Reason 5, the one this session's bounce sends, carries no data by the same figure and is correct | not fixed. The reason byte is right and only the payload is missing, so the repair is to carry the NOTIFICATION PDU and the FSM event code on the peer-down `rpc.StructuredEvent` the reactor emits and to pass them to `writePeerDown`; `peerDownReasonFromString` already distinguishes the four cases and would choose which one to attach. Reason 2 has a defined zero form, "two bytes both set to 0", so that half is a two-byte constant even before the event carries an event code |
| 2026-08-31 | yang-short-and-long-command-help (phase 8) | `ze-plugin:session-ping` | Found while writing the RPC's one-line summary. `ze-plugin-api.yang` declares the reply as `leaf pong { type boolean; description "Always true when alive"; }`, and `handlePluginSessionPing` (`internal/component/plugin/server/session.go`) answers `plugin.Map{"pong": os.Getpid()}`, an int. The old YANG description, `Health check (returns PID)`, agreed with the code and contradicted the leaf beside it, so the module disagreed with itself in two statements. A generated client that reads the model gets a boolean field and a number on the wire | not fixed. The code is the useful behavior: a caller identifying which daemon answered needs the pid, and a boolean carries nothing a successful reply does not already carry. So the repair is in the model, `leaf pid { type uint32; description "Process id of the daemon that answered."; }`, and the response key changes with it, which makes it a wire change on the plugin IPC boundary rather than a one-line edit |
| 2026-09-03 | none (owner asked for the le-rules-answers count) | `le-rules-answers` fixture, the lint's `empty` key (`internal/test/fixture/ui_fixture_le_rules_answers.go`) | The fixture read `lint["empty"]` as a `bool`. `LintReport.Empty` is `[]string` and carries no omitempty (`internal/le/rules/lint.go`), so a clean run marshals it as `null` and a dirty one as a list of population names. Neither satisfies a `.(bool)` assertion, so the check could not pass whatever the gate answered, and its message, "the lint read an empty or invalid population", named the healthy state as the failure. Measured: the lint's own JSON rendering answers `empty = null`. It was invisible because the rule-point floor two lines above was red first for the whole life of the line, so the fixture never reached it | fixed: the key is read as the list it is, absent or zero-length being the passing shape. Recorded because the shape of the miss is general: a boolean read of a report field is a guess about a payload, and the field beside it in the same struct literal is where the answer was |
| 2026-09-03 | local-ca | `handleShowPKILocalCAPEM` (`internal/component/pki/show.go`) against `parseCACert` (`internal/component/pki/config.go`) | The export prints PEM and the only consumer of that output takes base64 DER, so the two ends of the operator's copy-paste disagree. `handleShowPKILocalCAPEM` answers `root.CertificatePEM()`, headers and line breaks included. `parseCACert` calls `base64.StdEncoding.DecodeString` on the `certificate` leaf with no PEM tolerance anywhere in the package, and that decoder rejects both the `BEGIN` line and the newlines. So the root a hub exports cannot be pasted into the `pki ca` block of the client that must trust it: the operator has to strip and join by hand, or the config is refused with `pki: certificate decode`. AC-8 of the spec says the exported text is "directly usable as a client trust anchor", and it is not. The YANG says so too, `ze:help` on `list ca` reading "a base64-encoded DER X.509 certificate, with no PEM headers" | fixed 2026-09-03, the first of the two: `certificateDER` and `privateKeyDER` (`internal/component/pki/config.go`) decode a leaf that opens `-----BEGIN` as PEM and everything else as base64 DER, so the exported root pastes in unedited and every existing config still loads. Applied to all four leaves that carry X.509 material, not only the one the export feeds: `ca certificate`, `certificate certificate`, `intermediate` and `private key`. A value that opens a PEM block is refused AS a PEM, so a truncated paste is never reported as a base64 error. Proven by `TestExportedRootPastesIntoAPKICABlock` (`internal/component/pki/config_pem_test.go`), which runs the export handler, pastes its exact output into config TEXT, parses, loads the store, and verifies a leaf the root issued against `CAPool()`. The recorded RED: `the pasted root is refused by the pki parser: pki ca "fleet-hub-root": pki: certificate leaf is neither a PEM certificate nor base64 DER: illegal base64 data at input byte 0`. The pages that taught the manual strip and join are corrected in the same change |
