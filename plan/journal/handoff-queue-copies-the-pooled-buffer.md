| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-07-27 | fixit-bmp-sender-blocking-and-reload | bmp sender transmit path | moving socket I/O off the EventBus subscriber goroutine looked like a channel of `[]byte` between producer and drain. Every producer encodes into the session's single `scratch` buffer and reuses it on return, so each queued element needs a copy out of it: one heap allocation per Route Monitoring message, on the hot path whose comment records that it is allocation-free | the queue is a pooled page ring bounded in BYTES (`txQueue`, `internal/component/bgp/plugins/bmp/txqueue.go`), filled by copy under the existing write lock and drained by one goroutine per session. The byte accounting the bound needs is then the fill level, not a count of elements. `TestSenderRouteMonitoringHotPathIsAllocationFree` pins the property |
