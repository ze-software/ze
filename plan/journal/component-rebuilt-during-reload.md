| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-17 | spec-hub-deferred-api-auth-independent-of-ssh-block | AAA bundle lifecycle | BGP infrastructure hook reentry rebuilt and swapped the AAA bundle after no-BGP startup. A rejected BGP auto-load could leave candidate backends live, close an established session's TACACS authorizer, and stop the accountant before its matching STOP record | AAA construction now has one boot-owned attempt. Hook reentry reuses the installed bundle, and a failed initial build still consumes the attempt. `TestInfraSetupReentryReusesNoBGPBootBundle` and `TestInfraSetupReentryDoesNotRetryFailedNoBGPAAABoot` cover both paths |
| 2026-08-19 | - | firewall-irr prefix store, config reload | `configure` (`internal/component/firewall/plugins/irr/irr.go`) builds a NEW `PrefixStore` on every reload and `Open`s it from `cacheStorePath()`. `openExisting` (`internal/component/resolve/irr/store/store.go`) returns an error when `database.zefs` does not exist, so `persist` wrote nothing and `Open` loads nothing: every prefix fetched before the reload goes with the old store. `test/plugin/firewall-irr-table-term-commit.ci` fails on exactly this. The daemon logs `firewall irr: no cached prefix data for as-set AS-TEST` right after the SIGHUP that adds the table term, and `ze_wan` is never programmed. Verified red at BASELINE, with this session's two IRR fixes reverted, so neither fix caused it | fixed 2026-08-19: the store is created once and kept for the life of the plugin, and `configureStore` points it at the servers the new config names through `PrefixStore.UseClients`. The failure was also a fail-open in a firewall, because `OnConfigVerify` accepted the commit by reading the store the apply then discarded: `dropTablesMissingAProvidedSet` held `ze_wan` back with a WARN and the commit reported success. `TestReconfigureKeepsFetchedPrefixes` and `test/plugin/firewall-irr-table-term-commit.ci` are both red without it, the second under `unshare -Urn` against a binary whose config directory holds no `database.zefs`. The persistence half is unchanged and deliberate: `persist` is documented as a no-op when the file is absent, and the store does not create the database that `ze init` owns. So a fetch survives a reload everywhere, and survives a daemon RESTART only where the database exists, which a real install always has |
| 2026-08-19 | fixit-shutdown-waits-out-a-deadlock | bgp plugin removal at reload | Removing the bgp plugin from a running daemon takes the daemon's SHARED plugin API server down with it. `runBGPEngine` (`internal/component/bgp/plugin/register.go`) returns on TWO occasions, and its own comment says so: at daemon shutdown, and when bgp is removed at reload through `autoStopForRemovedConfigPaths` (`internal/component/plugin/server/server.go`). Its tail calls `bgpReactor.Stop()`, whose `Reactor.cleanup` calls `r.api.Stop()` -- and in the normal, non-standalone mode `r.api` is not the reactor's own server. It is the ONE `*pluginserver.Server` the hub built in `runHub`, reaching the reactor by four hops: `pluginserver.NewServer` -> `registry.SetPluginServer` -> `registry.GetPluginServer` read by `runBGPEngine` -> `Reactor.SetPluginServerAny`. So at shutdown the extra Stop is merely a wasted 3.5s deadlock wait; at RELOAD the daemon keeps running with its plugin API server stopped. Found while rejecting a candidate fix for that deadlock, because a re-entrancy guard on `ProcessManager.Stop` would have made this second case silent instead of removing it. Read-verified through `runBGPEngine`, `Reactor.cleanup` and `autoStopForRemovedConfigPaths`; NOT reproduced at runtime, and the reproduction is the next step | fixed by the same change, and this is why that change is an ownership guard rather than a re-entrancy one: `Reactor.cleanup` now guards `r.api.Stop()` and `r.api.Wait()` on `!r.externalServer`, the predicate `Reactor.abortStartup` in the same file already applies. A reactor that BORROWED the hub's server stops touching its lifecycle on either occasion; a standalone reactor, which owns its own server (`startAPIServer` under `Config.Standalone`, used by the ze-chaos in-process runner), still stops it. The runtime reproduction of the reload case is still owed |
| 2026-08-24 | - | AAA bundle lifecycle, config reload | **A LOCK OVER THE ACCEPTANCE TAIL SERIALIZES IT AND STILL INSTALLS THE WRONG CONFIGURATION.** `(*Server).reloadConfig` (`internal/component/plugin/server/reload.go`) releases `txLock` in its own deferred call, so everything after `s.ReloadConfig` in `runReloadContext` (`cmd/ze/hub/main_reload.go`) runs with no exclusion. Two reloads reach that tail: `handleSIGHUPReload` runs on its own goroutine, and `reloadAfterCommit` (`cmd/ze/hub/main.go`) is called from any ssh session editor through `sessionReloadHolder`. Adding a mutex was the obvious repair and it is not sufficient, which is the whole point of this row: mutual exclusion orders the CRITICAL SECTIONS and says nothing about which reload read its configuration first. Interleave R1 build, R2 build, R2 install, R1 install and the daemon ends on the SUPERSEDED tree's RADIUS/TACACS chain, while the install closes the chain that matched the accepted config. Nothing corrects it later, so the box authenticates against the rotated-away secret until the next reload. The same window lets an install land after `closeAAABundle` and revive a chain at shutdown with its TACACS+ accounting worker running past exit. Found by an independent review lens over adopted work, not by a failing test: no test existed that raced the tail | fixed in `5b24b076f`. Each reload takes a config-read order at `load()` time, and `acceptReloadedAAA` (`cmd/ze/hub/aaa_lifecycle.go`) refuses an order at or below the one it has already accepted, so a superseded reload publishes neither its identity nor its chain. The refused candidate is closed by the same deferred cleanup that closes an abandoned one, so refusing costs no socket and no worker. `TestAcceptReloadedAAAInstallsTheConfigurationReadLast`, `...RefusesAfterShutdown` and `...LeavesOneChainOpenUnderConcurrentReloads` cover it. **The general shape, which is why this is worth a row: when concurrent producers each derive state from a snapshot they read at different times, the acceptance point needs a READ ORDER it can refuse, not mutual exclusion. A lock makes the last WRITER win; correctness usually wants the last READER to win.** The peer session that met it says the same trap sits under any other reload path that rebuilds from a tree read before the lock |
| 2026-09-08 | ddos-timing-leaves-reach-no-worker | `ddos flowspec` responder lifecycle at config apply (`internal/plugins/ddos/flowspec/register.go`) | A config apply orphans a live FlowSpec announcement. `p.OnConfigApply` builds a fresh responder through `newResponder`, which leaves `active` false and `announcedAt` zero, and stores it in `activeResponder`. Nothing carries the outgoing responder's state and nothing withdraws the route, so the FlowSpec NLRI stays in the peer's RIB. `enforceMaxDuration` returns on `!r.active` and so does the clear path, so from that apply the announcement is bounded by neither `max-mitigation-duration` nor an `AttackCleared`: a peer keeps a discard rule for the victim for the life of the daemon while `show ddos flowspec` reports nothing announced. One ordinary `commit` on any `ddos flowspec` leaf reaches it. Read-verified at `newResponder`, `enforceMaxDuration` and `OnConfigApply`; not reproduced at runtime, and the reproduction is the next step. Found while clearing the identical BLOCKER in the ddos LOCAL responder, which shares the shape because ddos local ported its worker from this plugin | not fixed. The repair is the one ddos local took: an `adoptMitigation`-shaped carry of `active`, `target`, `announcedAt` and the match state from the responder the apply replaces, driven through one `replaceResponder` that both the configure and the apply path call, with a test over that function. It is a different spec's leaf (`plan/immediate/spec-ddos-direction-allowlist-deferred-flowspec-withdraw.md` owns the flowspec cap), and the flowspec responder carries more state than the local one (`match`, `limiter`, the probe), so what to carry is a decision rather than a copy |
| 2026-09-09 | ddos-timing-leaves-reach-no-worker | `copp` table lifecycle at config removal (`internal/plugins/copp/register.go`) | Deleting the `control-plane-protection` block leaves copp's nftables table in the kernel, and the comment at the tail of `runCoppPlugin` says the opposite: "Config removal while running still withdraws via OnConfigApply -> applyCoppPolicy(nil)". It does not. `parseCoppConfig` returns `found=false` for the empty body the reload delivers for a removed root, so `OnConfigVerify` sets `pendingPolicy = nil`, and `OnConfigApply` reads `if newPolicy == nil { return nil }` and withdraws nothing: nil is both "nothing staged" and "the section is gone", which is one field carrying two meanings. The plugin is then stopped, because copp declares `ConfigRoots: []string{"control-plane-protection"}`. Read-verified at `parseCoppConfig`, `OnConfigVerify` and `OnConfigApply`; NOT reproduced at runtime. Found while clearing the identical BLOCKER in ddos local, which the review pointed at copp to copy | not fixed here: copp was read-only for this work. The repair is the one ddos local took, and it is smaller for copp because the plugin has no responder: carry `found` from verify to apply beside `pendingPolicy`, and call `applyCoppPolicy(nil, ...)` when the section is gone. The false comment goes with it |
| 2026-09-09 | ddos-timing-leaves-reach-no-worker | config reload, plugins whose root is a CHILD of a deleted block (`internal/component/plugin/server/reload.go`) | Deleting a PARENT config block stops the child plugin without ever telling it. `diffMapsRecursive` (`internal/component/config/diff.go`) records ONE removed key for a deleted subtree, so removing `ddos { ... }` whole puts `ddos` in `diff.Removed` and nothing under it. `rootHasChanges` then matches only `k == root` or `k` prefixed by `root + "/"`, so a plugin declaring `WantsConfig: ["ddos/local"]` is not in `affected` and receives no verify and no apply; `parentRemoved` in `startup_autoload.go` matches ancestors, so the same plugin IS stopped. The plugin therefore has no opportunity to release what it installed. Measured for ddos local, whose nftables drop this leaves in the kernel exactly as deleting `ddos { local { ... } }` alone did before this spec fixed that path. Read-verified at `diffMapsRecursive`, `rootHasChanges` and `parentRemoved`; NOT reproduced at runtime | the DELIVERY defect is not fixed here: the producer is `internal/component/**`, outside this work's file scope. The repair is at the reload, not in each plugin: either `rootHasChanges` matches an ANCESTOR in `diff.Removed`, which makes every affected child receive the empty-body section the removal already sends for its own root, or `parentRemoved` and the affected-plugin walk are derived from one function so the two can never disagree about which plugins a removal touches. ddos local's own SYMPTOM is fixed (2026-09-09): `runEngine`'s exit path calls `stopResponder`, which withdraws whatever rule the responder still owns, so a stop that delivers no config still takes the drop with it. `test/plugin/ddos-parent-config-removed.ci` is RED in the QEMU guest with that defer cut, naming `table=ze_ddos-local chain=ingress rules=1` forty seconds after the block was deleted, and green with it. That covers ONE plugin: every other plugin whose root is a child of a deleted block is still stopped without being told |
| 2026-09-10 | ddos-timing-leaves-reach-no-worker | ddos-local startup, `internal/plugins/ddos/local/register.go` `runEngine` | An engine starts before dependency-tier handshakes, so a pre-configure reconcile can select the OS default before the configured firewall backend | Sweep in initial `OnConfigure` after the firewall tier and before subscriptions; verify/apply reloads retain the live rule. Configured-backend RED/GREEN and Linux stale-table readback cover the boundary |
