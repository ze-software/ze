# gate-fires-outside-its-population

The mirror of `gate-excludes-part-of-its-population`. A gate's trigger predicate
matches a population WIDER than the one it judges. So it refuses work it was
never meant to see. The usual cause is a policy change elsewhere that made an
old proxy signal common.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-22 | - | `scripts/dev/audit-test-relaxation.py`, the RFC-tagged branch | the population is a test that WEAKENED, and the trigger for an RFC-tagged file is that the file changed at all. For a `.ci` the tool counts expectations and can tell a removal from an addition; for a tagged `.go` file it reports `[WEAKENED]` and demands an owner row in `test/rfc-changed.md` for any diff, a pure addition included. Measured over one branch: seven of nine findings were files that only GAINED tests. A gate that charges owner approval for strengthening the suite spends the owner's attention on the safe direction, and it teaches every reader to skim past the two findings that matter | not fixed, and the conservatism is deliberate rather than accidental, so narrowing it is the owner's call. The habit meanwhile is to read every finding as a CANDIDATE and confirm at the diff which direction it moved, which is what the tool's own closing text asks for. The general practice: a gate whose trigger is "this file changed" is a proxy for the property it means to judge, and a proxy that fires on the safe direction as loudly as on the unsafe one carries no information at the point of decision |
| 2026-08-13 | - | commit helper, review gate | `spec_closure_stem` (`scripts/dev/commit_helper.py`) treats a NEW journal row that names a spec as a spec-closure signal. `review_gate_problems` then refuses the commit until an INDEPENDENT review artifact exists. That proxy was sound when a journal row appeared only at closure. CLAUDE.md now mandates one row for every defect an agent finds outside the work in hand. So an ordinary mid-spec commit that carries a journal row is read as a closure of a spec that stays open. The author is told to run a review phase they are not in. Met during RFC 7999 enrolment work, which closed nothing. The two other closure signals are precise, and neither fired: a new `plan/learned/` summary, or `git rm` of the spec file | worked around, not fixed. The row's Spec cell was set to `-`, and the spec named in the row prose instead. That contradicts `plan/journal/README.md`, where `-` means the defect was found OUTSIDE a spec. So the workaround costs the attribution the column exists for. The next agent who meets a defect mid-spec will reach for it again. Fixed 2026-08-22, and not by reading the spec's Status: the gate now asks WHOSE row it is. This row's own case survives that fix, because a row this session wrote naming the spec it claims is still this session's closure signal. The friction it names is the mid-spec row, and it is open |
| 2026-08-14 | - | commit_helper deferral scan | the deferral-language gate read `vendor/`. `DEFERRAL_SCAN_EXEMPT_DIRS` (`scripts/dev/commit_helper.py`) held only `ai/rules/` and `.claude/rules/`, so `deferral_in_diff_problems` scanned third-party source for phrases like "out of scope" and "postpone". `github.com/andybalholm/brotli` carries `TODO: Postpone decision until next block arrives?`, which blocked the commit that vendored templ. A dependency author's note is not a Ze deferral. No `plan/deferrals/` shard records one. The gate therefore had no reachable satisfying action. It blocks vendoring almost any large dependency | fixed. `vendor/` joins the exempt prefixes. Two tests pin it. The brotli comment under `vendor/` is exempt. The same comment under `internal/vendored/` is still caught, so the exemption keys on the prefix alone <!-- doc-links: ignore (negative example: a path outside vendor/, named to state where the exemption stops) --> |
| 2026-08-17 | - | `internal/test/runner.TestEveryExecSiteUsesChildEnv` | The text scan rejected `childEnv` because that function contained `append(os.Environ(), ...)`. The function is the required environment producer, not an exec site that bypasses it. The gate therefore made its own correct implementation red. | Fixed. The test parses Go syntax, permits `os.Environ` only inside `childEnv`, and rejects that call from every other runner function. |
| 2026-08-17 | - | commit helper, review gate | Appending a journal row by anchoring an `Edit` on the row above it and re-emitting that row makes git record the OLD row as added. `_journal_added_spec_stems` then reads its Spec cell, `_spec_closed_earlier` does not filter a spec that has not closed, and the fallback `return stems[0]` (`closing_spec_stem`, `scripts/dev/commit_helper.py`) names it as the spec being closed. `review_gate_problems` demanded an INDEPENDENT review artifact for `spec-fixit-zefs-diff-structural-ops`, which this session never claimed or touched. The docstring already anticipates the shape it produces -- "a commit that only EDITS one older row ... yields exactly ONE stem, months old" -- and the filter it added does not reach it, because the spec is recent and still open. Third override this session spent on gates that were not judging its work | worked around with `--review-override`, not fixed. Two halves. The editing habit is the cheaper one: append AFTER the last row without re-emitting it, and the diff carries one added line. The gate half is that a re-added identical line is indistinguishable from a new row in a diff but not in the file -- comparing the row TEXT against the same file at HEAD would tell them apart, and would cost nothing on a genuine closure |
| 2026-08-18 | - | `.claude/hooks/pretool-bash.py` | `check_destructive_git` matching a banned verb inside a quoted payload is DOCUMENTED and deliberate: `ai/rules/commands.md`, "The Bash Hook Matches Your Command Text, Including Search Patterns", states the hook is coarse on purpose and that one extra round-trip is cheaper than one real bare verb. The documented case is a `grep` whose search pattern spells a verb. The case it does not name is AUTHORING: a heredoc that writes a rule, a spec, or this journal row is refused for quoting the verb it exists to teach, and the documented workaround (decompose the phrase into a regex) is unavailable when the literal phrase is the content | no fix owed, the decision is settled. The authoring case is a doc increment to that section of `ai/rules/commands.md`, not a defect: write such a file with the Edit or Write tool, which the bash guard does not judge, rather than rewording the prose |
| 2026-08-18 | - | `scripts/vendor/check_web.go` | `vendorPackages` read every subdirectory of `third_party/web/` as a vendored package. `third_party/web/htmx-upgrade-check.py` lives beside the packages it checks, so `make ze-htmx-upgrade-check` writes `third_party/web/__pycache__/`, and the next `make ze-vendor-web-check` reported it UNSYNCED. One structural gate went red because a DIFFERENT gate ran, in the same verify, and the message asked the reader to add a bytecode cache to `sync_web.go`. Met while trying to commit: the red is a structural gate, so `commit_helper.py` refuses it and `--structural-red-ok` is an owner override | fixed. `ignoredNames` asks `git check-ignore` which entries the repository already calls not-content, and `vendorPackages` skips them. It asks git rather than matching `__pycache__` by name, so the next unpredicted cache is excluded on the same evidence. Outside a git checkout nothing is ignored, which reports more rather than less. A probe directory that git does NOT ignore is still reported UNSYNCED |
| 2026-08-18 | - | commit_helper deferral scan | `_deferral_prose` (`scripts/dev/commit_helper.py`) blanks quoted spans so quoted text is read as data, and a backslash-escaped quote closes the span that encloses it. The site search index writes one JSON object per page, and the pppoe-interop page quotes a code comment reading "LAC-initiated incoming calls are deferred to a later spec". The remainder of that JSON string therefore read as bare prose, `deferral_in_diff_problems` matched it, and the gate refused the gh-pages publish commit. A quotation cannot be recorded in a `plan/deferrals/` shard, and the published repository holds no `plan/` at all, so the gate had no reachable satisfying action. Same function and same shape as the 2026-08-14 row above, one escape along | fixed. `_deferral_prose` blanks the escaped quote before the pairing pass, so the enclosing span survives and the whole string stays data. Two tests pin it: the JSON quotation is data, and a real deferral sharing a line with a JSON escape is still caught |
| 2026-08-19 | - | `make ze-doc-links-check` | Red at HEAD, 33 dead path references, none of them documentation. A test script names a path that must NOT exist. The property under test is what the code does with an absent file. The gate reads that literal as a reference owing resolution. `scripts/dev/spec_doc_anchors_test.py` and `scripts/dev/hook-fixture-check.py` carry most of them. Met when I ran the gate over an unrelated change | fixed for this class, 31 findings down to 17. The 14 fixture lines took the `<!-- doc-links: ignore -->` marker the gate already offers, which is what `commit_helper_test.py` was already doing. The marker sits in a trailing Python comment, outside the string, so the fixture data it describes is unchanged. The gate was NOT taught to skip `*_test.py`: a test file can cite a real path too, and a blanket skip would stop checking those. The remaining 17 are real citations in four specs owned by other sessions and are not this class |
| 2026-08-21 | - | `make ze-doc-links-check` | Red at HEAD, 26 dead path references. Every one is in the three `plan/spec-record-answers-*.md` files that commit 1f1963a76 authored. 22 are forward references to `.ci` files those specs will create. 4 name paths this family moved or deleted. These are the "remaining 17 ... owned by other sessions" the row above left unfixed. Phase 2's move of `internal/component/plugin/answer_row.go` into `pkg/plugin/rpc/` grew the set. The gate is a `ze-precommit-verify` stage, so a red it never caused blocked this spec's closure | fixed. The 17 forward references took the `<!-- doc-links: ignore (...) -->` marker four other specs already use, and the 4 moved paths were repointed. The authoring gap is upstream. `plan/TEMPLATE.md` asks for Files to Create and never says that each row needs the marker. Every spec written from it therefore starts the gate red <!-- doc-links: ignore (the row names the path phase 2 moved out of, which is what it records) --> |
| 2026-08-22 | - | commit helper, review gate | Fourth row on this gate, and the first where the BLOCKED session is not the row's author. A journal CLASS file collects rows from several sessions. Two of its rows named another session's specs in their Spec cell, so `spec_closure_stem` read them as closure signals, and `commit_helper.py` refused my commit with `review-gate: BLOCKED - no independent-review artifact at tmp/review/fixit-flowspec-protocol-name-drift-<my session id>.md`. That artifact can never exist under my id: only the session that closed that spec produces it. My own row names no spec and is not a closure of anything | Fixed 2026-08-22 by attribution rather than by reading the spec's Status: a row naming an OPEN spec this session neither claims nor carries is another session's, and it is no longer this commit's closure signal (`_spec_belongs_to_another_session`). What this case adds is that the artifact is keyed to the COMMITTING session, which is what makes a foreign row unlandable rather than merely awkward, and that the 2026-08-13 workaround of setting the Spec cell to `-` is not available here: the rows belong to another session and are not mine to edit. The file was landed by that session instead, which works and does not scale: a shared class file can then only be committed by whichever session owns its spec-naming rows |
| 2026-08-22 | - | commit helper, review gate | Fifth row on this gate, and the one that moves the diagnosis. Here the DETECTION was right and the ATTRIBUTION was wrong, which the four rows above do not separate. A third session REWROTE an existing row of a shared class file, and a rewritten row reads as an added row in a diff against HEAD, so `_journal_added_spec_stems` correctly saw one added row naming a spec that a session really is closing. The blocked session had added twenty-two lines of header prose and no row at all. So the repair the earlier rows name, reading the spec's own Status, does not reach this case: the Status is legitimately in-progress and a closure really is under way. What the gate cannot see is WHOSE closure it is, and it resolves that by demanding the artifact from whichever session commits the file first, which in a shared checkout is arbitrary. A file can therefore become unlandable without gaining a single new row, and the session it blocks can be one that touched no row | Fixed 2026-08-22 along the route this row names. The two repairs are now known to be different. Reading Status fixes the case where nobody is closing anything; this case needs the gate to ask whether the added row is one THIS commit's session wrote, which a diff against HEAD cannot answer and the session marker can. The correct outcome was reached by accident: the file was dropped from the blocked commit and left for the closing session to carry, which is what `ai/rules/git-safety.md` already prescribes for foreign rows, and that session is the one that genuinely owes the artifact |
| 2026-08-22 | - | `scripts/dev/commit_helper.py`, the `--file` list | a `--file` names a PATH, and in a checkout three sessions share, a path carries whatever is in it at that moment rather than the hunk the author prepared. Another session named `docs/functional-tests.md` after checking its OWN section was ready, and committed two of this session's sections with it in `ce49715ef`: 57 added lines across three authors under a message about repairing dangling citations. The content is intact and the gates pass; what is lost is attribution, so a reader looking for where the allocation ceiling or the exec-channel frame section landed finds a commit about citations | not fixed, and the disposition is deliberate. `ai/rules/git-safety.md` bans rewriting history to reclaim attribution and says the content being preserved is the thing that matters, so this is disclosed rather than repaired. Worth recording because the correct behaviour was demonstrated twice in the same file on the same day: two agents of this session each declined to commit it, each having noticed it carried another session's in-flight work, and each said so rather than sweeping it. Checking that your own hunk is ready is not the same as checking that the file is |
| 2026-08-22 | - | commit helper, review gate | Sixth row, and the one that closes the class. `spec_closure_stem` (`scripts/dev/commit_helper.py`) read every added row naming a spec as THIS commit's closure, and `review_gate_problems` then demanded `tmp/review/<their-stem>-<my-session>.md`, a path only the closing session can write. The rows dated 2026-08-13, 2026-08-17 and 2026-08-22 on this gate are the same block, met by four sessions | fixed. `_spec_belongs_to_another_session` drops a stem naming an OPEN spec this session neither claims nor carries, and `closure_reminder` reads the same list through the same filter, because a filter that lands in one reader alone is how these two last disagreed. Two facts say the spec is this session's and both are load-bearing: the per-session claim, for every commit made while the work is in flight, and `plan/spec-<stem>.md` in the commit's own file list, because `ze-close` step 6b releases the claim BEFORE step 6d prepares the commit. A stem with no spec file at all stays a signal, so a misspelled Spec cell still fails closed. Seven tests pin it |
