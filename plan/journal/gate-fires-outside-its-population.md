# gate-fires-outside-its-population

The mirror of `gate-excludes-part-of-its-population`. A gate's trigger predicate
matches a population WIDER than the one it judges. So it refuses work it was
never meant to see. The usual cause is a policy change elsewhere that made an
old proxy signal common.

| Date | Spec | Surface | Symptom | Fix |
| 2026-09-05 | fixit-send-names-its-destination | `./le cli-grammar`, its `--flag in YANG` check | RED AT HEAD and unrelated to the work it was found under. The check's population is a flag DECLARED as operator surface in a YANG node. Its proxy is the literal `--` followed by a word, anywhere in the module text, so it also matches a description that MENTIONS a flag. `internal/component/hub/yang/ze-hub-conf.yang:120` and `:134` each describe how a leaf interacts with an offline flag ("the --pprof flag wins over it", "The --chaos-seed flag wins over"), which is the sentence an operator needs and is not a declaration. The gate reports `cli-grammar: FAILED (0 grammar, 2 flag-in-yang, 0 demo-launch, 0 flag-register)`, so the two prose hits are the whole failure. Both lines are committed content (`af79f468f0`) and neither file is edited in this tree | not fixed: recorded. It blocks no goal of the work in hand, and the repair is a scope decision rather than a one-line narrowing. The direction for whoever takes it: the check reads the module text, so it cannot tell a `description`/`ze:help` string from a node NAME or a leaf's default. Skipping the string arguments of `description` and `ze:help` would clear both hits and keep every declaration in the population; deleting the two sentences instead would cost an operator the reason the leaf and the flag disagree |
| 2026-09-03 | - | `test/plugin/iface-link-flap-during-commit.ci` and `flapCommitLead08` (`internal/test/fixture/plugin_fixture_08_flap.go`) | The test cannot build its own scenario on a current host, and the failure it reported named the wrong cause. It flaps a link while a config commit holds `dhcpMu`, and it asserts three things per round, one of which is that the burst outran the worker. That assertion was checked on EVERY round, including rounds the fixture itself had just measured as not overlapping a commit, so a round where the burst had nothing to outrun failed the run with "coalesced counter did not move; burst never outran worker" rather than spending one of the retries the round loop already budgets for exactly that. Scoping the assertion to `overlapped` let the run reach the end, and the end reported `only 0 of 3 wanted rounds overlapped a commit in 6 attempts ... ze_iface_link_worker_blocked_total did not move`, measured four times on the arm64 QEMU VM. That message was believed and it was wrong: the reload holds the lock exactly as designed, and the counter could not see it. The lead was suspected next and was innocent too, one second and 100 ms giving the same zero of six, because neither changes what the counter can see. The scenario was built on every one of those runs | BOTH halves fixed, and the second was mine. The scoping fix let the run reach the end, and the end blamed the lead, which was wrong: the reload holds `dhcpMu` exactly as designed and nothing had moved that work out from under the lock (`DHCPClient.Stop`, `internal/plugins/iface/dhcp/dhcp_linux.go`, still waits on each of forty clients in turn). What could not see it was the INSTRUMENT, added the same day in `1942e9806f`. The worker labels a block with the interface name on the queue entry it was about to handle, and the entry that meets a held lock is almost never the burst: the rate tracker pushes a carrier resync every second and `pushResync` (`internal/component/iface/link_queue.go`) builds a key with NO interface name, so the block counts under `name=""`. The burst then coalesces behind that resync and finds the lock free. So `{name="zeflapv0"}` reads zero through a genuine overlap, and the fixture read exactly that series. `flapBlocked08` sums every series of the counter, and the test then passed four of four -- WHICH WAS ALSO WRONG, and an independent review caught it the same day. The fixture read that counter over a window that opened before the SIGHUP, so a block recorded during the lead, before the burst existed, satisfied it; the hold contains a 1 Hz resync tick with certainty, so the check was true in every round that took the lock at all and the wanted-rounds guard could no longer fail for the condition it names. The green measured the guard going unfalsifiable. Narrowed to the burst window the answer is `0 of 3 wanted rounds overlapped`, so the stimulus is NOT being built and the file's original premise was closer to right than its correction. The deeper finding is that this counter cannot answer the question at all: the worker takes the lock once per drained entry, so at most one block exists per contiguous hold, and when the resync wins that race the burst's own entry never TryLocks. A wide window over-reports and a narrow one under-reports. The test needs a counter of events QUEUED WHILE THE WORKER IS BLOCKED, which does not exist. Also correcting a claim this row made on the way: per-round fixture stderr IS relayed. `printGenericReport` calls `truncateOutput(rec.ClientOutput, 30)` and `truncateOutput` (`internal/test/runner/report.go`) keeps `lines[:maxLines]`, the FIRST 30, so a pre-loop line survives and every round line is cut. The one real dead end stands: `internal/component/iface` publishes no config-apply counter to poll, and that gap is what made this cost three QEMU cycles and two agents to answer a question one scrape should answer. `ze_iface_config_applies_total`, beside the four counters `bindMetricsRegistry` already registers (`internal/component/iface/rate.go`), would say whether a reload reached the apply and when; `ze_as112_config_reload_total` and `ze_geodns_config_reload_total` are the in-tree precedents. Not built here. Two designs were surveyed and rejected for this repair and are worth knowing about: a `zetest` rendezvous inside the apply removes the timing race but needs a production call site with an empty body, and a participant fixture holding the reload is a trap, because participant order comes from ranging a Go map (`AllProcesses`, `internal/component/plugin/process/manager.go`) and is randomized per reload |
| 2026-09-03 | local-ca | `./le doc wiring`, its symbol-to-page binding | The check's population is a page whose CLAIM a change made wrong. Its proxy is a page that CITES a symbol the change touched, and the two differ wherever a large function serves many pages. Thirteen lines added to `runYANGConfig` (`cmd/ze/hub/main.go`) to load the daemon's certificate authority produced six drift rows on pages about the zefs user snapshot, the boot AAA bundle, the signal table and command completion, across `docs/architecture/api/architecture.md`, `api/commands.md`, `api/process-protocol.md`, `behavior/signals.md` and `cli/command-completion.md`. All six claims were read and all six are still true. `runYANGConfig` is the daemon's startup path, so it is cited by every page that describes anything happening at startup, and the proxy therefore charges each of them to any change anywhere in it. A concurrent session adding a looking-glass certificate to the same function met the same six rows in the same hour, which is the tell that the population is the FUNCTION's citers rather than the change's | not fixed: recorded, on the owner's decision of 2026-09-03. The alternative was six edits writing "still true" into five pages whose subject is unrelated, two of them owned by other phases of the same spec, which teaches the next session that a correct page still owes a touch and costs the review its scope. The repair direction, for whoever takes it: the binding is symbol-level and no gate can read a sentence, so the question it can actually answer is narrower than the one it asks. A citation could name the BEHAVIOR it depends on rather than the enclosing function, which is what a `<!-- source: -->` anchor already does for a small function and fails to do for a startup path of several hundred lines. Until then, a wide function's citers are a population no change can clear honestly |
| 2026-09-02 | - | `./le hook-check pretool-agent-skill`, `coveredSkill` (`internal/le/hookruntime/agent.go`) | The `/ze-review` trigger is a proximity pattern. It matches `review`, `critique` or `audit` within 60 characters of `diff`, `change`, `commit`, `branch`, `implementation`, `code` or `pr`. It carries no polarity. A brief that names the word to EXCLUDE the task matches exactly like one that asks for it. An implementation brief was refused twice. The first carried "audit wiring the attached model also lacks. Config mode is the change". The second carried "review. The owner ordered the change". Each disclaimer put the word within 60 characters of `change`, and few implementation briefs avoid that noun. The same file already subtracts this population for the OTHER gate: `isReviewWork` returns false when `implementationVerb` matches the first 160 characters. `agentSkill` applies no such subtraction, so one gate knows an implementation brief when it sees one and its neighbour does not. The third attempt passed only because both words were deleted from prose that described the work correctly | not fixed. An operator question surfaced it while an unrelated CLI change was built, and it blocks no goal of that change. Two repairs are open, and whoever takes it picks one. `coveredSkill` can take the `implementationVerb` subtraction that `isReviewWork` already has. Or the trigger can require the word in an IMPERATIVE position, rather than anywhere within 60 characters of a common noun |
| 2026-08-24 | - | commit helper, pre-commit verification gate | Seventh row, and it reopens the class the sixth declared closed. That row rests on `_spec_belongs_to_another_session` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)), which filters a row naming ANOTHER session's spec out of the review gate. A different gate reads the same proxy and does not take that filter. `spec_audit_problems` asks whether the CLAIMING session's own spec stem is among the added journal stems, and refuses with "has no `## Pre-Commit Verification` section, but this commit adds its closure artifact". So the sixth row closed the FOREIGN-spec case and left the OWN-spec case open, and the own-spec case is the one CLAUDE.md makes routine: a row is mandated for every defect an agent finds, and an agent finds most of them inside its own spec. Hit while committing three journal rows mid-spec, with two specs open and neither being closed. Unlike the review gate this one takes NO override: `--review-override` clears `review_gate_problems` and this fires afterwards | not fixed, and both escapes were refused deliberately. the retired `scripts/dev/spec-session.sh release` (current producer: `internal/le/spec/session/session.go`) silences it, because the gate returns early when no spec is claimed and `_release_session` (`.claude/hooks/lib/state-file.sh` (retired; now `internal/le/hookruntime/session.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->) only deletes a marker under `tmp/`, changing no record. That is gaming a gate rather than satisfying it. Filling the section satisfies it, but `pre_commit_verification_gaps` demands a data row in every evidence sub-table including AC Verified, and the spec's Phase 4 was part done, so the fill would have been a false statement in the one place the closure gate trusts. The rows were left uncommitted instead and rode to their closure commit. The shape for whoever fixes it: the gate needs the same question the review gate learned to ask, which is not "does a row name this spec" but "does this commit CLOSE it", and a commit adding no learned summary and removing no `plan/spec-*.md` closes nothing. Read at the producer 2026-08-24, and the gate has less to work with than it looks. `closes` is `any(pattern.match(p) for p in add_paths) or stem in _journal_added_spec_stems(...)`, where `pattern` is `^plan/learned/[0-9]+-<stem>.md$`. That first half is DEAD: commit `2cff2050a` replaced the learned corpus with this journal, so `plan/learned/` holds three index files and no numbered summary, and the pattern can never match again. The journal row is therefore the gate's ONLY closure signal, which is why it cannot tell a closure row from a mid-spec one: there is no second signal left to cross-check against. The function's own docstring says as much, and its "deliberate rather than a drift" sentence is about a different thing, the `_spec_closed_earlier` filter that `spec_closure_stem` applies and this gate does not. The fix is cheaper than the docstring's care suggests: `remove_paths` is already a parameter of this function and nothing consults it for the spec's own path, so the question "does this commit remove `plan/<claimed>`" is answerable where the wrong question is being asked |
|------|------|---------|---------|-----|
| 2026-08-26 | - | the retired `scripts/checks/checks_test.go` (current producer: `internal/le/repository/`) `TestFirstPartyGoProducerCommandsDisableCGO` | RED AT HEAD and unrelated to the work it was found under. The guard's population is a Go producer CALL SITE in a Python file, and its proxy is "an `env=` appears within 24 lines of the argv". Commit `414da7294` (2026-08-25) made every gate's argv DATA, so the argv now sits in a `Gate(argv=(...))` row or in a helper returning a tuple, and the environment is supplied once by `gateapp.action(opts, GATES, env=...)` at the bottom of the module. The invariant still holds -- `Toolchain.environment` (the retired `scripts/le/devtools/toolchain.py` (current producer: `internal/le/`)) does `dict(os.environ)` and pins `CGO_ENABLED=0`, and `gateapp.default_environment` returns it -- and the guard cannot see across the function boundary, so it reports 10 sites in the retired `scripts/le/application/` (current producer: `internal/le/`) and the retired `scripts/le/devtools/toolchain.py` (current producer: `internal/le/`). Reproduced against unmodified files: `git diff HEAD -- scripts/le (retired; current producer: `internal/le/`)/` is empty in the tree where it was measured | not fixed, and the attempt is recorded because it shows why. Two repairs were tried and both were reverted. Treating "no `subprocess.` in the window" as the data signal reported 15 further files whose runner sits outside the window. Treating "an `argv=` keyword or an enclosing `-> tuple[str, ...]`" as the signal left 5 sites, because `functional.py` states its argvs as a list literal assigned to `commands` and `toolchain.py` returns `list[str]`. A fourth shape would be a guess, and widening the proxy any further trades a per-site guard for a per-file one. The decision belongs to whoever made gates data: does the environment obligation move to the runner, and is routing through `gateapp` the whole of it? The guard MUST NOT be relaxed to a per-file `env=` search, which is the cheap answer and would stop it seeing a script that runs Go with no environment at all |
| 2026-08-24 | fixit-stored-route-relay-hardening | the stall watchdog in the retired `scripts/dev/ze-run.sh` (current producer: `internal/le/job/job.go`), met through the retired `ze-unit-reactor-test-race` (current: `go test -race ./internal/component/bgp/reactor/...`) | the watchdog's liveness predicate is "the holder is alive and its log has not grown for `STALL_SECONDS`" (default 1800), and a waiter that sees it calls `_break_stalled`, which does `kill -TERM` on the holder's process GROUP. The file's own comment argues that predicate is safe because "a growing log can" tell hung from slow "however long the job has been running". That is false for a target whose log cannot grow: `go test` without `-v` prints one result line per package when the package FINISHES, so `_ze-unit-reactor-test-race-impl`, which is one `go test -race -count=20` over one package, is silent for its entire run by construction. On a contended box that run exceeds 1800s, so the wrapper reaps its own healthy job. Measured here three times while two sibling `./le verify worktree` gates held the machine at load 42 on 32 cores: run 1 reached `go test`'s own 20m limit, and runs 2 and 3 were SIGTERMed at 30m2s and ~30m with `Terminated` and `Error 141`, each with ZERO data races and ZERO failed assertions in the captured log. Run 1 is a SECOND and already-recorded defect, not this one: the 2026-08-23 `ze-unit-reactor-test-race` row in `gate-verdict-depends-on-the-machine.md` measured one iteration at about 120s, so `-count=20` needs about 40 minutes against a `GO_TEST_TIMEOUT` of 20m and the gate cannot pass as configured. The two compose, which is why the target is currently unpassable on a shared box: raising the budget to clear the first walks into this one. The kill needs a waiter to arrive, so the target is reliable on an idle box and reaped on a busy one, which is the shape that makes it read as flakiness | not fixed, and there is NO per-invocation escape: `ZE_JOB_STALL_SECONDS=3600` plus `GO_TEST_TIMEOUT=90m` was tried as run 4 and was SIGTERMed the same way. The knob cannot help, because the kill is not made by the holder. `STALL_SECONDS` is assigned once at the top of the retired `scripts/dev/ze-run.sh` (current producer: `internal/le/job/job.go`) from the environment of whichever process is WAITING for a slot, and the stall branch that calls `_break_stalled` sits inside that waiter's loop, so the window deciding a holder's fate belongs to a sibling session's job and the holder cannot raise it. A holder is safe only if every concurrent waiter agrees, which no invocation can arrange. Four runs, four terminations, and zero data races and zero failed assertions in all four captured logs. Two repairs fit and the choice is a design decision. Either the impl half streams progress, which `-v` alone would do and which makes the existing liveness signal true rather than replacing it, or a job records in its registry entry that it produces no output until it ends and the waiter judges such a holder by process liveness alone. The first is preferable because it keeps one predicate for every job, and it needs no new field. The population to check before either lands is every `_*-impl` recipe whose body is a single `go test` without `-v` |
| 2026-08-24 | fixit-lint-blind-to-every-other-build-tag | `population()` in the retired `scripts/dev/lint_flavors.py` (current producer: `internal/le/verify/lint/matrix.go`) | the lint coverage assertion reads `git ls-files '*.go'`, which answers from the INDEX. A tracked file DELETED in the working tree is therefore still in the population, no pass can load it, and the driver reports `NOT COVERED BY ANY PASS`. `./le verify lint run` went red on `cmd/ze/hub/bgp_decode_nolink_test.go` <!-- doc-links: ignore (the test file this row records was never created) --> with all 19 passes at 0 issues, and no reachable answer existed short of committing the deletion: the file is gone, so no flavor row can select it and a RESIDUE entry would have to be deleted again in the same commit. Every session that deletes a `.go` file meets this between the deletion and its commit | fixed, two lines: `population()` skips a path that is not on disk. `test_a_file_deleted_in_the_working_tree_leaves_the_population` drives `population()` with a fake ROOT and a fake `git ls-files` answer, and is red at `Items in the first set but not the second: 'deleted.go'` against the old body |
| 2026-08-22 | - | the retired `scripts/dev/audit-test-relaxation.py` (current producer: `internal/le/testweakened/audit.go`), the RFC-tagged branch | the population is a test that WEAKENED, and the trigger for an RFC-tagged file is that the file changed at all. For a `.ci` the tool counts expectations and can tell a removal from an addition; for a tagged `.go` file it reports `[WEAKENED]` and demands an owner row in `test/rfc-changed.md` for any diff, a pure addition included. Measured over one branch: seven of nine findings were files that only GAINED tests. A gate that charges owner approval for strengthening the suite spends the owner's attention on the safe direction, and it teaches every reader to skim past the two findings that matter | not fixed, and the conservatism is deliberate rather than accidental, so narrowing it is the owner's call. The habit meanwhile is to read every finding as a CANDIDATE and confirm at the diff which direction it moved, which is what the tool's own closing text asks for. The general practice: a gate whose trigger is "this file changed" is a proxy for the property it means to judge, and a proxy that fires on the safe direction as loudly as on the unsafe one carries no information at the point of decision |
| 2026-08-13 | - | commit helper, review gate | `spec_closure_stem` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)) treats a NEW journal row that names a spec as a spec-closure signal. `review_gate_problems` then refuses the commit until an INDEPENDENT review artifact exists. That proxy was sound when a journal row appeared only at closure. CLAUDE.md now mandates one row for every defect an agent finds outside the work in hand. So an ordinary mid-spec commit that carries a journal row is read as a closure of a spec that stays open. The author is told to run a review phase they are not in. Met during RFC 7999 enrolment work, which closed nothing. The two other closure signals are precise, and neither fired: a new `plan/learned/` summary, or `git rm` of the spec file | worked around, not fixed. The row's Spec cell was set to `-`, and the spec named in the row prose instead. That contradicts `plan/journal/README.md`, where `-` means the defect was found OUTSIDE a spec. So the workaround costs the attribution the column exists for. The next agent who meets a defect mid-spec will reach for it again. Fixed 2026-08-22, and not by reading the spec's Status: the gate now asks WHOSE row it is. This row's own case survives that fix, because a row this session wrote naming the spec it claims is still this session's closure signal. The friction it names is the mid-spec row, and it is open |
| 2026-08-14 | - | commit_helper deferral scan | the deferral-language gate read `vendor/`. `DEFERRAL_SCAN_EXEMPT_DIRS` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)) held only `ai/rules/` and `.claude/rules/`, so `deferral_in_diff_problems` scanned third-party source for phrases like "out of scope" and "postpone". `github.com/andybalholm/brotli` carries `TODO: Postpone decision until next block arrives?`, which blocked the commit that vendored templ. A dependency author's note is not a Ze deferral. No `plan/deferrals/` shard records one. The gate therefore had no reachable satisfying action. It blocks vendoring almost any large dependency | fixed. `vendor/` joins the exempt prefixes. Two tests pin it. The brotli comment under `vendor/` is exempt. The same comment under `internal/vendored/` is still caught, so the exemption keys on the prefix alone <!-- doc-links: ignore (negative example: a path outside vendor/, named to state where the exemption stops) --> |
| 2026-08-17 | - | `internal/test/runner.TestEveryExecSiteUsesChildEnv` | The text scan rejected `childEnv` because that function contained `append(os.Environ(), ...)`. The function is the required environment producer, not an exec site that bypasses it. The gate therefore made its own correct implementation red. | Fixed. The test parses Go syntax, permits `os.Environ` only inside `childEnv`, and rejects that call from every other runner function. |
| 2026-08-17 | - | commit helper, review gate | Appending a journal row by anchoring an `Edit` on the row above it and re-emitting that row makes git record the OLD row as added. `_journal_added_spec_stems` then reads its Spec cell, `_spec_closed_earlier` does not filter a spec that has not closed, and the fallback `return stems[0]` (`closing_spec_stem`, the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)) names it as the spec being closed. `review_gate_problems` demanded an INDEPENDENT review artifact for `spec-fixit-zefs-diff-structural-ops`, which this session never claimed or touched. The docstring already anticipates the shape it produces -- "a commit that only EDITS one older row ... yields exactly ONE stem, months old" -- and the filter it added does not reach it, because the spec is recent and still open. Third override this session spent on gates that were not judging its work | worked around with `--review-override`, not fixed. Two halves. The editing habit is the cheaper one: append AFTER the last row without re-emitting it, and the diff carries one added line. The gate half is that a re-added identical line is indistinguishable from a new row in a diff but not in the file -- comparing the row TEXT against the same file at HEAD would tell them apart, and would cost nothing on a genuine closure |
| 2026-08-18 | - | `.claude/hooks/pretool-bash.py` (retired; now `internal/le/hookruntime/bash.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> | `check_destructive_git` matching a banned verb inside a quoted payload is DOCUMENTED and deliberate: `ai/rules/commands.md`, "The Bash Hook Matches Your Command Text, Including Search Patterns", states the hook is coarse on purpose and that one extra round-trip is cheaper than one real bare verb. The documented case is a `grep` whose search pattern spells a verb. The case it does not name is AUTHORING: a heredoc that writes a rule, a spec, or this journal row is refused for quoting the verb it exists to teach, and the documented workaround (decompose the phrase into a regex) is unavailable when the literal phrase is the content | no fix owed, the decision is settled. The authoring case is a doc increment to that section of `ai/rules/commands.md`, not a defect: write such a file with the Edit or Write tool, which the bash guard does not judge, rather than rewording the prose |
| 2026-08-18 | - | the retired `scripts/vendor/check_web.go` (current producer: `internal/le/vendorweb/vendorweb.go`) | `vendorPackages` read every subdirectory of `third_party/web/` as a vendored package. `third_party/web/htmx-upgrade-check.py` (retired; now `internal/le/htmxupgrade/scanner.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> lives beside the packages it checks, so `./le htmx-upgrade check` writes `third_party/web/__pycache__/` (gone with the Python scanner) <!-- doc-links: ignore (a Python bytecode cache that no longer exists after the 2026-08-28 interpreter cutover) -->, and the next `./le vendor-web check` reported it UNSYNCED. One structural gate went red because a DIFFERENT gate ran, in the same verify, and the message asked the reader to add a bytecode cache to `sync_web.go`. Met while trying to commit: the red is a structural gate, so `commit_helper.py` refuses it and `--structural-red-ok` is an owner override | fixed. `ignoredNames` asks `git check-ignore` which entries the repository already calls not-content, and `vendorPackages` skips them. It asks git rather than matching `__pycache__` by name, so the next unpredicted cache is excluded on the same evidence. Outside a git checkout nothing is ignored, which reports more rather than less. A probe directory that git does NOT ignore is still reported UNSYNCED |
| 2026-08-18 | - | commit_helper deferral scan | `_deferral_prose` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)) blanks quoted spans so quoted text is read as data, and a backslash-escaped quote closes the span that encloses it. The site search index writes one JSON object per page, and the pppoe-interop page quotes a code comment reading "LAC-initiated incoming calls are deferred to a later spec". The remainder of that JSON string therefore read as bare prose, `deferral_in_diff_problems` matched it, and the gate refused the gh-pages publish commit. A quotation cannot be recorded in a `plan/deferrals/` shard, and the published repository holds no `plan/` at all, so the gate had no reachable satisfying action. Same function and same shape as the 2026-08-14 row above, one escape along | fixed. `_deferral_prose` blanks the escaped quote before the pairing pass, so the enclosing span survives and the whole string stays data. Two tests pin it: the JSON quotation is data, and a real deferral sharing a line with a JSON escape is still caught |
| 2026-08-19 | - | `./le doc check links` | Red at HEAD, 33 dead path references, none of them documentation. A test script names a path that must NOT exist. The property under test is what the code does with an absent file. The gate reads that literal as a reference owing resolution. the retired `scripts/dev/spec_doc_anchors_test.py` (current producer: `internal/le/`) and the retired `scripts/dev/hook-fixture-check.py` (current producer: `internal/le/`) carry most of them. Met when I ran the gate over an unrelated change | fixed for this class, 31 findings down to 17. The 14 fixture lines took the `<!-- doc-links: ignore -->` marker the gate already offers, which is what `commit_helper_test.py` was already doing. The marker sits in a trailing Python comment, outside the string, so the fixture data it describes is unchanged. The gate was NOT taught to skip `*_test.py`: a test file can cite a real path too, and a blanket skip would stop checking those. The remaining 17 are real citations in four specs owned by other sessions and are not this class |
| 2026-08-21 | - | `./le doc check links` | Red at HEAD, 26 dead path references. Every one is in the three `plan/spec-record-answers-*.md` files that commit 1f1963a76 authored. 22 are forward references to `.ci` files those specs will create. 4 name paths this family moved or deleted. These are the "remaining 17 ... owned by other sessions" the row above left unfixed. Phase 2's move of `internal/component/plugin/answer_row.go` into `pkg/plugin/rpc/` grew the set. The gate is a `./le verify current mode full` stage, so a red it never caused blocked this spec's closure | fixed. The 17 forward references took the `<!-- doc-links: ignore (...) -->` marker four other specs already use, and the 4 moved paths were repointed. The authoring gap is upstream. `plan/TEMPLATE.md` asks for Files to Create and never says that each row needs the marker. Every spec written from it therefore starts the gate red <!-- doc-links: ignore (the row names the path phase 2 moved out of, which is what it records) --> |
| 2026-08-22 | - | commit helper, review gate | Fourth row on this gate, and the first where the BLOCKED session is not the row's author. A journal CLASS file collects rows from several sessions. Two of its rows named another session's specs in their Spec cell, so `spec_closure_stem` read them as closure signals, and `commit_helper.py` refused my commit with `review-gate: BLOCKED - no independent-review artifact at tmp/review/fixit-flowspec-protocol-name-drift-<my session id>.md`. That artifact can never exist under my id: only the session that closed that spec produces it. My own row names no spec and is not a closure of anything | Fixed 2026-08-22 by attribution rather than by reading the spec's Status: a row naming an OPEN spec this session neither claims nor carries is another session's, and it is no longer this commit's closure signal (`_spec_belongs_to_another_session`). What this case adds is that the artifact is keyed to the COMMITTING session, which is what makes a foreign row unlandable rather than merely awkward, and that the 2026-08-13 workaround of setting the Spec cell to `-` is not available here: the rows belong to another session and are not mine to edit. The file was landed by that session instead, which works and does not scale: a shared class file can then only be committed by whichever session owns its spec-naming rows |
| 2026-08-22 | - | commit helper, review gate | Fifth row on this gate, and the one that moves the diagnosis. Here the DETECTION was right and the ATTRIBUTION was wrong, which the four rows above do not separate. A third session REWROTE an existing row of a shared class file, and a rewritten row reads as an added row in a diff against HEAD, so `_journal_added_spec_stems` correctly saw one added row naming a spec that a session really is closing. The blocked session had added twenty-two lines of header prose and no row at all. So the repair the earlier rows name, reading the spec's own Status, does not reach this case: the Status is legitimately in-progress and a closure really is under way. What the gate cannot see is WHOSE closure it is, and it resolves that by demanding the artifact from whichever session commits the file first, which in a shared checkout is arbitrary. A file can therefore become unlandable without gaining a single new row, and the session it blocks can be one that touched no row | Fixed 2026-08-22 along the route this row names. The two repairs are now known to be different. Reading Status fixes the case where nobody is closing anything; this case needs the gate to ask whether the added row is one THIS commit's session wrote, which a diff against HEAD cannot answer and the session marker can. The correct outcome was reached by accident: the file was dropped from the blocked commit and left for the closing session to carry, which is what `ai/rules/git-safety.md` already prescribes for foreign rows, and that session is the one that genuinely owes the artifact |
| 2026-08-22 | - | the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`), the `--file` list | a `--file` names a PATH, and in a checkout three sessions share, a path carries whatever is in it at that moment rather than the hunk the author prepared. Another session named `docs/functional-tests.md` after checking its OWN section was ready, and committed two of this session's sections with it in `ce49715ef`: 57 added lines across three authors under a message about repairing dangling citations. The content is intact and the gates pass; what is lost is attribution, so a reader looking for where the allocation ceiling or the exec-channel frame section landed finds a commit about citations | not fixed, and the disposition is deliberate. `ai/rules/git-safety.md` bans rewriting history to reclaim attribution and says the content being preserved is the thing that matters, so this is disclosed rather than repaired. Worth recording because the correct behaviour was demonstrated twice in the same file on the same day: two agents of this session each declined to commit it, each having noticed it carried another session's in-flight work, and each said so rather than sweeping it. Checking that your own hunk is ready is not the same as checking that the file is |
| 2026-08-22 | - | commit helper, review gate | Sixth row, and the one that closes the class. `spec_closure_stem` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)) read every added row naming a spec as THIS commit's closure, and `review_gate_problems` then demanded `tmp/review/<their-stem>-<my-session>.md`, a path only the closing session can write. The rows dated 2026-08-13, 2026-08-17 and 2026-08-22 on this gate are the same block, met by four sessions | fixed. `_spec_belongs_to_another_session` drops a stem naming an OPEN spec this session neither claims nor carries, and `closure_reminder` reads the same list through the same filter, because a filter that lands in one reader alone is how these two last disagreed. Two facts say the spec is this session's and both are load-bearing: the per-session claim, for every commit made while the work is in flight, and `plan/spec-<stem>.md` in the commit's own file list, because `ze-close` step 6b releases the claim BEFORE step 6d prepares the commit. A stem with no spec file at all stays a signal, so a misspelled Spec cell still fails closed. Seven tests pin it |
| 2026-08-23 | - | commit helper, review gate | `spec_closure_stem` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)) read the removal of `plan/spec-<stem>.md` as commit B of a spec closure. It consulted nothing else in the commit. A spec that does not block the first release moves to `plan/future/`, and that commit removes the old path and adds the same file under the new directory. Nothing closed and no review ran. `review_gate_problems` then demanded `tmp/review/<stem>-<session>.md` for a spec that is still open. The reachable answers were an owner-only override, or two commits that each leave the tree inconsistent | fixed. `_spec_closing_removals` drops a removal whose basename the same commit adds back somewhere else. Both readers of a removal go through it, because a filter that lands in one reader alone is how those two last diverged. The general practice: a removal is evidence of intent only when nothing else in the same commit contradicts it. A gate that reads one half of a rename as a decision will refuse the other half |
| 2026-08-24 | - | the retired `scripts/dev/ste_check.py` (current producer: `internal/le/ste/ste.go`) | The prose gate judged three populations it does not mean to judge. `check_plain_words` matched `Cease` in `the Cease/MaxPrefixes Data field` and asked for the verb `stop`. That is the NOTIFICATION error code of RFC 4271 Section 4.5, and no rewrite of the sentence can clear it. `units_markdown` reads `website/faq/faq.md` as prose. That file is an HTML document behind a `.md` extension, so its `<section>` markup counts as a 61-word sentence. The same reader joins consecutive short lines into one paragraph, so a 15-line commit list in a root-level handover counts as one 187-word sentence. None of the three clears by rewriting. The STE rule names that failure. A checker that flags a protocol name or a code span gets switched off, and then it protects nothing | `Cease` fixed. `PLAIN_WORD_IDENTIFIERS` skips a capitalized match that no sentence boundary precedes, and two tests pin both directions. The other two are open. The markup case needs the unit reader to tell an HTML body from a Markdown one. The short-line case needs a paragraph whose lines each end without punctuation to be read as lines rather than as one sentence |
| 2026-08-24 | - | scoped gates in a shared checkout | **A gate whose scope SILENTLY WIDENS to `./...` reports on packages the author never touched, and a package mid-TDD is indistinguishable from a broken one.** `./le changed scope` falls back to linting every package and every feature when no verify run has recorded a green commit, and it says so in one line nobody reads: `verify-scope: no verify run has recorded a green commit ... so every package and every feature is selected`. That condition held for every session all night, because a verify takes 25 to 53 minutes and none had completed. Four times in one day a session reported a RED belonging to another session, each time a half-written function in a package being built test-first rather than a landed break. The most expensive instance cost a detour to a clean clone: `commandDecls` was called in `internal/component/bgp/plugins/rs/server.go` and not yet defined, which is correct mid-TDD and reads exactly like a compile error to anyone else. The reporter cannot tell the two apart, because the difference is intent and the tree carries none | not fixed. The row records the mechanism, which is distinct from its neighbours: the gate is not reading the wrong TREE, it is reading the right tree with the wrong SCOPE, and the widening is a fallback rather than a choice. Two directions of repair, and the second is the cheaper one: make the scoped gate refuse to run rather than silently widen when it cannot compute a scope, so the author learns the scope is unavailable instead of learning that somebody else's package is red. The detour paid for itself in this instance, which is a fact about luck and not about the gate: the clean clone showed three functional tests RED at HEAD that no working tree in the building could see |
| 2026-08-24 | website-asciinema-terminal-demos | `c_throwaway_tests` in `.claude/hooks/pretool-writeedit.py` (retired; now `internal/le/hookruntime/writeedit.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> | The guard means "a test file must not be written to `/tmp`", and it refuses any `test_*.py` whose path is not under `internal/`, `test/` or `cmd/`. `website/tools/` is none of those and is a real Python test root: the retired `Makefile:512` (current producers: `internal/le/` native action tables) runs `pytest -q website/tools/test_*.py` inside `ze-site-generate`, and two such files are already tracked there. So writing `website/tools/test_render_demos.py`, which this spec's TDD plan names, was BLOCKED with "Tests belong in the source tree, not /tmp" while the source tree is exactly where it was going. Every session that adds a site test meets it | worked around, not fixed: the file was written through Bash, which the guard does not judge, and this row is what the next author gets instead of the surprise. The fix is one entry, `website/tools` beside the three directory prefixes, and the reason it was not made here is that the hook file is under concurrent edit by another session in this checkout and a widened guard owes fixtures on both sides (`plan/journal/hook-existing-patterns-false-positive.md`, 2026-08-18) |
| 2026-08-25 | - | `c_auto_py_format` (`.claude/hooks/posttool-writeedit.py` (retired; now `internal/le/hookruntime/postwrite.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->) against the legacy Python tree | The hook formats a population the configuration says it does not own. `c_auto_py_format` runs `ruff format -w` on ANY `.py` a Write or Edit touches, excluding only `/vendor/`, `/third_party/` and `/tmp/`. `pyproject.toml` sets `quote-style = "single"`, and the comment three lines above that setting states the scope plainly: "The legacy tree is not formatted by this program, and `le lint` never asks ruff to check that it is." So the two disagree about which files are formatted, and the hook wins because it runs on every edit. Editing one line of `test/scripts/ze_api.py` (retired, no successor) <!-- doc-links: ignore (deleted 2026-08-28 by eae282592 with no replacement) -->, a legacy-scope file written with double quotes, requotes the WHOLE file: measured at 397 lines on one such edit. The author then cannot tell their own change from the reformat, and a reviewer reading the diff cannot either. It is not confined to Python: the same hook reformatted `.claude/hooks/pretool-writeedit.py` (retired; now `internal/le/hookruntime/writeedit.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> under this session's edits, which is how an `old_string` that matched the file a moment earlier stopped matching | not fixed. The workaround both sessions that met it today used is to write the file through Bash, which the hook does not judge, and then prove equivalence by running `ruff format` separately. That is a workaround with a cost: it routes edits around every OTHER check the writeedit hooks perform. The fix is for the hook to read the same scope the configuration declares rather than carrying its own three-prefix exclusion list, and it owes fixtures on both sides -- a legacy file left alone, a formatted-scope file still formatted |
| 2026-08-26 | le-is-a-ze-binary | YANG glue and plugin-import generators over `internal/le/**/testdata/**` | Both native generators skipped `internal/test/` and still walked parser fixtures whose paths deliberately contain `internal/component/.../yang`. Repository generation wrote untracked `embed.go` and `register.go` into those fixtures and put their internal packages into the product composition root. The latter stopped every build with Go's internal-package boundary error; the former changed a migration fixture before its test copied it. | Both discovery predicates now prune every `testdata/` subtree. Regression fixtures use production-shaped paths, and the generated fixture artifacts were removed. |
| 2026-08-26 | le-is-a-ze-binary | interface-resolution gate over compiled functional fixtures | Porting fixture helpers from interpreted files to non-test Go moved them into the gate's scan. Three helpers query literal veth, decoy, and PPPoE devices that they create inside test namespaces. The gate classified ten such calls as operator logical-name bypasses even though the fixture processes load no Ze interface backend. | Added three exact-file exemptions with the literal-device reason. A sibling-name test prevents each file exemption from widening to a prefix. |
| 2026-08-27 | le-is-a-ze-binary | repository cross-package wiring gate, `declaredSymbols` (`internal/le/repository/wiring.go`) | The declaration scan applied line regular expressions to raw source bytes. Go snippets inside test fixture strings therefore became exported declarations, including 34 synthetic `TestA` functions in the test-sensitivity corpus. The gate reported 442 unwired symbols, many that did not exist in the syntax tree. | `declaredSymbols` now parses Go and reads only top-level `FuncDecl` and type `GenDecl` nodes. It skips testdata parser fixtures and has a regression case containing exported-looking raw strings plus malformed testdata. |
| 2026-08-28 | (lint sweep, no spec) | `writeGoPatterns` nolint pattern (`internal/le/hookruntime/writeedit.go`) | The pattern is `(?m)//nolint(?::&#124;\b)` and its message reads "nolint without a specific linter and reason", but the expression matches EVERY nolint, the compliant `//nolint:gosec // <reason>` included. A Write or Edit adding the form that `docs/contributing/ze-go-style.md` and the errcheck guidance both require is refused with exit 2. The population it judges is the bare nolint; the population it matches is all of them. | fixed in the main thread the same day. The pattern now matches the two BAD shapes positively, because RE2 has no negative lookahead: a bare nolint, and one that names its linters but states no reason. `TestNolintGuardAllowsTheFormTheStyleGuideRequires` drives six spellings through the hook entry point. Repairing it needed a second fix first: this file DECLARES the banned patterns, so it carries every one as a literal and the guard refused the edit that repairs the guard. `writeGoPatterns` now excludes its own source, which is the exclusion `sweepExcluded` (`internal/le/doc/check/links.go`) already makes for the same reason. General practice: a guard that matches text cannot judge the file that declares that text, and a rule whose message describes a narrower population than its pattern matches is refusing the very behaviour it exists to require. |
| 2026-08-29 | (structural-red repair, no spec) | pre-commit structural gate, `structuralGateReds` (`internal/le/commit/verification.go`) | A structural stage that declares no usable failure paths is UNATTRIBUTABLE, and an unattributable red is charged to EVERY commit in the checkout. All three reds standing here declared exactly that. `runGoAction` (`internal/le/doc/wiring/delegate.go`) passed `nil` related paths for all 11 of its delegated sub-checks, and the lint stage declared no group at all because `streamCommand` (`internal/le/verify/lint/verifylint.go`) sent the child's output straight to `os.Stdout` and kept none of it. All three stages KNOW which files they failed about: the doc report cites `path:line`, golangci-lint prints `path:line:col:`, and the Go compiler prints the same. The tracked-build stage declared nothing at all. So the gate refused commits that touched none of the offending files, and its population was every session sharing the checkout rather than the session that broke it | fixed in both. The delegated half added `failurePathLister`, which a report opts into per-report so a check judging the tree as a whole keeps answering nil, and `docVerifyPage.FailingPaths` scans the cited paths and keeps those that resolve. The lint and tracked-build halves share one scanner and one emitter (`internal/le/verify/failuregroup`): lint tees the child's output into a `pathCollector`, tracked-build reads each failing flavor's compiler output, and both declare a kind that `groupRelatedPaths` accepts. A SECOND defect surfaced in the first attempt and is folded in: the lint declaration was written straight to `os.Stderr`, so it reached the operator's terminal and never the stage log, and the engine read the stage as declaring nothing. `declaredGroups` reads what the action RETURNED (`internal/le/verify/dispatch/dispatch.go`, `dispatch`, which hands `leroot.Run` a capturing writer), so both declarations now sit in `Report.Text()`. Measured: `./le doc wiring` names 14 doc files, `./le verify lint run` names the files its findings cite, and `./le repository tracked-build check` names `internal/test/fixture/misc_fixture_runner.go` where all three named none. All three tests discriminate: reverting a wiring to `nil` reddens `TestRunGoActionPutsTheReportsPathsIntoTheGroup`, `TestALintRedNamesTheFilesItFailedAbout` and `TestAFailingRunNamesTheFilesTheCompilerNamed`. The five structural stages that still declare nothing are unchanged, and a red that names no file stays charged to everybody, which is the honest answer rather than a wrong attribution |
| 2026-08-30 | (documentation rule, no spec) | closure detection, `closureStem` (`internal/le/commit/review.go`) reading `journal.AddedSpecEvidence` | The gate read every journal row new against HEAD as a spec CLOSURE, and a commit stages a file whole. Five sessions share this checkout, so `plan/journal/stale-artifact-reused.md` held rows from seven specs none of which this session touched, and `./le commit create` refused with "commit names more than one spec closure". No keyword clears it: `Options` (`internal/le/commit/prepare.go`) carries no override for this branch. The refusal is self-sustaining, because those rows can land ONLY through a commit that names the file, and that commit is the one being refused. So a shared journal class becomes uncommittable for everybody as soon as two sessions write to it, and the work sits in the tree until a `git clean` takes it | fixed. A removed `plan/spec-<stem>.md` is the closure, and it is read first; a journal row is evidence, read only when the commit removes no spec and only when the rows agree on one stem. `TestAJournalCarryingSeveralSessionsRowsClosesNothing` and `TestARemovedSpecOutranksForeignJournalRows` (`internal/le/commit/relocation_test.go`) hold both halves, and `TestClosureStemUsesOnlyNewJournalEvidence` keeps the single-session evidence path green. One commit still closes one spec: two removals are refused as before |
| 2026-08-29 | generated-command-usage | `TestReloadCompleteMarkerDoesNotCollide` (`cmd/ze/hub/main_reload_marker_test.go`) | RED AT HEAD and unrelated to the work it was found under. The guard's population is a LOG LINE the daemon can emit, and its proxy is "a line under `internal/` or `cmd/` that holds a double quote and the marker text", excluding only `main_reload.go` and `_test.go`. Compiled functional fixtures are neither, so the guard reports the marker's two CONSUMERS as collisions: `strings.Contains(daemon.String(), "sighup reload complete")` in `internal/test/fixture/misc_fixture_shellports.go` and `bytes.Count(contents, []byte("sighup reload complete"))` in `internal/test/fixture/ui_fixture_api_user_removed_by_reload.go`. A fixture that WAITS for the marker is the marker working, not a second line an `await=stderr` fence could match. Same shape as the 2026-08-26 and 2026-08-27 rows above: porting interpreted fixtures to non-test Go moved them inside a gate's scan | not fixed, and it needs a decision rather than a patch. Excluding `internal/test/fixture` by path is the cheap answer and it would blind the guard to a fixture that genuinely logs. The property the guard wants is "this literal reaches stderr", and the two hits READ the literal rather than write it, so the discriminator is the call, not the directory |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | closure detection, `closureStem` (`internal/le/commit/review.go`) reading `journal.AddedSpecEvidence` | The SAME branch as the 2026-08-30 row above, in its second shape. That repair scoped a journal row to the case where the rows agree on ONE stem, which leaves a session's own row naming its own claimed spec reading as a closure in the MIDDLE of that spec. CLAUDE.md requires a row for every defect walked into, so the ordinary in-progress commit carries exactly one such row, and the gate then demands a clean review artifact the spec cannot yet produce. Commit 80c0133c1, which filtered a FOREIGN session's row out of the python original, named this shape as still open on 2026-08-22: "attribution cannot reach that one". Attribution cannot, and neither can Status, because there is no closed status in `docs/contributing/spec-workflow.md` and closure IS the removal, so commit A of a two-commit closure is byte-for-byte an ordinary in-progress commit. Two of this session's commits were blocked by it | fixed at the source with Thomas's ruling. A removed spec file is now the ONLY closure signal, and a journal row never contributes a stem. Journal paths are still read for their shape, so a malformed row still refuses the commit. What this gives up is bounded and stated in the function comment: commit A lands its code before the artifact is read, commit B cannot, so no spec closes without a clean independent review. `TestClosureStemReadsRemovalsAndNotJournalRows` and `TestClosureStemStillRefusesAMalformedJournalRow` (`internal/le/commit/commit_test.go`) hold both halves; the first was forced red by restoring the stem contribution |
| 2026-09-02 | - | doc-drift check, `./le doc wiring` (`internal/le/doc/wiring/`) | The check's unit is a SYMBOL, and its population is a page claim about that symbol. `runYANGConfig` (`cmd/ze/hub/main.go`) is 900 lines of startup orchestration, and seven pages anchor a claim on it. One line changed at its `zecli.RunAttached` call site. That line gave the attached console a config editor. The check reported drift on all seven claims. Two of the claims are about that console, and the pages took the edit. The other five are about local user merge, the authorizer bundle, the plugin server pointer, and signal handling. No page claim there became wrong, so satisfying the check would mean writing text nobody needs | not fixed. The symbol is the wrong unit for a function this large. The operator cannot narrow the anchor either: a page cannot cite a line range that survives an edit above it. Two repairs are open. The check can compare the claim's own text against the changed HUNKS rather than the enclosing function. Or `runYANGConfig` can be split, which the seven distinct subjects of those claims already argue for |
| 2026-09-03 | radius-admin-eap | `./le rfc check` discrimination ratchet: `discriminationOwedTags` (`internal/le/rfc/discriminate.go`) over the cover key `unitKeyAt` mints in the same file | The population is the tags the tip commit ADDED against `HEAD^`, and the key it diffs on is RID plus polarity plus `<file path>::<func>`. A pure package MOVE rewrites the path in every key while adding no tag and changing no claim, so every tagged unit in a moved package reads as added and is billed a discrimination proof. Measured on the move of `internal/component/ike/eap` to `internal/core/eap`: 116 tagged units carry no record (43 rfc3748, 34 rfc5216, 23 rfc2759, 10 rfc7296, 6 rfc9190), against 34 records whose `unit` field a mechanical re-path does repair. Predicted from the producer, not observed: `./le rfc check` refuses to run in this tree because another session's `internal/component/radius/authenticator_test.go:400` does not parse | NOT FIXED. The record format already carries `unit-sha` and `claim-sha`, so a rename-aware baseline can forgive a cover whose function name, body and claim are byte-identical to one at `HEAD^` under another path, and keep billing a unit whose body or claim moved. The move is committed with the red predicted and named in its message |
