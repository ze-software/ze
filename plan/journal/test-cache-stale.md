| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-03-21 | - | build | `go test` cache hid compile break in dependent package | ran `go clean -testcache` before verify |
| 2026-03-28 | - | build | same stale cache after modifying exported identifier | touched importing package to force recompile |
| 2026-08-22 | fixit-spec-status-metadata-window | Go tests that drive a producer through exec | `go test` keys a cached verdict on the files the TEST BINARY opened, and an exec'd compiler or interpreter is not the test binary. Two tests this spec adds are entry-point tests for exactly that reason, and both were invisible to their own producer. `specStatusBinary` (`scripts/status/spec_status_test.go`) runs `go build` over `scripts/status/spec_status.go`, which carries `//go:build ignore` and so sits outside the package's source hash; `runSpecClosure` (`scripts/dev/spec_closure_check_test.go`) execs `scripts/dev/spec-closure-check.py`. Measured with the cache warm on a repeated command line: deleting the `statusUnparsed` fail-closed branch from `loadSpec` left `./scripts/status/` answering `ok (cached)`, and reverting `_status` to its 12-line window left `./scripts/dev/` answering the same. The implementation phase read both as discriminating because each mutation happened to run under a different `-run` filter, which is a cache miss for a reason unrelated to the edit | fixed at both call sites: each helper reads its producer with `os.ReadFile` before running it, which puts the file in the testlog the cache keys on. `scripts/status/verify_run_test.go` already did this for its own gate, so the pattern existed and neither helper had reached for it. Eleven more exec sites in `scripts/dev` and `scripts/status` drive other producers for other specs and still carry it. The habit is the repair: a test whose subject is not Go source must READ that subject, and a red-and-green pair is only evidence when the two runs share a command line |
