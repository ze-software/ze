| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-26 | - | token-economy report, transcript store read | `iter_records` (`scripts/dev/token_economy.py`) wraps `path.open` in `except OSError: return`. Thus, a transcript that cannot be opened yields no records. Its API calls, context tokens, tool results, and entire session disappear from every report figure. No output reports that omission. The tool exits 0 and prints a smaller number, which looks like a pass. Thus, nobody can distinguish an unreadable store file from a store with less work. `ai/rules/context-economy.md` and `ai/skills/ze-implement.md` cite this report. It supplies their figures by phase and agent type. A swallowed file therefore changes the numbers that support a rule. The adjacent per-line skip is deliberate and documented. A truncated final line belongs to a session that is still active. The per-file skip is undocumented and indistinguishable from an empty file | fixed in the port only, in `ScanTranscript` and `FindSessions` (`tokeneconomy/tokeneconomy.go` in the former top-level tool tree). The open error is stored in `Scan.Unreadable`, and `FindSessions` returns that list. `Answer` (`run.go`) warns on stderr and names the path. `Report.Unreadable` also puts the path in the payload. The page still prints because a report that states its gap is more useful than a silent refusal. The script remains unchanged until the swap deletes it. `TestTokenEconomyScriptStillDropsAnUnreadableTranscript` (`scripts/dev/token_economy_parity_test.go`) verifies that the script still fails open. Thus, the test becomes red if somebody repairs the script. The correct action is then to delete the script and that test together |
| 2026-08-20 | - | session id resolution, source 3 | `_sid_from_jwt` (`.claude/hooks/lib/session_id.py`) calls `base64.b64decode`. The module never imported `base64`. The `NameError` lands in that function's own `except Exception: pass`. So source 3 returned `''` for every caller since it was written, and `session_id()` fell through to source 4, which MINTS a uuid. Nothing was red. A minted id is a usable id, and the four sources are ordered rather than checked. An unreachable source therefore reads as an absent one. The cost falls on a caller whose only true id is the JWT claim: it gets a fresh identity per CLI-ancestor | fixed here: `import base64`. The class point: a broad `except` around a lookup makes a code defect indistinguishable from a legitimate absence. The fall-through it guards can then never be seen to be wrong |
| 2026-03-21 | - | parser | unknown keyword fell through to default branch silently | listed all valid cases explicitly and rejected unknown |
| 2026-03-21 | - | parser | parseSAFI fell through to unicast on unknown input | added explicit error for unknown SAFI |
| 2026-08-12 | - | unchecked bufio.Scanner, repo-wide | `bufio.NewScanner`, loop, use the result, never read `scanner.Err()`. `Scan` returns false on EOF, on a read error, and on a line above `bufio.MaxScanTokenSize` alike, so code that reads "loop ended" as "input finished" reports a truncated read as a complete one. A SINGLE `Scan()` is worse: a read that fails part way through the line hands the buffered PREFIX back as a successful token, so the truncated value is used and no branch runs at all. First fixed 2026-08-12 in `7198df15f`, which SCOPED THE SWEEP TO A DIRECTORY (`internal/component/bgp/plugins/`) when the class was a PATTERN. A defect class is enumerated by its shape across the tree, never by the directory the first instance was found in. Measuring the shape needs care too: a per-FILE grep for `.Err()` hid two of the worst sites, `cmd_plugin.go` behind a `ctx.Err()` and `process.go` behind an unrelated one, so the population is 23 non-test files and not the 21 that grep reported | 15 decode entry points fixed in `7198df15f`. Second pass fixed 11 more files where a truncated read reached a caller as a whole answer: two chaos-replay verdicts (`logs identical`, replay pass/fail), three gates whose partial read reported clean (`scripts/lint/consistency.go`, `scripts/docvalid/doc_drift.go`, `scripts/inventory/inventory.go`), the plugin CLI hex stdin read, the `ze bgp plugin cli` prompts that substituted defaults for answers, the SSH password `ze connect add` stores, `crashlog` panic traces now marked TRUNCATED, `relayStderrFrom` (`internal/component/plugin/process/process.go`) whose relay ended for the life of the plugin so a later panic reached no log, and `config.DetectFormat`, which dropped `bufio.Scanner` for a string split because a 64 KiB config line made it answer FormatSet for a file carrying metadata. 12 files were LEFT and each carries a comment saying why: the read denies a login or a rescue console, the result is already rendered as "not parsed", or the reader is a `strings.Reader` over this process's own short output. Every fix has a test driving a reader that fails mid-line, each proven red against the unfixed code |
| 2026-08-12 | - | hub reload guard, functional | `test/reload/mgmt-guard-reload-refuses-nonloopback.ci` is RED at HEAD: the refusal it asserts, `refusing to migrate mcp to non-loopback listener "0.0.0.0:18088" without authentication`, is absent from stderr, and the daemon's last line is `MCP server listening on http://127.0.0.1:18088/`. Reproduced twice in a `git archive HEAD` tree with an unmodified `ze-test`, so it is neither a working-tree artifact nor caused by the reload work running beside it. Its sibling `mgmt-guard-reload-auth-rebuild.ci` passes. The producing function was not read and the guard's silence was not traced: the row above owns `checkReloadExposure` and this is the test that would have caught its skip branch | not fixed. It does not block the peer-settings reload work that found it, which touches no listener path. Whoever owns the row above should run this one test first: a guard whose own functional test is red is the shape that row exists to prevent |
| 2026-08-12 | fixit-mgmt-listener-auth-guard-deferred-reload-auth-rebuild | hub reload guard | `checkReloadExposure` (`cmd/ze/hub/listener_migrate.go`) SKIPS a service `runningAuth` cannot classify. That branch is permissive. Handles reach the migrator before `markMgmtAuth` ran, so web and mcp were unclassified in that window. An unauthenticated web server could then migrate to `0.0.0.0:3443`, and web carries no loopback rule of its own | `markMgmtAuth` now runs before any handle, and `resolveAuthIntents` drops surfaces with no handle. The test at reload time, not at mark time: gating the MARK was the fail-open. `TestMarkMgmtAuthClassifiesBeforeAnyHandleExists` drives the window, and the round-3 test that pinned the old shape was DELETED |
| 2026-08-12 | - | hub startup, `--plugin` with a hub config | `TestRunHubConfigRejectsCLIPlugins` (`cmd/ze/hub/main_test.go`) expects `Run` to return 1 and print the `--plugin` guidance for a hub config. It got a running daemon instead: `Run` blocked in `waitLoop` (`cmd/ze/hub/main.go:1423`) on `sig := <-sigCh` for 19 minutes and took the whole package past `go test -timeout 20m`, so `make ze-unit-pkg-test PKG=./cmd/ze/hub` cannot pass. The daemon logged `explicit plugin startup failed ... plugin bgp-rib: TLS connect-back: context canceled` and then `Ze running`. Which branch skipped the refusal is NOT established here. Not caused by the reload-shutdown work in the same session: the added `awaitReloadWorker` call runs after `waitLoop` returns, which this run never reached, and the stack shows `handleSIGHUPReload` idle at `for range reloadCh` | not fixed, and the branch IS now established (2026-08-14, reproduced identically at 19m11s): there is no branch. The guidance strings the test asserts, `--plugin` and `plugin { external ... }`, exist NOWHERE in the tree except the test itself, and `ProbeConfigType` (`internal/component/config/probe.go`) no longer answers a hub type at all -- its comment states every config now runs one daemon on one parser. The refusal was deleted with the hub runtime and its test was left pinning it, so `Run` correctly starts a daemon and the test correctly waits forever. Repair is a decision about the replacement contract (`--plugin` beside a config `plugin` block is now merged by `MergeCliPlugins`), so it needs a spec rather than a same-session edit |
| 2026-08-13 | - | ppp NCP, RFC 1661 Section 5.7 | Ze sends no LCP Protocol-Reject. When the pool handler declines IPv6 (`runIPv6CPPhase`, `internal/component/l2tp/ppp/ncp.go` logs `IPv6CP declined by handler, continuing IPv4-only`), incoming IPv6CP frames are dropped in silence. `LCPProtocolReject` appears in `lcp.go` and `session_run.go` only as a code Ze RECEIVES; nothing builds one. Observed against pppd 2.5.1: the client retransmitted IPV6CP ConfReq nine times and gave up on `IPV6CP: timeout sending Config-Requests`, about 30 seconds of every dual-stack CPE's session spent on a protocol Ze had already refused. IPv4 is unaffected, so the session comes up | not fixed. RFC 1661 Section 5.7 says a Protocol-Reject MUST be sent when a packet arrives for an unsupported protocol in the Opened state, so this is a conformance question for the owner rather than a repair to fold into the PPPoE interop work |
| 2026-08-14 | spec-web-templ-migration | web config view | two of the six template names `nodeKindToTemplate` (`internal/component/web/handler_config_leaf.go`) answers cannot render. `RenderConfigToHTML` (`internal/component/web/render.go`) returns empty HTML on any error, so both failures become a blank panel. `config.NodeLeaf` maps to `leaf.html`. That name is in neither the embedded FS nor the parsed config set, so the lookup misses. `config.NodeFlex` maps to `flex.html`. Its markup reads `.Value`, `.Name` and `.LeafField`. The `ConfigViewData` the caller passes carries none of them, so execution fails on the missing field. The phase-1 golden capture found both: it renders `flex.html` from a map holding the fields the markup reads | FIXED 2026-08-15 in the interface defect pass, with no new markup. `configViewComponent` (`internal/component/web/handler_config_leaf.go`) answers `configContainer` for `config.NodeLeaf` and `config.NodeFlex`. `buildConfigViewData` (`handler_config_walk.go`) already fills `LeafFields` for a leaf. It calls `populateContainerView` for a flex node. Both are what that component reads. The default branch answers it too, so a node kind added later cannot render a blank panel. `TestConfigViewComponentResolvesEachKind` covers all six kinds. Each row asserts a VALUE as well as the layout class. A component that renders an empty frame is the same blank panel with a wrapper on it |
| 2026-08-14 | spec-web-templ-migration | web dashboard health | `templates/component/dashboard_health.html` ranges `.Columns` for its header and then hard-codes `index .Cells 0`, `1` and `2` for the body. A row carrying fewer than three cells fails the index. `RenderFragment` (`internal/component/web/render.go`) discards the error. The whole health panel renders empty rather than one bad row. Its sibling `log_table.html` ranges `.Cells` and has neither problem. A fourth column also renders a header with no cell under it | FIXED 2026-08-15 in the interface defect pass. `dashboardHealth` (`component_dashboard_health.templ`) ranges `row.Cells`, giving index 0 the status badge and every later cell a plain `td`, which is the shape `logTable` already had. A short row now renders what it has instead of emptying the panel, and a fourth column gets its cell. `cellAt` (`view_config.go`) went with the indices. No fixture moved: every producer writes three cells. `TestDashboardHealthRendersEveryCellOfEveryRow` feeds a two-cell row and a four-cell row. COMPLETED 2026-08-15 in the review follow-up. Ranging `row.Cells` under a header that ranges `v.Columns` moved the mismatch rather than closing it. A row with more cells than columns drew a `td` past the last `th`, and a zero-cell row drew an empty `tr`. `healthRowCells` (`page_dashboard.go`) now resizes every row to the header, and a cut is logged at Warn because it is a producer defect. `TestDashboardHealthDrawsOneCellPerHeaderColumn` counts `td` against `th` over four row shapes. `TestCapturedTableRowsCoverTheirHeader` (`markup_contract_test.go`) holds the same invariant over every captured fixture, which covers the sibling `workbenchTable` |
| 2026-08-14 | spec-netlab-integration | config file load, `system authentication user` | `plaintext-password` is honoured only on the editor commit path. The hashing lives in `internal/component/cli/editor_commit.go` (`nLeafPrefix = "plaintext-"`, `dropnEntries`) and `editor_commands.go`. A config file loaded at boot by `zeconfig.LoadConfig` never reaches it. The leaf is carried, `password` stays empty, `CheckPassword` (`internal/component/authz/auth.go`) returns false for an empty hash, and every login for that user is refused. `ze config validate` reports the file valid with no warning. Measured 2026-08-14 on a clean config dir. With `plaintext-password netlab` the daemon logs `SSH auth failure username=netlab`, and `ze cli -c "show version"` exits 1. With a bcrypt hash in `password` the same command returns the version. The YANG description states the leaf is "hashed into this leaf on commit". The boot path is outside what that documents. Nothing says so where an operator writes a config file | FIXED 2026-08-14 (spec-netlab-integration phases 1-2): `LoadConfig` (`internal/component/config/loader.go`) now calls the SAME `ApplyPasswordHashing` the editor commit path calls, beside `refuseInvalidCustomSections`, and warns once naming the file because the load path never rewrites the operator's config. Proven by `test/plugin/config-plaintext-password-at-boot.ci` (red first with `SSH auth failure username=lab`) and by `TestLoadConfigHashesPlaintextPassword` / `TestReloadHashesPlaintextPassword`. A rendered netlab config no longer needs a precomputed hash |
| 2026-08-14 | spec-netlab-integration | `ze cli -c`, output format | an explicit format pipe on the `-c` path lost to the `--format` flag default. `(*cliClient).Execute` (`internal/component/cli/client/main.go`) ran the chain through `cmd.ProcessPipesChecked`, producing JSON, and then handed the result to `printFormatted(formatted, format)`, whose `default:` branch unmarshals JSON and re-renders YAML. `format` is the `--format` flag, whose default is `"yaml"`. So `ze cli -c` running `show bgp peer list` with a `json compact` pipe printed YAML and exited 0, and any consumer parsing it saw a parse error with no cause. netlab validation is one: `contrib/netlab/ze.yml` puts that exact string in `netlab_show_command`. The interactive path never had it, because it goes through `ProcessPipesDetectLog`, which already models the precedence with its `HasFormat` flag | FIXED 2026-08-14 (spec-netlab-integration phase 4): new `renderCommandOutput` (`internal/component/cli/client/main.go`) prints the chain output as-is when `command.HasFormatPipe` says the command named a format, and falls back to `printFormatted` when it did not. Red first: `test/plugin/netlab-lab-profile.ci` failed on `json.loads` of YAML. Unit cover: `TestRenderCommandOutputFormatPipeBeatsFormatFlag`, its no-pipe twin, and `TestHasFormatPipe` |
| 2026-08-14 | - | lg route search, non-HTMX POST | a validation error reaches no rendered byte. `renderSearchError` (`internal/component/lg/handler_ui.go`) sets `Error` and then calls `renderPage(w, "search", data)` for a request with no `HX-Request` header. `search.html` renders `route_results` inside `{{if .Routes}}`, and `route_results` holds the only `error_banner` call. `renderSearchError` never sets `Routes`, so that branch is dead and the banner never runs. The operator receives HTTP 200 and an empty search form. The phase-1 handler capture is the proof: `internal/component/lg/testdata/handler/ui-search-invalid.txt` is a 200 whose body carries neither `error-banner` nor `invalid prefix`. The HTMX path is correct, because it renders `route_results` directly | FIXED 2026-08-14 in the templ port. `searchPage` (`internal/component/lg/search.templ`) renders the results panel when there are routes OR an error, and `renderSearch` (`handler_ui.go`) is the one path both call sites now take. Three handler fixtures moved and each now carries its banner. They are `ui-search-invalid`, `ui-search-empty` and `ui-search-result`. The last one sent `family=ipv4`, so it was a third instance of the same defect |
| 2026-08-15 | rfc4271-med-across-as | bgp policy filter text | `appendSingleAttr` (`internal/component/bgp/reactor/filter_format.go`) switches on `*attribute.MED` while `knownAttrParsers` (`internal/core/bgp/attribute/wire.go`) builds the value form `attribute.MED` (`simple.go`, `type MED uint32` with value receivers). The case can never match, so MULTI_EXIT_DISC is the one attribute the update text handed to every filter plugin does not name. Two consequences: a filter cannot see the metric it is asked to reason about, and `extractUint32Attr(updateText, "med")` (`internal/component/bgp/plugins/filter_modify/modify.go`) reads 0, so `increment med` and `decrement med` compute from zero rather than from the route's value. Every unit test around both writes the text by hand with `med N` in it, which is why the suite is green. Found by GoBGP: a gate that read the metric's presence from the text refused every configured removal, and `test/interop/scenarios/bgp-med-remove-configured-gobgp` was the only thing that could see it | not fixed here. The gate moved to the wire (`medPresentOnWire`, `internal/component/bgp/reactor/filter_delta.go`) and the spec closed. The renderer fix is one line and its blast radius is not: `med N` would appear in the text every filter plugin receives, which changes what `increment med` computes and what any text-comparing filter sees, so it wants its own spec |
| 2026-08-15 | fixit-ci-peer-block-silent-directives | functional test runner, `.ci` ze-peer stdin blocks | A line inside a `stdin=<peer>` block reached NEITHER parser. The block loop in `record_parse.go` forwarded only `expect=` and `action=`. It dropped the rest in silence. `consumes` (`internal/test/peer/expect.go`) answered false for `reject`. Each defect hid the other. `reject=bgp:` would have been a hard `unknown reject type` error anywhere else, and every site that carried one carried it inside a block. Fifteen directives across twelve files asserted nothing while they read as the negative half of a proof. One level down, `parseOptionConfig` failed soft the same way. `option=update:value=inspect-update-message` parsed into nothing, and a runner TEST pinned that as correct | `validatePeerBlockDirectives` (`internal/test/runner/peer_contract.go`) fails the file at parse time for any line neither parser reads. It DERIVES the accepted set. `peer.ClaimLine` reports ze-peer's own verdict and the runner's `parseLine` answers for the rest, so no third list exists to drift. `reject=bgp:conn=N:pattern=<hex>` became a real directive (`internal/test/peer/reject.go`), never consumed and re-checked on every frame. `parseOptionConfig` now errors on a value its option has no branch for. The lesson that generalises: a guard over a directive vocabulary must ASK the parsers, never enumerate them |
| 2026-08-15 | fixit-ci-peer-block-silent-directives | functional test runner, orphaned `stdin=peer` blocks | The line-level drop this spec closed has a BLOCK-level twin that is still open. A `.ci` that carries `cmd=` lines and a `stdin=peer` block no `cmd=...:exec=ze-peer` names starts no peer at all. Nothing reads that block. `writeExpectFile` (`internal/test/runner/runner_output.go`) builds ze-peer's input from `Record.Options` and `Record.Expects`, and the only reader of a stdin block is the `cmd=` loop in `runner_exec.go`. Seven committed files are in that state. `test/plugin/redistribute-import-reject.ci` is one, and its only `cmd=` is `seq=2`: the ze-peer at `seq=1` was deleted and its block left behind. So `option=update:value=send-route` announces nothing and `expect=bgp` is checked by nobody. The guard added by this spec ACCEPTED those files: it validated a block against ze-peer's parser without first asking whether any ze-peer reads it. Round 3 made `blockPeerMode` ask, so a `reject=` in an unread block is now refused. The other peer directives in one are not | not fixed. `validatePeerBlockDirectives` (`internal/test/runner/peer_contract.go`) carries the hole in its doc comment with the file named. Refusing an unread peer block is a two-line change to `blockPeerMode`. The cost is the seven files. Each then needs its peer restored and its assertions made real, which is test authoring and belongs with `spec-fixit-vacuous-functional-tests` rather than in a closing commit. Found by review round 2. FIXED 2026-08-16 for the seven files. Each now carries `cmd=background:seq=1:exec=ze-peer --port $PORT:stdin=peer`, so the block is read, and each assertion was made real rather than kept: the peer needs `family { ipv4/unicast }` on its session before ze emits the End-of-RIB the block asserts, and the observer needs a `read_line` pump before it polls, because ze asks for the filter verdict on the callback connection the poll parks. The two export files could not exercise an export filter at all with one peer, so each gained a second ze-peer on 127.0.0.2, the `bgp-rs` and `bgp-adj-rib-in` plugins that make ze-build forward between peers, and the export filter moved to the peer ze forwards TO. Every one was shown red by breaking the behaviour it names. The runner-side refusal of an unread peer block is still open |
| 2026-08-16 | fixit-ci-peer-block-silent-directives | bgp OPEN capabilities, a peer with no `family` block | A `bgp { peer X { session { ... } } }` that declares no `family` block negotiates no address family and ze then emits no End-of-RIB for that session, while the same session carries IPv4 unicast routes in both directions. `buildOpen` (`internal/component/bgp/reactor/session_negotiate.go`) uses the configured Multiprotocol capabilities, and falls back to `s.pluginFamiliesGetter` when the config declares none. In a daemon whose only plugin is an external observer that fallback produced nothing, so ze's OPEN carried the 4-octet-AS capability alone (measured: `04FDE8005A0102030408020641040000FDE8`), `NegotiatedCapabilities.Families` returned empty, and the family loop in `sendInitialRoutes` (`internal/component/bgp/reactor/peer_initial_sync.go`) had nothing to iterate. Nothing in the log or in `ze doctor` says a peer will never mark its end of RIB. Found while repairing the seven redistribution fixtures, whose peer blocks assert that marker | not fixed, and not established as a defect: it may be correct that a session with no negotiated family emits no marker, since RFC 4724 ties the marker to a family. What is established is that the config surface gives no signal either way. The fixtures were fixed by declaring `family { ipv4/unicast { prefix { maximum 10000; } } }` on each peer, which is what the reference fixture `test/plugin/add-remove.ci` already did. Whoever picks this up should decide whether a peer with no family block is a configuration ze should refuse, default, or warn about |
| 2026-08-18 | fixit-flowspec-protocol-name-drift | flowspec-firewall bridge, NLRI JSON parsing | the bridge read a shape nothing produces, and dropped in silence everything it could not read. `appendFlowSpecJSON` (`internal/component/bgp/plugins/nlri/flowspec/json.go`) writes `"destination":[["10.1.0.0/24/0"]]` and `"protocol":[["=tcp"]]`; `firstPrefix` called `netip.ParsePrefix` on the offset-suffixed text and `firstNumericVals` called `parseEqualityValue` on the name, and each SKIPPED the value on failure rather than refusing. Measured by feeding `FlowSpec.AppendJSON` output straight to `parseNLRIJSON`: of four components a peer announced, one survived. A peer's `drop tcp to 10.1.0.0/24 port 80` therefore installed an unconditional drop of port 80 to EVERY address on EVERY protocol -- a dropped match makes the rule WIDER, so the silent skip is worse than a refusal in the one direction that matters. Every unit test was green because each hand-wrote the JSON in the shape the parser wanted | FIXED in this spec. Every helper in `translate.go` now refuses what it cannot read: `firstPrefix` accepts the offset suffix and rejects a non-zero offset, `protocolValues` resolves a name through `firewall.ProtocolNumber` or reads digits, and `parseDigits` errors instead of returning -1. `TestParseNLRIJSONReadsWhatTheDaemonWrites` drives the REAL producer into the parser, which is what no earlier test did. RESIDUAL, not fixed: `formatBitmaskMatches` writes tcp-flags as names (`syn`, `syn,ack`) and ze has no bitmask-name parser, so a FlowSpec tcp-flags rule is now REFUSED and logged instead of enforced without its flag condition. That is the correct fail-closed behaviour and it is also a capability ze does not have; enforcing it needs its own spec |
| 2026-08-19 | - | `ze bgp decode --json`, community attributes | `parsePathAttributesZe` (`internal/component/bgp/cli/decode_update.go`) switches on the attribute code and carries cases for 1, 2, 3, 4, 5, 6, 7, 9, 10, 14, 15, 16 and 29 only. Codes 8 (COMMUNITIES, RFC 1997), 25 (IPV6_EXTENDED_COMMUNITIES, RFC 5701) and 32 (LARGE_COMMUNITIES, RFC 8092) reach no case and no default, so the switch falls through and the attribute leaves no trace: the operator gets a decode whose `attr` object does not mention them, rather than a hex form or an error. Measured against a 97-octet UPDATE carrying one of each: the output named `origin` and `extended-community` and nothing else. This is a SEPARATE producer from the receive-path renderer, which reaches `appendAttributeJSON` (`internal/component/bgp/format/text_json.go`) and whose community formatters were fixed the same day | FIXED 2026-08-19. The switch now has a `default` arm, so the unit repaired is the CLASS and not the three codes: `renderAttributeZe` (`decode_update.go`) answers with a key, a value and whether it UNDERSTOOD the octets, and a code it did not understand is filed under `attr-<code>` with its octets as hex, the spelling `appendAttributeJSON` already used. The third outcome is what the first attempt lacked and what eleven decode goldens caught: an arm can understand the octets and still have nothing to show, which is how a locally-originated route keeps its empty AS_PATH out of `attr` (RFC 4271 Section 5.1.2). The four community renderers share the daemon's per-community renderers (`Community.String`, `ExtendedCommunity.AppendDecoded`, `LargeCommunity.String`, hex for RFC 5701), and a length that is not a whole number of communities now takes the raw form rather than a truncated list. `test/decode/community-attributes.ci` was shown red at HEAD as the suite's only failure. The vocabulary question is ANSWERED but NOT reconciled: this surface keeps the singular `community` / `extended-community` that `ai/rules/cli.md` states for it, and the daemon's event stream keeps its plural. Moving the CLI to the plural must edit two RFC-tagged tests (`TestRFC8955TrafficRateNegativeDecodesAsZero`, `TestRFC8955TrafficActionUnusedBitsIgnoredOnDecode`, both calling `parseExtendedCommunities`) and four `test/decode/bgp-flow-*.ci` goldens, so it needs the owner |
| 2026-08-19 | - | extended-community naming, 4-byte ASN | `AppendDecoded` (`internal/core/bgp/attribute/extcomm_decoded.go`) switches on the full 16-bit type and sub-type, and names only the two-octet-AS forms. RFC 4360 Sections 3.2 and 3.3 define the four-octet-AS and IPv4-address Route Targets (`0x0102`, `0x0202`), and RFC 8955 Section 7.4 defines the matching redirect siblings (`0x8108`, `0x8208`). None is named, so each falls to the generic `0x<type><subtype>:<hex>` arm. Not a regression: the decoder this replaced named exactly the same set. It matters more now because one renderer serves two surfaces, the `ze bgp decode` CLI and the daemon's event JSON, so an operator running four-byte ASNs reads hex on both, and the FlowSpec firewall bridge matches none of it. Found by the independent review of the renderer's own commit | not fixed. It does not block the community-rendering repair that introduced the shared renderer, and the four arms are mechanical. The reason to take it deliberately rather than here is that `parseExtendedCommunities` (`internal/plugins/flowspec-firewall/translate.go`) would then start seeing `redirect:` spellings it does not match, so the naming and the bridge's vocabulary want deciding together. Separately, `appendAttributeText` (`internal/component/bgp/format/text_human.go`) still carries pointer arms for Origin, MED and LocalPref beside their value arms, which no producer reaches: the sole production caller is `appendAttributesText`, fed from `result.Attributes` (`internal/component/bgp/filter/filter.go`), which holds wire-parsed values. They were left in place on purpose, because a redundant arm cannot drop an attribute and removing one can, and `TestCommunityJSONFormattersRejectPointer` pins the opposite policy for communities, so the two want one decision |
| 2026-08-19 | - | `ze bgp decode` human output, the default mode | `formatAttributesHuman` (`internal/component/bgp/cli/decode_human.go`) writes a fixed list of seven attributes, so aggregator, originator-id, cluster-list, atomic-aggregate, bgp-ls and any unnamed code have never appeared in the DEFAULT output an operator gets without `--json`. Two of the seven were dead as well: the community and extended-community arms each asserted `[]any` while their producer returned `[]string` and `[]map[string]any`, so neither ever matched and both communities were dropped with the type assertion, not with a missing case | the two community arms are FIXED, and the other two community codes were added beside them, so all four now print. The missing five and the unnamed-code line are NOT fixed: a generic tail over the remaining keys is about ten lines, and what it should print for the bgp-ls map is a formatting decision rather than a repair |
| 2026-08-19 | cli-order-pipe | cli pipe operator classification | `foldFilters` (`internal/component/command/pipe.go`) sorts a parsed pipe chain in two. One part folds into server-side arguments, the other stays for the client to run. It has NO `default:` arm. A `pipeKind` named in neither case reaches NEITHER side: the operator is dropped, no error is produced, and the command answers as though nobody typed it. The drop is invisible until the operator is typed on a command that REGISTERS pipe filters, because that is the only path through this function. An operator added and tested against `show bgp peer list` alone therefore looks finished and does nothing on `show bgp rib`. Adding a pipe kind is a two-place change whose second place fails silently rather than at compile time | not fixed in the switch, and the shape is deliberate. An added `default:` is refused by `c_silent_ignore` (`.claude/hooks/pretool-writeedit.py`), and it would have to name a behaviour for a kind nobody has written yet. Covered by a test instead of a comment. `TestColumnOpsSurviveFoldFiltersOnFilteredCommand` drives both new kinds through the function. `test/ui/display-fill-filtered-command.ci` drives the real daemon against `show bgp rib`, which registers eleven filters. The next pipe kind owes itself the same pair |
| 2026-08-21 | - | `handleBgpOverview` (`internal/component/bgp/plugins/cmd/peer/summary.go`) argument handling | the handler reads `args[0]` and nothing else. A token that names a family is accepted and every token after it is dropped in silence, so `show bgp ipv4 rubbish` answers the ipv4-scoped summary and never tells the operator that `rubbish` reached nobody. The dispatcher delivers both tokens: `matchCommandTokens` (`internal/component/plugin/server/command.go`) consumes only the key's own tokens and `longerCommandPath` refuses nothing, because neither `show bgp ipv4` nor `show bgp ipv4 rubbish` is a registered path. `handleBgpSummary` behind it has the same shape, so the old `show bgp summary ipv4 rubbish` behaved identically and this predates the removal. Found reading the Boundary Tests row of spec-cli-show-bgp-is-the-command, which bounds the tokens at 0 or 1 and says nothing about a second token following a VALID family | not fixed. Refusing a second token changes what a shipped command accepts, and the spec that owns this handler bounds only the case where the FIRST token names no family. The class is the general one: a handler that indexes `args[0]` owes an answer for `args[1:]` |
| 2026-08-26 | le-is-a-ze-binary | test-health KPI history, `do_record` (`scripts/dev/testing_health.py`) | The recorder runs `git rev-parse --short HEAD` with `check=False` and ignores its return code. It uses the literal `"unknown"` when the output is empty. The sample is then appended to committed, append-only `test/health/history.ndjson`. Thus, a point that nobody can attribute to a commit becomes permanent. The only trace of the git failure is `unknown` in a field that readers treat as a SHA | fixed in the port only. `Record` (`testhealth/modes.go` in the former top-level tool tree) refuses and appends nothing. `TestScriptTestHealthStillRecordsAnUnknownShaWhenGitCannotName` (`scripts/dev/testing_health_parity_test.go`) drives both implementations through a `git` shim. The shim fails only for `rev-parse --short`. Thus, the test exercises the fall-through instead of deriving it from source. Delete the case with the script |
