| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-03-21 | - | parser | unknown keyword fell through to default branch silently | listed all valid cases explicitly and rejected unknown |
| 2026-03-21 | - | parser | parseSAFI fell through to unicast on unknown input | added explicit error for unknown SAFI |
| 2026-08-12 | - | unchecked bufio.Scanner, repo-wide | `bufio.NewScanner`, loop, use the result, never read `scanner.Err()`. `Scan` returns false on EOF, on a read error, and on a line above `bufio.MaxScanTokenSize` alike, so code that reads "loop ended" as "input finished" reports a truncated read as a complete one. A SINGLE `Scan()` is worse: a read that fails part way through the line hands the buffered PREFIX back as a successful token, so the truncated value is used and no branch runs at all. First fixed 2026-08-12 in `7198df15f`, which SCOPED THE SWEEP TO A DIRECTORY (`internal/component/bgp/plugins/`) when the class was a PATTERN. A defect class is enumerated by its shape across the tree, never by the directory the first instance was found in. Measuring the shape needs care too: a per-FILE grep for `.Err()` hid two of the worst sites, `cmd_plugin.go` behind a `ctx.Err()` and `process.go` behind an unrelated one, so the population is 23 non-test files and not the 21 that grep reported | 15 decode entry points fixed in `7198df15f`. Second pass fixed 11 more files where a truncated read reached a caller as a whole answer: two chaos-replay verdicts (`logs identical`, replay pass/fail), three gates whose partial read reported clean (`scripts/lint/consistency.go`, `scripts/docvalid/doc_drift.go`, `scripts/inventory/inventory.go`), the plugin CLI hex stdin read, the `ze bgp plugin cli` prompts that substituted defaults for answers, the SSH password `ze connect add` stores, `crashlog` panic traces now marked TRUNCATED, `relayStderrFrom` (`internal/component/plugin/process/process.go`) whose relay ended for the life of the plugin so a later panic reached no log, and `config.DetectFormat`, which dropped `bufio.Scanner` for a string split because a 64 KiB config line made it answer FormatSet for a file carrying metadata. 12 files were LEFT and each carries a comment saying why: the read denies a login or a rescue console, the result is already rendered as "not parsed", or the reader is a `strings.Reader` over this process's own short output. Every fix has a test driving a reader that fails mid-line, each proven red against the unfixed code |
| 2026-08-12 | fixit-mgmt-listener-auth-guard-deferred-reload-auth-rebuild | hub reload guard | `checkReloadExposure` (`cmd/ze/hub/listener_migrate.go`) SKIPS a service `runningAuth` cannot classify. That branch is permissive. Handles reach the migrator before `markMgmtAuth` ran, so web and mcp were unclassified in that window. An unauthenticated web server could then migrate to `0.0.0.0:3443`, and web carries no loopback rule of its own | `markMgmtAuth` now runs before any handle, and `resolveAuthIntents` drops surfaces with no handle. The test at reload time, not at mark time: gating the MARK was the fail-open. `TestMarkMgmtAuthClassifiesBeforeAnyHandleExists` drives the window, and the round-3 test that pinned the old shape was DELETED |
| 2026-08-12 | - | hub startup, `--plugin` with a hub config | `TestRunHubConfigRejectsCLIPlugins` (`cmd/ze/hub/main_test.go`) expects `Run` to return 1 and print the `--plugin` guidance for a hub config. It got a running daemon instead: `Run` blocked in `waitLoop` (`cmd/ze/hub/main.go:1423`) on `sig := <-sigCh` for 19 minutes and took the whole package past `go test -timeout 20m`, so `make ze-test-pkg PKG=./cmd/ze/hub` cannot pass. The daemon logged `explicit plugin startup failed ... plugin bgp-rib: TLS connect-back: context canceled` and then `Ze running`. Which branch skipped the refusal is NOT established here. Not caused by the reload-shutdown work in the same session: the added `awaitReloadWorker` call runs after `waitLoop` returns, which this run never reached, and the stack shows `handleSIGHUPReload` idle at `for range reloadCh` | not fixed. The shared checkout stopped compiling mid-investigation (another session's in-flight `filter_community` edit), so the test could not be re-run at HEAD to separate a pre-existing hang from that tree state |
