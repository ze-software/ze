| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-11 | bgp-update-delay | `internal/component/bgp/plugins/rib` test binary, `bestprev_shard_test.go` calling `peerRIB.Insert` | The package does not BUILD at HEAD: `too many arguments in call to peerRIB.Insert; have (family.Family, []byte, []byte, bool); want (family.Family, []byte, []byte)`. Observed against pure HEAD, with every working-tree change reverted through a `go vet -overlay` whose Replace map rewrote all 84 dirty Go files back to their HEAD content, so no session's uncommitted work is in the picture. A commit landed the four-argument call site without the three-argument declaration it needs, or landed the declaration's revert without the call site's. Every session that runs a unit-test action over `./internal/component/bgp/...` reads it as a red it did not cause, and the wave-of-red row in `docs/contributing/running-commands.md` sends them to look at the disk first | not fixed here. It is another spec's build and this session carries no `rib` file, so fixing it would fold an unrelated repair into a closure commit (`ai/rules/rule-precedence.md`). The transferable point is the diagnostic: a red that survives reverting every dirty file to HEAD is a red HEAD owns, and a `go vet -overlay` built from `git show HEAD:<path>` tells the two apart without a worktree and without a stash |
| 2026-09-11 | bgp-update-delay | `internal/component/bgp/reactor/reactor_notify.go`, the inbound End-of-RIB block calling `peer.updateDelayEndOfRIB`, whose declaration was never committed | The package does not BUILD at HEAD. `git grep updateDelayEndOfRIB HEAD` returns the call site and no file in HEAD declares the method. Commit `3dbcd23716` carried the call into history under the git-safety LANDING presumption while the file declaring the method stayed untracked in a session's working tree, so the reactor package has not compiled from a clean checkout since. The presumption itself is sound and was applied correctly; what was skipped is the test it is paired with, that a carried hunk MUST NOT name a symbol HEAD does not hold. A hunk reads as additive and safe right up until you check the referent, which is why the rule says to judge against HEAD rather than against the working tree, where the declaration was sitting one file away | not fixed in a commit of this session's own, because the repair is a deletion in a file another session is actively editing, and dropping it into a scoped commit would fold an unrelated fix into it. The deletion IS in the working tree now, so whoever next commits `reactor_notify.go` lands the repair; the session editing that package was told so directly. The transferable point is cheap and mechanical: before carrying a foreign hunk, grep the symbols it names against HEAD, not against the tree in front of you |
| 2026-09-11 | subscriber-reader-loops-retry-a-failing-socket-without-backoff | `strictPeersWithoutEngine` (`internal/component/bgp/config/bfd_strict_doctor.go`), reading `ps.BFD.Strict` | `./le repository tracked-build check` fails all six flavors: `ps.BFD.Strict undefined (type *reactor.BFDSettings has no field or method Strict)`. The consumer landed in `a8f7323f64`; the field is declared on `BFDSettings` (`internal/component/bgp/reactor/peer_settings.go`) in a working tree and in no commit. The third row of this class in one day | not fixed here: this closure carries no bgp file and the repair is one other session's uncommitted declaration, which lands when they commit `peer_settings.go` |
| 2026-09-11 | derived-indexes-answer-a-query | `checkDependencyDrift` reading `go.mod` against `website/data/dependencies.json` (`internal/le/site/dependencies.go`) | `./le site build` refuses with "go.mod requires github.com/prometheus/common directly with no entry in data/dependencies.json: add one", and `TestThisCheckoutHasNoDependencyDrift` is red for the same reason. Both files are COMMITTED and clean, so the refusal reproduces at HEAD: `git show HEAD:go.mod` carries the module in its direct block since `2ff00193e7`, `git grep -l prometheus/common HEAD -- '*.go'` names only vendored files, and the manifest has rows for `client_golang` and `procfs` and none for `common`. No site artifact can be built in this checkout, so any spec whose evidence is a site build is blocked | not fixed here: the repair is a decision between writing a reason for a module no first-party file at HEAD imports and dropping it from the direct block with `go mod tidy`, and a session is concurrently adding the first importer (`internal/le/interoplab/ipsec/helpers.go`, uncommitted). Either arm touches `go.mod` or another session's file, which a scoped commit must not fold in |
| 2026-09-12 | derived-indexes-answer-a-query (walked into by `./le commit debt-clear`) | `peerRIB.Insert` (`internal/component/bgp/plugins/rib`), production against its own tests at HEAD | HEAD's `rib` test files call `Insert(family, []byte, []byte, bool)` where the production method takes three arguments, so the package does not compile and `go vet` over a `git archive HEAD` extraction fails. Every verify run therefore dies at its first lint stage, which means NO session in this checkout can clear a verification-debt row: `debt-clear` re-runs the owed gates against HEAD and clears only on exit zero. Found trying to clear 20 rows this session owed. | fixed on owner instruction, by landing the repair that was already sitting in the working tree. `4e790af234` dropped the `asn4` argument from `PeerRIB.Insert`, `FamilyRIB.Insert` and `ParseAttributes`, and updated the `storage` package's own tests, but not the 15 test files of the `rib` package ABOVE it, nor `internal/plugins/ospf/rfc5882_shared_key_test.go`, which passed `nil` to a struct parameter. Both were dead at HEAD from that commit. The transferable point is the population: a signature change is finished when every package that CALLS it compiles, and the caller set is never the directory the declaration lives in. `go vet` over a `git archive HEAD` extraction now exits zero |
