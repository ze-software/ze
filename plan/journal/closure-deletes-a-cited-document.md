| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-11 | runtime-kernel-carries-every-tunnel-kind | functional test comments | 25 `.ci` files cite `ai/rules/qemu-testing.md` to justify their `option=needs-linux:caps=net-admin` line. The file does not exist. `git log --diff-filter=D` names ad809ea43 as the commit that deleted it, the rules merge from 102 files into 26. The rule that now carries the directive is `ai/rules/platform-linux.md`, and no citer was repaired. The same shape as the row below, one artifact class over: the merge repaired the rules and left the documents that pointed into them | not fixed. It is 25 files of test comments, none of which this spec touches, and the fix is one substitution the whole set wants at once rather than whichever file somebody opens next <!-- doc-links: ignore (the rules merge ad809ea43 deleted this file, which is what the row records; the live rule is ai/rules/platform-linux.md) --> |
| 2026-08-10 | fixit-spec-closure-leaves-dangling-spec-citations | workflow | Spec closure removes the spec file. Every sibling citation of it then points at nothing. The gate that finds them existed and was correct. No closure step fed it, so it drifted to 15 red rows | a BLOCKING step before commit B. The author who removes the spec names and repairs its citers, inside commit A |
| 2026-08-12 | - | Go source header | Found from `spec-ipsec-rfc9190` phase 1, which this row does not close. Second recurrence. `internal/component/lg/auth.go` carries a `// Design:` header naming `plan/spec-fixit-mgmt-listener-auth-guard.md`, which a spec closure has since removed. `make ze-doc-wiring-check` is red at HEAD over it, for a session that touched neither file | not fixed here. `internal/component/lg/` is outside this spec's package, and editing it would pull an unrelated package into this commit's lint and review scope <!-- doc-links: ignore (the row names a document that a closure deleted, which is the class this journal file counts) --> |
| 2026-08-11 | rfc-ledger-per-rfc-shards | workflow | Recurrence after the fix above. HEAD commit 8b85ada76 closed `spec-fixit-egress-filter-non-decision-channel`. It left a citation of that spec in `plan/deferrals/fixit-stored-route-relay-hardening.md`. `make ze-doc-links-check` is red at HEAD for a session that caused none of it | not fixed here. The repair belongs to the author of that closure |
| 2026-08-13 | - | spec cross-reference | Found from phase 4 of `spec-bcp194-6-blackhole`, which this row does not close. Fourth recurrence, same class, third artifact kind. Commit 965e798c1 removed `plan/spec-fixit-mgmt-listener-auth-guard-deferred-reload-auth-rebuild.md` on closure. `plan/spec-hub-deferred-api-auth-independent-of-ssh-block.md` still cites it at lines 152, 280 and 365. `make ze-doc-wiring-check` is therefore red at HEAD for a session that touched neither file. Four rows now span three artifact kinds: `.ci` comments, a Go `// Design:` header, and a spec body. That says the closure step added on 2026-08-10 checks spec citations only, and not every citer class | not fixed here. `plan/spec-hub-...` is another session's in-flight spec, and its three citations are its author's to repoint <!-- doc-links: ignore (the row names a document that a closure deleted, which is the class this journal file counts) --> |
| 2026-08-14 | fixit-peer-process-event-filter | Go source header | Fifth recurrence, second time in the Go header kind. `internal/component/bgp/reactor/forward_path_id.go:1` carries a `// Design:` header naming `docs/architecture/bgp/forward-rails.md`, which does not exist: commit 8c5bcc191 added the citation and no such document was ever committed. `make ze-doc-wiring-check` is red at HEAD over it, for a session that touched neither file | not fixed here. The reactor package is outside this phase's rename, and editing it would pull an unrelated package into this commit's lint and review scope <!-- doc-links: ignore (the row names a document that a closure deleted, which is the class this journal file counts) --> |
| 2026-08-15 | spec-web-templ-migration | web package test build | Sixth recurrence, and the first that stops a package from compiling. Commit aa43dd4cc removed `internal/component/web/templates` and kept the three files that read that tree. `fidelityTemplatesFS` (`internal/component/web/templ_fidelity_test.go`) carries `//go:embed templates`, so the TEST build of `internal/component/web` fails with `pattern templates: no matching files found`. `make ze-web-golden-check`, `make ze-unit-pkg-test PKG=./internal/component/web` and `go vet ./internal/component/web/` are red at HEAD for every session. The comment above that variable states that the tree and the file are deleted together. The production build is clean, so `bin/ze` and every `.wb` and `.ci` suite still run. The earlier rows in this file cost a doc gate. This one costs a package | not fixed here. The repair is to delete `templ_fidelity_test.go`, `templ_fidelity_cases_test.go` and `templ_fidelity_handler_test.go`. `.claude/hooks/pretool-bash.py` refuses `rm` over a `_test.go` path without user approval, which a subagent cannot give. `internal/test/golden/portcheck.go` and `make ze-templ-port-check` already carry the comparison those three files made <!-- doc-links: ignore (the row records a deletion, so the paths it names are what the tree no longer holds) --> |
| 2026-08-15 | - | web test build, and the gate over tracked content | Seventh recurrence, and the repair of the row above. Commit aa43dd4cc deleted `internal/component/web/templates` and kept three readers of that tree. No gate saw the break before the commit landed. `make ze-repository-tracked-build-check` is the only check that compiles what git holds, and it runs `go build`. `go build` compiles no `_test.go` file. So a deletion that only a test file reads reaches HEAD green. `go vet ./internal/component/web/` then reported `pattern templates: no matching files found` for every session in this checkout | fixed. The 15 fixture builders that `golden_test.go` consumes moved to `internal/component/web/golden_fixtures_test.go`. Four map-shaped builders fed the deleted comparison alone, so they were dropped rather than moved. The three fidelity files hold a `package web` line and a `test-relax` note. They are ready for removal through a commit script `--remove` flag. The gate blind spot itself is not fixed. `ai/rules/git-safety.md` names it, and no check compiles the tracked test population <!-- doc-links: ignore (the row records a deletion, so the paths it names are what the tree no longer holds) --> |
