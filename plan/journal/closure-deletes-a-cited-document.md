| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-11 | runtime-kernel-carries-every-tunnel-kind | functional test comments | 25 `.ci` files cite `ai/rules/qemu-testing.md` to justify their `option=needs-linux:caps=net-admin` line. The file does not exist. `git log --diff-filter=D` names ad809ea43 as the commit that deleted it, the rules merge from 102 files into 26. The rule that now carries the directive is `ai/rules/platform-linux.md`, and no citer was repaired. The same shape as the row below, one artifact class over: the merge repaired the rules and left the documents that pointed into them | not fixed. It is 25 files of test comments, none of which this spec touches, and the fix is one substitution the whole set wants at once rather than whichever file somebody opens next |
| 2026-08-10 | fixit-spec-closure-leaves-dangling-spec-citations | workflow | Spec closure removes the spec file. Every sibling citation of it then points at nothing. The gate that finds them existed and was correct. No closure step fed it, so it drifted to 15 red rows | a BLOCKING step before commit B. The author who removes the spec names and repairs its citers, inside commit A |
| 2026-08-12 | - | Go source header | Found from `spec-ipsec-rfc9190` phase 1, which this row does not close. Second recurrence. `internal/component/lg/auth.go` carries a `// Design:` header naming `plan/spec-fixit-mgmt-listener-auth-guard.md`, which a spec closure has since removed. `make ze-verify-wiring-docs` is red at HEAD over it, for a session that touched neither file | not fixed here. `internal/component/lg/` is outside this spec's package, and editing it would pull an unrelated package into this commit's lint and review scope |
| 2026-08-11 | rfc-ledger-per-rfc-shards | workflow | Recurrence after the fix above. HEAD commit 8b85ada76 closed `spec-fixit-egress-filter-non-decision-channel`. It left a citation of that spec in `plan/deferrals/fixit-stored-route-relay-hardening.md`. `make ze-doc-links` is red at HEAD for a session that caused none of it | not fixed here. The repair belongs to the author of that closure |
