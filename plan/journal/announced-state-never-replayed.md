# A peer is announced to a consumer whose copy of that peer's state is empty

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-08 | config-apply-ordering-covers-every-root | `peerSettingsFromOperationConfig` (`internal/component/bgp/reactor/operation.go`) and the parser it calls, `parsePeerFromTree` -> `parsePeerSettings` (`internal/component/bgp/reactor/config.go`) | A rolled-back `remove-peer` brings the session back and announces nothing. `applyConfigOperation` records the inverse of a remove as `a.r.AddPeer(settings)`, where `settings` came from `peerSettingsFromOperationConfig(op, op.Params.OldConfig)`. That parser never reads the peer's `update` block and never sets `PeerSettings.StaticRoutes`: the routes a peer originates are parsed by a different function, `internal/component/bgp/config/bgp_routes.go`, which this path does not call. Observed on the wire by `test/reload/config-apply-ordering-mixed-rollback.ci`: connection 2 is the re-added peer, it completes its OPEN and sends the End-of-RIB, and the `192.168.1.0/24` that connection 1 carried never appears. Producers read 2026-09-08. `add-peer` and `modify-peer` escape it because `candidatePeerSettingsFromOperationConfig` tries `peerSettingsFromReloadConfig` first, which loads the whole config file; the remove path has no such fallback, so its inverse is the one that restores a silent peer | FIXED, same day, in the spec's phase-4 work. The inverse no longer rebuilds the peer from the operation's config subtree: `runningPeerSettings` (`internal/component/bgp/reactor/operation.go`) answers with the settings the reactor is RUNNING, which are the full loader's own output for the active config, so no field that loader fills can be dropped by this path. `test/reload/config-apply-ordering-mixed-rollback.ci` now asserts the `192.168.1.0/24` on connection 2 and was observed RED with the old derivation restored; `TestApplyConfigOperationRemovePeerRollbackRestoresAnnouncedState` (`internal/component/bgp/reactor/operation_test.go`) covers the routes, the plugin routes, both filter chains and the loop-detection policy |
| 2026-09-06 | fixit-send-names-its-destination | the config-driven static routes of `test/exabgp-compat/etc/api-reload.1.conf`, observed on the wire by `test/exabgp-compat/api/api-reload.ci` | A peer configured with two static routes is advertised one of them. The config declares `route 1.0.0.0/24 next-hop 4.0.0.0` and `route 2.0.0.0/24 next-hop 4.0.0.0`; the frames the mock peer recorded hold `1.0.0.0/24` and then the End-of-RIB, and no `2.0.0.0/24` at any point. The reload to `api-reload.2.conf`, which drops the second route, therefore also sends no withdrawal for it, so the fixture's `A3` withdraw never arrives and the case now ends in a read timeout. CAUSE UNVERIFIED: the frames are the evidence and no producer was read for it, so whether the second route is lost in the static parser, in `peer_initial_sync.go`, or between them is not established here. Found while giving the API rails an Adj-RIB-Out, which collapsed this case's eight duplicate `3.0.0.0/24` announcements into one and left the missing route as the only remaining failure | not fixed. Reproduce with `ze-test exabgp api 33 --save <dir>` and read the frames. An A/B probe settles that it is not the suppression's doing: with the Adj-RIB-Out check forced off, the same run records `1.0.0.0/24`, the End-of-RIB and eight copies of `3.0.0.0/24`, and still no `2.0.0.0/24` |
| 2026-09-03 | none (walked into clearing the arm64 QEMU failures) | `firewallIRRColdCache07` and `firewallIRRTableTermCommit07` (`internal/test/fixture/plugin_fixture_07_irr.go`) against their `.ci` expectations | RETRACTED AND CORRECTED, same day. This row first read the failures as a product defect: the daemon logs `firewall: table held back, a rule names a set no owner has registered yet table=ze_wan set=irr_v4_AS-TEST`, and the row claimed the held-back table was never programmed once the set arrived. That was wrong, and the WARN was a red herring the fixture itself ASSERTS as the expected mid-state. The real fault: both fixtures run every check, pass, and `return nil` printing NOTHING, while their `.ci` each assert one stderr line (`lan stayed programmed, and the update brought wan back`; `both table terms and their IPv6 twins are in the kernel ruleset`). Neither string existed anywhere in the repository except in the expectation. A red that could not have been green, the inverse of `green-that-could-not-have-been-red.md` | FIXED. Both fixtures now emit the line their contract asserts, as the sibling `firewallIRRCommit07` already did. The firewall-irr group is 12 of 12 in the QEMU VM at -p 1 on runtime kernel 7.2 arm64, where it was 10 of 12. The lesson is the retraction, not the fix: a WARN in the log was read as the cause because it was the only thing in the log that looked like one, and the assertion nobody satisfied was invisible until the pattern was grepped for a producer. Producers read 2026-09-03 |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `internal/component/bgp/plugins/bmp` -- `primeSender` and `BMPPlugin.bounceMonitoredPeers` vs `senderSession.writeRouteMonitoring` | A BMP collector is told a peer is up and is never given that peer's routes. The only Route Monitoring producer for an Adj-RIB is the live update path, `handleStructuredEvent` -> `writeRouteMonitoring`; the two other call sites are both Loc-RIB replay in `bmp_locrib.go`. `primeSender` sends a Peer Up and stops. So a collector that attaches to a running ze, or one whose peers were bounced by the RFC 8671 Section 7.2 config-change sequence, holds nothing for each announced peer until that peer sends its next UPDATE, which on a stable session can be hours. RFC 7854 Section 4.9 gives a Peer Down the meaning "withdraw everything you hold for this peer", so the bounce is a withdraw with no matching announce. Found by review round 4, which read the comment on `bounceMonitoredPeers` saying the collector "re-learns each peer under the new configuration" and looked for the producer that would make it true | comment corrected to say what the code does, defect recorded not fixed. It predates the peer bounce and is not that change's regression: the transport drop the bounce replaces ended in the same empty state, because a reconnected collector was primed the same way. It blocks no goal of this spec, whose subject is the extraction sign-off gate. What an operator loses is the Adj-RIB-In half of BMP: Loc-RIB monitoring replays correctly, pre-policy monitoring is silently empty until traffic arrives |
