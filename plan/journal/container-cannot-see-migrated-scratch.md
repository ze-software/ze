| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-26 | cli-pipe-operator-coverage | `demos/terminal/render.py` `container_command` | the retired `ze-site-generate` (current: `./le site build`) mounted the repository at `/src`, but migrated `tmp/terminal-demos` was an absolute symlink to a host cache path that the container could not see. Demo validation failed before website generation. | `container_command` bind-mounts the resolved terminal-demo scratch directory at the same absolute path when the repository path is a symlink. The container can follow the symlink without exposing the cache parent. |
| 2026-08-30 | - | `internal/le/terminaldemo/render.go` `renderDemo`, and the recorder it starts (`pty.go` `newCastWriter`) | `renderDemo` deletes each expected artifact from the HOST just before starting the container that rewrites it. On macOS under colima the guest keeps its own virtiofs dentry for that name, so the recorder's `os.Create` resolved to the inode the host had removed and returned ENOENT. `./le terminal-demo render-all` failed on the first recording of a batch, while the identical `docker run` by hand always worked, because only `./le` performs the host-side delete. Measured: host deletes then guest creates, 9 failures in 10; guest deletes then guest creates, 0 in 6 | the recorder unlinks its own output before creating it, which is a guest operation, so the guest cache agrees with the create that follows: 0 failures in 10. Transferable: when a host and a container share a directory, the side that CREATES a name must also be the side that removed it. A delete on one side of a virtiofs or 9p mount does not invalidate the other side's cache, and the symptom is ENOENT on a path both sides can list |
| 2026-09-10 | spec-ipsec-dataplane-inspection | IPsec `prepareScenario`, L2TP `renderInitiatorConfig` | System-temp configs are outside the Docker VM share. The Ze container receives a directory at `/etc/ze/ze.conf` and cannot start. IPsec reaches system temp when no session ID is set. | Render both suites under the checkout's existing `tmp/interop-rendered` directory. |
| 2026-09-15 | spec-probe-do-not-fragment | `Run.scratchShare` (`internal/le/qemu/run.go`) and `runQEMUBuild` (`internal/appliance/kernelbuilder/qemu.go`) | Both launchers mount the checkout at `/workspace` over 9p and reach `/workspace/tmp/qemu/...`. `./le scratch migrate` leaves `tmp/` real and each child (`tmp/qemu`) an absolute symlink to a host cache path. `Run.scratchShare` exported a target only when `tmp/` itself was the symlink, so the guest's `mkdir -p /workspace/tmp/qemu/go-dl` followed a dangling link and every `./le qemu run` failed. The kernel builder exported nothing at all: `sh: /workspace/tmp/qemu/bin/amd64/ze-kernel-builder: not found`. | `tmplink.Shares` (`internal/core/tmplink`, the one declaration of the migratable children, which `internal/le/scratch` now derives from) answers one 9p share per symlink the guest can meet: `tmp/` itself or each migratable child of a real `tmp/`. Both launchers export every share and mount it at the link's own absolute path before the guest touches `/workspace/tmp`. |
