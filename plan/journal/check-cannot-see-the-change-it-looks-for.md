# Check cannot see the change it looks for

A check runs, answers, and is believed. Both of its inputs were narrowed before
the comparison, so the difference it exists to announce cannot appear in either
one. Nothing is silent and nothing is red: the check prints a confident line
about values that no longer carry the fact.

This is the input-side twin of `green-that-could-not-have-been-red`. There the
observation itself cannot come out otherwise. Here the observation is fine, and
each of the two things observed was replaced by a stand-in first.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-06 | - | `findUnsupportedFeatures` and `checkUnsupportedInPeerTree` (`internal/component/config/cli/cmd_migrate.go:248,285`) | The three warnings `ze config migrate` promises for ExaBGP's dead capabilities cannot fire, because the check reads the wrong tree. `configMigrateWithWarnings` (`:335`) calls `findUnsupportedFeatures(result.Tree)`, the MIGRATED output, and looks there for `capability.multi-session`, `capability.operational` and a peer-level `operational` block. Nothing puts any of the three into a migrated tree. The input side cannot supply them either: the command parses with the ze YANG schema, which has no such nodes, so a config carrying one dies at the parser first. Measured with a probe binary on a native config: `error: parse error: line 7: unknown field in capability: multi-session`, and `error: parse error: line 6: unknown field in peer: operational`. The ExaBGP-format route never reaches this code at all, because `ze exabgp migrate` refuses the config in `migrateCapability`. So all three branches are unreachable, and no test covers any of them: `git grep` finds no caller of either function outside `cmd_migrate.go`. `docs/architecture/config/deprecated-options.md` published the promised warnings as fact until this session | not fixed. Found while establishing what ze does with `multi-session` and `operational`, which is the refusal in `internal/exabgp/migration/`, not this command. The page WAS repaired in that work, so no reader is now told to expect a warning. The repair here is a deletion rather than a rewiring: the two capabilities are refused at two earlier boundaries that both name them, so a third statement of the same fact is the second declaration `ai/rules/principles.md` bans. Whoever owns `cmd_migrate.go` should confirm the peer-level `operational` block has no third route before removing it |
| 2026-09-05 | spec-verification-answers-one-agent | `./le verify reds file <path>`, `latestRun` (`internal/le/verify/reds.go`) against a `./le verify worktree` run | `latestRun` reads `<root>/tmp/verify` and answers about the newest directory there. A `verify worktree` run never writes there: `run` (`internal/le/verify/lifecycle.go`) calls `verifyengine.Run(ctx, path, ...)` with `path` the detached worktree, so `runMode` writes its stage logs to `<worktree>/tmp/verify/<mode>-<n>/`. They are not lost at the end: `saveLogs` (`internal/le/verify/cleanup.go`) copies that whole tree to `<root>/tmp/verify-worktree-logs/<stamp>/` before `removeWorktree` deletes the worktree, and it does so only when the run was RED. `latestRun` reads neither the live path nor the saved one. Asked during a live `verify worktree` at its sixteenth stage, the query answered about a `verify current` run that had ended 36 minutes earlier, verdict `undetermined`, `stages: 15 of 49 reported ... (the run has ended, so the rest never reported)`. Every field is true about the run it read and none of it is about the run the asker is waiting on. A `not-named` from such a stale run exits 0 and reads as a verdict on the run in flight. Found by dogfooding the feature during this spec's own closure, against the closure's own `verify worktree` | not fixed. The spec's AC-6 and AC-7 are about a run directory under `tmp/verify/` and are met; this is the other entry point, which `ai/rules/precommit-verify.md` names as the commit gate, so it is the case the feature most needs to serve. Two candidate repairs, and the choice is a decision rather than an edit: teach `latestRun` to search `tmp/verify-worktree/*/tmp/verify/` and `tmp/verify-worktree-logs/*/` as well; or have `run` name its worktree's run directory somewhere under the checkout root so one lookup finds either. Either way the answer MUST stop reading a finished older run as the subject when a live run exists elsewhere |
| 2026-09-04 | command-help-and-description | `./le rfc discriminate-record`, mutant route, over `internal/component/ike/engine` | The cost of proving a tag scales with the SIZE OF THE PACKAGE around the producer, not with the change. The same walk cost one minute for `internal/plugins/isis/spf` (9 files) and is unaffordable for `internal/component/ike/engine`: gomu walks 42 files at roughly 500 mutants each and runs the package suite per mutant, and that suite takes 35 seconds. gomu takes a path argument and IGNORES it, always walking the whole package, so the only way to scope it is `.gomuignore`, a shared committed file. Scoping to one file still left 563 mutants, over an hour, and gomu writes its report only at the END, so a run cut short produces nothing to resume from. Two further traps: `.gomuignore` honours directory prefixes and basename globs and silently matches NOTHING for a full relative path, which cost two runs before the file count moved from 42 to 1; and `./le rfc discriminate <stem> report <path>` READS a gomu report rather than writing one, so the path is an input and a missing file is the error you get first | not fixed. RFC5305-3-1 IS recorded, so the route works where the package is small. The IKE record is owed and the tag it belongs to is written. The escape was NOT taken: `no-break` claims no break exists, and one was watched turning these tests red by hand (breaking `childRekeyDHGroup` gives 'pfs enable sent a rekey with no Diffie-Hellman half'), so the proof exists and only the artifact is missing. What the tooling wants is a way to scope a mutation run to one FUNCTION, or to accept an observed red the recorder itself drives without a gomu report behind it |
| 2026-09-02 | - | le vendor-web update-report | The report that announces a newer vendored web asset cannot announce htmx 4.0.0, released five days earlier. Both inputs were narrowed. semverRE read only X.Y.Z from MANIFEST.md, so the vendored 4.0.0-beta6 read as 4.0.0. fetchLatestNpmVersion asked for the latest dist-tag alone, and htmx published 4.0.0 under next while latest stayed on 2.0.10. The two narrowings printed "4.0.0 -> 2.0.10 available", a recommendation to downgrade, over an upgrade the tree owed | fixed on the spot, as code related to the htmx 4.0.0 vendoring. semverRE keeps the prerelease suffix. newestPublishedRelease reads every dist-tag and answers the newest release among them. laterRelease orders a prerelease before the release sharing its triple. The rendering then separates an upgrade from a tree that runs ahead of every release |
| 2026-09-04 | - | `TestReservedPeerNamesSyncWithRPCs` (`internal/component/bgp/config/loader_test.go`) | The test keeps `reservedPeerNames` (`resolve.go`) in sync with the peer-command keywords an operator must not use as a peer name, and it passes under the tag set most runs use while the sync is broken. `PeerSubcommandKeywords` (`internal/component/plugin/server/rpc_register.go`) derives its set from `yang.DefaultLoader()`, so the set holds whatever cmd YANG modules the test binary LINKED. Without `ze_bgp` that set is non-empty and INCOMPLETE, and the loop only asserts about keywords it iterates, so a keyword that is missing is never checked. Measured the same minute on one checkout: no tags gives `ok`, `-tags ze_bgp` gives `FAIL: RPC keyword "withdraw" ... is missing from reservedPeerNames`. The author DID consider an unpopulated registry and wrote `require.NotEmpty(t, dynamicKeywords, ...)` at `:1373`; that guard is correct, present, and useless, because it cannot tell non-empty from complete, and a reviewer who sees it concludes the vacuity case is handled. CORRECTED 2026-09-04 by the owner: there is NO defect underneath, and the test is a false positive. `peer` is ALWAYS followed by a mandatory selector. `container peer` declares `leaf selector { mandatory true }` and the verbs are its SIBLINGS, in `ze-peer-cmd.yang`, `ze-cli-announce-cmd.yang` and `ze-update-cmd.yang` alike, and `ze-peer-cmd.yang`'s own `ze:help` says the selector is declared there so every command below it reads it. So an operator types `peer <selector> withdraw`, never `peer withdraw`, and a peer named `withdraw` is read in the selector slot with the verb still to come. `PeerSubcommandKeywords` derives its set from ADJACENCY IN THE PATH STRING, where the selector is a value rather than a node and so does not appear, and it therefore reports a collision the typed grammar cannot produce. The red is the test's, not the product's. Found while disproving an attribution: two sessions had each blamed three of this package's 110 failures on the other's uncommitted YANG, and the package's failure set is byte-identical with that file restored from HEAD | not fixed, and the repair is NOT to add `withdraw` to the map. Two faults sit here and the correction separates them. The test's derivation is WRONG: it reads adjacency in a path string where the mandatory selector does not appear, so it reports collisions the grammar cannot produce, and adding the word it names would enlarge a list against a threat that does not exist. Its tag-dependence is separately wrong: a guard whose population depends on build tags is only as strong as the weakest tag set anyone runs it under, and `require.NotEmpty` cannot tell non-empty from complete. A reader who fixes only the second keeps a check that is precise about the wrong thing. Open question this leaves: whether `reservedPeerNames` guards anything at all now, since its own comment describes a `peer list detail` ambiguity the mandatory selector prevents, which suggests the list outlived the grammar it was written for. `fixit-filter` found the same shape twice more the same day, in the YANG loader's discarded `LoadRegistered` error and in the config route parser for `ipv4/flow`; the second is measured but not diagnosed |
| 2026-09-03 | - | `ospf-broadcast-frr` network-LSA assertion (`internal/le/interoplab/bgp/check_extras.go`) | The assertion waited 60 seconds for `"Network Link States"` in `show ip ospf database network`. FRR 10.3.1 spells that section `Net Link States`, so the needle could not appear whatever ze originated and the scenario was red at HEAD. Found by symmetry while repairing the same misspelling in the `ospf-ptmp-frr` absence proof | fixed: the needle is now `Net Link States` beside ze's router id, so a bare heading no longer satisfies it and the assertion reads the network-LSA ze originates as the elected DR. `INTEROP_SCENARIO=ospf-broadcast-frr ./le integration interop` passes |
| 2026-09-08 | config-apply-ordering-covers-every-root | `iface-remove-address-before-interface`, a constraint rule registered in `internal/component/iface/operation.go`, read by `operationsRelated` (`internal/component/config/transaction/depgraph.go`) | The rule produced NO edge, ever, so deleting an interface had no ordering against the removal of its own addresses. Its relation `interface-address` compared `opIfaceName(before)` with `opAddrIface(after)`, and `opAddrIface` reads `Target.Interface` then `Params.Interface`. An interface operation carries its name in `Target.Name` and `Params.Name` (`ifaceInterfaceOperation`), so the right-hand side was "" for every pair the rule ever saw and the comparison could not come out true. MEASURED before any deletion, by building the graph with the nine rules as they stood over the operations the decomposer emits for an interface delete: 0 edges. The sibling rule `iface-add-interface-before-address` looked identical and DID fire, because there the interface operation is on the LEFT, where `opIfaceName` does read `Target.Name` | FIXED, at the class rather than the rule. The rule is deleted with the eight others that stated a produce and consume fact, and the ordering is now DERIVED: the address operation declares `Consumes` the interface and the interface operation declares `Produces` it, so the destroy-consumer-before-destroy-producer edge falls out of the pair. `TestBuildOperationGraphDerivedEdgesMatchDeletedRules` records the measured pre-deletion edge set per fixture and names this edge as one of the two the derivation ADDS, so the repair cannot be mistaken for a regression. Nothing else in the tree still selects an operation of another root by its label |
| 2026-09-11 | bgp-update-delay | a park-and-restore cycle over `internal/component/bgp/reactor/reactor_notify.go` and `internal/le/interoplab/bgp/checkers.go`, checked against `git status` and `git diff HEAD` | Content lost INTO HEAD leaves a ZERO working-tree diff, so both of the checks a session reaches for read the loss as clean. This spec's half was parked (reverted from the tree, preserved as `backups/bgp-update-delay-tracked-20260911-0115.patch`) while a sibling session landed a mutually entangled file, then restored. Two hunks did not come back: `peer.updateDelayEndOfRIB(eorFamily)`, the ONLY production caller of the convergence hook, and the `bgp-update-delay-frr` scenario key. Both had already been carried into HEAD by other sessions under the landing presumption (`ai/rules/git-safety.md`), so after the restore the file matched HEAD exactly: `git status` showed it clean and `git diff HEAD` was empty for those lines. Nothing was red either. Seven test functions call `updateDelayEndOfRIB` directly at fourteen call sites, so the unit suite proved the hold converges when TOLD and said nothing about whether the wire ever tells it, and `bgp-update-delay.ci` asserts `reason=max-delay` while REJECTING `reason=converged`. With the call gone the feature released on a timer alone and never converged, which is the behavior it exists to deliver. Found only by rebuilding the pre-park tree out of the patch's own blobs and diffing THAT against the restored tree | fixed: both hunks restored, and `test/plugin/bgp-update-delay-converges.ci` added so the convergence path has a functional test driving it through a daemon (forced RED by replacing the call with `_ = eorFamily`: FAIL at 61.7s, PASS at 2.7s restored). The transferable repair is the COMPARISON: after a restore, diff the restored tree against the PARKED PATCH, never against HEAD and never against `git status`. A park whose content other sessions carry into HEAD makes both of those blind by construction, and the blindness grows with how long the park lasts |
