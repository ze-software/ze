# Check cannot see the change it looks for

A check runs, answers, and is believed. Both of its inputs were narrowed before
the comparison, so the difference it exists to announce cannot appear in either
one. Nothing is silent and nothing is red: the check prints a confident line
about values that no longer carry the fact.

This is the input-side twin of `green-that-could-not-have-been-red`. There the
observation itself cannot come out otherwise. Here the observation is fine, and
each of the two things observed was replaced by a stand-in first.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-04 | command-help-and-description | `./le rfc discriminate-record`, mutant route, over `internal/component/ike/engine` | The cost of proving a tag scales with the SIZE OF THE PACKAGE around the producer, not with the change. The same walk cost one minute for `internal/plugins/isis/spf` (9 files) and is unaffordable for `internal/component/ike/engine`: gomu walks 42 files at roughly 500 mutants each and runs the package suite per mutant, and that suite takes 35 seconds. gomu takes a path argument and IGNORES it, always walking the whole package, so the only way to scope it is `.gomuignore`, a shared committed file. Scoping to one file still left 563 mutants, over an hour, and gomu writes its report only at the END, so a run cut short produces nothing to resume from. Two further traps: `.gomuignore` honours directory prefixes and basename globs and silently matches NOTHING for a full relative path, which cost two runs before the file count moved from 42 to 1; and `./le rfc discriminate <stem> report <path>` READS a gomu report rather than writing one, so the path is an input and a missing file is the error you get first | not fixed. RFC5305-3-1 IS recorded, so the route works where the package is small. The IKE record is owed and the tag it belongs to is written. The escape was NOT taken: `no-break` claims no break exists, and one was watched turning these tests red by hand (breaking `childRekeyDHGroup` gives 'pfs enable sent a rekey with no Diffie-Hellman half'), so the proof exists and only the artifact is missing. What the tooling wants is a way to scope a mutation run to one FUNCTION, or to accept an observed red the recorder itself drives without a gomu report behind it |
| 2026-09-02 | - | le vendor-web update-report | The report that announces a newer vendored web asset cannot announce htmx 4.0.0, released five days earlier. Both inputs were narrowed. semverRE read only X.Y.Z from MANIFEST.md, so the vendored 4.0.0-beta6 read as 4.0.0. fetchLatestNpmVersion asked for the latest dist-tag alone, and htmx published 4.0.0 under next while latest stayed on 2.0.10. The two narrowings printed "4.0.0 -> 2.0.10 available", a recommendation to downgrade, over an upgrade the tree owed | fixed on the spot, as code related to the htmx 4.0.0 vendoring. semverRE keeps the prerelease suffix. newestPublishedRelease reads every dist-tag and answers the newest release among them. laterRelease orders a prerelease before the release sharing its triple. The rendering then separates an upgrade from a tree that runs ahead of every release |
| 2026-09-04 | - | `TestReservedPeerNamesSyncWithRPCs` (`internal/component/bgp/config/loader_test.go`) | The test keeps `reservedPeerNames` (`resolve.go`) in sync with the peer-command keywords an operator must not use as a peer name, and it passes under the tag set most runs use while the sync is broken. `PeerSubcommandKeywords` (`internal/component/plugin/server/rpc_register.go`) derives its set from `yang.DefaultLoader()`, so the set holds whatever cmd YANG modules the test binary LINKED. Without `ze_bgp` that set is non-empty and INCOMPLETE, and the loop only asserts about keywords it iterates, so a keyword that is missing is never checked. Measured the same minute on one checkout: no tags gives `ok`, `-tags ze_bgp` gives `FAIL: RPC keyword "withdraw" ... is missing from reservedPeerNames`. The author DID consider an unpopulated registry and wrote `require.NotEmpty(t, dynamicKeywords, ...)` at `:1373`; that guard is correct, present, and useless, because it cannot tell non-empty from complete, and a reviewer who sees it concludes the vacuity case is handled. The defect it hides is real and is committed code neither session wrote: `actionWithdraw = "withdraw"` (`internal/component/bgp/plugins/cmd/commit/commit.go`) is a registered keyword, `reservedPeerNames` does not list it, so an operator can name a peer `withdraw` today. Found while disproving an attribution: two sessions had each blamed three of this package's 110 failures on the other's uncommitted YANG, and the package's failure set is byte-identical with that file restored from HEAD | not fixed. The completeness claim is the thing to repair, not the keyword: adding `withdraw` to the map leaves the next keyword unguarded the same way. A guard whose population depends on build tags is only as strong as the weakest tag set anyone runs it under, so either the test states the tag set it needs and refuses to run under a narrower one, or the keyword set is derived from something the binary cannot partially link. `fixit-filter` found the same shape twice more the same day, in the YANG loader's discarded `LoadRegistered` error and in the config route parser for `ipv4/flow`; the second is measured but not diagnosed |
| 2026-09-03 | - | `ospf-broadcast-frr` network-LSA assertion (`internal/le/interoplab/bgp/check_extras.go`) | The assertion waited 60 seconds for `"Network Link States"` in `show ip ospf database network`. FRR 10.3.1 spells that section `Net Link States`, so the needle could not appear whatever ze originated and the scenario was red at HEAD. Found by symmetry while repairing the same misspelling in the `ospf-ptmp-frr` absence proof | fixed: the needle is now `Net Link States` beside ze's router id, so a bare heading no longer satisfies it and the assertion reads the network-LSA ze originates as the elected DR. `INTEROP_SCENARIO=ospf-broadcast-frr ./le integration interop` passes |
