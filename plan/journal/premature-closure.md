| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-07 | daemon-backed-command-catalog (found while working on it) | `internal/component/plugin/all/testdata/plugins.snapshot`, `wire-methods.snapshot`, `yang-providers.snapshot` | The `firewall-domain` plugin registers, serves three wire methods and provides a YANG module, and none of the three expected-set snapshots names it. So `TestRegisteredPluginNames`, `TestRegisteredWireMethods` and `TestYANGSchemaProviders` were red at HEAD for every session that ran that package. Its spec `plan/spec-firewall-domain-group.md` was closed by `f339bf0ee7`, so no session owned the gap: closure took the spec off the ledger while four assertions still disagreed with the tree | FIXED here. The four entries were added, and the tests pass. The snapshots are an expected-set the closure owed and did not pay, which is what makes this the premature-closure class rather than a stale-artifact one. Two reds remain in that package and are NOT this: `TestGeneratedPluginImportsCurrent` and `TestYANGGlueCurrent` both fail with `package command-line-arguments is not a main package`, a generator-invocation fault unrelated to the plugin |
| 2026-03-30 | - | workflow | learned summary said 'future decorator wiring' but spec was closed | reopened spec and wired the feature |
| 2026-08-26 | - | `rfc/short/rfc9552.md` and `ai/RFC-REQUIREMENTS.md` | `./le test-health check` is red at HEAD. It has been red since `dd6b96ccf` (`fix(rfc): read every spelling of the obsoleted-by row`), 20 commits ago. Five gated MUST-level requirements of enrolled `rfc9552` have neither a test pair nor an annotation. The ledger accurately reports `No test 5`. The health page refuses an annotation split that does not sum to the ledger remainder. Thus, the gate exits 2 with `annotation split ... sums to 1727, but the ledger's remainder is 1732`. The ledger columns balance as `one 0 + annotated 1727 + notest 5 = 1732`. Therefore, the five unproven MUSTs cause the divergence, not a parse fault. Proof that the Go port equals its script exposed the defect. A bisection over `git clone --local` checkouts was green at `dd6b96ccf~1` | not fixed here. This is an RFC-compliance obligation, not a tool defect. `ai/rules/rfc-compliance.md` requires each requirement to be implemented and proven by a tagged test. The alternative is to correct its level against the RFC text. The tooling port cannot make either decision. The report for that step raised the issue to the owner |
