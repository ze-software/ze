# An exemption one mechanism grants is still billed by another

A gate offers a way to record that a member of a population does not carry the
evidence the population usually carries. The record is accepted. A second
mechanism of the same gate then counts that member against a budget the
exemption said it does not draw from.

Nothing is wrong on the day the exemption is written, because the budget has
room. The failure arrives later, at the boundary, when a member that DOES carry
the evidence is added and the budget refuses it. The refusal names the new
member, so the reader repairs the new member and never sees the exempted ones
consuming its room.

The shape that survives subtracts the exempted members before the comparison.
An exemption a gate grants and then charges for is two rules with one name.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-05 | - | `DeriveRegister` (`internal/le/rfc/inventory.go`), reached through `./le rfc check` | The register a sign-off may claim is decided by `keywordSites >= gated`. `gated` counts every gated requirement the summary declares; `keywordSites` counts the capitalised RFC 2119 sentences derived from the source. An extraction artifact separately SANCTIONS a gated requirement backed by no source sentence, through a section's `unsourced-ids` list, and the same `./le rfc check` run accepts it. The two disagree: an honestly recorded unsourced id is still billed against the keyword budget. rfc8671 sits exactly at the boundary, 10 keyword sites against 10 gated requirements, two of them unsourced (RFC8671-x-1 and RFC8671-x-3). Declaring the second MUST of RFC 8671 Section 5.2, "All mandatory attributes, such as next hop, MUST be either zero or have an empty length if they are unknown at the pre-policy phase completion", takes gated to 11, drops the derived register to `prose`, and reds the artifact on four further counts because the prose register derives a different site set. Found while repairing the false public conformance claim on rfc8671 | not fixed. That obligation stays undeclared and is recorded instead as an excluded `feature-out-of-scope` site at 5.2:1 of `rfc/extraction/rfc8671.json`, with the summary's `Support remaining` cell naming the site so the public row still discloses it. The repair subtracts the ids a section lists in `unsourced-ids` from `gated` before the comparison, so a requirement the walk already admits has no capitalised source sentence stops consuming the room a sourced one needs |
