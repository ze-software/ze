| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-03 | - | `gokrazy/kernel/runtime.config` and `runtime.require`, the runtime kernel's conntrack dump interface | `c262fc9954` added `CONFIG_NF_CONNTRACK_NETLINK=y` to give `conntrackWorker.Dump` (`internal/plugins/flowexport/conntrack_worker.go`) the ctnetlink interface it needs, and that is not a Kconfig symbol. The interface is `NF_CT_NETLINK`. Kconfig drops an unknown symbol in silence, so the merged `.config` carries no line for it at all, and a symbol that resolves to nothing is indistinguishable from one never asked for. Every dependency the wrong name would have needed was already satisfied, which is why nothing looked wrong: `NETFILTER=y`, `NETFILTER_ADVANCED=y`, `NETFILTER_NETLINK=y` and `NF_CONNTRACK=y` are all in the kernel built at 07:58, and the real symbol sits at line 1278 of that config as `# CONFIG_NF_CT_NETLINK is not set`. So the commit declared a kernel capability the kernel did not gain. It shipped because nobody built a kernel from it: the commit landed at 15:19 and the first build was at 19:30, four hours later, in a session that had recorded the change as unverified and was blocked on disk space until 80G came free | fixed by naming `CONFIG_NF_CT_NETLINK` in both files, with a comment above it recording the trap where the next reader meets it. `runtime.require` is the reason this cost thirty seconds rather than a forty-minute compile: it refused with `FATAL: kernel profile runtime: CONFIG_NF_CONNTRACK_NETLINK did not resolve to =y` before `make` ran. It is also the ONLY thing that can see this class, because the config file itself accepts any string, so a symbol named there and not in the require list would fail silently forever. Red to green measured in QEMU on the rebuilt arm64 kernel 7.2: `test/plugin/ddos-detect-characterize` passes in 4.8 s with no `conntrack dump failed` line, where the same test on the 07:58 kernel matched `ip daddr` alone after 568000 packets with that error logged every second |
| 2026-09-03 | - | `auth { radius { server ... } }`, `activateRadiusConfig` (`internal/component/l2tp/plugins/authradius/register.go:168`) | RADIUS accounting cannot be configured without RADIUS authentication, and no document or test states that as a limitation. One config block feeds auth, accounting and CoA: `activateRadiusConfig` builds the client, then calls `RegisterAuthHandler` (`:193`) and `acctInstance.setClient` (`:196`) together, so a server configured for accounting alone still takes the single auth slot. `radiusAuth.handle` then refuses every `AuthMethodNone` session under the RFC 2865 Section 4.1 credential rule, so a wholesale L2TP LNS that only bills, and never authenticates, cannot run. The `client == nil` arm of `handle` that answers "no-auth accepted (no RADIUS client)" is the behaviour the design wanted, and it is unreachable in production: `swapClient` and `setClient` have one caller each, both pass a non-nil client, and the slot is claimed after that | not fixed. `runNoAuthPhase` (`internal/component/l2tp/ppp/session_run.go`) already records no-auth as a policy decision an external handler may admit or deny, so the repair is for `handle` to accept `AuthMethodNone` without building a request, and to keep the Section 4.1 guard for every method that does carry a credential. Refusing the pairing at config verify instead would forbid accounting-only outright. Do not fold this into the radius-acct-wire repair recorded in `test-against-broken-path.md`: that test needs a real credential on the wire either way |
| 2026-09-03 | command-help-and-description | `internal/component/bgp/yang/ze-bgp-api.yang` | Five RPCs are declared in the BGP API module and no handler answers their wire method: `peer-show`, `peer-show-capabilities`, `peer-show-statistics`, `peer-add` and `peer-remove`. `grep 'WireMethod: "ze-bgp:'` finds handlers for `peer-detail`, `peer-capabilities`, `peer-statistics` and `ze-delete:bgp-peer`, and nothing at all for `peer-add`. So the schema publishes a grammar, a completion candidate and an OpenAPI operation for five calls that cannot be served, and the first three look like near-misses of an earlier rename that moved the handler's method and left the declaration. Found by an agent writing the `ze:help` explanations for this module, which is why it was caught: writing a real explanation forces a read of the producer, and there was none to read. The agent wrote their help from the declared input and output alone and claimed no runtime behavior, which is the correct answer but leaves the declarations live | not fixed. The repair is a decision rather than an edit: either the three renamed pairs are repointed and `peer-add` and `peer-remove` get handlers, or the five declarations are removed. Both are BGP command-surface work outside this spec, whose diff is help text and a documentation gate, and folding either in would cost this commit its focus (`ai/rules/rule-precedence.md`) |
| 2026-09-03 | command-help-and-description | `rpc bgp-peer-with` and `rpc bgp-peer-save` (`internal/component/cmd/set/yang/ze-cli-set-api.yang`), `rpc peer-update-hex` (`internal/component/bgp/plugins/cmd/update/yang/ze-bgp-cmd-update-api.yang`), `rpc command-help` and `rpc command-complete` (`internal/component/bgp/plugins/rib/yang/ze-rib-api.yang`) | Five RPC declarations that no handler serves. Each name is grepped against every `RegisterRPCs` call and answers nothing. No `ze:command` names them either. The two `set` ones are leftovers of a removal their own module records: `ze-cli-set-cmd.yang` carries `revision 2026-06-03 { description "Removed set bgp peer with/save."; }`. `peer-update-hex` is a stale duplicate. `handleUpdate` (`update_text.go`) dispatches text, hex, b64 and cursor from the ONE wire method `ze-bgp:peer-update`, so the hex form holds no wire method of its own. The two rib ones reach no CLI node. Their siblings `help`, `command-list` and `event-list` DO answer, as the plugin commands `show bgp rib help`, `commands` and `events`. Each declaration is published in the schema and in `ze help`, so an operator reads a command Ze cannot run | not fixed. Found while a session wrote one `ze:help` for each rendering declaration. No help text was written for these five, because an explanation of a command with no handler is a fabrication. The fix needs a decision rather than an edit. Either the rpc goes or the handler is written, and `set bgp peer with` was deliberately removed |
| 2026-09-02 | none (owner asked to admit the lint gate through `le job`) | `shares` (`internal/le/job/registry.go`), the job registry's ATTACH | Sharing could not happen after a wait, which is the only time the machine has a running job to share. `queue` sets `treeStale` on every busy iteration, and `take` measures the tree again for the job it ADMITS. Nothing measures it again for the job that is only ASKING, so `shares` compared a hash the waiter took before its wait against one the holder took at its claim. Several sessions work this checkout, so something changes during almost every wait and the answer was "different tree" for two jobs running identical work. Measured through the real gate before the fix: two `./le verify lint run` with one argv over one tree both waited behind a third session's lint, and then both ran the whole matrix. ATTACH is the reason the package exists to prefer one run over two, and it was reachable only by a job that found a holder on its FIRST scan | fixed on the spot, four lines: `shares` measures the asker's tree again when it is stale, and the work-key comparison moved above it so a job waiting behind unrelated work still pays nothing. `TestAWaiterMeasuresItsTreeAgainBeforeItDeclinesToShare` is red against the old body; the two neighbours hold the cost guard and the no-false-attach guard. End-to-end after the fix, the same two lints: the second printed `attaching to the lint already running for this tree`, replayed the first's output, and took its exit code |
| 2026-09-02 | - | `bgp { policy { loop-detection NAME { ... } } }`, `prependDefaultFilters` (`internal/component/bgp/config/peers.go`) | Configuring any `loop-detection` entry appears to reject every inbound route on every peer. `prependDefaultFilters` prepends the entry NAME to every peer's import chain, and no plugin declares `FilterTypes: ["loop-detection"]`, so `canonicalizeFilterRefs` cannot canonicalize it. `PolicyFilterChain` then calls `policyFilterFunc` with `pluginName` = `loop-detection`, `Server.CallFilterUpdate` answers `unknown plugin`, and the `FilterOnError` default is `OnErrorReject` (`internal/component/plugin/server/server.go`), so the whole table is dropped fail-closed. The in-process `LoopIngress` filter that does the real work is registered through `filterapi` and never needed the config name at all, so the config block adds nothing and costs everything. Nothing in `test/` configures `loop-detection`, which is why no suite sees it. Found while researching role-derived filter attachment for spec-bcp194-7-transit-asn | not fixed, and NOT reproduced at runtime: every producer above was read, no daemon was run. Whoever takes it decides which half is wrong, since `applyLoopDetectionConfig` reads the same ref to set `ps.LoopDisabled` and that IS wired. Either the name must resolve to a declared filter type, or `prependDefaultFilters` should stop injecting a name whose only consumer is the `inactive:` check in `filterChainContains` |
| 2026-09-02 | - | `.github/workflows/codeql.yml`, the `config:` block passed to `codeql-action/init` | `paths-ignore: gokrazy/modcache` is declared once for both matrix languages, but GitHub applies `paths` and `paths-ignore` only to an interpreted language, or to a compiled language analyzed WITHOUT building it. The Go job runs `build-mode: manual` with three `go build ... ./...` steps, so the exclusion is inert there and what gets extracted is whatever those builds compile. `./...` compiles `internal/test/fixture`, which no shipped binary links because its only importer sits behind the `ze_test` tag (`cmd/ze/ze_test_register.go`), and that package produced 26 of the 63 open alerts on the code-scanning page. A reader of the workflow sees an exclusion mechanism that does not exist for the language it is written under. Found while dismissing the code-scanning backlog | not fixed. The lever that works for a built Go analysis is the build step, so the repair is to analyze the shipped commands rather than `./...`, which extracts what actually ships and drops the fixture package on its own. Whoever takes it decides what coverage that loses, since `./...` also reaches `internal/le` and `pkg/plugin`, and checks whether the `gokrazy/modcache` entry earns its place in the javascript-typescript job before deleting it |
| 2026-08-31 | - | `announce flowspec destination-ipv6` and `source-ipv6` | The model advertises a form the encoder refuses. `flowspecFamilyName` (`internal/component/bgp/plugins/cmd/announce/announce.go`) sniffs the next token for a `:` only after the literals `destination` and `source`, so the `-ipv4` and `-ipv6` spellings fall through to the `ipv4/flow` default. `parsePrefixComponentText` (`internal/component/bgp/plugins/nlri/flowspec/plugin_encode_text.go`) then rejects the v6 prefix, and the operator who typed the keyword the usage line offered gets a family error. The keywords are declared in `grouping flowspec-components` (`ze-flowspec-cmd.yang`) and accepted by `isComponentKeyword`, so every layer agrees except the one that picks the family. Found during a read of the flowspec vocabulary for the R9 grammar findings | not fixed. Derive the family from the component keywords the codec already classifies, rather than from two literals and a colon: `kwDestinationIPv6` and `kwSourceIPv6` name the family outright, and a v6 prefix after the bare `destination` is the only case that needs the colon. Whoever takes it checks whether `update text ... nlri ipv6/flow` reaches the same picker, since that path names the family itself |
| 2026-08-31 | - | `traffic-class` as a FlowSpec match component | Two parsers hold one vocabulary and disagree. `config_builder.go` (`internal/component/bgp/plugins/nlri/flowspec`) maps `traffic-class` to `NewFlowDSCPComponent`, and `isFlowSpecComponentKeyword` (`internal/exabgp/bridge/bridge_command.go`) lists it as a component keyword. `plugin_encode_text.go` contains the string zero times: there is no `kw` const and no `parseComponentText` case, so `EncodeFlowSpecComponents` answers `unknown component: traffic-class` for the text path. A config block accepts the word and an API line refuses it. `test/encode/flow-encode.ci` uses it only inside config-block routes, which is why the suite is green. Found beside the row above | not fixed. The decision is which spelling is the real one: RFC 8956 Section 3.7 names Type 11 Traffic Class for IPv6 where RFC 8955 names it DSCP, and the codec already carries `dscp`. Either the text path gains the alias or the config path drops it, and the answer belongs with the plugin that owns the vocabulary |
| 2026-08-31 | restore-bespoke-interop-assertions (AC-5) | `test/interop/scenarios/bgp-med-remove-configured-gobgp/inject.msg`, the raw injector container | `scenarioPeers` (`internal/le/interoplab/bgp/prepare.go`) starts a `ze-test peer` injector at host 9 for every scenario directory holding an `inject.msg`, and that peer LISTENS on 179 rather than dialing. This scenario's `ze.conf` declares two peers, `frr-source` at .3 and `gobgp` at .5, and none at .9, so ze never dials the injector and its script never fires. The file announces the same 10.61.0.0/24 and 10.61.1.0/24 that `frr.conf` redistributes with `set metric 100` and `set community 65005:1`, so the directory reads as though the injector were the source while FRR is. Cost: one container and a 300 s script per run, and a reader who takes the injector's raw hex NEXT_HOP as the address the assertions read. The deleted Python checker (`a374622db~1`) imported no injector helper and asserted FRR's 172.30.0.3, which is the measured evidence that FRR is the source | not fixed. The restored checker asserts FRR's address, derived from the selected network, and its comment says why no base-network guard is owed. The repair is to delete the unused `inject.msg` and `inject-args`, or to declare the injector peer in `ze.conf` if it was meant to be a second source |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `internal/component/radius`, exported symbols | `./le repository check` reports three exported symbols in `internal/component/radius` with no cross-package non-test caller: `EncodeUserPassword` and `EncodeVSA` (`attr.go`) and `VerifyResponseAuth` (`packet.go`). Each is called only from `client.go` inside the same package, and only tests in that package reach them otherwise. Same shape and same cause as the ike engine row of 2026-08-14: the check reads CHANGED files, so the condition was invisible until an unrelated edit walked into it. Two neighbours in the same files ARE legitimately exported, `VerifyCoARequestAuth` and `VerifyCoAMessageAuthenticator`, both called from `internal/component/l2tp/plugins/authradius`, which is why the family reads as symmetric and hid the three. Found while landing the RFC 2865 NAS obligations, which edited `attr.go`, `client.go` and `packet.go` | not fixed. Two symbols the same review flagged WERE fixed here because they are in the same class and were named as blocking a gate: `VerifyResponseMessageAuthenticator` (`packet.go`) and `SubscribeEventBus` (`authradius/acct.go`) are now unexported, each having had exactly one in-package caller. The remaining three are PRE-EXISTING and the coordinator scoped this session out of them, so they stay for a deliberate pass. Unexporting them is mechanical, and the one thing to check first is whether a session landing RADIUS work is about to add a cross-package caller |
| 2026-08-30 | site-renderers-in-go (phase 10) | `internal/le/site/search.go`, the published search index | `searchRecord` carried two fields that were written and never read. `discoveryOrder` was set to the walk position of every record on the line above the sort, and `discoveryIsPage` was set true for every page record. Neither is read anywhere in the package, in any test, or in the published JSON, since both are unexported. `discoveryOrder` is the compound tie-break key the spec named as the ALTERNATIVE to a stable sort, and the code took the stable sort instead, so one property had two mechanisms and only one of them ran. A reader meeting the assignment loop directly above `sort.SliceStable` would reasonably read the sort as using it. Found while writing the AC-13 ordering tests, which needed the tie order to have exactly one producer to test | fixed on the spot. Both fields and both assignments are deleted, and the sort is named `sortSearchRecords` with its stability and its reason stated in a doc comment, so the index has one ordering mechanism and one place that documents it. `TestTwoSearchRecordsSharingAURLKeepTheDiscoveryOrder` drives it with forty records over four URLs and goes red against `sort.Slice`. The fixture is forty because Go sorts twelve or fewer by insertion, which is stable by accident, so a smaller case would pass against the unstable sort and prove nothing |
| 2026-08-30 | site-renderers-in-go (phase 10) | `internal/le/site/redirect.go`, the AC-12 legacy-URL pass | `rewriteLegacyPublicURLs` had NO caller. The retired build ran it over every published page and every Markdown mirror (`step_links`, `website/tools/build.py`) and the Go port stopped at the redirect stubs, so five absolute URLs in `docs/history.md` were published pointing at addresses only a stub answers, and the mirror a machine reader follows carried them with no stub in the path at all. Nothing saw it: a stub IS written for each of those addresses, so every link resolves in a browser and the coverage arithmetic never counts a stub. Found beside it, and separately dead: `legacyDocsDestinations` and `legacyDocsDestination` in the same file. Its five values are identical to `docsDestinationExact`'s, so `legacyDocsDestination(source)` answers the CURRENT destination for those five and `"docs/" + stem` for every other, which is already the first element of the loop that reads it. Both branches hit `if retired != destination` or set a row that exists, so the whole mechanism contributes nothing to the table | the rewrite is FIXED and wired: `renderProducers` now runs three passes, page producers, then `rewriteArtifactLegacyURLs`, then the producers that read the finished artifact, so the search index and `llms-full.txt` inline the address a reader reaches. `TestTheLegacyRewriteRunsBeforeTheDerivedProducers` and `TestTheLegacyRewriteReachesEveryPageAndMirror` hold it. The dead map is NOT fixed: it is outside phase 10's package and removing it changes the derivation of a 177-row table another phase owns. Whoever picks it up decides one thing, whether those five sources ever had a SECOND retired address the Python knew and this port lost, by reading `page_registry.url_redirects` at `eae282592^` |
| 2026-08-28 | - | bgp RFC 2545 Section 3 link-local next hop, `internal/component/bgp/reactor` | `TestDefaultOriginateAppendsLinkLocalWhenSection3Holds` and `TestSendAnnounceAppendsLinkLocalWhenSection3Holds` (`peer_initial_sync_test.go:529` and `:551`) are RED at HEAD (2958f7a18). Each asserts a 32-octet MP_REACH next hop carrying the global address then the link-local one, and the UPDATE on the wire carries the 16-octet global address alone (`\x80\x0e\x16\x00\x02\x01\x10` against the expected `\x80\x0e&\x00\x02\x01 `). Reproduced in a clean detached worktree at HEAD, both with the full feature tag set and with no tags, so it is not a working-tree or build-tag artifact. Found while running the bgp suite to check a lint-only change | not fixed, and not characterised further. `plan/journal/invariant-enforced-by-an-absent-call-site.md` row 2026-08-23 records the neighbouring RFC 2545 gap (no re-advertisement when the shared-subnet condition flips) and is a different surface: that row is about a stale 32-octet next hop, this one is about the 32-octet next hop never being built |
| 2026-08-24 | qemu-targets-boot-the-shipped-kernel | `gokrazy/kernel/runtime.config`, read as the platform ze's own MPLS code runs on | ze ships MPLS forwarding and a kernel with no AF_MPLS table. `dumpMPLSRoutes` (`internal/component/mpls/forwarding_linux.go`) calls `netlink.RouteList(nil, netlink.FAMILY_MPLS)`, and `addMPLSSwap` / `delMPLSSwap` (`internal/plugins/fib/kernel/mplsentry_linux.go`) install the in-label keyed swap and pop entries LDP and RSVP-TE produce. `grep -rn MPLS gokrazy/kernel/ tools/kernel-builder/common/` returned NOTHING: not one MPLS symbol in any fragment or require manifest. So every one of those calls fails on the appliance, and `docs/architecture/mpls/mpls-kernel.md` documents a design the shipped kernel cannot execute. Nothing saw it because the only lab that would have, `ze-qemu-ldp-frr-test`, booted stock Alpine, which carries MPLS. Moving it onto ze's kernel produced the answer on the first run: FRR zebra logged `Disabling MPLS support (no kernel support)`, rejected `mpls ldp` and every line under it, and the test timed out with `ze did not reach an operational LDP session with FRR`. Same class as CONFIG_NF_TABLES_INET, the qdisc set and CONFIG_DUMMY, all three found the same way | fixed at the config: `CONFIG_MPLS`, `CONFIG_LWTUNNEL`, `CONFIG_MPLS_ROUTING`, `CONFIG_MPLS_IPTUNNEL` and `CONFIG_NET_MPLS_GSO` are `=y` in `runtime.config` and listed in `runtime.require`, so `enforce_required_symbols` (`tools/kernel-builder/build.py` (retired; now `internal/le/deployment/hostkernel.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->) fails the BUILD if any stops resolving rather than shipping a kernel that cannot program a label. `LWTUNNEL` is there because the first build with the manifest refused with `CONFIG_MPLS_IPTUNNEL did not resolve to =y`, BEFORE the compile: the manifest works. Proof of the fix is that `af_mpls.o`, `mpls_iptunnel.o` and `mpls_gso.o` are in the image and zebra no longer logs `Disabling MPLS support`. It is NOT that the lab passes: `TestLDPInteropFRR` is still red for a separate, PRE-EXISTING reason (see `test-against-broken-path.md`, same date), so do not read a green LDP lab as the evidence. The general point is the one that spec exists for: a lab on stock answers a question about stock, and a feature's kernel dependency is only tested by the kernel the product ships |
| 2026-08-24 | fixit-lint-blind-to-every-other-build-tag | flavor-driver guards, the retired `scripts/dev/lint_flavors_test.py` (current producer: `internal/le/`) | commit `207865ad4` landed the retired `scripts/dev/lint_flavors.py` (current producer: `internal/le/verify/lint/matrix.go`) and left its test file UNTRACKED. the retired `scripts/dev/python_tests_test.go` (current producer: `internal/le/`) globs `*_test.py` from the WORKING TREE, so the guards read green for the session that wrote them and do not exist for anybody else: a clean checkout runs none of them. `test_base_passes_match_the_lint_recipe` is among them, and it is the only thing that notices when the retired Makefile (current producers: `internal/le/` native action tables) recipe and `BASE_PASSES` stop agreeing, which silently resizes every flavor's scope | fixed. `e610cd182` tracked the file, so a clean checkout now runs the guards. The general shape outlives this instance: a test discovered by a WORKING-TREE glob is a test that exists only for the session that wrote it, and the gate reports green either way. `git ls-files scripts/dev (retired; current producer: `internal/le/`)/` naming the file is the one-line check. |
| 2026-08-24 | fixit-ipsec-peer-reload-ignored | ike engine config reload, `internal/component/ike/engine/register.go` | The IKE engine registered `OnConfigVerify` and `OnConfigure` and NO `OnConfigApply`. The SDK's default callback table binds `callbackConfigApply` to `marshalStatusOK` (`pkg/plugin/sdk/sdk_callbacks.go`), so a plugin with no handler answers config-apply with OK and calls nothing. Every IPsec reload therefore verified the operator's edit, reported success, logged `config reload completed`, and applied nothing: `reconcilePeers` was reachable from startup and from operator `clear` alone. Measured on a running daemon, SIGHUP produced neither `ike: stopping peer` nor a second `ike engine configured`. It hid two further defects for as long as it lived, because a guard nobody runs gives no wrong answer for anyone to notice. The general point: a plugin API whose UNREGISTERED default is success cannot be audited by reading the plugin, and nothing in the tree compared the handler set against `WantsConfig` | fixed. `OnConfigApply` is registered and `ikeConfigStaging` carries what verify parsed across to apply, because the apply request carries diff sections rather than the configuration. An apply with nothing staged returns `errIKEApplyWithoutVerify` rather than OK, since both phases pick participants with one predicate and the stash is single-use. `TestIKEConfigApplyWithoutVerifyIsRefused` covers both refusals and `test/ipsec/ipsec-peer-reload-applies-selectors.ci` drives a real SIGHUP. What is NOT fixed and belongs to whoever picks it up: nothing yet detects the next plugin that declares `WantsConfig` and registers no apply handler |
| 2026-08-23 | - | `environment.mcp` port default, `extractMCPBlock` in `internal/component/config/loader_extract.go` | `internal/component/mcp/yang/ze-mcp-conf.yang` declares `refine port { default 8080; }` for `environment/mcp/server`, and the daemon never applied it: `extractMCPBlock` passed an EMPTY default port to `extractServerList`, so an entry that omitted the port carried `Port ""`, `ExtractMCPConfig` returned ok=false, and the daemon started NO MCP listener. `environment/web`, `environment/gnmi` and `environment/api-server` each pass a real default, so mcp was the only service whose schema promised a port the daemon ignored. The operator who followed the schema was then REFUSED by `MCPServersMissingPort` on both `ze config validate` and `ze doctor`, told to write the port the document had told him he could omit | fixed on the owner ruling of 2026-08-23, which chose agreement with the schema over retiring the refine: `extractMCPBlock` passes 8080, `RegisterListenerDefault("mcp", ...)` returns to `listener_defaults.go` so `ze doctor` probes the same endpoint, the `listener/mcp` inventory row moved from excluded to covered, and the missing-port refusal is deleted with both its operator surfaces. An upgrade now starts MCP on 127.0.0.1:8080 for an enabled mcp block that names no port, which the release notes owe. That block already asked for an MCP server, so nothing starts that the operator did not request |
| 2026-08-23 | fixit-stored-route-relay-hardening | plugin respawn, `internal/component/plugin/process/manager.go` and `internal/component/plugin/types.go` | `ProcessManager.Respawn` returns before doing anything unless the plugin config sets `RespawnEnabled` or `Respawn`, and NOTHING outside a test sets either. `plugin.PluginConfig` declares both fields, `cmd/ze/hub/main.go` copies `pc.Respawn` out of a `plugin.PluginConfig` nobody wrote, no YANG leaf reaches them, and grep over the tree outside `_test.go` finds no other writer. So the whole respawn machinery is inert in a shipped daemon: the per-window limit, the cumulative limit, the disabled set, the crash report and the restart counter. Its one non-test caller is `Server.restartPlugin` (`reload_tx.go`), which a reload rollback reaches when a plugin acks CodeBroken, so broken-plugin recovery has never run in production and reported success every time | not fixed, and deliberately: wiring an operator-facing `respawn` option is a feature with a YANG leaf, a config path and a doc, not a line fix, and the spec that found this owed only that the MECHANISM be correct. That half landed: a restart now re-runs the 5-stage handshake, and `Respawn` answers `ErrRespawnNotEnabled` instead of a bare nil. The next reader should decide with Thomas whether respawn is an operator option or should be removed; a limit nobody can reach is not a safety property |
| 2026-08-22 | record-answers-1-sdk-path | plugin SDK, `pkg/plugin/rpc` bridge | `DirectBridge.HasDispatchCommandAnswer` was exported with a test as its only caller, and it survived two reviews because eight sibling `Has*` accessors made it read as a symmetric family, while its own doc comment argued that deleting it would leave the registry drift guard blind to the answer slot. The wiring gate could not see it either: `check_cross_package_wiring` in the retired `scripts/dev/validate.py` (current producer: `internal/le/doc/wiring/checks.go`) collects symbols only from changed files under `internal/` and `cmd/`, and the move to `pkg/` had just taken the symbol out of that population. `CheckRowArity` was exported by the same move with no caller outside its own package | Counted the family instead of arguing about it. Eight of the nine accessors guard exactly one product call each in `pkg/plugin/sdk/sdk_engine.go` and this one guarded none, so it was the member with no caller rather than a set that works this way. The comment's premise was made false first: `TestWireBridgeDispatchInstallsTypedSlots` was given the real dispatcher `TestEngineOpJSONAndDirectMatch` already builds in the same file and now proves the slot by USING it, after which the accessor was deleted. `CheckRowArity` is unexported again |
| 2026-08-22 | fixit-child-rekey-answer-vs-installed-selectors | ipsec interop, BGP redistribute | `test/interop-ipsec/scenarios/ipsec-bgp-redistribute-frr` fails with `FRR route 10.200.0.0/24 did not appear within 30s`. Every IPsec assertion before it PASSES: the FRR BGP session is Established, strongSwan's SA and Child SA are up, both containers hold an ESP XFRM SA, and Ze holds the XFRM policy for 10.200.0.0/24. So the tunnel and the policy exist and the prefix derived from them never reaches the peer's BGP table. Reproduced twice, deterministic, once inside a full suite run (18 of 19 passed) and once alone with `NO_BUILD=1`. It is not caused by the Child SA rekey change that found it, which touches `applyChildRekeyResponse` and `newRekeyedChild` (`internal/component/ike/engine/rekey.go`) and no redistribution path. The lab compiles the whole working tree, which carries several other sessions' uncommitted files | not fixed. It blocks no goal of the spec that found it, whose interop proof is scenario initiator-rekey-answer-narrows. The next reader should confirm it at HEAD in a clean checkout first, since a shared working tree can supply the cause, and then read what announces an IPsec-derived prefix into BGP |
| 2026-03-30 | - | wiring | feature code exists, unit tests pass, but no user action reaches it | filled wiring test table and wired entry point |
| 2026-03-31 | - | wiring | looking-glass decorator code existed but was not wired | wired decorator to entry point |
| 2026-08-10 | - | bgp-adj-rib-in | bgp-rpki re-dispatched its RFC 6811 Section 4 re-validation decisions and the Adj-RIB-In dropped every one. Accept and reject looked in the pending map only. An installed route kept the state it arrived with, and an Invalid route stayed installed under `invalid reject`. The producer's tagged test stopped at the dispatch channel, and one consumer test asserted the drop as correct | added `applyToInstalled` and called it from all four consumer entry points, corrected the test that asserted the drop |
| 2026-08-18 | - | traffic control qdisc config | the `qdisc-type` enumeration offered `clsact` and `ingress`, and `translateQdisc` (`internal/plugins/traffic/netlink/translate_linux.go`) built both at `Parent: HANDLE_ROOT` with handle 1:0. Both attach at the ingress hook instead of the root, they carry no classes, and `newQdiscSnapshot` (`internal/plugins/traffic/netlink/snapshot_linux.go`) already refused to own a clsact because this backend cannot reproduce its state. The mirror path builds the correct shape next door, at ffff:0 with `HANDLE_CLSACT` (`clsactQdisc`, `internal/plugins/iface/netlink/mirror_linux.go`), and shares that hook with the sampling path | fixed by deleting the offer rather than by correcting the attachment: both values leave the enumeration, `translateQdisc` refuses them with a message naming the hook, and `raiseQdiscType` still names them when the backend READS an interface. `TestTranslateRejectsIngressHookQdiscs` |
| 2026-08-11 | fixit-route-removal-protocol-blind | iface netlink backend | every log call in `internal/plugins/iface/netlink` reached `slogutil.DiscardLogger()`. `init` in `ifacenetlink.go` was the only `loggerPtr.Store` in the package and the package registers through `iface.RegisterBackend`, so no `ConfigureEngineLogger` callback ever replaced it. 14 sites went nowhere, the AC-5 upgrade-orphan WARN among them, and the AC-5 test passed because it installed its own handler | replaced `loggerPtr` with `slogutil.LazyLogger("iface.netlink")` and added `TestRemoveRouteMissWarnsThroughTheProductionLogger`, which asserts the record reaches `slogutil.GlobalLogRing()` and so cannot be satisfied by a handler a test installs |
| 2026-08-11 | fixit-route-removal-protocol-blind | iface vpp backend | the same defect in the sibling backend: `init` in `internal/plugins/iface/vpp/ifacevpp.go` stores `slogutil.DiscardLogger()` and the package exposes no setter, so its 4 log sites are silent in production, including the Error that reports a panic in a VPP event handler. `internal/plugins/firewall/vpp` and `internal/plugins/traffic/vpp` name their own subsystem and do not have it | not fixed, outside this review round's scope; the netlink fix above is the pattern to copy |
| 2026-08-11 | - | appliance runtime kernel | `ze-iface-conf.yang` offers `gre`, `gretap` and `ip6gre` tunnel kinds with full parameter containers, and `internal/component/iface` creates them, but NO kernel config in the repository enables GRE: `CONFIG_NET_IPGRE` appears in neither `gokrazy/kernel/runtime.config` nor `tools/installer-kernel/*.config`, and `runtime.config` is a fragment that enables features explicitly. So an operator can configure a GRE tunnel, `ze config validate` accepts it, and the appliance answers `create tunnel (kind gre): operation not supported`. Found because `test/reload/tx-iface-tunnel-remove.ci` declares one: its failure cascades through `deliverConfigRPC` and stalls every BGP plugin at declare-capabilities, so reload 37, 38 and 39 fail together on one missing kernel option | not fixed. Adding `CONFIG_NET_IPGRE` to the runtime kernel is a product decision (image size, attack surface) and belongs to the owner. Until then three QEMU reload tests cannot pass, and the tunnel kinds should not be offered as if they work |
| 2026-08-10 | fixit-route-removal-protocol-blind | l2tp ppp teardown | `IfaceBackend` in `internal/component/l2tp/ppp/session.go` documents `gateway == ""` as "onlink via dev" and no backend implements it: `(*netlinkBackend).RemoveRoute` rejects an unparseable gateway before it reaches the kernel. The IPCP teardown in `ncp.go` passes exactly that, so the peer-route teardown fails on every session against the real backend and is logged at Debug. The fake backend in `helpers_test.go` accepts any string, so the unit tests are green | not fixed, needs the onlink form implemented (RTA_GATEWAY omitted, scope link) or the contract corrected and the caller changed |
| 2026-08-11 | fixit-bgp-shutdown-cease-notification | chaos peer sender, isis circuit | `./le changed scope` is RED at HEAD on `unused`, in packages this spec does not touch: `buildEORIPv4Unicast` (`internal/chaos/peer/sender.go`) and `(*Circuit).advertisedHoldTime` (`internal/plugins/isis/circuit/circuit.go`) have no caller. Both directories are clean in the working tree, so this is not another session's half-finished edit. The same run also flags `writeAVPUint8` and `readAVPUint8` (`internal/component/l2tp/avp.go`), but `internal/component/l2tp/` carries 19 modified files from a concurrent session, so those two are that session's in-flight state and not a HEAD defect | not fixed. Deleting a helper decides whether the feature it was written for is abandoned, and `internal/plugins/isis/` is the surface of a spec another session has claimed |
| 2026-08-11 | - | qemu-nightly workflow, ze-kernel-build | `b38706464` (2026-08-07) put both QEMU functional targets on ze's runtime kernel. It made a staged `tmp/kernel/vmlinuz` a hard precondition (`ze-qemu-kernel-guard`, the retired `mk/test-integration.mk` (current producer: `internal/le/integration/gates.go`)) and changed no workflow. `.github/workflows/qemu-nightly.yml` is the ONLY home of the `option=needs-linux:caps=` tests. On a hosted runner `~/.cache/ze` starts empty. So from the 2026-08-08 nightly every scheduled run died in about a minute on `tmp/kernel/vmlinuz not found` and ran zero tests. Job-level `continue-on-error: true` reported each of those runs as `success`, so the only visible signal was the job badge. The `needs-linux` job has in fact never passed since the workflow was created on 2026-07-27. The runs up to 2026-08-03 took 45 to 55 minutes and failed on real in-VM results. 2026-08-06 and 08-07 failed earlier still. Their cause was a compile break in `internal/component/ssh/ssh.go` that mixed `github.com/charmbracelet/ssh` with `charm.land/ssh` (since fixed). Two guards existed and neither one saw this. `TestQemuNightlyIsScheduledAdvisoryAndRunsTheLinuxOnlySuite` pins that the workflow CALLS the target. `TestQemuTargetsGuardTheStagedKernel` (the retired `scripts/evidence/qemu_kernel_wiring_test.go` (current producer: `internal/le/deployment/`)) pins that the target CARRIES the guard. Neither asks whether the caller can satisfy it | fixed. `ze-kernel-build` is split at the boundary the guard needs. `ze-kernel-vmlinuz-stage` (the retired `mk/build-gokrazy.mk` (current producer: `internal/appliance/cmd_build.go`)) materializes or builds and stages the kernel. `ze-kernel-build` keeps the gokrazy package assembly, which needs the retired `ze-gokrazy-deps-download` (current: `./le setup`) module cache that a VM boot never reads. The workflow runs the light half behind an `actions/cache` on `~/.cache/ze`, keyed on `gokrazy/kernel/**` plus `tools/kernel-builder/**`. It carries no `restore-keys`. `kernelCacheVariantFor` (`internal/appliance/cache.go`) keys the durable cache on the config fragments and the builder scripts, and NOT on `gokrazy/kernel/patches`. A prefix restore can therefore serve a tree built from another patch set. `timeout-minutes` goes 120 to 180 to leave room for a cold 30-minute kernel build. `TestQemuKernelPreconditionIsMetInTheSameJob` (the retired `scripts/dev/github_workflows_test.go` (current producer: `internal/le/`)) derives the guarded targets from the retired `mk/test-integration.mk` (current producer: `internal/le/integration/gates.go`) and the staging targets from the retired `mk/build-gokrazy.mk` (current producer: `internal/appliance/cmd_build.go`). It fails when a workflow JOB runs a guarded target and no target in that same job stages a kernel. Removal of the step reproduces the exact failure |
| 2026-08-11 | runtime-kernel-carries-every-tunnel-kind | iface tunnel helpers | two exported helpers in `internal/component/iface` had no caller at all, test or otherwise: `(TunnelKind).IsGREFamily` with its `greFamilyKinds` map (`tunnel.go`), and `TunnelKindNames` (`discover.go`). `golangci-lint` does not report either, because `unused` skips exported symbols; the retired `scripts/dev/validate.py` (current producer: `internal/le/doc/wiring/checks.go`) does, and it reads only files in the CHANGED set, so `TunnelKindNames` had been dead and invisible until this spec edited the file it lives in. `IsGREFamily` had no place left to be wired either: the `key` leaf it guards lives inside `container gre` in `ze-iface-conf.yang`, so YANG already refuses a key under a non-GRE case | both removed. Neither had a caller to preserve, and re-deriving the GRE-family question is one map away if a caller ever appears |
| 2026-08-12 | - | chaos peer sender, isis circuit, l2tp avp codec | resolves the 2026-08-11 `fixit-bgp-shutdown-cease-notification` row above, whose l2tp attribution was wrong. All three files are clean at HEAD. So all four `unused` symbols are HEAD defects, and none was a concurrent session's in-flight edit. Each was dead for a different reason. `buildEORIPv4Unicast` (`internal/chaos/peer/sender.go`) carried its own `Deprecated:` note naming the live replacement `BuildEOR("ipv4/unicast")`. It is a finished migration whose old half was never deleted. `(*Circuit).advertisedHoldTime` (`internal/plugins/isis/circuit/circuit.go`) is NOT the protocol defect it resembles. Both IIH producers, `buildLANHello` and `buildP2PHello` (`circuit/hello.go`), already set `HoldingTime` from the same precomputed `c.holdTime`. The holding time IS advertised, so only the redundant getter was dead. `writeAVPUint8` and `readAVPUint8` (`internal/component/l2tp/avp.go`) encode a shape RFC 2661 never defines. No AVP has a 1-octet Attribute Value. RFC 2661 line 2111 makes Proxy Authen ID 2 octets, and line 1110 makes Protocol Version two 1-octet fields inside a 2-octet value. The 1-octet fields that do exist belong to compound values, written by `writeAVPQ931Cause` and `writeAVPProxyAuthenID` in `avp_compound.go` | all four removed, with a comment in `avp.go` recording why no uint8 AVP helper belongs there. `./le changed scope` is green and the three packages' tests pass. the retired `scripts/dev/validate.py` (current producer: `internal/le/doc/wiring/checks.go`) reports one PRE-EXISTING issue this change neither caused nor fixed: `WriteAVPHeader` is exported with no cross-package non-test caller. That held at HEAD unchanged, because every caller sits in package `l2tp`. Unexporting it was tried and reverted: it pulls `avp_compound.go` into the changed set and reveals five more exported-dead value structs there (`ResultCodeValue`, `Q931CauseValue`, `CallErrorsValue`, `ACCMValue`, `ProxyAuthenIDValue`), which is a wider cleanup than this fix owns |
| 2026-08-12 | - | bgp egress, RFC 1997 well-known communities | Found during phase 3b of `plan/spec-bcp194-1-communities.md`, which stays open. Ze never honors NO_EXPORT, NO_ADVERTISE or NO_EXPORT_SUBCONFED on egress. `writeUpdateGated` (`internal/component/bgp/reactor/session_write.go`) consults the peer's configured export filter chain and never inspects community values, so a route carrying any of the three is re-advertised unless an operator adds a `filter_community_match` reject entry. `rfc/short/rfc1997.md` annotates RFC1997-Well-1, Well-2, Well-3 and Well-4 as `{gap}` and `docs/features/rfc-status.md` discloses it, so the gap is known and public rather than new. What is new is that a feature now DEPENDS on it: RFC 7999 Section 3.2 propagation guard added by this spec adds NO_EXPORT or NO_ADVERTISE to a BLACKHOLE-tagged route in order, in the RFC's own words, "to prevent propagation of the prefix outside the local AS". A conforming peer honors it, so the community is worth adding; Ze's own egress does not, so the purpose clause is unmet inside Ze. AC-19 of the spec states both halves and only the add half is implemented | fixed 2026-08-12, on Thomas's instruction, in its own change and not inside `spec-bcp194-1-communities`. `wireu.ScanWellKnown` reads the RECEIVED payload once per UPDATE and `WellKnown.AllowsEgressTo` (`internal/component/bgp/wireu/wellknown.go`) decides per destination; both forward rails ask it through `Reactor.wellKnownAllowsEgress` (`internal/component/bgp/reactor/forward_wellknown.go`). It did NOT become a `filterapi.FilterStageProtocol` filter, which was this row's guess: a filter registers from a plugin, and a plugin can be dropped, which would take an RFC MUST NOT with it. The row's other guess, "every peer outside the confederation", is right and is the RFC's own words: Ze names no confederation member-AS anywhere, so the boundary is the AS boundary. The word "received" in each clause bounds the check, and it is the reason the AS112 interop scenario still originates a NO_EXPORT route to an external peer. The four `{gap}` annotations are gone and `docs/features/rfc-status.md` now claims the RFC in full |
| 2026-08-12 | - | bgp rib show, received routes | A RECEIVED route's LARGE_COMMUNITY values reach no operator surface. `enrichRouteMapFromBundle` (`internal/component/bgp/plugins/rib/rib_attr_format.go`) emits `community` from the pool bundle and emits no `large-community` key at all, while its Adj-RIB-OUT sibling `enrichRouteMapFromRoute` in the same file emits all three widths. The pipeline has the matching hole: `newCommunityFilter` (`rib_pipeline.go`) parses one RFC 1997 value and `poolContainsCommunity` walks the buffer four octets at a time, and the filter table beside it offers `prefix`, `community`, `family` and `match` with no large-community entry. So `show bgp rib received` can neither display nor filter on RFC 8092 values that Ze parses, stores and forwards correctly. Found while writing `test/plugin/community-scrub-own-ga.ci`, whose first draft asserted large-community survival and could not read the answer | not fixed. It is observability rather than protocol behavior, and it is outside the ACs, files and phases of `plan/spec-bcp194-1-communities.md`. The `.ci` was rewritten onto standard communities, which the same scrub covers, and the large width keeps its unit proof in `TestScrubKeepList` and `TestScrubIgnoresForeignGA`. The fix is one key in `enrichRouteMapFromBundle` plus a `large-community` pipeline filter beside the existing one |
| 2026-08-12 | - | qemu + interop make targets | seven of the ten `ze-qemu-*-test` targets were invoked by nothing: not a workflow, not a script, not another target. `ze-qemu-ldp-frr-test` drove a real FRR ldpd peer in a VM and was named only by its own `.PHONY` line, by `make help`, and by the comment at the retired `mk/test-integration.mk` (current producer: `internal/le/integration/gates.go`) that EXCLUDED `internal/plugins/ldp` from `ZE_QEMU_INTEGRATION_PKGS` in its favor. Nothing had compiled that package under the `integration` tag since the exclusion landed, so `frr_interop_integration_linux_test.go` had rotted: `NewLIB`, `NewAdjacencyTable`, `Session.PeerAddresses` and `LIB.AllBindings` were all unexported or renamed, and the file did not build. The exclusion also dropped `TestWaitForInterfaceFoundResolves`, which needs no FRR at all. `ze-qemu-vrrp-keepalived-test` left VRRP with 150 unit tags and zero executed interop | exclusion removed (the FRR test already `t.Skip`s when zebra/ldpd are absent, which is how the unexcluded `internal/plugins/isis` sibling behaves), the four renamed symbols corrected, and `.github/workflows/qemu-nightly.yml` given two jobs: `protocol-labs` for the three stock-kernel labs and `runtime-kernel-labs` for the four that stage `tmp/kernel/vmlinuz`. `TestQemuAndInteropTargetsHaveACaller` (the retired `scripts/dev/github_workflows_test.go` (current producer: `internal/le/`)) now derives the targets from the retired `mk/*.mk` (current producer: `internal/le/`) and the callers from actual invocation, and refuses a `.PHONY` line, a printed `make help` hint and a `docs/` mention as callers |
| 2026-08-13 | - | pppoe access concentrator | Ze's PPPoE AC never asked a subscriber to authenticate. `handlePADR` (`internal/component/l2tp/pppoe/server.go`) built `ppp.StartSession` with no `AuthMethod`, so every subscriber reached `runNoAuthPhase` (`internal/component/l2tp/ppp/session_run.go`) with an empty Username. The rest of the auth path was fully built and wired around that hole: `startPPPoEAuthDrain` (`drain.go`) pumps the events, `ze-pppoe-conf.yang` had no auth leaf to set, and `l2tp-auth-local` answered `unknown user` for the empty name, so a PPPoE AC with a configured credential could not bring up ONE session. Discovery, which is all `test/pppoe/pppoe-basic.ci` covers, was healthy throughout | fixed. `auth-method` and `allow-no-auth` added to `ze-pppoe-conf.yang`, parsed into `Parameters` (`config.go`), carried to `InterfaceServer` and set on `StartSession`. `auth-method` defaults to `chap-md5`, matching the L2TP LNS, and `auth-method none` without `allow-no-auth true` is refused at startup. Proven end to end by `test/interop-pppoe/scenarios/02-ze-ac-pppd-client` against pppd 2.5.1 with the rp-pppoe plugin |
| 2026-08-13 | - | ppp session loop | `pppFrameReadBufSize` and `pppFrameWriteBufSize` (`internal/component/l2tp/ppp/session_run.go`) had no reader anywhere: every buffer in that file comes from `getFrameBuf`, which sizes from `MaxFrameLen` directly. `golangci-lint` does not report an unused CONST, so `./le changed scope` was green on the package throughout. Found while committing an unrelated change to the same package | both removed |
| 2026-08-13 | - | external plugin YANG | an out-of-process plugin can emit its YANG and the tooling reads it, but the DAEMON never sees it. `getExternalPluginYANG` (`internal/component/config/schema/cli/main.go`) execs a plugin with `--yang` and is called only from the schema CLI. The daemon builds its validator from `CollectPluginYANG` (`internal/component/plugin/inprocess.go`), which calls `GetInternalPluginYANG` and so reaches compiled-in plugins ONLY, never execing anything. So `ze schema` shows an external plugin's config model and `ze config validate` does not enforce it. Found while checking whether a README claim that a plugin extends the configuration model is true: it is true for a Go module compiled into the binary, and half true for the separate process the same sentence invites a reader to write | not fixed, and CORRECTED 2026-08-13 while reframing the README: there are TWO routes for an external plugin's YANG and BOTH are dead ends, so this is not a startup-ordering accident. The runtime handshake already harvests a spawned plugin's schema, `h.schema.Yang = input.Schema.YANGText` (`internal/component/plugin/server/subsystem.go`), and `SubsystemManager.RegisterSchemas` in the same file has **no non-test caller at all**, so that text reaches no validator either. The plugin declares its model over the protocol it already speaks, the server stores it, and nothing consumes it. CORRECTED AGAIN after a full end-to-end trace, and the reason is ORDERING, not a missing read: `LoadConfig` (`internal/component/config/loader.go`) builds its `pluginYANG` map from `CollectPluginYANG` and parses BEFORE it calls `ExtractPluginsFromTree` to learn which plugins to spawn, and `Parser.parseRoot` fails closed on an unknown top-level keyword. So a forked plugin's own config section is refused at parse, before the process that would declare its schema exists. The protocol declaration arrives after the decision it would inform, which makes the protocol route the FURTHEST from working, not the closest: it needs a two-phase load. The `--yang` exec is the nearest, because `getExternalPluginYANG` already works, `printYANG` already answers it, `LoadConfig` already threads the map, and a standalone parser for the `plugin { external }` block exists; the missing piece is a pre-pass, at the cost of one exec per plugin per boot and per SIGHUP plus a rule for a failed exec. Every fake plugin in `internal/test/plugins/` hides this: each registers into the PACKAGE registry at `init()` and is blank-imported under the `zetest` tag, so they prove the compile-time route while being described as plugins. `plan/journal/yang-module-not-in-schema.md` holds two rows of the same class whose recorded fix was adding the module at compile time, which is the workaround |
| 2026-08-13 | - | bgp dynamic peer groups | a dynamic group's `family` block never reaches the wire. `buildDynamicGroupSettings` (`internal/component/bgp/config/peers.go`) parses the group's timers, next-hop, local address and behavior flags, and never builds the Multiprotocol capabilities `parsePeerFromTree` builds for a static peer (`internal/component/bgp/reactor/config.go`). `buildOpen` (`reactor/session_negotiate.go`) therefore reads `configHasFamilies` false, and with no plugin supplying decode families ze's OPEN to a dynamic peer carries no MP capability at all. The negotiated family set is then empty, `sendInitialRoutes` iterates it and sends nothing, and a plugin's End-of-RIB is dropped by `logEORSuppressed` (`reactor/reactor_api_forward.go`) with reason `family-not-negotiated`. `ze config validate` accepts the group, and `test/parse/bgp-dynamic-peer-group.ci` covers exactly such a config. The FRR dynamic interop scenario does not see it because the plugins running there register decode families, which is the other branch of `buildOpen`. Found while writing `test/reload/reload-dynamic-peer-survives.ci`, the first functional test to establish a dynamic session: the End-of-RIB arrived in 7 runs of 8, from the plugin path racing the initial-sync drain, and the 8th is what exposed the empty family set | fixed 2026-08-13. The two SHARE one builder: `parseFamiliesFromTree` and `parseCapabilitiesFromTree` are now reached through `reactor.ApplyNegotiationConfig` (`reactor/config.go`), called by `parsePeerFromTree` and by `buildDynamicGroupSettings`, so the dynamic path repeats no loop. `buildDynamicPeerSettings` (`reactor/reactor_dynamic.go`) gained `RequiredAddPathFamilies` and `RefusedAddPathFamilies`: its field-by-field copy already carried the ADD-PATH capability, and advertising the capability while dropping the require/refuse set is a guard that fails open. Proven on the wire by `test/plugin/dynamic-peer-negotiates-configured-families.ci`, which asserts ze's whole OPEN and both End-of-RIB markers to a dynamic peer. Discrimination measured the same day: with the call removed, the OPEN arrives 12 octets shorter carrying ASN4 alone (`0025 ... 020641040000FFFD` against `0031 ... 0212010400010001010400020001...`), no End-of-RIB arrives, and that file is the ONLY one of the 629 in the plugin suite that goes red. What a dynamic peer still does not inherit is a separate list, in the report of this change: filters, static routes, `session > community > send`, `as-override`, `route-reflector-client`, `cluster-id`, `accept-srv6-prefix-sid`, `link-local`, `capture`, MD5, BFD, process bindings, and the YANG schema defaults. THAT LIST IS THE DEFECT, and it was closed as a CLASS the same day: hand-maintained copy lists produced four dynamic-peer bugs, so both were replaced by inheritance. `reactor.ParseDynamicGroupTemplate` (`reactor/config.go`) sends the group's tree through `parsePeerSettings`, the parser a static peer uses, and `buildDynamicGroupSettings` is deleted; `peersAndDynamicGroups` (`config/peers.go`) walks the group's template beside the static peers, so routes, filter chains, loop-detection defaults, the cluster-id sync, the port override and the peer YANG defaults reach both; `buildDynamicPeerSettings` copies the whole template struct and writes only 7 named divergences (name, group, address, port, PeerAS, connection mode, IsDynamic). The severe one was the filter chains: no `ImportFilters` assignment was reachable from the dynamic path, so an IXP route server applied NO import or export policy to any member. Two guards keep the class closed. `TestDynamicPeerInheritsEveryPeerSettingsField` walks `PeerSettings` by reflection and fails on a field that is neither inherited nor in the divergence table with a reason; `TestDynamicGroupTemplateMatchesAStaticPeer` parses one config holding a static peer and a dynamic group stating the same leaves and fails on any field they disagree on. Proven at the decision by `test/plugin/dynamic-peer-applies-group-filters.ci`, which sends one UPDATE holding two prefixes and asserts `prefix-list modify ... accepted=1 rejected=1`; removing the dynamic filter branch makes it red. Two behaviours got STRICTER, both matching the static path: a group whose family states no `prefix maximum` is now refused (RFC 4486), and so is a hold time RFC 4271 Section 4.2 refuses |
| 2026-08-13 | bcp194-6-blackhole | fib kernel discard route | RFC 7999 honoring was correct at every Ze layer and inert on Linux. `buildRichRoute` (`internal/plugins/fib/kernel/nexthop_linux.go`) set `route.Gw` from the change's next-hop whatever the route type was, and `fib_create_info` rejects RTN_BLACKHOLE, RTN_UNREACHABLE and RTN_PROHIBIT carrying a gateway, a device or multipath. Every BGP path resolves a next-hop, so the netlink message was refused whole and the kernel held ZERO ze routes. No test saw it: `test/plugin/fib-blackhole.ci` is a blind hold, and before the route-type carry-through landed the only producer of a route type in the tree was the `fakefib` test plugin, which made the FIB's own test vacuous about the chain above it | fixed. `buildRichRoute` returns early for `routetype.Discards()`, at the one place the netlink message is built, rather than clearing the next-hop upstream where VPP and `show rib` both want it. `TestBuildRichRouteDiscardCarriesNoNextHop` pins the shape and `TestNetlinkIntegration_BlackholeRouteWithNextHop` programs a real kernel in an ephemeral netns and reads back exactly one RTN_BLACKHOLE. The interop scenario now asserts `ip route show` inside the container rather than a Ze table |
| 2026-08-14 | spec-web-templ-migration | web templates | two files in the embedded template FS are parsed by no renderer. `NewRenderer` (`internal/component/web/render.go`) names every file it loads and neither `templates/terminal.html` nor `templates/notification_banner.html` appears. Each one's markup lives on as a Go string literal instead. They sit in `notificationBannerSource` (`internal/component/web/sse.go`) and in `cli_terminal.go`. The two banner copies have already diverged. The FILE wraps the banner in a `div id=notification-bar` carrying `hx-swap-oob`. The Go literal emits the inner banner alone. The phase-1 golden capture found this when it enumerated the template FS | not fixed, recorded for the post-port fix pass |
| 2026-08-14 | spec-web-templ-migration | web leaf inputs | `templates/input/number.html` defines `input_number` and no field type reaches it. `fieldFor` (`internal/component/web/render.go`) and `RenderField` build the template name as `input_` plus `FieldMeta.Type`. For a numeric leaf `valueTypeToFieldType` (`internal/component/web/fragment.go`) answers `uint16`, `uint32` or `int`, never `number`. Each one misses and falls back to `input_text`, which reads neither Min nor Max. The bounds `buildFieldMeta` computes reach no markup, and the operator gets a plain text box. Seen in the captured `testdata/golden/component/detail--fields.html`, where the uint16 field renders as `input type=text` with no min or max | FIXED 2026-08-15 in the interface defect pass. `fieldInputs` (`internal/component/web/field_input.go`) keys `uint16`, `uint32` and `int` onto `inputNumber`, which are the strings `valueTypeToFieldType` (`fragment.go`) actually answers. The dead `number` and `text` keys are gone: no leaf carried either. `TestFieldInputRegistryCoversTheDeclaredSet` now checks every registry key against `producedFieldTypes`, so a registration no producer reaches is a red test rather than a silent fallback. `TestNumericLeafEditorRendersItsSchemaRange` proves the min and the max reach the browser. Seven fixtures moved from `type=text` to `type=number` with `inputmode` and the range |
| 2026-08-14 | fixit-dynamic-group-peer-config | bgp event accessors | `./le repository check` is red at HEAD on three exported symbols in `internal/component/bgp/event.go`. None has a cross-package non-test caller: `ParseFamilyOps` (:284), `Event.GetDirection` (:431) and `Event.GetPeerSelector` (:523 at HEAD). Every one is an accessor over the plugin event JSON. Each looks like a delivered read path an operator surface would use. None is reached. This surfaced when `Event.GetPeerGroup` went in beside them. `./le repository check` does NOT flag that one, because the rpki and rib readers consume it | not fixed. It is pre-existing and blocks no goal of the spec in hand. The choice between wiring each accessor and deleting it is a behaviour decision per symbol, not one edit |
| 2026-08-14 | - | peer process bindings | A peer's `process <name> { receive [ update state ] }` list changes nothing. `parseOneReceiveFlag` (`internal/component/bgp/reactor/config.go`) parses it into `ProcessBinding.ReceiveUpdate` and `.ReceiveState`, and the only production read of either field copies them into the answer of `GetPeerProcessBindings` (`internal/component/bgp/reactor/reactor_api.go`), whose sole wrapper `Coordinator.GetPeerProcessBindings` (`internal/component/plugin/coordinator.go`) has no production caller. What actually gates delivery is `SubscriptionManager.GetMatching` (`internal/component/plugin/server/subscribe.go`), fed from what each plugin declares for itself through `SetStartupSubscriptions`, with a nil peer filter. So `test/plugin/community-scrub-own-ga.ci` gives bgp-rib `receive [ state ]` and bgp-rib receives every peer's UPDATEs anyway, which is the only reason that fixture works at all. Found while debugging that fixture's flake, which the list does not cause | not fixed. The operator-visible half is a config leaf that silently does nothing, and choosing between honouring it and refusing it is a behaviour decision, not one edit |
| 2026-08-14 | fixit-ike-responder-natt-port-float | ike engine exported symbols | `./le repository check` reports three exported symbols in `internal/component/ike/engine` with no cross-package non-test caller: `ChildSA` (`child.go`), `GenerateSPI` and `GenerateNonce` (`sa.go`). The `engine.ChildSA` hits elsewhere in the tree are `ChildSAEvent`, a different symbol. All three are used inside package `engine` alone, so each wants to be unexported. The condition is PRE-EXISTING and was invisible. The check reads CHANGED files, so it fires only once somebody edits one. This closure edited comments in both files and nothing else. That is what surfaced it. Measured: the diff of `child.go` and `sa.go` holds zero non-comment lines | not fixed. Unexporting three symbols is a code change with its own blast radius. The goal of the work in hand does not depend on it (`ai/rules/rule-precedence.md`). It must not be folded into a closing commit that has to stay single-focus. The wider point is worth keeping. A gate scoped to changed files hides a whole population until an unrelated edit walks into it. The count this row records is therefore a floor, not a total |
| 2026-08-14 | - | web cli bar | `templates/component/cli_bar.html` defined `cli_bar` and nothing rendered it. `NewRenderer` (`internal/component/web/render.go`) parsed the file into the layout group and into the workbench group. Neither `page/layout.html` nor `page/workbench.html` held a `{{template "cli_bar"}}` action, and no Go call site named it. `TestRenderLayout` asserts the bar must NOT appear on a normal page, because `/cli` is the full terminal now. The markup carried the element ids that page still owns | not fixed. The port carried the markup across as `cliBar` in `component_cli_bar.templ`, unchanged and still unrendered. Deleting it decides whether the CLI page keeps those ids, which is a behaviour decision |
| 2026-08-14 | - | web config template map | Five of the eleven templates `NewRenderer` (`internal/component/web/render.go`) parsed into the config map reach no caller. `nodeKindToTemplate` (`handler_config_leaf.go`) named the only keys any caller looked up. It answers six node kinds. `breadcrumb.html`, `commit.html`, `notification.html`, `command.html` and `command_form.html` are none of them. `RenderConfigTemplate` was the other way in and had NO caller at all, test included. The commit review an operator reaches is the diff modal. The pending-change notice is the commit bar. Each dead file is a second spelling of a live one | not fixed. The port carried all five across as `configBreadcrumb`, `configCommit`, `configNotification`, `configCommand` and `configCommandForm`, unchanged and still unrendered. `RenderConfigTemplate` is deleted, because a method with no caller is not a feature. Deleting the markup decides which spelling of the review page survives, which is a behaviour decision |
| 2026-08-14 | - | web config flex view | `configFlex` (`config_flex.templ`) has no production caller. `configViewComponent` (`internal/component/web/handler_config_leaf.go`) answers nil for `config.NodeFlex`, so no request reaches it. The template it was ported from, `flex.html`, reads `Name`, `Value` and `LeafField`. The caller passes a `ConfigViewData`, which carries none of the three. Five more components are dead the same way and were dead at HEAD: `sidebar`, `dashboardOverview`, `terminalPage`, `notificationBannerOOB` and `cliBar`. Each has a golden capture and no other caller | not fixed. The flex panel is blank for the operator today, and `plan/journal/silent-fall-through.md` owns that defect. Deleting a component decides what a flex node must show, which is a behaviour decision |
| 2026-08-15 | - | bgp rib storage readvertise | `(*RouteEntry).ToWireBytes` (`internal/component/bgp/plugins/rib/storage/familyrib.go:772`) has no non-test caller. `gopls references` answers five sites and every one is a `_test.go` in its own package (`familyrib_test.go:299`, `routeentry_test.go:205`, `rfc4271_test.go:63,97,132`); its own doc names the live alternative, "for exact wire reproduction, use msg-id cache forwarding instead". Two public dispositions rest on it as though it were the readvertise producer: the `{gap}` on `RFC4271-5-5` in `rfc/short/rfc4271.md` says "ToWireBytes replays every stored OtherAttrs entry into the readvertised attribute section", and the RFC 4271 row of `docs/features/rfc-status.md` repeats the claim. Found while placing the `RFC4271-5-3` fix, whose own `{gap}` pointed at the same rail; the fix went to `reactor.Session.publishBase` instead, which is where a received attribute is retained and relayed | not fixed. The goal of the work in hand did not depend on it (`ai/rules/rule-precedence.md`), and it must not be folded into a single-focus commit. Two things are wanted and they are separable: the symbol needs a caller or deletion, and the `RFC4271-5-5` annotation needs re-deriving against the rail that actually re-advertises |
| 2026-08-15 | - | web config editor, every inline field | the vendored htmx restores focus and the caret after a swap. It records `document.activeElement`, `selectionStart` and `selectionEnd`. After the swap it re-finds the element by its own id (`assets/htmx.min.js`, the swap function `$e`). No editor supplied one. `inputText`, `inputNumber`, `inputEnum` and `inputBool` put the id on the FRAME around the editor (`fieldWrapper`, `input_wrapper.templ`). The list-table cell had none at all. The lookup was `getElementById("")`, which answers null. The library's recovery therefore ran and restored nothing. Each editor replaces itself under `hx-swap="outerHTML"`, so an operator lost the caret on every field of the config editor | FIXED 2026-08-15. `fieldInputID` (`internal/component/web/view.go`) derives the id from the leaf path. That path is the one value both renders carry. It is escaped into `[A-Za-z0-9_-]`, so a dotted key such as `eth0.100` survives a CSS selector. The five editors and the list-table cell all name it. Proven in a browser. `agent-browser` typed into the real markup, served with the real htmx. The caret came back at the same offset for the field, the table cell and the dotted key. Removing the id from that same page loses focus to `BODY`, so the proof discriminates. `TestSelfReplacingControlsCarryAStableID` (`markup_contract_test.go`) holds the markup in `./le verify current mode full` |
| 2026-08-15 | fixit-vacuous-functional-tests | mcp tools/list | `test/plugin/mcp-header-mismatch-rejected.ci` fails deterministically (0/3 isolated, 0/1 in the full plugin suite) on its FIRST line, `wait-tool ze_execute`: `error: wait-tool: "ze_execute" absent from tools/list after 10s`. The server starts (`MCP server listening on http://127.0.0.1:<PORT>/`), so the list is served and the tool is not in it. `ze_execute` is handcrafted rather than derived, at `toolNameExecute` (`internal/component/mcp/tools.go`), and `internal/component/mcp/` and `internal/test/cli/` are both CLEAN against HEAD in this working tree, so the cause is elsewhere in the 60+ Go files other sessions hold uncommitted. Its sibling `test/plugin/mcp-tools-call-stateless.ci` waits on the same tool and PASSES, so the list build is not globally broken and the two configs differ in something that decides it | not fixed. The session that met it was told not to edit `internal/component/plugin/`, the one uncommitted surface that feeds the command registry the list is built from, because another session holds work there. Next step: run 308 against a tree built from HEAD to separate "red at HEAD" from "red under another session's uncommitted work", then diff its config against 318's |
| 2026-08-15 | fixit-peer-process-event-filter | mcp tools/list | that next step was run: `git archive HEAD` into a scratch tree, the retired `ze-test-build` (current: `go build -o bin/ze-test ./cmd/ze`) there, 308 three times. **Red at HEAD**, 1 of 3, so no session's uncommitted work causes it. The timing separates the two outcomes cleanly: the pass took 2.8s and each failure took 10.8s, which is the `wait-tool` 10s timeout plus startup, so `ze_execute` is not missing from the descriptor set -- it is LATE, and the poll gives up. In the working tree the same fixture is 0 of 3, so uncommitted work makes a race lose more often rather than creating it | still not fixed, and now correctly classed: this is a readiness race in MCP tool registration, not an unwired feature. What publishes `ze_execute` into tools/list, and what it waits for, is the next read (`toolNameExecute`, `internal/component/mcp/tools.go`) |
| 2026-08-15 | web-htmx4-cutover | .wb url expectation | `expect=url:contains=` could only ever FAIL. `checkURL` (`internal/component/web/testing/expect.go`) read `b.Snapshot()`, the accessibility tree, which never carries the address: `agent-browser get url` is the only command that answers it. The directive was documented in `docs/architecture/testing/runner-architecture.md` and no `.wb` file used it, so nothing was red and the format offered a history assertion nobody could write. Found while writing `test/web/history-full-page.wb`, whose subject is a pushed URL | fixed. `Browser.getURL` issues `get url`, `checkURL` reads it, and it gained the `not-contains` form its siblings have. `TestURLExpectationReadsTheAddressBar` (`directives_test.go`) asserts the command through the fake agent-browser, so a reader that stopped asking the browser for its address is red |
| 2026-08-15 | web-htmx4-cutover | looking glass peer stream | `handleUIEvents` (`internal/component/lg/handler_ui.go`) sent `event: peer-error` with `engine unavailable`. NOTHING consumed it: `peers.templ` listed `sse-swap="peer-update"` alone, so under htmx 2 the message was dispatched and dropped, and under htmx 4 a named event swaps nothing at all. The same tick also mishandled the commoner failure: `query` returns a `{"error": ...}` envelope on a dispatch error, which parses, so `zeData` was not nil and the stream pushed an EMPTY table body with no message. An operator watching a frozen table was told nothing either way | fixed here. `peerStreamBody` reads `engineError`, the producer the page banner already used, and sends the reason as the table body's only row over the one unnamed message the stream now carries. `test/web/lg-stream-error.wb` proves it in a real browser against `ze-test lg`, whose dispatcher always fails |
| 2026-08-16 | - | ike remote-access virtual IP pool | The `remote-access` container of `ze-ipsec-conf.yang` parses, and `registerIKE` (`internal/component/ike/engine/register.go`) builds a real allocator from it: at the `cfg.RemoteAccess != nil` branch it calls `eap.NewPool(ra.Pool.Range, ra.Pool.Range6, ra.Pool.DNS, ra.Pool.Domain)` and stores it in `ipPool`. The variable is then discarded by a bare `_ = ipPool` in the teardown path, and `eap.Pool.Allocate` has no non-test caller anywhere under `internal/component/ike/`. So an operator who configures an address pool for remote-access clients gets a daemon that accepts the config, allocates nothing, and assigns no address. The wiki and the IPsec guide both advertised it as working | not fixed here. Found during the wiki sync and corrected in the documentation only: `feature-inventory.md` and `docs/guide/ipsec.md` now say the pool is not wired. The code remains as described |
| 2026-08-16 | - | interop scenarios | Three interop scenarios declare `bgp-rs` and attach it to no peer, so the route server receives no peer-scoped event and relays nothing. `PeerScopedProcs` (`internal/component/plugin/server/delivery_graph.go`) reads the attach bindings, and `filterPermittedPeers` (`internal/component/bgp/reactor/send_permission.go`) drops a destination whose bindings grant no `send update`. All three were red at HEAD: `bgp-route-server-frr` on a route that never reached BIRD, `bgp-rfc7606-relay-shape-frr` on one that never reached FRR, `bgp-self-nexthop-withheld-frr` on a barrier that never released. 61 also shipped in `480897faf` without the two injector fixtures its raw peer needs, so it could not run at all. Commit `06c95f65d` corrected 480 `.ci` fixtures for the same attach requirement, and the interop population was not among them | fixed. `attach process rs` on both peers of each scenario, and for 47 an `internal adj-rib-in` instance with the receive half on the source peer and the send half on the destination, because its route reaches FRR by peer-up replay rather than by live forward. Each scenario is green after the change and red before it on the same tree. The 61 fixtures are committed with it |
| 2026-08-16 | - | SessionStart identity publication | registration matched only startup, so resume, fork, clear, and compact did not republish identity | changed to an all-source matcher and added a registration fixture |
| 2026-08-16 | - | SubagentStart context injection | the hook printed plain stdout, which Claude logged but did not inject | emitted JSON `hookSpecificOutput.additionalContext` and added an entry-point fixture |
| 2026-08-17 | spec-rfc4271-med-ibgp-readvertisement | bgp normal selected-route egress | `bgp-rib` records selected Loc-RIB state but no normal BGP producer sends that selected route to peers. Route-server and route-reflector plugins forward cached received UPDATEs, and BGP-to-BGP redistribution is rejected by the same-protocol loop guard. The MED guard can be proven on the common egress boundary and route-server rail, but RFC4271-9.1.2.2-2 cannot be closed as normal BGP selected-route readvertisement proof | not fixed here. Blocked the spec on a normal BGP RIB-selected route-to-peer egress producer and restored the RFC gap |
| 2026-08-19 | spec-fixit-zefs-diff-structural-ops | cli pending-change review | `formatChangeEntry` (`internal/component/cli/model_commands_session.go`) renders one pending change as a text line and has no production caller. `cmdShowChanges` returns a status count plus a config view with `forceChanges`, `cmdShowChangesAll` returns a count per session, and neither calls it; `gopls references` gives three sites, all in `internal/component/cli/model_commands_test.go`. It predates the commit this spec closes | not fixed here: found while proving the review renders a structural delete, and out of the spec's scope |
| 2026-08-17 | spec-fixit-zefs-diff-structural-ops | config reader schema routing | `schemaInfo.namespace` and `schemaInfo.yang` (`internal/component/config/reader.go`) had zero references: `gopls references` on both fields returned nothing, and no `schemaInfo` literal in the package or its tests sets either. Their `(optional)` comments described a plumbing nobody ever wired. `golangci-lint` reported both as `unused`, reddening `./le changed scope` at HEAD | removed both fields |
| 2026-08-19 | verify-scope-2-change-set-selector | `examples/plugin/go` | A tracked, shipped-as-documentation Go module that NO gate compiles. It is its own module (`module example/acme-monitor`), so `go list ./...` never reports it and every `./...` pattern in the repository misses it. Searched the retired `Makefile` (current producers: `internal/le/` native action tables), the retired `mk/` (current producer: `internal/le/`), the retired `scripts/` (current producer: `internal/le/`), `.github/workflows/` and every `_test.go`: the only references anywhere are two doc comments, in `cmd_plugin_external.go` (`internal/test/cli`) and `sdk.go` (`pkg/plugin/sdk`). So the example a plugin author is pointed at can stop compiling against the SDK it demonstrates, and nothing in the tree says so. Found while justifying a selector rule for the path, where I had asserted that `cmd_plugin_external.go` builds it; it does not, and the assertion was mine rather than a report's | not fixed, and the selector no longer pretends otherwise: the path seeds no package, because seeding one would run tests that cannot detect a break in the example. The repair is a gate that compiles the module -- `go build ./...` inside `examples/plugin/go` is the whole of it -- and it belongs beside the other cheap structural checks rather than in this spec. Until then a change to the example is unverified by construction |
| 2026-08-19 | - | `.ci` tests needing `caps=net-admin` | The netns runner exists, works, and is pointed at ONE test. `ZE_NETNS_PLUGIN_TESTS ?= system-kernel-log-show` (the retired `mk/test-integration.mk` (current producer: `internal/le/integration/gates.go`)) is the whole default list, and the retired `ze-netns-plugin-test` (current: `./le qemu netns-test`) passes exactly that to `ze-test bgp plugin`. A `.ci` carrying `option=needs-linux:caps=net-admin` runs nowhere else either: it SKIPS under the retired `ze-functional-plugin-test` (current: `./le functional plugin`) on an unprivileged host, which is every host that does not opt in. Searched the retired `Makefile` (current producers: `internal/le/` native action tables), the retired `mk/` (current producer: `internal/le/`), the retired `scripts/` (current producer: `internal/le/`) and the test lists for `flowspec-fw-protocol-sctp` and `flowspec-fw-untranslatable-keeps-others`: no hit anywhere, so nothing runs either one, and a green suite says nothing about them. The gap is the CLASS, not these two: any capability-requiring `.ci` is unreached unless a person names it on the command line. Found while making those two tests pass end to end, where the only way to run them at all was to pass the name by hand | not fixed. It does not block the FlowSpec repair, which is verified by naming the tests explicitly, and the repair here is a decision rather than an edit: either the default list is derived from the tests that declare `caps=net-admin`, the way suites are derived elsewhere, or the netns target keeps a hand-written list and something has to fail when a test is absent from it. A hand-written list that nobody is obliged to update is what produced this row. Partly closed 2026-08-19: the two flowspec tests are in the default list and pass there, 3.7s and 6.9s. The wider decision stands. And the gap had already cost what it costs: `system-kernel-log-show`, the ONLY test the list held, fails on this host on its own, `timeout 1 [613]` after 15.0s, with every capability granted (`cap_net_admin,cap_net_raw,cap_net_bind_service,cap_dac_override,cap_syslog+ep`). Reproduced twice, alone and in the list, so it is not flaky and not caused by the two additions. A target whose entire population is one test, which nobody runs, went red without anybody learning. Narrowed the same day, and the first reading of it was wrong: the failure is NOT specific to that test. `add-remove`, an unrelated long-standing test, times out identically under the retired `ze-netns-plugin-test` (current: `./le qemu netns-test`), `TIME` at 10.0s. Both drive an EXTERNAL observer plugin (`tmpfs=<name>.run` plus a `plugin { external ... }` block), which 448 of the `test/plugin` files do. The two flowspec tests added here pass in the same runner and drive a `cmd=foreground:exec=python3 driver.py` instead, with no external plugin, which is the difference. So the suspect is the external-observer path under the netns runner rather than either test. NOT established: whether this predates 2026-08-19. It was asserted as pre-existing on the strength of two reproductions, both taken AFTER that day's commits, which shows it is not flaky and shows nothing about when it started. SETTLED the same day by running the whole suite: the retired `ze-functional-plugin-test` (current: `./le functional plugin`) reports `system-kernel-log-show` PASS at 36.8s and `add-remove` PASS at 7.5s, so BOTH pass under their normal target and fail only under `ze-netns-plugin-test`. Neither test is broken and neither failure is caused by that day's commits. A first reading blamed netns being slower, and the timings refuse it: `add-remove` completes in 7.5s normally and dies at its 10.0s deadline in netns, while `system-kernel-log-show` completes well inside its own `timeout=15s` normally and dies at exactly 15.0s there. These do not run slowly in netns, they never complete. The difference the netns path adds and the functional path does not is `setcap cap_net_admin,cap_net_raw,cap_net_bind_service,cap_dac_override,cap_syslog+ep` on the throwaway `ze`. UNVERIFIED lead, stated so nobody has to rediscover it: file capabilities set `AT_SECURE=1`, which makes glibc strip `LD_*` and similar variables from the environment of the process and its children, and every one of these tests starts an EXTERNAL observer through `plugin { external ... }` running a Python script that imports `ze_api`. A stripped variable would leave that import failing and the observer silent, which is exactly the symptom. Nobody has read the producing code path, so this is a lead and not a finding |
| 2026-08-19 | none (FlowSpec encode defects) | bgp config, attribute 25 | `buildIPv6ExtCommunityFromString` (`internal/component/bgp/config/loader_routes.go`) was the ONLY encoder of an RFC 5701 IPv6-address-specific community, it emitted a correct 20 octets, and nothing could reach it. Two places decided which form an `extended-community` string named. `ParseExtendedCommunity` ran first, `parseFlowSpecRedirectNextHop` returned nil for an IPv6 address, and the fall-through refused the whole string with `invalid extended-community "redirect-to-nexthop"`, so the route was rejected before the 20-octet builder ran. Attribute 25 could be configured by nobody through this surface, and only by hand-writing `attribute [0x19 0xc0 ...]`. It also had no caller in any test, which is why nothing noticed | fixed: `ParseExtendedCommunity` is now the one place that decides, and it returns the 20-octet communities in `ExtendedCommunity.IPv6Bytes`; both callers read that field and the dead builder is deleted. `TestParseExtendedCommunityRedirectToNextHopIPv6` drives it from an operator's config string. The class lesson is that a second decision point is what hid it: the encoder was reachable in one reader's model and unreachable in the code, and no test named it either way |
| 2026-08-19 | cli-pipe-aliases | show bgp command tree | `handleBgpOverview` (`internal/component/bgp/plugins/cmd/peer/summary.go`) answers `show bgp` with the payload `handleBgpSummary` gives `show bgp summary`, and a unit test asserts the two `Data` values are equal. But `registerColumns` and `registerAliases` (`internal/component/bgp/plugins/cmd/peer/peer.go`) both register against `cmdBgpSummary` alone, and `commandMatchesPrefix` (`internal/component/command/column_order.go`) refuses a prefix longer than the command it matches. So `show bgp` reaches neither table: it renders its columns alphabetically, and piping it through the `peers` alias answers `unknown pipe operator: peers` while the same alias on `show bgp summary` draws the peer table. Found closing the alias spec, one commit after `647f33121` made `show bgp` answer | RECORDED, not fixed, and the obvious fix is wrong. Re-registering both against `show bgp` leaks them: the registries resolve by longest prefix, and `show bgp rib`, `show bgp rpki`, `show bgp rs` and `show bgp healthcheck` declare no alias set of their own, so the `summary` and `peers` aliases would attach to route output. The repair needs an exact-path registration, which no registry in `internal/component/command` has, so it is a design decision rather than an edit |
| 2026-08-21 | - | test plugin client, execute-command | `plugin-command-completion.ci` assigned `api._execute_command_handler` and nothing read it. `_handle_callback` (`test/scripts/ze_api.py` (retired, no successor) <!-- doc-links: ignore (deleted 2026-08-28 by eae282592 with no replacement) -->) carried no execute-command branch. `read_line` answered any method its own list does not name with a bare ok. So a Python plugin declares a command over Stage 1 and CANNOT answer it with data. Every `.ci` that needs a payload from a plugin uses a compiled-in Go one, which is why the gap stayed invisible. The doc comment on `_handle_callback` states the obligation one way only: every method `read_line` answers must be answered there too. Found writing `test/plugin/plugin-pipe-alias.ci`, whose assertion is a selection over the plugin's own payload | fixed. `on_execute_command` registers the handler. `_handle_callback` answers with the status and data shape `ExecuteCommandOutput` reads. `read_line` routes the method there rather than to the trailing bare ok. `plugin-command-completion.ci` takes `(command, args)` and registers through `on_execute_command` |
| 2026-08-21 | - | meta command help | `handleBgpCommandHelp` (`internal/plugins/meta/cmd/help.go`) read `Dispatcher().Lookup`, which is the BUILTIN table alone, and answered `unknown command` for every command any plugin declares. `lookupCommandHelp` (`internal/component/plugin/server/system.go`), which serves the `system` namespace, reads the plugin command registry after the builtins and answers. So the verb-first surface an operator is told to use could describe none of the commands plugins add, while the noun-first one could. Found writing the alias listing that surface owes | fixed. The handler reads `Dispatcher().Registry().Lookup` after the builtins, and reports the plugin's name as the source beside the usage hint the declaration carried. `test/plugin/plugin-pipe-alias-help.ci` asks for the help of a command an external Python plugin declared |
| 2026-08-21 | - | ze cli interactive TUI | A pipe alias a plugin declares is registered in the DAEMON, and `ze cli` with no command argument runs its Bubble Tea model in the CLIENT process: `runInteractiveSession` (`internal/component/cli/client/main.go`) builds it with `tea.NewProgram`, and `executeOperationalCommand` (`internal/component/cli/model_mode.go`) resolves the chain with `command.ProcessPipesDefaultFormatChecked` before it sends anything. So on that surface the declared name is neither offered by Tab nor resolvable, and an operator reads `pipe error: unknown pipe operator: totals`. The compiled-in aliases work there, which is why nothing noticed: Tab after the pipe character on `show bgp` offers `summary` and `peers` in the same client. The two surfaces that DO answer are the SSH exec channel, where `execMiddleware` (`internal/component/ssh/ssh.go`) resolves in the daemon, and the daemon-hosted TUI a plain ssh client with a pty reaches through `buildSessionModelFactory` (`cmd/ze/hub/session_factory.go`). Found writing the completion test for `plan/spec-plugin-registers-pipe-operations.md` phase 4, whose plan recorded the interactive CLI as needing no change | not fixed. The repair is a channel that carries the daemon's alias table to the client at session start, beside the runtime command list `buildRuntimeTree` already fetches, and it is a design decision rather than an edit: it needs a wire surface, a client-side registration whose collision rule differs from the plugin-facing one, and an answer for a plugin that stops mid-session |
| 2026-08-23 | fixit-vrrp-parent-ignores-the-selector | vrrp engine, `ze doctor`, vrrp metrics | A VRRP group whose hardware selector answers no device is CONFIGURED and not running, and nothing outside the log says so. `snapshots()` (`internal/plugins/vrrp/engine.go`) walks `e.instances`, so a group with no instance is absent from `show vrrp` rather than shown as down. The vrrp doctor check reads the config tree and holds no selector answer, and `ze doctor` is registered `Mode: "offline"` (`internal/component/doctor/register.go`) so it runs in the CLI process where the resolver mapping was never published. No metric covers it: the existing series are keyed `{device,group,vrid,family}` and an unbound group has no device. It also never retries: `apply` runs only on a config event (`register.go` OnConfigure / OnConfigApply), so a NIC that enumerates after the commit leaves the VIP unserved until somebody commits again. Pre-existing and unchanged by the binding -- `create` failed identically on `parentIfindex` before it -- and found by independent review of that binding | not fixed here, and deliberately not: the retry wants a long-lived watcher in the engine (`iface.Subscribe` on the LOGICAL name already routes appearing-device events to mac/match names that do not know their device, `logicalsForLocked` in `iface/resolve.go`), and the visibility wants a new metric series plus a show-surface decision with its own pipe and doc obligations. Both are a feature -- "a group defers until its hardware appears" -- rather than this spec's fix, and folding them in costs the commit its focus. What DID land is honesty: the log line now says the group will not start until the configuration is applied again |
| 2026-08-25 | - | `LinkPayload` and `StatePayload` (`internal/component/iface/iface.go`) | Two exported event-payload types with no cross-package caller of any kind. A repo-wide grep outside their own file returns `TestLinkPayloadFormat` and `TestStatePayloadFormat` (`internal/component/iface/iface_test.go`) and nothing else. The only reader of the declared shape of the link and state events is the test that asserts that shape. They date from `7751049e3` on 2026-03-30. This is their state at HEAD, unchanged by the mirror work. It surfaces now because `./le repository check` reds on them in a file that work edits. The red therefore reads as charged to a commit that did not cause it. `./le repository check` is not in `STRUCTURAL_GATES` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)), so it blocks no commit. One thing was NOT investigated, and that is why this row is no stronger. Whether anything publishes `TopicLinkCreated`, `TopicLinkDeleted`, `TopicLinkUp` or `TopicLinkDown` at all is unknown. So is whether a publisher builds the payload out of these types or out of another shape. Next step: read the monitor's publish path and compare what it emits against these two structs. A different shape makes this a declared format that contradicts its payload rather than dead code. The row then moves to that class | not fixed |
| 2026-08-28 | le-is-a-ze-binary | BGP peer reload from REST commits | `Server.reloadConfig` (`internal/component/plugin/server/reload.go`) completed the plugin transaction and only called `SetConfigTree`. It did not call `VerifyConfig` or `ApplyConfigDiff`. The REST commit reported `committed`, but the reactor kept the old peer set. `createReloadFunc` (`internal/component/bgp/config/loader_create.go`) also read the active file instead of the staged candidate. | wired reactor verify and apply into the reload transaction. The reactor reload now reads the candidate before the active config. |
| 2026-08-28 | le-is-a-ze-binary | the `functional` area's Docker-exec analysis (`internal/le/functional/register.go`) | The area's registered Description advertises "functional suites, fail-open Docker-exec analysis, and ExaBGP compatibility". The analysis does not exist: no file under `internal/le/functional/` contains the string `docker` at all, no action verb serves it, and the verbs are derived from the suite names plus `exabgp-test`. The only surviving mentions in the tree are that Description and a comment in `leaction.go` naming two gates, `docker-exec-selftest` and `docker-exec-check`, that nothing implements. The feature's test was deleted in the same migration: `internal/le/functional/dockerexec_test.go` (deleted with its baseline) <!-- doc-links: ignore (deleted 2026-08-28 by eae282592 together with the docker-exec baseline it read) --> carried 14 weakening findings and its own header declared it the evidence for AC-8 and AC-11 of spec-le-is-a-ze-binary, pinning the analyzer's fixpoint derivation, its producer-fixture classification, and its anti-vacuity floor against a scan that reports no sites and passes the ratchet | NOT fixed, and recorded here rather than repaired because the repair is a decision rather than a patch: either the analyzer is ported and its test with it, or the Description stops claiming it and the two gate names leave the comment. Found while writing this migration's `test/weakened.md` rows, where the deleted test needed a successor named and had none. The spec cannot close on its AC-8 and AC-11 evidence until this is answered |
| 2026-08-29 | generated-command-usage | `yangVerbs` and `isYANGVerb` (`cmd/ze/ze_core_dispatch.go`) against the verb tree `cli.YANGCommandTree()` builds | The argv dispatcher gates on a HARDCODED eight-verb map: `show set clear request delete update validate monitor`. The command tree declares SIXTEEN top-level verbs, and `zeUsage` lists all of them under "Operations (interact with the running daemon)" because that listing is derived from the tree. Six reach no dispatcher: `ze announce help`, `ze withdraw help`, `ze create help`, `ze debug help`, `ze system help` and `ze peer help` each answer `unknown command: <verb>` and print the top-level banner, on a build carrying every feature tag. `resolve` and `plugin` survive only because they own separate dispatch roots. The map also holds `validate`, which the tree does not declare. Measured against `bin/ze-host` built with `ze_core` plus every feature gate on 2026-08-29; `ze show bgp peer help` works from the same binary, so this is the map and not the build | not fixed, and it is not this session's to fix: the repair is either deriving the verb set from the tree, which is the registration-over-hardcoding answer, or removing the six from `zeUsage`'s listing if `ze` is deliberately a subset of `ze cli`. The listing and the dispatcher currently disagree, and only one of them can be right. It matters beyond help output: `plan/spec-generated-command-usage.md` writes four acceptance criteria as `ze <verb> ...` invocations over `create`, `announce` and `withdraw`, none of which the binary can reach |
| 2026-08-29 | generated-command-usage | `yangVerbs` and `isYANGVerb` (`cmd/ze/ze_core_dispatch.go`) | FIXED, and this row records the repair the row above left open. The verb set is DERIVED at dispatch time from the two registries a command can arrive through: the top-level children of `cli.YANGCommandTree()`, plus the first word of every path `registry.ListLocal()` holds, less every name `registry.ListRoot()` claims. The subtraction keeps `ze plugin` and `ze resolve` on their root handlers although the tree declares both, and it preserves `validate`, which no tree declares and one local handler needs. `ze announce help`, `ze withdraw help`, `ze create help`, `ze debug help`, `ze system help` and `ze peer help` each answer exit 0 with their own help page on a build carrying every feature gate | `TestEveryDeclaredVerbDispatches` walks the registries itself rather than asking `yangVerbs`, and drives `zeDispatch` with the operator's own `ze <verb> help`, so a verb that reaches no dispatcher fails on the exit code. `TestUndeclaredVerbFallsThroughToUsage` is its other half: a derivation answering true for everything would satisfy the first test and swallow every typing mistake. Discrimination measured on 2026-08-29 by restoring the literal map, which turned the first test red on all six verbs under `ze_core ze_distro` plus every gate. One hazard found on the way and fixed in the test: `zeFlags` is one variable for the whole test binary and a sibling test leaves a config override in it, which `zeDispatch` reads before argv, so the test resets it and gives it back |
| 2026-08-29 | generated-command-usage | `ze <path> help` under a verb a ROOT command owns, `yangVerbs` (`cmd/ze/ze_core_dispatch.go`) | The subtraction the row above documents has a consequence nothing records: a command the tree declares under a root-owned verb has NO reachable help page. `ze resolve dns a help` prints nothing and exits 0, `ze resolve dns ptr help` answers `reverse addr "help": dns: unrecognized address: help`, and `resolve dns aaaa`, `resolve dns txt`, `resolve cymru asn-name`, `resolve peeringdb max-prefix` and `resolve peeringdb as-set` behave the same way: the help word reaches the root handler as an argument and never reaches `extractHelpPath`. Measured on 2026-08-29 by running `ze <path> help` for all 49 commands whose authored usage prose phase 8 deleted, on a build carrying `ze_core` plus every feature gate: 42 printed their generated usage line and these 7 printed none. The generated line IS published for all 49 in `ze help command --json`, so only the operator help surface is blind | not fixed, and it blocks no goal of the spec that found it: the deleted prose was equally unreachable at that surface, so nothing regressed. The `yangVerbs` comment states root-wins as deliberate, so the question is whether a root command should fall through to the tree when the last argument is a help word, not whether the subtraction is wrong |
| 2026-08-29 | spec-vrf | bgp update batching, `internal/component/bgp/yang/ze-bgp-conf.yang:1020` and `:1027` | `leaf auto-commit-delay` and `leaf max-batch-size` have no reader. Neither name, nor its Go spelling (`AutoCommitDelay`, `MaxBatchSize`), appears anywhere under `internal/`, `pkg/` or `cmd/`, so both leaves validate, commit and change nothing while their descriptions promise batching and a per-cycle cap. Same shape as the interface unit `vrf` leaf fixed under spec-vrf on the same date. Found by a scan of every `leaf`/`leaf-list` name in `internal/**/yang/*.yang` against Go string literals, run while fixing that leaf | not fixed, and not characterised further; `internal/plugins/fib/p4/yang/ze-fib-p4-conf.yang` `leaf device-id` came back from the same scan and is unverified beyond the zero-hit count |
| 2026-08-30 | - | `ze interface migrate`: `cmdMigrate` (`internal/component/iface/cli/migrate.go`) against `firstFlagToken` (`internal/component/plugin/server/command.go`) | The offline CLI builds `interface migrate --from %s.%d --to %s.%d --address %s`, appends `--create` and `--timeout`, and dispatches that string to the daemon over SSH. `(*Dispatcher).Dispatch` refuses any flag-shaped token before the handler runs: `firstFlagToken` returns the first `-`-prefixed word and the answer is `unexpected flag "--from": flags are interpreted by the client, not the daemon`. So every `ze interface migrate` invocation fails. The daemon-side parser written for those flags, `parseMigrateArgs` (`internal/component/iface/cmd/cmd.go`), is unreachable for the same reason, and `container migrate` (`internal/component/iface/yang/ze-iface-cmd.yang`) declares no leaves under `ze:inherit "none"`, so nothing carries the values as grammar either. `grep -rn "interface migrate" test/ demos/` returns nothing, which is why no gate saw it. Found while inventorying `--flag` against keyword spellings across the command surface | not fixed, and not characterised further. The repair is the one the flag boundary implies: the five arguments select what is migrated, so they are grammar and belong in the YANG node as keyword selectors (`from`, `to`, `address`, `create`, `timeout`), with `cmdMigrate` and `parseMigrateArgs` changing with it. FIXED 2026-08-30, exactly that way. `container migrate` now declares the five as modifier groups (`from`, `to`, `address` required; `create`, `timeout` optional), so the published usage line reads `request interface migrate from <source> to <destination> address <prefix> [create <type>] [timeout <duration>]` and completion offers each keyword. `parseMigrateArgs` reads keyword/value pairs and refuses an unknown keyword, a repeated keyword, a keyword with no value and a missing required keyword, each by name. `cmdMigrate` takes the same grammar and sends the daemon's own path, `request interface migrate`, which it never named before. The flag form is deleted, with no alias and no deprecation branch. `test/plugin/iface-migrate.ci` is the gate that was missing; it went red when the daemon's keyword vocabulary was reverted to the flag spelling. The last clause of this row stays TRUE and is out of scope: nothing still checks that a client never sends a flag to the daemon |
| 2026-08-30 | spec-ppp-ra-refinements | l2tp PPP IPv6 subscriber service, `internal/component/l2tp/plugins/pool` and `internal/component/l2tp/ppp` | The whole IPv6 subscriber path is unreachable in the shipped daemon, so its Router Advertisement sender and its DHCPv6-PD server are dead code. `poolPlugin.handle` (`internal/component/l2tp/plugins/pool/register.go`) answers every address request whose family is not IPv4 with `Accept: false` and the reason "IPv6 not supported by static pool", and `l2tp.RegisterPoolHandler` has exactly one caller, that same file. `runNCPPhase` (`internal/component/l2tp/ppp/ncp.go`) reads the decline, sets `s.disableIPv6CP`, and leaves `s.ipv6cpState` at Initial, so the `s.ipv6cpState == LCPStateOpened` guard in `afterLCPOpen` (`internal/component/l2tp/ppp/session_run.go`) never passes and `startIPv6Service` is never called. `plan/journal/silent-fall-through.md` (row 2026-08-13) records the same refusal seen from outside, where pppd 2.5.1 gave up after nine IPv6CP Configure-Requests. The second half is disconnected independently: `session_run.go` passes a nil prefix allocator and `l2tp.GetPrefixHandler` has no caller, which `plan/spec-radius-subscriber-attributes.md` records as limitation L-1. Found while writing the functional and interop proof for `plan/spec-ppp-ra-refinements.md`, whose cease Router Advertisement has no reachable production caller as a result | not fixed. The repair is a feature slice with its own acceptance criteria: accept an IPv6 address request when the operator configured an IPv6 pool, wire the prefix handler and releaser into `startIPv6Service`, and prove it with a needs-linux `.ci` and an IPv6 L2TP interop scenario. Two test rows in `plan/spec-ppp-ra-refinements.md` wait on it |
| 2026-08-30 | - | legacy BGP reactor env vars, `internal/component/config/apply_env.go` and `environment.go` | `ai/rules/config.md` states a MUST that no code carries out: a legacy env var keeps its old key for compatibility AND registers an alias matching the YANG leaf name. Seven `environment/bgp/reactor` leaves are plumbed to legacy keys only -- `forward-queue-size` to `ze.fwd.chan.size`, `forward-batch-limit` to `ze.fwd.batch.limit`, `forward-pool-max-bytes` to `ze.fwd.pool.maxbytes`, `forward-pool-headroom` to `ze.fwd.pool.headroom`, `forward-teardown-grace` to `ze.fwd.teardown.grace`, `read-buffer-size` to `ze.buf.read.size`, `write-buffer-size` to `ze.buf.write.size` (`envPlumbingTable`, `apply_env.go:36-52`). `environment.go` registers each legacy key with a `Deprecated:` note naming the YANG leaf, and registers no `ze.bgp.reactor.<leaf>` alias for any of them, so an operator who reads the leaf name in `show configuration` and forms the documented env var key gets an unregistered key with no effect. The rule's own table listed two of the seven and presented the aliases as if they existed. Found while moving the naming tables out of `ai/rules/config.md` into `ai/patterns/config-option.md`, which required verifying each row at its producer | not fixed here: this is a code change to `environment.go` and `apply_env.go` with its own tests, outside a rule-and-docs pass. The corrected table now names all seven and says plainly that no alias is registered today (`ai/patterns/config-option.md`, "Legacy env var keys that predate the convention"), so the next reader inherits the gap rather than the claim |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff (phase 4, rfc8671) | BMP receiver, the `environment` config root | The bmp plugin declares `WantsConfig: []string{configRootBGP, configRootEnvironment}` (`runBMPPlugin`, `internal/component/bgp/plugins/bmp/bmp.go`), so a reload that changes `environment` makes it a transaction participant and delivers the candidate subtree. Nothing re-applies it. `startReceiver` runs only from the Stage-2 configure callback, which is served once per plugin process, so a reload that adds a listener, moves one, or sets `enabled false` changes no listener and the reload still reports success. `startReceiver` is also not idempotent: it appends to `bp.listeners` and binds again, where the sender's `startSender` opens with `stopSenders`. Found while wiring the SENDER reload rail for RFC 8671 Section 7.2 | not fixed, and not made worse: the config-apply handler this session added acts on the `bgp` root only, which leaves the receiver exactly where the SDK's accept-and-do-nothing default left it. The repair is the same shape as the sender's: give `startReceiver` a `stopListeners` of its own and call it from the reload rail. The one thing to settle first is `acceptLoop`, which logs `bmp: accept failed` as a Warn for any close that is not plugin shutdown (`bp.isStopping` reads `stopCh`), so a deliberate rebind needs a way to say so or every reload writes a false warning per listener |
| 2026-08-31 | yang-short-and-long-command-help | `writeHelp` (`internal/component/command/help.go`) | The function has NO non-test caller. `gopls references` on it returns 15 hits and every one is in `help_test.go`. A text grep over the whole tree, build tags included, finds it nowhere else. The operator's per-command page is `commandHelpPage` (`cmd/ze/command_help_page.go`), reached from `ze_core_dispatch.go` at two call sites. So the repository holds two renderers of one page, and only one of them ships. Phase 3 of the spec above fixed the parent-node behavior in BOTH, because the spec names `writeHelp` in its Wiring Test table. Found when this spec wired the declared long help into every renderer | FIXED 2026-08-31 (owner approved the deletion). `writeHelp`, `writeHelpLine` and `writeHelpEntry` are deleted. `HelpEntries`, `listedChildNames` and `describeChildren` stay, because `commandHelpPage`, `tui_menu.go` and `ze_core_usage.go` call `HelpEntries`. The 13 tests that drove `writeHelp` were RETARGETED onto `HelpEntries`. The two that asserted its own column format died with it. `cmdutil.DescribeCommand` is still a second copy of the "subcommands: a, b, c" fallback and is NOT fixed here |
| 2026-08-30 | rfcgate-6-supported-extraction-signoff (phase 4, rfc8671) | bmp sender statistics, `statistics-timeout` in `internal/component/bgp/plugins/bmp/yang/ze-bmp-conf.yang` and `senderConfig.StatisticsTimeout` in `internal/component/bgp/plugins/bmp/bmp.go` | The YANG leaf is declared, parsed from `bgp bmp sender statistics-timeout` into the sender config struct, and read by nothing. `senderSession.writeStatisticsReport` and `makeStatGauge` (`sender.go`) have no caller outside `sender_test.go`, so no timer fires and ze transmits no BMP Statistics Report at all, at any timeout the operator sets. An operator who sets the leaf gets silence and no error. `docs/guide/bmp.md` described the leaf as "Seconds between statistics reports (0 = disabled)" and its Protocol Details table gave Statistics Report the sender behavior "Periodic (if configured)", so the page promised a feature the daemon does not have. Found while walking RFC 8671 Section 6.2 for its extraction sign-off and classifying site 6.2:1, whose MUST binds the O flag on a Statistics Report ze never sends | not fixed: the extraction walk does not depend on it, and the site is excluded as binding a role ze does not implement. The page was corrected in the same session to say what the sender does today. The encoder is complete and now clears the O flag Section 6.2 requires, so the missing piece is the timer and the counter source: the RFC 7854 Section 4.8 stat types are per-peer counters ze holds in the reactor and not in the BMP plugin, so wiring them is its own work with its own event or command path. Either wire the leaf to a ticker that calls `writeStatisticsReport` with the RFC 7854 Section 4.8 and RFC 8671 Section 6.2 stat types, or delete the leaf, the encoder and the two doc rows together |
| 2026-08-30 | spec-rfcgate-6-supported-extraction-signoff | IPsec transport mode behind a NAT, `transportSelectorPairs` and `recordInitiatorSelectors` (`internal/component/ike/engine/transport_mode.go`, `ts_narrow.go`) | An operator can set `mode transport` on a site-to-site peer and Ze negotiates USE_TRANSPORT_MODE, but the peer cannot be behind an address-translating NAT, which is the deployment transport mode with NAT-T exists for. RFC 7296 Section 2.23.1 requires the address substitution on both roles: a responder replaces the IP address in TSi with the source address of the IKE packet it received, and an initiator replaces the addresses in the answered selectors with the ones from the IP header of the IKE packet. Ze does neither. `transportSelectorPairs` pins TSi and TSr to `sa.PeerCfg.LocalAddress` and `sa.PeerCfg.RemoteAddress`, and `recordInitiatorSelectors` refuses any answered selector that is not inside what Ze proposed, so a server that performs the substitution the RFC requires is answered with TS_UNACCEPTABLE. On the responder side nothing maps the peer's pre-NAT address onto the observed one, so the installed policy names an address no packet carries. Found while building the RFC 3948 Section 3.1.2 interop evidence, which reaches the transport-mode UDP-encapsulated receive path through strongSwan's `encap = yes` instead of a real NAT for exactly this reason | not fixed and not characterised further. The repair is the Section 2.23.1 substitution on both roles, storing the pre-substitution addresses so the SPD lookup and the installed selectors use the addresses this host sees. It needs its own acceptance criteria and an interop scenario with a NAT in the path, so it is a feature slice rather than an edit. `test/interop-ipsec/scenarios/natt-transport-inner-checksum` is the scenario it would extend |
| 2026-09-02 | publish-the-rfc-requirement-ledger (closure) | `internal/le/rfc`, exported symbols | `./le repository check` reports seven exported symbols with no cross-package non-test caller: `SectionSkipKinds`, `SiteDispositions`, `SectionDispositions`, `ParseExtractionArtifact`, `ExtractionStems` (`artifact.go`), `RowName` (`meta.go`) and `NoteCell` (`render.go`). All seven were exported before this spec, five of them by `a374622db`, and `git grep` at HEAD finds only same-package callers for each | not fixed and not this spec's. Unexporting one is a decision for the session that owns that surface, and three of them are in files another session is currently editing. Recorded because this class already collects rows and a deliberate pass over it is what earns the fix |
| 2026-09-03 | pki-full-chain | YANG string validation, `ValidateLeafValue` (`internal/component/config/schema.go`) and `leafNodeFromEntry` (`internal/component/config/yang_schema.go`) | Every `length "1..255"` in the tree was decorative. goyang parsed the restriction into `YangType.Length`, `yang_schema.go` never lifted it onto `LeafNode`, and `ValidateLeafValue` carried `Patterns` and `Ranges` but no length, so a 256-character value validated clean on ten leaves that had declared the bound for months (mrt, geodns, ddos/flowtriq, exabgp bridge). Nothing was red: the constraint was written, read by the YANG parser, and dropped one layer before the only consumer that could apply it. Found because a boundary row in the spec's TDD plan forced a test at the 255/256 edge, not because any gate noticed | fixed at the source: `LeafNode.Lengths`, `lengthRangesFromType` and `validateLengths`, counting characters rather than bytes (RFC 7950 Section 9.4.4). `TestValidateLeafValueLength` and `TestCertificateLeafLengthFromYANG` drive it from the built schema, and `test/parse/web-pki-certificate-name-too-long.ci` drives it through the daemon. The general lesson: a YANG restriction is inert until the schema builder lifts it AND the validator reads it, and neither half announces the other's absence |
| 2026-09-03 | - | OSPFv3 NSSA redistribution, `ospfv3-nssa-redist-frr` | The scenario is red at assertion 4: FRR never installs `2001:db8:7e5::/48`, the IPv6 prefix GoBGP announces to ze for `redistribute destination ospf import bgp`. FRR's LSDB holds ze's router, link and intra-prefix LSAs and no type-7 at all, so ze forms the NSSA adjacency and originates no NSSA-LSA for the redistributed route. Reproduced twice, once through `./le integration interop` and once in a hand-built two-container lab. The scenario's later assertions have therefore never run | not fixed. Found while replacing the vacuous absence proof in assertion 6, which cannot be exercised until the type-7 exists |
