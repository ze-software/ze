| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-23 | - | Repository verification inputs | Retained historical files still entered current source scans and supplied live hook names. | Exclude exactly `plan/verification-evidence/` through Go's rooted module ignore and the shared scanner predicate. Preserve canonical errors and test-removal checks. Fresh compiler runs ignored a synthetic broken archive package and rejected the same defect in a neighboring path. Historical records and archives remain unchanged. |
| 2026-09-22 | - | Credential evidence build directories under `plan/verification-evidence/2026-09-22-baseline/subscriber/l2tp-credential-preparation/` | Generated partial Go overlays retain `.go` names, so the registered lint action treats them as packages and reports missing deployment symbols. | Owner authorized archival and removal. Archived 224 generated snapshots with exact paths and hashes, compared every member with its loose source, then removed those copies. `plan/verification-evidence/2026-09-22-baseline/source-snapshots-removal-result.json` records the result. Drivers, logs, and captures remain. Lint was not rerun; the known PPP test declarations and separate style findings remain recorded in `plan/verification-evidence/2026-09-22-baseline/lint-2026-09-22-001.log`. |
