# Transient failure treated as fatal

A step fails for a reason that waiting fixes, and the caller exits instead of
waiting. When the operation had already changed state outside the process, the
abort leaves that state half-changed, and the record that would let anyone
finish it is written only on the success path.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-21 | - | the peer-side verdict of `test/plugin/cli-metrics-show.ci`, against the client assertion it runs beside | The test's own subject PASSED: the client printed "OK: metrics values returned 15413 bytes", which is the whole thing the case exists to assert. The run failed anyway, on the mock peer's "failed: read OPEN: EOF" after "new connection from 127.0.0.1:54000". Ze opened the connection and shut down before sending an OPEN, because the client had finished and the daemon was on its way out, and the harness reads any peer error as a failed test. So a teardown the case does not assert about decides its verdict, and the red says nothing about the metrics surface under test | not fixed, and the shape is a harness decision rather than a fixture edit: a peer that reads EOF on a connection the CLIENT'S OWN shutdown is closing has observed the expected end of the run, and a peer that reads EOF while the client is still working has not. Telling those apart needs the runner to know the client has exited, which is a change to what a peer block promises. Recorded here because the class earns its fix by row count, and because the next reader of this red will otherwise go looking at the metrics plugin, where nothing is wrong |
| 2026-08-15 | fixit-mirror-clsact-ownership | iface config apply, traffic mirroring | a mirror whose DESTINATION interface is absent aborts the whole commit and unwinds it. `SetupMirror` (`internal/plugins/iface/netlink/mirror_linux.go`) fails at `netlink.LinkByName(dstIface)`, `applyMirror` returns the error, and `applyConfig` (`internal/component/iface/config_apply.go`) answers with `return rollbackPartial()`. The same function refuses to do this one screen away for an absent SOURCE NIC: `absentPhysical` skips it with a warning because "an appliance must not brick because a configured NIC is missing". A capture interface that is an unplugged NIC is that same case on the other side of the mirror, and it is not covered. No test exercises an absent destination | not fixed, and it predates this spec: `applyMirror` returned errors this way before the mirror teardown work. Recorded because the two policies sit in one function and disagree. The fix is to extend the `absentPhysical` treatment to a mirror destination, or to say plainly why a destination is stricter than a source |
| 2026-08-12 | - | scripts/zeledon (retired; current producer: `internal/le/`) | Discord rate limited the last of the weekly update's 8 messages. post_weekly.py exited on the first failure, so ze-news held a post with no Coming up section, and the archive that marks a week as posted is written only after every chunk lands, so a re-run would have sent the other 7 again | send() now retries a rate-limited chunk on a fixed backoff, any other failure prints the --resume-from number that finishes the post, and post_weekly_test.py covers both paths plus the discord.sh lookup that had exited not-found on this machine |
| 2026-08-15 | fixit-peer-process-event-filter | bgp-rs peer-up replay | a peer-up replay of a large Adj-RIB-In drops routes when the read-buffer pool runs dry, permanently. `buildRelayUpdate` (`internal/component/bgp/reactor/reactor_api_relay.go:365` and `:410`) takes two pooled buffers per route and returns `errRelayBufferPool` when `getReadBuf` answers a nil `Buf`; `RelayStoredRoute` logs `relay-stored-route: reconstruction failed` and does `continue`, so that route is never retried and nothing else will send it. The pool is empty because the RECONSTRUCTION buffer is held until the cache evicts the entry, which waits on the per-destination writes, so a destination that is draining slowly starves the replay behind it. Seen under the retired `ze-functional-plugin-test` (current: `./le functional plugin`) at -p 8 in `test/plugin/modify-oversize-suppress.ci`, whose source sends 16373 prefixes in one 65535-byte UPDATE: the log carries `overflow buffer growing ... queued=10000` on 127.0.0.2 and then the reconstruction error repeated for many routes. Found while measuring suite flakiness; the fixture's own failure that run is a different question (a late destination takes the replay rail rather than the fast path, and the replay relays each prefix in its own small UPDATE, which is not the oversize case it asserts) | not fixed: the surface is the forward-overflow rail another session holds uncommitted (`forward_next_hop.go`, `session_write.go`). Its spec closed at 19:02 today (`27f41024e`) leaving `plan/known-failures/bgp-plugin-forward-overflow-two-tier.md`, so this row is for whoever picks that shard up. The next step is to decide what a replay owes a destination it cannot buffer for: a bounded wait and retry, or a peer-level failure that says the RIB is incomplete. Dropping one route silently-but-for-a-log leaves the destination with a RIB neither end agrees on, and the replay reports success |
| 2026-08-31 | fixit-rfc-drain-quota-never-armed | PPP NCP receive path, Configure-Nak and Configure-Reject | a malformed option list in a received Configure-REJECT tears the session down, where RFC 1661 Section 5.4 says of a received Configure-Reject "Invalid packets are silently discarded". `absorbIPCPReject` (`internal/component/l2tp/ppp/ncp.go`) returns `fatal=true` when `ParseIPCPOptions` errors, and `handleNCPPacket` turns that into `s.fail("peer Configure-Reject of mandatory option")`; `absorbIPv6CPReject` has the same shape. The sibling `absorbIPCPNak` returns on the parse error without applying anything, but the FSM still runs the RCN event, where RFC 1661 Section 4.3 says of RCN "An out of sequence or otherwise invalid packet is silently discarded". Found while fixing the Configure-REQUEST side of the same option walk | not fixed: this spec's commissioned scope is the Configure-Request path, which RFC 1661 Section 6 governs, and these three are the reception paragraphs of Sections 5.3, 5.4 and 4.3. The discard machinery the fix added (`ncpRequestDiscard` in `handleNCPPacket`) is the shape the fix would reuse: extend it so an invalid Nak or Reject skips `codeToEvent` instead of feeding it RCN, and stop `absorbReject` from reporting a parse failure as a peer rejection of a mandatory option FIXED 2026-09-20: the three absorb functions and handleNCPPacket return a three-valued ncpReply rather than a fatal boolean, so a Configure-Reject or Configure-Nak whose option list does not parse is DISCARDED (ncpReplyDiscard) and the session stands. RFC 1661 Section 5.4 and Section 4.3 both put an invalid packet on the silent-discard row, and a fatal there let a peer end a live session with one malformed reply. |
| 2026-09-15 | gtsm-related-icmp-ttl | `testbed.waitForIPv6` (`internal/component/gtsm/netns_linux_test.go`) | the poll that waits for a new IPv6 address to leave the tentative state called `netlink.AddrList` and treated any error as fatal, including `netlink.ErrDumpInterrupted`, which the kernel returns when the address set changes DURING the dump: DAD finishing is the very change the poll waits for. `TestGTSMTransmittedICMPErrorCarriesTTL255` failed once with `address list: results may be incomplete or inconsistent` in a run where the same package had passed a minute earlier | fixed in the closure: an interrupted dump keeps the addresses it returned and the loop lists again 50ms later; every other error stays fatal |
