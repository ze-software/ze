| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-03-21 | - | tests | `assert len(rpcs) == 14` broke on every feature addition | replaced literal with registry query |
| 2026-03-21 | - | tests | two sessions added features, both broke the same count assertion | used `>= min_expected` with documented floor |
| 2026-08-12 | - | verify stage golden | `stagesForMode` gained a `ze-relax-census` stage in both mode branches (`scripts/status/verify_run.go`, commit `a26dfb154`) and neither hand-duplicated golden list in `verify_run_test.go` was updated, so `TestStagesForModeMatchesGolden` was red at HEAD for every session that ran `./scripts/status` | added the stage to both goldens, in the position the code lists it. The test's own guidance names this two-sided edit; only the second side was missing |
| 2026-08-12 | - | interop harness ratchet | `test/interop/interop.py` gained 20 `_vtysh_quiet()` call sites whose return value nothing checks, and the committed floor in `scripts/dev/docker_exec_checked_test.py` stayed at 171. `TestRepoRatchet.test_the_real_tree_is_at_or_under_the_committed_baseline` counts 173 and is red at HEAD for every session that runs `make ze-test-pkg PKG=./scripts/dev`. The working tree holds no edit to either file | not fixed. Raising the floor is the move the ratchet exists to refuse, so the 20 sites need their return values checked or a `# fail-open-ok:` reason each. Found while closing eight spec retirements |
| 2026-08-12 | - | docker-exec fail-open ratchet | `make ze-test-pkg PKG=./scripts/dev` and `make ze-verify-wiring-docs` were RED at HEAD. `test/health/docker-exec-baseline.json` pins `unchecked: 171` and `scripts/dev/docker_exec_checked.py` measured 173. The report is hard to attribute by reading: it lists every unchecked site in the files the WORKING TREE changed, so any session touching `test/interop/` sees 44 sites in `interop.py` and reads them as its own. Measuring the count over `git archive <rev> test/` for each commit since the floor was set located it in one commit, `e4be51e5a`, and in one function: the second and third fallback probes of `notification_seen` (`test/interop/scenarios/shutdown-cease-frr/check.py`) read `docker_exec_quiet` output with `.splitlines()` and no emptiness test. `docker_exec_quiet` returns "" for a command that FAILED as readily as for one with nothing to say, and the caller reads "" as "ze sent no NOTIFICATION", so a broken vtysh would have been reported as an RFC 4271 defect in ze | fixed: each probe sets an `answered` flag before it reads, and `notification_seen` raises when no probe answered at all rather than returning the empty string that means "answered no". Count back to 171, floor unmoved, and the scenario re-run green |
