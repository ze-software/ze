| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-03-21 | - | tests | `assert len(rpcs) == 14` broke on every feature addition | replaced literal with registry query |
| 2026-03-21 | - | tests | two sessions added features, both broke the same count assertion | used `>= min_expected` with documented floor |
| 2026-08-12 | - | verify stage golden | `stagesForMode` gained a `ze-relax-census` stage in both mode branches (`scripts/status/verify_run.go`, commit `a26dfb154`) and neither hand-duplicated golden list in `verify_run_test.go` was updated, so `TestStagesForModeMatchesGolden` was red at HEAD for every session that ran `./scripts/status` | added the stage to both goldens, in the position the code lists it. The test's own guidance names this two-sided edit; only the second side was missing |
| 2026-08-12 | - | interop harness ratchet | `test/interop/interop.py` gained 20 `_vtysh_quiet()` call sites whose return value nothing checks, and the committed floor in `scripts/dev/docker_exec_checked_test.py` stayed at 171. `TestRepoRatchet.test_the_real_tree_is_at_or_under_the_committed_baseline` counts 173 and is red at HEAD for every session that runs `make ze-unit-pkg-test PKG=./scripts/dev`. The working tree holds no edit to either file | not fixed. Raising the floor is the move the ratchet exists to refuse, so the 20 sites need their return values checked or a `# fail-open-ok:` reason each. Found while closing eight spec retirements |
| 2026-08-12 | - | docker-exec fail-open ratchet | `make ze-unit-pkg-test PKG=./scripts/dev` and `make ze-doc-wiring-check` were RED at HEAD. `test/health/docker-exec-baseline.json` pins `unchecked: 171` and `scripts/dev/docker_exec_checked.py` measured 173. The report is hard to attribute by reading: it lists every unchecked site in the files the WORKING TREE changed, so any session touching `test/interop/` sees 44 sites in `interop.py` and reads them as its own. Measuring the count over `git archive <rev> test/` for each commit since the floor was set located it in one commit, `e4be51e5a`, and in one function: the second and third fallback probes of `notification_seen` (`test/interop/scenarios/shutdown-cease-frr/check.py`) read `docker_exec_quiet` output with `.splitlines()` and no emptiness test. `docker_exec_quiet` returns "" for a command that FAILED as readily as for one with nothing to say, and the caller reads "" as "ze sent no NOTIFICATION", so a broken vtysh would have been reported as an RFC 4271 defect in ze | fixed: each probe sets an `answered` flag before it reads, and `notification_seen` raises when no probe answered at all rather than returning the empty string that means "answered no". Count back to 171, floor unmoved, and the scenario re-run green |
| 2026-08-15 | - | docker-exec fail-open ratchet | The count rose again after the 2026-08-12 repair returned it to the floor: `scripts/dev/docker_exec_checked.py` measures 177 unchecked sites against the 171 pinned in `test/health/docker-exec-baseline.json`, so `TestRepoRatchet` is red at HEAD for every session that runs `make ze-unit-pkg-test PKG=./scripts/dev`. Not a working-tree artifact: no file holding an unchecked site is modified or untracked, so the six new sites are committed. Found while adding the loopback-address setup step | not fixed. Six sites need their return value checked or a `# fail-open-ok:` reason; raising the floor is the move the ratchet exists to refuse |
| 2026-08-15 | web-htmx4-prepare | assert-nothing ratchet | `make ze-test-sensitivity-check`, a `ze-precommit-verify` stage, is RED at HEAD: `scripts/checks/inert_tests.go --check` counts 143 tests with no reachable Error/Fatal/Fail call against the 135 pinned in `test/health/sensitivity-baseline.json`. The eight over the floor are committed, not a shared-checkout artifact: of the 109 files the report names, only `internal/component/bgp/reactor/reactor_test.go` is modified in the working tree. Found while running the gate over this spec's wiring phase, whose own new test was one more and is fixed in that phase | not fixed. Raising the floor is the move the ratchet exists to refuse, so the eight need a real assertion or a `// test-asserts-nothing:` reason each |
| 2026-08-18 | - | rules corpus floor | `scripts/dev/rules_points_test.py` pins `len(committed_rules()) == 28` by hand, and commit `e1fd46079` added the 29th rule (`ai/rules/precommit-verify.md`) without bumping it, so `SplitPartitionTest.test_roundtrip_covers_the_whole_corpus` was red at HEAD for every session running the repo-wide Python harness (`go test -run TestPythonUnitTests ./scripts/dev/`). Found while running that harness over unrelated work | fixed: bumped to 29 and recorded in the docstring which commit moved it. The hand-maintained number is deliberate (a SHRINKING corpus must not reach green on its own), so it was bumped rather than derived; what it costs is exactly this two-sided edit, half-done for the third time in this file |
| 2026-08-20 | cli-dispatch-child-guard | wire-method golden | `647f33121` registered a new RPC (`ze-bgp:overview`, `internal/component/bgp/plugins/cmd/peer/summary.go`) and never added it to the hand-maintained golden, so `TestRegisteredWireMethods` (`internal/component/plugin/all/all_test.go`) was red at HEAD for every session running `make ze-unit-pkg-test PKG=./internal/component/plugin/all`: `unexpected wire-methods: "ze-bgp:overview"`. `make generate` does not write that file, and no gate named it in the spec's own Integration Checklist. Found by the closure, three commits after the code landed | fixed: added in sorted position to `internal/component/plugin/all/testdata/wire-methods.snapshot`, and the package exits 0. Fourth two-sided edit in this file. A `ze:command` or a `RegisterRPCs` line is never the whole registration surface |
| 2026-08-23 | - | `docs/guide/ipsec.md` interop inventory | The page says "Sixteen scenarios run today" above a table of areas, and its source anchor reads "the sixteen strongSwan scenarios". `find test/interop-ipsec/scenarios -mindepth 1 -maxdepth 1 -type d` counts 19. The three the table names nowhere are child-rekey-narrowing, initiator-rekey-answer-narrows and responder-raises-child-rekey. Nothing compares the prose count with the directory, which is the check RA-DOC-010 in `plan/spec-release-audit-8-docs-onboarding.md` asked for and scoped to `test/interop/scenarios/` alone | not fixed, found while closing spec-fixit-ipsec-interop-cli-credentials. The count and the table are one edit, and the durable repair is RA-DOC-010's inventory check widened to both scenario directories rather than a third hand-typed number |
| 2026-08-25 | - | docker-exec fail-open ratchet | Fourth occurrence of this class, and the first since the floor was genuinely lowered. `6f3c26068` (2026-08-19) fixed sites and re-pinned `test/health/docker-exec-baseline.json` to 121. `scripts/dev/docker_exec_checked.py` now measures 125. So `TestRepoRatchet.test_the_real_tree_is_at_or_under_the_committed_baseline` is red at HEAD for every session that runs `make ze-unit-pkg-test PKG=./scripts/dev`. Of the 20 sites the report names, only `test/interop-l2tp/lab.py` is modified in the working tree, so the four over the floor are committed. Found when this session ran the Python harness over `scripts/dev/rfc_requirements.py` | fixed 2026-08-25 by a later session, which also corrected the attribution above. The report's "New site(s), in the test files this tree changed" heading is not a claim of newness: `touched_files` (`scripts/dev/docker_exec_checked.py`) narrows the list to changed files only "to name the likely author", so every listed site was in `test/interop-l2tp/lab.py` and `test/interop-pppoe/lab.py` because those were dirty, not because they rose. Measured instead by scanning three trees with `--root`: `6f3c26068` gives 121, HEAD gives 125, and the working tree gives 125. So the rise is entirely in committed code and the dirty lab files contribute nothing. Differencing the two site lists, and discounting line moves and the interop-ipsec scenario renames that dropped the numeric prefixes, leaves exactly four: two in `test/interop/scenarios/ospf-sr-frr/check.py` and two in `ospfv3-sr-frr/check.py`, both of which grew an instrument probe and a kernel probe when their `mpls -ls` readback learned to say WHICH of three causes made it empty. Six reads in those two files now carry `# fail-open-ok:` in the idiom `test/interop/interop.py` already uses. Three are diagnostic reads under an unconditional `raise`. Three are poll-loop reads where "" never matches 16100 and the check after the deadline raises. No return value was left unchecked and no floor was raised: the count fell to 119 and the baseline was LOWERED to 119 to keep it tight |
| 2026-08-25 | - | ci-sleep ratchet, `check_ci_sleep_ratchet` (`scripts/dev/verify_wiring_docs.py`) | `test/**/*.ci` holds 81 `time.sleep(` calls against a ceiling of 75, so `make ze-doc-wiring-check` is red. That gate is in `STRUCTURAL_GATES` (`scripts/dev/commit_helper.py`), so it refuses any commit carrying a `.ci` path. The red is entirely COMMITTED: the count is 81 at HEAD and 81 in the working tree, and no `.ci` file differs between them. Measured against `e269e3e5a`, the commit that last wrote `test/.ci-sleep-baseline`, where the count is exactly 75. Six sleeps arrived in three commits since, and none of them appended a delta line: `5e14f7f51` added one to `test/ipsec/ipsec-peer-reload-leaves-tunnel-alone.ci`, `b4c9d16bc` added one to `test/plugin/ddos-firewall-concurrency.ci`, and `182b7a2f3` added four to `test/plugin/iface-link-flap-during-commit.ci`. All six are LEGITIMATE by the repository's own stated criteria, which is what makes this a decision rather than a defect. Three are poll intervals inside bounded condition waits, the shape `test/.ci-sleep-baseline` already calls "a granularity knob on a real condition wait rather than a guessed duration". Two are deliberate timers with no queryable signal and their reasoning already written above them: `COMMIT_SETTLE = 1.0` waits for a reload to reach the `dhcpMu` block, whose hold lasts 1.1 to 3.3s, and `RESYNC_WINDOW = 1.6` waits two ticks of a tracker that pushes once a second. `check_ci_sleep_justification` in the same file names both categories as valid reasons. The ratchet counts occurrences, never unjustified ones, so a justified sleep still raises it | NOT FIXED, and it needs an owner decision rather than an edit. Two routes and neither is available to a session on its own. A `+N` line is the explicit-approval knob and is owner-only. Converting the three poll intervals is legitimate deduplication rather than gate-gaming, because module-level `wait_until` (`test/scripts/ze_api.py`) already exists for exactly this and its docstring says its internal sleep is exempt: the local `wait_until` in `iface-link-flap-during-commit.ci` is a reimplementation of it. That conversion needs a Linux netns or QEMU run to prove the wait is not vacuous, which is the bar every earlier conversion in the baseline file met, and it is not reachable from a darwin or unprivileged session. The two deliberate timers are not convertible at all and would still need `+2` |
| 2026-08-26 | - | `le integration` gate floor | `test/ui/le-functional-answers.ci` asserts `len(actions) >= 13` for the Go integration area. But `Table` (`integration/gates.go` in the former top-level tool tree) declares 12 actions, so the suite was already red. The floor was written when the area had 13 rows. Step 10b of the le migration moved `ze-qemu-vpp-hugepages-test` to `qemu` in the former top-level tool tree with its ported driver. That step did not lower the floor. The Python area declares 21 gates. Nine belong to the Go areas that own their gate-name family. Those nine are seven `ze-deployment-` gates, one `ze-evidence-` gate, and one `ze-qemu-` gate. Thus, 12 remain. The same file's header said that eight gates had moved. It named six `ze-deployment-` gates, one less than the seven that moved | fixed. The assertion is now `== 12`, with the arithmetic above it. The header count is now nine. A floor that only increases cannot detect a departing row. A gate that moves between areas looks exactly like a departing row to the count |
