| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-04 | radius-admin-interop-freeradius | `./le` launcher reading `bin/le`, over `./le integration interop-radius` | Seventh occurrence, same day and same shape as the row below, and the first where the stale binary made a checker report an expectation the tree does not hold. The new FreeRADIUS suite answered `1 passed, 2 failed`, and both failures named a want string of `user-password=present chap-password=absent` for a CHAP scenario. That is `papShape`, and `checkCHAP` passes `chapShape` at `internal/le/interoplab/radius/checkers.go:198`. Reading the source therefore could not explain the message, and an hour went into `Discover`, the checker registry and `textbuf` aliasing before the binary was suspected. `bin/le` was rebuilt by a peer session at 09:16 and the run was at 09:22, so the launcher's own staleness line never printed either: it samples one call in sixteen and only WARNS, by design (`warn_when_stale`, `le:117`). After `./le --update` the same break prints `chap-password=present`. The direction that matters here is the one the rows below keep naming: the stale binary did not go quiet, it produced a specific expectation that contradicted the file the reader was looking at | not fixed, and the six rows below already name the repair. This occurrence adds the cheapest possible tell for an interop suite: the want string a checker prints is derived from the BINARY, so when it disagrees with the source, suspect the binary before the logic. This session also rebuilt the SHARED `bin/le` in place three times, which the 2026-08-31 row names as the wrong move; `./le --name <x> integration interop-radius` builds from source on every call and leaves the shared binary alone |
| 2026-09-04 | fixit-tunnel-traffic-proof-is-one-directional | `./le` launcher reading `bin/le`, over `./le integration interop-ipsec` | Sixth occurrence, and the first to reach an INTEROP MEASUREMENT rather than a check. The 25-scenario strongSwan suite was run to record this spec's RED phase and answered 21 passed / 4 failed, with `psk-site-to-site` GREEN under the shunt, which is a plausible verdict and the wrong one. Nothing warned. The tell was in the failure TEXT: `esp-form-change` reported `ze to 172.28.0.3 did not carry lossless ESP`, the wording the working tree had already replaced with `ping from ze to 172.28.0.3 lost 100% of 3 packets`, so the run had executed the previous assertion. `bin/le` was built 2026-08-30; the assertion changed that morning. stderr for the run was EMPTY: the launcher's `bin/le is older than committed sources` line printed on other `le` invocations in the same session and not on this path. 26 minutes of Docker for a verdict about a tree that no longer existed. Re-run after `./le --update`: 12 passed / 13 failed | not fixed, and the five rows above already name the repair: a staleness line on the OUTPUT, naming the binary's build time against the newest source under `internal/le/`. This occurrence says where it costs most. A wrong check costs a reader one investigation; a wrong 26-minute interop run costs the run again, and its verdict is the kind a spec pastes into a Goal Validation table and an owner reads as measured fact. One caution this session earned the hard way: it refreshed the SHARED `bin/le` in place, which the 2026-08-31 row of this file names as the wrong move because it swaps the binary under every peer session mid-run. `./le --name <x> integration interop-ipsec` builds from source on every call and leaves the shared binary alone |
| 2026-09-03 | - | `test/plugin/ddos-incident-confidence.ci:109`, a daemon-spawned external plugin under QEMU | The test times out with zero messages received, and the cause is a host binary executed in the guest. Its config says `run "ze-test fixture plugin/ddos-incident-confidence"`, and the DAEMON spawns that, not the test runner, so `ZE_TEST_BIN` never reaches it: the daemon resolves the name to `/workspace/bin/ze-test`, which on a shared darwin checkout is a Mach-O. The guest shell reads the header as a script and the log says so, `/workspace/bin/ze-test: line 1: syntax error: unexpected "("`, followed by `plugin ddos-conf-probe: TLS connect-back: context canceled`. Same shape as the `bin/le` row in this file, and the fix there is the shape of the fix here: read the file's magic bytes rather than trusting the path. Found while proving the conntrack kernel fix; this test was WRONGLY predicted to share that cause and does not, because it never reaches the classifier at all | fixed at the daemon rather than in the `.ci`, because 677 `run "ze-test ..."` lines carry this and a test hardcoding an architecture-suffixed binary would lie about what an operator writes. `startExternal` PREPENDED the engine binary's own directory to the spawned plugin's PATH, so it beat the shim directory the runner already puts first (`setupBinShims` and `childPathEnv`, `internal/test/runner/runner.go`), which is the only reason the right binary lost. That directory is now a FALLBACK appended after the inherited PATH, which is what its own comment always said it was for, "even when it is not installed system-wide". The composition moved into `pluginPathEnv` so it can be tested: `TestPluginPathEnvKeepsTheEngineDirectoryAsAFallback` fails against the prepend. Red to green in QEMU on the arm64 runtime kernel 7.2, `ddos-incident-confidence` passing in 5.1 s with no `syntax error` line. One caution for the next reader: an operator with an OLDER ze on PATH now spawns THAT rather than the running binary's sibling, which is what the config asks for and is a change of behaviour all the same |
| 2026-09-01 | - | `trackedAndUntrackedSourceFiles` (`internal/le/site/build.go`) and `(*demoCatalog).assetRoot` (`internal/le/site/demo.go`) | Second occurrence of the 2026-08-31 two-roots row. Both roots now exist, so the build succeeds. It publishes a page that names a file it does not serve. The homepage stamped `cli-dashboard.cast?v=b5aa861b35`, the digest of the SOURCE copy, over the published bytes `f9032257`, a different recording of the same demo. Nothing copies one root to the other. The staging list is `git ls-files --cached --others --exclude-standard` run in `website/`. `website/.gitignore` ignores `/assets/demos/`, so the media is neither tracked nor listed, and it is never staged. `seedArtifact` then carries the previous publish's media forward. `assetRoot`'s comment states the opposite ("which the build stages into the artifact") | worked around by copying the source recording and its manifest entry into the published tree. The stamp, the bytes and the artifact manifest now agree. NOT fixed at either producer, for the reason the 2026-08-31 row gives: which root is authoritative is the demos subsystem's design call. Whoever settles it owns the comment too |
| 2026-09-01 | - | `./le` launcher, the `bin/le` existence cache | Fourth occurrence. `./le site build` died with `parse ze help command --json: json: unknown field "long-help"`. The catalog is generated by `go run ./cmd/ze`, so it carries today's fields. `bin/le` was built 07:25 on 2026-08-31, and `long-help` landed at 22:10 that night in `e691533a6`. The decoder is `DisallowUnknownFields`, so a field newer than the binary reads as a malformed catalog rather than as a stale reader. `./le --name <name> site build` built from source and succeeded unchanged | not fixed, same reasoning as the row below: the shared binary must not be rebuilt in place. The earlier rows already name the repair: a staleness line on the failure. This occurrence says where it belongs. A parse error naming a field the binary never heard of is the cheapest place to say it is older than the source |
| 2026-09-01 | publish-the-rfc-requirement-ledger | `./le` launcher, the `bin/le` existence cache | Same class as the 2026-08-30 row, opposite direction: there a stale binary made a check read CLEAN, here it REFUSED a correct commit. `./le commit create` answered `no independent-review artifact at tmp/review/<stem>-<session>.md` for a commit that removes no spec. `closureStem` (`internal/le/commit/review.go`) stopped reading journal rows as a closure signal in `46bfab99c` at 20:49 on 2026-08-31, and `bin/le` was built at 07:25 that day, so the running gate was the previous one and the two `plan/journal/` files in the population read as a closure. `./le --name <name> commit create ...` builds from source on every call and accepted the same population unchanged | not fixed, and the launcher is right to leave it: `bin/le` is shared, so deleting it stops every peer session, and the launcher's own comment says to pass `--name` instead. The cost is that the default path gives no signal at all. A staleness LINE on the refusal, naming the binary's build time against the newest source under `internal/le/`, would have turned a wrong verdict into a diagnosis |
| 2026-08-31 | yang-short-and-long-command-help | `./le doc check templ-output` over `internal/component/web/page_snapshot_templ.go` | The committed generated file does not match what `templ generate` writes for its own source. The check is red at HEAD and stays red. The committed form groups the two imports into one parenthesised block. templ emits them as two separate `import` lines. A formatter ran over the generated output at eae282592, and nothing re-checked it. Found by a regeneration of one edited template in the same directory, which rewrote this untouched file as a side effect. It was restored to its committed bytes rather than carried, because the drift is not this spec's. The other 40-odd `_templ.go` files in that package pass, so the drift is this one file | not fixed. The repair is one `templ generate -path internal` run committed on its own, and the question behind it is whether any formatter is allowed to touch a generated file at all: a gate that compares generated output to committed bytes fails whenever one is |
| 2026-08-31 | none (weekly update) | `le` (the wrapper script at the repository root), reading `bin/le` | A session that edits `internal/le/` and then runs a bare `./le <action>` exercises the binary it found, not the code it wrote: the no-name arm runs `bin/le` when it merely EXISTS. Measured: `bin/le` was built 00:32, `internal/le/site/markdown.go` was fixed 01:57, and three `./le site build` runs published the OLD renderer while the package's unit tests compiled the NEW source and passed. A green unit test is evidence about the source and a published page is evidence about the binary, and nothing said they had diverged | NOT a defect, and the first version of this row was wrong about it. The wrapper already carries the answer and says so at `le:93`: "Deleting it is not the way to pick a change up, because peer sessions execute this same path. Pass --name instead." `./le --name <x> <action>` builds `bin/le-<x>/le` on EVERY call, so the toolchain decides staleness and the session reads its own edits without touching the shared binary. This session instead rebuilt `bin/le` in place, twice, which swaps the binary under every peer session mid-run. The find is a READING failure, not a tooling gap: the mechanism was one arm above the comment that was read. Corrected after `mem-block` pointed at the code (2026-08-31) |
| 2026-08-31 | none (weekly update) | `renderEngine` (`internal/le/terminaldemo/actions.go:265`) and `(*demoCatalog).assetRoot` (`internal/le/site/demo.go:105`) | The renderer and the site build disagree about where demo media lives. The renderer defaults its output to `<parent>/gh-pages/assets/demos`, the PUBLISHED artifact tree; the site build reads `website/assets/demos`, the SOURCE tree, and `assetRoot`'s own comment states that design ("It sits in the website SOURCE tree, which the build stages into the artifact"). `website/assets/demos` did not exist, so every `./le site build` failed at its first producer with "this checkout holds no rendered terminal demonstrations: run `./le terminal-demo render-all`" -- advice that does not help, because the render writes to the other path and the next build fails identically | worked around by copying the 38 rendered artifacts from `gh-pages/assets/demos` into `website/assets/demos`, which let the build reach exit 0. NOT fixed at either producer, deliberately: the artifacts are TRACKED in the gh-pages repository (`git ls-files assets/demos` answers 38), so which path is authoritative is a design call the demos subsystem owns, not one to settle from a weekly-update session. Whoever settles it should also decide whether `website/assets/demos` is ignored, since it is currently neither ignored nor tracked |
| 2026-08-31 | none (weekly update) | `requireRendererImage` (`internal/le/terminaldemo/render.go:107`), image tag from `demos/terminal/manifest.json` `Renderer.Image` | The renderer image tag names the base OS, Playwright and font versions only, so it does not change when `demos/terminal/Dockerfile` changes. `requireRendererImage` asks Docker only whether that tag EXISTS. The 2026-08-24 port of the entrypoint from `container-entrypoint.sh` to the Go `ze-demo entrypoint` therefore left this host silently running an image built 2026-08-25 whose ENTRYPOINT was still `/usr/local/bin/terminal-demo-entrypoint`. `le terminal-demo render-all` died on `/src/demos/terminal/demo-lock.sh: No such file or directory` and `demo_lock_acquire: command not found`, exit 127, naming a script the tooling rewrite had deleted. The message points at a missing shell script, never at the stale image that is the actual cause | worked around here by `le terminal-demo image-build`, which rebuilds the tag in place, and that unblocked the site build. NOT fixed at the producer: the tag still cannot express a Dockerfile change, so the next Dockerfile edit re-arms this on every host that already holds the tag. The fix belongs in the tag or in `requireRendererImage`, which should compare the built image against the Dockerfile it came from rather than test for presence |
| 2026-08-24 | fixit-ipsec-peer-reload-ignored | ike engine reload, `applyIPsecConfig` in `internal/component/ike/engine/register.go` | `tr`, `trNATT` and `ipPool` are built only when the pointer is nil, and nothing rebuilds them. So an operator who edits `vpn ipsec interface`, or the `remote-access` pool, gets a commit that succeeds while both IKE sockets stay bound to the address the daemon started with and the pool keeps its original range. The peers themselves DO restart, because `peerConfigChanged` sees `LocalAddress` change, so the reload half-applies: the sessions are new and the socket under them is old. This was invisible while the engine registered no `OnConfigApply` and reload applied nothing at all; making reload work is what made it reachable | not fixed, and deliberately: it is not a regression of the spec that found it and not what that spec exists to fix, so it needs its own work. What that work must decide is not the create-if-nil line but the ORDER: a listen address that changes means closing a socket peers are mid-handshake on, so the rebuild has to be sequenced against the peer restarts rather than dropped in beside them |
| 2026-08-22 | fixit-child-rekey-answer-vs-installed-selectors | ipsec functional, QEMU | `test/ipsec/ipsec-teardown-leaves-nothing.ci` fails inside QEMU with `tunnel never reached the kernel: ip xfrm state holds []`. The real cause is logged above it: `plugin.relay plugin=engine-steps` reports "`/workspace/tmp/session/<sid>/bin/ze-test: line 1: \xcf\xfa\xed\xfe...: not found`", which is the Mach-O magic of the HOST binary. The `.ci` declares `run "ze-test engine-steps ./engine-steps.json"`, and it resolved to `<binDir>/ze-test` rather than to the `ZE_TEST_BIN` the QEMU target exports (`.../bin/ze-test-linux-arm64`). the retired `mk/helper-session.mk` (current producer: `internal/le/session/actions.go`) puts the host and the cross-compiled binary in ONE per-session `bin/`, so the wrong one is present and executable. Reproduced twice: alone under `--pattern teardown-leaves-nothing`, and inside a full `ipsec -a` run. It is not caused by the Go change that found it, which cannot make a daemon exec a Mach-O file | not fixed. It blocks no goal of the spec that found it, whose proof is the two rekey `.ci` and an interop scenario. The producer was not read: `newRunner` (`internal/test/runner/runner.go`) joins `binDir` with the bare name `ze-test` before the `ZE_TEST_BIN` override, which is a lead and not a finding. Whoever picks it up should read how the ze DAEMON resolves an `external` plugin's `run` string, since `runCommand` (`internal/test/runner/runner_exec.go`) and its binary switch are a different path |
| 2026-09-02 | rfc-ledger-single-declaration | `./le` launcher, the `bin/le` existence cache | Fifth occurrence, and the first where the stale binary accused a GENERATED FILE of being wrong. `./le rfc check` exited 2 with one line, naming a row of `rfc/not-enrolled.txt` and refusing its `out-of-scope` kind as "not one of ['backlog', 'blocked', 'non-normative']". That reads as a real ledger defect in a file the closure was judging, and the closed set it names is the one `readEnrolment` held BEFORE this spec. `bin/le` was built 2026-08-30 at 23:08; the migration landed 2026-09-01. `./le --name close rfc check` ran the same tree and reported 157 violations, none of them about the three ledger files. The direction that matters: the stale binary did not go quiet, it produced a confident, specific, wrong finding against the exact artifact under review | not fixed, and the four rows above already name the repair: a staleness line on the failure, naming the binary's build time against the newest source under `internal/le/`. This occurrence says what such a line is worth. A gate that cannot run is one thing; a gate that names a file and a value and is wrong about both costs the reader a full investigation of an artifact that was correct |
| 2026-08-10 | kernel-compose-make-q-assertion-is-vacuous | build | `gokrazy/kernel/Makefile` (retired; now `internal/le/deployment/gokrazykernel.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> has no rebuild trigger for `ARCH=` or `BUILDER=`, so `make ARCH=arm64` after an amd64 build reuses the amd64 `vmlinuz` | `$(REQUEST)` record plus a gated `FORCE`, the mechanism `tools/installer-kernel/Makefile` (retired; now `internal/le/deployment/hostkernel.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> already carried. `kernel-runtime-deps.ci` probes both variables |
| 2026-08-10 | kernel-compose-make-q-assertion-is-vacuous | appliance | `kernelCacheVariantFor` (`internal/appliance/cache.go`) omits `qemu-build.py` from the cache key, so an edit to the QEMU backend does not invalidate a cached kernel | `kernelBuilderScripts` globs every `.py` under `tools/kernel-builder/`. The hand-written list is gone |
| 2026-08-10 | kernel-compose-make-q-assertion-is-vacuous | appliance | `resolveInstallerKernel` (`internal/appliance/cmd_kernel.go`) replaces `build/kernel/Image` and leaves `build/kernel/.variant` describing the previous build | `invalidateKernelBuildRecords` deletes `.variant` with `.request`. `resolveRuntimeKernel` deletes its own `.request` too |
| 2026-08-12 | fixit-ike-request-window | ike engine | `runEstablished` (`internal/component/ike/engine/established.go`) clears `ownedSA` and `pendingRekey` on return but never `ps.pendingIKESwap`. `PeerSession` is reused across reconnect cycles (`reconcile.go`). An SA built by `respondIKERekey` and never confirmed therefore survives into the next cycle with its `SK_*`, against RFC 7296 Section 2.12. `handleInformationalOwned` then answers the next peer IKE Delete by swapping onto it | fixed 2026-08-19 under `fixit-ike-resource-lifetime-leaks`, which owns the same rule: `runEstablished`'s teardown defer calls `ps.setPendingIKESwap(nil)` beside the `pendingRekey` clear, so the unconfirmed swap SA forgets its `SK_*` and the slot is empty before the next cycle starts. `TestRunEstablishedClearsPendingIKESwapOnExit` (`established_test.go`, tagged `RFC7296-2.12-1` positive) reddens on all four assertions with that call deleted. Found by the 2026-08-03 review round of `fixit-ike-request-window`, which is not this defect's producer |
| 2026-08-12 | fixit-web-auth-deleted-user-survives-reload | hub auth | a user deleted from the configuration kept logging in until restart, and the reload reported success. `authz.LocalAuthenticator` held `Users` by value, so the boot list was the answer forever. The same boot list sat on FIVE credential surfaces. Those are the AAA chain's local backend, the web fallback, the `ze-session` cookie check, SSH public keys, and REST/gRPC `Bearer` auth. Fixing the web fallback alone changed nothing, because the chain answered first from the same snapshot | fixed at the PRODUCER: `LocalAuthenticator.UsersFunc` (`internal/component/authz/auth.go`) reads the live list per call and REPLACES `Users`, fed by one `liveLocalUsers` (`cmd/ze/hub/main_servers.go`) that every surface shares. Only a functional test found the second snapshot: a unit test cannot see a wiring fact |
| 2026-08-12 | fixit-config-validators-bypassed-at-startup | dev workflow | the spec closed on 2026-08-12 was reported broken at release-blocker priority because `./bin/ze - < config` started a daemon it must refuse. That binary answers `ze 26.08.04 (built 2026-08-04T16:17:59Z)`, eight days older than the fix commit `25689705f`, so the reproduction measured the change's absence. the retired `mk/helper-session.mk` (current producer: `internal/le/session/actions.go`) gives every AI session its own `tmp/session/<date>-<sid>/bin/`, which leaves the repository-root `bin/` written only by an off-session `make` and aged out by nothing. A binary built at HEAD refuses the same bytes, exit 1 | no code change. the retired `ze-session-binary-path` (current: `./le session scratch`) prints the binary this session builds, and a reproduction that contradicts a committed change must read the version stamp of the binary it ran before it becomes a bug report |
| 2026-08-12 | - | docker-exec fail-open ratchet | `python3 scripts/dev/docker_exec_checked.py (retired; current producer: `internal/le/doc/wiring/delegate.go`)` counts 173 unchecked fail-open call sites against a committed floor of 171 (`test/health/docker-exec-baseline.json` (retired, no successor) <!-- doc-links: ignore (deleted 2026-08-28 by eae282592 with no replacement) -->), so `./le doc wiring` is RED at HEAD. It is not a working-tree artifact: removing this session's only new interop scenario leaves the printed site list byte-identical, and the only tracked files under `test/` that differ from HEAD are two `test/health/*.json` data files and two `test/parse/*.ci`, none of which can hold a `docker exec` call. The checker's own message says "No changed test file explains the rise", which is the tell: a scenario added as an UNTRACKED file is invisible to its changed-set attribution, so whichever commit introduced the two sites recorded no explanation and the floor was never raised with them. Found while adding `test/interop/scenarios/bgp-wellknown-noexport-frr`, whose `check.py` contributes no site (it reads only asserting helpers) | not fixed. It needs the two sites identified and either checked or annotated `# fail-open-ok:`, and the floor is not a number to raise blindly. `./le doc check verify`, the gate `ai/rules/git-safety.md` names for a docs change, is green, so this session's evidence does not rest on it |
| 2026-08-12 | - | ipsec interop PKI fixtures | `test/interop-ipsec/lab_test.py::test_every_pki_scenario_resolves_to_decodable_der` (retired; now `internal/le/interoplab/ipsec/`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> fails on a fresh checkout with `cannot read PKI file test/interop-ipsec/pki/ze.pem`. The directory holds generated material that is not in git, and the test reads it directly rather than generating it or skipping when it is absent, so the retired `ze-unit-pkg-test PKG=./scripts/dev` (current: `go test -race ./scripts/dev`) is red for anyone who has not run the ipsec lab on that machine | not fixed. The choice between generating the fixtures in the test and skipping when the directory is empty belongs to the ipsec lab's owner |
| 2026-08-14 | fixit-test-harness-fail-open-guards | commit tooling | the retired `scripts/dev/commit_helper.py create --append` (current producer: `internal/le/commit/prepare.go`) derives the message file from `--session` and `--tag` alone, so a second block prepared with the SAME tag overwrites the first block's `tmp/commit-msg-<session>-<tag>.txt`. Both `-F` lines then read the surviving file. Closure commits A and B landed with correct CONTENT (17 files, then the spec removal) and an IDENTICAL message, commit A carrying commit B's body. Nothing warned: the helper reports `message=` and `script=` once per invocation, and the second report looks the same as the first | not fixed. The workaround is a distinct `--tag` per block, which no rule or skill states today. `ai/rules/git-safety.md` step 2 says the helper "never writes over an existing script unless `--script` names it", which is true of the SCRIPT and false of the MESSAGE, so a reader infers a protection the message file does not have. History rewriting is banned, so the two landed messages stand |
| 2026-08-15 | fixit-forward-overflow-two-tier-flake | repro tooling | the retired `scripts/dev/stress-repro.py` (current producer: `internal/le/stressrepro/run.go`) reported `*** REPRODUCED on invocation 1 ***` for a suspected flake, and the reproduction was an artifact. The capture showed `unknown field in peer: attach`, a config grammar the working tree parses and a day-old `bin/ze` did not, so nothing about the suspected defect was exercised. `_bin_from_env` honours `ZE_BIN`/`ZE_TEST_BIN` but FALLS BACK to `bin/ze`. the retired `mk/helper-session.mk` (current producer: `internal/le/session/actions.go`) leaves that path stale by construction, because canonical binaries build into a per-session directory. `ensure_binaries` checks only that the file exists. The tool's own docstring records this biting once before, in the OPPOSITE direction: a fix under test looked "still reproducing" because the run never contained it. So the verdict line is not evidence either way, in the one tool whose whole output is a verdict | no product change: the flake did not reproduce in 80 invocations once the session binaries were exported. The reusable check is cheaper than a diagnosis and belongs BEFORE it. Treat a reproduction whose symptom is a CONFIG PARSE ERROR as a stale binary until proven otherwise. That symptom cannot come from the defect under test. The tooling fix is filed at `plan/future/spec-stress-repro-refuses-a-stale-binary.md`, which makes the tool refuse a stale binary instead of trusting the caller |
| 2026-08-17 | hub-deferred-api-auth-independent-of-ssh-block | hub authorization reload | user authentication followed the live `ConfigProvider`, but the AAA authorizer retained the startup `authz.Store`, so profile and policy edits did not apply until restart | one atomic live local authz store is dereferenced on every local AAA decision and swapped only after `runReloadContext` fully succeeds |
| 2026-08-17 | hub-deferred-api-auth-independent-of-ssh-block | login-resolved authorization profiles | `WithProfileRecording` cached ordinary local profile assignments by username. A later accepted config generation could remove that user, but `authz.Store.Authorize` could still use the prior login result as an external-profile fallback | local authentication now records only the recovery profile, which has no config assignment. Ordinary local profiles come only from the accepted authorization store |
| 2026-08-18 | - | verify freshness certificate | `runVerify` (the retired `scripts/status/verify_run.go` (current producer: `internal/le/verify/engine/run.go`)) appends every stage result in its loop and calls `cfg.WriteStatus` only after the loop ends. `writeVerifyStatus` then calls `computeTreeHash(root)` at that moment, so the status file stamps the tree as it stands when the run FINISHES and asserts that hash was verified. In a shared checkout the early stages ran against an earlier tree, so the certificate is strictly stronger than the evidence behind it. It is the artefact the retired `scripts/dev/verify-status.sh check` (current producer: `internal/le/verify/status/answer.go`) reads to say FRESH and the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`) gates on, so a run spanning another session's edits certifies content its early stages never saw. Observed on 2026-08-18: one session edited `pretool-writeedit.py` and `ai/rules/*` at 13:35Z while a run started 13:22Z was at stage 14 | not fixed. The fix is a decision about what the hash should cover, not a patch: hashing at the START and refusing to write a certificate when the tree moved, or hashing per stage and recording the set each one saw |
| 2026-08-19 | spec-fixit-zefs-diff-structural-ops | job admission, parameterized make targets | The `ze-unit-pkg-test` rule in the retired `mk/test-unit.mk` (current producer: `internal/le/testunit/groups.go`) passes the fixed label `ze-unit-pkg-test` to the retired `scripts/dev/ze-run.sh` (current producer: `internal/le/job/job.go`), and PKG and RUN reach the `_ze-unit-pkg-test-impl` half only. `_scan_and_claim` in that script attaches a second asker to a running job when LABEL and TREE match, so two sessions testing DIFFERENT packages share one verdict: the second replays the first's log and exits with its code. Observed 2026-08-19: the retired `ze-unit-pkg-test PKG=./internal/component/cli/testing` (current: `go test -race ./internal/component/cli/testing`) attached to a run of `./internal/plugins/flowspec-firewall -run ^TestRouteWithNoActionIsCountedAndLogged$` and returned that test's FAIL. A false PASS is the same mechanism with the packages swapped. The script's own header states the rule this breaks: attaching on the label alone "would certify a session with a run that never saw its code" | not fixed. `MAY_ATTACH=0` makes the run queue instead, which is the escape, not the fix: the label must carry the parameters that change what the job judges (PKG and RUN), or the attach key must read the command rather than the label |
| 2026-08-18 | - | scoped verify freshness | The SCOPED route of the row above answers from an artifact nothing writes any more. the retired `scripts/dev/verify-status.sh check <PATH...>` (current producer: `internal/le/verify/status/answer.go`) never reads `tree_hash`. It compares `tmp/ze-verify-manifest.txt`, and `MANIFEST_FILE` is written at one place in that script, inside its own `write` subcommand. Nothing in the retired `scripts/` (current producer: `internal/le/`), the retired `mk/` (current producer: `internal/le/`), the retired `Makefile` (current producers: `internal/le/` native action tables) or `.claude/` invokes `verify-status.sh write`: `runVerify` (the retired `scripts/status/verify_run.go` (current producer: `internal/le/verify/engine/run.go`)) writes the status file itself through `writeVerifyStatus` and writes no manifest. So a scoped FRESH is judged against whatever manifest a past run left behind. On this checkout that file was 20 hours older than the status file beside it. The sentinel `119eae751` added protects the UNSCOPED route alone, which is the route `commit_helper.py` gates on, so the commit gate is covered and the scoped answer is not | not fixed. Found by an independent review of `119eae751`. Either `runVerify` writes the manifest for the paths it verified, or the scoped route stops reporting FRESH when no manifest belongs to the current run |
| 2026-08-19 | - | unit-test job admission | The key that decides "this is the same work, share one run" omits the parameter that decides what the work IS. `LABEL="${1:-}"` (the retired `scripts/dev/ze-run.sh` (current producer: `internal/le/job/job.go`)) is the bare target name, and `ze-unit-pkg-test` passes the literal `ze-unit-pkg-test` to it, so `PKG` and `RUN` never reach the label. The attach test is `[ "$label" = "$LABEL" ]` plus a `TREE` hash match and `STATE=running`. Two sessions running the retired `ze-unit-pkg-test` (current: `go test -race ./...`) on DIFFERENT packages in one checkout therefore match, and the second attaches and reports the first run's exit code as its own. Same shape as the 2026-08-10 row above, where `ARCH=` was missing from the rebuild trigger and an amd64 `vmlinuz` answered an arm64 build. Observed twice by an agent this session, once reporting green for `./scripts/dev` while the package it had asked about was red, which is the direction that matters: a package that was never compiled reads as passing. Sharing is correct for a whole-tree target, where the two jobs really are the same work; it is unsound only for the parameterised ones. `MAY_ATTACH=0` in the environment is an existing opt-out and is the trustworthy route while this stands | not fixed. The repair is to put the parameters that change the work into the label, so `ze-unit-pkg-test PKG=./internal/component/bgp RUN=^TestX$` and the same target on another package stop being one job. Everything that has ever reported a scoped unit-test verdict in a shared checkout is affected, this session's own agent reports included, so a green from a concurrent run is evidence of nothing until the label carries `PKG` |
| 2026-08-19 | - | job admission opt-out | `MAY_ATTACH=0` in the environment, which the row above names as the trustworthy route while the sharing key stands unfixed, did nothing at all. the retired `scripts/dev/ze-run.sh` (current producer: `internal/le/job/job.go`) assigned `MAY_ATTACH=1` unconditionally just above its admission loop, so an inherited value was destroyed before the first `_scan_and_claim` call and the `${MAY_ATTACH:-1}` test in the attach condition could never meet an unset variable. Measured by running two identical jobs in a throwaway registry: the second, with `MAY_ATTACH=0` exported, printed the attach banner and exited with the first's code. It was measured while a concurrent session was running the retired `ze-unit-pkg-test MAY_ATTACH=0 PKG=./internal/component/bgp/cli/` (current: `go test -race ./internal/component/bgp/cli/`), so a session taking the documented escape was still eligible to be handed a foreign verdict | fixed: `MAY_ATTACH="${MAY_ATTACH:-1}"` honours the caller, and `_job_params` drops the name from the work key so that declining to ASK for a shared verdict never becomes a rule that nobody may share this job's own honest one. `test_the_environment_can_decline_sharing` and `test_declining_to_attach_does_not_stop_others_sharing_this_job` (the retired `scripts/dev/ze_run_test.py` (current producer: `internal/le/`)) hold both halves |
| 2026-08-21 | - | `ze-site-generate` (the retired `Makefile` (current producers: `internal/le/` native action tables)), the terminal-demo definition check | the recipe runs `render.py --all --stamp-definition-hashes`, ignoring its exit status, and then runs `render.py --all --check-definition`, re-rendering when that fails. `stamp_definition_hashes` (`demos/terminal/render.py`) writes `definition_sha256 = definition_digest(current)` into the artifact manifest for every selected demo, and `verify_assets(definition_only=True)` compares that same field. The stamp therefore makes the check pass unconditionally, so a demo whose `.tape` changed is never re-recorded and the site keeps serving the old recording. Found while judging whether spec-cli-show-bgp-is-the-command could close on an outstanding re-render: `render.py --check-definition --demo zefs-config` reports the definition changed, and the phase-4 commit message says the next site build pays for it, which the recipe does not do | not fixed. `--stamp-definition-hashes` exists to backfill the field for artifacts rendered before it existed, so removing it from this recipe would re-record every such demo, which is a cost the website owner decides. The class needs the backfill to be a one-off command rather than a stage of every build |
| 2026-08-24 | - | Zeledon weekly operator documentation | Stale documentation in the retired `scripts/zeledon/README.md` (current producer: `internal/le/weekly/`) and the module docstring in the retired `scripts/zeledon/post_weekly.py` (current producer: `internal/le/weekly/weekly.go`) pointed operators at the deleted `gh-pages` weekly source tree. It named only one private location for `discord.sh` | Both files now name the canonical `website/changes/posts/<start>.md` source and document the `find_discord_sh` lookup order: `DISCORD_SH`, `~/Unix/bin/discord.sh`, `~/bin/discord.sh`, then `PATH` |
| 2026-08-24 | - | Zeledon weekly Discord archive | The 2026-08-10 update was present as three `ze-news` messages and as its canonical website source, but the retired `scripts/zeledon/weekly/2026-08-10-weekly.md` (current producer: `internal/le/weekly/`) was absent. The local archive is the `--all` idempotency record, so its absence made the posted week eligible for a duplicate send | Recreated the archive from Discord messages `1538690543318597715`, `1538690543740405622`, and `1538690548104429649`. The body matches the posted messages exactly, `posted` records their 2026-08-17 local date, and `backfilled: false` records their unstamped header |
| 2026-08-24 | - | Weekly website feature catalog | `website/data/features.json` had no DDoS or anomaly card although the 2026-06-25 milestone and the anomaly and DDoS guides describe the shipped capability | Added one DDoS and anomaly card for detection, incident history, and mitigation |
| 2026-08-24 | - | Weekly website performance headline | `website/performance/index.html` claimed a 2026-08-01 run with 55 ms convergence, 1,818,181 routes per second, and 547 ms withdrawal. The current source in `docs/performance.md` records its latest run on 2026-06-05 with different values | Replaced the headline date and values with 62 ms convergence, 1,612,903 routes per second, and 596 ms withdrawal from the source |
| 2026-08-26 | le-is-a-ze-binary | committed `le` composition root, `register.go` in the former standalone composition | Commit `93cd4c1e4` imports `rfc` in the former top-level tool tree, `sitefacts` in the former top-level tool tree, and `testhealth` in the former top-level tool tree, but that commit contains none of those packages. `./le repository tracked-build check` therefore fails when it builds the committed source population. | Removed only those three blank imports from `register.go` in the former standalone composition. The in-flight package trees remain unchanged. |
| 2026-08-26 | le-is-a-ze-binary | `ze_le` composition root | The tracked-build check fails because committed `zele/tools.go` in the former top-level tool tree imports absent `rfc` in the former top-level tool tree and `sitefacts` in the former top-level tool tree packages | Removed only those two blank imports from the scoped composition root. The in-flight package trees remain unchanged. |
| 2026-08-27 | cli-pipe-operator-coverage | website command-equivalent mappings after the le command cutover | `website/data/command-equivalents.json` still named `validate config` and `generate wireguard keypair` as Ze commands after the product registry stopped publishing them. The canonical renderer failed before the per-command pipe contract could run. | Removed the stale validation spelling from the commit mapping and removed the keypair mapping, which had no remaining Ze or vendor command. |
| 2026-08-27 | le-is-a-ze-binary | `test/interop/Dockerfile.ze`, built by `interoplab.(*Docker).Build` | The interop image still used Go 1.26 after the root module moved to Go 1.27.0. Its separate `go mod download` layer failed before compilation, so every native interop scenario stopped at image setup. The Dockerfile also parsed feature tags with awk, duplicating the native feature-tag producer. | The image uses Go 1.27 and the root vendor tree. `RunAt` derives feature tags through `featuretags.DaemonTags` and passes them as a typed Docker build argument; the Docker build has no manifest parser. |
| 2026-08-28 | le-is-a-ze-binary | runtime kernel cache, `kernelCacheVariantFor` in `internal/appliance/cache.go` | The cache identity hashed resolved config fragments and native builder sources, but not `gokrazy/kernel/patches/series` or its patch files. Editing, adding, removing, or renaming a runtime kernel patch could therefore return a kernel built from the previous patch tree. The compiled install fixture audit exposed the omitted producer. | `kernelPatchSources` discovers and sorts every target patch input. Cache hashes now include path names and contents, and `TestRuntimeCacheVariantIncludesEveryPatchInput` proves add, edit, and remove each change the runtime variant. |
| 2026-08-30 | - | `./le` launcher, the `bin/le` existence cache | A new `doc-drift` check was added to `internal/le/doc/wiring` and measured over 30 commits through `./le doc wiring`. Every run reported zero findings, and the check had never run. The `le` launcher builds `bin/le` only when the file is ABSENT, so the cached binary predated the change. The positive control read as clean too, which is the direction that matters: an absent check and a passing check print the same nothing. Same class as the 2026-08-12 and 2026-08-15 rows, where a stale `bin/ze` made a fix look missing and a flake look reproduced | discovery fixed, cache unchanged: `docs/guide/developer-setup.md` now states that the cache is an existence test rather than a freshness test, and that `rm bin/le` picks a change up. The measurement was redone against the rebuilt binary, where the same commits report findings on nine of twelve |
| 2026-08-29 | - | published command surfaces, `refreshNativeSurfaces` in `internal/le/site/build.go` | A full build seeds the artifact from the current `../gh-pages` checkout, then rendered `reference/cli/`, `reference/command-equivalents/` and `llms.txt` only when `reference/cli/index.html` was ABSENT. The pages existed, so no build ever rewrote them. Nothing wrote `data/cli-commands.json` either: the Python renderer that refreshed it from `ze help command --json` was retired at the interpreter cutover and no Go producer replaced it. The published catalog stayed frozen with a `syntax` field the live catalog no longer emits for 80 commands, so `./le docvalid doc-drift` reported 2940 disagreements while every `./le site build` reported success. Found while clearing the doc-wiring gate. | fixed. `refreshCommandSurfaces` reads the catalog from the binary through `docvalid.LiveCommandCatalog` and rewrites the catalog and every derived page on every build, so a page cannot outlive the release that wrote it. `TestBuildRepublishesStaleCommandSurfaces` reddens on both halves against the old guard, and `TestBuildPreservesExistingArtifactSeed` now proves the seed with a page the build does not write, which is what it always meant to prove. |
| 2026-08-30 | rfcgate-6-supported-extraction-signoff | `rfc/short/rfc4301.md` checklist annotations | Two annotation reasons disagree with the producing code, so a reader takes a false fact from the gate's own checklist. `RFC4301-4.1-3` says "the IKE child-SA path hardcodes tunnel mode for every peer SA, so a peer SA can never be transport", and `createFirstChildSA` (`internal/component/ike/engine/child.go`) sets `modeTransport` whenever `sa.UseTransportMode` holds, which `decideResponderTransportMode` and `recordInitiatorTransportMode` (`internal/component/ike/engine/transport_mode.go`) set from the operator's `mode` leaf. `RFC4301-4.4.1.1-1` says "SPParams has no port or ICMP-type/code field", and `dataplane.SPParams` carries `SrcPort`, `DstPort` and `UpperProto` (`internal/component/ike/dataplane/dataplane.go`), which `childPolicyParams` fills from `selectorPort`; only the ICMP type/code half is still absent | not fixed here. The extraction walk that found them landed `rfc/extraction/rfc4301.json` and does not edit the summary: every `{gap}` and `{not-applicable}` in that file is a VOID earlier answer under `ai/rules/rfc-compliance.md` (owner directive 2026-07-27), so the seven of them need Thomas's answer together rather than a text repair one at a time. The walk's report carries them as AC-8 asks |
