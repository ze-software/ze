| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-24 | fixit-ipsec-peer-reload-ignored | ike engine reload, `applyIPsecConfig` in `internal/component/ike/engine/register.go` | `tr`, `trNATT` and `ipPool` are built only when the pointer is nil, and nothing rebuilds them. So an operator who edits `vpn ipsec interface`, or the `remote-access` pool, gets a commit that succeeds while both IKE sockets stay bound to the address the daemon started with and the pool keeps its original range. The peers themselves DO restart, because `peerConfigChanged` sees `LocalAddress` change, so the reload half-applies: the sessions are new and the socket under them is old. This was invisible while the engine registered no `OnConfigApply` and reload applied nothing at all; making reload work is what made it reachable | not fixed, and deliberately: it is not a regression of the spec that found it and not what that spec exists to fix, so it needs its own work. What that work must decide is not the create-if-nil line but the ORDER: a listen address that changes means closing a socket peers are mid-handshake on, so the rebuild has to be sequenced against the peer restarts rather than dropped in beside them |
| 2026-08-22 | fixit-child-rekey-answer-vs-installed-selectors | ipsec functional, QEMU | `test/ipsec/ipsec-teardown-leaves-nothing.ci` fails inside QEMU with `tunnel never reached the kernel: ip xfrm state holds []`. The real cause is logged above it: `plugin.relay plugin=engine-steps` reports "`/workspace/tmp/session/<sid>/bin/ze-test: line 1: \xcf\xfa\xed\xfe...: not found`", which is the Mach-O magic of the HOST binary. The `.ci` declares `run "ze-test engine-steps ./engine-steps.json"`, and it resolved to `<binDir>/ze-test` rather than to the `ZE_TEST_BIN` the QEMU target exports (`.../bin/ze-test-linux-arm64`). `mk/session.mk` puts the host and the cross-compiled binary in ONE per-session `bin/`, so the wrong one is present and executable. Reproduced twice: alone under `--pattern teardown-leaves-nothing`, and inside a full `ipsec -a` run. It is not caused by the Go change that found it, which cannot make a daemon exec a Mach-O file | not fixed. It blocks no goal of the spec that found it, whose proof is the two rekey `.ci` and an interop scenario. The producer was not read: `newRunner` (`internal/test/runner/runner.go`) joins `binDir` with the bare name `ze-test` before the `ZE_TEST_BIN` override, which is a lead and not a finding. Whoever picks it up should read how the ze DAEMON resolves an `external` plugin's `run` string, since `runCommand` (`internal/test/runner/runner_exec.go`) and its binary switch are a different path |
| 2026-08-10 | kernel-compose-make-q-assertion-is-vacuous | build | `gokrazy/kernel/Makefile` has no rebuild trigger for `ARCH=` or `BUILDER=`, so `make ARCH=arm64` after an amd64 build reuses the amd64 `vmlinuz` | `$(REQUEST)` record plus a gated `FORCE`, the mechanism `tools/installer-kernel/Makefile` already carried. `kernel-runtime-deps.ci` probes both variables |
| 2026-08-10 | kernel-compose-make-q-assertion-is-vacuous | appliance | `kernelCacheVariantFor` (`internal/appliance/cache.go`) omits `qemu-build.py` from the cache key, so an edit to the QEMU backend does not invalidate a cached kernel | `kernelBuilderScripts` globs every `.py` under `tools/kernel-builder/`. The hand-written list is gone |
| 2026-08-10 | kernel-compose-make-q-assertion-is-vacuous | appliance | `resolveInstallerKernel` (`internal/appliance/cmd_kernel.go`) replaces `build/kernel/Image` and leaves `build/kernel/.variant` describing the previous build | `invalidateKernelBuildRecords` deletes `.variant` with `.request`. `resolveRuntimeKernel` deletes its own `.request` too |
| 2026-08-12 | fixit-ike-request-window | ike engine | `runEstablished` (`internal/component/ike/engine/established.go`) clears `ownedSA` and `pendingRekey` on return but never `ps.pendingIKESwap`. `PeerSession` is reused across reconnect cycles (`reconcile.go`). An SA built by `respondIKERekey` and never confirmed therefore survives into the next cycle with its `SK_*`, against RFC 7296 Section 2.12. `handleInformationalOwned` then answers the next peer IKE Delete by swapping onto it | fixed 2026-08-19 under `fixit-ike-resource-lifetime-leaks`, which owns the same rule: `runEstablished`'s teardown defer calls `ps.setPendingIKESwap(nil)` beside the `pendingRekey` clear, so the unconfirmed swap SA forgets its `SK_*` and the slot is empty before the next cycle starts. `TestRunEstablishedClearsPendingIKESwapOnExit` (`established_test.go`, tagged `RFC7296-2.12-1` positive) reddens on all four assertions with that call deleted. Found by the 2026-08-03 review round of `fixit-ike-request-window`, which is not this defect's producer |
| 2026-08-12 | fixit-web-auth-deleted-user-survives-reload | hub auth | a user deleted from the configuration kept logging in until restart, and the reload reported success. `authz.LocalAuthenticator` held `Users` by value, so the boot list was the answer forever. The same boot list sat on FIVE credential surfaces. Those are the AAA chain's local backend, the web fallback, the `ze-session` cookie check, SSH public keys, and REST/gRPC `Bearer` auth. Fixing the web fallback alone changed nothing, because the chain answered first from the same snapshot | fixed at the PRODUCER: `LocalAuthenticator.UsersFunc` (`internal/component/authz/auth.go`) reads the live list per call and REPLACES `Users`, fed by one `liveLocalUsers` (`cmd/ze/hub/main_servers.go`) that every surface shares. Only a functional test found the second snapshot: a unit test cannot see a wiring fact |
| 2026-08-12 | fixit-config-validators-bypassed-at-startup | dev workflow | the spec closed on 2026-08-12 was reported broken at release-blocker priority because `./bin/ze - < config` started a daemon it must refuse. That binary answers `ze 26.08.04 (built 2026-08-04T16:17:59Z)`, eight days older than the fix commit `25689705f`, so the reproduction measured the change's absence. `mk/session.mk` gives every AI session its own `tmp/session/<date>-<sid>/bin/`, which leaves the repository-root `bin/` written only by an off-session `make` and aged out by nothing. A binary built at HEAD refuses the same bytes, exit 1 | no code change. `make ze-session-binary-path` prints the binary this session builds, and a reproduction that contradicts a committed change must read the version stamp of the binary it ran before it becomes a bug report |
| 2026-08-12 | - | docker-exec fail-open ratchet | `python3 scripts/dev/docker_exec_checked.py` counts 173 unchecked fail-open call sites against a committed floor of 171 (`test/health/docker-exec-baseline.json`), so `make ze-doc-wiring-check` is RED at HEAD. It is not a working-tree artifact: removing this session's only new interop scenario leaves the printed site list byte-identical, and the only tracked files under `test/` that differ from HEAD are two `test/health/*.json` data files and two `test/parse/*.ci`, none of which can hold a `docker exec` call. The checker's own message says "No changed test file explains the rise", which is the tell: a scenario added as an UNTRACKED file is invisible to its changed-set attribution, so whichever commit introduced the two sites recorded no explanation and the floor was never raised with them. Found while adding `test/interop/scenarios/bgp-wellknown-noexport-frr`, whose `check.py` contributes no site (it reads only asserting helpers) | not fixed. It needs the two sites identified and either checked or annotated `# fail-open-ok:`, and the floor is not a number to raise blindly. `make ze-doc-verify`, the gate `ai/rules/git-safety.md` names for a docs change, is green, so this session's evidence does not rest on it |
| 2026-08-12 | - | ipsec interop PKI fixtures | `test/interop-ipsec/lab_test.py::test_every_pki_scenario_resolves_to_decodable_der` fails on a fresh checkout with `cannot read PKI file test/interop-ipsec/pki/ze.pem`. The directory holds generated material that is not in git, and the test reads it directly rather than generating it or skipping when it is absent, so `make ze-unit-pkg-test PKG=./scripts/dev` is red for anyone who has not run the ipsec lab on that machine | not fixed. The choice between generating the fixtures in the test and skipping when the directory is empty belongs to the ipsec lab's owner |
| 2026-08-14 | fixit-test-harness-fail-open-guards | commit tooling | `scripts/dev/commit_helper.py create --append` derives the message file from `--session` and `--tag` alone, so a second block prepared with the SAME tag overwrites the first block's `tmp/commit-msg-<session>-<tag>.txt`. Both `-F` lines then read the surviving file. Closure commits A and B landed with correct CONTENT (17 files, then the spec removal) and an IDENTICAL message, commit A carrying commit B's body. Nothing warned: the helper reports `message=` and `script=` once per invocation, and the second report looks the same as the first | not fixed. The workaround is a distinct `--tag` per block, which no rule or skill states today. `ai/rules/git-safety.md` step 2 says the helper "never writes over an existing script unless `--script` names it", which is true of the SCRIPT and false of the MESSAGE, so a reader infers a protection the message file does not have. History rewriting is banned, so the two landed messages stand |
| 2026-08-15 | fixit-forward-overflow-two-tier-flake | repro tooling | `scripts/dev/stress-repro.py` reported `*** REPRODUCED on invocation 1 ***` for a suspected flake, and the reproduction was an artifact. The capture showed `unknown field in peer: attach`, a config grammar the working tree parses and a day-old `bin/ze` did not, so nothing about the suspected defect was exercised. `_bin_from_env` honours `ZE_BIN`/`ZE_TEST_BIN` but FALLS BACK to `bin/ze`. `mk/session.mk` leaves that path stale by construction, because canonical binaries build into a per-session directory. `ensure_binaries` checks only that the file exists. The tool's own docstring records this biting once before, in the OPPOSITE direction: a fix under test looked "still reproducing" because the run never contained it. So the verdict line is not evidence either way, in the one tool whose whole output is a verdict | no product change: the flake did not reproduce in 80 invocations once the session binaries were exported. The reusable check is cheaper than a diagnosis and belongs BEFORE it. Treat a reproduction whose symptom is a CONFIG PARSE ERROR as a stale binary until proven otherwise. That symptom cannot come from the defect under test. The tooling fix is filed at `plan/future/spec-stress-repro-refuses-a-stale-binary.md`, which makes the tool refuse a stale binary instead of trusting the caller |
| 2026-08-17 | hub-deferred-api-auth-independent-of-ssh-block | hub authorization reload | user authentication followed the live `ConfigProvider`, but the AAA authorizer retained the startup `authz.Store`, so profile and policy edits did not apply until restart | one atomic live local authz store is dereferenced on every local AAA decision and swapped only after `runReloadContext` fully succeeds |
| 2026-08-17 | hub-deferred-api-auth-independent-of-ssh-block | login-resolved authorization profiles | `WithProfileRecording` cached ordinary local profile assignments by username. A later accepted config generation could remove that user, but `authz.Store.Authorize` could still use the prior login result as an external-profile fallback | local authentication now records only the recovery profile, which has no config assignment. Ordinary local profiles come only from the accepted authorization store |
| 2026-08-18 | - | verify freshness certificate | `runVerify` (`scripts/status/verify_run.go`) appends every stage result in its loop and calls `cfg.WriteStatus` only after the loop ends. `writeVerifyStatus` then calls `computeTreeHash(root)` at that moment, so the status file stamps the tree as it stands when the run FINISHES and asserts that hash was verified. In a shared checkout the early stages ran against an earlier tree, so the certificate is strictly stronger than the evidence behind it. It is the artefact `scripts/dev/verify-status.sh check` reads to say FRESH and `scripts/dev/commit_helper.py` gates on, so a run spanning another session's edits certifies content its early stages never saw. Observed on 2026-08-18: one session edited `pretool-writeedit.py` and `ai/rules/*` at 13:35Z while a run started 13:22Z was at stage 14 | not fixed. The fix is a decision about what the hash should cover, not a patch: hashing at the START and refusing to write a certificate when the tree moved, or hashing per stage and recording the set each one saw |
| 2026-08-19 | spec-fixit-zefs-diff-structural-ops | job admission, parameterized make targets | The `ze-unit-pkg-test` rule in `mk/test-unit.mk` passes the fixed label `ze-unit-pkg-test` to `scripts/dev/ze-run.sh`, and PKG and RUN reach the `_ze-unit-pkg-test-impl` half only. `_scan_and_claim` in that script attaches a second asker to a running job when LABEL and TREE match, so two sessions testing DIFFERENT packages share one verdict: the second replays the first's log and exits with its code. Observed 2026-08-19: `make ze-unit-pkg-test PKG=./internal/component/cli/testing` attached to a run of `./internal/plugins/flowspec-firewall -run ^TestRouteWithNoActionIsCountedAndLogged$` and returned that test's FAIL. A false PASS is the same mechanism with the packages swapped. The script's own header states the rule this breaks: attaching on the label alone "would certify a session with a run that never saw its code" | not fixed. `MAY_ATTACH=0` makes the run queue instead, which is the escape, not the fix: the label must carry the parameters that change what the job judges (PKG and RUN), or the attach key must read the command rather than the label |
| 2026-08-18 | - | scoped verify freshness | The SCOPED route of the row above answers from an artifact nothing writes any more. `scripts/dev/verify-status.sh check <PATH...>` never reads `tree_hash`. It compares `tmp/ze-verify-manifest.txt`, and `MANIFEST_FILE` is written at one place in that script, inside its own `write` subcommand. Nothing in `scripts/`, `mk/`, the `Makefile` or `.claude/` invokes `verify-status.sh write`: `runVerify` (`scripts/status/verify_run.go`) writes the status file itself through `writeVerifyStatus` and writes no manifest. So a scoped FRESH is judged against whatever manifest a past run left behind. On this checkout that file was 20 hours older than the status file beside it. The sentinel `119eae751` added protects the UNSCOPED route alone, which is the route `commit_helper.py` gates on, so the commit gate is covered and the scoped answer is not | not fixed. Found by an independent review of `119eae751`. Either `runVerify` writes the manifest for the paths it verified, or the scoped route stops reporting FRESH when no manifest belongs to the current run |
| 2026-08-19 | - | unit-test job admission | The key that decides "this is the same work, share one run" omits the parameter that decides what the work IS. `LABEL="${1:-}"` (`scripts/dev/ze-run.sh`) is the bare target name, and `ze-unit-pkg-test` passes the literal `ze-unit-pkg-test` to it, so `PKG` and `RUN` never reach the label. The attach test is `[ "$label" = "$LABEL" ]` plus a `TREE` hash match and `STATE=running`. Two sessions running `make ze-unit-pkg-test` on DIFFERENT packages in one checkout therefore match, and the second attaches and reports the first run's exit code as its own. Same shape as the 2026-08-10 row above, where `ARCH=` was missing from the rebuild trigger and an amd64 `vmlinuz` answered an arm64 build. Observed twice by an agent this session, once reporting green for `./scripts/dev` while the package it had asked about was red, which is the direction that matters: a package that was never compiled reads as passing. Sharing is correct for a whole-tree target, where the two jobs really are the same work; it is unsound only for the parameterised ones. `MAY_ATTACH=0` in the environment is an existing opt-out and is the trustworthy route while this stands | not fixed. The repair is to put the parameters that change the work into the label, so `ze-unit-pkg-test PKG=./internal/component/bgp RUN=^TestX$` and the same target on another package stop being one job. Everything that has ever reported a scoped unit-test verdict in a shared checkout is affected, this session's own agent reports included, so a green from a concurrent run is evidence of nothing until the label carries `PKG` |
| 2026-08-19 | - | job admission opt-out | `MAY_ATTACH=0` in the environment, which the row above names as the trustworthy route while the sharing key stands unfixed, did nothing at all. `scripts/dev/ze-run.sh` assigned `MAY_ATTACH=1` unconditionally just above its admission loop, so an inherited value was destroyed before the first `_scan_and_claim` call and the `${MAY_ATTACH:-1}` test in the attach condition could never meet an unset variable. Measured by running two identical jobs in a throwaway registry: the second, with `MAY_ATTACH=0` exported, printed the attach banner and exited with the first's code. It was measured while a concurrent session was running `make ze-unit-pkg-test MAY_ATTACH=0 PKG=./internal/component/bgp/cli/`, so a session taking the documented escape was still eligible to be handed a foreign verdict | fixed: `MAY_ATTACH="${MAY_ATTACH:-1}"` honours the caller, and `_job_params` drops the name from the work key so that declining to ASK for a shared verdict never becomes a rule that nobody may share this job's own honest one. `test_the_environment_can_decline_sharing` and `test_declining_to_attach_does_not_stop_others_sharing_this_job` (`scripts/dev/ze_run_test.py`) hold both halves |
| 2026-08-21 | - | `ze-site-generate` (`Makefile`), the terminal-demo definition check | the recipe runs `render.py --all --stamp-definition-hashes`, ignoring its exit status, and then runs `render.py --all --check-definition`, re-rendering when that fails. `stamp_definition_hashes` (`demos/terminal/render.py`) writes `definition_sha256 = definition_digest(current)` into the artifact manifest for every selected demo, and `verify_assets(definition_only=True)` compares that same field. The stamp therefore makes the check pass unconditionally, so a demo whose `.tape` changed is never re-recorded and the site keeps serving the old recording. Found while judging whether spec-cli-show-bgp-is-the-command could close on an outstanding re-render: `render.py --check-definition --demo zefs-config` reports the definition changed, and the phase-4 commit message says the next site build pays for it, which the recipe does not do | not fixed. `--stamp-definition-hashes` exists to backfill the field for artifacts rendered before it existed, so removing it from this recipe would re-record every such demo, which is a cost the website owner decides. The class needs the backfill to be a one-off command rather than a stage of every build |
| 2026-08-24 | - | Zeledon weekly operator documentation | Stale documentation in `scripts/zeledon/README.md` and the module docstring in `scripts/zeledon/post_weekly.py` pointed operators at the deleted `gh-pages` weekly source tree. It named only one private location for `discord.sh` | Both files now name the canonical `website/changes/posts/<start>.md` source and document the `find_discord_sh` lookup order: `DISCORD_SH`, `~/Unix/bin/discord.sh`, `~/bin/discord.sh`, then `PATH` |
| 2026-08-24 | - | Zeledon weekly Discord archive | The 2026-08-10 update was present as three `ze-news` messages and as its canonical website source, but `scripts/zeledon/weekly/2026-08-10-weekly.md` was absent. The local archive is the `--all` idempotency record, so its absence made the posted week eligible for a duplicate send | Recreated the archive from Discord messages `1538690543318597715`, `1538690543740405622`, and `1538690548104429649`. The body matches the posted messages exactly, `posted` records their 2026-08-17 local date, and `backfilled: false` records their unstamped header |
