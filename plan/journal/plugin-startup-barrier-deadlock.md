| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-10 | fixit-vpp-ipsec-inoperable | firewall | with `firewall { backend vpp; }` the firewall plugin waits for VPP to be reachable during its Config stage while the `vpp` plugin waits at the capability barrier, so the vpp plugin never connects GoVPP and both fail after 5s with `stage stalled ... waiting_for=Capability` and `firewall-vpp: vpp not reachable` | recorded, not fixed here. Reproduced on a fresh VPP with the firewall case alone, so it is not the IPsec case's state. `./le deployment vpp-test` reaches it now that the two defects above it are fixed |
| 2026-08-19 | verify-scope-1-shared-checkout-freshness (measurement only) | `ProcessManager.Stop` (`internal/component/plugin/process/manager.go`) | **Every ze daemon stop waits out a deadlock.** The wait is a CYCLE: `runBGPEngine` (`internal/component/bgp/plugin/register.go`) blocks in `Reactor.Wait` on `r.wg`; `r.wg` still holds `Reactor.monitor`, which runs `Reactor.cleanup` (`internal/component/bgp/reactor/reactor.go`), which calls `r.api.Stop()` into `(*Server).Stop` and a NESTED `ProcessManager.Stop`, whose `Process.WaitEngine` (`engine_wait.go`) waits on `p.engineDone` -- closed only when that same bgp engine returns. The engine cannot return promptly because it is waiting on the thing waiting for it, and the comment above the wait states the opposite: "An engine that returns promptly costs nothing here". Only two timeouts break it, `pluginStopGrace` (3s) bounding `WaitEngine` and the 500ms group wait after it. Measured 3001ms + 501ms across four traced runs, matching both constants to the millisecond, with a `--pprof` goroutine dump showing all three goroutines in the cycle; reproduced standalone with no peer ever connecting, so it is not session-related. Main thread verified each link against source independently. **The cost is not only ours**: an operator stopping ze waits 3.5s and is told `plugin engine did not finish its shutdown cleanup in time, resources it installed may be left behind` on a CLEAN stop -- so either that warning is noise on every shutdown, or the resources really are leaking. In tests it is 3.50s of a 7.5s `test/plugin` case and of every `reload` case, 2.0s of every `encode` case (the runner SIGKILLs at its own 2s grace instead), and about 2030s of the plugin suite's 4545s of test-time: **45% of that suite is this one timeout**. Second row of this class; the 2026-08-10 row above is a different plugin barrier deadlock, also unfixed | not fixed, and NOT a measurement artifact: the repair is to break the cycle rather than to shorten either timeout. `Reactor.cleanup` stopping the plugin API is what re-enters the manager, so the candidates are cleanup not owning that stop, or `WaitEngine` not waiting on an engine whose return it is itself blocking. Shortening `pluginStopGrace` would only make the leak warning arrive sooner |
