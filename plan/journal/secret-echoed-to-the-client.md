| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-14 | spec-web-templ-migration | web workbench form | `templates/component/workbench_form.html` renders a password field as `input type="password" value="{{.Value}}"`. The type attribute masks the characters on screen. It does nothing to the response body. The stored secret travels to the browser in the page source. It reaches view-source, the disk cache, and any proxy or extension that reads the document. No sibling branch in the template has the same exposure, because no other field type holds a secret. The golden capture is what made it visible: `testdata/golden/component/workbench_form--fields.html` line 64 carries the literal password from the test fixture | FIXED 2026-08-15 in the interface defect pass, as a PAIR. `formFieldValue` (`internal/component/web/workbench_form.go`) renders `config.SecretDataPlaceholder` for a non-empty password field, which is what `buildLeafField` (`handler_config_leaf.go`) already did for a bcrypt leaf. The other half is `parseConfigFormFields` (`handler_config_form.go`). It skips a field whose posted value IS the placeholder. An untouched secret is neither rewritten nor deleted. Masking alone would have destroyed the secret, because that function reads an empty value as a delete. `EditorManager.SetValue` guards only the bcrypt half, and a `plaintext-password` leaf is not bcrypt. `TestWorkbenchFormNeverRendersAStoredSecret` and `TestConfigFormKeepsASecretTheOperatorDidNotTouch` cover both halves. REOPENED and CLOSED 2026-08-15 in the review follow-up. The first fix masked on the FIELD TYPE, which is a rule the schema does not write. `buildFieldMeta` (`fragment.go`) and `buildLeafField` (`handler_config_leaf.go`) still keyed on `leaf.Bcrypt`, so every `ze:sensitive` leaf rendered in plaintext on the generic config path. `l2tp/shared-secret` and the IPsec pre-shared secret were among them, and a read-only session reaches those views. The mask now reads the schema: `maskSecretLeaf` (`secret.go`) answers on `Sensitive` or `Bcrypt` and is called by both builders, by the list-table cell and by `inheritedFieldValue` (`handler_config_entry.go`). A purpose-built page holds no leaf node, so `renderPageContent` (`workbench_pages.go`) hands each page a masked display tree. The type mask in `formFieldValue` is DELETED: one property must have one rule, and that second rule is what hid this. The write half widened with it. `EditorManager.SetValue` guards any secret leaf, and `HandleConfigAdd` skips a posted placeholder. `TestNoRenderPathEmitsAStoredSecret` asserts the property over six render paths, and `TestSecretMaskingFollowsTheSchemaMarking` unmarks the leaf and requires each one to publish again. REOPENED and CLOSED a third time 2026-08-15. Six render paths were not the population. Three more published a secret, and each had its own producer. `serializeTreeAtPath` and `serializeSetAtPath` (`internal/component/web/cli_terminal.go`) masked through `config.MaskBcrypt`, so the web CLI terminal's `show` verb serialized every `ze:sensitive` leaf in the clear. That verb needs no config authorization, so any authenticated session read it. `EditorManager.ContentAtPath` (`editor.go`) reached `Editor.DisplayContentAtPath` (`internal/component/cli/editor_mask.go`), bcrypt-only again, which is the CLI bar's `show`. `EditorManager.Diff` wrote `change.Previous` and `change.Value` raw, so the commit review disclosed the secret the operator rotated and the one they typed. New `config.MaskSecrets` and `config.LeafHoldsSecret` (`internal/component/config/mask.go`) are the one whole-tree mask and the one predicate. `MaskBcrypt` stays narrow because `ze config dump` calls it before it writes `$9$`. `EditorManager.Compare` is DELETED: it returned the editor's raw text diff, and `Diff` is the one diff the web renders. `TestNoRenderPathEmitsAStoredSecret` covers nine paths now, and each new one was proven by a revert. The second round also regressed one leaf. `plaintext-password` is marked `ze:ephemeral`, never `ze:sensitive`. Deleting the field-type mask therefore published the operator's cleartext password on the telemetry form and the generic leaf view. `ze:ephemeral` answers whether a value is persisted, so it does NOT belong in the secret predicate. The two modules were corrected instead (`internal/component/ssh/yang/ze-ssh-conf.yang`, `internal/component/telemetry/exporter/yang/ze-telemetry-conf.yang`), and `TestAWriteOnlyPasswordLeafIsMarkedSensitive` walks every YANG module for a `plaintext-` leaf that is not marked |
| 2026-08-15 | - | SSH CLI config display, six producers | The SSH CLI masks a `ze:bcrypt` leaf and publishes every `ze:sensitive` leaf in the clear. `config.LeafHoldsSecret` (`internal/component/config/mask.go`) is the one predicate the web half reads. `internal/component/cli` was fenced out of the pass that widened it. SIX producers in FIVE files publish the value. `computeTreeAnnotatedDiff` (`diff_tree.go`) and `Editor.annotatedViewOf` (`editor_annotated.go`) call `config.MaskBcrypt`. So do `Editor.DisplayContentAtPath` and `Editor.DisplayOriginalContentAtPath` (`editor_mask.go`). Each of those also answers the raw text when the parse failed. `Model.renderTreeAtPath` (`model_commands_show.go`) calls it for the set format. `Editor.activeContentAtPath` and `Editor.inactiveContentAtPath` (`editor.go`) call `config.MaskBcryptInPlace`. `Model.cmdShowFiltered` reaches them for `display active` and for `display inactive`. A seventh site is already correct, and it spells the predicate a THIRD way. `Model.searchConfig` (`model_search.go`) builds the union of `SensitiveKeys()` and `BcryptKeys()`. It keys that union on the leaf NAME, not on the schema node. One property, three spellings, one of them wrong. The names `showConfiguration` and `diffTrees` do not exist in this package. The producers above are what `gopls symbols` reports | not fixed, recorded. This row is the population, not a sample. `config.MaskSecrets` is the mask to call. `config.MaskBcrypt` must stay narrow, because `ze config dump` calls it before it writes `$9$`. The write half is already closed. `config.RejectMaskedSecretLeaves` refuses a masked `ze:sensitive` value at commit, at load and at upload. A fix to the read half needs no paired guard |
