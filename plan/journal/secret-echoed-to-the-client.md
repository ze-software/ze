| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-06 | - | the CLI session transcript, `TranscriptWriter.Record` (`internal/component/cli/transcript.go:62`) | Found while answering the audit-and-log question the `ze config set` fix was commissioned with. The transcript writes the operator's RAW command line to `~/.local/share/ze/transcripts/transcript-*.log`, so `ze cli -c "... plaintext-password hunter2"` stores the credential in a file. `redact.Command` (`internal/core/redact/redact.go`) already answers exactly this shape and is applied to the SSH exec LOG (`loggedCommand`), and not here. Three of the four wirings are the one-shot and interactive clients (`internal/component/cli/client/main.go:217,297,423`); the fourth is `ze config edit` (`internal/component/config/cli/cmd_edit.go:266`). Reach is BOUNDED and read at the producer: `WrapExecutorWithTranscript` wraps the OPERATIONAL executor alone (`Model.executeOperationalCommand`, `model_mode.go:177`), so a `set` typed in config mode is never recorded, and only the `-c` path (`executeWithTranscript`, `client/main.go:477`) records an arbitrary line. The file is 0600 in a 0700 directory on the operator's own machine, and `ze.cli.transcript` defaults to false | not fixed. One line: run the command through `redact.Command` in `TranscriptWriter.Record`, which is what the SSH log already does. Not verified: whether a config-mode `set` can reach `-c` at all |
| 2026-08-14 | spec-web-templ-migration | web workbench form | `templates/component/workbench_form.html` renders a password field as `input type="password" value="{{.Value}}"`. The type attribute masks the characters on screen. It does nothing to the response body. The stored secret travels to the browser in the page source. It reaches view-source, the disk cache, and any proxy or extension that reads the document. No sibling branch in the template has the same exposure, because no other field type holds a secret. The golden capture is what made it visible: `testdata/golden/component/workbench_form--fields.html` line 64 carries the literal password from the test fixture | FIXED 2026-08-15 in the interface defect pass, as a PAIR. `formFieldValue` (`internal/component/web/workbench_form.go`) renders `config.SecretDataPlaceholder` for a non-empty password field, which is what `buildLeafField` (`handler_config_leaf.go`) already did for a bcrypt leaf. The other half is `parseConfigFormFields` (`handler_config_form.go`). It skips a field whose posted value IS the placeholder. An untouched secret is neither rewritten nor deleted. Masking alone would have destroyed the secret, because that function reads an empty value as a delete. `EditorManager.SetValue` guards only the bcrypt half, and a `plaintext-password` leaf is not bcrypt. `TestWorkbenchFormNeverRendersAStoredSecret` and `TestConfigFormKeepsASecretTheOperatorDidNotTouch` cover both halves. REOPENED and CLOSED 2026-08-15 in the review follow-up. The first fix masked on the FIELD TYPE, which is a rule the schema does not write. `buildFieldMeta` (`fragment.go`) and `buildLeafField` (`handler_config_leaf.go`) still keyed on `leaf.Bcrypt`, so every `ze:sensitive` leaf rendered in plaintext on the generic config path. `l2tp/shared-secret` and the IPsec pre-shared secret were among them, and a read-only session reaches those views. The mask now reads the schema: `maskSecretLeaf` (`secret.go`) answers on `Sensitive` or `Bcrypt` and is called by both builders, by the list-table cell and by `inheritedFieldValue` (`handler_config_entry.go`). A purpose-built page holds no leaf node, so `renderPageContent` (`workbench_pages.go`) hands each page a masked display tree. The type mask in `formFieldValue` is DELETED: one property must have one rule, and that second rule is what hid this. The write half widened with it. `EditorManager.SetValue` guards any secret leaf, and `HandleConfigAdd` skips a posted placeholder. `TestNoRenderPathEmitsAStoredSecret` asserts the property over six render paths, and `TestSecretMaskingFollowsTheSchemaMarking` unmarks the leaf and requires each one to publish again. REOPENED and CLOSED a third time 2026-08-15. Six render paths were not the population. Three more published a secret, and each had its own producer. `serializeTreeAtPath` and `serializeSetAtPath` (`internal/component/web/cli_terminal.go`) masked through `config.MaskBcrypt`, so the web CLI terminal's `show` verb serialized every `ze:sensitive` leaf in the clear. That verb needs no config authorization, so any authenticated session read it. `EditorManager.ContentAtPath` (`editor.go`) reached `Editor.DisplayContentAtPath` (`internal/component/cli/editor_mask.go`), bcrypt-only again, which is the CLI bar's `show`. `EditorManager.Diff` wrote `change.Previous` and `change.Value` raw, so the commit review disclosed the secret the operator rotated and the one they typed. New `config.MaskSecrets` and `config.LeafHoldsSecret` (`internal/component/config/mask.go`) are the one whole-tree mask and the one predicate. `MaskBcrypt` stays narrow because `ze config dump` calls it before it writes `$9$`. `EditorManager.Compare` is DELETED: it returned the editor's raw text diff, and `Diff` is the one diff the web renders. `TestNoRenderPathEmitsAStoredSecret` covers nine paths now, and each new one was proven by a revert. The second round also regressed one leaf. `plaintext-password` is marked `ze:ephemeral`, never `ze:sensitive`. Deleting the field-type mask therefore published the operator's cleartext password on the telemetry form and the generic leaf view. `ze:ephemeral` answers whether a value is persisted, so it does NOT belong in the secret predicate. The two modules were corrected instead (`internal/component/ssh/yang/ze-ssh-conf.yang`, `internal/component/telemetry/exporter/yang/ze-telemetry-conf.yang`), and `TestAWriteOnlyPasswordLeafIsMarkedSensitive` walks every YANG module for a `plaintext-` leaf that is not marked |
| 2026-08-15 | web-htmx4-prepare | web config form, the refusal message | `maskSecretInMessage` (`internal/component/web/secret.go`) removed a refused secret with `strings.ReplaceAll(message, value, placeholder)`. The message is not written with the value; it is written with `%q`. `config.ValidateValue` (`internal/component/config/schema.go`) writes `invalid uint16: %q` and `validateUniqueOnSet` writes `duplicate %s %q`. A secret carrying a quote or a backslash therefore appears in the message ESCAPED, and the raw text the mask searches for is nowhere in it. The escaping hid the leak from the test as well: the fragment runs the message through `html.EscapeString`, so a body carrying `hunter2\&#34;4471bc` does not contain the raw value and an assertion written as `NotContains(body, secret)` passes over a leaking response. The mask reached the browser through two paths at once, the error fragment and the toast built from it | FIXED at closure. `maskSecretInMessage` also replaces `strconv.Quote(value)` minus its outer quotes, which is exactly what `%q` wrote. `TestRefusedSecretWithAQuoteNeverReachesTheBrowser` (`handler_error_test.go`) drives `POST /config/set/` and asserts on a distinctive TAIL of the value rather than the whole of it, because the whole is absent from a leaking body too. Proven by reverting the fix: the unfixed mask answers `invalid uint16: "hunter2\"4471bc"`. The general lesson is that a mask must know the FORMAT VERB that wrote the message it cleans, not only the value |
| 2026-08-15 | - | SSH CLI config display, six producers | The SSH CLI masks a `ze:bcrypt` leaf and publishes every `ze:sensitive` leaf in the clear. `config.LeafHoldsSecret` (`internal/component/config/mask.go`) is the one predicate the web half reads. `internal/component/cli` was fenced out of the pass that widened it. SIX producers in FIVE files publish the value. `computeTreeAnnotatedDiff` (`diff_tree.go`) and `Editor.annotatedViewOf` (`editor_annotated.go`) call `config.MaskBcrypt`. So do `Editor.DisplayContentAtPath` and `Editor.DisplayOriginalContentAtPath` (`editor_mask.go`). Each of those also answers the raw text when the parse failed. `Model.renderTreeAtPath` (`model_commands_show.go`) calls it for the set format. `Editor.activeContentAtPath` and `Editor.inactiveContentAtPath` (`editor.go`) call `config.MaskBcryptInPlace`. `Model.cmdShowFiltered` reaches them for `display active` and for `display inactive`. A seventh site is already correct, and it spells the predicate a THIRD way. `Model.searchConfig` (`model_search.go`) builds the union of `SensitiveKeys()` and `BcryptKeys()`. It keys that union on the leaf NAME, not on the schema node. One property, three spellings, one of them wrong. The names `showConfiguration` and `diffTrees` do not exist in this package. The producers above are what `gopls symbols` reports | FIXED 2026-08-15. Every display producer now calls `config.MaskSecrets`, and `config.MaskBcrypt` stays narrow for `ze config dump`. The row named six producers and the population was SEVEN. `Editor.blameView` (`editor.go`) served the blame view of `show` through `config.SerializeBlame` with no mask at all, so it published the `ze:bcrypt` hash the other six already hid. `Editor.SensitiveKeys`, `Editor.BcryptKeys` and `config.BcryptKeys` are DELETED with the search union that was their only caller. `Editor.setView` is now `displaySetView` and masks the tree, so search needs no predicate. `config.MaskBcryptInPlace` is replaced by `config.MaskSecretsInPlace`. The second-order effect landed here as it did on the web. Two masked texts read as equal. The config view and the compare view of `show` therefore answered that a rotated credential had not moved. `Editor.changedSecretsAt` and `Editor.changedSecretsBetween` (`editor_mask.go`) name the leaf from the UNMASKED trees. `viewportData.secretChanges` carries it, and `secretChangeLines` (`model_render.go`) writes the wording the web writes. One producer improved beyond the row. The display parse paths retry with `parseConfigLenient`, so a config that holds one stale field masks rather than publishing its own text. `TestNoCLIRenderPathEmitsAStoredSecret` covers thirteen render paths. `TestCLISecretMaskingFollowsTheSchemaMarking` unmarks the fixture leaf and requires each path to publish again. Every producer was proven by a revert. TWO holes stay OPEN and neither is in this package. `ze config show` (`cmdShow`, `internal/component/config/cli/cmd_show.go`) writes `ed.ContentAtPath` and `ed.Tree().ToMap()`, both unmasked. That command prints every secret, and its sibling `ze config dump` masks. `Editor.Diff` (`editor.go`) diffs raw text. It reaches the audit sink through `Model.recordConfigCommit`, and the REST API through `internal/component/api/config_session.go`. A config that parses NOWHERE still falls back to raw text on purpose. The schema resolves no leaf to mark there. The text is the on-disk form rather than a decoded secret. `TestModelContextHighlighting` is the requirement that the operator can still read the line to repair. BOTH CLOSED 2026-08-15. `showConfig` (`cmd_show.go`) writes `ed.DisplayContentAtPath` for the text. It writes `ed.DisplayTreeAtPath` for the JSON. The map was the half that mattered. Masking the text alone repeats the error this chain has made four times. `Editor.DisplayTreeAtPath` (`editor_mask.go`) is the new producer. It resolves a path with the rules `WalkPath` uses. `Editor.Diff` (`editor.go`) now diffs `DisplayOriginalContentAtPath` against `DisplayContentAtPath`. The row named two sinks and there are FIVE call sites. The commit audit record and the REST and gRPC session diff are two of them. The hub adapter (`cmd/ze/hub/editor_adapter.go`) is the third. The `--dry-run` form of `ze config set` and of `ze config deactivate` are the other two. One producer therefore closed all five. The second-order effect landed here, and a second defect came with it. Both masked sides read alike, so the line diff answered that a rotated credential had not moved. `changedSecretsAt` names the leaf. It writes the shared `secretChangeLine` (`editor_mask.go`) the config view already used. The second defect predates this pass. `Editor.Diff` compared RAW text, and `SetValue` writes the tree alone. Every tree edit therefore diffed as no change, so `ze config set --dry-run` printed nothing at all. Both sides now come from the tree. `TestNoCLIRenderPathEmitsAStoredSecret` covers sixteen render paths. `TestConfigShowMasksASecret` (`cmd_show_test.go`) covers the four `ze config show` shapes over a real `ze:sensitive` leaf. Each new path was proven by a revert. REOPENED and CLOSED a fourth time 2026-08-15 after an independent review. A THIRD command in the same directory published both halves of a rotation. `cmdDiffImpl` (`internal/component/config/cli/cmd_diff.go`) loads through `loadAndResolve`, which parses and therefore decodes the `$9$` value. `outputDiffText` printed `~ path: old -> new` and `outputDiffJSON` emitted the same pair. `maskDiffSecrets` masks the COMPUTED diff rather than the two trees. The rotation still reports as changed, and neither value is shown. Masking the trees first would give both sides one placeholder, which is the second-order effect this chain met twice before. `config.SecretKeys` (`mask.go`) answers the leaf NAME set from `LeafHoldsSecret`. `ResolveBGPTree` flattens group and peer inheritance, so a path in the resolved map addresses no schema node. `Editor.DisplayTreeAtPath` (`editor_mask.go`) failed OPEN where every sibling fails closed. `config.MaskSecrets` answers its input unchanged when the schema is nil, so that function returned the live tree and `ze config show --json` encoded it. New `Editor.displayTree` is the one branch set both display halves read. It answers nil for a nil schema, and nil for a config that parses nowhere. `showConfig` (`cmd_show.go`) reports that parse failure rather than writing `{}`. The raw-text fallback stays, because the operator must read the broken line to repair it. Its safety claim is DELETED. `Editor.WorkingContent` serializes the tree and `commitContent` writes it, and no serializer re-encodes `$9$`. A committed file therefore holds every `ze:sensitive` value in the clear. Cleartext at rest is a separate product question for Thomas. `TestConfigDiffMasksASecret` and `TestConfigDiffJSONNamesARotatedSecret` (`cmd_diff_test.go`), `TestConfigShowUnparsableConfig` (`cmd_show_test.go`) and `TestDisplayTreeAtPathFailsClosed` (`secret_test.go`) were each proven by a revert. `secretCLIRenders` covers eighteen render paths now |
| 2026-08-18 | - | update-check and self-update fetch URLs | The secret is not a config leaf this time, so every mask in the rows above is the wrong shape for it: it is the userinfo inside an operator-supplied URL, and it reaches the log in full. `ValidateUpdateCheckURL` (`internal/component/config/system/update.go`) admits a URL on `strings.HasPrefix(rawURL, "https://")` and never calls `url.Parse`, so `https://user:password@host` is accepted as well formed. `(*UpdateChecker).fetch` in the same file then writes the raw string into a `logger.Warn` attribute and into a formatted error, and `selfupdate.go` logs `su.url` and the download URL the same way. `internal/install/disk/download.go` repeats it for the installer image. Go's own `http.Client` strips the password from the `*url.Error` it returns, so a wrapped transport error is safe and every leak here is a hand-written `%s` or `slog` attribute. `internal/core/redact/redact.go` covers config keys and command tokens and has no URL form | not fixed. Two halves: parse the URL in the validator so its structure is judged rather than its first eight characters, and give `redact` a URL form every logging site uses. Not verified: whether the stored `UpdateStatus` carries the URL onward to the CLI or the web |
| 2026-09-03 | rir-delegation-source-override | `ValidateUpdateCheckURL` (`internal/component/config/system/update.go`), reused as the rule for a RIR delegation mirror | The FIRST half of the row above, met from a second direction and carrying a hole that row did not name. The validator judged the loopback exception by PREFIX as well, `strings.HasPrefix(rawURL, "http://127.0.0.1")`, so `http://127.0.0.1.example.com/ze` passed as loopback and the binary would have been fetched over plain HTTP from a host the operator does not own. Its localhost branch was already exact-spelling, so only the `127.0.0.1` half was open. It guards three call sites: the `update-check` leaf on both backends, and `resolveDownloadURL` (`selfupdate.go`), where the URL is chosen by whatever server answered the manifest. Walked into by reusing the rule rather than copying it, which is why it was read at all | the first half is FIXED. The rule is now `config.ValidateFetchURL` (`internal/component/config/validators.go`): it calls `url.Parse` and compares `parsed.Hostname()` against `127.0.0.1`, `::1` and `localhost`, so structure is judged rather than the first eight characters, and `ValidateUpdateCheckURL` delegates to it. Proven from both entry points and both walked red against the restored prefix form: `TestALoopbackPlainHTTPURLIsAcceptedAndALookalikeIsNot` (`validators_test.go`) and the two lookalike cases in `TestSelfUpdateDownloadURLValidation` (`selfupdate_test.go`), which reports `"http://127.0.0.1.example.com/ze" was accepted as loopback` against the old body. The SECOND half is still open and unchanged: the userinfo inside an accepted URL still reaches `logger.Warn` and the formatted errors in `update.go`, `selfupdate.go` and `internal/install/disk/download.go`, and `internal/core/redact/redact.go` still has no URL form. Parsing the URL does not redact it |
| 2026-09-06 | password-weakness-warning | `ze config set`, the confirmation line it writes to stderr | A NINTH producer, in a directory the 2026-08-15 pass closed four times over. `cmdSetImpl` (`internal/component/config/cli/cmd_set.go`) writes `fmt.Fprintf(os.Stderr, "set %s %s\n", displayPath, value)` at `:128`, and the dry-run branch writes `"dry-run: would set %s %s\n"` at `:110`. Both take the operator's RAW value, so `ze config set ... plaintext-password hunter2` echoes the password back on stderr. Read at the producer, not inferred. Every mask this class built is the wrong shape for it: `config.MaskSecrets`, `MaskSecretsInPlace`, `Editor.DisplayTreeAtPath` and `maskSecretInMessage` all clean a TREE, a DIFF or an ERROR MESSAGE, and this is none of those. It is the command's own acknowledgement of what the operator typed, written before any tree exists to mask. The 2026-08-18 row met the same shape from the other direction (a secret inside a URL, where every leaf mask was equally the wrong shape) and its second half is still open, so this is the second recorded escape from the leaf-shaped assumption. Found while implementing the password weakness warning, whose agent saw it, judged it out of its scope and reported it rather than folding it in, which is correct | not fixed at the time of writing; the fix is commissioned. The predicate exists and is the one this class already standardised on: `config.LeafHoldsSecret` (`internal/component/config/mask.go`) answers on `Sensitive` or `Bcrypt` from the schema node at the path. What it CANNOT answer is the case that matters most here, because `plaintext-password` is marked `ze:ephemeral` rather than `ze:sensitive` -- the 2026-08-15 row records that `ze:ephemeral` was deliberately kept OUT of the secret predicate, since it answers whether a value is persisted, and that the two modules carrying such a leaf were marked `ze:sensitive` individually instead. So the fix must confirm the leaf under test is marked, or it will mask everything except the password that prompted the row. `TestAWriteOnlyPasswordLeafIsMarkedSensitive` walks every YANG module for a `plaintext-` leaf that is not marked, and is the check that decides whether that hole is open. FIXED 2026-09-06. That hole is CLOSED: the check passes, and both `plaintext-password` leaves (`internal/component/authz/yang/ze-authz-conf.yang:52`, `internal/component/telemetry/exporter/yang/ze-telemetry-conf.yang:160`) carry `ze:sensitive` beside `ze:ephemeral`. The row named two producers and the population was EIGHT, in three packages. The shape is the one no mask fitted, so it got its own pair: `config.DisplayValueAtPath` answers the text a command may ECHO, and `config.DisplayMessageAtPath` answers the sentence that REFUSES it. Both read `secretAtPath`, which reads `LeafHoldsSecret` through new `Schema.LookupTokenPath` (`schema.go`), the schema-only walk over an operator's token path that steps over a list key. It FAILS CLOSED where `Editor.DisplayTreeAtPath` failed open: a nil schema and an unresolved path both answer the placeholder. `config.MaskSecretInMessage` is the web's `maskSecretInMessage` body promoted whole, and the web function now delegates, so the `%q` escape rule this class learned on 2026-08-15 has ONE home. The eight: `cmdSetImpl`'s success line, its dry-run line and its refusal (`internal/component/config/cli/cmd_set.go:128,110,97`); `Model.cmdSet`'s status line and refusal, and `Model.cmdInsert`'s (`internal/component/cli/model_commands_edit.go`); `executeTerminalSet` (`internal/component/web/cli_terminal.go`), which answered the value into the response body; and the adoption prompt of `ze config edit` (`cmd_edit.go:634`) through `PendingChange.Summary`, which now REQUIRES a schema so no unmasked spelling of it remains. A NINTH sink came with it and is the same property from the commit side: `Conflict.MyValue`, `OtherValue` and `PreviousValue` were raw leaf values, so a commit conflict published the credential ANOTHER operator typed, over six renderers. Masking at the four constructions (`editor_commit.go:114,302,686`, `editor_draft.go:650`) closed all six, and the path stays in the clear so the report still says which leaf is contested. `cmdInsert` is the one producer with no red-provable case: `ze:sensitive` and `ze:bcrypt` are fields of `LeafNode` alone, so no leaf-list can be marked and its secret polarity is unreachable by construction. Every other producer was proven by a revert, each asserting on a distinctive TAIL as well as the whole value: `TestConfigSetNeverEchoesASecret`, `TestConfigSetRefusalNeverEchoesASecret` and `TestConfigSetStillEchoesAValueTheSchemaDoesNotMark` (`cmd_set_secret_test.go`); `TestSSHCLISetNeverEchoesASecret`, `TestSSHCLISetStillEchoesAValueTheSchemaDoesNotMark`, `TestCommitConflictNeverEchoesASecret` and `TestPendingChangeSummaryNeverEchoesASecret` (`model_commands_edit_secret_test.go`); `TestWebTerminalSetNeverEchoesASecret` and `TestWebTerminalSetStillEchoesAValueTheSchemaDoesNotMark` (`cli_terminal_secret_test.go`). Each fix carries the opposite polarity, because a mask that fails closed on an unresolved path would hide EVERY value and satisfy the first assertion. The audit record and the log are CLEAN and were read at the producer: `Model.recordConfigCommit` takes `Editor.Diff()`, which the 2026-08-15 pass masked, and `cmdSetImpl` records no audit entry at all; nothing under `internal/component/config/cli/` logs a value, and the SSH exec log is redacted by `loggedCommand` (`internal/component/ssh/passwordauth.go:19`). ONE sink stays OPEN and is a row of its own below |
