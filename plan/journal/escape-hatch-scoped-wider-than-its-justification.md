| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-11 | relax-token-gate-is-per-file-not-per-change | hooks | `c_test_weakening`'s `test-relax:` hatch was matched against the whole new text. On `Write` that is the whole FILE, so one token written months earlier exempted every later overwrite of it: 468 test files, about a tenth of the suite, could be gutted silently. The audit had the mirror defect, attributing an added reason by tail slice so a token inserted anywhere but last made it quote a different, pre-existing justification back at the reviewer. One shape behind both: the token was matched against a FILE or a POSITION, never against the change that needed it | the hatch opens only on a justification the edit WRITES, keyed on the normalized sentence AND a raised count (`_writes_new_relax_reason`); the audit diffs reason multisets. An escape hatch must name the ONE thing it excuses, or it is a switch. Keying it on raw lines is not enough either: one space, or one zero-width character, made a months-old justification read as fresh |
| 2026-08-17 | fixit-review-loop-has-no-termination-bound | review gate tooling | The Review Gate loop had no state in which it stopped. Its first bound was a cap of three passes in `cmd_record` (`scripts/dev/review_gate.py`). Any `--rounds-reason` the same session wrote lifted that cap, without limit. The only toll was a sentence the constrained party composed | `ROUND_CAP` is 5, and a new `ROUND_OWNER_CAP` refuses a sixth round without `--owner-authorised` carrying Thomas's word. Both tolls are owed past the cap, and neither substitutes for the other. The rule bans an unasked use of that flag, in the same shape as the standing ban on `--push`. General practice: a cap whose exemption the capped party writes is a switch. Give the extra pass a toll only somebody else can pay |
| 2026-08-18 | - | hooks | `check_governed_doc_edit` (`.claude/hooks/pretool-bash.py`) admitted its escape on an EMPTY reason. It tested the raw command text with `["']?\S`. The optional quote ate the opening quote and `\S` matched the CLOSING one, so `ZE_ADMIT_GOVERNED_WRITE=""` and `=''` both admitted. A bare `=` refused. The reason is the only thing that makes this bypass auditable, because the escape's whole justification is that the reason lands in the transcript. Two things made it worse than a corner case. The refusal message prints the QUOTED spelling as the suggested form, so the cheapest copy-paste is the one that defeats the guard. And the fixture `governed-escape-needs-a-reason` covered the bare form alone. That is the one spelling the old test got right, so the fixture was green and vacuous. `ZE_ADMIT_RAW` never had this defect, because `_raw_job` strips the quotes first | `_governed_admit_reason` strips `'` and `"` before the emptiness test, the way `_raw_job` does. Four fixtures now cover the bare form, `=""`, `=''` and a whitespace-only reason. Each one fails against the old test. General practice: an escape hatch that takes a reason MUST strip the quotes, then test the reason. Its fixture MUST carry the spelling the refusal message suggests, not only the spelling that already passed |
