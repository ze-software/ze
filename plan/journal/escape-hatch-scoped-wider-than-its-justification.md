| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-11 | relax-token-gate-is-per-file-not-per-change | hooks | `c_test_weakening`'s `test-relax:` hatch was matched against the whole new text. On `Write` that is the whole FILE, so one token written months earlier exempted every later overwrite of it: 468 test files, about a tenth of the suite, could be gutted silently. The audit had the mirror defect, attributing an added reason by tail slice so a token inserted anywhere but last made it quote a different, pre-existing justification back at the reviewer. One shape behind both: the token was matched against a FILE or a POSITION, never against the change that needed it | the hatch opens only on a justification the edit WRITES, keyed on the normalized sentence AND a raised count (`_writes_new_relax_reason`); the audit diffs reason multisets. An escape hatch must name the ONE thing it excuses, or it is a switch. Keying it on raw lines is not enough either: one space, or one zero-width character, made a months-old justification read as fresh |
