| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-04 | bgp-attribute-defaults | the LOCAL_PREF ze puts on an UPDATE it sends to an internal peer when the operator configured none | The number 100 is written as a Go literal by six producers on five rails, with no declaration behind any of them: `packAttributesWithASPath` (`internal/component/bgp/rib/commit.go`), `(*UpdateBuilder).BuildUnicast` and `(*UpdateBuilder).BuildFlowSpec` (`internal/component/bgp/message/`), `buildBatchAnnounceUpdate` and `buildRIBRouteUpdate` (`internal/component/bgp/reactor/`), and `(*RIBManager).extractCandidate` (`internal/component/bgp/plugins/rib/rib_commands.go`). RFC 4271 Section 9.1.1 supplies no number and calls the degree of preference "a local matter", so 100 is a policy value ze inherited from FRR (`BGP_DEFAULT_LOCAL_PREF`) and BIRD (`default_local_pref`), and both of those daemons expose it as a knob. Found by the grep that validated assumption A-4 of `spec-bgp-attribute-defaults`, which had assumed no second reader of a default LOCAL_PREF existed; that assumption is broken | not fixed, and out of that spec's scope, which is the base the `increment` and `decrement` arithmetic starts from. That spec declares its own number once, at `bgp/defaults/attribute/local-preference` in `ze-bgp-conf.yang`, and the leaf's help says it does NOT supply this second value. The two are different facts that share a number today, so the repair is a decision about the iBGP emission default rather than a delete: whether the operator gets a knob for it, which is the half of FRR's `default_local_pref` ze does not implement, and that spec's Known Limitations name it as wanting its own spec |
| 2026-09-04 | - | the top-level `peer` command container, shared by `ze-cli-announce-cmd.yang`, `ze-raw-cmd.yang` and `ze-update-cmd.yang` (`internal/component/bgp/plugins/cmd/`) | The peer selector is declared twice for one merged container, and the two declarations publish different grammar. `ze-cli-announce-cmd` states `leaf selector { mandatory true; }` on the `peer` container itself, which `inheritArgDefs` (`internal/component/config/yang/command.go`) anchors to the `peer` keyword for every command below it: `peer <selector> announce unicast`, `peer <selector> withdraw all`. `ze-raw-cmd` and `ze-update-cmd` each declare a `selector` leaf on their COMMAND instead, and `withInheritedArgDefs` prefers a command's own leaf over the inherited one, so the model publishes `peer raw <selector>` and `peer update <selector>` with the value after the verb. The announce module's own 2026-08-31 revision says the selector belongs on the container and strips it from `withdraw all` for that reason; these two were left behind. Consequence beyond the inconsistency: `raw` and `update` are the only verbs an operator types immediately after `peer`, so they are the only two collisions with a peer NAME that the top-level container produces (`PeerSubcommandKeywords`, `internal/component/plugin/server/rpc_register.go`). Found while correcting that derivation to read the merged schema | not fixed. The repair is to delete both `leaf selector` declarations so the container's reaches them, which moves the published line to `peer <selector> raw` and `peer <selector> update`, and it changes what an operator types, so it needs the dispatcher tests, the help output and the peer-name reserved list judged together. SUPERSEDED 2026-09-04 by `plan/immediate/spec-fixit-selector-narrows-through-a-pipe.md`: Thomas ruled that an action's selector is typed before the verb and named the target form himself, "request peer <sel> raw ...". The spec moves `raw`, `update`, `announce` and `withdraw` off the top-level `peer` root and under `request peer`, which already declares the mandatory selector, so the published line becomes `request peer <selector> raw` rather than `peer <selector> raw` and the asymmetry closes as a consequence of the move. The `PeerSubcommandKeywords` collision list follows the merged tree, so it re-derives with no edit; the spec's R-8 holds the judgement this row asked for |
| 2026-09-04 | fixit-bgp-distance-declaration | cross-protocol route selection, every protocol that inserts into the Loc-RIB | Administrative distance is declared once in config and a SECOND time in each producer, and the two arbitration points read different copies. `rib { distance { } }` (`internal/component/sysrib/yang/ze-rib-conf.yang`) is the operator's declaration, and `(*sysRIB).effectivePriority` (`internal/component/sysrib/sysrib.go`) applies it. But each producer also stamps `locrib.Path.AdminDistance` from a private constant at insert: `spf.DefaultAdminDistance` 115 for IS-IS (`internal/plugins/isis/spf/install.go`), 110 for OSPF (`internal/plugins/ospf/spf/install.go`), and `DefaultAdminDistanceEBGP`/`IBGP` 20 and 200 for BGP (`internal/component/bgp/plugins/rib/rib_distance.go`). `locrib.selectBest` (`internal/core/rib/locrib/entry.go`) then picks the lowest STAMPED value, and it never reads the declaration. So `rib { distance { isis 5 } }` moves IS-IS ahead of BGP in sysrib and leaves it at 115 inside the Loc-RIB, where the two protocols have already met. All three producers call `InsertForward` on the same `*locrib.RIB`, so the two layers really can hold one prefix from different protocols. Found while removing the BGP half of a different duplication; the config side is now one declaration and this producer side is not | FIXED 2026-09-04, in the spec that found it, after an independent closure gate REFUSED to close on it. The row first said "not fixed, and deliberately so", recording that the owner had been offered three options and taken the third, land the sysrib half now. That framing was WRONG and the gate said so: the producer copies are not a separable defect, they are the spec's own AC-2, AC-5, AC-9 and User Story 2, and `ai/rules/completion.md` does not permit recording AC-required behavior instead of building it. The author had put it to the owner as a scope question three times without ever establishing that it broke the spec's acceptance criteria. The fix is a seam, `internal/core/rib/distance`, mirroring `internal/core/rib/igpcost`: sysrib publishes the resolved table on every configure and rollback (`publishDistances`, `internal/component/sysrib/register.go`), and all three producers read it AT THE STAMP SITE so a reload takes effect. One deliberate divergence from igpcost, which is the lesson worth keeping: igpcost answers 0 for an unset seam because 0 means "no interior cost known" there, while 0 is the BEST distance here, the one `connected` holds, so an unset distance seam answering 0 would silently beat every protocol. `Of` reports whether the declaration answered and `OrDefault` takes the caller's bootstrap, covered by `TestUnsetSeamDoesNotAnswerZero` |
| 2026-08-31 | - | `/project/activity/` under the dark theme | `activityStyle` (`internal/le/site/activitystyle.go`) states the widget's surfaces as near-white literals, an open-coded copy of the light half of the token palette: the hero is `linear-gradient(135deg, rgba(255, 254, 254, 0.96), rgba(233, 245, 255, 0.84))` and the cards follow it, while every text color reads `var(--text)`. `:root[data-theme="dark"]` (`website/assets/css/00-tokens.css:87`) and the `prefers-color-scheme: dark` block at `:241` move the text to a light ink and leave those literal surfaces where they are, so a reader whose browser asks for dark meets pale labels on white cards. Measured by rendering the same widget twice, once under each theme, while giving a talk deck the same page as an embed | not fixed. The embed states `data-theme="light"` (`renderActivity`, `internal/le/site/presentations.go`), which is a decision an embed can make and a published page cannot: the page follows its reader. The repair is to state those surfaces as tokens so the dark palette can move them |
| 2026-08-31 | yang-short-and-long-command-help | MCP tool generation, the `peer` argument on a depth-1 tool group | `groupCommands` (`internal/component/mcp/tools.go`) fills the same `action` struct in three arms, and the second-pass depth-1 arm omits `takesSelector` where the other two set it. A group built by that arm therefore reports `groupTakesSelector` false, and `buildToolDef` advertises no `peer` property. A model driving MCP then cannot address a peer on a command whose dispatcher DOES read an inline selector, such as a two-token `peer detail` in a prefix with no three-token sibling. The field's own comment says MCP must never infer it from the command name, which is exactly what an absent advert forces the model to do. Found while splitting the two help texts across the same three arms | not fixed. It is unrelated to the help split, which passes both texts through all three arms. The repair is one shared constructor for the three arms, so a field added to `action` cannot reach two of them and miss the third |
| 2026-08-31 | yang-short-and-long-command-help | web L2TP workbench form field help | `buildL2TPConfigFormData` (`internal/component/web/page_l2tp.go`) hand-writes the `Description` of all nine L2TP form fields as Go string literals, beside the YANG leaves that already declare the same fact. `buildFieldMeta` (`internal/component/web/fragment.go`) reads a leaf description off the schema for every other config form. This page is an open-coded copy of that one derivation. The two have already diverged. The YANG `hello-retries` description (`internal/component/l2tp/yang/ze-l2tp-conf.yang:98`) is a nine-line explanation. It names the ZLB ACK, the 31s retransmit exhaustion and the default. The page shows `Unanswered HELLO intervals before a peer is declared dead (0 disables; detection = retries x interval)`. An operator reading the web form and an operator reading `ze config` get different accounts of one leaf. No gate compares them. Found by the command help split, which changes every YANG-fed help surface and leaves this page unmoved | not fixed. It is out of the spec's scope, which is COMMAND help rather than CONFIG leaf help, and the spec's Known Limitations already record that config leaves keep one `description`. The repair is to read `l2tp/<leaf>` off the schema the way `buildFieldMeta` does, and to delete the nine literals rather than correct them |
| 2026-08-29 | - | `ze bgp rib out` route rendering, VPN families | `extractNextHopFromMPReach` (`internal/component/bgp/plugins/rib/ribout_entry.go`) is a second reader of the MP_REACH Network Address of Next Hop, open-coded beside `extractMPNextHopAddr` (`rib_bestchange.go`). It switches on the octet count and its `default` arm hands any other length straight to `net.IP(...).String()`. RFC 4364 Section 6.1 makes a VPNv4 next hop 12 octets (an 8-octet Route Distinguisher then the address) and RFC 4659 Section 3.2.1.1 makes the VPNv6 form 24 or 48, so `net.IP` sees a length it does not recognise and `Route.NextHop` renders as `?`. Display only: the value is built for the rib-out route listing, and the best-path rail reads the other function. Found while fixing that other function for the same lengths | not fixed. The best-path repair does not depend on it, and the RD strip now lives in `mpNextHopAddr` (`rib_bestchange.go`), which this call site could delegate to once it is given the SAFI it currently discards |
| 2026-08-28 | - | flowspec config parser, text encoder, JSON decoder | The three flowspec vocabularies each existed three or four times and had already diverged. `parseFlowProtocolMatches` (`internal/component/bgp/plugins/nlri/flowspec/config_builder.go`) carried a private `protoMap` holding `esp` and `ah` but NOT `icmpv6`, `ospf` or `sctp`, while `protocolNameToNumber` (`plugin_encode_text.go`) and `protocolNumberToName` (`plugin_decode.go`) held the latter three and not the former two. The TCP-flag tables disagreed on the `reset` and `urgent` aliases, the fragment tables on `df`/`isf`/`ff`/`lf`, and `tcpFlagsToString` (`types_numeric.go`) rendered bit 0x08 as `psh` where `tcpFlagValueToNames` rendered it `push`. The config path is the one that bites: an unmatched name is neither an error nor a log, it is a `continue`, so `protocol =sctp` in operator config produced a flowspec rule with NO protocol component and matched EVERY protocol. A firewall rule strictly wider than the operator wrote. Found by goconst, which was reporting the 36 repeated component names in this package as lint noise | fixed on the spot, because it is the code this session was already editing. `protocolNameToNumber` gains `esp` and `ah` and is now the ONLY protocol table: `config_builder.go` reads it and `protocolNumberToName` is derived from it by `reverseProtocolNames`. `tcpFlagNameToValue` gains the `reset` and `urgent` aliases and is the only TCP-flag table; `fragmentFlagNameToValue` is the only fragment table. `tcpFlagsToString` and `fragmentFlagsToString` both delegate to one `bitmaskToString` that walks the eight bits against the canonical name table, so encode and decode cannot spell a flag differently again. The 2026-08-22 row below predicted these copies and recorded them unfixed; this closes the flowspec-internal half of it. STILL OPEN, and not this session's to take: the silent `continue` on an unknown name. Every `parseFlow*` function in `config_builder.go` drops what it cannot parse, so a genuinely misspelled `protocol =tpc` still widens the rule instead of refusing it. Fixing it changes the signature of six parsers |
| 2026-08-22 | fixit-flowspec-protocol-name-drift | traffic-usage metric labels, flowspec NLRI text codec | `protoName` (`internal/plugins/trafficusage/metrics.go`) is a fourth private IP-protocol number-to-name table beside `firewall.ProtocolName`. It knows seven of the ten canonical names and renders every other number as decimal digits. It produces no `firewall.MatchProtocol`, so it cannot stall a reconcile, but `ze_traffic_usage_ingress_port_bytes_total` and its egress twin label an SCTP, OSPF or VRRP flow with digits while every other surface names it. The FlowSpec NLRI text codec holds two more, `protocolNameToNumber` (`internal/component/bgp/plugins/nlri/flowspec/plugin_encode_text.go`) and `protocolNumberToName` (`plugin_decode.go`), with further copies in `config_builder.go` and `internal/component/bgp/route/route_flowspec.go`. Those carry `igmp` and the `icmp6` alias, which no firewall backend accepts, so they are a second vocabulary rather than a copy to delete | not fixed. This spec's AC-6 names the flowspec, VPP and ddos-local copies, and all three are gone. The trafficusage copy is one `firewall.ProtocolName` call away, but taking it renames three live Prometheus label values, which is its own change. `TestProtocolTableIsSingleSource` (`internal/component/firewall/protocol_test.go`) sees neither site by design: its scope is the files that name `MatchProtocol`, and neither does |
| 2026-08-22 | - | `internal/exabgp/bridge/bridge_muxconn.go` | `parseMuxLine`, `formatMuxOK`, `formatDispatchRequest` and `formatFlushRequest` were a second implementation of the plugin RPC line format, open-coded beside `rpc.ParseLine`, `rpc.AppendOK` and `rpc.AppendRequest`. The bridge speaks that wire to the daemon over stdio, so changing the id encoding in `pkg/plugin/rpc` alone would have left the two ends unable to read each other. The spec's file list names neither the bridge nor `pkg/plugin/rpc/batch.go`, the other writer of the id | fixed: all four now call the `pkg/plugin/rpc` writers and readers, so one encoder and one decoder serve every speaker. Five tests in `bridge_test.go` are red when `formatMuxOK` writes the old spelling |
| 2026-08-19 | - | python observer SDK | `API.read_line` (`test/scripts/ze_api.py` (retired, no successor) <!-- doc-links: ignore (deleted 2026-08-28 by eae282592 with no replacement) -->) open-coded the `post-startup` callback that `_handle_callback` centralizes. `_call_engine` queues each inbound request it meets, and `read_line` drains that queue through `_handle_callback`. An observer that dispatches before it waits therefore lost the flag. `wait_for_post_startup` then ran to its 30s floor. `test/plugin/system-kernel-log-show.ci` averaged 38.0s against a 15s budget and timed out on every serial run | fixed: the branch moved into `_handle_callback`. Three tests in `ze_api_test.py` hold it. Two are red without the branch. The test went from 15.0s TIME to 3.9s PASS |
| 2026-08-19 | fixit-stored-route-relay-hardening | bgp reactor egress forward | `forwardUpdateCore` (`internal/component/bgp/reactor/reactor_api_forward.go`) rebuilds the wire after an egress modification with `wireu.NewWireUpdate(modified, ctxID)` and never re-stamps the source. Every other rebuild site does: `wireu/split.go`, `wireu/withdrawals.go`, `session_validation.go`, `session_read.go`. `buildFwdBody` keys ze's own RFC 7911 Path Identifier on the ingress path (source, received identifier), so a rebuilt wire keys every source's paths under the singleton config source: two clients that both chose identifier 1 reach one ADD-PATH destination under ONE identifier, and RFC 7911 Section 5 makes the second replace the first. An AS_PATH prepend towards an external peer is a rebuild, so the LIVE forward rail meets this as readily as the relay does | fixed on the spot: the source is read before the rebuild and re-stamped after. Found because `TestRelayAddPathRoundTrip` asserts the destination's identifier equals `fwdPathIDs.generate(src.SourceID(), 7)`, which holds only when the source survives the rebuild; the mutation that drops the re-stamp reddens it |
| 2026-08-10 | fixit-forward-readbuf-leak-deferred-pool-release-paths | bgp reactor forward pool | `fwdPool.releaseItem` (`forward_pool.go`,463) is the single release point for every pool resource an `fwdItem` holds. Two sites open-code it instead. They are the supersede path (`forward_pool.go`,745-751) and the RS direct-write path (`forward_rs.go`,506-507). Neither leaks today. Both copies cover the same two fields and neither zeroes them. A field added to `fwdItem` leaks at these two sites and nowhere else | not fixed, and the code is left alone deliberately. Folding the copies into `releaseItem` is its own change. It would widen a closure whose subject was a different exit. Recorded so the next `fwdItem` field lands with these two sites already named |
| 2026-08-12 | fixit-send-community-suppress-ignored | bgp attribute modification handlers | Five `AttrModHandler` implementations open-coded the last-op-wins fold. Four of them read `AttrModSet` only. An `AttrModSuppress` operation was consumed and the attribute re-emitted in silence. On codes 8, 16 and 32 that made `session { community { send none; }}` a live leak on both forward rails. Codes 35, 10 and 9 carried the same blind spot latently | One fold in the leaf package, `filterapi.LastSetOrSuppress`. Every handler delegates to it. The audit that found the last two read the INSTALLER `attrModHandlersWithDefaults`. A handler filled into a nil slot is invisible to a grep for `RegisterAttrModHandler` |
| 2026-08-14 | netlab-integration | functional test runner, parse suite | `setupWorkDir` (`internal/test/runner/parsing.go`) open-coded the tmpfs write loop that `(*tmpfs.Tmpfs).WriteTo` owns, and the copy wrote every file `0o644`. A `tmpfs=x.sh:mode=755` block in a `test/parse/*.ci` therefore reached disk unexecutable and `exec=./x.sh` died with "fork/exec: permission denied". The directive parsed, was accepted, and was ignored; every other suite writes through `WriteTo` and honors it | fixed: the copy is deleted and the parse suite writes through `WriteTo`, so `mode=` and the executable default of a `.sh` path both hold. `TestParsingSuiteHonorsTmpfsMode` (`internal/test/runner/tmpfs_test.go`) pins it |
| 2026-08-12 | - | interop rpki-frr observer | `rpki-check.py` open-coded `json.loads(result["data"])` because `ze_api` offers a guarded `dispatch` but no accessor for the payload it returns. A command answering `json.RawMessage` arrives already DECODED, so `data` is a dict, `json.loads` raises TypeError, and the retry loop caught only `json.JSONDecodeError`. The plugin died with ZE-OBSERVER-FAIL and the scenario was red at HEAD | fixed inside the scenario: one `payload()` accepts both shapes and the loop also catches TypeError. The class is not closed. 346 observer scripts hand-roll the same decode, and until `ze_api` exposes the accessor beside `dispatch`, each one carries its own copy of this bug. Found by the StayRTR RTR interop scenario |
| 2026-08-14 | netlab-integration | functional test runner, `.ci` suites | The mode fix landed on the parse path only. The main `.ci` path still flattens a tmpfs block to `path -> content`: `Record.TmpfsFiles` is `map[string][]byte` (`internal/test/runner/record.go`,192), `record_parse.go`,142-144 drops `f.Mode` while copying `v.Files`, and `runner_exec.go`,64 rebuilds a mode from the extension through `tmpfs.AddFile` -> `defaultModeForPath`. So `tmpfs=x.conf:mode=755` in a `test/plugin/*.ci` reaches disk `0644` and the directive is accepted and ignored, exactly as it was in `test/parse` before this spec fixed that half. Both `.ci` files this spec added avoid it only because a `.sh` path defaults to 755 | not fixed here. The fix is to carry `*tmpfs.Tmpfs` on `Record` the way `parsingTest` now does, and it touches 8 non-test sites across 4 files, two of which look the map up by base name for other purposes (`record_parse.go`,213 and `runner_exec.go`,192), plus `tmpfsHasErrExit` and `tmpfsRequestsDaemonShutdown`. That is a change to the runner all 633 functional tests go through, and it is not small enough to ride on a closing commit |
| 2026-08-15 | fixit-ci-peer-block-silent-directives | functional test runner, peer-block guard | The guard added to stop silently-dropped directives shipped its own open-coded copy of the parser it guarded. `connOf` (`peer_contract.go`) read `conn=` by a split of the line on `:`. ze-peer read the same line through `ci.ParseKVPairs`, where `pattern=` is a complex key that swallows the remainder and duplicate keys are last-wins. So `reject=bgp:pattern=AABB:conn=1` SATISFIED the guard and then failed inside ze-peer. ze-peer exits before it binds, which the runner reports as `peer did not start listening`. A hex typo was diagnosed as a bind timeout. With `reject=bgp:conn=2:conn=3:pattern=AA` the guard checked non-vacuity against a different connection from the one the peer enforced on | `peer.ParseRejectRule` exported as the ONE reader of the directive. `validatePeerBlockRejects` calls it and surfaces its error at parse time. `connOf` deleted. `TestPeerBlockRefusesMalformedReject` drives six spellings, the key-disorder one included, and asserts the diagnosis is never a bind timeout. Found by review round 1, not by a gate: no test compared the two readers |
| 2026-08-15 | web-htmx4-cutover | chaos web stats card | `renderStats` (`internal/chaos/web/dashboard.go`) wrote the donut BEFORE `<div id="stats">`, while `writeLayout` (`render.go`) writes the same donut INSIDE it. The page element swaps `outerHTML`, and `/sidebar/stats` answers `renderStats` every 500ms, so each poll left the previous donut behind as a sibling and added a new one: two stray SVGs a second, for as long as the dashboard was open. The two copies of one card diverged in content as well, the poll form carrying three stats the page form does not | fixed here, because the same payload is what the stream now sends out of band: the donut moved inside the div and `TestRenderStatsIncludesDonut` requires the fragment to open with `<div id="stats"`. Measured after the fix in a real browser: 1 donut-container at load and 1 after 35 seconds of streaming and polling. The two copies of the card are still separate |
| 2026-08-18 | - | tacacs+ client dial | `(*TacacsClient).dial` (`internal/component/tacacs/client.go`) builds its own `net.Dialer` and writes `&net.TCPAddr{IP: net.ParseIP(c.config.SourceAddress)}`. `net.ParseIP` answers nil for anything it cannot read, and a `TCPAddr` holding a nil IP is a WILDCARD bind, so a source address the operator configured and mistyped is silently discarded and the connection leaves by whichever address the route picks. The shared helper exists to stop exactly this: `(*RealDialer).SetSourceAddress` (`internal/core/network/network.go`) validates parseability and its comment says so. Ten other services dial through it, TACACS+ alone does not. Found while surveying which services can bind an outgoing session to a source address; the survey's own answer, that TACACS+ "can", is what the nil makes false | not fixed. The fix is to route the dial through `RealDialer` so the value is validated once, in the place that already knows how. Two properties are missing whatever the route: no producer anywhere in the tree checks that a configured source address is LOCALLY ASSIGNED, and no service outside BFD can bind to a source INTERFACE |
| 2026-08-19 | fixit-iface-selector-ignored-by-apply | iface config parse, bridge member | `parseIfaceConfig` (`internal/component/iface/config.go`) read the bridge `member` leaf-list with `m["member"].([]any)` rather than through `parseStringList`, the helper four sibling leaf-lists already use. A leaf-list carrying ONE value arrives as a bare string, so the assertion failed, the slice stayed empty, and a bridge configured with a single member enslaved NOTHING: no error, no warning, on the most ordinary bridge an operator writes. No file under `test/` had ever used `member`, so nothing saw it | fixed: `entry.Members = parseStringList(m, "member")`. `TestBridgeMemberListSurvivesOneMember` drives one, two and zero members. Found because a new `.ci` test written for a different bridge defect could not make the kernel enslave anything |
| 2026-08-19 | none (FlowSpec encode defects) | bgp flowspec traffic actions | THREE encoders held their own copy of the RFC 8955 Section 7 action vocabulary and disagreed about every one of it: the config parser (`parseFlowSpec*`, `internal/component/bgp/config/routeattr_community.go`), the `update text` API parser (`parseTrafficRateFunction`, `parseTrafficMarkingFunction`, `parseRedirect*`, `internal/component/bgp/route/route_community.go`), and the NLRI plugin (`flowSpecActionExtComm`, `parseRedirectTarget`, `internal/component/bgp/plugins/nlri/flowspec/encode.go`). Each defect was one copy being wrong where another was right: config discarded the DSCP error and had no bound while the other two bounded to 0-63; the plugin dropped a zero-valued action while config always emitted one; the API refused a 4-octet AS redirect that config and the plugin encoded as type 0x82; none of the three implemented the type 0x81 redirect Section 7.4 names. The precedent for the fix already existed one file away and said so in its own comment: `flowSpecActionKeywords` (`internal/core/bgp/attribute/flowspec_action.go`) was moved into the attribute package so "the two vocabularies cannot drift again", and the rest of the vocabulary was left behind | fixed: `internal/core/bgp/attribute/flowspec_encode.go` holds the traffic-rate, traffic-action, traffic-marking, rt-redirect and redirect-to-IP encoders, all three sites call it, and each open-coded copy is deleted rather than left forwarding. TWO input vocabularies remain deliberately different and are NOT averaged: config alone accepts the two-word `action <words>`, `mark <dscp>` and `redirect-to-nexthop <ip>` forms, which the API spells `traffic-marking` and does not offer for the others. That asymmetry is an input-shape gap rather than an encoder gap, and it is unfixed |
| 2026-08-23 | fixit-vrrp-parent-ignores-the-selector | `waitSettled` (`internal/plugins/vrrp/cmd_show_test.go`) | The copy here is a SPELLING, and it made a guard that could never fire. `instanceView.State` is produced by `viewState` (`internal/plugins/vrrp/vrrp.go`), which returns lower-case `"initialize"`; `waitSettled` compared that field against `fsm.StateInitialize.String()` (`internal/plugins/vrrp/fsm/fsm.go`), which returns `"Initialize"`. The two never match, so `moving` stayed false, `waitSettled` returned on its FIRST poll, and the settle it exists to perform never happened. The symptom was the exact failure the helper had been written to remove -- `TestClearStatisticsPreservesState` reporting `clear changed state "initialize" -> "master"` -- still arriving whenever the box was loaded, and its own comment records that history one function below the broken line. Walked into while running the vrrp unit suite for an unrelated fix, on a box where three sibling sessions were running lint and interop | fixed: the comparison goes through `viewState(fsm.StateInitialize)`, the same producer that FILLS the field, so there is one spelling rather than two. `TestWaitSettledActuallySettles` is the deterministic proof the old flake never gave: it asserts the settled view reads `master`, and it fails 5 of 5 with the pre-fix guard restored (`apply` returns before the instance goroutine is scheduled) against 5 of 5 green with the fix. The shape to look for is a comparison against `String()` on a type whose display value is produced by a DIFFERENT function |
| 2026-08-24 | fixit-ordered-list-loses-its-order | `(*SetParser).deleteFromList`, `(*SetParser).deleteFromInlineList` (`internal/component/config/setparser.go`) | A whole-entry `delete` removed the entry with a bare `delete(entries, key)` on the live map `Tree.GetList` hands out, bypassing `Tree.RemoveListEntry`, which is the only mutator that also drops the key from `t.listOrder`. Writing the same key again then appended it a second time, so the order named it twice and the plugin was handed the entry at the position it was DELETED from. The duplicate has the right length and names only keys the list holds, so `entryOrderLocked` cannot catch it. `Tree.GetListOrdered` returned the entry twice for the same reason, which is what the four config serializers print from. `Tree` has four such mutators and this bypass reached two of them; `DeleteList` was the third and was fixed one commit earlier for the same class of reason | both paths call `tree.RemoveListEntry(name, key)`. `TestToPluginMapFollowsAnEntryDeletedAndWrittenAgain` parses `set neighbor A / set neighbor B / delete neighbor A / set neighbor A`, asserts the delivered order is B then A, and asserts `GetListOrdered` returns two entries. It reddens against the bare map delete with `order is [10.0.0.2 10.0.0.1], want [10.0.0.1 10.0.0.2]`. The shape to look for is a bare `delete(m, k)` on a map a getter handed out, where a sibling field records something about that map's contents |
| 2026-08-26 | le-is-a-ze-binary | compiled VPP functional fixture, `newVPPStubState` (`internal/test/cli/cmd_vpp_stub.go`) | The native fixture opened its user-selected JSONL path with `os.OpenFile`, bypassing `internal/core/cliio`. It therefore gave `-` a filesystem meaning while every filename-taking command contract gives it stdout. The dash-stdio gate caught the new call. | The fixture now opens the log through `cliio.Create` and stores the shared `io.WriteCloser`, so path and stdout behavior use the same producer as other commands. |
| 2026-08-28 | - | `removePeerMetrics` (`internal/component/bgp/reactor/reactor_peers.go`), `incrRefreshReceived` / `incrRefreshSent` (`internal/component/bgp/reactor/peer_stats.go`) | The message-type label vocabulary exists twice and one spelling has drifted. The emitters stamp `type="refresh"` on `ze_peer_messages_received_total` and `_sent_total` for a ROUTE-REFRESH; the cleanup loop that runs when a peer is removed open-codes the same set as `[]string{"update", "keepalive", "eor", "notification", "open", "route_refresh"}`, so it deletes a series named `route_refresh` that nothing ever created. Every other member matches, so the leak is exactly one label value: a removed peer keeps its two `type="refresh"` counters forever, and their `peer` label is an address an operator can recreate with different config. `msgTypeUpdate` (`peer_stats.go`) is the only member of the set that HAS a constant, and the cleanup list does not use that either, which is what let the sixth spelling drift unseen. Found while hoisting this package's repeated literals for goconst, not by a search for it | not fixed. Correcting the spelling changes what `Delete` removes, and the goconst sweep this session was commissioned for carries a standing do-not-change-behaviour constraint over the reactor. The two other mirrors in the same file WERE made compiler-checked in the same pass, `peerStateNames` against `PeerState.String()` and `notifCodeNames` against `notificationCodeLabel`, so this is now the one message-label mirror still held together by hand. The fix is one constant set read by both the emitters and the cleanup list, which removes the class rather than the instance |
| 2026-08-29 | - | `./le commit create` on a spec closure, against the `./le spec session review record` that feeds it | `CheckReview` (`internal/le/commit/review.go`) spelled the review artifact's name itself, `tmp/review/<stem>-<session>.md`, and put the eight-hex COMMIT namespace in it that `SessionID` (`internal/le/commit/session.go`) mints. `recordReview` (`internal/le/spec/session/review.go`) writes that file under the HARNESS session id. A closure therefore recorded its review and the gate answered `no independent-review artifact at tmp/review/<stem>-a7b3c9d2.md` about a file sitting beside it under the other name, so EVERY spec closure was blocked rather than one. The two spellings were one identity until the native port: `commit_helper.py` shelled out to `review_gate.py check`, and one `session_id()` served the writer and the reader. Neither package's tests could see it, because each planted its fixture under a session id it had chosen | fixed by deleting the copy. `specsession.ReviewArtifactPath(root, spec)` is the single producer of the name and of the identity inside it, and `CheckReview` no longer takes a session argument, so a caller cannot supply the wrong one. `TestTheReviewGateReadsTheArtifactTheRecorderWrites` plants an artifact under the commit namespace and requires the gate to REFUSE it, then plants one at the recorder's path and requires it to pass. Measured RED against the old lookup before the fix was restored |
