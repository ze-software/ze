| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-10 | - | verify stages | `check_source_anchor_stale_paths` in `scripts/dev/validate.py` skipped `http`, `~` and absolute anchors as external, then resolved everything else against the repository root. Seven anchors name `../gh-pages/...`, the website's sibling checkout: green on a machine that clones both repositories side by side, red on a runner that clones one. Identical bytes, two verdicts | skipped anchors that escape the root, which is the PROPERTY the three spellings were instances of, plus a test that a `..` segment resolving back inside stays checked |
| 2026-08-11 | fixit-route-removal-protocol-blind | qemu integration | `make ze-qemu-needs-linux-test` and `ze-qemu-integration-test` cannot produce any verdict without two things `make ze-setup` never installs: an amd64 runtime kernel in the durable cache, and `7z` for `qemu-run.py` to extract the initramfs of a custom kernel. `ai/rules/platform-linux.md` makes a QEMU run mandatory for linux-only code, so on a fresh checkout the mandatory gate for a whole class of code is unrunnable until someone discovers both by hitting them one at a time. Cost here was two failed runs before either test executed, and the first failure was masked by a trailing echo in the invocation that returned 0 while make had returned 1 | not fixed. `make ze-setup` should install `7z` and either build or name the runtime kernel, and `ze-setup CHECK=1` should report both as absent |
| 2026-08-10 | - | hook fixtures | `point-overwrite-case-variant-slug` in `scripts/dev/hook-fixture-check.py` wanted exit 0 on a case-sensitive filesystem. Two checks own that path and the filesystem picks which one answers: where the file exists `c_point_overwrite` refuses it, and where it does not `c_enforce_naming` refuses the capitals in the `.md` name. The fixture asserted one number for a row whose verdict has two correct sources | asserted the code the filesystem earns, and moved the row out of the loop over the five directory-segment variants, whose basenames naming never sees |
| 2026-08-13 | - | plugin functional suite | `audit-config-commit` (test 52) failed under `make ze-plugin-test` with a five-second `urlopen` timeout in its own observer script, while 629 tests ran in parallel. Run alone it passes in 6.0s. The fixed client timeout is a number measured against a machine under full suite load, so the same bytes give two verdicts | not fixed, and the reproduction attempt is the reason: `ze-test bgp plugin 52` alone exits 0, so nothing deterministic was reachable here. The next step is to raise or remove the fixed timeout in the `audit-commit-test` observer, which asserts a commit lands rather than how fast it lands |
| 2026-08-14 | - | `make ze-test-pkg PKG=./scripts/dev` | `test_a_dated_regular_file_is_no_session_directory_in_any_copy` (`scripts/dev/session_bin_dir_test.py`) split the four session-directory resolvers. For a dated regular FILE, `make` answered `tmp/session/<yesterday>-<sid>` while shell (`[ -d ]`) and python (isdir) skipped it. `ZE_SCRATCH_DIR` (`mk/session.mk`) globbed `.../<sid>/` and read the trailing slash as a directory restriction, which the glob(3) macOS ships does not apply. Red at HEAD on darwin, green on a runner whose glob honors the slash | fixed here, 1 line: the pattern is `.../<sid>/.` with `$(patsubst %/.,%,...)`, which is directory-only on both globs. All 33 tests in that file pass |
| 2026-08-14 | fixit-dynamic-group-peer-config | `make ze-lint-changed` | `internal/plugins/iface/ra/ifacera.go:31`: `unused: const maxFinalAdvertisements is unused`. Its only non-test reader is `sender_linux.go`, which carries `//go:build linux`, so golangci-lint sees the const used on a linux runner and unused on a darwin workstation. The package reaches the lint set as a reverse dependency (`scripts/dev/changed-pkgs.sh`), so an unrelated change reds the gate for whoever runs it on macOS. Present in HEAD, unmodified in the tree | not fixed. The const and its reader belong in one build-tag scope, or the lint invocation needs the linux tags this machine does not supply |
| 2026-08-14 | - | ipsec functional suite | `make ze-ipsec-test` reports 14 of 15 on darwin. `test/ipsec/ipsec-show-dataplane.ci` configures `local-address 10.0.0.1`, and a host without that address cannot bind it. The run logs `ike: failed to start UDP transport ... listen udp4 10.0.0.1:1811: bind: can't assign requested address` and the same for the NAT-T socket. The file's last assertion is `stderr:contains=engine-steps: all steps passed`, and that is the assertion that fails. The `spi 0` step is NOT the cause, though its ERROR line reads like one: the test EXPECTS `command-error:contains=out of range 1..4294967295`, so that step passed. Two sibling files also configure a 10.x address, `ipsec-show-status.ci` and `ipsec-traffic-selector-config.ci`, and both pass, because neither needs the transport to start. The `.ci` carries no `option=needs-linux` | not fixed, and not fixed deliberately. Marking it `needs-linux` would SKIP it on darwin, which reduces coverage to reach green. The honest fixes are to bind an address the host has, or to give the harness the alias the test needs. Found in the closure of spec-fixit-ike-responder-natt-port-float. Not caused by it: every code edit in that closure is a comment, and a comment cannot change a bind result |
