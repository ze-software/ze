| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-03 | - | `./le functional vpp` on macOS | Three of five tests red after two fixture repairs in the same run, and the obvious reading, "macOS has no VPP", is wrong for at least two of them. These tests do not need VPP: the fixture sets `external true`, so the daemon logs `vpp: external mode, not execing VPP binary` and speaks the binary API to `ze-test vpp-stub` over a Unix socket. The stub IS the VPP, and it works here: `sockclnt_create`, `GoVPP connected` and `fib-vpp: running` all appear on macOS. `vpp-fib-route-lookup` IS host-specific, but what is missing is the iface backend, not VPP: `defaultBackendName` is `""` off Linux (`internal/component/iface/default_other.go`) and `vppRouteConfig` writes no `interface { backend ... }` block for the observer variant, so `EnsureBackend` returns `iface: no backend loaded`. The same config defaults to `netlink` on Linux. The VPP iface backend registers on darwin too (`internal/plugins/iface/vpp/register.go` carries no build tag), so naming it in that config would load it. `vpp-mpls-push` never reaches the daemon at all: `ze-peer` refuses check mode with an empty expectation set (`cmdPeer`, `internal/test/cli/cmd_peer.go`), and its sibling `vpp-fib-route` starts its peer from a script of the same shape, so the difference is in the script rather than the platform. `vpp-fib-route` was traced and is FIXED: `vppRouteConfig` built a peer that attached no process at all, and `runRIBPlugin` (`internal/component/bgp/plugins/rib/rib.go`) drops every event carrying no peer address, so the RIB stored no route, emitted no best-change, and `fib-vpp` had nothing to program. The daemon had said so at startup and the line read as boilerplate: `reconcileDelivery` (`internal/component/plugin/server/delivery_reconcile.go`) warns `process=bgp-rib`, "no peer-scoped event of any type reaches this process". Adding `attach process bgp-rib { receive [ update state refresh ]; send [ update ]; }` turns it green in 633ms against the 12.2s it spent timing out. The one-line form needs the `;` terminators a multi-line `.ci` block gets from newlines: without them the parser refuses the whole config, which is how the first attempt failed and how the discrimination was observed. Found while verifying an unrelated fixture change to `bufio.Scanner` error handling in `internal/test/fixture/misc_fixture_vpp.go` | not fixed. The suite is declared non-gating and "needs the Linux daemon (release evidence only)" in `./le functional list`, which is the label that made "the host" the first explanation reached for all three, and it was the right one for none of them cleanly. Two repairs WERE made in that run and both were machine-independent: `vppWorkPaths` built the AF_UNIX socket under the runner's work directory, which the runner nests inside the checkout, so the path passed 108 bytes and all five tests died before the daemon started; and `vppRouteConfig` still wrote the retired `process <name>` spelling inside the peer block, which `internal/component/config/retired.go` refuses in favor of `attach process`, so `vpp-fib-route-lookup` failed to load its config on any machine. The next step is to read the untruncated `vpp-fib-route` client output, which says whether the BGP session came up at all, and then to run the suite under `./le qemu netns-test suites vpp` to see which reds survive a Linux kernel |
| 2026-08-31 | yang-short-and-long-command-help | `./le verify worktree`, and every gate stage it runs | The subject under test is a fixed COMMIT in a detached worktree, and the `le` BINARY that runs the stages is built from the RUNNER'S WORKING TREE. So an uncommitted change to a gate travels into every other session's run while the code that gate judges does not. Measured tonight: this session added `docHelpShapeStage` to `docVerifyStages` (`internal/le/doc/wiring/docverify.go:76`, uncommitted) and `stage("doc check", "verify")` is committed at `internal/le/verify/engine/stages.go:60`, so a peer session's verify of its own clean commit went red on a stage that exists in nobody's HEAD. The peer read the red as its own and said so: "the tool is dirty while the subject is clean". The detached worktree is what makes it invisible, because it is the very mechanism that is supposed to isolate the run from working-tree state, and it isolates the SUBJECT while leaving the JUDGE dirty | not fixed, and it is a design question rather than a defect with an obvious repair. Building the gate binary from the same commit under test would make the isolation complete and would also stop a session from ever testing a gate it is developing, which is the reason the current shape exists. The cheap half is attribution rather than isolation: a stage whose producer is uncommitted in the runner's tree could SAY so in its output, so a red names itself as the runner's own in-flight work instead of looking like a regression in the commit. Until then the only mitigation is what happened here, which is a session announcing the new stage on the bus, and that depends on the session realising the binary travels. SECOND SURFACE the same night, which makes this a class rather than an incident: `./le cli-grammar` (`cligrammar.go`, `flags.go`) walks the tree on disk with `filepath.WalkDir`, so it judges the working tree and never HEAD. This session measured `cli-grammar: OK` exit 0 at 13:49 against 79d199a31; a peer measured exit 1 with twelve R9 flowspec findings an hour later; the whole difference is a third session holding `ze-flowspec-cmd.yang` and `ze-cli-announce-cmd.yang` uncommitted. Both numbers were then written into journal rows as properties of HEAD, which neither was. The sharper statement of the class, from the peer who found the pair: a gate's verdict is a function of more inputs than the thing it claims to be judging, and nothing in the output says which inputs were in play. `verify worktree` isolates the subject and not the judge; `cli-grammar` isolates neither |
| 2026-08-31 | yang-short-and-long-command-help | `TestActiveAnswerConsumerStreamsPastQueueDepth` (`pkg/plugin/rpc/mux_test.go`) | one FAIL in a whole-package `go test ./pkg/plugin/rpc/` while three phase agents ran unit groups on this box: `answer queue full: consumer fell behind, answer abandoned`. Same `ErrAnswerQueueFull` path the 2026-08-22 and 2026-08-24 rows name, and this is its FOURTH surface. The irony is that this case exists to prove a consumer CAN stream past `answerQueueDepth`, so the bound the class is about is what the case measures | not fixed, and NOT REPRODUCED: 5 of 5 pass with `-count=5` alone at 0.027s. Recorded for the recurrence count. The next step is unchanged and is the decision the 2026-08-22 row states: whether a consumer the scheduler starves SHOULD lose its answer, which is a design question about `answerQueueDepth` |
| 2026-08-31 | - | `test/ui/le-ste-answers.ci` on this workstation | The test reds in every local `./le functional ui` run, and passes in CI. `leSTEAnswers` (`internal/test/fixture/ui_fixture_le_ste_answers.go`) exports the tree to a throwaway directory and commits it with `git -c user.email=... -c user.name=... commit`. It sets an identity and nothing else. The commit then inherits the operator's global `commit.gpgsign=true`. gpg then needs a passphrase and has no tty: `gpg: cannot open '/dev/tty'`, `fatal: failed to write commit object`, exit 128. The fixture reports it as `git -c in the export failed with 128`. CI has no signing key, so the export commits unsigned and the test is green there. Found in a full `./le functional ui` run made for an unrelated new test. 215 of 230 passed, and this was one of the 15 reds | not fixed. The export repository is a throwaway in a temporary directory, and it is not this repository. Turning signing off for that one commit is therefore not the banned `--no-gpg-sign` on a Ze commit. The rule text in `ai/rules/git-safety.md` names the flag without that distinction, so the owner draws the line. Everything else the fixture needs is already passed as `-c`, which is where the setting belongs |
| 2026-08-29 | fixit-eap-tls-escape-hatch-kills-the-daemon | `./le integration interop-ipsec` on macOS | The IPsec interop lab reaches no strongSwan SA on this host. Measured from a detached worktree at `97a216888`, so no session's working-tree edits are in it: `eap-tls` reds at `wait for strongswan log "negotiated TLS 1.2" timed out`, `eap-tls13` and `psk-site-to-site` both red at `wait for strongSwan SA ze timed out before the peer became ready`. Both images build. `psk-site-to-site` is the lab's simplest scenario and this spec touches nothing in it, so the red is the host rather than any scenario. The CAUSE was not determined: Docker Desktop runs a Linux VM, so XFRM should exist, and no container log was captured because the lab removes its containers on every exit path. The consequence is what matters: a developer cannot re-measure any IPsec interop claim here, and CI cannot be leaned on for it either, because `.github/workflows/evidence-nightly.yml` marks the `ipsec-interop` job `continue-on-error: true`. So 22 scenarios, the only IPsec tests with a foreign peer, are enforced by nothing and reproducible on no machine this session could reach | not fixed. It blocks no goal of the spec that found it: AC-2's producer is proven by unit tests here, and the AC-3 and AC-5 interop PASS of 2026-08-24 stands as the last measurement, recorded as OWED rather than re-transcribed as fresh. The next step is a container log: run one scenario with the lab's cleanup suppressed and read charon's own output, which will say whether the kernel refuses the SA or the peer never sees the initiator. The wider question is the owner's, and it is the one this row exists for: an advisory nightly job is the only thing running these tests, so nothing turns red when they break |
| 2026-08-29 | - | a tool-presence probe that checks the tool's own version and not the toolchain it was built with | A compiled analyser reads the export data the local compiler writes. Built with an older toolchain it cannot, and it says so once per import rather than once: every file reports an import failure, and an import whose uses it then cannot see reports as unused. Each message names a file that is correct, so the run reads as a source defect in another package and the tool is the last thing suspected. A presence probe that asks only for the tool's own version reports it present throughout | the probe now reads the binary's own build stamp and refuses one built with a toolchain older than the module's directive, with an older build refused and a newer one accepted. The general practice: a probe for a compiled analyser MUST check the toolchain it was built with, not only its release, because the two go stale independently and only the second is visible to a version flag. Absence of a readable stamp is not evidence of staleness, so it passes |
| 2026-08-24 | fixit-stored-route-relay-hardening | `test/plugin/plugin-reads-engine-answer.ci` | one FAIL in a retired `ze-functional-plugin-test` (current: `./le functional plugin`) run of the whole suite (660/661, 32-way parallel), `expect exit-code -> expected exit code 0, got 1`, with the daemon reporting `failed to send request: rpc error: walk "system command list": answer queue full: consumer fell behind, answer abandoned`. That is the documented `ErrAnswerQueueFull` path the 2026-08-22 row already names on two other surfaces: `answerQueueDepth` holds 256 lines and `MuxConn.readLoop` never waits on a consumer, so a consumer goroutine the scheduler leaves out for 256 records loses its answer. This test drives the longest answer the daemon has by construction, its own docstring says so, which is why it meets the ceiling first under load | not fixed, and NOT REPRODUCED. It ran alone through the same runner and PASSED at 3.6s, 1 of 1. Found while running the functional gate for spec-fixit-stored-route-relay-hardening, whose diff reaches the stored-route relay only and touches no part of the answer-streaming path. Recorded for the recurrence count: one mechanism now has three surfaces, a unit test, an RFC 7606 functional test and this one. The next step is the decision the 2026-08-22 row already states and nobody has taken: whether a consumer the scheduler starves SHOULD lose its answer, which is a design question about `answerQueueDepth` rather than a test defect |
| 2026-08-24 | fixit-lint-blind-to-every-other-build-tag | `cmd/ze/hub` `TestEphemeralDaemonStartsSSH` | red inside the retired `ze-unit-pkg-test PKG=./cmd/ze/hub` (current: `go test -race ./cmd/ze/hub`), which passes `-race`: `open .../ephemeral-ssh.addr: no such file or directory` after the test's own 15s poll deadline (`main_test.go`, `TestEphemeralDaemonStartsSSH`). The daemon does start SSH -- the same tag set WITHOUT `-race` passes 3 of 3 at 1.6s, 1.7s and 4.6s -- so the assertion measures how long a race-instrumented daemon needs to reach the standalone SSH block in `run()` (`cmd/ze/hub/main.go`, the `sshBuildStandalone` call), not whether it gets there. With `-race` it is red 3 of 3, each at 15.0s. Not caused by this session's diff: the same case is red at HEAD, run in a throwaway `git worktree` at 3eeba4a05 carrying none of the working tree's changes. The delayed WARN `SSH server failed to start ... zefs: flush create temp ... no such file or directory` in the full-package run is a CONSEQUENCE of the timeout, not its cause: `t.TempDir()` is removed when the test returns and the daemon goroutine writes the host key after that | not fixed. The bound is a wall budget over an instrumented build, which is the class this file collects, and raising it without measuring what the race build costs would be guesswork on a contended box. The next step is to time the daemon's arrival at that call under `-race` on an idle machine and set the poll bound from the measurement, or to give the poll the load-scaled budget the other wall-budget rows here already ask for |
| 2026-08-24 | fixit-lint-blind-to-every-other-build-tag | golangci-lint's own run lock, and the retired `scripts/dev/ze-run.sh` (current producer: `internal/le/job/job.go`) for a caller that arrives without make | two independent facts made the lint verdict depend on what a SIBLING session was doing. golangci-lint v2.10.1 refuses to start while another instance holds the cache lock (`Error: parallel golangci-lint is running`, and `--allow-parallel-runners` is the flag that would permit it). `ZE_RUN_SLOTS` is 4 on this box, so the admission point allows two jobs to lint at once. The linter refuses that state. Measured here: four of thirteen flavor passes died on it in one run. The PostToolUse lint hook returned that same string for eight consecutive Go edits while another session's `./le changed scope` ran `golangci-lint run ./...`. Second fact, same subject: the `SLOTS` default in `ze-run.sh` is 1, and only the retired Makefile (current producers: `internal/le/` native action tables) exports the derived 4. The documented route is the retired `scripts/dev/ze-run.sh <label> golangci-lint run ...` (current producer: `internal/le/job/job.go`), which the lint-gate rule point `never-invoke-golangci-lint-directly` and `check_raw_test_invocation` push an agent onto. An agent taking that route gets ONE slot and queues behind every job in the tree. One such job waited 50 minutes behind a `ze-qemu-needs-linux-test` here before it was killed | the lock half is FIXED 2026-09-02: every golangci-lint argv Ze builds now carries `--allow-serial-runners`, which makes the child WAIT for the lock instead of giving up on it after five seconds (`internal/le/verify/lint.passPlan`, `internal/le/testchaos.Action.Argv`, `internal/le/hookruntime.postLint`). Measured before the fix, on a checkout four other sessions were working: 18 of 18 flavor passes exited `parallel golangci-lint is running`, nothing was linted, and the stage still declared the group `golangci-lint reported findings in the files named here` with an empty file list, so the red named no file and could not be acted on. `--allow-parallel-runners` was the option NOT taken: `./le verify lint run` does not admit itself through `internal/le/job`, so parallel runners would put N sessions x `-j` workers on the box with nothing counting them, which is the 2026-08-17 freeze that package exists to prevent. Waiting costs wall clock instead: a pass now queues behind a sibling's whole-tree lint, measured at over five minutes here. Both halves of the second decision are now CLOSED too: `./le verify lint run` claims the `lint` label in the job registry before it plans, so it queues rather than oversubscribing the box, and two sessions asking for the same work over the same tree share one run. Making that sharing reachable needed a fix in the registry itself (`plan/journal/unwired-feature.md`, same date). The second decision is still open. The second decision: does a direct caller inherit the same derived slot count the retired Makefile (current producers: `internal/le/` native action tables) computes? The lock's cost rises with this spec, which turns 2 lint invocations per full run into 15 |
| 2026-08-23 | fixit-ipsec-peer-reload-ignored | `test/ipsec/ipsec-dpd-holds-tunnel.ci` | one FAIL in a retired `ze-functional-ipsec-test` (current: `./le functional ipsec`) run (14 of 15), `TYPE: logging_mismatch`, with `ZE-OBSERVER-FAIL: engine-steps: plugin loop ended before steps ran error="stage 1 (declare-registration): mux conn read error: EOF"`. The observer's connection died during registration, upstream of any assertion the file makes. The whole suite was slow in that run: every passing test took 14s to 17s against a 1.4s average, and the failing one took its full 165s against a 21.1s average, so the box was measuring itself. Sibling agents were running `golangci-lint` and other suites on the same checkout at the time | NOT REPRODUCED and not caused by this session's diff. The identical command on the same bytes passed the whole suite 15 of 15 in 25.6s minutes later, with this test at 25.6s. The diff reaches `peerConfigChanged` and `ipsec.SiteToSitePeer.Equal` only, and neither is on any path to plugin registration. Recorded for the recurrence count: this is the per-test wall budget the 2026-08-17 row names, met on a ninth surface. The next step is the one that row already states, a budget that scales with load or a runner that serialises the wire tests |
| 2026-08-22 | fixit-link-event-drop-loses-carrier-state | `_job_params` in the retired `scripts/dev/ze-run.sh` (current producer: `internal/le/job/job.go`) | the retired `ze-unit-pkg-test PKG=./internal/component/iface RUN=...` (current: `go test -race ./internal/component/iface`) attached to a running job for `./pkg/plugin/rpc`, replayed that job's log and exited 0 for a package it never compiled. The work key is the command plus the make command-line definitions, and `_job_params` read those only from the text after a ` -- ` separator in MAKEFLAGS. GNU make writes that separator only when the command line ALSO carried a flag, so the invocation the retired Makefile (current producers: `internal/le/` native action tables) prescribes, which carries none, produced an EMPTY parameter set and every `ze-unit-pkg-test` on this tree shared one key. The host was GNU Make 3.81, which macOS ships. This is the pair of 2026-08-19 rows in `stale-artifact-reused.md` alive again after the fix that closed them, and it survived because every test of that fix fed a MAKEFLAGS carrying the separator | fixed on the spot, six lines. `_job_params` now strips the separator when make wrote one and recognises a definition by its NAME shape, so a per-invocation `--jobserver-fds=3,4` stays out whether or not a separator is present. `test_a_different_parameter_set_does_not_attach_without_a_separator` and `test_a_jobserver_option_is_not_read_as_a_parameter` in the retired `scripts/dev/ze_run_test.py` (current producer: `internal/le/`) hold the two halves. The first is red at `1 != 2` against the old body and 29 of 29 pass with the new one |
| 2026-08-22 | - | the retired `scripts/checks` (current producer: `internal/le/`) `TestStaticcheckFeatureMatrixIncludesTestFiles` and `TestMatrixRowFilterCatchesAGatedBreak` | Both fail inside the retired `ze-unit-test` (current: `./le test-unit`) on this machine with `coherent fixture exit = 1, want 0` and, under it, `internal error in importing ... export data version 4 is greater than maximum supported version 2` over `internal/byteorder`, `internal/cpu`, `internal/goarch`, `math/bits` and the synthetic `example.com/matrix/ssh`. That is the mechanism the 2026-08-22 `./le changed scope` row below names, met on a second surface: the fixtures compile with the local go1.27.0 and the analyser cannot read its export data. Both reproduce at HEAD from a pristine `git archive HEAD` tree with the same messages, so they predate the working tree | not fixed, found while running the retired `ze-unit-test` (current: `./le test-unit`) for spec-record-answers-2-only-encoding phase 3. The repair is the same one the lint row states and it is the owner's call: pin `GOTOOLCHAIN` where the analyser runs, or raise `go.mod`'s toolchain line and the analyser pin together <!-- doc-links: ignore (a Go standard library package named in a compiler error, not a path in this tree) --> |
| 2026-08-22 | - | `internal/component/lg` `TestLGHandlerGoldenOutput/ui-peer-download` | The golden byte comparison fails on this machine with `rendered bytes differ from testdata/handler/ui-peer-download.txt`, `length: fixture 454, render 468`. The two bodies carry the same 352-byte header block and the same gzip payload: the fixture decompresses to a 90-byte CSV, `prefix,next-hop,as-path,origin,local-pref,med` plus one route row. The failure output shows that CSV readable in the render immediately after the gzip header and unreadable in the fixture, so the difference is how deflate encoded one 90-byte input rather than what the handler produced. It reproduces at HEAD from a pristine `git archive HEAD` tree with the same two lengths, so it predates the working tree. Local Go is 1.27.0 and the fixture was captured on 2026-08-21 by 80f0b8b57 | not fixed, found while running the retired `ze-unit-test` (current: `./le test-unit`) for spec-record-answers-2-only-encoding phase 3. The next step is to capture the render and decompress it: if its payload matches the fixture's, a byte comparison over gzip output pins the compressor rather than the handler, and the capture belongs on the decompressed body |
| 2026-08-22 | - | `test/plugin/remove-private-as-export.ci` | one timeout in a `./le functional` run of all 22 suites, 628/630 in the plugin suite, with `FAIL: ze never forwarded the stripped route to receiver-peer`. That message is the expiry of the observer's own liveness bound: `updates_beyond_eor` polls `show bgp peer receiver-peer detail` 150 times at 0.1s, so the budget is 15s inside a 20s `option=timeout`. The sibling `remove-private-as-replace-peer.ci` carries the 2026-08-19 row above for the same shape, and this file's own docstring says it carried the identical gate | NOT REPRODUCED, and not introduced by this session. It ran ALONE three times through the same runner, 3 of 3 PASS at 2.8s to 3.3s, and the retired `scripts/dev/stress-repro.py "bgp plugin" --test remove-private-as-export --any-failure --iterations 12` (current producer: `internal/le/stressrepro/run.go`) at 32 burners and parallel 8 on a 16-CPU host did not reproduce it in 12 invocations. The same suite alone, on the same tree, half an hour earlier, passed it. The next step is the one the 2026-08-19 row already names: a per-test wall budget measures the machine on a shared checkout, so the repair is a budget that scales with load, or a runner that serialises the wire tests |
| 2026-08-22 | - | `./le changed scope` | The target is unrunnable on a machine whose local Go is newer than `go.mod`'s `toolchain` line, and the file it then reports cannot be fixed. `go.mod` names `toolchain go1.26.6`, which is a MINIMUM, so `GOTOOLCHAIN=auto` selects the local go1.27.0 and every package compiles to export data version 4. golangci-lint v2.10.1 pinned by the retired `scripts/dev/dev-setup.py` (current producer: `internal/le/`) cannot read that: built with go1.26.4 it panics with `file requires newer Go version go1.27`, and rebuilt with go1.27.0 it reports `could not import sync/atomic ... export data version 4 is greater than maximum supported version 2` on `internal/core/callsink/callsink.go`. Running the target as `GOTOOLCHAIN=go1.26.6 ./le changed scope` clears that and leaves one finding, `internal/component/bgp/plugins/bmp/fuzz_test.go:71 File is not properly formatted (gofmt)`, on a file unmodified since 3c9644e15. That finding has NO fix: go1.27's gofmt rewrites the comment alignment and go1.26's gofmt rewrites it back, measured both ways on the same file, and golangci-lint built with go1.26.6 still diffs the go1.27 form. The file only reaches the lint set because a missing `tmp/ze-verify.status` widens the scope to `./...` | not fixed, found while implementing spec-record-answers-2-only-encoding phase 1. Two candidates and both are the owner's call: pin `GOTOOLCHAIN` on the lint invocations in the retired `Makefile` (current producers: `internal/le/` native action tables), or raise `go.mod`'s toolchain line and the golangci-lint pin together. The formatter half needs whichever Go version the repo declares canonical, because no source form satisfies both |
| 2026-08-20 | streaming-answer-protocol | plugin functional suite | two the retired `ze-functional-plugin-test` (current: `./le functional plugin`) runs minutes apart on one tree failed DISJOINT sets: 638/640 with timeouts in `errors-config-abort-show` and `family-no-plugin-failure`, then 637/640 with `aaa-radius-admin`, `aaa-radius-fallback` and `as112-probe-anycast-not-loopback`. The five `answer-*.ci` passed in both, as did every test in each other run's failed set | not fixed and NOT reproduced. No new mechanism: `aaa-radius-admin` is the 2026-08-19 row below, and the class is the per-test wall budget the 2026-08-17 row names. Recorded for the recurrence count |
| 2026-08-20 | streaming-answer-protocol | `test/plugin/audit-config-commit.ci` | one FAIL in the retired `ze-functional-plugin-test` (current: `./le functional plugin`) (633/638) and 3 of 3 red in isolation, with `ZE-OBSERVER-FAIL: uncaught observer exception during shutdown: TimeoutError: timed out`. The same invocation against a `bin/ze` built at 20:00 the previous evening PASSES, which read as a working-tree regression. It was NOT one: the two binaries sat in different directories, and the directory is the variable | fixed 2026-08-20, one option line. The variable is the config directory ze derives from its own location. the retired `ze-build` (current: `go build -o bin/ze ./cmd/ze`) seeds a user into the session config directory, `bootUsers` is then non-empty, and `cmd/ze/hub/main.go` selects per-user API auth, which ignores the `token "secret"` the fixture configures. `withAuth` (`internal/component/api/rest/auth.go`) answers 401, `wait_for_rest` catches it as `URLError` and prints `FAIL: REST API did not respond`. Measured as a 2x2: each binary red 3 of 3 beside a seeded database and green beside a fresh one, so the bytes are not the variable. The fixture now pins `option=env:var=ze.config.dir:value=.`, the repair the 2026-08-17 row applied to `mgmt-guard-api-env-started-settings-survive.ci` for the same mechanism, and it is green 3 of 3 in BOTH roots. The 2026-08-13 and 2026-08-15 rows below are not contradicted: this is the same auth-surface class the 2026-08-15 row names |
| 2026-08-19 | cli-dispatch-child-guard | `test/plugin/remove-private-as-replace-peer.ci` | one FAIL at 671/672 in a retired `ze-functional-plugin-test` (current: `./le functional plugin`) run. The file carries `option=timeout:value=20s`, a wall-clock budget covering daemon start, two BGP sessions and the forwarded UPDATE. It expected the UPDATE and received a NOTIFICATION, Cease subcode 2, Administrative Shutdown. The client log ends `Shutting down... Ze stopped`, so that is ze's own shutdown notification arriving where the UPDATE was due, and the daemon was torn down before it forwarded. `conn_map` worked: the log binds conn=1 to 127.0.0.1 and conn=2 to 127.0.0.2, which is the accept-order race the file's own header says that option exists to remove. NOT REPRODUCED as a defect | not fixed, and not introduced by this session. `ze`, `ze-stripped` and `ze-test` were rebuilt from the working tree at 18:38, after every commit of the session, and the test ran ALONE three times through the same runner: PASS, PASS, PASS. No commit in the session touches the surface either. `git diff --name-only` over all five, matching aspath, as_path or remove-private, returns zero files. The next step is the one the 2026-08-17 row already names. A per-test wall budget measures the machine on a shared checkout, so the repair is a budget that scales with load, or a runner that serialises the wire tests |
| 2026-08-19 | - | the retired `ze-unit-pkg-test PKG=./scripts/dev` (current: `go test -race ./scripts/dev`) | `test_a_stalled_holder_is_broken_and_the_kill_carries_its_evidence` (the retired `scripts/dev/ze_run_test.py` (current producer: `internal/le/`)) gives a queued job a fixed 30s to win its slot. It ended in `subprocess.TimeoutExpired` at load average 31 on 32 cores. The same file passed all 27 tests 40 minutes earlier on the same bytes. The bound is wall clock over a fork and a directory scan, so it measures the host | not fixed, and outside the problem in hand. Admission is observable as state. Wait for the holder's `.rc` file, then for the waiter's own job entry |
| 2026-08-17 | - | `test/plugin/*.ci` wall budget | The plugin suite's per-test `option=timeout` is a wall-clock deadline for daemon start plus BGP session plus plugin startup, so on a shared checkout it reports the machine rather than the product. At load average 79 on 32 cores every sequential run of `test/plugin/interface-rate-show.ci` timed out at 16.9s to 48.1s against its 15s budget, and so did an untouched copy of `test/plugin/interface-type-show.ci` (20s budget, 49.9s). The failure is always `received messages: 0` waiting for the initial-sync End-of-RIB, upstream of any observer dispatch. At load average 4 the same file passed 6/6 in 2.8s to 3.5s; under `stress-repro --burners 8 --parallel 2` it failed 1 of 35 while the unmodified committed file failed at invocation 7 | not fixed, found while adding an assertion to that file. Raising a budget is banned (`ai/rules/testing.md`) and the number is not the defect: three unrelated committed tests behave the same, so the subject is ze startup latency under CPU starvation, or `withParallelHeadroom` giving a plain suite run the headroom it gives verify |
| 2026-08-16 | web-htmx4-cutover | `test/web/chaos-stream-swaps.wb` | one FAIL in a retired `ze-functional-web-test` (current: `./le functional web`) run that took 346.6s, against 102.6s and 155.1s for the same suite on the same bytes before and after it; the test passes standalone in 18.9s and passed 97/97 in both normal-load runs. NOT REPRODUCED as a defect. The file asserts a stream-driven change on `#toast-container`, the one chaos target no poll fills, so its wait is the whole test; `withParallelHeadroom` (`internal/test/runner/runner_exec_util.go`) multiplies a budget by 3 only under verify mode, and a plain the retired `ze-functional-web-test` (current: `./le functional web`) on a shared checkout gets no headroom while another session loads the machine | not fixed. Next step is one failing capture with -v, reading which step timed out: if it is the toast wait, the budget is what a 3x-slower host needs rather than what the author measured on an idle one |
| 2026-08-10 | - | verify stages | `check_source_anchor_stale_paths` in the retired `scripts/dev/validate.py` (current producer: `internal/le/doc/wiring/checks.go`) skipped `http`, `~` and absolute anchors as external, then resolved everything else against the repository root. Seven anchors name `../gh-pages/...`, the website's sibling checkout: green on a machine that clones both repositories side by side, red on a runner that clones one. Identical bytes, two verdicts | skipped anchors that escape the root, which is the PROPERTY the three spellings were instances of, plus a test that a `..` segment resolving back inside stays checked |
| 2026-08-11 | fixit-route-removal-protocol-blind | qemu integration | the retired `ze-qemu-needs-linux-test` (current: `./le qemu run command "./le qemu all-tests"`) and `ze-qemu-integration-test` cannot produce any verdict without two things the retired `ze-dev-setup` (current: `./le setup`) never installs: an amd64 runtime kernel in the durable cache, and `7z` for `qemu-run.py` to extract the initramfs of a custom kernel. `ai/rules/platform-linux.md` makes a QEMU run mandatory for linux-only code, so on a fresh checkout the mandatory gate for a whole class of code is unrunnable until someone discovers both by hitting them one at a time. Cost here was two failed runs before either test executed, and the first failure was masked by a trailing echo in the invocation that returned 0 while make had returned 1 | not fixed. the retired `ze-dev-setup` (current: `./le setup`) should install `7z` and either build or name the runtime kernel, and `ze-setup CHECK=1` should report both as absent |
| 2026-08-10 | - | hook fixtures | `point-overwrite-case-variant-slug` in the retired `scripts/dev/hook-fixture-check.py` (current producer: `internal/le/`) wanted exit 0 on a case-sensitive filesystem. Two checks own that path and the filesystem picks which one answers: where the file exists `c_point_overwrite` refuses it, and where it does not `c_enforce_naming` refuses the capitals in the `.md` name. The fixture asserted one number for a row whose verdict has two correct sources | asserted the code the filesystem earns, and moved the row out of the loop over the five directory-segment variants, whose basenames naming never sees |
| 2026-08-13 | - | plugin functional suite | `audit-config-commit` (test 52) failed under the retired `ze-functional-plugin-test` (current: `./le functional plugin`) with a five-second `urlopen` timeout in its own observer script, while 629 tests ran in parallel. Run alone it passes in 6.0s. The fixed client timeout is a number measured against a machine under full suite load, so the same bytes give two verdicts | not fixed, and the reproduction attempt is the reason: `ze-test bgp plugin 52` alone exits 0, so nothing deterministic was reachable here. The next step is to raise or remove the fixed timeout in the `audit-commit-test` observer, which asserts a commit lands rather than how fast it lands |
| 2026-08-14 | - | the retired `ze-unit-pkg-test PKG=./scripts/dev` (current: `go test -race ./scripts/dev`) | `test_a_dated_regular_file_is_no_session_directory_in_any_copy` (the retired `scripts/dev/session_bin_dir_test.py` (current producer: `internal/le/`)) split the four session-directory resolvers. For a dated regular FILE, `make` answered `tmp/session/<yesterday>-<sid>` while shell (`[ -d ]`) and python (isdir) skipped it. `ZE_SCRATCH_DIR` (the retired `mk/helper-session.mk` (current producer: `internal/le/session/actions.go`)) globbed `.../<sid>/` and read the trailing slash as a directory restriction, which the glob(3) macOS ships does not apply. Red at HEAD on darwin, green on a runner whose glob honors the slash | fixed here, 1 line: the pattern is `.../<sid>/.` with `$(patsubst %/.,%,...)`, which is directory-only on both globs. All 33 tests in that file pass |
| 2026-08-14 | fixit-dynamic-group-peer-config | `./le changed scope` | `internal/plugins/iface/ra/ifacera.go:31`: `unused: const maxFinalAdvertisements is unused`. Its only non-test reader is `sender_linux.go`, which carries `//go:build linux`, so golangci-lint sees the const used on a linux runner and unused on a darwin workstation. The package reaches the lint set as a reverse dependency (the retired `scripts/dev/changed-pkgs.sh` (current producer: `internal/le/`)), so an unrelated change reds the gate for whoever runs it on macOS. Present in HEAD, unmodified in the tree | not fixed. The const and its reader belong in one build-tag scope, or the lint invocation needs the linux tags this machine does not supply |
| 2026-08-14 | - | ipsec functional suite | the retired `ze-functional-ipsec-test` (current: `./le functional ipsec`) reports 14 of 15 on darwin. `test/ipsec/ipsec-dataplane-show.ci` configures `local-address 10.0.0.1`, and a host without that address cannot bind it. The run logs `ike: failed to start UDP transport ... listen udp4 10.0.0.1:1811: bind: can't assign requested address` and the same for the NAT-T socket. The file's last assertion is `stderr:contains=engine-steps: all steps passed`, and that is the assertion that fails. The `spi 0` step is NOT the cause, though its ERROR line reads like one: the test EXPECTS `command-error:contains=out of range 1..4294967295`, so that step passed. Two sibling files also configure a 10.x address, `ipsec-status-show.ci` and `ipsec-traffic-selector-config.ci`, and both pass, because neither needs the transport to start. The `.ci` carries no `option=needs-linux` | not fixed, and not fixed deliberately. Marking it `needs-linux` would SKIP it on darwin, which reduces coverage to reach green. The honest fixes are to bind an address the host has, or to give the harness the alias the test needs. Found in the closure of spec-fixit-ike-responder-natt-port-float. Not caused by it: every code edit in that closure is a comment, and a comment cannot change a bind result |
| 2026-08-15 | - | wireu shape cache | `TestWireUpdateMixesNLRIFieldsCachedPerMessage` (`internal/component/bgp/wireu/rfc7606_shape_test.go:111`) asserts a WALL-CLOCK ratio: repeat checks must be more than 20x cheaper than the first. It failed at 19.78x on the retired `ze-unit-pkg-test PKG=./internal/component/bgp/wireu` (current: `go test -race ./internal/component/bgp/wireu`), under `-race`, on a machine running several sessions' test suites at once. The property it means to pin is that `shapeOnce` computes the verdict once per message, and that is a structural fact about the code; the number it actually pins is how contended the host was during two loops. Found while running the package for an unrelated comment edit in `wire_update.go` (a diff of zero non-comment lines) | not fixed, and NOT reproduced: the identical command on the same tree passed on the next run (3.7s, whole package green), so the failure is load-dependent rather than deterministic and this row is the record `ai/rules/completion.md` allows for that case. Next step for whoever owns it: replace the timing ratio with a COUNT, since `shapeOnce` can be observed directly (a counter incremented inside the `Do` body, asserted to be 1 after N calls), which pins the same property on every machine at every load |
| 2026-08-15 | fixit-peer-process-event-filter | plugin and reload functional suites | Five plugin tests (`rest-api-commands`, `rest-execute`, `audit-config-commit`, `concurrent-config-commit`, `mgmt-guard-api-env-started-settings-survive`) and three reload tests (`mgmt-guard-reload-*`) failed under a hand-rolled `ZE_TEST_NO_BUILD=1 ZE_BIN=... ze-test bgp plugin --all` and PASSED under the retired `ze-functional-plugin-test` (current: `./le functional plugin`) on the same tree, minutes apart. Every one of them asserts on the API auth surface (`REST never answered on the boot token`, `API auth mode: single-token`), which reads a seeded session store: `$(ZEBIN_ZE)` runs `ZE_SEED_SESSION_STORE` and the isolated build the suite targets seeds its own, so a binary built by another route answers with a different auth mode. Cost was one full-suite diagnosis and an A/B build | not fixed, and the reproduction is the point: the failures do not exist under the target the rules name (`ai/rules/commands.md`, prefer make). A hand-rolled runner invocation is not the same command, and the divergence is silent -- it reports test failures, not a setup error. Whoever needs a single functional test should run the retired `ze-functional-plugin-test` (current: `./le functional plugin`) and read the log rather than invoking the runner directly |
| 2026-08-15 | fixit-peer-process-event-filter | `test/plugin/llgr-readvertise-multipeer.ci` | the reflection to the RR client is never written and the observer's barrier fails with `routes sent=0`; the client's socket shows the OPEN exchange and nothing after. NOT REPRODUCED as a defect: it failed 3 of 4 full `ze-functional-plugin-test` runs and 3 of 4 isolated runs while other sessions were running their own suites, then passed 7 of 8 isolated runs on the same bytes once the machine went quiet. Every arm of the narrowing measured that load and not the code: widening the peer's receive grants to `[ * ]` read as 4/4 against 1/4, and at 8 runs each both arms are 7/8. No `send refused` is logged in any failing run, so the send permission is not the cause | not fixed, and the fixture is left exactly as the migration wrote it: no evidence supports changing it. Next step is a failing run with the daemon at debug, taken on an idle machine, to say whether ze decides not to reflect or decides to and loses the write. Read `updates-sent` for the client peer, and `handleReceived` / `handleState` in `internal/component/bgp/plugins/rib/rib.go` |
| 2026-08-15 | fixit-peer-process-event-filter | `test/plugin/med-removal-before-decision.ci` | the observer sometimes reports `adj-rib-in never stored the route` with an empty peer map. The 2026-08-17 failure identifies the mechanism: `ze-peer` sends the UPDATE, receives Ze's initial End-of-RIB and closes before the observer reads the route. Ze then removes the disconnected peer and purges its Adj-RIB-In. Earlier isolated reruns passed because the observer won that race | fixed 2026-08-17. The peer expects Ze's End-of-RIB and the Administrative Shutdown notification that the observer requests only after it verifies the stored route. The protocol barriers keep the session established through the observation |
| 2026-08-15 | fixit-peer-process-event-filter | the retired `ze-functional-plugin-test ZE_PLUGIN_PARALLEL=1` (current: `./le functional plugin`) | the SERIAL suite is structurally red, and it is red on tests the parallel suite passes. One full serial run timed out five tests at their authored budget: `api-raw` (15.4s), `bgp-rs-reactor-fastpath-fallback` (15.0s), `open-in-established` (15.0s), `rs-fastpath` (20.0s), `system-kernel-log-show` (15.0s). None is contention: each one's MEASURED duration in `tmp/test-timings.json` is 17.4s to 32.1s over 68 to 70 samples, against an authored `option=timeout` of 15s or 20s. `withParallelHeadroom` (`internal/test/runner/runner_exec_util.go`) multiplies every budget by `ParallelTimeoutHeadroom` = 3 when concurrency is above 1 and by 1 when it is not, so the parallel suite gives these tests 45s and hides a duration that outgrew the number the author wrote. Found while measuring plugin-suite flakiness, where a serial run was the control arm | not fixed here, and the control arm is what the row is for: a serial run is NOT a clean baseline in this repository, so `serial passes, parallel fails` may not be read as `the cause is contention` until each test's own budget is checked against its measured duration. The repair is per test: either the budget is honest about the work, or the work is cut back to the budget. `bgp-rs-reactor-fastpath-fallback` is FIXED in this session's diff and its duration fell from 31.5s to 3.7s, which is what the other four need too |
| 2026-08-15 | fixit-peer-process-event-filter | tree-churn probe | `find internal test docs -newermt '-60 minutes'` returns ZERO matches on this macOS, for every window from 10 minutes to 24 hours, on a tree with files modified minutes earlier. BSD `find` parses `-newermt` as an absolute date and a relative string silently matches nothing, so a probe built on it reports a quiet tree whatever the truth is. It was cited to me as evidence that no other session was editing during four suite runs; measured against `git status --porcelain` hashes, three separate sessions edited `test/plugin/filter-family-export-flowspec.ci`, `test/scripts/ze_api.py` (retired, no successor) <!-- doc-links: ignore (deleted 2026-08-28 by eae282592 with no replacement) -->, the retired `mk/test-functional.mk` (current producer: `internal/le/functional/suites.go`) and the retired `Makefile` (current producers: `internal/le/` native action tables) during those runs, and one of those edits landed 5 minutes before the runner reached the fixture it changed | fixed by not using it: the contamination check in this session's harness is `git diff HEAD -- internal test cmd` piped to `shasum`, before and after each run, which is content-addressed and needs no clock. GNU find honours the relative form, so a probe written on Linux fails open on darwin and says nothing about it |
| 2026-08-15 | fixit-peer-process-event-filter | `test/plugin/llgr-readvertise-multipeer.ci` | the 2026-08-15 row above asks for a failing capture taken on an IDLE machine, and this is one. It failed at its 25s foreground budget in a SERIAL the retired `ze-functional-plugin-test` (current: `./le functional plugin`) (`-p 1`, one test on the host at a time) with the 1-minute load average between 4.2 and 6.1 on a 13-core box, and PASSED in 3.4s to 5.1s in all six PARALLEL runs of the same session plus the serial run 90 minutes earlier: 1 red in 8 runs on one tree. Its baseline is 4.2s over 140 samples with a 75s maximum, so the shape is a rare blow-up and not a slow test. This fixture is fully migrated (`attach process` for gr, bgp-rib and rs on both peers), so the delivery filter is not starving it | not fixed, and the mechanism question in the row above is unchanged. What this adds is that QUIET IS NOT THE CURE: the 7-of-8 green isolated runs recorded there were read as load-dependence, and a serial full-suite run on a near-idle host still lost it. The next step named there stands, and it no longer needs a busy machine to reproduce |
| 2026-08-16 | fixit-ci-peer-block-silent-directives | the retired `ze-functional-plugin-test` (current: `./le functional plugin`) under `-p 8` | two full plugin-suite runs 15 minutes apart failed a DIFFERENT set each time and neither set overlapped: run 1 failed `modify-accumulator-per-peer-isolation` alone (603 of 604), run 2 failed `med-not-propagated-across-as` and `med-removal-before-decision` (602 of 604). All three pass in isolation, measured 3 of 3, 2 of 2 and 2 of 2 on the same binaries. Run 1's daemon log names the mechanism for its own failure, `peer-up replay gave up waiting for adj-rib-in to reach the cut ... budget=5s`, which `plan/known-failures/bgp-plugin-rs-forward-duplicate-and-order.md` already records as a deliberate fail-open. `med-removal-before-decision` already has its own row above dated 2026-08-15 with the same shape. Found while repairing seven redistribution fixtures, which touch none of these three files | NOT REPRODUCED as a defect and not fixed. The repair in hand adds two ze-peer processes and the `bgp-rs` plugin to two fixtures, so it adds load to a parallel suite, and the failing set moving between runs is the signature of the budget rather than of an interaction. Next step is the one the 2026-08-15 row above already asks for: a failing capture on an idle machine, reading whether the peer reached Established before the observer's first poll |
| 2026-08-16 | - | `ze-dependency-vulnerability-check` verification stage | the same source tree received Darwin/arm64 locally and Linux/amd64 in CI, so the mandatory gate CAN return a machine-dependent verdict | kept the scanner host-native and used its exec wrapper to provide a Linux/amd64 execution environment |
| 2026-08-17 | spec-hub-deferred-api-auth-independent-of-ssh-block | `test/plugin/mgmt-guard-api-env-started-settings-survive.ci` | The token-mode assertion used the developer's default `ze.config.dir`, so a local zefs power user changed the result to per-user mode after API user resolution was corrected. The same bytes depended on the workstation database | set `ze.config.dir=.` so the fixture uses its per-test directory and has no zefs users |
| 2026-08-18 | shared-machine-job-admission | the retired `ze-unit-hook-test` (current: `./le hook-check unit`) | Four `session-id` fixtures in the retired `scripts/dev/hook-fixture-check.py` (current producer: `internal/le/`) failed under the retired `ze-unit-hook-test` (current: `./le hook-check unit`) and passed on the same bytes when the runner was started directly: `session-id-fork-parent-make-path`, `session-id-fork-parent-scratch-stable`, `session-id-fork-tool-make-path`, `session-id-human-make-path`. Each starts the retired `ze-session-binary-path` (current: `./le session scratch`) in a subprocess and compares its stdout byte for byte. The runner inherits `MAKELEVEL` and `MAKEFLAGS` from the parent make, so the child announces `make[1]: Entering directory` and `Leaving directory` around the path it prints. The invocation the retired Makefile (current producers: `internal/le/` native action tables) prescribes was the only one that could not pass | fixed on the spot, 3 lines: the runner drops `MAKELEVEL`, `MAKEFLAGS` and `MFLAGS` from `os.environ` once at import, and every child environment below derives from it. the retired `ze-unit-hook-test` (current: `./le hook-check unit`) is 473/473 fixtures and 206/206 golden, exit 0 |
| 2026-08-17 | fixit-ike-resource-lifetime-leaks | qemu evidence rail | the retired `ze-qemu-integration-test` (current: `./le qemu run command "./le qemu all-tests"`) reported "VM never reached a login prompt" on a 32-core box at load average 112, with no test having run. `BOOT_TIMEOUT` (the retired `scripts/evidence/qemu-run.py` (current producer: `internal/le/qemu/run.go`)) defaulted to 60 seconds while every install script beside it waits 300 (`ZE_INSTALL_BOOT_TIMEOUT`). The same command passed unchanged with `ZE_QEMU_BOOT_TIMEOUT=300`, so the verdict was the machine's load rather than the tree's state. A shared checkout with several agents running gates at once is the ordinary state of this machine | fixed on the spot: the default is 300, with the measurement in the comment. The cap exists to catch a VM that will never boot, so it costs nothing to sit clear of a slow one |
| 2026-08-18 | fixit-appliance-cert-replace-unvalidated | the retired `ze-functional-appliance-test` (current: `./le functional appliance`) | `test/appliance/vpp-hugepages-qemu.ci` fails with `ze appliance build failed: (gokrazy/modcache looks unpopulated -- run the retired make ze-gokrazy-deps-download (current: ./le setup) once, then retry)`, so the suite reports 11 of 12 on a machine that never ran that download and 12 of 12 on one that did. The QEMU evidence entries beside it self-skip on a missing prerequisite and print `INSTALL-QEMU: SKIP` while exiting 0 (`docs/functional-tests.md`); this one exits 1. Red at HEAD on this machine, unmodified in the tree, and found while proving a new `.ci` in the same suite | not fixed. Either the fixture self-skips on an unpopulated modcache as its neighbours do, or the retired `ze-dev-setup` (current: `./le setup`) populates it |
| 2026-08-19 | fixit-vacuous-functional-tests | `test/plugin/logging-level-filter.ci` wall budget | An independent review ran the unmodified committed file 8 times in sequence and got 4 FAIL and 4 PASS. The four failures were at load average 38 on 32 cores, the four passes at load average 16. The failure mode is not a missing message: ze-peer reports every expected message received and the run still hits the 10s wall. The budget is not this spec's doing and neither is its shape, because `option=timeout:value=10s` and the `timeout=10s` on the foreground step both exist at `84312fe01`, which predates the rewrite this spec shipped | not fixed, and deliberately not tuned. Raising the number until this machine agrees is banned (`ai/rules/testing.md`) and the next loaded machine undoes it. Recorded as the 23rd occurrence of the class; the fix belongs to a deliberate pass over this file, not to a closure commit that would lose its single focus |
| 2026-08-19 | fixit-cli-format-default-everywhere | `test/plugin/aaa-radius-admin.ci` | The fixture waits 10s (50 iterations of `sleep 0.2`) for `ze start` to write `127.0.0.1:<port>` into `daemon.log`, then prints `FAIL: SSH server did not start` and cats an EMPTY log. It failed that way in a full the retired `ze-functional-plugin-test` (current: `./le functional plugin`) at load average 13 on 32 cores, and again in a serial rerun of the failed set. Run alone on the same binaries it passes 3 of 3, at 7.4s, 15.7s and 7.0s against a 20s step budget, so the 15.7s sample sits inside the same daemon-start blow-up the rows above describe. Found while confirming A-1 for the plugin suite, which no earlier phase of that spec could run: the format change touches `execMiddleware` and the `ze cli` client, and this failure is upstream of both, before any command is sent | not fixed and NOT reproduced as a defect. 24th row of this class. The wait is a wall-clock deadline for daemon start, which is what the 2026-08-17 row already names as the subject: ze startup latency under CPU starvation, or `withParallelHeadroom` giving a plain suite run the headroom it gives verify. Raising the 10s loop is banned (`ai/rules/testing.md`) |
| 2026-08-19 | - | the `mcp` tests' readiness wait | A fixed IN-TEST deadline that no contention allowance reaches. `runMCP` (`internal/test/cli/cmd_mcp.go`) defaults `-timeout` to 10s and `waitReady` (`internal/test/cli/cmd_mcp_client.go`) reports `no listener on <addr> after <d>`. `ParallelTimeoutHeadroom` (`internal/test/runner/parallel.go`) widens the RUNNER's per-test budget when concurrency > 1, and it exists precisely because ".ci timeouts sit at 70-100% of the uncontended runtime" -- but this deadline lives inside the test binary, so the headroom passes it by. Discriminated by a concurrency sweep of `test/plugin`, counting that exact line per run at `ZE_PLUGIN_PARALLEL` = 8/16/32/64/8/8/32: **1 / 0 / 6 / 5 / 2 / 0 / 0**. At N=32 all SIX reds of that run were this one message, and no other run's failing set overlapped it. So it is the one failure cluster in that suite attributable to concurrency rather than to a port collision, which is what the sibling class records | not fixed. It BLOCKS raising the suite's concurrency: the sweep measured `plugin` at 590s for N=8 against ~190s at N=32, and adopting that would land the gain as flakes until this deadline scales with contention the way the runner's budget already does. The repair is to give it the same `parallelFactor` treatment, not a bigger constant |
| 2026-08-19 | verify-scope-3-selector-consumers, verify-scope-4-suite-budget-and-ci | a test that inherits the verify runner's own stage environment | SECOND instance of one shape in one session, so it is a class rather than two bugs. `execStage` (the retired `scripts/status/verify_run.go` (current producer: `internal/le/verify/engine/run.go`)) gives every stage `cmd.Env = append(os.Environ(), ...)` plus the run's variables, and `ZE_PACKAGES` is `go list ./...`, so `./scripts/dev` and `./scripts/checks` run their unit tests INSIDE that environment. A test that reads the ambient environment therefore judges the run instead of its fixture, and it can only fail from inside a verify -- never when the author runs it directly, which is the property that makes it expensive. (1) `verifyStageList` (the retired `scripts/dev/github_workflows_test.go` (current producer: `internal/le/`)) ran `./le verify list mode full` with the inherited environment, and the runner exports `ZE_VERIFY_MODE=1` while that target reads the same name as a MODE NAME, so the new shard test failed in every verify run: measured, `ZE_VERIFY_MODE=1 make ze-precommit-verify-list` exits 2 with `unknown mode "1"`. (2) `overriddenEnvironment` (the retired `scripts/checks/staticcheck_feature_matrix_test.go` (current producer: `internal/le/repository/`)) starts from `os.Environ()` and drops only the keys a case supplies, so a case that never names `ZE_VERIFY_SCOPE_TAGS` inherits the run's real answer: `TestStaticcheckFeatureMatrixEmitsPromisedRows` compares output byte-for-byte and fails on the extra stderr line, and the `no answer at all` subtest judged 3 rows of 5 while claiming to test an unset variable | (1) fixed by naming the mode as a make ARGUMENT, which beats the environment, plus `TestVerifyStageListIgnoresTheInheritedVerifyMode` driving it with the variable set. (2) fix in flight: filter the run's variables out of the inherited environment unconditionally rather than per case. The general repair is a helper every such test uses, so a variable ADDED to `execStage` later cannot silently reach a fixture: today each test is responsible for subtracting names it may not know exist |
| 2026-08-21 | - | `test/ui/cli-verb-daemon-dispatch.ci` step 5 | The fixture asserted `'platform' in out` for `ze show host` with the daemon up, and printed `ze show host (daemon up) returned no inventory:` followed by a COMPLETE inventory. `platform` is a section KEY, not a field, and it only reaches the rendering when two or more sections survive detection. On darwin every other section is Linux-only, because `DetectPlatform` (`internal/component/host/platform_other.go:15`) returns a `PlatformInfo` and nothing else, so the answer collapses to one key and `renderValue`'s single-key unwrap (`internal/component/command/pipe_table.go`, the `len(val) == 1` branch) strips the word from the output. Green on linux, red on darwin, same bytes. Deterministic: identical failure in two consecutive full `./le verify current mode full` runs | fixed on the spot, one assertion. It now requires `fd-limit-hard-max`, a FIELD `probeFDLimits` sets on linux (`platform_linux.go:33`) and `DetectPlatform`'s own `Getrlimit` sets on every other Unix, so it proves inventory content arrived whatever survived detection. Not a weakening: a section label that appears only above a section count is a rendering artifact, and naming a field asserts the thing the step exists to prove. NOT caused by the record-answers work: both producers are untouched by it, and the only change this session made to that renderer adds a `case nil` for a whole-answer null |
| 2026-08-21 | - | `./le verify lint run`, `./le changed scope`, the retired `mk/test-chaos.mk` (current producer: `internal/le/testchaos/actions.go`) | Both halves of the linter ceiling were HARDCODED for the 32-core, 31 GiB box and Ze is developed on more than one machine. `concurrency: 8` (`.golangci.yml`) is a quarter of 32 and a HALF of the 16-core machine; `ZE_LINT_MEMLIMIT := 4GiB` is about an eighth of 31 GiB and 6% of 64 GiB. The worker half also broke arithmetic that depends on it: the retired `Makefile` (current producers: `internal/le/` native action tables) sizes every heavy job at a quarter of the cores and `ZE_RUN_SLOTS` divides the box by that share to decide how many run at once, so four slots each running a lint was 32 workers on 16 cores, which is the oversubscription `spec-shared-machine-job-admission` exists to stop. Observed on the 16-core box: one run at 708% CPU with 18 threads, its sibling pinned at 3.97 GiB against the 4 GiB `GOMEMLIMIT`, GC-thrashing to save 0.55 GiB out of 64. Reported by the owner from a process monitor, not by any gate | fixed on the spot. Both halves now DERIVE. The worker half left `.golangci.yml`, which cannot divide, and became `ZE_LINT_RUN := $(ZE_LINT) run -j $(GO_TEST_PROCS)`; all five `golangci-lint run` sites moved to it. The memory half became an eighth of RAM floored at 4 GiB, which keeps the measured number on the box it was measured on. On the 16-core, 64 GiB machine that is `-j 4` and `GOMEMLIMIT=8GiB`, verified with `make -n ./le verify lint run`; `golangci-lint config verify` still exits 0. The class is the one this file names: a tuning constant measured on one machine is a verdict about that machine, and a comment stating the core count ages silently |
| 2026-08-21 | - | `test/install/ze-kernel-no-modcache-mutation.ci`, the retired `ze-functional-install-test` (current: `./le functional install`) | the fixture failed with an exit-code mismatch inside a full `./le verify worktree` over a2fbcbf38, and it is the one test whose subject is that the kernel build leaves the Go module cache unmutated. The owner had just reclaimed 96GB on this host, and `go env GOMODCACHE` held 21 entries when the failure was read, so the cache was refilling while the fixture ran and a build that repopulates it mutates exactly what the fixture asserts nothing mutates | not fixed, and NOT reproducible: 37 of 37 on a local re-run with nothing changed between them. The mechanism above is inference from the entry count and the reclaim, not a measurement, so it is recorded rather than claimed. CORRECTED 2026-08-22, and the first reading was wrong. It has now failed TWICE, both times inside `./le verify worktree`, and passed twice in the main checkout with nothing changed between them, so it tracks the CHECKOUT rather than the reclaim: a fresh worktree fails, a warm tree passes. That is the same shape as the two derived-index defects fixed in `a6cf73960`, where a gitignored generated artifact was absent in a clean checkout and two gates assumed it was there. The fixture only runs `make -n`, which exits 0 in the main tree and carries its own dry-run guard for a cold cache, so the difference is something the worktree lacks rather than the module cache being written. Next step: run `./le verify worktree keep`, then run `ze-test install 39` inside the kept worktree and read which of the fixture's exit branches fires. Do NOT re-derive it from the main tree, which is what made the first reading wrong. Since `b272b7f08` a red `./le verify worktree` copies its stage logs to `tmp/verify-worktree-logs/<stamp>-<sha12>` on its own, so the `KEEP=1` half of that step is now free and only the `ze-test install 39` half still needs the tree kept |
| 2026-08-22 | - | `test/exabgp-compat/bin/bgp`, `./le functional exabgp-test` | the whole suite fails 42 of 42 in about a second. Every test dies before any wire traffic with `TypeError: 'type' object is not subscriptable` at line 976, `updates_seen: list[str] = []`, in the body of `class Checker`. It was first reported as the suite running an interpreter too old to subscript a builtin, which is NOT established | not fixed, and the reported cause is wrong. Run standalone the script WORKS: `python3 test/exabgp-compat/bin/bgp --help` prints its usage under the default interpreter here, 3.14.7, and that class body executes on the way. So the suite supplies a different interpreter than the shell does. What was ruled out: the cached bytecode beside it is `cpython-314`, no `python` of any version sits in the session bin directory the runner prepends to PATH, and `list` is not shadowed anywhere above line 976. What was NOT found: which interpreter the suite actually execs the shebang against. Next step: find where the runner starts `bin/bgp` and make it print `sys.version` and `sys.executable` from inside the failing process, rather than inferring the version from the error text |
| 2026-08-22 | fixit-vacuous-functional-tests | `test/plugin/filter-family-export-flowspec.ci`, the observer's establishment barrier | the file FAILED once on a busy machine reporting `ZE-OBSERVER-FAIL: ze never sent its initial-sync End-of-RIB to both peers`, while the ze-peer transcript in the same failure shows BOTH peers received that exact frame. The barrier is `wait_peer_eor_sent(expected_peers=2)` (`test/scripts/ze_api.py` (retired, no successor) <!-- doc-links: ignore (deleted 2026-08-28 by eae282592 with no replacement) -->), 40 attempts at 0.25s, so it gives up 10s after `ready()` and the run then burns the remaining 20s of its wall. The red was first read as a wire-format break, because another session had 289 uncommitted lines in `pkg/plugin/rpc/message.go` rewriting the answer field encoding, and that story fitted | not fixed, and the first reading was wrong. Measured: three consecutive PASS in the SAME dirty tree 35 minutes later at load average 7.17, 4.4s to 4.6s each, and `originated-nexthop-peer-own.ci` passes in that tree while reading the same `show bgp peer detail` answer rail through `api.peer_counter`, so the rail is intact. The tree was never the variable. Not tuned: raising the attempt count is what `ai/rules/testing.md` bans, and this class earns a deliberate pass of its own. The closure pass settled it on stronger evidence. The answer-encoding work is COMMITTED at HEAD `50468ee34`. The file passes 4 of 4 in a clean detached worktree at that commit, 3.0s to 4.1s at load average 5.35. The diff blamed for the red is now in the tree that passes. The general practice: a red seen once under load is not a defect report until it survives a re-run on a quiet machine, and a plausible in-flight diff is the easiest wrong answer to reach for |
| 2026-08-22 | record-answers-2-only-encoding | `TestSendExecuteCommandReadsRecords/a long walk reads as a record sequence` (`internal/component/plugin/ipc/rpc_test.go`), and `test/plugin/rfc7606-54-bgpls-override-propagates.ci` | each reddened once on this host under load and neither reproduces alone. The unit test failed with `answer queue full: consumer fell behind, answer abandoned` while 20 Go packages compiled and ran together, which is the documented `ErrAnswerQueueFull` path: `answerQueueDepth` holds 256 lines and `MuxConn.readLoop` never waits on a consumer, so a consumer goroutine the scheduler leaves out for 256 records loses its answer. The functional test failed with `route server did not replay within 36.0s` at 60s of wall time under 16-way parallelism. Both are gate verdicts that read the machine rather than the code, and a session that meets one reads a green change as red | not fixed here. Reproduction was attempted for both: the unit test passed 30 of 30 with `-count=30` alone and the whole 23-package set re-ran green at exit 0, and the functional test passed alone in 3.9s against 60s under load. Recorded rather than fixed because neither reproduces, which is the one route `ai/rules/completion.md` leaves open. What a fix would have to decide: whether a consumer the scheduler starves SHOULD lose its answer, which is a design question about `answerQueueDepth` and not a test defect <!-- review, record-answers-2 --> |
| 2026-08-22 | record-answers-3-zero-alloc | `TestWriteAnswerUsesPooledBuffer/a payload built whole` and `TestWriteAnswerReleasesBufferOnError/the transport dies on the terminator of a document` (`internal/component/plugin/dispatch_test.go`) | both were RED at HEAD `53c345199` on this host, without the race detector. Neither has anything to do with the buffer they exist to guard. Each ceiling was an exact count over an answer whose payload the handler built whole. So each swallowed what `json.Marshal` costs for that payload. The marshal measured one allocation when the ceilings were written, and two on this toolchain. That put the answer one over. The pass WITH the race detector stayed green, because its slack absorbed the difference | fixed on the spot. Both ceilings now DERIVE the marshal's cost, measured in the test over the same response. They state on top of it only what the writer owes. That is nothing for the answer that reaches its terminator, and `answerWriteErrorAllocs` for the one whose transport died. Nothing here can pin the standard library's price. A ceiling that tries goes red on a Go release, for a reason nobody owns |
| 2026-08-23 | - | the retired `ze-unit-reactor-test-race` (current: `go test -race ./internal/component/bgp/reactor/...`) (the retired `Makefile` (current producers: `internal/le/` native action tables), `_ze-unit-reactor-test-race-impl`), found while closing fixit-fwdpool-backpressure-timing | The target runs `$(GO_TEST_RACE) -count=20 ./internal/component/bgp/reactor/...`, and `GO_TEST_RACE` carries `-timeout $(GO_TEST_TIMEOUT)`, which defaults to 20m for the whole binary rather than per iteration. Measured on 2026-08-23 on a 16-core darwin box: 10 iterations completed in 1201s, so one iteration costs about 120s and 20 of them need about 40 minutes. The run panicked with `test timed out after 20m0s` while naming whichever test was running at the moment the alarm fired, `TestNoConfigFeedsSentUpdatesToAReceivedOnlyPlugin`. That name is the tell for the trap: the report accuses a test that had been running 0s. Nothing about the verdict says the budget was the cause, and a reader who trusts the name goes looking for a hang in a test that does not hang | not yet fixed. The gate cannot pass as configured while the package costs 120s an iteration, so its budget and its count have to agree. The transferable half: a `-count=N` gate whose timeout covers the whole binary reports its own budget expiring as a hang in an arbitrary test, and the reported name is a function of scheduling rather than of the defect. Read the elapsed time beside the name before believing it |
| 2026-08-23 | - | `golangci-lint` invoked directly, without `make` | Four agents met one panic on one day, `file requires newer Go version go1.27 (application built with go1.26)`, and each read it as a finding against the code it had just written. The verdict came from the host toolchain. The the retired `Makefile` (current producers: `internal/le/` native action tables) exports `GOTOOLCHAIN` derived from `go.mod`, a bare invocation inherits none of it, and every package then compiles to export data the pinned linter refuses. Recurrence of the 2026-08-22 `./le changed scope` row above, reached around make rather than through it | the point `commands/lint-gate/never-invoke-golangci-lint-directly` carries the panic text and the mechanism, and directs every lint through `make` |
| 2026-08-25 | - | `test_a_stalled_holder_is_broken_and_the_kill_carries_its_evidence` (the retired `scripts/dev/ze_run_test.py` (current producer: `internal/le/`)) | SECOND occurrence of the 2026-08-19 row above, same test, same `subprocess.TimeoutExpired` against `ADMITTED_WITHIN`. It keeps the retired `ze-unit-pkg-test PKG=./scripts/dev` (current: `go test -race ./scripts/dev`) red. This row exists to kill two diagnoses that sound right and are not, because both were offered by different readers on this day. Neither is "the job queue is saturated by other sessions". The test cannot see the real queue: `ZeRunCase.setUp` makes `self.root` a fresh `tempfile.TemporaryDirectory()`, `ZeRunCase.spawn` passes `cwd=self.root`, and `ze-run.sh` resolves `JOBS_DIR="tmp/.ze-jobs"` against cwd rather than the git toplevel. `_scan_and_claim` iterates only that directory. `ZeRunCase.env` also pops `ZE_RUN_SLOTS`, so `SLOTS="${ZE_RUN_SLOTS:-1}"` takes the default. One slot, private empty queue, eight live sessions unable to reach it. Nor is it "the bound is too small and should be derived from queue depth": that is `hardcoded-count-in-test` reasoning applied to a bound with 90x headroom | NOT FIXED, and recorded rather than fixed because it was actively tried and did not reproduce. The single test on this box, eight sessions live, ran in 0.324s against the 30s bound. The whole module under plain `unittest` ran 29 tests in 137.039s and passed. So it does not reproduce outside the Go harness, and `TestPythonUnitTests` (the retired `scripts/dev/python_tests_test.go` (current producer: `internal/le/`)) declares no `t.Parallel`, which removes the obvious harness difference too. The 2026-08-19 next step stands and is still the right one: admission is observable as state, so wait for the holder's `.rc` file and then for the waiter's own job entry, rather than putting a wall clock over a fork and a directory scan. Do not raise the constant |
| 2026-08-28 | spec-le-is-a-ze-binary | `./le functional ui` | The native migration made several UI fixtures compile Go tools. The runner's default 64-way concurrency starved daemon startup deadlines: 38 otherwise unrelated tests failed or timed out in one run. The same 38 tests passed together at `-p 8` in 34.3 seconds. | `internal/le/functional.Suites` now gives the UI suite a fixed `-p 8`; `TestUISuiteBoundsNativeToolBuilds` pins the limit. |
| 2026-08-28 | spec-le-is-a-ze-binary | `internal/test/fixture/plugin_fixture_06_exec.go` and `internal/test/fixture/tunnel_fixture_pppoe.go` | Both files are new, carry no build constraint, and use Linux-only APIs: the first imports `github.com/google/nftables` and `github.com/vishvananda/netlink` for `fixture06DDOSTransitSetup` and `fixture06DDOSDropState`, the second calls `unix.Socket(unix.AF_PACKET, ...)` and `unix.SockaddrLinklayer` in `tunnelPPPoEOpen`. `go build ./...` and `go vet ./internal/test/fixture/` are red on macOS and green on Linux, so the server session that wrote them could not see it. The failure text names the vendored dependency rather than the fixture: `undefined: unix.NFPROTO_IPV4` in `vendor/github.com/google/nftables/xt/info.go` | fixed on the spot. Each file is split the way `routing_fixture_linux.go` already does it: the OS calls and their `Register` calls move to `plugin_fixture_06_ddos_linux.go` and `tunnel_fixture_pppoe_linux.go` under `//go:build linux`, and the portable half keeps the packet encoders `tunnelPPPoEPacket` and `tunnelPPPoEParseTags` that `tunnel_fixture_test.go` calls. `go vet ./internal/test/fixture/` is green for GOOS=darwin and GOOS=linux |
| 2026-08-28 | spec-le-is-a-ze-binary | `TestActivityDashboardFixture` (`internal/le/sourcerewrite/sourcerewrite_test.go`) | The test builds its fixture repository with `t.TempDir()` and compares `report.File` against `filepath.Join(repo, "tmp", "activity.html")`. `resolveActivityOptions` (`internal/le/sourcerewrite/activity.go`) takes the repository root from `git rev-parse --show-toplevel`, which answers the symlink-resolved path, and `writeActivity` joins the output onto that. On macOS `t.TempDir()` hands back `/var/folders/...` for a real `/private/var/folders/...`, so the two differ by that prefix and the test is red on this machine and green on Linux | fixed on the spot. The fixture root is now `filepath.EvalSymlinks(t.TempDir())`, so the test compares the same path the producer resolves. Every assertion is unchanged. Green on macOS |
| 2026-08-28 | spec-le-is-a-ze-binary | `./le setup check`, the `golangci-lint` row in `devsetup.Tools` (`internal/le/setup/tools.go`) | The row gives `Probe: []string{"golangci-lint"}`, so the check answers `present` on the strength of the binary existing. A golangci-lint built by a Go older than `go.mod`'s target cannot run: all 17 flavor passes in `./le verify lint run` died with `can't load config: the Go language version (go1.26) used to build golangci-lint is lower than the targeted Go version (1.27.0)`, the gate exited 3, and the PostToolUse lint hook returned the same two lines for every Go edit. `setup check` called that toolchain healthy | NOT fixed. The machine was repaired by reinstalling the pinned `v2.10.1` with the local go1.27.0, which is not the same as the check learning to see it. The probe would have to read `golangci-lint version`, which prints `built with go1.26.6`, and compare it against the `go` line in `go.mod`. This is the third row in this file for that subject: the 2026-08-22 `./le changed scope` row and the 2026-08-23 direct-invocation row are the earlier two, and both end in the same unanswered owner question about pinning `GOTOOLCHAIN` against raising the pins together |
| 2026-08-28 | spec-le-is-a-ze-binary | the `dragonfly` row of `flavorMatrix` (`internal/le/verify/lint/matrix.go`) | The row set `GOOS` and left `GOARCH` empty, so the pass took the host architecture. `go tool dist list` offers exactly one DragonFly pair, `dragonfly/amd64`, so on an arm64 host the pass targeted a combination Go does not build and golangci-lint answered `Running error: context loading failed: no go files to analyze`. The gate counted that as a failed flavor. The one file the pass exists to reach, `internal/core/privilege/drop_other.go` under `//go:build !linux && !darwin && !freebsd && !openbsd && !netbsd`, was linted on the amd64 server and by nothing on this machine | fixed on the spot: the row pins `GOARCH: "amd64"` and its `Why` states that DragonFly builds for no other architecture. `TestFlavorMatrixPinsEveryCurrentBuildInOrder` carries the new row |
| 2026-08-28 | spec-le-is-a-ze-binary | `kernelAutoBuilderFixture`, `kernelQEMUFixture` and `runSyntheticWorker` (`internal/test/fixture/install_fixture.go`) | Each builds a synthetic PATH by symlinking its stub executables (`docker`, `qemu-system-aarch64`, `go`, `tar`, `merge_config.sh`) to the literal `/bin/true`. Recent macOS releases ship that binary at `/usr/bin/true` only, and `os.Symlink` creates a link to an absent target without error, so every stub dangled, `commandAvailable` answered false for all of them, and `selectBuilder` (`internal/appliance/kernelbuilder/driver.go`) returned `no builder available; install docker or qemu and go`. `TestInstallNativeKernelFixtures` therefore read as "this machine has no Docker" when what it had was a non-portable mock. The fixture requires no real daemon: `selectBuilder` only calls `exec.LookPath` | fixed on the spot. `noopBinary` resolves `true` through `exec.LookPath` and returns an error when it is absent, so a missing stub target now says so instead of impersonating a missing builder. All four sites use it. Green on macOS with no Docker running |
| 2026-08-28 | spec-le-is-a-ze-binary | `TestConcurrentRunSharesOneAdmittedChildAndItsExit` (`internal/le/verify/lock/verifylock_test.go`) | The test drives two concurrent `Run` calls and asserts one claims the admission slot while the other attaches. Both answered `inside`. `insideParent` (`internal/le/job/job.go`) returns true whenever `ZE_RUN_JOB` is set, and `admit` short-circuits on it before reaching claim or attach. `childEnviron` sets that variable for the child of `./le job run`, which is the invocation the command rules require, so running the test the mandated way made both calls believe they were nested and the assertion could never hold. Deterministic, 5 of 5, and not a race: a plain run passes. `internal/le/job/contention_test.go` already documents the hazard and carries a `detach` helper for it | fixed on the spot: the test clears `ZE_RUN_JOB` and resets the env cache the way the sibling suite does. 5 of 5 green through the job wrapper and standalone |
| 2026-08-28 | spec-le-is-a-ze-binary | six more surfaces of one class: `transcriptRelativePath` (`internal/le/rules/session_coverage.go`), `parsePackageGraph` (`internal/le/changed/selector.go`), `journalRelativePath` (`internal/le/journal/validate.go`), `inPrivateWorkspace` (`internal/test/localdatacoverage/localdatacoverage.go`), `TestEnsureActionFixturePaths` (`internal/le/scratch`), `TestScratchSymlinkResolvesHostAndGuestPaths` (`internal/le/qemu`) | On macOS a temporary directory is handed out as `/var/folders/...` and its real path is `/private/var/folders/...`. Anything that canonicalises answers the second spelling, and a comparison against the first then fails, so the whole migrated tooling tree is green on the amd64 Linux server and red here. Four distinct producers of the resolved spelling appeared, which is why this is one class rather than one bug: `filepath.EvalSymlinks`, which additionally REFUSES a path whose leaf does not exist yet and so desynchronises an unborn target from an existing root; `git rev-parse --show-toplevel`; `go list`'s `{{.Dir}}`, because a child started with `cmd.Dir` set reports the kernel's spelling; and `os.Getwd`, which falls back to `getcwd(2)` after a `Chdir` because `PWD` is not updated. A fifth surface, `TestActivityDashboardFixture`, has its own row above | fixed, and the producers and the tests were separated rather than treated alike. Producer fixes: `resolveSymlinkPrefix` resolves the longest EXISTING prefix and rejoins the unborn tail, which is what the retired Python's `Path.resolve(strict=False)` did; `canonicalRoot` resolves the root once before every downstream call; the journal guard resolves root before joining, so it still refuses a symlink introduced INSIDE the relative path and no longer false-positives on the root's own canonicalisation; `inPrivateWorkspace` canonicalises the workspace at creation. Test fixes: the fixture root is resolved at `t.TempDir()`. The QEMU one keeps both halves discriminating, because `scratchShare` resolves the host path deliberately and leaves the guest path as literal mount-point text |
| 2026-08-28 | - | `TestEveryNativeScenarioHasCompleteInputs` (`test/interop-ipsec/parity_test.go`) | The `extra` map names `ze.conf.resolved` as a required input for seven scenarios (`clear-reestablish`, `delete-while-window-held`, `eap-mschapv2`, `eap-tls`, `invalid-ke-retry`, `peer-reload-narrowing`, `responder-eap-tls13`) and the loop `os.Stat`s each one. No such file is tracked: `git ls-files test/interop-ipsec/scenarios/` names none, and all eight present copies are untracked. They are renderer OUTPUT, not source. `renderZeConfig` (`internal/le/interoplab/ipsec/ipsec.go`) expands `%%PKI_B64:...%%` and appends `zeCLIConfig()`, and `prepareScenario` writes that result into a scratch work directory as `ze.conf`, never back into the scenario directory. The tell sits in the same test: it forbids `user interop` in the SOURCE `ze.conf`, and that string is exactly what every `.resolved` file carries. So the gate is green only on a machine where somebody once dumped a rendered config beside its source, and red on a fresh clone. An eighth copy, `eap-tls13/ze.conf.resolved`, is not even in the required list | fixed on the spot. The seven entries are gone from `extra`, the eight files are deleted, and the loop now REFUSES a `ze.conf.resolved` beside any source. That turns the leftover from an undeclared requirement into a stated one, so the next dump is caught where it lands rather than a clone later. Green |
| 2026-08-29 | fixit-linger-rejection-reaches-no-verdict | `./le functional plugin`, 703 fixtures, judged from three detached worktrees at HEAD | The plugin suite's FAILURE SET tracks machine load, and three runs of the same suite on the same box produced three sets whose intersection is EMPTY. Idle (load 3.09): 641/644, failing `419 mpls-forwarding-show`, `494 policy-test-errors`, `656 system-sockets-show`. Contended (load 27 rising to 50): 636/644, failing `61`, `249`, `250`, `394`, `412`, `416`, `495`, `656`, of which `249` and `250` were a deliberate mutation. Contended again (load 32 rising to 44): 641/644, failing `394 metrics-name-show`, `495 policy-test-reject-bad-hex`, `515 prefixsid-ebgp-discard-single-walk`. Ten distinct fixtures reddened across three runs and not one reddened in all three. The mechanisms are ordinary once seen: an observer that finishes before ze sends its initial-sync End-of-RIB, a plugin whose mux connection reads EOF during startup, and a relay that reports `read buffer pool exhausted` under contention. What makes it a class rather than a list is the inference it corrupts: a red diagnosed from a contended run names whichever fixture lost the race, so the diagnosis is about the box | not fixed as a whole, and three of the ten were fixed at the source rather than counted. `system-sockets-show`, `system-profile-show` and `mpls-forwarding-show` were observer races with an existing remedy their siblings already used, and each now waits for the End-of-RIB before returning. The practice the rest of the class needs costs one line in any report: state the load average beside the verdict, and re-run on an idle box before diagnosing any red. A verdict quoted without its load is not evidence about the tree |
| 2026-09-03 | spec-rir-seed-embed-and-zefs-refresh | `./le verify worktree`, the staticcheck feature-matrix stage | the stage ends the whole run with `Staticcheck exited -1 without a type-check verdict; verify that standalone Staticcheck supports -checks=-all and -matrix; matrix could not be judged`. Exit -1 is the process dying rather than answering, so the message asks the READER to go and check what the binary supports, and the run stops with no verdict for any later stage. Everything before it had passed: all 18 lint flavors reported 0 issues over the whole tree, and `test-sensitivity` reported its one known assert-nothing overage. Met at the closure of this spec, on a checkout several sessions share, after the run had already spent about 55 minutes queued behind another session's lint. `./le --update` had been run at the start of the session and `bin/le is older than committed sources` still printed afterwards, so the binary that ran the stage may not be the one the tree declares, which is this class's own shape one level down. NOT reproduced: the run was not repeated, because re-running a gate to reconfirm a result already read is banned and the product work was already committed | not fixed, and not this spec's to fix: the red is the GATE's own tooling and says nothing about the tree under test (`ai/rules/pre-release.md`). Recorded here rather than repaired. Whoever picks it up should settle three things before touching the matrix code: whether the staticcheck binary on the box accepts `-checks=-all` and `-matrix` at all, whether `-1` is a crash or a signal from the harness that killed the parent run, and whether the message should name the binary and its version instead of asking the reader to verify them. A stage that cannot get a verdict should say which binary it ran and what that binary answered, so the next reader does not have to reproduce a 55-minute run to find out |
