| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-09 | kernel-compose-make-q-assertion-is-vacuous | installer kernel build | an unconditional rebuild was replaced by a precise trigger. Three cases it had silently covered were left with no trigger at all | enumerated what the blanket mechanism covered, then gave each case its own trigger and its own probe |
| 2026-08-10 | doc-claims-are-checked-not-just-resolved | Stop hook | the snapshot classifier in the retired `.claude/hooks/session-end-summary.sh` (retired; now `internal/le/hookruntime/lifecycle.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> (current producer: `internal/le/hookruntime/lifecycle.go`) treats any line matching `^## Session:` as a block start, so a phase handoff QUOTING that heading is torn in two and part of it deleted. The session most likely to write it is one working on that hook | not fixed, needs the heading to count only at a block start outside an existing block |
| 2026-08-10 | fixit-rib-graph-ci-never-terminates | test runner teardown | a sink, echo or inject `ze-peer` exits only on a signal: the accept loop of `(*Peer).Run` in `internal/test/peer/peer.go` continues for every non-check mode. The teardown loop in `runOrchestrated` skips peer processes, so no code path ever signaled one. The 10s bound in `drainPeers` absorbed it, so nothing failed and all 29 scaffolding-peer tests in `test/plugin/` paid a flat `peerDrainGrace`: 28 `--mode sink` and one `--mode echo`. Only the three whose budget is 20s came near the deadline, which read as one broken test rather than a harness cost | added `terminateScaffoldPeers`, which SIGNALS every non-check peer before the drain barrier and waits for nothing, leaving the bounded `drainPeers` to reap it. Two mutation-proven unit tests, one per predicate term. Per-test latency only: 13.0s to 2.9s serially, and no suite wall-clock change |
| 2026-08-10 | - | plugin RPC | one context served reading the next request and writing the reply to the last one. `handleDaemonShutdown` cancels it inside its own handler, and `writeAppended` returns `ctx.Err()` without writing a byte, so the one caller certain to lose its answer was the caller that asked the daemon to stop. 20 of 35 local functional failures were that answer, lost after every assertion in the test had passed | gave replies their own context through `Server.replyContext`, and a unit test that goes red when the reply is sent on the canceled one |
| 2026-08-31 | - | pppoe client LCP | negotiateLCP (internal/component/l2tp/pppoeclient/session.go) answers every parseable peer Configure-Request with a Configure-Ack, so no peer option is judged at all: a Magic-Number of zero is acknowledged where RFC 1661 Section 6.4 says it MUST always be Nak'd if it is not Rejected outright, and an option whose Length is wrong for its Type draws no Configure-Nak either. The BNG-side negotiator (negotiatePeerOption, internal/component/l2tp/ppp/lcp_options.go) judges both, so the two ze paths answer the same peer differently | not fixed. Needs the client to run the peer's options through the same negotiator instead of acknowledging them unread, which means exporting a policy type the client can build |
