| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-03-21 | - | concurrency | comment claimed external synchronization but no caller held the lock | added the missing lock and deleted the false comment |
| 2026-08-10 | fixit-rib-graph-ci-never-terminates | plugin functional test | `test/plugin/plugin-registration.ci` dispatches `peer * update text nlri ipv4/unicast eor` with no barrier proving a session is established first, so the observer raises `RPC error from ze-plugin-engine:update-route: no established peers to send to`. Measured 8 serial runs each: 6/8 pass on the current tree and 4/8 on a build with this spec's fix disabled, so it predates the fix and is not caused by it | not fixed here: outside this spec's package boundary. Reported to the main thread with both pass rates |
| 2026-08-10 | fixit-forward-rail-initial-sync-ordering | bgp reactor test helpers | `captureSessionLog` said the logger override was race-free against a live session's cold-path logging. The override was; the SINK was not. It handed a bare `bytes.Buffer` to a process-global logger every session's keepalive and hold timers write through, so `make ze-unit-reactor-test-race` reported a data race between a keepalive Debug line and the test reading the buffer. A `syncBuffer` type for this exact hazard already existed one file away | made `captureSessionLog` return the existing `syncBuffer` and stated in its comment which half of the claim the sink carries |
| 2026-08-10 | fixit-vpp-ipsec-inoperable | ipsec dataplane | `RemoveSA` (`internal/component/ike/dataplane/vpp.go`) documented an ordering no caller keeps on a rekey: that the policy is removed first, so the refcounted VPP SA is free when the delete is sent. `removeChildSAExcept` (`internal/component/ike/engine/child.go`) sets `dropPolicy=false` for a make-before-break rekey, because both Child SAs share one policy per direction, and `removeChildSAOutgoing`/`removeChildSAIncoming` then skip `RemovePolicyParams` and call `RemoveSA` anyway. The PROTECT entry still holds VPP's reference, `deleteSAD` returns retval 0 with the SA installed, `RemoveSA` reads that as success and drops the identity from `b.sadIDs`, and `removeInstalled` iterates that map. The retired SA is leaked past `Close`, so it lives until VPP restarts | not fixed: inert while `vppPolicyInterface` refuses every policy IKE produces, so no rekey reaches this backend. The comment now names the `dropPolicy=false` exception instead of asserting the invariant, and the leak is covered by AC-5 of `plan/future/spec-ipsec-vpp-policy-interface.md`. A success code from a foreign system is not evidence the object is gone, and dropping the bookkeeping on it is what makes the leak permanent |
| 2026-08-12 | runtime-kernel-carries-every-tunnel-kind | plugin functional test | `test/plugin/iface-tunnel-restart.ci` treated `daemon.ready` as a barrier for its observer. It is not one. `sendPostStartupToAll` (`internal/component/plugin/server/startup.go`) hands each plugin its post-startup callback to a goroutine and does not wait. The same goroutine then closes `startupDone`. `cmd/ze/hub/main.go` writes the ready file with no happens-before against the observer being told. The runner proceeds to `cmd=stop` on that file (`waitReady`, `internal/test/runner/runner_exec.go`). SIGTERM reaches the daemon while the pass 1 observer is still in `wait_for_post_startup` whenever that goroutine loses the race. Pass 1 then records no ifindex, and pass 2 dies on `FileNotFoundError: 'restart-ifindex'` | added a `cmd=foreground` barrier polling for the recorded ifindex before the stop step, bounded by the engine's own 10s `postStartupTimeout`. Pass 1 now fails loudly when post-startup never arrives. Pass 2 names the missing file instead of raising. Also collected the client output on the runner's setup-script failure return, which reported a barrier's exit status without the message it printed |
| 2026-08-12 | - | plugin functional test | `test/plugin/llgr-readvertise-multipeer.ci` stated in its own header that the stale transition "does not rely on GR restart timers or cross-peer TCP-close races". It relied on exactly that. Its source `ze-peer` is check-mode without linger, so it closed the moment its End-of-RIB expect matched, and ze read the close as a peer-down that WITHDREW 10.0.0.0/24 from obsn. obsn then read the withdrawal as its seq=2. The depreference never arrived. The blind `time.sleep(1.5)` and `time.sleep(2.0)` were beating that withdrawal by under a second on this host. Counter barriers reach `mark-stale` about 1s later, and failed 5/5 | `option=linger:value=true` on the source holds the session open, which is what the header claimed. The three sleeps became `dispatch_until` on the best-route predicate, `wait_peer_eor_sent(2)`, and two polls on obsn's `updates-sent` minus `eor-sent`. 5x 5/5, avg 5.5s down from 11.3s. Dropping the two `mods.Op` calls in `LLGREgressFilter`'s iBGP branch fails it 3x 0/3 |
| 2026-08-12 | - | plugin functional test | `test/plugin/llgr-egress-state-unloaded.ci`, added by `6ec2a10bc` as a copy of the observer in the row above, carried the same three blind sleeps and the same race. Proven on this file rather than inherited: with the counter barriers in and `option=linger` commented out, 5x 0/5, obsn receiving `001B` `WITHDRAWN 10.0.0.0/24` as its seq=2 in place of the depreferenced form. Copying a test copies what its sleeps were hiding | same conversion under an `rfc-test-change-approved:` marker (the file carries `RFC requirement: RFC9494-4.3-3`), plus `option=linger:value=true`. No `expect`, `reject`, `hex` or config line touched. 5x 5/5 at avg 5.5s, and the `mods.Op` drop in `LLGREgressFilter`'s iBGP branch fails it 3x 0/3 |
| 2026-08-13 | - | plugin functional test | Found from phase 4 of `spec-bcp194-6-blackhole`, which this row does not close. Recurrence of the `test/plugin/plugin-registration.ci` row above, three days on and still unfixed. Same observer message, `RPC error from ze-plugin-engine:update-route: no established peers to send to`. Measured this session: 2 failures in 6 full `make ze-functional-plugin-test` runs, and 5/6 pass run alone and serially. That is within a point of the 6/8 recorded on 2026-08-10, so nothing has moved | not fixed here. `test/plugin/plugin-registration.ci` is outside this spec's surface, and the repair is the missing established-session barrier the 2026-08-10 row already names |
| 2026-08-13 | - | plugin functional test | Two more `.ci` fail ONLY under the parallel suite. I tried to reproduce each one alone and failed. `test/plugin/community-scrub-own-ga.ci` (`d5ae7eb47`, 2026-08-12) failed 2 of 6 full `make ze-functional-plugin-test` runs with `ZE-OBSERVER-FAIL: 65000:64 is in the keep-list and must survive the scrub`. Its failing runs took 45.0s against a 2.4s median, and it passes 6/6 alone. `test/plugin/rfc7606-54-discard-unrecognized-nlri.ci` (`a1aec5e6c`, 2026-08-01) failed 1 of the same 6 runs. It read its conn=2 peer's plain UPDATE in place of the fragmented edit-set message, and it also passes 6/6 alone. With the registration row above that is 3 tests, 5 failures over 6 suite runs, no test in common, and none in the surface this session changed. No mechanism is recorded for either because none was found | not fixed and not diagnosed. The next step is the parallel suite rather than the tests. Three unrelated files that fail only under `-p` point at the shared harness, not at three separate barriers. The 45s outlier is the strongest single lead |
| 2026-08-12 | - | plugin functional test | the three egress `.ci` added today for the mixed-UPDATE withdrawal work -- `test/plugin/wellknown-no-export-withdraw-egress.ci` (`ef5ed2a10`), `test/plugin/bgp-rs-control-community-withdraw-egress.ci` (`9759a6b4c`) and `test/plugin/control-community-withdraw-egress.ci` -- each ran the source peer and the destination peers as SEPARATE `ze-peer` processes with no barrier between them. Nothing then proves a destination is a live forward target when the source sends, and a destination that comes up late loses the WITHDRAWAL for good: the announcement is replayed from the Adj-RIB-In, and a withdrawal has nothing to replay from. So the flake lands exactly on the assertion each file exists to make. Measured serially on an unchanged tree: the RFC 1997 file 5/6, the route-server file 3/4, the general-rail file 3/4 | fixed on the two files that are not RFC-tagged: one `ze-peer` process holding three connections, `option=conn_map:value=remote-ip` for the conn numbering and `option=await_eor:value=true` for the barrier, which is the shape `test/plugin/rfc7606-relay-one-field.ci` already documents for this exact failure. Both then measured 6/6 and 12/12, and the general-rail file still goes 2/2 red under its discrimination probe. `wellknown-no-export-withdraw-egress.ci` carries `RFC requirement: RFC1997-Well-1`, so `.claude/hooks/pretool-writeedit.py` held it until Thomas approved the change directly; it then took the same three options under an `rfc-test-change-approved:` block, measured 8/8 serially against 5/6 before, and still goes 2/2 red when the RFC 1997 arm is reverted to a whole-message skip. No assertion moved in any of the three files: only the `conn=` number on each `expect=` and `reject=` line, and no `hex=` line at all. The `.ci` arm of `c_test_weakening` counts LINES, so each file cost one `test-relax:` token and the ceiling rose three times in one day for one false positive -- recorded at the foot of `test/relax-ceiling.txt`, where the remedy is fixing that arm rather than buying more passes <!-- doc-links: ignore (the ceiling file retired on 2026-08-16; this row records where the raises were written at the time) --> |
| 2026-08-13 | - | interop scenario | Found while working `spec-bcp194-6-blackhole`, which this row does not close. `test/interop/scenarios/bgp-rfc7999-blackhole-frr/check.py` reached its assertions with 10.100.0.1 absent from `ip route show` entirely, neither `blackhole` nor `via`. The RFC7999-3.3-1 positive then failed on a prefix that had not arrived, rather than on a prefix handled wrongly. Its wait step prints "waiting for all three prefixes to reach the Ze container's FIB" and did not hold. I tried to reproduce it on the same tree and failed. 1 red in 4 runs of one commit. The other 3 were green with `blackhole 10.100.0.1` present, and a deliberate mutation run in between reddened the intended assertion instead | not fixed and not diagnosed. No mechanism was found, so this row records the observation rather than a repair. The next step is the wait predicate. A barrier that returns without the prefix present turns an arrival race into a conformance failure, which is the shape of the `daemon.ready` row above |
| 2026-08-13 | - | cli config editor functional test | `test/plugin/cli-commit-reject.ci` failed once in three runs of the plugin suite over IDENTICAL code, with `FAIL: config editor did not report transactional commit failure` and exit 1. The daemon behaved correctly in the failing run and logged `config reload: transaction failed`, so the reject reached it; the editor script asserts the rejection is DISPLAYED with no barrier proving the TUI has repainted since the reload returned. Found while running the suite as evidence for the dynamic-peer family fix, which touches neither the editor nor config commit | not fixed. Three runs is a rate, not a mechanism, and the surface is outside that change. The next step is the wait predicate in the expect script: it needs a barrier on the editor output rather than on the commit returning |
| 2026-08-14 | - | plugin functional test | `test/plugin/mup-ipv4-announce.ci` declared ze's initial-sync End-of-RIB BETWEEN its two route frames, an order that holds only while the plugin's first `send()` beats `sendInitialRoutes` to the wire. Nothing enforced it: the plugin's `ready()` is what releases the peer's API-sync barrier, so the marker can be written before the first route arrives. CI is slower than a dev machine and lost that race on 2026-08-14 (run 31782226972), and the fixture then diffed the withdraw against the marker. The `cmd=api:...eor` line beside the marker named a command no plugin ever ran, which is what made the marker look like the fixture's own | fixed: the marker is declared first and the plugin holds `wait_peer_eor_sent()` before announcing, so every frame it sends is a post-EOR incremental update in send order. Reproduced the CI signature byte for byte with a 1s delay before the first send: 0/3 before the change, 5/5 after, and 20/20 with no delay. A plugin cannot send the marker itself, RFC 4724 Section 2 allows one per family per session and `ClaimInitialSyncEOR` drops the second |
| 2026-08-14 | - | plugin functional test | `test/plugin/community-scrub-own-ga.ci` (plugin 139) failed once in a full local plugin suite and once more in 5 isolated runs, the failing run stretching to the 15s budget while the passes take 1.4s. The daemon logged `end-of-rib send failed subsystem=bgp.routes peer=127.0.0.1 family=ipv4/unicast phase=initial-sync error="not connected"`, so the peer went away before the marker was written. Found while fixing the mup4 ordering above, which touches nothing this fixture reads. It passed in the CI run that reported mup4 | FIXED, and the mechanism was the peer closing first. The fixture asserts over per-session state and its peer block carried no `option=linger`, so `Peer.completed` (`internal/test/peer/peer.go`) returned the moment its one `action=send` was satisfied and the connection closed. `handleStructuredState` (`internal/component/bgp/plugins/rib/rib.go`) then took its `!isUp && wasUp` branch, called `peerRIB.Release()` and dropped the peer from `bgpPeers`, which is correct for a session configured `graceful-restart disable`. The observer was racing its own peer's disconnect. Measured: 4 reds in 40 sequential runs before, 40/40 after `option=linger:value=true`, with the 15s outliers gone. The barrier was also polling `show bgp adj-rib-in status`, a different plugin's table from the one the assertions read, which is why every red reported the keep-list value as scrubbed rather than the route as absent; it now polls the prefix in the RIB's own received table and `count_of` refuses an envelope carrying no count. Discrimination re-proved: with `scrub-own-ga false` the fixture goes 0/3 red naming `65000:99`. Six siblings share the no-linger shape (`community-tag`, `community-cumulative`, `community-relation-tag`, `community-priority`, `community-blackhole-noexport`, `adj-rib-in-query`) and measured 20/20 green today, so the shape is recorded here rather than churned |
| 2026-08-15 | fixit-peer-process-event-filter | plugin functional test | **FIFTH occurrence of the no-linger teardown race, and the first where a product change EXPOSED one rather than a host's timing.** `test/plugin/test-pipe-first-last.ci` went from 1.8s green to a deterministic 20s timeout, 3/3, reporting `adj-rib-in never reached 5 routes, last count=0`. The UPDATE was never lost: with `ze_log=debug`, `onMessageBatchReceived` logs `event=update dir=received procs=2` and bgp-rib's `observeForwardHandles` logs all five prefixes stored. The store was then DELETED about 1ms later by `(*RIBManager).handleState`'s `!isUp && wasUp` branch (`peerRIB.Release()`, `delete(r.bgpPeers, peerAddr)`), which is correct under this fixture's `graceful-restart disable`. The peer closed because `(*Peer).completed` (`internal/test/peer/peer.go`) returns as soon as its one End-of-RIB expectation matches and `config.Linger` is false. What changed is the WINDOW: phase 6 of this spec deleted an unconditional 500ms pre-sleep in `sendInitialRoutes` (`peer_initial_sync.go`), so ze's End-of-RIB now goes out at ~1ms and the window closed before the observer's first poll. Five hypotheses were excluded by A/B run first, including disabling the delivery filter entirely, which cost hours across two `/ze-debug` fan-outs | `option=linger:value=true`, the harness's own mechanism. No assertion, hex, or config value moved. 5/5 green at 1.8s average, back to baseline. Restoring the sleep was refused as a workaround: it re-breaks End-of-RIB ordering for every peer (`conf-watchdog.ci`) to widen one test's window, and leaves the race one slow host away. **THE CLASS HAS NOW EARNED A FIX.** Five rows here plus three more fixtures in `test/.ci-sleep-baseline` (`bgp-redistribute-*`, `llgr-readvertise-multipeer`, `nexthop-self`) share one mechanism: a check-mode `ze-peer` with no `option=linger` tears down state its own observer still needs. Every instance has been repaired per-fixture. The structural answer is for the runner to REFUSE the shape, or default linger on, when a fixture pairs an observer plugin with a check-mode peer, since the author who writes that pair never intends the teardown. That is a separable repo improvement, not this spec's scope, and it is raised for the owner rather than folded into a closing commit |
| 2026-08-16 | fixit-ci-peer-block-silent-directives | plugin functional test, an observer's shutdown barrier | The AC-3 repair replaced a blind sleep in `test/plugin/redistribute-export-reject.ci` and `-export-modify.ci` with `api.wait_peer_counter('updates-sent', 1, peer='127.0.0.2')`, and its comment said that threshold proves the forwarded UPDATE is on peer2's socket. It proves nothing. `writeUpdateGated` (`internal/component/bgp/reactor/session_write.go`) notifies `onMessageReceived` for the initial-sync End-of-RIB as well, and the sent branch of `reactor_notify.go` counts every `msgtype.TypeUPDATE` because `wireUpdate` is nil for a sent message and no `IsEOR` test is possible there. Establishment alone reaches 1, so the shutdown could still beat the frame the `expect=bgp:conn=1:seq=2` line asserts, in the two fixtures whose whole subject is what peer2 receives. `wait_peer_counter`'s own docstring warns against an absolute threshold for exactly this reason, and the one other caller (`test/plugin/bgp-redistribute-announce.ci`) waits for `base + 1`. A lifetime counter read against an absolute number is the same defect class as a directive nobody parses: it reads as a guard and decides nothing | fixed. The threshold is 2, the count peer2 reaches on this session (its own EOR, plus the one forward the export filter passes), and the comment now carries the mechanism instead of the claim. `attempts=40` keeps the wait inside the fixture's 15s budget so a lost write prints the barrier's message rather than the runner's timeout. Re-measured 14/14 over two runs of all seven redistribution fixtures. The rule is now in `ai/patterns/functional-test.md` ("An observer plugin answers callbacks before it polls"): a per-peer counter is a lifetime total, so wait for `base + N` or for a threshold that accounts for what establishment already counted |
| 2026-08-21 | - | plugin server unit test | `TestRouteToProcessBuildsRecords` failed 1 run in 3 under `RACE=1` with `written=0, want 1`, on a tree whose only diff in that package is two comments and one unrelated table literal. `gatedAnswerWriter.Write` (`internal/component/plugin/server/command_test.go`) counted the line AFTER `g.writer.Write`. The writer is a `net.Pipe`, so Write returns only once the reader has taken the bytes: the reader could observe the head, return from `routeToProcess` and read the counter while the head's increment was still pending. The assertion was reading its own instrumentation, not the behavior. | fixed here: the count is taken after the gate and before the write. Counting before the gate is the opposite fault, and reproduces at 4 runs in 5 (measured), because the second line raises the count to two while it waits. 6/6 green after |
| 2026-08-22 | - | bgp rib show pipelines | `showPipeline`, `inboundSource` and `outboundSource` (`internal/component/bgp/plugins/rib/rib_pipeline.go`) each state that holding `r.peerMu.RLock` across the drain keeps the `InEntry` pool handles live, naming `handleReceived` as a writer that releases them under `peerMu.Lock` (the comments call it I2). It does not. `handleReceivedStructured` (`rib_structured.go`), which is the path an in-process plugin takes, holds `peerMu.Lock` for phase 1 only, to create the peer's slots, and gives it back before phase 2 removes anything. `peerRIB.Remove` then reaches `FamilyRIB.Remove` (`storage/familyrib.go`), which calls `entry.Release()` under `PeerRIB.mu` alone. `RIBManager`'s own field comment agrees, "peerMu protects the peer-keyed maps ONLY". So a show walk can dereference a handle the RIB has already released, and in a release build a released slot returns to its shard free list and is re-interned with other bytes (`slotReuseEnabled`, `attrpool/validate_release.go`), so the row carries another route's attributes rather than an error. The claim reads as a safety property, which is why nobody re-derived it | FIXED for the best-path walk, which spec-record-answers-3-zero-alloc phase 5 converts: `PeerRIB.LookupRetained` takes a reference to the entry inside the storage lock and `bestSource.Next` gives it back on the next pull, so every dereference is covered by a reference rather than by a lock that never covered it. `TestBestPipelineHandleSafeOutsideLock` removes the route between the pull and the row and reads the attributes back. Also FIXED for `showPipeline` and its three sources, 2026-08-23, once the owner settled the payload shape that had blocked the conversion. The lifecycle change the row called for is what it took: the inbound sources take a reference to each buffered entry INSIDE `IterateSorted`, which runs under the same `PeerRIB` lock `Remove` releases handles under, and give it back when the buffer refills or the walk ends, so the cost is one peer's table of references rather than a whole walk's. `outboundSource` needed no reference at all, because `reconstructRoute` copies every wire byte into an owned `*Route`; it needed the LOCK, taken per peer around the `ribOut` map read. `TestShowPipelineNoLockAcrossWrite` takes the WRITE side of `peerMu` from inside the answer writer, so it fails if any line is written while a reader holds it. The conversion also introduced a nested-reader deadlock in `showProtocolPipeline`, which held `peerMu` across a drain whose `Next` now takes it: no test caught it, because nested readers deadlock only when a writer queues between them, and a `lint` finding of an unused release method is what led to the function |
| 2026-08-23 | - | bgp dynamic peer establishment | `resolveDynamicPeerSettings` (`internal/component/bgp/reactor/reactor_dynamic.go`) read `p.settings.ImportFilters` and `p.settings.ExportFilters` with no lock under a comment saying the establishment goroutine is the only writer of these fields. It was true when written. `applyHotSwappableSettings` (`peer_settings_apply.go`) now writes both from the config-reload goroutine through `hotSwappableSettings`, under `p.mu`, and a slice header is two words, so the reader can take the new pointer with the old length. `hotSwappableSettings`'s own doc comment named the pair as mutable while the comment 30 lines away denied it. The race detector reports it directly. Two more reads of the same population sat in `buildForwardFacts` (`peer_forward_facts.go`), which locked `ExportFilters` and then read `s.PeerAS` and `s.IsEBGP()` outside the lock under a comment saying every other field is set at construction, while `resolveDynamicPeerSettings` writes `PeerAS` under `p.mu` on every establishment | fixed all three. The filter pair is taken under `p.mu.RLock` before the template capture, and `buildForwardFacts` takes `peerAS` and `isEBGP` inside the RLock it already held. `resolveFilterVars` still allocates outside the lock, and the `dyn*` template capture stays outside it because a peer has one session at a time. Every comment that asserted the old population was rewritten to name the real writers. `TestDynamicPeerSettingsFilterReadRacesTheReload` runs a re-resolve loop against a reload loop, and removing the RLock turns it red under `-race` with the read at `reactor_dynamic.go` against the write in `hotSwappableSettings`. The two `buildForwardFacts` reads were each found by that same test going red before they were fixed |
| 2026-08-23 | - | bgp reactor, readers of the mutable PeerSettings fields | found while fixing the row above, and NOT diagnosed per site. `Peer.PeerAS` and `Peer.IsIBGP` (`internal/component/bgp/reactor/peer.go`) each carry a doc comment saying a cross-goroutine caller MUST use the accessor rather than `p.settings.PeerAS` or `settings.IsIBGP()`, because `resolveDynamicPeerSettings` writes that field under `p.mu`. Five sites read it without the lock anyway: `p.settings.IsIBGP()` in `sendRoute` (`peer_send.go`), `peer.Settings().IsIBGP()` in `forward_rs.go`, `peer.settings.PeerAS` in the PeerInfo builder (`reactor_notify.go`), `s.PeerAS` in `reactor_api.go`, and `ps.PeerAS` in `capture_replay.go`. `Peer.Settings()` hands the pointer back with no lock, so it is a route around every accessor. Whether each site can actually run concurrently with an establishment was NOT determined: sizing that is the characterization work `ai/rules/completion.md` keeps out of a walk-in row | not fixed. None of the five blocked the work in hand, which was `resolveDynamicPeerSettings` and the two reads in `buildForwardFacts` that its own race test reddened. Two of the five look like one-line swaps to the locked accessor and three sit on message-rate paths where taking `p.mu` is a cost decision rather than a typo, so the set was reported to the main thread instead of half-repaired |
