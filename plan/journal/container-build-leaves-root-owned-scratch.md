| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-24 | website-asciinema-terminal-demos | `demos/terminal/artifacts` and `/src/tmp/terminal-demos`, written by the demo renderer container | The demo container runs as root, because six demos build network-namespace labs and a `--user` drops every capability the manifest asks for: `docker run --user 1000:1000 --privileged` reports CapEff 0000000000000000, so `ip netns add` fails with EPERM. The entrypoint handed ownership back for the artifacts tree ALONE, and only on the clean path after the render returned. So an interrupted run left the shared HOME, the runtime dir, the demo state trees and the lock file root-owned, and the next plain `cp` of `tmp/` failed on the host. Same class as the kernel row below, one pipeline over | fixed at the producer: `container-entrypoint.sh` installs `give_ownership_back` as an EXIT trap BEFORE it takes the lock, with `INT` and `TERM` traps that exit so the EXIT trap runs on an interrupt too, and it walks both mounts the container writes rather than one. The lock file itself is covered, which the old placement could not be: `demo-lock.sh` creates it as root |
| 2026-08-24 | fixit-qemu-runtime-kernel | `tmp/kernel/build`, written by the containerized kernel build and cleaned by `ze-kernel-vmlinuz-stage` (the retired `mk/build-gokrazy.mk` (current producer: `internal/appliance/cmd_build.go`)) | The kernel build runs in Docker as root and leaves `tmp/kernel/build/lib/modules/<version>/` owned `root:root` in a scratch tree every other target owns as `thomas`. The next the retired `ze-kernel-vmlinuz-stage KERNEL_ARCH=amd64` (current: `ze appliance kernel`) then dies on `rm: cannot remove 'tmp/kernel/build/lib/modules/7.2.0/modules.builtin.bin': Permission denied` and exits 2, so no `tmp/kernel/vmlinuz` is staged and `ze-qemu-kernel-guard` refuses every QEMU functional target. Measured today at 16:28 by the session bumping the pin to 7.2, which also removed the 7.1.4 vmlinuz another session was mid-measurement on. This is the SECOND instance: a 2026-08-05 artifact blocked the same stage and was renamed aside to `tmp/kernel/build.stale-root-owned-2026-08-05`, where it still sits because nothing but `sudo rm` can remove it, and no agent may run that. A rename is not a fix -- it leaves the space consumed and the next instance arrives at the same place | not fixed. No agent can clear either directory. The stage target cannot repair this from inside its own permission set, so the fix belongs where the root-owned bytes are CREATED: the container must write the module tree as the invoking uid (a `--user` on the docker run, or a `chown -R` inside the container before it exits), so the scratch tree stays uniformly owned. Until then every kernel version bump arms the next session's QEMU targets to fail with a message about permissions rather than about the kernel |
| 2026-08-24 | qemu-targets-boot-the-shipped-kernel | `run_docker` (`tools/kernel-builder/run.py` (retired; now `internal/le/deployment/gokrazykernel.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->) | closes the row above, and corrects the `--user` half of its proposed fix. `--user` cannot work: the build also writes the named volumes `/build` and `/tmp/kbuild`, and docker creates a named volume owned `root:root 0755`. Measured on a volume created FRESH for the probe, so an unprivileged uid is denied there on a clean machine exactly as on one that has built before -- `--user` would deny the build its source tree instead of denying it the output. `/out` is the only mount that reaches the host | fixed at the producer: `repair_out_dir_ownership` runs a second `docker run ... chown -R <uid>:<gid> /out` from a `finally`, so a build that dies mid-`modules_install` has its partial tree repaired too. Proven by a real amd64 7.2 build through the patched driver: exit 0, 30 output paths, every one `thomas:thomas`. Argv and ordering pinned by `test/install/kernel-build-output-ownership.ci`, which also asserts the repair after a FAILED build |
| 2026-09-05 | - | `setupBinShims` (`internal/test/runner/runner.go:290`), reached by every `.ci` test that runs under the per-test network namespace mode | The runner creates its bare-name shim directory 0750 as root, and under `ZE_TEST_NETNS` the ze daemon runs as uid 1000. ze relays a plugin through `ze-test` on that PATH, the exec answers `/bin/sh: ze-test: Permission denied`, the plugin never starts, and the test dies on its observer's TLS connect-back rather than on anything that names the directory or the user. Measured today in the guest: `./le qemu netns-test suites ospf` fails all eight of its selected tests this way, so that action has proved nothing about test/ospf for as long as the mode has existed. `./le qemu all-tests` carried the identical defect in its own shim (`shim`, `internal/le/qemu/alltests.go`), where it cost 21 test/ospf timeouts, and that half is fixed in 7d7dd6620 | not fixed here. The shape is the same one line: the shim directory, and the scratch parent that holds it, have to be traversable by the credential-dropped user, because the runner is root and the daemon is not. It sits in `internal/test/runner`, and repairing it would be this session's third piece of scaffolding work (`ai/rules/pre-release.md`) |
