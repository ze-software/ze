# comment describes superseded behaviour

A package or function comment states what the code did before a change, and the
change lands without touching it. The comment then teaches the old contract to
every later reader, and it reads as authority because it sits above the symbol
it describes. `ai/rules/stale-comments.md` governs the write side. This class
counts the times the write side was skipped.

The cost is paid by a reader who trusts the comment instead of the producer.
That reader is often a documentation pass, so the wrong contract reaches an
operator-facing page before anybody runs the code.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-16 | - | bgp/filter_modify | Both package headers said the modifier always returns modify and unconditionally sets declared attributes on every route, and told the reader to compose with an earlier match filter for conditional modification. `handleFilterUpdate` has returned `sdk.FilterAccept` for a route that meets no stated condition since the match container landed in d9c724e38 | rewrote both headers against the producer, naming the accept-unchanged path |
| 2026-08-17 | spec-rfc4271-med-ibgp-readvertisement | bgp/config_direction_test | `TestReceivedOnlyGuardReadsEveryShape` still described two one-line alias shapes as parser-refused after the parser accepted them, so the reactor package gate failed on stale `viaParser: false` expectations instead of a guard defect | updated the case names, comments, and `viaParser` expectations to pin the parsed-tree path |
| 2026-08-17 | fixit-relax-audit-reports-the-wrong-token | test-weakening audit tooling, spec text | The spec described a positional slice in `run_audit`, which quoted the wrong `test-relax:` reason back at a reviewer. That scanner was retired for a row-based audit over `test/weakened.md`. Eight days later the fix had no code to edit. Nothing in the spec said so | Closure read `run_audit` at its producer first, and confirmed that `relax_reasons`, `_RELAX_LINE`, `test-relax:` and `"RELAXED"` are absent. The spec closed with no code change. `test_the_audit_scans_no_token` reads the script source, so the old path cannot return unnoticed. General practice: check that a spec's named mechanism still exists before you write its failing test |
| 2026-08-17 | spec-wire-edit-3-deferred-ac9-dead-code | bgp/reactor forward path, two `.ci` headers | Five prose surfaces name `getEBGPWire` and `wireu.rewriteASPathPrepend` as the live EBGP egress producers: the `forward_readbuf_leak_test.go` site-coverage header, `test/plugin/asn4-transcode-pooled-buffer.ci`, `test/plugin/bgp-rs-asn4-transcode.ci`, `internal/component/bgp/message/attr_discard.go`, and `internal/component/bgp/reactor/session_validation.go`. `getEBGPWire` was deleted by the AS-path fold (`e2037e598`) and no Go file defines it. `rewriteASPathPrepend` survives with no non-test caller. Two of the five also cite line numbers that have moved | Not fixed here. This spec deletes a different symbol (`ReceivedUpdate.EBGPWire`) and does not create or worsen these. Correcting them needs a read of the current `reactor_api_forward.go` and `forward_rs.go` producers, so it is its own pass |
| 2026-08-17 | fixit-review-loop-has-no-termination-bound | review gate rules and skills | The review-round cap moved from three to five, and six surfaces teach that number. The landing commit `2a9773663` moved four of them. `ai/skills/ze-review.md` and `plan/TEMPLATE-CLOSURE.md` still priced a fourth pass, and neither named the owner authorisation a sixth pass now needs | This closure rewrote both surfaces. `ai/rules/repo-maintenance.md` and `ai/rules/context-economy.md` still teach three, and so do their two point renders. A rule edit regenerates `ai/rules/CORE.md`, which another live session holds, so the four files are reported for routing rather than fixed here. General practice: sweep every surface that carries a number, in the commit that changes it. A grep for the old value is that sweep |
| 2026-08-19 | spec-fixit-dns-rfc1035-conformance | `rfc/short/rfc2181.md`, `docs/features/rfc-status.md` | The `{gap}` on `RFC2181-5.1-1` reads "geodns and as112 never set the TC bit or call miekg Truncate", and the RFC 2181 Remaining cell repeats it. `send` (`internal/core/dnsserver/handler.go`) calls `msg.Truncate(udpReplyLimit(r))` on every datagram reply and `Msg.Truncate` sets TC, so the premise is false for the datagram path | Not fixed here. This phase was scoped to the RFC 1035 row. RFC 2181 is ENROLLED, so dropping the `{gap}` needs a positive and a negative tagged test for `RFC2181-5.1-1` first, and the stream path (an RRSet over 65535 octets) still carries no bound. Reported for routing |
| 2026-08-19 | none (four-octet AS extended community encode fix) | bgp route text parser, extcomm renderer doc | The `AppendDecoded` doc comment (`internal/core/bgp/attribute/extcomm_decoded.go`) names `parseExtendedCommunity` (`internal/component/bgp/route/route_community.go`) as a parser that accepts the vocabulary it renders, and says one written as `target:8.8.8.8:8000` is read back the same way. `parseRouteTargetExtCommunity` in that file runs `strconv.ParseUint(parts[0], 10, 32)` on the administrator, so it refuses every IPv4 form, while `parseOneExtCommunity` (`internal/component/bgp/config/routeattr_community.go`) and `Builder.ParseExtCommunity` (`internal/core/bgp/attribute/builder_parse.go`) both accept it. Two spellings of one vocabulary and only one of them round-trips | not fixed. Outside the problem in hand, which is the four-octet AS administrator. Whoever fixes it decides whether the route parser gains the IPv4 form or the doc stops claiming it |
| 2026-08-19 | fixit-cli-format-default-everywhere | `docs/architecture/api/commands.md`, `docs/features/formatting.md` | The change moved WHERE the pipe chain runs, and the two documents naming the old place were not swept. `execMiddleware` (`internal/component/ssh/ssh.go`) splits the chain and applies it, and `Execute` (`internal/component/cli/client/main.go`) sends the chain intact and prints the answer, with `renderCommandOutput` and `printFormatted` deleted from that file. Both docs still read "remain client-side pipe operators" and "carries every other kind to the client", four claims across two files. The spec's own Documentation Update Checklist row 12 named this gap in advance ("the client stops formatting; say so where the CLI/daemon split is described") and the implementation phase read the row as paid once the FEATURE pages were edited. One of the four sentences had been edited AFTER the change, to add the new `raw` operator to its list, and its false half was left standing | fixed at closure, in commit A, with source anchors on `execMiddleware` and on `Execute, commandWithFormat, printDaemonOutput`. The lesson is the checklist row: a row naming an architecture claim is not answered by editing the feature page, and the review's documentation lens is the last thing that reads it |
