# failure report names a command that does not exist

A failure report earns its place by telling the reader what to run next. When
that command does not exist, the reader pays twice: once for the failure, and
once for finding out that the advice was wrong. The advice is never executed by
the code that prints it, so nothing goes red when the target it names is renamed
or was never created. Only a test that holds the printed name against the build
system makes that drift visible.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-19 | verify-scope-4-suite-budget-and-ci | mk/test-functional.mk (retired; current producer: `internal/le/functional/suites.go`) and scripts/status/verify_run.go (retired; current producer: `internal/le/verify/engine/run.go`) | A failed functional suite is told to rerun with `make ze-<suite>-test`, by the FAIL block in `_./le functional` and by `functionalSuiteRerun`. No such target exists for any of the 24 gating suites. The real name is `make ze-functional-<suite>-test`, and even that is absent for ldp, rsvpte and install, which have no individual target at all | fixed on both surfaces. The FAIL block prints `make ze-functional-%s-test`, `functionalSuiteRerun` returns the same name, the cap-expiry group carries it as its `rerun` field, and ldp, rsvpte and install gained the individual targets they lacked. Three tests hold it: `TestFunctionalSuiteRerunNamesARealMakeTarget` (scripts/status/verify_run_test.go (retired; current producer: `internal/le/verify/engine/verifyengine_test.go`)) checks the Go producer against the declared make targets for every all_suites member, and `TestEverySuiteCanBeRerun` and `TestCapExpiryTellsTheReaderWhatToRun` (scripts/dev/functional_suite_test.py (retired; current producer: `internal/le/`)) do the same for the two makefile producers |
| 2026-08-19 | none (owner-commissioned encode expectation fix) | internal/test/runner/report.go, internal/component/bgp/cli/main.go | The DEBUG block of a functional failure prints `ze bgp decode update <hex>`. Run from a shell it exits 1 with `invalid hex: encoding/hex: invalid byte: U+0055 'U'`, because `decode` takes the message kind as an option and `update` is parsed as the payload. The working form is `ze bgp decode --update <hex>`. The `Examples` list on the `ze bgp` help carries the same wrong form | not fixed |
| 2026-08-28 | le-is-a-ze-binary | internal/component/command/pipe_catalog.go `RenderOperatorReference` | The generated header of `docs/features/pipe-operators.generated.md` tells the reader to run `./le docvalid docs-pipe-operators-update`. The registry holds `pipe-operators-update`, so the printed name exits 1 with `no such action in docvalid`. The two call sites in `internal/le/docvalid/drift.go` `checkPipeOperatorReference` print the correct name, so only the generated page is wrong | fixed at `2958f7a18`, in the same commit that wrote this row. `RenderOperatorReference` writes `pipe-operators-update`, and the regenerated page on disk carries it. The row is kept because the class counts occurrences, and because it names the shape: a producer that PRINTS a command name is a second call site for that name, and a rename that reaches the dispatcher without reaching the string it emits leaves the tool telling its reader to run something that exits 1 |
| 2026-08-28 | - | internal/test/perfrunner/run.go `(*Runner).RunCLI` | The missing-binary branch tells the reader to build with `go build -o bin/ze-perf ./cmd/ze-perf`. That directory was folded into `cmd/ze` by eac6ec186, so the printed command exits with `no Go files`. The real build carries the personality tag: `go build -tags 'ze_perf ze_bgp' -o bin/ze-perf ./cmd/ze` | fixed. The message now names `./le perf-bench run`, the native action that performs the build, and the tagged command behind it. Nothing reads the printed name, so the same class can recur here |
| 2026-08-29 | - | the review re-check line every closure script carries, `reviewCheckCommand` (`internal/le/commit/review.go`) | The worst form of this class, because the string is EXECUTED rather than printed. It named `le`, which is not a command on PATH: the launcher is a file at the checkout root and is spelled `./le` everywhere else. A closure script runs the line under `set -e` before its first `git add`, so `./le commit create` produced a script that aborted with `le: command not found` and made no commit at all. CLAUDE.md already says to treat `command not found` in a commit's output as a failed commit, which is how it was caught, but nothing went red before that: no test ran the generated script, and no test read the line | fixed: the word is `./le`, and `TestTheGeneratedReviewRecheckNamesTheLauncherOnDisk` reads the generated line and requires the launcher first. What still holds for the class is that the line is verified by SPELLING and not by execution, so a rename of the action verbs behind it would pass this test and fail the script |
