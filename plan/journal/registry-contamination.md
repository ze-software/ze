| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-03-21 | - | tests | test A called `Reset()` on package-level registry, test B failed | added Snapshot/Restore pair with t.Cleanup |
| 2026-04-06 | - | tests | Snapshot/Restore/Reset in registry missed new global | included every new global in snapshot |
| 2026-08-09 | kernel-profile-fixtures-leak-into-registry | tests | two `.ci` wrote kernel profile fragments into the tracked directory the appliance profile registry scans, cleaned only by an EXIT trap, so a killed run left a buildable profile behind | tests build a scratch repository root and write there; a unit test refuses any on-disk profile outside the shipped set |
| 2026-08-10 | fixit-forward-readbuf-leak-deferred-pool-release-paths | bgp reactor tests | the package-global `bufMuxStd` read pool hands out nothing late in `make ze-race-reactor`. `TestForwardAdoptedHandleHeldUntilLastWrite` (`forward_readbuf_leak_test.go`,349) sees 0 adopted handles where 1 is required, and `TestOverflowItemSurvivesSafetyValveEviction` (`forward_overflow_own_bytes_test.go`,90, untracked) gets a nil buffer in the same run. Both pass alone at `-count=20`, so the trigger is the whole package's traffic through the one global. Reproduces with and without this spec's change, so it is not the Outgoing Peer Pool work. The shared byte budget is ruled out: the three tests that set it restore it with `defer` | not fixed: needs `withCollapseProbe` / `BufMux.collapseLocked` (`bufmux.go`) walked, and `bufMuxStd` in-use measured across repetitions to find which test does not give a handle back. Rehomed here from `plan/deferrals/fixit-forward-readbuf-leak.md`, whose only destination was the spec this row's closure deletes |
