| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-03-21 | - | tests | test A called `Reset()` on package-level registry, test B failed | added Snapshot/Restore pair with t.Cleanup |
| 2026-04-06 | - | tests | Snapshot/Restore/Reset in registry missed new global | included every new global in snapshot |
| 2026-08-09 | kernel-profile-fixtures-leak-into-registry | tests | two `.ci` wrote kernel profile fragments into the tracked directory the appliance profile registry scans, cleaned only by an EXIT trap, so a killed run left a buildable profile behind | tests build a scratch repository root and write there; a unit test refuses any on-disk profile outside the shipped set |
| 2026-08-10 | fixit-forward-readbuf-leak-deferred-pool-release-paths | bgp reactor tests | the package-global `bufMuxStd` read pool hands out nothing late in the retired `ze-unit-reactor-test-race` (current: `go test -race ./internal/component/bgp/reactor/...`). `TestForwardAdoptedHandleHeldUntilLastWrite` (`forward_readbuf_leak_test.go`,349) sees 0 adopted handles where 1 is required, and `TestOverflowItemSurvivesSafetyValveEviction` (`forward_overflow_own_bytes_test.go`,90, untracked) gets a nil buffer in the same run. Both pass alone at `-count=20`, so the trigger is the whole package's traffic through the one global. Reproduces with and without this spec's change, so it is not the Outgoing Peer Pool work. The shared byte budget is ruled out: the three tests that set it restore it with `defer` | not fixed: needs `withCollapseProbe` / `BufMux.collapseLocked` (`bufmux.go`) walked, and `bufMuxStd` in-use measured across repetitions to find which test does not give a handle back. Rehomed here from `plan/deferrals/fixit-forward-readbuf-leak.md`, whose only destination was the spec this row's closure deletes |
| 2026-08-13 | - | l2tp/pppoe subscriber auth | the PPP auth handler slot holds ONE handler (`RegisterAuthHandler`, `internal/component/l2tp/subscriber/handler_registry.go`), and `l2tp-auth-local` and `l2tp-auth-radius` both claimed it from `init()`. The owner was link order, not configuration. RADIUS won, and with no RADIUS server configured its `client == nil` branch (`plugins/authradius/handler.go`) answers every CHAP Response `no RADIUS client`, so an operator's local users were never consulted on either transport. Found on the wire: a pppd client completed LCP, answered Ze's CHAP Challenge and was refused, naming a server the config never mentioned. It was invisible because no L2TP or PPPoE test authenticated at all, every l2tp-interop scenario running `auth-method none` with `allow-no-auth true` | fixed. The RADIUS claim moved from `init()` into `activateRadiusConfig` (`plugins/authradius/register.go`), so precedence is RADIUS when a client is configured and local otherwise. `TestUnconfiguredRadiusLeavesTheAuthSlotToLocal` (`slot_ownership_test.go`) is an EXTERNAL test package on purpose: a package-level var in an internal test file initializes before the package's own `init()` and could never see what it did |
| 2026-08-14 | - | web route registry | `TestWebRouteRegistryRoundTrip` (`internal/component/web/webroute_test.go`) registers `GET /__test_wiring` through `RegisterWebRoute`, which has no removal, so every later test in the package sees a route no hub ever wired. It was invisible while nothing asked the registry what the hub serves. The phase-1 handler golden capture does ask, and it failed with `route "GET /__test_wiring" is live but no golden case reaches it` whenever the round-trip test ran first. File order hid it: `handler_golden_test.go` sorts before `webroute_test.go`, so only `-shuffle` or a rename exposed it | fixed. `restoreWebRoutes(t)` snapshots `webRoutes` under `routeMu` and puts it back in `t.Cleanup`. Proven both ways with a throwaway test that called the round-trip test first: red without the restore, green with it |
| 2026-08-17 | hub-deferred-api-auth-independent-of-ssh-block | plugin reload | removing an auto-loaded config owner stopped its process but left runtime registry claims, so a later reload failed on duplicate commands | both config-owner and orphan teardown use `rollbackStartupProcess`; focused tests prove every claim is reusable |
| 2026-08-23 | - | `TestBGPRemovedAtReloadLeavesTheHubPluginServerRunning` (`internal/component/bgp/reactor/reactor_shutdown_ownership_test.go`), found while closing fixit-fwdpool-backpressure-timing | `registerIdleEngine` calls `registry.Register` with the constant name `test-reactor-reload-survivor` into the process-global plugin registry, and nothing unregisters it. The test therefore passes exactly once per process. the retired `ze-unit-reactor-test-race` (current: `go test -race ./internal/component/bgp/reactor/...`) runs the package at `-count=20` in one process, so runs 2 and later fail on `registry: duplicate plugin name`. Measured on 2026-08-23: 10 of 10 iterations that completed before the run hit its timeout reported that failure, so the gate `ai/rules/testing.md` mandates for every reactor change is red at HEAD for a reason no reactor change caused. The test landed with `spec-fixit-shutdown-waits-out-a-deadlock` and its own package's mandated gate was never run at `-count=20` over it | fixed on the spot. `registerIdleEngine` now takes `registry.Snapshot()` before it registers and puts it back from `t.Cleanup` with `registry.Restore`, which is the pair the registry already published for tests and the same shape as the `restoreWebRoutes` row above. Proven both ways at `-count=20` in one process: the pre-fix file, run through a `go test -overlay` copy taken from HEAD, reds 19 of 20 iterations with `registry: duplicate plugin name`, and the fixed file passes 20 of 20. The overlay was verified to carry the pre-fix text by grepping it for `registry.Snapshot` and requiring 0, against 1 in the tree. The wider point is the one worth carrying: a test that writes to a process-global registry is correct exactly once per process, and nothing about it looks wrong at `-count=1`. Every package whose gate is a `-count=N` run needs its global-state writers to be idempotent across runs or to restore in `t.Cleanup` |
| 2026-08-23 | - | plugin process manager | `ProcessManager.Respawn` (`internal/component/plugin/process/manager.go`) replaced a process without joining it whenever `Running()` was false, and `Running()` reports the ENGINE only: `StartWithContext` also starts an event delivery loop that ends when `Stop` closes its channel. The map entry the respawn overwrote was the only handle on that goroutine. Tests of the same manager left an engine goroutine behind the same way, and it read the package-level `logger` while a later test swapped it, so `-race` named the swap rather than the leak that made it unsafe | fixed on the spot. Respawn stops and joins unconditionally, and each test stops the manager it starts. The point worth carrying: a package var one test swaps is safe only when every goroutine that reads it was JOINED before that test returned. A status flag such as `Running()` is not a join and neither is a goroutine that has in fact finished, because only waiting on the group that owns it orders the two. Nothing about the leak looks wrong at the swap site, and the race detector reports it only while the leaked read is still in its shadow, so the same defect is quiet on most runs |
