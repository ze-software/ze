| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-03-21 | - | hooks | new `.go` file rejected because first exported name collides | used package-qualified name |
| 2026-03-25 | - | hooks | generic name `Config` collided with existing type | renamed to subsystem-prefixed name |
| 2026-08-10 | - | hooks | `c_test_weakening` judged a `.ci` by its non-comment LINE count, so it refused every mechanical improvement (3 `time.sleep` calls collapsed into 1 `wait_until` barrier) and admitted every in-place gutting (2 real assertions swapped for `assert True`). The escape hatch became routine: 751 `test-relax:` tokens at HEAD across 466 files, 362 of them on `.ci`/`.et` excusing a timing refactor, none ever read. A false positive at that rate does not annoy a guard's users, it recruits them | counter replaced by what can fail a run (`_CI_COVERAGE`, `_CI_REJECT`, `_CI_EMPTY_NEEDLE`), tautologies refused (`_TAUTOLOGY`), the hatch keyed on a justification the edit WRITES (`_relax_reasons`, `_writes_new_relax_reason`), and the stock ratcheted (the retired `ze-relax-census` (current: `./le test-weakened check`), `test/relax-ceiling.txt`). `TEST-RELAX-AUDIT.md` <!-- doc-links: ignore (the ceiling file retired on 2026-08-16; this row records the 2026-08-10 fix as it was made) --> |
| 2026-08-12 | - | hooks | the 2026-08-10 rewrite did NOT close its own class: it replaced the line counter with a coverage counter but kept `cmd=` in the count, and `cmd=` re-creates the same false positive one shape along. Collapsing N `ze-peer` processes into 1 and re-pointing their assertions onto `conn=2`/`conn=3` of the survivor removes no coverage: every `expect=` and `reject=` still runs against the same wire. The counter reads it as removed expectations. Three `.ci` needed that exact merge on 2026-08-12, because separate peer processes with no barrier lose a withdrawal for good (an announcement replays from the Adj-RIB-In, a withdrawal has nothing to replay from), and each cost a `test-relax:` token: the ceiling went 752 to 755 in one day for one transformation. The `rfc-test-change-approved` marker satisfies the RFC arm only, so an RFC-tagged file pays twice. A ceiling that only rises stops being a ratchet | not fixed. The distinguishing signal is available and unread: in a merge the `cmd=` count falls while the `expect=`/`reject=` count holds and `conn=` values change, where a real deletion orphans assertions that then fail. Raising the ceiling again is the move this row exists to refuse |
| 2026-08-10 | - | hooks | the FIX above, first cut: three review lenses found the new `.et` arm unreachable (`is_test` never named `.et`, so 164 editor tests stayed unguarded), the new hatch defeated by re-indenting an old token one space, and `\bassert\b` matching prose so two words of comment paid for a deleted `expect=`. A guard rewritten to stop crying wolf can be inert and look stricter | `.et` added to `is_test`; hatch keyed on the normalized SENTENCE not the line; counter comment-stripped and statement-anchored. Each pinned by a fixture that fails when the fix is reverted |
| 2026-08-17 | ssh-optional-composition | gRPC stats handler | wiring check treated `stats.Handler` methods as unwired because grpc-go calls them through the interface | added exact allowlist entries for the four required interface methods |
| 2026-08-17 | spec-hub-deferred-api-auth-independent-of-ssh-block | repository cross-package wiring gate | `check_cross_package_wiring` treated generated gRPC service methods and the four `stats.Handler` callbacks as unwired because production reaches them only through interface registration/dispatch, never a direct method-name reference | derive generated service methods from each `Register*Server` binding and keep one exact receiver/method allowlist for external `stats.Handler`; focused fixtures prove registered receivers pass while lookalike and neighboring methods still fail |
| 2026-08-18 | spec-shared-machine-job-admission | hooks | `check_raw_test_invocation` splits a command on the pipe character before it looks at the command word, so a quoted BRE alternation inside a search argument is read as a pipeline. A `grep -n` over `docs/functional-tests.md`, whose quoted pattern used a BRE alternation to search for `bin/ze-test` and for `CGO_ENABLED=0 go test` together, was refused with "`go test` run raw", and the refusal even offered the retired `ze-unit-pkg-test PKG=docs/functional-tests.md` (current: `go test -race docs/functional-tests.md`). Its own docstring promises the opposite: "neither is a search whose PATTERN quotes a banned verb". The rule becomes unauditable from Bash exactly where it is being documented | not fixed (`.claude/hooks/` was outside this phase's package). `check_poll_loop` already solved the same problem with `SEARCH_COMMANDS`, one file above: split on statements first, and skip a segment whose command word is a search tool before you split it on the pipe character |
| 2026-08-17 | spec-hub-deferred-api-auth-independent-of-ssh-block | documentation wiring gate | `check_wiring` rejected an explicit cross-package `*ForTest` helper even though the repository wiring gate recognizes that suffix as the test-only API convention, so satisfying one required closure gate made the other require a fake production caller | teach the documentation wiring gate the same narrow `*ForTest` convention and pin it with a focused fixture; ordinary new exports still require production wiring |
| 2026-08-18 | - | hooks | `83d426a0a` fixed a real false positive in `_GOVERNED_SHELL_WRITE` (`.claude/hooks/pretool-bash.py` (retired; now `internal/le/hookruntime/bash.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->), where unanchored `cp` and `mv` matched inside "mcp" and refused a plain grep, and the fix narrowed past the true positives. The anchor it gave each verb accepted a line start, whitespace and three separator characters, and it dropped command substitution, because an open parenthesis and a backtick also open a command and neither was in that class. The newline-free span between a verb and its path dropped every backslash continuation for the same reason. Probed on identical inputs across both revisions: `out=$(cp build/x.md plan/spec-x.md)`, its backtick form, and three continuation spellings all BLOCKED before the fix and PASSED after it. The commit added no fixture in either direction. Neither the false positive it removed nor the coverage it lost can fail a run. An independent review of the commit found this, and no gate did | `_VERB_START` adds an open parenthesis and a backtick. `_ARG_SPAN` admits a backslash before a newline, and it is spelled so a backslash has one way to match. Five fixtures cover the dropped shapes. One more covers a BARE newline, which must still end the span so a verb cannot reach the next command's path. General practice: a commit that WIDENS a guard to kill a false positive owes fixtures on both sides. Proof that the false positive is gone says nothing about what the widening cut |
| 2026-09-02 | - | `gitVerbRun` and `bashBranchMove` (`internal/le/hookruntime/bash.go`) | `gitVerbRun` tests the character BEFORE a verb and never the one after it, so a pattern is a PREFIX match. `bashBranchMove` carries `git merge`, and `git merge-base --is-ancestor <sha> main` is therefore refused as branch integration. That command reads two commits and writes nothing. It is also the method `plan/journal/claim-outlives-the-evidence-it-cites.md` prescribes for testing whether a cited SHA survived a rebase, so the guard refuses the repair another journal class asks for. Met while asking whether two commits were already on `origin/main`; answered instead by reading `git log --oneline origin/main..HEAD` | not fixed, and not characterised further. The shape for whoever takes it: the loop already knows `at` and `len(verb)`, so a trailing word-boundary test beside the leading one costs one condition, and the fixtures owed run both ways, because a guard widened to admit `git merge-base` must still refuse `git merge` |
