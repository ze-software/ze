# Claim outlives the evidence it cites

The mirror of `ephemeral-record-stored-permanently`. A claim is written where it
survives, and the evidence it rests on is written where it does not. Nothing
breaks at the moment the evidence goes: the claim still reads as authority, still
names a path, and the reader who follows that path finds nothing. The claim is
then worse than no claim, because it costs a reader the time to discover it is
empty and it discourages the work of re-establishing what it asserted.

The general practice is that a claim and its evidence want the same lifetime. Where
that is impossible, the claim carries enough to be re-checked without the artifact,
or it is not recorded until the moment it is preserved.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-03 | - | `docs/architecture/rfc-may-decisions.md`, the RFC 4760 entry | The page's **RFC Text** block presented a quotation that is not in RFC 4760: "If a BGP speaker receives an UPDATE with MP_REACH_NLRI or MP_UNREACH_NLRI where the AFI/SAFI do not match those negotiated in OPEN, the speaker MAY treat this as an error." No such sentence exists in the document, and the words "MAY treat this as an error" appear nowhere in it. The cited section was wrong too: Section 6 defines SAFI VALUES and says only that "An implementation MAY support all, some, or none of the Subsequent Address Family Identifier values defined in this document". The governing text is Section 7, which grades three remedies rather than offering one. A fabricated quote is worse than a missing one, because it was the stated basis of a MAY decision and every later reader took it as read. Found while implementing the option that entry describes | fixed in the same change. The entry now cites Section 7, quotes it whole, and carries a MUST/SHOULD/MAY table with what ze does at each level. The transferable point: `ai/rules/rfc-compliance.md` requires an RFC claim to quote `rfc/full/`, and NOTHING checks a quotation block on a docs page against the RFC text it names. `./le rfc check` reads `rfc/short/` and the tagged tests, never `docs/architecture/`. A grep of the quoted sentence against `rfc/full/<stem>.txt` catches this in one command |
| 2026-09-03 | - | the unit row of the carrier table in `docs/contributing/rfc-implementation-guide.md`, against `carriers` in `internal/le/rfc/carriers.go` | The page renders the `unit` carrier as "`*_test.go` outside `internal/le/`" executed by "`./le test-unit all`". The producer declares `Prefix: ""`, which is every `_test.go` including `internal/le`'s own, and `Runner: "./le verify deps unit-cached"`. Both cells disagree with the code they render, so a reader deciding whether a tagged unit test earns the verify tier is told the wrong prefix and the wrong command. Walked into while fixing what `./le test-unit all` runs; it predates that change and does not depend on it | Not fixed here: the page's whole carrier table is a hand-written rendering of `carriers`, so reconciling one row leaves the other rows unchecked and the copy still uncompared. The repair worth doing is deriving the table, or gating it against `carriers` |
| 2026-09-01 | rfc-ledger-single-declaration | `internal/plugins/ospf/ldp_sync_test.go`, the `RFC5443-4-1` positive tag and the body comment of `TestLDPSyncTECostUntouched`; the same sentence in `rfc/short/rfc5443.md` Support coverage and on the RFC 5443 row of `docs/features/rfc-status.md` | The tag proves RFC 5443 Section 4 ("apply the cost-raising mechanism only to the IP link cost, not the TE link cost") on the premise "ze originates no TE LSA, so no TE metric is ever touched", and the test body repeats that "the guarantee holds trivially". Ze does originate TE LSAs: `teOriginateType1` builds the RFC 3630 Router-Address and per-link TE LSAs and `teOriginateType6` builds the RFC 5392 inter-AS ones (`internal/plugins/ospf/te_originate.go`), both registered as live opaque consumers by `registerOpaqueConsumer` (`internal/plugins/ospf/te.go`), reachable through `router-address` plus a per-interface `traffic-engineering` block. The assertions the test makes are true and are about `effectiveP2PCost` and `ldpSyncWithholdTransit` alone; what is false is the reason given for why they suffice, so a MUST is recorded as proven by an argument the product contradicts. Found while auditing what Ze implements per OSPF RFC, for the ledger move | not fixed here. The requirement is plausibly still MET by a different argument: nothing read here connects the LDP-sync override to the TE metric, and establishing that it does not is a read of the TE origination path this work did not do. The repair is to re-ground the claim on what the test body actually asserts, which stales the claim hash and owes a fresh `./le rfc discriminate-record` walk, and to correct the same sentence in the summary and on the public row. The general practice this class states, applied to a TAG: the prose half of a tag is a claim no gate reads, so a premise inside it ("ze originates no X") decays exactly like a citation, and it decays silently because the assertions under it keep passing |
| 2026-09-01 | publish-the-rfc-requirement-ledger (research) | `rfc/README.md`, against `docs/features/rfc-status.md` and the `rfc/full/` tree | A hand-written BGP-only status table, last touched 2025-12-31 (`15bfb6b0e`), states a per-RFC Status ("Core", "Implemented") for eleven RFCs and links each one to a text file beside itself. Every one of its 32 links is broken: the texts moved under `rfc/full/`, so `rfc/rfc4271.txt` and its siblings do not exist, and a reader following the table finds nothing. The Status column is also a second declaration of what `docs/features/rfc-status.md` holds as the public ledger, with no check comparing them, so the two can disagree and the reader cannot tell which side is wrong. Found while researching what the site could publish from the RFC corpus; nothing in that work blocks on it, because the publication reads `rfc/short/`, `rfc/requirements/` and the status ledger and never reads this file | not fixed. The repair is a deletion rather than an edit: `docs/features/rfc-status.md` is the public ledger and `rfc/full/` is discoverable without an index, so the question for whoever takes it is whether `rfc/` owes a README at all, and if it does, whether it can hold anything a generator does not already write. Two sibling directories, `rfc/requirements/` and `rfc/audit/`, carry no README while `rfc/discrimination/` and `rfc/extraction/` do |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `rfc/short/rfc9582.md`, its row in `rfc/enrolled.txt`, the `RFC 9582` row of `docs/features/rfc-status.md`, and `parseASPAPDU` (`internal/component/bgp/plugins/rpki/rtr_pdu.go`) | The summary enrolled as `rfc9582` is titled "The Resource Public Key Infrastructure (RPKI) to Router Protocol, Version 2" and declares thirteen requirements at sections 5.12 and 7. `rfc/full/rfc9582.txt` is "A Profile for Route Origin Authorizations (ROAs)", obsoletes RFC 6482, has no section 5.12, and its section 7 is IANA Considerations. The document that states these obligations is draft-ietf-sidrops-8210bis, whose section 5.12 is ASPA and whose section 7 is Protocol Version Negotiation, confirmed against revision 27 dated 13 August 2026. So thirteen requirement ids, thirteen `RFC requirement:` test tags, fourteen source comments, nine test comments and the public claim "RPKI RTR v2 ASPA records, Supported" all name a document that states none of it. This is the same shape as the `RFC8210-11-1` and `RFC4090-4.4-4` rows below, one enrolment deeper: there the section was wrong, here the whole document is. Reading the draft to confirm the attribution showed a second defect: the parser follows a superseded revision. Section 5.12 of revision 27 puts the Flags in header byte 2 and the Customer ASN at offset 8, carries no AFI field, and says of a withdrawal "there MUST be no Provider list, and the PDU Length MUST be 12", while `parseASPAPDU` reads Flags at byte 8 and an AFI byte at 9, takes the Customer ASN from bytes 12 to 16, and refuses any PDU shorter than 20 bytes, so a withdrawal from a current cache is rejected as too short | not fixed. Both re-attribution routes are refused by `./le rfc check`, measured in a throwaway clone of HEAD rather than reasoned from the source: renaming the stem to `draft-ietf-sidrops-8210bis` leaves one error, `checkEnrolment`'s "rfc9582 was un-enrolled, enrolment is monotonic"; keeping `rfc9582` enrolled and giving it a real ROA-profile summary leaves thirteen, one per id, from `checkRetiredRequirements`. Neither check can tell a stem whose obligations MOVED from a stem that stopped gating them, and `validateID` (`internal/le/rfc/summary.go`) anchors every id to its own file's stem, so the ids cannot follow the obligations into the correct summary. The missing mechanism is the stem-level analogue of the `relocated-to-spec` exclusion (`internal/le/rfc/artifact.go`): a recorded re-attribution that lets `checkEnrolment` accept the departure and `checkRetiredRequirements` accept the ids once the receiving stem is enrolled and declares their counterparts. The wire-format half is a separate decision, since the draft is still moving and nothing in the tree records which revision ze targets |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `internal/component/l2tp/session_metadata.go:32`, `internal/component/l2tp/plugins/authradius/extract.go:29`, `internal/component/l2tp/plugins/authradius/acct.go:149` | Three comments cite "RFC 2866 Section 5.18" for Acct-Interim-Interval. That section does not exist: RFC 2866's Section 5 ends at 5.13, Table of Attributes, and the string "Acct-Interim" appears nowhere in `rfc/full/rfc2866.txt`. The attribute is RFC 2869 Section 5.16, type 85. A reader checking the 4-octet width, the [60, 3600] clamp in `clampAcctInterval`, or the Access-Accept-only placement follows the citation and finds nothing, and the same reader never reaches the one MUST NOT the real section states. Found while walking RFC 2869 for its extraction sign-off | fixed here in all three comments, which now cite RFC 2869 Section 5.16. The general practice this class states, applied to an RFC CITATION: a section number is a path, and a wrong one costs the reader the same as a deleted artifact while still reading as authority. Nothing checks that a section number in a comment exists in the RFC it names |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `docs/contributing/rfc-implementation-guide.md:545`, a bullet carrying `<!-- source: internal/le/rfc/actions.go -- Actions -->` | The bullet opens "**Five native RFC actions clear different failures.**" and lists five. `actions` (`internal/le/rfc/actions.go:16-57`) holds seven: `selftest` and `tagged-scope` are absent from the page. The anchor directly under the list names `Actions` as the source, so the page claims to be derived from the very table it disagrees with, and a reader who trusts it never learns that `./le rfc selftest` exists. `runRealTreeSelftest` (`internal/le/rfc/selftest_core.go:311`) asserts `./le rfc check` is clean over the actual checkout, so `selftest` is the action that decides whether a new check can land at all: the one a reader most needs is the one the page omits. Found while reading the page as Required Reading for a spec that adds a check beside `checkUnprovenSupport`, whose prose and anchor sit ten lines below the miscount | not fixed here. The page edit this spec owes is to the `checkSummaryDisposition, checkUnprovenSupport, checkGapCountAgreement` bullet and its anchor at `:578`, a different bullet from the one that miscounts, so repairing the count is a separate read of `actions.go` and of what each of the two unlisted verbs is for. The general practice this class states, applied to a COUNT: a prose sentence that opens with a cardinal is a claim with the lifetime of the table below it, and an anchor naming that table makes the disagreement legible without making it gated. Nothing counts the list. |
| 2026-08-29 | fixit-eap-tls-escape-hatch-kills-the-daemon | a spec's own closure record: its Review Gate note, its AC evidence rows and its artifact path | Three claims written on 2026-08-24 were false four days later, and each one had read as authority in between. The Review Gate note said `go.mod` pinned `toolchain go1.26.6`, so the repository-wide half of `TestNoShippedGuidanceNamesARemovedGODEBUG` did not yet look for the setting and the AC-4 protection was pending; `go.mod` now reads `go 1.27.0` with no `toolchain` line, and the Go 1.27.0 `internal/godebugs` table carries the setting as removed, so the scan has been live and green since the bump. The AC-3 and AC-5 rows cited `python3 test/interop-ipsec/run.py <scenario>`, a runner `eae282592` retired when it ported the lab to `internal/le/interoplab/ipsec/`, so the only recorded proof of two acceptance criteria named a command that does not exist. The Review Gate's Artifact cell named `tmp/review/...-9ad8358c-...md`; `tmp/review/` holds one file for this stem and it is the `ebf5d9b3` one. All three were found by an independent review of the spec, not by any gate: a closure record is prose, and nothing re-reads it | corrected in the spec before closure, each claim re-derived from the tree rather than softened. The general practice, which this class already states and this row sharpens for CLOSURE prose: a closure record is written at the moment the work ends and read at some later moment by somebody deciding whether to trust it, so every fact in it that names a toolchain pin, a runner, or a path under `tmp/` is a fact with a shorter life than the record. Write the version-independent form of the claim beside it, or expect the record to be wrong before anyone reads it |
| 2026-08-29 | spec-mpls-9-rsvp-te-one-to-one-backup | rfc/short/rfc2205.md and rfc/short/rfc4090.md | Two summaries restated their RFC wrongly, and the restatement is what the gate and every later reader work from. `RFC2205-3.10-1` said "high bits 00" where RFC 2205 Section 3.10 says `0bbbbbbb`, which is half the population, and 3.10-2 and 3.10-3 were shifted one form each, so the summary told a reader that `01bbbbbb` is ignored when the RFC rejects it. `rfc4090.md` cited Section 4.4 for the DETOUR object, which is the RRO sub-object flags section; DETOUR is Section 4.2. The same misquote is in the requirement id `RFC4090-4.4-4` | fixed for the text under each id, which is the edit `check_retired_requirements` allows. The RFC 4090 Section 4.2 MUST was unextracted and is now `RFC4090-4.2-1` with positive and negative tags. NOT fixed: `RFC4090-4.4-4` keeps its wrong section, because the section is part of the id and renaming it retires a requirement |
| 2026-08-26 | port-test-functional-to-le | a `test/weakened.md` row, which is the artifact that ACCEPTS a coverage loss | Two rows written to justify deleted and weakened tests cited replacements that do not exist. The `verify_wiring_docs_test` row leaned on `TestVerifyWiringDocsRoutesGoChanges` to carry a branch; that name occurs nowhere in the tree except the row itself. The file declares eleven differently-named tests. The `fuzz_targets_test` row claimed the retired `scripts/le/fuzz_test.py` (current producer: `internal/le/`) adds two cases the deleted generator never had. One was that `FUZZ` and `PKG` reach `go test` unaltered, the other that a failing fuzzer stops the sweep. Neither existed. The producers of both, `_run_one` and the abort branch of `action` (the retired `scripts/le/application/fuzz.py` (current producer: `internal/le/`)), had no test at all. The same row said the file "asserts the same enumeration against the same tree". The deleted test compared discovery against an independent grep for equality, and the replacement asserted `len(...) > 50`, which a walk that drops one package still passes. The rows were written by the same session that wrote the replacements, from memory of what it had intended rather than from the file. The commit gate accepted them because it checks that a row EXISTS for each carrier and cannot check that a row is true. An independent review lens found them by grepping the cited names | The `fuzz_targets_test` row's claims are now TRUE, made so by writing the tests rather than by softening the row. `TestRunOnePassesTheCallerThrough` and `TestTheSweepStopsAtTheFirstFailure` (the retired `scripts/le/fuzz_test.py` (current producer: `internal/le/`)) cover the two producers, and `test_the_real_tree_matches_an_independent_grep` restores the equality against a second-opinion grep. Each was mutation-tested: hardcoding the name in `_run_one`, turning the sweep's `return code` into `continue`, and dropping one target from `discover` each turn the matching case red. The `verify_wiring_docs_test` row is repaired separately, in a commit carrying the ledger and no test file. Its sentence was wrong twice over rather than once, which only the second review pass established: the test it names does not exist, AND there is no read-and-reject branch for any test to drive. That branch was `is_fuzz_source` (the retired `scripts/dev/verify_wiring_docs.py` (current producer: `internal/le/doc/wiring/wiring.go`)), deleted with the gate, so nothing survives to cover and no replacement test was written. Two corrections of this Fix column are themselves instances of the class and are left in view rather than tidied. The first claimed both rows were fixed when only the fuzz row was. The second claimed the ledger row COULD not be repaired, because a later commit carrying it would hold a row matching no weakening and the gate would refuse. That was an excuse rather than a finding, and reading the producer disproves it: `weakened_problems` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)) returns early when the commit's file list holds no test path, and `is_test_path('test/weakened.md')` is False, so a commit carrying only the corrected ledger never reaches the refusal. What is genuinely beyond repair is only the history: `da417ecc4` will always hold the wrong row. The general practice, with one turn of the screw: a weakened-test row is a claim whose evidence is a test in another file, so it must be written by READING that file. A row naming a test is worth no more than a grep for the name, and the grep costs seconds |
| 2026-08-25 | rfc-superseded-requirements-carry-their-successor | the `(§...)` section citation on a requirement row of `rfc/short/rfc8210.md` | `RFC8210-11-1` reads "Cache identity SHOULD be verified and authenticated (§11)". Section 11 of RFC 8210 is Deployment Scenarios and states no such sentence. The obligation is in Section 13, Security Considerations, word for word as RFC 6810 stated it in ITS Section 11, so the citation was carried over from the predecessor's numbering when the summary was written. Found while pointing 67 `rfc6810` markers at their RFC 8210 targets, because `RFC6810-11-2` restates into that row and the successor's own text had to be read to place it | not fixed here, and the marker is unaffected: it names the ID, which is correct, and its reason records that RFC 8210 Section 13 is where the sentence lives. Nothing gates a section citation -- `parse_checklist_line` reads it into `Requirement.section` for grouping and display only, so a wrong one is invisible to every check in the retired `scripts/dev/rfc_requirements.py` (current producer: `internal/le/rfc/rfc.go`). The repair is one cell; the wider one is a check that a cited section exists in `rfc/full/<stem>.txt` |
| 2026-08-25 | - | a peer session's report, where the claim is CREDIT rather than a correction | A peer reported landing an orphaned change "with two review-found defects fixed". This session verified four of that peer's technical claims at their producers, one by one, and then accepted the credit claim without running anything -- and published it to the owner as "verified at the producer", which was true of the four and false of the fifth. The peer refused the credit and asked for it to be measured. Measured: `git worktree add --detach` at the commit's parent, `TestCoalescedUpRestoresBaseMetric` green there and green at HEAD, so the commit neither broke nor fixed it. The asymmetry looked at first like credit versus correction, and the sharper statement, which the peer supplied, is that a claim arriving ALREADY ASSEMBLED does not get the read a claim assembled here gets. It runs both ways in one afternoon: the peer handed over a four-item owed-list on the relaxation audit and had folded the `_RFC_TAG` arm into `_PY_SKIP_PAT` without reading `_RFC_TAG`, and this session caught that by reading the producer; this session took the peer's credit claim without reading anything. Credit is the more dangerous instance of it, because it resolves an open item and closes a worry, so nothing in the reading of it prompts a check. `evidence/claims-about-project-state/a-peer-correction-is-a-claim-not-evidence` names the correction half only, and this session had cited that very point to the same peer forty minutes earlier | the row is the fix, plus the general practice: a peer's CREDIT is a claim on the same footing as a peer's correction, and it is the more dangerous half because nobody re-checks good news. The mechanical form is that a peer's report may not close an item on an owner-facing list until this session has run the thing the report claims. The follow-on is its own instance of the same class and is recorded here rather than tidied away. Chasing the red, this session then reported a possible LOST-WORK event upward: the 14-file set carried `internal/component/iface/resolve.go`, the peer's 17-path commit did not, and `resolve.go` was clean. That was assembled from a path list and a `git log`, and it was wrong. Every one of the 14 is committed, 12 in the mirror commit and the remaining two in `f99860021`, whose only non-comment lines across the whole commit are two verification-debt ledger rows. The deeper error was in the ORIGINAL attribution, made a day earlier: the package was run with 310 dirty paths and the failure was charged to the 14 that happened to be in view, because they were the `iface` ones. A red measured over a tree of 310 modifications names no file |
| 2026-08-24 | fixit-vpp-slaac-no-dataplane-path | a `<!-- source: -->` anchor in `docs/guide/web-interface.md` | The anchor named `cmd/ze/hub/aaa_authenticator_web.go` for `liveAAABundleAuthenticator` and its `Authenticate` method. Both are declared in `cmd/ze/hub/aaa_lifecycle.go`, and the web file's own `// Related:` header says so. `./le docs-to-code index-check` was red at HEAD on it, which the 2026-08-19 row of `plan/journal/concurrent-session-corruption.md` records as blocking every commit in the checkout for every session. Found while running the generated-file gates for an unrelated YANG change | the anchor now names `cmd/ze/hub/aaa_lifecycle.go`, and `./le docs-to-code index-check` reports `all references valid` over 2192 code paths. The anchor moved rather than the symbol: a `Related:` header inside the cited file already carried the correct location, so the two surfaces disagreed and only one of them was gated |
| 2026-08-22 | - | spec Review Gate section against `tmp/review/` | The Review Gate table records a verdict, the round count, the lenses used and the path of the artifact that pins the reviewed files by hash. The table lives in the spec, which is tracked. The artifact lives under `tmp/`, which is cleaned on a cadence. A spec that records a clean verdict and does NOT close in the same session therefore keeps a citation that expires, and the hashes expire with it, so nothing about the recorded verdict can be re-checked. Two specs met this on one day: each read `clean` or `findings`, each named an artifact that no longer existed, and each cost a full independent re-review to close. The gate itself is sound: `commit_helper.py` requires the artifact at closure-commit time, so a spec that closes in the session that reviewed it is never exposed | not fixed. The narrow repair is to treat a recorded verdict as valid only for the session that recorded it, which is what the two-commit closure rule already assumes: a verdict is written when the closure commit is about to carry it, never in advance of one. The wider repair would give the artifact the lifetime of the claim, and that is a decision about where review evidence belongs rather than a defect in the gate |
| 2026-08-24 | fixit-stored-route-relay-hardening | a spec naming functional tests by their POSITIONAL id in a suite | The spec's Current Behavior, its R-1 early signal and its Completion checklist each required "the four target `.ci` tests (372, 378, 394, 395)" to stay green. The ids are positions in `ze-test bgp plugin -l`, which renumbers every test after any insertion. The suite was 495 tests when the number was written and is 702 now, so 372 resolves to `mcp-mrtr-unsolicited-state-rejected`, 378 to `mcp-tools-list-cache-hints`, 394 to `metrics-name-show` and 395 to `metrics-session-lifecycle` -- four MCP and metrics tests with no egress rail in them. Neither the commit that fixed the four (`64a9becc5`) nor the shard that discussed them (`6fb32c6a6`) records their NAMES. One surface did: `plan/deferrals/fixit-load-dependent-functional-failures.md` spells the cluster out, so the mapping survived in exactly one place and by luck, not by design. A checklist item that resolves to the wrong tests reads as one that passed | the spec now names all four (`remove-private-as-replace-peer`, `rfc7606-relay-one-field`, `role-otc-egress-filter`, `role-otc-egress-stamp`) with a then/now id table, and requires the whole suite besides. The general practice this class already states applies exactly: a `.ci` has a stable NAME and an unstable id, so the name is the citation and the id is never one |
| 2026-08-23 | - | a gate verdict quoted against HEAD in a shared checkout | A session reported `./le rules lint` red at HEAD. The commit that fixed it had landed about twenty minutes earlier, so the verdict was true when it ran and stale when it was sent. The evidence here is a TREE rather than a file under `tmp/`, and it expires the same way. The sender was not careless. HEAD names a different tree every few minutes in a checkout several sessions commit to, so neither the reader nor the author can re-test what the verdict claimed | the point `evidence/claims-about-project-state/a-peer-correction-is-a-claim-not-evidence` requires the sha to be quoted with the verdict, which is this class's own remedy of carrying enough to be re-checked without the artifact. It also requires a received verdict to be re-run before it is acted on |
| 2026-08-24 | - | commit SHAs cited in prose, after a rebase | A rebase of 50 local commits onto a diverged `origin/main` rewrote every SHA above the divergence point. Prose that cites one still READS as correct in the checkout where the rebase happened, and that is the trap: the old object is unreachable but still present, so `git cat-file -e <sha>` succeeds and `git show` works for everybody here. It is absent from a fresh clone, and it dies locally at the next `git gc`. So the citation is already broken and this checkout is the one place that cannot tell, which is the same shape as the shared working tree answering a question the committed state answers differently. Caught while writing a journal row for a DIFFERENT class: the draft cited five SHAs, three of which the rebase had just hollowed out, so recording one class would have created an instance of this one. Upper bound measured by the session that ran the rebase: 249 hex tokens of 9+ characters across `plan/`, `docs/` and `ai/` markdown, of which 48 already failed to resolve before that night. The newly-hollow ones are NOT in the 48, because they still resolve here | not fixed and not swept, which would be uncommissioned. The method for whoever takes it: resolve a citation by its SUBJECT LINE, never by its SHA, and test reachability with `git merge-base --is-ancestor <sha> main` rather than `git cat-file -e`, because resolvability is what makes the dead ones look alive. The durable repair is to stop citing bare SHAs in prose where a subject line would identify the commit as well and survive a rewrite |
| 2026-08-29 | - | a published capability claim resting on a plugin REGISTRATION rather than on a call path | A registration proves a function is reachable through a registry; it does not prove any running surface reaches that registry. Where the only resolver of a registry is an offline tool, a claim of live capability built on the registration is false while every path it names exists, so following each cited symbol confirms it and the claim still misleads | the general practice: an ORIGINATION or capability claim is evidenced by the ENTRY POINT that reaches the code, never by the registration that exposes it. Resolve the registry's own callers before writing the claim, and name that caller in it. A registration is evidence of wiring, and wiring is not reachability |
| 2026-08-30 | - | the debt printout of `./le cli-grammar`, which tells a reader where the reason for each forgiven violation lives | The printout sent readers to `internal/le/cligrammar/flags.go` for `flagRegisterDebt` and `flagDeclarationDebt`. Both maps live in `flagdebt.go`, which was split out of `flags.go` when the ledger grew; the printed sentence stayed behind. A reader following it finds the scan and no ledger, and the ledger is what the printout exists to point at. Found while widening F3, which added 11 entries to that ledger and so made the pointer more consulted than it was | fixed in `(Result).Text` (`internal/le/cligrammar/report.go`): the sentence now names `flagdebt.go`. The general practice this class states, applied to a code-owned citation: a file path written inside a printed message has the lifetime of the file layout, not of the message, and nothing re-reads it |
| 2026-08-30 | - | `ai/rules/platform-linux.md`, `docs/functional-tests.md` and `ai/rules/config.md`, each naming a producer that does not exist | Emptying two rules of documentation meant verifying every table before moving it, and five citations turned out to name nothing. Three named a test: `TestQemuAndInteropTargetsHaveACaller` (cited by the platform-linux point `what-actually-runs-these-suites/every-qemu-and-interop-target-needs-a-caller` and twice by `docs/functional-tests.md`) exists nowhere in the tree, and the obligation it claimed to gate, that every registered `./le qemu` and `./le integration` action has a caller, is gated by nothing; `TestCapabilityGatedTestsHaveAQemuHome` is really `TestCapabilityGatedTestsHaveANativeVMHome` (`internal/le/workflowcheck/workflowcheck_test.go:465`); `TestTheAreaPublishesTheNativeActions` does not exist, and the coverage it was cited for is `TestEveryIntegrationPackageIsNamed` and `TestEveryNamedIntegrationPackageExists` (`internal/le/qemu/integration_coverage_test.go`). Two named config surfaces: the config rule's YANG-path-to-env-var table gave `bgp / session / openwait` mapping to `ze.bgp.session.openwait` and `hub / server / idle-timeout` mapping to `ze.hub.server.idle-timeout`, and neither exists. The leaf is `environment/bgp/openwait` (`internal/component/bgp/yang/ze-bgp-conf.yang:1068`) registered as `ze.bgp.openwait` (`internal/component/config/environment.go:40`), and no `ze.hub.*` key is registered at all. A sixth: the initrd procfs table named `/proc/net/route`, `/sys/class/net/*/flags` and `internal/plugins/iface/netlink` as the installer's sources; none is read or imported by `internal/install` or `cmd/ze-installer`, which probe reachability over HTTP (`probeServer`, `internal/install/disk/network.go`) and program links through upstream `vishvananda/netlink` in `internal/install/disk/netlink_linux.go` | every citation corrected at the destination as the content moved: the QEMU caller point now says plainly that no gate checks that direction and names `TestEveryWorkflowNativeActionExists` as the one that checks the other, the two `docs/functional-tests.md` sentences are corrected, and the procfs and env-var tables were rebuilt from the producers before being written to `docs/architecture/appliance/installer-initrd.md` and `ai/patterns/config-option.md`. The general practice this sharpens: a rule is prose and nothing re-reads it, so a table of test names inside one decays exactly like a closure record. A citation that names a Go symbol costs one grep to check and buys a reader the difference between an obligation with a gate and an obligation with none |
| 2026-08-31 | fixit-rfc-drain-quota-never-armed | `rfc/short/rfc2918.md`, the Compliance Checklist row `RFC2918-3-4` | The row is tagged `[MAY]` and its own text reads "Reserved field MUST be ignored by the receiver (even if non-zero)", so the tag and the sentence under it disagree by two grades. RFC 2918 Section 3 states one modal over both conjuncts: "Res. - Reserved (8 bit) field. Should be set to 0 by the sender and ignored by the receiver." The receiver-side level is therefore SHOULD, which is neither what the tag says nor what the text says. The sibling row `RFC2918-3-3` renders the sender half of the same sentence correctly at `[SHOULD]`. Found during the WP-1 extraction walk, where section 3 sites no sentence at all, so all four of its requirement ids are carried as `unsourced-ids` and no site arithmetic reads their levels | FIXED the same day, at the owner's answer. It was raised as a which-way question because the tag is what `Requirement.Gated` (`internal/le/rfc/rfc.go`) reads, so correcting it changes what the gate demands of the receive path, and AC-8 of `plan/spec-fixit-rfc-drain-quota-never-armed.md` barred the work package that found it from changing any `rfc/short/` level. Thomas ruled retag rather than keep-and-prove, so the row now reads `[SHOULD]` with text matching the RFC's single modal, and the two other places in the file that also stated the sentence at MUST moved with it. `TestRFC2918ReservedOctetIgnoredOnReceive` and `TestRFC2918ReservedOctetDoesNotExemptTheMessage` bind it in both polarities |
| 2026-08-30 | flowspec-states-its-own-grammar | `internal/component/plugin/server/usage_model_test.go`, the tables that pin a GENERATED invocation form | Three rows asserted a line the model had stopped producing, and each was committed red and read by nobody until the next spec ran the package. `274e8e013` moved `ze:command` off the `announce` container onto its three forms, and `Usage` (`internal/component/command/usage.go`) answers nil for a node whose `WireMethod` is empty, so `announce` renders "" while `TestUsageRendersTheDeclaredValues` still expected `announce <selector> [tag <key> <value>] [for <duration>]`. `TestDeclaredValuesKeepAcceptedInvocations` kept `path: "announce"` under an `announce unicast ...` input, so its own `require.NotEmpty` fired on "the model declares no argument". The third had a different producer and the same shape: `show policy test peer` expected `[filter <filter>] [update <update>]` while `ze-policy-cmd.yang` declares `leaf name`, `leaf hex` and `ze:modifier "required"` on `update`, which renders `[filter <name>] update <hex>` -- the exact string the `Usage` doc comment uses as its own worked example, so the code and the test disagreed with a comment between them agreeing with the code | the three rows re-derived from the model, with `announce` replaced by `announce unicast` and `announce blackhole`, which is what the split produced. The general practice this sharpens: a table pinning a line a GENERATOR emits is evidence with the same lifetime as the model that emits it, so a commit that moves a `ze:command`, renames a leaf or changes a `ze:modifier` owes every row quoting that line, in that commit. Nothing else reads such a row until an unrelated spec compiles the package |
| 2026-08-30 | rfcgate-6-supported-extraction-signoff | two code comments citing RFC 4760 Section 6 for a sentence RFC 4760 does not contain | `validateUpdateFamilies` (`internal/component/bgp/reactor/session_validation.go`) and the `IgnoreFamilyMismatch` field (`internal/component/bgp/reactor/peer_settings.go`) both quoted RFC 4760 Section 6 as "If a BGP speaker receives an UPDATE with MP_REACH_NLRI or MP_UNREACH_NLRI where the AFI/SAFI do not match those negotiated in OPEN, the speaker MAY treat this as an error." No section of RFC 4760 states it. Section 6 assigns SAFI 1 and 2 and says only "An implementation MAY support all, some, or none of the Subsequent Address Family Identifier values defined in this document", and the document's three uppercase MAY occurrences are that sentence, the key-words paragraph and the Section 7 session-termination MAY. The behavior the comment describes is correct and the authority under it was invented, so a reader checking whether strict mode is RFC-grounded finds a quotation that reads as one and is not. Found while walking `rfc/full/rfc4760.txt` for the extraction sign-off, by grepping the source for the quoted words | both comments re-grounded on text RFC 4760 does state: Section 6's real MAY for why a non-negotiated <AFI, SAFI> arrives at all, and Section 7's "if the speaker determines that the attribute is incorrect" for what the receiver owes. Behavior unchanged. The general practice: a quotation inside a comment is checked by grepping `rfc/full/<stem>.txt` for a phrase of it, which costs one command, and `ai/rules/rfc-compliance.md` already requires the RFC's own text rather than a paraphrase |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `rfc/short/rfc7313.md`, the line numbers inside six checklist annotations | Every `{gap: ...}` and `{single-polarity: ...}` annotation on the RFC 7313 checklist cites a file and a line range, and all six ranges have moved. The received-BoRR and received-EoRR log-only arms are cited at `internal/component/bgp/plugins/rib/rib.go:751-756` by `RFC7313-4-4`, `RFC7313-4-5` and `RFC7313-4-7`; they are the `rpc.EventKindBoRR` and `rpc.EventKindEoRR` cases of `handleEvent` at `rib.go:844-849`. The BoRR-then-routes-then-EoRR dispatch is cited at `rib.go:1008-1015` by `RFC7313-4-1`, `RFC7313-4-2` and `RFC7313-4-6`; it is `handleRefresh` at `rib.go:1111-1113`. The non-zero-subtype early return is cited at `rib_structured.go:506`; it is `handleRefreshStructured` at `:539`. `sendRouteRefresh` is cited at `reactor_api_forward.go:112`; it is declared at `:251`. `routeRefreshNotificationData` is cited at `session_handlers.go:307-313`; it is declared at `:352`. Every SYMBOL named is still correct, so the annotations are re-checkable in seconds and none of them is wrong about what the code does. Found while reading the summary for the RFC 7313 extraction walk, which has to resolve each id against its producer before it maps a site to it | not fixed here. Nothing gates the citation: `./le rfc check` names no rfc7313 violation with the numbers as they stand, because the annotation body is read as a reason string and never resolved against a file. The repair is to drop the line ranges and keep the symbol names, which `ai/rules/writing.md` already requires (a line number is written only when the line IS the fact), and it touches the `{gap}` text of four MUST-level rows, which is a compliance surface this package is not commissioned to edit. The general practice this class states, applied to an RFC summary: a `{gap}` annotation is a claim about a producer, and it is worth exactly the symbol it names, because the line number beside that symbol expires at the next edit anywhere above it in the file |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `rfc/short/rfc4301.md`, `docs/features/rfc-status.md` RFC 4301 row | Three claims about RFC 4301 outlived the code they cite. The summary prose says "Ze delegates IKEv2 negotiation to strongSwan/charon" and "PAD is managed entirely by strongSwan's `ipsec.conf` / `swanctl.conf`"; ze runs a native IKEv2 engine (`internal/component/ike/engine`) and matches peer identity itself (`checkRemoteIdentity` and `remoteIDMatches`, `engine/remote_id.go`). The `RFC4301-4.1-3` annotation says "the IKE child-SA path hardcodes tunnel mode for every peer SA, so a peer SA can never be transport"; `createFirstChildSA` (`engine/child.go`) sets `modeTransport` when `sa.UseTransportMode`, which `engine/transport_mode.go` and the `transport-required` YANG leaf make reachable. The ledger note says "TCP/UDP/SCTP port and ICMP type/code selectors are not modeled or projected"; `dataplane.SPParams` carries `SrcPort`, `DstPort` and `UpperProto`, and the `traffic-selector` list in `ze-ipsec-conf.yang` carries `port` and `protocol` leaves, so only the ICMP type/code half is still absent. Found while walking RFC 4301 for its extraction sign-off | not fixed here. The requirement-row annotation needs the `correction <date>:` paragraph the summary format requires, and the ledger note sits on a `Supported` row whose whole claim this walk is re-opening, so both belong to the same decision rather than to a drive-by edit. The general practice this class states, applied to an RFC summary: an annotation is a claim about a named producing function, and nothing re-reads it when that function changes |
| 2026-09-02 | - | `interopCarriers` in `internal/le/rfc/carriers.go` | The published evidence tier of an interop test is derived from whether a scheduled workflow NAMES its action, and from nothing else: the tier becomes `tierNightly` when `scheduled[tree.action]` is non-empty. No code reads a run result, so every `interop/nightly` cell in `rfc/requirements/` is published as an EXECUTED tier for a job that has not passed. The `interop` job of `evidence-nightly` ran 1 h 30 min on 2026-09-02 and ended `Failed: interop, exit code 1`, and the workflow reported `success` because all six of its jobs carried `continue-on-error: true`. A reader of the public ledger sees interop evidence; the runner behind it is red. Found while removing that flag | not fixed. Removing `continue-on-error` makes the WORKFLOW state its own verdict and changes nothing about the tier, because the derivation never consulted a run. The fix carries a design question: where a build-time renderer gets a run verdict from, without making the site build depend on the network. The general practice this class states, applied to a tier: a tier is a claim that something EXECUTES the test, so it is derived from an execution result, or it is named `scheduled` rather than `nightly` |
| 2026-09-02 | bcp194-7-transit-asn | `docs/guide/plugins.md:726`, the AS-Path Length Filter section, against `countASPathHops` (`internal/component/bgp/plugins/filter_aspath_length/aspath_length.go`) and its own anchor two lines below | The page states "Path length counts AS_SEQUENCE entries individually and AS_SET as 1, following RFC 4271 Section 9.1.2.2". The filter counts every ASN of every segment as one hop. The producer of the text it reads, `(*attribute.ASPath).AppendText` (`internal/core/bgp/attribute/text_append.go:116`), flattens AS_SEQUENCE, AS_SET, AS_CONFED_SEQUENCE and AS_CONFED_SET into one space-separated list and writes no segment marker, and its own comment says the distinction "is NOT preserved on this path", so a member of an AS_SET is indistinguishable from a member of an AS_SEQUENCE by the time the filter sees it. An operator reading the page sets `max` expecting a set to cost one hop and gets a reject at its member count. Found while retiring the plugin private copy of the as-path reader | not fixed. The code cannot meet the claim without reading the wire bytes from `FilterUpdateInput.Raw`, which is a change of what the plugin consumes and of the decisions it makes, so it is a design question rather than a page edit: either the filter takes the raw segments and counts an AS_SET as one, or the page stops citing RFC 4271 Section 9.1.2.2 and states the flattened count the filter performs |
| 2026-09-02 | bcp194-7-transit-asn | `docs/features/configuration.md:192`, a `<!-- source: -->` anchor naming `internal/component/config/loader.go` for `registryName, ExpandDependencies` | `ExpandDependencies` is declared at `internal/component/config/loader.go:479`; `registryName` is declared nowhere in the tree. The only occurrences of the name are parameters of other packages, `CanonicalSubsystemName` (`internal/component/plugin/inprocess.go:27`) and `startup_claims.go:100`. The paragraph above the anchor explains a defect in terms of "keying on the label", so the reader who follows the citation to learn which key is used finds no such symbol. Neither file is modified in the working tree and the symbol is absent from `HEAD` as well, so this is committed rather than another session in flight. `./le docs-to-code index-check` is RED on it over the whole checkout, which blocks the commit of every session until it is repaired. Found while checking that the anchors of the two filter plugins still resolve after retiring their private as-path readers | not fixed. The repair needs a read of `ExpandDependencies` to learn what the renamed or inlined symbol is called now, which is a different package from the one this work touches. The anchor is either re-pointed at the surviving symbol or reduced to `ExpandDependencies` alone |
