| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-26 | le-is-a-ze-binary | the retired `scripts/dev/skill_sync.sh` (current producer: `internal/le/`), run as `--check` by `./le ai sync-check`, by `ze-generated-files-reconcile`, and by the retired `.claude/hooks/session-start.sh` (retired; now `internal/le/hookruntime/lifecycle.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> (current producer: `internal/le/hookruntime/lifecycle.go`) on every session start | Its mode comes from `case "${1:-}" in --dry-run) ... --check) ... esac`. The case has no default branch. Thus, any unrecognized word falls through to the sync branch. That branch writes CLAUDE.md, AGENTS.md, and the three skill mirrors. A mistyped `--check` in the read-only session hook silently regenerates them. A fixture with `--chekc` reproduced the defect: exit 0, `synced 1 skill(s) + ...`, and nine files written | fixed in the port by construction instead of by a new branch. The three modes are three verbs of an `leaction` area (`aisync/actions.go` in the former top-level tool tree). `leaction` refuses an undeclared action and answers 2 with the listing. `TestTheShellSyncsOnAnUnknownFlagAndThePortRefusesIt` (the retired `scripts/dev/skill_sync_parity_test.go` (current producer: `internal/le/`)) pins the script's fall-through and the port's refusal. Thus, the test becomes red if somebody adds the default branch |
| 2026-08-17 | - | discovery index generators | `--help` rewrites tracked files. `main` in the retired `scripts/dev/package_map.py` (current producer: `internal/le/discoveryindex/discoveryindex.go`), the retired `scripts/dev/code_to_docs.py` (current producer: `internal/le/docstocode/codetodocs.go`) and the retired `scripts/dev/docs_to_code.py` (current producer: `internal/le/docstocode/docstocode.go`) reads its mode as `"--check" in sys.argv` and takes the regenerate branch for every other argument, so a read-only-looking probe writes `ai/PACKAGE-MAP.md`, `ai/CODE-TO-DOCS.md` and `ai/DOCS-TO-CODE.md`. Found when an agent ran the three with `--help` to learn their interface and rewrote all three instead. The same shape as the row below: a flag that promises to report, and a tool that writes | not fixed. A minimal fix rejects an unrecognised argument before the regenerate branch, and each script has a sibling `*_test.py` that would carry the case |
| 2026-08-14 | - | vendored templ generator | `templ generate -check` deletes files. `HandleEvent` (`vendor/github.com/a-h/templ/cmd/templ/generatecmd/eventhandler.go`) calls `os.Remove` on an orphaned `*_templ.go` directly. Only `keepOrphanedFiles` gates it, and nothing on that path asks whether the run is a check. So the flag that promises to report instead of write still removes a tracked file. Found when `./le doc check templ-output` was wired for the web-templ-migration spec. Measured in a scratch tree: an orphan is gone after `templ generate -check -path <tree>`, exit 0 | fixed by the tool's own flag. `./le doc check templ-output` runs `templ generate -check -keep-orphaned-files`, and `HandleEvent` returns before `os.Remove` on that field. Measured 2026-08-14: the orphan survives the check and exit is 0. The flag also silences the report. the retired `scripts/dev/templ_orphan_check.py` (current producer: `internal/le/`) names an orphan now, as the prerequisite target `ze-templ-orphan-check`. `./le repository generate` runs templ WITHOUT the flag and carries no guard. Deleting a generated file whose source is gone is regeneration, and the removal shows in `git status`. No gate under `./le verify current mode full` names it in the same cycle: `ze-generated-files-reconcile` is run by nothing, and `TestVerifyStagesGuardGeneratedFiles` keeps it that way. A fresh checkout catches it one cycle later, where the file is on disk with no source. No other caller runs templ |
