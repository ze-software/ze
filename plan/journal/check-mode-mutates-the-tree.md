| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-17 | - | discovery index generators | `--help` rewrites tracked files. `main` in `scripts/dev/package_map.py`, `scripts/dev/code_to_docs.py` and `scripts/dev/docs_to_code.py` reads its mode as `"--check" in sys.argv` and takes the regenerate branch for every other argument, so a read-only-looking probe writes `ai/PACKAGE-MAP.md`, `ai/CODE-TO-DOCS.md` and `ai/DOCS-TO-CODE.md`. Found when an agent ran the three with `--help` to learn their interface and rewrote all three instead. The same shape as the row below: a flag that promises to report, and a tool that writes | not fixed. A minimal fix rejects an unrecognised argument before the regenerate branch, and each script has a sibling `*_test.py` that would carry the case |
| 2026-08-14 | - | vendored templ generator | `templ generate -check` deletes files. `HandleEvent` (`vendor/github.com/a-h/templ/cmd/templ/generatecmd/eventhandler.go`) calls `os.Remove` on an orphaned `*_templ.go` directly. Only `keepOrphanedFiles` gates it, and nothing on that path asks whether the run is a check. So the flag that promises to report instead of write still removes a tracked file. Found when `make ze-templ-output-check` was wired for the web-templ-migration spec. Measured in a scratch tree: an orphan is gone after `templ generate -check -path <tree>`, exit 0 | fixed by the tool's own flag. `make ze-templ-output-check` runs `templ generate -check -keep-orphaned-files`, and `HandleEvent` returns before `os.Remove` on that field. Measured 2026-08-14: the orphan survives the check and exit is 0. The flag also silences the report. `scripts/dev/templ_orphan_check.py` names an orphan now, as the prerequisite target `ze-templ-orphan-check`. `make generate` runs templ WITHOUT the flag and carries no guard. Deleting a generated file whose source is gone is regeneration, and the removal shows in `git status`. No gate under `make ze-precommit-verify` names it in the same cycle: `ze-generated-files-reconcile` is run by nothing, and `TestVerifyStagesGuardGeneratedFiles` keeps it that way. A fresh checkout catches it one cycle later, where the file is on disk with no source. No other caller runs templ |
