# Derived flags overwrite what the wire carried

An attribute's flag octet is recomputed from its TYPE when the attribute is
written back out, rather than kept from the octet the peer sent. Every value the
type implies survives. Every value the PATH carried is lost, and no test sees
it, because the encoder and the decoder agree with each other about what the
flags "should" be.

The tell is a `Flags()` method that returns a constant.

The mirror of the same axis belongs here too: a rail that copies the received
octet forward where the RFC requires ze to correct it. The rails are the same
ones, the octet is the same octet, and a reader who meets one half needs the
other.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-21 | - (rfc5036 MUST triage) | `EncodeInit` and `DecodeInit` (`internal/plugins/ldp/wire.go`) | The Loop Detection D bit of the Common Session Parameters was written and read at 0x04, inside the six reserved bits, where RFC 5036 Section 3.5.3 puts A at 0x80 and D at 0x40. Nothing set it on transmit, so the wire stayed conformant by accident, and on receipt a peer's D=1 was read as 0 while a reserved bit read as D | FIXED: `initFlagOnDemand` (0x80) and `initFlagLoopDetection` (0x40) carry the layout, `DecodeInit` reads A into `initMessage.OnDemand`, and `EncodeInit` writes PVLim only beside D=1; `TestRFC5036InitReservedBitsIgnoredOnReceipt` and `TestRFC5036InitOnDemandProposalKeepsDownstreamUnsolicited` pin both bits |
| 2026-09-20 | - (walked into while giving RFC 8669 BGP Prefix-SID a parser, for the ExaBGP compatibility suite) | `Flags()` on every known optional-transitive attribute in `internal/core/bgp/attribute`, re-encoded through `AttributesSizeWithContext` and `WriteAttrToWithContext` (`internal/component/bgp/rib/commit.go`) | RFC 4271 Section 5 states it as a MUST NOT: "If a path with a recognized, transitive optional attribute is accepted and passed along to other BGP peers and the Partial bit in the Attribute Flags octet is set to 1 by some previous AS, it MUST NOT be set back to 0 by the current AS." Ze sets it back to 0. Each known attribute's `Flags()` answers a constant derived from its type, so a re-encode writes `0xC0` whatever arrived, and the Partial bit an upstream AS set because IT did not recognize the attribute is erased at Ze. The bit records a fact about the PATH, not about the attribute, so nothing in the type can reproduce it. It binds AGGREGATOR (7), COMMUNITIES (8), EXT_COMMUNITIES (16), TUNNEL_ENCAP (23), IPV6_EXT_COMMUNITIES (25), LARGE_COMMUNITIES (32) and now PREFIX_SID (40). `OpaqueAttribute`, which every UNRECOGNIZED attribute still decodes to, keeps the received octet, which is why the defect is invisible until a code gains a parser: attribute 40 had the correct behavior this morning and lost it when it stopped being unknown | not fixed, and not folded into the work in hand: it is class-wide, it is reachable through the RIB commit path rather than through anything the ExaBGP suite exercises, and `ai/rules/rule-precedence.md` keeps an unrelated repair out of a closing change. The repair is one field: a parsed attribute carries the flag octet it was built from, and `Flags()` answers that octet with the type's value as the default for a locally originated attribute. `parseSpan` already has the received octet in hand when it builds the value. Proving it wants a test per attribute that receives Partial set and asserts it survives the re-encode, in both polarities |
| 2026-09-20 | - (walked into while fixing the Partial bit on the relay rail) | `writeRelayPayload` (`internal/component/bgp/reactor/relay_payload.go`), reached from `(*reactorAPIAdapter).RelayStoredRoute` (`reactor_api_relay.go`), and the live forward rail that sends the received buffer unchanged | The mirror of the row above, on the same octet. RFC 4271 Section 4.3 states it as a MUST: "For well-known attributes and for optional non-transitive attributes, the Partial bit MUST be set to 0." Nothing on receive cleared a Partial bit a peer set on a well-known or optional non-transitive attribute, because RFC 7606 Section 3(c) narrows the receive-side flags check to "the value of either the Optional or Transitive bits", so an ORIGIN arriving as 0x60 is neither malformed nor withdrawn. The zero-copy relay then put that octet back on the wire: it copies each surviving attribute with `copy(buf[off:], attrs[s.start:s.end])`, so an ORIGIN reached a route-server client as 0x60 and a MULTI_EXIT_DISC as 0xA0, measured at the destination peer | fixed: `attribute.ClearPartialOnWellKnownAndNonTransitive` is the exact complement of `SetPartialOnUnrecognizedTransitive`, and `(*Session).publishBase` calls it, which is the one place that owns these bytes for the RIB, the relay and every rebuild. `TestRFC4271PartialClearedOnTheRelayedWire` drives the relay from a received UPDATE to the destination's wire and carries the RFC4271-4.3-2 discrimination record |
| 2026-09-20 | - (walked into while fixing the row above) | `validateAttributeFlags` and the `wellKnownAttrs` map (`internal/component/bgp/message/rfc7606.go`) | RFC 7606 Section 3(c) binds every attribute: "If the value of either the Optional or Transitive bits in the Attribute Flags is in conflict with their specified values, then the attribute MUST be treated as malformed and the 'treat-as-withdraw' approach used." Ze checks five well-known codes, the two MP attributes and AIGP, and then returns nil under the comment "Optional attribute - flags not restricted". So a MULTI_EXIT_DISC, an ORIGINATOR_ID or a CLUSTER_LIST arriving with the Transitive bit set is accepted, and ze relays a non-conformant flags octet the row above cannot repair: the clear reads the class out of the octet, which is right for every attribute whose Optional and Transitive bits ze has validated and wrong for one it has not. The specified flags per type code are declared nowhere in the tree, and `wellKnownAttrs` is a hand-written copy of five of them | not fixed. Two ways in, and they need the same missing fact: declare the specified flags on `attribute.AttributeCode` and have this check read them, which closes RFC7606-3-c and makes `wellKnownAttrs` derived, or have the clear walk alone consult them, which repairs the octet ze emits while still accepting a malformed attribute the RFC says to withdraw. The first is the one the RFC asks for |
