| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-31 | yang-short-and-long-command-help | `./le docvalid help-shape` (`internal/le/docvalid/helpshape.go`) against the offline local command registry | The gate holds 601 command-tree nodes and 211 RPCs to the AC-3 summary shape and answers "Every command node and every RPC declares a summary of one short sentence." The catalog it is meant to govern carries a THIRD corpus: the offline local commands, which are not YANG nodes and which the gate never walks. `generate wireguard keypair` publishes `"Generate a WireGuard keypair. Prints private and public keys to stdout for use in your config."` -- two sentences, 16 words past the cap -- through `ze help command --json` and through every one-line surface that reads the catalog, and no gate refuses it | fixed. The gate walks a third surface, `local`, and reports its coverage beside the command and RPC counts. The population is the one `collectCommands` publishes: `registry.ListLocal` minus the paths the command tree holds, plus the four registrations `cmd/ze` declares in `package main`, which are read from SOURCE because Go forbids importing a main package. Measured 19 offline local commands, and `ze help command --json` from a binary carrying every feature tag holds the same 19 with no wire method. Eleven broke a rule and all eleven are rewritten, eight of them with a `LongHelp` beside the summary. A registration whose path is not a literal STOPS the gate rather than being skipped, and an empty population is an error rather than a report of full coverage. The class is NOT closed: `registry.ListRoot` carries 21 root-command summaries that `ze --help` prints and no gate reads |
| 2026-08-31 | restore-bespoke-interop-assertions | `TestCheckerPopulationMatchesProducer` (`internal/le/interoplab/bgp/bgp_test.go`) against the fifteen bespoke checkers in `check_rfc.go` | The gate exists to refuse a bespoke checker that also holds a generic fallback, and it reads MEMBERSHIP of `scenarioOperations`. Thirteen of the fifteen held the fallback INSIDE the checker body, as a one-line call to `checkScenario` (`check_engine.go`), which answers `scenario %s has no typed assertions` for exactly the names a bespoke checker owns. Two more carried a complete body behind that same call used as a guard, so every assertion under it was unreachable. The population the gate reads is the table; the population it needs is the body, and the two differ by the one construct the gate was written to catch. Roughly twenty `RFC requirement:` tags sat above those bodies, and `rfc/requirements/rfc2545.md`, `rfc/enrolled.txt` and `docs/features/rfc-status.md` each cited them as interop evidence, so a citation named a function that had never executed a line | fixed. All fifteen bodies are written, `grep -n 'checkScenario(ctx, check, "' check_rfc.go` returns nothing, and each decision is a pure predicate in `check_rfc_predicate.go` with a both-polarity subtest in `TestBespokeCheckerBranches`. The gate itself is UNCHANGED and still reads the table, so the class is open: no check compares an `RFC requirement:` tag with the assertions of the body under it. A second instance of the same shape was found the same night in `applySenderConfig` (`internal/component/bgp/plugins/bmp/bmp.go`), published as meeting RFC 8671 Section 7.2 while unreachable on the reload rail. The next step is a survey of how many tags promise more than their bodies assert, over a denominator nobody has counted |
| 2026-08-30 | rfcgate-6-supported-extraction-signoff | ze's EAP implementation and the rfc3748 requirement population: `PeerSession.handleRequest` (`internal/component/ike/eap/peer.go`), `NewSession` (`internal/component/ike/eap/eap.go`) and the checklist in `rfc/short/rfc3748.md` | RFC 3748 Section 5, at `rfc/full/rfc3748.txt`, states "All EAP implementations MUST support Types 1-4, which are defined in this document, and SHOULD support Type 254." The sentence carries no antecedent and ze has an EAP implementation. Ze supports Type 1 (Identity) and none of Type 2 (Notification), Type 3 (Nak) or Type 4 (MD5-Challenge). The obligation sat outside every checked population twice over. `rfc/short/rfc3748.md` declared no requirement row for it, so `./le rfc check` was green over an unimplemented MUST for as long as the summary existed, and the 2026-08-30 extraction walk then excluded site `5:2` on the ground that it binds a general-purpose EAP implementation, which is circular: the scoped profile that exclusion names IS the shortfall the sentence forbids. Section 5.4 closes the only escape the RFC offers: "However, if the EAP authenticator can be configured to authenticate peers locally (e.g., not operate in pass-through), then the requirement for support of the MD5-Challenge mechanism applies." Ze's authenticator authenticates locally, so the RFC names ze's case and applies the requirement to it | Types 2 and 3 are implemented by `plan/spec-eap-notification-and-nak.md`, which lands their requirement rows with both test polarities. Type 4 (MD5-Challenge) is a DEVIATION Thomas authorized on 2026-08-30, recorded here under `ai/rules/rfc-compliance.md`. His rationale is RFC 7296 Section 2.16, at `rfc/full/rfc7296.txt`: "EAP methods that do not establish a shared key SHOULD NOT be used, as they are subject to a number of man-in-the-middle attacks", and ze speaks EAP only inside IKEv2. Read the scope of that sentence exactly: it governs USING the method, and a SHOULD NOT about use does not discharge a MUST about support, which is why this needed an authorization rather than a reading. Two consequences stay open and are named in that spec. `rfc/extraction/README.md` publishes a CLOSED set of six exclusion kinds and none of them means an owner-authorized deviation, so sites `5:2` and `5.4:2` have no honest disposition and the rfc3748 sign-off is blocked on that owner decision. And `docs/features/rfc-status.md` still claims no tracked gap for RFC 3748, which this deviation makes false. SUPERSEDED 2026-09-01: Thomas WITHDREW the deviation and ordered Type 4 implemented, so every sentence above that treats it as standing is void (`ai/rules/rfc-compliance.md`). Type 4 is owed and its implementation is in progress. The two open consequences fall with the deviation. Sites `5:2` and `5.4:2` need no exclusion kind for an owner-authorized deviation. RFC 3748 carries a Type-4 gap until the code lands |
| 2026-08-30 | rfcgate-6-supported-extraction-signoff | `checkSuperseded` (`internal/le/rfc/check_core.go`), whose population is every summary whose Meta row names a successor, read through `parseSuccessorStem` (`internal/le/rfc/ledger.go`) | `rfc/short/rfc8203.md` declares an `Obsoleted-by` Meta cell of `-` while `rfc/short/rfc9003.md` in the same directory declares an `Obsoletes` Meta cell of `RFC 8203`, and `rfc/full/rfc9003.txt` carries `Obsoletes: 8203` in its own header. `parseSuccessorStem` finds no successor in the rfc8203 row, so the stem leaves the population and none of its nine requirement lines is asked for a `{superseded: ...}` disposition. The gate reads as live over the corpus while it is inert for this stem. The two summaries contradict each other and nothing compares them: the check derives the population from the predecessor's own row alone, so a missing back-link is invisible even when the successor's row states the link. Found while walking rfc8203 for its extraction sign-off, where RFC 9003's raised 128-to-255 length bound is what decides site `2:2` | not fixed. Two halves. The DATA half is a one-cell correction to `rfc/short/rfc8203.md`, but it then demands nine `{superseded: ...}` dispositions against `rfc/short/rfc9003.md`, which is a queued stem of the spec above, so it is not this walk's to write. The GATE half is the durable one: derive the population from BOTH directions, so a summary naming `Obsoletes <stem>` puts `<stem>` into the population whether or not `<stem>` names it back. `rfc/short/rfc5549.md`, `rfc5575.md`, `rfc6810.md` and `rfc7752.md` show the forward link is normally present, which is why the one absence went unnoticed |
| 2026-08-30 | rfcgate-6-supported-extraction-signoff | `parseStatusLedger` (`internal/le/rfc/ledger.go`), and every check that reads the map it returns | `docs/features/rfc-status.md` carries TWO rows for RFC 2759: the first says `Supported within PPP and IPsec EAP` and the second says `Partial`. The parser keys a row by stem and overwrites a repeat with no complaint, so the map holds the second row alone and the support claim in the first is invisible to `checkUnprovenSupport`, `checkStatusAgreement`, `checkGapCountAgreement` and `checkStatusCompleteness`. The page carries 153 RFC-keyed rows and 152 distinct keys, so RFC 2759 is the only stem in this state today. Found while re-deriving the support-claiming scope for the spec above: the spec counts rfc2759 in Class A tier 1 on the strength of the first row, and the gate reads `Partial` | not fixed. Which of the two rows states Ze's real position is a conformance judgement reserved to the owner (`ai/rules/rfc-compliance.md`), so the page is untouched. The parser half is fixable on its own: refuse a duplicate key instead of overwriting it, which would have surfaced the contradiction on the day the second row landed |
| 2026-08-30 | - | `bashGovernedWrite` (`internal/le/hookruntime/bash.go`), the guard that keeps a shell write out of plan/ and ai/rules/ | The guard reads the command's literal path text, so it never sees a write whose path is relative to a changed working directory. `cd ai/rules/points/cli/cli-grammar-keywords-before-values && cat > never-expose-selector-as-an-operator-keyword.md <<EOF` wrote a governed rule point and was waved through; the same payload spelled with the full path from the repository root was refused on the very next call. The redirect branch works, so the guard reads as live while every `cd`-then-write payload passes it. Found by walking into it: three heredoc batches rewrote 29 rule points under `ai/rules/points/cli/` before a fourth batch that named `ai/rules/points/testing/...` in full was blocked | not fixed. The evaded checks are `writePointOverwrite` and `writePointLanguage`, so a point written this way skips both; here `./le rules lint` caught nothing because the content was correct. A fix has to resolve the effective cwd across `cd`, `pushd` and a `(subshell)` before matching, or refuse a governed-tree `cd` outright |
| 2026-08-28 | - | a commit's file selection, and the sweep that produced it | HEAD was broken twice by one rule that looks sound and is not: the commit took the files the change had TOUCHED. A Go package is the compilation unit, not the file, so a change that edits three of a package's four files and commits those three leaves every declaration the fourth file owns undefined in the resulting tree. The working tree compiled throughout, because it still held the file that was left out; only the commit did not. The same shape appeared one level up in the sweep itself, whose detector read only the language it was renaming, so it could not see that documents, tests and stylesheets also NAME the thing being renamed and went stale with it | fixed both times by adding the omitted declarations. General practice: a commit's file set MUST be closed under "declares what the included files reference", which in practice means committing whole packages whenever a change crosses one, and judging it by compiling the COMMIT rather than the working tree, since the tree still holds what the commit omits. And a rename's blast radius is every file that NAMES the symbol, not every file that compiles against it, so a detector scoped to the source language under-reports by construction |
| 2026-08-28 | - | `bashGovernedWrite` (`internal/le/hookruntime/bash.go`), the guard that keeps a shell write out of plan/ and ai/rules/ | The in-place editor branch matched a STANDALONE `-i` preceded by an argument, so it saw almost no real invocation. `sed -i 's/a/b/' plan/x` was missed because the flag is the FIRST argument and the pattern demanded one before it, and every clustered spelling was missed because the flag is not standalone: `perl -0pi`, `perl -pi`, `sed -Ei`, `sed -i.bak`, `sed --in-place`. The redirect, tee and copy branches all worked, so the guard read as live while its widest branch was inert. Found by walking into it: a sweep of 229 files under plan/ ran as `perl -0pi` and was waved through, and only a later append with `cat >>` was refused | fixed: the argument prefix is optional and the flag is matched inside a cluster, `ruby` joins the interpreter list, and `TestGovernedWriteCatchesEveryInPlaceFlagSpelling` drives seven spellings through the hook entry point. All seven were RED against the old pattern, the bare `sed -i` included. `TestGovernedWriteLeavesReadsAlone` pins the negative half so a read is never claimed as a write |
| 2026-08-26 | le-is-a-ze-binary | the retired `ze-qemu-test-all` (current: `./le qemu run command "./le qemu all-tests"`) and the retired `ze-qemu-needs-linux-test` (current: `./le qemu run command "./le qemu all-tests"`), whose in-VM population is the 25 `fsuite` lines of the retired `scripts/evidence/qemu-all-tests.sh` (current producer: `internal/le/qemu/alltests.go`) | The repository declares 29 functional suites (`Suites`, `functional/suites.go` in the former top-level tool tree), but the script names 25. `runner`, `flow-export`, `vpp`, and `web` do not appear in the script. Thus, their tests execute in no VM phase. `runner` is one of the 24 gating suites. Four script comments were intended to prevent this defect. The `static`, `ipsec`, `ospf`, and `vrrp` lines each say that their tests otherwise "would execute NOWHERE". The list is hand-written, and no check compares it with the declared population. A comparison of the `fsuite` names with the suite table measured the difference | fixed in the port only. The script keeps the defect until the swap points the recipe at the port in step 14. `AllTests.verifySuiteCoverage` (`qemu/alltests.go` in the former top-level tool tree) refuses to start an incomplete run. Each declared suite must be in `vmSuites` or in `excludedSuites` with a reason. `vmSuites` now contains all 29 suites. `TestEveryDeclaredSuiteIsEitherRunOrExcluded` provides the fast form of the same guard. The result is not yet observed as green. `runner`, `flow-export`, and `vpp` have never run in the VM. The session that lands step 14 owns their first run |
| 2026-08-26 | le-is-a-ze-binary | the retired `scripts/dev/changed-groups.sh` (current producer: `internal/le/`), whose group table maps a changed path to a test group | The table declares `internal/component/l2tp/ppp/` as a `ppp` group. The preceding comment says, "Order matters: first match wins (most specific first)". But the loop iterates `"${!PREFIX_GROUP[@]}"` in bash hash order. That order puts `internal/component/l2tp/` first, so the `ppp` row is unreachable. The comment describes an order that the interpreter does not provide. The printed iteration order and a fixture with one changed PPP file measured the defect. The fixture answered `l2tp`. If reachable, the row would narrow a PPP change from all L2TP tests to only PPP tests. No Make target has ever named a `ppp` group | fixed in the port only by deleting the row instead of by reordering it. A prefix inside another prefix can only narrow the tests that the parent pattern already runs. `TestNoGroupPrefixSitsInsideAnother` (`changed/changed_test.go` in the former top-level tool tree) refuses a table with this shape. Both implementations answer `l2tp` for a PPP file, so the parity proof is unchanged |
| 2026-08-26 | - | `./le doc wiring`, whose ci-sleep ratchet counts every `time.sleep(` in every `.ci` under `test/` against a committed ceiling | The port of this gate to `le` found the defect. This case is worse than the walk-root case below because the gate invites permanent damage. `check_ci_sleep_ratchet` (the retired `scripts/dev/verify_wiring_docs.py` (current producer: `internal/le/doc/wiring/wiring.go`)) uses `except OSError: continue` for an unreadable `.ci` file. Thus, that file contributes no sleep, and a lower count looks like a pass. Below the ceiling, the gate says that the count dropped and requests a negative delta line. That delta permanently lowers the ceiling to a count from an unreadable tree. A fixture contained two `.ci` files with one sleep each. One file had mode 000, and the ceiling was 2. The result was `ci-sleep ratchet: count dropped to 1 (ceiling 2); append a -1 delta line` and exit 0. Two sibling checks have the same defect. `check_ci_sleep_justification` and `check_ci_log_subsystem_keys` each skip an unreadable `.ci` file. Thus, they miss a violation in that file. `read_current_or_empty` is a third instance. It answers `""` for a file that it cannot decode, so no changed-file predicate selects a gate | fixed in the port because the port replaces the script. Every reader in `docwiring/checks.go` in the former top-level tool tree answers through `gate.readFailure`. That function declares the failed group and exits 2. `readCurrentOrEmpty` (`docwiring/wiring.go` in the former top-level tool tree) distinguishes an absent file from an unreadable file. An absent file remains `""`. An unreadable file reaches `SelectedTargets` as an error. `TestTheRatchetRefusesATreeItCannotRead` and `TestTheRatchetCountsExactlyAtItsCeiling` become red if either guard is removed. `TestSleepRatchetScriptStillPassesOverATreeItCannotRead` (the retired `scripts/dev/discovery_parity_test.go` (current producer: `internal/le/`)) verifies the script's dropped count. Thus, that test becomes red if somebody fixes the script. The swap step deletes the script. Until then, the running gate keeps the defect, so the assertion points toward the fix |
| 2026-08-26 | - | `./le functional`, whose population is the 24 suites the gating run names | The port of this area to `le` found the defect. The module's docstring says that the module prevents this exact failure. `run_gating` (the retired `scripts/le/application/functional.py` (current producer: `internal/le/`)) resolves its run list with a comprehension. The comprehension filters out each name that `suite_named` cannot resolve. Thus, an unknown suite leaves both the run and the progress denominator instead of causing a refusal. The report says that every suite passed although one suite never started. This defect put `ipsec` in the retired Makefile (current producers: `internal/le/` native action tables) run list with no recipe. It counted toward the denominator, ran nothing, and still gave every `test/ipsec/*.ci` a merge-gate tier. A fixture used `encode` and `no-such-suite`, with stubbed build and execution seams. The script printed `PASS  all 1 suites` and exited 0 | fixed in the port because the port replaces the script. `GatingSuites` (`functional/suites.go` in the former top-level tool tree) answers `ErrNoSuchSuite` for a name absent from the suite table. `RunGating` refuses the run instead of reducing it. Suite names are constants, so a typo in either list is also a compile error. `TestAnUnknownGatingNameIsRefused` becomes red if the refusal is removed. `TestGatingRunStillDropsAnUnknownSuiteInTheScript` (the retired `scripts/le/functional_parity_test.go` (current producer: `internal/le/`)) verifies that the script still fails open. Thus, that test becomes red if somebody repairs the script. The swap step of the le migration deletes the script |
| 2026-08-26 | - | `./le ci-dispatch check`, whose population is every command string a test, a script or a Go call site sends to the daemon | Found while porting this gate to `le`, and it is the walk-root-skipped shape an eighth time. `ci_dispatch_commands.go` walks test, internal, cmd, pkg, scripts and demos relative to the working directory, and `scanAll` answers every walk error with `return nil` and skips a file it cannot read the same way. Its resolver comes from the LINKED registry rather than from the tree, so the one guard it does carry, refusing an empty command set, is non-empty whatever the tree holds and never fires. Demonstrated on the built script run in an empty directory: `Emitters checked: 0`, `ci-dispatch-check: OK`, exit 0 | fixed in the port rather than in the script, because the port replaces it: every walk and read error in `cidispatch` in the former top-level tool tree reaches the caller, and the emitter count is checked against `cidispatch.emitterFloor` so a tree that was never read is refused rather than passed. `TestATreeWithNoEmitterIsAnError` reddens with the floor removed, and `TestCIDispatchScriptStillPassesOverATreeItNeverRead` (the retired `scripts/checks/parity_test.go` (current producer: `internal/le/repository/`)) asserts the SCRIPT still answers OK, so it reddens the day somebody fixes the script and this row can be closed. The script is deleted at the swap step of the le migration |
| 2026-08-26 | - | the retired `ze-gokrazy-gosum-check` (current: `./le gokrazy-gosum`), whose population is every tracked `gokrazy/ze/builddir/**/go.sum` compared against the root `go.sum` | Found while porting this gate to `le`, and it is the walk-relative-to-the-working-directory shape again. `gokrazy_gosum_check.py` asks `git -C . ls-files` and opens `go.sum` by that bare name, so both are relative to wherever it was typed. From a subdirectory git answers only that subdirectory's paths, none of which start with `gokrazy/ze/builddir/`, and the gate then prints that it has nothing to check and exits 0 over a tree it never read. Demonstrated from the retired `scripts/` (current producer: `internal/le/`): `gokrazy-gosum: no tracked builddir go.sum files, nothing to check`, exit 0, against 7 files and 89 shared entries from the root. The script's own comment defends the zero-file case as honest, which it is; what it does not defend is the case where the zero comes from the caller's working directory | fixed in the port rather than in the script, because the port replaces it: `gokrazygosum` in the former top-level tool tree takes its tree from `lepath.Root()` and joins every path onto it, so the same run judges the checkout from any directory. `TestGokrazyGosumScriptStillPassesFromASubdirectory` (the retired `scripts/dev/parity_python_test.go` (current producer: `internal/le/`)) asserts the SCRIPT still fails open and the command does not, so it reddens the day somebody fixes the script and this row can be closed. The script is deleted at the swap step of the le migration |
| 2026-08-26 | - | `./le plugin boundary check`, whose population is every plugin package under the composition-root generator's search roots | Found while porting this gate to `le`, and it is the walk-root-skipped shape a seventh time. `plugin_process_boundary.go` skips any scan root whose `os.Stat` errors OR whose result is not a directory, in one `continue`, and those thirteen roots are its whole population. One skip carries two different facts: a root the generator declares for a domain whose plugins have not landed, and a root that should be there and is not. Demonstrated on the built script over a tree holding only a copy of the retired `scripts/codegen/plugin_imports.go` (current producer: `internal/le/plugin/imports/pluginimports.go`): `plugin-process-boundary: OK`, exit 0, no file read at all | fixed in the port rather than in the script, because the port replaces it: `pluginboundary` in the former top-level tool tree skips ONLY a root that does not exist, which is the generator's own contract for the same list, answers every other stat failure, and counts the files it read against `pluginboundary.scanFloor` so a tree that was never read is refused rather than passed. `TestATreeWithNoPluginRootIsAnError` and `TestAFileThatWillNotParseStopsTheRun` redden with either guard removed, and `TestBoundaryScriptStillPassesOverATreeItNeverRead` (the retired `scripts/checks/parity_test.go` (current producer: `internal/le/repository/`)) asserts the SCRIPT still answers OK, so it reddens the day somebody fixes the script and this row can be closed. The script is deleted at the swap step of the le migration |
| 2026-08-26 | - | `./le cli-grammar`, whose population is every .yang file under internal, every root registration under cmd/ze and internal, and every checked-in demo script | Found while porting this gate to `le`, and it is the same shape as the rows below in a sixth tool. `cli_grammar.go` walks `internal`, `cmd/ze` and `demos/terminal` relative to the working directory and discards every error it makes: the three `filepath.Walk` calls are each `_ =`, `os.Open` failure is a `return nil` or a `continue`, `sc.Err()` is dropped with the comment that a read error just yields fewer hits, and `forEachRegistryRootName` returns silently when `parser.ParseFile` fails. Demonstrated on the built script run in an empty directory: `Roots checked: 0`, no .yang file read, no demo script opened, `cli-grammar: OK`, exit 0 with three of its five feeders silent. Note the same function exits 2 for a scanner error saying an unread script is an unchecked call site, then skips a file it could not open | fixed in the port rather than in the script, because the port replaces it: every reader in `cligrammar` in the former top-level tool tree answers an error, `Check(tree, floor)` returns it, and each of the three populations is counted against `cligrammar.DefaultFloor` so a tree that was never read is refused rather than passed. `TestATreeTooSmallToBeTheOneAskedAboutIsAnError` and `TestAFileThatWillNotParseStopsTheRun` redden with either guard removed, and `TestGrammarScriptStillPassesOverATreeItNeverRead` (the retired `scripts/checks/parity_test.go` (current producer: `internal/le/repository/`)) asserts the SCRIPT still answers OK, so it reddens the day somebody fixes the script and this row can be closed. The script is deleted at the swap step of the le migration |
| 2026-08-26 | - | `./le command ownership`, whose population is every non-test Go file under cmd/ze and internal | Found while porting this gate to `le`, and it is the same shape as the four rows below in a fourth and fifth tool. `command_ownership.go` discards EVERY read error it makes. `forEachRegistryCall` and `fileImports` return silently when `parser.ParseFile` fails, the three `filepath.Walk` calls are each `_ =`, `os.ReadDir` failure is a `continue`, and `hasVariantBuildTag` answers false for a file it cannot open. A cmd/ze file with a syntax error therefore registers no root at all, so the gate reports `command-ownership: OK` over it and exits 0. Demonstrated on the built script over a two-file tree whose `cmd/ze/bad.go` <!-- doc-links: ignore (illustrative path, never a real file) --> declares an unallowlisted `registry.RegisterRoot("mystery"` with the closing paren missing: OK, exit 0. The same script run in ANY directory that is not a checkout answers OK too, because both walk roots are relative to the working directory and a missing root is skipped | fixed in the port rather than in the script, because the port replaces it: every reader in `commandownership` in the former top-level tool tree answers an error, `Check(tree, floor)` returns it, and the population is counted so a tree holding neither root is refused rather than passed. `TestAFileThatWillNotParseStopsTheRun` and `TestATreeTooSmallToBeTheOneAskedAboutIsAnError` redden with either guard removed, and `TestOwnershipScriptStillPassesOverWhatItCannotRead` (the retired `scripts/checks/parity_test.go` (current producer: `internal/le/repository/`)) asserts the SCRIPT still answers OK for both, so it reddens the day somebody fixes the script and this row can be closed. The script is deleted at the swap step of the le migration |
| 2026-08-26 | - | the retired `ze-iface-resolution-check` (current: `./le iface-resolution`, which takes no verb), whose population is every non-test Go file under cmd, internal and pkg | Found while porting this gate to `le`. `iface_resolution.go` skips a walk root it cannot stat, and those three roots are its whole population, so the gate run anywhere but a Ze checkout prints `iface-resolution: OK` and exits 0 having opened no file. Demonstrated on the built script inside a bare temporary directory. The Make target stands in the root so the running gate does not trip it today; the port cannot inherit that, because `lepath.Root` accepts `ZE_REPO_ROOT` and a developer types the command from anywhere | fixed in the port rather than in the script: `Check(tree, floor)` (`ifaceresolution/ifaceresolution.go` in the former top-level tool tree) counts the files it scanned and refuses a population below the floor, naming the count. The floor is a PARAMETER because a fixture is not a checkout, which is the shape `tracked_build.go` already uses for its own package floor. `TestATreeTooSmallToBeTheOneAskedAboutIsAnError` reddens with the guard removed, and `TestScriptStillPassesOverATreeItNeverRead` (the retired `scripts/checks/parity_test.go` (current producer: `internal/le/repository/`)) asserts the SCRIPT still answers OK, so it reddens the day somebody fixes the script |
| 2026-08-26 | - | the retired `ze-spec-status` and the retired `ze-spec-status-json` (current: `./le spec status`, whose JSON is the `json` pipe operator on the same action), whose population is every spec file directly under the spec directory | Found while porting this tool to `le`, and it is the third tool in this migration to carry the same shape. `loadAllSpecs` (the retired `scripts/status/spec_status.go` (current producer: `internal/le/spec/status/specstatus.go`)) calls `filepath.Glob` on a pattern relative to the WORKING DIRECTORY, and `filepath.Glob` answers an empty list and NO error when the pattern's directory does not exist. So the tool run anywhere but the repository root prints `Specs: 0 total ()` with three empty buckets and exits 0, which is a complete inventory of a population it never read. Demonstrated on the built script over a bare temporary directory. The two Make targets stand in the root, so the running gate does not trip it today; the port cannot inherit that, because `lepath.Root` accepts `ZE_REPO_ROOT` and a developer types the command from anywhere | fixed in the port rather than in the script, because the port replaces it: `Collect` (`specstatus/specstatus.go` in the former top-level tool tree) stats the spec directory first and refuses a tree that holds none, naming the pattern it could not read. ONE case stays an ANSWER and is named: an EMPTY spec directory, because every spec can legitimately be closed, and refusing that would be a different defect. `TestCollectRefusesATreeWithNoPlanDirectory`, `TestCollectRefusesAPlanThatIsNotADirectory`, `TestCollectAcceptsAnEmptyPlanDirectory` and `TestAnswerRefusesATreeWithNoPopulation` all redden with the guard removed, and `TestScriptStillFailsOpenOnATreeWithNoPopulation` (the retired `scripts/status/parity_test.go` (current producer: `internal/le/verify/engine/`)) asserts that the SCRIPT still exits 0 with a zero inventory, so it starts failing the day somebody fixes the script and this row can be closed. The script is deleted at the swap step of the le migration; until then the running gate carries the defect, which is why the assertion is written in the direction of the fix rather than as a comparison |
| 2026-08-26 | - | `./le docvalid doc-drift`, whose population is every `.ci` file under `test/` and every `_test.go` file under the tree | Found while porting this gate to `le`. The third tool in this migration with the same defect, and here HALF the guard was already present: `noteUnreadable` (the retired `scripts/docvalid/doc_drift.go` (current producer: `internal/le/docvalid/drift.go`)) records a scan that STOPS early, under a comment saying a short count agrees with a document that understates the tree, while `countMatchingLines` in the same file answers 0 with no finding when `os.Open` FAILS. A dangling symbolic link, a file closed to this user, and a mode changed under the walk all reach the second path and none of them is reported. The three `filepath.WalkDir` callbacks swallow their `err` the same way, so a directory the walk cannot enter contributes nothing and says nothing. Measured with a fixture holding one readable `func Test` and one dangling `gone_test.go`: the script answers `No documentation drift detected.` and exits 0 over a README claiming a count it could only reach by not reading the link | fixed in the PORT: `checker.countMatchingLines` and `checker.readLines` (`docvalid/scan.go` in the former top-level tool tree) report a file that is present and unopenable through `noteUnopenable`, and `walkFailure` reports a directory it cannot enter and keeps walking. `vanished` tells a file that DISAPPEARED under the walk from one that is still there and unreadable, which a shared checkout needs (the precedent is `inventory` in the former top-level tool tree). The script keeps the defect until step 14: `TestPortReportsTheFileTheScriptCountsBlind` (the retired `scripts/docvalid/parity_test.go` (current producer: `internal/le/docvalid/`)) asserts the SCRIPT still passes that fixture, so it reddens if somebody fixes the script, and the answer then is to delete it with the script |
| 2026-08-26 | - | `./le inventory`, in its default rendering and under the `json` pipe operator, whose population is every `.yang`, `.ci` and `.go` file under the tree | Found while porting this gate to `le`, and it is the `ze-consistency-check` row below repeated in a second tool. Five walks in the retired `scripts/inventory/inventory.go` (current producer: `internal/le/inventory/inventory.go`) -- `discoverYANGPaths`, `extractRPCs`, `loadAllCIContent`, `countCITests`, `countGoStats` -- each open with `if err != nil { return nil }` and each discard the `os.Open` or `os.ReadFile` error for a file the walk listed. EVERY number the page publishes is a count of what the walk saw, and the file's own header claims the output is always accurate, so a file the walk cannot read lowers a published count with nothing said. Demonstrated on the built script over a fixture tree carrying one dangling symbolic link under `internal/`: it publishes a Go-files count of 5 beside a Go-lines count of 8, which is five files holding the lines of four, and exits 0. The same file's `scanAll` already stopped the run on a scanner error, added by an earlier session for exactly this reason, so the walks were the readers left failing open | fixed in the port rather than in the script, because the port replaces it: `walkFiles` and `scanLines` (`inventory/inventory.go` in the former top-level tool tree) return the error, and `Collect` answers `(Inventory, error)`. TWO cases stay benign and each is named. The walk ROOT not existing means the tree has no such area, which is a fact about the tree rather than a scan that fell short. A file that VANISHED between the walk listing it and the tool reaching it was never part of the tree either, and `vanished()` tells it from a file that is still listed and cannot be read by asking `os.Lstat`: a dangling symbolic link fails to open with the same `fs.ErrNotExist` and its directory entry is still there. That second case is not a nicety -- this checkout is shared, and the first run of `TestAnswerAnswersTheInventory` over the real tree was refused by another session's temporary file under `tmp/` disappearing mid-walk. `TestCollectFailsOnAFileItCannotRead` (three cases: `.go`, `.yang`, `.ci`), `TestCollectFailsOnALineTooLongToScan` and `TestCollectTreatsAMissingAreaAsEmpty` each redden with `return nil` restored, and `TestPortReportsWhatTheScriptSkips` (the retired `scripts/inventory/parity_test.go` (current producer: `internal/le/inventory/`)) asserts that the SCRIPT still publishes the short count, so it starts failing the day somebody fixes the script and this row can be closed. The script is deleted at the swap step of the le migration; until then the running gate carries the defect |
| 2026-08-26 | - | the retired `ze-consistency-check` (current: `./le consistency`), whose population is every `.go` file under the tree it is pointed at | Found while porting this gate to `le`. `walkGoFiles` (the retired `scripts/lint/consistency.go` (current producer: `internal/le/consistency/consistency.go`)) opened its walk function with `if err != nil { return nil }`, so every error `filepath.Walk` reports is discarded. A directory the walk cannot read contributes no file, each of the seven checks over it then finds nothing, and an empty report is what a CLEAN tree produces. Demonstrated on the built script: pointed at a path that does not exist it prints `✓ All consistency checks passed` and exits 0. The population is the claim, and the tool asserted nothing about it. The same file's `scanLines` and `countLines` already reported a file they could not read in full, added by an earlier session for exactly this reason, so the walk was the one reader left failing open | fixed in the port rather than in the script, because the port replaces it: `walkGoFiles` in `consistency/consistency.go` in the former top-level tool tree reports the walk error as an `unreadable` ERROR finding and CONTINUES the walk, so one closed directory costs its own subtree and nothing else. `TestCheckReportsUnwalkableTree` and `TestCheckKeepsWalkingPastAnUnreadableDirectory` both redden with `return nil` restored, and `TestPortReportsWhatTheScriptFailsOpenOn` (the retired `scripts/lint/parity_test.go` (current producer: `internal/le/consistency/`)) asserts that the script still fails open, so it starts failing the day somebody fixes the script and this row can be closed. The script is deleted at step 14 of that spec; until then the running gate carries the defect, which is why the assertion is written in the direction of the fix rather than as a comparison |
| 2026-08-25 | fixit-lint-blind-to-every-other-build-tag | `./le verify lint run`, whose population is every tracked Go file and whose two passes read ONE build each | golangci-lint analyzes one GOOS, one GOARCH and one tag set for each run, and it exits 0 over a file it never loaded. Two passes ran, so 245 tracked own-source Go files were reported on by nothing on a Linux host and the gate read clean over all of them (144 on darwin: the number MOVES with the machine, because pass 1 takes the host GOOS, which is the shape of the defect in miniature). Four independent causes put a file there. The package set named four roots while the tree had more, so 55 files including the repository's own gates under the retired `scripts/` (current producer: `internal/le/`) were never pointed at. `.golangci.yml` turns every feature gate ON, so every file whose build line carries `!ze_<feature>` -- the code an operator reaches when a feature is OFF -- was excluded by construction. 41 tracked files carry such a line. 25 of them are in `cmd/ze/hub` alone. The `compile-out` row's own scope is narrower: the 33 of those files no base pass loads, derived on every run rather than written down. An earlier reading of this row said 47, which matches neither predicate. Personality and capability tags neither pass carried hid 42 more, `ze_installer` among them: the initrd's PID 1 is compiled, vetted and unit-tested, and `errcheck`, `noctx`, `staticcheck` and `gosec` had never seen a line of it. GOARCH and GOOS accounted for the rest. Three builds turned out not to COMPILE at all (`live`, `stress`, `ze_chaos`), which no gate could have said, because a lint pass cannot report a finding it cannot type-check | fixed, and the general repair is the ASSERTION rather than the extra pass. the retired `scripts/dev/lint_flavors.py` (current producer: `internal/le/verify/lint/matrix.go`) runs 17 flavor rows and then fails the run on any tracked Go file no pass loaded, deriving both the population and each row's scope from the tree on every run. Deriving is what makes drift impossible instead of detectable: a written list goes stale the moment a `//go:build debug` file lands in a new package, which is this defect class exactly. Two mechanisms were needed and neither was obvious. `--build-tags` only ADDS to the config list, measured, so the compile-out stubs needed a whole config: `tagless_config` derives a copy of `.golangci.yml` with an empty `build-tags` under `tmp/`, which makes the command line the entire tag set and lets ONE row drop all 36 gates. And a full pass costs 286s where a scoped one costs 8-39s, so each row lints only the packages the base passes do not reach. 245 blind files fell to 2, both named on every run with the change that would end their exclusion. The trap to carry forward: a gate's population is a CLAIM, and no gate checks its own -- the answer to "did you find anything" says nothing about "what did you look at", so a tool that takes a pattern owes an assertion that every member of the population was seen |
| 2026-08-24 | website-asciinema-terminal-demos | the demo publish tree `../gh-pages/assets/demos`, read as the population two readers judge | `verify_assets` (`demos/terminal/render.py`) checks the assets the artifact manifest NAMES, and `stage_terminal_media` (`website/tools/build-site.py`) stages the directory as it STANDS. Neither owns the difference, so an artifact a demo's kind stops producing is published for as long as the tree lives. Converting the 17 terminal demos from WebM to asciicast would therefore have left all 17 `.webm` and 17 `.png` in the published tree, and the acceptance criterion that counts them would have been false while every test was green. Found in the closure review, not by any gate | fixed at the writer: `remove_superseded_assets` (`demos/terminal/render.py`) runs under the demo lock after the manifest is written, and deletes only a file named `<this demo's id><a suffix some kind produces>` that this demo's own kind does not name. The candidate set is derived from `ASSET_EXTENSIONS`, so it can never reach the manifest, another demo, or a file the pipeline did not write. `test_a_render_removes_the_artifacts_its_kind_no_longer_produces` renders one demo of each kind in a single run and fails with the hunk reverted |
| 2026-08-23 | - | listener conflict detection, `config.CollectListeners` read as the population two conflict checks judge | A validator and the thing it validates read different sources. `ze config validate` (`internal/component/config/cli/cmd_validate.go` line 404) and the daemon's own startup check (`internal/component/bgp/config/loader_create.go` line 119) both feed `FindListenerConflict` from `config.CollectListeners`, which applies NO listener default. The defaults exist, and `RegisterBuiltinListenerDefaults` (`internal/component/config/listener_defaults.go`) is what fills them, but its only non-test caller is `registerListenerDefaultsOnce.Do` inside `collectSchemaListeners` (`internal/component/doctor/checks_listener.go` line 56). So the map is empty for every caller except `ze doctor`, and the conflict checker's population is the endpoints an operator SPELLED OUT. Every endpoint that comes from a default is structurally invisible to it: an empty server list, and an entry naming an ip with no port. Measured on 2026-08-23 with `environment.web` on an explicit 127.0.0.1:8080 and an enabled `environment.mcp` whose one entry names only an ip, so it takes the 8080 default: `ze config validate` answered configuration valid and exit 0. `ze doctor` does apply the defaults, but it probes each endpoint on its own and runs no conflict comparison, so neither command an operator runs to find a collision reports this one. The daemon starts one of the two and, for MCP, prints a warning that the bind failed and carries on (`startMCPServer`, `cmd/ze/hub/service_mcp.go`) | recorded, not fixed, on the coordinator's instruction. It is not specific to mcp: web, gnmi, looking-glass and both api-server endpoints carry registered defaults of the same shape, and applying the mcp port default added one more endpoint to a category the checker was already blind to. The narrow repair gives the two conflict callers the collector `ze doctor` uses. The question under it is why a registry the daemon's own startup check depends on is populated by a diagnostic command |
| 2026-08-23 | fixit-link-event-drop-loses-carrier-state | the iface resolver fan-out's subscriber set, read as the population a safety claim covered | the population is every consumer of `iface.Subscribe`, and the claim enumerated five of six. Three prose sites and two code comments said a full subscriber channel could safely DROP the newest event, because every consumer recomputes from a full listing on a timer: 30s for the IS-IS, OSPF and OSPFv3 rescans, 5s for the LDP discovery retry, every wake-up for VRRP. `grep -rn iface.Subscribe internal` returns six non-test callers. The missing one is `internal/plugins/iface/ra/sender_linux.go`, and it is the one the claim is false for. `Sender.onLinkEvent` sets `state.linkDown` on a down, and the send loop's timer branch returns WITHOUT rearming while that flag is set, so the next up event is the only thing that can restart router advertisements. Discard that one event and the interface stops advertising for the life of the process, with no timer anywhere to repair it. The claim was wrong about three of the five it did name as well: `RescanInterfaces` in the IS-IS, OSPF and OSPFv3 transports re-opens an enabled-but-closed circuit and reads no carrier at all, so it repairs a lost up and never a lost down | fixed at the PRODUCER rather than by correcting the enumeration. `sendLatest` (`internal/component/iface/resolve.go`) discards the OLDEST buffered event to make room for the newest, so the state an interface ENDED in reaches every subscriber, the six that exist and the seventh nobody has written yet. `TestResolverKeepsTheFinalStateWhenTheChannelFills` reddens with "the last event delivered is up, want down" against the bare non-blocking send restored, while `TestResolverDropIsCounted`, written by the same phase over the same code, stays GREEN against that mutant: a counter test cannot see WHICH event was discarded. The general practice is that a safety property stated over a hand-enumerated consumer set decays the day a consumer is added, so state it as a guarantee the producer holds and let every consumer inherit it |
| 2026-08-22 | record-answers-2-only-encoding | the spec's Files to Modify list, read as the population of a wire's speakers | the population is every file that WRITES or READS an answer line, and the list held the Go writers and readers its author had opened before the work began. Two speakers appear nowhere in it. `test/scripts/ze_api.py` (retired, no successor) <!-- doc-links: ignore (deleted 2026-08-28 by eae282592 with no replacement) --> is the harness reader that 580 `.ci` fixtures reach, measured at closure with `grep -rl ze_api test --include=*.ci`, and `internal/exabgp/bridge` reads the same lines for the ExaBGP compatibility path. `pkg/plugin/rpc/framing.go` and `pkg/plugin/rpc/answer_write.go` are two more, because child 1 had moved the writers there after this spec was written. Every one of the eight phases found a file the list omitted, and each found it when a fixture or a package went red rather than when the list was read | not a defect with a fix, a habit with one. Every phase widened the list in its own commit rather than treating the find as a one-off, and phase 3 routed the bridge through `rpc.ParseLine` so one writer and one reader serve it instead of a second reader drifting. It caught a real defect: phase 6 found the harness counting a counted text in CHARACTERS where the wire counts BYTES, which reddened three `show bgp rib graph` fixtures on box-drawing glyphs. The general practice is to derive a wire's population by grepping for its writer and its reader across every language in the tree, the test harnesses included, rather than from the file list a spec wrote before the first phase |
| 2026-08-22 | fixit-spec-status-metadata-window | the spec inventory's file population | both readers of a spec's metadata glob `plan/spec-*.md` and neither recurses: `loadAllSpecs` (the retired `scripts/status/spec_status.go` (current producer: `internal/le/spec/status/specstatus.go`)) and `_all_stems` with `_list` (the retired `scripts/dev/spec-closure-check.py` (current producer: `internal/le/`)). 41 tracked spec files live one directory down and are counted nowhere. For 38 of them that is correct and stated: `plan/future/README.md` says a spec there is work Ze intends to do that does not hold the first release, and moves back into `plan/` when the owner schedules it. The other 3 are `plan/to-review/`, which carries no README and is described by no rule, and whose specs read `skeleton`, `ready` and `design`, the same statuses the inventory counts as open work. So the inventory silently omits 3 specs whose own metadata says they are scheduled | not fixed, and not fixable inside this spec. Every acceptance criterion here is about how a spec's metadata table is PARSED, and which directories the glob walks is a question about its POPULATION. Answering it means first deciding what a spec in `plan/to-review/` IS, which is what `plan/future/` needed its README for before its exclusion could be called correct. That decision is the owner's. Raised with Thomas at closure on 2026-08-22. The two readers agree with each other today, so nothing drifts while the question waits |
| 2026-08-22 | fixit-spec-status-metadata-window | the retired `ze-spec-status` (current: `./le spec status`) summary line | the population is every spec the tool counted, and the per-status breakdown covered only the statuses a hardcoded slice named. `printTable` (the retired `scripts/status/spec_status.go` (current producer: `internal/le/spec/status/specstatus.go`)) walked `order`, eight status names written by hand, so a status absent from it was counted into the total and printed nowhere. Measured 2026-08-22 over the real tree: `Specs: 242 total (48 in-progress, 41 ready, 46 design, 93 skeleton, 11 blocked, 1 deferred)`, six counts summing to 240, because two specs carry `done` and the slice had never heard of it. The same slice omitted `verification`, and `specbucket.Category` filed it as `other`, so the status `ai/rules/planning.md` requires an implementing session to set before it commits was due to vanish from the summary and to sit beside blocked and deferred, which is where a reader looks for work nobody is carrying. The vocabulary itself already lived in two other places that disagree: the prose table in `ai/rules/planning.md` and the case statement in the retired `.claude/hooks/validate-spec.sh` (retired; now `internal/le/hookruntime/lifecycle.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> (current producer: `internal/le/hookruntime/lifecycle.go`), which accepts `done` while the rule does not list it | fixed on the spot, in the spec that owns the same tool. A status the reporting order does not name is now printed after the named ones, sorted, so the counts always sum to the total, and no fourth copy of the vocabulary was written: a name in the order buys a POSITION, so `verification` is named (committed work in flight) and `done` is not (terminal, and the sorted tail prints it last). `verification` also joins the committed-backlog bucket. `TestSpecStatusSummaryCountsEverySpec` (the retired `scripts/status/spec_status_test.go` (current producer: `internal/le/verify/engine/`)) compiles the tool and drives it over a plan tree of its own, and asserts the arithmetic rather than the words, so a status added later is covered without editing it. The habit is the repair: a breakdown printed beside a total is a claim that the two agree, so derive its tail from what was counted |
| 2026-08-20 | streaming-answer-protocol | closure commit file list, and the two gates over it | the population is the files a COMMIT must carry, and the list was built from a phase report's table rather than from the tree. That table named PRODUCING functions; `cmd/ze/hub/service_ssh.go` is the CALL SITE that routes to them, so it was named nowhere and committed never. `git status --porcelain` over `cmd/` shows it, and nobody ran one. Two gates read a population that held the file. `./le repository tracked-build check` reads what git holds, but it COMPILES, and the missing file only ADDS calls -- the `plugin.RecordRows(resp)` guard in the BGP executor factory and `dispatch.JSON` -> `dispatch.Answer` in the standalone one -- so nothing references its content and git's population compiles clean while the exec channel flattens the row generator and writes an answer of no rows. The five `answer-*.ci` then ran 5/5 green from the WORKING tree, where the file was present. Compiling-but-unwired passes both. The 2026-08-15 row below is the same gate's other blind spot, where the compile reached no `_test.go` | fixed at closure, found by running `git status --porcelain` over the whole tree instead of over the report's paths, and carried in commit A. The habit is the repair: a commit's file list is DERIVED from the tree and then checked against the report, never built from it. A report names what an agent changed and cannot name what it forgot. Neither gate is wrong -- compiling is the strongest property a compile can assert -- so what would see this is a functional test run against a build of git's population, which no target does today |
| 2026-08-20 | streaming-answer-protocol | record-versus-string pipe equivalence | the population is the CHAINS an operator can type, and the control covered only the chains that shape nothing. `TestTheRenderingIsTheSameOnBothSidesOfTheThreshold` (`internal/component/command/render_records_test.go`) compares the record path against the whole-payload path over five chains, and all five name a pure format operator: json, json compact, ndjson, yaml and raw. The operators that CHANGE the answer were tested against hand-written literals instead. One of those literals pinned a divergence. `RenderRecords` passes the answer's envelope to `plugin.CollapseRecords` for every chain. The count operator synthesizes one record through `recordsCounted` (`pipe_records.go`), and that record was filed under the command's envelope. `system command list` with the count and json-compact operators answered `{"commands":[{"count":1000}],"pipe":{"count":true}}` where `applyCount` (`pipe.go`) answers `{"count":1000,"pipe":{"count":true}}`. Live from the day phase 7 made `handleSystemCommandList` a generator, over one of the six operators every command supports | fixed at closure. `chainAnswersItsOwnDocument` states the property that decides it -- the chain answered for itself rather than shaping the command's rows -- and `answerDocument` (`render_records.go`) uses that record as the document. The literal is gone: `TestABoundedChainStopsTheWalkAndAnswersOneDocument` now compares against `renderedDocument`, the same whole-payload chain the format cases are judged by, so the two bounded operators joined the population. Mutation: with the early return disabled the test prints the two documents side by side. The general lesson is where a second path is built for an existing surface, the control belongs at the surface the operator reads and not at the layer the two paths share |
| 2026-08-20 | streaming-answer-protocol | plugin answer encoder | the population is the answer lines one 16 MB frame must carry, and the new bound covers the record lines alone. `boundedRecord` (`internal/component/plugin/dispatch.go`) rejects a record whose own line passes `rpc.MaxMessageSize`, which is AC-15. The sibling line is the DOCUMENT: a walk of at most `rpc.AnswerBufferThreshold` (256) records is collapsed by `CollapseRecords` (`internal/component/plugin/types.go`) into one `item=` line, and neither `writeDocumentAnswer` nor the collapse measures it. So 256 rows of 64 KB fail that one write, `writeRecordAnswer` returns before the terminator, and the consumer derives `VerdictTruncated` for an answer the daemon produced whole. The spec's stated win, the bound moving from the whole answer to one record, holds for a streamed answer and not for a bounded one | fixed 2026-08-22 by phase 7 of spec-record-answers-2-only-encoding. Child 1 had moved both writers to `pkg/plugin/rpc/answer_write.go`, and `writeDocumentLines` there now runs `boundedRecord` over the collapsed document as well as over each row, so the ONE line a bounded answer occupies is measured before it is built. The repair this row proposed was NOT taken: streaming the held records answers nothing for `WriteDocumentAnswer`, whose producer built its payload whole and has no records to stream, and AC-9 asks for the fault an over-wide record earns. A document with no line earns exactly that fault, named record one, and the head then states `map` rather than `doc`. That last part is not cosmetic: `CollapseAnswer` refuses a doc answer carrying a rejected row by name, because a document has nowhere to put one beside itself, so the alternative was a shape the buffered reader rejects. The answer reaches its terminator counting no record and one rejection, and the verdict derives to error rather than to truncated. `TestWriteDocumentAnswerBounded` covers the line at the limit, one byte over, and a bounded walk whose rows each fit, and `test/plugin/plugin-command-document-too-wide.ci` drives the same walk from `ze cli`. The habit is the repair: a bound belongs on the LINE a writer builds rather than on the value that prompted it, so every producer of that line inherits it |
| 2026-08-19 | fixit-irr-empty-answer-clears-set | firewall ruleset readback | the population is the tables `show firewall ruleset` can report, and it is the CONFIG's tables rather than the ones applied. `ApplyAll` (`internal/component/firewall/registry.go`) merges every owner's tables, hands the merged set to `Backend.Apply`, and never records the snapshot. Every `StoreLastApplied` call sits in `engine.go`, immediately after an `ApplyAll()`, and passes `cfg.Tables`: the config-derived set alone. So a table a plugin owner registered -- `firewall-irr` interface tables, `copp`, `policy-routes`, `ddos-local` -- is in the kernel and invisible to the readback. Measured: `test/plugin/firewall-irr-iface-no-blackhole.ci` refreshes AS-TEST, `refreshName` calls `applyTables` calls `ApplyAll`, which returns nil, and `show firewall ruleset ze_irr_iface` answers `table "ze_irr_iface" not found; no firewall tables have been applied` | fixed 2026-08-19, one writer for one population: `ApplyAll` (`internal/component/firewall/registry.go`) records the merged set with `StoreLastApplied(all)` when `Backend.Apply` returns nil, and the three `engine.go` calls are deleted (`ai/rules/no-layering.md`). Two edges are decided in comments: the no-backend nothing-to-apply return leaves the snapshot as it is, because no Apply ran and only `CloseBackend` knows the state is gone; an empty merged set a live backend accepted is a real withdraw and is recorded as an empty snapshot. Five tests in `registry_test.go` cover the plugin-only reconcile, the two-owner merge, a failed Apply keeping the previous snapshot, the withdraw, and the untouched no-backend path, each proven against a mutant. `test/plugin/firewall-irr-iface-no-blackhole.ci` goes green and fails on the mutant with `no firewall tables have been applied`. Its `show firewall ruleset` argument was also wrong: it named the kernel table `ze_irr_iface` while the handler matches the stripped name the CLI advertises, `irr_iface` |
| 2026-08-18 | fixit-flowspec-protocol-name-drift | firewall nft backend, table ownership sweep | the population is the tables the backend may delete, and it is derived from a NAME PREFIX that only one of the two producers applies. `shouldDeleteTable` (`internal/plugins/firewall/nft/backend_linux.go`) returns false for any table not starting with `ze_`, while `applyTable` programs `tbl.Name` verbatim. The firewall component prefixes its config-derived tables. Correction, 2026-08-23: this row named `copp`, `policy-routes` and `firewall-irr` as bare producers, and they are not. Those are OWNER keys, and each owner's TABLE name carries the prefix -- `ze_copp` (`internal/plugins/copp/translate.go`), `ze_pr` (`internal/plugins/policyroute/translate.go`), `ze_irr_iface` (`internal/component/firewall/plugins/irr/sets.go`), `ze_ddos-local` (`internal/plugins/ddos/local/responder.go`). The producers that registered a bare name were TWO, read at their literals on 2026-08-23: `flowspec` (`internal/plugins/flowspec-firewall/state.go`, `buildTable`) and `anomaly-shape` / `anomaly-shape6` (`internal/plugins/anomaly/shape/match.go`), which this row missed. So when a peer withdraws its last FlowSpec route, `applyRules` registers nil, `ApplyAll` hands the backend a desired set without `flowspec`, and the sweep declines to delete it: the kernel keeps enforcing a rule no owner still wants. Read-verified from both producing functions; not reproduced against a kernel, because every `caps=net-admin` test skips on an unprivileged host | not fixed. It does not block this spec, which is about which protocol names translate and about one bad route not stopping the reconcile. The repair is one decision covering all four owners: either the registry prefixes every owner's table names, or the sweep derives ownership from the registry rather than from a spelling. `flowspec-fw-withdraw.ci` asserts the wire withdrawal and never looks at the kernel, so nothing red says this today. FIXED 2026-08-23 by `spec-fixit-flowspec-table-never-swept`, and the repair took the first of the two routes: both bare producers now carry the prefix, `RegisterTables` refuses a name without it, and the backend deletes the names the older builds wrote once each so an upgrade strands nothing. Correction, 2026-08-24: "TWO" above counts the producers still bare in the tree at HEAD, and what an upgraded box holds is what SHIPPED. `git log -S` finds two more that shipped bare and were renamed earlier -- `copp` (`internal/plugins/copp/translate.go`, `0ee364e29` to `c5273da42`) and `ddos-local` (`internal/plugins/ddos/local/responder.go`, renamed in `1423ad437`) -- so the removal ledger carries four producers and five names. Reading the tree answers "who is bare now"; only the history answers "who left a table in a kernel" |
| 2026-08-18 | fixit-irr-empty-answer-clears-set | doc drift gate | `functionalGateSuites` (the retired `scripts/docvalid/doc_drift.go:174` (current producer: `internal/le/docvalid/drift.go`)) derives the release-gate suite list from the recipe of the `./le functional:` target, and that recipe no longer holds one. `e319364e7` moved the 23 `run_suite` lines into `_./le functional` and left `./le functional:` delegating one line to the retired `scripts/dev/ze-run.sh` (current producer: `internal/le/job/job.go`), so the walk breaks at the first non-tab line with zero suites, and `checkFunctionalTestsMD` reports "could not derive ./le functional suites from the retired Makefile (current producers: `internal/le/` native action tables)". `./le docvalid doc-drift` and `./le doc wiring` are red at HEAD in a clean the retired `Makefile` (current producers: `internal/le/` native action tables)/the retired `mk/` (current producer: `internal/le/`)/the retired `scripts/docvalid` (current producer: `internal/le/`) tree | not fixed. It does not block the IRR spec: nothing that spec touches feeds this derivation, and the red predates it. The derivation must follow the recipe to the `_./le functional` delegate, and `checkMakefileHelp` reads the same empty list |
| 2026-08-16 | web-htmx4-cutover | derived per-page asset sets | the population is the htmx attributes a page renders, and the derivation cannot see one whose NAME carries a digit. `attributePattern` (the retired `scripts/codegen/web_assets.go` (current producer: `internal/le/webassets/webassets.go`)) is `\s(hx-[a-z:-]+)=`, and htmx 4 reads `hx-status:400`, `hx-status:40x` and `hx-status:4xx` (`#de` in the beta6 core). A page whose only htmx attribute were status-scoped would resolve an EMPTY asset set and load no core, so the attribute that made it a page would be the attribute that stopped it working. The same regex is copied in `internal/test/markupcheck/head.go` and in `htmxAttributePattern` (`internal/component/web/markup_contract_test.go`), so the independent checker shares the blind spot rather than catching it | not fixed. No Ze markup uses a status-scoped attribute today, so the hole is latent; it was found by phase 5 while deciding whether one could replace the chaos retarget JS, and phase 6 records it rather than widening a regex three files hold copies of |
| 2026-08-15 | fixit-lint-blind-to-integration-tag | go lint | golangci-lint analyses ONE build, so its population is one GOOS plus the tags `.golangci.yml` lists, and everything else is not merely unchecked but silently reported as clean. 77 tracked files behind `//go:build integration` had never been loaded and carried 132 findings, measured uncapped; a capped run reads 94, because `max-issues-per-linter: 50` hides a third of them. The 2026-08-11 row below is the same hole seen from the other side, where an integration-tagged file had not COMPILED at HEAD and no host gate could say so | the integration half is FIXED: `./le verify lint run` and `./le changed scope` each run a second pass under `GOOS=linux --build-tags integration`, and all 132 are cleared. The tag alone would not have done it -- 75 of the 77 files are `integration && linux`, so on a darwin dev machine the tag without the GOOS reaches 0 of them. Re-measured at closure on 2026-08-23, what REMAINS is 144 tracked own-source Go files, from THREE causes rather than one. 55 sit outside `ZE_LINT_PKGS` and carry no build tag at all. 44 of those are under the retired `scripts/` (current producer: `internal/le/`), where this repository's own gates live. 47 are `//go:build !ze_<feature>` compile-out stubs. The lint build sets every gate ON, and no flag can UNSET one. An extra pass therefore cannot reach a stub the way it reached `integration`. 42 carry a positive tag neither pass has. `ze_installer` is the first of those at 13 files. It is compiled, vetted and unit-tested. What has never happened is a LINT pass loading it. Each personality tag names a mutually exclusive build, so they cannot share one tag list and each needs a pass of its own. Homed in spec-fixit-lint-blind-to-every-other-build-tag, with each cause measured |
| 2026-08-14 | fixit-bgp-egress-rail-divergence | spec citation gate | the population is the set of CITERS, and it is scoped to one directory. `find_dangling` (the retired `scripts/dev/spec-citation-check.py` (current producer: `internal/le/spec/citation/speccitation.go`)) iterates `spec_files(repo)` and its own docstring says "Scoped to `plan/spec-*.md` citers", so only a spec can be caught citing a spec. Source code cannot. Closing one spec found six Go comments naming it by full path, `errRelayAddPath` (`internal/component/bgp/reactor/reactor_api_relay.go`) among them, and `./le spec citation anchors` was green over all six before and after. A closure removes the file and every such comment becomes a citation of a path the tree does not hold, which is the exact rot the gate exists to refuse | the six comments were repointed or restated as bare stems when that spec closed. The GATE is unchanged and still cannot see a non-spec citer: `.go`, `.py`, `.sh`, `.yang`, `docs/` and `ai/` are all outside its walk. Widening the citer set is mechanical; the reason it was not done here is that the fix belongs to the gate, not to the closure that tripped over it |
| 2026-08-14 | fixit-dynamic-group-peer-config | bgp per-peer config keying | the population is the SPELLINGS of an address. `PeerKey` (`internal/component/bgp/configjson/traverse.go`) stores the operator's RAW text: it returns `PeerConfigKey{ID: ip}` with `ip` as `PeerRemoteIP` read it from the document, and on the name branch it parses the name only to test it, then keys the unparsed name. Every runtime reader produces the canonical form instead, because it holds a `netip.Addr` and calls `String()`. So `2001:0DB8::1` in the config keys an entry no reader can reach, and the peer silently gets the global policy. `blackholecfg` normalizes and does not have this; `role` and any later consumer of `PeerKey` inherit it | not fixed. It does not block the dynamic-group work: it is the STATIC path, and a listen-range member has no configured address at all. Found by the phase 5 agent, which normalized inside `rpki` alone. Whether `PeerKey` itself should normalize is one decision covering every consumer |
| 2026-08-12 | - | bgp inbound listener routing | a per-peer listener swallows every inbound on its local address, so a dynamic group sharing that address is never consulted. `AddPeer` (`internal/component/bgp/reactor/reactor_peers.go`) starts a listener for every passive peer and always passes that peer's key, and `startListenerForAddressPort` (`reactor.go`) installs `handleDirectConnection` whenever the key is valid, which routes the connection to that ONE peer with no remote-IP match and no `tryCreateDynamicPeer` call (`reactor_connection.go`). The shared listener, which `handleConnectionWithContext` serves and which is the only path that can create a dynamic peer, is reachable from AddPeer's MD5 listener-restart branch alone -- and that branch is the one place that passes an INVALID key, and only when the port is 179. Measured: a config with one passive `anchor` peer (remote 127.0.0.2, local 127.0.0.1, accept true) and a dynamic group over 127.0.0.0/8 answers an inbound from 127.0.0.1 with `session established peer=127.0.0.2` and logs no `dynamic peer created` | not fixed. Found while writing the functional proof for the reload row below, which cannot be written: a `.ci` that establishes a dynamic session passes with its subject reverted, because no dynamic peer exists in the run. Two independent things need a decision -- AddPeer's two listener call sites disagree about when a listener may be key-routed (the MD5 restart branch withholds the key on port 179, the create branch never does), and no `.ci` can bind 179, so no functional test reaches a dynamic peer on this harness at all. The draft that got as far as an established session, an active-role scripted peer and a shutdown-NOTIFICATION continuity assertion is under `test/draft/`, which `.gitignore` excludes, so it lives in one working tree only |
| 2026-08-12 | - | bgp forward rails | the RFC 7947 route-server gate refuses a whole UPDATE for a client its control communities exclude. `communityPolicy.ShouldForwardTo` (`internal/component/bgp/reactor/reactor_api_forward.go`, and the same branch in `forward_rs.go`) is derived from `wireu.ParseCommunityPolicy` over the ANNOUNCED route's communities, so it is a per-route decision applied to a whole message. A mixed UPDATE also carries withdrawn routes, which carry no attributes and were tagged by nobody, so a client excluded from prefix B loses the withdrawal of prefix A that an earlier UPDATE did advertise to it. Same shape as the RFC 1997 gate fixed in `ef5ed2a10`, whose `wireu.WithdrawalsOnly` is the ready-made repair. The RFC 4456 reflection `continue` two branches below is NOT the same defect: its condition reads only the source and destination roles, never a route attribute, so it excludes both halves consistently and the destination has its own session with the originator | fixed on BOTH rails, the ROUTE-SERVER one first. `reactorForwardRS` (`forward_rs.go`) now hands a refused client `wireu.WithdrawalsOnly` as its `destBaseWire`, exactly as the RFC 1997 gate above it does, and the derivation is shared behind one flag so an UPDATE that reaches neither gate still pays nothing. Proven by `TestForwardRSWithdrawsFromClientRefusedByControlCommunity` plus its no-withdrawal control, and by `test/plugin/bgp-rs-control-community-withdraw-egress.ci`; reverting the branch to `continue` reddens both and leaves the controls green. Then fixed on the GENERAL rail the same way: `forwardUpdateCore` (`reactor_api_forward.go`) hands the refused client the same withdrawal part behind the same shared flag, and `suppressedCount++` now fires only where the client receives nothing at all -- a client the withdrawal reaches was not skipped, and counting it would let `errAllDestinationsSuppressed` report a dispatch failure as a policy suppression. The LOCAL_PREF re-ask and the ASN4-width re-lookup needed no change: `ef5ed2a10` had already moved the first to `peerBaseWire != update.WireUpdate` and the second to `peerBaseWire != destBaseWire`, and both read the new base. Proven by `TestForwardWithdrawsFromClientRefusedByControlCommunity`, its no-withdrawal control, `TestForwardRefusedClientWithWithdrawalIsNotCountedSuppressed` for the counter, `TestForwardRailsAgreeOnControlCommunityRefusal` for the invariant the divergence broke, and `test/plugin/control-community-withdraw-egress.ci`; reverting the branch to `continue` reddens the general-rail `.ci` and leaves the route-server one green |
| 2026-08-12 | - | bgp reload peer reconcile | `reconcilePeersJournaled` (`internal/component/bgp/reactor/reactor_api.go`) diffs the config-derived peer list against EVERY entry of `r.peers`, and a dynamic peer is not in the config-derived list: it is created at runtime from a group template when a connection in the range arrives (`createDynamicPeer`, `reactor_dynamic.go`). Its key therefore reads as "no longer configured" and it lands in `toRemove`, so every established dynamic session is torn down on every reload. `SetDynamicGroups` (`reactor_dynamic.go`) already keeps the dynamic peers whose group survives, which is the population this diff was supposed to leave alone. Measured, not reasoned: an established peer with `IsDynamic` set is absent from `r.peers` after one `Reload()` whose config carries a different peer | fixed 2026-08-12. The diff now skips a dynamic peer the configuration does not name, and `SetDynamicGroups` owns that population outright: it already removed the peers whose group is gone or whose address left every range, and it now also removes the ones whose group TEMPLATE changed, compared as the settings `buildDynamicPeerSettings` would produce for that one address. A changed template therefore still reaches the peer, by restart. The recorded race stays unarmed on purpose: a dynamic peer whose address the new configuration DOES name is removed and re-added rather than swapped, so `applyHotSwappableSettings` never runs on one, and `TestReloadNeverSwapsSettingsOntoADynamicPeer` drives that branch with an entry the swap plan would otherwise accept |
| 2026-08-11 | runtime-kernel-carries-every-tunnel-kind | config validation mandatory check | `ze config validate` prints `mandatory field "kind" is missing` once per tunnel in a config it declares valid in the same breath. `ze-iface-conf.yang` models the discriminator as `choice kind` with `mandatory true` inside `container encapsulation`, and every tunnel in both configs measured selects a case. Measured on two configs: the one in the new `test/plugin/iface-tunnel-kinds.ci` warns 9 times and the one already shipping in `test/reload/tx-iface-tunnel-create.ci` warns 8 times, one per tunnel in each. So the check reads a satisfied mandatory choice as an unset mandatory leaf, and every conforming tunnel config in the tree carries the warning. The producing function was not read, so which check emits it is unestablished | not fixed. It does not block this spec: validate exits 0 and the daemon applies the config. The warning has been on every tunnel config ze ships for as long as the choice has been modeled this way |
| 2026-08-11 | fixit-config-validators-bypassed-at-startup | config validation walk | `yangSectionsToValidate` (`internal/component/config/cli/cmd_validate.go`) is a hand-written list of 17 names beside a schema that derives 36 top-level sections. It is wrong in BOTH directions. Six of its names -- `web`, `ssh`, `dns`, `looking-glass`, `mcp`, `managed` -- are not top-level sections at all: they sit under `environment`, which the list omits, so `tree.GetContainer` returns nil and those six iterations have never validated anything. And 25 real sections are missing, two of which carry live `ze:validate` bindings nothing else covers. The measured consequence: `test/parse/redistribute-invalid-source.ci` asserts exit 0 for `import rip` and explains it as "list key values are not validated by ze:validate at parse time", which is FALSE -- `Validator.validateListKey` does validate list keys, and a probe fires `"rip" is not a registered redistribute source` on that exact input. The test documents the section list's hole as if it were the validator's design | not fixed here. The spec implements the `LoadConfig` call against the list AS IS, because that scope measures green over 1387 parsed shipped configs. Widening the list is its own work and is gated on the three defects in the row below |
| 2026-08-11 | fixit-config-validators-bypassed-at-startup | config validators vs registries | three validators answer from a registry that does not hold what they are asked about, and all three are inert only because the section list never reaches them. `AddressFamilyValidator` (`internal/component/config/validators.go`) reads `registry.FamilyMap()`, whose own doc comment says it holds "only plugin-registered families (used for decode routing)" and excludes `builtinFamilies`; measured in a full-tag binary, `FamilyMap` is 19 names WITHOUT `ipv4/unicast` while `AllFamilies` is 23 WITH it. `TestAddressFamilyValidator_Validate` is green only because it resets the registry and registers a fake plugin claiming `ipv4/unicast`. Widening the walk to `bgp` and `redistribute` would therefore refuse 742 + 33 family sites across 687 shipped configs, which is essentially every BGP config in the tree. Same shape: `send-message-type` and `receive-event-type` read registries `RegisterPluginSendTypes` populates from `PeersFromTree` and `NewServer`, both strictly LATER than the point the startup-validation fix would call them, so they fail closed on valid input at exactly that moment | not fixed. Each is a separate defect with its own blast radius, and all three must land before the section list can widen |
| 2026-08-11 | fixit-forward-rail-initial-sync-ordering | bgp forward rails | the route-server rail's ordering gate covers the items a destination is owed through OVERFLOW and not the items it is owed through the pool CHANNEL. `overflowPending` (`internal/component/bgp/reactor/forward_pool.go`) is raised only by `DispatchOverflow`, so an item that reached `w.ch` through `TryDispatch` leaves it at zero. `reactorForwardRS` (`forward_rs.go`) falls back to `TryDispatch` exactly when `tryDirectWriteNoFlush` could not take `session.writeMu`, and the next UPDATE for that destination reads the gate as clear and takes that same `writeMu` with `TryLock` before the worker does. Same inversion this spec closed for the overflow window, one rail over | not fixed. It predates the gate: both the direct write and the `TryDispatch` fallback are older. A count covering channel items would send every later forward through the pool for the whole of any channel backlog, which needs its own tests and its own review |
| 2026-08-11 | fixit-route-removal-protocol-blind | qemu integration target | the retired `ze-qemu-integration-test` (current: `./le qemu run command "./le qemu all-tests"`) runs `go test -tags integration` with NO feature tags, so no plugin registers its YANG and the doctor's listener collection is empty: `TestCheckListeners_API`, `TestCheckListeners_PortInUse`, both `TestCollectSchemaListeners_SSH*`, `TestDoctorProbesEveryCoveredListener`, `TestDoctorGnmiDefaultEndpointIsProbed` and `TestDoctorDependencyInventory` all fail there. The same tree, same commit, compiled with the full tag list in the unit phase gives `ok internal/component/doctor 7.304s`. The target also caps `-timeout 120s` while `cmd/ze/hub` needs about 400s, so that package is cut off rather than failing. The gate compiles a different program from the one Ze ships and then judges it | not fixed. The target needs the feature tag list the unit phase already uses, and a timeout derived from the slowest package rather than a constant |
| 2026-08-11 | fixit-route-removal-protocol-blind | isis raw L2 transport | `internal/plugins/isis/transport/transport_integration_linux_test.go` calls `tr.OnMTUMismatch` while `transport.go` declares `onMTUMismatch`, so the package has not COMPILED at HEAD since the symbol was unexported. Nothing sees it: the file is `//go:build integration && linux`, and `go build ./...`, `./le changed scope` and every host-side gate compile neither that tag nor `_test.go` at all. Only a QEMU integration run reaches it, and its own build failure is what hides the tests it was written to run | corrected the call. The general hole stands: a `_test.go` behind a build tag no host gate carries is unverifiable until the tagged run happens, and that run is the slowest one in the tree |
| 2026-08-09 | fixit-dead-design-pointers-in-tests | doc gates | `go_files()` excluded `_test.go`, so 133 dead `// Design:` spec pointers accumulated where no gate looked. Non-test files held zero, which is the control | widened the list, and refused the pointer class that closure deletes |
| 2026-08-10 | doc-claims-are-checked-not-just-resolved | doc gates | 85 package directories under `internal/` have no anchor and no `docs/architecture/` page with every gate green: `checkDesignMD` in the retired `scripts/docvalid/doc_drift.go` (current producer: `internal/le/docvalid/drift.go`) reads `registry.All` against one table in `docs/DESIGN.md`, so `internal/plugins/explain` and `skills`, which register a CLI root instead, are invisible | not fixed |
| 2026-08-10 | doc-claims-are-checked-not-just-resolved | verify stages | `./le repository tree-check` runs three of `validate.py`'s five checks by passing an explicit empty changed-file set, which selects them only because `['']` is truthy in `main()`. A tidy that strips empty strings silently re-arms the other two under `./le verify current mode full`, and nothing asserts which checks take `changed` | not fixed, needs an explicit flag and a test over the changed-taking set |
| 2026-08-10 | - | RFC evidence | RFC 9234 Section 5 egress rule 1 names three destination roles. Every test gave the destination wire role 3. The `roleRSClient` arm of the gate in `OTCEgressFilter` was never reached, but `RFC9234-5-4` read as proven. A requirement that enumerates roles needs one test per role. The ledger counts bindings, never which arm of an OR each one takes | added `test/plugin/role-otc-rs-client-dest-stamp.ci`, mutation-verified against dropping that arm |
| 2026-08-10 | session-bin-directory | hooks, make | the population is the SPELLINGS of the guard's subject. A corpus written by the guard's author uses the spelling the author had in mind. `_SCRATCH_WRITE` anchored on a literal `tmp/`, so `./tmp/x` and the absolute `<repo>/tmp/x` passed while the Write surface refused them. All 196 rows spelled it the refused way. `ze-scratch-clean`'s `-not -name session` read as protecting `tmp/session/`, but `find tmp/` yields `tmp/` itself at depth 0, whose basename matches neither exclusion, so a stale root mtime `rm -rf`d the whole scratch tree. In both, the DECIDING function was already correct. Only what FED it was narrow | widened the candidate regex, added `-mindepth 1`, each with an allow-control and a mutation-verified test. The test that finds this is never another row in the same corpus. Enumerate the spellings, then drive the guard from its entry point with each one |
| 2026-08-10 | fixit-forward-readbuf-leak-deferred-pool-release-paths | commit gates | `deferral_shard_removal_problems` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)) reads a shard at HEAD to decide whether the removal destroys a live row. Closure is TWO commits in ONE script. Commit A resolves the last live row and commit B removes the shard, so the HEAD the gate reads at PREPARE time is one commit behind the HEAD the removal runs against. An all-terminal shard is refused, and the residue nobody collects is what the rule wanted removed. `_live_rows_added_elsewhere` already exists for the move case and reads only the SAME commit's adds | not fixed, and the shard is kept. The fix is to fold the prepared sibling commits of the same script into the snapshot the gate reads, the way `_prospective_added_lines` already does for the journal checks |
| 2026-08-10 | dead-learned-citations-outside-the-walked-corpus | doc gates | `check_markdown` in the retired `scripts/dev/check_doc_links.py` (current producer: `internal/le/doc/check/links.go`) walked `MD_GLOBS`, fourteen hand-written globs, while the population that CITES a path is every tracked file. 2953 dead references over 1373 targets had collected where no gate looked, `plan/spec-*.md` holding 2085 of them. The hand-written list is the tell: a corpus nobody can state is a coverage claim nobody can check | widened to every tracked file behind a shrink-only baseline of `(citer, target)` pairs. Repairing 1373 targets is a multi-session job, and a red gate that stays red teaches everyone to bypass it |
| 2026-08-10 | dead-learned-citations-outside-the-walked-corpus | doc gates | the shrink-only ratchet compared the baseline's SIZE against HEAD, so it excluded the population's identity. Repair one citation, add one dead citation, regenerate, and the total is unchanged while a pair the sweep had already failed on is grandfathered. A ratchet over a set MUST compare the set: `check_coverage_ratchet` in the retired `scripts/dev/rfc_requirements.py` (current producer: `internal/le/rfc/rfc.go`) fires on a requirement losing a polarity it held, never on a total | `check_baseline_growth` compares `current - head` and names each new pair. The test that finds this holds the SIZE constant, so a count comparison passes it and a set comparison does not |
| 2026-08-10 | fixit-rs-community-strip-arity | bgp filter_community | the whole-value check covers two of the three buffers `genericCommunityHandler` reads. A malformed `p.Value()` fails the plan and a malformed Remove `Buf` is refused and counted, but every Add `Buf` is emitted unchecked by `p.Op(i)`, so a producer that hands it a partial value writes an attribute length no peer sent. Not reachable today: all three producers allocate exactly `len(tokens)*width` or a fixed array. The file argues the Remove guard exists so a NEW producer is heard on its first route, and that argument covers Add identically | not fixed, needs the same `wholeValues` guard on the Add arm plus a test per width |
| 2026-08-10 | dead-learned-citations-outside-the-walked-corpus | doc gates | the shrink-only rule moved from a count to a set. The baseline header was corrected by hand. `write_baseline`'s inline copy kept the false text, so the next `--write-baseline` would have reverted the correction. Every gate stayed green because no gate reads a comment: every check in the module has paths and symbols for its population | hoisted the text to `BASELINE_HEADER`, the one home `write_baseline` emits, with `test_baseline_header_is_what_the_generator_writes` comparing it against the committed file. The general form is requirement 11 of `ai/rules/completion.md` |
| 2026-08-10 | fixit-l2tp-sccrq-tunnel-id-zero | RFC evidence | `RFC2661-24.10-1` names SCCRQ AND SCCRP. Both polarities were tagged, both tags drove SCCRP in `tunnel_initiator_test.go`, and the ledger read fully proven while the SCCRQ half dropped the datagram in silence at `handle` (`internal/component/l2tp/reactor.go`) and the published coverage cell claimed the reactor sent StopCCN. `check_coverage_ratchet` counts polarities per requirement id, never per message the requirement's own text names | implemented the SCCRQ answer and tagged both polarities on that path, in `reactor_sccrq_zero_tid_test.go` and `test/l2tp/rfc2661-sccrq-tunnel-id-zero.ci`. A requirement whose text enumerates two messages needs one tag pair per message, the same shape as the RFC 9234 destination-role row above |
| 2026-08-10 | fixit-vpp-ipsec-inoperable | RFC evidence | the strongest proof of `RFC7296-2.24-1`/`-2` on the VPP IPsec backend is a real VPP reporting `encap-copy-ecn` and `decap-copy-ecn`, and it lives in the retired `scripts/evidence/effective-vpp.py` (current producer: `internal/le/deployment/vppevidence.go`). `scan_tree` (the retired `scripts/dev/rfc_requirements.py` (current producer: `internal/le/rfc/rfc.go`)) walks `internal/`, `pkg/` and `test/` only, and `carrier_for` matches the suffixes `_test.go`, `.ci`, `.et` and `/check.py` only, so that file is outside the population twice over. A tag written there is not refused either: `carrier_for` returns `None`, `_refuse_unrun` never runs, and the tag is invisible. `ai/RFC-REQUIREMENTS.md` therefore publishes `unit/verify` for a requirement a real daemon proves, and `check_evidence_ratchet`, which is keyed on `kind/tier`, cannot see the tier it never counted | not fixed: a real-daemon evidence script is a carrier kind the ledger does not model, and inventing one for a single path is a design question rather than a defect fix. The evidence is written into the spec's Goal Validation instead, with the label it can honestly carry. The tell is the same as the doc-gate rows above: the population is a list of shapes somebody wrote down, and the best evidence grew outside it |
| 2026-08-11 | runtime-kernel-carries-every-tunnel-kind | iface link classification | `kernelTunnelKinds` (`internal/component/iface/discover.go`) lists seven kernel link types and omits vxlan, though `TunnelKindVxlan` is modelled in YANG and both backends build one. Two consumers read that set as their whole population: `infoToZeType` classifies a vxlan link as no ze type, so `DiscoverInterfaces` and `ze init` do not see it, and `zeManageable` (`internal/component/iface/config_apply.go`) answers false for it, which is reconcile's deletion scope, so a vxlan dropped from the config is never deleted | not fixed. One map entry closes it, and it changes both discovery output and reconcile's deletion scope for a kind no test in the tree creates, so it needs its own test rather than a line folded into this spec |
| 2026-08-12 | fixit-config-validators-bypassed-at-startup | hub config parser | `ze config validate` answers over a config shape the daemon parses with a different parser, so the two verdicts disagree by construction. A config whose only top-level block is `plugin` boots through `parsePluginBlock` (`internal/component/hub/config.go`), which accepts `external` and refuses everything else. The YANG schema models `plugin/internal/use`, and `InternalPluginNameValidator` offers 89 registered internal plugin names as its valid values. Measured on `plugin { internal p1 { use bgp } }` alone: `configuration valid` and exit 0 from `ze config validate`, exit 1 and `parse config: line 2: expected 'external', got internal` from `ze start`. The spec closed the day before exists to make those two callers agree on the same bytes | not fixed, needs its own spec. Either the hub parser accepts `internal`, or `ProbeConfigType` stops routing a plugin-only config away from the YANG daemon. Both decide which parser boots a config shape that already ships <!-- doc-links: ignore (internal/component/hub/config.go was deleted with the orchestrator runtime in 8d92e9fab) --> |
| 2026-08-13 | - | bgp config name uniqueness (found under spec-fixit-dynamic-group-peer-config, which does not close here) | the uniqueness gate over peer names excludes group names, so the two namespaces can collide. `ResolveBGPTree` (`internal/component/bgp/config/resolve.go`) collects every peer name into one `peerNames` map and refuses a duplicate; a group name goes through `validateGroupName` alone, which checks the SHAPE of the name and never compares it against a peer's. `bgp { peer ix {...} group ix {...} }` is therefore accepted. The population the gate must cover is peer names union group names, because every per-peer plugin index is keyed by one identity: `role`'s `filterPeerConfigs`, `rpki`'s `PeerActions` and `filter_community`'s `configs` are all `map[string]...`, so a group's template and a peer of the same name would answer each other's lookups. Found while giving a dynamic group's template a key of its own | not fixed, and not fixable inside this spec: the collision predates it and the fix is a refusal in `ResolveBGPTree`, which changes what configs load. This spec routes around it instead, so nothing it adds depends on the two namespaces being disjoint -- `configjson.PeerConfigKey` is a typed pair whose `Template` field separates them by construction, and `configjson.CapabilityGroupKey` prefixes `group:` on the one path whose contract (`rpc.CapabilityDecl.Peers`, a `[]string` crossing the plugin process boundary) forces one string space. `naming.ValidateNodeName` accepts no `:`, so that prefix holds by construction too. A-7 in the spec records the same finding |
| 2026-08-14 | spec-web-templ-migration | the retired make ze-web-golden-check (current: ./le web-assets check) | the target ran `go test -run 'TestWebGoldenOutput'`, and `go test -run` exits 0 over an empty selection. So the gate reported green the moment either golden test was renamed or deleted, which is the cheapest route from red to green and the one move a ratchet exists to refuse. Its whole population is two named tests, and a typo excluded both. Measured: `-run TestWebGoldenOutputTYPO` exits 0 with `[no tests to run]` | fixed. A `require-go-test` define in the retired `Makefile` (current producers: `internal/le/` native action tables) runs `go test -list '^NAME$'` and greps for an exact match before the run, so a missing name fails first. A build failure exits with go's own status, so a broken package is never mistaken for a missing test. Proven by renaming the test and watching the target exit 2 |
| 2026-08-14 | spec-web-templ-migration | web handler golden capture | `golden.RoutePatterns` (`internal/test/golden/routes.go`) derives the hub's route list from ONE file, `cmd/ze/hub/service_web.go`. It reads only `X.Handle` and `X.HandleFunc` selector calls in it. All 31 registrations sit there today: 30 with a literal pattern, one in the registry loop. A registration moved to a helper, or to a second file of that package, leaves the derived set. `AssertCoversNames` then reports nothing, because the route left the live side and the reached side together. The only floor is the fatal in `routes.go` for a file that registers no literal pattern. It catches a total move and not a partial one | not fixed, recorded. The fix derives the set from the package rather than from one file. That needs a package load and a rule for which calls count |
| 2026-08-14 | spec-web-templ-migration | web handler golden capture | `webLiveRoutes` (`internal/component/web/handler_golden_test.go`) reads `RegisteredWebRoutes()` in the TEST binary. Every `RegisterWebRoute` caller sits in package web today, and `webroute.go` documents the call as the extension point for other packages. A registration from another package links into the daemon and not into this test binary. The live side and the reached side lose it together. The check then stays silent over a route the hub serves and no case requests | not fixed, recorded. The fix needs a route inventory the test binary cannot shrink. That is the same population question as the row above |
| 2026-08-14 | spec-netlab-integration | doc-links citation grammar | `KNOWN_ROOTS` (the retired `scripts/dev/check_doc_links.py` (current producer: `internal/le/doc/check/links.go`)) is a hand-written list of top-level directories, and `candidate_paths` DISCARDS any span whose first segment is absent from it. Four tracked top-level trees are absent: `demos`, `docker`, `iso`, `tmp`. Every backticked reference into them, in any tracked file, is skipped rather than verified, so a rename inside them breaks citations that no gate reads. Measured by adding the roots-versus-tracked-directories assertion and running it: `[demos, docker, iso, tmp]`. `docs/guide/docker.md` cites `docker/Dockerfile`, which the ze-docker-build work changed the same day | not fixed for those four. `contrib` WAS added here, because this phase created that tree and its `ai/INDEX.md` discovery rows would otherwise have pointed into a directory the gate cannot see. Whether the list should be DERIVED from the tracked top-level directories, rather than hand-written, is one decision covering all four and every tree added after them |
| 2026-08-15 | spec-web-templ-migration | web markup contract tests | two guards read less than they claim. `jsBlock` (`internal/component/web/markup_contract_test.go`) counted raw braces. A closing brace inside a string or a regular expression ended the block early. Every assertion on the remainder then went quiet. `TestCapturedTableRowsCoverTheirHeader` skipped a table with no `tbody` and counted no skip, so nothing checked such a table | FIXED 2026-08-15. `balancedBlockAt` skips a quoted string, a line comment, a block comment and a regular expression. `TestBalancedBlockReadsPastTextAndComments` pins each case. A table with no `tbody` now has its header removed and its remaining rows checked, so nothing is skipped. `TestErrorDrawerWiringHoldsTogether` also matched `class="<name>"` verbatim, which reds on a correct base-plus-modifier edit. Its `querySelectorAll` loop asserted another feature's class against error markup. It reads one class inside the attribute now, through `markupHasClass`, and the loop carries the same `error` prefix filter as its sibling |
| 2026-08-15 | spec-web-templ-migration | AC-1 type-safety proof | `TestTemplComponentTypeSafety` renamed a field in a two-file fixture under `internal/component/web/testdata/` and required the build to break. That fixture WAS its whole population, so it proved templ checks a field name somewhere and said nothing about the 72 ported components the daemon serves. Phase 1 built it deliberately, because no component was ported yet, and the deliverable row said phase 4 must repoint it at a real one. The port landed and the repoint did not, so the only proof of AC-1 ran over code that ships to nobody | fixed at closure. The test renames `LayoutData.Title` in `internal/component/web/render.go` and loads package `web` itself through a `go/packages` overlay, so no broken file reaches disk and two sessions can run it at once. It requires an error POSITIONED in a `*_templ.go` and naming `Title`, which is the generated read of `v.Title` in `page_layout.templ`: an error anywhere else would prove the markup checked nothing. A clean load runs first and must report no error, so a package already broken cannot satisfy the second load for the wrong reason. The fixture package is deleted |
| 2026-08-15 | spec-web-templ-migration | ./le repository tracked-build check | the one check that reads what git holds runs `go build`, and `go build` compiles no `_test.go`. A commit that omits a DATA file only a test reads therefore lands green and leaves HEAD red for every other session, while every working tree carrying the edit stays green. Phase 3d marked `plaintext-password` `ze:sensitive` in two YANG modules and committed one, so `TestAWriteOnlyPasswordLeafIsMarkedSensitive` was red at HEAD and no session could see it. Measured on a clean export of HEAD, which is the only way to see it at all. Three more of this spec's files sat uncommitted the same way and no gate named them either: `./le repository check` found four dead source anchors in `docs/guide/web-interface.md` whose repair had been written and never committed | the four files are committed at closure. The blind spot is NOT fixed. `ai/rules/git-safety.md` already states that the tracked-build check compiles no test file, and nothing compiles the tracked test population. Row 10 of `plan/journal/closure-deletes-a-cited-document.md` is the same gate missing the same population, reached from the deletion side |
| 2026-08-15 | spec-web-templ-migration | ./le repository check cross-package wiring | the population is the set of CALLER FILES, and it drops a same-directory one before it can count. `_has_cross_pkg_ref` (the retired `scripts/dev/validate.py` (current producer: `internal/le/doc/wiring/checks.go`)) skips every match whose parent directory IS the symbol's package, and only then does it apply the inversion that lets a cross-package `_test.go` wire a helper under `internal/test/`. A Go external test package lives in the SAME directory under a different package name, so `markupcheck.ExemptionDrift` read as unwired although `package markupcheck_test` beside it calls it four times. The symbol is correctly exported: unexporting it would stop that test compiling | not fixed. The finding is transient rather than harmless: `changed_files` reads the working tree against HEAD, so it fires only while the file is uncommitted and it reaches whoever edits the file next, never a later commit. Every `internal/test/` helper with an external test package carries the same trap. The fix belongs to the gate, which must count a same-directory file whose package name differs, and it is one condition ordered before the directory skip |
| 2026-08-15 | fixit-peer-process-event-filter | config keyword migration sweep | the population is the set of CONFIG CARRIERS, and the pattern read only config text that STARTS a line. `git grep -lE '^[[:space:]]*process[[:space:]]+[^[:space:]{]+[[:space:]]*\{'` matches a Go raw-string config block, where every config line does start its own line, and misses `b.WriteString("    process managed-reject-plugin { }\n")` (`test/managed/config-push-transactional.ci:240`), whose config exists only at runtime. `test/managed` ran 12 of 13 and the refusal named the retired keyword. Re-swept with the anchor dropped over tracked plus untracked-not-ignored files: the same class holds two more carriers, and neither spells the keyword at all. `GenerateConfig` (`internal/chaos/scenario/config.go`) and `contrib/netlab/ze/ze.j2` each declare a plugin and attach it to no peer, which `PeerScopedProcs` (`internal/component/plugin/server/delivery_graph.go`) now feeds nothing | the keyword is fixed and `test/managed` is 13 of 13. Both generated-config carriers now attach what they run, with the tokens `TestEveryStartupSubscriptionIsExpressible` maps, and `TestConfigGenAttachesEveryPluginItRuns` parses the chaos output against the real schema. NOT fixed and NOT measured: 31 of the 60 `test/interop/scenarios/*/ze.conf` declare a plugin and carry no `attach process` line. They are literal configs, so the anchored sweep did see them and had nothing to rename; whether each scenario asserts on delivery was not established here |
| 2026-08-15 | spec-web-htmx4-cutover | the retired `.claude/hooks/validate-spec.sh` (retired; now `internal/le/hookruntime/lifecycle.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> (current producer: `internal/le/hookruntime/lifecycle.go`) functional-test check | the population is the set of FUNCTIONAL TEST SUITES and the check knew one of three. It accepted `.ci` alone, while the tree holds 1662 `.ci`, 165 `.et` for the config editor, and 89 `.wb` for the web interfaces. `test/web` holds 89 `.wb` and ZERO `.ci`, so a web or editor spec could not name its real tests at all. The spec named a `.ci` that was never written instead: spec-web-htmx4-prepare shipped claiming `test/web/*.ci` while its test is `test/web/web-error-fragment.wb`, and a later phase corrected the wiring row to `.wb` while the functional row kept the fiction. A gate that cannot express the truth teaches the spec to lie, which is worse than no gate: it reads as coverage | FIXED 2026-08-15. The check accepts `.ci`, `.et` and `.wb`, each commented with the suite that runs it, and still rejects a spec naming none. Two fixtures asserted the old message text, not the behaviour, and were updated; `hook-fixture-check.py --only validate-spec` is 25/25 |
| 2026-08-15 | web-htmx4-cutover | spec validator, Wiring Test table | the Wiring Test warning counted `.ci` alone. the retired `.claude/hooks/validate-spec.sh` (retired; now `internal/le/hookruntime/lifecycle.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> (current producer: `internal/le/hookruntime/lifecycle.go`) had just been corrected the same day so the FUNCTIONAL TESTS check accepts all three suites (`.ci` daemon, `.et` editor, `.wb` web), and the wiring check one screen below it was left on the old test. A web spec naming its real `test/web/*.wb` files was told to reference `.ci` functional tests, in a directory holding 89 `.wb` and zero `.ci` | fixed. The wiring check reads an alternation over the three suffixes ci, et and wb, and its message names all three. 25/25 hook fixtures pass |
| 2026-08-15 | - | site-health test census | `under_skip_dir` (`sitefacts.py`, in the sibling gh-pages repository) treats a `vendor` path component AT ANY DEPTH as a dependency tree, and the retired `scripts/vendor/` (current producer: `internal/le/`) holds Ze's OWN build tools. `63064d92f` added `check_web_test.go` and `sync_web_test.go` there, so both first-party tests fell out of the census and `TestSiteFactsSkipDirs.test_no_tracked_first_party_test_is_dropped` (`site_health_render_test.py:540`) is RED at HEAD: the retired `ze-unit-pkg-test PKG=./scripts/dev` (current: `go test -race ./scripts/dev`) fails on it. Found while running that target for an unrelated tool added to the retired `scripts/dev/` (current producer: `internal/le/`) | not fixed. It blocks nothing in `spec-web-htmx4-cutover` and the fix is a decision about the census population: skip only a Go module vendor tree (one carrying `modules.txt`), or only `vendor/` at the repository root. The test names the defect precisely, so it is a one-predicate change once that is settled |
| 2026-08-15 | fixit-bgpls-withdrawal-functional-proof | repro tooling | `usage_error_signature` (the retired `scripts/dev/stress-repro.py` (current producer: `internal/le/stressrepro/run.go`)) tells bad arguments from a real failure. It refuses an invocation only when the output holds one of three USAGE_SIGNATURES AND the `Commands:` banner. A suite token that names a real subcommand of the same multi-command binary never reaches the runner's dispatcher. `stress-repro.py plugin` ran `ze-test plugin`, which IS `ze plugin`. Its refusal reads `unknown plugin subcommand: -v` under a `Plugin Subcommands:` banner, and neither string is in the guard's population. The tool answered `*** REPRODUCED on invocation 1 ***` for a run that started no test. Every `ze` subcommand name sits in that hole: plugin, firewall, traffic, static, iface | not fixed, found while a different spec re-measured an acceptance criterion. The correct spelling passes the sub-suite as ONE token, `stress-repro.py "bgp plugin"`, which then ran 24 of 24 green. `plan/journal/stale-artifact-reused.md` holds a row where the same verdict line was an artifact for another reason. The tooling spec it filed is `plan/future/spec-stress-repro-refuses-a-stale-binary.md` |
| 2026-08-18 | - | bgp prefix-list ge/le validation | The population is the inconsistent combinations of prefix length, `ge` and `le`, and one member is missing. `parseOneEntry` (`internal/component/bgp/plugins/filter_prefix/config.go`) rejects a `ge` above the family maximum, an `le` above it, and `ge > le`. It never compares an explicit `ge` against the prefix's own length. So `10.0.0.0/24 ge 8 le 16` commits, and then matches nothing at all: `evaluatePrefix` (`filter_prefix/match.go`) requires the route's length to be at most `le` and at least the entry prefix's own bits, which cannot both hold. The operator gets a filter that is present, valid and inert. The YANG cannot catch it either, since `yang/ze-filter-prefix.yang` constrains both leaves to `range "0..128"` with no family awareness, so even `ge 48` on an IPv4 entry passes the schema and is caught only by the Go check that does not look | not fixed. One comparison in `parseOneEntry` closes it, with the open question whether the family-aware bound also belongs in a YANG `must` so the editor refuses it before commit |
| 2026-08-18 | - | the spec/design write guards | the population is the TOOL, not the edit. `c_design_without_lsp` and every other check in `.claude/hooks/pretool-writeedit.py` (retired; now `internal/le/hookruntime/writeedit.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> reaches only the four write tools its matcher names in `.claude/settings.json` (Write, Edit, MultiEdit, NotebookEdit), so a spec changed through Bash -- a heredoc, `sed -i`, a `python3 - <<PY` script -- passes none of them, silently. `.claude/hooks/pretool-bash.py` (retired; now `internal/le/hookruntime/bash.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> carries no spec-edit guard: its only two mentions of a spec path or of `design` are a comment and a scratch-name list. Measured on this session, which edited `plan/spec-anomaly-5-entity-matrix.md`, `plan/spec-anomaly-6-as-enrichment.md`, `plan/spec-shared-machine-job-admission.md`, two journal classes and two docs entirely through python heredocs and was never once asked for the evidence the guard exists to require. Found by the main-work session, which hit the guard on an `Edit` to `plan/spec-ipsec-remote-access.md` and noticed its own earlier Bash edits had not | not fixed, and it needs a ruling rather than a patch. Detecting "this shell command rewrites a spec" is undecidable in general, so the choices are narrow: forbid Bash edits to `plan/` and `ai/rules/` outright and let the writeedit hook own them, or move the design-evidence check to a commit-time gate where the changed-file set is a fact rather than a guess. A guard a working style defeats by accident is not a guard, and auto mode instructs agents to prefer Bash for file changes, so the accident is the default |
| 2026-08-18 | - | the Go write gates | SECOND producer of the row above, and the one that cost more. FOUR checks in `.claude/hooks/pretool-writeedit.py` (retired; now `internal/le/hookruntime/writeedit.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) --> fire on a `.go` write and every one returns None unless the tool is Write or Edit: `c_require_design_ref` (a `// Design:` header), `c_require_related_refs`, `c_require_test_first`, and `c_check_existing_patterns`. Go written through a Bash heredoc reaches none of them, and auto mode instructs agents to prefer Bash for file changes, so the bypass is the DEFAULT route. Correction, 2026-08-18: this row first named `c_pre_write_go` as the check that routes an author to `ai/rules/go-standards.md` and thence to `docs/contributing/ze-go-style.md`. That was inferred from its name and is WRONG. Read at the producer, `c_pre_write_go` requires a session-state file to exist and to mention the claimed spec; it says nothing about standards, and it is bound to no rule point ("session state is a Claude-only rule"). The bypass is therefore WIDER than this row first claimed, not narrower. Measured: one session wrote `dpd.go`, `detector.go`, `command_registry.go` and `process.go` entirely through heredocs, met none of the four, and shipped a two-fact opening guard in `timedOut` (nil-or-not-awaiting, in one condition) plus a three-fact error condition in `ignoredNames`. The moment that session used `Edit` instead, the post-edit hook returned a lint pass, a missing-boundary-test warning and a `SplitSeq` diagnostic on its own line -- none of it available for the whole day before | partly closed, and the split is decided by what a tree can PROVE. `go_design_ref_problems` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`), `5e69bd98b`) now enforces the `// Design:` header at commit time, where the changed-file set is a fact and the tool that wrote each file neither matters nor is recoverable. The other three stay hook-only BY CONSTRUCTION rather than by omission: each gates on a session-state marker, which describes what the AUTHOR did and is not a property of the commit. `check_governed_doc_edit` (`fc37752ca`) shuts `plan/` and `ai/rules/` but not Go, and widening that pattern per tree misses the next tree. Still open: nothing checks that a `// Design:` target RESOLVES, which is the same failure one layer up and is what the dangling `renderCommandOutput` anchors are. The discovery half is closed separately by owner directive: the style guide is a blocking read in EVERY session, stated in `ai/INSTRUCTIONS.md` so it reaches both generated agent files rather than Claude alone, and the three-trigger gate in `go-standards` that said not to open it for an ordinary edit is withdrawn |
| 2026-08-18 | spec-rib-package-dead-surface | `./le repository check` cross-package wiring | THIRD consequence of the population named in the 2026-08-15 row, and the one that makes a backlog number lie. `check_cross_package_wiring` (the retired `scripts/dev/validate.py` (current producer: `internal/le/doc/wiring/checks.go`)) builds `go_files` from `changed`, and `changed_files` is `git diff --name-only HEAD` plus untracked. There is NO whole-tree mode: `./le repository tree-check` passes an explicitly EMPTY changed set, and `main()`'s own comment states it "runs the three tree-wide checks and neither changed-file check". So dead exported surface in a file nobody is editing is invisible in BOTH modes, permanently, rather than merely transiently. Measured: the check reported 21 findings and zero under `internal/component/bgp/rib`, although `IncomingRIB` and `OutgoingRIB` (`incoming.go`, `outgoing.go`) are exported types with no cross-package non-test caller at all. The cost is not the silence, it is what the count is then read as: `plan/future/spec-fixit-unexport-package-private-symbols.md` opens by saying the check "reports 467 findings", which is a snapshot of one large working tree and never measured the tree. That spec is at phase 7/8, so a drained-looking backlog is being read against a number that was never an inventory | not fixed. Recorded rather than repaired because the repair is a tooling change with a blast radius across every package, and Thomas scoped `spec-rib-package-dead-surface` to the rib package alone (2026-08-18). Two things follow for whoever takes the gate: a tree-wide mode for this check would turn 21 findings into an unknown and much larger number, so it needs a baseline like `./le tier check` has rather than a flag; and the unexport spec's Task should stop quoting 467 as a total |
| 2026-08-19 | - | `check_governed_doc_edit`, the shell-write guard over `plan/` and `ai/rules/` | The tier-two pattern `_GOVERNED_SCRIPT_WRITE` (`.claude/hooks/pretool-bash.py` (retired; now `internal/le/hookruntime/bash.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->) is ORDERED. It requires the interpreter token, then the governed path, then the write primitive, in that sequence in one command string. A command that writes the script FIRST and runs the interpreter LAST reverses that order, so the guard returns None although all three elements are present and all three are literal. Measured this session: `cat > "$(scripts/dev/session-scratch.sh (retired; current producer: `internal/le/`))/fix-journal-row.py" <<'PYEOF'` carrying `pathlib.Path("plan/journal/declared-format-contradicts-payload.md")` and `p.write_text(...)`, then `python3 "$(...)/fix-journal-row.py"` as the last line, edited a `plan/` file with no refusal. The same edit spelled inline as `python3 - <<'PYEOF'` was refused one command later, which is the fixture shape. This is NOT the variable-path case the docstring already concedes, and tier one is correct to stay silent because the redirect target is the scratch path. The fixtures agree with the code and so cannot see it: `governed-heredoc-literal-blocks` and `governed-heredoc-variable-blocks` (the retired `scripts/dev/hook-fixture-check.py` (current producer: `internal/le/`), `run_governed_doc_edit`) both open with `python3 -`, so only the interpreter-first order is covered | not fixed. The repair is to stop requiring one order: test the interpreter token anywhere in the command, and accept the governed path either before or after the write primitive, then add a fixture for the write-script-then-run order beside the two that exist. It was attempted in this session and the harness classifier refused the edit to the hook twice, which is the right default for a guard file. FIXED the same day once the owner said so. `_GOVERNED_INTERPRETER` is now tested separately and `_GOVERNED_SCRIPT_WRITE` accepts the governed path on either side of the write call, so the three elements match in any order. Two fixtures added to `run_governed_doc_edit`: `governed-write-script-then-run-blocks` for the shape that escaped, and `governed-write-script-without-interpreter-passes` so a heredoc holding that text but executing nothing is still free. Proven both ways, and the discrimination is the point: the OLD ordered pattern returns False on the new fixture's command, so it would have been red before the change. 27/27 in that group |
| 2026-08-19 | verify-scope-0-umbrella | `check_governed_doc_edit`, the shell-write guard over `plan/` and `ai/rules/` | THIRD row on this check today, and the one the row above's repair does not reach. `main` (`.claude/hooks/pretool-bash.py` (retired; now `internal/le/hookruntime/bash.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->) reads `tool_input.get("command")` and nothing else, so both tiers can only match text the command string itself carries. A script written to disk in one turn and run as `python3 <scriptfile>` in a LATER turn presents no governed path and no write primitive at all: there is nothing to match, in any order. The row above proposes making `_GOVERNED_SCRIPT_WRITE` order-insensitive, which repairs the same-command heredoc case and leaves this one open, because reordering a pattern cannot find elements that are absent. Read-verified from `main`, `check_governed_doc_edit` and both patterns; not reproduced, because reproducing it means editing a `plan/` file behind the guard the repository relies on. Reported by another agent, whose own spec edits took this route and ran `validate-spec.sh` never | not fixed, and NOT repairable by widening the pattern: the command string does not hold the fact. The guard's own docstring concedes undecidability for a variable path inside a payload, and this is the stronger case where there is no payload. Two routes exist and both are the owner's to pick, because each reaches every concurrent session: refuse an interpreter invoked on a script path under the session scratch dir unless the escape declares a reason, or move the enforcement off the command string entirely and have `validate-spec.sh` run from a PostToolUse Bash hook that inspects the tree. The blocked hook edit recorded in the row above needs the same decision |
| 2026-08-21 | - | `cmdBgpChildren` (`internal/component/bgp/plugins/cmd/peer/peer.go`), the empty registrations that stop `show bgp` ordering its whole subtree | the population is every command path under `show bgp`, and the list was built from `./le command list`. That inventory walks `AllBuiltinRPCs` and the streaming prefixes alone (`collect`, the retired `scripts/inventory/commands.go` (current producer: `internal/le/command/list/commandlist.go`)), so it reports neither the plugin commands the rpki, rs, adj-rib-in and healthcheck plugins register as plugin NAMES nor the `show bgp decode` / `show bgp encode` offline handlers `registry.MustRegisterLocal` owns. Ten live paths therefore kept resolving the parent's two column orders and its `summary` and `peers` aliases. A second hole sat under the same list: the registry resolves the string the OPERATOR typed and the dispatcher folds a selector out of the middle of it, so `show bgp peer detail` is not a prefix of `show bgp peer 192.0.2.1 detail` and every selector spelling resolved `show bgp` whatever the leaf declared. `test/ui/show-column-order-absent-unchanged.ci` exists to catch exactly that and was RED on HEAD; the phase that moved the registrations never ran the ui suite | fixed by registering at the SHALLOWEST path of each branch rather than at each leaf, which covers the selector spellings, the plugin commands and any leaf added later in one entry apiece. `TestChildCommandsDoNotInheritTheSummaryOrder` drives the branches, the leaves and the selector forms. The habit is the repair: a population is DERIVED from what the runtime resolves, never from an inventory that reports one of the three registries |
| 2026-08-22 | fixit-flowspec-protocol-name-drift | nft backend table ownership vs the FlowSpec bridge | the population is every table ze owns in the kernel, and the delete-before-replace guard covers only the ones named `ze_*`. `shouldDeleteTable` (`internal/plugins/firewall/nft/backend_linux.go`) returns false for any table without `zeTablePrefix`, and `buildTable` (`internal/plugins/flowspec-firewall/state.go`) names its table `flowspec` while its own doc comment one line above calls it `ze_flowspec`. Every sibling owner is inside the prefix: `ze_copp`, `ze_pr`, `ze_irr_iface`, `ze_ddos-local`, and config tables get it from `tableNamePrefix` (`internal/component/firewall/config.go`). So the flowspec table is never deleted before `applyTable` re-adds it, and its rules ACCUMULATE: measured in the QEMU VM on 2026-08-22, one announced route left two identical `ip daddr 10.1.0.0/24 th dport 80 meta l4proto 132 drop` rules after a second reconcile. The same exclusion means a withdraw never removes the table, and `readTables` (`internal/plugins/firewall/nft/readback_linux.go`) skips it too, so `show firewall` readback reports nothing about it | recorded, not fixed here. It blocks no AC of this spec: the announced rule is installed and does enforce. Renaming the table is one token and its own change: on upgrade the old un-prefixed `flowspec` table is outside the guard's population too, so nothing deletes it and stale drop rules survive the restart. FIXED 2026-08-23 by `spec-fixit-flowspec-table-never-swept`: the table is `ze_flowspec`, `RegisterTables` refuses an unprefixed name, and the sweep deletes the legacy name once so the upgrade strands nothing |
| 2026-08-22 | fixit-irr-empty-answer-clears-set | `TestContendingFunctionalTestsDeclareExclusiveGroup` (`internal/test/runner/exclusive_group_test.go`) | the population is the functional tests that share a node-wide resource, and it named four clusters: ddos-flood, cos-vlan, bfd-ports and ipsec-xfrm. The 12 `test/plugin/firewall-irr-*.ci` files contend the same way and declared no group. `ifaceTableName` is the Go constant `ze_irr_iface` (`internal/component/firewall/plugins/irr/sets.go`). The config-derived tables collide as `ze_wan` and `ze_lan`. No `.ci` can vary either name. `Apply` (`internal/plugins/firewall/nft/backend_linux.go`) lists the node's tables and deletes every `ze_*` table it is about to program. So one daemon removes a sibling's live table, and the sibling reads back terms it never wrote. Measured in a privileged container on 2026-08-22: run in parallel 2 of 12 failed, run serially 12 of 12 pass. The suite was green only while whoever ran it remembered `-p 1` | fixed at closure. A fifth cluster row selects `firewall-irr-*.ci` on `option=needs-linux:caps=net-admin` with minChecks 12, and each of the 12 files declares `option=exclusive:group=firewall-irr-nft`. The ratchet is red without the declarations, naming all 12 files, and green with them. The habit is the repair. A ratchet written as a list of known clusters covers no cluster its author had not met. So the session that MEETS the contention owns adding the cluster, and not only the workaround it used to get green |
| 2026-08-23 | - | `check_governed_doc_edit` (`.claude/hooks/pretool-bash.py` (retired; now `internal/le/hookruntime/bash.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->) | the guard's population is a shell write to `plan/` or `ai/rules/`, and it decides by matching `GOVERNED_TREE` and a write call in the COMMAND TEXT. A write whose path and write call live in a SCRIPT FILE is in that population and invisible to it: the command text is `python3 tmp/session/<id>/scratch/journal.py` and carries neither. Measured here rather than reasoned. Three rows landed in `plan/journal/` through such a script with no refusal, and the same content written as `python3 - <<'PY'` one command later was refused, which is the exact before-and-after the guard's own comment records for the 2026-08-19 ordered-pattern miss. Nothing was lost this time because no journal-specific check exists in `pretool-writeedit.py`, so the bypassed checks were the generic ones. A rule point edit taken by the same route would skip `c_rendered_rules` and `c_point_overwrite` | not fixed. The hook is outside this agent's package, and the repair is a design choice rather than a pattern edit: reading the script a command names turns a text guard into a file guard, and the interpreter can be handed a body on stdin, through a here-string, or by a path built from a variable. Reported to the main thread. Writing to a governed tree from a script is now a route agents take by default, because auto mode tells them to prefer Bash for file changes and a multi-line exact replacement is what a script is for |
| 2026-08-23 | record-answers-3-zero-alloc | the search over a changed payload's readers, read as the population a shape change reaches | the population is every reader of the payload, and the search covered the key that was REMOVED. Converting `show bgp rib` from a per-peer map to one envelope under `routes`, 120 `.ci` files mentioned the old keys and 6 parsed the payload. Five consumed the changed command. The sixth, `med-removal-before-decision.ci`, parsed `adj-rib-in` from `show bgp adj-rib-in`, a different plugin whose handler still returns that shape (`rib_commands.go`, `internal/component/bgp/plugins/adj_rib_in`), so converting it broke a passing test. The NEW name cost more. `extractRoutes` (`internal/component/lg/handler_ui.go`) held a branch reading `routes` for a different, already-flat producer, ABOVE the grouped-shape branch. Once `show bgp rib` answered `routes`, every RIB answer took that branch and came back untouched, no row carried `peer-address`, and the looking-glass graph drew nothing. No search for the old key reaches that file, and no list of the command's consumers does either, because it consumes a generic payload. The first repair normalized every element and broke `TestExtractRoutes/prefixes fallback`. Six red fixtures across the looking-glass and MCP suites, none in the package that changed, and the pre-push gate is what found them | `extractRoutes` now states each branch's prior contract, so a flat branch passes through what it cannot normalize and the grouped branch skips what does not carry the shape. The point `testing/the-affected-population-is-not-the-edited-population/a-shape-change-has-two-populations-old-name-and-new` requires the new name to be searched as well as the old, and requires the repair to earn its own pass over both populations |
| 2026-08-24 | fixit-qemu-runtime-kernel | the QEMU kernel guard's population in the retired `mk/test-integration.mk` (current producer: `internal/le/integration/gates.go`) | The file holds **13** real `qemu-run.py` invocations. **6** pass `--kernel $(ZE_QEMU_KERNEL)`, and the guard's population is exactly those 6: each of those recipes also calls `$(ze-qemu-kernel-guard)` and declares `: ./le build-artifacts host`. The other **7** pass no `--kernel`, call no guard, and declare no `: ./le build-artifacts host`, so they boot the stock Alpine 6.12.13-0-virt kernel while ze pins 7.2 (`internal/appliance/kernel.version`) and refuses to build anything below 7.0 (`validate_version`, `tools/kernel-builder/build.py` (retired; now `internal/le/deployment/hostkernel.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->). The 7 fall in four groups. **Developer reproduction tools**: `ze-qemu-debug`, `ze-qemu-shell`. **General suites**: `_ze-qemu-integration-test-impl`, which runs `./internal/plugins/firewall/nft/...` against the kernel's nftables, and `_ze-qemu-netns-test-impl`, which runs a firewall `.ci` subset under nft in netns. **Protocol interop with no stated reason**: `_ze-qemu-ldp-frr-test-impl`, `_ze-qemu-isis-frr-test-impl`. **Protocol interop WITH a stated reason**: `_ze-qemu-vrrp-keepalived-test-impl`, whose header names the symbols it needs (macvlan, bridge, veth), records that stock has all three, and says staying on stock keeps the target runnable with no ~30-minute kernel build. Only that last group says why, and its reason cites the isis-frr/ldp-frr labs as precedent while those two carry no reason of their own. `ze-qemu-debug`'s own header says its indices "come from the ze-qemu-test-all summary line", so it reproduces a 7.2 failure on 6.12.13; its neighbouring comment already names this exact failure mode for a different cause ("FAILS here while PASSING in the real ze-qemu-needs-linux-test run, which is the worst possible signal from a tool whose entire job is reproducing a failure"). `ai/rules/platform-linux.md` states "All six kernel-consuming targets use that one guard", which agrees with the 6 and says nothing about the 7 | not fixed. The spec's own recorded design decision says "Only the two targets this spec names move to `--kernel`", so widening the guard's population is a scope change its owner decides, not a same-session fix. The fix is one `$(ze-qemu-kernel-guard)` + `--kernel $(ZE_QEMU_KERNEL)` + `: ./le build-artifacts host` per target, and `TestQemuTargetsGuardTheStagedKernel` (the retired `scripts/evidence/qemu_kernel_wiring_test.go` (current producer: `internal/le/deployment/`)) derives its user list from the file so it needs no edit. **Counting it cost three passes across two sessions, and the method is the lesson**: a fixed-window search forward from each `qemu-run.py` line under-reported the population at four, because a recipe can put its guard many lines above its invocation. The count is right only when you search UPWARD from each invocation to its own target definition line |
| 2026-08-24 | spec-website-asciinema-terminal-demos | the retired `scripts/dev/spec_doc_anchors.py` (current producer: `internal/le/doc/wiring/sources.go`), its `CODE_PREFIX` population | `spec_source_files` keeps a path only when `path.startswith(CODE_PREFIX)`, and `CODE_PREFIX` is `("internal/", "cmd/", "pkg/", "the retired scripts/ tree (current producers: internal/le/, internal/test/, and internal/appliance/)", "test/", "the retired mk/ tree (current producers: internal/le/)", "rfc/")`. `website/` and `demos/` are absent, so no file in either tree is ever looked up, on either edge. The mention edge has real anchors in both: `docs/contributing/gh-pages.md` names `website/tools/extract-plugin-registry.py` and `render-plugin-catalog.py`, `docs/architecture/testing/test-health.md` names `website/tools/render-test-health.py`, `docs/guide/command-catalogue.md` and `docs/guide/command-reference.md` both name `website/tools/render-command-equivalents.py`, and `docs/guide/irr-filtering.md` names `demos/terminal/irr-filter/ze.conf`. Met while answering row 16 for a spec whose `## Files to Modify` lists 12 paths: `--json` derived exactly one of them, the retired `mk/build-terminal-demo.mk` (current producer: `internal/le/`) | recorded, not fixed. This spec's 12 files carry no `<!-- source: -->` anchor of their own, so the under-reach costs it nothing and it does not block phase 1. Widening `CODE_PREFIX` would change the verdict of every spec touching `website/`, which is a population the prefix's owner decides |
| 2026-08-24 | - | one wire-byte change, landed short three times | **The population here is not a GATE's, it is a FIX's, and the same rule governs it: the affected population is not the edited population.** One RFC 4486 correction, four octets of a Cease/MaxPrefixes NOTIFICATION changing from the count that crossed a maximum to the configured maximum itself, took THREE commits to land because each one found only the carriers it happened to be looking at. Round one took the Go tests and shipped; HEAD went red on three `.ci` fixtures. Round two took the fixtures and shipped; HEAD went red on `test_the_ci_still_pins_the_cease_subcode_bytes` (the retired `scripts/dev/rfc_requirements_test.py` (current producer: `internal/le/`)), a PYTHON test that greps one of those `.ci` files for the literal hex. Every round ended believing the job was finished, and every next carrier was found by something else going red rather than by looking. The population spans three languages and no tool in this repo enumerates it: a Go assertion on a struct field, three `.ci` fixtures pinning a hex string, and a Python test asserting on the TEXT of one of those fixtures. What made it invisible is that each carrier is reached by a different search. A grep for the symbol finds the Go one. A grep for the command finds none of them. Only a grep for the literal `00000003` finds all four, and that search is obvious in hindsight and was not run on day one because after round one nothing looked outstanding | not a code fix; the change is landed and correct as of `4f2ebb9d4`. The row exists because this is the rule in `ai/rules/testing.md` that this very class file is the rationale for, violated three times in one night by the session that moved that rule's originating case into this journal the same morning. The practice that would have cost one command: **when a change alters a VALUE that appears on a wire or in a file, grep for the literal old value across the whole tree before claiming the change is landed** — not for the symbol, not for the command, for the bytes. A symbol search finds the producer; only a value search finds the pinners, and the pinners are in whatever language happened to be convenient |
| 2026-08-25 | - | `Server.ReloadConfig` (internal/component/plugin/server/reload.go), whose participant population is `pm.AllProcesses()`, meaning plugins already RUNNING | A config root that a reload ADDS is verified by nothing. A daemon that boots with no `traffic` section never starts the traffic plugin, so that plugin is absent from `AllProcesses()`; the reload finds no affected plugin, takes the early return, and prints `sighup reload complete`. Measured: SIGHUP-ing such a daemon onto a config carrying `backend vpp` with `qdisc hfsc` was ACCEPTED with no verify and no apply, while `ze config validate` refuses the same bytes with `qdisc hfsc: not supported by backend vpp` and `trafficvpp.Verify` refuses them in-process. The commit-time guard is skipped for exactly the case it exists for, a subsystem configured for the FIRST time. The three tests work around it by booting a no-op `traffic { control { backend tc } }` section, which makes the plugin a participant | Not fixed, needs its own spec. **CORRECTED 2026-08-26, and the correction is the point: the SYMPTOM above is measured, the MECHANISM in the Surface cell is NOT.** Written the same day, it named `pm.AllProcesses()` as the cause. Reading further does not support stating that as fact. `reloadConfig` calls `autoLoadForNewConfigPaths` IMMEDIATELY ABOVE that loop, for exactly this case -- its own comment says "when a user adds `fib { kernel { } }` to their config, the fib-kernel plugin needs to start before we can send it config" -- and it matches on `registry.ConfigRootsMap()`, where traffic declares `traffic/control`. So the plugin ought to start and ought to become a participant. Three candidates remain and none is separated: autoload never matched the added path, or it started the plugin too late to join this transaction, or the reload path consults only the RPC verify of running processes and never the IN-PROCESS verifier. The third fits both observations at once without needing autoload to fail: `traffic` registers `InProcessConfigVerifier: verifyTrafficConfig`, `VerifyPluginConfig` is reached by `runValidation` (`internal/component/config/cli/cmd_validate.go`) and by `ValidateSemantics`, and no caller of it was found on the reload path -- which is why offline validate refuses the bytes the daemon accepts. One run separates them: log whether `autoLoadForNewConfigPaths` fires for `traffic/control` on that reload. Whoever takes the spec starts there, and starts from the symptom rather than from this row's first guess |
| 2026-08-26 | - | the STE prose gate and its whole-tree report (the retired `scripts/dev/ste_check.py` (current producer: `internal/le/ste/ste.go`)) | The port of this gate to `le` found the same `except OSError: continue` shape as the ci-sleep row above. `ratchet` skips a changed document that it cannot read. The document contributes no habit count, so the ratchet reports no growth. Thus, the gate becomes green because it did not inspect the document. `main` skips unreadable documents in the whole-tree review. Each skipped document lowers `documents-reviewed` and every published habit total. A fixture had one changed file with mode 000 and three new hedges. It answered `ste_check: OK -- no habit grew in 0 changed document(s)` and exit 0. Root-owned and mode-000 files exist in this checkout. `plan/journal/container-build-leaves-root-owned-scratch.md` records the class that produces them | fixed in the port only. `Ratchet` (`ste/ratchet.go` in the former top-level tool tree) and `reviewFiles` (`ste/actions.go` in the former top-level tool tree) answer exit 1 and name the unreadable path. A file that vanished since it was listed is still skipped. Several sessions share this checkout, and a spec closure deletes files. The script keeps the fail-open behavior until the swap step of the le migration. `TestScriptSteStillPassesAFileItCannotRead` (the retired `scripts/dev/ste_parity_test.go` (current producer: `internal/le/`)) verifies that behavior. Thus, the test becomes red if somebody repairs the Python |
| 2026-08-26 | - | the repository gate's three tree-wide checks (the retired `scripts/dev/validate.py` (current producer: `internal/le/doc/wiring/checks.go`)) | The port of this gate to `le` found the `except OSError: continue` defect again. `check_source_anchor_line_numbers`, `check_source_anchor_stale_paths`, and `check_spec_ac_completeness` each skip a file that they cannot read. Thus, the file contributes no finding, and a lower count looks like a pass. The page does not say that the check skipped a file. A fixture had one mode-000 document with a stale anchor. The result was `./le repository check: all checks passed` and exit 0 | fixed in the port only. `readLines` and the two `os.ReadFile` calls in `repository/repository.go` in the former top-level tool tree return the read error to the caller. `Run` then stops, so an unreadable population causes exit 2 instead of a green result. The script keeps the fail-open behavior until the swap step of the le migration. `TestScriptRepositoryStillPassesOverAnUnreadableDocument` (the retired `scripts/dev/tier_parity_test.go` (current producer: `internal/le/`)) verifies that behavior. Thus, the test becomes red if somebody repairs the Python |
| 2026-08-26 | - | the module-tier gate's import scan, `collect_edges` and `find_engine_dirs` (the retired `scripts/dev/dep_audit.py` (current producer: `internal/le/`)) | The port of this gate to `le` found the defect. Both walks use `except OSError: continue` for a Go file that they cannot read. Thus, the file's import edges are not recorded. All five checks read those edges, and each reports a violation only when an edge exists. An unreadable file under `internal/core/` can therefore hide its own upward import from the direction gate. An unreadable file can also hide the dependency that determines an engine's tier. A fixture put an import of `internal/component/thing` <!-- doc-links: ignore (illustrative path, never a real package) --> in mode-000 `internal/core/bad/uses.go` <!-- doc-links: ignore (illustrative path, never a real file) -->. The gate reported no upward import | fixed in the port only. `CollectEdges` and `FindEngineDirs` (`tier/tier.go` in the former top-level tool tree) return the read error to the caller. `Check` stops, so an unreadable population causes exit 2 instead of a shorter violation list. The script keeps the fail-open behavior until the swap step of the le migration. `TestScriptTierStillPassesOverAnUnreadableGoFile` (the retired `scripts/dev/tier_parity_test.go` (current producer: `internal/le/`)) verifies that behavior. Thus, the test becomes red if somebody repairs the Python |
| 2026-08-26 | - | the spec acceptance-criteria check, `check_spec_ac_completeness` (the retired `scripts/dev/validate.py` (current producer: `internal/le/doc/wiring/checks.go`)) | The port of this gate to `le` found the defect, but neither implementation fixes it. A fix changes the verdict for most of `plan/`, so it requires a scope decision. The walk enters a table only after a line that contains `### Acceptance Criteria`. A `## Acceptance Criteria` heading does not contain that string. The 2026-08-26 measurement covered 230 specs. Forty-one specs had the three-hash heading, and 223 had the two-hash heading. Thus, the check reads a minority of the population. All 34 rows that it reaches are in implementation-audit tables. A spec with the common heading passes even when its criteria table has an empty demonstration column. `AC_ROW_RE` narrows the population again because it requires a four-column row | the port deliberately reproduces both restrictions. A wider population changes what `./le repository tree-check` reports about 189 specs. The gate-migration commit must not make that verdict change. `TestOnlyAnInProgressSpecsAcceptanceCriteriaAreRead` (`repository/repository_test.go` in the former top-level tool tree) pins the current section boundary. Thus, a later widening is a visible edit instead of drift |
| 2026-08-26 | le-is-a-ze-binary | CLI grammar call-site feeder, `demoLaunchHits` (`internal/le/cligrammar/cligrammar.go`) | The native cutover deleted every terminal-demo `.sh` file and made `.tape` and `.cjs` the checked-in definitions. The feeder still globbed only `*.sh`, read zero call sites, and failed its population floor. Its existing token parser then read `Type "ze --plugins piped into ze pipe count"` as no command and, after the carrier fix, treated the pipe after a flag-only `ze` invocation as the command word. | The feeder discovers current demo definitions, unquotes Tape `Type` commands, stops flag-only invocations at shell boundaries, and carries a floor for the 19 current definitions. |
| 2026-08-26 | le-is-a-ze-binary | source activity history, `collectActivity` (`internal/le/sourcerewrite/activity.go`) | The collector passed Git `--since=YYYY-MM-DD`. Git interpreted the date at a time of day, so a one-day report omitted commits from earlier on its start day. A fixture committed four Go lines at noon and the page reported zero lines and zero commits. | The lower bound is now an explicit UTC midnight, `YYYY-MM-DDT00:00:00Z`; the dashboard fixture fixes the commit and render clock to that day and requires all four lines. |
| 2026-08-28 | le-is-a-ze-binary | the zero-interpreter cutover, whose population is first-party development tooling | The deletion sweep classified `test/exabgp-compat/` as Ze tooling and removed 53 Python process fixtures, including `etc/run/api-health.run` (deleted, no successor) <!-- doc-links: ignore (deleted 2026-08-28 by eae282592 with no replacement) -->. Those files are external ExaBGP interoperability subjects. Removing them reduced the plugin API behavior available to the compatibility harness, while the 42-case encoding run still passed and hid the loss. | Restored all 53 fixtures byte-for-byte. `firstPartySource` now names the narrow `test/exabgp-compat/` exclusion, and `TestExaBGPCompatibilityProcessFixturesRemainExternalSubjects` requires the harness, wrapper, and API health process to remain. |
| 2026-08-28 | - | the per-flavor scope of `./le verify lint run` (`internal/le/verify/lint.(*Runner).plan`) and `internal/component/support/disk_unix.go` | A flavor lints only the packages that hold a file no EARLIER flavor selected, so a package whose only non-Linux file is shared with the host pass never reaches the BSD flavors. `disk_unix.go` is `//go:build !windows`, the host pass claims it, and `internal/component/support` is therefore absent from the freebsd, openbsd and dragonfly scopes. The package does not compile for FreeBSD at all: `stat.Bavail` is `int64` there and `int64 * uint64(stat.Bsize)` is a type error at `disk_unix.go:42`. `GOOS=freebsd go vet ./internal/component/support/` reports it; the gate reports `0 issues` over 3 packages | not fixed: found while sweeping goconst out of the long-tail packages, and it blocks no gate, because the flavor that would catch it never sees the package. Row only |
| 2026-08-29 | - | the session-start summary line (`internal/le/hookruntime.hookSessionStart`) beside the spec inventory (`internal/le/spec/status`) | The one line every session reads under-reported its own population: 231 specs, counts summing to 229. The hook held its own list of seven status names and, unlike every reader in `specstatus`, gave it no default, so a status outside the list was counted nowhere. The two it hid were `done` specs that were never closed, which is the exact state the line exists to surface. The same defect had already been found and fixed once in `specstatus.summaryOrder` on 2026-08-22, over the same two specs; that fix did not reach the second copy, because nothing tied the two together | pointed the hook at a new `specstatus.StatusPhrases`, so one vocabulary and one print order serve both surfaces. It is separate from `Collect` because `Collect` calls `gitDate` per spec, which is 231 processes, and a session-start hook needs no date. Three tests, each proven to fail when the sorted tail is dropped. General practice: a partial list of a vocabulary is safe ONLY where every reader has a default, which is what makes `specstatus`'s own lists correct and this one wrong. A list that decides what gets COUNTED must either be closed over the population or append what it does not name |
| 2026-08-29 | traffic-vpp-deferred-reply-timeout (review) | the QEMU integration package list, `integrationPackages` (`internal/le/qemu/alltests.go`) | `TestEveryIntegrationPackageIsNamed` derives its population from `//go:build integration`, so a package whose tests carry only `//go:build linux` is invisible to it and enters the list by hand or not at all. `./internal/plugins/firewall/vpp/...` was named by hand and survived the `mk/` retirement; `./internal/plugins/traffic/vpp/...` was in `mk/test-integration.mk` and was not carried over, so `TestVppReplyTimeoutBounds` and `TestNewGovppOpsBindsReplyTimeout` ran in no VM. The gate that exists to catch exactly this could not see them, and its own comment named the surviving half as "the standing example" without noticing the missing one | fixed, one line: the traffic backend is named beside the firewall one. The comment now names BOTH as the standing examples and says why the derived gate cannot find them, so the next reader knows a hand-maintained entry is the only route for a linux-only backend. The tests were not wholly unexecuted -- `planUnitCached` runs `go test` over the tree and CI runs it on ubuntu -- so what was lost is the deliverable the spec named, not the coverage. The general practice: a gate whose population is DERIVED from one signal is blind to a member that carries a different one, and every such member must be listed by hand, which makes the list the thing that rots |
| 2026-08-29 | spec-srv6-ebgp-egress-filter | BGP egress, the Prefix-SID attribute (code 40) | RFC 8669 Section 8 forbids the attribute crossing into another AS without configuration, and ze had no egress gate at all. The one code-40 removal that existed was a side effect of a DIFFERENT requirement: applyFactsNextHop (internal/component/bgp/reactor/peer_forward_facts.go) strips it when the next hop changes, for RFC 9252 Section 3.3, and returns at nhModeNone. So the population it covered was "eBGP peers whose next hop ze rewrites", which excludes every route-server client by construction (RFC 7947 Section 2.2.2 keeps their next hop) and every plain eBGP peer with next-hop unchanged. Five egress rails can put code 40 on a wire and only that sub-case of one was covered | fixed by giving the rule its own site, prefixSIDAllowedTo (internal/component/bgp/reactor/forward_prefix_sid.go), that every rail asks -- the shape localPrefAllowedTo already set for RFC 4271 Section 5.1.5 after its rails disagreed. General practice: an attribute prohibition owed to an RFC MUST be enumerated over the RAILS that can emit the attribute, never over the one rail where a removal already happens to exist; and a removal made for requirement A is not coverage of requirement B even when the bytes removed are the same |
| 2026-08-29 | improve-7-yang-handler-gate | config claim completeness, `claims.Audit` (`internal/component/config/claims/claims.go`) judged against `Server.reloadConfig` (`internal/component/plugin/server/reload.go`) | A-3 assumed operator config reaches a consumer by exactly two routes, a plugin's `ConfigRoots` and a hub `Schema.Handlers` prefix. A gate built on that union reported 5 of 36 top-level roots unclaimed, 172 leaves between them: `plugin`, `pppoe`, `storage`, `system` and `telemetry`. Every one of the five IS delivered, by a third route the model had no name for, a component reading the config tree directly: `ExtractPluginsFromTree` (`internal/component/config/loader.go`), `pppoe.ExtractParameters` (`internal/component/l2tp/pppoe/config.go`), `extractSmartConfig` (`cmd/ze/hub/main_system.go`), `ExtractTuningFromMap` (`internal/component/config/system/`) and `extractTelemetryConfig` (`internal/component/telemetry/exporter/server.go`). A completeness gate whose population model misses a delivery route publishes its own blind spot as a defect in the product, and the cheapest way to make it green is to widen the exception list until the blind spot is invisible | fixed by making the exception carry the evidence instead of the silence. Each of the five is an allowlist entry that NAMES its consuming symbol, so the file records the third route rather than suppressing five findings, and `auditAllowlist` refuses an entry with no reason WITHOUT suppressing its subtree, which is the direction that fails closed. Proven by mutation: clearing the `storage` entry's reason turns `TestClaimAllowlistReasons` red AND leaves the subtree still reported unclaimed. The gate ships green over 36 roots and 73 claims and sits in both `StagesForMode` populations (`internal/le/verify/engine/stages.go`). General practice: derive a completeness gate's population from every CONSUMER, and where you cannot enumerate them, make each exception name the consumer, so the list is evidence a reader can re-derive rather than a place to put things that are inconvenient |
| 2026-08-30 | - | the CLI grammar gate's flag feeder, `checkFlagRegister` (`internal/le/cligrammar/flags.go`) against a flag no `flag.NewFlagSet` declares | The feeder finds a command's flags by walking `flag.NewFlagSet` declarations, so a flag a handler reads out of `args` by hand is invisible to every one of its four findings. Three are already known: `dataDump` (`internal/component/config/cli/config_data.go`) scans its arguments for `--strip-private` inside a local-data handler, which is the exact shape F3 exists to refuse; `runPipe` (`cmd/ze/ze_core_pipe.go`) reads `--json` with `slices.Contains`; and `explain` (`internal/plugins/explain/main.go`) switches on the token in its own loop. A fourth kind hides differently: `cmdListSchema` built its set as `flag.NewFlagSet("schema "+strings.ToLower(header), ...)`, so a computed set NAME hid two real `--json` violations on `schema methods` and `schema events` until a reader found them by hand. The gate counts what it cannot place, 11 set names and 69 declarations on a set built elsewhere, so the hole is published rather than silent, but a count is not coverage | not fixed, and not characterised further. Closing it needs a second population, over hand-read tokens, and a hand-read token carries no set name to attribute it to a command, which is why the first pass did not attempt it. Found while removing the rendering flags the feeder does see |
| 2026-08-31 | - | `CheckPipeFlags` (`internal/component/command/grammar/flags.go:278`) and its feeder `checkFlagRegister` (`internal/le/cligrammar/flags.go:481`) against the rendering-flag ban | The gate refuses a rendering flag on a command the pipe layer already serves, and it reads the command path from a `flag.NewFlagSet` call site. Three shapes of command never enter that population, so the ban is unenforced on them and no finding is possible. `ze doctor`, `ze support`, `ze explain` and `ze skills` parse `--json` with a hand-written switch and call `flag.NewFlagSet` never, so the AST scan cannot see them. `ze interface show`, `routes` and `neighbors` name their flag set with a constant rather than a string literal, so `stringArg` fails and `surface.UnresolvedSetNames++` (`flags.go:314`) drops them silently. `ze bgp decode` and `ze plugin test` resolve as literals but yield the paths `decode` and `plugin-test`, whose first token matches no registered root in `offlineZeCommand` (`flags.go:522`), so `inSurface` is false. `flagRegisterDebt` (`internal/le/cligrammar/flagdebt.go:41`) is EMPTY, which reads as a clean surface and is instead the absence of a finding to forgive. The landing commit `f0ce6a8d4` already recorded two members of this class, `schema methods` and `schema events`, missed because `cmdListSchema` built its flag set name as a computed value; that was treated as two commands rather than as a shape | not fixed. Found while syncing the wiki, and it blocks nothing there: the nine commands still parse `--json` in live code, so a page documenting them is correct today. The class is that a gate reading a call site measures the call sites it can parse, never the command surface it claims to cover. A population derived from the registered command tree, with an explicit unresolved count that FAILS rather than increments, would close all three shapes at once. The silent `UnresolvedSetNames` counter is the part that hides it |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `./le rfc discriminate-record` revert route, `revertBody` (`internal/le/rfc/discriminate_action.go`) against `coaListener.isAllowedSource` (`internal/component/l2tp/plugins/authradius/coa.go`) | The revert route cannot prove any tag whose producer runs on a background goroutine, and the refusal does not say so. `revertBody` replaces the producer with `panic(...)`, which is right for a producer the test calls on its own goroutine: the panic surfaces as that test's failure. `isAllowedSource` is reached from `coaListener.serve`, a goroutine `newCoAListener` starts, so the panic kills the whole test binary before any test name is printed. The gate then refuses correctly and for the correct reason -- "the run went red under the break, and its output does not name TestCoASourceFilterDiscardsWhenNoServerResolved, so the failure is not this unit's" -- but the author reads it as a bad producer choice rather than as a route that structurally cannot reach this shape. None of the three escapes fits either: the producer is neither `foreign-producer`, nor `declaration-only`, nor `generated-producer`, it is ordinary Ze code with a real test. So a conformant author has no way to record a proof and no honest way to record an escape | recorded, not fixed. RFC5176-6.1-1 was added in both polarities with no discrimination record, and `./le rfc check` will name both units until one exists. The proof itself was OBSERVED by hand before the tag was written: disabling the empty-list early return reddened `TestCoASourceFilterDiscardsWhenNoServerResolved` with its own message, and the three neighbouring discard tests were each reddened by breaking their own guard. What is missing is the recorded artifact, not the evidence. The `mutant` route from a gomu report is the other way in and was not run here. A fourth escape, or a revert body that returns a poisoned value rather than panicking for a producer with a non-error signature, is what would close the shape |
| 2026-08-30 | - | the `.ci` coverage gate, `checkCLIHandlerCoverage` (`internal/le/repository/repository.go`), against the CLI verbs registered under `internal/component/bgp/plugins/` | The gate reports a CLI command that no `.ci` names, and it cannot see one a BGP plugin registers, for two independent reasons either of which is sufficient on its own. `CLIPaths` lists `internal/component/cli/`, `internal/component/cmd/` and `internal/plugins/`, so no file under `internal/component/bgp/plugins/` is ever read, which is every verb the BGP plugins own. And the extractor `RegisterPattern` is `MustRegister\w+\(\s*"([^"]+)"`, while the announce verb registers through `pluginserver.RegisterRPCs` with `RPCRegistration{WireMethod: ...}`, a call the regex cannot match. Measured on that verb: `init()` (`internal/component/bgp/plugins/cmd/announce/announce.go`) registers seven handlers, none is exercised by any `.ci`, and the gate has never reported one of them. The whole CLI exercise in the corpus is four `ze withdraw help` lines in `test/ui/test-withdraw-forms-are-separate-commands.ci`, which assert usage text with no daemon behind them. A path-prefix population and a single-call-shape extractor each look complete from the inside, and a gate carrying both publishes silence as coverage | not fixed, and deliberately so. Widening the population lights up coverage debt across every BGP plugin at once and lands it as ISSUE on whichever session commits next, so the owner gave it its own spec, to run after the announce work that found it. The two halves are owed together: repairing `CLIPaths` alone still matches nothing while the extractor knows only `MustRegister`, which is a green that proves less than it appears to |
| 2026-09-01 | rfcgate-6-supported-extraction-signoff | `./le rfc discriminate-record` unit route, `observationRunner.argv` (`internal/le/rfc/discriminate_observe.go:284`) against `internal/core/network/ttl_integration_linux_test.go` | The unit route cannot prove any tag whose carrier sits behind a build tag, and the run reports success rather than a refusal. `argv` builds the command with `o.toolchain.GoTest(gotoolchain.TestOptions{}, ...)`, and that zero value leaves `Tags` empty, so the compile uses `Toolchain.TestTags()` alone: the core tag plus the feature manifest, never `integration`. A file guarded `//go:build integration && linux` is then not compiled into the run, `-run '^TestX$'` matches nothing, and `go test` prints `testing: warning: no tests to run` and EXITS 0. Measured on this checkout: the four TTL tests pass in 0.235s under `-tags integration` and vanish without it, exit 0 either way. `observationRunner.attributes` never sees a `--- FAIL:` line because there was no failure to see, so the break reads as one the test does not discriminate. The sibling row above is the same class through a different mechanism, and the same three escapes miss for the same reason: the producer is ordinary Ze code with a real test, so it is neither `foreign-producer`, nor `declaration-only`, nor `generated-producer` | worked around, not fixed. RFC5082-3-1, RFC5082-3-3 and RFC5082-3-4 are proven in both polarities on a NEW carrier the runner does reach, `internal/core/network/ttl_gtsm_linux_test.go` guarded `//go:build linux` alone, and six verified records sit in `rfc/discrimination/rfc5082.json`. That leaves the four assertions in `ttl_integration_linux_test.go` untagged and unprovable by this tool, and every other build-tagged test in the tree in the same position. The fix is a `Tags` field on the request that `argv` passes through to `TestOptions`, derived from the carrier file's own `//go:build` line rather than authored, so a tag cannot be claimed for a file that does not carry it. Until then a build-tagged carrier must not be chosen for a tag that owes a record |
| 2026-09-01 | - | `./le rfc discriminate-record` revert route, `disableBody` and `observationRunner.judgeRed` (`internal/le/rfc/discriminate_action.go`, `discriminate_observe.go`) against `internal/component/l2tp/plugins/authradius/rfc5176_walk_test.go` | The revert route cannot prove any producer that runs on a goroutine the test does not own. The break replaces the body with `panic("BUG: ...")`, and a panic on a background goroutine takes the whole test binary down before the testing package can attribute a failure, so the output carries a raw stack and no `--- FAIL:` line naming the unit. `judgeRed` requires that line, so the proof is refused. The CoA listener is the shape: `coaListener.serve` (`coa.go:95`) reads the socket in its own goroutine and calls `handlePacket`, so every one of the 30 `RFC requirement:` tags in the RFC 5176 walk test names a producer reached only from there. This is not the build-tag row above: the carrier compiles, the test runs, and the break is applied; what fails is the ATTRIBUTION of the red. None of the three escapes fits either, because the producer is ordinary Ze code with a real test | fixed the same night. `observationRunner.killedByTheBreak` accepts a unit run that DIED carrying the halt's own marker, on the reasoning the interop branch beside it already uses: `argv` passes `-run ^<unit>$` over one package, so no other test could have been running. Three facts keep it honest and all three already existed: the run is selected to one unit, the panic text is the break's own, and `requireCleanGreen` plus `requireProducerReached` have already shown the unit passes clean and executes this producer. The revert route only, since a mutant substitutes an expression and never halts. The non-halting alternative was REJECTED: `revertBody` already states why a zero return is the silently-wrong value `ai/rules/principles.md` forbids. All 30 RFC 5176 tags are proven, and the gate caught two wrong producer guesses on the way -- `AppendTextAttr` and `applySubscriberCoA` are each never executed by the test that was about to claim them |
