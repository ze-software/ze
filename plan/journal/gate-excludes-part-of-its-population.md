| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-12 | - | bgp reload peer reconcile | `reconcilePeersJournaled` (`internal/component/bgp/reactor/reactor_api.go`) diffs the config-derived peer list against EVERY entry of `r.peers`, and a dynamic peer is not in the config-derived list: it is created at runtime from a group template when a connection in the range arrives (`createDynamicPeer`, `reactor_dynamic.go`). Its key therefore reads as "no longer configured" and it lands in `toRemove`, so every established dynamic session is torn down on every reload. `SetDynamicGroups` (`reactor_dynamic.go`) already keeps the dynamic peers whose group survives, which is the population this diff was supposed to leave alone. Measured, not reasoned: an established peer with `IsDynamic` set is absent from `r.peers` after one `Reload()` whose config carries a different peer | not fixed. It does not block this spec, whose goal is that a peer-settings edit APPLIES on reload: for a dynamic peer the edit arrives, disruptively, through the teardown and the next connection's fresh template. It also gates the recorded race in `plan/deferrals/fixit-dynamic-peer-settings-unlocked-read.md`, which needs `applyHotSwappableSettings` to run on a dynamic peer and cannot while the peer is removed instead of swapped. Both want one decision about what a reload owes a runtime-created peer |
| 2026-08-11 | runtime-kernel-carries-every-tunnel-kind | config validation mandatory check | `ze config validate` prints `mandatory field "kind" is missing` once per tunnel in a config it declares valid in the same breath. `ze-iface-conf.yang` models the discriminator as `choice kind` with `mandatory true` inside `container encapsulation`, and every tunnel in both configs measured selects a case. Measured on two configs: the one in the new `test/plugin/iface-tunnel-kinds.ci` warns 9 times and the one already shipping in `test/reload/test-tx-iface-tunnel-create.ci` warns 8 times, one per tunnel in each. So the check reads a satisfied mandatory choice as an unset mandatory leaf, and every conforming tunnel config in the tree carries the warning. The producing function was not read, so which check emits it is unestablished | not fixed. It does not block this spec: validate exits 0 and the daemon applies the config. The warning has been on every tunnel config ze ships for as long as the choice has been modeled this way |
| 2026-08-11 | fixit-config-validators-bypassed-at-startup | config validation walk | `yangSectionsToValidate` (`internal/component/config/cli/cmd_validate.go`) is a hand-written list of 17 names beside a schema that derives 36 top-level sections. It is wrong in BOTH directions. Six of its names -- `web`, `ssh`, `dns`, `looking-glass`, `mcp`, `managed` -- are not top-level sections at all: they sit under `environment`, which the list omits, so `tree.GetContainer` returns nil and those six iterations have never validated anything. And 25 real sections are missing, two of which carry live `ze:validate` bindings nothing else covers. The measured consequence: `test/parse/redistribute-invalid-source.ci` asserts exit 0 for `import rip` and explains it as "list key values are not validated by ze:validate at parse time", which is FALSE -- `Validator.validateListKey` does validate list keys, and a probe fires `"rip" is not a registered redistribute source` on that exact input. The test documents the section list's hole as if it were the validator's design | not fixed here. The spec implements the `LoadConfig` call against the list AS IS, because that scope measures green over 1387 parsed shipped configs. Widening the list is its own work and is gated on the three defects in the row below |
| 2026-08-11 | fixit-config-validators-bypassed-at-startup | config validators vs registries | three validators answer from a registry that does not hold what they are asked about, and all three are inert only because the section list never reaches them. `AddressFamilyValidator` (`internal/component/config/validators.go`) reads `registry.FamilyMap()`, whose own doc comment says it holds "only plugin-registered families (used for decode routing)" and excludes `builtinFamilies`; measured in a full-tag binary, `FamilyMap` is 19 names WITHOUT `ipv4/unicast` while `AllFamilies` is 23 WITH it. `TestAddressFamilyValidator_Validate` is green only because it resets the registry and registers a fake plugin claiming `ipv4/unicast`. Widening the walk to `bgp` and `redistribute` would therefore refuse 742 + 33 family sites across 687 shipped configs, which is essentially every BGP config in the tree. Same shape: `send-message-type` and `receive-event-type` read registries `RegisterPluginSendTypes` populates from `PeersFromTree` and `NewServer`, both strictly LATER than the point the startup-validation fix would call them, so they fail closed on valid input at exactly that moment | not fixed. Each is a separate defect with its own blast radius, and all three must land before the section list can widen |
| 2026-08-11 | fixit-forward-rail-initial-sync-ordering | bgp forward rails | the route-server rail's ordering gate covers the items a destination is owed through OVERFLOW and not the items it is owed through the pool CHANNEL. `overflowPending` (`internal/component/bgp/reactor/forward_pool.go`) is raised only by `DispatchOverflow`, so an item that reached `w.ch` through `TryDispatch` leaves it at zero. `reactorForwardRS` (`forward_rs.go`) falls back to `TryDispatch` exactly when `tryDirectWriteNoFlush` could not take `session.writeMu`, and the next UPDATE for that destination reads the gate as clear and takes that same `writeMu` with `TryLock` before the worker does. Same inversion this spec closed for the overflow window, one rail over | not fixed. It predates the gate: both the direct write and the `TryDispatch` fallback are older. A count covering channel items would send every later forward through the pool for the whole of any channel backlog, which needs its own tests and its own review |
| 2026-08-11 | fixit-route-removal-protocol-blind | qemu integration target | `make ze-qemu-integration-test` runs `go test -tags integration` with NO feature tags, so no plugin registers its YANG and the doctor's listener collection is empty: `TestCheckListeners_API`, `TestCheckListeners_PortInUse`, both `TestCollectSchemaListeners_SSH*`, `TestDoctorProbesEveryCoveredListener`, `TestDoctorGnmiDefaultEndpointIsProbed` and `TestDoctorDependencyInventory` all fail there. The same tree, same commit, compiled with the full tag list in the unit phase gives `ok internal/component/doctor 7.304s`. The target also caps `-timeout 120s` while `cmd/ze/hub` needs about 400s, so that package is cut off rather than failing. The gate compiles a different program from the one Ze ships and then judges it | not fixed. The target needs the feature tag list the unit phase already uses, and a timeout derived from the slowest package rather than a constant |
| 2026-08-11 | fixit-route-removal-protocol-blind | isis raw L2 transport | `internal/plugins/isis/transport/transport_integration_linux_test.go` calls `tr.OnMTUMismatch` while `transport.go` declares `onMTUMismatch`, so the package has not COMPILED at HEAD since the symbol was unexported. Nothing sees it: the file is `//go:build integration && linux`, and `go build ./...`, `make ze-lint-changed` and every host-side gate compile neither that tag nor `_test.go` at all. Only a QEMU integration run reaches it, and its own build failure is what hides the tests it was written to run | corrected the call. The general hole stands: a `_test.go` behind a build tag no host gate carries is unverifiable until the tagged run happens, and that run is the slowest one in the tree |
| 2026-08-09 | fixit-dead-design-pointers-in-tests | doc gates | `go_files()` excluded `_test.go`, so 133 dead `// Design:` spec pointers accumulated where no gate looked. Non-test files held zero, which is the control | widened the list, and refused the pointer class that closure deletes |
| 2026-08-10 | doc-claims-are-checked-not-just-resolved | doc gates | 85 package directories under `internal/` have no anchor and no `docs/architecture/` page with every gate green: `checkDesignMD` in `scripts/docvalid/doc_drift.go` reads `registry.All` against one table in `docs/DESIGN.md`, so `internal/plugins/explain` and `skills`, which register a CLI root instead, are invisible | not fixed |
| 2026-08-10 | doc-claims-are-checked-not-just-resolved | verify stages | `ze-validate-tree` runs three of `validate.py`'s five checks by passing an explicit empty changed-file set, which selects them only because `['']` is truthy in `main()`. A tidy that strips empty strings silently re-arms the other two under `ze-verify`, and nothing asserts which checks take `changed` | not fixed, needs an explicit flag and a test over the changed-taking set |
| 2026-08-10 | - | RFC evidence | RFC 9234 Section 5 egress rule 1 names three destination roles. Every test gave the destination wire role 3. The `roleRSClient` arm of the gate in `OTCEgressFilter` was never reached, but `RFC9234-5-4` read as proven. A requirement that enumerates roles needs one test per role. The ledger counts bindings, never which arm of an OR each one takes | added `test/plugin/role-otc-rs-client-dest-stamp.ci`, mutation-verified against dropping that arm |
| 2026-08-10 | session-bin-directory | hooks, make | the population is the SPELLINGS of the guard's subject. A corpus written by the guard's author uses the spelling the author had in mind. `_SCRATCH_WRITE` anchored on a literal `tmp/`, so `./tmp/x` and the absolute `<repo>/tmp/x` passed while the Write surface refused them. All 196 rows spelled it the refused way. `ze-clean-tmp`'s `-not -name session` read as protecting `tmp/session/`, but `find tmp/` yields `tmp/` itself at depth 0, whose basename matches neither exclusion, so a stale root mtime `rm -rf`d the whole scratch tree. In both, the DECIDING function was already correct. Only what FED it was narrow | widened the candidate regex, added `-mindepth 1`, each with an allow-control and a mutation-verified test. The test that finds this is never another row in the same corpus. Enumerate the spellings, then drive the guard from its entry point with each one |
| 2026-08-10 | fixit-forward-readbuf-leak-deferred-pool-release-paths | commit gates | `deferral_shard_removal_problems` (`scripts/dev/commit_helper.py`) reads a shard at HEAD to decide whether the removal destroys a live row. Closure is TWO commits in ONE script. Commit A resolves the last live row and commit B removes the shard, so the HEAD the gate reads at PREPARE time is one commit behind the HEAD the removal runs against. An all-terminal shard is refused, and the residue nobody collects is what the rule wanted removed. `_live_rows_added_elsewhere` already exists for the move case and reads only the SAME commit's adds | not fixed, and the shard is kept. The fix is to fold the prepared sibling commits of the same script into the snapshot the gate reads, the way `_prospective_added_lines` already does for the journal checks |
| 2026-08-10 | dead-learned-citations-outside-the-walked-corpus | doc gates | `check_markdown` in `scripts/dev/check_doc_links.py` walked `MD_GLOBS`, fourteen hand-written globs, while the population that CITES a path is every tracked file. 2953 dead references over 1373 targets had collected where no gate looked, `plan/spec-*.md` holding 2085 of them. The hand-written list is the tell: a corpus nobody can state is a coverage claim nobody can check | widened to every tracked file behind a shrink-only baseline of `(citer, target)` pairs. Repairing 1373 targets is a multi-session job, and a red gate that stays red teaches everyone to bypass it |
| 2026-08-10 | dead-learned-citations-outside-the-walked-corpus | doc gates | the shrink-only ratchet compared the baseline's SIZE against HEAD, so it excluded the population's identity. Repair one citation, add one dead citation, regenerate, and the total is unchanged while a pair the sweep had already failed on is grandfathered. A ratchet over a set MUST compare the set: `check_coverage_ratchet` in `scripts/dev/rfc_requirements.py` fires on a requirement losing a polarity it held, never on a total | `check_baseline_growth` compares `current - head` and names each new pair. The test that finds this holds the SIZE constant, so a count comparison passes it and a set comparison does not |
| 2026-08-10 | fixit-rs-community-strip-arity | bgp filter_community | the whole-value check covers two of the three buffers `genericCommunityHandler` reads. A malformed `p.Value()` fails the plan and a malformed Remove `Buf` is refused and counted, but every Add `Buf` is emitted unchecked by `p.Op(i)`, so a producer that hands it a partial value writes an attribute length no peer sent. Not reachable today: all three producers allocate exactly `len(tokens)*width` or a fixed array. The file argues the Remove guard exists so a NEW producer is heard on its first route, and that argument covers Add identically | not fixed, needs the same `wholeValues` guard on the Add arm plus a test per width |
| 2026-08-10 | dead-learned-citations-outside-the-walked-corpus | doc gates | the shrink-only rule moved from a count to a set. The baseline header was corrected by hand. `write_baseline`'s inline copy kept the false text, so the next `--write-baseline` would have reverted the correction. Every gate stayed green because no gate reads a comment: every check in the module has paths and symbols for its population | hoisted the text to `BASELINE_HEADER`, the one home `write_baseline` emits, with `test_baseline_header_is_what_the_generator_writes` comparing it against the committed file. The general form is requirement 11 of `ai/rules/completion.md` |
| 2026-08-10 | fixit-l2tp-sccrq-tunnel-id-zero | RFC evidence | `RFC2661-24.10-1` names SCCRQ AND SCCRP. Both polarities were tagged, both tags drove SCCRP in `tunnel_initiator_test.go`, and the ledger read fully proven while the SCCRQ half dropped the datagram in silence at `handle` (`internal/component/l2tp/reactor.go`) and the published coverage cell claimed the reactor sent StopCCN. `check_coverage_ratchet` counts polarities per requirement id, never per message the requirement's own text names | implemented the SCCRQ answer and tagged both polarities on that path, in `reactor_sccrq_zero_tid_test.go` and `test/l2tp/rfc2661-sccrq-tunnel-id-zero.ci`. A requirement whose text enumerates two messages needs one tag pair per message, the same shape as the RFC 9234 destination-role row above |
| 2026-08-10 | fixit-vpp-ipsec-inoperable | RFC evidence | the strongest proof of `RFC7296-2.24-1`/`-2` on the VPP IPsec backend is a real VPP reporting `encap-copy-ecn` and `decap-copy-ecn`, and it lives in `scripts/evidence/effective-vpp.py`. `scan_tree` (`scripts/dev/rfc_requirements.py`) walks `internal/`, `pkg/` and `test/` only, and `carrier_for` matches the suffixes `_test.go`, `.ci`, `.et` and `/check.py` only, so that file is outside the population twice over. A tag written there is not refused either: `carrier_for` returns `None`, `_refuse_unrun` never runs, and the tag is invisible. `ai/RFC-REQUIREMENTS.md` therefore publishes `unit/verify` for a requirement a real daemon proves, and `check_evidence_ratchet`, which is keyed on `kind/tier`, cannot see the tier it never counted | not fixed: a real-daemon evidence script is a carrier kind the ledger does not model, and inventing one for a single path is a design question rather than a defect fix. The evidence is written into the spec's Goal Validation instead, with the label it can honestly carry. The tell is the same as the doc-gate rows above: the population is a list of shapes somebody wrote down, and the best evidence grew outside it |
| 2026-08-11 | runtime-kernel-carries-every-tunnel-kind | iface link classification | `kernelTunnelKinds` (`internal/component/iface/discover.go`) lists seven kernel link types and omits vxlan, though `TunnelKindVxlan` is modelled in YANG and both backends build one. Two consumers read that set as their whole population: `infoToZeType` classifies a vxlan link as no ze type, so `DiscoverInterfaces` and `ze init` do not see it, and `zeManageable` (`internal/component/iface/config_apply.go`) answers false for it, which is reconcile's deletion scope, so a vxlan dropped from the config is never deleted | not fixed. One map entry closes it, and it changes both discovery output and reconcile's deletion scope for a kind no test in the tree creates, so it needs its own test rather than a line folded into this spec |
| 2026-08-12 | fixit-config-validators-bypassed-at-startup | hub config parser | `ze config validate` answers over a config shape the daemon parses with a different parser, so the two verdicts disagree by construction. A config whose only top-level block is `plugin` boots through `parsePluginBlock` (`internal/component/hub/config.go`), which accepts `external` and refuses everything else. The YANG schema models `plugin/internal/use`, and `InternalPluginNameValidator` offers 89 registered internal plugin names as its valid values. Measured on `plugin { internal p1 { use bgp } }` alone: `configuration valid` and exit 0 from `ze config validate`, exit 1 and `parse config: line 2: expected 'external', got internal` from `ze start`. The spec closed the day before exists to make those two callers agree on the same bytes | not fixed, needs its own spec. Either the hub parser accepts `internal`, or `ProbeConfigType` stops routing a plugin-only config away from the YANG daemon. Both decide which parser boots a config shape that already ships |
