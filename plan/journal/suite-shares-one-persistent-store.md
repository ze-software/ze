# Suite shares one persistent store

Every functional test in a session resolves the same on-disk store, so a test
that writes state into it changes what every LATER test in the session reads.
The defect presents as an order-dependent failure: the test passes alone, passes
first, and fails after the writer has run. Nothing in the failing test names the
writer, so the reader looks for a code regression in the wrong place.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-20 | - | functional runner | `setupBinShims` (`internal/test/runner/runner.go`) symlinks `ze` into the session tree so `DefaultConfigDir` resolves `<session>/etc/ze`, and every test in the session therefore opens the SAME `database.zefs`. A test that stores a local auth credential leaves `meta/auth/local/username=admin` in it, and `liveLocalUsers` (`cmd/ze/hub/main.go`) then reports one boot user for every later daemon. `main.go` prefers per-user auth over `ze.api-server.token`, so four `test/reload/mgmt-guard-*` tests that configure a boot token, or no auth at all, met an authenticated REST listener they never asked for. Two of them (`mgmt-guard-reload-auth-rebuild`, `mgmt-guard-reload-unbuilt-transport`) fail only with the polluted store and pass with a fresh one; the other two failed for an unrelated reason. Confirmed by moving the store aside and re-running the four | not fixed: recorded. The fix is a per-test config dir, or a runner step that reseeds the store between tests, and either one is a change to the runner's process model rather than to a test |
| 2026-08-22 | fixit-irr-empty-answer-clears-set | `DefaultConfigDir` (`internal/core/paths/paths.go`) under the functional runner | second instance of the row above, and it corrects that row's mechanism. The shared store is not the session's, it is the DEVELOPER's. `DefaultConfigDir` reads `ze.config.dir` first. Otherwise it runs `EvalSymlinks` over the running binary and hands the result to `ConfigDirFromBinary`, so a `ze` at `<repo>/bin/ze` opens `<repo>/etc/ze/database.zefs`. `setupBinShims` (`internal/test/runner/runner.go`) puts a bare-named symlink on PATH. Its own comment states that the EvalSymlinks sees through it, so the shim isolates the NAME and never the config dir. Measured: `firewall-irr-cold-cache-recovers` asserts an empty prefix cache on its first line. It failed alone in a fresh container while the daemon ran from `/repo/bin`, because earlier runs had left `meta/irr/AS-TEST` in the developer's store. Nothing in the failure names the store, and the store outlives the run | not fixed. The repair is a per-test config dir in the runner's process model, which reaches all 690 functional tests and is not this spec's to make. It is recorded where the reader meets it. A HARNESS NOTE in `test/plugin/firewall-irr-cold-cache-recovers.ci` names the two ways out. Run the daemon from a prefix outside the checkout, or move the store aside. The 12 firewall-irr tests now serialize (see gate-excludes-part-of-its-population). That stops a sibling warming the store mid-run, and does nothing about a previous run |
| 2026-08-24 | fixit-firewall-concurrency-deadlock | `cmd/ze/ze_core_start.go` startConfigPath branch, with `resolveKey` (`internal/component/config/storage/blob.go`) | third instance, and the leaked state is the whole CONFIG rather than a meta key. `ze start <file>` falls back to the filesystem only when `!store.Exists(configPath)`, and `resolveKey` drops the directory part, so every test's `ze-bgp.conf` is the one key `file/active/ze-bgp.conf`. Measured under QEMU: `ddos-transit-forward-drop`, which declares no plugin at all, started an external plugin named `ddos-firewall-concurrency-probe` and died with `/bin/sh: ./ddos-firewall-concurrency-probe.run: not found`. That name exists only in a `.ci` this session had added, and the run reproduced with that `.ci` REMOVED from the tree, so the source is the store rather than the file. `strings` over `tmp/session/<id>/etc/ze/database.zefs` finds the config under `file/20260824-020831.664/ze-bgp.conf`, an archive revision that `meta/config/rollback` names. WHICH producer applies an archive revision at startup is UNVERIFIED: the `file/active/ze-bgp.conf` key was absent from that store, so the `store.Exists` branch above cannot be the whole path | not fixed: recorded, and it is the same repair the two rows above name. Moving the store aside is NOT a workaround here -- the ddos suite got WORSE without it (`ddos-detect-characterize`, `-external-warns`, `-mitigate` all failed and `ddos-direction` timed out at 375s), so the suite now depends on state the store holds. Read any ddos QEMU red in this checkout against this row before charging it to a code change |
| 2026-08-24 | qemu-targets-boot-the-shipped-kernel | `test/install/ze-kernel-overlay.ci`, writing `~/.cache/ze/runtime-kernel/<key>/` | fourth instance, and the store is neither the session's nor a config dir: it is the durable KERNEL cache, and its reader is the build system rather than another test. The test drives a real the retired `ze-kernel-build KERNEL_ARCH=amd64` (current: `ze appliance kernel`) with a faked docker that emits a 518-byte marker vmlinuz, and it sets no `XDG_CACHE_HOME`. `ze-kernel-vmlinuz-stage`'s cache-MISS branch (the retired `mk/build-gokrazy.mk` (current producer: `internal/appliance/cmd_build.go`)) then POPULATES the developer's real cache with that fake. Measured: one the retired `ze-functional-install-test` (current: `./le functional install`) created `7.2-runtime-amd64-runtime-dc2eac42-533a04a3` holding a 518-byte `vmlinuz`, a 17-byte `config` and `lib/modules/7.1.1-ze/ze.ko`, after which `ze-qemu-kernel-guard` compared a real staged kernel against it and refused every QEMU target. The next stage is worse: it takes the HIT branch, materializes the fake to `tmp/kernel/vmlinuz`, and the guard then reports `Runtime kernel: ... matches` over a file QEMU cannot boot. The test saves and restores `tmp/kernel/build` and `tmp/kernel/vmlinuz`, so it knew about shared working-tree state and did not reach the cache | fixed: `XDG_CACHE_HOME="$work/cache"` on the make invocation, plus an assertion in the same test that the real cache holds nothing newer than the fixture -- the variable travels through make to ze-host, and a chain that stops reading it fails silently and poisons the host |
