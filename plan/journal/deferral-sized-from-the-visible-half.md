| Date | Spec | Surface | Symptom | Fix |
| 2026-09-04 | spec-lg-pki-certificate | the Known Limitations of `spec-pki-full-chain`, sizing the looking-glass follow-up | A deferral written as "a small follow-up consuming `pki.ServerTLSMaterial`" measured the code it could see and not the code it could not. The producer half was right: `pki.ServerTLSMaterial` (`internal/component/pki/tls.go`) returns the same material to any caller, and `buildLGService` (`cmd/ze/hub/service_lg.go`) needed one call to get it. The CONSUMER half was never read. `NewLGServer` (`internal/component/lg/server.go`) built one `tls.Config` at construction with a fixed `Certificates` slice and handed it to `tls.NewListener`, with no `GetCertificate` callback anywhere, so the looking glass could not change its certificate without rebinding its listeners. The spec's fail-closed and rotation goals then owed server code, an atomic pointer, a per-handshake callback and a rotation seam, none of which the sizing named. Naming a consumer as identical to another says nothing about whether that consumer can ACCEPT what the producer offers | not fixed as a class, and the sizing error cost the spec a phase rather than a defect. The cheap mechanism: a deferral that says "the same pattern, so this is small" names the CONSUMER's entry point and the reader opens it. One `gopls symbols internal/component/lg/server.go` would have shown no `GetCertificate` and no `UpdateTLSCertificate` before the sentence was written |
