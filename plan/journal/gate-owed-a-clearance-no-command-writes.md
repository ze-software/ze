| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-08 | spec-verification-debt-clearing | the `debt-discharge` case of `Answer` (`internal/le/commit/actions.go`), which takes its session from `lepath.CommitSession`, against `writeDischargeRecords` and `replaceDischargeRow` (`internal/le/commit/dischargerecord.go`) | A discharge record with WRONG evidence can be corrected only by the session that wrote it, and that session is gone. `dischargeDebt` writes to `dischargePath(session)`, `replaceDischargeRow` replaces a row only inside that one file, and no keyword overrides the session the verb resolves. So a later session's correct record does not supersede the stale one: both are read, the stale one fails to derive, and `debt-status` prints INVALID DISCHARGE for ever while the debt row counts open. Measured 2026-09-08 after the commit binding was tightened: `plan/verification-debt/discharged/cf1378ad.md` holds a record binding the row at line 44 of shard `95a19451.md` to commit `494f121ba0`, whose own row is line 62 of that shard, while line 44 was written by `f2d8612aab`. The correction is one cell of one row and no command writes it | not fixed, recorded. It blocks no goal of the work in hand, and hand-editing a record file is what that file's own header forbids. Two directions for whoever takes it: `debt-discharge` takes the record file's session as a keyword, so a correction lands where the stale row stands; or `writeDischargeRecords` drops a superseded row wherever in `discharged/` it stands, which is what the one-record-per-row rule already claims and today holds only inside one file |
| 2026-09-07 | none (walked into it answering "what blocks a push") | `./le commit debt-clear` (`clearDebtWith`, `internal/le/commit/actions.go`) against `refusePushWithDebt` (`internal/le/commit/prepare.go`) | The push gate cannot be satisfied, by construction rather than by circumstance. `refusePushWithDebt` refuses a push while ANY debt row anywhere is open, and `clearDebtWith` puts two of the seven gate names into `unrunnableSet` and skips them: `independent critical review` and `owner approval for an RFC-tagged test change`. Those never enter `passed`, and `clearDebtRows` (`internal/le/commit/debt.go`) writes `cleared` for nothing else, so no code path in the repository can ever flip them. Measured 2026-09-07 over `plan/verification-debt/`: 3160 open, of which 68 name the review gate and 4 the owner-approval gate. The remaining 3088 do clear on one green `verify.Run` over HEAD, so the shape is not "the debt is large", it is "72 rows are permanent". A second reading was excluded: the two names are not typos of a runnable gate, they are the `Name` values `debtGates` declares for `gateReviewOverride` and `gateRFCChangeOK` | not fixed. `docs/architecture/testing/verify-freshness-scope.md` already states the intended answer, that these rows are "answered by doing the work the row names... recorded through `internal/le/spec/session/review.go`", so the DOC describes a clearing path the CODE does not have, and the doc is the side that is right. The repair is that `clearDebtWith` reads the artifact each of those two gates names, a recorded review for one and the session's `test/rfc-changed/` shard for the other, and clears a row whose artifact exists. Same file also carries a second disagreement with that page: the page says a pass refuses "a gate string the runner table does not hold", and there is no runner table, so an unrecognised gate string is treated as runnable and cleared on green |
