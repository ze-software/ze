| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-31 | - | `internal/component/l2tp/plugins/authradius/coa_test.go`, `rfc5176_coa_test.go`, `rfc5176_message_authenticator_test.go`, `rfc5176_message_authenticator_optional_test.go` | Eight CoA and Disconnect listener tests failed together with `no response: read udp4 ...: i/o timeout` at exactly 2.00s. The listener answers on a UDP socket and the test waits on a fixed read deadline rather than on a condition, so a loaded box reads as a listener that never answered. The eight are TestCoAListenerUnknownSession, TestCoAListenerMissingEventTimestamp, TestDisconnectReplayReturnsCachedResponse, TestDisconnectListenerUnknownSession, TestRFC5176NoSessionIdNotActedOn, TestRFC5176ResponseAuthenticator, TestRFC5176MessageAuthenticatorAbsentIsAcceptedByDefault and TestRFC5176ListenerAcceptsConformantMessageAuthenticator The cause is now known and it is not the code: the owner confirmed on 2026-08-31 that this Linux VM keeps the checkout on a network drive that freezes for several seconds at a time, which is exactly a wave of socket deadlines expiring together at their nominal value. `docs/contributing/running-commands.md`, "When the storage stalls", carries the tell and the one re-run that reads it. | not fixed, and NOT reproducible on demand: seen once while a peer session held a `./le verify lint run` and a `unit-rfc` job on the same box, then 0 failures in a run restricted to the CoA, Disconnect and RFC 5176 tests and 0 in a full re-run of both packages. Next step for whoever owns it: raise the deadline or wait on the listener's own readiness rather than on a duration, the way this class's other rows were fixed |
| 2026-08-20 | - | `internal/component/plugin/server/startup_autoload_test.go` | `TestBridgeRollbackWaitsForDirectDispatch` fails intermittently with `bridge rollback completed before direct dispatch drained`. The assertion is an ordering one, so the test states the very race it is subject to: nothing signals that the direct dispatch has drained, and the rollback wins when the scheduler runs it first | not fixed, and NOT reproducible on demand: 1 failure in 9 runs of the retired `ze-unit-pkg-test PKG=./internal/component/plugin/server ARGS='-run TestBridgeRollbackWaitsForDirectDispatch'` (current: `go test -race ./internal/component/plugin/server`), then 0 in the following 6. The file is committed and untouched by the streaming work, whose own packages are green, so this is not that work's regression. Next step for whoever owns it: make the drain observable rather than assumed, the way this class's other two rows were fixed, since a bound that expires is what an ordering assertion reads as success. FIXED 2026-08-21, and the cause was not the assertions. `handleSingleProcessCommandsRPC` (`internal/component/plugin/server/dispatch.go`) drained the bridge only inside its `conn.HasBridge()` branch. `Process.Stop` nils the connection (`process.closeConns`), so a rollback that runs before that goroutine reads `proc.Conn()` sends it down the nil branch, which returns and lets its deferred `cleanupProcess` release `WaitRuntimeCleanup`, the only thing `rollbackStartupProcess` waits on before it frees the command names. Rollback then returned with a plugin-to-engine call still inside its handler, which is what all three assertions were reading. The drain is now a defer covering every exit path. `TestRuntimeHandlerDrainsBridgeWhenConnectionIsAlreadyClosed` pins it deterministically by calling `Stop` before the handler starts, and it fails on the old code in 0.00s |
| 2026-08-10 | fixit-rib-graph-ci-never-terminates | test runner | the drain barrier burned its full 10s `peerDrainGrace` on every `--mode sink` or `--mode echo` test in `test/plugin/`, because nothing signaled a `ze-peer` whose only exit is a signal. It read as latency for two days, and it was hiding a red: `event-predicate-wait.ci` failed at its 15s budget with `TYPE: timeout` while the daemon completed correctly | `terminateScaffoldPeers` sends SIGTERM after the arm switch and waits for nothing, so `drainPeers` reaps at once and stays the only bounded wait. A check-mode peer is skipped so the capture its verdict is read from survives |
| 2026-08-15 | fixit-peer-process-event-filter | `internal/component/bgp/reactor/peer_initial_sync.go` | the initial-sync hold waits `apiSyncTimeout` for a `plugin session ready` that only bgp-rib ever sends, so a peer granting any other process `send [ update ]` keeps `ShouldQueue` true for 2.5s and an event-driven announce raised inside that window drains AHEAD of the End-of-RIB. The marker then claims a route that was never in the initial routing update (RFC 4724 Section 2). Seen deterministically on 6 healthcheck/watchdog fixtures the moment the watchdog's send grant became truthful, and intermittently on `test/plugin/role-otc-rs-withdraw-eor.ci` (passed one run, failed the next, unchanged tree) | bgp-watchdog now sends the signal on every peer-up (`watchdogServer.signalReady`), which fixes the 6. The fixed 500ms pre-sleep is also gone, leaving one bounded wait. The residual is any OTHER permitted plugin that never signals, which still waits out the timeout: `spec-fixit-forward-rail-initial-sync-ordering` closed on 2026-08-11 and fixed the forward rails rather than this hold, so it has no owning spec |
| 2026-08-21 | - | `internal/component/plugin/server/startup_autoload_test.go` | THIRD occurrence in `TestBridgeRollbackWaitsForDirectDispatch`, at a DIFFERENT assertion: `startup_autoload_test.go:751`, `require.Error(t, s.registry.Register(replacement.Registration()))`, "An error is expected but got nil". Same shape as the two rows below it. The registration must fail while the original process still owns `commandName`, and `rollbackStartupProcess` frees that name when it completes, so a rollback that finishes early makes the replacement register cleanly. Nothing publishes "rollback has not released the registry yet", so the assertion reads a lost race as success | not fixed, and NOT reproducible: 1 failure in the first full-package run, then 0 in the following 3 (the retired `ze-unit-pkg-test PKG=./internal/component/plugin/server/ RACE=0` (current: `go test -race ./internal/component/plugin/server/`)), and green in isolation under `-run`. Not this spec's regression: the only file this spec changes in that package is `command_test.go`, which builds its own `NewDispatcher()` and touches neither `s.registry` nor the bridge. Next step unchanged, and now with a second assertion behind it: make the rollback's progress observable rather than assumed. FIXED 2026-08-21, and the cause was not the assertions. `handleSingleProcessCommandsRPC` (`internal/component/plugin/server/dispatch.go`) drained the bridge only inside its `conn.HasBridge()` branch. `Process.Stop` nils the connection (`process.closeConns`), so a rollback that runs before that goroutine reads `proc.Conn()` sends it down the nil branch, which returns and lets its deferred `cleanupProcess` release `WaitRuntimeCleanup`, the only thing `rollbackStartupProcess` waits on before it frees the command names. Rollback then returned with a plugin-to-engine call still inside its handler, which is what all three assertions were reading. The drain is now a defer covering every exit path. `TestRuntimeHandlerDrainsBridgeWhenConnectionIsAlreadyClosed` pins it deterministically by calling `Stop` before the handler starts, and it fails on the old code in 0.00s |
| 2026-08-30 | fixit-peer-pending-sync-settles-too-early | `internal/component/bgp/reactor/peer.go` `(*Peer).SignalAPIReady` | The 2026-08-15 row's residual is not merely unfixed, it is MASKED. `apiSyncCount` is an anonymous counter, so any `request peer <addr> plugin session ready` satisfies one unit of it whoever sent it, and `fc9c8bcaa` made `bgp-adj-rib-in` report on EVERY peer-up, including peers that attach it for events alone. In `test/plugin/role-otc-rs-withdraw-eor.ci` the barrier counts `rs` and `test-rs-eor` while the uncounted `bgp-adj-rib-in` answers for one of them, so the End-of-RIB can precede a counted process's routes (RFC 4724 Section 4). Naming the barrier's population and crediting a report to its sender makes it honest, and that EXPOSES the residual: `./le functional plugin` goes from 5 failures to 9, and the four added are route-server observers reading `EOR peers=1/3`, because `(*routeServer).replayForPeer` returns at its `stale` branch, and for a peer that is not a route-server client, without ever reaching `sendEOR`; a test fixture carrying `send [ update ]` never reports at all | NOT fixed, and the two halves cannot land apart: the honest barrier is correct and unshippable until every counted process reports on every peer-up, which is `plan/spec-bgp-session-ready-contract.md`'s declared subject, "the set that is COUNTED is not the set that SIGNALS". The named-population change is written and proven RED with its guard removed (`TestInitialSyncBarrierCreditsOnlyTheProcessesItCounts`, `internal/component/bgp/reactor/peer_initial_sync_test.go`), and is left uncommitted in the working tree for the re-cut |
| 2026-08-21 | - | `internal/component/plugin/server/startup_autoload_test.go` | SECOND occurrence of the 2026-08-20 row above, same assertion and same message: `bridge rollback completed before direct dispatch drained`, one failure inside a full `./le verify current mode full` (stage `ze-unit-test-cached`). It passes in isolation under `-race` | not fixed, and NOT this spec's regression despite the proximity. `git diff pkg/plugin/rpc/bridge.go` removes exactly ONE non-cosmetic line, a comment above `beginDispatch`; every other deletion is the struct field block re-aligned by gofmt after a field was added. `DispatchRPC`, `beginDispatch` and `endDispatch` keep their logic byte for byte, and the new `releaseOnWalkEnd` sits on `DispatchCommandAnswer`, which this test never calls. The next step is unchanged and now has two occurrences behind it: make the drain observable rather than assumed, since an ordering assertion reads an expired bound as success. FIXED 2026-08-21, and the cause was not the assertions. `handleSingleProcessCommandsRPC` (`internal/component/plugin/server/dispatch.go`) drained the bridge only inside its `conn.HasBridge()` branch. `Process.Stop` nils the connection (`process.closeConns`), so a rollback that runs before that goroutine reads `proc.Conn()` sends it down the nil branch, which returns and lets its deferred `cleanupProcess` release `WaitRuntimeCleanup`, the only thing `rollbackStartupProcess` waits on before it frees the command names. Rollback then returned with a plugin-to-engine call still inside its handler, which is what all three assertions were reading. The drain is now a defer covering every exit path. `TestRuntimeHandlerDrainsBridgeWhenConnectionIsAlreadyClosed` pins it deterministically by calling `Stop` before the handler starts, and it fails on the old code in 0.00s |
