| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-19 | cli-dispatch-child-guard | `test/plugin/rest-api-commands.ci` | The test fails on this checkout in 4 of 4 runs, alone and in a batch, with `ZE-OBSERVER-FAIL: REST API did not respond within timeout`. The daemon logs `REST API server starting on http://127.0.0.1:18081/` and `API auth mode: per-user (1 users)`, so the listener starts. 18081 is hardcoded twice in the `.ci` (`REST_PORT` and the config's `port`), which the per-test port allocator does not reach, and this box runs several sessions. The mechanism is NOT established: `wait_for_rest` catches `urllib.error.URLError`, and `HTTPError` is a subclass of it, so a 401 and an unbound port are the same answer to that poll | not fixed, and not caused by this spec: a `ze` built from a pristine `git archive HEAD` tree fails the test identically, which is the discrimination this row carries. The next step is to make `wait_for_rest` report the HTTP status it swallows, then decide between the port and the auth mode |
| 2026-08-19 | - | test/static, test/plugin, QEMU VM | `test/static/static-per-route-isolation.ci` asserts that a route via 203.0.113.1 is skipped as unresolvable, and its own header states the premise: that address is "deliberately in no connected subnet". `test/plugin/ddos-transit-forward-drop.ci` creates `zdd0` and addresses it `203.0.113.1/24`. One VM, one root namespace, and an interface outlives the test that made it, so the prefix one test needs ABSENT is another test's fixture. The collision is an address rather than a port, and the shape is the same: a hardcoded global the allocator does not reach. It surfaced the first time the suite ran anywhere (`f1698ab8e`), having been latent since both tests were written | not fixed. The isolation test needs its own namespace, or a prefix no other test claims. Measured in QEMU: 7 of 8 static tests pass, this one fails with `unresolvable route 172.16.0.0/12 was programmed, expected skipped` and `203.0.113.0/24 dev zdd0` in the dump it prints |
| 2026-08-10 | fixit-mgmt-listener-auth-guard | test tooling | two `test/plugin` tests hardcoded MCP port 18086 (`mgmt-guard-loopback-allowed.ci`, `rest-no-auth-readonly.ci`) with no exclusive group between them, so a parallel suite run failed one of them with `bind 127.0.0.1:18086: address already in use`. A third bound the wildcard `0.0.0.0:9339`, which collides with any concurrent second copy of the suite | moved the MCP listener to 18089 and took the gNMI test off the well-known port; the deterministic-port allocator still does not reach `.ci` literals |
| 2026-08-10 | fixit-eap-tls-clienthello-race | test tooling | `scripts/dev/stress-repro.py` cannot stress any `test/ipsec` test. It runs several copies of ONE selector at once through a thread pool. The second copy dies with `bind: address already in use` on 127.0.0.2 and on `0.0.0.0:4500`. Suites registered through `registerCIRoot` (`internal/test/cli/register.go`) take the per-test port from the deterministic allocator in `internal/test/runner/record_parse.go`, and never reserve it. `ipsec` is one of them. Only the `bgp` and `vpp` suites call `runner.ReservePorts` and rebase. So every concurrent copy of one test gets the same `$PORT` and `$PORT2`. Controlled against the committed `ipsec-sa-installed`, which fails the same way. The fault is pre-existing and suite-wide, not caused by the new test. The `0.0.0.0:4500` half is unexplained. `nattAddr` (`internal/component/ike/engine/testport.go`) returns the test port plus one when the `ze.test.ike.port` override is set, and the well-known 4500 only when it is not | not fixed. The next step is to give the `registerCIRoot` path the port reservation `cmd_bgp.go` already uses. The alternative is to make the tool refuse a stress run on a suite that reserves no ports. Today it reports the collision as a product failure |
| 2026-08-12 | - | interop harness | `_set_subnet_prefix` (`test/interop/interop.py`) rebased eight container addresses onto the fallback /24 and left five behind: `KEEPALIVED_IP`, `INJECT_IP`, `SPEAKER_IP`, `SPEAKER2_IP` and `VRRP_VIP` kept their 172.30.0.x spelling. `docker run --ip` then names an address outside the network it joins, and refuses the container. Every scenario with an injector, a speaker or keepalived therefore runs only on the default /24. The fallback exists for the concurrent-run case, which is exactly when it is reached | fixed: all five are rebased in `_set_subnet_prefix`, with `STAYRTR_IP` in the same list. Found when the StayRTR RTR scenario needed a sixth address |
| 2026-08-16 | - | test/plugin under high `-p` | The failing SET is not stable, which the row below could not yet show. CI run 31918268519 failed five relay-observer tests (`bgp-rs-fastpath-ebgp-shared`, `llgr-egress-state-unloaded`, `med-removal-export-refused`, `rfc7606-54-discard-unrecognized-mup-nlri`, `role-otc-rs-withdraw-eor`) with `routes sent=0` and `route server did not replay`. On this 16-CPU host NONE of the five reproduces: the full suite at `-p 8` gives 603/604 with `plugin-metrics-registered` the only red, the same suite at `-p 32` gives 602/604 with `med-removal-before-decision` and `rpki-aspa-invalid` red and `plugin-metrics-registered` green, and `role-otc-rs-withdraw-eor` passes 5 of 5 alone. Three runs, three disjoint failing sets, none matching CI's. `withholding originated route` appears ZERO times in all three, so the RFC 4271 Section 5.1.3 guard is not involved. A lens diagnosed 502 as a deterministic missing `attach process rs` block on `dest-peer`; the 5-of-5 pass refutes that as the cause, though the block is genuinely absent | not fixed, not reproducible here. The next step is a knob that is NOT the core count: the set moved without the knob moving, so oversubscription alone does not select it. Chasing a named test is the wrong unit while membership is this unstable |
| 2026-08-15 | - | test/plugin under high `-p` | THREE tests pass the suite at `ZE_PLUGIN_PARALLEL=8` (604/604, measured twice) and FAIL it at 32 (601/604): `remove-private-as-replace-peer` and `rpki-event-multi` as `mismatch`, and `rfc7606-54-discard-unrecognized-mup-nlri` as `ZE-OBSERVER-FAIL: route server did not replay within 36.0s (expected_peers=2, forward_prefix=10.0.0.0/24)`. The run was made to answer a DIFFERENT question, `spec-fixit-forward-overflow-two-tier-flake`'s "does it reproduce at `ZE_PLUGIN_PARALLEL` well above the core count", and that target test PASSED at 32 (10.7s against a ~2s norm, so it felt the contention and survived it). The three are not the RFC 4271 Section 5.1.3 guard: `withholding originated route` appears ZERO times in the whole capture. `-p` is the count of `.ci` tests running CONCURRENTLY (`mk/test-functional.mk` passes it as `-p N`), so 32 on a 16-CPU host is 2x oversubscription, which is the condition this class is about | not fixed, and not chased: none blocks the work in hand. The third's symptom is the one another session is already tracing on its own attach-process tests, so a second file carrying it is data for that trace rather than a new defect. What this row adds is the discrimination the class needs: the same tree, the same binaries, the same fixtures, one knob, 604/604 against 601/604. A load-dependent failure that reproduces on a knob is cheaper to chase than one that reproduces on a mood |
| 2026-08-17 | fixit-ike-resource-lifetime-leaks | the `ipsec` functional suite under `scripts/evidence/qemu-all-tests.sh` | `test/ipsec/ipsec-dataplane-show.ci` failed at -p 4 inside the QEMU VM, reporting only that stderr never carried `engine-steps: all steps passed`, and PASSED at -p 1 in the same VM and at -p 4 on the dev box (MEASURED 2026-08-17, three runs). The suite's tests share fixed endpoints rather than per-test ones: `ipsec-dataplane-show` and `ipsec-traffic-selector-config` both configure `local-address 10.0.0.1`, which no host in this rail carries, so both daemons log `bind: cannot assign requested address` for the IKE and NAT-T listeners. The mechanism behind the failed step is NOT established: the failing run also logged the shutdown warning added by this spec, so the two are not separated | worked around, not fixed: the new `fsuite ipsec` line runs the suite at -p 1, and says why. The teardown test in that suite needs serial anyway, because it reads the one XFRM table the VM has |
