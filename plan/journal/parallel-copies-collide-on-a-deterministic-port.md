| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-19 | cli-dispatch-child-guard | `test/plugin/rest-api-commands.ci` | The test fails on this checkout in 4 of 4 runs, alone and in a batch, with `ZE-OBSERVER-FAIL: REST API did not respond within timeout`. The daemon logs `REST API server starting on http://127.0.0.1:18081/` and `API auth mode: per-user (1 users)`, so the listener starts. 18081 is hardcoded twice in the `.ci` (`REST_PORT` and the config's `port`), which the per-test port allocator does not reach, and this box runs several sessions. The mechanism is NOT established: `wait_for_rest` catches `urllib.error.URLError`, and `HTTPError` is a subclass of it, so a 401 and an unbound port are the same answer to that poll | not fixed, and not caused by this spec: a `ze` built from a pristine `git archive HEAD` tree fails the test identically, which is the discrimination this row carries. The next step is to make `wait_for_rest` report the HTTP status it swallows, then decide between the port and the auth mode |
| 2026-08-19 | - | test/static, test/plugin, QEMU VM | `test/static/static-per-route-isolation.ci` asserts that a route via 203.0.113.1 is skipped as unresolvable, and its own header states the premise: that address is "deliberately in no connected subnet". `test/plugin/ddos-transit-forward-drop.ci` creates `zdd0` and addresses it `203.0.113.1/24`. One VM, one root namespace, and an interface outlives the test that made it, so the prefix one test needs ABSENT is another test's fixture. The collision is an address rather than a port, and the shape is the same: a hardcoded global the allocator does not reach. It surfaced the first time the suite ran anywhere (`f1698ab8e`), having been latent since both tests were written | not fixed. The isolation test needs its own namespace, or a prefix no other test claims. Measured in QEMU: 7 of 8 static tests pass, this one fails with `unresolvable route 172.16.0.0/12 was programmed, expected skipped` and `203.0.113.0/24 dev zdd0` in the dump it prints |
| 2026-08-10 | fixit-mgmt-listener-auth-guard | test tooling | two `test/plugin` tests hardcoded MCP port 18086 (`mgmt-guard-loopback-allowed.ci`, `rest-no-auth-readonly.ci`) with no exclusive group between them, so a parallel suite run failed one of them with `bind 127.0.0.1:18086: address already in use`. A third bound the wildcard `0.0.0.0:9339`, which collides with any concurrent second copy of the suite | moved the MCP listener to 18089 and took the gNMI test off the well-known port; the deterministic-port allocator still does not reach `.ci` literals |
| 2026-08-10 | fixit-eap-tls-clienthello-race | test tooling | the retired `scripts/dev/stress-repro.py` (current producer: `internal/le/stressrepro/run.go`) cannot stress any `test/ipsec` test. It runs several copies of ONE selector at once through a thread pool. The second copy dies with `bind: address already in use` on 127.0.0.2 and on `0.0.0.0:4500`. Suites registered through `registerCIRoot` (`internal/test/cli/register.go`) take the per-test port from the deterministic allocator in `internal/test/runner/record_parse.go`, and never reserve it. `ipsec` is one of them. Only the `bgp` and `vpp` suites call `runner.ReservePorts` and rebase. So every concurrent copy of one test gets the same `$PORT` and `$PORT2`. Controlled against the committed `ipsec-sa-installed`, which fails the same way. The fault is pre-existing and suite-wide, not caused by the new test. The `0.0.0.0:4500` half is unexplained. `nattAddr` (`internal/component/ike/engine/testport.go`) returns the test port plus one when the `ze.test.ike.port` override is set, and the well-known 4500 only when it is not | not fixed. The next step is to give the `registerCIRoot` path the port reservation `cmd_bgp.go` already uses. The alternative is to make the tool refuse a stress run on a suite that reserves no ports. Today it reports the collision as a product failure |
| 2026-08-12 | - | interop harness | `_set_subnet_prefix` (`test/interop/interop.py` (retired; now `internal/le/interoplab/bgp/`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->) rebased eight container addresses onto the fallback /24 and left five behind: `KEEPALIVED_IP`, `INJECT_IP`, `SPEAKER_IP`, `SPEAKER2_IP` and `VRRP_VIP` kept their 172.30.0.x spelling. `docker run --ip` then names an address outside the network it joins, and refuses the container. Every scenario with an injector, a speaker or keepalived therefore runs only on the default /24. The fallback exists for the concurrent-run case, which is exactly when it is reached | fixed: all five are rebased in `_set_subnet_prefix`, with `STAYRTR_IP` in the same list. Found when the StayRTR RTR scenario needed a sixth address |
| 2026-08-16 | - | test/plugin under high `-p` | The failing SET is not stable, which the row below could not yet show. CI run 31918268519 failed five relay-observer tests (`bgp-rs-fastpath-ebgp-shared`, `llgr-egress-state-unloaded`, `med-removal-export-refused`, `rfc7606-54-discard-unrecognized-mup-nlri`, `role-otc-rs-withdraw-eor`) with `routes sent=0` and `route server did not replay`. On this 16-CPU host NONE of the five reproduces: the full suite at `-p 8` gives 603/604 with `plugin-metrics-registered` the only red, the same suite at `-p 32` gives 602/604 with `med-removal-before-decision` and `rpki-aspa-invalid` red and `plugin-metrics-registered` green, and `role-otc-rs-withdraw-eor` passes 5 of 5 alone. Three runs, three disjoint failing sets, none matching CI's. `withholding originated route` appears ZERO times in all three, so the RFC 4271 Section 5.1.3 guard is not involved. A lens diagnosed 502 as a deterministic missing `attach process rs` block on `dest-peer`; the 5-of-5 pass refutes that as the cause, though the block is genuinely absent | not fixed, not reproducible here. The next step is a knob that is NOT the core count: the set moved without the knob moving, so oversubscription alone does not select it. Chasing a named test is the wrong unit while membership is this unstable |
| 2026-08-15 | - | test/plugin under high `-p` | THREE tests pass the suite at `ZE_PLUGIN_PARALLEL=8` (604/604, measured twice) and FAIL it at 32 (601/604): `remove-private-as-replace-peer` and `rpki-event-multi` as `mismatch`, and `rfc7606-54-discard-unrecognized-mup-nlri` as `ZE-OBSERVER-FAIL: route server did not replay within 36.0s (expected_peers=2, forward_prefix=10.0.0.0/24)`. The run was made to answer a DIFFERENT question, `spec-fixit-forward-overflow-two-tier-flake`'s "does it reproduce at `ZE_PLUGIN_PARALLEL` well above the core count", and that target test PASSED at 32 (10.7s against a ~2s norm, so it felt the contention and survived it). The three are not the RFC 4271 Section 5.1.3 guard: `withholding originated route` appears ZERO times in the whole capture. `-p` is the count of `.ci` tests running CONCURRENTLY (the retired `mk/test-functional.mk` (current producer: `internal/le/functional/suites.go`) passes it as `-p N`), so 32 on a 16-CPU host is 2x oversubscription, which is the condition this class is about | not fixed, and not chased: none blocks the work in hand. The third's symptom is the one another session is already tracing on its own attach-process tests, so a second file carrying it is data for that trace rather than a new defect. What this row adds is the discrimination the class needs: the same tree, the same binaries, the same fixtures, one knob, 604/604 against 601/604. A load-dependent failure that reproduces on a knob is cheaper to chase than one that reproduces on a mood |
| 2026-08-17 | fixit-ike-resource-lifetime-leaks | the `ipsec` functional suite under the retired `scripts/evidence/qemu-all-tests.sh` (current producer: `internal/le/qemu/alltests.go`) | `test/ipsec/ipsec-dataplane-show.ci` failed at -p 4 inside the QEMU VM, reporting only that stderr never carried `engine-steps: all steps passed`, and PASSED at -p 1 in the same VM and at -p 4 on the dev box (MEASURED 2026-08-17, three runs). The suite's tests share fixed endpoints rather than per-test ones: `ipsec-dataplane-show` and `ipsec-traffic-selector-config` both configure `local-address 10.0.0.1`, which no host in this rail carries, so both daemons log `bind: cannot assign requested address` for the IKE and NAT-T listeners. The mechanism behind the failed step is NOT established: the failing run also logged the shutdown warning added by this spec, so the two are not separated | worked around, not fixed: the new `fsuite ipsec` line runs the suite at -p 1, and says why. The teardown test in that suite needs serial anyway, because it reads the one XFRM table the VM has |
| 2026-08-23 | fixit-vrrp-parent-ignores-the-selector | test/vrrp under QEMU, the owned-macvlan registry | The shared global here is neither a port nor an address: it is the `ze:owned:` alias NAMESPACE. `reconcileOwnedDevices` (`internal/component/iface/config_apply.go`) deletes every macvlan carrying that alias prefix and absent from ITS OWN registry, which is right for one daemon per network namespace and fatal for two. Adding a second VRRP `.ci` made `vrrp-instance-up` fail with `open transport for zv4-8-10: resolve macvlan zv4-8-10: no such network interface`: the new test's daemon scanned the older test's device as an orphan and removed it, milliseconds after its own reconcile created it. Unique device names cannot help, which is what makes this different from every row above | fixed: both tests carry `option=exclusive:group=iface-owned-macvlan`, named for the `ze:owned:` alias namespace rather than for vrrp. The product behaviour is correct and stays -- a daemon that left foreign `ze:owned:` devices alone would leak every real orphan -- so the isolation belongs to the tests. Any future `.ci` that runs a daemon owning a macvlan joins that group |
| 2026-08-23 | fixit-vrrp-parent-ignores-the-selector | `test/vrrp/vrrp-idle.ci`, `test/vrrp/vrrp-doctor-quiet.ci` | Both fail in the QEMU VM under a concurrent vrrp suite, both as `ze doctor --json <conf>` exiting 1 with the runner reporting `Client produced no output`. Measured 2026-08-23 over four runs: `vrrp-idle` failed in 4 of 4 runs at 8-wide and 3-wide; `vrrp-doctor-quiet` failed in 2 of 3 at 8-wide and PASSED at 3-wide and alone (20.2s alone against a 9.1s average). Neither carries `option=needs-linux`, so both pass on darwin and neither is in `ze-qemu-needs-linux-test`; they are red under `ze-qemu-test-all`. Discrimination: the suite fails the SAME two with the new test excluded from the selection, so this predates the spec and is not the binding change | fixed 2026-08-24, and the row is misfiled: the mechanism is not a parallel collision. Running `ze doctor --json` on the fixture by hand kept the payload the runner drops, and it carries two `doctor-iface-missing` diagnostics at ERROR severity, so `ready` is false and the process exits 1. `checkInterfaces` (`internal/component/doctor/checks_linux.go`) emits that code for a configured `ethernet` entry with no `/sys/class/net/<device>` directory. The fixtures configure `eth0` and `eth1`; this dev box carries `ens18` alone and the QEMU VM carries `eth0` alone, which is also why `vrrp-doctor-quiet` (eth0 only) looked load-sensitive while `vrrp-idle` (eth0 and eth1) was deterministic: they differ in how many absent NICs they name, not in how much load they see. The doctor check is right and stays. The TEST was wrong about what it asserts: `ze doctor` reports on the whole machine, so its exit status asserted the host's NIC inventory when the property under test is that the VRRP check stays quiet. Both files drop the doctor exit assertion and assert the payload (`schema-version`) plus the existing `doctor-vrrp-*` rejects. `vrrp` is 9/9 in QEMU and 7/7 on the dev box |
| 2026-08-24 | fixit-eap-tls-escape-hatch-kills-the-daemon | `global_cleanup` (`test/interop-ipsec/lab.py` (retired; now `internal/le/interoplab/ipsec/`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->), the ipsec lab's fixed addresses | The shared global is a docker network address, and the cleanup is scoped so it can never reclaim one. `ZE_CONTAINER` and `SWAN_CONTAINER` carry a per-process `_SUFFIX`, and `global_cleanup` removes exactly those three names, so a container left behind by a KILLED run is outside every later run's reach: no name collides, `Scenario.setup`'s pre-clean does not see it, `docker network create` accepts a network that already exists, and the leftover keeps holding 172.28.0.2 or 172.28.0.3. That scope is verified by reading the function. What is NOT established is whether it caused the failure that found it: `eap-tls13` failed once with `docker run ze-ipsec-ze-696272 timed out after 60s` while a stale `ze-ipsec-swan-2262758` was present, and the retry PASSED, so the timeout is unattributed and was not chased | not fixed, and the scope gap and the timeout are two claims, only the first of them measured. It blocks no goal of the spec that found it. The repair is a pre-clean that reads `docker ps -a` for the `ze-ipsec-` prefix rather than three exact names, and the question it opens first is whether a lab may remove a container it did not start, which is the wrong answer for two developers sharing one box and the right one for a suffix nobody else uses |
