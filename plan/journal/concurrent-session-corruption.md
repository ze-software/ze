| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-06 | ipsec-transport-nat-selector-substitution | `main` at HEAD, over `internal/component/ike/engine/fsm.go` | Fourth occurrence of the caller-without-callee shape the 2026-09-04 `local-ca` row names, and this one is by this spec's own commit. `2e5da6d394` ("feat(ike): substitute transport-mode selectors behind a NAT") carries a seven-line hunk in `buildPeerTLSConfig` setting `cfg.CRLPEM = ca.CRLPEM()`, which is an RFC 9190 EAP-TLS revocation change and has nothing to do with NAT. Its two producers, `(*CACertEntry).CRLPEM` in `internal/component/pki/store.go` and the `CRLPEM` field of `eap.PeerTLSConfig`, are another session's UNCOMMITTED work, so `internal/component/ike/engine` does not compile at HEAD: `cfg.CRLPEM undefined`, `ca.CRLPEM undefined`. Found by building the IPsec lab from a tree extracted at HEAD, which is the only build in this repository that does not start from the working tree | not fixed, and not this session's to fix: removing the hunk deletes the wiring the pki session's uncommitted `rfc9190_crl_wiring_test.go` is written against, and committing their `store.go` is the cross-commit `ai/rules/git-safety.md` forbids. Their commit repairs it. The measurement it blocked was taken in a scratch copy with the hunk removed there and nowhere else. The habit named by every earlier row would have caught it: `git diff HEAD -- <path>` on every file the commit script names, `fsm.go` included |
| 2026-09-04 | fixit-redistribution-chain-drops-silently | the reachability argument for a `panic()`, `mustNameImportingProtocol` (`internal/component/config/redistribute/route.go`) | The design half of the row below, which a peer session wrote after paying 100 minutes for it. I added that panic and justified it the way `docs/contributing/ze-go-style.md` asks: I traced both callers, found each passes a name from the consumer registry, and concluded no peer can reach it. That was TRUE of the final state and FALSE of every intermediate one. The retired `IngressFilter` still existed in the shared tree for the hours between my hardening and my deletion, and it passes `""` on every inbound route, so for that window the panic was reachable from the wire on any config at all. 174 daemon panics and 144 of 670 plugin cases red, in another session's work, attributed at first to their own commit | not fixed, because the state that caused it no longer exists. The lesson is about the ARGUMENT rather than the code, and it is not about panics. ANY guard that refuses a value is judged against the callers that exist while it LANDS, not the callers that will exist when the change is finished. In a single tree the caller set is a fact; in a shared checkout it is a moment, and `docs/contributing/ze-go-style.md` says trace every caller without saying when. The inverse shape is `spec-fixit-filter-subject-drops-five-attributes`: a guard whose zero was quietly load-bearing until a legitimate zero arrived. Same question, opposite axis, and neither is visible to a gate run in the tree that holds both halves. A hardening and the deletion of the caller it hardens against belong in ONE commit, or the hardening waits for the deletion. Mine were hours apart and everyone else paid for the gap |
| 2026-09-04 | - (sent to judge five reds attributed to `367b0c359b`) | `./le functional plugin` over the shared checkout, and the three producers a peer session held uncommitted | `./le functional plugin` answered `fail 525/670`, 144 red, spread over adj-rib-in, every filter family, communities, prefixes and the route server. It reads as a wide product regression and it is none: 174 daemon log lines say `ingress filter panic, rejecting route ... panic="BUG: redistribute: the importing protocol has no name, and an empty name is not a verdict"`, so every inbound route was dropped before any policy filter ran. Three producers, two of them uncommitted work in flight: `initRedistribute` (`internal/component/bgp/config/loader_create.go`, modified) now calls `redistribute.SetGlobal(redistribute.NewEvaluator(rules))` for a zero-rule config where it used to install nothing; `IngressFilter` (`internal/component/bgp/plugins/redistribute_ingress/filter.go`, committed and unchanged) passes through only while the global evaluator is nil, so a config with no `redistribute` block now reaches `ev.Accept(route, "")`; and `(*Evaluator).Accept` (`internal/component/config/redistribute/evaluator.go`, modified) guards that empty name with a new `mustNameImportingProtocol` panic. The panicking half landed in the working tree ahead of the half that retires `redistribute_ingress`, which the new comment already describes as retired | not fixed, and not this session's to fix: every edited file belongs to a live peer session mid-spec. What the class needs is the discriminator, because the shape here is indistinguishable from a real wave and from the full-disk class: `./le verify worktree` checks out HEAD detached and runs the same suite there, and it answered `pass 661/670` with zero panic lines. So a plugin wave in a shared checkout is attributed by running the suite against the COMMITTED tree before any of it is read as a regression, and the five reds this session was sent to judge all passed at HEAD |
| 2026-09-04 | - | `d0affb5e4`, `docs/plugin-development/metrics.md` | The same defect as the row below, by the same hand, about an hour later, after that row had been written and after I had named the habit that prevents it. My commit added ONE table row for `ze_iface_link_events_queued_while_blocked_total`. It carries twenty insertions: six `redistribute-orchestrator` metric rows, two paragraphs describing them and a `<!-- source: ... -->` anchor, all of it another session's uncommitted documentation for `internal/component/bgp/plugins/redistribute_egress`. I named `docs/plugin-development/metrics.md` in the commit and did not diff it, which is the precise check the previous row prescribes. Caught only because the commit's own diffstat said 20 insertions where I expected 1, and I read it | not reverted, deliberately. The content is another session's finished prose and it is correct as far as it can be judged; removing it would DELETE their work from the tree, where committing it early only moves it. What it costs them is real but smaller: their own commit will show no diff for those lines, so their work landed under someone else's message with no attribution. They were told. The lesson the earlier row got wrong is that the fix is a habit: it is not, because the habit failed within the hour under exactly the conditions it was written for. It wants a mechanism. The commit tooling already refuses another session's STAGED files, and the same guard cannot be extended to unstaged edits because nothing in `le` knows which hunks this author wrote. What it CAN do is print the per-file diffstat against HEAD before it writes the script, so a 20-insertion file the author believes is a 1-line change is visible without being looked for. Proposed to the owner rather than added unilaterally to shared tooling |
| 2026-09-04 | - | `557f401028`, `internal/component/plugin/process/process.go` | I committed another session's uncommitted line and broke the tracked build at HEAD. My change was the PATH fallback in `startExternal`; the commit also carries `"ZE_PLUGIN_CA_PEM="+string(p.acceptor.RootPEM())`, which I did not write. It was in the working tree when I first read the function, from a live pki refactor, and `./le commit create` stages the WORKING TREE version of every file it is given, so naming `process.go` swept their in-flight line in with mine. `git show 557f401028^:...` has no `CA_PEM` line at all, `git log -S "RootPEM()"` names my commit as the one that introduced it, and HEAD has no producer: the method is defined at `internal/component/plugin/ipc/tls.go:492` in the working tree and nowhere at HEAD. About twelve of the 22 red stages another session's verification run reported are this one break, lint in every flavor, all six staticcheck parts, tracked-build, platform-vet, unit-cached, alloc and the .ci cache warm | RESOLVED, by the session that owned the other half, not by me. `8788a29f8b` landed the pki local-root work including the producer, so HEAD now carries `RootPEM` at `internal/component/plugin/ipc/tls.go` beside the consumer my commit had orphaned, and the chain builds. That is the repair the owner chose when it was put to him, and it arrived while the options were being weighed. The account of why I did not do it myself is kept because the reasoning stands for the next time: Committing the producer is refused by the session that owns it: `tls.go` is mid-refactor, it DELETES `CertFingerprint`, `TLSConfigWithFingerprint` and `GenerateSelfSignedCert` and moves with four other files, so landing any subset makes HEAD worse. Removing the line from HEAD means editing the working tree copy the pki session is still using, committing, and restoring it, which destroys their edit for the window and races their own commit. Escalated to the owner instead. The mechanism is the one this class already records and I had already been bitten by once tonight and caught: the commit script refuses another session's STAGED files and cannot see an unstaged edit to a file the commit names. The cheap habit that would have caught it is `git diff HEAD -- <path>` on every file named in a commit, read before running the script, not just on the ones you remember touching |
| 2026-09-04 | local-ca | `main` at HEAD, over `internal/component/plugin/process/process.go` and `internal/core/diagnostic/codes.go` | Two foreign commits carried this spec's uncommitted files, and one of them left `main` unable to COMPILE for eleven hours. `557f401028` ("fix(plugin): the engine directory is a PATH fallback") committed `process.go` with `"ZE_PLUGIN_CA_PEM="+string(p.acceptor.RootPEM())` in it, while `RootPEM` lives in `ipc/tls.go`, which stayed in the working tree. `git show HEAD:internal/component/plugin/ipc/tls.go` declares no `RootPEM` method, and a build at HEAD fails with `p.acceptor.RootPEM undefined`. `5193201e75` ("fix(bgp): a dynamic group with no role is reported") did the same to `codes.go`, carrying the two `doctor-pki-ca-root-*` codes. Nothing reported either: the commit script's staged-path guard is what should have, and the row above this one records why it cannot. Found at closure, while building a clean worktree at HEAD to establish whether nine functional failures predated this spec: the BASELINE would not build | not fixed here, and the repair is not this spec's: the guard is `renderBlock` (`internal/le/commit/script.go`) and the row above names the window. What this occurrence adds is the COST. The two earlier rows in this file describe a commit carrying an extra file; this one describes a commit carrying HALF of a two-file change, which is the shape that breaks the build rather than merely widening a diff. A cross-commit that splits a caller from its callee is not a tidiness problem. It is an outage on the default branch that no gate catches, because every gate this repository runs starts by building the WORKING tree |
| 2026-09-04 | command-help-and-description | the generated commit script's concurrency guard (`renderBlock`, `internal/le/commit/script.go`) | The guard cannot close the window it exists for, and it went BOTH ways in one session. Each block reads `git diff --cached --name-only` and aborts on a path the block did not name, and it does that BEFORE its own `git add`. `git commit` then commits whatever the index holds when IT runs. Everything staged by another session between the check and the commit is carried, and the guard has already passed. Measured, not inferred. Commit ad6a5bd7f0 named 4 journal files and landed 13: it carried another session's L2TP and RADIUS interop work, their `plan/spec-radius-acct-session-attributes.md`, their `plan/verification-debt/5db0ba0d.md` and a scenario config, under a message about journal rows. In the other direction and within the same hour, that session's commit 92e5562dc6 carried a `plan/journal/guard-added-to-one-half-of-a-pair.md` row this session wrote, which is why this session's own script then found nothing to add for that file. Two earlier commits the same day, d69ce9aa48 and a4c6a3da7d, took a journal row and a whole config reader (`entryLongHelp` in `internal/component/cli/completer.go`) from this session the same way | not fixed. The guard is checking the right thing at the wrong instant, and no ordering of check-then-add closes it while the index is shared: the window is between the last add and the commit, so a per-block guard cannot see into it. What would close it is committing from a temporary index (`GIT_INDEX_FILE`) so the block's staging is private, or `git commit -- <paths>` with the explicit pathspec the block already knows. Both make the commit name its own population instead of trusting a shared index to stay still. This is the fifth recorded instance in one day, and the first where THIS session was the one that took another's work |
| 2026-09-03 | none (unblocking `internal/component/doctor/doctor.go` on the owner's instruction) | `./le commit create`, the window between PREPARING a script and RUNNING it | Second occurrence of the row below, with a consequence that row does not name. I prepared a script naming three doctor files, and between the `create` and the `bash` another session's omnibus commit `a4c6a3da7d` (`feat(bgp): reject a path that carries a named ASN at a named position`) swept the whole working tree and took all three. My script then staged nothing but its own verification-debt row, so `4ba24d0ff9` is a commit whose SUBJECT describes the doctor check and whose CONTENT is three lines of debt bookkeeping. The fix I had made in that window, removing a doubled `checkRIRDelegationSources(tree)` call that would have reported every RIR diagnostic twice, is now in history under a subject about BGP path filtering, with nothing saying it happened. Verified at HEAD: one call each, so the de-duplication survived; only its attribution did not | not fixed, and not fixable by the author after the fact: the destructive git verbs that would un-write `4ba24d0ff9` are banned, so the misleading subject stays and this row is the correction. The row below asks for a working-tree comparison at commit time; this occurrence adds a cheaper and narrower ask, because the damage here was done by TIME rather than by content. The script could re-check, at the moment it runs, that the paths it names still differ from HEAD, and refuse with 'another session committed these while your script waited' rather than committing an empty shell under a full message. A commit that stages nothing it named is never what its author meant |
| 2026-09-03 | - | `./le repository tracked-build check` over HEAD, host and installer flavors | Same class as the 2026-08-24, 2026-08-28 and 2026-08-29 rows: a CONSUMER committed without its PRODUCER, and the tracked tree has not compiled since. `e691533a60`, 2026-08-31, subject `feat(cli): a command declares its summary and its explanation`, landed `node.Help` readers in `internal/le/wikicatalog/catalog.go` and `internal/component/web/handler_admin.go`. `internal/component/command/node.go` at HEAD carries `LongHelp` and no `Help`, so both flavors fail with `node.Help undefined (type *command.Node has no field or method Help)`. Three days standing. Walked into by running the check straight after an unrelated commit, which is what the 2026-08-24 row prescribes: the check is the only one that compiles what GIT holds | not fixed, and not this session's to fix. The producer is session dd5fc94a's uncommitted edit to `internal/component/command/node.go`, and committing another session's in-flight field is the cross-commit `ai/rules/git-safety.md` forbids. The owning session lands it. Recorded because the check judges HEAD rather than a commit, so every later commit reads as the one that broke the build, and the next session to run it will diagnose the wrong change |
| 2026-09-03 | rir-delegation-source-override | `internal/core/diagnostic/codes.go` and `internal/component/doctor/doctor.go`, the two CENTRAL enumerations a new doctor check has to edit | Two uncommitted hunks vanished from the working tree inside about 90 minutes, and both files came back holding another session's hunk alone. `codes.go` lost the `codeDoctorRIRSourceRefused` const and its `builtinCodes` entry while keeping `doctor-bgp-peer-no-role`; `doctor.go` lost `checkRIRDelegationSources(tree)` from `runChecks` while keeping `checkBGPPeersWithoutRole(tree)`. Neither loss is visible from `git status`, which reports the file modified either way, and neither is visible from a compile, because a doctor check with no caller is legal Go. The tell was a RED unit test in the new check's OWN package: `TestDoctorRIRSourceCodeIsRegistered` (`checks_rir_sources_test.go`) reported `Lookup(...)` nil, which is the code being unregistered rather than anything about the check. The mechanism is a read-modify-write from a buffer taken before the edit, and it cannot be attributed after the fact: a working tree records no author. What makes this class STRUCTURAL here rather than bad luck is the shape of the surface. `runChecks` is a hardcoded list of about thirty calls and `builtinCodes` a hardcoded slice, so EVERY new doctor check by every session edits the same two files, and two sessions adding a check in the same week collide by construction (`ai/rules/principles.md`, "a new feature MUST register itself"). The same shape then blocks the repair: `doctor.go` cannot be committed at all, because the peer's line names `checkBGPPeersWithoutRole`, whose definition is in their uncommitted `checks_config.go`, so a commit naming that path leaves HEAD unable to compile | the `codes.go` half is re-applied and committed here, beside the peer's row, and the check plus its three tests land with it. The `doctor.go` half is NOT committed and the check is unwired at HEAD, which this spec's closure reports rather than hides: the one line is re-applied in the working tree so the peer's own commit of `doctor.go` completes the wiring, and by then `checkRIRDelegationSources` is already at HEAD. Detection that cost nothing and is worth keeping: the new check's package carried a test asserting its diagnostic code is REGISTERED, not merely spelled, so the silent loss of an edit in a file the check does not own still went red. What would end the class rather than count it is a doctor check that registers itself, as plugins already do, which would delete both central lists |
| 2026-09-03 | none (walked into fixing the arm64 readiness cluster) | the generated commit script's concurrency guard (`./le commit create`, `internal/le/commit`) | The guard refuses a concurrent session's STAGED files and cannot see a concurrent session's UNSTAGED working-tree edits to a file the commit NAMES. `git add -f -- <path>` takes the working tree, so session 7c2e06d5's commit fcf8d815d3 (`fix(test): preflight bind addresses on the peer path too`) carried nine lines of this session's uncommitted readiness change in `internal/test/runner/runner_exec.go`, comment and all. The change is correct and is now in main, but its commit says nothing about it: the subject is bind-address preflight, so a reader looking for when the daemon readiness handshake was rekeyed finds nothing, and it landed with no discrimination proof of its own. The near-miss is worse than the miss: this session held a pre-edit copy of that file and almost restored it, which would have reverted the other session's committed work with no git verb involved | NOT fixed. The guard's population is the index; the exposure is the working tree. A commit that names a path could compare that path's working-tree content against what the author's own session last wrote to it, and refuse a difference it did not make; what that costs, and whether a session can even know its own last write, is a design question this row does not settle. Producers read 2026-09-03 |
| 2026-09-02 | - | `internal/component/bgp/reactor`, the forward path-identifier rail, observed under `go test -race` over the working tree | `TestRelayMultiPathPreserved` (`reactor_api_relay_test.go:725`) reports a data race on one address. The WRITE is `fwdRegenerateRawPathIDs` (`forward_path_id.go:354`) copying the payload into the destination buffer `buildFwdBody` took from the read-buffer pool, on the test goroutine inside `RelayStoredRoute`. The READ is `fwdReleaseWithdrawnPathIDs` (`forward_path_id.go:472`) walking a cache entry's body from `(*RecentUpdateCache).evictLocked` on a `fwdPool` worker, reached through `Release`. So a buffer the eviction walk still reads is being written as a destination copy, which means the pool handed out memory the cache had not finished with. This session met it while changing `session_prefix.go`, which shares no state with either rail | FIXED 2026-09-03. The question this row left open had one answer: `evictLocked` and `Delete` (`recent_cache.go`) each called `ReturnReadBuffer` and `returnFwdHandles` BEFORE `fwdReleaseWithdrawnPathIDs`, and that walk parses the Withdrawn Routes and MP_UNREACH sections, which are slices into the buffer just freed. A goroutine taking the slot between the two statements makes the walk read the next UPDATE, so the identifiers released belong to no path and the ones this UPDATE withdrew leak for the session's life. The walk now runs first at both sites. The order, not the race window, is the defect, so `TestEvictionWalksTheBodyBeforeItFreesTheBuffer` asserts the order in the source; it was observed RED under the old order and GREEN under the new one |
| 2026-09-01 | eap-notification-and-nak | `internal/core/eap/eap.go`, `NewSession`'s default arm, during a hand-applied discrimination break | An agent proving its NEW UNTAGGED sweep discriminates replaced `return nil, fmt.Errorf("%w: type %d", ErrUnsupportedMethod, methodType)` with `m = newMSCHAPv2Method(config)` ON DISK, for about fifteen minutes, prolonged by job-queue contention. Two other sessions read the tree inside that window. A peer's automated security review flagged it, correctly describing it as a silent downgrade of every unnamed method type to the weakest method; this session then VERIFIED it at the producer against `git show HEAD:`, confirmed it as real, told the peer so, and instructed an agent to fix a defect that did not exist. The evidence rule was followed exactly and still produced a false finding, because in a shared checkout a producer read is a read of one MOMENT: `git diff HEAD` over the same function five minutes later shows the arm untouched. The cost was a peer's review time, this session's verification, three messages, and a false claim passed to a third party, which the peer had already carried to Thomas as a product security regression before either of us re-read. **The sharp point, in the peer's words: the window does not merely produce a wrong answer, it produces a wrong answer that SURVIVES verification, because the evidence rule's own remedy is what produces it.** THREE occurrences on 2026-09-01 across two sessions: this one, and session `single` forcing red phases for two untagged tests by editing `internal/le/rfc/check_baseline.go` and `docverify.go` on disk for about ninety seconds each. Those two went unnoticed only because nobody sampled that window, which is luck rather than method | not fixed. The protection already exists and does not reach this case: `./le rfc discriminate-record` applies its break through a Go OVERLAY precisely so that, in the words of `docs/contributing/rfc-conformance-gates.md`, "no file on disk is modified and a concurrent session in the same checkout sees nothing". That route is available only for an RFC-TAGGED unit. An author verifying that an UNTAGGED test discriminates, which `ai/rules/testing.md` asks of every new test, has no overlay route and edits the file. The repair direction is to give the hand-verification the same overlay: a `./le` action that applies a named break to a named producer under an overlay and runs one test, usable for any test rather than only a tagged one. Until then the cheap mitigation is that a finding about a hunk another session is actively editing is re-read before it is reported, and that the reader asks the editing session rather than the tree |
| 2026-08-31 | - | `test/weakened.md`, the same ledger a third time in one evening | A correct read of the contract does not avoid the collision. That is what the two rows below do not yet say. The header prescribes replacing the last commit's rows. So I read `git status --porcelain test/weakened.md` first, found it clean at HEAD, and replaced three committed rows. A peer then needed the file for its own commit and asked me to give it back. It also read my row as having displaced a third session's row, which it had not. Clean at HEAD says only that the last author committed. It says nothing about who is about to write | not fixed. The ledger is one shared path with no owner and no reservation. The collision is in the mechanism rather than in anyone's care. Two shapes remove it. A per-session ledger the commit script reads and deletes, the way `./le commit create` already writes a per-session script and debt file. Or a row keyed by the commit session id, so several authors coexist in one file. Three occurrences on 2026-08-31, across three sessions, counting the two rows below |
| 2026-08-31 | fixit-rfc-drain-quota-never-armed | an incomplete signature change in `internal/component/l2tp/ppp/lcp_options.go`, `WriteLCPOptions` gaining a second return value with eleven call sites left unclosed | The correctness rule already exists and this is not a new one: `ai/rules/principles.md` measures a change by what it can REACH rather than by the files edited, and `gate-excludes-part-of-its-population.md` requires a commit's file set be closed under "declares what the included files reference". What neither says is the AMPLIFICATION, which has a different owner and a different cost. In a private checkout an open signature change is a style preference and the author pays for it in one build. In a nine-session checkout it stops `le` itself rebuilding, so every peer loses `./le commit create`, and a peer with a red of its own cannot separate it from yours: one session had two failing `cmdutil` tests it could not diagnose and correctly chose to re-measure later rather than reason about a signal it could not attribute. That is the same starting point as three wrong ownership claims made the same night, each of which began with a signal that looked interpretable and was not. **The instruction the author owes an agent is therefore stronger here than the rule: close every call site in the SAME edit as the signature, and treat compilation as outranking the feature.** Ownership was also recoverable only by luck: an untracked test file happened to sit beside the change. The same night, an unrelated break in `internal/le/rfc/` was attributable only because a session had written its spec name into a re-seal comment, and this session guessed its owner wrong and passed that guess to a peer before checking | not fixed, and the fix is not a gate. Nothing can refuse an intermediate working-tree state, and it should not: the tree is red mid-edit by design. What would help is the ownership half, since both recoveries tonight were accidents. A session that cannot ask "who is editing this path" reasons from the most recent thing it saw, which is right often enough to feel reliable and wrong often enough to cost a peer an hour |
| 2026-08-31 | fixit-rfc-drain-quota-never-armed | the same two ledgers, one session later, recorded because occurrence count is what earns this class its fix | Third and fourth occurrence of the row below, on the same night, between a different pair of sessions. `test/weakened.md` refused my commit while it held four rows from landed commit `6ff7353d7`, and `test/rfc-changed.md` would have refused it while it held two uncommitted rows of another session's. What the row below does not say, and what cost the most here: **nothing tells an author the file is replace-per-commit until the gate refuses them.** The contract is 45 lines into `test/rfc-changed.md`, under a heading a reader reaches after the format, the carrier table and the who-writes-the-row section, and `test/weakened.md` states it only by reference to that file. The refusal text is where an author learns it, which means every author learns it by being blocked once. Clearing another session's uncommitted rows also cannot be done safely without asking that session, and asking took two message round trips; the second one is what established that the rows were stale and safe to drop. A gate that refuses is doing its job, but a gate that only teaches its contract by refusing has made a first offence mandatory | not fixed. Same repair direction as below, and this occurrence adds one requirement to it: whatever replaces the shared path must let an author see, without committing, whose rows are in the file |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | `test/rfc-changed.md` and `test/weakened.md`, the two REPLACE-PER-COMMIT ledgers, read by `internal/le/commit/rfcchange.go` and `internal/le/commit/actions.go` | Both files are single shared paths whose own contract says "Delete the rows of the last commit. Write the rows of this one. This file never accumulates." That works for one author and cannot work for five sessions committing from one checkout. The BLOCKING failure is benign and is what happened tonight twice: `test/weakened.md` still carried two rows from landed commit `01d0474da`, and the gate refused a commit naming rows it does not weaken until they were deleted; then `test/rfc-changed.md` already held two uncommitted rows from session `15-interrop-fix` for a different commit, so writing six rows for this one would have destroyed that session's record. The SILENT failure is the one that matters and no gate can see it: session A writes its rows, session B replaces the file, session A commits, and A's commit lands carrying B's approval record. The file is present and the row count is plausible, so `rfcchange.go` passes it. For `test/rfc-changed.md` specifically that publishes a forged-by-accident record of an OWNER decision into git history, which is the single thing that file exists to make impossible: its own "Who writes the row" section states that a row with no answer from the owner behind it is a forgery, not a shortcut. The stale-row case is already evidence that nobody checks: deleting `01d0474da`'s two rows was only safe because this session first confirmed they were in HEAD, and no gate asks for that check, so the next session will not make it | not fixed. Two sessions coordinated by hand instead: `15-interrop-fix` commits first and messages when the file is free. That is the 2026-04-13 row's remedy, "coordinated manually after identifying file ownership", reached again eight rows later on a different artifact, which is the recurrence this class exists to count. The direction a repair would take is a per-session namespace rather than one shared path, so that concatenating the sessions' rows is the gate's job and not an author's discipline; the shape is not specified here, because a defect walked into gets one row and the work in hand closes (`ai/rules/completion.md`). What earns the fix is this class file now holding rows from two different sessions describing the same mechanism. **A third shared artifact, same night, same family, recorded here rather than in a second row because the mechanism is identical:** `ai/RFC-REQUIREMENTS.md` and the 178 shards under `rfc/requirements/` are regenerated by `./le rfc index-update`, and regenerating them is nobody's job by default. It happens when a session trips over the staleness. `./le rfc check` had carried a stale-index violation for hours while five sessions read it as somebody else's red, and when one session finally ran the command it rewrote shards for `rfc1661`, `rfc2918` and `rfc4760` as well as its own, so the index had drifted from at least FOUR sessions' work. The commit then publishes every session's requirement ids under one session's message, which is the accidental-forgery shape one file over: a diff whose subject describes a third of what it carries. Two sessions coordinated by hand again, agreeing that the one whose message can honestly name the regeneration carries it. An unowned shared generated artifact in a five-writer checkout has the same failure as an unowned shared ledger, and the same repair direction: give it an owner, or make its refresh a step the gate performs rather than a chore a reader notices |
| 2026-08-29 | site-renderers-in-go | `./le commit create` under `ZE_REPO_ROOT`, judged by what reached two PUBLISHED artifacts | The debt writer writes `plan/verification-debt/<session>.md` into whichever tree `ZE_REPO_ROOT` names, and two sibling trees that are published artifacts have now received one. `gh-pages` HEAD carries `plan/verification-debt/c7beceff.md`, committed by "website: publish generated site" and live on ze-software.net, where no route map explains it and no source produces it; the wiki received `plan/verification-debt/d97ae77d.md` through `ZE_REPO_ROOT=../wiki ./le commit create`, into a repository that had no `plan/` directory at all. Found from both ends independently, by a route reconciliation here and by the session running the wiki sync, which is what makes it a class rather than a slip: the route is normal usage and it recurs on every sync | not fixed. The debt row belongs to the SESSION and to the ze checkout, never to whatever tree a commit happens to be prepared against, so the writer should resolve its path from the le checkout rather than from `ZE_REPO_ROOT`. Recorded rather than fixed because the writer is `internal/le/commit` and neither session that met this owns it, and because removing the two published files is a deletion of user-visible work |
| 2026-08-29 | - | `internal/component/bgp/plugins/nlri/ls/types_descriptor.go`, judged by `./le repository tracked-build check` over HEAD | Third recurrence of the 2026-08-24 and 2026-08-28 rows, same shape and a different symbol. Commit `cf4cbb558` landed `types_descriptor.go` calling `uint16ToBytes`, and the definition of `uint16ToBytes` sits in `types.go` in that session's UNCOMMITTED work. Five of six flavors fail `undefined: uint16ToBytes`, so HEAD does not compile and the red is charged to whichever session next runs the check after its own commit, which is how this one was found | not fixed. `types.go` is another session's uncommitted file and committing it here is the cross-commit `ai/rules/git-safety.md` forbids, so the owner lands it. Recorded because the class now has three occurrences inside six days and every one of them is a commit whose helper stayed behind: the population a commit needs is the CALLEES of what it carries, and no gate reads that before the script runs |
| 2026-08-19 | fixit-cli-format-default-everywhere | a shared DOC, not a shared plan log | commit `5e501176e` published another session's uncommitted text. Its closure agent named `docs/architecture/api/commands.md` and `docs/features/formatting.md` in an explicit `--file` list, which is the discipline `ai/rules/git-safety.md` asks for, and it did not help. A second session's agent had rewritten both files minutes earlier to say the `fill` pipe operator was removed, which was false: only its `overall` way was going. The commit carried 42 and 53 lines of that text under a message about the CLI output format, so a reader of either the message or the diff learns nothing about what actually changed. It also left `./le docs-to-code index-check` red at HEAD on two anchors naming `displayInChain`, which blocked every commit in the checkout for both sessions. The rule already names this trap for a shared single-file plan LOG and says the ban on bare staging verbs "fixes staging TIMING; it cannot fix staging GRANULARITY". The same sentence is true of any file two sessions edit, and a doc is the likelier case, because two sessions working the same subsystem write the same page | fixed forward in `143d475e9` by the session that wrote the text, which is the right owner: whoever holds the uncommitted content commits it, or the correction reproduces the capture with the roles swapped. History is not rewritten. The durable lesson is one line and it generalises past both sessions: an explicit `--file` list fixes WHEN a file is staged, never WHOSE content is in it. Before committing a shared surface, read the diff of each file rather than trusting that you wrote it |
| 2026-08-14 | - | spec claim marker | subagents share their parent's spec claim, so concurrent phase agents on DIFFERENT specs overwrite each other's. `spec-session.sh` keys the marker `tmp/session/.session-${sid}` on `_session_id`, which resolves through the single resolver `.claude/hooks/lib/session_id.py` (retired; now `internal/le/hookruntime/session.go`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->; a subagent runs inside the parent session and resolves the same id. `.claude/rules/planning.md` states each session records its spec in its OWN marker so "many agents editing main concurrently never collide", which holds across SESSIONS and not across subagents within one. It matters here because this repo delegates every phase by default, so the colliding case is the normal case rather than an edge. Observed with three agents claiming three different specs in sequence, each replacing the last. The claim drives the WIP cap, the post-compaction recovery path and the Stop hook's open-work check, so a stolen claim silently mis-states all three | not fixed. The main thread can re-claim after a subagent finishes, which is what happened here, but that is a manual repair and it only works when someone notices. A marker keyed on the agent as well as the session would remove the collision; the rule's wording needs correcting either way, since it currently promises an isolation the mechanism does not provide |
| 2026-04-13 | - | workflow | another session's commit picked up our uncommitted files via shared staging | coordinated manually after identifying file ownership |
| 2026-04-16 | - | workflow | concurrent `./le verify current mode full` corrupted the shared log file | waited for other session's verify to finish |
| 2026-08-15 | - | shared checkout, uncommitted behaviour change | A behaviour-changing edit that is CORRECT and UNCOMMITTED costs every other session in the checkout, and the cost is misattribution rather than breakage. Two guards landed in the working tree and could not be committed: the RFC 4271 Section 5.1.3 refusal (`originatedNextHopIsPeerOwn`, `internal/component/bgp/reactor/forward_next_hop.go`) and the peer-block directive guard (`validatePeerBlockDirectives`, `internal/test/runner/peer_contract.go`). Between them they redden any fixture giving a session's two ends one address, and any `.ci` line inside a peer block that no consumer claims. Both were verified across all 23 functional suites. Neither could be committed, because the 18 repaired `.ci` fixtures also carry another session's uncommitted `process { }` to `attach process { }` grammar, which HEAD's parser cannot read, so committing them would put unparseable files at HEAD. Measured cost in one day, in both directions: this session attributed `test-pipe-first-last` to the other session's migration, which their own A/B runs had already excluded; and that session attributed six failures to this guard against a tree ten minutes stale. Neither party was careless. Both were reasoning about a tree changing underneath them with no `git log` entry to read | mitigated, not fixed. The guards' own failure output carries the explanation at the point of failure -- the log line names the RFC and the action, and the parse error names file, line, directive and reason -- so WHAT and WHY are answered even uncommitted. What is missing is WHO and "this is not in HEAD", which a commit message would have carried. Every active session was messaged with the two signatures. The general point is the one worth keeping: in a shared checkout, the window between a correct edit and its commit is a period in which other people's evidence is quietly wrong, so a change that alters what a test does is worth landing sooner than one that does not, and a change that CANNOT be landed is worth announcing |
| 2026-08-17 | staticcheck-feature-tag-matrix | shared spec corpus | another session's in-progress `plan/spec-ze-website-0-umbrella.md` cites six child specs absent on disk, making `./le doc wiring` and the retired `scripts/dev` (current producer: `internal/le/`) package red | attributed the red and used this spec's scoped passing gates while the owning session completes the children |
| 2026-08-18 | - | generated rule render | Running `./le rules render-update` with another session's uncommitted POINT edit in the tree renders their text into `ai/rules/<rule>.md`, and committing that render publishes their content while its source stays uncommitted. HEAD's render then carries a row HEAD's points do not produce, so `./le rules render-check` disagrees with itself, and here the published row also named the retired `scripts/dev/ze-run.sh` (current producer: `internal/le/job/job.go`), an UNTRACKED script, which turned `check_doc_links.check_hook_names` red at HEAD (a `./le doc wiring` stage). Checking the render's diff SIZE is what missed it: 2+/1- looked like one own edit and was two, one per session | fixed: committed the one-line point source so HEAD's render matches HEAD's points. The general guard is to diff a generated file's CONTENT, never its size, before committing it, because a render is a fold over sources several sessions are editing |
| 2026-08-19 | - | parallel editing subagents, one working tree | The general shape: verification that reads a SHARED MUTABLE workspace cannot bound what it owns, so every red it meets is attributed to somebody else. A main thread ran several editing agents at once in one checkout. Each agent's build and gate runs read a tree its siblings were mid-edit in, and no agent could tell a sibling's half-finished edit from a foreign session's, because a working tree records no author. Observed: a closure agent reported the retired `ze-functional-appliance-test` (current: `./le functional appliance`) unbuildable and attributed the break to another session, quoting a 3-argument call against a 2-argument signature in `internal/component/bgp/plugins/adj_rib_in/rib.go`. That edit belonged to a sibling agent of the SAME main thread, mid-refactor, with its new file still untracked. The misattribution then licensed two `--structural-red-ok` overrides, so a red the thread owned was recorded as a red it did not. The same shape makes a full `./le verify current mode full` dishonest while any sibling is editing, because the run certifies a tree nobody authored | not fixed in the tooling. The practice that holds without it: an agent that CHANGES files must verify against a workspace whose contents it controls, or its verdict is about a tree nobody wrote, and a red must not be attributed to another session without evidence naming the author. Serialising the editing agents, or giving each one an isolated worktree, removes the condition rather than the symptom. The isolation route is filed at `plan/spec-parallel-agents-verify-a-tree-nobody-authored.md` |
| 2026-08-23 | fixit-flowspec-table-never-swept | a shared TEST file, uncommitted rows, and the gate that was supposed to notice | An uncommitted edit to `internal/test/runner/exclusive_group_test.go` vanished from the working tree inside a one-hour window, and the file came back byte-identical to HEAD. Two cluster rows registering the `flowspec-nft` exclusive group were written at about 09:30 and were gone at about 10:30, with `git diff HEAD` on that path empty. NO commit in the window touched the file. This session first reported the loss as a sibling committing over it in `be05e65c6`, and that was WRONG: that commit is 2026-08-22 15:52, eighteen hours earlier, and its version of the file never carried the rows. The mechanism is NOT established and the removing agent cannot be named after the fact, because a working tree records no author. Two candidates fit the evidence equally and it does not distinguish them: a path-scoped `git checkout --` or `git restore`, which move no HEAD and so leave NO reflog entry for the reflog to exclude, and a read-modify-write from a buffer another session read before the edit, which is live here because a sibling was editing the same package that morning and left `internal/test/runner/caps_declaration_lint_test.go` untracked in it. What let it survive an hour unnoticed is separable, and it is the more useful half: the gate those rows feed, `TestContendingFunctionalTestsDeclareExclusiveGroup` (`internal/test/runner/exclusive_group_test.go`), iterates a HARDCODED `clusters` slice literal, so DELETING a cluster row drops that cluster out of the gate's population instead of reddening it. Its `minChecks` fails closed on a glob matching nothing, and nothing fails closed on a cluster nobody lists, so the run that appeared to confirm the rows was green with them and would have been green without them | rows re-applied and confirmed with `git diff HEAD` on the path, which reports the 9 added lines, rather than by re-running the gate, which cannot tell the two states apart. The practice worth keeping costs one command: after editing a file in a shared checkout, confirm the edit with a diff on that path, never with a gate whose own population that edit defines. Not reproduced on demand and not attributed, which is why both mechanisms are named rather than one being guessed; the diff is the cheap detection that covers either. The gate's blind spot is left open and is the larger find: a hand-maintained cluster list that shrinks in silence is `gate-excludes-part-of-its-population` living inside a ratchet, and closing it needs a ratchet over the SET of clusters rather than over each cluster's members |
| 2026-08-24 | fixit-config-list-readers-assert-the-wrong-shape | a shared source file, two SIBLING specs, and `main` at HEAD | Recurrence of the 2026-08-19 row, one step worse, and between two specs of the SAME session rather than two sessions. Commit `0cc2cf949` named `internal/component/firewall/config.go` in its `--file` list for its own `parseFlowtable` hunk, and the path also held `spec-fixit-ordered-list-loses-its-order`'s `parseChain` hunk, which imports `internal/core/configorder`. That package was UNTRACKED. So HEAD carried an import of a package git did not hold, and `main` did not build from a fresh clone until `d687efe7e` landed it. The same commit published the sibling's rule text: the point it added names `configorder.Entries`, `internal/core/configorder`, `Tree.ToPluginMap` and `configorder.OrderKey`, while its own message states "the rule point names no symbol from the unlanded ordering work". Nobody was careless twice; the `--file` list was explicit and correct, and an explicit list of PATHS still carries whatever each path holds (`ai/rules/git-safety.md`) | The break was repaired forward by `d687efe7e`, by the spec that owned the package, which is the right owner. What is worth keeping is that the detection already EXISTS and costs 45 seconds: `ai/rules/git-safety.md` requires `./le repository tracked-build check` immediately after any commit carrying a `.go` path, because it is the only check that compiles what GIT holds rather than what the working tree holds. It was not run, and it is exactly the check an import of an untracked package cannot survive. Re-verified at this spec's closure: the check is green on all six flavors, and every package imported by every file `0cc2cf949` touched has tracked `.go` files. The rule needs no change; the habit does. After a commit in a shared checkout, run the tracked-build check rather than trusting that a green working tree means a green HEAD |
| 2026-08-23 | - | `./le discovery-index update`, and the commit that would have carried its output | The regeneration wrote `ai/PACKAGE-MAP.md` rows for `internal/core/configorder` and `internal/core/configvalue`. Both were another session's uncommitted work, so the commit naming that artifact would have published a derived file that describes code nobody can see. `commit_helper.py` refused it in the right words: regenerated from a tree holding sources this commit does not contain. It cost two attempts. Recurrence of the 2026-08-18 rule-render row above, one target over, and sixteen `make ze-*-update` targets read the working tree with no warning | regenerated from HEAD plus this session's own files, which the gate then accepted. The point `repo-maintenance/canonical-sources-and-sync-direction/a-regeneration-reads-the-whole-working-tree` requires a regenerated artifact to be diffed before it is named in a commit, and carries that recipe |
| 2026-08-24 | - | rule rendering, shared checkout | **The commit that DOCUMENTS this defect committed it.** `3acfe680b`, subject "rules(repo): a regeneration reads the whole working tree", rendered `ai/rules/repo-maintenance.md` while its point file was dirty in the tree, so the rendered rule landed carrying a `journal-row-shape` row its own point did not have. `./le rules render-check` is STRUCTURAL, so a stale HEAD blocked commits for every session, and a regeneration in a clean tree wanted to DELETE the row rather than keep it. Invisible from every working tree in the building: the gate reads the tree, and this checkout held the point HEAD lacked, so every local run answered green. Three siblings the same night, each found only by an isolated `git clone --local` at HEAD: `internal/core/configorder` committed as a consumer with its producer left behind (`0cc2cf949`), `rfc/requirements/rfc4486.md` stale against a tagged test committed without its regeneration (`8ad11a6f7`), and this. Three different gates, `./le rfc check`, `./le rules render-check` and `./le changed scope`, each answering about a tree nobody was committing | fixed in `637cb5b7d`, which commits the point so the two agree. `ai/rules/planning.md` was the MIRROR case, render BEHIND its point, and a peer's instruction to land both point files would have moved the red rather than cleared it; `b75ff1335` moves point and render in ONE commit. **The rule the four instances share: a generated file and its source MUST move in the same commit. When they have already drifted, the DIRECTION decides the repair, so read which side is ahead before regenerating. A regeneration that wants to DELETE content is saying the render is ahead of its source, never that the content is unwanted.** What would end the class rather than count it: sixteen `ze-*-update` targets derive from the working tree and not one warns |
| 2026-08-25 | - | `test/weakened.md` and `test/rfc-changed.md`, the per-commit ledgers | A per-commit ledger read from DISK means whoever leaves rows in the tree holds the repository. The asymmetry is exact, and it is worth stating precisely because half of the gate is right. `weakened_problems` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)) keys what a commit WEAKENS on that commit's own `--file` and `--remove` lists, and its docstring says why: "several sessions share this checkout, so a check that read the working tree at large would refuse a commit for a colleague's in-flight weakening". It then delegates to `check_weakened_tests.py`, which pairs those weakenings against the rows of `test/weakened.md` ON DISK, unscoped. So the WEAKENINGS are per-commit and the ROWS are per-checkout. Any row another session is holding for its own pending commit refuses every commit that does not perform that exact weakening, with "names X, which this commit does not weaken ... delete it". Measured today: five foreign rows refused one commit five times, once each. The only documented remedy is deleting another session's pending evidence, which `ai/rules/never-destroy-work.md` forbids at rung 1, so two correct sessions deadlock. It cost `fix` and `system` forty minutes on `test/rfc-changed.md`, which has the identical contract and the identical reach, and it cost `fix` and this session about an hour on `test/weakened.md`. The file cannot express two pending commits, and nothing in it says so | not fixed, and the workaround is not a fix. It was cleared by hand: `fix` moved its seven rows to a scratch file with an md5, told this session the window was open, and restored them after `49e001628` landed. That works and does not scale, and it needs a conversation each time. Two shapes would end the class rather than count it. Key each row to the commit that will carry it, so the file can hold more than one session's pending rows and each commit reads only its own. Or scope the ROW side the way `weakened_problems` already scopes the weakening side, and ignore a row that names no test in this commit's file list rather than refusing on it. The second is the smaller change and it is where the asymmetry already lives |
| 2026-08-26 | le-is-a-ze-binary | compiled `le` UI fixtures, beginning with `leDiscoveryAnswers` (`internal/test/fixture/ui_fixture_le_discovery_answers.go`) | The functional runner executes compiled fixtures with the checkout as their current directory. Ten fixtures treated `os.Getwd()` as private scratch and built their temporary `le` binary at `<cwd>/le`; three more detected the collision and failed. `leDiscoveryAnswers` first removed that path, then `go build -o` replaced the tracked POSIX launcher with an ELF binary. The all-suite run was stopped when the active build command exposed the overwrite. | Every `le` fixture now gets a system temporary workspace and binary from `temporaryLEFixtureWorkspace`; the discovery fixture has a regression test that requires an out-of-checkout `<workspace>/le`. The suite remains stopped until the owner authorizes byte-exact restoration of the overwritten launcher. |
| 2026-08-28 | le-is-a-ze-binary | `internal/le/scratch/scratch.go`, judged by `./le repository tracked-build check` over HEAD | Commit `a374622db` landed `deviceOf` returning `stat.Dev` with no conversion. `syscall.Stat_t.Dev` is `int32` on darwin and `uint64` on linux, so against the function's `uint64` result the tracked tree does not compile on the build host: five of six flavors (distro, test-runner, appliance, setup, host) fail with `cannot use stat.Dev (variable of type int32) as uint64 value in return statement`. Only `installer` passes, because it cross-compiles for linux, which makes the break read as arch-specific rather than as a commit that never compiled here. The repair already exists uncommitted in the same file, `uint64(stat.Dev)`, and it cannot be landed on its own: that file also carries the migration's export renames, `CheckoutID` to `checkoutID`, `ScratchTarget` to `scratchTarget` and `CacheTarget` to `cacheTarget`, whose only callers sit uncommitted in `actions.go` beside it. Committing the one-line fix alone therefore moves the red rather than clearing it | not fixed. The four files under `internal/le/scratch/` land together or not at all, and the session that owns the migration is the right owner. Recorded because `build: vendor every tool le runs` inherited the red: the check judges HEAD, so a commit that names none of these paths still reads as the one that broke the build, and the next session to run it will diagnose the wrong commit |
| 2026-08-29 | fixit-linger-rejection-reaches-no-verdict | `internal/component/bgp/reactor`, judged by building a detached worktree at HEAD `f3379e684` | Fourth recurrence of the 2026-08-24, 2026-08-28 and 2026-08-29 rows above, same shape, three symbols at once. Commit `f3379e684`, subject `fix(bgp): keep the Prefix-SID inside the domain unless told otherwise`, landed consumers whose producers stayed in the working tree: `payloadHasAttr` is called from `forward_rs.go` and `reactor_api_forward.go` and defined in the UNCOMMITTED `forward_local_pref.go`, `propagatePrefixSID` is read in `forward_prefix_sid.go` and declared in the UNCOMMITTED `peer_forward_facts.go`, and `buildStaticRouteUpdateNew`, `toStaticRouteUnicastParams` and `toPluginParams` each gained a parameter in `peer_initial_sync.go` that their definitions do not have. So HEAD does not compile, and no session can run a functional suite against it: `./le functional plugin` stops at the isolated-binary build. Found while trying to run AC-4 in a clean worktree at HEAD, which is exactly the route this class keeps breaking | not fixed. Every producer is another live session's uncommitted file and committing them here is the cross-commit `ai/rules/git-safety.md` forbids, so the owner lands them. Repaired forward by `ce992f2e7`, subject `fix(bgp): land the producers f3379e684 committed consumers for`, about 40 minutes later and by the owning session, which is the right owner; `./le repository tracked-build check` is green on all six flavors again. Worked around in the meantime by running the AC-4 mutation at `106d9e063` instead, one commit earlier, which builds. Recorded because the class now has four occurrences inside six days and the detection is still only `./le repository tracked-build check` run AFTER the fact: the population a commit needs is the CALLEES of what it carries, and no gate reads that before the script runs |
| 2026-08-29 | - | `internal/component/bgp/reactor`, judged by a race run over the working tree and a rerun in a detached worktree at HEAD `2958f7a18` | `TestRFC9552BGPLSAttributeWellFormedIsKept` (`rfc9552_test.go:95`) is RED: the BGP-LS Attribute count is 0 where the test requires 1, so the attribute is not on the wire. `TestNoConfigFeedsSentUpdatesToAReceivedOnlyPlugin` (`config_direction_test.go:72`) is RED beside it with 16 documents parsed against 95 refused; that one walks every config in the WORKING TREE, which currently carries a tree-wide prose rewrite, so it is probably an artifact of that. Neither could be judged at HEAD: the reactor TEST package does not compile there, with four arity errors in `peer_test.go` and `session_prefix_test.go` against `buildStaticRouteUpdateNew`, `toStaticRouteUnicastParams` and `countPrefixEntries`. That build break is the fourth-recurrence row above, and its consequence is this one: while it stands, no red in this package can be attributed to HEAD or to a working tree, by anybody | not fixed and not characterised further. Reproduction attempted and failed for the reason stated, so this is recorded rather than fixed (`ai/rules/completion.md`). The package is another session's live edit surface, 20-odd files modified, and this session was instructed not to touch it. Next step for whoever owns the reactor: land the producers the test files already call, then re-run those two tests and decide whether the RFC 9552 red is a real attribute-retention defect |
| 2026-08-29 | - | `./le verify deps alloc` over `internal/perf/allocgate.go` ceilings, producer `(*Session).checkPrefixLimits` (`internal/component/bgp/reactor/session_prefix.go:418`) | The gate exits 1 with three ceiling breaches: `BenchmarkCheckPrefixLimitsInstalled` 1 alloc/op against ceiling 0, `BenchmarkCheckPrefixLimitsOffered` 1 against 0, and `BenchmarkCheckPrefixLimitsInstalledChurn` 3 against 2. It is not the benchtime escape the ceiling's own comment warns about: the run pinned 300 iterations, which is the value that comment says makes the amortised zero legitimate. `session_prefix.go` and `session_prefix_count_test.go` are both clean against HEAD, so the change is in something `checkPrefixLimits` calls rather than in the benchmark. Met while closing `spec-wire-edit-3-deferred-ac9-dead-code`, whose AC-5 asks this gate to be green; the same run reports NO benchmark absent from the output, which is the property that spec's delete could have broken | not fixed. The fix lives in `internal/component/bgp/reactor`, which this session was instructed not to touch, and the producer cannot be bisected because the package's test build is broken at HEAD (row above). The gate is non-structural, so it takes a verification-debt row and refuses a push rather than a commit. Next step: whoever owns the reactor re-runs `./le verify deps alloc` once the test package builds at HEAD, and reads what `checkPrefixLimits` gained since the ceilings were measured |
| 2026-08-30 | generated-command-usage | `internal/le/commit/script.go`, `renderBlock`, judged by running a generated block against a repository another session had staged into | A refused commit script left the index DIRTIER than it found it, which is how two sessions deadlocked over one index for about an hour. `renderBlock` emitted `git add` and `git rm` BEFORE the concurrency guard, so a script that refused over a foreign path had already staged its own. Both halves of the deadlock arrived that way, from opposite directions: session 05845e68 ran `git mv` on a rule point, which stages by itself, and then a commit script aborted and added its own paths on top; session 5e5aef32 then ran its own script, which staged two `internal/le/docvalid` files before reporting the first session's. Each script's guard then refused the other's paths and neither could proceed. Nothing was mis-committed, because the guard did its job -- the cost is that the ONLY exit is `git restore --staged`, which `CLAUDE.md` forbids to every agent, so the deadlock necessarily reaches the owner. Third contention mode on one index this day, after staged-file collision and a transient `.git/index.lock` | fixed here. The guard is emitted first. It reads the same index either way, and its exclusion list names the commit's own paths, which are simply not staged yet at that point, so moving it up costs it nothing and it still sees every foreign path. `TestRefusedBlockLeavesTheIndexAsItFoundIt` runs a whole block with both an add and an rm against a foreign-staged repository and asserts the index is untouched after the refusal; against the old ordering it reports `staged = [gone.txt mine.txt tracked.txt]`, which is the defect exactly |
| 2026-08-31 | - | `closureStem` (`internal/le/commit/review.go:97`) reading `plan/journal/<class>.md`, a file two sessions had each added one row to | The journal is an ACCUMULATING file rather than one of the replace-per-commit ledgers the rows below describe, so two authors writing it is the designed use and neither had done anything wrong. The gate still serialises them. A commit naming the file carries EVERY added row, and `closureStem` derives the spec closure from the `Spec` cell of what it reads, so a peer's row citing `yang-short-and-long-command-help` made `./le commit create` demand `tmp/review/yang-short-and-long-command-help-<MY session id>.md`, an independent-review artifact for a spec this session neither owns nor can review. My own row cited `-`. The refusal names a spec the author has never heard of and an artifact path keyed to their own session, which reads as a broken gate rather than as a peer's row, and the only keyword that clears it is `review-override` on somebody else's review. That is the escape hatch this one must not use: taking it would publish a verification-debt row against another session's spec under this session's name | not fixed. Worked around by committing the part that did not touch the shared file (`46a965ad0`, two stale comments) and asking the peer to land the journal file with only its row. Two message round trips, the same manual coordination the 2026-04-13 row reached. The repair direction differs from the ledgers below because the file is not replace-per-commit and needs no per-session namespace: the gate should derive the closure stem from the rows THIS commit adds, attributed to the committing session, rather than from every added row in a file it happens to name. A row's `Spec` cell is a fact about its author, and the gate reads it as a fact about the commit |
| 2026-09-04 | spec-radius-acct-session-attributes | `internal/le/commit/script.go`, `renderBlock`, the window INSIDE the script between its own `git add` and its own `git commit` | Third occurrence, and it moves the window the row above proposes to guard. My script's concurrency guard passed, its `git add -f` staged nine files, and before its `git commit -F` ran, another session's commit `ad6a5bd7f0` (`journal: nine finds from the config help pass`) took every one of them. My `git commit` then reported "no changes added to commit" and exited 1, so the interop checker, its two new tests, the lab mock, the scenario config and two doc pages are in history under a subject about journal rows, and the message I wrote (which carried the xl2tpd source reading that settles this spec's open question) reached nobody. A re-check at script START would not have caught this: the theft happened AFTER the guard and AFTER the add, inside a window of milliseconds | not fixed. The narrower ask this occurrence supports: `git commit` in the generated script could name its paths (`git commit -F <msg> -- <paths>`), which commits the paths from the INDEX and fails loudly when they carry nothing, or the script could compare `git diff --cached --name-only` against its own path list immediately before committing and refuse with 'another session committed these while your script ran'. Either turns a silent misattribution into a refusal the author can act on |
| 2026-09-04 | fixit-tunnel-traffic-proof-is-one-directional | `docs/architecture/testing/interop.md` and `plan/journal/green-that-could-not-have-been-red.md`, committed by `367b0c359b` | The mirror of the row above, seen from the other side, and this time nothing failed so nobody could notice. The implementation session committed its Go in `16b2db78c9` and left its Phase 7 page edits uncommitted in the shared tree. Eleven hours later a BGP filter session's commit `367b0c359b` staged both files whole and carried them, so the fifth vacuity trap, the strongSwan lab drop-in section and the journal Fix cell all live in history under `fix(bgp): a policy filter reads the five attributes it was never given`. The content is correct and reaches every reader; only the attribution is lost, and closure found it only because it ran `git log -S` over its own sentence rather than trusting the spec's Documentation checklist | not fixed. The rule already says the page edit rides in the SAME work as the code (`ai/rules/documentation.md`), and the gap is that nothing checks it: a spec naming `docs/` files in Files to Modify could have its closure compare that list against the commits that carry the spec stem, and say which page landed somewhere else |
| 2026-09-05 | site-facts-from-committed-data | `check` (`internal/le/site/facts/staleness.go`) against `derive` and `warnUncommitted` (`internal/le/site/facts/sitefacts.go`, `actions.go`) | The gate and the regeneration it names do not read the same thing, and in a checkout several sessions share that gap has no honest closer. `check` materializes HEAD in a throwaway worktree and derives THERE, which is the property that makes its verdict the same in every session. `derive` takes its POPULATION from `git ls-files` but its CONTENT from `os.ReadFile` on the working tree, deliberately, because a person is meant to run `update` in the tree they are about to commit. So the moment HEAD drifts, the gate goes red at HEAD and the only regeneration that would clear it truthfully is one run in a CLEAN tree. Six sessions were in this checkout on the day it was measured, four of them holding Go edits, so no such tree existed. Running `update` anyway writes a number about nobody's commit, which is the class this whole tool was built to remove, and `warnUncommitted` says so on stderr without stopping it. Measured at commit `0e04097158dd`: `repo.detail_comments` committed `4,100+` and derived `4,200+`. Neither the drift nor the red belongs to any one session: annotation counts move a little in almost every commit | not fixed, and the fix is a design choice rather than a repair. Two shapes answer it. `update` could read CONTENT from the same materialized HEAD the check uses, which makes the file a pure function of the commit and makes the dirty-tree warning unnecessary, at the cost that a person can no longer regenerate ahead of the commit that moves the count. Or `update` could take a `commit <sha>` keyword, leaving the working-tree read as the default and giving a shared checkout a way out. The second is smaller and keeps the workflow the spec chose. Whoever picks one should read `docs/architecture/site-facts.md`, which states the pattern for the other sixteen producers and would inherit the same gap |
| 2026-09-05 | - (walked into while running fourteen implementation agents from one session) | `./le spec session claim` and `current`, over a session that spawns subagents | One session's subagents share ONE spec claim, so the marker answers for whichever agent claimed LAST and every earlier claim is silently gone. `resolveSessionID` (`internal/le/lepath/session.go`) reads `CLAUDE_CODE_SESSION_ID` before it walks the process tree, and a subagent inherits its parent's environment, so fourteen concurrent agents resolve to one id. `(o specOwner).Claim` (`internal/le/spec/session/session.go`) then `writeAtomic`s `o.markerPath()`, which is `tmp/session/.session-<SessionID>` (`:300`), with no per-agent component and no check that the marker already names a different spec. Measured: an agent claimed `spec-support-export.md`, `claim` confirmed it, and minutes later `current` answered `spec-crash-capture.md` from a sibling agent. The consequences compound in three directions. `current` is what `.claude/rules/post-compaction.md` recovery reads, so a compacted agent resumes on a spec it was never working on. `Release` (`:146`) removes the one marker, so any agent finishing first releases the claim of all thirteen others; the agent that met this correctly declined to call it, and had no other way to give its claim back. And the `ready`-to-`in-progress` transition still fires per claim, so the WIP tally counts every spec while the marker names one, which is how the in-progress count reached 36 against a cap of 12 | not fixed. The narrow fix is the one `tmp/commit-<session>-<tag>-<random>.sh` already took for commit scripts and that `pointer-shared-across-the-names-it-indexes` asks for twice: give the marker path a component that distinguishes agents within a session, and have `Release` remove only the claim it can prove it wrote. The wider question the row does not settle is whether a subagent SHOULD be a session at all for this purpose, since it shares the checkout, the index and the id but not the transcript. Until then an agent fleet claims for the transition and the WIP tally, never for `current`, and no agent in a fleet calls `release` |
| 2026-09-05 | kernel-capability-gate | `test/weakened.md`, the replace-per-commit ledger, fifth recorded occurrence across four sessions and six days | An agent wrote its commit's 15 rows into the ledger exactly as the header prescribes, and in doing so displaced another session's rows for a `bgp/rib` deletion. That session's commit is now refused until it rewrites them, and it has not been told. Nothing here was done wrong: the file's own contract says "This file is REPLACED for each commit. It never accumulates", the author followed it, and following it is what causes the collision. What this occurrence adds to the four above is that it happened inside a SIXTEEN-agent fleet from one session, where the coordination remedy the earlier rows fell back on -- message the peer, agree who commits first -- does not scale: there is no peer to message when the colliding writers are siblings that do not know each other exist. The 2026-08-31 rows already name the repair (a per-session namespace the gate concatenates, rather than one shared path an author must be careful with) and already say that occurrence count is what earns it | FIXED 2026-09-06. Both ledgers are now a directory of shards, one per commit session: `test/weakened/<session>.md` and `test/rfc-changed/<session>.md`, named by `lepath.CommitSession`, the same eight hex characters that already name the session's commit script, message and verification-debt shard. Two writers cannot reach one path, so no author has to be careful with another's rows; `./le commit create` refuses a commit that names a foreign shard, which closes the silent case where a commit publishes somebody else's justification under its own subject. A row whose text git holds at HEAD no longer refuses anything: the gate proves it landed and drops it from the shard the commit carries, which retires the delete-the-last-commit's-rows chore that three sessions had to perform by hand. `./le test-weakened check` prints every shard and its rows, so an author sees the population without preparing a commit. The evidence this row and the four above it record is what earned it; the SIXTH occurrence, found while implementing this, is that a session had already invented the same layout by hand (`test/weakened/c7ef7dc3.md`, committed by `8c7f0a5bf2` under a subject about the `.ci` parser) and a second session had already overwritten its rows there. Original entry: FIX COMMISSIONED, 2026-09-05, rather than counted a fifth time. The same shape in the commit tooling one file over -- a message file keyed on session and tag while its script carries a random suffix -- reached three occurrences today and is being fixed in the same pass. Both are one defect wearing two names: a shared artifact whose identity omits the writer. The ledger's repair must also satisfy the requirement the fourth row added, that an author can see whose rows are in the file WITHOUT committing, since every author currently learns the contract by being refused once |
| 2026-09-06 | crash-capture, vpp-isolated-cpus | `0e72b398f2`, over `internal/appliance/config.go`, `kernelargs.go` and `kernelargs_test.go` | SEVENTH occurrence, and the first that happened AFTER the author was explicitly warned about those exact files. The crash-capture commit carries another agent's `IsolatedCPUs`, `validateIsolatedCPUs` and `isolatedCPUKernelArgs` work, so `git log -S isolcpus -- internal/appliance/kernelargs.go` names a commit whose subject is "a kernel panic survives the reboot". Verified at HEAD, not inferred: those three symbols are in `git show HEAD:internal/appliance/kernelargs.go` and `git status --porcelain internal/appliance/` is empty. The owning agent had been told, in writing and by name, to `git diff HEAD` those two files before running its script and that the isolated-CPU hunks must be neither carried nor deleted; the sweep happened anyway. The same commit also REPAIRED an eleven-hour break in `cmd/ze/hub` caused by the same mechanism running the other way: a peer had swept `main.go` calling `harvestKernelCrashes` into `4e25253540` while `main_system.go`, which defines it, stayed uncommitted. So one commit is both a victim and a perpetrator of this class on the same morning | not fixed, and the habit-based mitigation is now DISPROVEN rather than merely unreliable. Six rows recommended care; the seventh happened under an explicit, specific, per-file warning delivered minutes beforehand, which is the strongest form that mitigation can take. Nothing is reverted: the destructive git verbs are banned and the content is correct where it sits, so what was lost is attribution and searchability, not code. The mechanical fixes this file has proposed twice remain unbuilt and are now commissioned: commit from a temporary index via `GIT_INDEX_FILE` so a block's staging is private, or `git commit -- <pathspec>` so the commit names its own population instead of trusting a shared index. Either makes the author's care irrelevant, which after seven occurrences is the only property worth having |
| 2026-09-06 | crash-capture, firewall-domain-group | `0e72b398f2` again, over `cmd/ze/hub/` | EIGHTH occurrence, same commit as the row above, a SECOND victim, and this one broke the build. The same commit swept a third agent's two `cmd/ze/hub/` edits, which call `rpc.RegisterDNSResolver`. That function was in the firewall agent's uncommitted work, so main carried a caller with no callee until `164f03607f` landed the other half. One commit therefore did all three things this class describes on one morning: it REPAIRED an eleven-hour break (a peer had swept `main.go` calling `harvestKernelCrashes` into `4e25253540` while `main_system.go` stayed uncommitted), it took one session's finished appliance work and published it under a subject about kernel panics, and it took a third session's half of a two-file change and left main unable to compile. Two victims, both killed mid-edit by the account rate limit and neither in a position to notice | not fixed. The commit script's guard reads `git diff --cached --name-only` and aborts on a path the block did not name, which cannot see an UNSTAGED edit to a file the commit DOES name -- and `git add -f -- <path>` takes the working tree. In a checkout where sixteen agents were killed mid-edit at once, every file is a candidate. What is new here is not the mechanism, which six earlier rows describe, but the DENSITY: three instances in one commit, in a window where half the tree is somebody's unfinished sentence. The fix is commissioned on the strength of these two rows |
| 2026-09-06 | rib-package-dead-surface | `./le session scratch ensure` (`internal/le/session`), against a `./le commit create file-list` argument written into what it returns | The scratch directory it answers is keyed to the PARENT session, so every subagent under one parent shares one directory and an ordinary filename is one file for all of them. A sibling agent's `>` replaced this session's `files.txt` between the write and the read, and the replacement was another session's commit population: nine `internal/test/runner/*.go`, three docs pages, two journal class files and twenty-six `.ci` tests. `./le commit create dry-run` then answered exit 0 over a 43-file population of which THREE were this session's, and every one of the other forty showed `+0 -0`. The zero-change display is the only tell, and it reads as reassurance. Two false findings were drafted off the corrupted comparison before the population was printed and read: that the weakening gate had stopped demanding rows for a test deleted from a surviving file (it had not; the corrupted list simply did not name the file), and that naming the legacy flat `test/weakened.md` dragged four unrelated `.ci` files into the gate (it did not; they were in the replaced list). Both were withdrawn | not fixed. Worked around by writing the list under a name carrying the shell PID and re-checking its md5 immediately before each use. The durable repairs are a per-AGENT scratch directory rather than a per-parent one, and a `create` that prints its resolved population before its verdict rather than after, since a population read silently from a file is the one input the author never re-reads |
| 2026-09-06 | ipsec-transport-nat-selector-substitution | `main` at HEAD, over `internal/le/interoplab/ipsec/checkers.go` and `test/interop-ipsec/parity_test.go` | The contamination `2e5da6d394` carried is THREE files, not the one the row above records. Beside the `fsm.go` CRLPEM hunk it also committed `checkResponderEAPTLS13RevokedClient` and its `scenarioCheckers` entry, and the `responder-eap-tls13-revoked-client` row in `ipsecScenarios`. `git ls-tree -d HEAD test/interop-ipsec/scenarios/` shows no such directory: its eleven fixture files are the same pki session's UNTRACKED work, so `TestEveryNativeScenarioHasCompleteInputs` has a registered scenario with no inputs at HEAD, on top of a package that does not compile. Found at closure by reading the parity list the commit changed, which the first pass had read only for the three rows this spec owns | not fixed, and not this session's to fix for the reason the row above gives: all three are one other session's feature and its commit repairs them together. What the two rows say jointly is that ONE contaminated commit put a compile error and a fixture-less test registration into `main`, and that finding the first hunk is not finding the contamination. `git diff HEAD -- <path>` on every path the script names, before running it, is what would have caught all three |
