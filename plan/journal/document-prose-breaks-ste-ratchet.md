| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-05 | fixit-send-names-its-destination (found running `./le doc check verify`) | `ze-bgp-conf:bgp/defaults/attribute` (`internal/component/bgp/config/yang/`), and three source anchors naming `getHelpExtension` and `Node.Help` | `./le doc check verify` is red for four reasons, none of them this session's. One config summary runs 382 characters and 65 words where the row renders 96 and STE Rule 6.3 allows 25: it is a paragraph about RFC 7311 Section 4.1 written into the SUMMARY slot, and it belongs in the `ze:help` beside it. Three anchors on `docs/architecture/api/commands.md:1608` and `docs/features.md:44` name symbols their file no longer declares. Both arrived with af79f468f, which gave 2,900 YANG nodes a description | unfixed, and left to the session that landed af79f468f. The summary slot has a length the renderer enforces, so a summary that outgrows it is not a style opinion: the row it renders into is 96 characters wide |
| 2026-08-16 | - | docs and plan prose | Concurrent edits increased STE habit counts | preserved the claims and rewrote the flagged sentences |
| 2026-08-16 | - | journal row | A hook mapping row added a semicolon and a frozen phrase. | Replaced them with finite sentences. |
