| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-09 | problem-journal | doc gates | 82 of 1611 source anchors named a symbol absent from the file they cite, all green | spec to verify the symbol list each anchor already carries |
| 2026-08-14 | fixit-learned-dead-paths | doc gates | `path_resolves` (`scripts/dev/check_doc_links.py`) tests a cited path with `os.path.exists`, so it measures the CHECKOUT rather than the tracked corpus. That is the same producer shape the deleted `path_problem` had, narrowed by `gitignored()` covering the build-output case. Recorded because the two specs that owned this class were deleted on 2026-08-14 once their tooling was found gone: `learned_staleness.py`, `learned_repath.py` and `plan/.learned-staleness-baseline` were removed with the journal migration in `2cff2050a`, and no make target survives. The live remnant is real: `make ze-spec-citation-check` still reports dead line-tokens in `plan/learned/HOOK-FRICTION.md`, and `plan/learned/` now holds only three aggregates | not fixed. The class needs a gate that reads the tracked corpus, not a spec against tooling that no longer exists <!-- doc-links: ignore (the journal migration in 2cff2050a removed this file, which is what the row records) --> |
| 2026-08-10 | doc-claims-are-checked-not-just-resolved | doc gates | `claim_is_declared` in `scripts/dev/code_to_docs.py` resolves a dotted claim by its member alone, so an anchor claiming `Peer.Shutdown` passes against `func (s *Session) Shutdown()`: the reference resolves and the receiver is unchecked | not fixed, needs receiver-aware resolution and its own measurement |
| 2026-08-10 | fixit-l2tp-sccrq-tunnel-id-zero | l2tp comments | the SUPPLIER is `docs/research/l2tpv2-implementation-guide.md`, whose own numbering runs to Section 26 and has no relation to the RFC's, which stops at Section 13. `rfc/short/rfc2661.md` copied that numbering into its headings and into eight Compliance Checklist anchors, and every author who read the summary then wrote guide numbers into the source: "HELLO Keepalive (Section 15)" became `S15` in `tunnel_fsm.go`, "Tunnel-Specific Traps (Section 24)" became `S24.17`, `S24.19` and `S24.25`. Numerically-valid-but-wrong numbers travel the same way and are worse, because they resolve: `S9.5` for the Tie Breaker AVP is Proxy PPP Authentication, `S4.2` for the shared secret is Mandatory AVPs. No gate reads a section number in a `// RFC:` header or an `RFC NNNN Section X` comment: `check_doc_links.py` resolves the PATH beside it and `make ze-rfc-check` reads `RFC requirement:` tags | 36 comment and string locations fixed in round 4 (`tunnel_fsm.go`, `tunnel_initiator.go`, `tunnel.go`, `reactor.go`, `reactor_dial.go`, `reactor_test.go`), including three inside returned error strings and a systematically shifted message-to-section map in `tunnel_initiator.go` (6.2 named SCCCN, 6.4 named SCCRP). The summary's five headings, its trap table and all eight guide-derived checklist anchors are corrected. 49 out-of-range citations remain, in 16 files: 14 this spec never touched, and 21 of the 49 anchors sit in the two it did edit for a different class, `session_fsm.go` 18 and `session_initiator.go` 3. The rest: `genl_linux.go` 6, `session.go` 4, `ppp/proxy.go` 3, and 11 others with 1-2 each. The convention: every tracked file under `internal/component/l2tp/` recursively, an anchor that names RFC 2661 explicitly, a bare `N` normalised to `N.0` because RFC 2661 heads its top-level sections that way, out of range when the number heads no line of `rfc/full/rfc2661.txt`. 226 attributed anchors, 49 out of range. The denominator moves as citations are corrected and the residual does not, because both halves of the class are invisible to it: an in-range wrong section, and a guide TRAP number that names no section for the regex to read. Resolving a section number against `rfc/full/<stem>.txt` is mechanical and nothing does it |
| 2026-08-10 | fixit-l2tp-sccrq-tunnel-id-zero | rfc checklist ids | a requirement id is anchored to the section its line cites and `_validate_id` (`scripts/dev/rfc_requirements.py`) refuses any disagreement, while `check_retired_requirements` refuses a vanished id. An id minted against a WRONG section is therefore frozen with it: `RFC2661-24.10-1` cites `(§24.10)`, RFC 2661 has no Section 24, and neither ratchet can be satisfied by correcting the anchor | the anchor is unchangeable, so the real sections went into the requirement TEXT, which `ai/rules/rfc-compliance.md` allows. Nine RFC 2661 ids carry a frozen anchor that names the wrong section: three are guide-derived numbers RFC 2661 does not have (§24.10, §24.12, §15) and six name a real section stating a different rule (§4.1, §4.2, §9, §9.5, and §10 twice). A rename route that preserves the ratchets, or an alias column, would fix the class |
| 2026-08-10 | fixit-route-removal-protocol-blind | l2tp ppp | the `IfaceBackend` comment in `internal/component/l2tp/ppp/session.go` says "AddRoute programs the kernel to reach the peer via pppN (spec-l2tp-6c-ncp AC-6)". The package calls `AddRoute` in `ipv6_service.go` only, for the delegated IPv6 prefix. No call installs the IPv4 peer route the comment describes, while `ncp.go` tries to REMOVE it on teardown | not fixed, needs the missing install or the retraction of the claim and its AC |
| 2026-08-10 | fixit-l2tp-sccrq-tunnel-id-zero | l2tp comments | a dedicated sweep for this exact defect is not a control for it. Round 4 corrected 36 sites and reported the six files it touched as clean; round 5 found 31 more in those same files and their siblings, every one NUMERICALLY IN RANGE, which is why a reader's eye and the out-of-range count both passed them. `S4.1` for "the Message Type AVP MUST be first" (the rule is S4.4.1) at 21 sites, two inside returned operator-visible error strings; `S4.4.2` for the Protocol Version AVP (S4.4.3) and for StopCCN session clearing (S6.4), the second putting the code in disagreement with the summary's own `RFC2661-9-1`; `S4.2` for the Challenge Response construction (S4.4.3, mandated by S5.1.1) at 10 sites; and two comments citing S8.1 as the AUTHORITY for tracking a peer's moving source port when S8.1 requires the opposite | 31 sites fixed in round 5 across 12 files, `RFC2661-4.1-2` annotated as the ninth frozen id. The in-range half of this class is invisible to every count that exists, so it grows silently: `plan/future/spec-rfc-anchor-resolution-check.md` |
| 2026-08-11 | - | rule prose, hook messages | `ai/rules/planning.md` "Enforcement (model phases)", `scripts/dev/review_gate.py:161` and the refusal text in `.claude/hooks/pretool-agent-skill.py` each described `c_model_phase` in `.claude/hooks/pretool-writeedit.py` as a live BLOCKING gate. The function went with the Opus 4.8 implementation model and the same rule section says so six paragraphs above. `check_hook_names` (`scripts/dev/check_doc_links.py`) reads `ai/rules/repo-maintenance.md` only, so no gate resolves a check name written in a rule, a comment, or an operator-visible message | all three corrected while adding the two-session handoff. The guard half stays homed at `plan/deferrals/rules-as-points.md` |
| 2026-08-13 | - | rfc 7999 ledgers, blackhole comment | found under spec-bcp194-6-blackhole, which stays open. two prose claims about code, both false, neither read by any gate. `docs/features/rfc-status.md` and `rfc/not-enrolled.txt` each said RFC7999-3.1-2 has no producer, because Ze originates no BLACKHOLE-tagged announcement. It does. `handleAnnounceBlackhole` (`internal/component/bgp/plugins/cmd/announce/announce.go`) attaches `attribute.CommunityBlackhole` and sends it to the peers the command selector names. The `blackholePropagationGuard` doc comment (`internal/component/bgp/plugins/filter_community/blackhole.go`) said Ze does not honor NO_EXPORT or NO_ADVERTISE on egress. It called RFC1997-Well-1, Well-2 and Well-3 gaps. None carries a `{gap}`. Each carries both polarities. `Reactor.wellKnownAllowsEgress` is the live egress gate. That second claim was load-bearing. It sized the remaining RFC 7999 sender-side work several times too large | all three corrected. The class needs a gate. A claim that an id is `{gap}`, or that a named symbol does not exist, is checkable against `rfc/short/` and the tree. Nothing checks either |
| 2026-08-14 | fixit-ike-responder-natt-port-float | ike RFC requirement tags | two tests tag `RFC7296-2.21.4-5` and prove something else. That requirement is "A peer receiving such an unprotected Notify payload MUST NOT respond". `TestErrResponderWindowDoesNotReflectToObservedSource` and `TestErrUnprotectedMessageDrawsNoCachedResponse` (`internal/component/ike/engine/rfc7296_notify_error_test.go`) both drive a forged IKE_AUTH REQUEST at the cached message id. Section 2.21.4 does not govern that case. For an unprotected request it says the node "MAY send a response". The governing MUST is `RFC7296-2.4-12`, Section 2.4, "Implementations MUST limit the rate at which they take actions based on unprotected messages". `rfc/requirements/rfc7296.md` derives 2.21.4-5's coverage from those four tag lines. The ledger therefore credits a Notify obligation with proof from a request test. Found in review round 2 of the spec, after round 1 found the SAME false claim in the prose of the two guards these tests cover | not fixed by this session, and the reason is a guard rather than a judgement. `.claude/hooks/pretool-writeedit.py` refuses an edit that changes an `RFC requirement:` tag and directs the change to Thomas. The comments in `engine/responder.go` and `engine/inbound.go` ARE corrected, so the code now names `RFC7296-2.4-12` while these two tests still name 2.21.4-5. Retagging is ratchet-safe. 2.21.4-5 keeps a positive and a negative at BOTH tiers, from `notify_error_test.go` and from `test/ipsec/ipsec-error-notify-no-loop.ci`. So `check_coverage_ratchet` sees no lost polarity. `check_evidence_ratchet` sees no lost kind or tier. `RFC7296-2.4-12` gains a second proof. Owner decision |
| 2026-08-15 | rfc4271-med-across-as | doc gates | `make ze-doc-verify` went red on `ai/digests/web.md`, whose anchor `templates/component/oob_error.html:2-3` names a file commit `aa43dd4cc` ("render the web interface through templ") deleted. The refactor moved every template to a `.templ` source and updated its own goldens, and left the hand-maintained digest naming the removed path | fixed the anchor to `component_oob_error.templ:6-7`, the `oobError` component that now writes the same out-of-band swap. The digest anchor gate is what caught it, one commit later and in a session working on something else |
| 2026-08-15 | fixit-ike-test-discrimination | docs/functional-tests.md, IPsec interop row | the row claimed "Dataplane checks gate on XFRM availability", which reads as a skip when XFRM is absent. Nothing gates and nothing skips: `wait_xfrm_sa` and `assert_esp_accepted` (`test/interop-ipsec/lab.py`) both call `log_fail` and then `raise AssertionError`. The same cell also enumerated "Ze as initiator (`01`-`05`)", stale against 16 scenario directories | fixed the fail-closed claim against the two producers. The enumeration was replaced twice and both replacements were false, measured against all 16 directories: the `responder-` prefix does not predict the role (`18-cookie-challenge` and `23-esp-form-change` have strongSwan initiating without it), and seven scenarios' `swanctl.conf` state no role at all. The claim was then DROPPED, because a summary page restating what a directory holds drifts whether it is written as a list or as a rule |
| 2026-08-15 | fixit-rfc-row-level-and-anchor-drift | rfc checklist levels | the earlier rows of this class are all about a wrong REFERENCE. This is the same failure in the LEVEL, and it is invisible to the same gates. `make ze-rfc-check` verifies that a row parses, that its id agrees with its citation, and that a tagged test exists per polarity; it never opens `rfc/full/<stem>.txt` to compare a row's TEXT or LEVEL against the sentence it quotes, and `rfc/extraction/README.md` states that bound. Two rows held Ze to MUST over sentences their RFC writes as SHOULD: `RFC7296-2.8-1` against §2.8.1 ("the SA created with the lowest of the four nonces used in the two exchanges SHOULD be closed by the endpoint that created it") and `RFC7947-x-3` against §2.2.3 ("this attribute SHOULD be propagated to other route server clients, and the route server SHOULD NOT modify its value"). Both were gated AND proven in both polarities, so every gate was green over text misquoting its source. Being stricter than the RFC is not a conformance loss; the misquote is that a reader cannot tell a deliberate choice from a transcription error | levels corrected under the same ids, no row deleted and no id renumbered, on the owner's authorisation. The second half is the trap: correcting a level then collides with a SELF-TEST FLOOR that cannot tell a corrected level from a deleted row. `GATED_FLOOR` (`scripts/dev/rfc_requirements_test.py`) counts gated rows and refuses a decrease, which is exactly the guard against deleting a row to reach green, so the honest correction and the dishonest deletion present identically to it. Lowered 222 -> 221 carrying the row and the RFC sentence in its comment, which is the only thing that distinguishes them. Still open, and NOT fixed here because it is a decision rather than an edit: `test_rfc7296_ids_are_neither_retired_nor_demoted` is named for demotion and cannot see one. It builds its polarity baseline from `r.gated` over the CURRENT rows, so a demoted id LEAVES the baseline instead of failing against it, and this change passed straight through it. Six of the 23 pre-pilot ids sit at SHOULD and five always did, so "every pre-pilot id stays gated" is not the missing assertion: a real level ratchet needs a recorded MUST-level subset and a place to record an authorised correction. Measured in review: there is NO level ratchet anywhere. None of the 23 `check_*` functions in `scripts/dev/rfc_requirements.py` reads `Requirement.level` against HEAD -- `check_coverage_ratchet` compares polarity sets, `check_retired_requirements` id sets, `check_evidence_ratchet` kind/tier -- and `GATED_FLOOR` is the only floor in the tree and covers `rfc7296` alone. So `RFC7947-x-3` went MUST -> SHOULD with no gate firing at all, and only the rfc7296 half of this correction cost anything. Any RFC without a floor can be silently demoted today |
| 2026-08-16 | - | forward rail doc comment | the doc comment on `ForwardUpdate` (`internal/component/bgp/reactor/reactor_api_forward.go`) carried three claims about the fan-out and all three were false. "IBGP peers receive the original bytes unchanged" is the load-bearing one: an IBGP destination records no AS_PATH edit and still records the LOCAL_PREF and MED decisions through `applyFactsLocalPref` and `applyFactsMED`, and a policy chain can replace its payload outright, after which `peerBaseWire` is not the source. "EBGP wire versions are lazily cached per ASN4/ASN2 variant" describes a cache the file's own body says was DELETED when the AS-path family was folded into the edit set. The zero-copy condition was stated as encoding contexts matching, when the real gate is `mods.HasModifications()` being false. The AS_PATH sentence was true but incomplete: it omitted the RS-client exception (RFC 7947 §2.2.2, transcode only) and the secondary-AS ordering (RFC 7705 §3.3). The class is the same as the 2026-08-13 row: prose about code, read by no gate, and this one had drifted through at least one refactor that rewrote the very mechanism it describes while leaving the paragraph above it | fixed: the block now answers "what does this peer receive" from which edits its facts record, and says so explicitly, because reading the answer off the session type is the mistake the old text invited. Found by a review agent, confirmed against the producers, and fixed on the owner's word that the comment is wrong. The class still needs its gate: nothing resolves a behavioural claim in a doc comment against the function under it, which is why a refactor that deletes a cache can leave the sentence describing it |
| 2026-08-16 | - | hook mapping | The Bash hook mapping named parent session `updatedInput` as a check. No producer function had that name, so `hook_table` refused it. | Removed the non-check row. The architecture prose still describes the mechanism. |
| 2026-08-17 | - | documentation source anchors | Source anchors named five identifiers removed by producer renames: `authenticatePasswordResult`, `isLocalTransport`, `authenticatePublicKeyResult`, `TacacsAuthenticator`, and `LocalUsersLive`. The source-anchor gate rejected the stale references. | Updated the anchors to the current identifiers. Regenerated `ai/CODE-TO-DOCS.md` so the reverse index matches the live anchors. |
| 2026-08-19 | - | independent-review gate | The general shape: a record whose SUBJECT and whose EVIDENCE arrive as two independent inputs, with a check on one and none on the other. `cmd_record` (`scripts/dev/review_gate.py`) built the header and the file hashes from `--spec` and `--files`, then wrote the body from `--findings-file` verbatim. The hashes were checked and the body was not, so an artifact could carry one subject's identity over another subject's evidence and satisfy every reader of it, `commit_helper.py`'s closure gate included. The same call had a second, quieter form of the class: `if fp.exists()` meant an unreadable evidence path degraded to `(none recorded)`, so a mistyped path recorded as a review with nothing to say. Both defeat `ai/rules/evidence.md`, which the gate exists to serve | fixed at the producer. `cmd_record` refuses an absent `--findings-file`, refuses a `findings` verdict carrying no findings, and refuses findings that name none of the reviewed files, since a finding names the file and symbol that produce the behavior. `FindingsBelongToTheReviewCase` (`scripts/dev/review_gate_test.py`) holds all three; its refusal halves return 0 instead of 2 against the pre-fix producer. The general practice this class keeps asking for: verify that evidence belongs to the subject a record names, and refuse absent evidence rather than defaulting it to empty |
| 2026-08-21 | - | rfc summary annotations | `ai/rules/rfc-compliance.md` requires every `{gap}`, `{not-applicable}`, `{partial}` and `{single-polarity}` annotation to name the code it judges, file and symbol, because an annotation that names no code is an assertion. The three `{single-polarity}` annotations in `rfc/short/rfc7606.md` (`RFC7606-3.h-1`, `RFC7606-3.h-2`, `RFC7606-5.1-3`) name none. Each argues from the RFC's own logic, which is why the omission reads as harmless, and each is the sole reason its requirement needs no negative test. No check reads the annotation body: `scripts/dev/rfc_requirements.py` parses the annotation KIND and its reason string, and nothing resolves a symbol inside it. Found during the `/ze-rfc-audit rfc7606` re-read of `RFC7606-5.1-3`, where the annotation's substance was re-derived from the RFC and holds | not fixed. Editing the annotation changes `requirement_sha` and stales every verdict under the row, so the correction and its re-audit belong together. The class needs a check that an annotation names a symbol the tree resolves |
