| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-07-30 | - | workflow | `make ze-rfc-check` failed because another session shifted tagged test line numbers | regenerated `ai/RFC-REQUIREMENTS.md` before verify |
| 2026-07-30 | - | workflow | same staleness from a different concurrent session's edits | diffed regenerated ledger to identify owning package |
| 2026-08-11 | rfc-ledger-per-rfc-shards | git | commit 7ec29b6e6 was an unrelated IPsec fix. It absorbed the split `ai/RFC-REQUIREMENTS.md` while the 177 `rfc/requirements/` files stayed untracked. HEAD then held an index citing files no commit provided | the closure commits those files. History is not rewritten |
| 2026-08-12 | - | workflow | `make ze-doc-verify` red on a README-only edit: `rfc/requirements/rfc1997.md` stale because another session's uncommitted `forward_wellknown_test.go` shifted four tagged line numbers | regenerated with `make ze-rfc-index-update`, which now carries that session's in-flight line numbers |
| 2026-08-15 | fixit-peer-process-event-filter | `internal/component/bgp/reactor/session_write.go`, `forward_next_hop.go` | 15 next-hop fixtures went red mid-phase (every `redistribute-*`, `bgp-redistribute-*`, `forward-mpreach-nexthop-self-two-peer`, `ddos-flowspec-announce`) on `withholding originated route: its next hop is this peer's own address`. Another session's uncommitted work DELETED `sessionEndsShareOneAddress(peer, local)` and dropped the local address from `originatedNextHopIsPeerOwn`, so the exemption that let a loopback fixture announce 127.0.0.1 to a peer at 127.0.0.1 is gone. The same suite was 602/602 an hour earlier on the same branch | not mine to fix: the files are that session's in-flight work and are not committed. Attributed by `git status`, by the producing function, and by the failing population being exactly the next-hop family. The owning session must finish or revert it |
| 2026-08-15 | fixit-peer-process-event-filter | `test/plugin/test-pipe-first-last.ci` | the fixture's observer polls `show bgp rib received count` and sees 0 for its whole 10s budget, so ze is never asked to shut down and the runner times out at 20s. Passed at 602/602 at 10:12 and failed on all four later runs of the same suite. REPRODUCED, and every change of this phase excluded one by one: the send permission refuses nothing in that run (0 refusals logged), the initial-sync sleep removal was A/B'd under `make` and the failure did not flip, and the remaining edits are the ExaBGP converter, a test snapshot and docs. Same window as the next-hop row above, in the same package | unresolved. Next step is one run of `make ze-functional-plugin-test` on a tree the other session is not editing, which is the only way to tell a real regression from that session's work. Do not re-attempt diagnosis while `git status` shows `internal/component/bgp/reactor/*` modified by another session |
| 2026-08-15 | web-htmx4-prepare | `rfc/requirements/rfc9568.md` | `make ze-doc-verify` and `make ze-doc-wiring-check` red on a change that touched no RFC file. The file cites `internal/plugins/vrrp/transport/transport_integration_linux_test.go:239` and the `RFC9568-5.1.2.3-1` tag now sits at :241: another session holds that file modified and uncommitted. Same mechanism as the 2026-08-12 row | not regenerated. `make ze-rfc-index-update` rewrites every `rfc/requirements/*.md`, and seven of them are held modified by other sessions, so regenerating would carry their in-flight state into my tree. Attributed by `git status` and by the two-line shift |
| 2026-08-16 | - | rfc ledger and audit records | `make ze-doc-verify`, `make ze-rfc-check` and the `./scripts/dev` package tests are red at HEAD for every session in this checkout. `ai/RFC-REQUIREMENTS.md`, `rfc/requirements/rfc7606.md` and `rfc/requirements/rfc8654.md` are stale against their sources, and seven RFC 7606 audit verdicts read SHIFTED or STALE. Commit `527714c81` moved lines in `internal/component/bgp/reactor/session_validate_test.go` and changed `test/plugin/rfc7606-relay-one-field.ci`, and regenerated neither ledger nor seal. Found while committing unrelated work, which names none of those files. Eighth row of this class | not fixed here. `make ze-rfc-index-update` and `make ze-rfc-reseal` clear all but two of them. `RFC7606-5.1-2` and `RFC7606-5.1-3` need `/ze-rfc-audit rfc7606`, because the `.ci` their verdict judged is what changed, and `ze-rfc-reseal` refuses that case by design |
| 2026-08-16 | - | RFC requirements ledger | `make ze-doc-verify` found RFC 4301 and RFC 4303 shards stale after concurrent source edits | regenerated all shards with `make ze-rfc-index-update` |
| 2026-08-17 | staticcheck-feature-tag-matrix | RFC requirements ledger | `make ze-doc-verify` found `ai/RFC-REQUIREMENTS.md` plus `rfc4271` and `rfc7947` shards stale after concurrent source edits | regenerated all shards with `make ze-rfc-index-update` |
| 2026-08-17 | staticcheck-feature-tag-matrix | RFC 4271 requirements | another session added positive and negative `RFC4271-9.1.2.2-2` tests while `rfc/short/rfc4271.md` retained `{gap}`, making broad `scripts/dev` tests red | attributed the mismatch to the producing session and did not edit its in-progress protocol work |
| 2026-08-19 | fixit-no-family-block-sends-no-end-of-rib | RFC requirements ledger, the regeneration itself | `make ze-rfc-check` was red on one stale shard. Commit `4d8251a26` left that drift behind, so this closure ran `make ze-rfc-index-update` to clear it, as the 2026-08-16 and 2026-08-17 rows above did. The generator reads the WORKING TREE. This checkout held another session's uncommitted `rfc/enrolled.txt`, `rfc/short/rfc1195.md` and `rfc/short/rfc5303.md`. The run rewrote 15 tracked shards plus `ai/RFC-REQUIREMENTS.md`, and their in-progress text went in. `rfc/requirements/rfc5303.md` now spells `packet/tlv_core.go DecodeP2PThreeWayTLV` where HEAD spelled a line number. `rfc/requirements/rfc1195.md` LOST a test binding their edits removed. The earlier rows record the regeneration as the fix and say nothing about what it absorbs, which is the gap this row fills | the working tree is left green, and NEITHER closure commit carries a shard or `ai/RFC-REQUIREMENTS.md`. They stay uncommitted for the session that owns the sources to commit beside them. The check is cheap and belongs before the regeneration, not after. `git status --porcelain rfc/` names every source the generator will read from a state git does not hold |
| 2026-08-19 | - | `rfc/audit/rfc7606.json`, `make ze-rfc-reseal` | `TestRFCRequirementsSelftest` and `make ze-rfc-check` red on 86 violations: 40 stale shards, 34 SHIFTED verdicts and 12 STALE ones. The 12 are a new mechanism for this class. Commit `e269e3e5a` swept the retired `rfc-test-change-approved:` markers out of the corpus, and comment text sits inside the unit `unit_shas` hashes, so 15 tagged units in 11 files changed sha on comments alone. `reseal` refuses that state by design and asks for a human re-read of tests whose assertions nobody touched. The edit-time guard answers the same question the other way: `_behavior_bytes` (`.claude/hooks/pretool-writeedit.py`) reads a comment-only change as no behaviour change | cleared: `make ze-rfc-reseal`, then a real `/ze-rfc-audit rfc7606` over the 12, then `make ze-rfc-index-update`. All 12 stay `enforced`. Before any re-reading, each recorded unit sha was shown equal to the same unit at `e269e3e5a^`, and every old-vs-new unit diff is comment-only. The two definitions of a mechanical edit disagreeing is the root cause and is NOT fixed here |
| 2026-08-25 | port-test-functional-to-le | `rfc/short/rfc9552.md` | `TestRealTreeIsGreen` was red on five violations. All were RFC 9552 MUST rows with no test and no annotation: `RFC9552-5.2.1.1-1`, `-5.2.1.1-2`, `-8.2.2-9`, `-8.2.3-5`, and `-8.2.6-2`. This was not staleness or a concurrent-tree effect. The summary was unmodified, and its last commit was `02ca02af6`. A tree-wide grep found none of the five identifiers in any `.go` or `.ci`, including modified files. The verification of an unrelated port found the defect | not fixed here or characterized further. Unproven MUSTs require Thomas's decision (`ai/rules/rfc-compliance.md`, "Leave a MUST implemented but unproven"). The port that found this defect touches no BGP-LS code |
