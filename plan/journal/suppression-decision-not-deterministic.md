| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-14 | fixit-peer-process-event-filter | plugin relay | `test/plugin/modify-oversize-suppress.ci` failed once in a full `make ze-plugin-test` run: the observer reported 11539 UPDATEs beyond the End-of-RIB reaching 127.0.0.2, the destination whose OPEN carries no extended-message capability and for which the oversize modification must be suppressed. The assertion is on state, not on elapsed time. Reproduction attempted: a second full `make ze-plugin-test` run passed all 589 tests, so the mechanism is unknown and the leak is not deterministic. The phase-1 change to that file is one config keyword and touches no delivery path | not fixed, one occurrence recorded. Next step is to run that selector under `scripts/dev/stress-repro.py` and, when it reproduces, read the size query in the modify path that decides suppression |
| 2026-08-15 | fixit-peer-process-event-filter | plugin relay | Second occurrence, same file and same shape: 16373 UPDATEs beyond the End-of-RIB reached 127.0.0.2 in a full `make ze-plugin-test` run. The run before it, on the SAME config, passed that test, so the config is not the variable. Phase 3 deleted that peer's `attach process test-oversize` block, which granted neither send nor a custom event type, so it changed no auto-load token and no API-sync count | not fixed, second occurrence recorded. Two occurrences now sit on one mechanism, which is what earns the deliberate pass named in the first row |
