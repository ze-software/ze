| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-14 | fixit-peer-process-event-filter | plugin relay | `test/plugin/modify-oversize-suppress.ci` failed once in a full the retired `ze-functional-plugin-test` (current: `./le functional plugin`) run: the observer reported 11539 UPDATEs beyond the End-of-RIB reaching 127.0.0.2, the destination whose OPEN carries no extended-message capability and for which the oversize modification must be suppressed. The assertion is on state, not on elapsed time. Reproduction attempted: a second full the retired `ze-functional-plugin-test` (current: `./le functional plugin`) run passed all 589 tests, so the mechanism is unknown and the leak is not deterministic. The phase-1 change to that file is one config keyword and touches no delivery path | not fixed, one occurrence recorded. Next step is to run that selector under the retired `scripts/dev/stress-repro.py` (current producer: `internal/le/stressrepro/run.go`) and, when it reproduces, read the size query in the modify path that decides suppression |
| 2026-08-15 | fixit-peer-process-event-filter | plugin relay | Second occurrence, same file and same shape: 16373 UPDATEs beyond the End-of-RIB reached 127.0.0.2 in a full the retired `ze-functional-plugin-test` (current: `./le functional plugin`) run. The run before it, on the SAME config, passed that test, so the config is not the variable. Phase 3 deleted that peer's `attach process test-oversize` block, which granted neither send nor a custom event type, so it changed no auto-load token and no API-sync count | not fixed, second occurrence recorded. Two occurrences now sit on one mechanism, which is what earns the deliberate pass named in the first row |
| 2026-08-21 | - | plugin relay | THIRD occurrence, same file and same shape: 12926 UPDATEs beyond the End-of-RIB reached 127.0.0.2 in a full the retired `ze-functional-plugin-test` (current: `./le functional plugin`) run, at 620/621. The three counts now recorded are 11539, 16373 and 12926, so the leak is not a fixed quantity either. The failing run took 191.6s against 129.4s and 158.5s for the two passing runs on the same tree, which puts it with the load-correlated rows in `gate-verdict-depends-on-the-machine.md` rather than against them | not fixed, and REPRODUCTION WAS ATTEMPTED AND FAILED, which is the one case `ai/rules/completion.md` allows to be recorded. The immediately preceding full run of the same suite on the same bytes passed 621/621, and so did the run before the change that preceded it. Phase 3 of this spec is implicated by proximity and not by mechanism: it moved the answer LINE WRITERS from `internal/component/plugin/dispatch.go` to `pkg/plugin/rpc/answer_write.go`, and no in-tree handler returns rows, so every `execute-command` answer still takes the value path that `TestExecuteCommandValueAnswerIsUnchanged` pins byte for byte against pre-change literals. The next step is unchanged and now overdue at three occurrences: run this selector under the retired `scripts/dev/stress-repro.py` (current producer: `internal/le/stressrepro/run.go`) and, when it reproduces, read the size query in the modify path that decides suppression |
