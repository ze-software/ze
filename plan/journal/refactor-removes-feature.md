| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-03 | none (walked into it while binding the fixture population to its producers) | four more hook gates the fixture population describes and no code implements | The row below found ONE lost gate this way. Binding all twenty-six categories through `hookruntime.Probe` found four more, and the count is what makes this a class rather than a coincidence. `commit-gate` names NO producer at all in `fixtureCategories` (its runner and owner are empty strings), and its eight rows describe a deferral gate in `./le commit create`; `checkSourceGates` (`internal/le/commit/prepare.go`) has none, and the only live enforcer of deferral language is `postDeferral`, a WARNING on a markdown write. `test-first`'s six rows describe a gate refusing a Go edit in a package with no test, with `_test.go`, `_gen.go`, `.pb.go` and `cmd` exemptions; nothing in `hookruntime` does that, and `writeSpecStatus`, which the row named as its owner, refuses something else entirely. `delegation-reminder` asserts the reminder says delegation `needs no permission` and cites `planning.md`; the hook writes one sentence carrying neither. `subagent-context` asserts the injected context carries `ONE message`, `Read the range`, `ai/rules/context-economy.md`, `select:LSP`, `gopls symbols` and a spec digest; `hookSubagentContext` emits none of them, which was verified by reading a context it actually injected. Every one of these is a rule an agent is told exists | The FIXTURES are fixed: each pair now states what its producer does, and all twenty-six categories but `script-weakening-arms` ask the producer through `Probe` or `ProbeLifecycle`, so no further claim can drift unseen (`e2eb1a0fb3`). The GATES are not rebuilt, and that is the open work: four behaviours the repository believes it enforces and does not. Deciding which of the four is worth rebuilding is a spec, not a session that tripped over them. The transferable point the row below could not yet make: one lost gate reads as an accident, five read as a property of the migration, and the only reason the other four stayed hidden for as long is that fixing the first one did not ask the same question of its neighbours |
| 2026-09-03 | finish-ci-coverage (at closure) | the spec-write design gate, and the fixtures that proved it | `eae2825926` replaced the Python hooks with Go and the T-4 behaviour did not come across. `writeDesignEvidence` (`internal/le/hookruntime/writeedit.go`) clears on ANY `.lsp-invoked-<id>` marker, and failing that on ANY `.source-read-*-<id>` glob hit: no `## Files to Modify` read, no per-kind demand, no per-kind clock. That is precisely the WIDENED state the spec says it replaced, restored. What makes this worse than an ordinary lost feature is that the FIXTURES survived and stayed green. `categoryDesignGate` (`internal/le/hookcheck/fixtures.go`) evaluates `strings.Cut(value, ":")` and passes when the two halves match, so it compares a synthetic string with itself and never invokes the gate at all. Fifty-two green fixtures said nothing about the producer, and the spec cited them as its proof. T-5's daemon-scoped `.ci` demand is absent from `hookValidateSpec` for the same reason | not fixed here: the closure that found it changes no product code, and rebuilding the subject match is its own work. The spec's claims were corrected in place rather than closed over, so the record no longer says a gate exists that does not. The transferable point, and the reason this is a class rather than an incident: a migration that keeps the FIXTURE NAMES and drops the behaviour they exercised leaves a suite that is green, is cited as evidence, and proves nothing. A fixture whose predicate never calls the producer cannot go red, so nothing in the tree could have told anyone. Look for the call, not the count |
| 2026-08-28 | - | lint sweep over the test fixtures | An `unparam` finding was taken as authority to delete a parameter, and the parameter was a PROTOCOL input. Every call site passed the same value, so the linter was right about the code and wrong about the subject: the argument selects a bit whose two values drive two different required receiver behaviours, and a builder that can only produce one of them can prove only half of the pair. Nothing went red, because no test exercised the value the fixtures could no longer build. Found by reading the sweep's report, not by a gate | reverted: the parameter is restored with its original position, and a `//nolint:unparam` on the function carries the reason and cites the clause. General practice: a linter judges the code as written, never what the code must remain ABLE to do, so a finding that proposes REMOVING a parameter, a branch or a return from test infrastructure is a coverage question before it is a lint question. Where the removed input is one a specification gives meaning to, the finding is suppressed with that citation rather than obeyed |
| 2026-06-15 | - | refactor | `update bgp peer prefix` removed in batch cleanup as 'bypassing editor' but it used the editor | restored from git history and adapted to current APIs |
| 2026-08-09 | kernel-compose-make-q-assertion-is-vacuous | installer kernel build | a Python builder replaced the shell recipe and moved the phony `FORCE` onto `$(OUT)/Image`. An incremental build became an unconditional one, unmentioned in the commit message | re-gated `FORCE` on a parse-time comparison of the requested arch-profile-builder against the recorded one |
| 2026-08-16 | web-htmx4-cutover | chaos dashboard Freeze control | htmx 4 has no trigger filter. `hx-trigger="every 500ms [!window._frozen]"` kept rendering and kept parsing: htmx 4 reads the interval and ignores the condition, so the checkbox stopped meaning anything at the cutover. 15 poll requests in 3 seconds with it ticked, measured in a browser. Nothing was red, because a golden fixture holds MARKUP and this is a runtime behaviour. No row of the upgrade's own surface map named the loss, and the scanner has no category for it | a marker attribute (`freezePoll`) on the eight polls the control owns, plus one listener canceling `htmx:before:request` when the request reports `ctx.sourceEvent.type === "every"` and its source carries the marker (`internal/chaos/web/render.go`). `TestFreezeStopsThePollsItOwns` makes a new poll a decision rather than a default, and `TestFreezeListenerReadsTheMarker` reds on 4 needles if the listener goes |
| 2026-08-31 | fixit-peer-pending-sync-settles-too-early | `checkNoFamilyEndOfRIB` and 14 sibling RFC checkers (`internal/le/interoplab/bgp/check_rfc.go`), reached through `checkScenario` (`check_engine.go`) | Found while auditing this spec's own interop citation. Each of those checkers is registered in `specialCheckers` (`check_special.go`) and its body is `return checkScenario(ctx, check, <its own scenario name>)`. `checkScenario` reads `scenarioOperations` (`checkers.go`) and answers `scenario %s has no typed assertions` when the key is absent, and the key IS absent for all 15: `bgp_test.go` refuses a bespoke checker that also has a generic entry, so the two halves of the mechanism contradict each other. Those 15 checks therefore cannot pass, whatever the foreign daemon does. `TestEveryCheckerFailsClosedWithoutPeerEvidence` stays green because it asserts only that a checker FAILS with no evidence, and `TestRFCInteropCheckerNames` accepts any error whose text contains the scenario name, which this one does. Introduced by the le-personality migration of 2026-08-27 and 2026-08-28 (`a374622db`, `eae282592`), not by this spec. `docs/features/rfc-status.md` cites one of the 15 as public evidence for RFC4724-4-1 | recorded, not fixed. It spans four protocols and 15 requirements, none of them this spec's subject, and this spec's goal is proven by `test/plugin/initial-sync-barrier-raw.ci` against a separate BGP peer process instead. Whoever takes it owes each of the 15 its typed operations, and owes the two unit tests a discrimination they do not have today: a checker that never reaches the lab must be told apart from one that reached it and found nothing |
| 2026-09-05 | shared-machine-job-admission (at closure) | `ZE_RUN_SLOTS`, read by `NewIn` (`internal/le/job/job.go`), and the admitted job population | The same le-personality port retired the Makefile that EXPORTED the slot count, so `SlotsDefault = 1` governed and a 32-core box ran one heavy job at a time. Measured: two `./le verify lint run` invocations waited 28 minutes behind one holder while 24 cores idled. The spec's phase 7 had derived the number as cores divided by the per-job share, and stated in words that four fit. The same port cost the admitted POPULATION: the rollout wrapped 106 make targets, those targets became native actions, and two of them admit today (`verify lint run`, `verify lock run`), so `./le verify current mode full` holds no slot at all while it runs 26 stages. Nothing was red either time. A default is what a test supplies for itself, so no test reads it, and a population that shrinks admits MORE work rather than failing | `defaultSlots` derives the count from `gotoolchain.CoresPerJob` inside the package that uses it, and `helperCommand` pins `ZE_RUN_SLOTS=1` so the serialization cases stop passing on a small box and failing on a large one. The population is `plan/spec-native-action-job-admission.md`. The transferable point, on top of the row below: that row's port kept a CONSUMER and dropped its PRODUCER; this one dropped a value a CALLER used to supply, which is worse to find, because there is no consumer left unsatisfied. The code reads correct, the fallback is a plausible number, and only somebody who asks what the number OUGHT to be can see it |
| 2026-09-02 | none (CI shard 1 triage) | `ZE_VERIFY_SCOPE_TAGS`, read by `scopeMatrix` (`internal/le/staticcheckfeaturematrix/staticcheckfeaturematrix.go`) | The le-personality port (`eae282592`) retired `scripts/status/verify_run.go`, whose `selectChangeSet` ran the change-set selector once per run and gave every stage the feature-tag answer. The replacement, `run.go` (`internal/le/verify/engine/`), sets only `ZE_VERIFY_MODE`, so nothing in the tree writes `ZE_VERIFY_SCOPE_TAGS`: the only other mention is `scopeEnv` in a test fixture (`internal/test/fixture/ui_fixture_le_checks_answers.go`). Every verify therefore judges all 38 matrix rows where the design says 3 for a single-tag change, and `docs/contributing/running-commands.md` still states that a verify run publishes the answer. The selector itself survived the port (`internal/le/changed/selector.go`, `printTags`), so only the wiring is gone. Nothing is red: the gate is CORRECT when unscoped, only slower | FIXED 2026-09-05 at the closure of `spec-verify-scope-2-change-set-selector`, which is the spec whose Files to Modify named `run.go`. `publishChangeScope` (`internal/le/verify/engine/scope.go`) selects the change set once before the first stage, writes both answers into the run's own log directory, and names them in `ZE_VERIFY_SCOPE_PACKAGES` and `ZE_VERIFY_SCOPE_TAGS`; the doc sentence now carries that producer as its anchor. `TestVerifyRunPublishesTheScopedAnswerAGatedChangeProduces` goes red when the call is removed. The transferable point: the row said the CI red it was found under was unexplained, and that is exactly why nothing else would have found it -- a port that keeps a CONSUMER and drops its PRODUCER leaves every gate correct and only slower, so no red can name it and only a reader asking who writes the variable can |
| 2026-09-05 | verify-scope-1-shared-checkout-freshness | `verifyScopeDebtClearDriver` (`internal/test/fixture/misc_fixture_runner.go`), driving `test/runner/verify-scope-debt-clear.ci` | The scenario that proves a verification-debt row is cleared by a GATE rather than by an edit did the edit itself. The driver ran a fixture "gate" whose whole body prints `fixture-gate-ran-green`, then rewrote the ledger's status cell from open to cleared with `strings.Replace` and its own hands, then asserted its own replacement was present, then printed `green-gate-cleared-its-row` and `cleared 1 row(s), 1 still open`, which the `.ci` matched on stdout. No product code ran: `le commit debt-clear`, `clearDebt` and `clearDebtRows` were never invoked. The spec's Functional Tests row still claimed the scenario "drives `commit_helper.py debt-clear` against two throwaway repos" and named the break that would make it red. Same shape as the 2026-09-03 row above, and worse in one way: the marker STRINGS were kept, so the `.ci` reads as a specification of behaviour nothing performs | fixed: the driver now runs the real `bin/le commit debt-clear` against a scratch checkout and judges the LEDGER, and the gate-exit decision itself got `TestDebtClearingHonorsTheGateExit` (`internal/le/commit/commit_test.go`) behind a new `clearDebtWith` seam, run once against a green verdict and once against a red one. The seam exists because through the real dispatcher that decision is an hour-long verification, which is why it had no test at all. Transferable: when a scenario cannot afford the real gate, the answer is a seam at the DECISION, never a driver that performs the outcome and then reads it back |
