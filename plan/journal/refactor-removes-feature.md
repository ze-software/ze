| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-28 | - | lint sweep over the test fixtures | An `unparam` finding was taken as authority to delete a parameter, and the parameter was a PROTOCOL input. Every call site passed the same value, so the linter was right about the code and wrong about the subject: the argument selects a bit whose two values drive two different required receiver behaviours, and a builder that can only produce one of them can prove only half of the pair. Nothing went red, because no test exercised the value the fixtures could no longer build. Found by reading the sweep's report, not by a gate | reverted: the parameter is restored with its original position, and a `//nolint:unparam` on the function carries the reason and cites the clause. General practice: a linter judges the code as written, never what the code must remain ABLE to do, so a finding that proposes REMOVING a parameter, a branch or a return from test infrastructure is a coverage question before it is a lint question. Where the removed input is one a specification gives meaning to, the finding is suppressed with that citation rather than obeyed |
| 2026-06-15 | - | refactor | `update bgp peer prefix` removed in batch cleanup as 'bypassing editor' but it used the editor | restored from git history and adapted to current APIs |
| 2026-08-09 | kernel-compose-make-q-assertion-is-vacuous | installer kernel build | a Python builder replaced the shell recipe and moved the phony `FORCE` onto `$(OUT)/Image`. An incremental build became an unconditional one, unmentioned in the commit message | re-gated `FORCE` on a parse-time comparison of the requested arch-profile-builder against the recorded one |
| 2026-08-16 | web-htmx4-cutover | chaos dashboard Freeze control | htmx 4 has no trigger filter. `hx-trigger="every 500ms [!window._frozen]"` kept rendering and kept parsing: htmx 4 reads the interval and ignores the condition, so the checkbox stopped meaning anything at the cutover. 15 poll requests in 3 seconds with it ticked, measured in a browser. Nothing was red, because a golden fixture holds MARKUP and this is a runtime behaviour. No row of the upgrade's own surface map named the loss, and the scanner has no category for it | a marker attribute (`freezePoll`) on the eight polls the control owns, plus one listener canceling `htmx:before:request` when the request reports `ctx.sourceEvent.type === "every"` and its source carries the marker (`internal/chaos/web/render.go`). `TestFreezeStopsThePollsItOwns` makes a new poll a decision rather than a default, and `TestFreezeListenerReadsTheMarker` reds on 4 needles if the listener goes |
| 2026-08-31 | fixit-peer-pending-sync-settles-too-early | `checkNoFamilyEndOfRIB` and 14 sibling RFC checkers (`internal/le/interoplab/bgp/check_rfc.go`), reached through `checkScenario` (`check_engine.go`) | Found while auditing this spec's own interop citation. Each of those checkers is registered in `specialCheckers` (`check_special.go`) and its body is `return checkScenario(ctx, check, <its own scenario name>)`. `checkScenario` reads `scenarioOperations` (`checkers.go`) and answers `scenario %s has no typed assertions` when the key is absent, and the key IS absent for all 15: `bgp_test.go` refuses a bespoke checker that also has a generic entry, so the two halves of the mechanism contradict each other. Those 15 checks therefore cannot pass, whatever the foreign daemon does. `TestEveryCheckerFailsClosedWithoutPeerEvidence` stays green because it asserts only that a checker FAILS with no evidence, and `TestRFCInteropCheckerNames` accepts any error whose text contains the scenario name, which this one does. Introduced by the le-personality migration of 2026-08-27 and 2026-08-28 (`a374622db`, `eae282592`), not by this spec. `docs/features/rfc-status.md` cites one of the 15 as public evidence for RFC4724-4-1 | recorded, not fixed. It spans four protocols and 15 requirements, none of them this spec's subject, and this spec's goal is proven by `test/plugin/initial-sync-barrier-raw.ci` against a separate BGP peer process instead. Whoever takes it owes each of the 15 its typed operations, and owes the two unit tests a discrimination they do not have today: a checker that never reaches the lab must be told apart from one that reached it and found nothing |
