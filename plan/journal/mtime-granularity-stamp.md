| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-21 | - | `CounterEncoder.Encode` and `EncodeTemplate` (`internal/plugins/flowexport/ipfix/adapter.go`), the IPFIX message header Export Time | The Template message was stamped with `time.Now()` at send and the Data message with `snap.Time`, read earlier by `register.go` before the exporter loop reached the Template. Both are whole seconds, so whenever the second boundary fell between the two reads the Data Set left the box one second BEFORE the Template describing it, which RFC 7011 Section 8.2 forbids and which a collector applying that section discards. Found while triaging RFC7011-8.2-2 | fixed: `EncodeTemplate` remembers the Export Time it wrote and `Encode` clamps the Data header to it; the records keep the snapshot time. `TestRFC7011StaleSnapshotClampedToTemplateTime` drives a snapshot five seconds old through the pair |
| 2026-09-05 | bgp-pcap-decode | raw packet capture (`internal/component/bgp/reactor/reactor.go`, `BGPRawCaptureSnapshot`) | a capture timestamp loses every sub-second digit crossing the plugin boundary. The snapshot formats it with `2006-01-02T15:04:05Z07:00`, a layout carrying no fractional seconds, and `exportBGPPcap` parses that string back. So every message captured within one second gets an identical pcap timestamp, and a burst cannot be ordered across the two directions. `internal/component/bfd/engine/raw_capture.go:77` and `internal/component/bgp/reactor/capture.go` format the same way | NOT fixed here. Within one direction the order is carried by the TCP sequence numbers the export now writes, so this spec's ACs hold without it. Widening the layout to RFC3339Nano would change the BFD pcap bytes, which AC-7 of this spec requires to stay byte-identical, and it would change the `timestamp` field `test/plugin/diag-capture.ci` asserts on. The two need deciding together |
| 2026-08-09 | kernel-compose-make-q-assertion-is-vacuous | installer kernel build | a stamp file listed as an ordinary make prerequisite read as not-newer and lost a profile switch. GNU Make 3.81 resolves mtimes to one second, and the same recipe wrote both files | compared the stamp's CONTENT at parse time, so no mtime decides the answer |
