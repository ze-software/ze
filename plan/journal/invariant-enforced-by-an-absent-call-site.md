| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-15 | web-htmx4-cutover | the derived per-page asset sets | a page's head must load the htmx core before any htmx extension: an extension is a script that calls `htmx.registerExtension` when it runs, so it throws when the core has not run. `sorted` (the retired `scripts/codegen/web_assets.go` (current producer: `internal/le/webassets/webassets.go`)) returned the set in NAME order and nothing else ordered it, so the invariant held because `htmx.min.js` sorts before `sse.js` and before `hx-sse.min.js`. A renamed extension breaks every page that streams, with no console error a fixture can see and no test that fails | `loadOrder` replaces `sorted` and moves the core to the front, and `TestPagesServeHtmx4` (`internal/component/web/markup_contract_test.go`) reads `assets[0]` back. A copy of the generator with the ordering removed emits `hx-sse.min.js` first for both streaming pages, so the assertion discriminates |
| 2026-08-15 | rfc4271-med-across-as | bgp ingress and egress filter converters | RFC 4271 Section 5.1.4's configured MULTI_EXIT_DISC removal is honored on the import chain and MUST NOT be honored on export, because Section 9.1.2.2 states that comparing on a metric and then advertising the route without it has been proven to cause route loops. What enforces that is not a guard. `ExtractMEDRemoveOps` (`internal/component/bgp/reactor/filter_delta.go`) has exactly one call site, in `runIngressPolicyChain` (`internal/component/bgp/reactor/filter_ordered.go`), and the export converter `textDeltaToModOps` skips the token. `appendMEDRemove` (`internal/component/bgp/plugins/filter_modify/filter_modify.go`) warns the operator, and deleting that warning changes no byte on the wire, so the visible half is the half that does not bind. Unifying the two converters is the obvious tidy-up and it would delete an RFC obligation without touching anything a reviewer would grep for | the reason is written where the code is read: the doc comment on `ExtractMEDRemoveOps` quotes both sections, `docs/architecture/bgp/egress-attribute-rules.md` states the rule and why it is not on the egress rail, and two tests go red if the omission is filled, `TestHandleFilterUpdateMEDRemoveIsImportOnly` and the zero-op assertion inside `TestMEDRemovalMechanismIsConfigurable`. The same shape decided the sibling rule's PRECEDENCE and inverted it: `applyFactsLocalPref` records its Suppress after the egress filter pass so it beats a filter's Set, and `applyFactsMED` must not, because Section 5.1.5 prohibits the attribute outright while Section 5.1.4 prohibits only relaying somebody else's value. Copying the sibling would have satisfied RFC4271-5.1.4-1 and broken MED as a feature |
| 2026-08-17 | fixit-ike-resource-lifetime-leaks | `clear vpn ipsec sa` against a responder that has just established | the Child SA's two XFRM states and two policies stayed in the kernel for the remaining 60s of the test. MEASURED while writing `test/ipsec/ipsec-teardown-leaves-nothing.ci`: the responder logged `child-sa: installed` at 20:50:18.865, the clear dispatched at .914, and `ip xfrm state` still held both SPIs a minute later. What releases a stopped session's Child SA is `cleanupChild`, and its only call sites are the exit arms of `maintainSA` (`internal/component/ike/engine/established.go`), so a `StopGraceful` that lands before that loop runs finds no call site to reach it. The same clear two seconds later removes both states, so the test waits for `uptime-seconds` to reach 1 first | recorded, not fixed here. It blocks no goal of this spec: AC-5 is about what a teardown leaves behind, and the test asserts that on a settled tunnel. For an operator it means a clear issued in the first moments of a tunnel can report success and leave the kernel holding the SA |
| 2026-08-18 | shared-machine-job-admission | the verify runner's stage environment | every stage of a verify inherits this process's environment, and one expression is what does it: `cmd.Env = append(os.Environ(), "ZE_VERIFY_MODE=1")` in `execStage` (the retired `scripts/status/verify_run.go:594` (current producer: `internal/le/verify/engine/run.go`)). the retired `scripts/dev/ze-run.sh` (current producer: `internal/le/job/job.go`) admits one heavy job at a time and exports `ZE_RUN_JOB`, so a wrapped stage runs inside its parent's slot: `./le verify lint run` is wrapped and runs as a stage of `./le verify current mode full`, which holds the slot. Replace that expression with a fresh environment and the stage cannot see `ZE_RUN_JOB`, so it queues for the slot its own parent holds. Nothing releases it, no test goes red, and the symptom is a verify that hangs for as long as anyone lets it | `TestEveryStageInheritsTheParentEnvironment` (the retired `scripts/status/verify_run_env_test.go` (current producer: `internal/le/verify/engine/`)) parses every non-test file of the package and reports an `.Env` assignment that neither calls `os.Environ` nor extends the same field, which is the PRODUCING expression rather than the variable name, so renaming `ZE_RUN_JOB` does not silence it. Measured: replacing the expression with `[]string{"ZE_VERIFY_MODE=1"}` fails the test at `verify_run.go:594`, and `TestEnvAssignmentsJudgeEveryShape` pins the parser against ten shapes, aliased and dot imports included |
| 2026-08-18 | - | `show system`, plugin command inventory | `RegisteredCommands` (`internal/component/plugin/process/process.go`) returns a copy of `registeredCommands`, and two consumers report its length as fact: `"command-count": len(p.RegisteredCommands())` in `internal/component/cmd/show/system.go` and in `internal/component/plugin/server/system.go`. Nothing appends to that slice. `addRegisteredCommand` and `removeRegisteredCommand` are its only writers and neither has a call site, so the count an operator reads is always 0 and reads as an answer rather than as a missing one. `6ff9d56e6` (chore: repair repository structural gates, 2026-08-17) unexported the pair rather than deleting it, which traded an exported-without-caller finding for an `unused` one and left `./le verify lint run` red at HEAD ever since. Met while trying to commit: `./le verify lint run` is a structural gate, so `commit_helper.py` refuses every commit prepared from a tree that carries it, and the escape is an owner override | recorded, not fixed. The two directions are opposite behaviour changes, not a cleanup: delete the writers and `command-count` becomes an honest constant zero to remove from both consumers, or wire `addRegisteredCommand` into the command registration path and the number becomes real. Picking one is Thomas's. The five `wireEncoding` symbols in the same `unused` cluster have no consumer at all and are a plain deletion once he rules |
| 2026-08-18 | - | spec WIP cap | `ZE_SPEC_WIP_CAP` (default 12) is checked in the retired `scripts/dev/spec-session.sh` (current producer: `internal/le/spec/session/session.go`) on the `ready -> in-progress` transition of `claim` alone. Nothing enforces it at the state itself, and writing the Status cell as in-progress by hand reaches the same state with no check, as does a session that never claims. Measured 2026-08-18: 32 specs read `in-progress`, 2.7x the cap the tool reports it holds. The effect is not bookkeeping: an agent that DOES use `claim` is refused and stops, while the sessions that bypassed it are the reason the count is high, so the discipline falls hardest on the sessions following it | not fixed. The general shape is the point: a limit checked on one optional path to a state does not bound that state, and it reports a bound it does not hold |
| 2026-08-19 | - | the `.et` editor suite's per-test wall clock | The `.ci` path bounds every test: `RunOptions` carries a per-test timeout, `parallelFactor` widens it by `ParallelTimeoutHeadroom` under concurrency, and a test that overruns is killed and reported. The `.et` path has none of it. `cmdEditor` (`internal/test/cli/cmd_editor.go`) drives `parallelRunner` directly rather than `Runner.Run`, and calls `editortesting.RunETFile(t.Path)` -- whose signature is `RunETFile(path string) *TestResult` (`internal/component/cli/testing/runner.go`), taking NO context and returning only when the test returns. So 166 editor tests have no wall-clock bound of their own, and a hung one is bounded only by `SUITE_RUN`'s 600s `timeout` killing the whole process group, which reads as the SUITE failing rather than the test. The same bypass costs that suite `caps=` skips and exclusive groups, because only `record_parse.go` sets those and the `.et` path parses no `Record`. `web` (`.wb`) takes the same shortcut. Measured while auditing why the 24 suites run at five different concurrencies; the editor suite's own median is 4.0s and its max 46.3s, so nothing is hanging today | not fixed. The bound belongs where the `.ci` path already puts it: pass the runner's context into `RunETFile` and let it return on cancellation, rather than adding a second timeout mechanism beside the one that works. Doing so also raises the question of whether `.et` and `.wb` should go through `Runner.Run` at all, which is a larger decision about one framework or three |
| 2026-08-19 | fixit-vpp-loopback-recreated-per-apply | iface config apply, the dummy create step | `applyConfig` (`internal/component/iface/config_apply.go`) promises that a create step meeting a name it already holds keeps the interface and reports success. What enforces that promise is a fallback on the ERROR branch: it calls `b.CreateDummy`, and only when that returns an error does it ask `b.GetInterface` whether the name exists. The netlink backend reaches the fallback, because the kernel answers EEXIST. The vpp backend never did: `create_loopback` carries a MAC and names no interface, so VPP allocated a fresh one and returned success, and the tolerance sat one branch away from every call. The promise held for one backend and was absent for the other, at a cost of one leaked dataplane interface per apply, with no error, no log line and no failing test. `GetInterface` could not have rescued it either, because the vpp dump matches VPP's own name, `loopN`, while the operator's name is `lo0` | the backend answers the new `iface.ErrInterfaceExists` (`internal/component/iface/backend.go`) and sends nothing, and the step reads that sentinel with `errors.Is` BEFORE the GetInterface fallback, leaving `created` false so the undo does not delete a device an earlier apply made. `TestCreateDummyKeepsExistingLoopback` and `test/plugin/vpp-loopback-reapply.ci` are both red with the check removed. The shape to look for: a tolerance written as an error fallback is a tolerance for backends that FAIL, and a backend that succeeds at the wrong thing walks past it |
| 2026-08-23 | - | bgp per-family prefix maximum, `count offered` | the DEFAULT `count offered` bounds nothing a peer cannot lift. `checkPrefixLimits` (`internal/component/bgp/reactor/session_prefix.go`) sends every withdrawn section to `applyPrefixDelta`, which calls `prefixCounts.add` with a negative delta and no test of whether the family ever held those prefixes, and the count clamps at 0 rather than going negative. So a peer announces `maximum` prefixes, withdraws that many it never announced, and announces `maximum` more: the counter reads `maximum` while ze holds twice that, and the cycle repeats without bound. No identity-free repair exists, and the proof is short. Every offered-mode counter is a function of the announcements A and the withdrawals W alone. Safety needs the counter to be at least the number of prefixes held R, an adversary makes every withdrawal unmatched so R equals A, and any counter that decrements on a withdrawal is below A after the first one. The only safe identity-free counter is therefore A itself, which never decrements, and that tears down a conforming peer whose table churns or refreshes. `installed` is already immune, and the same session measured it green through `TestPrefixCountInstalledIgnoresUnmatchedWithdraw` | NOT fixed. `plan/deferrals/fixit-offered-prefix-count-freed-by-unmatched-withdraw.md` recorded this on 2026-08-08 and reached the same place, so this row is the second occurrence and the first with the impossibility argument written down. The repair is a decision Thomas owns rather than a patch: make `installed` the default, which moves the YANG `default "offered"` in `internal/component/bgp/yang/ze-bgp-conf.yang` and the absent-key answer in `PrefixCountFor` (`internal/component/bgp/reactor/peer_settings.go`), and costs one map entry per prefix bounded by maximum+1. Giving `offered` identity was refused because that IS `installed`, and never decrementing was refused because it converts a defeatable limit into a false teardown |
| 2026-08-23 | - | bgp RFC 2545 Section 3 link-local next hop | `refreshPeerLinkScopes` (`internal/component/bgp/reactor/reactor_iface.go`) re-settles the shared-subnet condition for every peer after an interface address event and deliberately re-advertises nothing, which its own godoc argues for from Section 3 stating no obligation to re-advertise a route already sent. When the condition goes TRUE to FALSE the peer keeps a 32-octet next hop whose link-local half it can no longer resolve on its own link, so those prefixes blackhole until an unrelated update for them arrives. Thomas ruled on 2026-08-23 that a flip is itself an event owing the peer a new advertisement. The FALSE to TRUE direction is harmless | NOT fixed, and the package boundary is why. A forced re-announce needs the Adj-RIB-Out, which the reactor does not hold: `outboundResend` and `collectGroupedRibOutRoutesForFamily` (`internal/component/bgp/plugins/rib/rib_commands.go`, `rib_replay.go`) own it, reachable today by the `clear bgp rib out` command or by a structured refresh event synthesized from a ROUTE-REFRESH the peer sent. The reactor has no engine-to-plugin trigger for either, and faking a received ROUTE-REFRESH would put a peer action nobody took on the monitor, mrt and capture streams. So the fix spans the reactor and the rib plugin and needs the trigger designed, which was reported to the main thread for a re-cut rather than half-built |
| 2026-08-23 | - | RADIUS and TACACS+ server configuration on a running daemon | `swapAAABundle` (`cmd/ze/hub/aaa_lifecycle.go`) had exactly two non-test callers, `infra_setup.go` and `main.go`, and both run at startup. So the AAA chain a running daemon serves is the one the BOOT tree described, and a rotated shared secret, a moved server address, a changed timeout or a backend added or removed was accepted into the configuration and applied to nothing until a restart. The local backend was already immune because it re-reads the accepted generation per login. The remote backends are not and cannot be: a RADIUS or TACACS+ client holds the address, the secret and the timeout it was CONSTRUCTED with. Both were WRITTEN for the missing caller, and say so: the tacacs Close comment reads "Close runs on every AAA bundle swap (config reload or clean shutdown)" and the radius one "Close drains the client's UDP socket on every AAA bundle swap (config reload or shutdown)". Neither had ever run for a reload. Adding the reload caller was not the whole fix, because the same defect had two more instances and each was a SURFACE holding a captured value. ssh: `infra_setup.go` handed the server `bundle.Authenticator` and `bundle.Authorizer` by value and `zessh.Server.Start` reads its authenticator once. The post-start dispatcher: `infra_setup.go` handed it `bundle.Authorizer` and `bundle.Accountant`, `SetPostStartFunc` runs once and no reload re-enters it, so `Dispatcher.isAuthorized` fell back to the retired authorizer for every request carrying no per-session one, and `BeginAccounting` enqueued onto the accountant the swap's Close had already stopped, whose send path drops the record and returns no error | `runReloadContext` (`cmd/ze/hub/main_reload.go`) builds a CANDIDATE bundle from the reloaded tree in the fallible phase and swaps it at the acceptance point beside `publishAcceptedLocalIdentity`, with a deferred Close for the candidate any later failure abandons, because Build has already opened the new socket and started the accounting worker. A build that fails REFUSES the reload rather than installing nothing. Both surfaces now hold indirections over the atomic slot instead of values: ssh gets `liveAAABundleAuthenticator{}` and `liveAAABundleAuthorizer{}`, the dispatcher gets `liveAAABundleAuthorizer{}` and `newLiveAAABundleAccountant()`, which is the wiring the no-BGP path already used. The authorizer gained `BindProfiles`, without which `aaa.BindProfiles` returns it unchanged and a break-glass recovery session is authorized by username instead of by its bound profile. Five tests are each red under their own mutation of a producer. The shape to look for: a Close comment that names a caller, no such caller, and then every surface that took a COPY of the thing being replaced |
| 2026-08-23 | - | iface port mirroring, `reconcileOnReady` | a mirror the operator removed from the config file while ze was down stayed installed in the kernel on the next boot, and tc kept copying every packet to a destination the operator had deleted. `removeStaleMirrors` (`internal/component/iface/config_mirror.go`) decides teardown from a delta between two configs, so a boot applying with a nil previous config has nothing to compare and never removes anything. The same hole is reachable while running: that function skips a teardown whose interface the backend cannot read, and the apply that skipped it consumes the delta, so every later commit compares two configs that agree. Its own godoc had named the fix since 2026-08-14 and `plan/deferrals/fixit-mirror-clsact-ownership.md` had carried it as an unassigned deferral: reconcile live kernel state instead of a config delta. `reconcileOnReadyWithJournal` (`config_apply.go`) is the pass that does exactly that for addresses, for owned macvlan devices and for pruning, and it had no mirror call site | `Backend.ListMirrors` reports the live mirrors, implemented on netlink by reading the clsact hooks of every link for ze's own priority-1 matchall mirred filter, and on vpp by dumping device SPAN rather than the in-memory map that empties on restart. `reconcileMirrors` runs inside the reconcile pass, retires every mirror the configuration does not ask for and re-installs any that differs, and removes NOTHING when the read fails, because "I cannot tell" is not "there is nothing there". It acts only on an interface `mirrorScope` names, which is every device the current or previous configuration configures: review found that removing on the filter SHAPE alone would tear down another tool's identical capture on an unmanaged interface, on every apply, and the sibling passes already decide ownership positively. The cost of that boundary is one case out of reach, an interface whose whole stanza was deleted while ze was down, and it is documented rather than hidden. Four unit tests go red with the pass or its scope removed, and five integration tests prove it against a real kernel in a netns. `removeStaleMirrors` stays: it runs before the installs, so a retired mirror stops copying at the start of the apply rather than at its reconcile phase |
| 2026-08-23 | fixit-vrrp-parent-ignores-the-selector | `RegisterOwnedMacvlan`'s name-conflict check, and the vrrp owned-device naming that defeats it | The check reads as a guard against two owners claiming one macvlan name, and for VRRP it can NEVER fire. Its loop skips its own owner (`internal/component/iface/device_owner.go`), and VRRP derives the owner FROM the device name (`ownerString`, `internal/plugins/vrrp/instance.go`), so a same-name registration is always SAME-owner and falls through to an unconditional overwrite. That matters because the name is composed from the parent's IFINDEX plus the vrid (`ComposeOwnedDeviceName`), so two different parent NAMES sharing one ifindex compose one macvlan name, which is exactly what a kernel RENAME produces. A rebuild across a rename therefore re-registers the single device and then unregisters it, and `UnregisterOwnedMacvlan`'s own doc states what follows: the next reconcile orphan-deletes the device and its addresses. The VIP leaves the wire while the FSM still reports master. Found when a review asked what actually stops the collision a code comment claimed was refused; the honest answer was nothing | VRRP no longer relies on it: the move branch keys on the macvlan name rather than on the parent string, so a rename is not a move and no same-name registration is attempted. The registry check is unchanged and remains correct for its real purpose, which is CROSS-owner conflict. The transferable part is the reasoning error, not the code: a guard whose predicate excludes your caller cannot protect your caller, and a caller that derives its own identity from the value being checked is always excluded. Read the predicate against your own caller before citing a guard as protection |
| 2026-08-24 | fixit-flowspec-table-never-swept | the firewall's one-time removal of the tables an older ze build wrote bare | The removal lives inside `Backend.Apply` (`internal/plugins/firewall/nft/backend_linux.go`), and `ApplyAll` (`internal/component/firewall/registry.go`) returns BEFORE it reaches a backend when the merged desired set is empty and none is loaded: the autoload was guarded on `len(all) > 0`, so a withdraw with nothing loaded stayed a no-op. Every owner of a bare name registers only on an EVENT -- the FlowSpec bridge on an UPDATE, the anomaly-shape responder and ddos-local on a detection -- so the box the migration exists for is exactly the box that never calls it. An upgraded router whose peer has stopped announcing, with no `firewall {}` section, would have kept enforcing the previous build's rules for its whole life, and every proof of the removal was green: the integration tests call `Apply` with a non-empty set and the plugin test announces a route first, so all of them arrive with a backend already loaded | the guard now admits `len(all) > 0` OR `LegacySweepPending()`, so an EMPTY set loads a backend exactly once, and each event-driven owner runs one empty `ApplyAll` at startup while the flag is set. `legacySweepReached` clears it on the first successful Apply, which is what keeps this a migration with an end rather than a standing deletion policy. `TestAnEmptyReconcileReachesABackendWhileTheSweepIsPending` pins the registry half, and `test/firewall/firewall-legacy-table-removed-with-no-config.ci` drives a whole box with no firewall section at all. The shape to look for: a one-time migration whose only trigger is a code path that runs when there is work to do, on a box whose defining property is having none |
