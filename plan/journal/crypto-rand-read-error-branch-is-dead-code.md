| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-09 | ipv6cp-accepts-and-proposes-a-zero-interface-identifier | `generateIPv6CPInterfaceID` (`internal/component/l2tp/ppp/ipv6cp.go`), and every other `rand.Read` caller that checks `err != nil` (`chap.go`'s challenge draw, `mschapv2.go`'s challenge draw, `session_run.go`'s `generateMagic`, and any other caller repo-wide) | `crypto/rand.Read`'s own doc comment on the toolchain this repo builds with (Go 1.27.1, read at `crypto/rand/rand.go` in the toolchain's own source tree): "Read fills b with cryptographically secure random bytes. It never returns an error, and always fills b entirely. Read calls io.ReadFull on Reader and crashes the program irrecoverably if an error is returned." So every `if _, err := rand.Read(...); err != nil` branch in this codebase is dead code on the shipped toolchain: a real read failure calls `fatal()` (an unrecoverable process crash) before any error value reaches the caller. Found while writing a test for this spec's own new `suggestIPv6CPInterfaceID` failure branch: substituting `rand.Reader` with an always-erroring reader crashed the whole test binary with `fatal error: crypto/rand: failed to read random data` instead of producing an observable Go error | not fixed here: out of scope (touches `chap.go`, `mschapv2.go`, `session_run.go`, and any other repo-wide `rand.Read` caller, none named in this spec's Files to Modify). This spec's own new failure branch (`buildNakOrReject`, `ncp.go`) is proven a different way instead: `TestIPv6CPNakSuggestionFailureRejectsAndKeepsIPv4Session` forces `suggestIPv6CPInterfaceID`'s own retry-exhaustion (reachable independently of `rand.Read` ever returning an error) by feeding a deterministic byte stream that is valid before the "u" bit clear and becomes the invalid all-zero value after it, on every draw |
