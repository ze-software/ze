# A failing gate prints no cause

| 2026-09-19 | - | `TestNoConfigFeedsSentUpdatesToAReceivedOnlyPlugin`, `internal/component/bgp/reactor/config_direction_test.go` | The guard's self-check `require.Greater(parsed.documents, parsed.refused)` failed with `"19" is not greater than "112"`, and its message names two causes: "a surface that used to parse stopped parsing, or the walk is offering the parser texts that are not documents". The real cause is a THIRD it does not name: the package was compiled without the feature tags, so `internal/component/plugin/all` carried none of the gated plugins and the schema `ParseTreeForValidation` builds lacked their YANG modules. 87 of the 112 refusals were `unknown field in capability: graceful-restart` (32), `in bgp: rpki` (26), `in session: rs-client` (11) and three `policy` fields (18), each declared in a plugin module that was not compiled in. With `-tags` derived from `feature-gates.txt` the same test passes. The guard's OTHER assertions do name this cause ("the schema is not loaded in this binary"), so the vocabulary existed and this one did not use it | not fixed, one occurrence. The cost is real and repeatable: the message sent this session to read the YANG modules for a schema gap that does not exist, and the numbers read exactly like a product defect in `ze config validate`. The fix is one clause naming the under-loaded schema, or better, an assertion that refuses a binary whose schema resolves nothing past ze-extensions BEFORE the ratio is judged, so the precise refusal fires first. `ai/rules/commands.md` already says to run tests through the registered `./le` action for this reason; the guard should say so when it fires |

| 2026-09-13 | none (settling a recorded intermittent before the first green push) | `ze-test exabgp api`, the invocation `internal/le/functional/exabgp.go` builds for the `exabgp-api` stage | The runner exits 2 and prints NOTHING: zero bytes on stdout and stderr together. Measured four ways, all identical: through `uv run --with paramiko --with exabgp==5.0.13` as the stage runs it, without `uv`, with the stage's own `ZE_TEST_NO_BUILD`, `ZE_BIN` and `ZE_TEST_BIN` set, and with the session binaries the stage itself built. `--help` answers exit 0 and 1111 bytes from the same binary, so the program runs and the argv reaches it. Exit 2 is the usage code, and Go's flag package prints usage on it, so whatever refuses here is not the flag parser. The same command inside the stage runs 40 scenarios, so the refusal is about the environment the stage supplies and nothing says which part | not fixed. It blocked no goal: the suite runs correctly through `./le functional exabgp-test`, and only a by-hand run of one population is unreachable. What it cost was the experiment `plan/journal/gate-verdict-depends-on-the-machine.md` asks for in its 2026-09-06 row, `ze-test exabgp api --parallel 1 --all`, which is still unrun and is the one measurement that would say whether `api-reload` and `api-open` are the suite's own parallelism or the reload path. A silent exit 2 is the shape `ai/rules/principles.md` names: code that cannot answer must say so |
A command runs a gate, the gate fails, and the operator gets the verdict with
nothing under it. The cause exists: the gate built a structured report and
named the assertion that failed. Something between the gate and the terminal
dropped it, usually a wrapper whose own summary assumed the gate had already
written to the terminal itself.

The tell is a wrapper that renders only its own summary and stores the inner
answer in a field nothing reads on the default path. Ask, of every layer above
a gate: when this layer prints, has the layer under it printed anything at all?

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-14 | - | `./le integration interop`, the `interoplab.SuiteReport` (`internal/le/interoplab/lab.go`) the BGP lab answers with | Recurrence of the 2026-09-01 row for the one gate it named "the same holds for". `Sweep.Text` (`internal/le/leaction/leaction.go`) now renders a failing native answer, but only one that implements `Text()`. The IPsec lab's `Report` does and the BGP lab's `SuiteReport` did not, so a red `ospf-ipsec-frr` printed `Failed: interop` and nothing else while the report held the scenario name and `wait for frr output timed out`. Read through the `json` pipe operator, as the earlier row did. The same shape a level down: `waitContains` (`internal/le/interoplab/bgp/check_engine.go`) discarded the peer's last output on a timeout, so even the JSON said the wait timed out and not that FRR sat in `ExStart` | fixed. `SuiteReport.Text` renders the failed scenarios and their errors under the summary, and `withLastOutput` carries the peer's last answer on every `waitContains`, `waitContainsFold` and `waitContainsAny` timeout |
| 2026-09-13 | none (clearing the last red before the owner's first green push) | `./le commit debt-clear part 5 of 6`, the verdict it prints | The pass exited 1 and named no stage. Its console carried `verify-worktree: full-part-5-of-6 exit=1` and nothing else about the red: the eight stage results live in a 49K combined log, and the one that failed is line 738 of it. The lifecycle already held the fact. `validateResult` (`internal/le/verify/engine/run.go`) gives a red stage a `stage-failed` failure carrying its name, `run` (`internal/le/verify/lifecycle.go`) copies it into `Report.Failure`, and `debtClearResult.Text` renders `Diagnostics` alone, so the field printed nowhere | FIXED 2026-09-13. `run` now appends one diagnostic per stage that exited non-zero, naming the stage and its own log file, before the log-save block. Every red stage is named rather than the first, because the population carries on after a stage judges the tree and finds it wrong. `debt-clear` prints them with no edit of its own: it already carries the lifecycle's diagnostics into its answer. `TestTheConsoleNamesTheStageThatEarnedTheExitCode` (`internal/le/verify/lifecycle_test.go`) holds both halves, the naming and the silence of a green run |
| 2026-09-12 | none (owner asked to fix the rib Insert signature) | `Answer` (`internal/le/ifaceresolution/ifaceresolution.go`), read through a verify stage log | `iface-resolution` exited 2 in a full verify run and its stage log holds three lines: the header, the command, and `### Stage result: iface-resolution exit=2`. The reason is in no file the run wrote. `Answer` refuses with `fmt.Fprintf(os.Stderr, ...)` and returns `(nil, 2)`; `leroot.Run` returns on a nil payload before it writes anything to the `errOut` it was handed; and `dispatch` captures only the buffers it passes to `leroot.Run`. So the one sentence that explains the red goes to the process's real stderr and is interleaved into whatever the top-level caller redirected, 565 lines away from its own stage. It cost an investigation to recover, and it mattered: the sentence names a dead allowlist entry, which is what identified the red as the stale binary's rather than the tree's | not fixed. The shape is the one the rows below keep naming, one layer lower: the refusal path of an `Answer` has no seam at all, because the signature returns `(any, int)` and a nil payload is how a gate says it could not run. Every le gate that refuses this way is silent in a verify log, so the repair is a decision rather than a line: either a refusal carries a payload that renders itself, or `leroot.Run` gains a way to take the reason. The binary staleness behind this occurrence is rowed in plan/journal/stale-artifact-reused.md |
| 2026-09-02 | none (owner asked why lint was unrunnable) | `./le verify lint run`, its failure group | `declareLintFailureGroup` (`internal/le/verify/lint/failuregroup.go`) declares the summary "golangci-lint reported findings in the files named here" whatever made the stage red, and the paths it names come from a collector that only sees diagnostic lines. A child that never linted collects none, so the group says findings were reported and names no file. Measured here before the lock fix: 18 of 18 flavor passes exited on golangci-lint's start lock, nothing was linted, and the stage still declared that summary with `"related":[]`. Any start failure reproduces it: a config the child refuses, a missing binary, an OOM | not fixed. The lock cause is gone (`plan/journal/gate-verdict-depends-on-the-machine.md`, 2026-08-24 row), so the summary is now usually true. It stays wrong for every other start failure. The repair is a decision rather than a line: a group with no paths is not a findings group, so the stage owes a second group kind for a child that could not start, and the engine reads the kind |
| 2026-09-01 | eap-notification-and-nak | `le integration`, a native gate's report | `Area.Sweep` (`internal/le/leaction/leaction.go`) keeps each action's answer in `SweepRow.Answer` and renders only `Sweep.Text`, whose comment reads "Each action's own output has already streamed to the terminal by the time this is read". That holds for a `commandRunner` action, which streams a subprocess. It is false for a `nativeRunner` action, whose answer is a `leroot.Prose` report nothing else prints. Both integration gates are native, so a failing `./le integration interop-ipsec` printed exactly two lines, "Failed: interop-ipsec", while `ipsec.Report.Text` held the scenario name and the assertion that timed out. Measured here: the same run through the `json` pipe operator answered `wait for strongswan log "EAP/RES/NAK" timed out before the peer became ready`. The same holds for `./le integration interop` | not fixed. It blocked nothing: the `json` pipe operator renders the payload the gate already returns, and every RED in this spec's discrimination proof was read that way. The repair is a decision rather than a line, because the sweep cannot tell a streaming action from a Prose one without asking the action, and printing an inner report for every row would double the output of the areas whose actions do stream |
| 2026-09-02 | none (dependabot alert 28) | `le verify deps vulnerability`, a native gate's report | `./le verify deps vulnerability` printed one blank line and "Failed: vulnerability", and exited 127. The cause was that `govulncheck` is not on PATH, which a shell reports as 127 and the sweep discards with everything else the action said. The action's own description states the tool must be installed ("scan the Linux/amd64 dependency graph with an installed govulncheck"), and `./le setup --check` names no govulncheck, so a fresh machine reaches this gate with the tool absent and is told only that a stage failed. `go install golang.org/x/vuln/cmd/govulncheck@latest` made the same command pass | not fixed. It blocked nothing here: installing the tool cleared it, and the dependency bump the gate was asked to judge is verified. The row sits in this class rather than in a setup class, because the missing tool is nameable in one line and the gate withheld that line |
| 2026-09-02 | none (owner asked to clear the verification debt) | `./le functional exabgp-test`, the runner's bind-address preflight | `ensureBindAddresses` (`internal/test/runner/loopback.go`) exists so a host missing `fd00::2` is told the exact `./le setup` command instead of paying a whole-test timeout, and its call site says so (`internal/test/runner/runner_exec.go:596`). It reads two populations only: the stdin blocks and the tmpfs `.conf` files a `.ci` embeds, scanned for Ze-syntax `connection { local { ip <addr> } }`. An exabgp-compat fixture names its config with `option=file:` and declares the address in ExaBGP syntax as `local-address fd00::2;`, so neither the population nor the grammar matches and the preflight sees nothing. Measured here: `conf-ipself6` and `conf-llnh-update` each burned 180 seconds and reported `context deadline exceeded`, with the daemon's only output `Ze running`, while `./le setup check` named the cause in one line as `[MISSING] loopback-addresses (fd00::2 (REQUIRED))`. The third `bind:ipv6` fixture, `conf-llnh-lla-only`, binds `::1` and passed, which is why the suite reads as two flaky cases rather than one absent address | FIXED 2026-09-03. The first reading was wrong about which runner: `option=file:<name>` does not parse in `EncodingTests` at all (measured), because the exabgp-compat suite has its own parser and its own runner in `internal/test/cli/cmd_exabgp.go`, and that suite ran no preflight of any kind. `EnsureConfigFileBindAddresses` (`internal/test/runner/loopback.go`) now exports the scan over configs held on disk, `configLocalAddresses` reads the ExaBGP `local-address` leaf beside the Ze `connection { local { ip } }` block, and `runOneExaBGPTest` calls it before it starts either process. `Record.ConfigFile` was deliberately NOT added to the embedded path: 166 fixtures set it to a name that resolves to a tmpfs file never written to disk, so reading it would have failed a population that passes. Discrimination recorded both ways: removing the call and removing the grammar each restore the original symptom, `context deadline exceeded` at the fixture's own timeout with no cause in the error |
