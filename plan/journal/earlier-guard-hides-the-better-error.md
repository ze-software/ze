| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-10 | fixit-isis-hostname-ascii | config | a YANG `pattern` and a `ze:validate` validator both guard one leaf. The pattern runs first at parse, in `ValidateLeafValue`, and aborts the file before the tree walk reaches the validator. `ze config validate` on a bad `isis system-id` renders `does not match pattern "[0-9a-fA-F]{4}\\..."`. The validator's `expected xxxx.xxxx.xxxx` is unreachable | the new hostname leaf declares no `pattern`, so its validator's message is the one the operator reads. `system-id` still carries both and still shows the regex |
| 2026-08-12 | fixit-ike-negotiation-conformance | ike crypto | a new length refusal in `SharedSecret` (`crypto/dh.go`) runs before the MODP small-value range check. The range check became reachable only by a value of exactly the modulus length. `TestDHInvalidPublicKey` passed one octet and stayed GREEN, because `ErrPublicKeyLength` WRAPS `ErrInvalidPublicKey` and `errors.Is` still matched. The guard stopped being exercised and no test said so | each case pads to `modp2048Len` and asserts `ErrInvalidPublicKey` AND NOT `ErrPublicKeyLength`, so a later length-check change cannot re-silence it. Disabling the range comparison now reddens all four cases. The one-octet fixture it replaced stayed green under that same mutation |
