| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-10 | fixit-eap-tls-clienthello-race | ike eap-tls | `test/ipsec/ipsec-eap-tls-clienthello.ci` runs two ze daemons. It stays GREEN in 3.5s with the original defect on the wire. `tlsMethod.Process` (`internal/component/ike/eap/eap_tls.go`) answers a bare fragment acknowledgement with a bare fragment acknowledgement. The peer half then sends its ClientHello on the next round, and the exchange recovers. strongSwan refuses the same answer and fails the method. So `test/interop-ipsec/scenarios/04-eap-tls` is the only test that sees the 67-octet answer to a 67-octet Start | not fixed, and no assertion in a ze-to-ze test can fix it. A functional test whose two ends share one implementation cannot see a leniency both ends share. The blindness is recorded in the `.ci` header and in the spec. The interop scenario stays the evidence for the wire form. The next step for the class is a rule that a wire-form claim needs a foreign peer, never a second copy of ze |
