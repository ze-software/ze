| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-10 | fixit-forward-readbuf-leak-deferred-pool-release-paths | bgp reactor forward rail | a leak test asserted that the Outgoing Peer Pool free count is unchanged across the call. A pool that never lent a buffer ends at the same baseline. The assertion would have passed over an untouched pool | added `TestForwardModBufTakenOnRebuild`. It asserts the dispatched item carries a non-zero `peerBufIdx`, so the loan is proven before the balance is. Each rail's release was then discriminated by reverting it, and only that rail's test went red |
| 2026-08-12 | fixit-config-validators-bypassed-at-startup | reload `.ci` | `test/reload/config-reload-invalid-validator.ci` claimed "the checker completing with no further expectation is the proof the session survived". The peer owes nothing past the SIGHUP, so it exits clean whether the daemon lives or dies, and `Checker.expectingClose` (`internal/test/peer/checker.go`) accepts an EOF after a sighup action by design. `test/reload/reload-bad-config.ci` carries the identical claim in its own comment and is untouched | fixed here: `action=sigterm` after the SIGHUP plus `expect=exit:code=0`, the shape `test/reload/signal-quit.ci` uses. `reload-bad-config.ci` and any other `.ci` whose last action is a sighup are unexamined |
| 2026-08-14 | spec-web-templ-migration | web traffic page | two sort tests asserted an order that several formulas produce. Neither saw the defect it named. The producer is `buildTrafficRows` (`internal/component/web/page_traffic.go`), `TotalRate = RxBytes + TxBytes`. `TestTrafficTableData_SortByRate` used rows of 100/100, 5000/5000 and 1000/1000. RX alone, TX alone and the sum order those identically. Its "sorted by total rate" claim held for all three. A first repair of the sibling test picked rx-heavy(1000,0) and balanced(900,500). That fixture reds for RX alone and stays green for TX alone. It discriminated in one direction | fixed. Both tests read one `trafficSortFixture`: tx-led(10,100), rx-led(90,5), middling(50,30). Its sum order differs from its RX-only order and from its TX-only order. Both assert all three positions. `TestTrafficSortFixtureDiscriminates` pins the property, so one edit cannot weaken both. Proven by breaking the formula to each term and watching both tests red |
| 2026-08-14 | fixit-peers-from-tree-stale-shape | bgp reactor config parse | migrating a test onto a MORE FORGIVING parser keeps its shape and drops its meaning. `TestReactorVerifyConfigValid` and `TestReactorVerifyConfigNoMutation` asserted only "no error" after `VerifyConfig`. That was proof under a parser which refused an unreadable peer shape. `PeersFromTree` (`internal/component/bgp/reactor/config.go`) instead SKIPS a peer whose error wraps `ErrIncompleteConfig` and returns success, so a tree nobody can read satisfies the same assertion | both tests now assert the peer count, and `TestBGPVerifyEstimate` asserts its pre-added peers parsed. Measured with a probe: the old flat fixture yields zero peers and no error through `PeersFromTree`, so every count assertion reds on the shape these tests carried before the migration |
| 2026-08-15 | fixit-ike-test-discrimination | ipsec interop scenarios eap-tls and eap-tls13 | a NECESSARY assertion stood in for the SUFFICIENT one, and the observer was on the wrong side. Both EAP-TLS scenarios stopped at `wait_xfrm_sa` on each container and called `log_pass`. An XFRM SA proves two ends installed keys, never that the keys agree. Measured by flipping one octet of the outbound ESP key in `installChildSA` (`internal/component/ike/engine/child.go`): both SA-presence assertions still passed, and so did ze's OWN ESP counter, because sending is not proof of acceptance | both scenarios now ping across the tunnel and call `assert_esp_accepted` (`test/interop-ipsec/lab.py`) for ze AND for strongSwan. Only the peer's inbound counter caught the key flip, so the peer-side call is the interop assertion and the local one is a control. The same session found the general form: two guards in `internal/component/ike/` were green under their own revert, and the fix is a test whose oracle sits where the failure is visible |
| 2026-08-16 | fixit-ci-peer-block-silent-directives | ze-peer `option=linger` beside `reject=bgp` | `option=linger` is documented as the mechanism that holds a negative wire assertion open past the last positive one, and a rejection it finds cannot fail the test. `Peer.completed` (`internal/test/peer/reject.go`) prints the `successful` token BEFORE it enters the linger loop, and `failedCheckPeers` (`internal/test/runner/peer_contract.go`) decides each check peer purely on that token being present in its output. The loop does detect the frame and does return a failing Result, and the verdict was already published. Measured on a two-peer fixture whose export filter was mutated to accept the route it must refuse: the peer printed `successful`, then `lingering`, then the forbidden `180A0000` frame, and the runner reported PASS. The eight committed fixtures pairing linger with `reject=bgp` are not vacuous for a second reason, that their forbidden bytes arrive in or before the frame completing their expectations, but `test/plugin/filter-family-export-flowspec.ci` states in its own header that linger is what catches a route arriving after the End-of-RIB, and that claim is false | not fixed here: the repair is in `internal/test/peer/reject.go`, whose hash is pinned by another closure's review artifact. The two export fixtures were built around a FENCE instead, a second forwarded route the filter passes, delivered after the subject route on the same TCP stream, so the rejection fires before the peer publishes anything. Both were shown red by mutating the filter decision. Whoever fixes the runner should move the token print to after the linger loop, or give the peer a failure token the runner reads |
| 2026-08-19 | - | `ze bgp decode` extended community unit test | `TestExtendedCommunities` (`internal/component/bgp/cli/decode_test.go`) built a thirteen-row table whose `want` cells each carried a `string` and a `value`, and its loop compared only the `string`. The unread column was not merely unread: six of its numbers were not the big-endian reading of their own octets, `route_target` claiming 562954248388609 for bytes that read 563379450150913. A table that looks like two assertions and performs one leaves the second half free to rot, and the next reader trusts a number nothing ever checked | FIXED. The loop now compares `value` too and the six numbers were corrected to their octets. The added assertion found the sixth on its first run, which is the argument for adding it |
