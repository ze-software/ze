| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-25 | fixit-le-replaces-make | `make ze-chaos-lint`, which lints `internal/chaos/` and is reached only through `ze-chaos-verify` | The gate exits 2 with 50 findings: goconst 37, noctx 10, modernize 3. It has presumably been red for some time and nobody has been told, because it sits in `ze-chaos-verify` and NOT in `ze-precommit-verify`, so no commit and no CI job ever asks it. Found while porting `mk/test-chaos.mk` to `le`, and proved pre-existing rather than caused by the port: the pre-port recipe `@$(ZE_LINT_RUN) $(CHAOS_PACKAGES)` was expanded from a probe makefile that includes the real `Makefile` and produces byte-identical output, the same three counts and the same exit 2. The shape worth naming is not the 50 findings. It is that a gate exists, runs, and reports a verdict nobody reads, so its red is indistinguishable from its never having been run. A gate outside the blocking set has no ratchet holding it, and drift there is silent by construction | not fixed. Two decisions are owed and neither is a code change: whether `internal/chaos/` is in scope for the same lint standard as the rest of the tree, and if so what makes the answer visible -- a place in `ze-precommit-verify`, a cadence member that reports it, or a recorded exception. Fixing the 50 findings without deciding that leaves the next drift equally silent |
