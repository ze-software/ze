| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-10 | - | `./le --name continue-ospf verify lint run` | Lint completed after OSPF closure: 93 host findings, 104 Linux/integration findings, two capability findings, and one tracked file outside every lint pass. No OSPF or DDoS diagnostic was reported. The final interop-checker scoped run reports only two `check_paths_limit.go` findings per target: staticcheck and wastedassign | Left red. Full-tree output is `tmp/session/2026-09-09-5bc855cc-5761-4012-8d90-7a9823029ab3/scratch/ospf-closure-lint.log`. Do not rerun unchanged checks or repair unrelated lint during the continuation |
| 2026-09-10 | spec-ospf-auto-cost-reference-bandwidth | `./le --name continue-ospf repository check`, `commit audit`, and `doc check verify` | Repository check reports 24 BGP wiring findings. Commit audit reports 16 changed-test findings across concurrent work. Documentation verification reports 3759 drift findings, eight summary-rule failures, and unrelated source-anchor failures | Left red during OSPF closure. The OSPF race suite and selected FRR interop action pass. The inherited lifecycle test moved to its own file without changing the existing RFC6549 test |
| 2026-09-07 | connected-static-reach-the-locrib | `./le functional static`, case 2 `static-interface-nexthop-no-backend` | The case runs at its 15s budget and passes or times out depending on the run: three runs on this host gave 15.0s TIME, 17.4s TIME and 12.7s PASS, against the suite's own recorded average of 4.0s for it. Everything the case asserts happens in the timing-out runs too, so the budget is what it fails: `tun404` is named, `static: route skipped, kept rest of section` is logged, and the fixture prints `DAEMON-UP-WITH-ROUTE-SKIPPED`; what runs out is `staticFinish` (`internal/test/fixture/netfilter_fixture_static.go:30`), SIGTERM then `waitDead`, while the daemon has already printed `Ze stopped.`. Proved pre-existing rather than caused by the work in hand: the 17.4s timeout was on a HEAD tree carrying every other session's uncommitted files and none of this session's. It is the only case in the suite that runs on an unprivileged host, the other seven carry `caps=net-admin` and SKIP, and the suite is release evidence only and in no blocking set, so a case sitting on its budget has had nobody to report it to | not fixed. Row only, per the walked-into-defect rule: the work in hand is the static relocation, whose behavior this case exercises identically before and after (the refusal moved from `buildRoute` to `staticPath` and logs the same two lines). Whoever takes it owns two questions and neither is the daemon's: why teardown costs 8-13s when the daemon's own shutdown is complete in the captured output, and whether a 15s budget is right for a case whose average is 4s |
| 2026-09-05 | spec-router-advertisement | `./le iface-resolution` (`internal/le/ifaceresolution.Check`) over this checkout | The gate exits 1 at HEAD, naming one direct kernel resolution site: `internal/test/fixture/plugin_fixture_08_tunnel_ttl_linux.go:32`, `netlink.LinkByName(device)`. Same gate and same class as the 2026-09-02 row above, different file: a fixture landed with no allowlist entry and its red went unreported. Proved pre-existing rather than caused by the work in hand. The file is committed and unmodified, and it arrived in `90412c682`. The RA work this session verified adds no resolution site of its own, so AC-11 of `plan/spec-router-advertisement.md` holds over the sole remaining finding | not fixed. Row only, per the walked-into-defect rule: the work in hand was the RA feature, which the red does not block. Second row for this gate in four days, which is the recurrence signal a deliberate pass over `internal/test/fixture/` should act on |
| 2026-09-02 | - | `./le iface-resolution` (`internal/le/ifaceresolution.Check`) over this checkout | The gate exits 1 at HEAD. It names two direct kernel resolution sites in `internal/test/fixture/vrrp_accept_mode_linux.go`, lines 70 and 80, both `netlink.LinkByName`. Three sibling fixture files carry an allowlist entry each. This one carries none, so the fixture landed without its exemption and the red went unreported. Proved pre-existing rather than caused by the work in hand. The file is committed and unmodified. The findings come from `scanFile`, which this session did not change | not fixed. Row only, per the walked-into-defect rule: the work in hand was the allowlist liveness accounting, which the red does not block |
| 2026-08-30 | site-renderers-in-go (phase 10) | `./le verify lint run scope ./internal/le/site/...` | 18 `goconst` findings, red at HEAD and in every flavour: five docs source paths stated in both `docsmanifest.go` and `redirect.go`, `"data"` and `"tmp"` across `pages.go`, `search.go` and `seo.go`, and `"changes"` and `"dependencies"` beside the `changesProducer` and `dependenciesDirectory` constants that already spell them. Proved pre-existing rather than caused by phase 10: every flagged site is in a file this phase did not touch, or on a line it did not add, and the phase's own additions state none of the literals (`git diff internal/le/site/redirect.go`). The package was written across nine phases in three days and nobody linted it until the tenth | not fixed, and the scope CANNOT go green in this working tree whatever is done to the 18: `internal/le/site/zzscratch_census_test.go` is an untracked scratch file whose removal the owner must approve, and it carries the run's one `noctx`. Beside that, the findings block nothing in phase 10 and their package is `internal/le/site`, whose nine earlier phases each own part of it; folding a seven-file constant sweep into the closing commit costs that commit its single focus. The one finding phase 10 DID introduce, a `modernize` `stringscutprefix` in `llmsfull_test.go`, was fixed on the spot. Whoever takes the sweep should note that `"data"` carries two meanings in this package, an artifact directory and a CLI category name in `catalog.go`, so one constant for both would overload the name |
| 2026-08-25 | fixit-le-replaces-make | `./le test-chaos lint`, which lints `internal/chaos/` and is reached only through `ze-chaos-verify` | The gate exits 2 with 50 findings: goconst 37, noctx 10, modernize 3. It has presumably been red for some time and nobody has been told, because it sits in `ze-chaos-verify` and NOT in `./le verify current mode full`, so no commit and no CI job ever asks it. Found while porting the retired `mk/test-chaos.mk` (current producer: `internal/le/testchaos/actions.go`) to `le`, and proved pre-existing rather than caused by the port: the pre-port recipe `@$(ZE_LINT_RUN) $(CHAOS_PACKAGES)` was expanded from a probe makefile that includes the real the retired `Makefile` (current producers: `internal/le/` native action tables) and produces byte-identical output, the same three counts and the same exit 2. The shape worth naming is not the 50 findings. It is that a gate exists, runs, and reports a verdict nobody reads, so its red is indistinguishable from its never having been run. A gate outside the blocking set has no ratchet holding it, and drift there is silent by construction | not fixed. Two decisions are owed and neither is a code change: whether `internal/chaos/` is in scope for the same lint standard as the rest of the tree, and if so what makes the answer visible -- a place in `./le verify current mode full`, a cadence member that reports it, or a recorded exception. Fixing the 50 findings without deciding that leaves the next drift equally silent |
| 2026-08-31 | fixit-rfc-drain-quota-never-armed | `./le doc check links` | Exit 1 with three broken Design references, all at HEAD (`c630cf08b`, clean working tree for the three files): the header comments of `internal/le/site/activityscript.go` and `internal/le/site/activitystyle.go` resolve to `internal/le/site/the`, and `internal/le/sourcerewrite/activitymeasure.go` to `internal/le/sourcerewrite/one`. Each one opens `// Design:` with prose ("the activity page's own script", "one measurement of a repository's commit calendar") where the checker reads the first token after the keyword as a path. Found while validating a journal edit during the rfc1877 extraction walk, so nothing in that walk blocks on it. No stage of `./le verify current mode full` runs this check, which is why three files landed with it red | not fixed. The three headers sit in a package another session is actively writing (`site-renderers-in-go`), and a `// Design:` line stating prose rather than a path is a shape the checker could accept explicitly instead |
| 2026-08-31 | - | `./le cli-grammar` | The check that enforces the R1-R9 CLI grammar ruleset is in no blocking set, so a grammar violation fails no automated gate. `./le verify list mode full` prints 43 stages and none of them is a grammar stage, no workflow names the action, and the only reference to the `cligrammar` package outside its own directory is the one line in `internal/le/register.go` that registers the action, which makes it reachable rather than run. `ai/rules/cli.md:23` mandates the R1-R9 ruleset in prose, and prose is the whole enforcement. The check itself works, and what it answers is worth stating precisely because two sessions measured it differently on the same day. `cligrammar` walks the tree on disk (`filepath.WalkDir` in `cligrammar.go` and `flags.go`), so it judges the WORKING TREE and not HEAD. Against HEAD `79d199a31` it printed `cli-grammar: OK` at 13:49; in this checkout an hour later it exited 1 with twelve R9 findings, the flowspec `destination-ipv4`, `destination-ipv6`, `destination-port`, `source-ipv4`, `source-ipv6` and `source-port` keywords under both `announce` and `peer announce`. The difference is not time: it is that the `grammar` session holds `ze-flowspec-cmd.yang` and `ze-cli-announce-cmd.yang` uncommitted and rewritten, so the verdict moves with one session's unstaged edits. An earlier version of this row said the findings were at HEAD and had landed on 2026-08-30; both halves were taken from a peer's message and neither survived checking. The absence from every blocking set is the verified claim, from `./le verify list mode full` and from grepping the callers of the `cligrammar` package. This is the same shape as the 2026-08-25 row above, one command later: a gate outside the blocking set has no ratchet holding it, so its red is indistinguishable from its never having been run | not fixed, and the twelve findings are not the defect. They belong to the flowspec keyword change `grammar` is mid-way through and go green with it. What is owed is the decision the 2026-08-25 row already asked for and nobody answered: either `./le cli-grammar` joins `./le verify current mode full`, or the R1-R9 mandate in `ai/rules/cli.md` stops claiming an enforcement that does not exist. Adding it to the stage list needs the tree green first, so the order is `grammar`'s flowspec commit, then the wiring |
| 2026-09-05 | - | `internal/component/doctor`, its whole test build | `codes` was declared twice in one package: `checks_redistribute_test.go` (no build tag, added by `3c71ff73e`) and `checks_mpls_linux_test.go` (`//go:build linux`, from `eae282592`). On linux both compile, so the package failed to typecheck and NOT ONE of its tests had built or run since. Both files are committed on main and the working tree was clean for that directory, so this was nobody's uncommitted work. The shape worth naming is not the duplicate: it is that TEN agent sessions read this red on the same afternoon, each correctly applied `ai/rules/principles.md` ("a red you did not cause belongs to another session"), and each named it as foreign and moved on. In a checkout many sessions share, a committed red is attributed to a peer by every reader, so the rule that stops sessions fighting over each other's work also guarantees that a red nobody caused is a red nobody fixes. Its cost compounds silently: a package with no test build is a package with no coverage, and the doctor checks are what tell an operator their configuration is unreachable | fixed here. One helper survives, in the untagged file so every platform reaches it, keeping the indexed loop the linux copy had (`Diagnostic` is 184 bytes, gocritic rangeValCopy). `go test ./internal/component/doctor/` builds and passes again. The class needs more than this fix: a red at HEAD that no session claims wants an owner by construction, not by whoever tires of reading it first |
| 2026-09-05 | le-is-a-ze-binary | `TestNoPythonLeRemains` (`internal/le/contract_test.go`) | The test walks every TRACKED file and refuses a first-party `.py`, `.sh`, `.bash` or `.mk`. It has been red since `49c145d3b` on 2026-08-31, which committed `plan/rfcgate-6-partial-walks/classify-rfc3748.py` and `classify-rfc5176.py` as preserved evidence of four RFC extraction walks. That commit belongs to a different spec, three days after the swap this test guards landed in `eae282592`, and five days passed with nobody reading the red. The shape is what makes it invisible: the test's population is the WHOLE checkout, so the session that breaks it is never the session that owns it, and the owning spec was already implemented and waiting on closure | FIXED, 2026-09-05, in `456bf8fc1`, and by the third route neither session had: build the capability the scripts carried, then delete them. `./le rfc extraction-classify decisions <path>` (`internal/le/rfc/actions.go`) applies one authored decision per site and per section from one decisions file, which is the step `le` did not provide and the only reason the two scripts existed. The owner authorised the deletion once the native verb existed. `firstPartySource` (`internal/le/contract_test.go`) was NOT narrowed: it still excludes only `vendor/`, `third_party/` and `test/exabgp-compat/`, so the test carries AC-12 unweakened and `go test -run TestNoPythonLeRemains ./internal/le/` passes. The transferable point is about the SHAPE rather than these two files: a tree-wide contract test is a standing obligation on every future commit, and a spec cannot close it once, so a spec that declares one owes a way for the breaking session to meet it rather than for the owning session to police it. Here that way was a `le` verb, and the throwaway script was the tell that one was missing |
