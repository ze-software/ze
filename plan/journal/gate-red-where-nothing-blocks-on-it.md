| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-30 | site-renderers-in-go (phase 10) | `./le verify lint run scope ./internal/le/site/...` | 18 `goconst` findings, red at HEAD and in every flavour: five docs source paths stated in both `docsmanifest.go` and `redirect.go`, `"data"` and `"tmp"` across `pages.go`, `search.go` and `seo.go`, and `"changes"` and `"dependencies"` beside the `changesProducer` and `dependenciesDirectory` constants that already spell them. Proved pre-existing rather than caused by phase 10: every flagged site is in a file this phase did not touch, or on a line it did not add, and the phase's own additions state none of the literals (`git diff internal/le/site/redirect.go`). The package was written across nine phases in three days and nobody linted it until the tenth | not fixed, and the scope CANNOT go green in this working tree whatever is done to the 18: `internal/le/site/zzscratch_census_test.go` is an untracked scratch file whose removal the owner must approve, and it carries the run's one `noctx`. Beside that, the findings block nothing in phase 10 and their package is `internal/le/site`, whose nine earlier phases each own part of it; folding a seven-file constant sweep into the closing commit costs that commit its single focus. The one finding phase 10 DID introduce, a `modernize` `stringscutprefix` in `llmsfull_test.go`, was fixed on the spot. Whoever takes the sweep should note that `"data"` carries two meanings in this package, an artifact directory and a CLI category name in `catalog.go`, so one constant for both would overload the name |
| 2026-08-25 | fixit-le-replaces-make | `./le test-chaos lint`, which lints `internal/chaos/` and is reached only through `ze-chaos-verify` | The gate exits 2 with 50 findings: goconst 37, noctx 10, modernize 3. It has presumably been red for some time and nobody has been told, because it sits in `ze-chaos-verify` and NOT in `./le verify current mode full`, so no commit and no CI job ever asks it. Found while porting the retired `mk/test-chaos.mk` (current producer: `internal/le/testchaos/actions.go`) to `le`, and proved pre-existing rather than caused by the port: the pre-port recipe `@$(ZE_LINT_RUN) $(CHAOS_PACKAGES)` was expanded from a probe makefile that includes the real the retired `Makefile` (current producers: `internal/le/` native action tables) and produces byte-identical output, the same three counts and the same exit 2. The shape worth naming is not the 50 findings. It is that a gate exists, runs, and reports a verdict nobody reads, so its red is indistinguishable from its never having been run. A gate outside the blocking set has no ratchet holding it, and drift there is silent by construction | not fixed. Two decisions are owed and neither is a code change: whether `internal/chaos/` is in scope for the same lint standard as the rest of the tree, and if so what makes the answer visible -- a place in `./le verify current mode full`, a cadence member that reports it, or a recorded exception. Fixing the 50 findings without deciding that leaves the next drift equally silent |
