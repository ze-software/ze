| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-16 | weakened-per-commit | hooks and the commit gate | A `test-relax:` justification explains ONE diff. It was stored in the test file forever. After the commit it described a change that file's reader can no longer see. The end state is fixed. The pile grows until nobody can read it. Then nobody reads it. Then writing one costs nothing, and the gate that demands one is theatre. It reached 780 tokens across 466 files. 402 had never been triaged, and 3 in 4 excused an improvement rather than a loss. A ceiling and a census were built to cap the pile, which measured the symptom | The record moved to `test/weakened.md`, one row of test name and reason, REPLACED per commit. It cannot accumulate, so no ceiling is needed and both retired. Git history is the permanent store. `git log -p -- test/weakened.md` shows each set of rows beside the change it accepted. The general form is now a rule point: a record every reader pays for must change what the reader does. History goes to `plan/learned/` or the spec instead |
