# Removal leaves the file on disk

A command removes a path from the tree it commits and does not remove it from
the working tree. The commit is right, `git log` is right, and the file is still
there, now untracked. Every gate that answers a question by asking the
FILESYSTEM then reads the old tree, and the one that reads git reads the new
one, so the two disagree with nothing saying which is current.

The tell is a gate that goes green over a removal it should have noticed, and a
`git status` carrying a `??` line for a path the last commit deleted.

`/ze-close` names the shape from the other side: `find_dangling` resolves a
citation with a filesystem test, "which sees an untracked file, so the working
tree reads green while the tree your commit produces is red". The same test that
hides a dangling citation hides the removal that created it.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-12 | derived-indexes-answer-a-query | the same `renderSharedIndexRepair` path (`internal/le/commit/script.go`), reached through `/ze-close` commit B | FOURTH instance, five days after the row below said the deliberate pass was owed rather than optional, and nothing has changed at the source. Closure commit `6542cc081` removed `plan/spec-derived-indexes-answer-a-query.md` and left a byte-identical untracked copy, which `shasum -a 256` matched against `537444e970`'s blob. Two things this occurrence adds. The script's two `git update-index --force-remove` calls are the WHOLE removal, one against the private index and one against the shared one, and the generated script carries no `rm` at any line, so the residue is a property of the generator rather than of any session's diligence. And `./le spec citation` had just run GREEN over this spec minutes earlier while the file was still tracked, so the gate that would notice the residue is the same gate the residue defeats | removed by hand after `diff` and `shasum -a 256` proved the on-disk bytes identical to commit A's blob, which is the practice the 2026-09-06 row records and the third session in a row to perform it. Not fixed at the source. The repair is one line in `renderCommitBlock`: a `remove` path whose on-disk content matches the blob just committed is deleted from the working tree, and one that does NOT match is left alone and named, because that difference is a session's unsaved edit and `ai/rules/never-destroy-work.md` governs it |
| 2026-09-07 | rib-package-dead-surface | the same `renderSharedIndexRepair` path (`internal/le/commit/script.go`), reached through `/ze-close` commit B | SECOND instance in two days, and the recurrence is what this row is for. Closure commit `9c3b673ebc` removed `plan/spec-rib-package-dead-surface.md` from git and left a byte-identical untracked copy on disk, exactly as the row below describes. A third case was found while confirming this one: `plan/spec-firewall-domain-group.md` is untracked in the same tree and `f339bf0ee7` closed it, so that closure leaked too and nobody noticed. Neither leak was reported by a gate. The count is therefore at least three closures across three sessions, which is no longer a session forgetting to tidy up | Not fixed at the source, and this row exists to say the deliberate pass the row below proposed is now owed rather than optional. Two residues were removed by hand here after checking each against the blob its own commit A recorded; the firewall-domain-group copy was LEFT, because it belongs to another closure and the rule that lets a session clean up after itself does not make it the owner of someone else's leftovers. The repair is unchanged: remove the working-tree path in `renderSharedIndexRepair` once the commit has read the blob out of the private index. What this row adds is the cost of not doing it, which is that every closure from now on either leaks or spends a turn on `never-destroy-work` deciding whether it may delete its own output |
| 2026-09-06 | ze-test-dns-stub | `./le commit create ... remove <path>`, through `renderCommitBlock` and `renderSharedIndexRepair` (`internal/le/commit/script.go`) | The generated script removes the path with `git update-index --force-remove` against the private index and again against the shared one. Neither touches the working tree, and nothing in `prepare.go` does either. So closure commit B removed `plan/spec-ze-test-dns-stub.md` from git and left an identical untracked copy on disk. `./le spec citation` then answered `OK (322 specs)` over a tree that holds 321, because it resolves a spec by asking whether the file exists. The count moved to 321 only after the file was deleted by hand | Not fixed at the source. This closure removed its own residue with `rm` after confirming the on-disk bytes matched what commit A recorded, which is where the content survives. The source repair is two lines in `renderSharedIndexRepair`, after the commit has read the blob out of the private index, and it belongs to a deliberate pass rather than to a closing session: every commit in this repository flows through that script, the golden-text tests in `internal/le/commit` assert what it renders, and a wrong edit stops every other session committing |
