# Record written before the operation succeeds

A command persists a record of what it is about to do. It then fails, and the
work never happens. The record outlives the failure. The next reader cannot
tell it apart from a record of real work, because both are just rows.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-08 | verification-debt-clearing | `recordDebt` cover count (`internal/le/commit/debt.go`), called from `Create` (`internal/le/commit/prepare.go`,341) | Third occurrence of the row below, in the shape `de31341fd7` gave the ledger on 2026-09-07. A second `create` for one commit no longer leaves an orphan row: `openDebtRowAt` finds the open row for the same gate and reason, and `extendDebtRow` raises its cover to `(+1 more)`. The commit that never happened is now INVISIBLE, because the inflated row still names a real commit and reads like a row covering two. `debtCovered` reads that count, so `dischargeCommits` (`internal/le/commit/discharge.go`) demands two commits for a row that covers one and no discharge can answer it. `dfea2968.md`,45 and `dfea2968.md`,67 were both stuck that way; each covers ONE commit (`c848301fd9`, `83d426a0ac`, the only commits `git log --all` finds for those subjects), and each was two pre-dedup rows with byte-identical subjects at `de31341fd7^:plan/verification-debt/dfea2968.md`,75 and ,80 and ,99 and ,102. A runnable gate hides the same inflation, because `debt-clear` selects on the gate name and never reads the count | not fixed. The two counts were corrected in the ledger and both rows discharged; the producer was left alone because the spec in hand does not own it. The record still lands before the script that commits, so the repair is the one the 2026-08-19 row already names: the cover is raised by the commit that happens, never by the create that prepares it |
| 2026-08-28 | - | `Create` verification-debt shard (`internal/le/commit/prepare.go`) | Second occurrence of the row below, in the native command that replaced the Python helper, so the port carried the ordering across. `recordDebt` is called at `prepare.go`,183, and `nextTag` (`script.go`,161) refuses a tag that is not alnum/dot/underscore/dash at `prepare.go`,206 -- after the rows are already on disk. `tag "fix(bgp)"` therefore left three rows in `plan/verification-debt/5184512c.md` naming a commit that `git log --all --grep` finds zero of. Orphan rows cannot be told from real debt by reading the ledger, which stands at 1066 open rows; `debt-clear` re-runs a gate for them, and `push` refuses over them until somebody deletes them by hand. Note what is NOT wrong, because the 2026-08-19 row invites the guess: the 72-character subject check is NOT part of this. `Message` runs at `prepare.go`,79, well before `recordDebt`, so an over-long subject is refused cleanly. I claimed both in the first draft of this row and had to correct it after reading the order | fixed: `validateTag` is extracted from `nextTag` and called at `prepare.go`,86, beside `Message` and before any side effect. `recordDebt` itself does NOT move -- the debt file is appended to `paths` and feeds `result.Verify` and the `commitBlock`, so the record has to stay where it is and the refusing checks have to come earlier. `TestCreateRefusesBadTagBeforeRecordingDebt` drives `tag "fix(bgp)"` and asserts the refusal leaves no shard in `plan/verification-debt/`; it fails with "refused create left 1 debt shard(s) behind" when the early call is removed. The three orphan rows were deleted by hand, as the 2026-08-19 occurrence did |
| 2026-08-19 | - | commit_helper verification-debt shard | `create` calls `record_debt` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)) before it validates the subject. A create refused for a 73-character subject had already appended its two rows. The retry carried a shortened subject, which renders a different row, so the shard held two rows naming a commit that does not exist. `--push` refuses over them. `debt-clear` re-runs a gate for them. Met when I committed the test-coverage gate | not fixed. The two orphan rows were deleted by hand. The record belongs after the last check that can refuse a create, and `create` has several |
