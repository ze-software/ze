# Requirement met on the rails the spec planned

A protocol rule is centralized at one site so no two producers can disagree, and
every producer the spec enumerated asks there. The requirement is then recorded
as met. The set that was enumerated is the set the spec set out to change, not
the set that WRITES the artifact the rule governs, so a producer nobody listed
keeps emitting the thing the rule forbids.

The centralization is what hides it. One site answering the question reads as
completeness, and the tags on the tests that drive the listed producers are each
honest about what they assert. Nothing in the tree compares the set of askers
against the set of writers, so the gap lives entirely in the sentence the
summary writes, and that sentence is what the public ledger publishes.

`ai/rules/principles.md` states the general form: the work a change owes is
measured by what the change can now REACH, never by the files it edited. Here
the measure to use is different again, and narrower: before recording a MUST as
met, enumerate every function that WRITES the artifact the requirement governs,
and show that each one reaches the site that answers.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-05 | srv6-ebgp-egress-filter | bgp egress, the BGP Prefix-SID attribute against RFC 8669 Section 8 | `prefixSIDAllowedTo` (`internal/component/bgp/reactor/forward_prefix_sid.go`) is the single site that answers "may this destination be sent attribute 40", and four producers ask it: `forwardUpdateCore`, `reactorForwardRS`, `buildStaticRouteUpdateNew` and `toPluginParams`. A fifth writes UPDATEs and does not. `buildBatchAnnounceUpdate` (`internal/component/bgp/reactor/reactor_api_batch.go`) copies the caller's attribute block verbatim into `plan.emit` and its only removal is `plan.drop` of LOCAL_PREF, so attribute 40 in that block reaches an external peer with no leaf set. It carries the API announce, the grouped announce and `sendStaleReadvertise`, the RFC 9494 stale readvertise, whose base is a route's received attribute block. The spec knew the rail was ungated and still took the `{gap}` annotation off RFC8669-8-1 and wrote `docs/features/srv6.md` as "removed from every UPDATE sent to an EBGP peer", so the ledger reported a proven MUST for a week. The same package produced this shape once before, which is what `localPrefAllowedTo` was written to end | FIXED on 2026-09-05 in `4054ed854`, by `spec-prefix-sid-announce-rail-boundary`. `buildBatchAnnounceUpdate` takes the destination's `propagate-srv6-prefix-sid` leaf, asks `prefixSIDAllowedTo` beside the LOCAL_PREF drop, and records `plan.drop` of code 40; the leaf is a field of `announceBuildKey`, so a build group cannot share one UPDATE across peers that answer differently. The owner wrote the `test/rfc-changed.md` approval row that unblocked the parameter widening. Before that, closure measured the leak rather than inferring it, with `TestAnnounceRailKeepsPrefixSIDInsideTheSRDomain` (now `internal/component/bgp/reactor/forward_prefix_sid_announce_rail_test.go`), red at HEAD; restored the `{gap}` and the Meta count ten to eleven in `rfc/short/rfc8669.md`; corrected two rows of `docs/features/srv6.md`; and homed the remainder at `spec-prefix-sid-announce-rail-boundary`. Both are now back the other way, because the code caught up |
| 2026-09-05 | prefix-sid-announce-rail-boundary | the fix's own TEST set: `sendStaleReadvertise` (`internal/component/bgp/reactor/reactor_api_batch.go`), the second of three entries into the gated rail | The same shape one level down. The fix enumerated all five RAILS and gated them, and the tests enumerated one of the three ENTRIES into the rail it fixed. `test/plugin/prefixsid-announce-rail-boundary.ci` drives `send bgp * update hex`, which enters `AnnounceNLRIBatch` with `Stale == 0`, so it never reaches `sendStaleReadvertise`. That call site reads the destination's leaf and hands it to the same builder, and a constant written there would have leaked attribute 40 on every RFC 9494 LLGR re-advertisement toward an LLGR-capable eBGP peer, with the whole announce-rail suite green. The spec's own Wiring Test table named a `.ci` for that entry and the delivered `.ci` covered the other one, so the table read complete | FIXED at closure. `TestStaleReadvertiseKeepsPrefixSIDInsideTheSRDomain` (`internal/component/bgp/reactor/forward_prefix_sid_readvertise_rail_test.go`) drives `AnnounceNLRIBatch` with `Stale: 1` and a readvertise egress filter registered, through a piped session, and reads the flushed frame; both `RFC8669-8-1` polarities carry a `rfc/discrimination/rfc8669.json` record observed red under a disabled `sendStaleReadvertise`. Discrimination checked separately at the call site: with `propagatePrefixSID := true` substituted there under a Go overlay, the new test fails on its own assertion and the announce-rail test stays green. Transferable, and it sharpens the class: after enumerating every function that WRITES the artifact, enumerate every CALLER of the one you changed, because a shared guard is only as good as the weakest argument any caller passes it |
| 2026-09-08 | none (found while specifying `spec-forwarded-as-path-obeys-rfc6793-for-every-destination`) | the originating UPDATE encoders, `internal/component/bgp/message/update_build*.go` | RFC 6793 Section 4.2.2 obliges an AS4_PATH beside a two-octet AS_PATH whenever the path holds a non-mappable AS number, and `wireu` is the single site that answers that question for the FORWARDED rails. The originating encoders are a producer nobody enumerated: a grep for `AS4Path` over `internal/component/bgp/message/update_build*.go` returns nothing outside tests, so a route ze ORIGINATES whose configured AS_PATH carries an AS number above 65535 reaches a two-octet peer as AS_TRANS with the real number carried nowhere. The text-route parser has no `as4-path` keyword either, so an operator cannot supply it by hand. Reachable through `writeUpdate` and `SendAnnounce`, wired at `peer_run.go`, which is the rail `exportFilterForBody` filters | FIXED on 2026-09-11. The rule moved DOWN rather than being restated: `attribute.AS4PathFor` and `attribute.AS4AggregatorFor` (`internal/core/bgp/attribute/as4.go`) now own "is one owed toward this peer, and what goes in it", `wireu.as4PathForPath` calls them for the forwarding rails, and `message` reaches them too. `message` cannot import `wireu` -- `wireu/wire_update.go` and `wireu/split.go` import `message`, so the tier direction forbids it and copying the rule into the encoder would have been the second declaration `ai/rules/principles.md` bans. Two new `UpdateBuilder` methods carry it, `appendASPath` and `appendAggregator` (`internal/component/bgp/message/update_build.go`), and all SEVEN builders append through them: BuildUnicast, BuildGroupedUnicast, BuildVPN, BuildLabeledUnicast, BuildEVPN, BuildFlowSpec and BuildPlugin, which each held a verbatim copy of the six-line AS_PATH block. BuildFlowSpec takes no configured AS_PATH and still needed it, because a four-octet LOCAL AS prepended to a self-originated route is non-mappable on its own. `internal/component/bgp/message/rfc6793_originate_as4_test.go` drives every builder in a table rather than one of them, which is what this class asks for, and was red at HEAD on all seven for AS4_PATH and on all four AGGREGATOR writers; ten tags across RFC6793-4.1-6, -4.2.2-2, -3, -5 and -6 carry `rfc/discrimination/rfc6793.json` records observed red under a disabled `AS4PathFor` and `AS4AggregatorFor`. Still open and named here rather than fixed: `internal/component/bgp/reactor/reactor_wire.go` holds its own `as4PathForASNs` and `anyNonMappableAS` over a flat `[]uint32` for the RouteSpec announce rail, which emits the attribute correctly and is a THIRD statement of the same rule |
