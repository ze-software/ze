# One field carries two meanings

A single field answers two different questions, and the two agree for as long as
one role holds. They diverge the moment the roles cross, and the reader that
asked the second question silently gets the first question's answer. The tell is
a field whose writer names one concept and whose reader names another.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-18 | fixit-child-sa-rekey-policy | ike | `sa.IsInitiator` is the IKE SA role, and `narrowChildSelectors` (`internal/component/ike/engine/ts_narrow.go`) read it as the role of the EXCHANGE in hand to orient the configured policy into TSi/TSr. RFC 7296 Section 1.3.2 lets either end start a CREATE_CHILD_SA, so a peer-initiated Child SA rekey on a tunnel ze initiated compared ze's local prefix against the peer's, found no intersection, and answered TS_UNACCEPTABLE to every rekey the peer started once traffic selectors were configured | fixed. Every caller of that function is answering somebody else's proposal, so the orientation is the constant "this node is the responder" |
| 2026-08-18 | fixit-child-sa-rekey-policy | ike | `sa.NextMsgID` is this node's own request counter, and `finishResponderEstablish` (`internal/component/ike/engine/responder.go`) set it from the Message ID of the peer's IKE_AUTH request. RFC 7296 Section 2.2 gives each end two independent counters, so the responder's first self-initiated request went out as id 2 while a conforming peer expected 0, and `classifyInbound` (`internal/component/ike/engine/msgid.go`) matches exactly. An original responder could raise no exchange at all: no DPD probe, no Delete, no Child SA rekey, no IKE SA rekey. MEASURED in QEMU as a Child SA rekey that reached nobody and hard-expired five seconds later | fixed. The responder's own counter stays 0 and `resumeRequestsAfter` is deleted |
| 2026-08-19 | verify-scope-4-suite-budget-and-ci | `ZE_VERIFY_MODE` | One environment variable carries a BOOLEAN and a MODE NAME. `execStage` (`scripts/status/verify_run.go`) exports `ZE_VERIFY_MODE=1` to every stage, and `verifyModeEnabled`, behind `VerifyModeEnabled` (`internal/test/runner/parallel.go`), reads it as a flag that turns a suite's silent environment skips into hard failures. The `ze-precommit-verify-list` target (`Makefile`) reads the SAME name as a mode name, passing `$(or $(ZE_VERIFY_MODE),ze-precommit-verify)` to `--list`. So any `make ze-precommit-verify-list` reached from inside a running verify is handed the mode `1`, which `stagesForMode` does not know, and `runVerify` turns the empty stage list into exit 2. The comment above that target makes it sharper: the variable was renamed from `MODE` to `ZE_VERIFY_MODE` precisely because "make imports the environment" and a stray `export MODE=...` "would silently make this print the wrong list" -- and the verify runner is the thing that exports the colliding name. Found while writing the sharded CI workflow, which must list the stages and then run them with the flag set. **NOT latent: it bit a second caller within the hour.** `verifyStageList` (`scripts/dev/github_workflows_test.go`) runs the list target with the inherited environment, and `ZE_PACKAGES` is `go list ./...`, so `./scripts/dev` runs inside the `ze-unit-test-cached` stage -- with `ZE_VERIFY_MODE=1` already exported to it. The new shard-coverage test therefore fails inside EVERY verify run. Measured: `ZE_VERIFY_MODE=1 make ze-precommit-verify-list` exits 2 with `unknown mode "1"`, the same command with `ZE_VERIFY_MODE=ze-precommit-verify` appended as a make argument exits 0 and prints 30 stages | not fixed, and worked around in the caller: the workflow names the mode on the command line (`make ze-precommit-verify-list ZE_VERIFY_MODE=ze-precommit-verify`) so the make-level assignment beats the inherited environment, and sets `ZE_VERIFY_MODE: "1"` only on the step that RUNS the stages. The repair is to split the two meanings, because the workaround protects one caller and leaves the trap for the next: the boolean wants its own name, or the list target wants an argument rather than a variable make imports from the environment |
| 2026-08-19 | none (extended-community renderer work) | bgp config | The 4-octet Global Administrator sub-field means an IPv4 unicast address under Type 0x01 (RFC 4360 Section 3.2) and an AS number under Type 0x02 (RFC 5668 Section 2), and `parseRouteTargetOrOrigin` (`internal/component/bgp/config/routeattr_community.go`) writes an AS number into the Type 0x01 slot: for `target:65536:100` it takes the `ASN > 65535: Use Type 1 (IPv4) format if number fits in 16 bits` branch and emits `0102 00010000 0064`, and `parseRouteTargetOrOrigin4` does the same for every `target4:`/`origin4:` spelling, with the comment stating the choice. A conforming peer decodes that Route Target as IPv4 address 0.1.0.0, so the two ends disagree on what the community identifies. The sibling branch is right: the `L` suffix reaches `ecTypeTransitive4ByteAS`, and `parseSingleExtCommunity` (`internal/core/bgp/attribute/builder_parse.go`) and `parseRouteTargetExtCommunity` (`internal/component/bgp/route/route_community.go`) both pick Type 0x02 for an AS above 65535. The same function truncates in the other branch: with `num > 0xFFFF` it selects Type 0x02 and writes `num` into two octets, so `target:65536:70000` encodes as 4464 with no error. Found while completing `ExtendedCommunity.AppendDecoded`, which now renders the Type 0x01 form as a dotted quad and makes the mis-encoding readable rather than hex | fixed 2026-08-19. `fourByteASExtCommunity` (`internal/component/bgp/config/routeattr_community.go`) is now the one writer of the four-octet AS form, it refuses a number above 65535 instead of truncating, and `parseRouteTargetOrOrigin`, `parseRouteTargetOrOrigin4` and `parseFlowSpecRedirect` all route through it. `parseRouteTargetExtCommunity` (`internal/component/bgp/route/route_community.go`) carried the truncation half and now refuses the same pair |
| 2026-08-19 | none (FlowSpec encode defects) | bgp flowspec route actions | `FlowSpecActions.MarkDSCP`, `.RateLimit` and `.RateLimitPackets` (`internal/component/bgp/types/types.go`) each carried the VALUE and, by being zero, the claim that no action was asked for. `flowSpecActionExtComm` (`internal/component/bgp/plugins/nlri/flowspec/encode.go`) guarded on `> 0`, and both meanings are real values: RFC 8955 Section 7.1 gives a traffic-rate of 0 the meaning "discard all traffic for this flow", and DSCP 0 is CS0. So `then mark 0` sent no marking, and `then rate-limit 0` sent a FlowSpec route with NO traffic filtering action, which Section 7 leaves at the default accept -- the operator wrote a discard rule and the peer was told to forward. `ParseFlowSpecArgs` accepted both inputs as valid, so nothing anywhere reported the loss | fixed by splitting the two meanings: each numeric action gained a companion `*Set` flag the parser records, and the encoder emits when the flag is set OR the value is non-zero, so a caller that fills only the value is unaffected. `TestEncodeRouteEmitsZeroValuedActions` drives all three from `EncodeRoute`. The clean type is a pointer, and it was NOT taken: it would have forced an edit to `TestFlowSpecTrafficMarkingReservedBytesZero`, which carries an `RFC requirement:` tag and cannot be changed without the owner |
| 2026-08-19 | cli-column-order | cli table rendering | `uptime` names the peer session's age in a `show bgp summary` peer row and the daemon's age in the record that holds those rows. One command answers with both shapes. A column order keyed by command path alone ranks one `uptime` and applies that rank to the other. That puts the daemon's uptime seventh in a record where it belongs third. `handleBgpSummary` (`internal/component/bgp/plugins/cmd/peer/summary.go`) builds both shapes into one answer, so no command-path lookup can tell them apart | fixed in the design rather than found in the field. `RegisterColumns` (`internal/component/command/column_order.go`) takes one `ColumnOrder` per record shape. `bestColumnOrder` (`internal/component/command/pipe_table.go`) picks the declared order that names the most of the keys in hand, so the key SET identifies the shape. `TestRenderPicksOrderMatchingTheRecordShape` holds it. The next command that answers with more than one shape meets the same question |
