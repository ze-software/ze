# One field carries two meanings

A single field answers two different questions, and the two agree for as long as
one role holds. They diverge the moment the roles cross, and the reader that
asked the second question silently gets the first question's answer. The tell is
a field whose writer names one concept and whose reader names another.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-30 | - | `parseRDFromArgs` and `EncodeFlowSpecComponents` (`internal/component/bgp/plugins/nlri/flowspec/plugin_encode_text.go`) | Found while making `AddComponent` refuse a component it cannot hold, which turned a later panic into an immediate one. `parseRDFromArgs` scans every position for the `rd` keyword and returns the constant 2 as its consumed count, meaning "the pair occupies two tokens". Its one caller reads that count as "drop this many tokens from the FRONT", `args = args[consumed:]`. The two agree only while `rd` is first. `nlri ipv4/flow-vpn add destination 10.0.0.0/24 rd 100:1` therefore dropped `destination 10.0.0.0/24`, announced a VPN rule with no match criteria, and then reached the component walk holding `rd 100:1`, where `parseComponentText` returned a nil component with consumed 2. The nil was appended, and `componentLen` called `Len()` on it: a nil-interface panic reachable from an operator's own command | fixed 2026-08-30 with the flowspec component-uniqueness change, because the same walk now calls `c.Type()`. `parseRDFromArgs` returns the ARGUMENTS with the matched pair cut out rather than a count, so the caller has no offset to get wrong, and the `rd` case of `parseComponentText` returns `errRdOnlyForVpnFamily` rather than a nil component, since a family with no Route Distinguisher should refuse the keyword rather than ignore it. `TestEncodeFlowSpecVPNRDAnywhereInTheArguments` asserts the leading and trailing forms encode to the same NLRI; restoring `args[2:]` makes it red |
| 2026-08-25 | fixit-lint-blind-to-every-other-build-tag | `sigCh` in `run` (`cmd/ze/hub/main.go`), read as "a signal the operator sent" by a consumer added five years after its writers | One channel answers two questions: "did an OS signal arrive" and "did something in this process ask for shutdown". `signal.Notify` fills it with SIGINT, SIGTERM and SIGHUP; `monitorStdinEOF` (`main_system.go`) injects one SIGTERM when the upstream pipe closes; and `apiServer.SetShutdownFunc` injects a SIGTERM every time an operator runs `request shutdown`. For as long as the only reader was `waitLoop`, which asks the first question and the second with one answer -- stop -- the two agreed and the overload cost nothing. `89c35d8ed` added a SECOND reader that asks a narrower question. `forceExitOnSignal` treats the next value as "the operator is impatient" and calls `os.Exit(1)`. It cannot be: `forceExitOnSignal` reads the next VALUE and never asks which signal it is, so a repeated `request shutdown`, a supervisor that sends SIGTERM twice, and a SIGHUP arriving after shutdown has started each take the forced-exit path and return 1 where a clean stop returned 0. The SIGHUP case is the sharpest, because a config-reload request now kills the daemon. `monitorStdinEOF` is the one producer that cannot reach it, and only because it sends once and returns. The commit body names Ctrl+C alone, so the record is narrower than the code | fixed at `67d1e9cb4`, by the second channel this row proposed. `run` now builds its own `forceCh`. It calls `signal.Notify(forceCh, syscall.SIGINT, syscall.SIGTERM)` (`cmd/ze/hub/main.go`). The watchdog therefore reads a channel that `signal.Notify` alone fills. No in-process injector reaches it. `apiServer.SetShutdownFunc` writes `sigCh`, `monitorStdinEOF` writes `sigCh`, and SIGHUP is registered on `sigCh` only. A late config-reload request no longer kills the daemon. A supervisor's second SIGTERM still exits 1, which is the feature. This row was written 2.5 minutes before that commit and said NOT fixed. A later session corrected it on 2026-08-25 after reading the producer. The trap to carry forward: a channel that has had ONE reader for its whole life has never been asked which of its producers wrote a value, so the day a second reader arrives is the day the overload starts costing, and the diff that adds that reader is the diff where the producers must be enumerated |
| 2026-08-24 | fixit-stored-route-relay-hardening | the Adj-RIB-In stored route, the exclusive-role claim, and the learned RFC 9234 role | Three instances in one spec. Each cost a different thing. (1) `RawRoute.NLRIHex` answered two questions at once. It named the prefix bytes and it named whether a path identifier sits in front of them. The two ingest paths stored different framings. `buildRelayUpdate` (`internal/component/bgp/reactor/reactor_api_relay.go`) then refused every ADD-PATH source rather than guess. A route server lost the peer-up replay of one client's routes to EVERY destination. (2) A claim is daemon-wide and event delivery is per-peer. `replayOwned` answered "somebody owns replay" for a peer whose owner receives nothing. Nobody served that peer. (3) `filterRemoteRoles[peer] == ""` meant two things. It meant "this peer's OPEN declared no role" and it meant "no OPEN was ever recorded". `getFilterConfig` (`internal/component/bgp/plugins/role/role.go`) sent an operator to read an export policy. The thing to read was validate-open | The fix is a SECOND field, never a normalisation of the first. On a cross-process contract the second field's ZERO VALUE MUST be the refusal. `rpc.StoredRoute` gained `PathID` and a three-valued `NLRIFraming`. Its zero is `NLRIFramingUnrecorded`. A forked plugin built against the old struct omits the member. An omitted JSON number decodes to 0. RFC 7911 makes 0 a legal identifier. A bare `PathID uint32` would read "this producer does not know" as "path 0". `Server.UnheldRoles` retracts the claim on each event rather than qualifies the flag. The engine speaks only to withdraw, so silence still means the claim stands. `recordNoRemoteRole` WRITES an empty string where it deleted the key before. The trap to carry forward is this. Normalisation of the ambiguous representation looks like the smaller change and cannot work. The missing fact was never in the bytes |
| 2026-08-18 | fixit-child-sa-rekey-policy | ike | `sa.IsInitiator` is the IKE SA role, and `narrowChildSelectors` (`internal/component/ike/engine/ts_narrow.go`) read it as the role of the EXCHANGE in hand to orient the configured policy into TSi/TSr. RFC 7296 Section 1.3.2 lets either end start a CREATE_CHILD_SA, so a peer-initiated Child SA rekey on a tunnel ze initiated compared ze's local prefix against the peer's, found no intersection, and answered TS_UNACCEPTABLE to every rekey the peer started once traffic selectors were configured | fixed. Every caller of that function is answering somebody else's proposal, so the orientation is the constant "this node is the responder" |
| 2026-08-18 | fixit-child-sa-rekey-policy | ike | `sa.NextMsgID` is this node's own request counter, and `finishResponderEstablish` (`internal/component/ike/engine/responder.go`) set it from the Message ID of the peer's IKE_AUTH request. RFC 7296 Section 2.2 gives each end two independent counters, so the responder's first self-initiated request went out as id 2 while a conforming peer expected 0, and `classifyInbound` (`internal/component/ike/engine/msgid.go`) matches exactly. An original responder could raise no exchange at all: no DPD probe, no Delete, no Child SA rekey, no IKE SA rekey. MEASURED in QEMU as a Child SA rekey that reached nobody and hard-expired five seconds later | fixed. The responder's own counter stays 0 and `resumeRequestsAfter` is deleted |
| 2026-08-19 | verify-scope-4-suite-budget-and-ci | `ZE_VERIFY_MODE` | One environment variable carries a BOOLEAN and a MODE NAME. `execStage` (the retired `scripts/status/verify_run.go` (current producer: `internal/le/verify/engine/run.go`)) exports `ZE_VERIFY_MODE=1` to every stage, and `verifyModeEnabled`, behind `VerifyModeEnabled` (`internal/test/runner/parallel.go`), reads it as a flag that turns a suite's silent environment skips into hard failures. The retired `ze-precommit-verify-list` target (the retired `Makefile` (current producer: `./le verify list mode full`, in the `internal/le/` native action tables)) reads the SAME name as a mode name, passing `$(or $(ZE_VERIFY_MODE),ze-precommit-verify)` to `--list`. So any run of that target reached from inside a running verify is handed the mode `1`, which `stagesForMode` does not know, and `runVerify` turns the empty stage list into exit 2. The comment above that target makes it sharper: the variable was renamed from `MODE` to `ZE_VERIFY_MODE` precisely because "make imports the environment" and a stray `export MODE=...` "would silently make this print the wrong list" -- and the verify runner is the thing that exports the colliding name. Found while writing the sharded CI workflow, which must list the stages and then run them with the flag set. **NOT latent: it bit a second caller within the hour.** `verifyStageList` (the retired `scripts/dev/github_workflows_test.go` (current producer: `internal/le/`)) runs the list target with the inherited environment, and `ZE_PACKAGES` is `go list ./...`, so `./scripts/dev` runs inside the `ze-unit-test-cached` stage -- with `ZE_VERIFY_MODE=1` already exported to it. The new shard-coverage test therefore fails inside EVERY verify run. Measured: `ZE_VERIFY_MODE=1 make ze-precommit-verify-list` exits 2 with `unknown mode "1"`, the same command with `ZE_VERIFY_MODE=./le verify current mode full` appended as a make argument exits 0 and prints 30 stages | not fixed, and worked around in the caller: the workflow names the mode on the command line (`make ze-precommit-verify-list ZE_VERIFY_MODE=ze-precommit-verify`) so the make-level assignment beats the inherited environment, and sets `ZE_VERIFY_MODE: "1"` only on the step that RUNS the stages. The repair is to split the two meanings, because the workaround protects one caller and leaves the trap for the next: the boolean wants its own name, or the list target wants an argument rather than a variable make imports from the environment |
| 2026-08-19 | none (extended-community renderer work) | bgp config | The 4-octet Global Administrator sub-field means an IPv4 unicast address under Type 0x01 (RFC 4360 Section 3.2) and an AS number under Type 0x02 (RFC 5668 Section 2), and `parseRouteTargetOrOrigin` (`internal/component/bgp/config/routeattr_community.go`) writes an AS number into the Type 0x01 slot: for `target:65536:100` it takes the `ASN > 65535: Use Type 1 (IPv4) format if number fits in 16 bits` branch and emits `0102 00010000 0064`, and `parseRouteTargetOrOrigin4` does the same for every `target4:`/`origin4:` spelling, with the comment stating the choice. A conforming peer decodes that Route Target as IPv4 address 0.1.0.0, so the two ends disagree on what the community identifies. The sibling branch is right: the `L` suffix reaches `ecTypeTransitive4ByteAS`, and `parseSingleExtCommunity` (`internal/core/bgp/attribute/builder_parse.go`) and `parseRouteTargetExtCommunity` (`internal/component/bgp/route/route_community.go`) both pick Type 0x02 for an AS above 65535. The same function truncates in the other branch: with `num > 0xFFFF` it selects Type 0x02 and writes `num` into two octets, so `target:65536:70000` encodes as 4464 with no error. Found while completing `ExtendedCommunity.AppendDecoded`, which now renders the Type 0x01 form as a dotted quad and makes the mis-encoding readable rather than hex | fixed 2026-08-19. `fourByteASExtCommunity` (`internal/component/bgp/config/routeattr_community.go`) is now the one writer of the four-octet AS form, it refuses a number above 65535 instead of truncating, and `parseRouteTargetOrOrigin`, `parseRouteTargetOrOrigin4` and `parseFlowSpecRedirect` all route through it. `parseRouteTargetExtCommunity` (`internal/component/bgp/route/route_community.go`) carried the truncation half and now refuses the same pair |
| 2026-08-19 | none (FlowSpec encode defects) | bgp flowspec route actions | `FlowSpecActions.MarkDSCP`, `.RateLimit` and `.RateLimitPackets` (`internal/component/bgp/types/types.go`) each carried the VALUE and, by being zero, the claim that no action was asked for. `flowSpecActionExtComm` (`internal/component/bgp/plugins/nlri/flowspec/encode.go`) guarded on `> 0`, and both meanings are real values: RFC 8955 Section 7.1 gives a traffic-rate of 0 the meaning "discard all traffic for this flow", and DSCP 0 is CS0. So `then mark 0` sent no marking, and `then rate-limit 0` sent a FlowSpec route with NO traffic filtering action, which Section 7 leaves at the default accept -- the operator wrote a discard rule and the peer was told to forward. `ParseFlowSpecArgs` accepted both inputs as valid, so nothing anywhere reported the loss | fixed by splitting the two meanings: each numeric action gained a companion `*Set` flag the parser records, and the encoder emits when the flag is set OR the value is non-zero, so a caller that fills only the value is unaffected. `TestEncodeRouteEmitsZeroValuedActions` drives all three from `EncodeRoute`. The clean type is a pointer, and it was NOT taken: it would have forced an edit to `TestFlowSpecTrafficMarkingReservedBytesZero`, which carries an `RFC requirement:` tag and cannot be changed without the owner |
| 2026-08-19 | cli-column-order | cli table rendering | `uptime` names the peer session's age in a `show bgp summary` peer row and the daemon's age in the record that holds those rows. One command answers with both shapes. A column order keyed by command path alone ranks one `uptime` and applies that rank to the other. That puts the daemon's uptime seventh in a record where it belongs third. `handleBgpSummary` (`internal/component/bgp/plugins/cmd/peer/summary.go`) builds both shapes into one answer, so no command-path lookup can tell them apart | fixed in the design rather than found in the field. `RegisterColumns` (`internal/component/command/column_order.go`) takes one `ColumnOrder` per record shape. `bestColumnOrder` (`internal/component/command/pipe_table.go`) picks the declared order that names the most of the keys in hand, so the key SET identifies the shape. `TestRenderPicksOrderMatchingTheRecordShape` holds it. The next command that answers with more than one shape meets the same question |
| 2026-08-19 | cli-order-pipe | cli column selection | The SAME `uptime` collision, one layer up. Here it decides which RECORD the `display` operator was about, not which order to apply. `selectRecord` (`internal/component/command/pipe_columns.go`) marks a record as the chosen one when ANY of its keys is in the keep-set. It then drops every key it did not name, nested containers included. `tableStyle.orderKeys` (`internal/component/command/pipe_table.go`) applies the same test, so the table and the JSON never disagree. `show bgp summary` carries `uptime` in the record that HOLDS the peer rows and in every peer row. So a `display` of address, remote-as, state and uptime matches at the TOP level. It answers with the aggregate `uptime` alone, because `peers` is a key the operator did not name. The whole peer array is dropped. A flat field-name grammar over a nested payload cannot tell the two depths apart, and no depth was in the syntax the owner specified | not a defect and NOT fixed. The resolution is load-bearing. The `peers` and `summary` aliases (`registerAliases`, `internal/component/bgp/plugins/cmd/peer/peer.go`) are each a `display` over that same top-level record. Both stop working under a rule that prefers the deepest match. Recorded as a Known Limitation of spec-cli-order-pipe, with the route around it: reach the rows through the alias first, then display over what it returned. Whoever adds a depth-aware selector owes the aliases a migration, not a flag |
