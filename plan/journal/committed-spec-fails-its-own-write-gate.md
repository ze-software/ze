| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-08 | - | `hookValidateSpec` (`internal/le/hookruntime/lifecycle.go`) | A spec sits in `plan/` in a state its own write-time validator refuses, so the next session to edit it is blocked for a defect it did not make. Measured on 2026-09-08 over the 44 in-progress specs nobody held: 8 are refused at their HEAD content. Six for `Design document(s) declared by this spec's own code, never named in it` (`spec-ipsec-dataplane-inspection`, `spec-managed-server-hardening`, `spec-ospf-accept-lifetime-receive-window`, `spec-ospf-auto-cost-reference-bandwidth`, `spec-static-route-tag-reaches-no-consumer`, `spec-improve-8-fuzz-decode-context`), one for placeholder text in Data Flow Entry Point (`spec-policyroute-interface-list-matches-no-packet`), one for a missing `./le verify worktree` checklist item (`spec-fixit-unexport-package-private-symbols`). Only the placeholder check reads the Status field, and it is gated on `skeleton` alone, so every refusal was already firing while the spec read `in-progress`. Confirmed by re-running the hook with the status field restored | not fixed. The refusals belong to each spec's own authoring, not to the status field the work in hand changed. The hook runs on the `Write` and `Edit` tools only, so a spec authored through a shell heredoc never meets it and its author never learns |
| 2026-09-09 | ddos-timing-leaves-reach-no-worker | `hookValidateSpec` (`internal/le/hookruntime/lifecycle.go`), the code-block arm | `spec-ddos-timing-leaves-reach-no-worker` fails its own validator at HEAD: the round 2 review section quotes three lines of fixture Go in a ` ```go ` fence, and the arm refuses any spec carrying a fenced block tagged go, rust, java, c, cpp, javascript or typescript. Every later `Edit` of that spec is answered with the failure, whatever the edit changed. The block was written by an earlier session in this same spec, so this is the SAME class as the row above, met from the other side: not a spec the status field made invalid, but a spec whose own review evidence the gate cannot tell from implementation code | not fixed here, and deliberately: the two available repairs are both wrong. Dropping the `go` tag leaves the block and evades the regex, and rewriting the three lines as prose edits a quoted review. The gate's intent is that a spec MUST NOT carry implementation code; quoted evidence a reviewer read is a different thing, and the check has no way to tell them apart. The repair belongs at the check: exempt a fenced block inside a `## Review Gate` section, or match only a block that declares Go the spec is PROPOSING rather than quoting |
