# discarded-error-becomes-destructive

A caller discards an error slot because, under the behaviour of the day, the
worst case was a smaller or emptier output. The operation later gains the power
to DELETE, and the same discard turns a partial read into data loss that reports
success. The discard is not where the defect was introduced. The new power is.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-26 | le-is-a-ze-binary | `scripts/dev/worktree_update.sh`, which stashes, rebases onto main, and pops | `branch="$(git -C "$wt" branch --show-current)"` prints nothing and exits 0 for a detached HEAD. Thus, `set -e` detects no error, and the script keeps an empty branch name. The script then rebases the detached checkout and reports `done (HEAD: <sha>)`. The rebased commits are reachable only through HEAD and the reflog. The next checkout in that worktree drops them. The empty answer is harmless where the echo reads it and destructive two lines later. A real linked worktree on a detached HEAD reproduced the defect: the script exited 0 and moved HEAD | fixed in the port only. The script keeps the defect until the swap deletes it in step 14. `Updater.One` (`letools/worktree/worktree.go`) refuses an empty branch with `ErrNoBranch` before it stashes anything. `TestTheShellRebasesADetachedWorktreeAndThePortRefusesIt` (`scripts/dev/worktree_update_parity_test.go`) drives both implementations over real worktrees. It verifies that the shell moves HEAD and the port does not. Thus, the test becomes red if somebody repairs the script |
| 2026-08-11 | rfc-ledger-per-rfc-shards | tooling | `run_write` discarded the `parse_errs` slot of `_collect_for_check`. That was safe while the write only rendered one file. Once the write also PRUNED, a summary that failed to parse rendered no rows. Its stem left the rendered set. The prune then deleted that RFC's tracked evidence file and the run exited 0. An absent `rfc/short/` deleted all 177 the same way | `run_write` refuses before any write or delete. It refuses on a parse error, and on a render with no rows. Two tests drive the entry point, and each fails when its guard is removed |
| 2026-08-11 | - | tooling | `run_check_fresh` discarded the same slot, one function from the row above. It deletes nothing, so the loss is a DIAGNOSIS. The unparsed stem renders no rows. The gate then called the index stale, and called that RFC's file an orphan. Both readings are false. The second one invites a reader to hand-delete the evidence file. One tree gave two answers: `ze-rfc-check` named the parse error, `ze-doc-verify` named an orphan | Report the errors and exit 2 first. The comparison never runs. The lesson the row above missed: a discarded error slot is a defect at EVERY caller. Fix all of them in one pass. The blast radius differs, so the harm looks absent at the caller that only reports |
| 2026-08-19 | none (FlowSpec encode defects) | bgp config, `mark <dscp>` | `parseFlowSpecMark` (`internal/component/bgp/config/routeattr_community.go`) was `dscp, _ := strconv.ParseUint(dscpStr, 10, 8)` and had no error return at all, so no caller could report one. strconv answers MaxUint on a range error and zero on a syntax error, which put the operator's mistake on the wire as a DIFFERENT valid-looking action: `mark 300` encoded 0xff, setting both RFC 8955 Section 7.5 reserved bits, and `mark abc` encoded 0x00, remarking every matching packet to best-effort. The sibling encoder one package away, `parseTrafficMarkingFunction` (`internal/component/bgp/route/route_community.go`), returned an error and bounded to 0-63 with the RFC sentence quoted above it | fixed: the parse error is returned, the 0-63 bound moved into the shared `attribute.FlowSpecTrafficMarking`, and the signature change reached its one caller. `TestParseExtendedCommunityMarkDSCPBound` drives 0, 46, 63 and refuses 64, 200, 300, `abc`. The tell was structural and cheap to look for: a parser returning a bare `[]byte` where every sibling returns `([]byte, error)` cannot report anything, so the discard is forced by the signature rather than chosen at the call |
| 2026-08-26 | - | plugin-imports codegen, the product composition root | `fileImports`, `fileCallsRegisterRPCs`, `pkgImportsPath`, and `fileImportsPath` (`scripts/codegen/plugin_imports.go`) each answer `false` when `os.Open` fails. None asks `bufio.Scanner.Err()` whether the scan ended early. Thus, the tool reports no imports for a listed file that it cannot read. That answer was less harmful when the tool only reported. It became destructive when the same discovery started driving the write that `make generate` runs. A schema package, an RPC command package, or an event namespace then vanishes from `internal/component/plugin/all/all.go`. The run prints `Generated ... with N schemas` and exits 0. The product then ships without that registration. The loader does not register the YANG module, so the declared config leaf is refused as unknown. A dangling symbolic link reproduced the defect. The walk lists the link, and `os.Open` answers `fs.ErrNotExist` for every privilege level. `generatedTagFile` reads files in the same way. Thus, an unreadable `all_<tag>.go` is neither removed nor reported | fixed in the port only (`letools/pluginimports`). Every reader answers `(bool, error)`, and every read failure stops the run. A generator cannot safely skip a file that it cannot read. `TestAFileTheWalkCannotReadStopsTheRun` drives all four walks. `TestALineTooLongToScanStopsTheRun` drives the scanner bound. The script keeps the defect until the swap step of the le migration. `TestScriptPluginImportsStillDropsAFileItCannotRead` (`scripts/codegen/parity_test.go`) verifies that the script still has the defect. Thus, the test becomes red if somebody fixes the script. The correct action is then to delete the script and that test together |
| 2026-08-26 | - | go-extract, the file-split tool agents run | `main` (`scripts/dev/go_extract.go`) reads the destination with `if existing, err := os.ReadFile(dstPath); err == nil` and treats EVERY failure of that read as the file not being there, so it writes a fresh `package X` header plus the extracted text over it. A destination that exists and cannot be read is therefore a destination whose content is destroyed and reported as a successful split. Reproduced with a write-only file, where reading is refused and writing is not: a colleague's 400-line file became four functions under a package clause, and the tool printed `extracted 1 symbols`. The discard was safe while the tool only APPENDED; it is destructive because the same branch chooses to CREATE | fixed in the PORT only (`letools/goextract`). `destBody` treats `fs.ErrNotExist` as a new file and refuses on any other read failure, before either write. `TestPlanRefusesADestinationItCannotRead` drives `PlanMove` over an unreadable destination. The SCRIPT keeps the defect until the swap step of the le migration, and `TestScriptGoExtractStillOverwritesADestinationItCannotRead` (`scripts/dev/go_extract_parity_test.go`) asserts that it still does |
