| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-26 | le-is-a-ze-binary | the retired `scripts/dev/changed-groups.sh` (current producer: `internal/le/`) and the retired `scripts/dev/changed-pkgs.sh` (current producer: `internal/le/`) | Git, `go list`, and scope-file failures collapse to an empty selection with exit 0. `_ze-unit-test-race-changed-impl` then skips the changed-group pass. | `Selector.ChangedFiles`, `Selector.Packages`, and `Scope.fromFile` (`changed` in the former top-level tool tree) return errors. `selectHere` exits 2. the retired `scripts/dev/changed_parity_test.go` (current producer: `internal/le/`) pins each shell failure. |
| 2026-08-26 | le-is-a-ze-binary | the retired `scripts/dev/skill_sync.sh` (current producer: `internal/le/`) | Missing skill or instruction sources still produce exit 0. The script reports generated files that it did not write. | `Mirror.sources` (`aisync/aisync.go` in the former top-level tool tree) rejects either missing source population. `Sync`, `Check`, and `Preview` return 1. the retired `scripts/dev/skill_sync_parity_test.go` (current producer: `internal/le/`) pins the shell failure. |
| 2026-07-27 | - | guard | empty `addrs` slice passed per-address loop, bound `0.0.0.0:3443` unauthenticated | carried presence separately from value |
| 2026-08-22 | fixit-child-rekey-answer-vs-installed-selectors | ike engine child SA selectors | an EMPTY selector set reads as "no constraint" in two guards, and one producer can supply it. `adoptAuthResponseNegotiation` (`internal/component/ike/engine/transport_mode.go`) calls `recordInitiatorSelectors` only when both TSi and TSr are present, and returns ok when either is absent, so an authenticated IKE_AUTH response carrying SAr2 and no TS payload leaves `sa.NegotiatedPairs` nil. `createFirstChildSA` (`internal/component/ike/engine/child.go`) copies that nil into `ChildSA.Selectors`. Two readers then treat the absence as an answer: `coversFloor` (`internal/component/ike/engine/ts_narrow.go`) is guarded by `len(floor) > 0` at both call sites, so the RFC 7296 Section 2.9.2 floor is SKIPPED for the rest of that SA's life and every later rekey may narrow freely on both roles; and `selectorPort` (`child.go`) returns `AnyPortMatch` for an empty set, so the kernel policy carries no port constraint. The `.ci` and the interop scenario cannot see it because both peers answer TS. Found while reviewing the floor guard this spec adds, not while writing it | not fixed. It blocks no acceptance criterion of the spec that found it: every conforming peer sends TSi and TSr in the IKE_AUTH response (RFC 7296 Section 1.3.3), and each AC is proven for an SA that negotiated a set. The fix belongs to the IKE_AUTH exchange rather than to the rekey, and it is a wire-visible refusal with its own interop cost, so it needs its own proof against strongSwan. The general practice: a guard skipped on an empty input is a guard whose producer must be read, because absence and permission are the same value |
| 2026-07-27 | - | guard | `maxMsgID == 0` read as no cut, replay ran unbounded | used explicit tracked bool |
| 2026-07-27 | - | guard | `PeerAS == 0` for dynamic peers made `internal` always false | used `*uint64` pointer for presence |
| 2026-08-18 | - | static routes config apply | `parseStaticConfig` (`internal/plugins/static/config.go`) returns a nil slice both for a reload that carries no static section and for one that declares no route. `OnConfigApply` (`internal/plugins/static/register.go`) read that nil as "nothing to do", so emptying the static configuration withdrew nothing and every route stayed programmed. The trigger is a section PRESENT and empty: deleting the whole root withdrew anyway, by a different path, because `collectProcessesForRemovedConfigPaths` (`internal/component/plugin/server/startup_autoload.go`) stops a plugin whose root vanished and `routeManager.shutdown` (`inject.go`) removes every route. The commit message says "deleting the static configuration" and overstates it | fixed: `pendingSection` carries delivery beside the routes, so a delivered empty section reaches `applyRoutes` and its set-difference withdraws. `TestPendingSectionSeparatesEmptyFromAbsent`, `TestRouteManagerWithdrawsAllOnEmptyConfig`, and `test/static/static-reload-empty-section-withdraws.ci`, which keeps the container present so the plugin-stop path cannot pass the test for it |
| 2026-08-18 | - | static routes config apply, second defect in the FIX | the fix above introduced a worse one, found by the review of its own commit. `take` cleared delivery at APPLY time only, and an apply is never reached when another plugin fails the same transaction: `TxCoordinator.runVerify` (`internal/component/config/transaction/orchestrator.go`) aborts, and no plugin-facing callback runs, since `configTxBridge` (`internal/component/plugin/server/config_tx_bridge.go`) subscribes to `EventRollback` alone. Static declares `WantsConfig: {"static", "interface"}` and skips non-static sections, so it participates in interface-only reloads without setting anything. A deletion verified in an aborted transaction therefore stayed delivered, and the next interface-only reload took it and withdrew every route the running config still declared. The guard it replaced made that case a no-op | fixed: `pendingSection.reset`, called at the head of the verify callback, scopes delivery to one transaction. `TestPendingSectionResetDropsAnAbortedTransaction`. The lesson is about the fix, not the defect: moving state from a guard into a longer-lived holder buys a new lifetime question, and this one was answered only at the apply end |
| 2026-08-18 | - | plugin SDK verify-to-apply seam | the same nil-conflation is live in at least three siblings: `internal/plugins/copp/register.go`, `internal/plugins/policyroute/register.go` and `internal/plugins/ntp/register.go` each guard `OnConfigApply` on a nil parsed config. Every `OnConfigApply` callback in the tree discards its `[]sdk.ConfigDiffSection` argument and reads state smuggled from the verify callback, so the shape is produced by the shared convention rather than by any one plugin. `rpc.ConfigDiffSection` (`pkg/plugin/rpc/types.go`) already carries `Root`, `Added`, `Removed` and `Changed`, so delivery is on the wire at apply time and needs no side channel | not fixed. Found by the review of the static fix, and NOT verified beyond reading the three named guards: the count of affected plugins is the reviewer's, not re-derived here |
| 2026-08-10 | fixit-vpp-ipsec-inoperable | ipsec dataplane | `SAParams` carries no direction, so `IPSEC_API_SAD_FLAG_IS_INBOUND` can never be set and every VPP SA is programmed outbound | fixed: `SAParams.Dir` (`ike/dataplane/dataplane.go`) set by `installChildSA` (`ike/engine/child.go`) and read by `vppSAFlags`. An unset direction is refused by `vppUnsupportedSA` rather than read as outbound |
| 2026-08-10 | fixit-egress-filter-non-decision-channel | bgp gr egress filter | a nil `egressState` was read as "no route is stale" and accepted. A stale route then went to a peer that never sent the LLGR Capability (RFC 9494 Section 4.3). The state stays nil for the whole process when the GR engine runs out of process | nil resolves to `hasLLGR=false` in `LLGREgressFilter` (`gr_egress.go`), so the peer takes Section 4.3's withdraw or Section 4.6's depreference. `checkGRInProcess` (`doctor.go`) reports the out-of-process arrangement, which is wrong for a peer that did negotiate LLGR |
| 2026-08-11 | fixit-otc-src-role-meta-fallback | bgp role plugin, OTC filters | `OTCEgressFilter` read a missing `meta["src-role"]` as "no restriction", so the RFC 9234 Section 5 leak guard went unevaluated for every caller without ingress metadata, the Adj-RIB-In relay path included. The same empty-string shape then turned up at four more readers that no AC named: the egress destination role, the ingress source role, a `srcCfg == nil` early return that suppressed a MUST-level stamp, and a capability-learned role that outlived the session that taught it | fixed at the READ rather than at the caller: `resolveSrcRole` and `resolvePeerRole` (`internal/component/bgp/plugins/role/otc.go`) resolve from the config complement, the early return is deleted, and `recordDrop` (`role/metrics.go`) counts every suppression so none is invisible. The lesson the four extra readers teach: fixing one caller of a zero-value read leaves the shape live at every other reader of the same value |
| 2026-08-12 | - | host thermal inventory | `readOneHwmon` (`internal/component/host/thermal_linux.go`) drops a channel when `readFileInt` returns 0, so a real 0.0 degC reading is skipped as unreadable. `readFileInt` gives 0 for a missing file and for a zero value alike, so presence is not carried | not fixed |
| 2026-08-12 | fixit-config-validators-bypassed-at-startup | config validation redaction | `isSensitiveLeaf` (`internal/component/config/validate_sections.go`, moved verbatim from `cli/cmd_validate.go`) split a validation error path on "." to take the leaf name. `walkTree` (`internal/component/config/yang/validator.go`) joins with `Byte('/')` and `SensitiveKeys` (`schema.go`) keys on the bare leaf, so the lookup matched nothing and every failing sensitive leaf answered "not sensitive" and printed its value. Its unit test built the path by hand in the "." shape, so it agreed with the defect | fixed: the separator is "/", and `TestIsSensitiveLeafReadsTheProducersPathSeparator` drives the paths the walk emits. The pre-fix separator goes red on it |
| 2026-08-12 | - | ze-perf benchmark result | `perf.RunBenchmark` (`internal/perf`) returned a result with `RoutesReceived` 0 and a NIL error, so a benchmark that forwarded nothing reads as a completed run with a throughput measured over no routes. `TestBenchmarkIterDelayZero` is the only thing that notices, and it is red on the shared tree today. Reproduced twice, alone and in a batch. Not caused by this session: the only change here to `internal/perf` is a comment in `allocgate.go`, and nothing this sweep touched is on that path | fixed 2026-08-12, and the zero was two answers wearing one value. A DUT that forwards nothing IS a measurement and reports every route lost (`TestBenchmarkTimeout` pins it), so the error is not "received zero": it is that `runIteration` (`internal/perf/benchmark.go`) wrote `rawMsgs, _ = receiveRaw(...)` and threw away why the receive loop stopped. The producer of the zero was `forwardUpdates` and `receiveRaw` losing wire framing: both set a 100ms read deadline, called a reader that reads the header and then the body, and retried on timeout. A deadline expiring between the two discards the header, and the retry frames on the body, where a body byte pair reads as a Length field. The 100ms poll and the 100ms warmup share a period, so the arrival lands on the boundary once in ten iterations. Fixed at the reader: `readHeaderPolled` (`internal/perf/session.go`) arms `msgBodyTimeout` once any byte is consumed, so a poll that expires never leaves the stream inside a message, and `runIteration` now reports a receive failure instead of counting it as zero routes |
| 2026-08-12 | - | bgp rpki origin validation | `(*ROACache).Validate` (`internal/component/bgp/plugins/rpki/validate.go`) answers `ValidationNotFound` for three different facts: no VRP covers the prefix, the ROA cache is empty, and the prefix did not parse. `not-found accept` is the YANG default (`yang/ze-rpki.yang`), so all three accept the route. `rp.active` (`rpki.go`, `startSessions`) is set when cache servers are CONFIGURED, not when a sync completes. A cache that is reachable but whose VRPs ze mis-decoded therefore stamps NotFound on every prefix and accepts it. The transport warning `rtr: session error, will retry` (`rtr_session.go`) does not fire for that case, because the session is healthy and the VRP count is non-zero | fixed 2026-08-12, as two states rather than one. The unparseable prefix leaves NotFound: `Validate` parses before it consults the cache and answers `ValidationInvalid` with a warning, which the default invalid action rejects. The parse guard had been unreachable, because `findCovering` parsed the same string first and returned an empty covering set, so the caller left by the "nothing covers this" branch; `findCovering` now takes the parsed `*net.IPNet`. `active` keeps its meaning (a cache server is CONFIGURED), which the adj-rib-in validation gate needs before any data can arrive, and the missing state is per-session: `RTRSession.synced` is set at End of Data and reported as `synced`, `sessions-synced` and a per-server `synced` by `show bgp rpki status`, `cache`, `summary`, `roa <prefix>` and `validate`. The accept of a genuine NotFound is untouched, since it is a deliberate decision with its own leaf |
| 2026-08-14 | fixit-test-harness-fail-open-guards | test harness | Four harness guards read an empty answer as a valid one. `acceptConnMapBatch` (`internal/test/peer/peer_connmap.go`) answered `Success: true` for a run cancelled before the checker finished. `Ze.rib_count` (`test/interop/interop.py` (retired; now `internal/le/interoplab/bgp/`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->) answered 0 for a query that failed, and 0 is a legitimate RIB size. `docker_exec_quiet` (`test/interop/interop.py` (retired; now `internal/le/interoplab/bgp/`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->) turns any non-zero exit into `""`, and 171 call sites read that answer without testing it. `run_rs_observer` (`test/scripts/ze_api.py` (retired, no successor) <!-- doc-links: ignore (deleted 2026-08-28 by eae282592 with no replacement) -->) advertised `ZE-OBSERVER-FAIL` behind a 30.0s constant while all 17 `.ci` callers ran a 10s, 15s or 20s foreground timeout, so the runner always killed the process first and the diagnosis had never once reached anyone | fixed: the accept consults `(*Checker).Completed`, `Ze.rib_count` raises and names the container and the command, and the retired `scripts/dev/docker_exec_checked.py` (current producer: `internal/le/doc/wiring/delegate.go`) derives the fail-open set to a fixpoint and ratchets the count in `test/health/docker-exec-baseline.json` (retired, no successor) <!-- doc-links: ignore (deleted 2026-08-28 by eae282592 with no replacement) --> down only. Two traps for the next session. A MEMBERSHIP test is not an emptiness test: `"DIS" in out` is False on `""`, so it is the fail-open shape rather than a guard against it, and applying that rule reclassified 54 sites the hand survey had called checked. A CONSTANT bound compared against a deadline enforced in another file is reachable at none of its call sites or at all of them, and which one is an accident: `(*Runner).testBudgetEnv` (`internal/test/runner/runner_exec_util.go`) publishes the wall clock the child races and every inner wait takes a share below 1.0 of it. The derivation is published on ONE path: `runTest` (`internal/test/runner/runner_exec.go`) builds `clientEnv` without `testBudgetEnv`, so only `runOrchestrated` carries it, and an observer `.ci` with no `cmd=foreground` line would fall back to the 30.0s constant that was the defect |
| 2026-08-14 | fixit-peers-from-tree-stale-shape | bgp reactor config parse | deleting a duplicate parser also deleted the one check only IT made. `mapMap` (`internal/component/bgp/reactor/config.go`) answers "key absent" and "key present, wrong type" with the same `false`. `PeersFromTree` turned that single `false` into `nil, nil`, which reads as "no peers configured". So a `peer` node arriving as a string, a list or a number read as a config with no peers. `ApplyConfigDiff` (`reactor_api.go`) applies that by reconciling the reactor to zero peers: every session torn down, reload reports OK. The deleted `parsePeersFromTree` had distinguished the two with `invalid peer section type`, and the removed-behaviour audit is what found the loss | fixed in `PeersFromTree`, so the production route gains the guard too: read the raw value, return `nil, nil` only when the key is ABSENT, error with the type found otherwise. `TestPeersFromTreeRejectsWrongTypedPeerSection` covers all three shapes and pins the absent case as a success. Discrimination measured: a probe proved `mapMap` answers false for each shape, so the pre-fix code returned no peers and no error |
| 2026-08-14 | rfc4271-med-across-as | bgp filter text delta to wire | `textDeltaToModOps` (`internal/component/bgp/reactor/filter_delta.go:250-257`) spells "the filter dropped this attribute line" as `Op(code, AttrModSet, nil)`. `genericAttrSetHandler` (`filter_delta_handlers.go:47`) calls `p.Op(idx)`, which appends NO fragment for an empty buffer (`AttrPlan.Op`, `filterapi/editset.go:388-391`), then `p.Emit`, which writes a 3-byte header with length 0 (`AttrPlan.emit`, `:511`). A removal therefore reaches the wire as a ZERO-LENGTH attribute for every generic code, MULTI_EXIT_DISC among them, where RFC 4271 Section 5.1.4 requires four octets and a conformant receiver applies RFC 7606. `AttrPlan.ValueLen`'s own doc comment names the trap ("an attribute with an empty value is not the same thing as no attribute") and this handler never asks it. Read from the producers on the way to the MED removal mechanism; not reproduced on the wire | not fixed |
| 2026-08-18 | fixit-iface-selector-ignored-by-apply | iface vpp backend, iface resolver | `detailsToInfo` (`internal/plugins/iface/vpp/query.go`) never set `InterfaceInfo.OsName`; the netlink backend has always set it (`linkToInfo`, `show_linux.go`). `bindingFromInfo` (`internal/component/iface/resolve.go`) copies that field into `Binding.OsName`, and `resolveOS` (`dispatch.go`) read an empty `OsName` as "resolution unavailable" and fell back to the LOGICAL name. So on the VPP backend every `os-name` and `mac/match` selector steered nothing: every by-name dispatch op reached a device named after the config entry. Found from the producer while making `resolveOS` fail closed; no unit test could see it because the apply-path fake backend left the same field empty | fixed: `detailsToInfo` sets `OsName` to the interface name it already reads, and the fake in `internal/component/iface/config_test.go` sets it in both `ListInterfaces` and `GetInterface`. `TestResolverMappingPublishedBeforeApply` goes red without the fake half |
| 2026-08-18 | fixit-firewall-irr-term-fails-validation | firewall-irr refresh | `test/plugin/firewall-irr-empty-answer-keeps-last-good.ci` and `firewall-irr-iface-no-blackhole.ci` both fail on `a refresh that learned nothing reported success`: with the mock IRR server in `-empty-after-first` mode, the SECOND `update firewall irr as-set AS-TEST` still returns status done. Both are UNTRACKED files another session is still writing in this shared checkout, and both were red before this spec for a different reason (the forwarder discarded the command's argument, so they failed on the FIRST update); the argument fix moved them onto this assertion. They also account for the two names `TestCIAcceptOnlyLint` reports. The other seven `firewall-irr-*.ci` pass | not fixed. It does not block this spec, whose goal is that a table term validates, commits and reaches the kernel, and that is proven by `firewall-irr-table-term-commit.ci`. It was the subject of `spec-fixit-irr-empty-answer-clears-set`, which owned what the store returns for an empty answer and CLOSED on 2026-08-22, so both tests are green. The stem is bare because a `plan/` path would cite a file the tree no longer holds |
| 2026-08-19 | fixit-no-family-block-sends-no-end-of-rib | BGP capability negotiation, the address-family intersection | `Negotiate` (`internal/core/bgp/capability/negotiated.go`) built `localFamilies` and `remoteFamilies` from Multiprotocol capabilities alone, then intersected them. A side that advertised NO Multiprotocol capability therefore contributed the EMPTY set, which reads "declared nothing" as "supports nothing". The protocol says the opposite: RFC 4271 Section 4.3 carries IPv4 unicast in the UPDATE message with no capability at all. Every consumer of `nc.Families()` then acted on the empty answer. The family loop in `sendInitialRoutes` (`internal/component/bgp/reactor/peer_initial_sync.go`) never ran, so a peer with no `family` block got no End-of-RIB marker. `startEORTimer` (`internal/component/bgp/reactor/session_health.go`) returned early and armed no timer. `validateUpdateFamilies` (`internal/component/bgp/reactor/session_validation.go`) rejected an MP_REACH for a family the session really did carry. The defect was invisible because the BASE protocol kept working. Routes flowed both ways and only the barrier was missing, which reads as a slow peer | fixed: a side that advertised no Multiprotocol capability is now read as advertising `ipv4/unicast`. Each side is judged on its own, before the intersection. The default is one family and never a wildcard, so an ipv6-only side against a silent side still intersects to nothing. The OPEN bytes are unchanged, because the fix states what was NEGOTIATED rather than what Ze advertises. `TestNegotiateImplicitIPv4UnicastWhenNoMultiprotocol` pins all five rows of the truth table. `TestInitialSyncEORSentWhenNeitherSideDeclaredAFamily` carries `RFC requirement: RFC4724-4-1 positive`. `test/encode/no-family-peer-open-and-eor.ci` asserts both frames as hex, and FRR 10.3.1 decodes the marker in `test/interop/scenarios/no-family-peer-eor-frr`. The same ten lines closed a second hole nobody had specced. A peer that sends no capabilities at all is valid RFC 4271, and Ze used to negotiate no family with it, even when Ze's own config declared ipv4/unicast |
| 2026-08-21 | cli-show-bgp-is-the-command | cli command documentation, YANG rpc metadata | removing `show bgp summary` moved the payload's wire method from `ze-bgp:summary` to `ze-bgp:overview`, and the parameter SCHEMA is keyed by that wire method in two places the spec never listed. `AllRPCDocs` (`internal/component/config/yang/cli/tree.go`) reads `paramIndex[doc.WireMethod]`, and `buildParamMeta` (`cmd/ze/hub/command_meta.go`) compares `rpc.Name` against the rpc half of the wire method. A miss in either one is the zero value, so `ze yang doc show bgp` would have printed no output parameters and the hub metadata no input parameters, while the command itself answered the right payload. `rpc summary` lives in `internal/component/bgp/yang/ze-bgp-api.yang`, which the spec's Files to Modify never named, and no gate reads a doc entry that lost its schema | fixed in phase 3: `rpc summary` became `rpc overview`, so the rpc name follows the wire method. `TestDocCommandWithOutputParams` drives `show bgp` and asserts the output block still carries `uptime`. The general shape is that a command rename is also a rename of the rpc in the matching `-api` module, and both lookups that join the two fail silently when it is not |
| 2026-08-22 | - | as112 healthcheck command | `handleAS112Health` (`internal/plugins/as112/health.go`) answered a failing probe with `Status: StatusError` and an EMPTY `Error`, its `Data` carrying `healthy:false`. `responseFailure` (`internal/component/plugin/dispatch.go`) turns that into `errStatusErrorNoMessage`, so `ze cli` printed `unknown error` and dropped the payload, while the record answer kept the payload because the HEAD carried the status word. Two surfaces, two different answers to one response, and the handler's own doc comment said "Returns a non-error Response" where the code returned an error one. It surfaced when the head's status left the wire and the terminator became the one line an outcome is stated on: a failure with no text there derives to VerdictDone (`rpc.Verdict`) | fixed: the failing branch states `Error: as112 healthcheck: <target> did not answer 10.in-addr.arpa. SOA`, so the terminator carries a reason a consumer can act on and the CLI prints the target rather than `unknown error`. `test/plugin/as112-health.ci` now asserts the reason names the target on the failing path and the payload on the healthy one, so it tells a command that ran and reported unhealthy from one that never reached the handler |
| 2026-08-22 | fixit-child-sa-rekey-policy | ipsec interop lab | A new scenario read Ze's XFRM state through `lab.ze_xfrm_state`, copied from the sibling scenario beside it. Its `docker_exec_quiet` (`test/interop-ipsec/lab.py` (retired; now `internal/le/interoplab/ipsec/`) <!-- doc-links: ignore (retired 2026-08-28 by eae282592) -->) returns `""` for a failed read AND for a kernel holding no state. The scenario reads an empty SPI set as "this host has no XFRM" and skips every dataplane assertion. A broken `docker exec` therefore skips them in silence and the scenario reports PASS. The retired `ze-functional-docker-exec-check` caught it at 122 unchecked sites against a floor of 121. It has NO successor: `internal/le/completeness_record_test.go` records the retirement, and the reason is that the subject is gone, both the Python harness the scan read and the `test/health/docker-exec-baseline.json` (retired, no successor) <!-- doc-links: ignore (deleted 2026-08-28 by eae282592 with no replacement) --> floor. That gate was then a stage of the pre-commit verify, so it went red for every session in the shared checkout | fixed: `esp_spis` reads through `docker_exec`, which raises and names the container, the command and the stderr, so an empty set is a reading. The floor was NOT raised. Copying a reader from a sibling test carries its fail-open with it, and a skip branch is where that fail-open becomes invisible |
| 2026-08-24 | fixit-vpp-lcp-reachability | vpp lcp doctor plugin probe | `vppctlShowPlugins` (`internal/plugins/iface/vpp/doctor.go`) returns `(string(out), nil)` for ANY zero exit, empty and truncated stdout included, so a non-answer arrives at the caller in the same shape as an answer. `checkVPPLCPPlugin` read "linux_cp_plugin.so is not in this text" as proof the running VPP does not load the plugin and emitted an ERROR, which tells an operator to rebuild a VPP that is fine. The branch beside it already reasoned the opposite way for a non-zero exit, and said so in its own comment, so the code carried both readings at once. A substring absent from nothing is not evidence | fixed: `checkVPPLCPPlugin` requires the `Plugin path is:` line that `vppctl show plugins` prints ahead of its rows (`vppctlPluginsHeader`) before it trusts an absence, and falls to the same probe WARNING without it. `TestCheckVPPLCPPluginUntrustedOutputWarns` drives empty, whitespace-only and headerless output; removing the guard turns all three subtests red. The habit: when a command's exit status and its output are two separate facts, a reader that consults only the exit status has read half the answer |
| 2026-08-22 | fixit-irr-empty-answer-clears-set | firewall-irr refresh, resolving the 2026-08-18 row above | the store guard was correct and both `.ci` files stayed red. The message was `a refresh that learned nothing reported success`. The operator's only force-refresh command never reached the server. `LookupPrefixes` (`internal/component/resolve/irr/client.go`) answers from a one-hour `prefixCache`, and `PrefixStore.resolve` (`internal/component/resolve/irr/store/store.go`) called it. So `update firewall irr as-set X` replayed the good answer cached an hour earlier. The empty-answer branch was unreachable from the surface an operator drives. Every unit test of the guard was green throughout | fixed: `RefreshPrefixes` (`internal/component/resolve/irr/client.go`) queries the server and never reads that cache. `resolve` calls it rather than `LookupPrefixes`, and `queryPrefixes` caches only a non-empty result. Both `.ci` files pass on Linux, 12 of 12 in the firewall-irr suite, and go red with the Phase 3 guard reverted. The habit is the repair. A guard is proven at the surface the operator drives. A cache in front of it can make the guard unreachable while every unit test of it stays green |
| 2026-08-26 | - | iana-asn codegen, the compiled RIR delegation seed | `main` (the retired `scripts/codegen/iana_asn.go` (current producer: `internal/le/ianaasn/ianaasn.go`)) writes the parse result and accepts an empty parse. Five HTTP 200 responses with no ASN record leave `var seedRIRTable = []RIREntry{}` committed in `internal/component/resolve/irr/rir_table.go`. The run prints `wrote ... (0 collapsed ranges from 0 records)` and exits 0. This table is the resolver's whole fallback when zefs holds nothing, so every IRR lookup answers nothing. No irr package test detects the problem because those tests read the emptied table. Because `generate` streams into a truncated file, a partial write leaves the package with half a table | fixed in the PORT only (`ianaasn` in the former top-level tool tree). `Write` refuses an empty ASN parse and writes each complete table in one call. `TestARunThatParsedNothingWritesNothing` and `TestTheTableIsWrittenInOneCall` drive both behaviors. The SCRIPT keeps the defect until the swap step of the le migration. `TestScriptIanaAsnStillWritesAnEmptyTable` (the retired `scripts/codegen/parity_test.go` (current producer: `internal/le/repository/`)) asserts that it still does. The only test seam is a CONNECT proxy that terminates TLS because the script keeps five fixed https URLs in a package variable |
| 2026-08-26 | - | evidence daemon build tags | `feature_tags` (the retired `scripts/evidence/feature_tags.py` (current producer: `internal/le/deployment/`)) answers an EMPTY list for a manifest that declares no gate. `daemon_build_tags` then returns `ze_core ze_distro` with no error. A comment-only manifest reproduced the defect on 2026-08-26. Seven evidence scripts use these functions to cross-compile a daemon and drive it against a real peer. Thus, the proof runs with every protocol compiled out and stops at "unknown top-level keyword: l2tp" before it reaches a kernel feature. The error looks like a protocol defect and repeats the exact failure that the module's docstring says the derivation prevents | fixed in the PORT only (`featuretags/daemontags.go` in the former top-level tool tree). `DaemonTags` returns an error and names the file that it read. `DaemonBuildTags` refuses to build a tag line from that error. The SCRIPT keeps the defect until the swap step of the le migration. `TestThePortRefusesTheManifestTheScriptIgnores` (the retired `scripts/evidence/l2tp_parity_test.go` (current producer: `internal/le/deployment/`)) asserts that the script still fails open. Thus, this row becomes red when somebody fixes the script |
| 2026-08-26 | - | le toolchain environment, the derived feature tag set | `_feature_tags` (the retired `scripts/le/devtools/toolchain.py` (current producer: `internal/le/`)) answers an EMPTY tuple when `feature-gates.txt` cannot be read, and `Toolchain.test_tags` then renders `ze_core` alone, byte-identical to `Toolchain.core_tags`, whose own docstring calls a reduced set "a defect everywhere else". Every argv an area dispatches to the Go toolchain is built from `test_tags`, so a gate that reads the live command surface would judge a product with every feature module compiled out, and would say nothing about the substitution. `_go_toolchain` in the same module fails open the same way: an unreadable `go.mod` leaves GOTOOLCHAIN unset, which the module docstring itself records as making golangci-lint print "0 issues" and exit non-zero on a cold cache. Root discovery guards neither read, because `repo_root` (the retired `scripts/le/paths.py` (current producer: `internal/le/`)) returns `ZE_REPO_ROOT` without testing either marker, and `lepath.Root` (`lepath/lepath.go` in the former top-level tool tree) does the same | not fixed in the SCRIPT, and that is the ruling rather than an omission: the owner directed on 2026-08-26 that a defect found in code under migration is fixed in the PORT, so the script keeps it until the swap deletes it. `toolchain.py` has no Go counterpart yet, so its port owes three things: a read that refuses an unreadable manifest, a read that refuses a manifest declaring no `ze_` tag, and a parity case asserting the script still answers an empty set for both, which reddens the day anybody repairs the Python. `DaemonTags` (`featuretags/daemontags.go` in the former top-level tool tree) already shows the closed shape, with `TestAManifestWithNoGateIsAnError` and `TestAnAbsentManifestIsAnError` each pinning an error rather than an empty set. **Fixed in the PORT, 2026-08-26.** `gotoolchain.New` (`gotoolchain/gotoolchain.go` in the former top-level tool tree) reuses `DaemonTags` for the manifest and refuses an unreadable `go.mod` in `goToolchainPin`. The three obligations are discharged by `TestAnAbsentManifestIsAnError`, `TestAManifestWithNoGateIsAnError` and `TestAnUnreadableGoModIsAnError` (`gotoolchain/gotoolchain_test.go` in the former top-level tool tree), and by `TestTheScriptStillFailsOpenOnAnAbsentManifest` and `TestTheScriptStillFailsOpenOnAManifestWithNoGate` (the retired `scripts/le/fuzz_parity_test.go` (current producer: `internal/le/`)), which redden the day anybody repairs the Python |
| 2026-08-26 | - | rules corpus gates, both scripts | `main` (the retired `scripts/dev/rules_lint.py` (current producer: `internal/le/`)) and `roundtrip` (the retired `scripts/dev/rules_points.py` (current producer: `internal/le/rules/points.go`)) report success over a corpus they read NOTHING from. An `ai/rules/` directory with no rule prints `rules_lint: 0 rule file(s) conform` and exits 0. An absent `ai/rules/points/` prints `rules_lint: 0 rule point(s) state an RFC 2119 level` and exits 0 because `check_points` returns zero. A corpus with no rule prints `rules-points: all 0 rules round-trip byte-identical` and exits 0. `render_all` and `report_gate_map` already refuse this shape and say that the run read nothing and must not report success. On 2026-08-26, a fixture showed that three of five gates fail closed on an empty population and two fail open | fixed in the PORT only (`rules` in the former top-level tool tree). `Lint` (`lint.go`) and `RoundTrip` (`render.go`) name each empty population in an `Empty` field and return 1 with the sibling gates' words. An UNREADABLE points directory is now an error instead of zero. `TestLintRefusesTheEmptyCorpusTheScriptPasses` and `TestLintRefusesAnUnreadableCorpus` drive these cases. The SCRIPTS keep the defect until the swap step of the le migration. `TestScriptRulesLintStillPassesAnEmptyCorpus` and `TestScriptRulesRoundTripStillPassesAnEmptyCorpus` (the retired `scripts/dev/rules_parity_test.go` (current producer: `internal/le/`)) assert this behavior and become red when somebody fixes the Python |
| 2026-08-26 | - | rules digest generators, two more scripts over the same corpus | `main` (the retired `scripts/dev/rules_index.py` (current producer: `internal/le/`)) writes the parse result and accepts an empty parse. On 2026-08-26, a fixture `ai/rules/` directory held no rule. Update mode OVERWRITES `ai/rules/INDEX.md` with an empty header, prints `wrote ... (0 rules)`, and exits 0. The destroyed file maps topics to rules, so the loss stays silent until an agent searches for a rule. On the same day, `payload_report` counted an absent `ai/rules/CORE.md` as zero, lowered the total, printed MET with extra headroom, and exited 0. `render_all` and `report_gate_map` already refuse this shape with a message that the run read nothing and must not report success | fixed in the PORT only (`rules` in the former top-level tool tree). `indexRows` (`index.go`) and `LoadRules` (`digest.go`) refuse an empty rules directory with the sibling generators' words, so the index stays intact. `Payload` (`artifacts.go`) names every absent part in a `Missing` field and returns 1. It leaves the measured lines unchanged and qualifies the verdict instead of restating it. `TestTheIndexRefusesARulesDirectoryItReadNothingFrom` and `TestThePayloadRefusesToMeasureAFileThatIsNotThere` (`internal/le/rules/digest_test.go`, then in the former top-level tool tree) drive these cases. The SCRIPTS keep the defect until the swap step of the le migration. `TestScriptRulesIndexStillOverwritesAnIndexItReadNothingFor` and `TestScriptRulesPayloadStillCountsAnAbsentFileAsZero` (the retired `scripts/dev/rules_digest_parity_test.go` (current producer: `internal/le/`)) assert this behavior and become red when somebody fixes the Python |
| 2026-08-26 | - | le setup, the unprivileged-userns probe | `userns_state` (the retired `scripts/le/devtools/system.py` (current producer: `internal/le/`)) returns `Userns.NA` for an OSError after it confirms that the knob exists. NA means "this kernel has no such knob, so there is nothing to do", and `_visit_userns` renders "n/a: no apparmor userns knob". Some machines instead have a present knob set to 1. Chrome cannot start on those machines, and every agent-browser web functional test fails with "No usable sandbox!". Both setup modes still report green. A read failure is not an answer about the value | fixed in the PORT only, under the 2026-08-26 rule for code in migration. `(*Setup).UsernsState` (`devsetup/system.go` in the former top-level tool tree) returns UsernsNA with no error when the knob does not exist. It returns UsernsNA with the read error for an unreadable knob. `visitUserns` (`devsetup/setup.go` in the former top-level tool tree) reports the second state as MISSING. `TestAnUnreadableUsernsKnobIsNotAnAnswer` (`devsetup/devsetup_test.go` in the former top-level tool tree) pins both states |
| 2026-08-26 | - | VPP interface evidence, the vppctl query | `plugin_loaded` (the retired `scripts/evidence/effective-vpp-iface.py` (current producer: `internal/le/deployment/vppiface.go`)) discards the query's EXIT STATUS. It tests the error text for the plugin name. Thus, a failed `show plugins` query answers "not loaded" for every plugin. The wireguard and LCP scenarios depend on that answer. If the CLI socket is unavailable, the container skips two of the four features. It gives an evidence-backed reason and exits 0 after it proves only half of its claim. The same file's `vppctl_text` fails CLOSED after a non-zero status, but no caller uses it. Thus, the fail-closed helper exists while every call site uses the fail-open helper. The GRE scenario is a second form of the defect. `run_tunnel_evidence` searches for the needle `gre` in text that includes the error from the failed probe. The error `show gre tunnel: unknown input 'gre tunnel'` contains that needle. Thus, the fallback reports a tunnel that was never created. A recording docker reproduced both defects on 2026-08-26 | fixed in the PORT only (`deployment/vppiface.go` in the former top-level tool tree). `pluginLoaded` answers an error that names the query. `awaitFeature` accepts evidence only from a successful query. The SCRIPT keeps both defects until the swap step of the le migration. `TestThePortRefusesThePluginQueryTheScriptIgnores` and `TestThePortRefusesTheGREEvidenceTheScriptAccepts` (the retired `scripts/evidence/vpp_iface_parity_test.go` (current producer: `internal/le/deployment/`)) cover the two defects. The tests assert that the script still fails open in each case. Thus, this row becomes red when somebody repairs the Python |
| 2026-08-26 | - | perf nudge, `_git` (the retired `scripts/dev/perf-suggest.py` (current producer: `internal/le/perfbench/perfbench.go`)) | `_git` returns an empty string for both a failed command and a command with nothing to say, because it ignores OSError and return codes. `changed_hot` folds four answers into a set, so a missing git, non-repository directory, or unreadable checkout produces an empty list and silent exit 0. The script's docstring names that outcome as the failure that matters for an ADVISORY. This under-coverage nudge on a real regression cannot detect its own failure. On 2026-08-26, a run outside a repository produced no output and exit 0 despite a changed hot-path file | fixed in the PORT only (`ChangedHot`, `perfbench/perfbench.go` in the former top-level tool tree). A failed query returns an error that names the query and root. The nudge reports the error and STILL exits 0 because an advisory never blocks a build. The SCRIPT keeps the fail-open behavior until the swap step of the le migration. `TestPerfNudgeScriptStillGoesSilentOnAGitItCannotRun` (the retired `scripts/dev/perf_parity_test.go` (current producer: `internal/le/`)) asserts that it does. Thus, this row becomes red when somebody repairs the Python |
| 2026-08-26 | - | the STE prose ratchet, `candidates` and `git_lines` (the retired `scripts/dev/ste_check.py` (current producer: `internal/le/ste/ste.go`)) | The port of this gate to `le` found the defect. `git_lines` catches `CalledProcessError` and `FileNotFoundError`, returns `None`, and lets `candidates` produce an empty list. `run_gate` then prints `ste_check: OK -- no habit grew in 0 changed document(s)` and exits 0. Thus, a checkout with an unborn HEAD, locked index, or absent `git` PASSES the prose gate regardless of its prose. A fixture with three fresh hedges and no commit reproduced the defect because the script returned OK and exit 0. The port returned exit 1 and named the git failure, and `commit_helper.py` confirms that this fail-open behavior is on the commit path | fixed in the PORT only. `gitLines` (`ste/files.go` in the former top-level tool tree) returns `ErrNoGit` joined with git's error, and `Ratchet` propagates it. Thus, an unanswerable population returns exit 1 instead of a green gate. The SCRIPT keeps the fail-open behavior until the swap step of the le migration. `TestScriptSteStillPassesAGitItCannotAsk` (the retired `scripts/dev/ste_parity_test.go` (current producer: `internal/le/`)) asserts that it does. Thus, this row becomes red when somebody repairs the Python |
| 2026-08-26 | - | the digest anchor gate, `digest_files` and `main` (the retired `scripts/dev/digest_check.py` (current producer: `internal/le/digest/digest.go`)) | The port of this gate to `le` found the defect. When `ai/digests` is not a directory, `digest_files` returns an empty list and `main` prints `checked 0 anchors across 0 digests, all resolve`. The gate cannot distinguish a renamed directory, removed digests, and a tree whose every anchor resolves. The unread tree gets the greenest verdict, and a directory that holds only its README has the same result. A fixture with no `ai/digests` reproduced the defect. It returned exit 0 with the all-resolve line | fixed in the PORT only. `Check` (`digest/digest.go` in the former top-level tool tree) returns `ErrNoDigests` for a population that it did not find. The action returns 2 instead of 1, so callers distinguish this state from a rotted anchor. The SCRIPT keeps the fail-open behavior until the swap step of the le migration. `TestScriptDigestStillPassesATreeWithNoDigests` (the retired `scripts/dev/tier_parity_test.go` (current producer: `internal/le/`)) asserts that it does. Thus, this row becomes red when somebody repairs the Python |
| 2026-08-26 | - | the repository gate's changed set, `changed_files` (the retired `scripts/dev/validate.py` (current producer: `internal/le/doc/wiring/checks.go`)) | The port of this gate to `le` found the defect. `changed_files` runs two git commands, reads output only after exit 0, and ignores every other result. Thus, an absent git, non-checkout directory, locked index, and clean tree all return the same empty changed set. `check_cross_package_wiring` and `check_cli_handler_coverage` then return before they read a file. `./le repository check` prints all checks passed. A run outside a repository returned exit 0 despite an unwired exported symbol in the tree | fixed in the PORT only. `ChangedFiles` (`repository/repository.go` in the former top-level tool tree) returns an error that names the failed git argv, and `runCheck` (`repository/actions.go` in the former top-level tool tree) returns 2 instead of 1. A failed changed-set query differs from a tree that has a finding. The SCRIPT keeps the fail-open behavior until the swap step of the le migration. `TestScriptRepositoryStillReadsAFailedGitCommandAsAnEmptyChangedSet` (the retired `scripts/dev/tier_parity_test.go` (current producer: `internal/le/`)) asserts that it does. Thus, this row becomes red when somebody repairs the Python |
| 2026-08-26 | le-is-a-ze-binary | test-health page, `collect_inert` (the retired `scripts/dev/testing_health.py` (current producer: `internal/le/testhealth/testhealth.go`)) | the collector reads the sensitivity detector's JSON by string key with an empty fallback: `raw.get("assert-nothing") or []` and `raw.get("tag-orphan") or []`. A renamed or absent key answers an empty list. The page then publishes ZERO inert tests and ZERO stranded files, which is the goal state for both. But the page measured nothing, and `tag-orphans` is a fact that `--check` GATES. The `tests-scanned` guard beside it covers neither list key. The same file's `_gated_list` docstring names this exact defect. That guard was written to close the defect, but it is one call site away | fixed IN THE PORT ONLY. `collectInert` (`testhealth/collect.go` in the former top-level tool tree) calls `testsensitivity.Scan` as a FUNCTION. Thus, there is no key to rename and no fallback to take. `TestScriptTestHealthStillReadsAnEmptyInertListWhenTheDetectorRenamesItsKeys` (the retired `scripts/dev/testing_health_parity_test.go` (current producer: `internal/le/`)) configures a stub detector for the script. The test becomes red when somebody repairs the Python. Delete the test when you delete the script |
| 2026-08-26 | le-is-a-ze-binary | test-health quality ratchet, `read_quality_floors` (the retired `scripts/dev/testing_health.py` (current producer: `internal/le/testhealth/testhealth.go`)) | the reader validates that `test/health/quality-baseline.json` is a JSON OBJECT. It does not validate its VALUES. In contrast, its sibling `read_baseline` validates that every floor is a non-negative int. A floor of `false` reaches `quality_status` as 0. Every percentage then clears the floor, which disables the regression signal with no diagnostic. A `null` floor means "no floor recorded" and has the same result. Replacing a number with a boolean is the cheapest way to make a metric read green forever | fixed IN THE PORT ONLY. `readQualityFloors` (`testhealth/baseline.go` in the former top-level tool tree) refuses a floor that is not a number. `TestScriptTestHealthStillAcceptsAQualityFloorThatIsNotANumber` runs the same tree with a floor of 90 and with `false`. It gets `warn` for 90 and `ok` for `false`. Thus, the test measures the disabled signal directly |
| 2026-08-26 | le-is-a-ze-binary | BGP interop oracle, `checkOTCWithdrawal` (`internal/le/interoplab/bgp/check_rfc.go`) | Removing the bespoke checker's generic fallback left both operation slices empty. The loop ran zero times and returned nil without reading peer evidence. `TestNativeInteropScenarioCoverage` reproduced the false pass against a lab that refuses every query. | `checkOTCWithdrawal` now declares and executes the session, route, OTC, withdrawal, receiver-log, attribute-error, and final-session assertions directly. The unused `opBIRDNoAS` branch was removed. |
| 2026-08-29 | mpls-10-rsvp-te-reload-completeness | RSVP message decode, `DecodeMessage` (`internal/plugins/rsvpte/wire.go`), which records `HasTimeValues` and never requires it | TIME_VALUES is mandatory in a PATH and in a RESV (the RFC 2205 Section 3.1.3 message formats list it unbracketed), and it is the only object that tells a receiver the period the sender refreshes at. A PATH that omits it is accepted, and every consumer then supplies a value of its own: `receivedRefreshPeriod` (`internal/plugins/rsvpte/engine.go`) falls back to `DefaultRefreshPeriod`, so a sender that refreshes every 300 seconds is given 30 seconds of lifetime here and ze expires its state. The absent mandatory object reads as a valid default instead of as the malformed message Section 3.1 says it is | NOT FIXED. Refusing the message is a wire-visible change (a PATH ze accepts today would answer with a PathErr), so it needs the owner's answer on which error code and value the refusal carries |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | peer operational state, `sendNotificationWithin` (`internal/component/bgp/reactor/session_write.go`) and `lastErrorString` (`internal/component/bgp/plugins/cmd/peer/summary.go`) | `sendNotificationWithin` calls `onNotifSent` only inside its `if err == nil` branch, so a NOTIFICATION the socket refused records no code, no subcode and no time. `Peer.recordNotification` (`internal/component/bgp/reactor/peer_stats.go`) is the only writer of those counters, and `lastErrorString` returns the empty string whenever `LastNotifTime` is zero. A peer torn down by a BFD Down Cease whose write failed reports `last-error` as blank on `show bgp peer` and on the public looking glass, which is the answer a healthy peer gives. The failed send is logged at WARN by `logNotifyErr` (`internal/component/bgp/reactor/session.go`) and reaches no queryable state. RFC 9384 Section 4 asks for the reason in operational state for exactly this case: "When there is a total loss of connectivity between two BGP speakers, it may not have been possible for the Cease NOTIFICATION message to have been sent. Even so, BGP speakers SHOULD provide this reason as part of their operational state." Read at every producer 2026-08-31 | NOT FIXED, and not here. The obligation is a SHOULD, so it gates nothing and it did not block the rfc9384 walk. The fix is not the one-line move of `onNotifSent` out of the success branch: `last-error` would then report notifications that never left the box, so the surface must say whether the reason was delivered or only intended, and that is a change to a field the public looking glass renders. `recordNotification` also sets `notificationExchanged`, which suppresses the duplicate session-dropped error in `peer_run.go`, so a failed send would change a second behaviour at the same time |
| 2026-08-31 | - | `seq02` (`internal/test/fixture/plugin_fixture_02_bfd.go`), the BFD auth-persistence probe | Four situations return 0 and the one caller cannot tell them apart: `os.Stat` on the store failing, the `ze data ... list` listing command failing, the listing holding no `meta/bfd/auth/` key, and the `ze data ... cat` of that key failing. A fifth is quieter still, `seq, _ := strconv.Atoi(...)`, which answers 0 for content that will not parse. That fifth is a DIFFERENT defect wearing the same clothes, and the two should not be repaired together: the four returns are a design that chose one answer for several situations, while the `_` discard is the banned discarded error of `ai/rules/go-standards.md`. Nothing downstream can separate them because zero is a legal sequence number, which is also why no linter reads the value as suspicious. The caller (`:229`) polls `probe.seq02(...) > 0` until `bfdRunBudget02` expires, so all five read as "the plugin has not written a sequence yet", which is a reason to keep waiting. Two of them are reasons to STOP and report. That asymmetry is what makes this the expensive form of the class: the failure is not a wrong value but a wrong DIAGNOSIS, produced by a fixture whose whole job is to say which side is broken. Found while renaming the offline `ze data ls` subcommand to `list` (`79d199a31`), which made line 173 stale. Had the rename missed it, the fixture would have timed out with nothing anywhere naming the retired subcommand, and the reader would have spent that time in BFD auth code. The fixture's owner read the same shape independently and confirmed it at the producer | not fixed, and nothing is stale today: the rename was applied and the owner is carrying it in their own commit. The repair is to separate "not there yet" from "I could not look": return `(int, error)` so the poller keeps waiting on a real absence and aborts on a failed command, or at minimum log the failing command before returning 0. Not done here because the file is another session's mid-change BFD Simple Password work, and restructuring its error handling under them costs more than the defect. Whoever next owns that fixture should also read `restoredSeq02` directly below it, which carries one of each shape: a bare `return 0` for "no match in the log", and its own `strconv.Atoi(match[1])` discard for a capture that will not parse |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff (rfc2866 walk) | RADIUS accounting and access client, authradius | RFC 2866 Section 4.1 states "Either NAS-IP-Address or NAS-Identifier MUST be present in a RADIUS Accounting-Request", restated at Section 5.13 Note 1, and RFC 2865 Section 4.1 states the same for an Access-Request. `source-address` and `nas-identifier` are both optional leaves with no default (`internal/component/l2tp/plugins/authradius/yang/ze-l2tp-auth-radius-conf.yang`), and `buildAcctPacket` and `buildAuthAttrs` each emitted the attribute only when its leaf was set, so a configuration naming servers alone put packets on the wire that named no NAS at all. The absent leaf was read as a valid answer on both paths | fixed. `appendNASIdentity` (`internal/component/l2tp/plugins/authradius/nasidentity.go`) is now the one producer for both builders: NAS-IP-Address for a set source-address, the operator's NAS-Identifier when set, and the host name when neither is, which is how `radius.NewAAA` already names this device. Driven red first at the producer, then green, and proven by RFC2866-4.1-3 positive and negative |
