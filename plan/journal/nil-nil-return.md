| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-03-21 | - | error-handling | function returned `(nil, nil)` on error path | replaced with `(nil, err)` |
| 2026-03-21 | - | error-handling | `sync.Once` cached `(nil, err)` result, second call lost the error | rewrote to explicit state plus mutex |
| 2026-09-08 | plugin-query-mode | `handshakeAt` (`internal/component/plugin/leaf_test.go`) | The helper answers `(nil, nil)` for two different failures: the dialed connection is not a `*tls.Conn`, and the handshake presented no peer certificate. Its callers read the certificate, so neither failure can be told from a handshake that succeeded, and a test that stops presenting a certificate passes on the same line. Found because `show plugin declarations` put `internal/component/plugin` in the lint scope: `./le verify lint run` reports `nilnil: 2` in all three flavors, and the file is committed and unmodified in this tree | not fixed, and it blocks nothing in phase 1: no file this phase wrote carries a lint finding. The repair is a sentinel error for each arm, so a caller branches on which one it met |
