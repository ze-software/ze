| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-03 | command-help-and-description | `captureStdout` (`internal/component/config/schema/cli/main_test.go:16`) | `TestPrintedSchemaFormsStillRender/show` hangs to the 10-minute timeout once the schema it prints passes 64 KB. The helper writes into an `os.Pipe` and calls `io.ReadAll` only AFTER `fn()` returns, so the writer blocks the moment the output fills the pipe buffer and the reader that would drain it has not started. The hung subtest is `cmdShow([]string{"ze-bgp-conf"})`, and `ze-bgp-conf.yang` reached 78 KB while this spec was adding help text to it. Nothing about the schema is wrong: the test is a producer-consumer with the consumer scheduled second, and it was correct only while every module stayed under the buffer. Found by an agent whose own file, at 79 KB, is not printed alone by any test and so did not trigger it | not fixed. The repair is a concurrent reader in `captureStdout`, started before `fn()` runs. It is test infrastructure in a package this spec does not otherwise touch, and `ai/rules/pre-release.md` puts a red that is the TEST's own defect behind the product work rather than in front of it. The trap for the next author: the failure arrives as a timeout with no output, so it reads as a slow schema walk rather than as a blocked write |
| 2026-03-21 | - | tests | `net.Pipe()` write blocked because no goroutine was reading | started reader goroutine before write |
| 2026-03-21 | - | tests | same deadlock in a different test using sequential write-then-read | wrapped write in its own goroutine |
| 2026-08-15 | fixit-peer-process-event-filter | tests | `TestHoldDownRefusesInboundAndEndsOnStop` (`internal/component/bgp/reactor/peer_prefix_hold_test.go`) failed the whole-package `-race` run with "io: read/write on closed pipe" on `SetReadDeadline`, so the pipe was already closed when the test armed the deadline. NOT reproduced: 3 of 3 passes with `-race -count=3 -run` alone, so it needs the loaded parallel run. The file is unmodified in the working tree and this session changed nothing on that path | open. Next step: run the whole package under `-race` in a loop and capture the goroutine that closes the pipe, then order the close after the deadline rather than racing it |
