# fixture-encodes-an-impossible-state

A functional fixture gives two roles the same identity because one host can hold
both: the same address on both ends of a session, one port for two listeners, one
identifier for two speakers. The protocol forbids the state, so the fixture asserts
behaviour about a deployment that cannot exist.

Nothing is wrong until a guard reasons about that identity. The guard is correct,
the fixture reddens, and the session that meets the red has to prove the guard
right before it can call the fixture wrong. The cheap move at that moment is to
weaken the guard, which is the move that must not happen.

The rule this class produced is
`ai/rules/points/testing/functional-test-gate/a-fixture-must-encode-a-topology-that-can-exist.md`.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-15 | - | bgp reactor egress, test/plugin | `test/plugin/relay-withdraw-nexthop-self.ci` gave its dest peer `remote ip 127.0.0.2` and `local ip 127.0.0.2`, so both ends of one BGP session held one address. `precomputeNextHop` (`internal/component/bgp/reactor/peer_forward_facts.go`) resolves `next-hop self` to `PeerSettings.LocalAddress`, and `buildForwardFacts` in the same file sets the guard's comparand from `PeerSettings.Address`, so the address on the wire WAS the peer's own address. `egressNextHopIsPeerOwn` (`internal/component/bgp/reactor/forward_next_hop.go`), added by commit 480897faf for RFC 4271 Section 5.1.3, therefore withheld the advertisement and the fixture's observer timed out waiting for it. A real session cannot reach that state: two ends of a TCP connection between two hosts hold different addresses, and loopback is the only reason the fixture could be written | fixed in the fixture, not in the guard: Ze's local address on the dest session became 127.0.0.3, distinct from both peers, and the asserted next-hop hex moved with it. Every assertion the file makes survives, including the withdrawal's exact-hex negative. The guard was left alone deliberately -- an exemption for "an address that is also Ze's own local address" could only ever fire on a fixture, because the RFC forbids the peer's address on the wire whoever else holds it. The suite that would have caught this at commit time is the retired `ze-functional-plugin-test` (current: `./le functional plugin`), which the surface table did not name for a Go change; it does now |
| 2026-08-15 | fixit-peer-process-event-filter | test/plugin | `test/plugin/attach-process-delivery-graph.ci` asserted its peer's initial-sync End-of-RIB (`expect=bgp:...00170200000000`) from a peer block that configures no `session { family { } }`. Ze then advertises no MP capability, negotiates no family, and `sendInitialRoutes` (`internal/component/bgp/reactor/peer_initial_sync.go`) loops over `nc.Families()`, which is empty, so no marker can ever be written. `show bgp peer detail` reads `families: []` and `eor-sent: 0` for an established session. The fixture passed only while its observer shut the daemon down before the peer noticed the frame was missing | fixed in the fixture: peer1 configures `ipv4/unicast`, and the observer holds `api.wait_peer_eor_sent`, the barrier ze_api documents for any test whose peer asserts that frame. Both halves were needed -- the family makes the marker possible and the barrier makes the assertion honest |
| 2026-08-16 | - | hook phase-gate fixture | The human-model fixture inherited `CLAUDE_CODE_FORK_SUBAGENT` from the invoking harness, which changed its verdict. | The fixture clears the fork flag and adds payload-ID acknowledgement coverage. |
| 2026-08-16 | - | SubagentStart hook fixture | The hook fixture called SubagentStart with no event payload, a state Claude cannot produce. | The fixture passes JSON with the intended session ID. |
| 2026-08-18 | fixit-iface-selector-ignored-by-apply | iface apply-path fake backend | the fake `Backend` in `internal/component/iface/config_test.go` accepted `SetMTU`, `SetAdminUp`, `SetAdminDown`, `AddAddress` and `GetMACAddress` on a device it did not hold, while every one of those answers "not found" on netlink (`manage_linux.go`, each starts at `netlink.LinkByName`). `GetInterface` returned only the name and type, so the apply path read MTU 0 and an empty state for every device, and its MTU and admin-state undos were unobservable no-ops. `CreateVLAN` left the child with no MAC, where a kernel VLAN INHERITS the address of its parent. Six tests were passing on states no kernel produces | fixed: the fake refuses an absent device on all five, `GetInterface` makes the same projection as `ListInterfaces`, and `CreateVLAN` inherits the parent MAC, index and MTU. Six tests needed their SETUP corrected (the device the previous config had made was never put in the fake) and no assertion changed. The VLAN half was load-bearing: a VLAN carrying the parent MAC makes the parent match its own `mac/match` selector twice, which the live-kernel integration test caught and every host test had missed |
| 2026-08-19 | verify-scope-1-shared-checkout-freshness | commit-helper unit fixture | `TestFullVerifyCoverage` in the retired `scripts/dev/commit_helper_test.py` (current producer: `internal/le/`) wrote its `internal/a.go` fixture as `package a` with no `// Design:` header, a state `design_ref_problems` (the retired `scripts/dev/commit_helper.py` (current producer: `internal/le/commit/prepare.go`)) refuses for every committed `.go` path. `_create` runs that refusal after the full-verify coverage gate the tests exist to exercise, so two cases asserting `rc == 0` reddened on the design gate and never reported on the gate under test | fixed in the fixture, not in the guard: both writers of `internal/a.go` now emit a `// Design:` header, and no assertion changed. The guard is right -- a fixture is not an exemption from a rule every real commit obeys <!-- doc-links: ignore (fixture path named by a journal row, created only inside a temporary repository) --> |
| 2026-08-28 | - | bgp reactor receive path, RFC 9552 §8.2.2 | `TestRFC7606Section54PropagatesUnknownBGPLSType` (`internal/component/bgp/reactor/session_validation_nlritype_test.go`) builds its companion "known type" NLRI as `lsWireNLRI(1, 0x02, 0x00)`: a Node NLRI whose Total NLRI Length is 2. RFC 9552 §5.2 Figure 7 makes a Node NLRI body Protocol-ID (1 octet) plus Identifier (8 octets) plus the Local Node Descriptors TLV, so 2 octets cannot hold it and §8.2.2's third bullet ("The sum of all TLV lengths found in a Link-State NLRI corresponds to the Total NLRI Length field of all its descriptors") is unsatisfiable for it. No BGP-LS Producer can emit that NLRI. The fixture passed only while ze ran no syntactic validation of a Link-State NLRI; implementing RFC9552-8.2.2-9 (`RetainWellFormedNLRI`, `internal/component/bgp/message/rfc7606_bgpls_nlri.go`) discards it, and the test's own assertion, that the UNKNOWN type beside it is propagated, is unaffected either way | NOT FIXED: the fix is to give the companion NLRI a conforming body (Protocol-ID 2, a zero Identifier, TLV 256 holding sub-TLV 512), which changes a test carrying `RFC requirement: RFC7606-5.4-1`. `weakened.Proposed` (`internal/le/testweakened/proposed.go`) refuses that edit until `test/rfc-changed.md` names the test, and that row records an OWNER approval an author may not write for themselves. Raised to the owner |
| 2026-08-29 | lg-deferred-birdwatcher-route-counts | looking glass API, golden capture | `mockDispatch` (`internal/component/lg/server_test.go`) answers `show bgp` with `"remote-as":"65001"` and `"routes-received":"100"`, quoted strings where the producer `handleBgpSummary` (`internal/component/bgp/plugins/cmd/peer/summary.go`) writes numbers: `mergeRibRouteCounts` assigns int, and `realSummaryJSON` in `handler_api_test.go` records the real shape. `getNum` (`internal/component/lg/handler_api.go`) has no string case, so the captured response of the PUBLIC endpoint, `testdata/handler/api-protocols-bgp.txt`, holds `"neighbor_as": 0` for a peer whose AS is 65001 and four zero counts for a peer holding 100 routes. No engine can produce that response | NOT FIXED: the count half is now visible rather than hidden, because `routeCountsAvailable` reports the counts unavailable instead of claiming a real source over them. Correcting the fixture types is a separate pass: it re-baselines every lg golden that shows a peer row, and each re-baseline owes a divergence reason in `lgPortHandlers` (`internal/component/lg/port_check_test.go`) |
