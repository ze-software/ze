| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-14 | spec-web-templ-migration | web workbench table | the empty-state row of `templates/component/workbench_table.html` renders `td colspan=0`. HTML gives colspan a minimum of 1. The cell spans one column instead of the table. The empty message sits under the flags column. The count is available: the template ranges `.Columns` two lines above. Seen in the captured `testdata/golden/component/workbench_table--empty.html` line 21 | FIXED 2026-08-15 in the interface defect pass. `wbTableEmptyColspan` (`internal/component/web/view_fragment.go`) answers `len(v.Columns) + 2`, for the flag column and the action column, and `workbenchTable` writes it. `TestWorkbenchTableEmptyRowSpansEveryColumn` covers three column counts. A fixed number passes none of them. 18 fixtures moved, each from colspan 0 to the count its header drew |
| 2026-08-14 | spec-web-templ-migration | web add-entry buttons | four templates hard-code the same DOM id `btn-add-entry`: `component/finder.html`, `component/list_table.html`, `component/sidebar.html` and `component/workbench_table.html`. `full_content` renders `finder` beside `detail`. `detail` renders `list_table`. One page therefore carries the id twice. An id is unique per document, and `document.getElementById` answers the first match | FIXED 2026-08-15 in the interface defect pass. The content-area control keeps `btn-add-entry`: `listTable` and `workbenchTable` are alternatives and never render together. `finder` and `sidebarSection` call `addEntryButtonID` (`view_fragment.go`). It names the shell and the list URL, so two list columns get two distinct ids. `TestRenderedPageCarriesNoDuplicateDOMID` renders `fullContent` and `oobResponse` over a fixture with two add columns and refuses any repeat. Proven to discriminate: restoring the literal reds both subtests. COMPLETED 2026-08-15 in the review follow-up. Both content-area components read one name, `addEntryContentID` (`view_fragment.go`), so the invariant a reader checks is "one content area per document". The guard reached `listTable` through `detail` and never reached `workbenchTable`, which every workbench page renders. `TestRenderedPageCarriesNoDuplicateDOMID` now renders the workbench shell around a table page as a third case. Proven to discriminate: two workbench tables in one page name the id twice and red it |
| 2026-08-14 | spec-web-templ-migration | web error panel | `templates/component/oob_error.html` sets `hx-swap-oob=className` on the error panel aside. The vendored `assets/htmx.min.js` holds eight swap styles: innerHTML, outerHTML, beforebegin, afterbegin, beforeend, afterend, delete and none. It does not hold the string className anywhere. The value names no swap htmx implements | FIXED 2026-08-15 in the interface defect pass. `oobError` (`component_oob_error.templ`) now carries ONE swap, `beforeend` over `#error-list`. No htmx swap can set a class. `outerHTML` over the panel would discard the errors already listed. The collapsed state moved to `syncErrorPanel` (`assets/cli.js`), on `htmx:oobAfterSwap`. The drawer also gained the way to open at all. `#error-toggle` carries `data-action=toggle-error-panel` and `initActions` handles it. `ze-error-update` has a listener. `TestOutOfBandSwapValuesNameASwapHTMXImplements` scans every `.templ` against the swap set, and `TestHTMXSwapStylesMatchTheVendoredLibrary` grounds that set in the shipped library. The test was COMPLETED 2026-08-15 in the review follow-up. `TestErrorDrawerIsReachableFromTheBrowser` asserted one attribute and three substrings of `cli.js`. It would have passed with the toggle branch deleted, with `initErrorPanel` never called, and with `syncErrorPanel` reading a class `errorItem` does not write. `TestErrorDrawerWiringHoldsTogether` replaces it. It reads each function body out of `cli.js`. The load handler must call `initErrorPanel`. Every id, class and action is checked against the markup the components render. Each of those three breaks reds it by name. No browser runs: the `.wb` harness executes no script, and `agent-browser` has no hook in `make ze-precommit-verify` |
| 2026-08-14 | spec-web-templ-migration | web list table | `templates/component/list_table.html` opens its data row with `tr class="{{if .HasPendingChanges}}row--pending{{end}}"`, A row with no pending change renders an empty class attribute. It carries no base class at all. Its sibling `component/workbench_table.html` names a base class and appends the modifier. Seen in the captured `testdata/golden/component/list_table--editable.html` line 75 | FIXED 2026-08-15 in the interface defect pass. `listRowClass` (`view_fragment.go`) answers `finder-table-row`, and appends `row--pending`, which is the shape `wbTablePendingClass` already had. `TestGoldenFixturesCarryNoEmptyClassAttribute` walks every captured `.html` and refuses `class=""` anywhere. Proven to discriminate: it named the three pre-fix fixtures before they were re-baselined |
| 2026-08-14 | spec-web-templ-migration | web workbench tables, log and event rows | five call sites escaped a cell with `template.HTMLEscapeString`. Each then handed the result to a template that escapes again. A namespace, a log line or a tool's output holding `<`, `&` or `"` reached the operator as the literal text `&#34;`. The sites were `handleDashboardEventsPage` and `parseLogOutput` (`internal/component/web/page_dashboard.go`), `parseIssueRows` and its plain-text fallback (`page_logs.go`), and `runToolCommand` (`page_tools.go`). The phase-1 handler capture holds the proof: `testdata/handler/nav-show-events.txt` at HEAD carries `{&amp;#34;result&amp;#34;:&amp;#34;golden&amp;#34;}` in a table cell | FIXED 2026-08-14 in the templ port. Each hand escape is deleted, so the component escapes once. A value reaching a `template.HTML` field keeps its hand escape and is correct: `handler_tools.go` writes overlay output that way, and `templ.Raw` escapes nothing. Proven by the one moved fixture, and by the handler sweep that compared the other 108 responses byte for byte and found no other change |
