| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-14 | spec-web-templ-migration | web workbench table | the empty-state row of `templates/component/workbench_table.html` renders `td colspan=0`. HTML gives colspan a minimum of 1. The cell spans one column instead of the table. The empty message sits under the flags column. The count is available: the template ranges `.Columns` two lines above. Seen in the captured `testdata/golden/component/workbench_table--empty.html` line 21 | not fixed, recorded for the post-port fix pass |
| 2026-08-14 | spec-web-templ-migration | web add-entry buttons | four templates hard-code the same DOM id `btn-add-entry`: `component/finder.html`, `component/list_table.html`, `component/sidebar.html` and `component/workbench_table.html`. `full_content` renders `finder` beside `detail`. `detail` renders `list_table`. One page therefore carries the id twice. An id is unique per document, and `document.getElementById` answers the first match | not fixed, recorded for the post-port fix pass |
| 2026-08-14 | spec-web-templ-migration | web error panel | `templates/component/oob_error.html` sets `hx-swap-oob=className` on the error panel aside. The vendored `assets/htmx.min.js` holds eight swap styles: innerHTML, outerHTML, beforebegin, afterbegin, beforeend, afterend, delete and none. It does not hold the string className anywhere. The value names no swap htmx implements | not fixed, recorded for the post-port fix pass |
| 2026-08-14 | spec-web-templ-migration | web list table | `templates/component/list_table.html` opens its data row with `tr class="{{if .HasPendingChanges}}row--pending{{end}}"`, A row with no pending change renders an empty class attribute. It carries no base class at all. Its sibling `component/workbench_table.html` names a base class and appends the modifier. Seen in the captured `testdata/golden/component/list_table--editable.html` line 75 | not fixed, recorded for the post-port fix pass |
