| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-05 | spec-verification-answers-one-agent | `startHelper` (`internal/le/job/contention_test.go`) | The helper that starts a holder process took an `environ []string` that all four of its call sites left nil, and `unparam` reported it on every lint of the package. A parameter no caller ever fills reads as a supported knob: the next case writes one instead of asking why the holder needs a deterministic environment, and `helperCommand` already pins `MAKEFLAGS` and `ZE_RUN_SLOTS` for a reason the parameter does not state | fixed on the spot, in the file this spec was already editing: the parameter is DELETED and `startHelper` passes nil itself, with a comment saying a case that needs a variable set is the waiter rather than the holder. `runHelper` and `waitingHelper` keep theirs, which real cases fill |
| 2026-09-08 | spec-vrrp-deferred-accept-mode-dataplane | `vrrpKeepalivedConfig` (`internal/le/qemu/vrrp_keepalived_linux.go`) | The keepalived-lab config builder takes a `priority int` that all three call sites fill with `vrrpKAPriority`, and `unparam` reports it on the GOOS=linux integration lint. The parameter reads as a per-scenario knob the lab does not have: every scenario runs the peer daemon at one priority, and the value is a package constant either way. Present at HEAD with two call sites; this spec's tracked-uplink scenario added the third | NOT fixed. Deleting the parameter reaches `VRRPParityConfigs` (`internal/le/qemu/guest_parity_linux.go`), a third file outside the two this work package was scoped to, and the finding is a lint nit on a test-lab helper rather than a defect in the product |
