# Late write lands on the successor

A goroutine scoped to one session, one connection, or one generation outlives it,
because nothing joins it and its bounded waits are longer than the gap before the
successor starts. Its last write then lands on shared state the successor already
owns. The teardown that cleared the state is correct, and the entry guard that
stopped a second goroutine starting is correct. What is missing is any way for
the LAST write to know which generation it belongs to.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-09 | spec-ospf-auto-cost-reference-bandwidth | `nsmAdapter.InterfaceDown` and `(*engine).shutdown` (`internal/plugins/ospf/instance.go`) | The callback used an unjoined goroutine. Shutdown could return while `lsdbTopology` still read engine state. The inherited race run reported 14 races at HEAD and 12 with the re-price change | Fixed 2026-09-10. Physical and virtual interfaces enqueue one coalesced request for the joined maintenance worker. Cancellation prevents new registration and rejects late callbacks. `TestDeferredOriginationJoinsTheEngine` fails when the join is removed and when late callbacks are admitted. The fixed OSPF subtree passes under the race detector. Logs: session `5bc855cc-5761-4012-8d90-7a9823029ab3`, scratch `job-ospf-unjoined-red-3587ae2e.log`, `job-ospf-late-red-020c7074.log`, `job-ospf-fixed-tree-3c4f78c3.log` |
| 2026-08-31 | fixit-peer-pending-sync-settles-too-early | `sendInitialRoutes` (`internal/component/bgp/reactor/peer_initial_sync.go`) and the teardown clear in `runOnce` (`peer_run.go`) | Nothing joins the per-session `sendInitialRoutes` goroutine, and `runOnce`'s own comment says the teardown can return while it is still inside its bounded waits. `waitForAPISync` is bounded at 2s, so a session that tears down and re-establishes inside that window has its successor's `initialSyncEOROwed` cleared by the predecessor's goroutine at the end of the marker loop. `pendingSync` then reports the new session settled while its marker is still owed, and `AnnounceEOR` stops deferring to it. The identical shape already existed on `sendingInitialRoutes`, which the pre-split code cleared at the same point, so the flag split copied a hazard rather than adding one. Not reproduced: it needs a teardown and a re-establishment inside one 2s wait | recorded, not fixed. It does not block the spec that found it, and the repair is a session generation the two flags do not carry today: the goroutine would compare the generation it started under against the peer's current one before every store. The `CompareAndSwap(1, 2)` at the top of `sendInitialRoutes` guards concurrent ENTRY and cannot guard a late EXIT |
| 2026-09-09 | ddos-timing-leaves-reach-no-worker | engine-event dispatch to a subscriber a config apply retired (`(*engineEventSubscribers).dispatch`, `internal/component/plugin/server/engine_event.go`) | `dispatch` snapshots the handler list under `RLock`, releases the lock and then invokes, and its own doc states the contract: an unregistered handler "IS still called once more". So `unsubscribe()` inside a config apply does not stop a handler that was already dispatched, and that handler runs on the responder the apply replaced, reading THAT responder's old config to decide what to do. In ddos local the effect was a drop rule INSTALLED through a responder nothing could reach afterwards, which no cap worker and no clear could then remove. The hazard is the CONTRACT rather than the caller: every engine-event subscriber that acts on state a config apply replaces carries it, and each one has to be safe against a late call of its own | ddos local's own half is FIXED: a `retired` field on the responder, set under `mu` where ownership moves and checked at the top of `applyMitigation`. `TestLocalRetiredResponderInstallsNothing` and `TestLocalRetiredResponderDoesNotReclaimACarriedRule` are both red without it. The DURABLE fix, if one is wanted, belongs in `engine_event.go`, which was read-only for this work: `dispatch` would re-check registration inside the loop before each invocation, which turns the contract from "one more call" into "no call after unregister" and needs a pass over every subscriber that relies on the current wording. NOT reproduced at runtime: the window is the netlink reconcile inside `replaceResponder`'s critical section. Read-verified at `dispatch`, `replaceResponder` and `applyMitigation` |
