| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-28 | - | `(*Runner).runOrchestrated` and `(*Runner).runTest` (`internal/test/runner/runner_exec.go`) | A `.ci` run leaves its daemon and fixture files in the repository root. Found there on this day: `daemon.log`, `daemon.ready`, `database.zefs`, `testkey`, `testkey.pub`, `ssh.addr`, `ssh-pubkey.conf`, `mock.addr`, `router-name.txt`, `local-save.ndjson`, eleven `.conf` fixtures, eight `.body` / `.frame` pairs, and the directories `crash/`, `meta/`, `rollback/` and `ze-bfd-auth-persist-<n>/`. None is ignored, so each one shows as untracked beside real work and every session sees a working tree it did not dirty. The producer is the working directory rather than the fixtures: `runOrchestrated` creates a per-test directory only when the record carries a `tmpfs=` block or engine steps, and sets the child's `proc.Dir` only when that directory exists. The legacy `runTest` path never sets `.Dir` at all. `ze-test` itself never chdirs, so a child with no per-test directory inherits the shell's cwd, which is the repository root where `./le` runs. 1503 of 1789 `.ci` files carry no `tmpfs=` block. The fixtures are correct given a per-test cwd: they write bare relative names, and `internal/test/sessionpath` already roots per-test directories under the session scratch | fixed on the spot. `Record.WorkDir` is created for every record and removed when the test ends, and `childWorkingDirectory` gives it to every child. `TmpfsTempDir` keeps its narrower meaning, set only when the record declares files, because three consumers read it as that question: whether to arm the daemon readiness file, where a sendfile lives, and which directory a `file=` check is rooted at. The 69 `.ci` files that run `exec=go test ./internal/...` keep the repository root, and so do the three that run `./le`: `repositoryAnchoredBinary` names the two tools that resolve their arguments against the module rather than against the test. The legacy path sets `.Dir` on both its children and anchors a relative config path against the root first. `WriteToTemp` had one caller and is deleted with its test. Proven by reverting: `TestRecordWithoutTmpfsStillRunsOutsideTheCheckout` reads the child's own `pwd` and goes red with the old guard restored, naming the repository directory it ran in. The `parse` suite, 317 tests, leaves the root clean |
