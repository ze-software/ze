| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-03 | lg-pki-certificate | `cmd/ze/hub/meta/ca/{key,cert}` | A hub test leaves a CA private key and its certificate inside the package directory. Both are UNTRACKED and neither has ever been in git history (`git log --all -- 'cmd/ze/hub/meta/**'` is empty), so nothing is published; the exposure is that neither path is ignored, so a future `git add -A` from any session would commit a private key. The 2026-08-28 row above fixed this class for `.ci` runs by giving every record a `WorkDir`, and this is the Go-test half of the same class: `go test` runs each binary with its cwd set to the package directory, and file-backed storage roots its state beside the config path (`storagePointerPath`, `internal/component/config/storage/pointer.go:346`, joins `filepath.Dir(configPath)` with `meta`), so a test passing a bare relative config path writes `meta/` into `cmd/ze/hub/`. Surfaced by an automated review that read the key as a committed secret and proposed a history purge; there is no history to purge | the belt landed, the root did not. Checked on 2026-09-04: `.gitignore:134` now carries `**/hub/meta/`, `cmd/ze/hub/meta/` is gone from the working tree, and `git log --all -- 'cmd/ze/hub/meta/**'` is still empty, so no key was ever published and none can be committed from that path again. Another session added the ignore while this work ran. What is NOT fixed is the producer: a Go test still writes file-backed storage into its own package directory, because `go test` sets cwd to the package and `storagePointerPath` (`internal/component/config/storage/pointer.go:346`) roots `meta/` beside the config path, so a test passing a bare relative config path puts daemon state in the source tree. The ignore hides that everywhere it is aimed and nowhere else. The row above fixed the same class for `.ci` runs by giving every record a `WorkDir`; the Go half still owes one |
| 2026-08-29 | generated-command-usage | `internal/test/fixture/bounded.json` and `full.json` | Both are TRACKED, and both are runtime output: `plugin01AnswerBounded` (`internal/test/fixture/plugin_fixture_01_answer.go`) writes each one with `os.WriteFile` from what the daemon answered on that run, then compares the two. So the committed copies are one machine's `system command list` from whenever it last ran, and they are stale the moment any command's help text changes. Found because the tracked `bounded.json` still carries `withdraw`'s retired help, the four-form usage sentence naming tag, tag star, id and all, for a command that is now three. Nothing asserts against the committed bytes, so it reddens no test; it dirties the working tree of every session that runs the fixture, and it publishes a command surface the binary no longer has | not fixed, and it does not block this spec: the fixture overwrites both files before it reads them, so the stale content is never an input. The repair is one line of `.gitignore` plus `git rm` of the two, which is a different change from a spec about generated usage lines |
| 2026-08-28 | - | `(*Runner).runOrchestrated` and `(*Runner).runTest` (`internal/test/runner/runner_exec.go`) | A `.ci` run leaves its daemon and fixture files in the repository root. Found there on this day: `daemon.log`, `daemon.ready`, `database.zefs`, `testkey`, `testkey.pub`, `ssh.addr`, `ssh-pubkey.conf`, `mock.addr`, `router-name.txt`, `local-save.ndjson`, eleven `.conf` fixtures, eight `.body` / `.frame` pairs, and the directories `crash/`, `meta/`, `rollback/` and `ze-bfd-auth-persist-<n>/`. None is ignored, so each one shows as untracked beside real work and every session sees a working tree it did not dirty. The producer is the working directory rather than the fixtures: `runOrchestrated` creates a per-test directory only when the record carries a `tmpfs=` block or engine steps, and sets the child's `proc.Dir` only when that directory exists. The legacy `runTest` path never sets `.Dir` at all. `ze-test` itself never chdirs, so a child with no per-test directory inherits the shell's cwd, which is the repository root where `./le` runs. 1503 of 1789 `.ci` files carry no `tmpfs=` block. The fixtures are correct given a per-test cwd: they write bare relative names, and `internal/test/sessionpath` already roots per-test directories under the session scratch | fixed on the spot. `Record.WorkDir` is created for every record and removed when the test ends, and `childWorkingDirectory` gives it to every child. `TmpfsTempDir` keeps its narrower meaning, set only when the record declares files, because three consumers read it as that question: whether to arm the daemon readiness file, where a sendfile lives, and which directory a `file=` check is rooted at. The 69 `.ci` files that run `exec=go test ./internal/...` keep the repository root, and so do the three that run `./le`: `repositoryAnchoredBinary` names the two tools that resolve their arguments against the module rather than against the test. The legacy path sets `.Dir` on both its children and anchors a relative config path against the root first. `WriteToTemp` had one caller and is deleted with its test. Proven by reverting: `TestRecordWithoutTmpfsStillRunsOutsideTheCheckout` reads the child's own `pwd` and goes red with the old guard restored, naming the repository directory it ran in. The `parse` suite, 317 tests, leaves the root clean |
