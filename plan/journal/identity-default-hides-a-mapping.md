| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-19 | fixit-iface-selector-ignored-by-apply | iface config apply, logical-name resolution | An interface entry selects its hardware by `mac/match` or aliases it with `os-name`, and roughly twenty-eight READ consumers reached the right device through `iface.Resolve`. The one WRITER did not: `desiredState` keyed every address, MTU, MAC, sysctl, admin state, mirror and VLAN parent by the logical entry name, and `reconcileOnReadyWithJournal` called the backend on that key. A `mac/match` entry carrying an address was refused outright. Where a device happened to carry the entry's name, everything landed on the wrong physical port with no error. What hid it for the life of the feature is that `ze init` writes `os-name` equal to the kernel name on every entry, so the generated config makes the mapping the IDENTITY and the whole translation layer reads as a no-op. Every test fixture inherited that default, and the integration tests that would have seen it carry the `integration` build tag and do not run on the developer platform | fixed at the writer: `bindDevices` resolves every ethernet entry once per apply from one `ListInterfaces`, `deviceFor` answers every phase, and no path falls back to the logical name. The shape to look for is a mapping whose DEFAULT is the identity. Its readers can all be correct while a writer ignores it, and no fixture built from the default configuration can tell the two apart. The test that finds it is one whose logical name DIFFERS from the kernel name, which is why the four new `.ci` files each make a device under one name and select it under another |
| 2026-08-22 | fixit-iface-selector-ignored-by-apply | iface owned-device and owned-address registries, vrrp | The apply path translates now. The two plugin-facing REGISTRIES beside it do not. `desiredState` (`internal/component/iface/config_apply.go`) merges `ownedAddresses()` under the name a plugin registered. `reconcileOwnedDevices`, in the same file, hands `MacvlanSpec.Parent` to `CreateMacvlanDevice`, which reaches `netlink.LinkByName(spec.Parent)` (`internal/plugins/iface/netlink/macvlan_linux.go`). VRRP fills that parent from `spec.ParentDevice`. `groupsForFamily` (`internal/plugins/vrrp/groups.go`) sets it to the config entry name plus any VLAN suffix. So a VRRP group on a `mac/match`-bound entry builds its virtual-MAC device on whatever wears the LOGICAL name. Today's two producers hide it as `ze init` hid the original: as112 registers on `lo`, and VRRP's VIPs register on the macvlan it just made | NOT fixed. No acceptance criterion of the closing spec depends on it. Recorded because the shape recurs: fixing the WRITER does not fix the registries that feed it. Each registry is a separate entry point holding a name nobody translated. VRRP already resolves the same string for its ifindex and its readiness probe (`iface.Resolve`, `internal/plugins/vrrp/register.go`). The parent is therefore translated on two paths of three and raw on the third |
| 2026-08-23 | fixit-vrrp-parent-ignores-the-selector | vrrp group binding, iface/ra doctor | The VRRP half of the row above is FIXED: `engine.apply` (`internal/plugins/vrrp/engine.go`) binds each group's `ParentDevice` through `iface.ResolveDevice` before the diff, so the macvlan parent, the per-device sysctls and the transport all take the device the selector answered, and a group whose selector answers no device or several never enters the desired set. The walk that closed it found ONE surviving site of the same shape, and it is not a registry: `checkRAForwarding` (`internal/plugins/iface/ra/doctor.go`) iterates `ifaceBlock.ListKeys(kind)` and hands each CONFIGURED name to `readIPv6Forwarding` (`internal/plugins/iface/ra/doctor_linux.go`), which reads `/proc/sys/net/ipv6/conf/<name>/forwarding`. For a `mac/match` or `os-name` interface that path names another device or none, and an absent path returns `known=false`, so the check goes SILENT rather than loud-and-wrong -- the identity default hiding a mapping one more time, in the register where a missing warning looks like a clean bill of health | VRRP fixed; the RA doctor NOT fixed, and `iface.ResolveDevice` cannot fix it as written -- for a reason that is itself the find. A doctor check runs on TWO surfaces with different state. `ze doctor` is registered `Mode: "offline"` as a LOCAL command (`registry.MustRegisterLocalMeta("doctor", Run, ...)`, `internal/component/doctor/register.go`), so it executes in the `ze` CLI process, where `setResolverConfig` (`internal/component/iface/resolve.go`) has never run: it is called only from the daemon's `applyAndPublish`, so the selector maps are empty and every name resolves to itself. The `ze-show:doctor` RPC form (`HandleShowDoctor`, `internal/component/doctor/cmd/show.go`) runs INSIDE the daemon, where the maps are published. So one check would answer two ways depending on which surface reached it, which is worse than answering wrongly on both. The fix has to read the selector out of the config tree the check already holds and match it against a live listing, the way `bindDevices` does, and then it is right on both surfaces |
| 2026-08-23 | fixit-vrrp-parent-ignores-the-selector | traffic tc backend | A THIRD site, found by independent review AFTER the row above claimed the walk was complete -- which is the reusable lesson: an enumeration is a claim like any other, and "I looked everywhere" is the one claim its author cannot check. `resolveOSName` (`internal/plugins/traffic/netlink/backend_linux.go`) calls `iface.Resolve` and returns the LOGICAL name whenever it fails, so every tc operation on a `mac/match` or `os-name` interface configures whatever device wears the entry's name. Its own comment calls the fallback "best-effort ... identical to pre-resolver behavior", which is the identity default admitting itself in writing. Three call sites: `linkByName(resolveOSName(name))` at the Apply and ListQdiscs paths, and `restoreOriginalLocked`, whose fallback is DELIBERATE and documented (a device that is gone has no snapshot, so restore is a correct no-op) | not fixed, and the scope call is recorded rather than hidden. `iface.ResolveDevice` is the right answer for two of the three sites, but the helper returns no error and the third site wants the fallback, so the fix threads a new error through a tc snapshot/restore path in a commit about VRRP. The next step is to give `resolveOSName` an error return, take `ResolveDevice` at the two kernel-lookup sites, and leave `restoreOriginalLocked` its documented no-op |
