| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-26 | - | `./le docvalid command-contract`, in its default rendering and under the `json` pipe operator, which the retired `ze-command-contract-check-json` target answered | Found while porting this gate to `le`, when the port's byte comparison against the script failed against the SCRIPT's own second run. `validate` (the retired `scripts/docvalid/commands.go` (current producer: `internal/le/docvalid/contract.go`)) sorts its command table with `sort.Slice` on the wire method ALONE, which is not a stable sort, and the rows arrive from `walkEntry` ranging `entry.Dir`, a map. One wire method can own more than one local command, and their rows therefore shuffle. Measured: three runs over the same tree disagree at four positions under `ze-show:bgp-rib`, `ze-show:env-list`, `ze-show:bgp-filter`, and `ze-show:route-count`. A fourth run happened to match the third byte for byte. Sorting by wire method AND YANG path fixes both outputs; this row records the reason the second key is part of the contract | fixed in the Go producer: command entries sort by wire method, then YANG path |
| 2026-08-26 | - | the retired `ze-consistency-check` (current: `./le consistency`) output | Found while porting this gate to `le`. The same class outside a command answer, on a GATE's report. `checkCrossRefs` (the retired `scripts/lint/consistency.go` (current producer: `internal/le/consistency/consistency.go`)) collects its findings into `map[dir]map[file][]ref` and then ranges that map twice to produce them, so the 1154 cross-reference lines arrive in a different order on every run. Measured: two consecutive runs of the built script over this checkout produce the same 1250 lines and differ from each other as sequences. A report that shuffles cannot be diffed between two runs, so no reviewer can see what a change did to it and no ratchet can be built over it. Found while porting the tool, when the port's byte-comparison against the script failed against the SCRIPT's own second run | fixed in the port: both ranges walk `sortedKeys`, which is the whole change. `TestCheckIsDeterministic` (`consistency` in the former top-level tool tree) seeds eight directories and requires two runs to answer the same findings in the same order, and reddens against the map-ranging body. Because the script's order is not stable, the retired `scripts/lint/parity_test.go` (current producer: `internal/le/consistency/`) and `test/ui/le-consistency-answers.ci` compare the two reports as a SET of lines rather than as a sequence, which still pins every finding, every group count and the summary. The general shape is the one this file already carries, met on a build tool rather than on an answer: a map drained into an ordered output is a random order, and it stops being an internal detail the moment somebody reads the output twice |
| 2026-08-24 | plugin-declares-answer-shape | cli show bgp rs, cli show bgp healthcheck | Two more producers drain a map straight into an answer's row set, so the row order differs on every call. `peerStatus` (`internal/component/bgp/plugins/rs/server_handlers.go`) ranges `rs.peers`, a `map[string]*PeerState`, and reaches `show bgp rs peers`. The summary branch of `handleShow` (`internal/component/bgp/plugins/healthcheck/healthcheck.go`) ranges `m.probes` and reaches `show bgp healthcheck`. This is the fourth and fifth instance of one class in this line of work, after `reactorAPIAdapter.Peers`, `SoftClearPeer` and the three rpki caches. Found by the Phase 4 agent while reading each producer to derive its column order, not by a search for it | fixed 2026-08-24, before either command declares, for the reason this row already gave: a declared row shape publishes `first`, `last` and `display` as supported, and publishing a row selector over a random order is the failure `ai/rules/cli.md` names. `peerStatus` walks `slices.SortedFunc(maps.Keys(rs.peers), comparePeerAddress)`, which orders by parsed address so `192.0.2.2` precedes `192.0.2.10` where the text order puts it after, and every IPv4 peer precedes every IPv6 one. `comparePeerAddress` is total over arbitrary strings rather than over addresses alone: the map key arrives from the engine, so a key that does not parse sorts after every key that does and the text breaks the remaining tie. Without that, one malformed address would leave a pair tied and return the whole answer to map order. The healthcheck summary branch walks `slices.Sorted(maps.Keys(m.probes))`; the probe name is the map key, so it is unique and no tie is left. Neither producer runs on a wire path: both are reached only from a command handler. `TestPeerStatusAnswersSortedOrder` and `TestHealthcheckListAnswersSortedOrder` each seed eight entries in an order that is neither sorted nor the text order and require the full sequence on 32 consecutive calls, so a map-ranging implementation is right by luck with probability at most 8^-32 and fails rather than flakes. Both were run against the map-ranging producer first: the rs one failed on call 0, the healthcheck one answered `[radius syslog web dns ntp api tftp bgp]` |
| 2026-08-24 | plugin-declares-answer-shape | cli show bgp rpki | Three diagnostic producers in the rpki plugin drain a map into an answer's row set, so the row order and, past the limit, the row MEMBERSHIP differ on every call. `aSPACache.Entries` and `aSPACache.lookupCustomer` (`internal/component/bgp/plugins/rpki/aspa_cache.go`) range `c.records` and the per-customer provider set, reaching `show bgp rpki aspa`; `ROACache.Entries` (`roa_cache.go`) ranges `c.ipv4` then `c.ipv6` and RETURNS mid-range at `limit`, reaching `show bgp rpki roa`, so its `truncated` answer is a random 1000 of the VRPs. Found while reshaping the aspa lookup branch to answer rows: a strict row-equality assertion between the two branches flaked on provider order, so `TestAspaLookupAnswersRows` compares providers as a set | fixed 2026-08-24, ahead of Phase 4 declaring the shapes: each producer answers a sorted order and the limit takes a prefix of it. `ROACache.Entries` walks `sortedPrefixKeys` (address, then prefix length, then the key text), and the VRPs of one prefix by max length then ASN; a key `netip.ParsePrefix` rejects keeps its rows and sorts first, because dropping it would answer a VRP set the cache does not hold. The limit still counts across the concatenation, IPv4 then IPv6, which is what the map-ranging version did. `aSPACache.Entries` sorts customers ascending with each provider set ascending, and `lookupCustomer` sorts its providers while still answering a non-nil empty slice for a record that authorizes none, which `aspaCommand` separates from an absent record. The ASPA limit carried the same random-SUBSET defect as the ROA one and not only a random order: `Entries` broke out of the map range at the limit. No validation path reaches any of the three (`findCovering`, `checkPair`), so the sort is on the command goroutine only. Four tests in `roa_cache_order_test.go` and `aspa_cache_order_test.go` require the full row sequence on 32 consecutive calls, over seed data where text order and address order disagree (`192.0.2.10/32` sorts before `192.0.2.2/32` as text and after it as an address) and a v4/v6 mix pins the family order. With five IPv4 keys and three IPv6 ones a map-ranging implementation is right by luck with probability at most 15^-32, so it fails rather than flakes |
| 2026-08-24 | cli-show-bgp-answer-shapes | cli clear bgp soft | `SoftClearPeer` (`internal/component/bgp/reactor/reactor_api.go`) drains `familySet`, a `map[string]bool`, into a `[]string` by ranging it, so the slice arrives in a different order on every call. It reaches an operator as the `families-refreshed` key of the `clear bgp <peer> soft` answer (`internal/component/bgp/plugins/route_refresh/handler/clear_soft.go`). So `json` over one unchanged clear differed run to run whenever more than one family refreshed. Found during the sibling `Peers` fix in the same file, not by a search for it | fixed 2026-08-24 with `slices.Sorted(maps.Keys(familySet))`, both imports already present for the sibling fix. Recorded as its own row rather than folded into the `Peers` row, because the two are separate producers reaching separate commands. The count of occurrences is what this file exists to carry. The general shape: a map drained into a slice is a random order. That order stops being an internal detail the moment the slice reaches an answer |
| 2026-08-10 | fixit-forward-rail-initial-sync-ordering | mcp | `test/plugin/mcp-tools-list-deterministic-order.ci` fails at HEAD: `tools-order stable=false calls=8 diverged-on-call=7 names=identical byte-offset=22848`. The tool NAMES are stable and the bytes are not, so the divergence is inside a tool's serialized schema, not in the list order the test is named for | not fixed here: found while running the retired `ze-functional-plugin-test` (current: `./le functional plugin`) for an unrelated spec, and it blocks no goal of that spec |
| 2026-08-15 | web-htmx4-prepare | chaos web | `sortPeers` (`internal/chaos/web/handlers.go`) is not a total order: `slices.SortFunc` is not stable and the indices arrive from a map range (`ActiveSet.Indices`, and the `Peers` map in `writeAllPeers`), so tied rows kept the map order. `GET /peers?sort=status` answered a different row order on every request, and the table polls every 500ms | fixed here: the comparison breaks a tie by peer index, ascending in both directions. Two golden cases pin one order per call site (`peers-sort-status`, `viz-all-peers-sort-chaos`); the fix changes no other captured fixture |
| 2026-08-15 | web-htmx4-cutover | chaos web golden capture | `TestChaosCaptureIsDeterministic/viz-panels` fails intermittently on a tree it also passes on: `2h58m ago` becomes `2h59m ago`. `formatElapsed` truncates `time.Since(ev.Time)`, and the seeded event time is `time.Now()-3h+offset`, so a rendered age is `3h-offset` plus however long that build and render took. The exact trigger is NOT established: two runs passed early in the session and two failed 40 minutes later, on the same bytes, and the diverging text is a `<span class="event-time">` this spec never touches. The comment above `chaosGoldenRun` says a three-hour run makes the age change only on the minute, which is true and is the exposure | not fixed here. The fix is a clock seam on `DashboardState` (`s.now()`) replacing the eight `time.Now()`/`time.Since` reads in `render.go`, `viz.go` and `viz_timeline.go`, and it moves the age text in every capture that holds one. Doing that inside the htmx 4 cutover would move fixtures for a reason the cutover's `TestChaosPortFidelity` explanations do not cover, which is the evidence that spec rests on |
| 2026-08-24 | cli-show-bgp-answer-shapes | cli show bgp | `reactorAPIAdapter.Peers` (`internal/component/bgp/reactor/reactor_api.go`) ranges `r.peers`, the `map[netip.AddrPort]*Peer` field of `Reactor` (`reactor.go`), so every array-shaped peer answer arrives in a different order on each call. The `first 1` operator over `show bgp peer * statistics` answered 192.0.2.1 on one run of `test/ui/show-bgp-peer-rows.ci` and 192.0.2.2 on the next, same config and same binary. `show bgp`, `show bgp health` and `show bgp peer * capabilities` read the same producer. `show bgp peer list` escapes it by keying its rows on the address, which `rowSet` then sorts | fixed 2026-08-24: `reactorAPIAdapter.Peers` now walks `slices.SortedFunc(maps.Keys(r.peers), netip.AddrPort.Compare)` instead of ranging the map, so every consumer takes one order from the one producer. The key is the whole `netip.AddrPort`, not the `Address` this row first proposed. Two peers can share an address on different ports, which is why `PeerSettings.PeerKey` carries the port. An address-only sort leaves that pair tied and random. `TestPeersAnswersSortedOrder` (`reactor_api_peer_order_test.go`) registers eight peers unsorted, including one such pair and an IPv6 tail, and requires peer-key order on 32 consecutive calls. A map-range implementation answers sorted by luck with probability at most 1/8 per call, so it fails rather than flakes |
| 2026-08-24 | website-asciinema-terminal-demos | demos/terminal web-config poster | The browser demo's committed poster takes two values from one unchanged renderer image. `./le terminal-demo render-all`, under renderer image `vhs-0.11.0-playwright-1.55.0`, rendered `dd42e311` at 21:30 and 22:05 and `300f2e8e` at 21:57, same host binary, same `--release`. A second image did the same, and the two images AGREED with each other in each window, so the input is environmental rather than per-image. The two posters differ in 21 of 1,693,440 pixels (SSIM 1.000000, PSNR 76.7 dB) and every one of them is the left border of a `.config-input` box or a corner of the Save or Discard button. Not the fonts: the same pair of values appears with three different font sets. Found while comparing two renderer images for A-6, where it first read as a font difference | not fixed here, and it did not block A-6: the two images under comparison were rendered back to back and came out byte-identical. HYPOTHESIS, unproven: the poster is `page.screenshot()` taken immediately after a `waitForFunction` on the hostname field's value (`demos/terminal/web-config/demo.cjs:82`), and `.config-input` carries `transition: border-color 0.15s` with `.btn` carrying `transition: background 0.15s, border-color 0.15s` (`internal/component/web/assets/style.css:437` and `:293`), so a screenshot can land inside a transition. Next step is a probe that screenshots twice 250 ms apart in one session and diffs the pair; if the second is stable the fix is a settle before the screenshot, not a retry |
