| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-07-30 | - | config | `enabled != true` early return skipped security settings parsing | split into `extractXBlock` plus two exported callers |
| 2026-08-03 | - | config | same pattern on looking-glass TLS flag | same split: parse everything, gate nothing |
| 2026-08-10 | fixit-mgmt-listener-auth-guard | config | third service: `ExtractGNMIConfig` dropped token and TLS paths, so an env-started gNMI listener refused to boot naming the token the operator had written | same split: `extractGNMIBlock` plus `ExtractGNMISettings` and `ExtractGNMIConfig` |
| 2026-08-10 | fixit-mgmt-listener-auth-guard | config | fourth service, and the one the guard was modelled on: `ExtractAPIConfig` reads `tls-cert` and `tls-key` inside the gRPC `enabled == configTrue` branch. `ze.api-server.grpc.enabled` sets `GRPCOn` when the block did not, so `service_grpc.go` receives empty TLS paths and serves gRPC in clear on `0.0.0.0:50051`. The token survives (read above the gate), so the credential crosses the wire unencrypted | same split: `extractAPIBlock` plus `ExtractAPISettings` and `ExtractAPIConfig`, consumed by `resolveAPIListeners` (cmd/ze/hub/api_infra.go) |
| 2026-08-10 | fixit-mgmt-listener-auth-guard | config | the same fourth service loses two more halves at that gate: a whole `ExtractAPIConfig` returns not-ok when neither transport says `enabled true`, so an env-started transport dropped the shared `token` AND every address the block named. A block carrying a token and a loopback `server` produced a refusal telling the operator to set the token they had already written | same split, one call: `resolveAPIListeners` reads settings above the gate and each transport's `enabled` flag decides only whether it binds |
| 2026-08-10 | fixit-mgmt-listener-auth-guard | config | fifth service: `extractWebBlock` gates the address on `enabled`, so `--web`, `ze.web.listen` or `ze.web.enabled` left `webAddrs` empty and `resolveWebListeners` published `0.0.0.0:3443` over the address the block named. With `ze.web.insecure` set, the guard then refused to boot naming an address written in no config file | read the address from `ExtractWebSettings` beside the certificate, only when something else already said START |
| 2026-08-10 | fixit-mgmt-listener-auth-guard | config | MCP carries the residue of this class and must NOT be fixed by copying the split: `ze.mcp.enabled` synthesizes `127.0.0.1:8080` in `cmd/ze/hub/main.go`, so a dormant block naming port 9000 silently gets 8080. That default is narrower than any address a block can name, so honoring the block would MOVE MCP to a routable address the guard passes whenever the block also carries a token | not fixed, deliberately: the residue is functional, the copy would be an exposure. Asymmetry recorded in the spec's Notes |
| 2026-08-31 | rfcgate-6-supported-extraction-signoff (phase 4, rfc8671) | BMP sender config reload | `runBMPPlugin`'s `OnConfigure` (`internal/component/bgp/plugins/bmp/bmp.go`) calls `setSenderPolicy` unconditionally, then guards `startSender` with `len(snd.Collectors) > 0`. `startSender` is the only caller of `stopSenders` outside plugin shutdown, so a reload that removes every collector never stops the running sessions: they stay connected to collectors the config no longer names, and they adopt the new route-monitoring policy with route-mirroring turned off under them. RFC 8671 Section 7.2 requires a change that alters the behavior of an existing BMP session to bounce that session; on this path nothing is bounced. Found while classifying RFC 8671 site 7.2:1 | fixed on the spot. The reload path moved into `applySenderConfig`, which is the whole `bgp`-root reload and has no guard: `startSender` opens with `stopSenders()` and ranges over an empty collector map harmlessly, so removing every collector now stops every session. The extraction of the method is what lets a test reach the reload without an SDK plugin. `TestRFC8671RemovingEveryCollectorBouncesTheSession` drives it and goes red against the guard, which is how the guard was confirmed as the cause. The site is now mapped to RFC8671-7.2-1 |
