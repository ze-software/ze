| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-03-21 | - | tests | old-vs-new comparison test where both sides were broken | rebuilt fixture from real production output |
| 2026-03-21 | - | tests | ExaBGP migration tests used Ze syntax as input, no migration code exercised | captured fixture from live run |
| 2026-03-29 | - | tests | `.ci` test used `cmd=api` syntax the real parser did not accept | fixed fixture to match production input |
| 2026-08-10 | - | tests | `test/plugin/bgp-gtsm-reject.ci` (index 78) times out at 5.0s with zero received messages, standalone and in the suite. Reproduced on a pristine `git archive` of committed HEAD, so it predates the working tree. It carries `expect=exit:code=1` and no `tmpfs=` block, so it cannot reach the `runOrchestrated` teardown branch changed the same day | not fixed, found while clearing the ze-lint fault |
| 2026-08-12 | fixit-config-validators-bypassed-at-startup | tests | `test/parse/config-startup-refuses-invalid-validator.ci` never reached the code it was written to hold, for two independent reasons, and it was committed unrun. Its config carried only a `plugin` block, which `ProbeConfigType` (`internal/component/config/probe.go`) grades `ConfigTypeHub`, so `ze` ran `runOrchestratorWithData` and died at `parse config: line 2: expected 'external', got internal`, a hub parse error raised before `LoadConfig`. And `runOneCommand` (`internal/test/runner/parsing.go`) rewrites a bare `-` argument into the path of the `stdin=` file. So `exec=ze -` became `ze <path>`, a bare-config invocation form that no longer exists. The daemon answered `unknown command` with its usage | fixed the test: the config is a `tmpfs=` file carrying a second top-level block so the same bytes route to `runYANGConfig`, started with `ze start <file>`. Mutation-verified: with the `refuseInvalidCustomSections` call removed from `LoadConfig` the daemon starts and the test fails on the exit code |
| 2026-08-12 | - | dnsserver DoH truncation proof | `TestDoHIgnoresEDNSUDPSize` (`internal/core/dnsserver/secure_test.go`), tagged `RFC8484-6-1 positive`, builds its own `dns.HandlerFunc` and writes the reply itself. It never calls `dnsserver.Authoritative`, which is what every production DoH answer goes through, so it cannot see a truncation bug in the harness. Measured: with `send` (`handler.go`) mutated to truncate every transport rather than datagrams only, that test stayed green while `TestRFC1035_StreamTransportNotTruncated` went red | not fixed. The tagged assertion is correct and the fix is to drive it through `Authoritative`, which is an edit only the owner may authorise on an RFC-tagged test |
| 2026-08-12 | - | ipsec-interop PKI fixtures | `test_every_pki_scenario_resolves_to_decodable_der` (`test/ipsec-interop/lab_test.py:316`) calls `resolve_pki_placeholders` (`lab.py:678`), which opens `test/ipsec-interop/pki/ze.pem`. That file is generated by the lab and is absent from a checkout that has not run it, so the test raises `FileNotFoundError` rather than skipping or generating. Red at HEAD; the working tree holds no edit under `test/ipsec-interop/` | not fixed. Found while closing eight spec retirements <!-- doc-links: ignore (the ipsec lab generates this file, so a fresh checkout does not hold it, which is what the row records) --> |
| 2026-08-12 | - | ipsec-interop lab | `test_every_pki_scenario_resolves_to_decodable_der` (`test/ipsec-interop/lab_test.py`) resolved every scenario's PKI placeholders against the SHARED `test/ipsec-interop/pki`, restating a rule instead of calling it. The producer, `Scenario._find_pki_dir` (`test/ipsec-interop/lab.py`), prefers the scenario's own `pki/`. Scenario 25-responder-eap-tls13 carries `ze.pem` there and the shared directory holds `client.pem`. The case died with FileNotFoundError, and `make ze-test-pkg PKG=./scripts/dev` was red at HEAD for every session. That scenario was also missing from the expected-set and count assertions the case ends with | fixed: the case calls `Scenario._find_pki_dir` per scenario, refuses a scenario that resolves none, and the set and count now name five scenarios and 15 leaves. Found by the scripts/dev gate run for the StayRTR RTR interop scenario |
| 2026-08-13 | - | pppoe interop lab | `test/pppoe-interop/scenarios/01-pppoe-chap-ipv4` could not reach its first assertion on any machine. Its `accel-ppp.conf` lists no `cli` module and carries no `[cli]` section, while `wait_accel_ready` and every session assertion in `lab.py` run `accel-cmd`, which speaks to that listener on localhost:2001. accel-pppd started and served PPPoE; `accel-cmd` answered `Connection to "localhost:2001" failed` and the run died at setup. So the one scenario that faces Ze's PPPoE client at a third-party AC had never executed, which is consistent with `make ze-deployment-pppoe-accel-docker-test` having no caller but the manual `ze-release-evidence` | fixed by adding `cli` to `[modules]` and a `[cli]` section with `tcp=127.0.0.1:2001`. The scenario passes: PADI through PADS, CHAP, IPCP address `10.11.0.2` peer `10.11.0.1`, a ping through the session, and accel-ppp's own session table showing then dropping subscriber `alice` |
| 2026-08-14 | - | cmd/ze/hub unit tests | `TestRunHubConfigRejectsCLIPlugins` asserted the `--plugin is not supported with hub/orchestrator configs` refusal that commit 8d92e9fab deleted along with the orchestrator runtime. Nothing removed the test with the behaviour, so it kept calling `Run` on a config that now BOOTS: the call parked in `waitLoop` (`cmd/ze/hub/main.go`) and the package hit the 20 minute `go test` timeout on every CI verify run from 2026-08-12. A hang costs the tests queued behind it too: the timeout dump shows `TestBuildTag_Gate11_SymbolMatrix` and `TestAwaitReloadWorker` still parked in `t.Parallel` after 19 minutes | replaced by `TestRunPassesCLIPluginsToConfigLoad`, which drives the same entry point with an unresolvable `ze.` plugin name: the failure then comes from `MergeCliPlugins`, downstream of the deleted guard and upstream of the daemon, and a deadline around `Run` reports a return to booting in seconds rather than at the package timeout. The package runs in 68s |
| 2026-08-15 | rfc4271-med-across-as | test harness (`ze-test peer`) | `action=send:conn=2:...` under `option=conn_map:value=remote-ip` writes to the FIRST session rather than to conn=2's. Two peers at 127.0.0.1 and 127.0.0.2, each announcing its own prefix: the Adj-RIB-In ends with ONE route, keyed to 127.0.0.1, holding conn=2's UPDATE (AS_PATH 65002, next-hop 2.2.2.2, 10.2.0.0/24), and ze logs `modify apply peer=127.0.0.1` twice while conn=2's peer never applies its own policy. Reproduced twice, deterministic. Every committed `conn_map` test SENDS on conn=1 only, so a two-source test is the untested path and a wrong-session delivery would read as a product bug | not fixed here: found while adding an Adj-RIB-In observation for RFC4271-5.1.4-2, and it blocks no goal of that spec, which asserts the same ordering from one peer |
| 2026-08-15 | rfc4271-med-across-as | bgp forward rail | `test/plugin/relay-withdraw-nexthop-self.ci` fails 3/3 in isolation on the working tree, and it PASSED in a full `make ze-plugin-test` run 25 minutes earlier. Commit `480897faf` ("never advertise a peer its own address as next hop") landed between the two runs. The observer reports `dest peer was never sent the advertisement`, and ze logs `withholding route: its next hop is this peer's own address ... rfc="RFC 4271 Section 5.1.3"` for the same peer, so the new withhold is what suppresses the advertisement that test exists to see. That commit's own `forward_next_hop_test.go` is still UNTRACKED and 2 of its cases fail in `make ze-test-pkg PKG=./internal/component/bgp/reactor`, so the author is mid-flight | not fixed here: another session's work, landed minutes ago, and it blocks no goal of this spec. Either the withhold is wider than Section 5.1.3 asks or that `.ci` encodes the old behaviour, and only its author can say which |
