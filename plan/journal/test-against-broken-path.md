| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-23 | - | `test/plugin` | Seven tests fail deterministically under `make ze-functional-plugin-test` and again when re-run serially with `-p 1`: `lg-graph-lab`, `task-no-extension`, `task-rib-routes`, `show-rib-under-load`, and `bmp-lg-disconnect`, `bmp-lg-ingest`, `bmp-lg-bestpath-isolation`. The three bmp-lg tests TIME OUT at the 20s budget against a 2.0-2.4s historical average, so they were fast until recently. A/B against HEAD: `internal/test/runner/runner_exec.go` and `runner_validate.go` were replaced with their committed versions, the isolated pair was rebuilt, and the same seven fail the same way, so none of them belongs to the working tree. Only `bmp-lg-disconnect` was looked at: its observer prints `FAIL: no routes before disconnect` and the payload it dumps DOES carry one route, keyed `127.0.0.1:50299:10.0.0.1` with the ephemeral source port in the peer key. The other six were not characterised. `sysctl-list` failed in the parallel run and passed serially, so it is load-sensitive and is not counted here | not fixed. This session met them when it closed spec-fixit-redistribute-establishment-stall, whose own tests all pass in the same run. The next step is one test at a time. Start from whether the bmp-lg trio regressed together |
| 2026-08-22 | record-answers-1-sdk-path | plugin answer producers | AC-7 claimed the socket and the bridge produce one answer, and the test the spec named for it, `TestDirectBridgeDispatchCommandAnswer`, drove a hand-written stub handler inside `pkg/plugin/rpc`. It proved the two TRANSPORTS carry one answer and said nothing about the two PRODUCERS, so `plugin.AnswerFor`, the whole in-process producer, reached the review gate with no test at all and neither did its one caller `Server.dispatchCommandAnswer`. The criterion looked covered because a passing test named it | `TestAnswerForAgreesWithTheWire` (`internal/component/plugin/dispatch_test.go`) builds one response shape twice from a fresh generator and takes it through `rpc.WriteRecordAnswer` and through `AnswerFor`, reading both as a consumer reads them, over nine cases including the threshold boundary and a failing walk. Mutation-tested twice: changing the threshold test from `<=` to `<` and dropping `Message` from the streamed terminator each redden it, and the failing-walk-that-streams case exists because the first table missed the second mutation |
| 2026-08-22 | - | tests | `test/plugin/attach-process-reload.ci` fails deterministically, 4 of 4 runs standalone and once under `make ze-functional-plugin-test`, with `ZE-OBSERVER-FAIL: uncaught observer exception: json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)` from the `reload-added` observer. Reproduced with a `ze` built from a pristine `git archive HEAD` tree and a pristine `test/scripts/ze_api.py`, so it predates the working tree. The observer prints its last checkpoint, `RELOAD-ADDED: fed only after the reload attached it`, before it throws, so the throw is later than that line. NARROWED 2026-08-22, and the throwing call now has a containing frame. The observer's own `is_state` catches `JSONDecodeError`, and so does `API.wait_for_event`, which is why the throw is in neither. After its checkpoint the observer makes exactly two more calls, `shutdown_fire_and_forget` and `wait_for_shutdown`. `API.wait_for_shutdown` (`test/scripts/ze_api.py`) drains callbacks by calling `read_line` inside `except (IOError, OSError)`, which does NOT catch a decode error, so anything raised beneath `read_line` propagates straight out of it and into the excepthook. The exact decode under `read_line` is still not pinned, and the guard being too narrow to contain it is a defect in its own right: a malformed line during shutdown kills the observer rather than being reported. Note the repair is not to widen the catch and swallow it, which would hide a real protocol error. The original note follows. The throwing call was NOT identified: `_observer_excepthook` writes the traceback to the observer stderr and the relay carries only the sentinel | not fixed, found while making the command answer unconditional. The next step is to carry the observer traceback through the relay, or to run the observer outside the runner |
| 2026-03-21 | - | tests | old-vs-new comparison test where both sides were broken | rebuilt fixture from real production output |
| 2026-08-18 | - | `demos/terminal/web-config/` | the validator and the recording drive different paths. `validate.sh` authenticates with HTTP Basic and posts to `/config/commit` with curl. `demo.cjs` logs in through the form and drives the workbench. So the validator cannot see a break in what the recording does. In one run it printed `validated web-config output` and the recording then failed at `demo.cjs:75`, on a 30s timeout waiting for `#commit-bar.visible` to go away. After the diff modal's Confirm Commit, the pending-change bar never clears. Reproduced twice through `render.py --demo web-config`. It stops `make ze-terminal-demo-render-all` after 5 of the 17 terminal demos. The success branch of `handleCommitPost` (`internal/component/web/handler_config_commit.go`) does write an out-of-band `#commit-bar` with count 0. So either that branch is not reached, or the swap is not applied. Which of the two is NOT determined | not fixed. The validator half is what let it land. A validator that proves the server route works says nothing about the browser flow the recording drives. A browser demo needs a validator that drives a browser |
| 2026-03-21 | - | tests | ExaBGP migration tests used Ze syntax as input, no migration code exercised | captured fixture from live run |
| 2026-03-29 | - | tests | `.ci` test used `cmd=api` syntax the real parser did not accept | fixed fixture to match production input |
| 2026-08-10 | - | tests | `test/plugin/bgp-gtsm-reject.ci` (index 78) times out at 5.0s with zero received messages, standalone and in the suite. Reproduced on a pristine `git archive` of committed HEAD, so it predates the working tree. It carries `expect=exit:code=1` and no `tmpfs=` block, so it cannot reach the `runOrchestrated` teardown branch changed the same day | not fixed, found while clearing the ze-lint fault |
| 2026-08-12 | fixit-config-validators-bypassed-at-startup | tests | `test/parse/config-startup-refuses-invalid-validator.ci` never reached the code it was written to hold, for two independent reasons, and it was committed unrun. Its config carried only a `plugin` block, which `ProbeConfigType` (`internal/component/config/probe.go`) grades `ConfigTypeHub`, so `ze` ran `runOrchestratorWithData` and died at `parse config: line 2: expected 'external', got internal`, a hub parse error raised before `LoadConfig`. And `runOneCommand` (`internal/test/runner/parsing.go`) rewrites a bare `-` argument into the path of the `stdin=` file. So `exec=ze -` became `ze <path>`, a bare-config invocation form that no longer exists. The daemon answered `unknown command` with its usage | fixed the test: the config is a `tmpfs=` file carrying a second top-level block so the same bytes route to `runYANGConfig`, started with `ze start <file>`. Mutation-verified: with the `refuseInvalidCustomSections` call removed from `LoadConfig` the daemon starts and the test fails on the exit code |
| 2026-08-12 | - | dnsserver DoH truncation proof | `TestDoHIgnoresEDNSUDPSize` (`internal/core/dnsserver/secure_test.go`), tagged `RFC8484-6-1 positive`, builds its own `dns.HandlerFunc` and writes the reply itself. It never calls `dnsserver.Authoritative`, which is what every production DoH answer goes through, so it cannot see a truncation bug in the harness. Measured: with `send` (`handler.go`) mutated to truncate every transport rather than datagrams only, that test stayed green while `TestRFC1035_StreamTransportNotTruncated` went red | not fixed. The tagged assertion is correct and the fix is to drive it through `Authoritative`, which is an edit only the owner may authorise on an RFC-tagged test |
| 2026-08-12 | - | ipsec-interop PKI fixtures | `test_every_pki_scenario_resolves_to_decodable_der` (`test/interop-ipsec/lab_test.py:316`) calls `resolve_pki_placeholders` (`lab.py:678`), which opens `test/interop-ipsec/pki/ze.pem`. That file is generated by the lab and is absent from a checkout that has not run it, so the test raises `FileNotFoundError` rather than skipping or generating. Red at HEAD; the working tree holds no edit under `test/interop-ipsec/` | not fixed. Found while closing eight spec retirements <!-- doc-links: ignore (the ipsec lab generates this file, so a fresh checkout does not hold it, which is what the row records) --> |
| 2026-08-12 | - | ipsec-interop lab | `test_every_pki_scenario_resolves_to_decodable_der` (`test/interop-ipsec/lab_test.py`) resolved every scenario's PKI placeholders against the SHARED `test/interop-ipsec/pki`, restating a rule instead of calling it. The producer, `Scenario._find_pki_dir` (`test/interop-ipsec/lab.py`), prefers the scenario's own `pki/`. Scenario 25-responder-eap-tls13 carries `ze.pem` there and the shared directory holds `client.pem`. The case died with FileNotFoundError, and `make ze-unit-pkg-test PKG=./scripts/dev` was red at HEAD for every session. That scenario was also missing from the expected-set and count assertions the case ends with | fixed: the case calls `Scenario._find_pki_dir` per scenario, refuses a scenario that resolves none, and the set and count now name five scenarios and 15 leaves. Found by the scripts/dev gate run for the StayRTR RTR interop scenario |
| 2026-08-13 | - | pppoe interop lab | `test/interop-pppoe/scenarios/01-pppoe-chap-ipv4` could not reach its first assertion on any machine. Its `accel-ppp.conf` lists no `cli` module and carries no `[cli]` section, while `wait_accel_ready` and every session assertion in `lab.py` run `accel-cmd`, which speaks to that listener on localhost:2001. accel-pppd started and served PPPoE; `accel-cmd` answered `Connection to "localhost:2001" failed` and the run died at setup. So the one scenario that faces Ze's PPPoE client at a third-party AC had never executed, which is consistent with `make ze-deployment-docker-pppoe-accel-test` having no caller but the manual `ze-evidence-release-verify` | fixed by adding `cli` to `[modules]` and a `[cli]` section with `tcp=127.0.0.1:2001`. The scenario passes: PADI through PADS, CHAP, IPCP address `10.11.0.2` peer `10.11.0.1`, a ping through the session, and accel-ppp's own session table showing then dropping subscriber `alice` |
| 2026-08-14 | - | cmd/ze/hub unit tests | `TestRunHubConfigRejectsCLIPlugins` asserted the `--plugin is not supported with hub/orchestrator configs` refusal that commit 8d92e9fab deleted along with the orchestrator runtime. Nothing removed the test with the behaviour, so it kept calling `Run` on a config that now BOOTS: the call parked in `waitLoop` (`cmd/ze/hub/main.go`) and the package hit the 20 minute `go test` timeout on every CI verify run from 2026-08-12. A hang costs the tests queued behind it too: the timeout dump shows `TestBuildTag_Gate11_SymbolMatrix` and `TestAwaitReloadWorker` still parked in `t.Parallel` after 19 minutes | replaced by `TestRunPassesCLIPluginsToConfigLoad`, which drives the same entry point with an unresolvable `ze.` plugin name: the failure then comes from `MergeCliPlugins`, downstream of the deleted guard and upstream of the daemon, and a deadline around `Run` reports a return to booting in seconds rather than at the package timeout. The package runs in 68s |
| 2026-08-15 | rfc4271-med-across-as | test harness (`ze-test peer`) | `action=send:conn=2:...` under `option=conn_map:value=remote-ip` writes to the FIRST session rather than to conn=2's. Two peers at 127.0.0.1 and 127.0.0.2, each announcing its own prefix: the Adj-RIB-In ends with ONE route, keyed to 127.0.0.1, holding conn=2's UPDATE (AS_PATH 65002, next-hop 2.2.2.2, 10.2.0.0/24), and ze logs `modify apply peer=127.0.0.1` twice while conn=2's peer never applies its own policy. Reproduced twice, deterministic. Every committed `conn_map` test SENDS on conn=1 only, so a two-source test is the untested path and a wrong-session delivery would read as a product bug | not fixed here: found while adding an Adj-RIB-In observation for RFC4271-5.1.4-2, and it blocks no goal of that spec, which asserts the same ordering from one peer |
| 2026-08-15 | rfc4271-med-across-as | bgp forward rail | `test/plugin/relay-withdraw-nexthop-self.ci` fails 3/3 in isolation on the working tree, and it PASSED in a full `make ze-functional-plugin-test` run 25 minutes earlier. Commit `480897faf` ("never advertise a peer its own address as next hop") landed between the two runs. The observer reports `dest peer was never sent the advertisement`, and ze logs `withholding route: its next hop is this peer's own address ... rfc="RFC 4271 Section 5.1.3"` for the same peer, so the new withhold is what suppresses the advertisement that test exists to see. That commit's own `forward_next_hop_test.go` is still UNTRACKED and 2 of its cases fail in `make ze-unit-pkg-test PKG=./internal/component/bgp/reactor`, so the author is mid-flight | not fixed here: another session's work, landed minutes ago, and it blocks no goal of this spec. Either the withhold is wider than Section 5.1.3 asks or that `.ci` encodes the old behaviour, and only its author can say which |
| 2026-08-15 | web-htmx4-cutover | web browser test runner | `waitMs` (`internal/component/web/testing/runner.go`) slept without touching the browser, and `agentEnv` sets `AGENT_BROWSER_IDLE_TIMEOUT_MS=60000`. So `action=wait:ms=70000` returned to a daemon that had reaped itself, holding no page, and every assertion after it read `(empty page)`. No `.wb` had ever waited that long, so the combination shipped unexercised, and the first test to need it (`lg-stream-swaps.wb`, whose assertion is an uptime crossing a minute) looked like a streaming failure | fixed here: the wait sleeps in slices of half the window and issues one `get url` between them, and the env value derives from the same constant. Measured with the window at 6s: one command at 4s kept the page and no command lost it. `lg-stream-swaps.wb` then passed at 83.8s |
| 2026-08-15 | fixit-bgpls-withdrawal-functional-proof | bgp route server, opaque BGP-LS relay | `test/plugin/rfc7606-54-bgpls-override-propagates.ci` was the only `.ci` that touched BGP-LS relaying. It cannot see the opaque-withdrawal defect on any platform. Its assertions stop at the ANNOUNCEMENT, and no route-server `.ci` had ever closed a peer, so the withdrawal path carried unit proof alone. The unit tests are blind to it as well, because the defect is a DISPATCH refusal. `appendParsedRecords` (`internal/component/bgp/plugins/rs/server_inventory.go`) recorded an unparsed family as `n.String()`. For `*nlri.WireNLRI` that is the size summary `wire[bgp-ls/bgp-ls](23 bytes)`, and the update command answered `family not supported in text mode` | fixed by `test/plugin/rfc9552-52-rs-opaque-withdraw-peer-down.ci`, tagged `RFC9552-5.2-8`. Two route-server clients run on SEPARATE `ze-peer` processes, so the departing client's `action=close` is reachable past the shared `(conn, seq)` cursor. The receiver asserts the whole MP_REACH on announcement and the EXACT MP_UNREACH frame after the departure. The discrimination was measured through a source OVERLAY, which maps the one file to a mutated copy under `tmp/` at build time. The daemon under test is then a real binary built from broken source, and the shared checkout stays byte-clean, so no other session's gate goes red for it |
| 2026-08-16 | - | graceful restart interop | `test/interop/scenarios/bgp-graceful-restart-frr/ze.conf` declares `internal gr { use bgp-gr; }` but its peer's only attach block is `attach process announce-routes`. `bgp-gr` subscribes to `open direction received`, `state` and `eor` (`internal/component/bgp/plugins/gr/gr.go`), which are all peer-scoped types, and `PeerScopedProcs` (`internal/component/plugin/server/delivery_graph.go`) delivers only when the peer's config grants the process, failing closed on a peer that grants nothing. An internal plugin is an ordinary `Process` that starts as a goroutine (`process.go`, `startInternal`), so the same gate applies to it. If the scenario still passes, its assertions do not depend on the GR plugin and the one GR interop test is vacuous | NOT FIXED, and NOT reproduced: settling it needs a docker run of the scenario, which this session did not do. No interop scenario attaches `rib` or `gr`, yet bgp-rfc7999-blackhole-frr is cited in `rfc/enrolled.txt` as asserting a kernel discard route through the RIB, so the RIB is likely fed by in-process interfaces rather than the event bus and the two plugins may not be alike. Next step is to run scenario 13 and read whether `bgp-gr` logs a received `open` |
| 2026-08-16 | - | qemu integration suite | `TestXFRMReadbackShowsInstalledSA` (`internal/component/ike/dataplane/xfrm_readback_integration_linux_test.go:85`) fails under `make ze-qemu-integration-test` with `ReplayWindow = 32, want 64`. `internal/plugins/ospf` fails two XFRM tests beside it, `InstallPolicy(proto 89): xfrm: policy update: invalid argument` and `installer did not record an installed SA` (`ipsec_integration_linux_test.go:101,133`). Found while running that suite as evidence for a lint burn-down that touches none of the three files. Every other package the burn-down edits is green in the same run | not fixed and not diagnosed. Two readings are open and one read decides between them: the product sets a replay window the kernel does not keep, or the test asserts a value the kernel never keeps. The next step is the producer that sets `ReplayWindow`, against what the QEMU kernel reports back |
| 2026-08-16 | hub-deferred-api-auth-independent-of-ssh-block | hub web-user unit tests | `service_web_users_test.go` called `setAPIConfigDir` from five tests, but no file in `cmd/ze/hub` defined it. `make ze-unit-pkg-test PKG=./cmd/ze/hub` stopped at compilation before it could run the API authentication tests. | added the test helper beside its callers. It sets `ze.config.dir` through the typed environment registry and clears it in test cleanup. |
| 2026-08-16 | hub-deferred-api-auth-independent-of-ssh-block | API shared-state unit construction | `buildAPIShared` always started `ConfigSessionManager.RunCleanup` with `plugin.Server.Context()`. Unit builders use an unstarted plugin server, whose context is nil. The cleanup goroutine panicked at `ctx.Done()` after the API assertions ran, so the hub package failed after three minutes. | start cleanup only when the plugin server has a lifecycle context. Production retains server-lifetime cleanup, and unit builders no longer start a goroutine with no lifetime. |
| 2026-08-16 | hub-deferred-api-auth-independent-of-ssh-block | hub service registration | The API UI suite could not build because the listener migration types were renamed to unexported `listenerMigrator` and `tlsUpdatable`, but the three gated service registration files and the web TLS test still named the removed exported types. | updated every stale reference to the canonical unexported types before rerunning the blocked suite. |
| 2026-08-17 | - | BGP forward dedup test | `TestDifferentBaseSameEditSetNeverShares` used a changed MED as the harmless raw-override marker on iBGP egress. RFC 4271 Section 9.1.2.2 requires the selected MED to survive that rail, so the new guard correctly restored the source value and made both test bases equal. | The raw override now changes one COMMUNITY value. The test still proves that equal edit sets over different bases do not share materialization, without requiring prohibited MED behavior. |
| 2026-08-17 | ssh-optional-composition | developer-script verification rule | `ai/rules/commands.md` requires `make ze-test-pkg PKG=./scripts/dev`, but the Makefile has no `ze-test-pkg` target | not fixed; ran the script's direct Python test |
| 2026-08-17 | ssh-optional-composition | CLI syntax validator test | `TestValidateSyntaxMissingSemicolon/missing_semicolon_single_line` deterministically expects `bgp { router-id 1.2.3.4 }` to fail, but `configValidator.validateSyntax` returns no errors. The focused package target reproduces it after the SSH schema dependency fix. | not fixed; the SSH schema ownership goal does not depend on single-line semicolon policy |
| 2026-08-17 | ssh-optional-composition | `internal/component/config/infra` bare-core tests | The package test with only `ze_core` parses BGP and TACACS+ fixture fields from always-built `authz_test.go`, so unrelated optional schemas stop the base authentication tests before their assertions. | not fixed; no-SSH product behavior is covered with the feature set each fixture requires |
| 2026-08-17 | - | `scripts/dev` session-bin unit test | `test_every_ze_core_recipe_seeds_and_only_on_session` (`scripts/dev/session_bin_dir_test.py`) asserted over a make target named `ze`, which commit 8f0b5d78f (`refactor(make): normalize public target names`) renamed to `ze-build`. `make -n ze` answers `No rule to make target 'ze'`, so the case died in `make_n` with CalledProcessError before any assertion, and `make ze-unit-pkg-test PKG=./scripts/dev` was red at HEAD for every session. Its two sibling rows already carried the `-build` suffix, so the rename missed one entry of three. Found while running that gate as evidence for the insomniacslk/dhcp bump | fixed: the row names `ze-build`. The 33 cases pass, and the same case reproduced the failure before the edit |
| 2026-08-18 | fixit-firewall-irr-term-fails-validation | `test/plugin/firewall-irr-update.ci` | its only content assertion was `expect=stderr:pattern=` followed by `irr.*refreshed`, a backslash-escaped alternation, and `irr.*success`. The runner compiles the pattern as a Go regexp, where a backslash before the alternation makes it a literal pipe, so the assertion asked for one string containing `irr`, `refreshed`, a pipe, `irr` and `success` in that order. Nothing could ever match it, and no successful refresh was logged at all, so the test asserted nothing about the refresh it exists to prove | fixed both halves: `logRefreshLearned` logs the counts a refresh learned, mirroring `logEmptyRefresh` on the other outcome, and the pattern is now `irr.*refreshed` |
| 2026-08-22 | fixit-child-sa-rekey-policy | ipsec interop | The suite held a strongSwan Child SA rekey scenario throughout, `05-child-rekey`. It cannot fail for the defect the spec fixed. Ze is the connection INITIATOR there, and that side's request counter was always right. `finishResponderEstablish` (`internal/component/ike/engine/responder.go`) spent the peer's IKE_AUTH id on a responder-role Ze's OWN counter, so that side raised no exchange a conforming peer answers. Every other responder scenario has Ze ANSWERING an exchange charon started (07, 09, 11, 25). Nothing had a responder-role Ze speak first. A wire-visible change shipped with the interop box ticked and no scenario that reverting it reddens | fixed: `test/interop-ipsec/scenarios/26-responder-raises-child-rekey` puts charon on `start_action = start` with every rekey timer at 0, and Ze on `connection-type respond` with a 30s ESP lifetime. The only rekey on the wire is Ze's. Mutation-verified in a throwaway worktree with `sa.NextMsgID = msgID + 1` restored: charon parses no REKEY_SA request at all. "An interop scenario exists for this feature" is not the claim "an interop scenario fails when this change is reverted" |
| 2026-08-22 | fixit-zefs-diff-structural-ops | editor and functional test harnesses, config storage backend | The daemon runs on a zefs blob (`ze.storage.blob` defaults to `true`, `cmd/ze/ze_core_dispatch.go`) and both test harnesses ran on the filesystem, so a blob-only defect was invisible to every test that could have caught it. `newHeadlessModel` (`internal/component/cli/testing/headless.go`) built its editor with `cli.NewEditor`, which is filesystem-only, so 164 `.et` tests exercised a backend the daemon does not use. One layer up the same hole is still open: `internal/test/runner/runner_exec.go` sets `ze.storage.blob=false` on every functional daemon it starts, so no `.ci` or `.wb` can see a blob-only daemon defect either. The defect that surfaced it was `blobStorage.List` resolving a directory prefix to a FILE key, which hid every pending structural operation from the review an operator reads before commit | the `.et` half is FIXED: both headless constructors take a `storage.Storage`, `option=storage:value=blob` serves it, and `TestRunnerBlobStorageWritesBlobNotFile` reads both stores after a commit so the option cannot silently resolve to the filesystem. The `.ci`/`.wb` half is NOT fixed and has no spec: the pin carries its own stated reason, keeping per-test configs out of the developer's shared zefs active pointer, so the fix is a per-test opt-in rather than removing it. The general practice: a harness that pins a backend, a mode or a flag pins which defects its whole suite can see, and that pin is a coverage claim nobody writes down |
| 2026-08-23 | - | ipsec-interop scenario 04-eap-tls | `test/interop-ipsec/scenarios/04-eap-tls/ze-env` sets the `tlsunsafeekm` GODEBUG setting to its old value. Go 1.27 REMOVED that key, and the runtime fatals before `runtime.main` on a removed GODEBUG set to its old value, so ze dies at container start and the scenario fails at `strongSwan SA 'ze' did not reach ESTABLISHED within 90s` with no IKE packet sent. Measured on Go 1.27.0 in a full `python3 test/interop-ipsec/run.py`: 17 of 19 scenarios pass and this is one of the two reds. `cmd/ze/main.go` tells an OPERATOR to set the same variable to reach a peer that needs the unsafe EKM export, so the shipped guidance now kills the daemon rather than enabling the path it names. 06-eap-tls13 and 25-responder-eap-tls13 pass because neither sets it | FIXED 2026-08-24 by spec-fixit-eap-tls-escape-hatch-kills-the-daemon, and the answer to the product question is that NOTHING replaces the escape hatch: Go offers no knob, so ze says so instead. `ze-env` sets nothing, `eapTLS12ExportRefused` (`internal/component/ike/eap/eap_tls.go`) names the peer, the negotiated version, RFC 7627 and three remedies, and `TestNoShippedGuidanceNamesARemovedGODEBUG` refuses the assignable form in any tracked file. The scenario is REPURPOSED rather than retired: it asserts the completed TLS handshake, the attributed refusal, and that neither end installs an XFRM SA. PASS, and RED under the bare-wrap mutation. The general practice: when a scenario's only reason to be green was an escape hatch, removing the hatch does not retire the scenario, it changes what the scenario proves |
| 2026-08-23 | - | `test/plugin/config-edit-ssh.ci` and `API.wait_for_shutdown` (`test/scripts/ze_api.py`) | A SECOND fixture carries the observer-shutdown decode failure the 2026-08-22 row above records for `attach-process-reload.ci`. Same sentinel, `ZE-OBSERVER-FAIL: uncaught observer exception: json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)`, this time from the `config-edit-test` observer, seen under `make ze-functional-plugin-test` on 2026-08-23. Two claims in that row are now settled, and one of them settles the other way. The traceback DOES reach the runner here: the relay carried the frames as `plugin.relay` WARN lines, naming `config-edit-test.run` line 57 calling `api.wait_for_shutdown()`. So the throwing call is identified, and it is the frame that row had reached by reading the source rather than by seeing it. The fixture's own checkpoints both printed OK first, so its assertions passed and only the shutdown drain threw. | not fixed. `test/scripts/ze_api.py` was outside this session's scope and the failure blocks nothing this session set out to do. The next step is unchanged and now starts from a confirmed frame rather than a hypothesis |
| 2026-08-23 | - | ipsec-interop scenario 02-ipsec-bgp-redistribute-frr | The tunnel, both XFRM SAs and the Ze XFRM policy for 10.200.0.0/24 all come up, then the scenario fails at `FRR route 10.200.0.0/24 did not appear within 30s`. The daemon log says why: `send refused: no peer this selector names attaches this process with the required send permission: selector *, process redistribute-orchestrator, send type update`. The scenario's `ze.conf` declares a `redistribute` block importing ipsec into bgp and a bgp peer carrying no `attach process` block, so the redistributed prefix never reaches the wire and the IPsec half of the scenario is proven while its BGP half is not. The 2026-08-16 pass that made a runtime subscription intersect `DeliveryGraph.Receivers` in `PeerScopedProcs` updated 19 route-server fixtures and did not reach this one | not fixed, found while closing spec-fixit-ipsec-interop-cli-credentials. Adding the attach block to the fixture is a workaround unless the daemon is right to demand it, and that is the open question: `plan/spec-fixit-redistribute-establishment-stall.md` is the adjacent open spec and neither it nor `plan/spec-review-redistribute-orchestrator.md` names this send permission |
| 2026-08-24 | fixit-flowspec-table-never-swept | `test/plugin/policy-routes-show.ci` in the QEMU tier | Fails deterministically: once in a full `make ze-qemu-needs-linux-test` run, and 3 of 3 alone in a fresh VM (`make ze-qemu-debug NOBUILD=1 RUN='ze-test bgp plugin 487'`), each as `TYPE: timeout` against its own `option=timeout:value=15s`. The daemon reaches the state the test asserts about, logging `firewall backend loaded backend=nft`, `firewall config applied tables=0` and `policy routes applied count=1`, so the policyroute apply completed. What fails after that is `ze cli -c "show policy routes"`, which the driver retries 50 times; the full-suite run prints its error verbatim as `cannot connect to daemon: connect to 127.0.0.1:2222: dial tcp 127.0.0.1:2222: connect: connection refused`, and the test config declares no ssh and no api-server listener. The test was admitted to the QEMU tier by `4c91e5e2f`, whose header argues for removing its `ZE_QEMU` exclusion and records no run there. Walked into while closing an unrelated firewall spec, whose diff touches `internal/plugins/policyroute/register.go` only to carry the new `RegisterTables` error, and the success line above is that path completing | not fixed, and not characterised beyond this row. The two facts a fix starts from are here: it is deterministic in a fresh VM, and the daemon is healthy at the moment the CLI cannot reach it |
