| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-22 | - | lint | `prealloc` v1.0.2 nil-dereferenced on `for range iter.Seq[rpc.Record](rows)` and took all of `goanalysis_metalinter` down. The runner error named the package, never the expression, so the crash reads as a broken tool rather than as one new line of source | wrote the sequence into a typed variable, so the range expression is an identifier instead of a generic conversion |
| 2026-08-26 | - | scripts/zeledon (retired; current producer: `internal/le/`) weekly publication | `process_one` (the retired `scripts/zeledon/post_weekly.py` (current producer: `internal/le/weekly/weekly.go`)) reads `meta["covers"]` with no guard, and `parse_post` above it accepts any front matter block. A weekly post whose front matter omits the covers line therefore aborts with `KeyError: 'covers'` and a five-frame traceback naming line 257, where every other operator mistake in the same tool is refused with a sentence naming the file. The sweep hits it too, so one malformed file stops a catch-up run of ten weeks with a stack trace | the Go port refuses it as an operating error that names the file and quotes the line to write: `Post.Covers` answers `ErrNoCovers` and `readPost` wraps it with the path (`weekly/weekly.go` in the former top-level tool tree, `weekly/poster.go` in the former top-level tool tree). `TestCoversReportsAPostThatDeclaresNoWeek` covers it. The script keeps the traceback until step 14 of the le migration deletes it, so this row is the record that the behaviour is not being carried over |
| 2026-08-26 | - | scripts/dev (retired; current producer: `internal/le/`) RFC conformance gate | `CARRIERS` (the retired `scripts/dev/rfc_requirements.py` (current producer: `internal/le/rfc/rfc.go`)) is built at import time. It calls `functional_suites()` and `scheduled_workflow_targets()`. Each function raises `ParseError` when its input cannot be read. The import runs before `main()`. Thus, the error lands outside every `try` in the five drivers. If `.github/workflows` is absent, every mode aborts with a nine-frame traceback and exit 1. The module docstring promises exit 2 when the gate cannot run. A caller that distinguishes 2 from 1 receives a crash result instead of an unreadable-tree result | the Go port builds the table per call and answers the documented code. `Carriers` and `ScheduledWorkflowTargets` (`internal/le/rfc/carriers.go` in the former top-level tool tree) return the error. `extractionStatusAnswer` (`internal/le/rfc/actions.go` in the former top-level tool tree) reports it and answers 2. `TestTheScriptCrashesWhereTheCommandRefuses` (the retired `scripts/dev/rfc_parity_test.go` (current producer: `internal/le/`)) pins both halves. Thus, the test becomes red if somebody repairs the script |
| 2026-08-26 | - | scripts/dev (retired; current producer: `internal/le/`) RFC audit re-seal | `load_audit` (the retired `scripts/dev/rfc_requirements.py` (current producer: `internal/le/rfc/rfc.go`)) accepts a null `reaudit_history`. Its guard treats the field as absent or a list of strings, and `None` satisfies the first condition. `_write_audit` then calls `setdefault` on that key and gets `None` instead of the default. Its append causes `AttributeError` outside every `try` in the driver. Thus, `--reseal` aborts with a traceback and exit 1 where the docstring promises 2. The tool decides to re-stamp the record. It then crashes before the staged write. The record keeps the stale fingerprints, and each new run reproduces the failure | the Go port reads a null history as an absent history. `writeAudit` (`internal/le/rfc/reseal.go` in the former top-level tool tree) type-asserts the field and appends to a fresh list when the assertion fails. `TestTheScriptCrashesOnANullReauditHistory` (the retired `scripts/dev/rfc_parity_test.go` (current producer: `internal/le/`)) pins both halves. It also verifies that the port preserves the earlier note in the history. Thus, the test becomes red if somebody repairs the script |
