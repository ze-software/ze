| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-22 | - | lint | `prealloc` v1.0.2 nil-dereferenced on `for range iter.Seq[rpc.Record](rows)` and took all of `goanalysis_metalinter` down. The runner error named the package, never the expression, so the crash reads as a broken tool rather than as one new line of source | wrote the sequence into a typed variable, so the range expression is an identifier instead of a generic conversion |
| 2026-08-26 | - | scripts/zeledon weekly publication | `process_one` (`scripts/zeledon/post_weekly.py`) reads `meta["covers"]` with no guard, and `parse_post` above it accepts any front matter block. A weekly post whose front matter omits the covers line therefore aborts with `KeyError: 'covers'` and a five-frame traceback naming line 257, where every other operator mistake in the same tool is refused with a sentence naming the file. The sweep hits it too, so one malformed file stops a catch-up run of ten weeks with a stack trace | the Go port refuses it as an operating error that names the file and quotes the line to write: `Post.Covers` answers `ErrNoCovers` and `readPost` wraps it with the path (`letools/weekly/weekly.go`, `letools/weekly/poster.go`). `TestCoversReportsAPostThatDeclaresNoWeek` covers it. The script keeps the traceback until step 14 of the le migration deletes it, so this row is the record that the behaviour is not being carried over |
