| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-08-31 | rfcgate-6-supported-extraction-signoff | the forced-RED protocol itself, run against `handlePacket` (`internal/component/l2tp/plugins/authradius/coa.go`) in the SHARED checkout | The protocol this repository requires — mutate the producer, capture the failure, restore — disables a real guard in the shared working tree for as long as the mutation is live, and every other session sees it. An agent proving its RFC 5176 Section 3.4 tests discriminate wrote `if !hasMessageAuthenticator && false` and then `if false && !radius.VerifyCoAMessageAuthenticator(...)`. The second is a straight authentication bypass: a CoA-Request carrying any Message-Authenticator value, forged or not, is accepted on a path that can disconnect a subscriber. Within minutes two peer sessions and an automated security review each independently reported it as a CRITICAL defect, and the main thread — having verified the code and the RFC, and having already told the agent once to fix it — concluded the agent had disabled a security guard twice after being told, and KILLED it. The agent's dying message was "Restored and clean. Now the third forced RED: removing the verification so the wrong-attribute test must redden." It was doing exactly what its brief demanded. Four independent readers reached the same wrong conclusion from correct observations, because a mutation in a shared tree is indistinguishable from residue: nothing in the file says "this line is temporary", and the comment above it correctly described the behaviour the mutation had just removed, which made the disagreement between code and comment read as the tell | not fixed, and the repair is a protocol change rather than a code edit. Three candidates, increasing in cost: the mutation runs in a throwaway `git worktree` so the shared tree never holds it; or the producer takes a test-only injection point so discrimination is proven without editing the guard at all; or the mutating agent writes a marker file naming the file, the line and the expected restore time, which every reviewer and peer checks before reporting. The second is the only one that makes the window not exist. The general practice this class already states, turned on the PROVER rather than the guard: the proof that a guard works runs the code without it, so the place that proof runs must be a place where running unguarded costs nothing -- and a checkout five sessions are committing from is not that place. Cost measured tonight: three false CRITICAL reports, one killed agent mid-protocol with its third red uncaptured, and a main thread that published a wrong account of its own agent's conduct before the evidence arrived |
| 2026-08-10 | session-bin-directory | build | mutation-testing a build recipe's path guard executed the unguarded path FOR REAL: the seed step reached the shared `bin/ze` and ran `ze init` against the operator's own `etc/ze` | the operator's database survived only because the recipe carries no `--force`, which is what calls `moveAsideDB`. The command a guard protects must be non-destructive on its own, because the proof that the guard works runs that command without it |
