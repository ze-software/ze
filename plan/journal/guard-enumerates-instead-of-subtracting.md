# Guard enumerates what matters instead of subtracting what does not

A guard that lists the fields it cares about is correct on the day it is written.
It is wrong on the day somebody adds a field. The new field is not on the list,
so it compares equal. The guard then answers "nothing changed" about a thing
that changed, nothing goes red, and the caller drops a correctly computed value
one line later and reports success.

The shape that survives is the opposite one. Compare the WHOLE derived set, then
SUBTRACT the members one decision proved it can ignore. A field nobody
classified lands on the conservative side by construction, which is the
fail-closed direction `ai/rules/evidence.md` asks for. Distrust any equality
helper, diff predicate, or header comparison whose body reads as a column of
field names.

| Date | Spec | Surface | Symptom | Fix |
|------|------|---------|---------|-----|
| 2026-09-02 | - | `heavyArea` (`internal/le/hookruntime/bash.go`), which decides whether an `le` command needs job admission and refuses a lossy pipe over it | The guard names five heavy areas: `verify`, `functional`, `integration`, `qemu` and `test-unit`. `test-chaos` runs three suites, one of them race-instrumented over `./internal/chaos/...`. It is on no list, so `./le test-chaos all` is admitted, and a pipe into `head` throws its output away with nothing said. The registry already knows which areas are heavy: every action carries `Writes` and a `Gate`, and `leroot.Register` files each area under a group. The guard reads none of it and repeats the answer as five string literals. An area added tomorrow is therefore unguarded by default, which is the fail-open direction. I found it when I changed the four suites so that a bare name lists. That change forced the same list to grow an exemption rather than a derivation | not fixed. The exemption for the two bare listings was added because it was the change in hand. The subtraction wants a heaviness fact declared once at registration and read here, and that is a separate piece of work |
| 2026-08-24 | fixit-ipsec-peer-reload-ignored | ipsec reload peer reconcile | The same shape as the 2026-08-13 row, one component over, and TWICE in one package. `peerConfigChanged` (`internal/component/ike/engine/reconcile.go`) named eight of the twelve members of `ipsec.SiteToSitePeer`, and `peersEqual` (`internal/component/ike/ipsec/types.go`), feeding `IPsecConfig.Changed`, named a DIFFERENT eight. `TrafficSelectors`, `Mode` and `TransportRequired` were in neither. An operator narrowed a live peer's traffic selectors to stop carrying a prefix, the commit succeeded, `show configuration` agreed, and the tunnel kept carrying it. The recurrence point beyond the BGP row: TWO field lists over one type is worse than one, because the two also disagree with each other, and neither author could see the other's omissions | fixed. `SiteToSitePeer.Equal` is `reflect.DeepEqual` over the whole value and is the ONE producer, which both call. `peersEqual` is deleted. `peerConfigChanged` also compares the RESOLVED `IKEGroup` and `ESPGroup`, because a peer holds their NAMES and none of the crypto, so a cipher rotation edits no peer block and was ignored too. `TestPeerConfigChangedIsFailClosed` walks `reflect.VisibleFields` so a member added tomorrow needs no memory of the test, `TestSiteToSitePeerEqualAcrossTwoParses` and `TestGroupsEqualWhateverOrderTheyArriveIn` hold the no-op-reload side, and `test/ipsec/ipsec-peer-reload-applies-selectors.ci` plus interop scenario peer-reload-narrowing prove it to the kernel and to strongSwan |
| 2026-08-13 | bgp-peer-settings-reload-ignored | bgp reload peer reconcile | `peerSettingsEqual` (`internal/component/bgp/reactor/reactor_api.go`) decided whether a peer changed on reload by comparing a hand-written list of about 15 of `PeerSettings`' roughly 50 fields. `ImportFilters`, `ExportFilters`, `RouteReflectorClient`, `ASOverride`, `RSClient`, `ClusterID`, `NextHopMode`, `MD5Key` and about 25 more were absent, and `StaticRoutes` was compared by length alone. A reload touching only those fields reported "functionally equivalent", so `reconcilePeersJournaled` put the peer in neither `toRemove` nor `toAdd` and dropped the freshly parsed settings. The datapath kept enforcing the old chain while `bgp peer` DISPLAYED the new one. The display reads the re-parsed config and the enforcement reads the running peer. `peerDiffCount` reported 0 changes. An operator tightening policy to block a prefix saw success and got the old permissive chain, which is the failure direction that looks like the safe one | fixed. Both replacement guards are SUBTRACTIONS from a derived set. `peerSettingsEqual` is `reflect.DeepEqual` over the whole struct minus `Capabilities`, which compare semantically by wire encoding. `peerSettingsSwapPlan` (`peer_settings_apply.go`) decides swap-or-restart as "not equal once one copier has neutralized the fields it delivers". A field added tomorrow therefore forces a restart until somebody classifies it on purpose. The plan also RETURNS that copier, so the set it judged deliverable is the set the apply site writes. `openHeaderEqual` (`peer_settings_negotiation.go`) got the same treatment over `message.Open`, and it is the sharper warning. Its hand-picked list was TOTAL over the struct when it was written, so a plain revert stays green and only a field escaping the list reproduces the failure. Proven by `TestPeerSettingsRestartRequiredIsFailClosed` over six unclassified fields, `TestOpenHeaderEqualCoversEveryOpenField`, and `test/reload/reload-import-policy-applies.ci`, which asserts the edited peer REJECTS the route while a control peer accepts it |
| 2026-08-22 | fixit-child-rekey-answer-vs-installed-selectors | ipsec reload peer reconcile | `peerConfigChanged` (`internal/component/ike/engine/reconcile.go`) decides whether an IPsec peer changed on reload by comparing eight named fields of `ipsec.SiteToSitePeer`: the two addresses, the two group names, the connection type and three `Auth` members. `TrafficSelectors`, `Mode`, `TransportRequired` and the rest are absent, so a reload that narrows a peer's traffic selectors puts that peer in neither the removal list nor the start list. That is half the defect. `startPeerSession` (same file) is the ONLY writer of `ps.peerCfg`, so a session the guard leaves alone also keeps the config it was born with. `sa.PeerCfg` is copied from it (`initiator.go`, `responder.go`), inherited across every rekey (`newRekeyedChild`, `rekey.go`), and proposed by `proposeChildTSPayloads` (`rekey.go`). A live tunnel therefore offers its startup selectors until the daemon restarts, and the operator's edit reaches no wire. The BGP row above is the same shape one component over. Found while writing AC-4 of the spec: RFC 7296 Section 2.9.2's case, where "the policy was changed in a way such that the currently used SA is against the policy", needs a peer whose proposal has since narrowed, and no Ze daemon can be one | not fixed. It wants the same subtraction the BGP fix used, plus the swap-or-restart decision that fix also had to make, and a live write to `sa.PeerCfg` races the peer goroutine that reads it. The functional test reaches the case through a test-only override instead (`narrowedRekeyPairs`, `internal/component/ike/engine/testport.go`) |
| 2026-08-30 | flowspec-states-its-own-grammar | `TestModelDeclaresEveryComponentKeyword` (`internal/component/bgp/plugins/nlri/flowspec/component_parity_test.go`), the guard on a deliberately created copy | The spec declared the FlowSpec component keywords a second time, in YANG, so an operator could read the grammar off `announce flowspec`. `isComponentKeyword` (`plugin_encode_text.go`) stays the producer, and AC-4 asked for a check that the copy cannot drift. The check enumerated. It walked the model's containers against a hand-written `encoderComponentKeywords` slice sitting BESIDE the function, so it compared copy two against copy three and never read the producer at all. A keyword added to `isComponentKeyword` alone appears in neither loop and the test passes: the one direction the guard exists for is the one it could not see, and it fails open toward "the model is complete" while an operator types a component the usage line never offers. The comment above it claimed the opposite, which is what let it survive its own author's review. The recurrence point past the four rows above: those enumerate FIELDS of a type the compiler knows, so `reflect.VisibleFields` subtracts for them. This one enumerates STRING CONSTANTS, where no reflection reaches and the honest subtraction is to read the producer's own source | fixed at closure, by the reviewer rather than the author. `encoderComponentKeywords` now READS the `// FlowSpec component keywords.` const block out of `plugin_encode_text.go`, so a constant added tomorrow arrives on the encoder side by itself and needs no memory of this test. Both loops then compare the model against the producer. Three mutations go red, the encoder-only one included, and it refuses to run at all when the marker comment or the block is gone rather than reporting an empty set as agreement |
| 2026-08-28 | - | `validateSpec` (`internal/le/hookruntime/lifecycle.go`), the Current Behavior check | The guard enumerates the source extensions it accepts, `go`, `sh`, `rs`, `ts`, `js` and `mk`, and refuses any spec whose Current Behavior lists only other kinds. A spec whose subject is YANG, a `.ci` or `.et` test, an RFC summary, or, since the 2026-08-28 interpreter cutover, a retired `.py` tool cannot satisfy it, and the message it prints, "Current Behavior section must list source files read", tells the author the section is empty when it is not. Measured on 2026-08-28: `plan/spec-interop-image-tag-race.md`, `plan/spec-interop-wire-capture.md`, `plan/spec-interop-suite-red.md` and `plan/spec-radius-admin-interop-freeradius.md` each carry a full Current Behavior list and each is refused, at HEAD and in the working tree alike | Not fixed. The subtracting shape is to accept any tracked path the spec names and refuse only the kinds a spec never reads, and the message must say which kinds it wanted |
