# Verification debt -- commit session 337457ee

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row with `make ze-verify-debt-clear`:
it re-runs the gate the row names and writes `cleared` only when that
run exits 0. Most of those gates judge the WORKING TREE, so a cleared
row says the gate was green in this checkout, other sessions'
uncommitted files included. It does not say the gate is green over the
commit alone: `discovery-index freshness` and
`ze-repository-tracked-build-check` are the two gates re-judged over
what git holds. A human MAY delete the
shard once every row is cleared.
`scripts/dev/commit_helper.py create --push` refuses while any row here
is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-19 | 337457ee | fix(vpp): a mirror destination that fails no longer abandons the rest | ze-precommit-verify (not FRESH-green) | Shared checkout. The last full run (started 13:16Z, exit 1 at 14:44Z) is red in stages this commit cannot reach: unit failures in bgp/config and scripts/dev, functional mismatches in bgp/prefix/rfc7606/reload/web/install/appliance, doc-wiring on bgp attribute and route files. No failure group names an iface or vpp path. Local evidence: make _ze-unit-pkg-test-impl PKG=./internal/plugins/iface/vpp/ RUN='TestRemoveMirror/TestCreateDummy' passed race-instrumented, and the new test was proven red against the early return. | open |
| 2026-08-19 | 337457ee | fix(vpp): a mirror destination that fails no longer abandons the rest | ze-precommit-verify structural gates (red) | The three unattributed structural reds belong to other sessions. ze-lint: its verify excerpt is 'parallel golangci-lint is running', and a whole-tree make ze-lint-changed in this session reports 8 issues, every one in scripts/evidence/l2tp-pppox-diag/main.go and scripts/evidence/l2tp-tunnel-diag/main.go, none in any file this commit carries. ze-staticcheck-feature-matrix-check: 'exceeded the 25m0s deadline; matrix could not be judged', a loaded-machine timeout. ze-generated-files-check: its ze-doc-index-check step reports one stale anchor, docs/architecture/api/process-protocol.md naming runHub in cmd/ze/hub/main.go, a file another session is editing. A Go change to mirror.go plus a test, a digest line, a spec and two journal rows cannot affect any of the three. | open |
| 2026-08-19 | 337457ee | fix(vpp): a mirror destination that fails no longer abandons the rest | full ze-precommit-verify over this commit's Go | The review-gate fix to mirror.go and apply_test.go was written after the 13:16Z full run started, so that run does not cover it, and no new full run could be started and watched to completion: this harness caps a foreground Bash call at 10 minutes against a 60 to 90 minute pass, and the detached route with a completion event was refused by the permission classifier. Reporting the cap rather than killing a run at the ceiling, per ai/rules/precommit-verify.md. Owed gate: a full make ze-precommit-verify over this commit's Go. | open |
| 2026-08-19 | 337457ee | fix(vpp): a mirror destination that fails no longer abandons the rest | owner approval for an RFC-tagged test change | Nobody approved an RFC-tagged change, because this commit changes none: grep 'RFC requirement:' over internal/plugins/iface/ returns no file, and the two Go files here are internal/plugins/iface/vpp/mirror.go and apply_test.go. The gate cannot run for a reason outside this commit: another session's in-flight edit to .claude/hooks/pretool-writeedit.py deleted _RFC_APPROVED while moving the approval from an in-file comment to a ledger, and commit_helper._rfc_hook_parts still requires that third symbol, so it returns None. Repairing their half-landed refactor from here would collide with the session that owns it. Reported to the main thread on 2026-08-19. | open |
| 2026-08-19 | 337457ee | plan: close spec-fixit-vpp-loopback-recreated-per-apply | ze-precommit-verify (not FRESH-green) | Markdown-only removal in a shared checkout whose verify record is red for other sessions' work; the same reasons as the preceding commit apply and nothing here compiles. | open |
| 2026-08-19 | 337457ee | plan: close spec-fixit-vpp-loopback-recreated-per-apply | ze-precommit-verify structural gates (red) | Same three unattributed reds as the preceding commit, all owned elsewhere: ze-lint (its 8 issues are in scripts/evidence/l2tp-pppox-diag/main.go and scripts/evidence/l2tp-tunnel-diag/main.go), ze-staticcheck-feature-matrix-check (25m deadline, matrix not judged), ze-generated-files-check (a stale anchor in docs/architecture/api/process-protocol.md naming runHub). This commit deletes one markdown file and can affect none of them. | open |
| 2026-08-19 | 337457ee | plan: close spec-fixit-no-family-block-sends-no-end-of-rib | ze-precommit-verify structural gates (red) | All three charged reds are attributed and none is this commit's; it carries five markdown files and no Go. ze-lint: 'Error: parallel golangci-lint is running' -- a concurrent session held golangci-lint's single-instance lock and no finding was emitted. ze-staticcheck-feature-matrix-check: 'Staticcheck exceeded the 25m0s deadline; matrix could not be judged' on a box running a dozen sessions. ze-generated-files-check: it fails through ze-doc-index-check on one source anchor, 'docs/architecture/api/process-protocol.md:312: source anchor cmd/ze/hub/main.go names runHub, which is not declared there'. That doc is M in git status, runHub exists nowhere under cmd/ze/hub/, and that directory holds two more M test files and one untracked one, so another session is mid-edit. Every other item in that stage reads current or up to date. I re-ran make ze-doc-index-check after my documentation edits: 2159 code paths checked, that same single claim is the only failure, ai/CODE-TO-DOCS.md is clean, and my two new anchors resolve. | open |
| 2026-08-19 | 337457ee | plan: remove spec-fixit-no-family-block-sends-no-end-of-rib | ze-precommit-verify structural gates (red) | This commit removes one markdown file and can affect no gate. The same three reds as the previous commit: ze-lint lost golangci-lint's single-instance lock to a concurrent session, ze-staticcheck-feature-matrix-check exceeded its 25m deadline on a loaded box, and ze-generated-files-check fails through ze-doc-index-check on a source anchor in another session's uncommitted docs/architecture/api/process-protocol.md naming runHub in cmd/ze/hub/main.go. | open |
