# Verification debt -- commit session 2d2bc99a

Gates that had not run green over these commits when they were made.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-04 | 2d2bc99a | docs(index): the package map follows the EAP package to core | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
| 2026-09-04 | 2d2bc99a | docs(index): the package map follows the EAP package to core | discovery-index freshness | The generated index is stale only for internal/le/interoplab/radius, whose radius.go is untracked in this shared checkout and belongs to another session. Regenerating in full would carry that in-flight package into a committed generated file. This commit corrects the one row that a committed package move (c54d97dcdb) left wrong and adds no package of its own. | open |
