# Verification debt -- commit session 7b84bb4b

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row with `make ze-verify-debt-clear`:
it re-runs the gate the row names and writes `cleared` only when that
run exits 0. Most of those gates judge the WORKING TREE, so a cleared
row says the gate was green in this checkout, other sessions'
uncommitted files included. It does not say the gate is green over the
commit alone: `discovery-index freshness` and
`ze-repository-tracked-build-check` are the two gates re-judged over
what git holds. A human MAY delete the
shard once every row is cleared.
`scripts/dev/commit_helper.py create --push` refuses while any row here
is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-19 | 7b84bb4b | fix(docs): name the function the hub shutdown anchor points at | ze-precommit-verify structural gates (red) | ze-doc-wiring-check and ze-generated-files-check were red ON THIS ANCHOR; this commit is the fix, and both gates exit 0 now. Only the verify record is stale. | open |
| 2026-08-19 | 7b84bb4b | plan: spec the streaming answer protocol | ze-precommit-verify structural gates (red) | Same stale verify record as commit a, whose anchor fix already turned both gates green. | open |
| 2026-08-19 | 7b84bb4b | feat(hooks): derive a spec's owed design docs instead of asking | ze-precommit-verify structural gates (red) | Same stale verify record as commit a, whose anchor fix already turned both gates green. | open |
| 2026-08-19 | 7b84bb4b | fix(docs): fill is live, only its overall way was removed | ze-precommit-verify structural gates (red) | This commit IS the fix for the doc-index red. make ze-doc-index-check now exits 0; only the verify record is stale. | open |
| 2026-08-19 | 7b84bb4b | feat(rpc): record answers on the plugin wire, negotiated per peer | ze-precommit-verify structural gates (red) | The verify record carries the doc-index red that 143d475e9 fixed. ze-doc-index-check and ze-generated-files-check both exit 0 now. | open |
| 2026-08-19 | 7b84bb4b | feat(rpc): record answers on the plugin wire, negotiated per peer | full ze-precommit-verify over this commit's Go | Scoped runs are green: ze-unit-pkg-test over ./pkg/plugin/... and ./internal/component/plugin/... both exit 0, race-instrumented. The owner declined a full verify earlier this session. | open |
| 2026-08-19 | 7b84bb4b | fix(cli): only the overall way of / fill is removed | ze-precommit-verify structural gates (red) | Same stale verify record as the previous commit, whose doc-index red 143d475e9 already fixed. | open |
| 2026-08-19 | 7b84bb4b | fix(cli): only the overall way of / fill is removed | full ze-precommit-verify over this commit's Go | ze-unit-pkg-test over ./internal/component/command/... is green apart from TestFirstNStopsTheGenerator, the deliberate phase-1 wiring test this spec turns green in phase 6. | open |
| 2026-08-19 | 7b84bb4b | plan: streaming answer protocol, phases 1 to 5 green | ze-precommit-verify structural gates (red) | Same stale verify record; ze-doc-index-check exits 0 after 143d475e9. | open |
| 2026-08-20 | 7b84bb4b | feat(cli): the pipe chain runs a record at a time | ze-precommit-verify structural gates (red) | ze-doc-wiring-check is red on ApplyPipesRecords having no production caller, which phase 7 wires, plus another session's ci-sleep count. No new red from this phase. | open |
| 2026-08-20 | 7b84bb4b | feat(cli): the pipe chain runs a record at a time | full ze-precommit-verify over this commit's Go | ze-unit-pkg-test over ./internal/component/command/... exits 0 race-instrumented, including the five phase-6 tests. The owner declined a full verify earlier this session. | open |
| 2026-08-20 | 7b84bb4b | plan: streaming answer protocol, phase 6 closes R-3, R-6 and R-10 | ze-precommit-verify structural gates (red) | Same stale verify record; ze-doc-index-check exits 0 after 143d475e9. | open |
| 2026-08-20 | 7b84bb4b | fix(test): pin the config dir so audit-config-commit ignores the dev database | ze-precommit-verify structural gates (red) | Same stale verify record; ze-doc-index-check exits 0 after 143d475e9. | open |
| 2026-08-20 | 7b84bb4b | fix(test): pin the config dir in audit-config-commit | ze-precommit-verify structural gates (red) | ze-doc-wiring-check is red on ApplyPipesRecords having no production caller, which phase 7 was to wire and is now blocked pending an owner ruling. ze-generated-files-check follows it. This commit changes one .ci and cannot affect either. | open |
| 2026-08-20 | 7b84bb4b | feat(rpc): type= on the head says how to read each record | ze-precommit-verify structural gates (red) | ze-doc-wiring-check wiring passes; the ci-sleep ratchet 76/75 is another session's and this commit cannot affect it. | open |
| 2026-08-20 | 7b84bb4b | feat(rpc): type= on the head says how to read each record | full ze-precommit-verify over this commit's Go | Scoped runs green race-instrumented over ./pkg/plugin/..., ./internal/component/plugin/... and ./internal/component/command/..., plus ze-build, ze-test-build and answer-not-negotiated on a live daemon. | open |
| 2026-08-20 | 7b84bb4b | plan: type= and the run-time streaming decision | ze-precommit-verify structural gates (red) | Same as the previous commit: wiring passes, the ci-sleep ratchet is another session's. | open |
