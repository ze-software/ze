# Verification debt -- commit session 7b84bb4b

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row with `./le commit debt-clear`:
it re-runs the gate the row names and writes `cleared` only when that
run exits 0. Most of those gates judge the WORKING TREE, so a cleared
row says the gate was green in this checkout, other sessions'
uncommitted files included. It does not say the gate is green over the
commit alone: `discovery-index freshness` and
`./le repository tracked-build check` are the two gates re-judged over
what git holds. A human MAY delete the
shard once every row is cleared.
the retired `scripts/dev/commit_helper.py create --push` (current producer: `internal/le/commit/prepare.go`) refuses while any row here
is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-19 | 7b84bb4b | fix(docs): name the function the hub shutdown anchor points at | ./le verify current mode full structural gates (red) | ./le doc wiring and ./le repository generated-check were red ON THIS ANCHOR; this commit is the fix, and both gates exit 0 now. Only the verify record is stale. | cleared |
| 2026-08-19 | 7b84bb4b | plan: spec the streaming answer protocol | ./le verify current mode full structural gates (red) | Same stale verify record as commit a, whose anchor fix already turned both gates green. | cleared |
| 2026-08-19 | 7b84bb4b | feat(hooks): derive a spec's owed design docs instead of asking | ./le verify current mode full structural gates (red) | Same stale verify record as commit a, whose anchor fix already turned both gates green. | cleared |
| 2026-08-19 | 7b84bb4b | fix(docs): fill is live, only its overall way was removed | ./le verify current mode full structural gates (red) | This commit IS the fix for the doc-index red. ./le docs-to-code index-check now exits 0; only the verify record is stale. | cleared |
| 2026-08-19 | 7b84bb4b | feat(rpc): record answers on the plugin wire, negotiated per peer | ./le verify current mode full structural gates (red) | The verify record carries the doc-index red that 143d475e9 fixed. ./le docs-to-code index-check and ./le repository generated-check both exit 0 now. | cleared |
| 2026-08-19 | 7b84bb4b | feat(rpc): record answers on the plugin wire, negotiated per peer | full ./le verify current mode full over this commit's Go | Scoped runs are green: ze-unit-pkg-test over ./pkg/plugin/... and ./internal/component/plugin/... both exit 0, race-instrumented. The owner declined a full verify earlier this session. | open |
| 2026-08-19 | 7b84bb4b | fix(cli): only the overall way of / fill is removed | ./le verify current mode full structural gates (red) | Same stale verify record as the previous commit, whose doc-index red 143d475e9 already fixed. | cleared |
| 2026-08-19 | 7b84bb4b | fix(cli): only the overall way of / fill is removed | full ./le verify current mode full over this commit's Go | ze-unit-pkg-test over ./internal/component/command/... is green apart from TestFirstNStopsTheGenerator, the deliberate phase-1 wiring test this spec turns green in phase 6. | open |
| 2026-08-19 | 7b84bb4b | plan: streaming answer protocol, phases 1 to 5 green | ./le verify current mode full structural gates (red) | Same stale verify record; ./le docs-to-code index-check exits 0 after 143d475e9. | cleared |
| 2026-08-20 | 7b84bb4b | feat(cli): the pipe chain runs a record at a time | ./le verify current mode full structural gates (red) | ./le doc wiring is red on ApplyPipesRecords having no production caller, which phase 7 wires, plus another session's ci-sleep count. No new red from this phase. | cleared |
| 2026-08-20 | 7b84bb4b | feat(cli): the pipe chain runs a record at a time | full ./le verify current mode full over this commit's Go | ze-unit-pkg-test over ./internal/component/command/... exits 0 race-instrumented, including the five phase-6 tests. The owner declined a full verify earlier this session. | open |
| 2026-08-20 | 7b84bb4b | plan: streaming answer protocol, phase 6 closes R-3, R-6 and R-10 | ./le verify current mode full structural gates (red) | Same stale verify record; ./le docs-to-code index-check exits 0 after 143d475e9. | cleared |
| 2026-08-20 | 7b84bb4b | fix(test): pin the config dir so audit-config-commit ignores the dev database | ./le verify current mode full structural gates (red) | Same stale verify record; ./le docs-to-code index-check exits 0 after 143d475e9. | cleared |
| 2026-08-20 | 7b84bb4b | fix(test): pin the config dir in audit-config-commit | ./le verify current mode full structural gates (red) | ./le doc wiring is red on ApplyPipesRecords having no production caller, which phase 7 was to wire and is now blocked pending an owner ruling. ./le repository generated-check follows it. This commit changes one .ci and cannot affect either. | cleared |
| 2026-08-20 | 7b84bb4b | feat(rpc): type= on the head says how to read each record | ./le verify current mode full structural gates (red) | ./le doc wiring wiring passes; the ci-sleep ratchet 76/75 is another session's and this commit cannot affect it. | cleared |
| 2026-08-20 | 7b84bb4b | feat(rpc): type= on the head says how to read each record | full ./le verify current mode full over this commit's Go | Scoped runs green race-instrumented over ./pkg/plugin/..., ./internal/component/plugin/... and ./internal/component/command/..., plus ze-build, ze-test-build and answer-not-negotiated on a live daemon. | open |
| 2026-08-20 | 7b84bb4b | plan: type= and the run-time streaming decision | ./le verify current mode full structural gates (red) | Same as the previous commit: wiring passes, the ci-sleep ratchet is another session's. | cleared |
| 2026-08-20 | 7b84bb4b | feat(cli): the operator sees the answer as it arrives | ./le verify current mode full structural gates (red) | ./le doc wiring exits 0 on this working tree: the ci-sleep ratchet is back to 75 of 75 and the discovery index is regenerated. | cleared |
| 2026-08-20 | 7b84bb4b | feat(cli): the operator sees the answer as it arrives | full ./le verify current mode full over this commit's Go | Green: ze-test bgp plugin --pattern answer- 5/5, ze-test ui --all 194/194, unit tests race-instrumented over the touched trees, plus ze-build and ze-test-build. A full verify is cleared for closure. | open |
| 2026-08-20 | 7b84bb4b | feat(rpc): an oversized record faults, the answer continues | ./le verify current mode full structural gates (red) | ./le doc wiring exits 0 on this tree; the doc-links reds are in other sessions' plan/ specs and this commit cannot affect them. | cleared |
| 2026-08-20 | 7b84bb4b | feat(rpc): an oversized record faults, the answer continues | full ./le verify current mode full over this commit's Go | Green: ./internal/component/plugin/... and ./pkg/plugin/... race-instrumented, ze-test bgp plugin --pattern answer- 5/5, ./le doc wiring 0. A full verify runs at closure. | open |
| 2026-08-20 | 7b84bb4b | docs(api): the answer protocol, where its design lives | ./le verify current mode full structural gates (red) | Same as the previous commit. | cleared |
| 2026-08-20 | 7b84bb4b | plan: streaming answer protocol, all eight phases green | ./le verify current mode full structural gates (red) | ./le doc wiring and ./le repository tracked-build check both exit 0 on this tree; the doc-links reds are in other sessions' plan/ specs. | cleared |
| 2026-08-20 | 7b84bb4b | fix(cli): wire the exec channel to the row generator | ./le verify current mode full (not FRESH-green) | ./le verify current mode full is IN FLIGHT over this exact tree, launched after the last Go write; 12 of 30 stages green, zero ENOSPC, stage 13 running. It has NOT passed. The owner asked for the commit now, so it lands on the started run rather than the finished one. | open |
| 2026-08-20 | 7b84bb4b | fix(cli): wire the exec channel to the row generator | ./le verify current mode full structural gates (red) | The record is STALE, not red: it is yesterday's run (2026-08-19T19:48:47Z, exit=1). Both named gates were re-run on this tree just now and each exits 0 -- ./le doc wiring prints 'Wiring/doc/inventory gates passed', ./le repository generated-check prints 'All generated files are up to date'. Logs: gate-wiring.log, gate-generated.log in the session scratch. | cleared |
| 2026-08-20 | 7b84bb4b | fix(cli): wire the exec channel to the row generator | full ./le verify current mode full over this commit's Go | Owner said 'commit' at 2026-08-20. The gate's condition is met and only its RECORD lags: a full ./le verify current mode full was launched over this exact tree AFTER the last Go write and is still running (12 of 30 stages green, stage 13). The status file it reads is yesterday's because the in-flight run has not written its record yet. It has NOT passed and I do not claim it has; the --unverified debt row owns that. | open |
| 2026-08-20 | 7b84bb4b | close: remove spec-streaming-answer-protocol | ./le verify current mode full (not FRESH-green) | Same in-flight full verify as the previous commit in this script. This commit carries no Go: it removes the spec file alone. | open |
| 2026-08-20 | 7b84bb4b | close: remove spec-streaming-answer-protocol | ./le verify current mode full structural gates (red) | Same stale record as the previous commit. Both named gates re-measured green on this tree (exit 0 each), and a git rm of one plan/ file cannot affect either. | cleared |
