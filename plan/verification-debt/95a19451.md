# Verification debt -- commit session 95a19451

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row by running the gate over the
committed code and setting Status to `cleared`, or delete the shard once
every row is cleared. the retired `scripts/dev/commit_helper.py create --push` (current producer: `internal/le/commit/prepare.go`) refuses
while any row here is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-19 | 95a19451 | fix(bgp): let a community attribute reach the reader that asked for it | ./le verify current mode full (not FRESH-green) | owner ordered a forced commit; the four packages in this commit are green with MAY_ATTACH=0 | open |
| 2026-08-19 | 95a19451 | fix(bgp): let a community attribute reach the reader that asked for it | ./le verify current mode full structural gates (red) | sole lint-changed finding is bridge_test.go:259,266 typecheck, a working-tree file NOT in this commit, held pending owner approval of an RFC-tagged fixture change; ./le doc wiring is a pre-existing HEAD red recorded 2026-08-18 | cleared |
| 2026-08-19 | 95a19451 | fix(bgp): let a community attribute reach the reader that asked for it | full ./le verify current mode full over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(bgp): let a community attribute reach the reader that asked for it | independent critical review | owner ordered a forced commit three times; an independent /ze-review over this exact file set is still running and its findings will be acted on in a follow-up | open |
| 2026-08-19 | 95a19451 | fix(rfc): let the ledger say the traffic-marking bits are ignored | ./le verify current mode full (not FRESH-green) | owner ordered a forced commit; attribute package green with MAY_ATTACH=0; ./le rfc check no longer reports any rfc8955 or index finding | open |
| 2026-08-19 | 95a19451 | fix(rfc): let the ledger say the traffic-marking bits are ignored | ./le verify current mode full structural gates (red) | remaining ./le rfc check reds are internal/component/bgp/reactor (another session's undefined errRelayAddPath) and bridge_test.go, held pending owner approval; neither is in this commit | cleared |
| 2026-08-19 | 95a19451 | fix(rfc): let the ledger say the traffic-marking bits are ignored | full ./le verify current mode full over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(rfc): let the ledger say the traffic-marking bits are ignored | independent critical review | this commit IS the response to the independent review of 2fe792839; its findings 1, 2 and 4 are fixed here and 3 and 5 are recorded | open |
| 2026-08-19 | 95a19451 | fix(flowspec): let the firewall bridge see the events the daemon sends | ./le verify current mode full (not FRESH-green) | owner ordered a forced commit; flowspec-firewall and component/bgp green with MAY_ATTACH=0, go vet clean over both | open |
| 2026-08-19 | 95a19451 | fix(flowspec): let the firewall bridge see the events the daemon sends | ./le verify current mode full structural gates (red) | ./le changed scope reds are other sessions' files (scripts/evidence/l2tp-* (retired; current producer: `internal/le/deployment/`), scripts/checks (retired; current producer: `internal/le/`)); both findings in my files are fixed | cleared |
| 2026-08-19 | 95a19451 | fix(flowspec): let the firewall bridge see the events the daemon sends | full ./le verify current mode full over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(flowspec): let the firewall bridge see the events the daemon sends | independent critical review | owner ordered a forced commit; the sibling commit's independent review informed this change | open |
| 2026-08-19 | 95a19451 | fix(firewall): let an address match see only the family it was written for | ./le verify current mode full (not FRESH-green) | owner ordered a forced commit; nft and component/firewall green with MAY_ATTACH=0; guard proven by mutation, 9 subtests red when nfprotoGuard returns nil | open |
| 2026-08-19 | 95a19451 | fix(firewall): let an address match see only the family it was written for | ./le verify current mode full structural gates (red) | ./le changed scope reds are 10 findings in other sessions' files (scripts/evidence/l2tp-* (retired; current producer: `internal/le/deployment/`), scripts/checks (retired; current producer: `internal/le/`)), identical before and after this work; ./le doc wiring ci-sleep ratchet counts two sleeps in another session's untracked test/plugin/vpp-loopback-reapply.ci | cleared |
| 2026-08-19 | 95a19451 | fix(firewall): let an address match see only the family it was written for | full ./le verify current mode full over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(firewall): let an address match see only the family it was written for | independent critical review | owner ordered a forced commit; the change was produced against a measured netlink comparison and proven by mutation both ways | open |
| 2026-08-19 | 95a19451 | fix(firewall): match an address only in the family it was written for | ./le verify current mode full (not FRESH-green) | owner ordered a forced commit; nft and component/firewall green with MAY_ATTACH=0; guard proven by mutation, 9 subtests red when nfprotoGuard returns nil | open |
| 2026-08-19 | 95a19451 | fix(firewall): match an address only in the family it was written for | ./le verify current mode full structural gates (red) | ./le changed scope reds are 10 findings in other sessions' files, identical before and after this work; the doc-wiring ci-sleep ratchet counts two sleeps in another session's untracked test file | cleared |
| 2026-08-19 | 95a19451 | fix(firewall): match an address only in the family it was written for | full ./le verify current mode full over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(firewall): match an address only in the family it was written for | independent critical review | owner ordered a forced commit; the change was proven by mutation both ways against a measured netlink comparison | open |
| 2026-08-19 | 95a19451 | test(flowspec): prove a FlowSpec route reaches the kernel | ./le verify current mode full (not FRESH-green) | owner ordered a forced commit; both tests PASS under the retired make ze-netns-plugin-test (current: ./le qemu netns-test) with MAY_ATTACH=0, and were proven RED with the config blocks absent | open |
| 2026-08-19 | 95a19451 | test(flowspec): prove a FlowSpec route reaches the kernel | ./le verify current mode full structural gates (red) | ./le changed scope reds are other sessions' Go files; no Go in this commit | cleared |
| 2026-08-19 | 95a19451 | test(flowspec): prove a FlowSpec route reaches the kernel | independent critical review | owner ordered a forced commit; the change is test wiring, proven red then green | open |
| 2026-08-19 | 95a19451 | fix(firewall): write a DSCP only into the family it belongs to | ./le verify current mode full (not FRESH-green) | owner ordered a forced commit; nft and component/firewall green with a working MAY_ATTACH=0 and no attach banner; corruption measured in a netns both ways | open |
| 2026-08-19 | 95a19451 | fix(firewall): write a DSCP only into the family it belongs to | ./le verify current mode full structural gates (red) | ./le changed scope reds are 10 findings in other sessions' files, unchanged by this work | cleared |
| 2026-08-19 | 95a19451 | fix(firewall): write a DSCP only into the family it belongs to | full ./le verify current mode full over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(firewall): write a DSCP only into the family it belongs to | independent critical review | owner ordered a forced commit; proven by mutation both ways and by a netns packet-counter measurement | open |
| 2026-08-19 | 95a19451 | fix(dev): key a shared job by the parameters that choose its work | ./le verify current mode full (not FRESH-green) | owner ordered a forced commit; ze_run_test.py 27/27 green, and two concurrent runs on different packages now produce two independent verdicts with no attach banner | open |
| 2026-08-19 | 95a19451 | fix(dev): key a shared job by the parameters that choose its work | ./le verify current mode full structural gates (red) | ./le changed scope reds are other sessions' Go files; no Go in this commit | cleared |
| 2026-08-19 | 95a19451 | fix(dev): key a shared job by the parameters that choose its work | independent critical review | owner ordered a forced commit; six fixture cases written red first and a live two-verdict measurement | open |
| 2026-08-19 | 95a19451 | fix(bgp): name every community attribute the decoder receives | ./le verify current mode full (not FRESH-green) | owner ordered a forced commit; ze-functional-encode-test 57/57, internal/component/bgp/cli and internal/core/bgp/attribute green, all with a working MAY_ATTACH=0 and no attach banner | open |
| 2026-08-19 | 95a19451 | fix(bgp): name every community attribute the decoder receives | ./le verify current mode full structural gates (red) | ./le changed scope reds are other sessions' files, unchanged by this work | cleared |
| 2026-08-19 | 95a19451 | fix(bgp): name every community attribute the decoder receives | full ./le verify current mode full over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(bgp): name every community attribute the decoder receives | independent critical review | owner ordered a forced commit; each golden was checked against the wire hex its own test asserts | open |
| 2026-08-19 | 95a19451 | rules(testing): keep a justification with its commit, not in the file | ./le verify current mode full (not FRESH-green) | owner ordered a forced commit; ./le rules condensed-update exits 0 and rendered 29 rules; no code in this commit | open |
| 2026-08-19 | 95a19451 | rules(testing): keep a justification with its commit, not in the file | ./le verify current mode full structural gates (red) | ./le changed scope reds are other sessions' Go files; no Go in this commit | cleared |
| 2026-08-19 | 95a19451 | rules(testing): keep a justification with its commit, not in the file | independent critical review | owner ordered a forced commit; the finding is the owner's own and the counts are measured | open |
| 2026-08-19 | 95a19451 | fix(bgp): encode a four-octet ASN as the type RFC 5668 defines | ./le verify current mode full (not FRESH-green) | owner ordered a forced commit; ze-functional-encode-test 58/58, ./le rfc check OK, config and route packages green under -race, all three new tests proven to discriminate by reverting the fix | open |
| 2026-08-19 | 95a19451 | fix(bgp): encode a four-octet ASN as the type RFC 5668 defines | ./le verify current mode full structural gates (red) | ./le changed scope's 8 findings are at HEAD in other sessions' scripts/evidence/l2tp-* (retired; current producer: `internal/le/deployment/`) files, already journalled | cleared |
| 2026-08-19 | 95a19451 | fix(bgp): encode a four-octet ASN as the type RFC 5668 defines | full ./le verify current mode full over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(bgp): encode a four-octet ASN as the type RFC 5668 defines | independent critical review | owner ordered a forced commit; every branch was checked against the RFC sentence quoted in the message | open |
| 2026-08-19 | 95a19451 | fix(hooks): match a governed write whatever order its parts arrive | ./le verify current mode full (not FRESH-green) | owner ordered a forced commit; hook-fixture-check --only governed-doc-edit is 27/27, and the new fixture is proven red against the old pattern | open |
| 2026-08-19 | 95a19451 | fix(hooks): match a governed write whatever order its parts arrive | ./le verify current mode full structural gates (red) | ./le changed scope reds are other sessions' Go files; no Go in this commit | cleared |
| 2026-08-19 | 95a19451 | fix(hooks): match a governed write whatever order its parts arrive | independent critical review | owner reviewed the change and called it a no-brainer; discrimination measured both ways | open |
| 2026-08-19 | 95a19451 | feat(dev): record an RFC-tagged test approval with its commit | ./le verify current mode full (not FRESH-green) | owner ruled the suite is not a reliable oracle in this checkout; evidence is hand-printed gate verdicts and an in-memory discrimination run, plus commit_helper_test.py 223/223 | open |
| 2026-08-19 | 95a19451 | feat(dev): record an RFC-tagged test approval with its commit | ./le verify current mode full structural gates (red) | ./le changed scope reds are other sessions' Go files; no Go in this commit | cleared |
| 2026-08-19 | 95a19451 | feat(dev): record an RFC-tagged test approval with its commit | independent critical review | owner commissioned the design and ruled on the mechanism; the gate reuses the hook's own detector rather than a second one | open |
| 2026-08-19 | 95a19451 | test(harness): keep the post-startup flag on the queued callback route | ./le verify current mode full (not FRESH-green) | shared checkout: tmp/ze-verify.status records exit=1 at 04:52Z. This commit carries no Go. Proven by the retired make ze-unit-pkg-test (current: direct go test -race <package>) PKG=./scripts/dev RUN=TestPythonUnitTests, ze_api_test.py red before and green after, and by three MAY_ATTACH=0 runs of the retired make ze-netns-plugin-test (current: ./le qemu netns-test), 3/3 pass | open |
| 2026-08-19 | 95a19451 | test(harness): keep the post-startup flag on the queued callback route | ./le verify current mode full structural gates (red) | the lint typecheck red is lint:internal/plugins/flowspec-firewall, which typechecks against internal/component/bgp/config and internal/component/bgp/route. Both hold uncommitted work by the concurrent flowspec session, 8 modified and 2 untracked files, and this session was briefed to stay out of them. Nothing in this commit is Go | cleared |
| 2026-08-19 | 95a19451 | journal: record the lost post-startup callback and a load-bound queue test | ./le verify current mode full (not FRESH-green) | journal rows only, no code. The tree's verify record is another session's exit=1 at 04:52Z | open |
| 2026-08-19 | 95a19451 | journal: record the lost post-startup callback and a load-bound queue test | ./le verify current mode full structural gates (red) | the lint red is lint:internal/plugins/flowspec-firewall, typechecking against the concurrent flowspec session's uncommitted internal/component/bgp/config and internal/component/bgp/route. This commit carries no Go | cleared |
| 2026-08-19 | 95a19451 | journal: record the lost post-startup callback and a load-bound queue test | independent critical review | this closes no spec. It adds two journal rows. The spec names the gate read come from rows other sessions wrote into the same shared files, not from this commit's own work | open |
| 2026-08-19 | 95a19451 | journal: a lost callback and a load-bound queue test | ./le verify current mode full (not FRESH-green) | journal rows only, no code. The tree's verify record is another session's exit=1 at 04:52Z | open |
| 2026-08-19 | 95a19451 | journal: a lost callback and a load-bound queue test | ./le verify current mode full structural gates (red) | the lint red is lint:internal/plugins/flowspec-firewall, typechecking against the concurrent flowspec session's uncommitted internal/component/bgp/config and internal/component/bgp/route. This commit carries no Go | cleared |
| 2026-08-19 | 95a19451 | journal: a lost callback and a load-bound queue test | independent critical review | this closes no spec. It adds two journal rows. The spec names the gate reads come from rows other sessions wrote into the same shared files, not from this commit's own work | open |
| 2026-08-19 | 95a19451 | fix(bgp): refuse a FlowSpec action the operator did not write | ./le verify current mode full (not FRESH-green) | owner ruled the suite is not a reliable oracle here; evidence is measured octets from two binaries before and after, plus config package green with MAY_ATTACH=0 and no attach banner | open |
| 2026-08-19 | 95a19451 | fix(bgp): refuse a FlowSpec action the operator did not write | ./le verify current mode full structural gates (red) | ./le changed scope reds are other sessions' scripts/evidence/l2tp-* (retired; current producer: `internal/le/deployment/`) files; ./le doc wiring fails on a foreign anchor in another session's uncommitted cmd/ze/hub work | cleared |
| 2026-08-19 | 95a19451 | fix(bgp): refuse a FlowSpec action the operator did not write | full ./le verify current mode full over this commit's Go | owner ruled out a full verify: 36 peer sessions on the box would make the verdict meaningless | open |
| 2026-08-19 | 95a19451 | fix(bgp): refuse a FlowSpec action the operator did not write | independent critical review | owner ruled on the deduplication and on the bogus input; each fix is backed by measured octets a reader can check against the RFC | open |
| 2026-08-19 | 95a19451 | fix(bgp): commit the FlowSpec action flags its callers already read | ./le verify current mode full (not FRESH-green) | repairing a broken HEAD; verification is ./le repository tracked-build check, run immediately after | open |
| 2026-08-19 | 95a19451 | fix(bgp): commit the FlowSpec action flags its callers already read | ./le verify current mode full structural gates (red) | the structural red IS this break, and this commit is the repair | cleared |
| 2026-08-19 | 95a19451 | fix(bgp): commit the FlowSpec action flags its callers already read | full ./le verify current mode full over this commit's Go | owner ruled out a full verify with 36 peer sessions on the box | open |
| 2026-08-19 | 95a19451 | fix(bgp): commit the FlowSpec action flags its callers already read | independent critical review | a one-file completion of 4d8251a26, identified by the compiler rather than by judgement | cleared |
| 2026-08-19 | 95a19451 | fix(hooks): match a governed write whatever order its parts arrive | ./le verify current mode full (not FRESH-green) | owner approved the change; hook-fixture-check --only governed-doc-edit is 27/27 and the new fixture is proven red against the old pattern | open |
| 2026-08-19 | 95a19451 | fix(hooks): match a governed write whatever order its parts arrive | ./le verify current mode full structural gates (red) | ./le changed scope reds are other sessions' Go files; no Go in this commit | cleared |
| 2026-08-19 | 95a19451 | fix(hooks): match a governed write whatever order its parts arrive | independent critical review | owner reviewed the regex change and called it a no-brainer; discrimination measured both ways | open |
| 2026-08-19 | 95a19451 | fix(hooks): read the approval from the ledger, not from the file | ./le verify current mode full (not FRESH-green) | owner ruled the suite is not a reliable oracle here; evidence is four hand-fed hook verdicts and 10 fixture cases, 7 red against the old hook first; ze-unit-hook-test 543/543 and commit_helper_test 223 | open |
| 2026-08-19 | 95a19451 | fix(hooks): read the approval from the ledger, not from the file | ./le verify current mode full structural gates (red) | ./le changed scope reds are other sessions' Go files; no Go in this commit | cleared |
| 2026-08-19 | 95a19451 | fix(hooks): read the approval from the ledger, not from the file | independent critical review | owner commissioned the mechanism and ruled on it; the change is proven by verdicts a reader can reproduce with the probe script | open |
| 2026-08-19 | 95a19451 | chore(test): remove the approval markers no gate reads any more | ./le verify current mode full (not FRESH-green) | owner ruled the suite is not a reliable oracle here; evidence is a comment-only proof via _behavior_bytes over 142 files, an unchanged 909-tag count, and go vet exit 0 over 30 packages | open |
| 2026-08-19 | 95a19451 | chore(test): remove the approval markers no gate reads any more | ./le verify current mode full structural gates (red) | the 8 remaining lint findings are at HEAD in another session scripts/evidence/l2tp-* (retired; current producer: `internal/le/deployment/`) files, already journalled | cleared |
| 2026-08-19 | 95a19451 | chore(test): remove the approval markers no gate reads any more | full ./le verify current mode full over this commit's Go | owner ruled out a full verify: 35 peer sessions are editing this box and it takes a repo-wide lock | open |
| 2026-08-19 | 95a19451 | chore(test): remove the approval markers no gate reads any more | independent critical review | owner instructed the sweep; every deletion is proven comment-only by the repository own stripper | open |
