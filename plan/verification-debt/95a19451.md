# Verification debt -- commit session 95a19451

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row by running the gate over the
committed code and setting Status to `cleared`, or delete the shard once
every row is cleared. `scripts/dev/commit_helper.py create --push` refuses
while any row here is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-19 | 95a19451 | fix(bgp): let a community attribute reach the reader that asked for it | ze-precommit-verify (not FRESH-green) | owner ordered a forced commit; the four packages in this commit are green with MAY_ATTACH=0 | open |
| 2026-08-19 | 95a19451 | fix(bgp): let a community attribute reach the reader that asked for it | ze-precommit-verify structural gates (red) | sole lint-changed finding is bridge_test.go:259,266 typecheck, a working-tree file NOT in this commit, held pending owner approval of an RFC-tagged fixture change; ze-doc-wiring-check is a pre-existing HEAD red recorded 2026-08-18 | open |
| 2026-08-19 | 95a19451 | fix(bgp): let a community attribute reach the reader that asked for it | full ze-precommit-verify over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(bgp): let a community attribute reach the reader that asked for it | independent critical review | owner ordered a forced commit three times; an independent /ze-review over this exact file set is still running and its findings will be acted on in a follow-up | open |
| 2026-08-19 | 95a19451 | fix(rfc): let the ledger say the traffic-marking bits are ignored | ze-precommit-verify (not FRESH-green) | owner ordered a forced commit; attribute package green with MAY_ATTACH=0; ze-rfc-check no longer reports any rfc8955 or index finding | open |
| 2026-08-19 | 95a19451 | fix(rfc): let the ledger say the traffic-marking bits are ignored | ze-precommit-verify structural gates (red) | remaining ze-rfc-check reds are internal/component/bgp/reactor (another session's undefined errRelayAddPath) and bridge_test.go, held pending owner approval; neither is in this commit | open |
| 2026-08-19 | 95a19451 | fix(rfc): let the ledger say the traffic-marking bits are ignored | full ze-precommit-verify over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(rfc): let the ledger say the traffic-marking bits are ignored | independent critical review | this commit IS the response to the independent review of 2fe792839; its findings 1, 2 and 4 are fixed here and 3 and 5 are recorded | open |
| 2026-08-19 | 95a19451 | fix(flowspec): let the firewall bridge see the events the daemon sends | ze-precommit-verify (not FRESH-green) | owner ordered a forced commit; flowspec-firewall and component/bgp green with MAY_ATTACH=0, go vet clean over both | open |
| 2026-08-19 | 95a19451 | fix(flowspec): let the firewall bridge see the events the daemon sends | ze-precommit-verify structural gates (red) | ze-lint-changed reds are other sessions' files (scripts/evidence/l2tp-*, scripts/checks); both findings in my files are fixed | open |
| 2026-08-19 | 95a19451 | fix(flowspec): let the firewall bridge see the events the daemon sends | full ze-precommit-verify over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(flowspec): let the firewall bridge see the events the daemon sends | independent critical review | owner ordered a forced commit; the sibling commit's independent review informed this change | open |
| 2026-08-19 | 95a19451 | fix(firewall): let an address match see only the family it was written for | ze-precommit-verify (not FRESH-green) | owner ordered a forced commit; nft and component/firewall green with MAY_ATTACH=0; guard proven by mutation, 9 subtests red when nfprotoGuard returns nil | open |
| 2026-08-19 | 95a19451 | fix(firewall): let an address match see only the family it was written for | ze-precommit-verify structural gates (red) | ze-lint-changed reds are 10 findings in other sessions' files (scripts/evidence/l2tp-*, scripts/checks), identical before and after this work; ze-doc-wiring-check ci-sleep ratchet counts two sleeps in another session's untracked test/plugin/vpp-loopback-reapply.ci | open |
| 2026-08-19 | 95a19451 | fix(firewall): let an address match see only the family it was written for | full ze-precommit-verify over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(firewall): let an address match see only the family it was written for | independent critical review | owner ordered a forced commit; the change was produced against a measured netlink comparison and proven by mutation both ways | open |
| 2026-08-19 | 95a19451 | fix(firewall): match an address only in the family it was written for | ze-precommit-verify (not FRESH-green) | owner ordered a forced commit; nft and component/firewall green with MAY_ATTACH=0; guard proven by mutation, 9 subtests red when nfprotoGuard returns nil | open |
| 2026-08-19 | 95a19451 | fix(firewall): match an address only in the family it was written for | ze-precommit-verify structural gates (red) | ze-lint-changed reds are 10 findings in other sessions' files, identical before and after this work; the doc-wiring ci-sleep ratchet counts two sleeps in another session's untracked test file | open |
| 2026-08-19 | 95a19451 | fix(firewall): match an address only in the family it was written for | full ze-precommit-verify over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(firewall): match an address only in the family it was written for | independent critical review | owner ordered a forced commit; the change was proven by mutation both ways against a measured netlink comparison | open |
| 2026-08-19 | 95a19451 | test(flowspec): prove a FlowSpec route reaches the kernel | ze-precommit-verify (not FRESH-green) | owner ordered a forced commit; both tests PASS under make ze-netns-plugin-test with MAY_ATTACH=0, and were proven RED with the config blocks absent | open |
| 2026-08-19 | 95a19451 | test(flowspec): prove a FlowSpec route reaches the kernel | ze-precommit-verify structural gates (red) | ze-lint-changed reds are other sessions' Go files; no Go in this commit | open |
| 2026-08-19 | 95a19451 | test(flowspec): prove a FlowSpec route reaches the kernel | independent critical review | owner ordered a forced commit; the change is test wiring, proven red then green | open |
| 2026-08-19 | 95a19451 | fix(firewall): write a DSCP only into the family it belongs to | ze-precommit-verify (not FRESH-green) | owner ordered a forced commit; nft and component/firewall green with a working MAY_ATTACH=0 and no attach banner; corruption measured in a netns both ways | open |
| 2026-08-19 | 95a19451 | fix(firewall): write a DSCP only into the family it belongs to | ze-precommit-verify structural gates (red) | ze-lint-changed reds are 10 findings in other sessions' files, unchanged by this work | open |
| 2026-08-19 | 95a19451 | fix(firewall): write a DSCP only into the family it belongs to | full ze-precommit-verify over this commit's Go | owner ordered a forced commit before a full verify could run | open |
| 2026-08-19 | 95a19451 | fix(firewall): write a DSCP only into the family it belongs to | independent critical review | owner ordered a forced commit; proven by mutation both ways and by a netns packet-counter measurement | open |
