# Verification debt -- commit session 0f48b34d

Gates that had not run green over these commits when they were made.
One row holds one gate and one reason, and covers every commit this
session made under it. `git log -- <this file>` names those commits.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-15 | 0f48b34d | fix(qemu): share a relocated tmp child with the guest | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=130, at 2026-09-05T17:25:15Z) | open |
| 2026-09-15 | 0f48b34d | fix(qemu): share a relocated tmp child with the guest (+1 more) | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-09-15 | 0f48b34d | probe: Don't Fragment mode, the error queue and the unprivileged socket | full native verification (not FRESH-green) | le verify worktree not run: this host's memory watchdog kills any run past about ten minutes beside the other sessions. The main thread ran the full lint (0 findings in these packages), the unit suite by groups plus the race recipe, the functional plugin suite plus the three capability-gated .ci as root, and QEMU under the runtime kernel 7.2 with the integration package ok (scratch logs lint-full.log, verify-test-unit-*.log, job-verify-unit-rest-*.log, verify-functional-plugin.log, verify-df-*.log, kernel-qemu-3.log, qemu-df-3.log, qemu-probe-int.log); closure re-ran go test -race over the three packages and the two DF .ci as root (close-pkgs.log, close-df-ci.log) | open |
