# Verification debt -- commit session 5ca02605

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row with `make ze-verify-debt-clear`:
it re-runs the gate the row names and writes `cleared` only when that
run exits 0. Most of those gates judge the WORKING TREE, so a cleared
row says the gate was green in this checkout, other sessions'
uncommitted files included. It does not say the gate is green over the
commit alone: `discovery-index freshness` and
`ze-repository-tracked-build-check` are the two gates re-judged over
what git holds. A human MAY delete the
shard once every row is cleared.
`scripts/dev/commit_helper.py create --push` refuses while any row here
is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-19 | 5ca02605 | fix(cli): guard the nil config completer ze cli always has | ze-precommit-verify structural gates (red) | reds name flowspec-firewall typecheck plus two whole stages, none of which this commit can reach; go vet on that package exits 0 now, so the record is stale against the current tree | open |
| 2026-08-19 | 5ca02605 | fix(cli): guard the nil config completer ze cli always has | full ze-precommit-verify over this commit's Go | owner is mid-rewrite of the test harness and it is deliberately broken tree-wide; owner ordered this commit | open |
| 2026-08-19 | 5ca02605 | refactor(cli): flatten bgp summary and rename prefix-summary | ze-precommit-verify structural gates (red) | reds name flowspec-firewall typecheck plus two whole stages; go vet on that package exits 0, so the record is stale and this commit cannot reach any of them | open |
| 2026-08-19 | 5ca02605 | refactor(cli): flatten bgp summary and rename prefix-summary | full ze-precommit-verify over this commit's Go | owner is mid-rewrite of the test harness and it is deliberately broken tree-wide; owner ordered this commit | open |
| 2026-08-19 | 5ca02605 | feat(cli): built-in column order plus display and fill operators | ze-precommit-verify structural gates (red) | reds name flowspec-firewall typecheck plus two whole stages; go vet on that package exits 0, so the record is stale and this commit cannot reach any of them | open |
| 2026-08-19 | 5ca02605 | feat(cli): built-in column order plus display and fill operators | full ze-precommit-verify over this commit's Go | owner is mid-rewrite of the test harness and it is deliberately broken tree-wide; owner ordered this commit | open |
| 2026-08-19 | 5ca02605 | feat(cli): pipe aliases, with summary and peers over show bgp | ze-precommit-verify structural gates (red) | reds name flowspec-firewall typecheck plus two whole stages; go vet on that package exits 0, so the record is stale and this commit cannot reach any of them | open |
| 2026-08-19 | 5ca02605 | feat(cli): pipe aliases, with summary and peers over show bgp | full ze-precommit-verify over this commit's Go | owner is mid-rewrite of the test harness and it is deliberately broken tree-wide; owner ordered this commit | open |
| 2026-08-19 | 5ca02605 | feat(cli): guard the daemon match, and make show bgp answer | ze-precommit-verify structural gates (red) | reds name flowspec-firewall typecheck plus two whole stages; go vet on that package exits 0, so the record is stale and this commit cannot reach any of them | open |
| 2026-08-19 | 5ca02605 | feat(cli): guard the daemon match, and make show bgp answer | full ze-precommit-verify over this commit's Go | owner is mid-rewrite of the test harness and it is deliberately broken tree-wide; owner ordered this commit | open |
| 2026-08-19 | 5ca02605 | feat(cli): guard the daemon match, and make show bgp answer | ze-precommit-verify structural gates (red) | reds name flowspec-firewall typecheck plus two whole stages; go vet on that package exits 0, so the record is stale and this commit cannot reach any of them | open |
| 2026-08-19 | 5ca02605 | feat(cli): guard the daemon match, and make show bgp answer | full ze-precommit-verify over this commit's Go | owner is mid-rewrite of the test harness and it is deliberately broken tree-wide; owner ordered this commit | open |
| 2026-08-19 | 5ca02605 | feat(cli): guard the daemon match, and make show bgp answer | ze-precommit-verify structural gates (red) | reds name flowspec-firewall typecheck plus two whole stages; go vet on that package exits 0, so the record is stale and this commit cannot reach any of them | open |
| 2026-08-19 | 5ca02605 | feat(cli): guard the daemon match, and make show bgp answer | full ze-precommit-verify over this commit's Go | owner is mid-rewrite of the test harness and it is deliberately broken tree-wide; owner ordered this commit | open |
| 2026-08-19 | 5ca02605 | fix(test): three ci assertions that were wrong about their subject | ze-precommit-verify structural gates (red) | reds name flowspec-firewall typecheck plus two whole stages; go vet on that package exits 0, the record is stale, and this commit carries no Go | open |
| 2026-08-19 | 5ca02605 | fix(test): three ci assertions that were wrong about their subject | full ze-precommit-verify over this commit's Go | owner is mid-rewrite of the test harness; owner ordered these commits | open |
| 2026-08-19 | 5ca02605 | docs(demo): show display, fill and the peers alias in quickstart | ze-precommit-verify structural gates (red) | reds name flowspec-firewall typecheck plus two whole stages; go vet exits 0 there, the record is stale, and this commit carries no Go | open |
| 2026-08-19 | 5ca02605 | docs(demo): show display, fill and the peers alias in quickstart | full ze-precommit-verify over this commit's Go | owner is mid-rewrite of the test harness; owner ordered these commits | open |
| 2026-08-19 | 5ca02605 | fix(yang): agree the show bgp description across both modules | ze-precommit-verify structural gates (red) | reds name flowspec-firewall typecheck plus two whole stages; go vet exits 0 there and this commit carries no Go | open |
| 2026-08-19 | 5ca02605 | fix(yang): agree the show bgp description across both modules | full ze-precommit-verify over this commit's Go | owner is mid-rewrite of the test harness; owner ordered these commits | open |
| 2026-08-19 | 5ca02605 | close: cli column order renders operator-first, not alphabetical | ze-precommit-verify (not FRESH-green) | closure commit carries no Go; the owner is mid-rewrite of the test harness and the tree is deliberately red tree-wide | open |
| 2026-08-19 | 5ca02605 | close: cli column order renders operator-first, not alphabetical | ze-precommit-verify structural gates (red) | the recorded reds name flowspec-firewall typecheck plus two whole stages; this commit carries two markdown files and can reach none of them | open |
| 2026-08-19 | 5ca02605 | close: cli column order renders operator-first, not alphabetical | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has run since the harness rewrite began; this commit carries no Go | open |
| 2026-08-19 | 5ca02605 | close: cli column order renders operator-first, not alphabetical | ze-precommit-verify (not FRESH-green) | closure commit carries no Go; the owner is mid-rewrite of the test harness and the tree is deliberately red tree-wide | open |
| 2026-08-19 | 5ca02605 | close: cli column order renders operator-first, not alphabetical | ze-precommit-verify structural gates (red) | the recorded reds name flowspec-firewall typecheck plus two whole stages; this commit carries two markdown files and can reach none of them | open |
| 2026-08-19 | 5ca02605 | close: cli column order renders operator-first, not alphabetical | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has run since the harness rewrite began; this commit carries no Go | open |
| 2026-08-19 | 5ca02605 | close: remove spec-cli-column-order | ze-precommit-verify (not FRESH-green) | closure commit carries no Go; the owner is mid-rewrite of the test harness and the tree is deliberately red tree-wide | open |
| 2026-08-19 | 5ca02605 | close: remove spec-cli-column-order | ze-precommit-verify structural gates (red) | the recorded reds name flowspec-firewall typecheck plus two whole stages; this commit removes one markdown file and can reach none of them | open |
| 2026-08-19 | 5ca02605 | close: remove spec-cli-column-order | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has run since the harness rewrite began; this commit carries no Go | open |
| 2026-08-19 | 5ca02605 | close: cli display and fill choose an answer's columns | ze-precommit-verify (not FRESH-green) | make ze-precommit-verify has not run and must not: the owner is mid-rewrite of the test harness and the tree is deliberately broken there. This commit carries markdown only, no Go and no build input. | open |
| 2026-08-19 | 5ca02605 | close: cli display and fill choose an answer's columns | ze-precommit-verify structural gates (red) | ze-repository-check and audit-test-relaxation.py are red on the owner's UNCOMMITTED harness rewrite (config/validators.go, plugin/process/manager.go, test/peer/checker.go, scripts/dev/changed_pkgs_test.go, scripts/status/verify_run_test.go). This commit touches none of them. Its one hit in a file this spec edited, ApplyJSON in internal/component/command/pipe.go, is pre-existing: git grep for command.ApplyJSON is empty at 53e84d473~, at 80c9fbcf7~ and at HEAD. | open |
| 2026-08-19 | 5ca02605 | close: remove spec-cli-order-pipe | ze-precommit-verify (not FRESH-green) | Same run as commit A: ze-precommit-verify has not run and must not while the owner rewrites the test harness. This commit removes one markdown file. | open |
| 2026-08-19 | 5ca02605 | close: remove spec-cli-order-pipe | ze-precommit-verify structural gates (red) | Same reds as commit A, all on the owner's uncommitted harness rewrite. Removing a spec file cannot affect them. | open |
| 2026-08-19 | 5ca02605 | docs(cli): the dispatcher guard and show bgp as a command | ze-precommit-verify structural gates (red) | reds name flowspec-firewall typecheck plus two whole stages and the owner uncommitted harness files; this commit carries documentation only | open |
| 2026-08-19 | 5ca02605 | docs(cli): the dispatcher guard and show bgp as a command | full ze-precommit-verify over this commit's Go | owner is mid-rewrite of the test harness; owner ordered these commits | open |
