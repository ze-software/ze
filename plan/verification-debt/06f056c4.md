# Verification debt -- commit session 06f056c4

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row by running the gate over the
committed code and setting Status to `cleared`, or delete the shard once
every row is cleared. `scripts/dev/commit_helper.py create --push` refuses
while any row here is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-18 | 06f056c4 | fix(site): make dark page titles green/blue and selectable | ze-precommit-verify structural gates (red) | ze-doc-wiring-check is red for another session's in-flight ai/rules/points/rfc-compliance retirement (10 points vanished with no RETIRED.md rows); this commit changes only website CSS, which no verify stage reads, so it cannot affect that red | open |
| 2026-08-18 | 06f056c4 | fix(site): follow the interop test rename in the lab pages | ze-precommit-verify structural gates (red) | ze-doc-wiring-check is red for another session's in-flight ai/rules/points/rfc-compliance retirement; this commit changes only website HTML, which no verify stage reads, so it cannot affect that red | open |
| 2026-08-18 | 06f056c4 | fix(commit-helper): treat a JSON-escaped quote as data | ze-precommit-verify structural gates (red) | ze-doc-wiring-check is red for another session's in-flight ai/rules/points/rfc-compliance retirement (10 points gone with no RETIRED.md rows); this commit touches scripts/dev/commit_helper.py and its test only, so it cannot affect that red | open |
| 2026-08-18 | 06f056c4 | fix(site): publish only the pages a build actually changed | ze-precommit-verify structural gates (red) | ze-doc-wiring-check is red for another session's in-flight ai/rules/points/rfc-compliance retirement; this commit changes the website generator and its tests only, which no verify stage reads, so it cannot affect that red | open |
| 2026-08-18 | 06f056c4 | fix(site): stop the theme toggle tilt sticking after a tap | ze-precommit-verify structural gates (red) | ze-doc-wiring-check is red for another session's in-flight ai/rules/points/rfc-compliance retirement; this commit changes website CSS only, which no verify stage reads, so it cannot affect that red | open |
| 2026-08-18 | 06f056c4 | fix(site): give every section title the page-title gradient | ze-precommit-verify structural gates (red) | ze-doc-wiring-check is red for another session's in-flight ai/rules/points/rfc-compliance retirement; this commit changes website CSS and one journal row, which no verify stage reads, so it cannot affect that red | open |
| 2026-08-18 | 06f056c4 | fix(demos): open on the poster and show the init input | ze-precommit-verify structural gates (red) | ze-doc-wiring-check is red for another session's in-flight ai/rules/points/rfc-compliance retirement; this commit changes VHS tapes and one transcript, which no verify stage reads, so it cannot affect that red | open |
| 2026-08-18 | 06f056c4 | fix(cli): honor the configured output format on every surface | ze-precommit-verify structural gates (red) | ze-doc-wiring-check is now exit 0 after I converted the ci-sleep poll reddening it for every session; remaining 20:38 reds are another session's ai/rules/points work plus a run that overlapped five agents' edits | open |
| 2026-08-18 | 06f056c4 | fix(cli): honor the configured output format on every surface | full ze-precommit-verify over this commit's Go | no full run can cover this tree: five sessions share the checkout, the only recent full run stamped a tree its early stages never saw (plan/journal/stale-artifact-reused.md), and other sessions are committing now. Scoped equivalents over a still tree: go build ./... exit 0, staticcheck-feature-matrix 38 rows, plugin/server exit 0, web full 516s exit 0, functional-ui 181/181 | open |
| 2026-08-18 | 06f056c4 | fix(aaa): revoke a break-glass session on credentials, not on reload | ze-precommit-verify structural gates (red) | remaining 20:38 reds are another session's ai/rules/points work; ze-doc-wiring-check and ze-test-weakened-check are exit 0 as of the previous commit | open |
| 2026-08-18 | 06f056c4 | fix(aaa): revoke a break-glass session on credentials, not on reload | full ze-precommit-verify over this commit's Go | five sessions share this checkout and the only recent full run stamped a tree its early stages never saw (plan/journal/stale-artifact-reused.md); scoped over a still tree: go build ./... exit 0, cmd/ze/hub exit 0, tracked-build OK on the previous commit, functional-ui green | open |
