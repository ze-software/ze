# Verification debt -- commit session 06f056c4

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row by running the gate over the
committed code and setting Status to `cleared`, or delete the shard once
every row is cleared. the retired `scripts/dev/commit_helper.py create --push` (current producer: `internal/le/commit/prepare.go`) refuses
while any row here is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-18 | 06f056c4 | fix(site): make dark page titles green/blue and selectable | ./le verify current mode full structural gates (red) | ./le doc-wiring is red for another session's in-flight ai/rules/points/rfc-compliance retirement (10 points vanished with no RETIRED.md rows); this commit changes only website CSS, which no verify stage reads, so it cannot affect that red | cleared |
| 2026-08-18 | 06f056c4 | fix(site): follow the interop test rename in the lab pages | ./le verify current mode full structural gates (red) | ./le doc-wiring is red for another session's in-flight ai/rules/points/rfc-compliance retirement; this commit changes only website HTML, which no verify stage reads, so it cannot affect that red | cleared |
| 2026-08-18 | 06f056c4 | fix(commit-helper): treat a JSON-escaped quote as data | ./le verify current mode full structural gates (red) | ./le doc-wiring is red for another session's in-flight ai/rules/points/rfc-compliance retirement (10 points gone with no RETIRED.md rows); this commit touches scripts/dev/commit_helper.py (retired; current producer: `internal/le/commit/prepare.go`) and its test only, so it cannot affect that red | cleared |
| 2026-08-18 | 06f056c4 | fix(site): publish only the pages a build actually changed | ./le verify current mode full structural gates (red) | ./le doc-wiring is red for another session's in-flight ai/rules/points/rfc-compliance retirement; this commit changes the website generator and its tests only, which no verify stage reads, so it cannot affect that red | cleared |
| 2026-08-18 | 06f056c4 | fix(site): stop the theme toggle tilt sticking after a tap | ./le verify current mode full structural gates (red) | ./le doc-wiring is red for another session's in-flight ai/rules/points/rfc-compliance retirement; this commit changes website CSS only, which no verify stage reads, so it cannot affect that red | cleared |
| 2026-08-18 | 06f056c4 | fix(site): give every section title the page-title gradient | ./le verify current mode full structural gates (red) | ./le doc-wiring is red for another session's in-flight ai/rules/points/rfc-compliance retirement; this commit changes website CSS and one journal row, which no verify stage reads, so it cannot affect that red | cleared |
| 2026-08-18 | 06f056c4 | fix(demos): open on the poster and show the init input | ./le verify current mode full structural gates (red) | ./le doc-wiring is red for another session's in-flight ai/rules/points/rfc-compliance retirement; this commit changes VHS tapes and one transcript, which no verify stage reads, so it cannot affect that red | cleared |
| 2026-08-18 | 06f056c4 | fix(cli): honor the configured output format on every surface | ./le verify current mode full structural gates (red) | ./le doc-wiring is now exit 0 after I converted the ci-sleep poll reddening it for every session; remaining 20:38 reds are another session's ai/rules/points work plus a run that overlapped five agents' edits | cleared |
| 2026-08-18 | 06f056c4 | fix(cli): honor the configured output format on every surface | full ./le verify current mode full over this commit's Go | no full run can cover this tree: five sessions share the checkout, the only recent full run stamped a tree its early stages never saw (plan/journal/stale-artifact-reused.md), and other sessions are committing now. Scoped equivalents over a still tree: go build ./... exit 0, staticcheck-feature-matrix 38 rows, plugin/server exit 0, web full 516s exit 0, functional-ui 181/181 | open |
| 2026-08-18 | 06f056c4 | fix(aaa): revoke a break-glass session on credentials, not on reload | ./le verify current mode full structural gates (red) | remaining 20:38 reds are another session's ai/rules/points work; ./le doc-wiring and ./le test-weakened check are exit 0 as of the previous commit | cleared |
| 2026-08-18 | 06f056c4 | fix(aaa): revoke a break-glass session on credentials, not on reload | full ./le verify current mode full over this commit's Go | five sessions share this checkout and the only recent full run stamped a tree its early stages never saw (plan/journal/stale-artifact-reused.md); scoped over a still tree: go build ./... exit 0, cmd/ze/hub exit 0, tracked-build OK on the previous commit, functional-ui green | open |
| 2026-08-18 | 06f056c4 | feat(cli): make every command answer with structured data | ./le verify current mode full structural gates (red) | remaining 20:38 reds are another session's ai/rules/points work; ./le doc-wiring and ./le test-weakened check are exit 0, and tracked-build is OK on both commits landed this session | cleared |
| 2026-08-18 | 06f056c4 | feat(cli): make every command answer with structured data | full ./le verify current mode full over this commit's Go | five sessions share this checkout and the only recent full run stamped a tree its early stages never saw (plan/journal/stale-artifact-reused.md); scoped over a still tree: go build ./... exit 0, plugin and web packages green, web full 516s exit 0, staticcheck-feature-matrix 38 rows, tracked-build OK | open |
| 2026-08-19 | 06f056c4 | fix(demos): assert the rendering a demo asks for | ./le verify current mode full structural gates (red) | ./le doc-wiring and ./le test-weakened check are exit 0; remaining 20:38 reds are another session's ai/rules/points work | cleared |
| 2026-08-19 | 06f056c4 | fix(demos): assert the rendering a demo asks for | full ./le verify current mode full over this commit's Go | this commit carries no Go: shell validators, one VHS tape and one transcript. Individually validated green; the aggregate render is blocked on machine load, not on this change | open |
| 2026-08-19 | 06f056c4 | fix(demos): make eight validator assertions able to fail | ./le verify current mode full structural gates (red) | ./le doc-wiring and ./le test-weakened check are exit 0; remaining reds are another session's ai/rules/points work | cleared |
| 2026-08-19 | 06f056c4 | fix(demos): make eight validator assertions able to fail | full ./le verify current mode full over this commit's Go | carries no Go: shell validators and one Python PTY driver. Each demo validated green individually; the aggregate render is blocked on machine load rather than on this change | open |
| 2026-08-19 | 06f056c4 | fix(demos): budget the screen for the raw JSON the quickstart shows | ./le verify current mode full structural gates (red) | ./le doc-wiring and ./le test-weakened check are exit 0; remaining reds are another session's ai/rules/points work | cleared |
| 2026-08-19 | 06f056c4 | fix(demos): budget the screen for the raw JSON the quickstart shows | full ./le verify current mode full over this commit's Go | carries no Go: one VHS tape and its transcript | open |
| 2026-08-19 | 06f056c4 | fix(demos): re-record every demonstration and measure its length | ./le verify current mode full structural gates (red) | ./le doc-wiring and ./le test-weakened check are exit 0; remaining reds are another session's ai/rules/points work | cleared |
| 2026-08-19 | 06f056c4 | fix(demos): re-record every demonstration and measure its length | full ./le verify current mode full over this commit's Go | carries no Go: two VHS tapes, two transcripts and the artifact manifest | open |
| 2026-08-19 | 06f056c4 | fix(commit-helper): do not scan a repo that cannot record a deferral | ./le verify current mode full structural gates (red) | ./le doc-wiring and ./le test-weakened check are exit 0; remaining reds are another session's ai/rules/points work | cleared |
| 2026-08-19 | 06f056c4 | fix(commit-helper): do not scan a repo that cannot record a deferral | full ./le verify current mode full over this commit's Go | Python only, no Go. 207 commit-helper tests pass; every Go commit tonight had tracked-build OK | open |
| 2026-08-19 | 06f056c4 | docs(spec): record the three suites A-1 was waiting on | ./le verify current mode full structural gates (red) | ./le doc-wiring and ./le test-weakened check are exit 0; remaining reds are another session's ai/rules/points work | cleared |
| 2026-08-19 | 06f056c4 | docs(spec): record the three suites A-1 was waiting on | full ./le verify current mode full over this commit's Go | one spec file, no code | open |
