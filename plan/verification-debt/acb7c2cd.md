# Verification debt -- commit session acb7c2cd

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row with `./le commit debt-clear`:
it re-runs the gate the row names and writes `cleared` only when that
run exits 0. Every gate runs inside one throwaway worktree at HEAD, so
a cleared row says the gate was green over the COMMIT rather than
beside the uncommitted files several sessions keep in this checkout.
When no worktree can be made, nothing clears and the pass exits 1: the
fallback it refuses, judging the working tree, is the whole reason the
worktree is there. A human MAY delete the
shard once every row is cleared.
the retired `scripts/dev/commit_helper.py create --push` (current producer: `internal/le/commit/prepare.go`) refuses while any row here
is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-22 | acb7c2cd | fix(commit-helper): read a closure signal as the committing session's own (+61 more) | ./le verify current mode full (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
| 2026-08-22 | acb7c2cd | test(tools): the go twin asserts the refusal the code emits (+26 more) | full ./le verify current mode full over this commit's Go | no full ./le verify current mode full recorded (tmp/ze-verify-full.json is missing) | open |
| 2026-08-23 | acb7c2cd | fix(plugin): every spawn clears the stop re-entry guard | ./le verify current mode full (not FRESH-green) | concurrent-session interference in a shared checkout. ./le verify current mode changed reached stage 13 of 27 with two reds, and neither is in this commit's files: ./le changed scope fails on internal/component/firewall/plugins/irr/irr_test.go with 'undefined: assertTermSets', and ./le fs-persistence check fails on an os.OpenFile of a loose ze-dhcpmu-hold log under the system temp directory in internal/component/iface/register.go. Both files are another session's uncommitted work. This commit's own packages lint clean: ./le changed scope standalone reported '0 issues.' on both the host and the GOOS=linux pass. ./le tier check, ze-iface-resolution-check, ./le plugin boundary check, ./le rfc check, ./le config coercion check, ./le dash-stdio check, ./le port-defaults check, ./le config claims, ./le test-sensitivity check and ./le test-weakened check all exit 0. The full gate over this commit is owed and stays owed; nothing is pushed. | open |
| 2026-08-23 | acb7c2cd | close: the ipsec lab renders the CLI account for every scenario | ./le verify current mode full (not FRESH-green) | This commit carries markdown, one Python test file and one Python scenario file: no .go, go.mod, go.sum or vendor path, so no full ./le verify current mode full coverage is owed. verify-status reports STALE (never verified in this checkout). Scoped gates green: lab_test.py 29/29, scenario 10 live against strongSwan, audit-test-relaxation clean, ./le spec citation anchors OK. ./le repository check reports 5 unwired-export findings and all five are in internal/component/command (answer_shape.go untracked, pipe.go modified), another session's in-flight Go work that this commit does not touch. | open |
| 2026-08-23 | acb7c2cd | close: remove spec-fixit-ipsec-interop-cli-credentials (+1 more) | ./le verify current mode full (not FRESH-green) | Removes one markdown file and carries no code. The previous commit's reason applies unchanged. | open |
| 2026-08-23 | acb7c2cd | plan: close spec-fixit-fwdpool-backpressure-timing | ./le verify current mode full (not FRESH-green) | This commit carries four markdown files and this shard: no .go, go.mod, go.sum or vendor path, so no full ./le verify current mode full coverage is owed for it. verify-status reports STALE, never verified in this checkout. The spec's code landed in c08237575 and was re-verified for this closure rather than assumed. A race-built binary of internal/component/bgp/reactor ran the two subject tests 860 times under 32 CPU burners on 16 cores at GOMAXPROCS=2 with 0 failures, against 4 failures over the same 860 pre-fix executions, and 4000 further executions at GOMAXPROCS=4 were clean. A go test -overlay mutation of fwdPool.Dispatch reds both tests in 1.00s. ./le spec citation anchors exits 0, and audit-test-relaxation.py over c08237575~1 reports 13 RFC-tagged findings none of which names a reactor forward-pool file. the retired make ze-unit-reactor-test-race (current: go test -race ./internal/component/bgp/reactor/...) is RED at HEAD and this commit is not the cause: TestBGPRemovedAtReloadLeavesTheHubPluginServerRunning fails on every iteration after the first with 'registry: duplicate plugin name', and the run then exceeded its own 20m budget at about 120s an iteration. Both are recorded in plan/journal/registry-contamination.md and plan/journal/gate-verdict-depends-on-the-machine.md. Across the 10 iterations that completed, neither subject test failed once. ./le doc check links is red for 15 findings in another session's plan/immediate/spec-cli-pipe-operator-coverage.md <!-- doc-links: ignore (a record of a state that existed on 2026-08-23; the spec closed and the path cannot resolve, and repointing it would falsify what this row observed) -->, which this commit does not touch. | open |
| 2026-08-23 | acb7c2cd | test(reactor): the ownership test restores the registry it wrote | ./le verify current mode full (not FRESH-green) | verify-status reports STALE, never verified in this checkout. The one Go file here is test-only and was proven both ways at -count=20 in one process: pre-fix reds 19 of 20 iterations, fixed passes 20 of 20, with the overlay verified to carry the pre-fix text by grep count. ./le changed scope found no verify-scope record so it fell back to ./... and reported 4 issues, none in this commit's file: 2 gocritic in internal/component/bgp/wireu/mpwire_test.go, 1 unparam in internal/component/bgp/plugins/adj_rib_in/nlri_hex_test.go, 1 wastedassign in internal/component/bgp/plugins/adj_rib_in/rib.go. All three files are another session's in-flight work. | open |
| 2026-08-23 | acb7c2cd | test(reactor): the ownership test restores the registry it wrote | full ./le verify current mode full over this commit's Go | no full ./le verify current mode full recorded (tmp/ze-verify-full.json is missing). The commit's only Go file is a _test.go in internal/component/bgp/reactor and adds three statements to a test helper. the retired make ze-unit-reactor-test-race (current: go test -race ./internal/component/bgp/reactor/...), the gate that owns this package, is red for a second reason this commit does not address: the package costs about 120s an iteration and -count=20 does not fit the 20m timeout GO_TEST_RACE carries for the whole binary, recorded in plan/journal/gate-verdict-depends-on-the-machine.md. | open |
| 2026-08-23 | acb7c2cd | fix(bgp): bound an attribute write to the width it declares | discovery-index freshness | ai/PACKAGE-MAP.md regenerates from the whole working tree, and ten concurrent agents hold uncommitted sources; regenerating here would carry their in-flight files into this commit. Owed once the tree quiesces. | cleared |
| 2026-08-23 | acb7c2cd | fix(rib): the commit rail asks before it encodes a next hop (+1 more) | discovery-index freshness | ai/PACKAGE-MAP.md regenerates from the whole working tree, and several concurrent agents hold uncommitted sources; regenerating here would carry their in-flight files into this commit. | cleared |
| 2026-08-23 | acb7c2cd | fix(ipsec): make a peer reload reach the running tunnels (+1 more) | discovery-index freshness | ai/PACKAGE-MAP.md regenerates from the whole working tree, and concurrent agents hold uncommitted sources. | cleared |
| 2026-08-23 | acb7c2cd | fix(ipsec): stage the config with the parser that has no side effects | discovery-index freshness | ai/PACKAGE-MAP.md regenerates from the whole working tree, and several concurrent agents hold uncommitted sources. | cleared |
| 2026-08-23 | acb7c2cd | fix(lint): clear six findings the full gate caught in today's commits (+1 more) | discovery-index freshness | ai/PACKAGE-MAP.md regenerates from the whole working tree, and five specs' uncommitted work is still in it. | cleared |
