# Verification debt -- commit session 7a97a337

Gates that had not run green over these commits when they were made.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-29 | 7a97a337 | fix(le): pin the staticcheck release that can read Go 1.27 export data | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=130, at 2026-08-28T23:14:12Z) | open |
| 2026-08-29 | 7a97a337 | fix(le): pin the staticcheck release that can read Go 1.27 export data | native structural checks (red) | The charged red IS this commit's subject and it is already repaired. tmp/ze-verify-failures.json holds the SIGINT-killed run of 2026-08-28T22:18Z, whose staticcheck-feature-matrix red is the export-data-version failure this commit fixes. A re-run of that exact gate against the current tree answers checked 38 rows, exit 0. The index carries no route to refresh one stage, so the record stays red until the next full verify. | open |
| 2026-08-29 | 7a97a337 | fix(le): pin the staticcheck release that can read Go 1.27 export data | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-08-29 | 7a97a337 | fix(le): pin the staticcheck release that can read Go 1.27 export data | discovery-index freshness | ai/PACKAGE-MAP.md is uncommitted in this shared checkout and its whole diff is another session's le namespace reorg, renaming internal/le/commandlist to internal/le/command/list and 22 siblings. This commit adds no package and moves none; regenerating the map here would carry that session's rename under this message. | open |
| 2026-08-29 | 7a97a337 | fix(le): republish the command surfaces on every site build | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=130, at 2026-08-28T23:14:12Z) | open |
| 2026-08-29 | 7a97a337 | fix(le): republish the command surfaces on every site build | native structural checks (red) | tmp/ze-verify-failures.json holds the SIGINT-killed run of 2026-08-28T22:18Z. Its staticcheck-feature-matrix red is the export-data-version failure the preceding commit fixes, and a re-run of that gate against the current tree answers checked 38 rows, exit 0. The index carries no route to refresh one stage, so the record stays red until the next full verify. | open |
| 2026-08-29 | 7a97a337 | fix(le): republish the command surfaces on every site build | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-08-29 | 7a97a337 | fix(le): republish the command surfaces on every site build | discovery-index freshness | ai/PACKAGE-MAP.md is uncommitted here and its whole diff is another session's le namespace reorg. This commit adds no package and moves none, so regenerating the map would carry that rename under this message. | open |
