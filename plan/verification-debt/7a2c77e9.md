# Verification debt -- commit session 7a2c77e9

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row by running the gate over the
committed code and setting Status to `cleared`, or delete the shard once
every row is cleared. `scripts/dev/commit_helper.py create --push` refuses
while any row here is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-19 | 7a2c77e9 | plan: record three VPP dataplane gaps found in a coverage sweep | ze-precommit-verify (not FRESH-green) | docs-only commit (plan/**/*.md), which ai/rules/precommit-verify.md excludes from the gate; the recorded non-structural reds (scripts/checks, scripts/dev, ldp, functional suite) are other sessions' in-flight work | open |
| 2026-08-19 | 7a2c77e9 | plan: record three VPP dataplane gaps found in a coverage sweep | ze-precommit-verify structural gates (red) | all three structural reds are other sessions' in-flight work and no plan/*.md can affect them: ze-lint-changed is gofmt on internal/component/ssh/ssh.go:835, ze-staticcheck-feature-matrix-check is flowspec.DecodeFlowComponent undefined in internal/plugins/flowspec-firewall/value_completeness_test.go, and ze-doc-wiring-check passes standalone (exit 0, ci-sleep ratchet OK 75<=75) after this tree state | open |
