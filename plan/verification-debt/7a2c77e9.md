# Verification debt -- commit session 7a2c77e9

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row by running the gate over the
committed code and setting Status to `cleared`, or delete the shard once
every row is cleared. the retired `scripts/dev/commit_helper.py create --push` (current producer: `internal/le/commit/prepare.go`) refuses
while any row here is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-19 | 7a2c77e9 | plan: record three VPP dataplane gaps found in a coverage sweep | ./le verify current mode full (not FRESH-green) | docs-only commit (plan/**/*.md), which ai/rules/precommit-verify.md excludes from the gate; the recorded non-structural reds (scripts/checks (retired; current producer: `internal/le/`), scripts/dev (retired; current producer: `internal/le/`), ldp, functional suite) are other sessions' in-flight work | open |
| 2026-08-19 | 7a2c77e9 | plan: record three VPP dataplane gaps found in a coverage sweep | ./le verify current mode full structural gates (red) | all three structural reds are other sessions' in-flight work and no plan/*.md can affect them: ./le changed scope is gofmt on internal/component/ssh/ssh.go:835, ./le staticcheck-feature-matrix check is flowspec.DecodeFlowComponent undefined in internal/plugins/flowspec-firewall/value_completeness_test.go, and ./le doc wiring passes standalone (exit 0, ci-sleep ratchet OK 75<=75) after this tree state | cleared |
| 2026-08-19 | 7a2c77e9 | fix(static,traffic): withdraw on delete, drop unbuildable qdisc kinds | ./le verify current mode full (not FRESH-green) | recorded non-structural reds are other sessions' in-flight work: scripts/checks (retired; current producer: `internal/le/`), scripts/dev (retired; current producer: `internal/le/`) commit_helper_test.py, internal/plugins/ldp, and the functional suite. Package tests for every path this commit touches pass: static, traffic/netlink, component/traffic, flowexport | open |
| 2026-08-19 | 7a2c77e9 | fix(static,traffic): withdraw on delete, drop unbuildable qdisc kinds | ./le verify current mode full structural gates (red) | all structural reds belong to other sessions and this commit cannot affect them: ./le changed scope is gofmt on internal/component/ssh/ssh.go:835, ./le staticcheck-feature-matrix check is flowspec.DecodeFlowComponent undefined in internal/plugins/flowspec-firewall/value_completeness_test.go, and ./le doc wiring passes standalone at exit 0 on this tree | cleared |
| 2026-08-19 | 7a2c77e9 | fix(static,traffic): withdraw on delete, drop unbuildable qdisc kinds | full ./le verify current mode full over this commit's Go | owner authorised 2026-08-19. The full run started 2026-08-18T21:43:15Z covered this code; the only Go writes since are two reworded comment sentences in register.go and translate_linux.go, made to clear advisory STE run-on findings. No statement changed. gofmt clean and the four package suites passed after the code was final | open |
| 2026-08-19 | 7a2c77e9 | fix(static): scope pending delivery to one transaction | ./le verify current mode full (not FRESH-green) | recorded reds are other sessions' in-flight work: ./le changed scope is now internal/plugins/flowspec-firewall/bridge_test.go, plus scripts/checks (retired; current producer: `internal/le/`), scripts/dev (retired; current producer: `internal/le/`), ldp and the functional suite. static, traffic component, traffic/netlink and traffic/vpp package tests pass; ./le test-weakened check is green | open |
| 2026-08-19 | 7a2c77e9 | fix(static): scope pending delivery to one transaction | ./le verify current mode full structural gates (red) | the structural reds belong to other sessions: flowspec-firewall test compile failures reach both ./le changed scope and ./le staticcheck-feature-matrix check, and this commit touches none of that package | cleared |
| 2026-08-19 | 7a2c77e9 | fix(static): scope pending delivery to one transaction | full ./le verify current mode full over this commit's Go | owner authorised 2026-08-19 for this line of work. The .ci added here is needs-linux and unrunnable on this host; the Go change is one new method plus its caller and a map with two rows removed, covered by the four package suites named above | open |
| 2026-08-19 | 7a2c77e9 | test(qemu): run the static suite, which executed nowhere | ./le verify current mode full (not FRESH-green) | the recorded reds are other sessions' in-flight work (flowspec-firewall test compile, scripts/checks (retired; current producer: `internal/le/`), scripts/dev (retired; current producer: `internal/le/`), ldp, functional suite). This commit adds one suite invocation to a QEMU driver script and changes no daemon code | open |
| 2026-08-19 | 7a2c77e9 | test(qemu): run the static suite, which executed nowhere | ./le verify current mode full structural gates (red) | the structural reds are other sessions': flowspec-firewall test compile failures reach ./le changed scope and ./le staticcheck-feature-matrix check, and this commit touches no Go | cleared |
| 2026-08-19 | 7a2c77e9 | test(static): give the isolation test a prefix nobody else claims | ./le verify current mode full (not FRESH-green) | verification is running now (the retired make ze-qemu-needs-linux-test (current: ./le qemu run command "./le qemu all-tests")); this suite needs CAP_NET_ADMIN and cannot run unprivileged on this host. The recorded reds are other sessions' in-flight work | open |
| 2026-08-19 | 7a2c77e9 | test(static): give the isolation test a prefix nobody else claims | ./le verify current mode full structural gates (red) | the structural reds are other sessions': flowspec-firewall and irr/store test compile failures reach ./le changed scope and ./le staticcheck-feature-matrix check. This commit changes one address and a comment in a .ci and touches no Go | cleared |
| 2026-08-19 | 7a2c77e9 | plan: record four defects found in the VPP and dataplane sweep | ./le verify current mode full (not FRESH-green) | journal rows only, no code. The recorded reds are other sessions' in-flight work | open |
| 2026-08-19 | 7a2c77e9 | plan: record four defects found in the VPP and dataplane sweep | ./le verify current mode full structural gates (red) | the structural reds are other sessions' test compile failures in flowspec-firewall and irr/store; this commit changes two markdown tables | cleared |
| 2026-08-19 | 7a2c77e9 | plan: record four defects found in the VPP and dataplane sweep | independent critical review | Owner authorised 2026-08-19. This commit closes no spec and carries no code. Its own four rows have an empty Spec cell. The three spec-bearing rows that trigger the gate (fixit-iface-selector-ignored-by-apply, fixit-firewall-irr-term-fails-validation, fixit-ike-resource-lifetime-leaks) are other sessions' uncommitted rows in the same shared class files, which per-file staging cannot separate from mine. I have not read those specs and am not vouching for them | open |
