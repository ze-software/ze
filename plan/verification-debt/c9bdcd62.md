# Verification debt -- commit session c9bdcd62

Gates that had not run green over these commits when they were made.
One row holds one gate and one reason, and covers every commit this
session made under it. `git log -- <this file>` names those commits.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-12 | c9bdcd62 | feat(le): le publishes the command surface it declares (+47 more) | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=1, at 2026-09-11T10:56:02Z) | open |
| 2026-09-12 | c9bdcd62 | feat(le): le publishes the command surface it declares (+1 more) | native structural checks (red) | iface-resolution is red at HEAD on a stale allowlist entry naming internal/le/interoplab/bgp/isis_inject_linux.go. That file is unmodified in this tree and this commit does not include it, so the red predates the change and no path in this population produces it. | open |
| 2026-09-12 | c9bdcd62 | feat(le): le publishes the command surface it declares (+40 more) | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-09-12 | c9bdcd62 | fix(le): a help word in a value slot is the keyword's value (+7 more) | native structural checks (red) | iface-resolution is red at HEAD on a stale allowlist entry naming internal/le/interoplab/bgp/isis_inject_linux.go. That file is unmodified in this tree and is not in this population, so the red predates the change. | open |
| 2026-09-12 | c9bdcd62 | fix(le): every published verb is dispatched through its own table (+2 more) | native structural checks (red) | iface-resolution is red at HEAD on a stale allowlist entry naming internal/le/interoplab/bgp/isis_inject_linux.go. That file is unmodified by this session and is not in this population, so the red predates the change. | open |
| 2026-09-13 | c9bdcd62 | refactor(le): one reader for the feature manifest, not nineteen (+5 more) | native structural checks (red) | iface-resolution is red at HEAD on a stale allowlist entry naming internal/le/interoplab/bgp/isis_inject_linux.go, unmodified by this session and not in this population. | open |
| 2026-09-13 | c9bdcd62 | fix(le): six gates green, and the feature table publishes whole | native structural checks (red) | iface-resolution is red at HEAD on a stale allowlist entry naming internal/le/interoplab/bgp/isis_inject_linux.go. That file is unmodified by this session and is not in this population. | open |
| 2026-09-13 | c9bdcd62 | chore: land the in-flight work this checkout holds | native structural checks (red) | iface-resolution is red at HEAD on a stale allowlist entry naming internal/le/interoplab/bgp/isis_inject_linux.go. The entry predates this session. | open |
| 2026-09-13 | c9bdcd62 | fix(le): the launcher fixture carries the reader the launcher sources | native structural checks (red) | tier/check and iface-resolution are charged from tmp/ze-verify-failures.json, written 2026-09-11 12:56. Both answer OK with exit 0 in this checkout now: ./le tier check prints engine placement clean and ./le iface-resolution prints OK. Neither red carries a path, so the index charges them to every commit. Rowed in plan/journal/stale-artifact-reused.md. | open |
| 2026-09-13 | c9bdcd62 | fix: the committed tree answers for itself again | native structural checks (red) | tier/check and iface-resolution are charged from tmp/ze-verify-failures.json, written 2026-09-11 12:56. Both were run against this tree just now and answer exit 0: tier check prints engine placement clean, non-engine categories clean and core import direction clean; iface-resolution prints OK. Neither recorded red carries a path, so the stale index charges them to every commit. Rowed in plan/journal/stale-artifact-reused.md. | open |
| 2026-09-13 | c9bdcd62 | fix(web,hub): the golden captures show every feature again | native structural checks (red) | tier/check and iface-resolution are charged from tmp/ze-verify-failures.json, written 2026-09-11 12:56. Both were run against this tree and answer exit 0. Neither recorded red carries a path, so the stale index charges them to every commit. Rowed in plan/journal/stale-artifact-reused.md. | open |
| 2026-09-13 | c9bdcd62 | chore(le): re-baseline the hook source ratchet (+16 more) | native structural checks (red) | tier/check and iface-resolution are charged from tmp/ze-verify-failures.json, written 2026-09-11 12:56, and both answer exit 0 against this tree. Neither recorded red carries a path, so the stale index charges them to every commit. Rowed in plan/journal/stale-artifact-reused.md. | open |
| 2026-09-13 | c9bdcd62 | plan(le): the closure record for le publishing its surface | full native verification (not FRESH-green) | verify status is STALE from another session's failed run on 2026-09-11. This commit carries no Go. The le unit suite ran green over every package this spec touches on 2026-09-13: internal/le, leaction, leroot, ste, stressrepro, functional, testchaos, testunit and doc/wiring all ok. | open |
| 2026-09-13 | c9bdcd62 | spec: close le-publishes-its-command-surface | full native verification (not FRESH-green) | verify status is STALE from another session's failed run on 2026-09-11. This commit removes one markdown file and carries no Go. | open |
| 2026-09-13 | c9bdcd62 | fix(test): the internal bridge fixtures name a process block | native structural checks (red) | both gates answer OK when run at HEAD in this tree: ./le tier check exits 0 with four OK lines, ./le iface-resolution prints iface-resolution: OK. The charge comes from a recorded verify run whose stage carried no path attribution, and this commit changes two .ci fixtures and no Go | open |
| 2026-09-13 | c9bdcd62 | fix(bgp): a FlowSpec announce carries no next-hop on any rail | native structural checks (red) | both charged gates answer OK when run at HEAD in this tree: ./le tier check exits 0 and ./le iface-resolution prints iface-resolution: OK. The charge comes from a recorded verify run whose stage carried no path attribution | open |
| 2026-09-13 | c9bdcd62 | fix(hub): a SIGTERM during startup is not swallowed by a plugin (+6 more) | native structural checks (red) | both charged gates answer OK when run at HEAD in this tree: ./le tier check exits 0 and ./le iface-resolution prints iface-resolution: OK; the charge comes from a recorded verify run whose stage carried no path attribution | open |
