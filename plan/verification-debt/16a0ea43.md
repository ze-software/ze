# Verification debt -- commit session 16a0ea43

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row with `./le commit debt-clear`:
it re-runs the gate the row names over the committed code and writes
`cleared` only when that run exits 0. A human MAY delete the shard once
every row is cleared. the retired `scripts/dev/commit_helper.py create --push` (current producer: `internal/le/commit/prepare.go`) refuses
while any row here is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-19 | 16a0ea43 | close: appliance replace-cert validates before it writes | ./le verify current mode full (not FRESH-green) | The commit carries only spec and journal text: no .go, go.mod, go.sum or vendor path, so no full verify run is owed for it. This spec's code landed in 890ad584a and d2202c50a. the retired make ze-functional-appliance-test (current: ./le functional appliance) could not build because internal/component/bgp/plugins/adj_rib_in/rib.go calls splitRawNLRIHex with three arguments against a two-argument signature; the file is M with a .tmp file beside it, so another session is mid-edit. ./le repository check reports 26 unwired-export findings, every one in a file another session holds M, and audit-test-relaxation reports 4 findings, every one in another session's M test file. Scoped gates for this spec are green: ze-unit-pkg-test on ./internal/appliance/... and ./internal/component/doctor/... both exit 0, and ./le spec citation anchors exits 0. | open |
| 2026-08-19 | 16a0ea43 | close: appliance replace-cert validates before it writes | ./le verify current mode full structural gates (red) | Both reds belong to other sessions' in-flight work and this commit carries no code. ./le staticcheck-feature-matrix check fails on internal/plugins/flowspec-firewall/value_completeness_test.go, which calls flowspec.DecodeFlowComponent under without_ze_gnmi; that file is M in git status and the symbol exists nowhere in the tree, so that session is mid-implementation. ./le doc wiring fails through ./le doc check verify on the ci-sleep ratchet, 77 time.sleep calls against a ceiling of 75; both extra sleeps are in test/plugin/vpp-loopback-reapply.ci, which is untracked and was written one minute before the run. Neither file is in this commit, and a commit of two markdown files cannot affect either. | cleared |
| 2026-08-19 | 16a0ea43 | close: remove spec-fixit-appliance-cert-replace-unvalidated | ./le verify current mode full (not FRESH-green) | The commit removes one spec file and carries no .go, go.mod, go.sum or vendor path. This spec's code landed in 890ad584a and d2202c50a, and the closure commit before this one carries the audit that verified it. The tree's reds belong to other sessions, attributed in the previous commit's reason. | open |
| 2026-08-19 | 16a0ea43 | close: remove spec-fixit-appliance-cert-replace-unvalidated | ./le verify current mode full structural gates (red) | Same two reds as the previous commit, and this one removes a markdown file. ./le staticcheck-feature-matrix check fails on internal/plugins/flowspec-firewall/value_completeness_test.go, which is M and calls a symbol that exists nowhere in the tree. ./le doc wiring fails through the ci-sleep ratchet on the untracked test/plugin/vpp-loopback-reapply.ci. Neither file is in this commit. | cleared |
| 2026-08-19 | 16a0ea43 | fix(bgp): send End-of-RIB for a peer that declares no family | ./le verify current mode full structural gates (red) | ./le verify lint run lost golangci-lint's single-instance lock to a concurrent session ('parallel golangci-lint is running', no finding emitted); ./le staticcheck-feature-matrix check exceeded its 25m deadline on a box running a dozen sessions; ./le doc wiring's two live groups are kind=files naming internal/component/bgp/{config,route} test files and internal/core/bgp/attribute/flowspec_encode.go, none of them in this commit. Its ci-sleep ratchet, the only part this session could redden, reports OK (75 <= ceiling 75). | cleared |
| 2026-08-19 | 16a0ea43 | fix(bgp): keep the source id when the forward rail rebuilds a wire | ./le verify current mode full structural gates (red) | Same three environmental gates as 8f0d02e81: ./le verify lint run lost golangci-lint's single-instance lock to a concurrent session, ./le staticcheck-feature-matrix check exceeded its 25m deadline on a loaded box, and ./le doc wiring's live groups are kind=files naming other sessions' files. | cleared |
| 2026-08-19 | 16a0ea43 | fix(bgp): relay a stored route from an ADD-PATH source | ./le verify current mode full structural gates (red) | Same three environmental gates as 8f0d02e81. | cleared |
| 2026-08-19 | 16a0ea43 | fix(ike): refuse a Child SA rekey that proposes no traffic selectors | ./le verify current mode full structural gates (red) | Same three environmental gates as 8f0d02e81. | cleared |
| 2026-08-19 | 16a0ea43 | fix(firewall): record the applied tables where they are applied | ./le verify current mode full structural gates (red) | Same three environmental gates as 8f0d02e81. | cleared |
| 2026-08-19 | 16a0ea43 | fix(vpp): keep an existing loopback instead of creating another | ./le verify current mode full structural gates (red) | Same three environmental gates as 8f0d02e81. | cleared |
| 2026-08-19 | 16a0ea43 | fix(vpp): keep an existing loopback instead of creating another | full ./le verify current mode full over this commit's Go | A full ./le verify current mode full started 2026-08-19T13:16:07Z and is at stage 26/30 now. It covers this content: ifacevpp.go carries an mtime of 14:13:06 only because the .ci's revert-check mutated and restored the file, which rewrote the bytes identically. No source change was made after the run started. | open |
| 2026-08-19 | 16a0ea43 | fix(irr): make a forced refresh reach the IRR server | ./le verify current mode full structural gates (red) | Same three environmental gates as 8f0d02e81. | cleared |
| 2026-08-19 | 16a0ea43 | fix(dns): count and log a reply the transport refused to write | ./le verify current mode full structural gates (red) | Same three environmental gates as 8f0d02e81. | cleared |
| 2026-08-19 | 16a0ea43 | fix(tooling): make the review gate refuse evidence it cannot check | ./le verify current mode full structural gates (red) | Same three environmental gates as 8f0d02e81. | cleared |
| 2026-08-19 | 16a0ea43 | fix(interop): render the CLI account once and fail closed on a missing snapshot | ./le verify current mode full structural gates (red) | Same three environmental gates as 8f0d02e81. | cleared |
| 2026-08-19 | 16a0ea43 | fix(interop): render the CLI account once, fail closed on a snapshot | ./le verify current mode full structural gates (red) | Same three environmental gates as 8f0d02e81. | cleared |
| 2026-08-19 | 16a0ea43 | fix(test): repair two assertions that could no longer fire | ./le verify current mode full structural gates (red) | Same three environmental gates as 8f0d02e81. | cleared |
| 2026-08-19 | 16a0ea43 | fix(test): repair two assertions that could no longer fire | full ./le verify current mode full over this commit's Go | The two Go files here were written after the 13:16:07Z full run started: forward_pool_test.go is test-only and was proven by 250 load iterations plus a mutation run, and main.go gains two comment lines carrying the ze-dispatch-check marker, whose gate I ran directly and which now exits 0. | open |
| 2026-08-19 | 16a0ea43 | docs(plan): record two problem classes and move two specs | ./le verify current mode full structural gates (red) | Same three environmental gates as 8f0d02e81. | cleared |
| 2026-08-19 | 16a0ea43 | docs(plan): record two problem classes and move two specs | independent critical review | The two plan/spec-*.md removals are RELOCATIONS into plan/future/, not closures. Byte-identical content plus a header note stating why each is not a release defect; neither spec is claimed complete and neither has a Review Gate to satisfy. The gate reads any spec removal as a closure, which is right for a close and has no case for a move. | open |
