# Verification debt -- commit session 66fc17ca

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row by running the gate over the
committed code and setting Status to `cleared`, or delete the shard once
every row is cleared. the retired `scripts/dev/commit_helper.py create --push` (current producer: `internal/le/commit/prepare.go`) refuses
while any row here is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-19 | 66fc17ca | fix(hooks): make three agent-facing guards say what they claim | ./le verify current mode full structural gates (red) | owner instruction 2026-08-19. The recorded ./le doc wiring red is from the 21:43Z run and is stale: its cause was an unjustified pause call at line 120 of test/ui/web-recovery-session-survives-commit.ci pushing the ci-sleep ratchet to 76 against a baseline of 75, and that file is clean in git since 6a63cf0e5, which landed after the run. Two other sessions measured the gate exit 0 against this tree, one of them three times after repairing both causes. This commit scope is green: the retired make ze-unit-hook-test (current: ./le hook-check unit) 531/531, ./le rules points-roundtrip-check and ./le rules lint exit 0, and every new fixture was proven to fail against the pre-fix code | cleared |
| 2026-08-19 | 66fc17ca | fix(hooks): make three agent-facing guards say what they claim | independent critical review | no spec closes here: hooks, a commit gate, a rule point and four journal rows. The closure proxy reads spec-shared-machine-job-admission from a row I REPAIRED rather than authored: that row held three backtick-quoted pipe characters, which journal_row_cells splits on with no unescaping, so it parsed as MALFORMED and would have refused any commit carrying the file. The trap is recorded in plan/journal/gate-fires-outside-its-population.md | open |
| 2026-08-19 | 66fc17ca | fix(iface): bind every apply path to the selector, not to the logical name | ./le verify current mode full structural gates (red) | owner instruction 2026-08-19. The recorded ./le doc wiring red is stale, repaired in git since 6a63cf0e5 and measured exit 0 against this tree by two other sessions. Own scope green: iface packages race-instrumented, integration-tagged Linux tests green including TestIntegrationApplyConfigFailureRollsBackCreatedLinks, both new .ci passing in a netns with their two pre-existing siblings | cleared |
| 2026-08-19 | 66fc17ca | fix(iface): bind every apply path to the selector, not to the logical name | full ./le verify current mode full over this commit's Go | no full verify has started since these Go files were written; the tree carries three other sessions' in-flight work. Each fix was proven by reverting it and confirming the test goes red, then restoring | open |
| 2026-08-19 | 66fc17ca | fix(iface): bind every apply path to the selector, not to the logical name | independent critical review | spec-fixit-iface-selector-ignored-by-apply.md does NOT close here: it stays in-progress for its Review Gate, and another session is running one fresh independent reviewer over the final state with both authorship halves in scope. This commit is the repair half of that state. The spec mentions bridge, bond and member nowhere, so neither defect repaired is an acceptance criterion. CLEARED 2026-08-22: that review ran over all five commits and recorded `tmp/review/fixit-iface-selector-ignored-by-apply-ebf5d9b3-b158-40df-bba0-32a51591883e.md`, 0 BLOCKER and 0 ISSUE | cleared |
| 2026-08-19 | 66fc17ca | fix(iface): apply the selector on every path, not the logical name | ./le verify current mode full structural gates (red) | owner instruction 2026-08-19. The recorded ./le doc wiring red is stale, repaired in git since 6a63cf0e5 and measured exit 0 against this tree by two other sessions. Own scope green: iface packages race-instrumented, integration-tagged Linux tests green including TestIntegrationApplyConfigFailureRollsBackCreatedLinks, both new .ci passing in a netns with their two pre-existing siblings | cleared |
| 2026-08-19 | 66fc17ca | fix(iface): apply the selector on every path, not the logical name | full ./le verify current mode full over this commit's Go | no full verify has started since these Go files were written; the tree carries three other sessions' in-flight work. Each fix was proven by reverting it and confirming the test goes red, then restoring | open |
| 2026-08-19 | 66fc17ca | fix(iface): apply the selector on every path, not the logical name | independent critical review | spec-fixit-iface-selector-ignored-by-apply.md does NOT close here: it stays in-progress for its Review Gate, and another session is running one fresh independent reviewer over the final state with both authorship halves in scope. This commit is the repair half of that state. The spec mentions bridge, bond and member nowhere, so neither defect repaired is an acceptance criterion. CLEARED 2026-08-22: that review ran over all five commits and recorded `tmp/review/fixit-iface-selector-ignored-by-apply-ebf5d9b3-b158-40df-bba0-32a51591883e.md`, 0 BLOCKER and 0 ISSUE | cleared |
| 2026-08-19 | 66fc17ca | fix(irr): keep last-known-good per family, and end the deferral | ./le verify current mode full structural gates (red) | owner instruction 2026-08-19. The recorded ./le doc wiring red is stale, repaired in git since 6a63cf0e5 and measured exit 0 against this tree by two other sessions. Own scope green: race tests across firewall, firewall/plugins/irr, resolve, filter_irr, copp, policyroute and nft, plus the new .ci passing three times in a netns | cleared |
| 2026-08-19 | 66fc17ca | fix(irr): keep last-known-good per family, and end the deferral | full ./le verify current mode full over this commit's Go | no full verify has started since these Go files were written; the tree carries three other sessions' in-flight work. Each fix was proven by reverting it and confirming the test goes red, then restoring | open |
| 2026-08-19 | 66fc17ca | fix(irr): keep last-known-good per family, and end the deferral | independent critical review | spec-fixit-firewall-irr-term-fails-validation.md does NOT close here: it stays in-progress for its Review Gate, and another session is running one fresh independent reviewer over the final state with both authorship halves in scope. This commit CHANGES one of that spec's Key Design Decisions, from whole-reconcile waiting to per-table, because the decision as written creates a wait nothing can end on a cold cache. The spec amendment is owed and is deliberately held until that reviewer reports, so the divergence it was commissioned to judge stays visible in the artifact | open |
| 2026-08-19 | 66fc17ca | fix(flowspec): enforce every value, or refuse the rule | ./le verify current mode full structural gates (red) | owner instruction 2026-08-19. The recorded ./le doc wiring red is stale, repaired in git since 6a63cf0e5 and measured exit 0 against this tree by two other sessions. Own scope: nine new tests, all nine failing against HEAD's translate.go and engine.go and green after, race-instrumented; gofmt and go vet clean | cleared |
| 2026-08-19 | 66fc17ca | fix(flowspec): enforce every value, or refuse the rule | full ./le verify current mode full over this commit's Go | no full verify has started since these Go files were written; the tree carries three other sessions' in-flight work. ./le changed scope was attempted three times and never admitted by the job lock behind a sixty-minute run, which is shared-machine contention rather than a finding | open |
| 2026-08-19 | 66fc17ca | fix(flowspec): enforce every value, or refuse the rule | independent critical review | no spec closes here. spec-fixit-flowspec-protocol-name-drift.md is status ready and its AC-3 already agrees the design implemented, that every listed value is enforced or the rule is refused; it was not edited. The journal row added records a separate defect found during this work and left unfixed, now owned by another session | open |
| 2026-08-19 | 66fc17ca | docs(firewall): document the per-table wait and how to end it | ./le verify current mode full structural gates (red) | owner instruction 2026-08-19. The recorded ./le doc wiring red is stale, repaired in git since 6a63cf0e5 and measured exit 0 against this tree by two other sessions. This commit is prose only | cleared |
| 2026-08-19 | 66fc17ca | docs(firewall): document the per-table wait and how to end it | independent critical review | no spec closes here: prose describing behaviour that landed in 20ad3ebb6 | open |
| 2026-08-19 | 66fc17ca | style(ssh): gofmt a file that was committed unformatted | ./le verify current mode full structural gates (red) | owner instruction 2026-08-19. The recorded ./le doc wiring red is stale, repaired in git since 6a63cf0e5 and measured exit 0 against this tree by two other sessions. Own scope: gofmt -l is clean on the file after the change | cleared |
| 2026-08-19 | 66fc17ca | style(ssh): gofmt a file that was committed unformatted | full ./le verify current mode full over this commit's Go | gofmt only, no behaviour change; ./le repository tracked-build check ran OK over the committed tree after the four commits preceding this one | open |
| 2026-08-19 | 66fc17ca | style(ssh): gofmt a file that was committed unformatted | independent critical review | no spec closes here: one gofmt line and the journal row counting the class | open |
| 2026-08-19 | 66fc17ca | plan(irr): amend the deferral decision the code already changed | ./le verify current mode full structural gates (red) | owner instruction 2026-08-19. The recorded ./le doc wiring red is stale, repaired in git since 6a63cf0e5 and measured exit 0 against this tree by two other sessions. This commit is spec prose only | cleared |
| 2026-08-19 | 66fc17ca | plan(irr): amend the deferral decision the code already changed | independent critical review | no spec closes here: this spec stays in-progress. The amendment corrects one Key Design Decisions row to match code landed in 20ad3ebb6, after the independent review of that spec reported | open |
