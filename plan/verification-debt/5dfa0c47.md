# Verification debt -- commit session 5dfa0c47

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row by running the gate over the
committed code and setting Status to `cleared`, or delete the shard once
every row is cleared. `scripts/dev/commit_helper.py create --push` refuses
while any row here is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-18 | 5dfa0c47 | fix(ike): the dead-peer verdict needs a repeated attempt | ze-precommit-verify (not FRESH-green) | shared checkout. The recorded verify is red on internal/plugins/anomaly/detect/detector.go (goconst) and internal/component/plugin/process/process.go (9 unused, dead at HEAD since 6ff9d56e6), on internal/plugins/anomaly/observe/subscribe_test.go (assert-nothing floor, another session's untracked file), and on scripts/dev/package_map.py (another session's uncommitted generator change, which reddens 4 commit-gate-index fixtures in ze-unit-hook-test). None of those paths is in this commit. This commit's own scope is green: make ze-unit-pkg-test PKG=./internal/component/ike/engine with race, make ze-rfc-check, and ze-lint-changed reports nothing under internal/component/ike/ | open |
| 2026-08-18 | 5dfa0c47 | fix(ike): the dead-peer verdict needs a repeated attempt | ze-precommit-verify structural gates (red) | ze-lint is red on internal/component/plugin/process/process.go alone: 9 unexported symbols dead at HEAD since 6ff9d56e6 unexported them rather than deleting them. Diagnosed, not guessed: their only callers were the text-protocol handlers e71b418dd deleted and the session-api-encoding handler 0c8521227 deleted, and both have live successors (the dispatcher CommandRegistry, and Process.Encoding/SetEncoding). Journal row written to plan/journal/invariant-enforced-by-an-absent-call-site.md; the fix is put to Thomas because it deletes committed code | open |
| 2026-08-18 | 5dfa0c47 | fix(ike): the dead-peer verdict needs a repeated attempt | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has started since these Go files were written. A ze-precommit-verify-changed run reached 25 of 27 stages before I stopped it at 40 minutes; it was red only for the paths named above. The changed packages were run directly instead: ike/engine green with race, plus the total RFC gate | open |
| 2026-08-18 | 5dfa0c47 | build: admit heavy jobs through one point in a shared checkout | ze-precommit-verify (not FRESH-green) | shared checkout. The recorded verify is red on internal/plugins/anomaly/detect/detector.go, internal/component/plugin/process/process.go, internal/plugins/anomaly/observe/subscribe_test.go and scripts/dev/package_map.py, none of them in this commit. Own scope green: ze_run_test.py 20/20, ze-unit-pkg-test PKG=./scripts/status/... with race, ze-unit-hook-test 473/473 run directly, ze-vendor-web-check exit 0 inside a real verify | open |
| 2026-08-18 | 5dfa0c47 | build: admit heavy jobs through one point in a shared checkout | ze-precommit-verify structural gates (red) | ze-lint red on internal/component/plugin/process/process.go alone, 9 symbols dead at HEAD since 6ff9d56e6; diagnosed and journalled, fix put to Thomas because it deletes committed code. ze-generated-files-check red on mk/test-fuzz-targets.mk reported stale by make generate: that file is in this commit and the staleness is the wrapper pair this commit adds, so the generator owes the same shape and that is the next step. ze-doc-wiring-check red on ze-spec-citation-check, 12 dangling plan/spec citations at HEAD, already recorded in plan/deferrals/ | open |
| 2026-08-18 | 5dfa0c47 | build: admit heavy jobs through one point in a shared checkout | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify started since these Go files were written. A ze-precommit-verify-changed reached 25 of 27 stages before I stopped it at 40 minutes, red only for the paths named above | open |
| 2026-08-18 | 5dfa0c47 | build: admit heavy jobs through one point in a shared checkout | discovery-index freshness | ai/PACKAGE-MAP.md is stale for two OTHER sessions' work and none of mine: its whole diff is the Total: line removed by the uncommitted scripts/dev/package_map.py, plus three rows for the untracked internal/plugins/anomaly/observe package. Committing it would carry that generator change without its generator and publish rows for files absent from HEAD. This commit's own new source is a _test.go in an existing package, which adds no row | open |
| 2026-08-18 | 5dfa0c47 | fix(vendor): stop a bytecode cache reading as a vendored package | ze-precommit-verify (not FRESH-green) | shared checkout. The recorded verify is red on paths this commit does not touch (anomaly/detect, plugin/process, anomaly/observe, package_map.py). Own scope green: make ze-vendor-web-check exit 0, both from the command line and as stage 21 of a real verify run | open |
| 2026-08-18 | 5dfa0c47 | fix(vendor): stop a bytecode cache reading as a vendored package | ze-precommit-verify structural gates (red) | ze-lint red on internal/component/plugin/process/process.go alone, 9 symbols dead at HEAD since 6ff9d56e6, diagnosed and journalled. ze-generated-files-check and ze-doc-wiring-check as recorded on the two commits before this one. This commit REMOVES one cause of the generated-files red rather than adding to it | open |
| 2026-08-18 | 5dfa0c47 | fix(vendor): stop a bytecode cache reading as a vendored package | full ze-precommit-verify over this commit's Go | check_web.go is a //go:build ignore build tool run by go run, not part of any compiled binary, so tracked-build and the unit suites never load it. Its own gate is what verifies it, and that gate ran twice green: from the command line, and as stage 21 of a ze-precommit-verify-changed after the edit | open |
| 2026-08-18 | 5dfa0c47 | fix(vendor): stop a bytecode cache reading as a vendored package | discovery-index freshness | ai/PACKAGE-MAP.md is stale for two other sessions' work: the Total: line removed by the uncommitted scripts/dev/package_map.py, and three rows for the untracked internal/plugins/anomaly/observe package. This commit adds no package | open |
| 2026-08-18 | 5dfa0c47 | fix(anomaly): name the feature string the floor and the rows share | ze-precommit-verify (not FRESH-green) | shared checkout. Own scope: make ze-lint-changed no longer reports goconst anywhere, and the two anomaly test failures were shown to be identical with the edit reverted, so they belong to that session's in-flight work | open |
| 2026-08-18 | 5dfa0c47 | fix(anomaly): name the feature string the floor and the rows share | ze-precommit-verify structural gates (red) | ze-lint red on internal/component/plugin/process/process.go alone after this commit, which is the half diagnosed in the message and journalled rather than fixed. ze-generated-files-check and ze-doc-wiring-check as recorded on the three commits before this one | open |
| 2026-08-18 | 5dfa0c47 | fix(anomaly): name the feature string the floor and the rows share | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify started since this edit. It extracts a constant with an identical value and changes no behaviour; make ze-lint-changed and the anomaly package suite were run over it directly | open |
| 2026-08-18 | 5dfa0c47 | fix(anomaly): name the feature string the floor and the rows share | discovery-index freshness | ai/PACKAGE-MAP.md is stale for two other sessions' work, as recorded on the three commits before this one. This commit adds no package | open |
| 2026-08-18 | 5dfa0c47 | fix(anomaly): name the feature string the floor and the rows share | independent critical review | no spec is being closed. The journal file carries three ADDED rows because the two above mine were written by other sessions and never committed, so the gate reads the first one's Spec cell, fixit-ike-resource-lifetime-leaks, as a closure signal. That spec is still in-progress and its file was committed as such in d85aa3720. My own row's Spec cell is '-'. This is the trap already recorded twice in plan/journal/gate-fires-outside-its-population.md, so it gets no new row | open |
