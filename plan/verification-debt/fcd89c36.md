# Verification debt -- commit session fcd89c36

Gates that had not run green over these commits when they were made.
One row holds one gate and one reason, and covers every commit this
session made under it. `git log -- <this file>` names those commits.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-09 | fcd89c36 | ze reads and writes an AS number in every RFC 5396 notation (+16 more) | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=1, at 2026-09-05T00:11:54Z) | open |
| 2026-09-09 | fcd89c36 | ze reads and writes an AS number in every RFC 5396 notation (+11 more) | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-09-09 | fcd89c36 | bfd: one session per neighbor, and the strict-mode capability and FSM (+1 more) | discovery-index freshness | ai/PACKAGE-MAP.md is generated from the WORKING TREE, which holds three other sessions' untracked packages; regenerating it here would publish rows for files HEAD does not have | open |
| 2026-09-09 | fcd89c36 | bfd: the positive interop scenario and the Section 4.4 ledger row | discovery-index freshness | ai/PACKAGE-MAP.md is generated from the WORKING TREE, which holds three other sessions' untracked packages; this commit adds no Go package, and regenerating the map here would publish rows for files HEAD does not have | open |
| 2026-09-11 | fcd89c36 | bgp: BFDSettings declares the three strict-mode leaves its reader needs | discovery-index freshness | ai/PACKAGE-MAP.md describes internal/component/bgp/reactor one line per package, at a level three new struct fields on an existing type do not change. No package is added, removed or repurposed. | open |
| 2026-09-11 | fcd89c36 | bgp: hold the initial routing update until the RIB converges | native structural checks (red) | doc wiring is red for 29 files of other sessions (ike, bfd, config/transaction, bgp/message, core/bgp/attribute) plus five design-refs of the paths-limit and as-notation work, so the stage is red without this commit. Its rows for THIS spec are the park cycle showing through: docs/guide/configuration.md and docs/features/bgp-protocol.md landed in HEAD while the code was parked, so their anchors now see update_delay.go and ParseUpdateDelay change with no page edge beside them. Both pages were read against their producers in this closure and are accurate, so editing them to move the checker would be prose written for a gate | open |
| 2026-09-11 | fcd89c36 | bgp: hold the initial routing update until the RIB converges | discovery-index freshness | ai/PACKAGE-MAP.md was regenerated with ./le discovery-index update and came back byte-identical: this spec adds files to packages the map already names and creates no package | open |
| 2026-09-11 | fcd89c36 | repoint every citer of spec-bgp-as-notation before it is removed | discovery-index freshness | ai/PACKAGE-MAP.md is generated from the WORKING TREE, which holds another session's untracked package internal/test/failsyscall (failsyscall_linux.go, options.go). Regenerating it here would publish a row for files HEAD does not have. This commit adds, removes and repurposes no package: it edits two doc comments, a baseline list and three prose files. | open |
| 2026-09-11 | fcd89c36 | close spec-bgp-as-notation: the three files it waited on are in HEAD | discovery-index freshness | ai/PACKAGE-MAP.md is generated from the WORKING TREE, which holds another session's untracked package internal/test/failsyscall (failsyscall_linux.go, options.go). Regenerating it here would publish a row for files HEAD does not have. This commit removes one spec under plan/ and touches no package at all. | open |
| 2026-09-11 | fcd89c36 | chore(plan): record a selector test asserting an order nobody promises (+7 more) | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=1, at 2026-09-11T10:56:02Z) | open |
| 2026-09-11 | fcd89c36 | chore(plan): record a selector test asserting an order nobody promises | native structural checks (red) | Both reds belong to other sessions and this commit carries one journal row, no Go. tier/check fails on internal/le/interoplab/pppoe/check_ipv6cp.go, check_padr_replay.go and check_service_name.go importing internal/component/l2tp/pppoe without //go:build ze_l2tp, which is the subscriber session's interop work. The only dirty file under internal/component/iface is a one-line change to operation_address_swap_test.go carrying nothing of this session's three specs. Fixing either from here would fold an unrelated repair into a single-row commit. | open |
| 2026-09-11 | fcd89c36 | bfd: a first packet finds its session, and strict mode drives the FSM | native structural checks (red) | All three reds are outside this commit's 57 paths, each confirmed by running the gate. tier/check names internal/le/interoplab/pppoe/check_ipv6cp.go, check_padr_replay.go and check_service_name.go importing internal/component/l2tp/pppoe without //go:build ze_l2tp; this commit touches no file under internal/le/. iface-resolution names a stale allowlist entry for internal/le/interoplab/bgp/isis_inject_linux.go, a tracked file this commit does not modify and whose finding somebody else already repaired without deleting the entry. doc wiring is red tree-wide over pages belonging to ike, command/pipe, config/transaction and iface; every page whose claim this diff falsified was repaired in this same commit (fsm.md, fsm-active.md, fsm-established.md, peer-lifecycle.md, docs/guide/bfd.md) and a re-run of ./le doc wiring names none of them. | open |
| 2026-09-11 | fcd89c36 | spec: close bgp-bfd-strict (+1 more) | native structural checks (red) | The same two reds as commit A, both outside this commit, which removes one file under plan/ and nothing else. tier/check names internal/le/interoplab/pppoe importing internal/component/l2tp/pppoe without //go:build ze_l2tp. iface-resolution names a stale allowlist entry for internal/le/interoplab/bgp/isis_inject_linux.go, a file this commit does not touch. | open |
| 2026-09-11 | fcd89c36 | doc: the citation baseline drops a pair nothing can cite | native structural checks (red) | The same reds as the two commits before it, all outside this commit, which deletes one line of testdata. tier/check names internal/le/interoplab/pppoe importing internal/component/l2tp/pppoe without //go:build ze_l2tp; iface-resolution names a stale allowlist entry for internal/le/interoplab/bgp/isis_inject_linux.go; doc wiring is red over other sessions' pages. | open |
| 2026-09-11 | fcd89c36 | chore(deps): prometheus/common is a direct dependency | native structural checks (red) | Both reds predate this commit and belong to other sessions; it carries one go.mod line and no Go. tier/check fails on internal/le/interoplab/pppoe/check_ipv6cp.go, check_padr_replay.go and check_service_name.go importing internal/component/l2tp/pppoe without //go:build ze_l2tp, which is the subscriber session's interop work. iface-resolution is red with the only dirty file under internal/component/iface being a one-line change to operation_address_swap_test.go that carries nothing of this session. Removing an // indirect marker cannot reach either. | open |
| 2026-09-11 | fcd89c36 | chore(plan): retire the handover for the three closed BGP specs | native structural checks (red) | Both reds predate this commit and belong to other sessions; it carries prose only. tier/check fails on internal/le/interoplab/pppoe/ importing internal/component/l2tp/pppoe without //go:build ze_l2tp, the subscriber session's interop work. iface-resolution's only dirty file is a one-line change to internal/component/iface/operation_address_swap_test.go carrying nothing of this session. Deleting a spent handover cannot reach either. | open |
| 2026-09-11 | fcd89c36 | chore(plan): BFD interop evidence predates the transport it proves | native structural checks (red) | Both reds predate this commit and belong to other sessions; it carries one journal row and no Go. tier/check fails on internal/le/interoplab/pppoe/ importing internal/component/l2tp/pppoe without a build tag. iface-resolution's only dirty file is a one-line change to internal/component/iface/operation_address_swap_test.go carrying nothing of this session. | open |
