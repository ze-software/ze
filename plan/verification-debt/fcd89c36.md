# Verification debt -- commit session fcd89c36

Gates that had not run green over these commits when they were made.
One row holds one gate and one reason, and covers every commit this
session made under it. `git log -- <this file>` names those commits.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-09 | fcd89c36 | ze reads and writes an AS number in every RFC 5396 notation (+12 more) | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=1, at 2026-09-05T00:11:54Z) | open |
| 2026-09-09 | fcd89c36 | ze reads and writes an AS number in every RFC 5396 notation (+7 more) | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-09-09 | fcd89c36 | bfd: one session per neighbor, and the strict-mode capability and FSM (+1 more) | discovery-index freshness | ai/PACKAGE-MAP.md is generated from the WORKING TREE, which holds three other sessions' untracked packages; regenerating it here would publish rows for files HEAD does not have | open |
| 2026-09-09 | fcd89c36 | bfd: the positive interop scenario and the Section 4.4 ledger row | discovery-index freshness | ai/PACKAGE-MAP.md is generated from the WORKING TREE, which holds three other sessions' untracked packages; this commit adds no Go package, and regenerating the map here would publish rows for files HEAD does not have | open |
| 2026-09-11 | fcd89c36 | bgp: BFDSettings declares the three strict-mode leaves its reader needs | discovery-index freshness | ai/PACKAGE-MAP.md describes internal/component/bgp/reactor one line per package, at a level three new struct fields on an existing type do not change. No package is added, removed or repurposed. | open |
| 2026-09-11 | fcd89c36 | bgp: hold the initial routing update until the RIB converges | native structural checks (red) | doc wiring is red for 29 files of other sessions (ike, bfd, config/transaction, bgp/message, core/bgp/attribute) plus five design-refs of the paths-limit and as-notation work, so the stage is red without this commit. Its rows for THIS spec are the park cycle showing through: docs/guide/configuration.md and docs/features/bgp-protocol.md landed in HEAD while the code was parked, so their anchors now see update_delay.go and ParseUpdateDelay change with no page edge beside them. Both pages were read against their producers in this closure and are accurate, so editing them to move the checker would be prose written for a gate | open |
| 2026-09-11 | fcd89c36 | bgp: hold the initial routing update until the RIB converges | discovery-index freshness | ai/PACKAGE-MAP.md was regenerated with ./le discovery-index update and came back byte-identical: this spec adds files to packages the map already names and creates no package | open |
