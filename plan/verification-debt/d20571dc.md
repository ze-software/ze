# Verification debt -- commit session d20571dc

Gates that had not run green over these commits when they were made.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-06 | d20571dc | fix(le): the test ledgers get one shard per commit session | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=130, at 2026-09-05T17:25:15Z) | open |
| 2026-09-06 | d20571dc | fix(le): the test ledgers get one shard per commit session | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-09-06 | d20571dc | fix(le): the test ledgers get one shard per commit session | owner approval for an RFC-tagged test change | Both named units are the gate's own FIXTURES and neither is evidence behind a published claim: the tag is a string literal inside proposed_test.go and audit_test.go (RFC requirement: RFC2119-1-1), an id with no rfc/short/ summary and no row in ai/RFC-REQUIREMENTS.md. The edits retarget those fixtures at the shard path and change no assertion. An owner-approval row for them would be a forgery under the rule this ledger exists to enforce. | open |
