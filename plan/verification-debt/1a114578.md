# Verification debt -- commit session 1a114578

Gates that had not run green over these commits when they were made.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-07 | 1a114578 | fix(bgp): the relay rail sizes the header the peer's flags ask for | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=130, at 2026-09-05T17:25:15Z) | open |
| 2026-09-07 | 1a114578 | fix(bgp): the relay rail sizes the header the peer's flags ask for | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-09-07 | 1a114578 | fix(bgp): the relay rail sizes the header the peer's flags ask for | discovery-index freshness | adds no package and edits no // Package doc comment, so ai/PACKAGE-MAP.md, which carries one line per package taken from that comment, is unchanged by this commit | open |
