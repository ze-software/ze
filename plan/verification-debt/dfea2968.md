# Verification debt -- commit session dfea2968

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row by running the gate over the
committed code and setting Status to `cleared`, or delete the shard once
every row is cleared. `scripts/dev/commit_helper.py create --push` refuses
while any row here is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-18 | dfea2968 | fix(gates): follow $(MAKE) delegation when reading a make recipe | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red (2026-08-18); ze-doc-drift-check and ze-unit-pkg-test on ./scripts/evidence/ and ./scripts/status/... are green on the changed packages | open |
| 2026-08-18 | dfea2968 | fix(gates): follow $(MAKE) delegation when reading a make recipe | ze-precommit-verify structural gates (red) | the record's structural reds are ze-doc-wiring-check and ze-doc-links-check, both blocked on another session's in-flight ipsec-interop rename and its RETIRED.md rows; this commit touches neither, and it FIXES the third (doc_drift) | open |
| 2026-08-18 | dfea2968 | fix(gates): follow $(MAKE) delegation when reading a make recipe | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has completed since these edits; the harness caps a foreground call below a full pass and the owner runs it | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on the table, not a line count | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red (2026-08-18); make ze-unit-pkg-test PKG=./scripts/status/... is green | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on the table, not a line count | ze-precommit-verify structural gates (red) | the record's structural reds (ze-doc-wiring-check, ze-doc-links-check) are another session's in-flight ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on the table, not a line count | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has completed since these edits; the harness caps a foreground call below a full pass and the owner runs it | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on the table, not a line count | discovery-index freshness | ai/PACKAGE-MAP.md's only working-tree change is three rows for internal/plugins/anomaly/observe, an UNTRACKED plugin another session is still building. Committing the index here would publish rows pointing at files absent from HEAD, which is the incoherence discovery_index_problems exists to refuse. The specmeta row lands with whoever commits that plugin, or with a later regeneration | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on its table | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red (2026-08-18); make ze-unit-pkg-test PKG=./scripts/status/... is green | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on its table | ze-precommit-verify structural gates (red) | the record's structural reds (ze-doc-wiring-check, ze-doc-links-check) are another session's in-flight ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on its table | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has completed since these edits; the harness caps a foreground call below a full pass and the owner runs it | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on its table | discovery-index freshness | ai/PACKAGE-MAP.md's only working-tree change is three rows for internal/plugins/anomaly/observe, an UNTRACKED plugin another session is still building. Committing the index here would publish rows pointing at files absent from HEAD, which is the incoherence discovery_index_problems exists to refuse | open |
