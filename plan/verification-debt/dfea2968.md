# Verification debt -- commit session dfea2968

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row by running the gate over the
committed code and setting Status to `cleared`, or delete the shard once
every row is cleared. `scripts/dev/commit_helper.py create --push` refuses
while any row here is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-18 | dfea2968 | fix(gates): follow $(MAKE) delegation when reading a make recipe | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red (2026-08-18); ze-doc-drift-check and ze-unit-pkg-test on ./scripts/evidence/ and ./scripts/status/... are green on the changed packages | open |
| 2026-08-18 | dfea2968 | fix(gates): follow $(MAKE) delegation when reading a make recipe | ze-precommit-verify structural gates (red) | the record's structural reds are ze-doc-wiring-check and ze-doc-links-check, both blocked on another session's in-flight ipsec-interop rename and its RETIRED.md rows; this commit touches neither, and it FIXES the third (doc_drift) | open |
| 2026-08-18 | dfea2968 | fix(gates): follow $(MAKE) delegation when reading a make recipe | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has completed since these edits; the harness caps a foreground call below a full pass and the owner runs it | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on the table, not a line count | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red (2026-08-18); make ze-unit-pkg-test PKG=./scripts/status/... is green | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on the table, not a line count | ze-precommit-verify structural gates (red) | the record's structural reds (ze-doc-wiring-check, ze-doc-links-check) are another session's in-flight ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on the table, not a line count | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has completed since these edits; the harness caps a foreground call below a full pass and the owner runs it | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on the table, not a line count | discovery-index freshness | ai/PACKAGE-MAP.md's only working-tree change is three rows for internal/plugins/anomaly/observe, an UNTRACKED plugin another session is still building. Committing the index here would publish rows pointing at files absent from HEAD, which is the incoherence discovery_index_problems exists to refuse. The specmeta row lands with whoever commits that plugin, or with a later regeneration | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on its table | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red (2026-08-18); make ze-unit-pkg-test PKG=./scripts/status/... is green | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on its table | ze-precommit-verify structural gates (red) | the record's structural reds (ze-doc-wiring-check, ze-doc-links-check) are another session's in-flight ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on its table | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has completed since these edits; the harness caps a foreground call below a full pass and the owner runs it | open |
| 2026-08-18 | dfea2968 | fix(status): anchor the spec metadata parse on its table | discovery-index freshness | ai/PACKAGE-MAP.md's only working-tree change is three rows for internal/plugins/anomaly/observe, an UNTRACKED plugin another session is still building. Committing the index here would publish rows pointing at files absent from HEAD, which is the incoherence discovery_index_problems exists to refuse | open |
| 2026-08-18 | dfea2968 | fix(test-runner): let a linger rejection reach the verdict | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red (2026-08-18); TestPeerVerdictFailsOnRetractionAfterSuccess and TestRejectedPrintsTheRejectionMarker are green, and golangci-lint on both packages reports 0 issues | open |
| 2026-08-18 | dfea2968 | fix(test-runner): let a linger rejection reach the verdict | ze-precommit-verify structural gates (red) | the record's structural reds (ze-doc-wiring-check, ze-doc-links-check) are another session's in-flight ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | fix(test-runner): let a linger rejection reach the verdict | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has completed since these edits; the harness caps a foreground call below a full pass and the owner runs it | open |
| 2026-08-18 | dfea2968 | fix(commit-gate): seed the generator the index gate reads | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red (2026-08-18); make ze-unit-hook-test is 473/473 and the five fixtures were proven to discriminate by stubbing the producer | open |
| 2026-08-18 | dfea2968 | fix(commit-gate): seed the generator the index gate reads | ze-precommit-verify structural gates (red) | the record's structural reds (ze-doc-wiring-check, ze-doc-links-check) are another session's in-flight ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | fix(anomaly): stop rare-port firing for every ordinary receiver | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red (2026-08-18); make ze-unit-pkg-test PKG=./internal/plugins/anomaly/detect/ is green, and the test failed 3/3 before the change | open |
| 2026-08-18 | dfea2968 | fix(anomaly): stop rare-port firing for every ordinary receiver | ze-precommit-verify structural gates (red) | the record's structural reds (ze-doc-wiring-check, ze-doc-links-check) are another session's in-flight ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | fix(anomaly): stop rare-port firing for every ordinary receiver | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has completed since these edits; the harness caps a foreground call below a full pass and the owner runs it | open |
| 2026-08-18 | dfea2968 | docs: repair dead citations and record two journal rows | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red (2026-08-18); check_doc_links.py reports 0 dead path references after these edits | open |
| 2026-08-18 | dfea2968 | docs: repair dead citations and record two journal rows | ze-precommit-verify structural gates (red) | ze-doc-links-check's remaining red is the doc_citation_baseline.txt shrink-only rule against another session's in-flight ipsec-interop rename, which this commit does not touch; ze-doc-wiring-check's is that session's RETIRED.md rows | open |
| 2026-08-18 | dfea2968 | docs: repair dead citations and record two journal rows | independent critical review | no spec closes here: this commit carries citation repairs and two journal rows, and touches no spec's Review Gate or closure prose. The four specs it edits gain only per-line doc-links markers on paths their own unimplemented work will create | open |
| 2026-08-18 | dfea2968 | rules: give a capped harness a route to run the verify | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red (2026-08-18); make ze-rules-points-roundtrip-check reports all 29 rules round-trip byte-identical | open |
| 2026-08-18 | dfea2968 | rules: give a capped harness a route to run the verify | ze-precommit-verify structural gates (red) | the record's structural reds (ze-doc-wiring-check, ze-doc-links-check) are another session's in-flight ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | perf(reactor): remove the eBGP wire cache and its benchmarks | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red; reactor is green race-instrumented and ze-lint-changed reports 0 issues | open |
| 2026-08-18 | dfea2968 | perf(reactor): remove the eBGP wire cache and its benchmarks | ze-precommit-verify structural gates (red) | the record's structural reds are another session's ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | perf(reactor): remove the eBGP wire cache and its benchmarks | full ze-precommit-verify over this commit's Go | no full ze-precommit-verify has completed since these edits; the harness caps a foreground call below a full pass | open |
| 2026-08-18 | dfea2968 | perf(reactor): remove the eBGP wire cache and its benchmarks | discovery-index freshness | ai/PACKAGE-MAP.md's only change is three rows for an UNTRACKED plugin another session is still building | open |
| 2026-08-18 | dfea2968 | perf(reactor): remove the eBGP wire cache and its benchmarks | independent critical review | no spec closes here: another session's cache removal, committed to unblock the shared ledger | open |
| 2026-08-18 | dfea2968 | test(isis): drive TLV decoders from wire bytes, not our encoder | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red; isis is green and lint reports 0 issues | open |
| 2026-08-18 | dfea2968 | test(isis): drive TLV decoders from wire bytes, not our encoder | ze-precommit-verify structural gates (red) | the structural reds are another session's ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | test(isis): drive TLV decoders from wire bytes, not our encoder | full ze-precommit-verify over this commit's Go | no full verify has completed since these edits; the harness caps a foreground call below a full pass | open |
| 2026-08-18 | dfea2968 | test(isis): drive TLV decoders from wire bytes, not our encoder | discovery-index freshness | PACKAGE-MAP's only change is rows for an untracked plugin another session is still building | open |
| 2026-08-18 | dfea2968 | test(isis): drive TLV decoders from wire bytes, not our encoder | independent critical review | no spec closes here: another session's test-shape change, committed to drain the shared ledger | open |
| 2026-08-18 | dfea2968 | fix(flowspec): refuse a rule it cannot translate, never widen it | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red; 25 packages green under go test -race and ze-functional-parse-test passes 311/311 | open |
| 2026-08-18 | dfea2968 | fix(flowspec): refuse a rule it cannot translate, never widen it | ze-precommit-verify structural gates (red) | the structural reds are another session's ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | fix(flowspec): refuse a rule it cannot translate, never widen it | full ze-precommit-verify over this commit's Go | no full verify has completed since these edits; the harness caps a foreground call below a full pass | open |
| 2026-08-18 | dfea2968 | fix(flowspec): refuse a rule it cannot translate, never widen it | discovery-index freshness | PACKAGE-MAP's only change is rows for an untracked plugin another session is still building | open |
| 2026-08-18 | dfea2968 | fix(flowspec): refuse a rule it cannot translate, never widen it | independent critical review | the spec does not close here: code and tests only, spec stays open for its Review Gate | open |
| 2026-08-18 | dfea2968 | fix(irr): an empty answer must not empty a live filter | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red; irr and firewall are green under go test -race, and 9 of 9 firewall-irr .ci pass under unshare -Urn, up from 0 of 9 | open |
| 2026-08-18 | dfea2968 | fix(irr): an empty answer must not empty a live filter | ze-precommit-verify structural gates (red) | the structural reds are another session's ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | fix(irr): an empty answer must not empty a live filter | full ze-precommit-verify over this commit's Go | no full verify has completed since these edits; the harness caps a foreground call below a full pass | open |
| 2026-08-18 | dfea2968 | fix(irr): an empty answer must not empty a live filter | discovery-index freshness | PACKAGE-MAP's only change is rows for an untracked plugin another session is still building | open |
| 2026-08-18 | dfea2968 | fix(irr): an empty answer must not empty a live filter | independent critical review | neither spec closes here: code and tests only, both stay open for their Review Gate | open |
| 2026-08-18 | dfea2968 | fix(appliance): validate TLS material before replacing it | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red; internal/appliance is green under go test -race and ze-functional-appliance-test passes appliance-replace-cert | open |
| 2026-08-18 | dfea2968 | fix(appliance): validate TLS material before replacing it | ze-precommit-verify structural gates (red) | the structural reds are another session's ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | fix(appliance): validate TLS material before replacing it | full ze-precommit-verify over this commit's Go | no full verify has completed since these edits; the harness caps a foreground call below a full pass | open |
| 2026-08-18 | dfea2968 | fix(appliance): validate TLS material before replacing it | discovery-index freshness | PACKAGE-MAP's only change is rows for an untracked plugin another session is still building | open |
| 2026-08-18 | dfea2968 | fix(appliance): validate TLS material before replacing it | independent critical review | the spec does not close here: code and tests only, spec stays open for its Review Gate | open |
| 2026-08-18 | dfea2968 | fix(iface): apply the selector the resolver already honors | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red; iface, plugins/iface and doctor are green under make ze-unit-pkg-test, and the integration and .ci tests pass in a private netns | open |
| 2026-08-18 | dfea2968 | fix(iface): apply the selector the resolver already honors | ze-precommit-verify structural gates (red) | the structural reds are another session's ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | fix(iface): apply the selector the resolver already honors | full ze-precommit-verify over this commit's Go | no full verify has completed since these edits; the harness caps a foreground call below a full pass | open |
| 2026-08-18 | dfea2968 | fix(iface): apply the selector the resolver already honors | discovery-index freshness | PACKAGE-MAP's only change is rows for an untracked plugin another session is still building | open |
| 2026-08-18 | dfea2968 | fix(iface): apply the selector the resolver already honors | independent critical review | neither spec closes here: code and tests only, both stay open for their Review Gate | open |
| 2026-08-18 | dfea2968 | fix(ike): keep each end's own Message ID counter | ze-precommit-verify (not FRESH-green) | owner instructed the commit to land with the gate red; make ze-rfc-check exits 0 at 2966 gated requirements and 3558 tags, ike is green across 9 packages, and ze-test ipsec --all passes 17/17 in QEMU | open |
| 2026-08-18 | dfea2968 | fix(ike): keep each end's own Message ID counter | ze-precommit-verify structural gates (red) | the structural reds are another session's ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | fix(ike): keep each end's own Message ID counter | full ze-precommit-verify over this commit's Go | no full verify has completed since these edits; the harness caps a foreground call below a full pass | open |
| 2026-08-18 | dfea2968 | fix(ike): keep each end's own Message ID counter | discovery-index freshness | PACKAGE-MAP's only change is rows for an untracked plugin another session is still building | open |
| 2026-08-18 | dfea2968 | fix(ike): keep each end's own Message ID counter | independent critical review | the spec does not close here: code, tests and the RFC row only, spec stays open for its Review Gate | open |
| 2026-08-18 | dfea2968 | test(flowspec): spell two compound test files as the repo does | ze-precommit-verify (not FRESH-green) | owner instructed commits to land with the gate red; ./internal/plugins/flowspec-firewall/ is green after the rename | open |
| 2026-08-18 | dfea2968 | test(flowspec): spell two compound test files as the repo does | ze-precommit-verify structural gates (red) | the structural reds are another session's ipsec-interop rename and its RETIRED.md rows; this commit touches neither | open |
| 2026-08-18 | dfea2968 | test(flowspec): spell two compound test files as the repo does | full ze-precommit-verify over this commit's Go | no full verify has completed since these edits; the harness caps a foreground call below a full pass | open |
| 2026-08-18 | dfea2968 | test(flowspec): spell two compound test files as the repo does | discovery-index freshness | PACKAGE-MAP's only change is rows for an untracked plugin another session is still building | open |
| 2026-08-18 | dfea2968 | test(flowspec): spell two compound test files as the repo does | independent critical review | no spec closes here: a file rename and a prose correction to the weakened-test ledger | open |
| 2026-08-18 | dfea2968 | test(flowspec): spell two compound test files as the repo does | ze-precommit-verify (not FRESH-green) | owner instructed commits to land with the gate red; ./internal/plugins/flowspec-firewall/ is green after the rename | open |
| 2026-08-18 | dfea2968 | test(flowspec): spell two compound test files as the repo does | ze-precommit-verify structural gates (red) | session 5dfa0c47 has since greened ze-lint, ze-generated-files-check and ze-doc-wiring-check; the remaining doc-links red is doc_citation_baseline.txt being shrink-only against the ipsec-interop rename | open |
| 2026-08-18 | dfea2968 | test(flowspec): spell two compound test files as the repo does | full ze-precommit-verify over this commit's Go | no full verify has completed since these edits; a full run follows now that the structural gates are green | open |
| 2026-08-18 | dfea2968 | test(flowspec): spell two compound test files as the repo does | discovery-index freshness | PACKAGE-MAP is now owned by session 5dfa0c47's anomaly-observe commit; this rename adds no package | open |
| 2026-08-18 | dfea2968 | test(flowspec): spell two compound test files as the repo does | independent critical review | no spec closes here: a file rename and a prose correction to the weakened-test ledger | open |
| 2026-08-18 | dfea2968 | docs: shrink the citation baseline instead of regrowing it | ze-precommit-verify (not FRESH-green) | owner instructed commits to land with the gate red; make ze-doc-links-check now exits 0 | open |
| 2026-08-18 | dfea2968 | docs: shrink the citation baseline instead of regrowing it | ze-precommit-verify structural gates (red) | the verify record is from 09:27 and predates this fix and session 5dfa0c47's; this commit is what greens ze-doc-links-check, and a full verify follows to refresh the record | open |
| 2026-08-18 | dfea2968 | docs: shrink the citation baseline instead of regrowing it | independent critical review | no spec closes here: citation markers and a baseline shrink | open |
| 2026-08-18 | dfea2968 | fix(hooks): gate shell writes to the spec and rule trees | ze-precommit-verify (not FRESH-green) | owner instructed commits to land with the gate red; make ze-unit-hook-test is 489/489 and the check was probed live against five real command shapes | open |
| 2026-08-18 | dfea2968 | fix(hooks): gate shell writes to the spec and rule trees | ze-precommit-verify structural gates (red) | the verify record is from 09:27 and predates today's fixes; the four structural gates are green now and a full verify follows | open |
| 2026-08-18 | dfea2968 | fix(hooks): gate shell writes to the spec and rule trees | independent critical review | no spec closes here: a hook check and its fixtures | open |
