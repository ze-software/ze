# Verification debt -- commit session 58c78b78

Gates that had not run green over these commits when they were made.
One row holds one gate and one reason, and covers every commit this
session made under it. `git log -- <this file>` names those commits.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-12 | 58c78b78 | feat(ipsec): ze reads its own dataplane back from the kernel (+4 more) | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=1, at 2026-09-11T10:56:02Z) | open |
| 2026-09-12 | 58c78b78 | feat(ipsec): ze reads its own dataplane back from the kernel | native structural checks (red) | tier/check and iface-resolution both exit 0 when run against this tree from a session-private le build, and the certificate charges them for want of path attribution rather than an observed red. doc wiring is red, and every one of its ten remaining rows names a file this commit does not carry: configvalue, bgp redistribute_binding and replay, bmp_locrib, ike rekey and responder_eap, qemu vrrp_keepalived, le/ste. The fourteen rows that named this commit's own symbols were repaired in it, across eight pages. | open |
| 2026-09-12 | 58c78b78 | feat(ipsec): ze reads its own dataplane back from the kernel | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-09-12 | 58c78b78 | chore(plan): close spec-ipsec-dataplane-inspection | native structural checks (red) | tier/check and iface-resolution both exit 0 against this tree from a session-private le build; the certificate charges them for want of path attribution, not an observed red. This commit removes one markdown file, which neither gate reads. | open |
| 2026-09-12 | 58c78b78 | chore(journal): a closed spec survives on disk as an untracked copy | native structural checks (red) | tier/check and iface-resolution both exit 0 against this tree from a session-private le build, and are charged for want of path attribution. This commit adds one markdown file, which no structural gate reads. | open |
| 2026-09-12 | 58c78b78 | chore(handover): the IPsec spec the handover routes to is closed | native structural checks (red) | tier/check and iface-resolution exit 0 against this tree from a session-private le build, and are charged for want of path attribution. This commit edits one markdown file at the repository root, which no structural gate reads. | open |
| 2026-09-12 | 58c78b78 | chore(journal): land two rows whose sessions are gone | native structural checks (red) | tier/check and iface-resolution exit 0 against this tree from a session-private le build, and are charged for want of path attribution. This commit adds two journal rows, which no structural gate reads. | open |
