# Verification debt -- commit session 34f056af

Gates that had not run green over these commits when they were made.
Each row is work owed, not a defect: a defect is fixed, never recorded
(`ai/rules/completion.md`). Clear a row with `./le commit debt-clear`:
it re-runs the gate the row names and writes `cleared` only when that
run exits 0. Every gate runs inside one throwaway worktree at HEAD, so
a cleared row says the gate was green over the COMMIT rather than
beside the uncommitted files several sessions keep in this checkout.
When no worktree can be made, nothing clears and the pass exits 1: the
fallback it refuses, judging the working tree, is the whole reason the
worktree is there. A human MAY delete the
shard once every row is cleared.
the retired `scripts/dev/commit_helper.py create --push` (current producer: `internal/le/commit/prepare.go`) refuses while any row here
is open.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-08-25 | 34f056af | fix(demos): make each transcript state what its demo shows | ./le verify current mode full (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
| 2026-08-25 | 34f056af | fix(demos): record a demo that shows a clock at wall-clock speed | ./le verify current mode full (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
| 2026-08-25 | 34f056af | fix(cli): make the selected peer row readable | ./le verify current mode full (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
| 2026-08-25 | 34f056af | fix(cli): make the selected peer row readable | full ./le verify current mode full over this commit's Go | no full ./le verify current mode full recorded (tmp/ze-verify-full.json is missing) | open |
| 2026-08-25 | 34f056af | fix(bgp): read the local AS from the path the schema declares | ./le verify current mode full (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
| 2026-08-25 | 34f056af | fix(bgp): read the local AS from the path the schema declares | full ./le verify current mode full over this commit's Go | no full ./le verify current mode full recorded (tmp/ze-verify-full.json is missing) | open |
| 2026-08-25 | 34f056af | fix(cli): mark the selected peer row without a background | ./le verify current mode full (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
| 2026-08-25 | 34f056af | fix(cli): mark the selected peer row without a background | full ./le verify current mode full over this commit's Go | no full ./le verify current mode full recorded (tmp/ze-verify-full.json is missing) | open |
| 2026-08-25 | 34f056af | fix(cli): print the idle banner once above the prompt | ./le verify current mode full (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
| 2026-08-25 | 34f056af | fix(cli): print the idle banner once above the prompt | full ./le verify current mode full over this commit's Go | no full ./le verify current mode full recorded (tmp/ze-verify-full.json is missing) | open |
| 2026-08-25 | 34f056af | fix(website): version the cast URL from the manifest at runtime | ./le verify current mode full (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
| 2026-08-25 | 34f056af | fix(cli): give every command one error surface | ./le verify current mode full (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
| 2026-08-25 | 34f056af | fix(cli): give every command one error surface | full ./le verify current mode full over this commit's Go | no full ./le verify current mode full recorded (tmp/ze-verify-full.json is missing) | open |
| 2026-08-25 | 34f056af | fix(cli): refresh the bgp dashboard once a second | ./le verify current mode full (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
| 2026-08-25 | 34f056af | fix(cli): refresh the bgp dashboard once a second | full ./le verify current mode full over this commit's Go | no full ./le verify current mode full recorded (tmp/ze-verify-full.json is missing) | open |
| 2026-08-25 | 34f056af | docs(guide): let the setup check report its own exit code | ./le verify current mode full (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
| 2026-08-25 | 34f056af | fix(website): round every published magnitude to its boundary | ./le verify current mode full (not FRESH-green) | verify-status is not FRESH-green: STALE: no status file (never verified) | open |
