# Verification debt -- commit session 6c8300b9

Gates that had not run green over these commits when they were made.
One row holds one gate and one reason, and covers every commit this
session made under it. `git log -- <this file>` names those commits.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-08 | 6c8300b9 | build: bump eight go modules, reapply the two vendor patches (+4 more) | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=1, at 2026-09-05T00:11:54Z) | open |
| 2026-09-08 | 6c8300b9 | build: bump eight go modules, reapply the two vendor patches (+3 more) | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-09-08 | 6c8300b9 | build: bump eight go modules, reapply the two vendor patches (+1 more) | discovery-index freshness | the discovery index is generated over internal, pkg and cmd with vendor in its skip set, so no vendored Go file this commit carries can change ai/PACKAGE-MAP.md; recorded in plan/journal/gate-fires-outside-its-population.md | open |
| 2026-09-08 | 6c8300b9 | the index-freshness trigger reads the walk's own population | discovery-index freshness | the marker search runs over the whole file, so sources.go matches on its own packageMarker constant and on the marker quoted in feeds' doc comment; it describes no package the map covers, and le discovery-index check reports ai/PACKAGE-MAP.md up to date over 766 packages. The committed map is held back because another session's uncommitted hunk in it names internal/core/bgp/asn, a package HEAD does not carry | open |
| 2026-09-08 | 6c8300b9 | the index-freshness trigger reads the header, not the whole file | discovery-index freshness | the index is stale on internal/core/bgp/asn, an untracked package another session is writing (asn.go, 16:34 today); HEAD's map has no row for it. le discovery-index check reported the map up to date at 15:58, after this commit's own edits to internal/le/discoveryindex were in the tree, and none of the three Go files here carries a package-header line or is a register.go, so no row this commit could change is among the stale ones | open |
