# Verification debt -- commit session 69f4bea4

Gates that had not run green over these commits when they were made.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-05 | 69f4bea4 | fix(qemu): all-tests keeps its transport and accounts for every phase | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=130, at 2026-09-05T17:13:05Z) | open |
| 2026-09-05 | 69f4bea4 | fix(qemu): all-tests keeps its transport and accounts for every phase | native structural checks (red) | verify lint/run is red on 51 files, every one of them another session's in-flight work (internal/component/ike, internal/component/pki, internal/core/eap, internal/le/verify, internal/le/functional/exabgp.go). Its own failure group names none of the files in this commit, and the two packages this commit touches, internal/le/qemu and internal/le/gaterun, report 0 issues in that same run | open |
| 2026-09-05 | 69f4bea4 | fix(qemu): all-tests keeps its transport and accounts for every phase | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-09-05 | 69f4bea4 | fix(qemu): all-tests keeps its transport and accounts for every phase | discovery-index freshness | ran ./le discovery-index update: it rewrote ai/PACKAGE-MAP.md byte identical, because this change adds files to an existing package and no package | open |
