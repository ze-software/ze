# Verification debt -- commit session 4350711e

Gates that had not run green over these commits when they were made.
One row holds one gate and one reason, and covers every commit this
session made under it. `git log -- <this file>` names those commits.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-11 | 4350711e | the package map is derived on demand, and staleness is loud (+14 more) | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=1, at 2026-09-11T10:56:02Z) | open |
| 2026-09-11 | 4350711e | the package map is derived on demand, and staleness is loud | native structural checks (red) | tier/check and iface-resolution are red before this commit and the gate charges them for want of path attribution: neither names a file this commit carries. doc wiring's one finding against this commit is fullStages changing under the gh-pages.md anchor, and that page claims only that the site publishes the stage population it reads, which removing a stage does not falsify. | open |
| 2026-09-11 | 4350711e | the package map is derived on demand, and staleness is loud (+13 more) | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-09-11 | 4350711e | a derived artifact is rebuilt when missing, not on every session start (+1 more) | native structural checks (red) | tier/check and iface-resolution are red before this commit and name no file it carries. doc wiring's finding against it is fullStages under the gh-pages.md anchor, whose claim removing a stage does not falsify. | open |
| 2026-09-11 | 4350711e | the session-start budget comment names what actually spends it | native structural checks (red) | tier/check and iface-resolution are red before this commit and name no file it carries. doc wiring's finding is fullStages under the gh-pages.md anchor, whose claim this commit does not touch. | open |
| 2026-09-11 | 4350711e | the pages say a derived artifact is built only when it is absent | native structural checks (red) | both reds are at HEAD and name files this commit does not carry. tier/check names three committed internal/le/interoplab/pppoe/ files importing internal/component/l2tp/pppoe without //go:build ze_l2tp, already rowed on 2026-09-09 in plan/journal/feature-test-missing-build-tag.md. iface-resolution names a dead allowlist entry for internal/le/interoplab/bgp/isis_inject_linux.go, rowed today in plan/journal/claim-outlives-the-evidence-it-cites.md. This commit's only Go hunk is a comment rewrap in internal/le/hookruntime/lifecycle.go, which neither gate reads | open |
| 2026-09-11 | 4350711e | close the spec: the package map is derived on demand | native structural checks (red) | the same two HEAD reds commit A carries. tier/check names three committed internal/le/interoplab/pppoe/ files importing internal/component/l2tp/pppoe without //go:build ze_l2tp, rowed on 2026-09-09 in plan/journal/feature-test-missing-build-tag.md. iface-resolution names a dead allowlist entry for internal/le/interoplab/bgp/isis_inject_linux.go, rowed today in plan/journal/claim-outlives-the-evidence-it-cites.md. This commit removes one markdown file and neither gate reads it | open |
| 2026-09-11 | 4350711e | gate the PPPoE wire checkers and repoint the iface allowlist (+1 more) | native structural checks (red) | this commit IS the repair for both recorded reds, so tmp/ze-verify-failures.json cannot be green until it lands. After it, ./le tier check exits 0 and ./le iface-resolution answers OK with exit 0, both run from a session-private le build of this tree; ./le commit debt-clear re-runs both against HEAD. | open |
| 2026-09-11 | 4350711e | the RFC ledger family is derived on demand, not committed | native structural checks (red) | the reds at HEAD are other sessions committed work and name no file this commit carries: le site build refuses on a go.mod requirement with no dependencies.json entry, three internal/le/rfc tests pin hand-maintained counts, and internal/le/site goldens are stale. All are journalled. | open |
| 2026-09-12 | 4350711e | a derived artifact declares whether the session hook builds it | native structural checks (red) | the reds at HEAD are other sessions committed work and name no file this commit carries: le site build refuses on a go.mod requirement with no dependencies.json entry, and two internal/le/rfc tests pin hand-maintained counts that the committed summary corpus moved. Both are journalled. | open |
| 2026-09-12 | 4350711e | the package map registration declares its session-start policy | native structural checks (red) | the reds at HEAD are other sessions committed work and name no file this commit carries. | open |
| 2026-09-12 | 4350711e | a spec under plan feeds the RFC ledger, and the page says which | native structural checks (red) | three reds in internal/le/rfc are at HEAD and are not this commit: two tests pin hand-maintained counts the committed summary corpus moved, and the native-implementation fixture digests HEAD and is one commit behind by its own design. All are journalled. | open |
| 2026-09-12 | 4350711e | a spec under plan feeds the RFC ledger, and two pins move | native structural checks (red) | the native RFC fixture digests HEAD's own blobs for this package, so it is one commit behind by its own design and is resealed in the commit after this one. The other reds at HEAD are other sessions committed work. | open |
| 2026-09-12 | 4350711e | reseal the native RFC fixture over the ledger move | native structural checks (red) | the remaining reds at HEAD are other sessions committed work and name no file this commit carries. | open |
| 2026-09-12 | 4350711e | close the derived-indexes spec, and fix the page AC-6 made wrong | full native verification (not FRESH-green) | The shared certificate is STALE from another session's failed run at 2026-09-11T10:56:02Z. This commit's own evidence: the twelve tests this spec names pass over nine packages, and ./le verify lint run reports no finding in any file it carries. | open |
| 2026-09-12 | 4350711e | close the derived-indexes spec, and fix the page AC-6 made wrong (+1 more) | native structural checks (red) | Both charged gates were run live against a binary built from this tree and both exit 0: ./le tier check says engine placement clean, and ./le iface-resolution says OK. They are charged for want of path attribution against a stale certificate, not observed red. | open |
| 2026-09-12 | 4350711e | close the derived-indexes spec, and fix the page AC-6 made wrong | full native verification over this commit's Go | The Go this commit carries is two comment blocks in internal/le/rfc test files. No statement, declaration or signature changes, and the rfc package tests pass. | open |
| 2026-09-12 | 4350711e | spec: close spec-derived-indexes-answer-a-query | full native verification (not FRESH-green) | The shared certificate is STALE from another session's failed run at 2026-09-11T10:56:02Z. This commit removes one markdown file and carries no code. | open |
| 2026-09-12 | 4350711e | journal: closure commit B leaks the spec onto disk, fourth time | full native verification (not FRESH-green) | The shared certificate is STALE from another session's failed run at 2026-09-11T10:56:02Z. This commit adds one journal row and carries no code. | open |
| 2026-09-12 | 4350711e | journal: closure commit B leaks the spec onto disk, fourth time | native structural checks (red) | Both charged gates were run live against a binary built from this tree and both exit 0: ./le tier check says engine placement clean, and ./le iface-resolution says OK. Charged for want of path attribution against a stale certificate, not observed red. | open |
| 2026-09-12 | 4350711e | the rib and ospf test call sites follow the signature that changed | native structural checks (red) | tier/check and iface-resolution are charged from tmp/ze-verify-failures.json, written 2026-09-11T09:58Z. Both were re-run against this tree with a freshly built binary and exit zero: tier check prints engine placement clean and core import direction clean, iface-resolution prints OK. The recorded iface red named an allowlist entry for internal/le/interoplab/bgp/isis_inject_linux.go that the committed map no longer holds, so only a stale bin/le still reproduces it. Neither gate reads a file this commit carries. | open |
