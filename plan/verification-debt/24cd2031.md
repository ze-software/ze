# Verification debt -- commit session 24cd2031

Gates that had not run green over these commits when they were made.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-05 | 24cd2031 | fix(bgp): key one message's announced paths instead of rescanning them (+1 more) | full native verification (not FRESH-green) | verify-status is not FRESH-green: STALE: last verify failed (exit=1, at 2026-09-05T15:41:36Z) | open |
| 2026-09-05 | 24cd2031 | fix(bgp): key one message's announced paths instead of rescanning them | native structural checks (red) | the doc wiring red names 21 files and none of them is in this commit: a design-refs group of 16 files across ike, sysrib, capture, fib and the replay tooling, and a doc-drift group over docs/architecture/testing/qemu-integration.md, verify-freshness-scope.md, internal/le/commit/actions.go and internal/le/qemu, which is the work another session has open in this checkout | open |
| 2026-09-05 | 24cd2031 | fix(bgp): key one message's announced paths instead of rescanning them | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-09-05 | 24cd2031 | chore(plan): close the RFC 7911 own-path-id spec | native structural checks (red) | the same doc wiring red as the commit before this one: 21 files, none of them in either commit, and this one removes a spec and adds nothing | open |
