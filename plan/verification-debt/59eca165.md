# Verification debt -- commit session 59eca165

Gates that had not run green over these commits when they were made.
Clear rows only through `le commit debt-clear` after the named gate exits 0.

| Date | Session | Subject | Gate owed | Reason | Status |
|------|---------|---------|-----------|--------|--------|
| 2026-09-05 | 59eca165 | spec(bgp): close the protocol event capture and replay spec | full native verification (not FRESH-green) | verify worktree not run: the shared job queue is held by a peer session's lint; targeted unit, functional and repository checks are green and named in the body | open |
| 2026-09-05 | 59eca165 | spec(bgp): close the protocol event capture and replay spec | full native verification over this commit's Go | no full native verification covers this commit's Go | open |
| 2026-09-05 | 59eca165 | spec: close spec-improve-3-event-replay | full native verification (not FRESH-green) | verify worktree not run: the shared job queue is held by a peer session's lint | open |
