# Learned-summary staleness baseline -- shrink-only ceiling.
#
# The number below is how many dead references (scripts/dev/learned_staleness.py)
# plan/learned/ is currently tolerated to carry: dead `## Files` paths plus dead
# `plan/learned/NNN` citations. More than this fails the gate; fewer rewrites this
# file down automatically, so the ceiling only ever tightens.
#
# The corpus could not start clean (1,856 dead references when the gate landed),
# so this is the plan/.citation-baseline idiom: grandfather the known rot, refuse
# to let it grow. Raising this number by hand is re-blessing a regression.
339
