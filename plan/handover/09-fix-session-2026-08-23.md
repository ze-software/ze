# Session handoff -- 2026-08-23, session "fix" (ebf5d9b3)

Goal given: review every fixit spec, fix each defect at its source, prioritise RFC MUST
then SHOULD, move genuine improvements to plan/future/, and record root causes as process
rules without case-specific detail.

Stopped on the weekly token limit. All subagents killed. NOTHING PUSHED.

================================================================================
LANDED -- 15 commits
================================================================================

2a3c185f0  rules: a defect record outlives the defect it records
29cfb09fd  fix(ddos): let the baseline follow load it refused to learn
07b4d3304  fix(bgp): count the VPN route distinguisher in the MP_REACH split
710205bd4  fix(bgp): bound an attribute write to the width it declares
dabda7698  fix(mcp): apply the port default the schema declares
d3401fe3b  test(l2tp): prove the RADIUS CoA listener answers on its port
5e5aafea7  fix(rib): the commit rail asks before it encodes a next hop
8b9a8c88a  fix(traffic): bind a reply deadline on the VPP channel        [clean review]
5e14f7f51  fix(ipsec): make a peer reload reach the running tunnels      [clean review]
a40bedef5  fix(ipsec): stage the config with the parser that has no side effects
a71539631  fix(bgp): take the lock on every read of a reloadable peer field
6d3fd922d  docs(journal): three finds from the BGP reactor sweep
5aba59a6b  fix(lint): clear six findings the full gate caught in today's commits
d53e73ea9  fix(eap): stop telling operators to set a GODEBUG that kills ze  [clean review]
(last)     fix(config): read a leaf-list and a list through one reader     [clean review]

The single biggest defect fixed: the IKE engine registered OnConfigVerify and OnConfigure
and NO OnConfigApply. The SDK answers config-apply OK when no handler is registered, so
EVERY IPsec reload verified the edit, logged "config reload completed", and applied
nothing. Measured on a running daemon.

================================================================================
WAITING ON THOMAS -- 2 items
================================================================================

1. TestPrefixExceedTeardown. The RFC 4486 commit is prepared and HELD. The change is one
   assertion, byte(4) -> byte(3), correcting a test that pinned the wrong bytes the defect
   emitted. Strictly a correction, not a weakening. The gate's approval is owner-only and
   no row was written. Everything else for that commit is in the tree.

2. Max-prefix "offered" mode is provably defeatable and no identity-free repair exists.
   Any offered-mode counter is a function of announcements and withdrawals alone; safety
   requires it at or above routes held; an adversary makes every withdrawal unmatched so
   routes-held equals announcements. Any counter that decrements is unsafe after the first
   withdrawal, and the only safe one never decrements and tears down conforming peers.
   Recommendation: make "installed" the default. Second time routed up.

================================================================================
RESUME HERE -- 4 specs mid-review
================================================================================

Artifacts are under tmp/review/<stem>-ebf5d9b3-*.md

1. ORDERED-LIST ORDER LOSS -- the release blocker. Mid-implementation, stopped at
   "all 11 findings are mine, none foreign. Fixing."
   plan/spec-fixit-ordered-list-loses-its-order.md

2. VRRP PARENT SELECTOR -- round 4, 1 BLOCKER.
   `if err != nil || dev == in.dev { in.reconfigure(spec) }` folds two cases. On a
   parentIfindex failure the instance adopts a parent its macvlan is not on, and
   evaluateReadiness then resigns the master and drops the VIP.
   Fix: on err != nil set spec.ParentDevice = in.spec.ParentDevice before reconfiguring.
   FIRST fix the fake: fakePlatform.parentIfindex never errors, so the guard's error arm
   has no test. Add ifindexErr beside the existing macvlanErr. Third round running that
   the fake's inability to express a case is why that case shipped.

3. FLOWSPEC TABLE SWEEP -- round 3 review was mid-flight and stopped saying "the ledger
   deletion went further than reported". Re-run it before trusting round 2. Both round-1
   BLOCKERs were confirmed closed.

4. AAA RELOAD + MIRROR RECONCILE -- round 2, 0 BLOCKER, 2 ISSUE, both test-strength.
   TestApplyConfigRemovesNoMirrorWhenTheDataplaneCannotBeRead reads as a proof and is not
   one: seed mir0 -> cap0 AND have the config ask for it. The ssh wiring line
   (infra_setup.go:174-175) has no test a revert would fail.
   The QEMU red is UNATTRIBUTED, not proven pre-existing: one pass on a tree without the
   change is owed before a push, not before the commit.

================================================================================
OPEN DEFECTS FOUND, NOT FIXED
================================================================================

* A BGP prefix-list with 2+ entries CANNOT LOAD. ParseBGPSubtree receives a keyed map at
  every count, so its slice branch is unreachable and its map branch refuses len > 1.

* Firewall rule order is RANDOMISED on every config load. parseChain ranges a Go map with
  no sort, on a first-match-wins dataplane. Silent.

* Both share one root: ToMap DISCARDS ORDER IT ALREADY HOLDS. Tree.listOrder is maintained
  and GetListOrdered exists; ToMap consults neither.
  Twelve ordered-by user declarations, five broken, three loud and two silent. Derive with:
      grep -rn "ordered-by user" --include='*.yang' internal/

* A sixth unbounded govpp consumer: newVPPBackend (internal/component/ike/dataplane/vpp.go)
  binds no reply deadline and holds its channel for the backend's LIFETIME, so every IPsec
  SA and SPD operation waits on it.

* ze config validate calls CollectListeners, not CollectListenersWithDefaults which sits in
  the same file. Collisions involving a defaulted listener are invisible to the command an
  operator runs to find collisions.

* ospfv3-sr-frr has NEVER exercised OSPFv3 router-information: its ze.conf nests it under
  address-family ipv6 while the schema declares it under ospf, so the parse fails first.
  ospf-sr-frr's SR reception path has never been observed either -- mpls is not installed
  in Dockerfile.ze and Docker Desktop exposes no /proc/sys/net/mpls.

* Interop scenario 04-eap-tls: repurpose its check or retire it. Retiring deletes tracked
  work, so it is Thomas's call. AC-5 of that spec stays open.

* ze-doc-links-check is red on seven dead references, five to
  plan/spec-fixit-redistribute-establishment-stall.md, deleted in 8f3a80bf9 with nothing
  rehomed: ai/rules/testing.md:1154,
  ai/rules/points/testing/ci-sleep-justification/the-specs-that-block-converting-these-sleeps.md:6,
  plan/deferrals/fixit-migrate-sleeps-infra.md:8,
  plan/journal/test-against-broken-path.md:36,
  plan/spec-bgp-session-ready-contract.md:187
  OWNER NOT ESTABLISHED. I earlier read the commit's author field as evidence it was
  Thomas's own work; every commit on this branch is authored AND committed "Thomas Mangin",
  so that field distinguishes nothing. Five of the seven clear in one pass by whoever owns
  the sleeps work.

================================================================================
VERIFICATION STATE
================================================================================

* make ze-repository-tracked-build-check: green at every Go commit.
* make ze-verify-worktree COMMIT=HEAD: RED, and every lint finding in it was MINE.
  All six fixed in 5aba59a6b.
* ~97 verification-debt rows open under session acb7c2cd. --push refuses until cleared.
* ai/PACKAGE-MAP.md carries configvalue but NOT configorder: the sibling's package is still
  uncommitted, and its row was dropped so the index matches the tree the commit produces.
  Regenerate once that lands.

================================================================================
THE TRIAGE RESULT
================================================================================

All 155 live rows across 96 deferral shards were re-verified against HEAD by three
read-only agents.

    23 of 155 recorded defects NO LONGER EXISTED -- fixed, status never updated.
    ~37 still held as genuine defects.
    ~55 were improvements, ~35 not code claims at all.

That 15% figure is why the rule in 2a3c185f0 exists: finding a defect and closing its
record are separate acts done by different sessions, so a backlog drifts toward describing
what was once wrong rather than what is wrong now.

================================================================================
PROCESS NOTES FOR THE NEXT SESSION
================================================================================

* A gate permanently red for foreign reasons stops being a gate. ze-lint-changed lints the
  whole module; it was red on other sessions' in-flight packages all day; every agent
  correctly fell back to a scoped run; six lint defects reached main behind a green nobody
  had asked. Every agent reported "zero findings in my files" and none was lying.

* The full gate cannot converge while three sessions commit every 8-13 minutes. It ran
  cleanly the moment every agent stopped. Run it in a frozen window, or against a commit in
  a worktree -- which is also what catches citations that resolve on the machine that wrote
  them and die in a fresh clone, because it checks the COMMIT rather than the checkout.

* make ze-unit-pkg-test has NO -count=1 hook (mk/test-unit.mk takes only PKG and RUN), so a
  green quoted from it may be cached.

* A backgrounded `<cmd>; echo "exit=$?"` reports the ECHO's status. The gate exited 2 and
  the harness said 0.

* Three separate pieces of uncommitted work were destroyed in the shared tree today by
  three different mechanisms. Confirm an edit with a diff on the path, never with a gate
  whose own population that edit defines.

* test/rfc-changed.md and test/weakened.md hold rows for one pending commit at a time, so
  they serialise commits across concurrent sessions, and their only documented remedy is
  deleting another session's pending row.

* A guard built against a historical defect owes exactly one proof: it must go RED on that
  defect's own text. Not a synthetic fixture, not a mutation chosen against the predicate
  you just wrote.

* Every independent review this session returned findings, and roughly half were defects
  the FIX introduced rather than inherited. Three of four reviews caught something neither
  the implementer nor I would have found: a reader excluded as correct that could not load
  two entries, a fix that missed the case its spec existed for, and a fix that introduced a
  failover.
