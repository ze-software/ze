---
kind: directive
level:
stage:
---
**The re-vendor deletes ~60 tracked files and adds ~60 new ones. Never use bare `git rm`/`git add`: stage the whole change through the commit-helper script at closure so the deletion and addition land in one commit.**
