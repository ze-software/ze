---
kind: directive
level:
stage:
---
**A `SKIP` is not evidence.** Under a hardware accelerator the hugepage proof treats a no-answer as a FAIL; if it skips for want of KVM access, fix that (on Linux, group membership: `make ze-setup CHECK=1`) and rerun.
