# Learned Summaries Index

Curated index of `plan/learned/` summaries that capture structural decisions, patterns, and gotchas.
Task-completion-only summaries (the majority) are omitted. Full list: `ls plan/learned/`.

Three meta-summaries sit alongside the numbered per-spec summaries. Read the one that
matches your question first — each one points at the specific numbered summaries that
hold the full record, so one file of reading replaces hundreds.

| Question | File |
|----------|------|
| "Why is the code as it is?" | `plan/learned/DESIGN-HISTORY.md` — design evolution by subsystem, abandoned approaches, load-bearing invariants |
| "Am I about to fall into a known trap?" | `plan/learned/RECURRING-PATTERNS.md` — patterns that recurred 5+ times, with avoid-it-by and recover-if-you-hit-it |
| "Why did this hook reject my code?" | `plan/learned/HOOK-FRICTION.md` — every hook false positive with verified workaround |

## Core Architecture

System boundaries, component design, lifecycle patterns, subsystem separation.

- [001](plan/learned/001-initial-implentation.md) -- Zero-copy ContextID, per-type pools, lazy iterators, goroutine-per-peer
- [013](plan/learned/013-unified-commit-system.md) -- CommitService abstraction, grouping by config not command, implicit EOR
- [015](plan/learned/015-fsm-active-design.md) -- FSM active connect state machine design
- [133](plan/learned/133-internal-migration.md) -- internal/ package restructuring, reactor -> component/bgp
- [149](plan/learned/149-unified-subsystem-protocol.md) -- Unified subsystem protocol, in-process vs async
- [157](plan/learned/157-hub-separation-phases.md) -- Hub separation into 7 phases
- [165](plan/learned/165-reactor-service-separation.md) -- Reactor service separation from protocol
- [244](plan/learned/244-reactor-interface-split.md) -- Reactor interface split for testability
- [247](plan/learned/247-plugin-restructure.md) -- Plugin restructure, circular import resolution
- [760](plan/learned/760-subscriber-session-model.md) -- Unified subscriber session model: shared Session struct across PPPoE/L2TP, handler delegation, event bridge pattern
- [761](plan/learned/761-vpp-fib-query.md) -- RouteLookup added to Backend interface: dispatch through active backend, VPP via IPRouteLookupV2, eliminates netlink bypass
- [762](plan/learned/762-rs-dynamic-peers.md) -- IXP route server: dynamic peer groups from prefix ranges, RS-client transparent AS-path (RFC 7947), community-based selective forwarding, connection-time peer creation bypassing parsePeerFromTree
- [763](plan/learned/763-backend-aware-completion.md) -- ze:backend extended from commit-time validation to completion-time filtering: config completer reads entry.Exts, command tree stores Backend on Node, backends derived from config tree not component imports
- [912](plan/learned/912-irr-prefix-store.md) -- Shared IRR PrefixStore in resolve/irr/store subpackage: per-entry zefs keys (meta/irr/{name}), cross-process sharing via disk not a struct, subpackage breaks the peeringdb import cycle (no interface needed), read-first Open (write lock only to migrate)
- [945](plan/learned/945-tiers-1-rule-and-audit.md) -- Module tiers (core/component/plugin) decided by dependency direction, made auditable: `ai/rules/module-tiers.md` + `dep_audit.py --check` engine gate (`make ze-tier-check`); transitional migration baseline (not an allowlist); gate must reuse the generator's pluginDirs (engine-package granularity excludes nested sub-plugins)
- [1089](plan/learned/1089-layout-2-core-import-gate.md) -- Core import-direction gate: `internal/core/` may not import component/plugins, enforced in `dep_audit.py --check` over the existing import graph (no depguard, one parser); shrink-only baseline at PAIR granularity (file+package) with mandatory fix routes so a baselined file gaining a new upward import still fails; `plugin_process_boundary.go` scan roots now DERIVED from the generator's pluginDirs+nestedPluginDomains (13 roots, l2tp/firewall newly covered, zero findings) -- never a second hardcoded list; source anchors must name real paths, never globs (code_to_docs.py does not expand `*`)
- [1092](plan/learned/1092-layout-0-umbrella.md) -- Layout umbrella (4 children closed same-day): enforce the checkable subset (core import direction, derived scan roots), keep the rest advisory (naming glossary, protocol skeleton) -- the tiers Path-B lesson applied; an umbrella closes by handing off destinations (spec-rename set, spec-reactor-split), not by finishing everything itself; bare-token grep referrer lists are landmines (`qos-map` config keyword vs `qos-map.md` path)
- [1019](plan/learned/1019-traffic-usage-monitor.md) -- Lazy consumer-refcounted trafficstat service: aggregation layer extracted from ddos/detect, MonitorProvider registry for TUI (replaces per-feature CLI model fields), import cycle broken via registry.GetMetricsRegistry() neutral indirection, flow deltas summed not diffed
- [1015](plan/learned/1015-cp-survival-5-detect-5-characterization.md) -- DDoS Stage-2 characterization: bounded conntrack recent-flow ring + `flow-recent` RPC (filters by dst prefix, not iface -- conntrack is host-global), heuristic family classifier (SYN detected from half-open conntrack *state* since ConntrackFlow has no flags), responder split (local narrows in place + TCP flags; flowspec announces only on characterized, critical blackhole-fallback), graded Severity. Load-bearing gotchas: a generation guard is unsound unless the check and the emit are atomic (hold the mutex across both); `sync.WaitGroup.Go` can race `Wait` at shutdown because trafficstat calls subscribers outside its lock
- [1108](plan/learned/1108-ddos-detect-enhancements.md) -- DDoS detector enhancements ported from Flowtriq ftagent: bandwidth (BPS) trigger as a SECOND `baseline` instance (reuse the tested type, don't generalize) OR'd into the PPS trigger; `bps-floor` leaf in bits/s converted ÷8 because `iface` `RxBps` is actually bytes/s (name is a misnomer); baseline persistence via the tc-snapshot idiom (versioned JSON, atomic write serialized by a `saveMu`, restore guarded on version+min-sample+sanity) with an I/O-free constructor + explicit `restore()` in register.go; additive `AttackCharacterized.Confidence` (0-100 `GradeConfidence` from start-time signals only, no duration) wired to observe/flowtriq (each needed a NEW `Characterized` subscription) + `confidence-min` responder gates. Gotchas: `env.Get` caches os.Environ so `t.Setenv` can't reach a path helper mid-run (inject the field); a constructor that resolves a real config path makes unit tests calling `Stop()` write outside their tempdir
- [1027](plan/learned/1027-dns-server-harness.md) -- DNS server harness: geodns's listener lifecycle/EDNS0 client-IP/authoritative-answer wrapper/CIDR matcher extracted to `internal/core/dnsserver`, overriding the 3+-use-case heuristic at 2 consumers (geodns + approved as112) same as `internal/core/probe` did; thin `geodnsServer` adapter keeps geodns's original `apply`/`stopAll` call shape so its own tests needed zero edits; `Freebind` (`IP_FREEBIND`) is a `//go:build linux`/`!linux` split via plain `syscall`, no new dependency. Gotcha: a moved function becoming exported (Go capitalization) forces test call-site renames even when no assertion changes -- and a spec's own literal grep-based deliverable check can be stricter than its acceptance criterion (comments vs identifiers)
- [1083](plan/learned/1083-unify-startup.md) -- Unified plugin startup 5-stage handshake: one `runStartupHandshake(ctx, startupSink)` shared by the engine (`engineStartupSink`, full registration + barrier) and hub (`hubStartupSink`, harvest commands/schema + nil payloads, no barrier), replacing two inline drivers that duplicated the magic method strings. Injected sink over an `if hub {...}` switch (plugin-self-containment); barrier is an injected concern generalizing the engine's pre-existing nil-coordinator (ad-hoc) mode. Delivery modeled as sink methods because config/registry send-error policy genuinely diverges per caller. Gotcha: package-private interface ⇒ UNEXPORTED methods (else `ze-validate` flags "no cross-package caller"); dropped the immediate `handlePluginConflict` (subsumed by deferred rollback) to keep SendError-before-Stop ordering

## Wire/Encoding

Buffer-first, zero-copy, attribute pools, UPDATE building, NLRI parsing.

- [059](plan/learned/059-spec-pool-handle-migration.md) -- Pool handle migration from mutex stores
- [073](plan/learned/073-spec-buffer-writer.md) -- BufWriter WriteTo(buf, off) int pattern
- [076](plan/learned/076-spec-wire-update.md) -- WireUpdate lazy parsing design
- [092](plan/learned/092-pack-to-writeto-migration.md) -- Pack() to WriteTo() migration
- [102](plan/learned/102-buffer-first-migration.md) -- Buffer-first migration, Span type abandoned
- [105](plan/learned/105-pathattributes-removal.md) -- PathAttributes struct removal (lazy over eager)
- [176](plan/learned/176-per-attribute-deduplication.md) -- Per-attribute-type pool dedup design
- [204](plan/learned/204-update-shared-parsing.md) -- Shared UPDATE parsing for wire/API
- [721](plan/learned/721-bgp-2-aspa.md) -- ASPA path verification: RTR v2 per-session version, ROACache O(1) counter, route tracker for re-validation
- [722](plan/learned/722-spec-bgp-4-aspa-policy.md) -- ASPA policy enforcement: override ordering (ASPA reject wins over origin accept), re-validation via validateCh, origin policy is hardcoded not configurable
- [764](plan/learned/764-attr-flags-json.md) -- Attribute flags in Ze native JSON: includeFlags parameter on shared formatter, static flags for pool RIB, unwrap at extractRoutes for LG consumers
- [765](plan/learned/765-gc-pressure-reduction.md) -- GC pressure reduction: [256]bool for attr codes, inline FNV-1a, NextHopAddrs inline struct, LargeCommunities dedup fast path, clear() map reuse; stack arrays that escape via closure/interface/return are NOT optimizations
- [821](plan/learned/821-spec-attrpool-shard.md) -- attrpool sharding: split single RWMutex into 16 content-hashed shards, shard id in high 4 bits of the 26-bit Slot field (Handle ABI unchanged), fan-out compaction leaves scheduler untouched, read path 7.3x; h.Slot() is composite shardID<<22|slot not a dense index
- [1077](plan/learned/1077-unify-buffer-lifetime.md) -- Buffer-lifetime enforcement (DESIGN-REVIEW findings 2+6): the 4 hold-past-boundary contracts (WireUpdate.Snapshot eager-copy, attrpool Handle never-copy, ribForwardHandle lazy-copy, redistevents borrow-only) are DISTINCT layers with mutually-exclusive copy semantics -- keep separate, unify the ENFORCEMENT + VOCABULARY, do NOT merge. New `internal/core/memguard` build-tagged poison primitive (`if memguard.Enabled { Poison(...) }` folds to zero release cost); attrpool ABA guard = debug "no slot reuse" (`slotReuseEnabled`) turning stale-after-reuse into the existing ErrSlotDead (Handle stays packed uint32, 64-bit generation deferred). GOTCHAS: bare `go test -tags debug ./...` FALSELY fails without `ze_core`+features (missing plugin/config-schema registration -- fails identically WITHOUT debug); `IsPoisoned` on a non-4-aligned sub-slice reads the pattern out of phase; sync.Pool reuse is non-deterministic under -race so a re-acquired-batch assertion must check the no-leak invariant not an exact value; redistevents uses a struct sentinel not byte-poison because netip.Addr carries a GC-scanned z pointer
- [767](plan/learned/767-tokenizer-no-escape.md) -- Command tokenizer: removed backslash escape handling, backslash is a normal character; no per-byte escape scan on every command
- [770](plan/learned/770-precomputation-review.md) -- Precomputation critical review: 3 of 7 proposals rejected after source validation showed trivially cheap operations; profile before optimizing
- [771](plan/learned/771-performance-optimization-campaign.md) -- Full optimization campaign: Ze 91ms->71ms convergence (22%), BIRD gap from 1.82x to 1.61x; profile-driven, lifecycle-boundary precomputation
- [859](plan/learned/859-perf-hot-alloc-reduction.md) -- Hot-path allocation reduction: value-type struct keys (netip.Prefix, compactRouteKey, withdrawalKey) replace string maps across rib/adj-rib-in/RS; adj-rib-in bypasses bgp.Event; Ze 71ms->62ms, now matches BIRD
- [875](plan/learned/875-filter-delta-parse-once.md) -- Filter modify path parses filter text once and passes maps to the three extractors (was 4 parses per modified UPDATE); golden-corpus equivalence gate for AS-path refactors; modify path -39% time, -29% allocs
- [785](plan/learned/785-rfc7606-validation-cache.md) -- RFC 7606 validation cache: rejected, validation too cheap (67-138 ns) to benefit from caching despite 77-97% hit rate; cache lookup cost rivals validation cost

## Plugin System

Registration, SDK, event flow, lifecycle, hook integration.

- [253](plan/learned/253-nlri-plugin-extraction.md) -- NLRI codec extraction to plugins
- [905](plan/learned/905-bgp-family-checklist.md) -- BGP family integration checklist: 12-section pattern from SR-Policy/PATHS-LIMIT/SRv6 post-mortem; BLOCKING gates in /ze-spec and /ze-implement
- [757](plan/learned/757-typed-route-result.md) -- Typed RouteResult replaces map[string]any in update-route, eliminating int/float64 transport divergence
- [781](plan/learned/781-remove-private-as.md) -- Remove-private-as: plugin intent + reactor wire rewrite; Set+Prepend composition
- [806](plan/learned/806-install-1-dhcp-pxe.md) -- DHCP PXE extension: additive options in existing buildReply, server-wide pxeConfig, 1500-byte reply buffer, siaddr+option 66 dual-set for PXE ROM compat
- [807](plan/learned/807-install-2-tftpserver.md) -- TFTP server plugin: RFC 1350 read-only, ephemeral port per transfer, channel semaphore for concurrency, SO_BINDTODEVICE on Linux, 32MB block-number limit acceptable for bootloaders
- [811](plan/learned/811-install-3-image-server.md) -- imageserver plugin: own HTTP listener, http.ServeFile for images/boot/zefs, path traversal via flat filename validation, zefs built at configure time with ze init key patterns
- [867](plan/learned/867-yang-rename-ownership.md) -- YANG rename (schema/ -> yang/) + command YANG ownership (component/ -> plugins/): folder test, codegen cleanup gaps, acronym map, intrinsic vs removable subsystems
- [1060](plan/learned/1060-vpp-crash-reconciliation.md) -- VPP crash recovery: reload iface backend via LoadBackend in reconcileOnVPPReady to clear stale state (dead channel, stale name map, stale bridge domains, fired sync.Once); matches fib/vpp full-reinit pattern; existing tests unaffected (LoadBackend("vpp") fails silently when no factory registered)
- [1016](plan/learned/1016-observation-feed.md) -- Shared observation feed: multi-subscriber iface fan-out (CoW atomic-ptr), typed in-process Feed (not EventBus), alloc-free Observation value type, non-blocking pub with bounded channels
- [977](plan/learned/977-traffic-usage.md) -- Pure-Go eBPF TCX plugin (traffic-usage): asm.Instructions assembled in-process, no C/.o/clang; BPF_PROG_TEST_RUN tests load-bearing; parse-then-account avoids verifier packet-pointer invalidation; StoreImm has no DWord form; go mod vendor needs chmod -R u+w
- [980](plan/learned/980-feature-gate-1-lg.md) -- per-feature compile-out (ze_<feature> tags): construction registry in package hub (Service=Reconfigurable+Name+Shutdown, not ze.Subsystem) + gated service_<x>.go/register_<x>.go; generator emits all_<tag>.go for schema gating; ZE_FEATURES Makefile var; .golangci.yml lints the feature-on set; dep_audit DISABLEABLE gate forbids always-on direct imports. lg pilot: ze_core build links 0 lg symbols, ze_lg links 183
- [981](plan/learned/981-feature-gate-2-ssh.md) -- ssh compile-out (ze_ssh): doesn't fit the listener registry (built inside shared startup with AAA/dispatch/reactor inputs + interactive session model), so uses a dedicated seam (ssh_infra.go nil-able sshBuild/sshWirePostStart/sshBuildStandalone hook vars). Extract-then-gate: AAA stays always-on, only ssh construction moves behind ze_ssh; two construction paths (hook + no-bgp). ze-stripped KEEPS ssh (management plane); no-ssh proof = bare -tags ze_core. Tag goes in 4 places (ZE_FEATURES/TestBuildTags/.golangci/featureTags)
- [984](plan/learned/984-feature-gate-3-web.md) -- web compile-out (ze_web): extract-then-gate because always-on install/init/lg paths borrowed web cert helpers; `internal/core/selfcert` owns cert/TLS, web uses the construction registry (`service_web.go`/`register_web.go`), `RunWebOnly` is a nil-able standalone seam, `blobCertStore` stays always-on for lg, and ze_core/ze-stripped link zero web symbols while default feature builds keep web
- [983](plan/learned/983-feature-gate-manifest-ssot.md) -- single-source feature-gate manifest (feature-gates.txt): the tag<->package fact was hardcoded in 5 places (Makefile ZE_FEATURES, .golangci.yml build-tags, TestBuildTags, generator featureTags, dep_audit DISABLEABLE) across 4 languages; collapse to one `<tag> <pkg>` manifest that program consumers read directly, and drift-gate the one static consumer (.golangci.yml) via dep_audit --check. Generalizable: one manifest + derive consumers + drift-gate the non-deriving one
- [986](plan/learned/986-feature-gate-4-gnmi.md) -- gnmi compile-out (ze_gnmi): dedicated seam (gnmi_infra.go) like ssh not the registry, because richer ctor deps + reload-notifier coupling + bind-once/no-Reconfigure; reload closure calls a nil-able gnmiReloadNotify hook instead of naming *zegnmi.ChangeNotifier. KEY: a feature can own THREE blank imports (component pkg + config schema + gnmi-cmd command schema for `show gnmi`), so generator loadFeatureTags now gates the direct <pkg> in addition to <pkg>/yang and the manifest allows multi-row per feature. ze-stripped/ze_core link 0 gnmi symbols
- [987](plan/learned/987-feature-gate-5-mcp.md) -- mcp compile-out (ze_mcp): construction registry (like lg/web) since MCPServerHandle is already Reconfigurable+Shutdown. KEY structural move: the always-on API command lister reused the zemcp-typed serverCommandLister, pinning mcp into every binary; extracted a NEUTRAL always-on command-metadata type (command_meta.go: commandMeta/commandMetaSource) that API and the gated mcpCommandLister both adapt -- reusable pattern when two surfaces (one disableable) share a builder. Also: dep_audit disableable scan now skips non-production trees (DISABLEABLE_NONPROD_PREFIXES = internal/chaos/, internal/test/) because ze-chaos imports component/mcp untagged but is not in the production daemon (corrected umbrella A-5). ze_core links 0 mcp symbols, ze_mcp links 342
- [989](plan/learned/989-feature-gate-6-api.md) -- REST/gRPC compile-out, PER-ENCODING (ze_rest + ze_grpc, NOT a combined ze_api): user wanted to ship gRPC-without-REST or vice-versa. Two nil-able seam hooks (restBuild/grpcBuild in api_infra.go) share an always-on engine/sessions builder (buildAPIShared); each gated transport builds only its server. KEY YANG pattern: when one config container has independently-disableable children, keep the parent in an always-on base module (ze-api-conf = api-server{token}) and contribute each child from a gated module (api/rest/yang, api/grpc/yang) via Ze's same-named-container MERGE (recurses into nested api-server; no augment needed). Generator gates the transport schemas for free from the manifest <pkg>/yang mapping. Splitting the YANG breaks committed config tests that imported only the base schema -> add the relocated rest/grpc yang imports. nm: ze_rest=REST-only, ze_grpc=gRPC-only
- [990](plan/learned/990-feature-gate-7-monitoring.md) -- Prometheus exporter compile-out (ze_telemetry): FIRST core-level seam -- two start sites in DIFFERENT components (hub standalone + bgp reactor) must reach it, so the hook `metrics.StartExporter` lives in the always-on leaf both import (`internal/core/metrics`), not the hub; ze-tier-check stays green (a core leaf may hold a nil-able hook var set by a component). Setter is the hub's gated register_telemetry.go (`metrics.StartExporter = exporter.Start`), NOT the exporter's init -- the generator only blank-imports DISCOVERED (plugin/schema/rpc/namespace) packages, so a non-discoverable exporter pkg can't ride all_ze_telemetry.go (which gates only the schema). DUMMY decision: user asked for a "dummy default", but forcing GetMetricsRegistry()->NopRegistry regresses ike's nil-gated 5s ticker goroutine for zero gain (skip==no-op-record); kept the nil contract, NopRegistry stays always-on+available, proven by TestRegistryCollectsWithoutExporter. Gate ONLY the part nothing always-on imports (HTTP exporter); collection API + dummy stay always-on. Schema relocated telemetry/yang->telemetry/exporter/yang for one manifest line. nm: ze_core=0 exporter+0 collector+82 metrics; ON=30 exporter
- [995](plan/learned/995-feature-gate-8-protocols.md) -- routing-protocol compile-out (ze_isis/ze_ldp/ze_ospf/ze_rsvpte): FIRST gated PLUGINS (not components) -- pure blank-import partitioning, NO register_<x>.go/seam; protocol .go files are NOT source-tagged, the generator moves their all.go blank imports into all_<tag>.go and dead-code elimination drops the unreferenced pkgs. A-1 PASSED (zero always-on/cross-protocol importers, no Registration string deps) so B-2 codec extraction NOT needed -- gate each protocol whole. One protocol = several manifest lines (engine + transport + cli), multi-dir per tag (gnmi precedent, no generator change). Self-containment: moved ldp/rsvpte command schema (ze-<p>-cmd.yang) OUT of sibling ldp-cmd/rsvpte-cmd INTO <proto>/yang/ (matching isis/ospf, which keep conf+cmd yang together; <proto>/cli is Go-only) so deleting internal/plugins/<proto>/ removes the whole protocol; other *-cmd dirs are for COMPONENTS and stay siblings. BOTH composition roots gated: generated all.go AND hand-written cmd/ze/ze_core_dispatch.go (isis/ospf CLI moved to gated companions dispatch_{isis,ospf}.go //go:build ze_core && ze_<proto>). FIRST gated sdk.NewWithConn ENGINES + multi-package features -> fixed two dep_audit model gaps: is_registration_importer now matches all_<tag>.go (else a gated engine's blank import there reads as a tier "misplaced engine -> move to component"); disableable check skips same-tag importers (engine importing its own transport sub-pkg is intra-feature, dropped together, not an always-on pin). Absent compile-out tests MUST live in cmd/ze/hub (Makefile GO_TEST_CORE runs only there). nm: bare ze_core links 0 each (isis/ldp/ospf/rsvpte); ze_core ze_ospf links ospf only
- [1177](plan/learned/1177-feature-gate-9-vrrp.md) -- VRRP compile-out (ze_vrrp): first NON-protocol gated plugin, proving the 995 blank-import-partitioning shape generalizes to any self-registering internal/plugins engine. VRRP is the SIMPLEST shape (ldp/rsvpte class): registers CLI via reg.CLIHandler not a programmatic cli dispatch pkg, so ONE composition root (generated all.go), NO cmd/ze/dispatch_vrrp.go companion. Two manifest lines (vrrp + vrrp/transport; yang auto-derived from <pkg>/yang; fsm/packet drop by DCE, no register.go). Zero code changes to derived consumers (dep_audit/Makefile/runner) -- the vrrp->transport intra-feature import rides the protocol-era _same_feature_importer generalization; only .golangci.yml hand-edited. Default-ON via ZE_FEATURES. nm proof folded into the existing consolidated build_tag_protocols_absent_test.go (reuse the one bare-core build). GOTCHA: vrrp config is a YANG augment under interface units (not a top-level block), so the absent-reject test needs a minimal valid interface wrapper whose only gated token is `vrrp`; golden snapshots already list vrrp and don't change (snapshot test runs under full features, never bare core)
- [1000](plan/learned/1000-cli-object-rooting.md) -- Object-rooted operational CLI: each command roots at its owning component/plugin, no shared `ip` namespace (show ip route->show route, show ip arp->show neighbor with show arp as the IPv4 alias, show ip ospf/clear ip ospf->show ospf/clear ospf); address family is a positional keyword filter validated case-sensitively by the YANG enum ArgDef before the handler (the handler's ToLower was dead code), NOT a namespace or --flag; the row-cap is a `limit N` keyword leaf (matching the YANG `limit` ArgDef), not --limit. Traps: OSPF dispatches on the literal command-path string so PluginCommand/CommandDecl/switch/dbSubviewType all change in lockstep (wire method ze-show:ospf-* unchanged); the plugin/all wire-method snapshot is golden-file (regenerate `go test -tags '...' ./internal/component/plugin/all/ -update`, flag AFTER the package); feature plugins (ospf/isis/ldp/rsvpte/gnmi) need build tags or registry tests report false-missing; do NOT blanket-replace command strings in vendor-comparison docs/specs -- FRR/Cisco columns and interop references legitimately keep `show ip ...`
- [988](plan/learned/988-kernel-build-consolidation.md) -- kernel build consolidation: ONE driver `tools/kernel-builder/run.py` (docker/qemu select + single arch->platform map + single build-time `kernel.version` reader + provenance sidecar) called by both Makefiles AND `cmd_kernel.go` (Go's inline docker/qemu argv deleted); `# ze-include: <name>` shared config fragments in `common/` mirror `# ze-base`, resolved by run.py (make path) + kernelreg.go (Go verified path), empty paired `.require` so a missing shared fragment fails at include-resolution; runtime kernel gets 982's verified registry+floor + `--target runtime` + directory/tree cache keyed by target; out-of-tree runtime consumption via `go.mod replace` of rtr7/kernel -> assembled `tmp/kernel/pkg` (gok resolves via `go list -mod=mod` which honours the replace), pinned modcache never mutated, no `.ze-pinned-kernel`. KEY: behaviour preservation provable cheaply by fragment-line-set equality (same directives fed to merge_config => identical resolved .config) + config-only docker baseline; `X86_SYSFB` is dropped by olddefconfig in 7.1.1
- [812](plan/learned/812-install-5-bootstrap-mode.md) -- bootstrap mode: EmitBootstrapConfig separate from EmitConfig, ethernet-only DHCP, SSH from zefs creds, falls through on failure, inserted between template and web-only in startup switch
- [813](plan/learned/813-install-6-installer-initrd.md) -- installer initrd: busybox-based shell init, wget+dd stream to first non-removable disk (sysfs removable check), blockdev --rereadpt for partition re-read, zefs injection to /perm/ze/, kernel-level DHCP via ip=dhcp cmdline
- [815](plan/learned/815-install-7a-namespace.md) -- namespace migration: cmd/ze/appliance/ moved to cmd/ze/install/appliance/, deprecated alias prints warning, no root registration at new location
- [816](plan/learned/816-install-7b-vendor-builder.md) -- vendor builder: gokBuildFn replaces gokBinary shell-out, go run -mod=vendor ./cmd/ze-gok, GOMODCACHE isolation, gokSizeArg uses strconv.AppendInt
- [817](plan/learned/817-install-7c-vendor-updater.md) -- vendor updater: local copy of gokrazy/updater (stdlib-only), authTransport for all requests, StreamTo+Switch+Reboot sequence, --testboot/--no-reboot flags
- [982](plan/learned/982-install-11-hw-kernel-profiles.md) -- install-11 kernel profile registry: shared builder is Python `build.py`, profile registry is `<name>.config` + `<name>.require` with one-level `# ze-base`, Go enforces manifests plus universal floor after `build/config`, raw `make` remains unverified to preserve no-Ze-binary kernel builds, cache variants hash registry/manifest/builder inputs.
- [818](plan/learned/818-flow-export-1-counter-export.md) -- flow export counter closure: IPFIX octetTotalCount/packetTotalCount (not Delta), single MaxDatagramSize, per-datagram metric counting, template timestamp on success only, show flow-export YANG wiring
- [819](plan/learned/819-flow-export-2-flow-records.md) -- flow export spec-2 integration: FlowSample/ConntrackFlow neutral types + factory-registered flow encoders (import-graph constraint), platform-independent workers delegating to _linux netlink, BestChange typed-handle enrichment (next-hop only), deadlock-safe Stop ordering
- [820](plan/learned/820-flow-export-0-umbrella.md) -- flow export umbrella: single-collection/multiple-consumers via iface callback, registration over imports, buffer-first, in-process SDK component; spec-1 complete, spec-2 CI-gated
- [1145](plan/learned/1145-plugin-internal-keyword.md) -- Plugin internal keyword: explicit `plugin internal <name> { use <builtin> }` config, cross-list uniqueness, internal plugins do not use external encoder/respawn/timeout leaves
- [884](plan/learned/884-cos-plugin.md) -- CoS plugin: shared registry in core/cos, YANG container-merge over augment for interface binding, InProcessConfigVerifier ordering relies on alphabetical registry.All() ("cos" < "interface")
- [887](plan/learned/887-cos-vendor-radius.md) -- Vendor VSA CoS/rate: hardcoded 5-vendor switch over registry, MikroTik rate as "Nbit" FilterID string for shaper pipeline, Ze "cos:" prefix wins over vendor VSA
- [894](plan/learned/894-show-enricher.md) -- Show enricher registry: core/show leaf package, plugins register enrichers for show commands via init(), handlers call Enrich()/EnrichBrief(), panic recovery, alphabetical ordering
- [895](plan/learned/895-show-enricher-v2.md) -- External enrichment: declare at Stage 1, proxy enricher in server, ze-plugin-callback:enrich-show with 2s timeout, Unregister for cleanup, web service-locator explicit Enrich(), fakeenrich test plugin
- [828](plan/learned/828-codec-callback-passthrough.md) -- NLRI decode single-marshal: DecodeNLRIHex returns any, registry marshals once; RunCLIDecode callers share function so need own marshal
- [830](plan/learned/830-typed-inter-plugin-dispatch.md) -- Typed exact-command inter-plugin dispatch: `DispatchCommandArgs` over rebuilt strings, `CommandArgsAuthorizer` over canonical fallback, command/args boundary pinned by tests
- [858](plan/learned/858-typed-peer-selector.md) -- Typed peer selector: BGPReactor takes `*selector.Selector` not string; SDK `*Sel` variants for DirectBridge; `SoftClearPeer` fixed for name/ASN; GR stays string at args boundary
- [922](plan/learned/922-cross-plugin-switch-audit.md) -- Cross-plugin switch audit: most are correct Go (backend lowering has no virtual-dispatch alt); only producer-owned re-derivation is a smell -> hoist as a method on the producer's type (`RouteAction.Verb()`, `Path.IsEBGP`, `AddPathMode.Label()`); 3 dup-hidden bugs fixed; `ze-validate` undercounts a typed enum reached only via its constants (fixed in validate.py)
- [938](plan/learned/938-bug-review-0-umbrella.md) -- Review-only bug campaign pattern: generated aggregator inventory first, child reports by owner, accepted findings routed to TDD-ready fix specs, closure must include spec audits and fix-spec audit sections before final report claims artifact completion
- [1028](plan/learned/1028-as112-1-iface-address-registry.md) -- Generic plugin-owned address registry in `internal/component/iface` (`RegisterOwnedAddresses`/`UnregisterOwnedAddresses`, consumed by `desiredState()`) so any in-process plugin's addresses survive reconciliation like YANG config. Registration fires a coalescing reconcile-trigger (same shape as the existing vpp-ready trigger) so it lands in the same op, not a later commit. GOTCHAS (every "obvious" fix was wrong on the first try, each caught only by dedicated adversarial re-review, not the original TDD cycle): unconditionally creating a `"lo"` desiredState key to make unregister-prunes-the-kernel work strips the kernel's own 127.0.0.1/::1 whenever there's no YANG loopback config; the fix for THAT ("track every interface ever registered," `everOwnedIfaces`) never forgot an interface, permanently treating it as ze-managed after one register/unregister cycle; the fix for THAT (`staleIfaces`, cleared after a clean reconcile pass) still had a race where a blanket `clearStaleIfaces()` could discard a concurrently-added entry for a different interface -- final form has `ownedAddresses()`/`desiredState()` return the exact `staleNames` a pass's own snapshot saw, and `clearStaleIfaces(names)` deletes only those. Also: `reconcileMu` deliberately serializes ALL THREE reconcile trigger paths (config commits, vpp events, registry events), not just the two background ones -- excluding config commits (tried, reverted) avoids a bounded blocking risk but reintroduces an unbounded, more-frequent one (a commit rolling back because it raced a registry-triggered reconcile over the same address)
- [1035](plan/learned/1035-as112-0-umbrella.md) -- AS112 umbrella roll-up: host addresses (`/32`,`/128`, bound on `lo`) vs. covering prefixes (`/24`,`/48`, announced by BGP) are distinct object types, easy to conflate -- announcing the host addresses instead of the covering prefixes would be wrong (widely filtered); `asn.local 112 replace-as` is a global-routing foot-gun if applied to a publicly-peered group (injects the node into the global AS112 system with no RFC 7534 coordination) so it defaults to the operator's own ASN, opt-in per-group; nearly the whole feature is composition of already-tested mechanisms (BGP communities, watchdog/healthcheck), the only new code is the DNS plugin + a small generic iface registry
- [1033](plan/learned/1033-as112-2-dns-server.md) -- as112 DNS-serving core (RFC 7534/7535 zones, TXT identification, allow-from, health/doctor checks): zone-boundary matching must use `dns.IsSubDomain`, not string-suffix (sibling names were wrongly treated as in-zone); YANG listener anchors need `config false` LIST items directly under the container, a wrapper container never materializes in the parsed Tree; `dnsserver.Manager.applied` needs a sentinel reset on EVERY failure path (not just the first-tested case) plus a generation-counter to detect an accept-loop crash, or a later `Apply` with an unchanged signature silently no-ops on a dead listener; `isOnBox` must recognize the plugin's own anycast addresses (not just loopback) since routing-dependent kernel behavior can present either as the query source
- [1034](plan/learned/1034-as112-3-bgp-integration.md) -- as112 BGP watchdog integration (conditional announce/withdraw, community/origin-AS, shared watchdog groups): zero new as112 Go code but its healthcheck-probe design (real `ze cli -c` over SSH, not synthetic) surfaced two shared-infra BLOCKERs -- `service_ssh.go`'s SSH executor discarded `Response.Status`, so `ze cli -c` always exited 0 regardless of operational outcome (fixed via `responseExecErr`); `parseCommunityText` hardcoded only 3 of ~15 well-known community names, silently dropping `nopeer`-configured routes on watchdog replay (fixed by delegating to the canonical `attribute.ParseCommunity`). Also: env vars set on the daemon's own process environment "for a subprocess's benefit" can leak into the daemon's own unrelated use of the same variable name -- bake them into the subprocess's own command string instead. The two advisory doctor checks (`doctor-as112-watchdog-missing-withdraw`, `doctor-as112-global-origin-uncoordinated`) originally deferred for lack of a "home" were later built in `internal/component/doctor` (dependency with no narrower owner, per `ai/rules/doctor-checks.md`) -- see the entry's Open/Deferred section
- [1032](plan/learned/1032-as112-review-hardening.md) -- AS112 review-hardening across two independent /ze-review fork passes (2 BLOCKER, 7 ISSUE, 10 NOTE, all fixed+re-verified): `dns.Server.NotifyStartedFunc` closes a bind/Stop socket-leak race (waits for both UDP+TCP started-channels before `bind()` returns); new `sdk.Plugin.IsInternal()` (`p.bridge != nil`) lets a plugin detect it's calling another in-process package's plain functions from a forked-subprocess context where they silently no-op -- as112 refuses to start when external (whole feature depends on it), `cos` only warns (partial degradation) -- severity should track how much value survives running external, not be copy-pasted; `iface.RegistryReconcileStatus()` surfaces the previously fire-and-forget registry-reconcile trigger's outcome, wired into BOTH of `show as112`'s branches (early-return included -- a registry failure from a different consumer is visible regardless of as112's own state); a doc comment with no blank-line separator silently attaches to the wrong declaration (not a compile error); test-isolation reset helpers must be updated when a package gains new resettable global state. Tooling gotchas: `go test -tags ... -update` needs `-args` before the custom flag or `go test` misresolves the package pattern against the module-root `tools.go`; `-tags linux` on Darwin is NOT `GOOS=linux`; `make ze-linux-test`'s Docker has no elevated caps by default (CAP_NET_ADMIN netns tests correctly skip), use `ze-qemu-integration-test` or a one-off `docker run --privileged` for real coverage
- [1045](plan/learned/1045-plugin-process-boundary.md) -- generalized 1032's `IsInternal()` fix into `ai/rules/plugin-process-boundary.md` + `make ze-plugin-boundary-check` (`scripts/checks/plugin_process_boundary.go`, modeled on the existing `ze-iface-resolution-check`): the repo sweep this required found 3 MORE live unguarded instances before any tooling existed -- `iface.SubscribeCollectNotify` (traffic-usage's only attach mechanism, flow-export's only data source -- both now refuse external; ddos-detect's trafficstat+iface fallback -- now warns). Gotcha: a "refuses external" unit test asserting only the exit code is a false green -- `runEngine(pluginEnd)` against a plain net.Pipe() returns 1 either way, with the guard immediately or without it only after `p.Run()`'s ~30s SDK handshake timeout; assert elapsed time too. Gotcha: validating a brand-new static checker's own correctness needs an independent cross-check (e.g. a plain `grep -rln` over the same pattern list) confirming it actually matched something, not just that it reported OK

- [1046](plan/learned/1046-traffic-analysis-restructure.md) -- Traffic-analysis FACTS tier: extracted `internal/core/stats` (rolling Window, mean/stddev/quantile, Shannon entropy, EWMA, interval-regularity), rebuilt `trafficstat` on it (behavior-identical; `window_test.go` is the characterization harness), moved the severity VERDICT out of the neutral layer to a display-only CLI computation, and added `trafficfeature` (a second `observation.Feed` consumer) exposing neutral per-source signals (fan-out, out/in ratio, dest-port entropy, new-peer, rare-port, coarse beaconing) for BOTH the ddos and the new anomaly domains. The dividing line: analysis computes neutral numbers; detection plugins own the verdict + response.
- [1048](plan/learned/1048-anomaly-1-detect.md) -- Behavioral anomaly DETECTOR (report-only), a security domain SEPARATE from volumetric ddos: new `anomalyevent` (source/entity-oriented, distinct namespace) + `anomaly/detect` consuming `trafficfeature`. Per-(entity,feature) EWMA baseline, cohort rarity (leave-one-out `cohortStats`, +Inf ratio excluded so an exfiltrator cannot mask peers -- review-driven), capped/discounted correlation (Zstrong + weight*sum, not naive), confirm/clear, and freeze-learn (score-before-update + warmup gate) so a sustained anomaly cannot poison the entity's own baseline. Emits + a bounded incident ring (`show anomaly`). Wires `stats.EWMA`.
- [1049](plan/learned/1049-anomaly-2-shape.md) -- Shadow-first anomaly RESPONDER: subscribes to `anomalyevent`, installs a surgical per-source firewall action (`MatchSourceAddress`+`Limit`). Pinned Responder State Machine -- one mutex, whole-owner-set re-register per change, a RESPONDER-LEVEL monotonic generation guard (a per-record counter collides across re-arm -- review-driven), timed auto-revert (fires regardless of clear), blast-radius cap, kill-switch (the canonical path, not a side-effect -- review-driven), allowlist. Injectable clock + mockable firewall for deterministic tests; `reverted_total` counts both the clear and auto-revert paths.
- [1055](plan/learned/1055-config-apply-ordering.md) -- Config-apply ordering via a transaction OPERATION GRAPH: decompose a config diff into typed ops, `BuildOperationGraph` -> `TopologicalSort` -> executor `Verify/Execute/Commit` with reverse-order rollback (`internal/component/config/transaction/`). Decomposition + constraint rules are COMPONENT-OWNED (iface/bgp register via `init()`, transaction package owns only generic graph/solver/executor machinery -- plugin-removal invariant). Reached from the real reload path (`plugin/server/reload.go` `runTxCoordinator` -> `SetOperationPlanner`). Address-only cross-interface cycles relax with `AllowDual` (dual-presence swap); other cycles rejected. GAP recorded at closure: functional reload tests rotate router-ids (no ADDRESS ops) so the IP-cycle/`AllowDual` path is UNIT-tested only, not functional; spec boundary caps (op-count 10000, cycle-depth 100) unenforced.
- [1075](plan/learned/1075-unify-rpc-dispatch.md) -- Unified plugin->engine RPC dispatch (DESIGN-REVIEW finding 2): each op existed in up to 3 magic-string tables (JSON switch, Direct switch, `wireBridgeDispatch` `Set*` list) + a 4th SDK branch table. Replaced with ONE `engineOp` registry (`server/dispatch_registry.go`): method + `handle func(*Server,*process.Process,json.RawMessage)(any,error)` (proc PASSED not captured, so a method-expression entry has zero per-request alloc -- R-3) + optional `typedWire` bridge descriptor. JSON path, Direct path, and `wireBridgeDispatch` (now a loop) all derive from it; drift guard `TestPluginRPCRegistryCoversAllPaths`. KEY: the registry lives in the SERVER package, NOT the leaf `registry` package the spec named -- built-in ops bind Server/proc/bridge types the leaf cannot import (A-2 broken); codec RPCs stay in leaf `CollectRPCHandlers` as the fallback. `rpc.Method*` constants shared SDK<->engine kill the string drift. GOTCHAS: the spec cited `TestRPCRegistrationExpectedMethods` as the guard but that tests `AllBuiltinRPCs()` -- a SEPARATE `ze-system:*`/`ze-plugin:*` `RPCDispatcher`, not `ze-plugin-engine:*`; `RPCCallError.Error()` prepends `"rpc error: "` so JSON `SendError(err.Error())` double-prefixed while Direct didn't (unified `rpcErrMessage` sends raw `.Message`, aligning them -- AC-2); `return nil,nil` from an `(any,error)` handler trips `nilnil` (needs `//nolint:nilnil`); const `+` concat is hook-blocked even at compile time. Pre-existing unrelated reds: `plugin/all` stale codegen, `plugins/ospf` flaky `-race`.
- [1124](plan/learned/1124-vrrp-first-hop-redundancy.md) -- VRRP first-hop redundancy (RFC 9568 v3 default + RFC 3768 v2 opt-in, IPv4+IPv6, RFC-strict virtual-MAC from day one) as a 6-spec umbrella: edge plugin `internal/plugins/vrrp/` (pure codec + pure actions-as-values FSM + per-instance raw-socket transport with GARP/NA announcers) plus a generic owned-macvlan registry grafted onto iface (parallel to the as112 address registry, kernel IFLA_IFALIAS orphan marker). KEY DECISIONS: v3/IPv4 checksum TRANSMITS the RFC 5798 pseudo-header form (NOT the RFC 9568 message-only clarification) because keepalived 2.3.1 requires it, rx dual-accepts + flags message-only; macvlan PRIVATE mode + VIP at the parent SUBNET prefix (not /32) for virtual-MAC ARP ownership (recipe in 1122); PER-INSTANCE address owner `vrrp:<dev>` (a shared owner can't drop one instance's VIPs); metric labels `{device,group,vrid,family}` (a logical interface isn't unique per virtual router); YANG group keyed by NAME with mandatory `vrid` leaf (new rule: no dup vrid per unit+family); internal milliseconds everywhere but computed skew/master-down are `time.Duration` (sub-ms skews). GOTCHAS: first IPv6 advert sources the transient auto link-local because `RegisterOwnedAddresses` is async (see 1122, cosmetic, do not re-attempt); transport metrics were dead until `ConfigureMetrics` forwarded the registry via `SetMetrics`; each instance opens its own parent rx socket so rx routes on `RxItem.Key` not vrid; macvlan IFLA_IFALIAS is ignored at create (LinkSetAlias fallback + adopt-remark); parent-down does NOT propagate to macvlan oper-state (M-DOWN flag only -> readiness keys on the PARENT). Proven vs keepalived 2.3.1 (`scripts/evidence/effective-vrrp-keepalived.py`); accept-mode enforcement + priority tracking + native VPP + automated v2/IPv6 interop DEFERRED (`plan/deferrals.md`).

## Configuration

YANG schema, migration, config reload, editor, environment variables.

- [008](plan/learned/008-config-migration-system.md) -- Heuristic version detection, 3-version migration chain
- [065](plan/learned/065-spec-remove-version-numbers.md) -- No version numbers in config (YANG-transformable)
- [166](plan/learned/166-yang-only-schema.md) -- YANG as sole schema source of truth
- [175](plan/learned/175-config-editor-validation.md) -- Config editor validation pipeline
- [226](plan/learned/226-config-reload-6-remove-bgpconfig.md) -- BGPConfig removal, map[string]any
- [232](plan/learned/232-editor-tree-canonical.md) -- Editor tree canonical representation
- [716](plan/learned/716-iface-2-urpf.md) -- rpf-check enum over raw sysctl integer; three-state nil/disable/value pattern; sysctl profile ordering issue
- [882](plan/learned/882-vlan-qos-map.md) -- VLAN 802.1p QoS maps: VLANSpec struct (TunnelSpec precedent), nil-means-unconfigured, duplicate-canonical-key rejection, defense-in-depth validation at 3 layers
- [883](plan/learned/883-vlan-qos-lab.md) -- VLAN QoS wire-level lab: AF_PACKET TCI decode, single-netns veth with static ARP, nftables meta priority counters for ingress, negative controls as first-class tests
- [725](plan/learned/725-spec-cpe-3-dhcp-ranges.md) -- YANG container-to-list migration, composite pool with per-segment bitmaps, format detection for backward compat
- [743](plan/learned/743-config-schema-stamp.md) -- Schema stamp as comment line (not YANG leaf), emitted at persistence site only, prep for downgrade recovery
- [746](plan/learned/746-cpe-4-firewall-global-options.md) -- Firewall global-options: keyword-to-sysctl mapping via EventBus default layer; inverted semantics for ignore-type sysctls
- [915](plan/learned/915-firewall-irr-iface.md) -- Per-interface IRR source validation: separate ze_irr_iface table, prerouting hook, policy-accept with per-interface drop terms
- [1005](plan/learned/1005-cp-survival-2-copp-port179.md) -- CoPP for BGP: system plugin mirrors policyroute pattern (RegisterTables), input-hook chain with established-first safety, exported ParseRateSpec
- [1007](plan/learned/1007-cp-survival-3-egress-cs6-sched.md) -- DSCP u32 selector fix: translateFilter populates TcU32Sel with per-family keys (IPv4 TOS byte + IPv6 traffic class); shared dscp.Parse in internal/core/dscp
- [758](plan/learned/758-config-graph.md) -- Config dependency graph for agent impact analysis: derived from validation code paths, 7 edge kinds, plugin registry integration
- [759](plan/learned/759-archive-pruning.md) -- Archive commit-revisions pruning: stable prefix from dual-timestamp diff, mtime-oldest-first, file:// only, uint16 max-keep
- [860](plan/learned/860-yang-required-generic.md) -- Generic ze:required enforcement: anchor-scoped walker, ValidateTreeAllModules for multi-module YANG sections, bare-form migration to mandatory true
- [1058](plan/learned/1058-redist-source-registration.md) -- Registry-backed config validation must be populated at init(), not engine-run (config validate imports plugins but never starts them); walkTree now validates list KEYS not just children (ze:validate on a list key was dead code); producer↔source parity test
- [1180](plan/learned/1180-rpki-per-peer-action.md) -- Per-peer config keyed by remote IP: delivered tree keys peers by NAME with IP at connection>remote>ip, so plugins keying by peer identity MUST use configjson.PeerRemoteIP (role read the stale flat remote/ip -> silently keyed by name -> OTC filter miss); "config that does nothing" bug class (a parsed leaf with no consumer: RPKI not-found + role remote-IP both found inert); a vacuous test assertion (total>=0 not ==0) hides the bug

## CLI/API

Command structure, text format, IPC, RPC dispatch.

- [072](plan/learned/072-cli-run-merge.md) -- CLI run command consolidation
- [081](plan/learned/081-update-text-parser.md) -- Unified text parser for API commands
- [110](plan/learned/110-consolidate-update-commands.md) -- Update command consolidation
- [132](plan/learned/132-spec-test-cmd-consolidation.md) -- Test command consolidation
- [143](plan/learned/143-api-command-restructure-step-3.md) -- API command restructure (8 steps)
- [209](plan/learned/209-yang-ipc-dispatch.md) -- YANG-driven IPC dispatch
- [229](plan/learned/229-command-context-server-refactor.md) -- CommandContext server refactor
- [245](plan/learned/245-rib-command-unification.md) -- RIB command unification
- [727](plan/learned/727-diag-core.md) -- 9 built-in diagnostic commands (procfs package, build-split patterns, singleflight without x/sync, BFD capture provider interface)
- [728](plan/learned/728-diag-netlink-monitor.md) -- Netlink monitor streaming (unified output channel, YANG verb tree placement, register_*.go hook bypass)
- [729](plan/learned/729-diag-traceroute.md) -- ICMP traceroute (ttlSetter interface for IPv4/IPv6 TTL, pure Go over library, argTimeout goconst pattern)
- [901](plan/learned/901-env-autocomplete.md) -- Env autocomplete (local handlers don't create tree nodes, ensureEnvPath with desc guard, registry-derived shell roots, core envcatalog)
- [738](plan/learned/738-cli-grammar.md) -- CLI grammar: closed keywords before free-form values (YANG sub-containers consume dispatch tokens, compatibility only after release)
- [730](plan/learned/730-diag-capture-interface.md) -- AF_PACKET live capture (mdlayher/packet + go-pcap BPF, portable/linux file split, Ethernet link type for raw sockets)
- [755](plan/learned/755-ze-doctor.md) -- Offline system readiness checks: diagnostic code taxonomy, error/warning severity, platform-split checks, shared resolve package
- [788](plan/learned/788-doctor-improvements.md) -- Doctor schema-driven listener inventory, RegisterListenerDefault pattern, show doctor provider, dependency inventory guardrail
- [837](plan/learned/837-doctor-check-registry.md) -- Doctor check registry: explicit phase/order/component metadata, plugin binary check first migration, registry code metadata consistency through diagnostic.Lookup
- [838](plan/learned/838-doctor-check-ownership.md) -- Doctor check ownership: runtime dependency check registration, check function, and unit test live in the owning plugin/component/backend/command package; `internal/component/doctor` owns only runner coverage and checks with no narrower owner
- [863](plan/learned/863-plugin-doctor-checks.md) -- Plugin doctor check registration: external plugins declare doctor checks in Stage 1, invoked via callback at runtime; two parallel paths (Go registry for offline, plugin callback for runtime)
- [1021](plan/learned/1021-spec-dev-bootstrap.md) -- Unified dev setup (ze-setup): replaced Makefile platform branching with Python script; check-name drift test against applianceDoctorChecks() (not recording mock, because checkE2fsprogs uses file stat not LookPath); e2fsprogs keg-only on macOS resolved by Go Cellar glob; uv not in apt repos
- [868](plan/learned/868-test-web-parallel.md) -- Parallel web tests: per-test ze daemon + agent-browser session isolation; web suite migrated from bespoke loop to ParallelRunner (two test engines, not three)
- [872](plan/learned/872-structural-review-fixes.md) -- Structural review fix pass: generators/checkers over hand-maintained text (arch map, link checker, drift check); netip.Addr peer keys; ownership moves; verify-gate soundness (mode/skips, reverse deps, fail-not-skip); six parallel subagents on disjoint files
- [791](plan/learned/791-spec-cli-default-format.md) -- Configurable default CLI output format: env.Get in command package, session override via env.Set, intercept placement before isConfigCommand
- [795](plan/learned/795-cmd-typed-args.md) -- YANG-typed command arguments: ArgDefs on Node from YANG leaves, two-phase dispatcher validation, completer auto-generates enum suggestions, mergeYANGEntry second pass for leaf children
- [792](plan/learned/792-platform-detection.md) -- Runtime platform detection: gokrazy/systemd/container/plain-linux/darwin classification, cgroups v1+v2, FD limits, set system file-descriptors
- [794](plan/learned/794-cli-session-transcript.md) -- CLI session transcript: executor wrapping for command+output recording, YANG enumeration, best-effort file writes, cmd/ vs internal/ hook constraints
- [796](plan/learned/796-doctor-platform-coherence.md) -- Platform-aware doctor checks: thread PlatformInfo as local, severity tiers by platform, env overrides for deterministic functional tests, naming convention (thing checked, not analysis type)
- [803](plan/learned/803-gnmi.md) -- gNMI component: segment-based paths, ChangeNotifier, time.After(0) trap
- [804](plan/learned/804-gnmi-yang.md) -- gNMI YANG config, show command, Prometheus counters, external commit notify
- [809](plan/learned/809-pol-3-validation.md) -- Policy plain names: unique filter names as default operator form, show policy chain output shape change (name+canonical), prefixed forms as escape hatch
- [810](plan/learned/810-show-command-pipe-filters.md) -- Command-owned pipe filters: PipeFilter registration, longest-prefix lookup, FoldFilters rewrite, show verb-first grammar, generic pipe code BGP-free
- [822](plan/learned/822-pipe-first-last.md) -- Pipe first/last: dual-path generic+server-side pattern, FoldFilters 3-tuple return with metadata map, pipe metadata dict in JSON output, table/text renderers skip pipe key
- [826](plan/learned/826-ipc-dispatch-data-raw.md) -- DispatchCommandOutput raw JSON: `Data` is `json.RawMessage`, errors move to `Error`, callers must avoid byte-slice substring assertions
- [827](plan/learned/827-dispatch-response-passthrough.md) -- Execute-command response passthrough: SDK handlers return Go values, SDK marshals once, pipeline JSON strings must be wrapped as `json.RawMessage`
- [829](plan/learned/829-command-verb-first.md) -- Verb-first commands: small root verb set, deprecated aliases only for released names, longest-prefix dispatch needs deprecated-prefix lookup
- [1056](plan/learned/1056-cli-grammar-gate.md) -- CLI grammar gate: R1-R8 mechanized in one `grammar` pkg + one `command.Verbs` registry; static gate (in ze-verify) + strengthened validateCommandName; `create` blessed as runtime verb; checker over-flag pitfalls (R6/R7); path-rename = wire break
- [1057](plan/learned/1057-cli-grammar-runtime-audit.md) -- CLI grammar Feeder 3: a live daemon-boot `system command list` audit is PROVABLY REDUNDANT -- built-ins are 100% YANG-derived (`LoadBuiltinsWithAliases` skips no-YANG handlers = Feeder 1's subset) and plugin cmds are rejected at `CommandRegistry.Register` (Feeder 2, no bypass), so the merged surface is conforming by construction. Pivoted to two in-process unit guards (`grammar_audit_test.go`): re-check the runtime built-in assembly (`AllBuiltinRPCs` x `WireMethodToPaths` + `ExemptCategory` by wire method -- the RPC strips it) and lock the `Register` reject-boundary. Verify producers before building a belt-and-suspenders live harness.
- [814](plan/learned/814-pol-4-explain.md) -- Policy dry-run: show policy test command, TracePolicyFilterChain trace helper, narrow PolicyDryRunner interface, per-filter decision trace, wire diff output
- [849](plan/learned/849-command-surface-ownership.md) -- Command surface ownership: container-merge YANG, owner/cmd handler pattern, dedicated feature modules (ping/traceroute), exported doctor check registry, generic-stay allowlist, substring-marker YANG gotcha
- [850](plan/learned/850-appliance-command-plugin.md) -- Appliance command plugin: reversed 815, moved appliance from cmd/ze/install/appliance/ to internal/appliance/ as self-contained command provider, ze appliance replaces ze install appliance, clean break (supersedes 815)

## Web Interface

Web UI, HTMX, templates, looking glass, chaos dashboard.

- [266](plan/learned/266-chaos-web-foundation.md) -- Chaos web foundation, SSE debounce, OOB swaps
- [268](plan/learned/268-chaos-web-route-matrix.md) -- Route matrix visualization pattern
- [741](plan/learned/741-graceful-listener-migration.md) -- Graceful listener migration on config reload (bind-before-close, cross-service conflict detection)
- [756](plan/learned/756-web-auto-reload.md) -- Web UI auto-reload on commit: late-bound commit hook on EditorManager, moved reloadAfterCommit outside apiCfgOK guard

## RIB/Routing

Route storage, selection, forwarding, communities, path selection.

- [010](plan/learned/010-rib-config-design.md) -- RIB config design, storage model
- [173](plan/learned/173-plugin-rib-pool-storage.md) -- RIB pool storage design
- [275](plan/learned/275-spec-forward-pool.md) -- Forward pool, per-peer worker goroutines
- [717](plan/learned/717-rib-2-multicast.md) -- Multicast RPF via generic LPM on sharded Loc-RIB; query-all-shards pattern; wiring gap caught by review
- [772](plan/learned/772-nh-cascade-wiring.md) -- NH cascade wiring: async OnChange to avoid shard deadlock, LPM race fix, ECMP-aware cascade
- [774](plan/learned/774-fib-depth-2-ecmp.md) -- ECMP is two mechanisms: bgp-rib multipath (within BGP) vs sysrib ecmpCollect (cross-protocol); locrib path means sysrib only sees one "bgp" entry
- [776](plan/learned/776-srv6-prefix-sid.md) -- SRv6 via lazy OtherAttrs extraction; SID resolvability reuses NH resolver; transposition at emission time; Reserved-byte trap in RFC 9252 wire format
- [906](plan/learned/906-srv6-review-fixes.md) -- SRv6 RFC compliance: encoder/extractor must share constants; transposition errata 7652 (high-order bits); short-circuit must cover all emitted fields; store derived state only after all gates pass
- [783](plan/learned/783-rib-peer-lock-split.md) -- RIB peerMu lock split: narrow r.mu to peer-keyed maps only, push RLock into helpers, three-phase UPDATE handler for concurrent peer processing
- [784](plan/learned/784-rib-rs-fastpath.md) -- locrib ForwardHandle: interface-based zero-copy wire bytes on Change for state-tracker consumers; two-trigger model (receive-path for forwarders, OnChange for state trackers) stays
- [789](plan/learned/789-adjribout-compact-storage.md) -- ribOut compact storage: 16 B entry + pool handle replaces 385 B *Route per peer; engine OutgoingRIB is dead code (zero production callers); refcounted source tracking
- [823](plan/learned/823-rib-show-bounded-dump.md) -- show bgp rib lock-scope reduction: sources snapshot peerMu-protected refs then iterate via PeerRIB's own lock; lazy outboundSource buffers per-peer; bestPipeline releases peerMu before terminal drain
- [824](plan/learned/824-rib-feed-replay-batch.md) -- Replay batching: stateful cursor protocol with delta encoding reduces reconnect replay from O(routes) to O(attr-sets) UpdateRoute calls; RegisterProcessCleanup hook pattern for cross-package cleanup without import cycles
- [1062](plan/learned/1062-redistribute-late-join-replay.md) -- Redistribute late-join: point-in-time fan-out misses peers not in the reactor map at injection; closed with re-emit-on-request (`redistevents.ReplayRequest` + echoed opaque `ReplayID` token, payload-carrying not signal) so the returning `RouteChangeBatch` stays peer-agnostic and the orchestrator holds `ReplayID->peer` (TTL map, held past `Emit` because out-of-process producers re-emit async). Loc-RIB does NOT feed peers (Approach C incoherent). GOTCHA: the reactor suppresses a duplicate announce to a session that already has the route, so replay-to-`*` vs targeted is WIRE-INDISTINGUISHABLE for an already-up peer -- targeting (R-1) is unit-tested (`RouteEntry.Peer`), NOT `.ci`-testable. A reconnect `.ci` is a FALSE-PASS for this (mutation-proven: it passes with the replay disabled, so the route arrives by another path; the reactor does not persist routes across reconnects -- `peer.go:143` -- so a reconnect is not a clean new-peer isolation); isolate the replay with a CONFIG-ADD instead (`bgp {}` zero peers, inject, then add the peer via reload -- no map entry, no opQueue) and prove the real dynamic/inbound path with interop (`redist-late-join-dynamic-frr`, FRR dialing a ze dynamic group on port 179). Mutation-test every `.ci` (disable the producing fn and re-run) -- three late-join `.ci` shipped as false-passes. Destination-agnostic evaluator tightened: `ImportRule.Destination` scopes `Accept` (empty = back-compat agnostic)
- [1066](plan/learned/1066-as112-bgp-redistribute.md) -- as112 originates its four covering prefixes into BGP as a redistribute producer (like static/connected), triggered by `import as112`, reusing the 1062 `ReplayID` replay path. Added a GENERIC origin-ASN + community capability on `RouteChangeBatch` (any source can originate as a virtual router with its own ASN). KEY GOTCHA: the spec's "no grammar change needed -- the consumer just emits the tokens" was WRONG -- the announce path (`buildBatchASPath`/`writeMandatoryAttrs`) sends an explicit `as-path` VERBATIM with no eBGP prepend (route-server transparency / ExaBGP exact-path), so `as-path 112` reaches an eBGP peer as `[112]` and enforce-first-as rejects it. Required a NEW `origin-as` reactor primitive (update-text token → `NLRIBatch.OriginAS` → AS_PATH `[asn]` to iBGP / `[localAS,asn]` to eBGP) distinct from verbatim `as-path`: "set an origin AS" is NOT "send an exact AS_PATH". Watchdog serving gate reads DNS listener liveness LIVE on each reconcile (`servingFn`=`mgr.serving`), NOT a stored bool -- two concurrent listener edges racing a snapshot left covering prefixes announced-while-not-serving (RFC 7534 blackhole); live-read converges (lock order p.mu→s.mu, emit lock-free, `-race` regression test). Feature-not-wired caught in review (producer built but `OnConfigure` never called it); the real-plugin `OnConfigure→apply` seam needs interop/`.ci`, not unit tests (fakeas112 uses a separate producer). Community rendered `<hi>:<lo>` round-trips every uint32 incl. well-known (NO_EXPORT 0xFFFFFF01→65535:65281); `ReleaseBatch` NILs the config-owned Community slice, never `clear()`s it
- [1074](plan/learned/1074-unify-route-events.md) -- Unified route-change events (DESIGN-REVIEW finding 2): the BGP RIB `BestChangeBatch` (rich, 15 FIB fields, feeds sysrib + flow enrichment) and the pooled `RouteChangeBatch` (lean value-type leaf, 8 producers) are DIFFERENT LAYERS, not duplicates -- fix the lossy adapter, do NOT merge the structs. Bridge `convertBestChange` made lossless: carries `Metric` + a new per-entry `OriginAS` (0 = fall back to batch `OriginASN`, preserving as112), and replaces the silent unmapped-action drop with a warn (log the raw `action_code` -- `RouteAction.String()` renders unknown enumerants as "unspecified") + an `UnknownActionSkips()` atomic. GOTCHAS: the replay path never calls the bridge (only incremental add/update/withdraw with Metric/OriginAS already populated reach it); `Metric` now carried but still unconsumed (`configredist.RouteEntry` has no Metric field -- kernel producer already set it, so BGP is merely consistent); editing any doc forces the `ze-discovery-index` gate which surfaces UNRELATED pre-existing `ai/DOCS-TO-CODE.md` staleness; `internal/core/textbuf` `TestPoolPreservesCapacityWithoutString` flakes under full-suite GC pressure (sync.Pool cap dropped), passes in isolation
- [1080](plan/learned/1080-unify-redist-loop-guard.md) -- Unified redistribution loop guard (DESIGN-REVIEW finding 2): the invariant "a protocol's routes never redistribute back into itself" was inlined 3× with 2 identity forms -- 2 runtime guards compared `ProtocolID` (`ProtocolIDOf(x); ok && id==b.Protocol`), 1 config-evaluator guard compared name strings. Collapsed the COMPARISON (not the 3 call sites -- they guard different edges: evaluator `return false`, egress per-consumer `continue`+`filteredProtocolTotal`, replay whole-batch early `return`) behind one predicate `redistevents.WouldLoop(source, dest string) bool` = `source == dest`. KEY: name-keyed BY DESIGN, NOT ID-keyed -- Guard 1 must reject `origin==importing` for config-only source names never registered as producers, so an ID-keyed form fails-open and regresses; the registry is a bijection so for registered protocols name-equality is EXACTLY the old ID-equality, and both runtime guards already hold `name := ProtocolName(b.Protocol)`, so name-keying adds no lookup (it removed the per-consumer `ProtocolIDOf` lookups). Do NOT "optimize" `WouldLoop` to compare IDs. GOTCHA: `ze-verify-wiring-docs` emits a non-blocking ADVISORY (user-facing code changed w/o a functional-test change) keyed on the file path not on behavior -- expected for a behavior-preserving refactor; existing `redist-*` interop covers it, do not add a redundant `.ci`

## Protocol/RFC

Graceful restart, route refresh, capability negotiation, session management.

- [007](plan/learned/007-family-negotiation.md) -- Four family modes (enable/disable/require/ignore)
- [033](plan/learned/033-spec-eor-tracking.md) -- End-of-RIB handling (RFC 4724)
- [254](plan/learned/254-rfc7606-enforcement.md) -- RFC 7606 treat-as-withdraw enforcement
- [128](plan/learned/128-graceful-restart-plugin.md) -- Graceful restart plugin design
- [574](plan/learned/574-bgp-4-bmp.md) -- BMP receiver + sender (RFC 7854), config-as-strings, synthetic OPENs
- [647](plan/learned/647-bmp-5-sender-compliance.md) -- BMP sender compliance: real OPENs, Route Mirroring, ribout dedup
- [879](plan/learned/879-l2tp-priority.md) -- L2TP control message priority: Ns-at-send-time invariant enables queue reordering; kernel P-bit limitation
- [919](plan/learned/919-mpls-kernel.md) -- Kernel MPLS FIB: labeled BestChange routed through rich-route RTA_ENCAP; push uses RouteAdd (no-clobber) vs RouteReplace for relabel; AF_MPLS swap/pop via separate mpls-fib topic
- [920](plan/learned/920-mpls-ldp.md) -- LDP (RFC 5036): dynamic interface reload via discoveryManager reconcile (stop Hellos → adjacency ages out); **plugin config delivered root-wrapped + string-numbers + keyed-map lists — a parser reading the wrong shape leaves the engine idle and unit tests that bypass the parser miss it**; show-proxy must use PluginCommand+ForwardToPlugin (re-Dispatch recurses to stack overflow)
- [1110](plan/learned/1110-ddos-direction-allowlist.md) -- **`ordered-by user` YANG list order does NOT survive plugin config delivery**: the config Tree keeps order in a side-slice (`tree.go` `listOrder`) but plugins receive an unordered `map[string]any` (JSON), so a rule list loses insertion order (policyroute works around it with an explicit `order` leaf + `sort.Slice`). Resolved by making the ddos traffic policy LONGEST-PREFIX-MATCH (most-specific wins, ties → deny) instead of first-match — validate the delivery shape before committing to ordering semantics. Also: the detector is the single policy enforcement point and encodes the exempt/mitigate decision on the event (`SuppressMitigation` flag, zero=mitigate) because responders get only their own subtree and cannot read the detect-owned policy. Direction (local vs remote) via a new `iface.AddressIsLocal` (netlink RTN_LOCAL / VPP local path). QEMU `.ci` gotchas: ddos behavioral tests use the in-daemon `ze_api` observer probe (the `daemon.pid` file handshake is dead); a loopback victim needs a sink socket (ICMP backscatter mis-resolves it); loading the firewall nft backend deadlocks command dispatch under flood; parallel ddos flood tests interfere on shared `lo` (run serially, `-p 1`).
- [921](plan/learned/921-mpls-rsvp-te.md) -- RSVP-TE (RFC 3209/2205): RESV soft-state refresh at egress/transit; link-failure→PathErr sourced from iface EventDown (no IGP); RRO prepend per §4.4; same config-shape + show-proxy traps as LDP; **AC-12 unsatisfiable — FRR has no rsvpd daemon**
- [925](plan/learned/925-mpls-rsvp-te-fast-reroute.md) -- RSVP-TE Fast Reroute (RFC 4090) facility backup: local repair is a single in-worker FIB reprogram in handleLinkDown (push bypass label over protected label); **AF_MPLS swaps must use RouteReplace not RouteAdd — local repair re-programs an existing in-label, RouteAdd EEXISTs on a live kernel and the fake FIB + isolated-program QEMU test both hide it**; bypass paths configured explicitly (no CSPF), keyed in the same lspTable via a reserved tunnel-id base; **node protection needs RRO label recording — the PLR pushes the NNHOP's recorded label, not the NHOP's**; one-to-one detour split to mpls-9
- [923](plan/learned/923-isis-8-dis-broadcast.md) -- IS-IS DIS election + pseudo-node (ISO/IEC 10589 §8.4.5): pure per-level election (priority desc, MAC desc) + damping in circuit/dis.go; pseudo-node LSP reuses the isis-6 Originator (non-zero pseudonode SourceID, members at metric 0); own-LSP star encoding in levelState; **isis-5 reconcile does NOT re-advertise a live circuit's priority** (engine AC-5 via DIS-loss/join, not reconcile); **an abruptly-departed DIS's pseudo-node ages out over MaxLifetime — purge-before-yielding only applies to a node losing the role while present**
- [957](plan/learned/957-ospf-3-ip-transport.md) -- OSPFv2 raw IPv4 transport: per-interface RX/TX socket split, iface resolver before socket bind, `IP_TTL` plus `IP_MULTICAST_TTL` both set to 1, IP multicast membership via address-bound `IPMreq`, and QEMU peer-netns veth tests for real multicast behavior
- [958](plan/learned/958-ospf-4-component-config.md) -- OSPFv2 config backbone: plugin/YANG/runtime stay self-contained under `internal/plugins/ospf`; `ze config validate` needs a static section allow-list; OSPF depends on `interface` for router-id derivation; dispatcher checks receiving-interface area; ranges are IPv4-only in YANG and Go; link-up/down callbacks keep engine state aligned with raw sockets
- [959](plan/learned/959-ospf-5-interface-ism.md) -- OSPFv2 interface ISM follows the IS-IS per-interface runtime pattern but keeps ISM and NSM separate; Hello DR/BDR fields are interface addresses, not Router IDs; passive and loopback records exist without raw sockets; BackupSeen requires 2-Way; inactivity timers must schedule exact neighbour deadlines
- [960](plan/learned/960-ospf-6-neighbor-nsm.md) -- OSPFv2 Neighbor State Machine: ISM drives Hello-derived events, NSM owns DD/LSReq synchronization only; nil LSDB means no-request until spec-ospf-7 wires `SetLSDB`; DD/LSReq/LSUpdate chunk by OSPF payload budget (`InterfaceMTU - IPv4HeaderLen`), not full MTU; packet-handler tests must dispatch encoded packets through the engine
- [961](plan/learned/961-ospf-7-lsdb-flooding.md) -- OSPFv2 LSDB/flooding: IS-IS-style full regeneration for Router/Network-LSAs; `lsdb` owns retransmit lists and purge retention; neighbor Loading only drains requests after LSDB acceptance; Type 5 cleanup is AS-wide across normal areas; MinLSInterval needs timer retry plus unchanged-body skip; MaxSequenceNumber restart happens when the acknowledged MaxAge purge resets own sequence
- [962](plan/learned/962-ospf-8-spf-rib.md) -- OSPFv2 SPF/RIB: IS-IS-shaped graph/SPF/route/install/computer split without shared protocol code; FIB install is one Loc-RIB Path per equal-cost next-hop, never redistevents; Loc-RIB must emit ECMP membership-only `ChangeUpdate` without a misleading ForwardHandle and sysrib startup replay must carry `PathGroup.ECMPNextHops`; carrier-down keeps config in `running`, keeps the down active interface in topology for area membership, and suppresses its links during origination so stale Network-LSAs flush; `.ci` regexes are unquoted and tmpfs commands do not run from the repo root
- [972](plan/learned/972-ospf-af-unify.md) -- OSPFv3 unified engine follow-up: one `ospf` engine with Transport, Codec, and AFPrefixStrategy seams; `ospfv3/{types,packet,transport}` stay leaf modules consumed by the engine; OSPFv3 next-hop source is neighbor link-local per interface, not Router-LSA link data; Link-LSAs are interface-scoped and must participate in DD/LSReq exchange or Loading never drains; scope-typed LS Types need helper classification; real v6 redist interop requires BGP source init registration plus generic `redistevents` best-path production.
- [974](plan/learned/974-ospfv3-4-link-lsa.md) -- OSPFv3 Link-LSA + DR Intra-Area-Prefix aggregation (spec-ospfv3-4): a new link-local LSDB flooding scope (per-interface store reusing `areaDB`) alongside area/AS; the DR aggregates attached routers' Link-LSA prefixes (excluding NU/LA option bits and link-local addresses) into a Network-referenced Intra-Area-Prefix-LSA; link-scope LSAs MUST flow through DD summaries + LSReq lookup or Loading never drains; v6-only (OSPFv2 never touches the link store)
- [975](plan/learned/975-ospfv3-5-nssa-redist.md) -- OSPFv3 NSSA redistribution (spec-ospfv3-5, RFC 3101): a v6 ASBR inside an NSSA injects Type-7 (0x2007), the elected ABR translates to Type-5; reuses the v4 NSSA policy and `ExternalLSA.WriteTo` (the NSSA-LSA body is byte-identical, differing only in LS Type/scope); P-bit lives in PrefixOptions not header Options; Part B bridges BGP best-path changes into the generic redistribution producer (`bgpredist.EmitBestChange`) so `import bgp` installs real routes; the redist framework still couples to a configured BGP reactor (follow-up)
- [971](plan/learned/971-ospf-14-must-remediation.md) -- OSPFv2 MUST-compliance remediation (spec-ospf-14, audit-then-fix over ospf-1..13): 18 RFC-MUST gaps fixed across auth (RFC 2328 App D/5709/7474), config validation (App C.3), flooding (§13/Table 19), NSSA (RFC 3101). KEY DECISIONS: AC-14 auto-default scoped to **totally-stubby (no-summary) NSSAs only** (`isABR && (NoSummary||DefaultOriginate)`), regular NSSAs stay operator-gated (FRR/Cisco parity); AC-18 boot count = ZeFS blob `KeyOSPFAuthBootCount` read+incremented once in `newEngine`, fallback = **SHA-1(time.UnixNano) truncated to 32 bits** (plain seconds collide on fast restart); P-bit clamp + `HigherRIDType5Exists` suppression enforced at the **LSDB origination boundary** (no caller bypass); AC-1 trailer length is **exact `!= len(wire)`** not `>=`; AC-5 recvSeq reset wired through `nsmAdapter` on NeighborDown/InterfaceDown. GOTCHAS: injected new-diagnostics/lint LAG+go STALE -- trust `go vet`/`go test`/grep; two flooding tests encoded the OLD buggy behavior and had to be updated to RFC-correct (correctness sync, not weakening); `floodExcept` signature change rippled to 6 callers; AC-3/6/7 wired to `ze config validate` via `verifyOSPFConfigSections` (InProcessConfigVerifier) so the `.ci` proves the user surface (cost/transmit-delay 0 caught by YANG range, key-id 256 by `ErrKeyIDTooWide`); **AC-14 end-to-end is Linux-CI/QEMU-only** (ABR needs 2 interfaces in 2 areas; loopback-only daemon harness can't) -- engine unit test proves it, interop variant left as open item. OSPFv3 isolation held (v2/v3 share no code, `ospfv3/types` import-guard). Open item: AC-14 no-summary auto-default interop in `ospf-stub-nssa-frr` (currently only the explicit `default-originate` path).
- [970](plan/learned/970-ospfv3-3-ipv6-transport.md) -- OSPFv3 raw IPv6 transport (spec-ospfv3-3): `internal/plugins/ospf/v3/transport/` mirrors the OSPFv2 transport orchestrator (Backend/InterfaceHandle/Transport, iface EventBus lifecycle, rescan) but swaps the IPv4 raw socket for one `golang.org/x/net/ipv6.PacketConn` per interface (PPP-RA pattern). KEY DIVERGENCES: IPv6 raw sockets deliver the upper-layer payload with NO IP-header strip -- `src` from `ReadFrom`, `dst`/`ifindex`/`hopLimit` from the ancillary ControlMessage (`SetControlMessage(FlagDst|FlagInterface|FlagHopLimit)`); the address-bound IPv6 checksum is FINALIZED ON TX by the transport (only it knows the egress source) via `packet.FinalizePacketChecksum`, binding the same link-local as `ControlMessage.Src` so the on-wire source == the pseudo-header source (kills the "passes on veth, fails at a real peer" class). Instance ID demux (RFC 5340 §4.2.1) lives in the TRANSPORT rxLoop (umbrella + named test) reading byte 14 via `packet.PeekInstanceID`, per-interface Instance ID from `EnableInterface(name, instanceID)`. INTEROP LESSON (design review vs FRR source): FRR ospf6d DROPS received multicast OSPFv3 with `hoplim != 1` (RFC 5340 App A.1) -- hop limit 1 is MANDATORY, GTSM-255 is BFD's not OSPF's. `SetChecksum(true,12)` kernel offload rejected (can't zero the checksum for RFC 7166 signed packets). Link-local source lags link-up (IPv6 DAD) -> `ErrNoLinkLocal` + rescan/addr-added retry. QEMU veth round-trip asserts PEER `VerifyPacketChecksum`. Added `packet.FinalizePacketChecksum`+`packet.PeekInstanceID` to the ospfv3-2 codec. Next: ospfv3-4-plugin-config.
- [969](plan/learned/969-ospfv3-2-wire.md) -- OSPFv3 packet+LSA codec (spec-ospfv3-2): `internal/plugins/ospf/v3/packet/` is a SEPARATE codec mirroring the OSPFv2 buffer-first conventions (`WriteTo(buf,off) int`, lazy `RawBytes` re-flood, opaque unknown-type passthrough, length-validated decode) but sharing no code; consumes `ospfv3/types`. KEY DIVERGENCE: the OSPFv3 packet checksum is the IPv6 upper-layer checksum over a pseudo-header (src16+dst16+len32+zero24+nextHeader=89), so `PacketChecksum(src,dst,pkt)`/`VerifyPacketChecksum` TAKE the IPv6 addresses (transport supplies them); `Packet.WriteTo` leaves the checksum field zero and transport finalizes (also covers the RFC 7166 auth-trailer case). The LSA Fletcher checksum is byte-identical to OSPFv2 (`lsa[2:length]`, non-zero), re-owned here. BLOCKER LESSON (review): a round-trip test of a wire-WRONG-but-self-consistent encoding passes green -- the first AS-External impl put the E/F/T flags at body offset 6 + an 8-bit Referenced LS Type, but RFC 5340 §A.4.7 puts flags in byte 0 (sharing the Metric word) + a 16-bit Referenced LS Type @6; the spec table AND research digest had the same error so nothing caught it until an independent review vs the RFC. FIX = flags@0, `types.LSType` 16-bit reftype@6, + a hardcoded GOLDEN VECTOR. **Every wire codec needs >=1 golden/hardcoded-bytes vector per structure, not only round-trips (round-trips prove encode==decode, not encode==RFC).** Fletcher-255 treats 0x00==0xff (both 0 mod 255) so a tamper test must avoid those byte values. Next: ospfv3-3-ipv6-transport (raw IPv6 proto-89, QEMU).
- [968](plan/learned/968-ospfv3-1-types.md) -- OSPFv3 leaf types (spec-ospfv3-1, FIRST OSPFv3 child): `internal/plugins/ospf/v3/types/` is a SEPARATE copy of the OSPFv2 leaf conventions, NOT a shared package (umbrella guide §15: do NOT unify v2/v3; FRR ships two daemons) -- a `go/parser` import-guard test enforces no ospf/ospfv3-sibling/component/rib imports; OSPFv3 LS Type is 16-bit with embedded flooding scope (U-bit 0x8000, S2/S1 scope 0x6000 shift 13, 13-bit function 0x1fff), so `LSType.Scope()/FunctionCode()/UBit()` decode it and the LSDB never re-derives scope; `LSAKey=(LSType,LinkStateID,AdvertisingRouter)` has NO separate scope field (the type carries it); width fidelity for FRR interop = 24-bit Options + 24-bit Metric + IPv6 prefix byte-len `((PrefixLength+31)/32)*4` with zero-padding validation; GOTCHA `uint32(<negative typed const>)` overflows at COMPILE time (use int32+%d); AreaID parses dotted-quad OR plain int. Next: ospfv3-2-wire (packet+LSA codec)
- [967](plan/learned/967-ospf-13-cli-diag-interop.md) -- OSPFv2 CLI/diag/web/interop (spec-ospf-13): presentation layer over the engine, NO new protocol logic; CLI the IS-IS/LDP way (`ze-show:ospf-*`/`ze-clear:ospf-*` RPC proxies in cmd_show.go + ze-ospf-cmd.yang command tree, proxy `PluginCommand` MUST match engine `CommandDecl` exactly); ONE generic web view (`snapshot_views.go` snapshotHandlers + `sse_snapshot.go` loop + `page_snapshot.go` escaped shell) with IS-IS refactored onto it + OSPF a parallel thin adapter (`//nolint:dupl` on the PACKAGE line -- dupl attributes two-file clones there, function-level nolint does not suppress); doctor reuses `parseOSPFConfig` via `tree.ToMap()`->`{"ospf":{...}}` (same shape as `ExtractConfigSubtree`) so verdict can't diverge from runtime; `ze_ospf_*` namespace asserted via a recording `metrics.Registry`; shared `show > ip` YANG container description must be copied VERBATIM or the command-tree merge warns; config-test JSON needs nested list key + explicit key leaf (`areas:{area:{"0.0.0.0":{area-id:...}}}`); on-startup/on-shutdown max-metric parsed-but-unarmed engine-wide (summary `stub-router.active`=always-only, documented); 6 FRR `ospfd` interop scenarios + `FRROSPF` harness auto-discovered (`ospfd=yes`), Linux-CI-gated like the IS-IS siblings; review gate 0/0 (3 NOTES)
- [966](plan/learned/966-ospf-12-auth.md) -- OSPFv2 authentication (RFC 2328 App D / 5709 / 7474): the ospf-2 codec already excludes the 8-byte auth field from the checksum and zeroes it for AuType 2/3, so auth is just the crypto backend on top; sign at ONE transport chokepoint (`transport.SetSigner` in `SendPacket`) that rewrites the AuType byte + fixes the checksum (recompute for AuType 1 since the AuType byte is in the checksum region, zero for crypto) + appends the digest, instead of threading auth into every encoder; verify at ONE dispatcher chokepoint (`authOK`) after checksum/area, before any handler; Keyed-MD5 is `MD5(packet||key16)` NOT HMAC, while HMAC-SHA uses `hmac.New(H, Ko)` with Ko derived to L octets (Go zero-pads Ko to block B, matching RFC 5709's Ko-XOR-Ipad); a per-chain `extended-sequence` bool selects AuType 3 (64-bit seq trailer + 0x0001 protocol-id key suffix); `$9$` secrets via `secret.Decode` with plaintext fallback; per-(iface,neighbor,key-id) non-decreasing sequence replay; boot-count NVRAM persistence is a documented limitation (intra-session replay still enforced)
- [965](plan/learned/965-ospf-11-stub-nssa.md) -- OSPFv2 stub/NSSA (RFC 3101): extend existing scaffolding (areaTypes, flood filter, OptionNP, Type7=LSTypeNSSA sharing the Type 5 body codec) rather than duplicate; stub Type 3 default injection lives in the SPF summary originator (`applyAreaTypePolicy`, threaded via `SummaryInput.Policies`), not the LSDB; the P-bit rule sets Type 7 P=1 only when the router cannot inject Type 5 directly (`externalScope.canType5` = normal/backbone attachment OR no NSSA attachment) and FA!=0; translator election is local (highest Router ID among NSSA Router-LSA B-bit ABRs) with a stability grace (`translatorEffective` keeps translating for stability-interval after losing); §2.5 preference is a new primary key on the external candidate (Type7-P1 < Type5 < Type7-P0) ahead of trap #7; NSSA reconciliation runs from BOTH reconcile and the 1s tick so it needs its own `nssaMu`; a Type 7 P-bit toggle on an unchanged body must still re-originate (body-compare alone misses it); the translated Type 5 shares the AS-wide key with self-redistributed Type 5 (documented collision); split `dispatcher` out of `instance.go` at the 1000-line cap
- [964](plan/learned/964-ospf-10-as-external-asbr.md) -- OSPFv2 AS-External/ASBR: two never-merged paths (FIB install vs `redistevents`); path type is the primary external key (trap #7 E1>E2); the redistribution source AND consumer share the name `ospf` so self-import auto-rejects; `default-information originate` lives on the engine (reads Loc-RIB via the in-process `locrib.Default()` singleton), `always` unconditional vs conditional-on-a-non-OSPF-RIB-default re-evaluated live by a Loc-RIB `OnChange` watcher (handler runs under the shard write lock -- never re-enter the RIB, defer to a worker); the watcher's second goroutine exposed a latent LSDB race (`installOriginated` mutated the `*Entry` after `install` released `d.mu`) fixed by an `installLocked` that holds the lock across install + Header + markPurged; `hasNonOSPFDefault` excludes OSPF's own source to avoid a self-sustaining loop; a `defaultInfoOriginated` flag stops a withdraw purging a redistribute-owned `0.0.0.0/0`
- [963](plan/learned/963-ospf-9-inter-area-abr.md) -- OSPFv2 inter-area/ABR: inter-area is a producer/consumer pair over the ABR's own §16.1 route table (intra costs are both the Type 3/4 metric and the cost-to-ABR), one `locrib.Path` per prefix, trap #8 backbone-only acceptance is the load-bearing loop-freedom rule; review gate caught a latent spec-8 gap where transit/broadcast-LAN subnets (Network-LSA prefixes, not stub links) were installed/summarized nowhere -- fix emits `VertexNetwork` routes (§16.1(4)), skipping directly-connected LANs on install but including them for summary; LS-ID collision uses increment-until-free not RFC Appendix E (collision-free but FRR wire LS-IDs may differ, ospf-13 interop); verify agent numeric findings against the constant (`LSInfinity = 0x00ff_ffff`, not 65535)

### OSPF extension family (ext-1..16, unified AF-neutral engine per [972])
- [1029](plan/learned/1029-ospf-ext-1-opaque-framework.md) -- OSPFv2 opaque-LSA carrier (RFC 5250): UNEXPORTED `registerOpaqueConsumer(type,scope,onOriginate,onReceive)` (all consumers in-package); scopes link/area/AS = Type 9/10/11; generic TLV iterator + LS-ID split `OpaqueType()`/`OpaqueID()`; pull-model `OnOriginate` origination seam; each consumer owns its typed decoder, generic fallback is type/length/hex
- [1030](plan/learned/1030-ospf-ext-2-traffic-engineering.md) -- OSPFv2 TE LSAs (RFC 3630 + RFC 5392 inter-AS): TE opaque (Opaque Type 1) Router-Address + Link sub-TLVs; inter-AS TE is an AS-wide Type-11 opaque; `isASWideType = t.ASExternal() || t == LSTypeOpaqueAS`; landmine fixed in review: Type-11 AS-wide retransmit + the floodLink O-bit gate
- [1031](plan/learned/1031-ospf-ext-3-router-information.md) -- OSPF Router-Information LSA (RFC 7770): `registerRITLV(type,scope,build)` hook; U=1 code values are 0xA00C/0xC00C/0x800C (NOT the U=0 0x200C/0x400C/0x000C the RFC digest wrongly showed); `ASExternal()` (function-code-5-only) vs `ASWide()` (AS-wide store/flood) is a load-bearing split
- [1039](plan/learned/1039-ospf-ext-4-extended-link-prefix.md) -- OSPFv2 Extended Prefix/Link opaque LSAs (RFC 7684): Opaque Type 7 (prefix) / 8 (link) consumers of ext-1; `registerPrefixSubTLV`/`registerLinkSubTLV(subType, extSubTLVCodec{Build,Receive,Render})`; exactly one Extended-Link LSA per P2P/transit link, correlated to the Router-LSA link by Link Type/ID/Data; landmine: `<=` vs `<` dedup
- [1050](plan/learned/1050-ospf-ext-5-segment-routing.md) -- OSPF Segment Routing SR-MPLS (RFC 8665 v4 / RFC 8666 v6): shared RI capabilities (SRGB/SRLB/algorithms) + per-AF Prefix-SID sets. LANDMINE (spec was wrong): the forwarding label uses the NEXT-HOP router's SRGB with penultimate-only PHP (RFC 8665 §5), not the originator's (originator-SRGB blackholes heterogeneous-SRGB nets) -- threaded via a new `spf.NextHop.Router`. LANDMINE: the per-AF `v6caps` override must be explicitly cleared when the IPv6 SR block is removed (srTestReset masks it in unit tests)
- [1051](plan/learned/1051-ospf-ext-6-ti-lfa.md) -- OSPF TI-LFA / base LFA fast reroute (RFC 5286 + TI-LFA draft + RFC 8665): a compute + carry-through + install feature, no wire change; per-neighbour SPF is the existing Dijkstra re-rooted; strict inequalities + Errata 2323; backup carry-through on `locrib.Path` (IN `Equal` for change-detection, EXCLUDED from arbitration key). LANDMINE (proved by a theorem): the "S as its own P-node, push a local Adj-SID" TI-LFA repair is DEAD CODE -- any neighbour adjacent to S and in Q-space is already a base LFA, so every reachable TI-LFA Adj-SID repair is a REMOTE-node Adj-SID (needs remote Extended-Link Adj-SID decode). Do NOT test an AC with a fake resolver that inverts the production contract
- [1043](plan/learned/1043-ospf-ext-7-virtual-links.md) -- OSPF virtual links (RFC 2328 §15 / RFC 5340): a synthetic Area-0 P2P interface whose packets are ROUTED across the transit area (`SendPacketRouted`, TTL/hop-limit >1); received packets demux by (source RouterID + backbone Area + a REAL enrolled transit interface), NOT by ifindex; the V-bit goes in the TRANSIT-area Router-LSA (the spec AC-8/9 "backbone V-bit" wording is stale/wrong); IPv6 endpoints resolve global src/dst from the transit-area Intra-Area-Prefix-LSAs; dispatcher `areaOK` signature changed to carry the full Header
- [1040](plan/learned/1040-ospf-ext-8-nbma-p2mp.md) -- OSPF NBMA + Point-to-Multipoint network types (RFC 2328/5340): network-type nbma|point-to-multipoint + poll-interval + nbma-neighbor config; PtMP originates per-neighbour /32 (v4) or /128 (v6) host stub routes; `MetricFromBytes` relaxed to accept 0 (host routes / FRR); §9.5.1 Hello eligibility
- [1041](plan/learned/1041-ospf-ext-10-bfd.md) -- BFD for OSPF (RFC 5880/5881): per-interface `bfd {min-tx;min-rx;multiplier}` (ms, stored us); OSPF subscribes to the bfd COMPONENT via onNeighborFull/onLost event sinks (never imports it, plugin-removal invariant); `LinkScopeLSDB` unexported at integration
- [1042](plan/learned/1042-ospf-ext-11-ldp-igp-sync.md) -- OSPF LDP-IGP synchronisation (RFC 5443/6138): per-interface `ldp-sync {enable;holddown}`; cost-out to LSInfinity applies ONLY to the p2p/transit link (the stub keeps its configured cost via a withhold flag at origination, NOT an info.Cost override). LANDMINE: an LDP `adj.Interface` write after the table mutex released was a live data race (fix: set it under the lock). OSPF subscribes to LDP by literal JSON event strings, not by importing the ldp package
- [1044](plan/learned/1044-ospf-ext-9-graceful-restart.md) -- OSPF Graceful Restart (RFC 3623 v4 + RFC 5187 v6): IPv4 Grace-LSA rides the ext-1 link Type-9 opaque carrier; IPv6 is a native v3 link-scope LSA. LANDMINE: the OSPFv3 Grace function code 11 (0x000B) collides with the OSPFv2 Opaque-AS Type 11 in the AF-neutral `types.LSType` -- use a distinct internal sentinel `LSTypeGraceV6 = 0x800B` mapped ONLY at the v6 codec seam. Exit triggers need PRODUCTION `onFull` wiring, else the restarter only exits at grace-expiry (the exact black hole GR prevents)
- [1036](plan/learned/1036-ospf-ext-12-multi-instance.md) -- OSPFv2 Multi-Instance (RFC 6549): per-interface `instance-id` leaf-list; the Instance ID lives at packet-header offset 14 (the former AuType high octet); per-instance transport isolation (deviation from shared-transport fan-out); instance-mismatch drops counted
- [1052](plan/learned/1052-ospf-ext-14-debug-introspection.md) -- OSPF Debug & Introspection tooling, both AFs: decode + inspect + explain + authz-gated inject (the in-process replacement for FRR ospfclient); a per-family decoder registry (Opaque Type v4 / LS Type v6) where each consumer registers its OWN decoder; inject is DOUBLE-GATED fail-closed (authz `deny debug` + an engine debug-enablement flag, never on web) and SPF-explain is read-only (retained lastCandidates + a runs counter). LANDMINE: the inject denial that actually fires in production is `Edit.Default = Deny` (because `IsReadOnlyPath("debug ...")` is false), NOT the `deny "debug"` Run rule -- test the production `readOnly=false` path
- [1037](plan/learned/1037-ospf-ext-15-multi-af.md) -- OSPFv3 Multiple Address Families (RFC 5838): per-AF v6 engine set (`v6EngineSet.spawn`); the AF is carried in the v3 Options `OptAF` bit + the Instance-ID range; ctor renamed to `newEngineWithCodecAF`. LANDMINE: an unconditional `SetV4OverV3Injector` made `injectorFor(IPv4Unicast)` always return the v4-over-v3 wrapper, SILENTLY DROPPING OSPFv2 IPv4 redistribution when no ipv4-unicast AF is configured (fix: fall back to the OSPFv2 injector unless the AF is actually configured)
- [1038](plan/learned/1038-ospf-ext-16-ipsec-auth.md) -- OSPFv3 IPsec AH/ESP authentication (RFC 4552): rides the IKE dataplane. LANDMINE: a 2-transport-SA model (dst=ff02::5 + dst=link-local) blackholes unicast DBD, unicast LSU retransmit, and ff02::6 AllDRouters, stalling the adjacency in ExStart -- fix is a single wildcard-address SA (src=::,dst=::) bound by reqid + a proto-89 traffic selector, plus interface-scoped policies. IKE stays byte-identical (Sel nil, IfIndex 0). Kernel XFRM is QEMU-validated

## Observability

Metrics, telemetry, Prometheus exporters, third-party format compatibility.

- [653](plan/learned/653-netdata-os-collectors.md) -- Netdata-compatible OS collector framework, 138 metrics, counter-wrap protection, per-collector config via YANG, verify names against source not summaries
- [736](plan/learned/736-iface-rate.md) -- Interface rate tracker: raw backend stats (not baseline-adjusted), 12 GaugeVec, stale label cleanup, ticker+stop-channel lifecycle
- [808](plan/learned/808-smart-management.md) -- YANG-modeled SMART disk health: core/smart ioctl library (host wraps, no smartctl), storage.Manager ticker+stopCh, three-tier temperature alerting via report bus, self-test scheduling, `show storage smart` RPC via atomic pointer; hub-wired (not a plugin)

## Security/Auth

Authentication, authorization, appliance bootstrap, and credential boundaries.

- [831](plan/learned/831-appliance-auth-hardening.md) -- Appliance auth hardening: local admin uses `meta/auth/local/*`, config-file users share web/API/SSH auth loading, mutation paths enforce RBAC across web CLI, REST, and gRPC
- [1159](plan/learned/1159-fixit-cli-credential-resolution.md) -- CLI credential resolution: the client is NOT an identity authority (`ze cli --user` is only SSH; the daemon authenticates every username at `ssh.go:433-453`), so client-side identity restriction buys nothing `ssh alice@host` cannot bypass -- a proposed root-gate on `--user` was dropped for that reason, and because role accounts (`thomas` -> `noc`) require a free choice of login name. Two structural rules came out of it: prompting is a CALLER policy (`allowPrompt`), never inferred from tty state, because tab completion runs on a tty and a tty check hangs the shell; and the zefs store is one credential SOURCE among flag/env/defaults, not a precondition (it is one shared `0600` file under a binary-derived `/etc/ze`, so every non-installing user was locked out). Store-open failures are classified: missing/unreadable continues, corruption still surfaces

## Agent Workflow

Agent rules, self-improvement, discovery paths, and development-time inventories.

- [832](plan/learned/832-self-improving-discovery.md) -- Self-improving discovery: feature/tool/check/test changes must update docs, rules, indexes, and verification paths; `ze-verify-wiring-docs` backs changed-file discovery gates
- [833](plan/learned/833-commit-helper-tooling.md) -- Commit helper tooling: `scripts/dev/commit_helper.py` owns session reuse, message files, executable user-run scripts, ignored-path rejection, `git commit -F`, and learned-summary gating for workflow/tooling/rule changes
- [835](plan/learned/835-commit-request-fast-path.md) -- Commit requests are a fast path: create the user-run helper script, skip late completeness reviews, and treat `verify-status.sh check` FRESH as a hard no-rerun signal for `make ze-verify`
- [876](plan/learned/876-commit-helper-session-collision.md) -- Per-session tmp/ artifacts must embed a session-unique path component: the shared commit-session-id let a concurrent session's `--replace` overwrite another's prepared commit script; helper now keys the ID file by Claude session fingerprint
- [1006](plan/learned/1006-feature-surface-gate.md) -- Feature Surface Gate generalizes the BGP Family Gate to all feature types: /ze-spec forces reading the matching pattern doc + filling the template Integration/Documentation checklists; /ze-review gains a doctor-check row (the surface gated nowhere else) and a discovery-index row; de-orphans `discovery-updates.md` from the skills
- [1086](plan/learned/1086-claude-runs-commit-script.md) -- Claude runs the commit script itself (`bash tmp/commit-<SESSION>.sh`) instead of handing it to the user; the script -- not the human -- is what makes add + delete + commit atomic. Bare `git commit`/`git add` tool calls stay banned ("committing outside a script is not" allowed); no hook change (the `git commit` block still forces the script path); supersedes the "user runs the script" mechanism in [833]/[835]
- [1155](plan/learned/1155-learned-numbers-collide-across-branches.md) -- `learned-next` cannot prevent duplicate learned numbers ACROSS branches: `learned_next` (`commit_helper.py:1120`) takes `max(highest+1, counter)` off the LOCAL filesystem and the `O_EXCL` create at `:1122` only defeats a racing session in the SAME tree, so two branches allocate the same number by construction and the duplicate appears only on merge/rebase (one 2026-07-16 rebase produced five at once; 22 duplicate numbers existed corpus-wide). `learned_index.py` globs and renders, so `LEARNED-FULL-INDEX.md` grew two rows with one `#` and nothing complained. New `scripts/dev/learned_numbers.py --check` (in `ze-doc-test` + `ze-regen-check`) asserts uniqueness and H1-matches-filename; `--fix` resolves. GOTCHAS: `.counter` is NOT a defence -- the file-prefix scan backstops a stale counter, so resolving a `.counter` merge conflict prevents nothing; `--fix` keeps the MOST-REFERENCED summary at the contested number (ties: earliest add), which touched zero `.go` files because every `// Design:`-cited summary was already the most-referenced in its group
- [1093](plan/learned/1093-followup-hooks.md) -- Enabled three dead agent-guard hooks: `c_format_alloc` (BGP format-file guard) live with a comment exemption; `validate-spec.sh` arrow + `set -e` crash fixed (kept exit-2 blocking per a 124-spec survey); four commit-gated checks re-homed from `pretool-bash.py` (they matched the `git commit` string the sanctioned path never sends) into `commit_helper.py` creation-time gates + a ported spec-audit keyed to the per-session marker. `deferral_in_diff` matches PROSE only (`_deferral_prose` blanks quoted/backticked spans) so the detector does not self-trip. New `hook-fixture-check.py` + `make ze-hook-test`. Gotcha: `hook-parity-check.py` golden was macOS-blessed; `/tmp` fixture dirs trip `c_system_tmp_we` on Linux -- fixed via `~/.cache`, blessed nothing

## Testing

Test patterns, infrastructure, chaos testing.

- [1172](plan/learned/1172-rfc-requirement-coverage.md) -- RFC MUST-to-test coverage gate: authored test-side `RFC requirement:` tags + derived two-way ledger, mandatory both-polarity rule, section-anchored IDs, fingerprinted semantic audit, RFC-tagged-test approval hook. Lessons: a committed pilot's "green" can be a lie (confounded tags passing via an unrelated rule), the gate's own ratchets shipped unwired, and an NLRI-syntax walk is add-path-blind unless the negotiated per-family state is threaded into it
- [274](plan/learned/274-spec-test-diagnostics.md) -- Test diagnostic improvements
- [258](plan/learned/258-bgp-chaos-families.md) -- Chaos family fuzzing
- [265](plan/learned/265-bgp-chaos-selftest.md) -- Chaos self-test patterns
- [608](plan/learned/608-concurrent-test-patterns.md) -- Concurrent-test flake patterns (locked-write/unlocked-read, subscribe-before-broadcast, gate-handler, barrier FIFO, cleanup-drains-work)
- [881](plan/learned/881-test-flake-under-load.md) -- Host-load test flakiness: wall-clock timeouts under CPU starvation, near-timeout classification, contended-run labeling, timing baseline pollution prevention
- [723](plan/learned/723-chaos-actions-v2.md) -- Parameterized chaos actions: string-map params over typed unions, opt-in scheduling, per-instance weights
- [787](plan/learned/787-chaos-inprocess-scheduling.md) -- In-process chaos: feed vc.Now() to existing schedulers, reconnectDialer factory for stochastic reconnection, tick-channel pattern
- [797](plan/learned/797-interop-gap-coverage.md) -- Interop gap coverage: 5 scenarios (RR, policy, RPKI, BMP, max-prefix) with FRR/BIRD/GoBGP peers, concurrent Docker subnet retry, parse coverage for IXP/large-scale/RPKI/redistribution
- [800](plan/learned/800-bgp-chaos-integration.md) -- Chaos integration tests: fork mode default, run() testability, port auto-allocation
- [802](plan/learned/802-chaos-multi-target.md) -- Multi-target chaos: FRR/BIRD config gen, temp-file fork, single-port dialing, BIRD channel mapping limits
- [842](plan/learned/842-scoped-verify-committed-gap.md) -- Scoped verify committed gap: ze-verify-changed tested only the working-tree diff, so a regression committed before verifying was skipped on the clean tree; changed set now adds packages committed since the last green verify (`scripts/dev/changed-pkgs.sh`, baseline from `tmp/ze-verify.status`)
- [843](plan/learned/843-verify-debugging-protocol.md) -- Verify failure-routing protocol: tested `verify_run.go` owns ze-verify, writes compact text+JSON failure index, groups by stage boundary first (never across), native `VERIFY FAILURE GROUP: {json}` manifests over text parsing, `ZE_VERIFY_MODE=1` env-gated rendering; the house convention for machine-readable test output
- [892](plan/learned/892-spec-validate-command.md) -- Post-verify validation tool (`make ze-validate`): Python-based doc/spec hygiene checks (stale anchors, line-number anchors, unwired exports, spec AC completeness); grep-based cross-package search over Python rglob for speed
- [908](plan/learned/908-test-trace-mode.md) -- Per-step trace output: leaf `internal/test/trace` package, dual human+machine format (`VERIFY STEP: {json}`), two-tier gate (failure default, `-v` all), incremental `.ci` recording over full collect-all refactor
- [1100](plan/learned/1100-followup-l2tp-call.md) -- Driving a mutating in-process RPC from a `.ci`: enable `api-server { token "secret"; rest {...} }` and POST `{"command":...}` to `/api/v1/execute` with `Authorization: Bearer` (no-auth REST is read-only, so a `request` verb needs the token). For an ze-as-INITIATOR test the inline Python peer BOTH triggers the dial (bg thread POSTs the blocking RPC) AND answers on the wire (fixed `$PORT2`); mirror the L2TP reliable Ns/Nr for the initiator direction, relying on loopback order. Real-peer interop: ze dials a real xl2tpd LNS (scenario 03) — but xl2tpd has NO OCRQ answerer (logs "Unimplemented message 7"), so the call fails by design and the proof is the established control connection; only accel-ppp `mode lac` answers OCRQ (undocumented source-only CLI) → documented A-6 exemption. Self-contained interop scenarios ship their own `run.py` and `test/l2tp-interop/run.py` delegates to them (skips the ze=LNS preflight/image-build). Confirm "pre-existing failure, not my regression" by building `bin/ze` at the parent commit via `git archive` and re-running.
- [1120](plan/learned/1120-payload-predicate-waits.md) -- Payload-predicate waits (Layer 2 sleep-elimination): symmetric predicate waits on both test surfaces. Python `ze_api` gains `wait_until` (pure predicate poll -- needed for KERNEL-state waits that are neither dispatch nor event), `dispatch_until` (result predicate; `dispatch_until_done` becomes its `status=="done"` wrapper), and `wait_for_event(timeout, predicate)`. Go engine-steps gain `expect=output:matches=`(regex, compiled at PARSE time)/`absent=`(output-only, non-vacuous by convention)/`json=<path>=<value>` (first-`=` split, stringified-leaf compare) -- `Match==""`+omitempty keeps engine-steps.json byte-identical. GOTCHAS: (1) an external observer receives NO peer STATE events (`receive [ state ]` and `request subscribe bgp event state` both give events=0) -- to functionally test event delivery, use an **echo-mode ze-peer** (`--mode echo`, peer.go:476) that reflects an announced UPDATE back so the daemon delivers a received-update event. (2) The ci-sleep ratchet is DORMANT until a `.ci` changes, so `test/.ci-sleep-baseline` silently drifts above the true count (found 456 committed vs 462 real); compute the true count with the ratchet's own rglob, and raising a baseline needs explicit user approval. (3) `// test-relax:` needs the literal `//` even in a `.ci` (`# // test-relax:`). (4) touching a file re-runs `ze-validate` over ALL its exported symbols -- pre-existing unwired exports surface and must be unexported or named at the cross-package use site.
- [1171](plan/learned/1171-fixit-reject-fence-observability-deferred-external-plugin-signals.md) -- `await=stderr:contains=` `.ci` fence (sleep-elimination for the reject-fence bucket): a runner primitive that blocks until the daemon's relayed stderr carries a line, then tears the daemon down. GOTCHA: an in-daemon observer plugin CANNOT watch a plugin that refuses startup -- `StartupCoordinator.PluginFailed` aborts the ENTIRE startup barrier, killing co-located plugins, and the daemon doesn't exit on the failure; so a refuse is fenceable only on its relayed stderr, not a queryable-state poll (the warn case is different -- it completes startup). Needles must be plugin-SPECIFIC (bare "refusing to start as an external plugin process" is shared by as112/traffic-usage/flow-export) and colon-free (kv splitter truncates at `:`); the ci-sleep ratchet regex counts `time.sleep(` even inside comments. Authoritative mutation = invert the producer's `IsInternal()` guard, not swap plugins.
- [1101](plan/learned/1101-followup-test-infra.md) -- Test-infra wave (property/QEMU-qdisc/two-peer/stress/chaos-iface) + THE LLGR RAIL GAP: `LLGREgressFilter` (gr_egress.go:57) runs only on the ForwardUpdate rail (forward_rs.go:324, reactor_api_forward.go:490) while the only `meta["stale"]` producer (rib_replay.go:299) flows through `AnnounceNLRIBatch` (reactor_api_batch.go:28) which drops `ctx.Meta` and calls no egress filter (sdk_engine.go:42) -- so RFC 9494 per-peer stamping never fires in production despite green unit tests AND a green single-peer `.ci` (owner: spec-rib-arch-7). Other keepers: stdlib `testing/quick` + fixed seeds suffices (no rapid dep); listener conflicts are symmetric but NOT transitive (wildcard counterexample); privileged kernel-state tests = QEMU suite enrollment (`fsuite traffic -p 1`, `option=needs-linux`), not a new runner; `-race` targets need `make <t> CGO_ENABLED=1` (Makefile:16 exports 0; go1.26 refuses); derive CLI help lists from the data tables (`engine.V2ActionNames()`) instead of hardcoding; `ze-validate` unwired-export grep cannot see registry closures (CLIRun false positive -- do not unexport, tagged cmd/ze tests call it).

## Build/Deployment

Build system, Docker, CI, toolchain upgrades.

- [753](plan/learned/753-docker-go126.md) -- Docker support: two-stage build to scratch, CGO_ENABLED=0, Go 1.26 upgrade across all project references
- [754](plan/learned/754-makefile-split.md) -- Makefile split into mk/ includes: tiered help, component test groups, contributor testing docs
- [853](plan/learned/853-build-tag-split.md) -- Positive build tags (ze_distro, ze_appliance, ze_setup) replace negative ze_stripped; no-tag default is minimal; Go _linux.go suffix gotcha
- [854](plan/learned/854-install-8-appliance-iso.md) -- Appliance ISO installer: transport envelope around raw image, ze.source=iso initrd mode, gzip compression, media-id exclusion, UEFI GRUB boot, hard checksum enforcement, power-off not reboot
- [870](plan/learned/870-kernel-build-convergence.md) -- shared kernel builder: runtime and installer converge on `tools/kernel-builder/` with Linux 7.0.11, runtime fragment tracking, Docker-first builder selection, and modcache overlay/restore flow
- [1103](plan/learned/1103-fixit-appliance-evidence-config.md) -- appliance seed config flow: `daemonRunning` now requires ze's `SSH-2.0-ze` banner (positive-ID over "any TCP listener"); `ze init --seed` skips baking build-host interfaces into the active config that would shadow the first-boot template
- [1106](plan/learned/1106-gokrazy-l2tp-evidence-networking.md) -- gokrazy L2TP evidence AC-3 CLOSURE: the appliance L2TP code was correct; every remaining blocker was harness/logging. `printk.devkmsg=on` stops the kernel `/dev/kmsg` rate-limiter DROPPING the `web server listening` log the harness greps (the "web hang" was a dropped log, not a hang). qemu 8.2.2 slirp does NOT deliver inbound UDP hostfwd to the guest, so the evidence harness now uses TAP+bridge+dnsmasq (+ a temporary ufw hole for DHCP) instead of slirp. GOTCHAS: xl2tpd 1.3.18 truncates `-c` paths >~90 chars; dnsmasq `--bind-interfaces` cannot receive the broadcast DISCOVER (use `--interface`+`--port=0`); "IPv6 not supported by static pool" is the benign IPv4-only IPv6CP-decline reason (not fatal); in-process instrumentation is a Heisenbug -- dump goroutines externally via `ssh <appliance> "show system goroutines full"`
- [1105](plan/learned/1105-vpp-host-tuning.md) -- VPP host tuning: `poll-sleep-usec` is a **unix**-section directive (YANG leaf under `cpu`, emitted in `unix`); hugepage reservation via `image.hugepages` → derived gokrazy instance config in a temp parent dir (raw-JSON patch preserving unknown fields; never edit the checked-in `gokrazy/ze/config.json`). GOTCHAS: `env.Get` ABORTS on an unregistered key (register `ze.test.doctor.*` overrides or `ze doctor` crashes when the check runs); cross-session staging can commit a caller (`cmd_build.go`) without its untracked definition (`kernelargs.go`), leaving HEAD uncompilable-from-clean; linux-tagged **non-integration** tests already run under `make ze-unit-test` (`go test -race ./...`) on linux, so adding a package to `-tags integration` `ze-qemu-integration-test` runs zero of them

## Gotchas

Reusable lessons extracted from gotchas sections across summaries.

- (001) Freeform config parsing without schema causes data extraction failures; schema-driven (YANG) prevents this
- (008) Preserve insertion order of conditional rules -- they apply sequentially, not by specificity
- (013) EOR semantics extend RFC 4724 beyond graceful restart; document RFC violations when accepting them
- (102) Over-engineered specialized types (e.g., Span) often lose to native types; prefer native until proven insufficient
- (133) Renaming packages to short common nouns causes variable shadowing in callers
- (149) Do not force async protocols for in-process communication; adds complexity for no benefit
- (165) Organizational separation is distinct from protocol redesign; reuse existing infrastructure
- (176) Preserve attribute flag values separately -- do not hardcode reconstruction flags
- (247) Check dependency graphs before large restructurings; circular imports are blocking
- (253) Import cycles in test files reveal over-tight coupling; use external test packages
- (266) SSE debounce in HTTP layer prevents blocking the main event loop
- (275) Concurrent sends racing with channel close require WaitGroup coordination to avoid panic
- (647) Early return in event handler blocks housekeeping (caching, cleanup) that must run regardless of sender state
- (647) KEEPALIVE has nil RawBytes; check RawMessage != nil, not RawBytes != nil, for messages with no body
- (652) Verify "does not exist" claims during child spec RESEARCH; umbrella assumed show interface was missing but it was fully implemented
- (652) subsystem-list was hardcoded to ["bgp"]; always check stub implementations before assuming real data flows
- (708) Boolean three-state offloads avoid VyOS bootstrap activation script; ethtool_sset_info.sset_mask is __u64 not __u32; cap kernel-reported feature count before allocating
- (720) Editor commit-triggered archives run in CLI process, cannot emit daemon events; EventEmitter callback pattern enables future plugin backends
- (731) Plugin-to-infrastructure state exposure via registry.SetNTPSyncProvider; plugins register RPCs in their own init() to avoid import boundary violations
- (733) PKI store: shared certificate infrastructure; config.Tree lists via AddListEntry not GetOrCreateContainer; ECDSA keySize via Curve.Params().BitSize not generic Size()
- (734) IPsec data model: L2TP-pattern tree-walker parser; algorithm enum strings match strongSwan naming for ipsec-4 consumption; DHGroup as uint8 with range validation over iota enum; cross-ref validation via function callbacks for testability
- (735) XFRM interfaces: standalone type not TunnelKind (no endpoints); VTI deliberately excluded; GetXFRMInfo for operational display of managed and unmanaged interfaces; XfrmPolicyList is system-wide, filter by Ifid client-side
- (737) IPsec EAP extension: extend AuthMode enum not separate enum; EAP auth shares x509 cert branch; single pool per remote-access; match parser method to YANG node type (leaf vs list)
- (739) IKEv2 crypto primitives: flat map registry over registration pattern for fixed algorithm set; MODP private keys in [2,p-2]; prf+ capped at 255 iterations; constant-time PKCS#7 unpadding; Go GC prevents secure memory wiping
- (740) IKEv2 engine: plugin registration for config delivery; JSON-to-Tree dual storage for container/list ambiguity; per-peer goroutine lifecycle from PPPoE pattern; constant-time PSK verification; store peer config in PeerSession not SA to avoid reconcile race
- (742) Child SA and dataplane: Dataplane interface with Register/Load/Get following iface.Backend pattern; dp.Get() returns nil if Load() never called (silent no-op trap); IfID must come from config not runtime lookup (plugin subprocess has no iface backend); VPP backend compiles but needs govpp/binapi/ipsec vendored; DPD lastSent must init to now() not zero
- (744) EAP and NAT-T: MD4 removed from Go, must implement from scratch; RFC 2759 magic constants exclude trailing null (RFC C array size is off-by-one); EAP-TLS pipes crypto/tls through async net.Conn transport; DetectNAT must accept []byte not [8]byte for 20-byte SHA-1 hashes; NATDetected flag on SA propagates to child SA UDP encap automatically
- (745) IPsec CLI and diagnostics: show/clear/monitor commands for IKE SAs, Child SAs, proposals; ipsec-status daemon RPC returns structured IKE/Child SA state
- (805) EAP interop: initiator must omit AUTH in first IKE_AUTH to signal EAP willingness; server AUTH verified before EAP processing; eap/peer.go separates initiator dispatch from server-side eap.go; shared test PKI across scenarios 03/04; Alpine strongSwan includes eap-mschapv2 and eap-tls plugins natively
- (748) Self-update: SelfUpdater parallel to UpdateChecker (hub starts one or the other based on auto-apply); FNV-1a spread deterministic per device+version; manifest field named Ver to avoid hook false positive; atomic rename with .prev hard-link backup; history persisted across restart
- (749) AI agent tooling: lower-kebab diagnostic codes over short prefixes; dropped errors/warnings arrays (diagnostics is single source); repair metadata on warnings is intentional for fix-plan; FindListenerConflict returns structured pair without breaking ValidateListenerConflicts callers; block-version-config hook needs string-concatenation workaround in test files
- (753) `scratch` Docker image has no shell; `docker exec` debugging requires a multi-stage override or separate debug image
- (754) Adding a named test group to `mk/test-unit.mk` requires adding the exclusion pattern to `ZE_GROUP_REST`
- (756) Web commit hook runs synchronously in HTTP handler; slow reload blocks the response (acceptable because reload is <100ms)
- (759) Archive pruning prefix computed by diff of two timestamps at different dates; if filename format has no time token, prefix equals full filename
- (768-a) Enum-over-string for text event pipeline: Event.Type stays string (non-BGP types), TypeKind caches the parsed EventKind; FamilyOperation.Action typed as RouteAction; local familyOperation types in format/rs/rr/persist are independent; NLRI index map[string] is Go-idiomatic and not improvable
- (768-b) Doctor health checks: unconditional prefix counting needs familyString early-return guard; AuditTables must guard nil LastApplied; VPP health check must gate on socket existence; health check kernel calls need 1s timeout goroutine; plugin-crash error must come after validation guards; EOR timer needs familyCount=0 guard; pending map needs cap
- (786) Backend-specific health checks belong in their backend plugin packages (RegisterHealthCheck pattern), not cmd/show/; block-init-register hook requires explicit registration, not direct health.Register in init()
- (790) Debug flags: three-tier resolution (global > per-subsystem > default) in zefs `state/debug/` keys; `storage.BlobStoreFrom()` exposes underlying BlobStore from Storage interface; `state/` is the new namespace for runtime state keys
- (797) Interop helpers must not bind peer IPs as default args (subnet chosen at setup time); BMP sidecar must start before Ze (races PeerUp); structured adj-rib-in must preserve legacy IPv4 NEXT_HOP from path attributes; BGP interop runner accepts only one scenario filter argument
- (801) YANG sub-containers do not work for positional-argument CLI commands: dispatcher prefix-match breaks when a `<name>` arg sits between the command and sub-keyword; route sub-commands inside the handler via arg inspection instead of separate wire methods
- (833) Commit script generation should be mechanical: use `scripts/dev/commit_helper.py` so session IDs, message files, ignored-path checks, executable scripts, and learned-summary decisions are enforced before the user runs the script
- (835) When the user asks for a commit, do not re-audit implementation or rerun gates. Use `scripts/dev/commit_helper.py` immediately, and if `scripts/dev/verify-status.sh check` is FRESH, never rerun `make ze-verify` or `make ze-verify-changed`
- (871) Thomas Owner Override lets the repository owner explicitly request commit-script preparation without a fresh verify run. It was added for OpenAI rigidity, not Anthropic. Agents still use `commit_helper.py`, never disable hooks, and report skipped verification plainly
- (874) Leaf-list nodes MUST use the multi-value Tree API in every write/apply path: serializers read multiValues, scalar Set is silently dropped. Session change-tracking is per-(path,member) via MetaEntry.Member; ordered member ops (insert/deactivate/activate) are structural ops so position is exact at commit. SSH session editors now wire SetReloadNotifier (commit = apply + propagate); doReload rewrites resolv.conf
- (890) Env-to-YANG promotion for BGP plugin config: create plugin yang/ module augmenting /bgp:bgp, add ConfigRoots+Features+YANG to registration, OnConfigure handler applies YANG value via SetChanSize, env var renamed to mirror YANG path per config-naming.md
- (917) SR-Policy migration + encoding: InProcessConfigRouteParser on plugin Registration replaces hardcoded family switch cases. PluginRoute carries pre-built NLRI + attribute bytes. fullRawAttribute must strip header for WriteAttrTo; ExaBGP TunnelEncap uses draft sub-TLV types. MPLS S-bit MUST be zero per RFC 9830; both Ze and ExaBGP were non-compliant. Bridge must pass tunnel-encap tokens through, not drop them.
- (1009) Spec selection moved from a shared `tmp/session/selected-spec` file (append/remove discipline, concurrency-fragile, dead commit gate) to per-session claims via `scripts/dev/spec-session.sh` into each session's own `.session-<SID>` marker; auto `ready->in-progress` transition moved into `claim`; helper must not use `set -u` (shared session-id.sh reads an unset env var)
- (1047) Offline-fallback for read-only daemon commands: `registry.RegisterOfflineFallback(path, handler)` serves host-local reads (show host, show crashes) in-process only when the daemon is unreachable, never shadowing the daemon. It is dead code unless `cmdutil.RunCommand` is patched to skip the "unknown command" rejection for fallback-registered paths (RunCommand rejects non-tree commands before cli.Run). Verb-first CLI cleanup: debug set/delete (VyOS-style, not invented enable/disable), event list -> show event list. Moving a YANG ze:command container changes its dispatch key (LoadBuiltins keys on YANG path, command.go:59), so a "verb-first rename" of a command a plugin sends by its bare path is a wire break -- grep for senders first. command list/help/complete -> show command list/help/complete (its one sender, plugin-cli-debug.ci, updated); plugin encoding/format/ack STAY noun-first (session directives; set plugin collides with the config-tree plugin node)
- (1078) BGP received-UPDATE filtering unified into ONE Stage-ordered pass: the external per-peer `PolicyFilterChain` becomes a single ordered step at new terminal `filterapi.FilterStagePeerChain=300`, which runs AFTER OTC/Annotation (the original spec had the order backwards -- code ran the whole in-process pass, incl OTC, before the external chain). `filterapi` stays a stdlib leaf, gaining only the stage constant + exported `LessOrder` comparator; the reactor binds the step at `startAPIServer`. Egress is order-only at `forwardUpdateCore`; the 3 single-kind egress paths (RS fast path, injected routes, default-originate) are intentionally left separate (unifying them would change behavior). Egress is asymmetric to ingress: egress in-process filters defer into `ModAccumulator` and the policy chain reads the ORIGINAL payload, so ingress/egress use separate step-result types
- (1082) Unified the `{status,data,error}` command-result envelope + dispatcher across every surface: `plugin.Response` wins in place; `api.ExecResult`/`CallerIdentity`/`Executor` and the five surface `CommandDispatcher` types become ALIASES of `plugin.CommandDispatcher`/`plugin.CallerIdentity` -- aliases keep the names so only the ~15 dispatcher CALL sites change, not the ~31 web signatures that merely thread it. Flatten centralized once in `plugin.ResponseJSON` + `CommandDispatcher.JSON`; the API engine returns the typed `*Response` directly (finding-3: drops the marshal-to-string then reparse round trip). New `plugin.Text` renders pre-rendered text verbatim on text surfaces (web BGP-decode) but encodes as a JSON string in the API. GOTCHAS: the envelope is MARSHAL-ONLY (`Data` is the `ResponseData` marker interface, so `json.Unmarshal` into `plugin.Response`/`api.ExecResult` fails when a `data` field is present; no production code unmarshals it). Removing the API re-parse changes REST/gRPC JSON key ORDER + number fidelity (int64 no longer coerced to float64) -- semantically equal and more faithful, NOT byte-identical; do not "restore" it by re-adding the round trip. `serverDispatcher` must NOT thread `context.Background()` as `RequestContext` -- the old adapter left it nil so `CommandContext.Context()` fell back to the shutdown-cancellable SERVER context; thread only a genuine per-request ctx (verify context-fallback claims against `CommandContext.Context()`, not intuition). `validate.py` floods with unwired-export false positives on a handler-heavy refactor (see 1081).
- (1081) Late-join replay unified onto ONE vocabulary in new leaf `internal/core/replay` (`Request{ReplayID}` token + `Broadcast=MaxUint64` sentinel + `IsReplay`); the token-correlated shape wins because broadcast is the special case where the token addresses everyone (a payload-less signal cannot target one consumer). `redistevents.ReplayRequest` becomes a `type = replay.Request` alias so producers/orchestrator churn zero. Two payload-less `RegisterSignal` broadcasts (bgp-rib, system-rib) become `Register[*replay.Request]`; two write-only `Replay bool` fields retire to a token-derived marker via a `type alias`+embedded-pointer `MarshalJSON`/`UnmarshalJSON` that preserves the legacy `replay` bool wire (best-change round-trips through JSON in-tree). `RegisterSignal`/`SignalEvent` now have zero users but stay as a primitive. GOTCHA: touching a widely-imported core leaf makes `changed-pkgs.sh` return ~200 reverse-dep pkgs, so `ze-lint-changed`/`ze-validate` scan that closure and surface PRE-EXISTING debt (a `goconst` already red on main; `validate.py` unwired-export false-positives -- it can't see `RunEngine: RunFoo` func-values, same-package-only use, or map-value type usage) in every file the change edits; budget for it. `json.Marshal` never dispatches a pointer-receiver `MarshalJSON` on nil (returns null), so the marshaler is nil-safe.
- (1094) Web/CLI UX finish: `helpfmt.RenderWriter` is a shared error-capturing render writer with return-less `Str`/`Line` methods (chosen over `fmt.Fprintln(rw,...)`, which errcheck flags on a custom writer -> ~250 nolints); CLI render paths now exit non-zero on a broken pipe. `zeweb.WebRoute{Pattern,Wrap,Build}` + optional `Enabled`/`Portal` registry replaces hardcoded l2tp/isis/ospf/gokrazy routes -- features self-register from `register_*.go`, the hub iterates (http.ServeMux panics on a leftover duplicate). i18n = catalog+English-fallback `Translate` wired as a `t` FuncMap helper (login page only). `.wb` harness gains `option=viewport/locale/auth` + `action=login` (unit-tested via the `installFakeAgentBrowser` command-log seam). GOTCHA: byte-identity of `ze help` must be checked by sorted-set diff (map-iteration non-determinism in `aihelp.Services`); gosec G101/G117 + misspell false-positive on translation keys and the French word "Connexion"; Chrome is env-blocked (`libatk`) so all `.wb` browser proofs are Go-tier
- [1099](plan/learned/1099-iface-resolve-0-umbrella.md) -- iface-resolve umbrella CLOSURE record: 7 planned sub-specs delivered as 5 units (949 model, 950 resolver+IS-IS, 951 mac/match, 952 consumers, 953 dispatch+guard); resolver API = `iface.Resolve/Addresses/Subscribe` (resolve.go:65/71/80); AC-U1 lives on as the standing `ze-iface-resolution-check` gate (run by verify_run.go stagesForMode BOTH branches, NOT the dead Makefile `_ze-verify-impl` list). GOTCHAS: the umbrella outlived its delivery (sat `ready` while all children were closed -- put umbrella closure on the last child's checklist); the planned AC-U5 test name (`iface-show-mapping.ci`) never existed, the evidence is `show interface ... detail` QEMU coverage in 949.
- [1098](plan/learned/1098-followup-vpp-iface.md) -- VPP iface backend tunnels/mirror/wireguard/LCP via GoVPP binapi. Mirror->SPAN maps ingress/egress to rx/tx/both `is_l2=false` (device SPAN), delete keyed on `(from,to,is_l2)` so the backend tracks entries per source. Wireguard: `wireguard_interface_create` takes the key in one message with no key-update, so `CreateWireguardDevice` is a no-op and Configure does the real create; peers reconcile ReplacePeers-style; PSK REJECTED (no field this API revision); plugin enabled via a `vpp.plugins.wireguard` toggle (mirrors `lcp.enabled`->plugin, honours `plugin default {disable}`). LCP: `SetupLCPPair` reads netns from `vppcomp.GetActiveLCPSettings` (keeps iface component free of a vpp dep), root markers->"" host netns, host name capped 15B + collision guard; wired in BOTH `applyConfig` Phase 1 AND `recreateManagedInterface` (review caught the recreate path). Doctor checks are the config-aware gate (health.go socket-absent-Healthy left as-is: no config context). Real VPP 25.10 evidence proves GRE+SPAN+wireguard; A-3 CONFIRMED. GOTCHAS: `ligato/vpp-base` ships `wireguard_plugin.so` but NOT `linux_cp_plugin.so`/`linux_nl_plugin.so`, and `lcp.enabled`+no-plugin FAILS the whole apply on any loopback (exact-or-reject; `doctor-vpp-lcp-netns` does not catch plugin absence); commit_helper deferral gate trips on spec/comment prose ("deferred to"/"out of scope"); `// test-relax:` token needs `//` even inside a `#`-comment `.ci`.
- [1097](plan/learned/1097-followup-vpp-traffic.md) -- VPP traffic `filter dscp` = POLICE-BY-DSCP (classify the DSCP/TOS bits and steer to the class policer, SAME pipeline as `filter protocol`, NOT a QoS remark -- record/map/mark only remarks, it cannot police by match); offsets IPv4 TOS byte 15 mask 0xFC, IPv6 traffic-class bytes 14/15 masks 0x0F/0xC0 (DSCP is not byte-aligned in IPv6). Multi-class steering groups per-interface per-family sessions by MASK: one table per distinct mask with a session per class (distinct `HitNextIndex`), distinct-mask tables chained via `ClassifyAddDelTable.NextTableIndex` (real VPP v25.10: `NextTbl` read-back, chain fall-through). `qdisc prio` + `mark` stay REJECTED (no VPP scheduler; no faithful SKB fwmark) with evidence-backed errors; multi-class requires EVERY class filtered; duplicate steering (same type+value) across classes rejected (sessions keyed by match -> silent last-wins). Builds on [1096](plan/learned/1096-followup-vpp-traffic-protocol.md). GOTCHAS: `list match{key type}` = one match per type per class; the traffic `.ci` suite is timing/stderr-capture sensitive on a loaded box (changed AND unchanged tests fail; rely on unit + real-VPP evidence, do not raise sleep baselines).
- [1095](plan/learned/1095-followup-subsystem.md) -- DoT (RFC 7858) + DoH (RFC 8484) extend the shared `dnsserver` harness (one `dns.Handler`, four transports: cleartext UDP/TCP, TLS-wrapped `dns.Server`, `net/http` + in-memory `dns.ResponseWriter`); listener signature folds a leaf-cert fingerprint so rotation rebinds, self-signed fallback cached to avoid reload churn; DoH allow-from denial -> HTTP 403. DNSSEC is the RFC 4035 STUB model (DO bit + CD=0, trust upstream SERVFAIL; strict rejects, permissive logs, AD=0 accepted), config in `system{dns{dnssec-validation}}` not a new resolver container. SLAAC = kernel-cooperating `IFA_F_*`->`AddrInfo.Origin` classification, NOT an RA client. GOTCHAS: a hand-written plugin `.ci` missing the `cmd=background:...ze-peer`/`cmd=foreground:...ze` trailer directives fails as a 45s TIMEOUT with "received messages: 0" (copy a known-good `.ci`, do not hand-assemble); observer event payloads are JSON strings, `json.Unmarshal` not type-assert; config parse does not materialize YANG defaults, Go must mirror them; scalar `leaf listen-port {type zt:port}` sidesteps the `listener_defaults.go` port-defaults lint; a kernel-timed IPv6 address is the flag-equivalent of SLAAC (test proxy, no radvd)
- (1085) Config leaf-list per-member deactivation moved from an in-band `inactive:` value prefix to an out-of-band `Tree.inactiveMembers` map; `GetSlice`/`GetMultiValues`/`ToMap` are now active-only (deactivated = not in effect), a new `GetMultiValuesState` serves serialize/diff/reactor. The reactor filter chain went `[]string` -> `filterapi.FilterRef{Name,Inactive}` end-to-end; `inactive:` survives ONLY at input (parse normalization), serialize output, and display/API seams (`FilterRefStrings`). GOTCHAS: the spec's premise that per-member deactivation reaches the reactor via `ToMap` was FALSE (it reaches it via `GetMultiValues`/`extractFilterChain`); A-1's "prefix never a valid input" was validated with a grep that missed Go-embedded config seeds AND `docs/guide/redistribution.md`, which documents the inline `[ inactive:X ]` form -- it is a real feature, normalized at the parse boundary (`stripInactiveMemberPrefix`), not an artifact to delete.
- [1104](plan/learned/1104-startup-resilience.md) -- Startup/config-apply resilience: an appliance must boot + commit config with all external services (RADIUS/TACACS/RPKI/BMP/NTP/hub) down. Audit found STARTUP already lazy everywhere (resolve-per-request OR detached-goroutine-with-bounded-dial); the residual risk is OnConfigApply callbacks, where the transaction coordinator's apply deadline makes a blocking apply a FAILED COMMIT. Fixes: NTP `doSync` gets stop-checks between serial `ntp.Query` calls (beevik `Query` has no context, so stop-checks not cancellation; keep the handoff synchronous for single-clock-writer, ApplyBudget 5->10); authradius `serverIPs` gets ONE shared `context.WithTimeout` (750ms) across all servers, not per-server (N x timeout overruns the 1s budget); managed hub gains a STATELESS config-tree doctor probe (`doctor-hub-unreachable`) not an in-process snapshot (`ze doctor` is a separate process; `DoctorCheckContext` has only the parsed config). New pattern of record: external clients use "detached goroutine + bounded dial + backoff + non-blocking event path" OR "resolve-per-request"; never dial in a constructor/apply callback. A doctor check that predicts daemon behavior must mirror the daemon's actual selection (probe `Clients[0]` only, the hub the daemon connects to). GOTCHAS: NTP `setClock` is build-tagged + privileged so seam it (`setClockFn`/`ntpQueryFn`) to unit-test sync; `ze doctor --json <config>` is static (deterministic AC-5); bare `go test` on `internal/component/doctor` needs the full `make` feature-gate tags.
- [1102](plan/learned/1102-followup-bgp-feature.md) -- BGP follow-up umbrella (GR metric lifecycle, spy-metric behavioral tests, raw default-originate guard, web decorators, Prometheus phase 6) worked item-per-commit. Keepers: RED-verify spy-metric tests by temporarily neutering the producer (count-only assertions pass vacuously); reject-at-runtime beats pre-encoding for the default-originate raw-filter gate; AS-Confederation OTC is vacuously RFC 9234 §5-correct for a single-AS speaker (role.go:66) -- re-deferred until confederation-member support exists; VPN ATTR_SET was an L3VPN feature mis-bundled into a GR row (split to spec-gr-advanced + future L3VPN spec); per-item commits never trigger spec closure -- spec-closure-check.py --list is what surfaces the completed-but-not-closed umbrella.
- [1111](plan/learned/1111-ownership-0-umbrella.md) -- ownership umbrella CLOSURE record (DESIGN-REVIEW finding #1): production ownership was ALREADY correct (hub owns Server/EventBus/Engine at `main.go:441/453/502`; BGP borrows via `register.go:144/149`, zero hub->bgp imports) -- the review read the reactor `api` field not the wiring (caller-vs-producer trap). Three real remnants delivered: P1 rs-invariant (`filterapi` capability seam `EnableRSForwarding`/`RSForwardingEnabled` gates the reactor RS fast path; delete plugin -> inert), P2 coordinator-types (typed Configure hooks + `BGPBootstrap` replace the `any` bag), P3 reactor-modes (explicit `Config.Standalone`, default borrow errors loudly). GOTCHAS: `DESIGN-REVIEW.md` was NEVER a tracked repo file (annotate-criterion unachievable; record lives in the 4 learned summaries); children carried the INVALID Status `done`, making `spec-closure-check.py` under-report them (high-confidence tier keys on `in-progress`); umbrella outlived delivery (children committed 07-04/05, closed only at the 07-13 truth-audit).
- [1112](plan/learned/1112-netlink-ci-harness.md) -- netlink `.ci` harness (Fix A daemon-readiness for background `ze`, Fix B per-test netns + setcap, Fix C `ze_api` EOF). Fix B: enter a per-test netns via `LockOSThread`+`netns.NewNamed` on the goroutine thread (children fork-inherit it, A-5), drop `ze` to a normal user via `SysProcAttr.Credential` off a `setcap`'d binary (A-4: root breaks readiness, written after `dropPrivileges`); the R-2 gate `refuseHostNetnsFirewall` runs first in nft `(*backend).Apply` and refuses (fails closed) when `ZE_TEST_NETNS_HOST` is set but the process is in the host netns (test-only env, prod untouched). GOTCHAS: netns teardown must be ONE `t.Cleanup` in order (a split `defer Close()`/`UnlockOSThread()` runs first -> `netns.Set` on a closed fd / migrated thread); `setcap` fails on the 9p mount (`security_model=none`, no xattr) so copy to a world-traversable tmpfs first (the uid-dropped `ze` execs it); chown BOTH the tmpfs dir and the tmpfs-less `ze-config-*` dir to the target uid; `ConfigDirFromBinary` needs a `bin/` layout or `ze cli` can't find the daemon (pin `ze.config.dir`); attribute a suspected netns regression by reproducing it with `ze` as ROOT (policy `nftables flush: operation not supported` reproduced as root = Alpine kernel gap, not Fix B; copp `backend not loaded` = config lacks a `firewall{backend nft}` block); nft 1.1.1 renders `tcp dport`->`th dport`, `ip saddr @set`->`@nh,96,32 @set`, `icmp type 8`->`echo-request` (make `.ci` asserts version-robust with `expect=stdout:pattern=` alternations).
- [1165](plan/learned/1165-fixit-vpp-lcp-netns-remediation.md) -- `ze doctor` told operators to break their own dataplane: `doctor-vpp-lcp-netns` recommended `vpp.lcp.netns host|root`, but to VPP those are ordinary namespace NAMES (`lcp_set_default_ns` formats `/var/run/netns/%s` and opens it, `third_party/vpp-linux-cp/src/lcp.c:73-74`), so following the advice fails LCP pair creation. VPP's netns model is TWO-LEVEL: an EMPTY per-pair netns falls back to the GLOBAL default (`lcp_itf_pair_create`, `lcp_interface.c:850-855`), which ze itself always writes from the SAME leaf (`startupconf.go:106`) -- so `""` means "VPP's own namespace" only when the default is unset, which ze never leaves unset. Detection is CORRECT (`RealListenerFactory.Listen` `network.go:167` is a bare `net.ListenConfig`, BGP has no netns awareness); there is NO config-only remedy, only deployment placement (run ze where the TAPs are) until `plan/spec-bgp-netns.md` lands. HOW IT SURVIVED: the false claim had FOUR assertion sites (message, 2 doc comments, `codes.go` registry Description) and ZERO verification sites -- evidence base was Go comments plus a generated binapi stub (`lcp.ba.go:354` "optional tap netns", which cannot express a resolution rule); both `ai/rules/no-fabrication.md` banned patterns fired at once. Five unit tests covered the check and all five asserted only COUNT+CODE -- a test proving a check FIRES says nothing about whether its advice is SURVIVABLE (rule candidate: `ai/rules/doctor-checks.md` could require asserting the REMEDIATION; sibling `doctor-vpp-wireguard` is equally unasserted). GOTCHAS: a citation into a scratch fetch ROTS when the source is vendored -- comments cited `lcp_interface.c:856-861`, correct against the git-ignored `tmp/vpp-lcp/` copy, while the same commit vendored a DIFFERENT copy to `third_party/` where that range is the sub-interface block (the vendoring pass substituted the PATH and left the LINE NUMBERS; tell was 3 unreflowed ~125-char comment lines); the same pass credited `lcp_get_default_ns` with formatting the path (it only returns the stored name). Assert diagnostic text by REGEXP over banned/required properties, never golden strings (`spec-bgp-netns` AC-3 will rewrite the message). STILL LIVE: the check is SILENT for `netns host`/`root` (detection gap, `plan/deferrals.md` 2026-07-16 -> `plan/spec-fixit-vpp-lcp-reachability.md`); `test/ui/doctor-vpp-lcp-netns.ci` absent (AC-12 there). <!-- doc-links: ignore (this .ci is intentionally absent per the summary; AC-12 deferred) -->
- [1169](plan/learned/1169-cli-root-namespace-grammar.md) -- CLI root-namespace grammar: the `cli_grammar` gate enforced R1-R9 only over the YANG tree, never over `MustRegisterRootHandler` roots, so the root namespace drifted (`ze format` = carrier named after one operator KIND; `traffic-control`/`isis-decode`/`ospf-decode`/`update-serve` = two-token namespaces hyphenated). Renamed carrier->`ze pipe` (`pipe` = the in-tree name for the operator set); split the three tools into `traffic`/`isis`/`ospf` ROOT + closed-keyword sub-dispatch (`control`/`decode`); `update serve` became a LOCAL handler (`update` is a YANG verb -> a root named `update` is dead behind isYANGVerb, so use the `show version` local-meta path). NEW 4th grammar feeder `grammar.CheckRootNamespace(roots, namespaces)` = CROSS-SURFACE R9 (`CheckSiblings` can't see roots; no `traffic` root existed to be `traffic-control`'s sibling, which is why the gate stayed green over 4 violations); the gate AST-scans roots + `container` names; fixture red/green lives in the `grammar` pkg because the gate is `//go:build ignore` (untestable in-package). GOTCHAS: freeing `traffic`/`isis`/`ospf` COLLIDED with the `ze-test <suite>` roots (`internal/test/cli` imports plugin/all) -> `duplicate root command` panic, fixed with `// codegen:skip` on the 3 `cli/register.go` (tools reach ze via DIRECT dispatch imports, not plugin/all, matching firewall/iface/l2tp); `ze update serve` blocks so AC-8 `.ci` = foreground shell-wrapper + `http=GET` (http=wait alone isn't self-validating -> BGP peer-exchange path); test/ui `.ci` contains-checks are CUMULATIVE per file so `!contains` negatives need an isolated file; `traffic-control` also = config identity (stays), `isis-decode`/`ospf-decode` = pure command (renamed everywhere incl ~10 `.ci` exec lines); `validate.py` didn't treat `scripts/` gates as callers (grammar helpers read as dead -- fixed).
- [1225](plan/learned/1225-rfc7606-relay-shape.md) -- RFC 7606 Section 5.1 second bullet closed on the RELAY path (origination and both splitters were already compliant; `buildFwdBody` emitted an UPDATE whole whenever it FIT, in whatever shape it arrived). ONE definition of "mixed" (`message.NLRIBearingFieldCount` over the three wire sections) shared by the parsed and lazy-wire paths; TWO entry points on purpose (`SplitWireUpdate`'s fast path changed in place since the relay is its only caller, but `Splitter.Split` keeps its size-only fast path and `SplitCompliant` is the relay's, because origination is compliant by construction and must not pay an attribute walk per send); the verdict is CACHED on the WireUpdate because the forward loop runs once per destination over the same pointer (3.3ns vs 51.7ns, both allocation-free). MISTAKE (2nd time in 2 specs, cf 1224): a test asserting the ABSENCE of something proved nothing -- `AllocsPerRun` == 0 passed with the cache deleted, since neither side allocates; ask what would still be absent if the mechanism were removed. Corollary: a fixture at the extreme (all 4 fields) cannot pin a `> 1` boundary -- boundary tests belong in the package owning the comparison. TEST-HARNESS: `Checker.check` matches an arriving message against EVERY pending rule for the current connection (checker.go:400-418), so a `.ci` cannot assert ORDERING, and declaring an EOR expectation opts INTO the initial-sync race rather than out of it (unmatched KEEPALIVE/EOR are silently accepted); `bgp-rs-reactor-fastpath.ci` fails ~1/6 on an unmodified tree for that reason -- do not inherit its `rs-fast-path enable` if the test does not need it (75% -> 20/20 stable without). INTEROP NOT DELIVERED (parked, `tmp/parked-interop/`) but the attempt paid for itself: it found ze's route-server delta replay emitting NEXT_HOP TWICE (FRR: "attribute type 3 appears twice ... discard attribute", 15 UPDATEs received, ZERO prefixes installed, no NOTIFICATION, `Inbound filtered: 0`) and the type-253 attribute-discard marker going out on the wire -- BOTH reproduce with this change reverted. The plugin `.ci` suite cannot see this class: `ze-peer` asserts only the bytes it was told to expect, a real daemon applies RFC 7606 to what it receives.
