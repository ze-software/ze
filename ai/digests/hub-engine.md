# Hub / engine / coordinator: current flow digest

<!-- LIVING digest: the current shape of this subsystem. Update it when you change the subsystem. -->
<!-- Generated by tracing the code; verify file:line before relying on a detail. -->
<!-- digest-base: internal/component/hub internal/component/engine internal/component/managed -->

## What it is
The protocol-agnostic wiring that boots the daemon and connects everything else: process bootstrap (`cmd/ze/hub`), the `Engine` supervisor that starts/stops/reloads registered `ze.Subsystem`s in order, the `Coordinator` (a protocol-agnostic facade "created by the hub at startup" that lets any reactor register itself and defer work until every plugin has finished starting), and the managed/fleet client that lets an edge instance pull its config from a remote hub. Two different "subsystem" mechanisms coexist in the real code: `ze.Subsystem` + `Engine.RegisterSubsystem` (used today only by L2TP and PPPoE) and the older Coordinator/`ReactorLifecycle` path (used by BGP, loaded as a config-path plugin through the plugin server, not through the Engine). This digest covers bootstrap and coordination; the per-message engine<->plugin wire protocol (5-stage handshake, EventBus, DirectBridge) is `ai/digests/plugin-transport.md`.

## Flow
1. **Entry and config-type probe.** `ze start` (`cmd/ze/ze_core_start.go` `cmdStart`) resolves blob storage, detects managed mode via `isManaged` (`cmd/ze/ze_core_start.go`), and calls `hub.Run` / `hub.RunWithManagedClient` (`cmd/ze/hub/main.go`) which both call `run()` (`cmd/ze/hub/main.go`). `run` reads the config bytes then `zeconfig.ProbeConfigType` (`internal/component/config/probe.go`) dispatches: `ConfigTypeBGP`/`ConfigTypeUnknown` -> `runYANGConfig` (`cmd/ze/hub/main.go`), the path every real BGP deployment takes; `ConfigTypeHub` -> `runOrchestratorWithData` (`cmd/ze/hub/main.go`), the separate multi-process Orchestrator (step 13). BGP always wins when both a top-level `bgp{}` and `plugin{}` block are present (`internal/component/config/probe.go`), so the Orchestrator path is reached only by pure-plugin configs.
2. **Coordinator creation.** Inside `runYANGConfig`, after config parse/evolution/PKI setup, `coordinator := zePlugin.NewCoordinator(configTree)` (`cmd/ze/hub/main.go`) builds the `*plugin.Coordinator` (`internal/component/plugin/coordinator.go`), documented as "Created by the hub at startup" (`internal/component/plugin/coordinator.go`). `coordinator.SetBootstrap(registry.BGPBootstrap{...})` (`cmd/ze/hub/main.go`) stashes config-load state (config path, CLI plugins, chaos flags, store, health/MRT callbacks) the BGP reactor factory reads back later, avoiding an import cycle between the hub and BGP.
3. **Server + Engine construction.** `pm := pluginmgr.NewManager()` (`cmd/ze/hub/main.go`, `internal/component/plugin/manager/manager.go`) implements `ze.PluginManager` (`pkg/ze/plugin.go`). `apiServer, _ := pluginserver.NewServer(serverConfig, coordinator)` (`cmd/ze/hub/main.go`) hands the Coordinator to the plugin server as its `plugin.ReactorLifecycle` (`internal/component/plugin/server/server.go`). `apiServer.SetProcessSpawner(pm)` (`cmd/ze/hub/main.go`) and `registry.SetEventBus(apiServer)` (`cmd/ze/hub/main.go`) make the plugin server double as the `ze.EventBus` (`pkg/ze/eventbus.go`); there is no separate bus. `eng := engine.NewEngine(apiServer, configProvider, pm)` (`cmd/ze/hub/main.go`, `internal/component/engine/engine.go`) builds the `ze.Engine` (`pkg/ze/engine.go`) supervisor.
4. **Subsystem registration (Engine-owned, protocol-agnostic).** Anything implementing `ze.Subsystem` (`pkg/ze/subsystem.go`) registers with the Engine before `Start`: L2TP (`eng.RegisterSubsystem(l2tp.NewSubsystem(...))`, `cmd/ze/hub/main.go`, `internal/component/l2tp/subsystem.go`) and PPPoE (`cmd/ze/hub/main.go`, `internal/component/l2tp/pppoe/subsystem.go`), each gated on its config block being present. `Engine.RegisterSubsystem` (`internal/component/engine/engine.go`) rejects duplicate names and rejects any call after `Start`. BGP is deliberately NOT registered this way; see Invariants.
5. **Engine.Start: subsystems only, no plugin process yet.** `eng.Start(startCtx)` (`cmd/ze/hub/main.go`, `engine.go`) calls `e.plugins.StartAll(ctx, e.eventBus, e.config)` (`engine.go`), which for `pluginmgr.Manager` only marks itself started and stores ctx/eventBus/config (`internal/component/plugin/manager/manager.go`); no OS process is forked yet. It then starts every registered subsystem in registration order (`engine.go`), stopping already-started subsystems in reverse and calling `plugins.StopAll` on the first failure (`engine.go`).
6. **Plugin server start: the real fork happens here, after the Engine.** `apiServer.StartWithContext(startCtx)` (`cmd/ze/hub/main.go`, `internal/component/plugin/server/server.go`) launches `runPluginStartup` (`internal/component/plugin/server/startup.go`) in a goroutine: five ordered phases, config-path plugins (bgp, iface, fib, ...) first, then explicit `plugin{external}` plugins, then auto-load by family/event/send-type, each phase calling `pm.SpawnMore` (`internal/component/plugin/manager/manager.go`) -> `spawnProcesses` (`internal/component/plugin/manager/manager.go`) and the 5-stage handshake (owned by `plugin-transport.md`). Because config-path plugins run in phase 1, BGP's reactor exists before anything that depends on it.
7. **BGP registers into the Coordinator during its own config callback.** The BGP plugin's `OnConfigure` handler (`internal/component/bgp/plugin/register.go`) type-asserts the server's reactor to `registry.CoordinatorAccessor` (`internal/component/plugin/registry/interfaces.go`), builds the reactor via the registered factory, and calls `coord.SetReactor(bgpReactor.ReactorLifecycleAdapter())` (`internal/component/bgp/plugin/register.go`, `Coordinator.SetReactor` `internal/component/plugin/coordinator.go`), the only reactor the Coordinator's named map holds in practice (`RegisterReactor`, `internal/component/plugin/coordinator.go`, is otherwise unused today). It starts the reactor's listeners immediately (`internal/component/bgp/plugin/register.go`) but defers peer/session startup by registering `coord.OnPostStartup(func() { bgpReactor.StartPeers() })` (`internal/component/bgp/plugin/register.go`, `Coordinator.OnPostStartup` `internal/component/plugin/coordinator.go`).
8. **Startup barrier -> deferred peer start.** When `runPluginStartup` finishes all five phases it calls `signalStartupComplete` (`internal/component/plugin/server/startup.go`), which freezes the schema/subsystem/command registries for lock-free dispatch, fans a post-startup callback out to every plugin (`sendPostStartupToAll`, `internal/component/plugin/server/startup.go`), then calls `s.reactor.SignalPluginStartupComplete()` (`internal/component/plugin/server/startup.go`). The Coordinator (`internal/component/plugin/coordinator.go`) forwards to the BGP reactor and invokes its single stored `postStartup` closure (`internal/component/plugin/coordinator.go`), the step-7 hook: peers start only once every plugin phase has finished, protocol-agnostically. Back in `main.go`, `apiServer.WaitForStartupComplete(startupCtx)` (`cmd/ze/hub/main.go`, `internal/component/plugin/server/startup.go`) blocks until this barrier fires before the `ze.ready.file` is written (`cmd/ze/hub/main.go`).
9. **Managed client and run loop.** Only for blob-backed managed instances, `go managed.RunManagedClient(managedCtx, *managedClient)` (`cmd/ze/hub/main.go`, `internal/component/managed/client.go`) starts the fleet client goroutine (step 11). `waitLoop` (`cmd/ze/hub/main.go`) then blocks on SIGINT/SIGTERM/SIGHUP or plugin-server exit (`waitForServerDone`, `cmd/ze/hub/main.go`).
10. **Reload: SIGHUP or managed push both drive `doReload`.** A SIGHUP is read by `handleSIGHUPReload` (`cmd/ze/hub/main.go`, defined `cmd/ze/hub/main_reload.go`); a managed config push instead calls `reloadAfterCommit` (`cmd/ze/hub/main.go`) via the `OnCommit` hook wired in `wireManagedCommit` (`cmd/ze/hub/managed.go`). Both funnel into `doReload` (`cmd/ze/hub/main_reload.go`): load the config tree once, `s.ReloadConfig(ctx, newTree)` (plugin-server diff/apply, owned by `plugin-transport.md`), refresh the shared `zeconfig.Provider` root-by-root (`applyLoadedTreeToProvider`, `cmd/ze/hub/main_reload.go`), then `eng.Reload(reloadCtx)` (`engine.go`) fans out to every registered `Subsystem.Reload` (`pkg/ze/subsystem.go`). Any failure after the plugin-server step triggers `rollbackReload` (`cmd/ze/hub/main_reload.go`), replaying the prior tree through the plugin server and restoring the ConfigProvider snapshot.
11. **Managed client connection loop.** `RunManagedClient` (`internal/component/managed/client.go`) loops `runConnection` (`client.go`) with `Backoff` (`internal/component/managed/reconnect.go`, reset on successful auth, `client.go`): TLS-dial the hub, auth, `fetchAndProcess` (`client.go`) which calls `Handler.ValidateConfig` (`internal/component/managed/handler.go`) and, on success, the `ClientConfig.OnCommit` closure (the same `reloadAfterCommit` from step 10), then starts a `Heartbeat` (`internal/component/managed/heartbeat.go`, `NewHeartbeat` `:29`) and blocks in `notificationLoop` (`client.go`) handling hub-pushed `config-changed`/`ping` RPCs (`handleHubRequest`, `client.go`) until disconnect, heartbeat timeout, or context cancel.
12. **Shutdown: reverse of startup, plugin server first.** On signal, `main.go` calls `apiServer.Stop()` (`cmd/ze/hub/main.go`) before `eng.Stop(stopCtx)` (`cmd/ze/hub/main.go`, `engine.go`), which stops every subsystem in reverse registration order (`engine.go`) and finally `plugins.StopAll` (`engine.go`, `internal/component/plugin/manager/manager.go`), tearing down the shared `ProcessManager` and TLS acceptor.
13. **Alternate path: the multi-process Orchestrator.** `runOrchestratorWithData` (`cmd/ze/hub/main.go`) parses via `hub.ParseHubConfig` (`internal/component/hub/config.go`), builds `hub.NewOrchestrator(cfg)` (`internal/component/hub/hub.go`), composing a `pluginserver.SubsystemManager` + `SchemaRegistry` + `Hub` router (`hub.go`) that forks each configured plugin as a separate `ze <name> --child` process instead of an in-process DirectBridge/goroutine plugin. `Orchestrator.Start` (`hub.go`) forks all, registers schemas, then `Freeze()`s both registries (`hub.go`). Its own SIGHUP handler calls `Orchestrator.Reload` (`internal/component/hub/reload.go`), which diffs plugin definitions (`diffPluginDefs`, `reload.go`), starts added/changed plugins before stopping removed ones, and rolls back on any start failure (`reload.go`). This path never touches `engine.Engine`, `ze.Subsystem`, or the Coordinator.

## Key files
| File | Role |
|------|------|
| `internal/component/hub/hub.go` | `Orchestrator`: composes `SubsystemManager`/`SchemaRegistry`/`Hub` router for the fork-based multi-process path |
| `internal/component/hub/config.go` | `ParseHubConfig`: 3-section tokenizer (`env{}`, `plugin{}`, remaining blocks stored for routing) |
| `internal/component/hub/reload.go` | `Orchestrator.Reload`: plugin-def diff, start-before-stop ordering, rollback on failure |
| `internal/component/hub/schema.go` | `ConfigStore` (VyOS-style live/edit state) and `SchemasByPriority`, used by the Orchestrator path only |
| `internal/component/hub/router.go` | `matchEventPattern`: wildcard event-pattern matching helper for the Orchestrator |
| `internal/component/engine/engine.go` | `Engine`: `RegisterSubsystem`, ordered `Start`/`Stop`/`Reload` over `ze.Subsystem`s |
| `internal/component/managed/client.go` | `RunManagedClient`/`runConnection`: TLS connect, auth, config fetch, heartbeat, notification loop |
| `internal/component/managed/handler.go` | `Handler.ValidateConfig`: decode + caller-supplied validation of hub-pushed config |
| `internal/component/managed/heartbeat.go` | `Heartbeat`: missed-pong liveness timer feeding the reconnect loop |
| `internal/component/managed/reconnect.go` | `Backoff`: exponential reconnect delay with jitter |
| `cmd/ze/hub/main.go` | Real bootstrap entry point: Coordinator + Engine + plugin server wiring, run loop, shutdown |
| `cmd/ze/hub/main_reload.go` | `doReload`/`handleSIGHUPReload`: single-tree-snapshot reload with rollback |
| `cmd/ze/hub/managed.go` | `wireManagedCommit`: bridges a managed config push into the same reload path as SIGHUP |
| `cmd/ze/ze_core_start.go` | `ze start` CLI: managed-mode detection, dispatch to `hub.Run`/`hub.RunWithManagedClient` |

## Invariants & gotchas
- **Two coexisting "subsystem" mechanisms.** `ze.Subsystem` + `Engine.RegisterSubsystem` (`pkg/ze/subsystem.go`, `engine.go`) is used today only by L2TP and PPPoE. BGP (and iface/fib/etc.) load as config-path plugins through the plugin server and register into the Coordinator instead (step 7); despite `plan/learned/425-arch-0-system-boundaries.md` saying "the BGP daemon is a subsystem," it does not implement `ze.Subsystem` or call `Engine.RegisterSubsystem` in the current code.
- **Engine.Start finishes before any plugin process exists.** `Manager.StartAll` (called from `engine.go`) is a Phase-1 no-op (`internal/component/plugin/manager/manager.go`); the real fork happens later and separately via `apiServer.StartWithContext` (`cmd/ze/hub/main.go`). L2TP/PPPoE are fully started before BGP even begins its 5-stage handshake.
- **`Coordinator.postStartup` is a single closure, not a list** (`internal/component/plugin/coordinator.go`). Only BGP calls `OnPostStartup` today (`internal/component/bgp/plugin/register.go`); a second protocol reactor calling it would silently overwrite BGP's deferred-peer-start hook. `RegisterReactor`'s named-reactor map (`internal/component/plugin/coordinator.go`) is likewise unused beyond the single `"bgp"` entry set by `SetReactor`.
- **`ConfigTypeBGP` always beats `ConfigTypeHub`** when both a `bgp{}` and `plugin{}` block are top-level (`internal/component/config/probe.go`), so the `internal/component/hub` Orchestrator is reached only by pure-plugin configs; it has its own integration test (`test/hub/startup_test.go`) but is effectively unreachable for any real BGP daemon started via `ze start`.
- **Docs describe an aspirational, not-yet-collapsed design.** `docs/architecture/hub-architecture.md` is explicitly headed "Design aspiration (partially implemented)"; `docs/architecture/subsystem-wiring.md` and `docs/architecture/plugin-manager-wiring.md` both describe an intermediate "BGPSubsystem wraps Server" shape that the live code does not have (`Server`/`apiServer` is created directly in `cmd/ze/hub/main.go`, before any reactor exists). Both docs point back at `cmd/ze/hub/main.go` as authoritative; trust the code over the diagrams.
- **The fleet hub server side is unwired.** `internal/component/managed` is the client only. Its hub-side counterpart, `ManagedConfigService` (`internal/component/plugin/server/managed.go`), implements `HandleConfigFetch`/`BuildConfigChanged` and is exercised only by its own unit tests; nothing in production code constructs it or wires it into the plugin server's TLS acceptor/dispatcher, so no `ze` instance can currently serve config to a managed client despite the client side being fully wired into `cmd/ze/hub/main.go`.
- **Managed pushes and SIGHUP share one commit path.** `wireManagedCommit`'s `OnCommit` (`cmd/ze/hub/managed.go`) writes a candidate version then calls the exact same `reloadAfterCommit` closure SIGHUP/API/SSH commits use (`cmd/ze/hub/main.go`), so a managed push gets the same validate/diff/apply/rollback guarantees as any other commit.
- **Reload rollback covers the plugin server and ConfigProvider, not subsystems individually.** `rollbackReload` (`cmd/ze/hub/main_reload.go`) replays the prior tree through `s.ReloadConfig` and restores the ConfigProvider snapshot, then calls `eng.Reload` again with the restored provider; it does not separately undo whatever partial state a subsystem's own `Reload` mutated before the failure.
- **Orchestrator reload starts new/changed plugins before stopping old ones** (`internal/component/hub/reload.go`), the same "never leave nothing running" principle as `Engine.Start`'s failure rollback, but implemented independently (the Orchestrator never calls into `engine.Engine`).

## See also
- `docs/architecture/hub-architecture.md`, aspirational multi-process Hub design behind the Orchestrator path; explicitly marked partially implemented
- `docs/architecture/subsystem-wiring.md`, Engine/Bus/Subsystem migration history; its own header says the live code in `cmd/ze/hub/main.go` is authoritative, not the diagrams
- `docs/architecture/plugin-manager-wiring.md`, two-phase PluginManager startup (process spawn vs protocol handshake) referenced in steps 5-6
- `docs/architecture/fleet-config.md`, managed/fleet client-hub config protocol design ("hub infrastructure is universal")
- `plan/learned/425-arch-0-system-boundaries.md`, why Engine/ConfigProvider/PluginManager/Subsystem are 4 separate interfaces in `pkg/ze/`
- `ai/digests/plugin-transport.md`, the per-message engine<->plugin path (5-stage protocol, EventBus, DirectBridge) that this digest's Coordinator and plugin-server references delegate to
- `ai/rules/architecture.md`, one-page component map and boundaries
