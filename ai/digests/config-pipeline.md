# Config pipeline (YANG): current flow digest

<!-- LIVING digest: the current shape of this subsystem. Update it when you change the subsystem. -->
<!-- Generated by tracing the code; verify file:line before relying on a detail. -->
<!-- digest-base: internal/component/bgp/config internal/component/bgp/reactor internal/component/config -->

## What it is
The path that turns a text config file into live BGP peers. A YANG schema drives a
tokenizer + hierarchical parser into a generic `*config.Tree`; the BGP layer resolves
peer-group inheritance into a flat `map[string]any` and parses that into
`[]*reactor.PeerSettings`, which the reactor adds as peers. The same pipeline re-runs on
SIGHUP to reconcile the delta.

## Flow
1. **File → Tree.** `config.LoadConfig` (`internal/component/config/loader.go`) → `ParseTreeWithYANG` → `parseTreeWithYANG` (`internal/component/config/loader.go,89`) loads the YANG schema (`config.YANGSchema`, `internal/component/config/yang_schema.go`), picks a format with `DetectFormat` (`internal/component/config/serialize_set.go`), then for hierarchical input runs `NewParser(schema).Parse(input)` (`internal/component/config/parser.go,34`). `Parse` builds a `tokenizer` (`internal/component/config/tokenizer.go`; auto-semicolon insertion at `tokenizer.go`) and `parseRoot` (`parser.go`) walks tokens, validating each keyword against the schema (`p.schema.Get`, rejects unknown at `parser.go`). Set-style input goes through `parseSetWithMigration` → `NewSetParser` instead (`internal/component/config/loader.go`).
2. **Prune + env.** `PruneInactive(tree, schema)` strips `inactive:` subtrees (`internal/component/config/loader.go`, `internal/component/config/prune.go`); environment values are extracted and applied. `LoadConfig` returns `LoadConfigResult{Tree, Plugins, ConfigDir}` (`internal/component/config/loader.go,67`).
3. **Reactor build entry.** `LoadReactorWithPlugins` / `LoadReactorFile*` (`internal/component/bgp/config/loader.go,148`) → `CreateReactor` (`internal/component/bgp/config/loader.go`) → `CreateReactorFromTree` (`internal/component/bgp/config/loader_create.go`).
4. **Tree → map → PeerSettings.** `CreateReactorFromTree` calls `PeersFromConfigTree(tree)` (`loader_create.go`, `internal/component/bgp/config/peers.go`), which: prunes inactive again (`peers.go`); `ResolveBGPTree(tree)` → `map[string]any` resolving 3-layer bgp→group→peer inheritance and flattening every peer into a `peer` map keyed by **peer name** (`peers.go`, `internal/component/bgp/config/resolve.go`); `CheckRequiredFields` (warns only, `peers.go`, `resolve.go`); `applyPeerSchemaDefaults` applies YANG defaults per peer (`peers.go,399`); then `reactor.PeersFromTree(bgpTree)` parses each peer map into `*reactor.PeerSettings` via `parsePeerFromTree` (`peers.go`, `internal/component/bgp/reactor/config.go`). Routes, filter chains, loop-detection, and port overrides are patched onto the settings from the `*Tree` afterward (`peers.go`).
5. **PeerSettings → live peers.** Back in `CreateReactorFromTree`: assemble `reactor.Config`, `reactor.New(cfg)` (`loader_create.go`), then `r.AddPeer(ps)` per peer (`loader_create.go`) and `r.SetDynamicGroups(...)` (`loader_create.go`). `CreateReactor` also wires `r.SetConfigPath` + `r.SetReloadFunc(createReloadFunc(...))` (`internal/component/bgp/config/loader.go`).
6. **Reload (SIGHUP).** `SignalHandler` registers SIGHUP (`internal/component/bgp/reactor/signal.go`); `OnReload` is wired at `internal/component/bgp/reactor/reactor.go`. If a coordinator config loader exists it uses the transactional `api.ReloadFromDisk` (verify/apply); otherwise `adapter.Reload()` (`internal/component/bgp/reactor/reactor_api.go`) calls `reloadFn` = `createReloadFunc` (`loader_create.go`): re-read file (blob store, FS fallback), re-parse, `PeersFromConfigTree`, refresh dynamic groups, then `reconcilePeers(newPeers, "reload")` (`reactor_api.go`) diffs by peer (add new, remove missing, remove+re-add changed) under a rollback journal.

## Key files
| File | Role |
|------|------|
| `internal/component/config/tokenizer.go` | Lexer; value tokens → auto-inserted `;` on newline/EOF |
| `internal/component/config/parser.go` | Hierarchical parser; schema-driven, rejects unknown keywords → `*Tree` |
| `internal/component/config/tree.go` | Generic `*Tree` (containers/lists/leaves) + `ToMap()` flattening |
| `internal/component/config/loader.go` | `LoadConfig`, `ParseTreeWithYANG`, format detect, plugin extraction |
| `internal/component/config/prune.go` | `PruneInactive`, drop `inactive:` subtrees before use |
| `internal/component/config/yang_schema.go` | `YANGSchema()`, loads/compiles the YANG-derived schema |
| `internal/component/config/schema_defaults.go` | `ApplyDefaults`, YANG defaults are the single source of truth |
| `internal/component/bgp/config/resolve.go` | `ResolveBGPTree`, bgp→group→peer deep-merge, flatten to peer map |
| `internal/component/bgp/config/peers.go` | `PeersFromConfigTree`, orchestrates resolve→defaults→PeersFromTree→routes/filters |
| `internal/component/bgp/config/loader.go` | `LoadReactor*` entry points; `SetReloadFunc` wiring |
| `internal/component/bgp/config/loader_create.go` | `CreateReactorFromTree` (AddPeer) + `createReloadFunc` |
| `internal/component/bgp/config/plugins.go` | Inline plugin extraction from peer `process` bindings |
| `internal/component/bgp/reactor/config.go` | `PeersFromTree` / `parsePeerFromTree`, map → `PeerSettings` |
| `internal/component/bgp/reactor/reactor_api.go` | `Reload` + `reconcilePeers` (journaled peer delta) |
| `internal/component/bgp/reactor/signal.go` | SIGHUP/SIGTERM/SIGUSR1 handler; SIGHUP → reload |

## Invariants & gotchas
- **Schema-driven, fail-closed parse.** Unknown top-level keyword (`parser.go`) or unknown field (`parser.go`) is a hard error unless the container carries `ze:allow-unknown-fields` (`parser.go`). A **new leaf must be added to the YANG schema first** or it will not tokenize into the tree at all.
- **YANG presence ≠ wired behavior.** To reach `PeerSettings`, a new leaf must ALSO be read in `parsePeerFromTree` (`reactor/config.go`) or the route/filter extractors in `peers.go`. Adding it to YANG alone parses it into the tree/map but has no runtime effect.
- **Walker does NOT enforce choice/case.** `mandatory true` and inner-`choice` exclusivity are not enforced by the parser walker (see `ai/rules/repo-maintenance.md`); plugins using `choice` add Go-side validation in their own parser. `ze config validate` does not invoke `OnConfigVerify`.
- **Inheritance flattening.** `ResolveBGPTree` produces a flat `peer` map keyed by **peer name** (not IP), deep-merging bgp→group→peer in precedence order (`resolve.go`). Leaf-lists in `cumulativePaths` (filter ingress/egress community tag/strip, from `ze:cumulative`) **accumulate** across levels instead of the most-specific level replacing (`resolve.go,509`). `group-name` is injected into each resolved peer for `PeerSettings.GroupName`.
- **The tree is mutated in place.** `PeersFromConfigTree` prunes inactive nodes on the passed tree (`peers.go` doc); callers needing the original must `tree.Clone()` first.
- **Partial config boots.** `CheckRequiredFields` only warns; peers that fail `parsePeerFromTree` with `ErrIncompleteConfig` are skipped (`reactor/config.go`), so the daemon can start for editing. Hard validation errors (bad router-id, duplicate remote IP, connect+accept both false, GR/route-refresh without a send-update process) still fail the load.
- **Defaults are YANG, not Go constants.** RFC defaults (hold-time, connect-retry, port) come from `applyPeerSchemaDefaults` → `config.ApplyDefaults` (`peers.go`), not from `NewPeerSettings`.
- **Routes/filters read the `*Tree`, not the map.** They need config-internal types (`StaticRouteConfig`, `ParseRouteAttributes`) that `reactor` cannot import, so `peers.go` extracts them directly from the tree and accumulates bgp+group+peer layers (`peers.go`).
- **`ToMap` scalar/slice ambiguity.** A single-element multi-value collapses to a `string`; multiple elements stay `[]string` (`tree.go`). Map consumers must handle both (`deepMergeAt`/`toAnySlice` and the reactor parsers do).
- **Reload delta = per-peer reconcile.** `reconcilePeers` adds new, removes missing, and **remove+re-adds changed** peers (session bounce) under a rollback journal (`reactor_api.go`). Two reload paths exist: transactional coordinator (`ReloadFromDisk`) vs the simple `adapter.Reload`.

## See also
- `docs/architecture/config/yang-config-design.md`, YANG-modeled config design
- `docs/architecture/config/syntax.md`, config file syntax + loading (cited from most pipeline files)
- `docs/architecture/config/tokenizer.md`, tokenizer / auto-semicolon design
- `ai/rules/repo-maintenance.md`, Config pipeline note + YANG choice/case non-enforcement
