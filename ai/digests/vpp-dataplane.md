# VPP dataplane: current flow digest

<!-- LIVING digest: the current shape of this subsystem. Update it when you change the subsystem. -->
<!-- Generated by tracing the code; verify file:line before relying on a detail. -->
<!-- digest-base: internal/component/vpp internal/core/vpp/events internal/plugins/fib/vpp internal/plugins/iface/vpp -->

## What it is
On gokrazy there is no systemd, no process supervisor, no `/etc/init.d`: whatever starts VPP and keeps it alive has to be ze itself. The `vpp` component (`internal/component/vpp`) is that supervisor. It is a plugin like any other (registered through `registry.Registration`, driven by the SDK 5-stage protocol), but its `OnStarted` hook launches a self-contained `VPPManager` goroutine that owns the *entire* VPP process lifecycle: writing `startup.conf`, binding DPDK NICs, `exec`-ing the `vpp` binary, connecting GoVPP, health/crash monitoring, exponential-backoff restart, and teardown. It never talks BGP or routes itself. `startup.conf` opens three independent VPP control channels on three separate Unix sockets: the binary API (`api.sock`, programmatic GoVPP requests), the stats segment (`stats.sock`, shared-memory counters), and the CLI socket (`cli.sock`, raw text VPP CLI for packet trace/runtime diagnostics). Two independent plugin backends sit downstream of the binary API and pull a GoVPP channel from the running manager: `fib-vpp` (`internal/plugins/fib/vpp`) programs the FIB from ze's system RIB, and `iface-vpp` (`internal/plugins/iface/vpp`) implements the generic `iface.Backend` (interfaces, addresses, bridges, VLANs, link state) for VPP. Both are real GoVPP binary-API drivers, not stubs, though several `iface.Backend` methods (veth, tunnels, XFRM, WireGuard, mirroring) are explicitly `errNotSupported` today.

## Flow

**(A) Registration and config.** `vpp` registers in `init()` with `ConfigRoots: []string{"vpp"}`, `RunEngine: runVPPEngine` (`internal/component/vpp/register.go:34-63`). `OnConfigure` parses the YANG "vpp" subtree into `*VPPSettings` via `ParseConfigSection` → `ParseSettings` (`internal/component/vpp/register.go:99-114`, `config.go:166-180`, `config.go:184-252`); `Validate()` checks socket paths, sizes, PCI addresses (`config.go:255-294`). Settings start as `{Enabled: false}` so a failed/absent config can't nil-deref later (`internal/component/vpp/register.go:95`). `OnConfigApply` just warns that reload needs a daemon restart (`internal/component/vpp/register.go:118-121`): VPP process parameters are not hot-reloadable.

**(B) Lifecycle owner spawned off the SDK loop.** `OnStarted` creates a cancelable `mgrCtx` and runs `NewVPPManager(settings, defaultConfDir, defaultVPPBinary).Run(mgrCtx)` in its own goroutine (`internal/component/vpp/register.go:123-139`); the SDK event loop keeps handling config/RPC traffic independently. On engine shutdown, `p.Run` returns, `mgrCancel()` fires, and the plugin blocks on `mgrDone` before exiting (`internal/component/vpp/register.go:150-156`), clean shutdown is synchronous from the plugin's point of view even though the manager runs concurrently.

**(C) Run(): the process supervision loop.** If disabled, `Run` just blocks on `ctx.Done()` (`vpp.go:150-154`). Otherwise it validates settings, then branches on `External`: when `false` (ze owns the binary) it writes `startup.conf` via `GenerateStartupConf` (`vpp.go:160-166`, `startupconf.go:22-115`) and calls `DPDKBinder.BindAll` to rebind configured NICs to `vfio-pci` (`vpp.go:167-170`, `dpdk.go:45-64`, loading `vfio`/`vfio_pci`/`vfio_iommu_type1` first); when `External` is `true` it skips both because an external supervisor owns the VPP lifecycle and ze only connects as a GoVPP client (`vpp.go:171-174`). It then registers the connector globally via `setActiveConnector` so `fib-vpp`/`iface-vpp` can fetch it later with `GetActiveConnector()` (`vpp.go:176-178`, `vpp.go:75-79`), and defers `DPDKBinder.UnbindAll` to restore original NIC drivers no matter how `Run` exits (`vpp.go:180-184`, `dpdk.go:68-90`).

**(D) Restart loop.** `Run` calls `runOnce` in a `for` loop with exponential backoff (1s doubling, capped at 30s via `maxRestartBackoff`) (`vpp.go:186-204`, `vpp.go:139`). A non-nil `ctx.Err()` after `runOnce` means shutdown was requested: close the connector and return `nil` (`vpp.go:189-193`). Otherwise the process exited unexpectedly: emit `vpp/disconnected`, wait out the backoff (or `ctx.Done()`), and restart.

**(E) runOnce(): exec, connect, wait.** When not external, `exec.CommandContext(ctx, vppBinary, "-c", confPath)` starts VPP with stdout/stderr wired to the ze process's own (`vpp.go:225-234`); external mode logs and skips exec entirely (`vpp.go:235-237`). Either way, `Connector.Connect` is given a 30s sub-context and retries `core.AsyncConnect` against the binary-API Unix socket up to 10 times, 1s apart, blocking on the connection-state channel without holding the connector's mutex (`vpp.go:240-249`, `conn.go:44-88`). A connect failure kills and reaps the just-started process before returning (`vpp.go:243-247`).

**(F) Readiness signal.** On successful connect, `Run` emits `("vpp","connected")` the first time or `("vpp","reconnected")` on every subsequent success (`vpp.go:252-257`, event names in `events.go:12-14`). This EventBus emission is the *only* readiness gate dependants get, there is no separate "ready" RPC.

**(G) Telemetry (optional, best-effort).** If a metrics registry was configured, `runOnce` opens a *separate* stats-segment connection (`connectStats`, `stats_conn.go:18-25`, distinct socket from the binary API) and starts a `statsPoller` goroutine that polls `GetInterfaceStats`/`GetNodeStats`/`GetSystemStats` every `Stats.PollInterval` seconds, converting cumulative VPP counters to Prometheus counter deltas (handling counter reset on VPP restart) (`vpp.go:260-276`, `telemetry.go:134-173`, `telemetry.go:217-230`). Stats failure only disables telemetry (`vpp.go:264-266`); it never fails the manager.

**(H) Blocking + cleanup.** `runOnce` blocks on `cmd.Wait()` (process exit) or `<-ctx.Done()` in external mode (`vpp.go:279-287`), then always cancels the poller, clears the stats provider, and calls `connector.Close()` before returning (`vpp.go:288-295`).

**(I) Shutdown mechanism, doc/code drift.** The doc comment above `Run` claims the shutdown sequence sends "SIGTERM VPP, wait, unbind NICs" (`vpp.go:145`), but `cmd.Cancel` is never overridden, so `exec.CommandContext`'s Go-1.20+ default fires: `cmd.Process.Kill()`, i.e. a hard `SIGKILL`, not a graceful `SIGTERM`. There is no signal-then-wait grace period anywhere in this package. NIC unbind (`vpp.go:180-184`) still runs via `defer` regardless.

**(J) Interface backend consumer.** `iface-vpp` registers `newVPPBackend` as the `"vpp"` `iface.Backend` (`iface/vpp/register.go:13-19`). Channel acquisition is lazy: `ensureChannel` only calls `connector.NewChannel()` on first real method use, because the `iface` component's `OnConfigure` can run before `vpp`'s `OnStarted` finishes its handshake; if not ready it returns `iface.ErrBackendNotReady` (`internal/component/iface/backend.go:23`) without caching, so the next call retries (`ifacevpp.go:108-151`). `populateOnce`/`populateNameMap` seed the ze-name↔`SwIfIndex` map from `SwInterfaceDump` on first successful channel acquisition (`ifacevpp.go:153-162`, `iface/vpp/query.go:202-215`).

**(K) Readiness-gated reconciliation (consumer side).** `internal/component/iface` subscribes to `vpp/connected` and `vpp/reconnected` once per config apply (`internal/component/iface/register.go:264-272`, `internal/component/iface/register.go:514-517`) and, on either event, non-blocking-notifies a dedicated worker channel (EventBus handlers must not block on I/O) that calls `reconcileOnVPPReady` → `reconcileOnReadyWithJournal` (`internal/component/iface/config_apply.go:1068-1100`, `internal/component/iface/config_apply.go:773-828`). If the backend is still not ready when config was first applied, `applyConfig` logs and falls back to an additive-only `addDesiredAddresses` pass so the daemon stays usable before VPP is up (`internal/component/iface/config_apply.go:744-754`, `internal/component/iface/config_apply.go:1040-1054`).

**(L) Interface/address/bridge programming.** Each `iface.Backend` method is a direct GoVPP request/reply pair, checked for both a transport error and a non-zero `Retval`: `CreateDummy`→`CreateLoopback` (`ifacevpp.go:198-212`), `CreateBridge`→`BridgeDomainAddDelV2` (`ifacevpp.go:218-241`), `CreateVLAN`→`CreateVlanSubif` (+ QoS record/egress-map/mark pipeline) (`ifacevpp.go:243-278`, `ifacevpp.go:330-387`), `AddAddress`/`RemoveAddress`→`SwInterfaceAddDelAddress` (`ifacevpp.go:441-487`), `SetAdminUp`/`SetAdminDown`→`SwInterfaceSetFlags` (`ifacevpp.go:517-553`), `SetMTU`→`SwInterfaceSetMtu` (`ifacevpp.go:557-577`), `BridgeAddPort`/`BridgeDelPort`→`SwInterfaceSetL2Bridge` (`ifacevpp.go:619-660`), `DeleteInterface` tries `DeleteLoopback` then falls back to `DeleteSubif` (`ifacevpp.go:413-437`).

**(M) Routes are explicitly out of scope for iface-vpp.** `AddRoute`/`RemoveRoute`/`ListRoutes` on the VPP `iface.Backend` all return `errNotSupported`, pointing callers at the `fib-vpp` plugin instead (`ifacevpp.go:503-513`). Route programming is a separate plugin with its own connector/channel.

**(N) fib-vpp: RIB → FIB.** `fib-vpp` declares `Dependencies: []string{"rib", "vpp"}` (`fib/vpp/register.go:21-27`). In `OnStarted`, `initBackend` independently calls `vppcomp.GetActiveConnector()` and `connector.NewChannel()` (falling back to a `mockBackend` no-op if VPP isn't up yet) (`fib/vpp/register.go:107-124`), then `fib.run` subscribes to `(sysrib, best-change)` on the EventBus and emits a `ReplayRequest` to get a full-table replay (`fibvpp.go:377-390`, using `sysribevents.BestChange`). `processEvent` walks each `BestChangeEntry`: SRv6/MPLS-labeled entries route to dedicated backends (`fibvpp.go:226-233`, `fibvpp.go:270-298`); everything else dispatches on `Action.Verb()`, install/replace call `addVPPRoute`/`replaceVPPRoute`, remove calls `delVPPRoute`, each ultimately hitting `IPRouteAddDel` over GoVPP with rich (ECMP/ TableID/RouteType) or plain single-path shape (`fibvpp.go:210-268`, `fib/vpp/backend.go:93-158`, `fib/vpp/backend.go:182-206`). Installed state is tracked in an in-memory `installed` map keyed by prefix string so deletes and `flushRoutes` know the right table ID even though sysrib withdrawals carry `TableID=0` (`fibvpp.go:194-208`, `fibvpp.go:300-351`).

**(O) fib-vpp readiness handling is independent of iface-vpp's.** `fib-vpp` also subscribes to `vpp/connected` and `vpp/reconnected`; on either it re-runs `initBackend` (rebuilding the channel and MPLS/SRv6 sub-backends against the fresh connector) and restarts the `fib.run` goroutine via `startFib` (`fib/vpp/register.go:137-149`). `iface-vpp` and `fib-vpp` never share a GoVPP channel, each calls `connector.NewChannel()` independently, so a channel-level failure in one does not directly affect the other.

**(P) Interface event monitor + health.** `StartMonitor` issues `WantInterfaceEvents{EnableDisable:1}` and `SubscribeNotification` for `SwInterfaceEvent`, translating each VPP interface transition into the same `(namespace, event-type, JSON)` shape `ifacenetlink` emits so downstream subscribers (BGP reactor, web UI) stay backend-agnostic (`iface/vpp/monitor.go:65-113`, `iface/vpp/monitor.go:174-197`); a panic in a handler is recovered per-event so the monitor goroutine survives (`iface/vpp/monitor.go:161-169`). Separately, `iface-vpp` registers a health check that treats a *missing* API socket as healthy (VPP simply not deployed) but a present-but-unreachable socket as down (`iface/vpp/health.go:17-36`).

**(Q) Readback: what's actually in VPP vs. what ze thinks it installed.** `ListKernelRoutes` (`iface/vpp/fib.go:60`) dumps VPP's FIB via `ip_route_v2_dump` and `ListNeighbors` (`iface/vpp/neighbor.go:47`) dumps the ARP/ND cache via `ip_neighbor_dump`, both resolving `SwIfIndex` back to ze interface names through the same name map `ifacevpp.go` maintains. These feed `show route`/`show neighbor` CLI output and are independent of `fib-vpp`'s in-memory `installed` map (N), the map is ze's bookkeeping of what it pushed, this is a live read of VPP's own state, so the two can be compared to catch drift.

**(R) The third channel: CLI socket diagnostics.** Separately from the binary API and stats connections, `execCLI` dials the VPP CLI socket directly (`/run/vpp/cli.sock`, overridable via `ze.test.vpp.cli.socket` for tests) and writes a raw text command, reading the response until an idle timeout (`internal/component/vpp/trace_linux.go:32-64`). `TraceStart`/`TraceShow`/`TraceClear`/`ShowRuntime` wrap `trace add`/`show trace`/`clear trace`/`show runtime` (`trace_linux.go:66-86`); on non-Linux builds these are stubbed to return `errTraceNotAvailable` (`internal/component/vpp/trace_other.go:9-14`). `iface-vpp`'s `cmd_show.go` registers these as `ze-show:vpp-trace-start`/`vpp-trace-show`/`vpp-trace-clear`/`vpp-runtime` RPC handlers (`iface/vpp/cmd_show.go:20-25`) so operators can start a packet trace and inspect per-node runtime counters from the ze CLI without a separate `vppctl` session.

## Key files
| File | Role |
|------|------|
| `internal/component/vpp/vpp.go` | `VPPManager`: the whole process-supervision state machine (`Run`/`runOnce`), package-level connector/stats-provider/event-bus registries dependants pull from |
| `internal/component/vpp/register.go` | Plugin registration, YANG config wiring, `OnStarted` spawning `VPPManager.Run` off the SDK loop |
| `internal/component/vpp/conn.go` | `Connector`: GoVPP `AsyncConnect`/`NewChannel`/`WaitConnected`/`Close` around the binary-API socket |
| `internal/component/vpp/config.go` | `VPPSettings` parse + `Validate` (socket paths, sizes, PCI addresses, netns) |
| `internal/component/vpp/startupconf.go` | Generates VPP's `startup.conf` (unix/api-segment/socksvr/cpu/buffers/dpdk/plugins/linux-cp/heapsize/statseg sections) |
| `internal/component/vpp/dpdk.go` | `DPDKBinder`: vfio-pci bind/unbind, driver save/restore, sysfs plumbing |
| `internal/component/vpp/dpdk_linux.go` | Linux `modprobe` implementation of `loadModule` |
| `internal/component/vpp/stats_conn.go` | Opens the (separate) stats-segment connection |
| `internal/component/vpp/telemetry.go` | `statsPoller`: interface/node/system counter polling → Prometheus deltas |
| `internal/component/vpp/govpp_logrus.go` | Bridges GoVPP's logrus logging into ze's `slog` |
| `internal/component/vpp/trace_linux.go` | Third control channel: raw CLI-socket dial (`execCLI`) for packet trace / `show runtime` |
| `internal/component/vpp/trace_other.go` | Non-Linux stub returning `errTraceNotAvailable` for the same API |
| `internal/core/vpp/events/events.go` | `Namespace = "vpp"` and the `connected`/`disconnected`/`reconnected` event-type constants, the readiness contract every dependant subscribes to |
| `internal/plugins/iface/vpp/ifacevpp.go` | `iface.Backend` impl: interface/address/bridge/VLAN lifecycle via GoVPP; lazy channel acquisition |
| `internal/plugins/iface/vpp/register.go` | Registers the `"vpp"` `iface.Backend` + the VPP health check |
| `internal/plugins/iface/vpp/monitor.go` | `WantInterfaceEvents` subscription → ze `iface` EventBus translation |
| `internal/plugins/iface/vpp/query.go` | `SwInterfaceDump` queries; seeds the name↔`SwIfIndex` map |
| `internal/plugins/iface/vpp/fib.go` | `ListKernelRoutes` readback of VPP's FIB via `ip_route_v2_dump` |
| `internal/plugins/iface/vpp/neighbor.go` | `ListNeighbors` readback of VPP's ARP/ND cache via `ip_neighbor_dump` |
| `internal/plugins/iface/vpp/cmd_show.go` | Wires `ze-show:vpp-trace-*`/`vpp-runtime` RPCs to the CLI-socket trace API |
| `internal/plugins/iface/vpp/health.go` | VPP API-socket health check (missing socket = healthy/not-deployed) |
| `internal/plugins/fib/vpp/register.go` | fib-vpp plugin registration; `OnStarted` backend init + vpp-ready resubscription |
| `internal/plugins/fib/vpp/fibvpp.go` | Subscribes `(sysrib, best-change)`, dispatches install/replace/remove/MPLS/SRv6, tracks installed state |
| `internal/plugins/fib/vpp/backend.go` | `govppBackend`: `IPRouteAddDel` calls (plain and rich/ECMP), plus the test `mockBackend` |
| `internal/component/iface/register.go` | (consumer, outside this base) `subscribeReconcileOnReady` wiring to `vpp/connected`/`vpp/reconnected` |
| `internal/component/iface/config_apply.go` | (consumer, outside this base) `reconcileOnVPPReady`, `ErrBackendNotReady` deferral, additive-only fallback |

## Invariants & gotchas
- **Ze is the process owner, full stop.** No systemd on gokrazy means `VPPManager` is not a thin wrapper: it writes config, binds hardware, execs, health-checks, and restarts. Anyone changing VPP startup behavior touches `vpp.go`/`startupconf.go`/`dpdk.go` together, they are one lifecycle, not three independent features.
- **`External: true` inverts almost every step.** When set, ze never writes `startup.conf`, never binds DPDK NICs, never execs the binary, and never kills anything on shutdown, it is purely a GoVPP client of a VPP instance some other supervisor owns (`vpp.go:161-174`, `vpp.go:235-237`). Code paths must keep checking `m.settings.External`, not just `cmd != nil`, when adding new lifecycle behavior.
- **Shutdown is a hard kill, not graceful.** Despite the `vpp.go:145` comment, `exec.CommandContext`'s uncustomized `Cancel` sends `SIGKILL` via `Process.Kill()` on `ctx` cancellation, there is no SIGTERM-then-wait grace window. If VPP needs a clean shutdown path (e.g. to flush FIB state) this is a real gap, not a documentation nit.
- **Readiness has exactly one signal: the EventBus event.** `vpp/connected` (first handshake) and `vpp/reconnected` (post-crash) are the only readiness contract (`events.go:12-14`). There is no synchronous "wait for VPP" RPC that plugins call at startup, `iface-vpp` and `fib-vpp` each independently poll-on-demand (`ensureChannel`, `GetActiveConnector`) and separately subscribe to the same two events to retry/rebuild.
- **Two independent GoVPP channels, two independent readiness retries.** `iface-vpp` and `fib-vpp` each call `connector.NewChannel()` on their own; neither is aware of the other's channel health. A stale channel in one does not necessarily show up in the other, and both must independently re-subscribe to `vpp/connected`/`vpp/reconnected` to recover (`fib/vpp/register.go:137-149` vs. `ifacevpp.go:108-151` + `internal/component/iface/register.go:514-517`).
- **`ErrBackendNotReady` must not be cached.** `ensureChannel` deliberately returns the sentinel without marking `chReady` so the very next call retries the connector lookup (`ifacevpp.go:124-131`); callers up the stack (`internal/component/iface/config_apply.go:823-825`) treat it as "defer, don't fail" rather than a hard error.
- **Config reload cannot restart VPP.** `OnConfigApply` explicitly warns and does nothing (`internal/component/vpp/register.go:118-121`); CPU pinning, memory, DPDK NIC list, and API socket path are effectively startup-once. Changing them needs a ze/VPP process restart, not a config commit.
- **Stats segment is a second, independent socket.** The binary-API connection (`conn.go`) and the stats connection (`stats_conn.go`) are separate Unix sockets with separate lifecycles; a stats-connect failure only disables telemetry (`vpp.go:264-266`) and never affects route/interface programming.
- **Routes go through fib-vpp only.** `iface-vpp`'s `AddRoute`/`RemoveRoute`/`ListRoutes` are hard `errNotSupported` (`ifacevpp.go:503-513`), there is no accidental double-path for route installation between the two plugins.
- **fib-vpp falls back to a no-op mock, not an error, when VPP isn't up.** `initBackend` installs `mockBackend` if the connector or channel isn't available at `OnStarted` time (`fib/vpp/register.go:110-119`), so the plugin starts cleanly and silently drops route programming until `vpp/connected` fires and `initBackend`+`startFib` rerun.
- **Not every `iface.Backend` method is implemented.** `CreateVeth`, `CreateTunnel`, `CreateXFRM`, WireGuard methods, and mirror setup are all `errNotSupported` stubs on the VPP backend (`ifacevpp.go:214-216`, `ifacevpp.go:389-411`, `ifacevpp.go:668-674`), this is a partial dataplane driver for interfaces even though route programming (fib-vpp) and core lifecycle are fully wired.
- **Health check treats "no socket" as healthy.** `checkVPPHealth` returns `StatusHealthy` when `/run/vpp/api.sock` does not exist at all, on the theory that VPP is simply not deployed on this system (`iface/vpp/health.go:21-23`); only a present-but-unreachable socket is `StatusDown`. A misconfigured socket path would silently read as healthy.
- **Three sockets, three independent failure domains.** Binary API (`api.sock`, GoVPP requests), stats segment (`stats.sock`, telemetry), and CLI socket (`cli.sock`, `execCLI` trace/runtime diagnostics) are separate connections with no shared health state, the CLI-socket path (`trace_linux.go`) has its own hardcoded default path and its own dial-per-call model (no persistent connection, no reconnect logic), unlike the binary API's `Connector` and the stats poller's long-lived connection.
- **Readback and bookkeeping can diverge.** `fib-vpp`'s `installed` map (N) is ze's memory of what it pushed; `ListKernelRoutes`/`ListNeighbors` (Q) are live dumps of VPP's actual FIB/ARP state. Nothing reconciles the two automatically, a route pushed by `fib-vpp` but silently dropped by VPP (or manually removed via `vppctl`) shows as installed in `show fib vpp` but absent from `show route` until the next full replay.

## See also
- `docs/guide/vpp.md`, user-facing status/overview of what is wired vs. designed-but-not-yet
- `docs/research/vpp-deployment-reference.md`, startup.conf production values, NIC driver matrix, IPng/VyOS deployment reference this component's defaults are drawn from
- `docs/research/vpp-deployment-notes.md`, consolidated research notes behind the above
- `docs/research/ze-vpp-analysis.md`, feasibility analysis that shaped this design
- `internal/plugins/static/vpp/backend.go`, a second, separate VPP FIB backend (static-route plugin), same `IPRouteAddDel` pattern, not traced here
- `ai/rules/plugin-design.md`, cross-boundary value types, plugin placement conventions this component follows
