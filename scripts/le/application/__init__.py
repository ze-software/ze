"""The subprograms.

One module per area. Each exposes `add_arguments`, `options`, `action` and
`main`, which is all the dispatcher knows about any of them
(`le/registry.py`).
"""

from __future__ import annotations
