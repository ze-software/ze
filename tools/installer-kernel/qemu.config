# QEMU/emulation profile for the ze installer kernel.
#
# Virtio NIC + block: the only drivers needed under QEMU virt.
# No framebuffer or EFI stub (QEMU uses serial console).

CONFIG_VIRTIO=y
CONFIG_VIRTIO_PCI=y
CONFIG_VIRTIO_MMIO=y
CONFIG_VIRTIO_NET=y
CONFIG_VIRTIO_BLK=y
